---
milestone: v1.3.3
milestone_name: Печатные формы и приватность данных
audited: 2026-08-18T11:45:00Z
audited_at_head: eca26d24
updated: 2026-08-19T09:10:00Z
updated_at_head: fc9fe1b8
status: tech_debt
scores:
  requirements: 10/11
  phases: 4/5
  integration: 12/12
  flows: 2/2
gaps:
  requirements:
    - id: "DOC-10"
      status: "satisfied"
      resolved: "2026-08-19 — живая печать на Windows из релизной сборки v1.3.3, дефектов нет"
      phase: "Phase 36 (act-pagination)"
      claimed_by_plans: ["36-02-PLAN.md", "36-03-PLAN.md", "36-05-PLAN.md"]
      completed_by_plans: []
      verification_status: "human_needed"
      evidence: >
        36-VERIFICATION.md: status human_needed, score 2/4 — Success Criterion #1
        (акт на одно устройство целиком умещается на одном листе вместе с полным описанием)
        требует геометрического подтверждения реального рендера PDF, которое пользователь
        осознанно отложил 2026-08-13. Ни один SUMMARY фазы 36 не отмечает DOC-10 как
        completed — 36-05-SUMMARY прямо пишет «DOC-10 (SC#1) intentionally NOT marked».
        REQUIREMENTS.md: `[ ]` / Pending. Все три источника согласованы: требование НЕ закрыто.
        Код-путь N=1 реализован и покрыт extraction-тестами (html_act_render.rs), но именно
        эти тесты не видят разбиение на листы — принципиальное ограничение, зафиксированное
        в проектной памяти (act-pdf-word-fidelity).
        ЗАКРЫТО 2026-08-19: пользователь выполнил живую проверку на Windows/WebView2 из
        релизной сборки v1.3.3 (portable-zip) — N=1 умещается на одном листе; заодно закрыты
        печать N>1 при настройках диалога по умолчанию, живой LAN-браузерный транспорт и
        изоляция печатного DOM. Дефектов не выявлено, правок кода не потребовалось.
        36-VERIFICATION.md: human_needed → passed (2/4 → 4/4); 36-UAT.md: partial → complete;
        REQUIREMENTS.md: DOC-10 `[x]` / Complete.
    - id: "QA-04"
      status: "partial"
      phase: "Phase 38 (Nyquist-покрытие Фазы 32)"
      claimed_by_plans: []
      completed_by_plans: []
      verification_status: "missing"
      evidence: >
        Содержательно требование выполнено: 32-VALIDATION.md на HEAD имеет
        `nyquist_compliant: true`, `status: complete`, `validated: 2026-08-18`,
        `validated_by: /gsd-validate-phase 32 (retroactive audit)`, все строки Per-Task
        Verification Map зелёные, 0 пробелов. Но у Фазы 38 нет ни каталога, ни PLAN,
        ни SUMMARY, ни собственного VERIFICATION.md — единственный артефакт закрытия
        это флип frontmatter внутри файла ЧУЖОЙ фазы плюс docs-коммит. По матрице
        3-source cross-reference (VERIFICATION missing + SUMMARY missing) это `partial`,
        а не `satisfied`. Дополнительно: тот же ретроаудит сам зафиксировал живой дефект
        (fmt-дрейф) и вынес его за скобки; дефект закрыт уже в ходе ЭТОГО аудита
        (INT-01, b26f6173 + b4a7dc52), а не фазой 38.
  integration:
    - id: "INT-01"
      severity: "blocker"
      resolved: "2026-08-18 (коммиты b26f6173 + b4a7dc52) — CI зелёный на b4a7dc52, все шаги success"
      from: "Фазы 34–36 (rustfmt-дрейф в 6 файлах)"
      to: ".github/workflows/ci-fast.yml (job `fast`)"
      issue: >
        `cargo fmt --all -- --check` падает на HEAD в 11 местах
        (pdf/html_templates.rs:37; pdf/minijinja_env.rs:254,321,388;
        tests/html_act_render.rs:396,776; tests/html_header_parity.rs:20,63,123;
        tests/org_settings.rs:66; tests/pdf_render_act.rs:457). В ci-fast.yml шаги идут
        последовательно без continue-on-error, и `cargo fmt --check` стоит ПЕРЕД
        clippy / cargo test / svelte-check / pnpm lint. Подтверждено прямым просмотром
        последнего прогона (run на eca26d24): fmt = failure, clippy/test/svelte-check/lint =
        skipped. CI красный на КАЖДОМ прогоне минимум с 2026-08-11.
      impact: >
        Единственное автоматическое свидетельство по DOC-04..DOC-11 — это Rust-тесты и
        `pnpm lint` (включая durable-гейты check-print-isolation.mjs и
        check-pagedjs-csp-hash.mjs). Ни один из них сейчас в CI не исполняется. Гейты
        присутствуют и корректны, но не работают как гейты. Privacy-гейт (PRIV-02) —
        единственный шаг, который реально отрабатывает, так как стоит первым.
      affected_requirements: ["DOC-04", "DOC-05", "DOC-06", "DOC-07", "DOC-08", "DOC-09", "DOC-10", "DOC-11", "QA-04"]
      fix: >
        Закрыто в два шага, а не в один. (1) `cargo fmt --all` — 6 файлов, 28 вставок,
        только переформатирование (b26f6173). (2) За fmt пряталась вторая поломка: clippy
        падал на `crates/trackly-app/src/services/act_service.rs:3318` — тестовый конструктор
        `item()` из плана 36-06 (D-17, group_items_for_print), 9/7 аргументов, lib test.
        Добавлен `#[allow(clippy::too_many_arguments)]` по уже принятому в репозитории образцу
        (report_service.rs:606, requests_sqlite.rs:137, cartridges_sqlite.rs:171/232) —
        b4a7dc52. После этого прогон ci-fast на b4a7dc52 зелёный целиком: fmt, clippy,
        cargo test --workspace, svelte-check, pnpm lint — все success. Первый зелёный CI
        с 2026-08-08.
    - id: "INT-02"
      severity: "warning"
      from: "ui/src/lib/pdfPreview/bootstrapScript.js (десктоп/превью)"
      to: "ui/src/features/acts/PdfPreviewModal.svelte → printViaTopLevel() (LAN)"
      issue: >
        Paged.js-хендлер повтора thead (`RepeatTableHeadHandler`, решение D-15 фазы 36)
        существует в двух независимо поддерживаемых копиях; в LAN-копии стоит комментарий
        «mirror ... keep the two in sync». Сейчас обе копии структурно эквивалентны, то есть
        DOC-11 работает на обоих транспортах. Общего источника и автоматической проверки
        синхронности нет — правка одной копии без второй регрессирует один транспорт.
      affected_requirements: ["DOC-11"]
  flows:
    - flow: "создать акт → предпросмотр → печать (N=1, оба транспорта)"
      resolved: "2026-08-19"
      breaks_at: "—  (закрыто живой проверкой на Windows)"
      detail: >
        Кодовая цепочка полна и покрыта extraction-тестами, но SC#1 требует именно
        геометрического подтверждения. Отложено пользователем 2026-08-13. CI как
        подстраховка снова работает (INT-01 закрыт), но геометрию он всё равно не видит.
      affected_requirements: ["DOC-10"]
    - flow: "создать акт → предпросмотр → печать (N>1, LAN-браузер)"
      resolved: "2026-08-19"
      breaks_at: "—  (первое живое подтверждение LAN-транспорта выполнено)"
      detail: >
        Наличие хендлера в обоих путях подтверждено статически (grep + чтение кода),
        живого прогона в LAN-браузере в рамках фазы 36 не выполнялось. Отложено
        пользователем 2026-08-13.
      affected_requirements: ["DOC-11"]
nyquist:
  compliant_phases: ["34", "35", "37"]
  partial_phases: ["36"]
  missing_phases: ["38"]
  overall: partial
  notes: >
    36-VALIDATION.md существует, но frontmatter: `status: draft`, `nyquist_compliant: false`,
    `wave_0_complete: false` — фаза, у которой в вехе больше всего незакрытых хвостов,
    единственная без подтверждённого Nyquist-покрытия. У Фазы 38 своего VALIDATION.md нет
    по построению (фаза без каталога); её предмет — 32-VALIDATION.md, который compliant.
tech_debt:
  - phase: "34-document-header"
    items:
      - "34-VERIFICATION.md: два human_verification-пункта закрыты подтверждением пользователя 2026-08-11 (34-HUMAN-UAT.md, status resolved) — долга не осталось, зафиксировано для полноты"
  - phase: "36-act-pagination"
    items:
      - "ЗАКРЫТО 2026-08-19: живая ручная UAT выполнена на Windows из сборки v1.3.3 — печать N=1, печать N>1 при дефолтных настройках диалога, LAN-транспорт end-to-end, изоляция печатного DOM, прогон на Windows/WebView2. Дефектов не выявлено"
      - "36-UAT.md: status complete (8 pass / 1 skipped). Остаётся skipped только прямая проверка экрана Настройки → Шаблоны на рабочей БД — косвенно закрыта живым UAT 36-04..36-06 и автотестами upgrade-пути"
      - "36-VALIDATION.md: nyquist_compliant false, wave_0_complete false — кандидат на /gsd-validate-phase 36"
      - "RepeatTableHeadHandler продублирован между десктопным и LAN-путём без общего источника и без гейта синхронности (INT-02)"
  - phase: "37-data-privacy"
    items:
      - "PRIV-03 осознанно отложена и не привязана ни к одной фазе: история git НЕ переписана, репозиторий остаётся публичным — гарантия гейта распространяется только на HEAD"
      - "pre-commit хук требует core.hooksPath=.githooks и обходится через --no-verify; в CI гейт обязателен — асимметрия по построению"
  - phase: "cross-milestone"
    items:
      - "ЗАКРЫТО 2026-08-18: CI был красным с 2026-08-11 из-за fmt-дрейфа + clippy-нарушения (INT-01); clippy/test/svelte-check/lint не исполнялись большую часть вехи. Зелёный прогон восстановлен на b4a7dc52"
      - "Известный пре-существующий хвост: cargo test --workspace виснет на login_remember_persistent_cookie (память проекта) — полный прогон не является дешёвой проверкой"
---

# Milestone Audit — v1.3.3 «Печатные формы и приватность данных»

**Аудит на HEAD `eca26d24`, 2026-08-18. Обновлён 2026-08-19. Статус: `tech_debt`.**

Веха закрыта по фазам (34–38, 5/5). На момент первого прогона аудита два требования из
одиннадцати не имели подтверждения и был найден сквозной блокер — **CI не исполнял собственные
гейты**. Оба блокера закрыты в течение суток:

- **INT-01** (CI) — 2026-08-18, коммиты `b26f6173` + `b4a7dc52`.
- **DOC-10** (печать N=1) — 2026-08-19, живая проверка на Windows из релизной сборки v1.3.3.

Остаётся один `partial` (QA-04, трассируемость) и накопленный tech debt — блокеров нет.

---

## 1. Требования (3-source cross-reference)

Источники: `REQUIREMENTS.md` (traceability), `*-VERIFICATION.md` по фазам,
`requirements_completed` во frontmatter каждого `*-SUMMARY.md`.

| REQ | Фаза | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Итог |
|-----|------|--------------|---------------------|-----------------|------|
| DOC-04 | 34 | passed (4/4) | 34-02, 34-03, 34-06 | `[x]` | **satisfied** |
| DOC-05 | 34 | passed | 34-01..34-04, 34-06 | `[x]` | **satisfied** |
| DOC-06 | 34 | passed | 34-02, 34-05, 34-06 | `[x]` | **satisfied** |
| DOC-07 | 35 | passed (5/5) | 35-02, 35-04, 35-05, 35-06 | `[x]` | **satisfied** |
| DOC-08 | 35 | passed | 35-01..35-07 | `[x]` | **satisfied** |
| DOC-09 | 35 | passed | 35-02, 35-04, 35-05, 35-06 | `[x]` | **satisfied** |
| DOC-10 | 36 | **passed (4/4)** ✅ | — (закрыт живым UAT) | `[x]` | **satisfied** |
| DOC-11 | 36 | passed (4/4) | 36-04, 36-06 | `[x]` | **satisfied** |
| PRIV-01 | 37 | passed (5/5) | 37-01, 37-02 | `[x]` | **satisfied** |
| PRIV-02 | 37 | passed | 37-03, 37-04 | `[x]` | **satisfied** |
| QA-04 | 38 | **отсутствует** | **отсутствует** | `[ ]` | **partial** |

**Orphan-детекция:** сирот нет — все 11 требований traceability-таблицы имеют хотя бы одну
привязку к фазе и хотя бы один cross-phase touchpoint.

**Обновление 2026-08-19:** DOC-10 закрыт живой проверкой печати на Windows (сборка v1.3.3),
`36-VERIFICATION.md` переведён human_needed → passed (2/4 → 4/4), `36-UAT.md` partial →
complete. Тем же прогоном получено первое живое подтверждение LAN-транспорта, поэтому
оговорка по DOC-11 снята.

**Расхождение, требующее правки:** `REQUIREMENTS.md` держит QA-04 как `[ ]` / Pending, тогда как
ROADMAP.md уже закрыл Phase 38. Одно из двух надо привести в соответствие — но не раньше,
чем будет решено, чем именно закрывается QA-04 (см. §4).

---

## 2. Фазы

| Фаза | VERIFICATION | Score | Комментарий |
|------|--------------|-------|-------------|
| 34 Единая шапка документов | passed | 4/4 | human-UAT закрыт пользователем 2026-08-11 |
| 35 Тело акта приёма-передачи | passed | 5/5 | после re-verification, gaps_remaining пуст |
| 36 Пагинация акта | **passed** ✅ | 4/4 | закрыто живым UAT на Windows 2026-08-19 |
| 37 Приватность данных | passed | 5/5 | PRIV-03 явно вынесена в Future Requirements |
| 38 Nyquist-покрытие Фазы 32 | **отсутствует** | — | нет каталога фазы; предмет — 32-VALIDATION.md |

**4/5 фаз с `passed`.** У фазы 38 своего VERIFICATION.md нет по построению — см. §4.

---

## 3. Интеграция между фазами

### ✅ INT-01 (был blocker, закрыт 2026-08-18) — CI не исполнял собственные гейты

`cargo fmt --all -- --check` падает на HEAD в **11 местах**, в файлах, которые трогали именно
фазы 34–36:

```
crates/trackly-app/src/pdf/html_templates.rs:37
crates/trackly-app/src/pdf/minijinja_env.rs:254, 321, 388
crates/trackly-app/tests/html_act_render.rs:396, 776
crates/trackly-app/tests/html_header_parity.rs:20, 63, 123
crates/trackly-app/tests/org_settings.rs:66
crates/trackly-app/tests/pdf_render_act.rs:457
```

В `ci-fast.yml` job `fast` шаги идут последовательно, без `continue-on-error`, и
`cargo fmt --check` стоит перед `cargo clippy`, `cargo test`, `pnpm svelte-check`, `pnpm lint`.
Фактический результат последнего прогона (на `eca26d24`):

```
success  Privacy gate
success  Build Svelte SPA (ui/dist)
failure  cargo fmt --check
skipped  cargo clippy
skipped  cargo test
skipped  pnpm svelte-check
skipped  pnpm lint
```

CI красный на **каждом** прогоне минимум с 2026-08-11 — то есть почти всю веху. Практическое
следствие: единственное автоматическое свидетельство по DOC-04..DOC-11 (Rust-тесты рендера,
структурные CSS-гейты, `check-print-isolation.mjs`, `check-pagedjs-csp-hash.mjs`) не
исполнялось ни разу. Гейты написаны, подключены и корректны — но не работают как гейты.

Privacy-гейт (PRIV-02) — единственный, кто реально отрабатывает, потому что стоит первым шагом.

Тот же дрейф был замечен ретроаудитом Фазы 32 (`32-VALIDATION.md`) 2026-08-18 и явно вынесен
за границы той фазы. Никто его после этого не закрыл.

**Фикс — потребовалось два шага, а не один.**

1. `cargo fmt --all` — 6 файлов, 28 вставок, только переформатирование, токены идентичны
   (`b26f6173`).
2. За fmt пряталась вторая поломка того же происхождения: `cargo clippy -D warnings` падал на
   `crates/trackly-app/src/services/act_service.rs:3318` — тестовый конструктор `item()` из
   плана 36-06 (D-17, `group_items_for_print`), 9 аргументов при лимите 7, таргет lib test.
   Добавлен `#[allow(clippy::too_many_arguments)]` по уже принятому в репозитории образцу
   (`report_service.rs:606`, `requests_sqlite.rs:137`, `cartridges_sqlite.rs:171/232`)
   — `b4a7dc52`.

**Результат:** прогон `ci-fast` на `b4a7dc52` зелёный целиком — Privacy gate, build SPA,
`cargo fmt --check`, `cargo clippy`, **`cargo test --workspace`**, `pnpm svelte-check`,
`pnpm lint` — все `success`. Первый зелёный CI с 2026-08-08.

Практическое следствие для вехи: автоматическое свидетельство по DOC-04..DOC-11 (тесты рендера,
структурные CSS-гейты, `check-print-isolation.mjs`, `check-pagedjs-csp-hash.mjs`) впервые за
две недели реально исполнено и зелёное — на Linux-раннере, включая полный
`cargo test --workspace`, который локально на macOS не гоняется из-за известного хэнга
`login_remember_persistent_cookie`.

**Урок для процесса:** тот факт, что за fmt-дрейфом пряталась вторая поломка, — прямой
аргумент против закрытия вехи с красным CI. Красный шаг маскирует всё, что стоит за ним,
и цена ошибки растёт молча.

### 🟡 INT-02 (warning) — дублированный Paged.js-хендлер между транспортами

`RepeatTableHeadHandler` (D-15, фаза 36) существует в двух независимых копиях:
`ui/src/lib/pdfPreview/bootstrapScript.js` (десктоп/превью) и inline внутри
`printViaTopLevel()` в `ui/src/features/acts/PdfPreviewModal.svelte` (LAN). Обе копии сейчас
эквивалентны — DOC-11 работает на обоих транспортах. Общего источника и проверки синхронности
нет; правка одной копии молча регрессирует другой транспорт. Это ровно тот класс дефекта,
который в проекте уже случался (см. память `lan-print-dom-leakage`).

### ✅ Что проверено и подтверждено целым

| Стык | Вердикт |
|------|---------|
| `_header.html` включается всеми тремя формами после правок 35/36 (DOC-04) | цел |
| 3 легитимных источника `border-bottom` в underline-гейте — осознанное расширение 2→3, покрыто тестом (DOC-07) | цел |
| CSS переноса длинного ФИО из 35-07 пережил рерайт 36-02 (DOC-08) | цел |
| Цепочка `_legacy_defaults` v20→v25 непрерывна, каждая версия в `KNOWN_LEGACY_DEFAULTS`, дыра v23 из 35-06 закрыта (DOC-06) | цел |
| `org.full_name`: UI → DB → оба транспорта → три формы (DOC-05) | цел |
| Privacy-гейт покрывает `.html`, `_legacy_defaults/**`, фикстуры; пути к файлу хэшей в pre-commit и CI совпадают; fix fail-open/`--add` из 37-REVIEW жив на HEAD (PRIV-01/02) | цел |

---

## 4. QA-04 / Фаза 38 — трассируемость

Содержательно требование выполнено: `32-VALIDATION.md` на HEAD имеет `nyquist_compliant: true`,
`status: complete`, `validated: 2026-08-18`, все строки Per-Task Verification Map зелёные,
0 пробелов. Возражений к самому аудиту нет.

Возражение к форме: у Фазы 38 нет ни каталога, ни PLAN, ни SUMMARY, ни VERIFICATION. Единственный
артефакт закрытия — флип frontmatter внутри файла другой фазы плюс docs-коммит. Для REQ-ID это
тонкое основание, и оно дополнительно подорвано тем, что единственная находка того же аудита
(fmt-дрейф) была вынесена за скобки и не закрыта.

**Варианты:** (а) закрыть INT-01 и принять `32-VALIDATION.md` как достаточное свидетельство,
отметив QA-04 `[x]`; (б) сгенерировать минимальный `38-VERIFICATION.md`, ссылающийся на
`32-VALIDATION.md`, чтобы трассируемость была однородной с остальными фазами.

---

## 5. Nyquist-покрытие

| Фаза | VALIDATION.md | `nyquist_compliant` | Действие |
|------|---------------|---------------------|----------|
| 34 | есть (`status: complete`) | `true` | — |
| 35 | есть (`status: audited`) | `true` | — |
| 36 | есть (`status: draft`) | **`false`**, `wave_0_complete: false` | `/gsd-validate-phase 36` |
| 37 | есть (`status: draft`) | `true` | — |
| 38 | нет (фаза без каталога) | — | предмет — `32-VALIDATION.md`, он `true` |

**Overall: partial.** Фаза 36 — единственная без подтверждённого покрытия, и она же несёт
основную массу отложенного.

---

## 6. Итог

Веха содержательно сделана: шапка, тело акта, пагинация и privacy-гейт работают, десять из
одиннадцати требований имеют кодовое подтверждение.

**Закрыто в ходе аудита:**

- ~~**INT-01**~~ (2026-08-18) — CI не исполнял свои гейты почти всю веху. Закрыт двумя
  коммитами (`b26f6173`, `b4a7dc52`); прогон на `b4a7dc52` зелёный целиком, включая
  `cargo test --workspace`. Автоматическое свидетельство по DOC-04..DOC-11 снова
  производится.
- ~~**DOC-10**~~ (2026-08-19) — живая печать N=1 подтверждена на Windows/WebView2 из
  релизной сборки v1.3.3. Тем же прогоном закрыты печать при дефолтных настройках диалога,
  живой LAN-транспорт и изоляция печатного DOM. Дефектов не выявлено, правок кода
  не потребовалось — то есть отложенный риск не реализовался.

**Остаётся (не блокеры):**

1. **QA-04** — трассируемость: содержательно требование выполнено, но у фазы 38 нет своего
   VERIFICATION.md, и `REQUIREMENTS.md` всё ещё держит `[ ]` / Pending. Решение — см. §4.
2. **Nyquist фазы 36** — `36-VALIDATION.md`: `nyquist_compliant: false`. Кандидат на
   `/gsd-validate-phase 36`.
3. **INT-02** — дублированный Paged.js-хендлер между транспортами, без гейта синхронности.
4. **PRIV-03** — история git не переписана, репозиторий публичный; риск принят осознанно.

**Статус вехи: `tech_debt`** — все одиннадцать требований содержательно выполнены, блокеров нет,
остался накопленный долг под ревью перед `/gsd-complete-milestone`.
