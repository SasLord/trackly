---
phase: 28-support-admin-windows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/requests/RequestsMasterDetail.svelte
  - ui/src/features/requests/RequestsSearchAndTabs.svelte
  - ui/src/features/requests/RequestsList.svelte
  - ui/src/features/requests/RequestListRow.svelte
autonomous: true
requirements: [WIN-06]
must_haves:
  truths:
    - "D-02 (перенос из Фазы 27, Заявки): обе панели RequestsMasterDetail на `--tr-surface-raised` + border + `box-shadow var(--tr-elev-1)` — принятый регресс D-13 Фазы 26 закрыт для окна Заявок; master больше не сливается с фоном"
    - "D-05 (перенос из Фазы 27, Заявки): RequestsSearchAndTabs использует примитив `Tabs variant=\"underline\"` вместо самописных `<button class=\"tab\">`; поведение переключения статус-фильтра не изменилось, кнопка «Создать заявку» на месте"
    - "D-03 (перенос из Фазы 27, Заявки): RequestsList/RequestListRow переведены на `Table`/`TableRow` с 4 колонками (Тип/Описание/Автор/Статус) вместо двухстрочной карточки; поля не изменились, только раскладка"
    - "SC #4: поведение выбора заявки, фильтрации, создания заявки — неизменно"
  artifacts:
    - path: "ui/src/features/requests/RequestsList.svelte"
      provides: "Список заявок на Table/TableRow"
      contains: "Table"
    - path: "ui/src/features/requests/RequestsMasterDetail.svelte"
      provides: "Master-detail поверхность на --tr-surface-raised"
      contains: "--tr-surface-raised"
  key_links:
    - from: "ui/src/features/requests/RequestsList.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "импорт и потребление Table + TableRow"
      pattern: "import Table"
    - from: "ui/src/features/requests/RequestsSearchAndTabs.svelte"
      to: "ui/src/lib/components/Tabs.svelte"
      via: "импорт Tabs, строковый key-адаптер (StatusTab включает null)"
      pattern: "import Tabs"
---

<objective>
Перевести структурный слой окна **Заявок (WIN-06)** на дизайн-систему: поверхности master-detail
(D-02), табы статус-фильтра (D-05), список (D-03). Это механический перенос решений, уже
зафиксированных и исполненных Фазой 27 для окна Актов (Заявки структурно идентичны) — плейбук
применяется 1:1, без переоткрытия серых зон. **Ни одно поле, действие или workflow не меняется
(SC #4)** — только токены/примитивы.

Purpose: закрыть регресс D-13 Фазы 26 для Заявок и перевести список/фильтр на общие примитивы.
Output: 4 файла Заявок на `Tabs`/`Table`/`TableRow`/`--tr-surface-raised`, без bespoke-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/features/acts/ActsMasterDetail.svelte
@ui/src/features/acts/ActsSearchAndTabs.svelte
@ui/src/features/acts/ActsList.svelte
@ui/src/features/acts/ActListRow.svelte
@ui/src/lib/components/Tabs.svelte
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: RequestsMasterDetail (D-02) + RequestsSearchAndTabs (D-05)</name>
  <read_first>
    - ui/src/features/requests/RequestsMasterDetail.svelte (текущие поверхности: `.master` строка 31 `background: var(--tr-surface)`, `.detail` строка 39 `background: var(--tr-bg)`; grid `35% / 65%` строка 24; `min-height: calc(100vh - 240px)` строка 27; `<1099px` fallback строки 46–51 — grid и fallback сохранить дословно)
    - ui/src/features/acts/ActsMasterDetail.svelte (эталон D-02 Фазы 27: обе панели `--tr-surface-raised`+border+`box-shadow: var(--tr-elev-1)` строки 31–48; FIX B1 `flex: 1 1 auto; min-height: 0` на `.master-detail` вместо viewport-relative `min-height` строки 24–29; `.master` — flex-column с `.master > :global(*) { flex: 1 1 auto; min-height: 0 }` строки 36–48, чтобы дочерний Table (fillHeight) сам управлял скроллом)
    - ui/src/features/requests/RequestsSearchAndTabs.svelte (самописные `<button class="tab">` строки 47–57 + CSS 81–111; `TABS` — `{key: StatusTab, label}` без count строки 28–35; `StatusTab` включает `null` для «Все» строка 21; кнопка «Создать заявку» строка 60 — оставить вне Tabs; НЕТ поля поиска в этом компоненте — не искать несуществующий Input)
    - ui/src/features/acts/ActsSearchAndTabs.svelte (эталон D-05 Фазы 27: строковый key-адаптер `tabItems` строки 42–46, `<Tabs variant="underline" tabs={tabItems} active={activeTab} .../>` строки 59–65)
    - ui/src/lib/components/Tabs.svelte (контракт: `Tab.key: string`, `active: string`, `onchange: (key: string) => void`, встроенный `count`-слот опционален)
    - ui/src/styles/_tokens.scss (сверить: `--tr-surface-raised`, `--tr-elev-1`, `--tr-border`, `--tr-radius-md`)
  </read_first>
  <action>
    (D-02) В `RequestsMasterDetail.svelte` заменить фоны `.master`/`.detail`:
    `background: var(--tr-surface-raised); border: 1px solid var(--tr-border);
    border-radius: var(--tr-radius-md); box-shadow: var(--tr-elev-1);` на обеих панелях. Grid
    `35% / 65%` (строка 24) и `<1099px` fallback (46–51) — не трогать (SC #4). Дополнительно
    перенести FIX B1 из `ActsMasterDetail` (флекс-паттерн вместо viewport-relative `min-height`) —
    так как List (Task 2) и DetailPanel (2-й плейбук-плейс, уже перенесён Фазой 27, здесь только
    RequestDetail в плане 28-02) сами управляют внутренним скроллом через `fillHeight`, а
    `min-height: calc(100vh - 240px)` — унаследованный паттерн Фазы 6, который не тестировался с
    этими примитивами. Заменить `.master-detail { min-height: calc(100vh - 240px) }` на
    `flex: 1 1 auto; min-height: 0;`, `.master` на flex-column (`display: flex; flex-direction:
    column; min-height: 0;`) + добавить `.master > :global(*) { flex: 1 1 auto; min-height: 0; }`
    по образцу ActsMasterDetail строк 36–48. Свериться визуально в Task 2 (после List на Table).
    (D-05) В `RequestsSearchAndTabs.svelte` заменить самописный таб-бар на `<Tabs variant="underline"
    tabs={tabItems} active={String(filter.status)} ariaLabel="Статус заявок" onchange={(key) =>
    handleTabClick(key === 'null' ? null : (key as StatusTab))} />`. Ключевой адаптер (обязателен —
    `StatusTab` включает `null`, `Tabs` требует string-key): `tabItems = TABS.map((t) => ({ key:
    String(t.key), label: t.label }))` — без `count` (текущий компонент не передаёт статус-счётчики,
    добавлять их — вне объёма, SC #4 не изобретает новых данных). Удалить bespoke `.tab`/`.tabs` CSS
    (81–111). `<script>`-логику (`handleTabClick`, `canCreate`) не менять. Кнопка «Создать заявку»
    остаётся `<Button variant="primary">` рядом с `Tabs`, вне их разметки.
  </action>
  <acceptance_criteria>
    - `grep -c "var(--tr-surface-raised)" ui/src/features/requests/RequestsMasterDetail.svelte` == 2 (обе панели)
    - `grep -c "var(--tr-elev-1)" ui/src/features/requests/RequestsMasterDetail.svelte` &gt;= 1 И `grep -c "grid-template-columns: 35% 65%" ui/src/features/requests/RequestsMasterDetail.svelte` == 1 (grid сохранён)
    - `ui/src/features/requests/RequestsSearchAndTabs.svelte` содержит `import Tabs` И `<Tabs` И `grep -v '^\s*//' ui/src/features/requests/RequestsSearchAndTabs.svelte | grep -c 'class="tab"'` == 0
    - `grep -c "String(t.key)" ui/src/features/requests/RequestsSearchAndTabs.svelte` &gt;= 1 (строковый адаптер присутствует)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: master/detail панели «всплывают»/разделены рамкой в зависимости от темы (как D-02 Фазы 27); статус-табы переключают фильтр как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Заявки в обеих темах: панели раздельны (raised+elev), табы статуса переключаются, «Создать заявку» на месте</human-check>
  </verify>
  <done>MasterDetail на --tr-surface-raised+elev-1 (grid цел), SearchAndTabs на Tabs без bespoke .tab, поведение фильтра неизменно.</done>
</task>

<task type="auto">
  <name>Task 2: RequestsList + RequestListRow → Table/TableRow (D-03)</name>
  <read_first>
    - ui/src/features/requests/RequestsList.svelte (bespoke `.rows` скроллер + `.pagination` футер строки 54–101; `emptyConfig`-проп уже приходит готовым из `RequestsPage` — сохранить контракт; loading/empty ветки строки 27–38)
    - ui/src/features/requests/RequestListRow.svelte (двухстрочная карточка `.top`(type-badge+desc+status-badge)/`.bottom`(author+date) строки 82–100; select-состояние `.row.selected` строки 118–121; `isAdRestore` доп.badge строки 96–98)
    - ui/src/features/acts/ActsList.svelte (эталон обёртки: `<Table columns={4} loading={skeletonLoading} empty={isEmpty} emptyTitle emptyBody head={tableHead} footer={footer} framed={false} fillHeight>` строки 88–116; `skeletonLoading`/`isEmpty` derived строки 39–40)
    - ui/src/features/acts/ActListRow.svelte (эталон строки: `<TableRow selected>` + `<td onclick>`×N + `role="button"`/`onkeydown` на первой ячейке строки 52–66)
    - ui/src/lib/components/Table.svelte, ui/src/lib/components/TableRow.svelte (контракты props; `:global(> td)`-ловушка — комментарий строк 100–106 TableRow.svelte)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Перевести список заявок на `Table`/`TableRow` по эталону Актов, БЕЗ пагинации (`RequestsList`
    сейчас не пагинирует — footer показывает только `{items.length} записей` + spinner, сохранить
    этот футер как `footer`-snippet `Table`, не добавлять пагинацию, которой не было).
    `RequestsList.svelte`: обернуть в `<Table columns={4} loading={loading &amp;&amp; items.length===0}
    empty={!loading &amp;&amp; items.length===0} emptyTitle={emptyConfig.heading}
    emptyBody={emptyConfig.body} head={tableHead} footer={footer} framed={false} fillHeight>`.
    `tableHead`-snippet: `<th>Тип</th><th>Описание</th><th>Автор</th><th class="th-status">Статус</th>`.
    `footer`-snippet: сохранить текущий `<footer class="pagination">{items.length} записей</footer>`
    + spinner-при-loading как есть (переименовать класс, если конфликтует с Table's own `.tr-table-footer`
    — оставить `pager-info` стиль). `emptyConfig.actionLabel`/`onAction`-кнопку (строки 35–37) перенести
    в `footer` или оставить условно в empty-ветке через emptyBody — Table сам не рендерит action-кнопку
    в empty-state, поэтому кнопку «Создать заявку»/аналог из emptyConfig нужно отрендерить дополнительно
    (например через отдельный conditional `<Button>` рядом с Table, видимый только при `isEmpty &&
    emptyConfig.actionLabel`) — как в текущем коде, просто снаружи Table. Удалить bespoke `.rows`/
    `.loading`/`.empty`/`.pagination` (54–101), их роль забирает `Table`.
    `RequestListRow.svelte`: `<TableRow selected={selected} class="request-row">` с 4 `<td class="cell"
    onclick={onclick}>` — Тип (Badge type + опциональный warning-badge `isAdRestore`), Описание
    (`shortDesc`), Автор (`request.requesterName ?? '—'`), Статус (Badge). `role="button"`/`tabindex="0"`/
    `onkeydown={handleKeydown}` — на первой ячейке (единственная точка входа клавиатуры, как ActListRow).
    Существующая логика в `<script>` (`statusVariant`/`statusLabel`/`typeLabel`/`shortDesc`/
    `relativeDate`/`isAdRestore`/`handleKeydown`) не меняется — переносится только разметка.
    Относительная дата (`relativeDate`) — второй строкой/tooltip в ячейке «Автор» или «Статус» —
    Claude's Discretion по финальной раскладке колонок (поля не убираются, только перекомпоновка
    из 2-строчной карточки в 4-колоночную таблицу). **Ловушка `:global()`:** CSS для caller-`<td>`
    писать формой `.request-row :global(> td)`, НЕ `:global(.request-row > td)`.
    **Не менять** `Table.svelte`/`TableRow.svelte` — не ломать существующих потребителей (Устройства,
    витрина, ActFormItemsTable, окна Фазы 27).
  </action>
  <acceptance_criteria>
    - `ui/src/features/requests/RequestsList.svelte` содержит `import Table` И `<Table`
    - `ui/src/features/requests/RequestListRow.svelte` содержит `import TableRow` И `<TableRow`
    - `grep -v '^\s*//' ui/src/features/requests/RequestsList.svelte | grep -c 'class="pagination"'` == 0 И `grep -v '^\s*//' ui/src/features/requests/RequestListRow.svelte | grep -c 'class="row"'` == 0 (bespoke удалены)
    - `git diff --name-only` не содержит `ui/src/lib/components/Table.svelte` или `ui/src/lib/components/TableRow.svelte`
    - Все существующие поля видны в разметке: `grep -c "typeLabel\|shortDesc\|requesterName\|statusLabel" ui/src/features/requests/RequestListRow.svelte` &gt;= 4
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: список заявок — табличная строка, клик/Enter выбирает и подсвечивает (accent-полоса), пустое/загрузочное состояние из Table, «Восстановление доступа»-badge на месте
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Список заявок = таблица; клик выделяет строку; тип/описание/автор/статус видны; badge «Восстановление доступа» на месте; те же поля, что раньше</human-check>
  </verify>
  <done>RequestsList/RequestListRow на Table/TableRow с 4 колонками; bespoke .pagination/.row удалены; потребители Table не сломаны; поля не изменились.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. Окно Заявок — чисто визуальная ре-токенизация (SC #4): поля, действия,
статус-фильтр, API не меняются. Единственный релевантный риск — случайное поведенческое изменение,
протащенное под видом «чисто визуальной» правки (например, потеря `null`-ветки статус-фильтра «Все»
при string-адаптации Tabs, или смена select-логики строки списка).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering (regression) | RequestsSearchAndTabs string-адаптер (`StatusTab` включает `null`) | mitigate | явный `key === 'null' ? null : key` round-trip в `onchange`, acceptance criteria проверяет наличие адаптера |
| T-28-01-02 | Tampering (regression) | RequestsList/RequestListRow → Table/TableRow | mitigate | `Table`/`TableRow.svelte` не входят в дифф (grep-гейт), все 4 поля присутствуют в новой разметке, human-check обеих тем |
| T-28-01-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается — только существующие примитивы |

Угроз уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Регресс существующих потребителей Table/TableRow (Устройства, витрина, ActFormItemsTable, окна Фазы 27) — визуально не сломаны
</verification>

<success_criteria>
Структурный слой Заявок (master-detail, статус-фильтр, список) целиком на `Tabs`/`Table`/`TableRow` и
токенах `--tr-*`, без bespoke-классов; регресс D-13 закрыт для Заявок; поля/действия/workflow сохранены
(SC #4).
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-01-SUMMARY.md` when done
</output>
