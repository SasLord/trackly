---
phase: 28-support-admin-windows
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/settings/BackupSettings.svelte
  - ui/src/features/settings/NetworkSettings.svelte
autonomous: true
requirements: [WIN-08]
must_haves:
  truths:
    - "D-04 (Настройки, BackupSettings): расписание (select) → Select, ретенция (raw number input) → Input; `.folder-code` (путь бэкап-папки) обязательно переходит с bare `font-family: monospace` на `--tr-text-mono` (DS-03, UI-SPEC §4 явно называет этот сайт «обязательное применение»)"
    - "D-04 (Настройки, NetworkSettings): порт/bind-адрес/путь к сертификату → Input/Select, чекбокс «Требовать вход в десктопе» → Checkbox; функция серверного тумблера/desktop-lock не меняется"
    - "SC #4: сохранение расписания бэкапа, ручной бэкап, старт/стоп сервера, disabled-состояния (сервер запущен → поля параметров disabled) — без изменений"
  artifacts:
    - path: "ui/src/features/settings/BackupSettings.svelte"
      provides: "Панель бэкапов на Select/Input с --tr-text-mono путём"
      contains: "--tr-text-mono"
  key_links:
    - from: "ui/src/features/settings/BackupSettings.svelte"
      to: "ui/src/styles/_tokens.scss"
      via: "класс tr-mono (глобальный, global.scss) на элементе .folder-code"
      pattern: "tr-mono"
---

<objective>
Ре-токенизировать внутренности двух панелей окна **Настроек (WIN-08)**: `BackupSettings` (D-04 +
обязательный переход `.folder-code` на `--tr-text-mono`, DS-03) и `NetworkSettings` (D-04, крупнейшая
из «средних» панелей — тумблер сервера, параметры порт/bind/сертификат, чекбокс desktop-lock).
**Ни одна форма/действие не меняет поведение (SC #4)**.

Purpose: закрыть D-04 для двух панелей средней сложности + обязательный DS-03 mono-фикс.
Output: 2 файла на `Input`/`Select`/`Checkbox`, без raw `<input>`/`<select>`/`.form-*`-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-UI-SPEC.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/features/devices/DeviceFormBody.svelte
@ui/src/lib/components/Input.svelte
@ui/src/lib/components/Select.svelte
@ui/src/lib/components/Checkbox.svelte
@ui/src/styles/_tokens.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: BackupSettings ре-токенизация + обязательный --tr-text-mono (D-04, DS-03)</name>
  <read_first>
    - ui/src/features/settings/BackupSettings.svelte (ПРОЧИТАН ЦЕЛИКОМ; 337 строк — `<code class="folder-code">{backupFolder ?? 'Не выбрана'}</code>` строка 134 с bare `font-family: monospace` в scoped CSS строка 264; `<select id="backup-schedule" class="form-select" bind:value={schedule}>` строки 147–156 с 3 `<option>`; `<input id="backup-retention" class="form-input" type="number" bind:value={retention}>` строки 166–173 с суффиксом «копий»; `pickFolder`/`saveConfig`/`runManualBackup` — НЕ менять)
    - ui/src/styles/_tokens.scss строка 263 (`--tr-text-mono: 500 13px var(--tr-font-mono)`) + ui/src/styles/global.scss строки 97–100 (`.tr-mono { font: var(--tr-text-mono); font-variant-numeric: tabular-nums; }` — ГЛОБАЛЬНЫЙ класс, доступен в любом компоненте БЕЗ `:global()`, достаточно `class="folder-code tr-mono"` на markup-элементе)
    - ui/src/lib/components/Select.svelte (контракт: `value: string`, `children: Snippet` для `<option>`)
    - ui/src/lib/components/Input.svelte (контракт: `type?:'number'`, `value: string` — `retention` число, нужен `String()`/`Number()` round-trip)
  </read_first>
  <action>
    (DS-03, обязательное) На `<code class="folder-code">` добавить `tr-mono` в class-list:
    `class="folder-code tr-mono"`. В scoped `.folder-code` CSS удалить `font-family: monospace` (шрифт
    теперь задаёт глобальный `.tr-mono`), остальные layout-свойства (overflow/ellipsis/max-width/
    background/padding/radius) оставить как есть.
    (D-04) Заменить `<select class="form-select">` (расписание) на `<Select id="backup-schedule"
    value={schedule} disabled={!backupFolder} onchange={(v) => (schedule = v)}>` с теми же 3
    `<option>` внутри (children snippet). Заменить `<input class="form-input" type="number">`
    (ретенция) на `<Input id="backup-retention" type="number" value={String(retention)}
    oninput={(v) => (retention = Number(v) || 1)} />`, суффикс «копий» — оставить рядом как отдельный
    `<span class="input-suffix">`. Удалить bespoke `.form-select`/`.form-input` CSS, заменённые
    примитивами. `pickFolder`/`saveConfig`/`runManualBackup`-логика, disabled-условия (`!backupFolder`)
    — НЕ менять.
  </action>
  <acceptance_criteria>
    - `grep -c 'class="folder-code tr-mono"' ui/src/features/settings/BackupSettings.svelte` == 1
    - `grep -c "font-family: monospace" ui/src/features/settings/BackupSettings.svelte` == 0
    - `ui/src/features/settings/BackupSettings.svelte` содержит `import Select` И `import Input` И `&lt;Select` И `&lt;Input`
    - `grep -v '^\s*//' ui/src/features/settings/BackupSettings.svelte | grep -c 'class="form-select"\|class="form-input"'` == 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: путь к папке рендерится моноширинным (как инвентарные номера), расписание/ретенция сохраняются, ручной бэкап работает, disabled-подсказка при отсутствии папки показывается
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Настройки → Бэкапы: путь к папке моноширинный, расписание/ретенция редактируются и сохраняются, «Создать резервную копию вручную» работает</human-check>
  </verify>
  <done>BackupSettings на Select/Input; .folder-code на --tr-text-mono вместо bare monospace; поведение неизменно.</done>
</task>

<task type="auto">
  <name>Task 2: NetworkSettings ре-токенизация (D-04)</name>
  <read_first>
    - ui/src/features/settings/NetworkSettings.svelte (ПРОЧИТАН ЦЕЛИКОМ; 452 строки — Section 1 «Серверный режим»: `<input type="number" bind:value={settings.port} disabled={saving||serverRunning}>` строки 180–188, `<select bind:value={settings.host} disabled={saving||serverRunning}>` строки 193–201 (2 option), `<input type="text" bind:value={settings.cert_path}>` строки 206–213 (placeholder); Section 2 «Безопасность рабочего стола»: `<input type="checkbox" checked={settings.desktop_lock_enabled} onchange={(e)=>toggleDesktopLock(...)}>` строки 234–239; `.fingerprint { font-family: monospace }` строка 346–350 — вне явного объёма правки (не в списке UI-SPEC §9.3 обязательных, аналог `.db-path-code` StorageSettings), НЕ трогать)
    - ui/src/lib/components/Input.svelte, Select.svelte, Checkbox.svelte (контракты)
    - ui/src/features/devices/DeviceFormBody.svelte (эталон field+error разметки, если нужен паттерн `.field`+label)
  </read_first>
  <action>
    Заменить 3 raw-поля Section 1: порт → `<Input id="net-port" type="number"
    value={String(settings.port)} disabled={saving||serverRunning} oninput={(v) => (settings.port =
    Number(v) || 0)} />`; bind-адрес → `<Select id="net-host" value={settings.host}
    disabled={saving||serverRunning} onchange={(v) => (settings.host = v)}>` с теми же 2 `<option>`;
    путь к сертификату → `<Input id="net-cert" type="text" value={settings.cert_path}
    disabled={saving||serverRunning} placeholder="Оставьте пустым для самоподписанного сертификата"
    oninput={(v) => (settings.cert_path = v)} />`. Заменить чекбокс Section 2 на `<Checkbox
    id="desktop-lock" checked={settings.desktop_lock_enabled} disabled={lockToggling}
    onchange={(checked) => toggleDesktopLock(checked)}>Требовать вход в десктопе</Checkbox>`. Удалить
    bespoke `.form-input`/`.form-select`/`.checkbox-label` CSS, заменённые примитивами. `.fingerprint`
    (отпечаток сертификата) — НЕ трогать (вне явного объёма, симметрично `.db-path-code`
    StorageSettings). `loadSettings`/`saveSettings`/`toggleServer`/`toggleDesktopLock`-логика — НЕ
    менять.
  </action>
  <acceptance_criteria>
    - `ui/src/features/settings/NetworkSettings.svelte` содержит `import Input` И `import Select` И `import Checkbox`
    - `grep -v '^\s*//' ui/src/features/settings/NetworkSettings.svelte | grep -c 'class="form-input"\|class="form-select"\|class="checkbox-label"'` == 0
    - `grep -c "font-family: monospace" ui/src/features/settings/NetworkSettings.svelte` == 1 (`.fingerprint` намеренно не тронут)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: порт/bind-адрес/сертификат редактируются и disabled при запущенном сервере; старт/стоп сервера работает; чекбокс desktop-lock переключается с toast-подтверждением
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Настройки → Сеть: запустить/остановить сервер, изменить параметры (при остановленном), переключить «Требовать вход в десктопе»</human-check>
  </verify>
  <done>NetworkSettings на Input/Select/Checkbox; disabled-условия и toggle-логика неизменны; .fingerprint намеренно не тронут.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. `NetworkSettings` управляет запуском/остановкой LAN HTTP-сервера
(server_toggle) и desktop-lock требованием — обе функции уже защищены существующими Tauri-командами;
эта волна не касается их авторизации/логики, только markup ре-токенизация.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | Tampering (regression) | NetworkSettings disabled-условия (`saving||serverRunning`) при переносе на Input/Select | mitigate | disabled-проп переносится буквально на каждый примитив, acceptance criteria + human-check проверяют disabled-состояние при запущенном сервере |
| T-28-06-02 | Tampering (regression) | NetworkSettings desktop-lock чекбокс (влияет на требование входа в приложение) | mitigate | `onchange`-обработчик (`toggleDesktopLock`) переносится буквально, не переписывается; human-check подтверждает toast |
| T-28-06-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается |

Угроз уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Human-check подтверждает: бэкап-конфигурация и сетевые настройки работают идентично прежним
</verification>

<success_criteria>
BackupSettings и NetworkSettings — на `Input`/`Select`/`Checkbox` без bespoke `.form-*`-классов;
путь к бэкап-папке моноширинный через `--tr-text-mono`; disabled-условия, тумблер сервера и
desktop-lock сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-06-SUMMARY.md` when done
</output>
