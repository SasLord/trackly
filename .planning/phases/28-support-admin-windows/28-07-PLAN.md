---
phase: 28-support-admin-windows
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/settings/ActiveDirectorySettings.svelte
  - ui/src/features/settings/OrgSettings.svelte
autonomous: true
requirements: [WIN-08]
must_haves:
  truths:
    - "D-04 (Настройки, ActiveDirectorySettings): чекбокс «Использовать AD» → Checkbox; два radio «Автоматически принимать»/«Требовать подтверждения» → Radio (bind:group-адаптер вместо двух независимых checked/onchange — `settings.auto_accept: boolean` требует string-group-адаптер, аналогично string-key-адаптерам Tabs D-05/D-06); 5 read-only bootstrap-полей → Input/Checkbox disabled"
    - "D-04 (Настройки, OrgSettings): 10 raw текстовых полей (название/ИНН/КПП/адрес×2/телефон/факс/email/ОКПО/ОГРН) → Input; секция логотипа (уже на Button, скрытый file-input) не трогается"
    - "SC #4: сохранение настроек AD (enabled/auto_accept), проверка подключения, сохранение реквизитов организации, загрузка/удаление логотипа — без изменений"
  artifacts:
    - path: "ui/src/features/settings/OrgSettings.svelte"
      provides: "10 полей реквизитов организации на Input"
      contains: "import Input"
  key_links:
    - from: "ui/src/features/settings/ActiveDirectorySettings.svelte"
      to: "ui/src/lib/components/Radio.svelte"
      via: "bind:group-адаптер вокруг boolean settings.auto_accept"
      pattern: "import Radio"
---

<objective>
Ре-токенизировать внутренности двух панелей окна **Настроек (WIN-08)**: `ActiveDirectorySettings`
(D-04 — единственный файл фазы с настоящим structural-gotcha: два radio на boolean-поле требуют
`Radio.svelte`'s `bind:group`-адаптер, не прямой 1:1 замены) и `OrgSettings` (D-04 — наибольший объём
raw-полей, 10 текстовых инпутов). **Ни одна форма/действие не меняет поведение (SC #4)**.

Purpose: закрыть D-04 для двух панелей с наибольшим числом raw-контролов.
Output: 2 файла на `Input`/`Checkbox`/`Radio`, без raw `<input>`/`.form-*`-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-UI-SPEC.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/features/devices/DeviceFormBody.svelte
@ui/src/lib/components/Input.svelte
@ui/src/lib/components/Checkbox.svelte
@ui/src/lib/components/Radio.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActiveDirectorySettings ре-токенизация + radio-group-адаптер (D-04)</name>
  <read_first>
    - ui/src/features/settings/ActiveDirectorySettings.svelte (ПРОЧИТАН ЦЕЛИКОМ; 361 строка — чекбокс «Использовать AD» строки 89–99 `checked={settings.enabled}` `onchange={(e)=>(settings.enabled=(e.target as HTMLInputElement).checked)}`; ДВА `<input type="radio" name="ad-reg-mode">` строки 105–133 — `checked={settings.auto_accept}`/`onchange={()=>(settings.auto_accept=true)}` и `checked={!settings.auto_accept}`/`onchange={()=>(settings.auto_accept=false)}`, оба `disabled={saving||!settings.enabled}`; `<details class="advanced-details">` с 5 read-only bootstrap-полями (host:port/domain/base_dn/name_attr/no_tls_verify, все `disabled`) строки 149–184; `saveSettings`/`testConnection` строки 128–161 — НЕ менять)
    - ui/src/lib/components/Radio.svelte (контракт: `group = $bindable(string|number|null)`, `value: string|number`, `disabled?`, НЕТ прямого `onchange`-пропа — состояние читается через `bind:group`)
    - ui/src/lib/components/Checkbox.svelte (контракт: `checked = $bindable`, `onchange?: (checked:boolean)=>void`)
    - ui/src/lib/components/Input.svelte (контракт: `value: string`, `disabled?`)
  </read_first>
  <action>
    Заменить чекбокс «Использовать AD» на `<Checkbox id="ad-enabled" checked={settings.enabled}
    disabled={saving} onchange={(checked) => (settings.enabled = checked)}>Использовать Active
    Directory</Checkbox>`.
    **Radio-group-адаптер (обязателен — Radio требует `bind:group` на ОДНОЙ переменной, текущий код
    держит независимый `boolean settings.auto_accept`):** ввести производную строку `let regMode =
    $derived(settings.auto_accept ? 'auto' : 'confirm')` НЕЛЬЗЯ напрямую забиндить как `bind:group`
    (derived не bindable) — вместо этого использовать `$state`-прокси, синхронизированный
    двунаправленно: `let regMode = $state&lt;'auto'|'confirm'&gt;('auto')`, `$effect(() => { regMode =
    settings.auto_accept ? 'auto' : 'confirm'; })` (внешний источник — `loadSettings()`/`onMount`
    перезаписывает `settings`, эффект держит `regMode` синхронным), и `$effect(() => { settings.auto_
    accept = regMode === 'auto'; })` (изменение через Radio обновляет `settings.auto_accept`, которое
    читает `saveSettings()`). Разметка: `<Radio bind:group={regMode} value="auto"
    disabled={saving||!settings.enabled}>Автоматически принимать</Radio>` +
    `<Radio bind:group={regMode} value="confirm" disabled={saving||!settings.enabled}>Требовать
    подтверждения</Radio>`, текст-описание под каждым (`<span class="helper-text">`) — сохранить
    дословно. Если двойной `$effect`-цикл создаёт нежелательный re-render loop — альтернатива:
    вычислять `regMode` только внутри `loadSettings()` (`regMode = s.auto_accept ? 'auto' :
    'confirm'` сразу после `settings = s`) и синхронизировать `settings.auto_accept` только в
    `onchange`-подобном месте — но `Radio` не даёт `onchange`, поэтому единственный надёжный путь —
    `$effect` на `regMode` → `settings.auto_accept`; проверить в браузере, что переключение radio не
    зацикливается (эффект #2 не должен ретриггерить эффект #1, т.к. эффект #1 читает `settings.auto_
    accept`, а не `regMode`).
    5 read-only bootstrap-полей (`<details>`-блок) → `<Input value={...} disabled />`/`<Checkbox
    checked={...} disabled>` (host:port составное значение — как есть, `disabled` без `onchange`).
    Удалить bespoke `.checkbox-label`/`.radio-label`/`.form-input` CSS. `saveSettings`/
    `testConnection`-логика, `save-row`-кнопки (`Сохранить настройки`/`Проверить подключение`) — НЕ
    менять.
  </action>
  <acceptance_criteria>
    - `ui/src/features/settings/ActiveDirectorySettings.svelte` содержит `import Checkbox` И `import Radio` И `import Input`
    - `grep -c "bind:group" ui/src/features/settings/ActiveDirectorySettings.svelte` &gt;= 2 (оба Radio связаны с одной group-переменной)
    - `grep -v '^\s*//' ui/src/features/settings/ActiveDirectorySettings.svelte | grep -c 'type="radio"\|type="checkbox"'` == 0 (raw radio/checkbox заменены)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: включение/выключение AD работает, переключение между «Автоматически принимать»/«Требовать подтверждения» сохраняет корректное значение (проверить `saveSettings` payload — `Проверить подключение` реагирует на `settings.enabled`), read-only поля показывают bootstrap-конфиг
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Настройки → Active Directory: включить AD, переключить оба radio-режима, сохранить — проверить, что «Сохранено» соответствует выбранному режиму (не наоборот); «Проверить подключение» работает при включённом AD</human-check>
  </verify>
  <done>ActiveDirectorySettings на Checkbox/Radio/Input; radio-group-адаптер синхронизирован с settings.auto_accept без зацикливания; read-only поля сохранены.</done>
</task>

<task type="auto">
  <name>Task 2: OrgSettings ре-токенизация — 10 текстовых полей (D-04)</name>
  <read_first>
    - ui/src/features/settings/OrgSettings.svelte (ПРОЧИТАН ЦЕЛИКОМ; 508 строк — 10 raw `<input class="form-input">` в `.form-grid`: org-name/org-inn/org-kpp/org-address/org-address-line2/org-phone/org-fax/org-email/org-okpo/org-ogrn, строки 233–334, все `bind:value`; секция логотипа (строки 344–376) уже на `Button` + hidden `<input type="file">` — НЕ трогать, T-07-04-05 требует `<img>`-рендер, не менять; `saveOrg`-логика — не показана в этом диапазоне, НЕ менять)
    - ui/src/lib/components/Input.svelte (контракт: `value: string`, `type?: 'text'|'number'|'search'` — email-поле использует `type="email"`, `Input.svelte` НЕ включает `'email'` в типе — оставить `type="text"` с тем же `placeholder`, т.к. `Input` не поддерживает native email-валидацию; если это регрессия HTML5-валидации — задокументировать в SUMMARY, не блокирует SC #4 т.к. серверная валидация остаётся авторитетной)
    - ui/src/features/devices/DeviceFormBody.svelte (эталон `<div class="field">`+`<label class="label">`+`Input` разметки)
  </read_first>
  <action>
    Заменить все 10 raw `<input class="form-input" bind:value={...}>` на `<Input id="..."
    value={...} oninput={(v) => (... = v)} placeholder="..." />` — построчная замена `bind:value` →
    `value`+`oninput`-адаптер (Input.svelte не поддерживает `bind:value` двусторонне через bindable
    в этой версии контракта — проверить фактический контракт `Input.svelte`: если `value =
    $bindable('')`, доступен `bind:value` напрямую, использовать `bind:value={orgName}` и т.д. без
    ручного `oninput`-адаптера — предпочтительнее, проще, меньше кода). Поле `org-email`
    (`type="email"`) → `<Input type="text" .../>` (Input не поддерживает `'email'` — см. read_first,
    задокументировать отличие). Удалить bespoke `.form-input` CSS (429+), заменённый примитивом.
    Секция логотипа (строки 344–376, file-upload) — НЕ трогать. `saveOrg`-логика — НЕ менять.
  </action>
  <acceptance_criteria>
    - `ui/src/features/settings/OrgSettings.svelte` содержит `import Input` И `grep -c '&lt;Input' ui/src/features/settings/OrgSettings.svelte` &gt;= 10
    - `grep -v '^\s*//' ui/src/features/settings/OrgSettings.svelte | grep -c 'class="form-input"'` == 0
    - Секция логотипа не тронута: `grep -c 'type="file"' ui/src/features/settings/OrgSettings.svelte` == 1
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: все 10 полей реквизитов редактируются и сохраняются («Сохранить настройки организации»), логотип загружается/удаляется как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Настройки → Организация: отредактировать несколько полей, сохранить, перезагрузить страницу — значения сохранены; логотип загружается/удаляется без ошибок</human-check>
  </verify>
  <done>OrgSettings на Input для всех 10 текстовых полей; секция логотипа не тронута; сохранение работает как раньше.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. `ActiveDirectorySettings` управляет включением AD-аутентификации на уровне
приложения — эта волна не меняет `settings_set_ad`/`ad_test_connection`-вызовы, только перенос
markup + обязательный radio-group-адаптер. Главный риск — адаптер инвертирует или теряет
`auto_accept`-значение при сохранении.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-01 | Tampering (regression) | ActiveDirectorySettings radio-group-адаптер (`regMode` ↔ `settings.auto_accept`) | mitigate | явный двунаправленный `$effect`-контракт задокументирован в action, human-check проверяет, что сохранённое значение соответствует выбранному режиму (не инвертировано) |
| T-28-07-02 | Tampering (regression) | OrgSettings 10 полей реквизитов (используются в печатных актах — Фаза 20) | mitigate | `bind:value`/`oninput`-адаптер переносит значения без потери, human-check подтверждает сохранение+перезагрузку |
| T-28-07-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается |

Угроз уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Human-check подтверждает: AD-режим сохраняется корректно (не инвертирован), 10 полей организации сохраняются
</verification>

<success_criteria>
ActiveDirectorySettings и OrgSettings — на `Checkbox`/`Radio`/`Input` без raw-контролов; radio-group-
адаптер корректно синхронизирован с boolean `auto_accept`; логотип и все 10 полей реквизитов сохранены
(SC #4).
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-07-SUMMARY.md` when done
</output>
