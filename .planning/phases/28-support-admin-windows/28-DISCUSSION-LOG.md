# Phase 28: Окна поддержки и администрирования - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-22
**Phase:** 28-support-admin-windows
**Areas discussed:** Суб-навигация, Таблица отчётов, Редактор шаблонов, Выбор периода (свёрнут в Суб-навигацию)

---

## Выбор серых зон (multiselect)

Представлены 4 серые зоны, специфичные для Фазы 28 (Заявки и Пользователи механически идут по
плейбуку Фазы 27, не обсуждались). Пользователь выбрал: **все четыре** — Суб-навигация, Таблица
отчётов, Редактор шаблонов, Выбор периода.

В ходе скаута выяснилось: **PeriodSelector уже использует примитив `DatePicker`** для дат. Остаток
(режимы Месяц/Год/Диапазон + месяц/год-селекты) сворачивается в решение по суб-навигации/примитивам,
поэтому «Выбор периода» не стал отдельным решением.

---

## Суб-навигация и сегментные переключатели

| Option | Description | Selected |
|--------|-------------|----------|
| Tabs везде + счётчики через примитив | Единый `Tabs`: underline для типов/разделов, segmented для домена/режимов; счётчики через count-slot, при необходимости расширить примитив под accent-тон | ✓ |
| Tabs везде, счётчики — простой span | Адоптировать Tabs без расширения; accent-тон Badge у активного таба упрощается | |
| Оставить bespoke, только ре-токенизация | Не трогать структуру суб-навов; минимальный риск, но 3 разных таб-паттерна | |

**User's choice:** Tabs везде + счётчики через примитив
**Notes:** Расширять общий примитив, а не форкать. Покрывает ReportSubNav (2 уровня), SettingsSubNav
(7 разделов) и сегментный переключатель PeriodSelector.

---

## Таблица отчётов

| Option | Description | Selected |
|--------|-------------|----------|
| На примитив Table | Общий `Table`/`TableRow`, колонки строятся динамически — продолжение D-03; planner сверяет API под динамические колонки/итоги | ✓ |
| Специализированная, только ре-токенизация | Оставить свой `<table>`, обернуть в тот же язык поверхностей — если динамика плохо ложится на TableRow | |

**User's choice:** На примитив Table
**Notes:** Если динамические колонки/итоги плохо ложатся на текущий `TableRow` API — расширить
примитив, а не форкать (то же правило, что для Tabs). Данные/логика отчёта не меняются (SC #4).

---

## Редактор шаблонов (TemplateEditor)

| Option | Description | Selected |
|--------|-------------|----------|
| Строго визуально: хрома + форм-контролы | Кнопки/поля/селекты → примитивы; область правки и превью не трогать; строго SC #4 | ✓ |
| Глубже: включая область редактора | Переработать область редактирования/превью; риск задеть DB-backed шаблоны и seed/upgrade | |

**User's choice:** Строго визуально: хрома + форм-контролы
**Notes:** Редактор функционально-чувствителен (шаблоны в БД, ловушка seed/upgrade). Граница строгая.

---

## Готовность

Пользователь подтвердил готовность к CONTEXT.md после фиксации всех четырёх решений (все —
рекомендованные, консервативные, последовательные с Фазами 26–27).

## Claude's Discretion

- Сопоставление колонок списков/таблицы с API `TableRow`; нужно ли расширять `Table`/`TableRow` под
  динамические колонки отчётов.
- Нужно ли и как расширять count-slot `Tabs` под accent-тон счётчиков.
- Точный состав секций `RequestDetail` при переносе на detail-примитивы (без изменения полей).
- Дробление на волны/планы; порядок — общие примитивы первыми, затем 4 окна параллельно.
- Конкретные значения ре-токенизации без прецедента — из `_tokens.scss` и окон Фаз 26–27.

## Deferred Ideas

- Полноценная мобильная адаптивность окон Фазы 28 (WIN-06…09 не покрывают).
- Редизайн раскладок (секции деталей/панелей, формы, область редактора шаблонов).
- AA-контраст, focus ring, паритет Tauri vs LAN-браузер — QA-02/QA-03, Фаза 30.
- Грep-гейт на `:global(` в plain `.scss` (WR-15 Фазы 24).
