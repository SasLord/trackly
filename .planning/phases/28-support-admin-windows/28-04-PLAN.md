---
phase: 28-support-admin-windows
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/reports/ReportTable.svelte
  - ui/src/features/reports/ReportFilters.svelte
  - ui/src/features/reports/ReportsPage.svelte
autonomous: true
requirements: [WIN-07]
must_haves:
  truths:
    - "D-07 (Отчёты, серая зона B): ReportTable переведён на общий примитив `Table`/`TableRow` с ДИНАМИЧЕСКИМИ колонками (Column[] меняется по типу отчёта), без расширения примитива API — `head`/`children` snippet уже рендерят произвольную разметку"
    - "D-07: месяц/локация-разделитель ReportTable рендерится как голый `<tr class=\"report-separator\">` внутри Table's children-сниппета, а НЕ через `TableRow group={true}` — разделитель статичен (без сворачивания/счётчика), форсировать его в group-семантику потребовало бы либо необязательных пропов, либо фиктивного onToggleGroup"
    - "PageHeader adoption (находка пэттерн-мапера, консистентность компонентов SC #1): ReportsPage потребляет примитив `PageHeader` вместо bespoke `<header class=\"page-header\"><h1 class=\"page-title\">`"
    - "SC #4: набор колонок, экспорт CSV/печать, error/empty-состояния, содержимое разделителей-строк — без изменений"
  artifacts:
    - path: "ui/src/features/reports/ReportTable.svelte"
      provides: "Таблица отчётов с динамическими колонками на Table/TableRow"
      contains: "Table"
  key_links:
    - from: "ui/src/features/reports/ReportTable.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "динамический head/children snippet под Column[]"
      pattern: "import Table"
    - from: "ui/src/features/reports/ReportsPage.svelte"
      to: "ui/src/lib/components/PageHeader.svelte"
      via: "импорт PageHeader (title, без actions — в шапке Отчётов нет кнопок)"
      pattern: "import PageHeader"
---

<objective>
Перевести таблицу данных и шапку окна **Отчётов (WIN-07)** на дизайн-систему: `ReportTable`
(динамические колонки под тип отчёта + статичный месяц/локация-разделитель — вторая реальная серая
зона фазы, D-07, решена в 28-UI-SPEC.md §6.2 кодовой проверкой: `Table`/`TableRow` уже покрывают кейс
без расширения API, разделитель — голый `<tr>`, не `group`-режим), аудит `ReportFilters` (почти
целиком уже на `Button`), `ReportsPage` → `PageHeader`. **Ни один набор колонок, экспорт или
error/empty-текст не меняется (SC #4)**.

Purpose: закрыть D-07 (единая табличная инфраструктура для Отчётов) и SC #1 для оставшихся файлов окна.
Output: 3 файла на `Table`/`TableRow`/`PageHeader`, без bespoke `<table>`/header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-UI-SPEC.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/features/acts/ActsPage.svelte
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
@ui/src/lib/components/PageHeader.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportTable → Table/TableRow, динамические колонки + голый tr-разделитель (D-07)</name>
  <read_first>
    - ui/src/features/reports/ReportTable.svelte (ПРОЧИТАН ЦЕЛИКОМ; 243 строки — `Column{key,label}` проп строки 25–28; `grouped` derived вставляет `{type:'separator',label}` при смене `month_key`(temporal)/`location_name`(snapshot) строки 76–98; loading/error/empty ветки строки 101–114 — error-текст «Не удалось загрузить отчёт. Попробуйте ещё раз.» KEEP; bespoke `<table class="report-table">` строки 115–144 с `.month-separator` строки 234–242 — стили переносятся ДОСЛОВНО в новый `.report-separator`)
    - ui/src/lib/components/Table.svelte (контракт: `columns:number`, `head:Snippet`, `children?:Snippet` — оба рендерят произвольную разметку, динамический набор колонок ложится напрямую; `loading`/`empty`/`emptyTitle`/`emptyBody` — только 2 состояния, нет `error`)
    - .planning/phases/28-support-admin-windows/28-UI-SPEC.md §6.2 (полный код-снипет разделителя-строки и стилей `.report-separator`, footer НЕ нужен — текущий ReportTable без итоговой строки)
    - ui/src/lib/components/TableRow.svelte (контракт `group`-режима — НЕ использовать для разделителя, см. обоснование в 28-UI-SPEC §6.2)
  </read_first>
  <action>
    Обернуть тело `ReportTable.svelte` в `<Table columns={columns.length} loading={loading}
    empty={rows.length===0 &amp;&amp; !loading} emptyTitle="Нет данных за выбранный период"
    emptyBody="Измените диапазон дат или выберите другой тип отчёта." head={tableHead}>` (KEEP оба
    текста дословно, UI-SPEC §7.2). `tableHead`-snippet: `{#each columns as col}<th>{col.label}</th>
    {/each}`. Тело — `{#each grouped as item}{#if 'type' in item &amp;&amp; item.type==='separator'}
    <tr class="report-separator" aria-hidden="true"><td colspan={columns.length}>{item.label}</td>
    </tr>{:else}{@const row=item}<TableRow>{#each columns as col}{@const cellVal=
    formatCellValue(row,col.key)}<td title={cellVal}>{cellVal}</td>{/each}</TableRow>{/if}{/each}` —
    разделитель как голый `<tr>` РЯДОМ с `<TableRow>` внутри `Table`'s `children`, НЕ через `TableRow
    group={true}` (обоснование: group-режим — контракт сворачивания с `groupExpanded`/`onToggleGroup`,
    разделитель месяца/локации статичен). Стили `.report-separator td` — перенести ДОСЛОВНО из текущего
    `.month-separator td` (строки 234–242: `padding: var(--tr-space-2xs) var(--tr-space-md); height:
    var(--row-height-dense); background: var(--tr-surface-sunken); font-size:
    var(--tr-font-size-body); font-weight: var(--tr-font-weight-semibold); border-top: 1px solid
    var(--tr-border-strong); color: var(--tr-text-primary);`) — свериться пиксель-в-пиксель со старым
    значением при исполнении. `error`-состояние (нет эквивалента в `Table`'s loading/empty API) —
    оставить sibling-веткой ВНЕ `Table`, как в текущем коде (`{#if error}<div class="state
    state-error">…{:else}<Table …>…{/Table}{/if}`), текст «Не удалось загрузить отчёт. Попробуйте
    ещё раз.» — KEEP. Удалить bespoke `.report-table`/`thead`/`th`/`td`/`tbody tr:hover` CSS,
    заменённые Table/TableRow. `footer`-слот `Table` НЕ вводить — текущий ReportTable не имеет
    итоговой строки, добавлять её «на будущее» — вне объёма (SC #4). `formatCellValue`/`formatMonthKey`
    и `grouped`-derived логика в `<script>` — НЕ менять.
  </action>
  <acceptance_criteria>
    - `ui/src/features/reports/ReportTable.svelte` содержит `import Table` И `import TableRow` И `<Table` И `<TableRow`
    - `grep -c 'class="report-separator"' ui/src/features/reports/ReportTable.svelte` &gt;= 1 И `grep -c 'group={true}' ui/src/features/reports/ReportTable.svelte` == 0 (разделитель НЕ через group-режим)
    - Копирайт сохранён: `grep -c "Нет данных за выбранный период\|Измените диапазон дат\|Не удалось загрузить отчёт" ui/src/features/reports/ReportTable.svelte` == 3
    - `git diff --name-only` не содержит `ui/src/lib/components/Table.svelte` или `ui/src/lib/components/TableRow.svelte`
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: таблица каждого из 8 типов отчёта (Акты/Возвраты/В работе/На складе/Расход/Заправки/…) рендерит верные динамические колонки; разделитель месяца/локации выглядит идентично старому `.month-separator`; ошибка/пустое состояние работают
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть каждый тип отчёта (домен Устройства и Картриджи): колонки соответствуют типу, разделитель месяца/локации выглядит как раньше, пустой период и ошибка загрузки показывают прежний текст</human-check>
  </verify>
  <done>ReportTable на Table/TableRow с динамическими колонками, разделитель — голый tr (не group), error-ветка сохранена вне Table, Table/TableRow не модифицированы.</done>
</task>

<task type="auto">
  <name>Task 2: ReportFilters аудит (D-04) + ReportsPage → PageHeader</name>
  <read_first>
    - ui/src/features/reports/ReportFilters.svelte (ПРОЧИТАН ЦЕЛИКОМ; 100 строк — GAP-R4 уже убрал все фильтры-поля, компонент целиком на `Button` строки 71–91 — «Экспорт CSV» и «Печать / Экспорт PDF»; ожидаемый объём правок — 0, только подтвердить отсутствие остаточных bespoke-классов)
    - ui/src/features/reports/ReportsPage.svelte (bespoke `<header class="page-header"><h1 class="page-title">Отчёты</h1></header>` строки 422–424, БЕЗ кнопок в шапке — в отличие от ActsPage; scoped CSS `.page-header`/`.page-title` — найти по grep)
    - ui/src/features/acts/ActsPage.svelte (эталон PageHeader; здесь — БЕЗ actions-snippet, т.к. в шапке Отчётов сейчас нет кнопки)
    - ui/src/lib/components/PageHeader.svelte (контракт: `title: string`, `actions?: Snippet` — опционален, можно не передавать)
  </read_first>
  <action>
    В `ReportFilters.svelte` провести аудит: подтвердить, что компонент целиком на `Button` (GAP-R4
    уже убрал фильтры-поля) и не содержит остаточных bespoke `.filter-*`/raw-input классов —
    ожидаемо 0 изменений кода, кроме подтверждения. Если найден остаточный raw HTML — заменить на
    соответствующий примитив по D-04.
    В `ReportsPage.svelte` заменить bespoke `<header class="page-header"><h1 class="page-title">
    Отчёты</h1></header>` на `<PageHeader title="Отчёты">` БЕЗ `actions`-snippet (в текущей шапке нет
    кнопки — Export/Print живут в `ReportFilters`, не в шапке). Импортировать `PageHeader` из
    `$lib/components/PageHeader.svelte`. Удалить scoped CSS `.page-header`/`.page-title`. Остальная
    структура страницы (ReportSubNav/PeriodSelector/ReportFilters/ReportTable, `onMount`-логика
    загрузки данных) не меняется.
  </action>
  <acceptance_criteria>
    - `ui/src/features/reports/ReportsPage.svelte` содержит `import PageHeader` И `<PageHeader title="Отчёты"` И `grep -v '^\s*//' ui/src/features/reports/ReportsPage.svelte | grep -c 'class="page-header"'` == 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: экспорт CSV и «Печать / Экспорт PDF» работают как раньше; шапка «Отчёты» — PageHeader
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Отчёты: шапка — PageHeader; кнопки экспорта/печати работают</human-check>
  </verify>
  <done>ReportFilters подтверждён без остаточных bespoke-классов; ReportsPage на PageHeader; поведение неизменно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. Отчёты read-only отображают агрегированные данные из существующих
CSV/PDF-экспорт эндпоинтов — риск ограничен потерей столбца/разделителя при переносе на динамический
Table API.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Tampering (regression) | ReportTable динамические колонки/разделитель | mitigate | `formatCellValue`/`grouped`-derived логика не переписывается, разделитель — голый `<tr>` (не group), human-check проходит все 8 типов отчёта |
| T-28-04-02 | Information Disclosure (regression) | ReportTable error/empty состояния (текст пользователю) | mitigate | acceptance criteria проверяет дословное сохранение всех 3 текстов (empty title/body, error) |
| T-28-04-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается |

Угроз уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Human-check подтверждает: каждый тип отчёта рендерит верные колонки, экспорт/печать работают
</verification>

<success_criteria>
Таблица данных и шапка Отчётов — на `Table`/`TableRow`/`PageHeader` без расширения примитивов;
динамические колонки, разделитель-строка, экспорт и error/empty-тексты сохранены дословно (SC #4).
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-04-SUMMARY.md` when done
</output>
