---
phase: 28-support-admin-windows
plan: 10
type: execute
wave: 2
depends_on: [28-01, 28-02, 28-03, 28-04, 28-05, 28-06, 28-07, 28-08, 28-09]
files_modified: []
autonomous: false
requirements: [WIN-06, WIN-07, WIN-08, WIN-09]
must_haves:
  truths:
    - "D-02 (перенос из Фазы 27, Заявки): обязательный визуальный UAT обеих тем подтверждает, что master/detail-панели Заявок читаются раздельно — в тёмной `--tr-surface-raised` светлее `--tr-surface` (всплывают), в светлой разделены рамкой/тенью (урок D-17 Фазы 26)"
    - "SC #1: окно Заявок (список, деталь, селекты категории/принтера) использует новые токены/компоненты"
    - "SC #2: окно Отчётов использует новые токены/компоненты"
    - "SC #3: окно Настроек (организация, шаблоны, бэкапы, вкладка AD) использует новые токены/компоненты; D-08 граница TemplateEditor подтверждена — область редактирования/превью не тронута"
    - "SC #4: окно Пользователей использует новые токены/компоненты; каждое существующее поле/действие/workflow всех четырёх окон на месте и работает — изменение чисто визуальное"
  artifacts: []
  key_links: []
---

<objective>
Финальная сквозная проверка Фазы 28: полная сборка (`pnpm --dir ui build`) + **обязательный
визуальный UAT обеих тем** по четырём окнам (Заявки/Отчёты/Настройки/Пользователи). Это последняя
волна — она закрывает мандат D-02 («обязательный визуальный UAT в обеих темах», перенос из Фазы 27)
и якорь SC #1–#4 (чисто визуально, ничего не сломано, D-08 граница TemplateEditor соблюдена). Фаза не
имеет фронтенд-тестов; визуал проверяется глазами (D-18 Фазы 26).

Purpose: подтвердить SC #1–#4 и D-01…D-08 до закрытия фазы.
Output: пройденный build-гейт + подтверждённый человеком both-theme UAT по всем четырём окнам.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Полная сборка + токен/тип/lint гейты</name>
  <read_first>
    - CLAUDE.md (гоча: перед LAN-браузер-проверкой нужен `pnpm --dir ui build` — серверный режим отдаёт `ui/dist`; `cargo tauri dev` хотрелоадит только desktop-webview; prebuild тянет `cargo test -p trackly-app --test export_bindings` — один `cargo test` за раз, не запускать параллельно)
  </read_first>
  <action>
    Прогнать финальные гейты Фазы 28 из корня репозитория: `node ui/scripts/check-tokens.mjs`,
    `pnpm --dir ui svelte-check`, `pnpm --dir ui lint`, `pnpm --dir ui build` (build тянет prebuild
    `cargo test -p trackly-app --test export_bindings`; один `cargo test` за раз). Собранный
    `ui/dist` нужен для LAN-браузер-проверки в Task 2. Любой ненулевой код — устранить до чекпоинта.
  </action>
  <acceptance_criteria>
    - `node ui/scripts/check-tokens.mjs` код 0
    - `pnpm --dir ui svelte-check` код 0
    - `pnpm --dir ui lint` код 0
    - `pnpm --dir ui build` код 0 (свежий ui/dist собран)
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check && pnpm --dir ui lint && pnpm --dir ui build</automated>
  </verify>
  <done>Все автоматические гейты зелёные; свежий ui/dist собран для LAN-проверки.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Both-theme визуальный UAT по четырём окнам (D-01…D-08, SC #1–#4)</name>
  <action>Провести блокирующую человеческую проверку обеих тем по Заявкам/Отчётам/Настройкам/Пользователям согласно how-to-verify ниже; не авто-одобрять (gate=blocking).</action>
  <what-built>
    Четыре окна поддержки и администрирования (Заявки WIN-06, Отчёты WIN-07, Настройки WIN-08,
    Пользователи WIN-09) переведены на дизайн-систему Фаз 23–25 и оболочку Фазы 26: Заявки —
    механический перенос плейбука Фазы 27 (master-detail `--tr-surface-raised`+`--tr-elev-1` D-02,
    фильтр-табы D-05, список D-03, деталь на DetailPanel с PrinterDetail-заголовком D-01); Отчёты и
    Настройки — суб-навигация на `Tabs` без расширения примитива (D-06), таблица отчётов с
    динамическими колонками на `Table`/`TableRow` (D-07); Пользователи — простейшее окно
    (список+модалка) на `Table`/`Input`/`Select`/`Checkbox`; TemplateEditor — строго визуальная
    правка (D-08), область редактирования/превью не тронута; все четыре page-orchestrator перешли на
    `PageHeader` (устранён компонентный разнобой). Поля/действия/workflow не менялись (SC #1–#4).
  </what-built>
  <how-to-verify>
    Проверить в ОБЕИХ темах (переключатель темы) — desktop (`cargo tauri dev`) и, при желании,
    LAN-браузер (свежий `ui/dist` собран в Task 1).
    1. **D-02 (обе темы, критично, перенос из Фазы 27):** открыть Заявки. В ТЁМНОЙ теме master и
       detail должны «всплывать» над контент-фоном (raised светлее surface); в СВЕТЛОЙ — разделяться
       рамкой и тенью. Нет слияния панелей с фоном.
    2. **Заявки (SC #1):** статус-табы переключаются, «Создать заявку» открывает форму, деталь
       заявки показывает заголовок (2 бейджа + автор/дата), все lifecycle-кнопки (Подтвердить/Принять
       в работу/Выполнить/Установить картридж/Отклонить/Отменить/Удалить) появляются по прежним
       условиям под каждой ролью (admin/specialist/employee), история заявки рендерится, 4
       confirm-модалки работают.
    3. **Отчёты (SC #2):** домен-переключатель (Устройства/Картриджи) и тип-отчёта-табы со
       счётчиками переключаются; режим периода (Месяц/Год/Диапазон) работает, disabled при
       snapshot-отчёте; таблица показывает верные динамические колонки для каждого типа отчёта,
       разделитель месяца/локации выглядит как раньше; экспорт CSV и печать/PDF работают.
    4. **Настройки (SC #3):** 7 разделов переключаются через суб-навигацию; каждая панель
       (Сеть/Организация/Хранилище/Бэкапы/Порог остатка/Шаблоны/AD) сохраняет значения; в AD-вкладке
       переключение между «Автоматически принимать»/«Требовать подтверждения» сохраняет корректный
       режим (не инвертирован); в Шаблонах — **проверить особо строго** — переключение типа шаблона,
       редактирование тела, превью, сохранение и сброс до умолчания работают идентично прежнему
       (D-08 граница).
    5. **Пользователи (SC #4):** список — таблица с 6 колонками, статус-бейдж; создание/редактирование
       пользователя работает (валидация); удаление показывает INLINE-подтверждение «Удалить?»/«Да»/
       «Нет» — НЕ модалку (UI-SPEC §7.4).
    6. **Регресс других окон:** Устройства, Акты, Картриджи, Принтеры, витрина компонентов — визуально
       не затронуты (Tabs/Table/TableRow не модифицировались в этой фазе).
  </how-to-verify>
  <resume-signal>Напишите «approved» либо перечислите визуальные/функциональные проблемы по окнам/темам</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — это проверочная волна. Подтверждает, что визуальная ре-токенизация Фазы
28 не внесла регрессов поведения (SC #1–#4) и что граница D-08 (TemplateEditor) соблюдена буквально.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10-01 | Tampering (regression) | четыре окна целиком | mitigate | полный build-гейт + блокирующий human-verify обеих тем по всем окнам и ключевым действиям |
| T-28-10-02 | Tampering (D-08 boundary violation) | TemplateEditor (DB-backed, seed/upgrade-ловушка) | mitigate | отдельный явный пункт how-to-verify требует строгой проверки save/reset/preview именно на этой панели |
| T-28-10-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов в фазе не устанавливалось |

Угроз уровня `high` фаза не вносит; чекпоинт не авто-одобряется (gate=blocking).
</threat_model>

<verification>
- Все автоматические гейты (check-tokens, svelte-check, lint, build) код 0
- Человек подтвердил both-theme UAT по Заявкам/Отчётам/Настройкам/Пользователям (D-01…D-08) и ключевые действия (SC #1–#4)
</verification>

<success_criteria>
Build зелёный; человек подтвердил, что четыре окна повсеместно на новой дизайн-системе (SC #1–#4),
master/detail Заявок читаются в обеих темах (D-02), граница D-08 соблюдена, и все поля/действия/
workflow сохранены.
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-10-SUMMARY.md` when done
</output>
