---
phase: 28-support-admin-windows
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/reports/ReportSubNav.svelte
  - ui/src/features/reports/PeriodSelector.svelte
autonomous: true
requirements: [WIN-07]
must_haves:
  truths:
    - "D-06 (Отчёты, серая зона A): ReportSubNav — оба уровня (домен-нав `segmented` + тип-отчёта-нав `underline` со счётчиками) переведены на примитив `Tabs`, БЕЗ расширения примитива — встроенный count-слот Tabs уже даёт accent-тон активному счётчику, дословно совпадающий с прежним `<Badge variant={active?'accent':'default'}>`"
    - "D-06 (Отчёты): PeriodSelector — сегментный переключатель режима (Месяц/Год/Диапазон) переведён на `Tabs variant=\"segmented\"` с per-tab `disabled` при `isSnapshot` (уже поддержано пропом `Tab.disabled`); месяц/год `<select>` переведены на примитив `Select`"
    - "Контракт навигации не изменился: переключение домена/типа/режима, значения счётчиков, ARIA (tablist/aria-selected для underline, role=group для segmented) работают как раньше"
  artifacts:
    - path: "ui/src/features/reports/ReportSubNav.svelte"
      provides: "Двухуровневая суб-навигация Отчётов на Tabs"
      contains: "Tabs"
    - path: "ui/src/features/reports/PeriodSelector.svelte"
      provides: "Сегментный переключатель периода на Tabs"
      contains: "Tabs"
  key_links:
    - from: "ui/src/features/reports/ReportSubNav.svelte"
      to: "ui/src/lib/components/Tabs.svelte"
      via: "два экземпляра Tabs (segmented для домена, underline с count для типа)"
      pattern: "import Tabs"
---

<objective>
Перевести суб-навигацию окна **Отчётов (WIN-07)** на дизайн-систему: двухуровневая навигация
(`ReportSubNav` — домен + тип отчёта) и сегментный переключатель периода (`PeriodSelector`) — оба
сейчас самописные `<button class="tab">`. Это единственные два файла первой из двух реальных серых
зон фазы (D-06), уже решённой в 28-UI-SPEC.md §6.1 кодовой проверкой: **примитив `Tabs` уже покрывает
оба кейса без расширения** — не форкать/не расширять.

Purpose: закрыть D-06 (единый компонент навигации вместо трёх разных таб-паттернов) для Отчётов.
Output: 2 файла на примитиве `Tabs` (2 варианта: `segmented`, `underline` + count), без bespoke
`.tab`/`Badge`-счётчиков.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-UI-SPEC.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/features/devices/DeviceFilters.svelte
@ui/src/lib/components/Tabs.svelte
@ui/src/lib/components/Select.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportSubNav → двойной Tabs (домен segmented + тип underline+count) (D-06)</name>
  <read_first>
    - ui/src/features/reports/ReportSubNav.svelte (ПРОЧИТАН ЦЕЛИКОМ; 178 строк — `DOMAINS`/`DEVICE_REPORTS`/`CARTRIDGE_REPORTS` конфиг строки 17–54; самописный `.domain-nav`+`.report-nav` строки 82–115; `<Badge variant={active?'accent':'default'}>{statusCounts?statusCounts[r.key]??0:r.key===activeReport?rowCount:'–'}</Badge>` строка 109–111 — обратить внимание: fallback `'–'` строкой, `Tabs.count` типизирован `number`)
    - ui/src/lib/components/Tabs.svelte (контракт: `Tab{key,label,count?,disabled?}`, `variant: 'underline'|'segmented'`, встроенный `.tab-count` с accent-тоном на active строки 106–124 — уже даёт то, что раньше давал `<Badge variant="accent">`)
    - .planning/phases/28-support-admin-windows/28-UI-SPEC.md §6.1 (подтверждение кодом: не форкать/не расширять Tabs; таблица сопоставления Потребитель→вариант)
  </read_first>
  <action>
    Заменить `ReportSubNav.svelte` на два экземпляра `Tabs` рядом: `<Tabs variant="segmented"
    tabs={DOMAINS.map((d)=>({key:d.key,label:d.label}))} active={activeDomain} ariaLabel="Домен
    отчётов" onchange={(key)=>onDomainChange(key as DomainKey)} />` для домена (Устройства/Картриджи,
    без count) и `<Tabs variant="underline" tabs={activeReports.map((r)=>({key:r.key,label:r.label,
    count: statusCounts?(statusCounts[r.key]??0):(r.key===activeReport?rowCount:0)}))}
    active={activeReport} ariaLabel="Тип отчёта" onchange={onReportChange} />` для типа отчёта.
    **Разрешение fallback-несоответствия (`'–'`-строка vs `number`-тип count):** заменить строковый
    fallback `'–'` на `0` — `Tabs.count` типизирован `number`, строка не проходит тайпчек; на практике
    `statusCounts` почти всегда присутствует (`reports_get_report_counts` уже используется), так что
    это редко достижимая ветка — минорное визуальное отличие, зафиксировать как известное отклонение
    (не расширение примитива, разница в редком edge-case, см. 28-PATTERNS.md «No Analog Found»).
    Удалить импорт `Badge` — count-слот `Tabs` заменяет его целиком. Удалить весь bespoke `.report-sub-
    nav`/`.domain-nav`/`.report-nav`/`.tab` CSS. Обернуть оба `Tabs` в `<div class="report-sub-nav">`
    (сохранить flex-row/wrap-обёртку верхнего уровня, только внутренние табы теперь — Tabs). Props-
    интерфейс компонента (`activeDomain`/`activeReport`/`rowCount`/`statusCounts`/`onDomainChange`/
    `onReportChange`) НЕ меняется — потребитель `ReportsPage.svelte` не требует правок здесь.
  </action>
  <acceptance_criteria>
    - `ui/src/features/reports/ReportSubNav.svelte` содержит `import Tabs` И два вхождения `&lt;Tabs` (`grep -c '&lt;Tabs' ui/src/features/reports/ReportSubNav.svelte` == 2)
    - `grep -c "import Badge" ui/src/features/reports/ReportSubNav.svelte` == 0 (Badge больше не используется)
    - `grep -v '^\s*//' ui/src/features/reports/ReportSubNav.svelte | grep -c 'class="tab"'` == 0
    - Props-контракт сохранён: `grep -c "activeDomain\|activeReport\|onDomainChange\|onReportChange" ui/src/features/reports/ReportSubNav.svelte` &gt;= 4
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: домен-переключатель (segmented pill) и тип-отчёта-табы (underline+счётчик) переключаются, счётчики совпадают с прежними значениями
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Отчёты: переключить домен (Устройства/Картриджи) и тип отчёта — счётчики на активной вкладке совпадают с прежними, ARIA-роли не сломаны (tablist/segmented group)</human-check>
  </verify>
  <done>ReportSubNav на двух Tabs (segmented+underline), Badge не импортируется, props-контракт для ReportsPage не изменён.</done>
</task>

<task type="auto">
  <name>Task 2: PeriodSelector → Tabs segmented (режим) + Select (месяц/год) (D-06)</name>
  <read_first>
    - ui/src/features/reports/PeriodSelector.svelte (ПРОЧИТАН ЦЕЛИКОМ; 281 строка — `MODES` конфиг строки 90–94; самописные `.period-buttons`/`.period-btn` строки 100–113 с `disabled={isSnapshot}`; месяц/год `<select class="period-select">` строки 116–133 внутри `mode==='month'`/`'year'` ветки; `setMode`/`onMonthChange`/`onYearChange`/`$effect` range-валидация строки 46–86 — НЕ трогать, только разметка)
    - ui/src/lib/components/Tabs.svelte (контракт `Tab.disabled?: boolean` уже поддержан — `disabled={isSnapshot}` per-tab тривиален)
    - ui/src/lib/components/Select.svelte (контракт: `value: string`, `onchange?: (v:string)=>void`, `children: Snippet` для `<option>`)
    - .planning/phases/28-support-admin-windows/28-UI-SPEC.md §6.1
  </read_first>
  <action>
    Заменить `.period-buttons` (самописные `<button class="period-btn">`) на `<Tabs variant="segmented"
    tabs={MODES.map((m)=>({key:m.key,label:m.label,disabled:isSnapshot}))} active={mode} ariaLabel="Режим
    периода" onchange={(key)=>setMode(key as PeriodMode)} />`. Удалить bespoke `.period-buttons`/
    `.period-btn` CSS. Заменить месяц/год `<select class="period-select">` (строки 116–133, обе ветки
    `mode==='month'`/`'year'`) на `<Select value={String(selectedMonth)} onchange={(v) =>
    onMonthChangeAdapter(v)}>`/`<Select value={String(selectedYear)} onchange={(v) =>
    onYearChangeAdapter(v)}>` с `<option>` внутри (children snippet). `Select.value` типизирован
    `string`, текущий код держит числовое состояние (`selectedMonth`/`selectedYear`) — обернуть
    существующие `onMonthChange`/`onYearChange` (которые сейчас читают `e.currentTarget.value` из
    нативного `<select>`) тонкими строковыми адаптерами (`const onMonthChangeAdapter = (v: string) =>
    { selectedMonth = Number(v); onPeriodChange({...}); }` и аналогично для года) — сохранить ТУ ЖЕ
    логику пересчёта периода, не переписывать её. `DatePicker` (диапазон-режим, разметка далее в
    файле) уже на примитиве — не трогать (явно исключено UI-SPEC). `isSnapshot`-hint текст «Отчёт
    отражает текущее состояние» (KEEP, UI-SPEC §7.2) и ошибка диапазона «Начало не может быть позже
    окончания» (KEEP) — не менять.
  </action>
  <acceptance_criteria>
    - `ui/src/features/reports/PeriodSelector.svelte` содержит `import Tabs` И `import Select` И `&lt;Tabs variant="segmented"`
    - `grep -v '^\s*//' ui/src/features/reports/PeriodSelector.svelte | grep -c 'class="period-btn"'` == 0
    - `grep -v '^\s*//' ui/src/features/reports/PeriodSelector.svelte | grep -c 'class="period-select"'` == 0
    - Копирайт сохранён: `grep -c "Отчёт отражает текущее состояние\|Начало не может быть позже окончания" ui/src/features/reports/PeriodSelector.svelte` == 2
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: режимы Месяц/Год/Диапазон переключаются, disabled при snapshot-отчёте, месяц/год-селекты работают, ошибка диапазона показывается
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Отчёты: переключить режим периода (Месяц/Год/Диапазон), сменить месяц/год через Select, проверить snapshot-отчёт (кнопки disabled) и ошибку диапазона дат</human-check>
  </verify>
  <done>PeriodSelector на Tabs segmented + Select; логика периода/валидации не изменена; копирайт сохранён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. Отчёты read-only отображают агрегированные данные — риск ограничен
случайным изменением фильтра/периода при переносе разметки на примитивы.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Tampering (regression) | ReportSubNav count-fallback (строка `'–'` → число `0`) | accept | известное минорное визуальное отличие, зафиксировано в action/acceptance_criteria; `statusCounts` почти всегда присутствует на практике |
| T-28-03-02 | Tampering (regression) | PeriodSelector month/year Select string↔number адаптер | mitigate | round-trip через `Number(v)` в onchange, существующие `onMonthChange`/`onYearChange` не переписываются, human-check проверяет переключение |
| T-28-03-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается |

Угроз уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Human-check подтверждает: навигация Отчётов (домен/тип/период) работает идентично прежней
</verification>

<success_criteria>
Суб-навигация Отчётов (домен, тип отчёта, режим периода) целиком на примитиве `Tabs` без расширения
и без bespoke таб-паттернов; месяц/год-выбор на `Select`; поведение и копирайт сохранены.
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-03-SUMMARY.md` when done
</output>
