---
phase: 28-support-admin-windows
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/users/UsersList.svelte
  - ui/src/features/users/UserListRow.svelte
  - ui/src/features/users/UserFormModal.svelte
  - ui/src/features/users/UsersPage.svelte
autonomous: true
requirements: [WIN-09]
must_haves:
  truths:
    - "D-03 (перенос из Фазы 27, Пользователи): UsersList/UserListRow — сырой `<table>` переведён на `Table`/`TableRow`; empty-state использует ТОЛЬКО `emptyTitle` («Пользователи не найдены»), БЕЗ `emptyBody` — второй строки в текущем UsersList нет, добавлять её вне объёма (SC #4)"
    - "D-03: статус-бейдж «Активен»/«Заблокирован» переведён с bespoke `.badge--active`/`.badge--blocked` на примитив `Badge` (variant success/default); inline-подтверждение удаления («Удалить?»/«Да»/«Нет») сохраняется ДОСЛОВНО — НЕ заменяется модалкой (UI-SPEC §7.4 явно запрещает)"
    - "D-04 (везде): UserFormModal — самый явный пример raw HTML внутри Modal-обёртки (6 полей) — переведён на Input/Select/Checkbox; валидация не меняется"
    - "PageHeader adoption (находка пэттерн-мапера, консистентность компонентов SC #1): UsersPage потребляет PageHeader вместо bespoke <header class=\"page-header\">, кнопка «+ Добавить пользователя» переносится в actions-snippet"
  artifacts:
    - path: "ui/src/features/users/UsersList.svelte"
      provides: "Список пользователей на Table/TableRow"
      contains: "Table"
    - path: "ui/src/features/users/UserFormModal.svelte"
      provides: "Форма пользователя на Input/Select/Checkbox"
      contains: "import Input"
  key_links:
    - from: "ui/src/features/users/UsersList.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "импорт и потребление Table + TableRow"
      pattern: "import Table"
    - from: "ui/src/features/users/UsersPage.svelte"
      to: "ui/src/lib/components/PageHeader.svelte"
      via: "импорт PageHeader (title + actions snippet с кнопкой «+ Добавить пользователя»)"
      pattern: "import PageHeader"
---

<objective>
Перевести окно **Пользователей (WIN-09)** на дизайн-систему целиком: список на `Table`/`TableRow`
(D-03, перенос из Фазы 27 — самое простое окно, без master-detail), форма создания/редактирования на
`Input`/`Select`/`Checkbox` (D-04, наибольшая концентрация raw HTML в модалке фазы), шапка на
`PageHeader`. **Ни одно поле, действие (включая inline-подтверждение удаления) не меняется (SC #4)**.

Purpose: закрыть SC #4 (Пользователи повсеместно на новых токенах/компонентах) — последнее окно фазы.
Output: 4 файла на `Table`/`TableRow`/`Badge`/`Input`/`Select`/`Checkbox`/`PageHeader`, без bespoke-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-UI-SPEC.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/features/acts/ActsList.svelte
@ui/src/features/acts/ActListRow.svelte
@ui/src/features/acts/ActsPage.svelte
@ui/src/features/devices/DeviceFormBody.svelte
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
@ui/src/lib/components/Badge.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: UsersList + UserListRow → Table/TableRow + Badge (D-03)</name>
  <read_first>
    - ui/src/features/users/UsersList.svelte (ПРОЧИТАН ЦЕЛИКОМ; 65 строк — сырой `<table class="users-table">` строки 18–35, 6 колонок «Логин/ФИО/Роль/Email/Статус/Действия»; empty-state одна строка «Пользователи не найдены» строка 16 — БЕЗ второй строки; НЕТ loading-состояния вообще, НЕ добавлять его — вне объёма)
    - ui/src/features/users/UserListRow.svelte (ПРОЧИТАН ЦЕЛИКОМ; 129 строк — bespoke `<tr class="user-row">` с 6 `<td>` строки 34–58; статус через bespoke `<span class="badge badge--active/--blocked">` строки 40–44; inline-подтверждение удаления `confirmDelete`-стейт → «Удалить?»/«Да»(destructive)/«Нет» строки 47–56 — established паттерн, НЕ заменять модалкой)
    - ui/src/features/acts/ActsList.svelte (эталон обёртки Table — БЕЗ пагинации/skeleton, т.к. UsersList их не имеет: `<Table columns={6} empty={items.length===0} emptyTitle="Пользователи не найдены" head={tableHead}>`)
    - ui/src/lib/components/Table.svelte (контракт: `loading`/`skeletonRows` опциональны — не задавать `loading`, если состояния нет в исходном коде)
    - ui/src/lib/components/Badge.svelte (variant `success`/`default` — соответствует роли `.badge--active`/`.badge--blocked`)
  </read_first>
  <action>
    `UsersList.svelte`: обернуть в `<Table columns={6} empty={items.length===0}
    emptyTitle="Пользователи не найдены" head={tableHead}>` (БЕЗ `emptyBody` — UI-SPEC §6.4
    обязательное сужение, второй строки нет в текущем коде, не изобретать). `tableHead`-snippet:
    `<th>Логин</th><th>ФИО</th><th>Роль</th><th>Email</th><th>Статус</th><th class="th-actions">
    Действия</th>`. НЕ задавать `loading`-проп (компонент сейчас не имеет loading-состояния —
    добавлять skeleton вне объёма SC #4). Удалить bespoke `.users-table`/`.th`/`.empty-state` CSS.
    `UserListRow.svelte`: `<TableRow class="user-row">` с 6 `<td class="cell">` — Логин/ФИО/Роль/
    Email как есть, Статус через `{#if user.is_active}<Badge variant="success">Активен</Badge>
    {:else}<Badge variant="default">Заблокирован</Badge>{/if}` (замена bespoke `.badge--active`/
    `.badge--blocked`), Действия — inline-подтверждение (`confirmDelete`/«Удалить?»/«Да»/«Нет»)
    ПЕРЕНОСИТСЯ ДОСЛОВНО (строки 47–56), НЕ заменяется модалкой (UI-SPEC §7.4). Удалить bespoke
    `.badge`/`.cell`/`.btn-action` CSS, заменённые примитивами (кнопки «Изменить»/«Удалить»/«Да»/
    «Нет» оставить как есть — маленькие текстовые кнопки без явного примитива-эталона в этом файле,
    Claude's Discretion: можно оставить bespoke `.btn-action`-класс для этих 4 мелких inline-кнопок,
    т.к. `Button`-примитив рассчитан на более крупные CTA — сверить визуально, не критично для SC #4).
    **Ловушка `:global()`:** `.user-row :global(> td)`, НЕ `:global(.user-row > td)`.
  </action>
  <acceptance_criteria>
    - `ui/src/features/users/UsersList.svelte` содержит `import Table` И `&lt;Table` И `grep -c "emptyBody" ui/src/features/users/UsersList.svelte` == 0
    - `ui/src/features/users/UserListRow.svelte` содержит `import TableRow` И `import Badge` И `&lt;TableRow`
    - `grep -v '^\s*//' ui/src/features/users/UserListRow.svelte | grep -c 'badge--active\|badge--blocked'` == 0
    - Inline-подтверждение сохранено: `grep -c "Удалить?\|confirmDelete" ui/src/features/users/UserListRow.svelte` &gt;= 2
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: список пользователей — таблица с 6 колонками; статус — Badge; клик «Удалить» показывает inline «Удалить?»/«Да»/«Нет» (НЕ модалку); пустое состояние — одна строка без подзаголовка
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Пользователи: таблица с 6 колонками, статус-бейдж, «Удалить» показывает inline-подтверждение (не модалку), пустой список показывает только «Пользователи не найдены»</human-check>
  </verify>
  <done>UsersList/UserListRow на Table/TableRow/Badge; inline-подтверждение удаления сохранено дословно; empty-state без emptyBody.</done>
</task>

<task type="auto">
  <name>Task 2: UserFormModal → Input/Select/Checkbox (D-04) + UsersPage → PageHeader</name>
  <read_first>
    - ui/src/features/users/UserFormModal.svelte (ПРОЧИТАН ЦЕЛИКОМ; 303 строки — самый явный пример raw HTML в Modal фазы: 6 полей — Логин `<input type="text">` строки 129–138 (disabled+readonly при edit-режиме), ФИО `<input type="text">` строки 146–153, Пароль `<input type="password">` строки 160–169 (label меняется по mode), Роль `<select class="form-select">` строки 177–187 (3 option), Email `<input type="email">` строки 195–202, Активен `<input type="checkbox">` строки 205–210; error-текст `<span class="field-error">` под login/password; `<div class="server-error">` под общей ошибкой строка 212–214; `validate()`/`handleSave()` — НЕ менять)
    - ui/src/features/devices/DeviceFormBody.svelte (эталон field+error: `<div class="field" class:has-error>` + `<label class="label">` + примитив + `{#if fieldErrors[...]}<p class="field-error">` строки 196–260)
    - ui/src/features/users/UsersPage.svelte (ПРОЧИТАН ЦЕЛИКОМ; 173 строки — bespoke `<header class="page-header"><h1 class="page-title">Пользователи</h1><div class="header-actions"><Button variant="primary" onclick={openCreate}>+ Добавить пользователя</Button></div></header>` строки 108–113)
    - ui/src/features/acts/ActsPage.svelte (эталон PageHeader с actions-snippet, содержащим Button)
    - ui/src/lib/components/Input.svelte (ПОДТВЕРЖДЕНО чтением при ревизии плана: контракт
      `type?: 'text'|'number'|'search'` — НЕТ `'password'`, НЕТ `'email'`; компонент не форвардит
      произвольные DOM-события, только `oninput`. Пароль ОБЯЗАН остаться raw `<input
      type="password">` — рендер через `Input type="text"` ЗАПРЕЩЁН, это сняло бы маскировку и
      показало бы вводимые символы пароля открытым текстом), Select.svelte, Checkbox.svelte,
      PageHeader.svelte (контракты)
  </read_first>
  <action>
    `UserFormModal.svelte`: заменить 5 из 6 raw-полей на примитивы. Логин → `<Input id="uf-login"
    value={form.login} invalid={loginErr!==null} disabled={saving||mode==='edit'}
    placeholder="Логин пользователя" oninput={(v) => (form.login = v)} />` (readonly-семантику
    воспроизвести через `disabled` — Input не имеет отдельного `readonly`-пропа, `disabled` в
    edit-режиме уже используется рядом, сохранить); ФИО → `<Input id="uf-fullname"
    value={form.full_name} disabled={saving} placeholder="Полное имя"
    oninput={(v) => (form.full_name = v)} />`; Роль → `<Select id="uf-role" value={form.role}
    invalid={roleErr!==null} disabled={saving} onchange={(v) => (form.role = v)}>` с теми же 3
    `<option>`; Email → `<Input id="uf-email" type="text" value={form.email} disabled={saving}
    placeholder="user@example.com" oninput={(v) => (form.email = v)} />` (Input не поддерживает
    `type="email"`, `type="text"` достаточен, серверная валидация остаётся авторитетной — этот
    отказ от нативной HTML5 email-валидации задокументировать в SUMMARY для traceability); Активен →
    `<Checkbox id="uf-active" checked={form.is_active} disabled={saving}
    onchange={(checked) => (form.is_active = checked)}>Активен</Checkbox>`.

    **Пароль — ОБЯЗАТЕЛЬНОЕ raw-исключение из D-04 (НЕ discretion, НЕ вариант на выбор):** оставить
    существующий `<input id="uf-password" type="password" class="input"
    class:invalid={passwordErr!==null} disabled={saving} placeholder={mode==='create'?'Не менее 8
    символов':'Оставьте пустым'} bind:value={form.password}>` БЕЗ перевода на примитив `Input` —
    `Input.svelte` не поддерживает `type="password"` (типы ограничены `'text'|'number'|'search'`), а
    рендер через `Input type="text"` КАТЕГОРИЧЕСКИ ЗАПРЕЩЁН: это сняло бы маскировку и показывало бы
    вводимые символы пароля открытым текстом — реальная security/UX-регрессия, а не безобидное
    визуальное отличие. Обернуть raw `<input>` в тот же `.field`+`.label`-паттерн, что и остальные
    поля (визуальная консистентность обёртки сохраняется), переиспользовать существующий класс
    `.input`/стили токенов `Input`-примитива для самого `<input>` там, где это не требует новых
    CSS-правил — но HTML-тег и `type="password"` остаются raw. Это единственное осознанное
    raw-исключение из D-04 в фазе — зафиксировать в SUMMARY (см. T-28-09-01).

    Field-error разметку (`{#if loginErr}<span class="field-error">`, аналогично для `passwordErr`)
    сохранить рядом с каждым полем. Удалить bespoke `.form-input`/`.form-select`/`.checkbox-label`
    CSS (оставить минимально необходимый CSS для raw `input[type="password"]`, если он использует
    свой собственный класс, не создавать новый параллельный стиль). `validate()`/`handleSave()`-
    логика — НЕ менять.

    В `UsersPage.svelte` заменить bespoke `<header class="page-header">` на `<PageHeader
    title="Пользователи">{#snippet actions()}<Button variant="primary" onclick={openCreate}>+
    Добавить пользователя</Button>{/snippet}</PageHeader>` по эталону ActsPage. Импортировать
    `PageHeader`. Удалить scoped CSS `.page-header`/`.page-title`/`.header-actions`.
  </action>
  <acceptance_criteria>
    - `ui/src/features/users/UserFormModal.svelte` содержит `import Input` И `import Select` И `import Checkbox`
    - `grep -v '^\s*//' ui/src/features/users/UserFormModal.svelte | grep -c 'class="form-input"\|class="form-select"\|class="checkbox-label"'` == 0
    - Пароль остаётся замаскирован (обязательный гейт): `grep -c 'type="password"' ui/src/features/users/UserFormModal.svelte` == 1
    - Поле пароля не мигрировало на текстовый Input: `grep -c 'id="uf-password"' ui/src/features/users/UserFormModal.svelte` == 1 (единственное вхождение — raw `<input type="password">`)
    - `ui/src/features/users/UsersPage.svelte` содержит `import PageHeader` И `&lt;PageHeader title="Пользователи"` И `grep -v '^\s*//' ui/src/features/users/UsersPage.svelte | grep -c 'class="page-header"'` == 0
    - Кнопка «+ Добавить пользователя» сохранена: `grep -c "Добавить пользователя" ui/src/features/users/UsersPage.svelte` &gt;= 1
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: создание/редактирование пользователя работает (валидация логина/пароля/роли), шапка — PageHeader, новое поле пароля остаётся замаскировано (точки/звёздочки) при вводе, не отображает символы открытым текстом
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Создать нового пользователя и отредактировать существующего: все поля работают, валидация срабатывает, шапка — PageHeader; поле пароля остаётся замаскировано (точки/звёздочки) — символы пароля не отображаются открытым текстом</human-check>
  </verify>
  <done>UserFormModal на Input/Select/Checkbox для 5 из 6 полей; пароль — обязательное raw `type="password"`-исключение из D-04 (замаскирован, не Input type="text"); email — задокументированное `type="text"`-отклонение от нативной HTML5-валидации; UsersPage на PageHeader с сохранённой кнопкой.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. `UserFormModal` создаёт/редактирует учётные записи (роль/пароль) —
чисто визуальная правка, `validate()`/`handleSave()`/бэкенд-вызовы (`users_create`/`users_update`)
не меняются. Потеря нативной маскировки пароля была бы реальным риском только при рендере через
`Input type="text"`, что теперь явно запрещено планом: пароль остаётся raw `<input
type="password">` как единственное осознанное исключение из D-04 (Input.svelte не поддерживает
`type="password"`).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09-01 | Information Disclosure | UserFormModal поле пароля — Input не поддерживает `type="password"` | mitigate | ОБЯЗАТЕЛЬНО сохранить raw `<input type="password">` как единственное исключение из D-04 (рендер через `type="text"` ЗАПРЕЩЁН — снял бы маскировку пароля); grep-гейт `type="password"` count==1 в acceptance_criteria; human-check подтверждает визуальную маскировку; валидация длины пароля (`passwordErr`) не меняется |
| T-28-09-02 | Tampering (regression) | UsersList/UserListRow inline-удаление (единственный не-модальный destructive-паттерн фазы) | mitigate | UI-SPEC §7.4 явно запрещает замену модалкой; acceptance criteria grep-гейт на `confirmDelete`/«Удалить?» |
| T-28-09-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается |

Угроз уровня `high` фаза не вносит (пароль на экране создания видим только вводящему пользователю,
не логируется и не персистится на клиенте дольше сессии формы).
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Human-check подтверждает: список/форма пользователей работают идентично прежним, inline-удаление не заменено модалкой, поле пароля остаётся замаскировано
</verification>

<success_criteria>
Окно Пользователей — на `Table`/`TableRow`/`Badge`/`Input`/`Select`/`Checkbox`/`PageHeader` без
bespoke-классов (кроме обязательного raw-исключения для поля пароля); inline-подтверждение удаления
и решение по полю пароля явно задокументированы; поведение сохранено (SC #4).
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-09-SUMMARY.md` when done — include a one-line
note documenting (1) поле пароля осталось raw `<input type="password">` как обязательное исключение
из D-04 (Input.svelte не поддерживает `type="password"`), и (2) поле email потеряло нативную
HTML5 `type="email"`-валидацию (Input.svelte не поддерживает `type="email"`; серверная валидация
авторитетна).
</output>
</output>
