---
phase: 28-support-admin-windows
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/users/UserFormModal.svelte
  - ui/src/features/reports/PeriodSelector.svelte
autonomous: true
gap_closure: true
requirements: [WIN-09, WIN-07]
must_haves:
  truths:
    - "GAP-1 (UserFormModal.svelte:192, Роль): the role picker uses custom Dropdown (flat + variant=\"select\"), same 3 options (Администратор/Специалист/Сотрудник), same form.role write, same disabled/invalid handling"
    - "GAP-1 (PeriodSelector.svelte:114,119,127, Месяц/Год): both month and year pickers use custom Dropdown; this also fixes the reported regression where the selected value was not displayed (native Select's bindable-without-bind: value sync bug) — Dropdown's one-way controlled-value convention displays the current value explicitly"
    - "GAP-3 partial (С/По range inputs): the range-mode date pickers have adequate spacing/alignment between the two label+DatePicker groups, matching the other filter-row controls' visual rhythm"
  artifacts:
    - path: "ui/src/features/users/UserFormModal.svelte"
      provides: "Роль picker on custom Dropdown"
      contains: "Dropdown"
    - path: "ui/src/features/reports/PeriodSelector.svelte"
      provides: "Месяц/Год pickers on custom Dropdown, value always displayed"
      contains: "Dropdown"
  key_links:
    - from: "ui/src/features/users/UserFormModal.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "import Dropdown, variant=\"select\" flat={true}"
      pattern: "import Dropdown"
    - from: "ui/src/features/reports/PeriodSelector.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "import Dropdown, variant=\"select\" flat={true}"
      pattern: "import Dropdown"
---

<objective>
Close GAP-1 (28-VERIFICATION.md) for the remaining native-`Select` sites in **Пользователи**
(`UserFormModal.svelte:192`, Роль) and **Отчёты** (`PeriodSelector.svelte:114,119,127`,
Месяц/Год) — replace with the custom `Dropdown.svelte` primitive (flat + `variant="select"`).
The PeriodSelector swap also resolves the reported "selected value not displayed" regression:
`Select.svelte`'s internal `bind:value` on a `$bindable` prop that the parent never binds two-way
desyncs from parent-driven reactive updates; `Dropdown`'s explicit one-way controlled-value
convention (documented in its own source) does not have this failure mode. While in this file,
also close the GAP-3 partial item on the С/По range-mode date inputs (spacing/alignment) since
that markup lives in this exact component.

Purpose: recurring correction (native combobox -> custom Dropdown) + fix a real display bug + tidy
the adjacent range-input spacing gap noted in the same live UAT pass.
Output: 2 files on `Dropdown` instead of `Select`; PeriodSelector's month/year value is always
visible; С/По inputs read with clear visual separation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-VERIFICATION.md
@ui/src/lib/components/Dropdown.svelte
@ui/src/features/cartridges/CartridgeFormBody.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: UserFormModal.svelte — Роль native Select -&gt; Dropdown</name>
  <files>ui/src/features/users/UserFormModal.svelte</files>
  <read_first>
    - ui/src/features/users/UserFormModal.svelte lines 14 (`import Select from
      '$lib/components/Select.svelte'`), 37-41 (`roleOptions` — already an `{ value, label }[]`
      array: admin/Администратор, manager/Специалист, employee/Сотрудник), 190-206 (`<div
      class="form-field" class:has-error={roleErr !== null}><label class="form-label"
      for="uf-role">Роль</label><Select id="uf-role" value={form.role} invalid={roleErr !== null}
      disabled={saving} onchange={(v) => (form.role = v)}>{#each roleOptions as opt}<option
      value={opt.value}>{opt.label}</option>{/each}</Select>{#if roleErr}<span
      class="field-error">{roleErr}</span>{/if}</div>`)
    - ui/src/lib/components/Dropdown.svelte (prop contract, `invalid`/`disabled` props exist —
      map directly; no `id`/`for`, implicit wrapping label needed)
    - ui/src/features/cartridges/CartridgeFormBody.svelte lines 249-281 («Модель» field — closest
      analog: existing `{value,label}`-shaped options array reused as Dropdown groups, plus a
      field-error span sibling)
  </read_first>
  <action>
    Replace `import Select from '$lib/components/Select.svelte'` with `import Dropdown from
    '$lib/components/Dropdown.svelte'`. `roleOptions` already has the right shape (`{ value,
    label }`) — reuse it directly as Dropdown's `groups`, with `getGroupId={(o) => o.value}`. Add
    a derived `selectedRoleLabel` (find in `roleOptions` by `value === form.role`) and a typed
    `noExpandRole(): { value: string; label: string }[]` returning `[]`. Replace the `<label
    class="form-label" for="uf-role">Роль</label><Select ...>...</Select>` block with the
    implicit-label wrapping pattern: `<label class="form-label dropdown-label"><span>Роль</span>
    <Dropdown variant="select" flat={true} value={selectedRoleLabel} placeholder="Выберите роль"
    searchPlaceholder="Поиск" invalid={roleErr !== null} disabled={saving} loading={false}
    groups={roleOptions} getGroupId={(o) => o.value} getGroupName={(o) => o.label}
    getGroupCount={() => 0} isGroupExpandable={() => false} isGroupSelected={(o) => o.value ===
    form.role} onExpandGroup={noExpandRole} getMemberId={(o) => o.value} getMemberName={(o) =>
    o.label} onSearch={() => {}} onPickGroup={(o) => (form.role = o.value)} onPickMember={() =>
    {}} /></label>`. Keep the `{#if roleErr}<span class="field-error">{roleErr}</span>{/if}`
    sibling and the outer `class:has-error` wrapper unchanged. Add a `.dropdown-label` CSS rule if
    not already present (this file already has `Input`/`Select`/`Checkbox` imports per 28-09's
    D-04 migration — match its existing `.form-field`/`.form-label` naming conventions).
  </action>
  <acceptance_criteria>
    - `grep -c "import Select from" ui/src/features/users/UserFormModal.svelte` == 0
    - `grep -c "import Dropdown from" ui/src/features/users/UserFormModal.svelte` == 1
    - `grep -c "onPickGroup={(o) => (form.role" ui/src/features/users/UserFormModal.svelte` == 1
    - `grep -c "invalid={roleErr !== null}" ui/src/features/users/UserFormModal.svelte` &gt;= 1 (invalid state preserved)
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <done>UserFormModal's Роль picker renders via custom Dropdown; same 3 options, same form.role write, invalid/disabled states preserved.</done>
</task>

<task type="auto">
  <name>Task 2: PeriodSelector.svelte — Месяц/Год native Select -&gt; Dropdown + С/По range spacing (GAP-1 + GAP-3 partial)</name>
  <files>ui/src/features/reports/PeriodSelector.svelte</files>
  <read_first>
    - ui/src/features/reports/PeriodSelector.svelte full file (220 lines): line 7 (`import Select
      from '$lib/components/Select.svelte'`), 28-29 (`selectedMonth`/`selectedYear` `$state<number>`),
      34-50 (`MONTHS` array), 49-50 (`years` array), 68-80 (`onMonthChange`/`onYearChange` — take a
      string, `Number(v)`, unchanged), 112-132 (month-mode: two `<Select>` for month+year;
      year-mode: one `<Select>` for year — 3 `<Select>` elements total, all reusing the same
      `MONTHS`/`years` source arrays), 167-185 (`.period-controls` — currently overrides Select's
      internals via `:global(.select-wrapper) { width: auto; min-width: 110px; }` and
      `:global(.select) { height: 28px; font-size: ... }` to compact the field into the
      28px-tall filter row), 187-205 (`.period-range`/`.range-label` — С/По range mode; `gap:
      var(--tr-space-2xs)` inherited from `.period-controls` is the likely cause of the reported
      "misaligned" look between the two С/По groups)
    - ui/src/lib/components/Dropdown.svelte (prop contract; note internal class names differ from
      Select's — root `.tr-dropdown`, select-variant trigger `.tr-dropdown-field-button` — any
      `:global()` override targeting Select's old `.select-wrapper`/`.select` classes must be
      re-targeted at Dropdown's classes or it silently does nothing)
    - ui/src/features/cartridges/CartridgeFormBody.svelte lines 288-310 («Состояние» field — same
      shape: numeric `id`/string `label` options array, `isGroupSelected` by numeric equality)
  </read_first>
  <action>
    Replace `import Select from '$lib/components/Select.svelte'` with `import Dropdown from
    '$lib/components/Dropdown.svelte'`. Build `monthOptions = MONTHS.map((name, i) => ({ id: i + 1,
    label: name }))` and `yearOptions = years.map((y) => ({ id: y, label: String(y) }))` (module or
    component scope, next to `MONTHS`/`years`). Add derived `selectedMonthLabel` (find in
    `monthOptions` by `id === selectedMonth`) and `selectedYearLabel` (find in `yearOptions` by `id
    === selectedYear`). Add typed `noExpandMonth(): { id: number; label: string }[]` and
    `noExpandYear(): { id: number; label: string }[]`, both returning `[]`. Replace all THREE
    `<Select value={...} onchange={...}>{#each ...}<option>...</option>{/each}</Select>` instances
    (month-mode's month select at line 114, month-mode's year select at line 119, year-mode's year
    select at line 127) with `<Dropdown variant="select" flat={true} .../>` wired to the
    corresponding options array, `getGroupId={(o) => o.id}`, `getGroupName={(o) => o.label}`,
    `getGroupCount={() => 0}`, `isGroupExpandable={() => false}`, `isGroupSelected` by numeric
    `id` equality, `onExpandGroup` to the matching typed no-op, `onSearch={() => {}}`,
    `onPickGroup={(o) => onMonthChange(String(o.id))}` (or `onYearChange` for the year instances),
    `onPickMember={() => {}}` — no `id`/`for` attribute needed since these fields have no
    associated `<label>` in the current markup (visually bare pickers in the controls row, keep
    it that way). Update the CSS: replace `:global(.select-wrapper) { width: auto; min-width:
    110px; }` and `:global(.select) { height: 28px; font-size: var(--tr-font-size-label); }`
    inside `.period-controls` (lines ~176-184) with Dropdown-targeted equivalents — `:global(.tr-
    dropdown) { width: auto; min-width: 110px; }` and `:global(.tr-dropdown-field-button) { height:
    28px; font-size: var(--tr-font-size-label); }` — so the compact 28px filter-row height is
    preserved with the new primitive (Dropdown's select-variant trigger is a `<button
    class="tr-dropdown-field-button">`, not an `<input>`). Additionally (GAP-3 partial, same file):
    in the `.period-range` rule (~line 188), add `gap: var(--tr-space-md);` to open up breathing
    room between the two С/По `<label class="range-label">` groups (currently inherits the tight
    `var(--tr-space-2xs)` from `.period-controls`, which reads as misaligned/cramped) — leave
    `align-items: center` and `flex-wrap: wrap` as-is.
  </action>
  <acceptance_criteria>
    - `grep -c "import Select from" ui/src/features/reports/PeriodSelector.svelte` == 0
    - `grep -c "import Dropdown from" ui/src/features/reports/PeriodSelector.svelte` == 1
    - `grep -c "<Dropdown" ui/src/features/reports/PeriodSelector.svelte` == 3 (month-mode month + month-mode year + year-mode year)
    - `grep -c ":global(.tr-dropdown)" ui/src/features/reports/PeriodSelector.svelte` &gt;= 1 (re-targeted size override, not the stale `.select-wrapper` selector)
    - `grep -c ":global(.select-wrapper)\|:global(.select)" ui/src/features/reports/PeriodSelector.svelte` == 0 (no dead selectors left behind)
    - `grep -A2 "\.period-range {" ui/src/features/reports/PeriodSelector.svelte | grep -c "gap: var(--tr-space-md)"` == 1
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <done>PeriodSelector's Месяц/Год pickers render via custom Dropdown with the current value always visible (fixes the display regression); compact 28px filter-row sizing preserved via re-targeted CSS; С/По range groups have clear spacing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundary — component swap inside authenticated Users-admin and Reports screens.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-13-01 | Tampering (regression) | UserFormModal role picker | mitigate | acceptance criteria assert identical write target + preserved invalid state |
| T-28-13-02 | Tampering (regression) | PeriodSelector month/year pickers + period recalculation | mitigate | reuses existing `onMonthChange`/`onYearChange` functions unchanged — only the field markup changes, not the period-recalculation logic |
| T-28-13-03 | Information Disclosure (dead CSS) | PeriodSelector `:global()` overrides | mitigate | explicit grep gate proving no stale `.select-wrapper`/`.select` selectors remain (would silently do nothing and regress the compact filter-row look) |
| T-28-13-SC | Tampering (supply-chain) | npm installs | accept | no new packages |

No `high`-severity threats introduced.
</threat_model>

<verification>
- `pnpm --dir ui svelte-check` exits 0
- `pnpm --dir ui lint` exits 0 (includes `check-tokens.mjs`)
- `pnpm --dir ui build` exits 0
- No `import Select from '$lib/components/Select.svelte'` remains in either file
- No stale `:global(.select-wrapper)`/`:global(.select)` selectors remain in PeriodSelector.svelte
</verification>

<success_criteria>
GAP-1 closed for UserFormModal.svelte (Роль) and PeriodSelector.svelte (Месяц/Год); the
value-not-displayed regression is fixed; GAP-3's С/По range-spacing item is closed in the same
file where the markup lives.
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-13-SUMMARY.md` when done
</output>
