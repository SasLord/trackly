---
phase: 28-support-admin-windows
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/requests/RequestDetail.svelte
  - ui/src/features/requests/RequestFormModal.svelte
autonomous: true
gap_closure: true
requirements: [WIN-06]
must_haves:
  truths:
    - "GAP-1 (RequestDetail.svelte:673, Роль): the confirm-registration role picker uses the custom Dropdown primitive (flat + variant=\"select\"), not native Select — same 3 options (Сотрудник/Специалист/Администратор), same selected value, same onchange side-effect on approveRole"
    - "GAP-1 (RequestFormModal.svelte:238, Категория): the free-form category picker uses the custom Dropdown primitive (flat + variant=\"select\"), not native Select — same options (Без категории + server-provided categories), same selected categoryId, same onchange side-effect"
    - "SC #1: no field, action, or workflow changed in Заявки — this is a component swap only (Select -> Dropdown), per GAP-1 scope"
  artifacts:
    - path: "ui/src/features/requests/RequestDetail.svelte"
      provides: "Роль confirm picker on custom Dropdown"
      contains: "Dropdown"
    - path: "ui/src/features/requests/RequestFormModal.svelte"
      provides: "Категория picker on custom Dropdown"
      contains: "Dropdown"
  key_links:
    - from: "ui/src/features/requests/RequestDetail.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "import Dropdown, variant=\"select\" flat={true}"
      pattern: "import Dropdown"
    - from: "ui/src/features/requests/RequestFormModal.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "import Dropdown, variant=\"select\" flat={true}"
      pattern: "import Dropdown"
---

<objective>
Close GAP-1 (28-VERIFICATION.md) for the two remaining native-`Select` sites in the **Заявки**
window: `RequestDetail.svelte:673` (Роль, confirm-registration modal) and
`RequestFormModal.svelte:238` (Категория, free-form request). Replace both with the custom
`Dropdown.svelte` primitive (flat + `variant="select"`) — the same primitive already used by
`RequestFormModal`'s own printer picker's sibling window (Phase 27 Cartridges, commit `80d0b41`) —
so the opened options menu is our own styled UI, not the browser-native OS combobox. Component
swap only: same options, same selected id/value, same onchange side-effects (SC #1 — no
workflow change).

Purpose: recurring correction (Phase 27 + Phase 28 UAT) — native `<select>`/`Select.svelte` must
never remain once the custom `Dropdown` primitive exists for this exact need.
Output: 2 files on `Dropdown` instead of `Select`, zero remaining `Select` imports in these files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-VERIFICATION.md
@ui/src/lib/components/Dropdown.svelte
@ui/src/features/cartridges/CartridgeFormBody.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: RequestDetail.svelte — Роль (подтверждение) native Select -&gt; Dropdown</name>
  <files>ui/src/features/requests/RequestDetail.svelte</files>
  <read_first>
    - ui/src/features/requests/RequestDetail.svelte lines 21 (`import Select from
      '$lib/components/Select.svelte'`), 55 (`let approveRole = $state('employee')`), 668-680
      (the confirm-registration Modal: `<label class="label" for="approve-role">Роль</label>` +
      `<Select value={approveRole} id="approve-role" onchange={(v) => (approveRole = v)}>` with 3
      hardcoded `<option>`: employee/Сотрудник, manager/Специалист, admin/Администратор)
    - ui/src/lib/components/Dropdown.svelte (full prop contract — `variant`, `flat`, `value`
      one-way controlled, `groups`/`getGroupId`/`getGroupName`/`getGroupCount`/
      `isGroupExpandable`/`isGroupSelected`/`onExpandGroup`/`getMemberId`/`getMemberName`/
      `onSearch`/`onPickGroup`/`onPickMember`). Dropdown has NO `id`/`for` prop — the caller must
      switch from `<label for="...">` + `id="..."` association to an implicit wrapping label.
    - ui/src/features/cartridges/CartridgeFormBody.svelte lines 75-97, 196-224 (the canonical
      flat + variant="select" wiring pattern from Phase 27-G1 — `KIND_OPTIONS` array +
      `kindLabel` derived + `noExpandKind()` typed no-op + `.dropdown-label` wrapping-label CSS
      pattern lines 348-361 — copy this pattern verbatim, do not re-derive from scratch)
  </read_first>
  <action>
    Replace the `import Select from '$lib/components/Select.svelte'` (line 21) with `import
    Dropdown from '$lib/components/Dropdown.svelte'`. Define a `ROLE_OPTIONS` const (module-level
    or component-level, next to `approveRole`'s declaration) with the 3 existing options as
    `{ id: string, label: string }`: `employee`/«Сотрудник», `manager`/«Специалист»,
    `admin`/«Администратор» — identical labels/values to the current hardcoded `<option>` list, no
    reordering. Add a derived `approveRoleLabel` (find by `id === approveRole`) for the Dropdown's
    `value` prop, and a typed `noExpandRole(): { id: string; label: string }[]` returning `[]`
    (mirrors `noExpandKind` — flat mode never actually calls `onExpandGroup`, but Dropdown's
    generic signature needs a typed function so `TMember` doesn't infer `never[]`). Replace the
    `<label class="label" for="approve-role">Роль</label><Select ...>...</Select>` block with the
    CartridgeFormBody `.dropdown-label` wrapping pattern: a `<label class="label dropdown-label">`
    that wraps both the `<span class="label-text">Роль</span>` and the `<Dropdown variant="select"
    flat={true} value={approveRoleLabel} placeholder="Выберите роль" searchPlaceholder="Поиск"
    loading={false} groups={ROLE_OPTIONS} getGroupId={(o) => o.id} getGroupName={(o) => o.label}
    getGroupCount={() => 0} isGroupExpandable={() => false} isGroupSelected={(o) => o.id ===
    approveRole} onExpandGroup={noExpandRole} getMemberId={(o) => o.id} getMemberName={(o) =>
    o.label} onSearch={() => {}} onPickGroup={(o) => (approveRole = o.id)} onPickMember={() =>
    {}} />`. Copy the `.dropdown-label { display: flex; flex-direction: column; gap:
    var(--tr-space-2xs); }` CSS rule from CartridgeFormBody into this component's `<style>` block
    if not already present (check first — RequestDetail may already define a `.label` class this
    can extend). Everything else in the confirm-registration modal (title, body text, footer
    buttons, `handleApproveConfirm`) stays untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "import Select from" ui/src/features/requests/RequestDetail.svelte` == 0
    - `grep -c "import Dropdown from" ui/src/features/requests/RequestDetail.svelte` == 1
    - `grep -c "employee.*Сотрудник\|Сотрудник" ui/src/features/requests/RequestDetail.svelte` &gt;= 1 (option labels preserved verbatim)
    - `grep -c "onPickGroup={(o) => (approveRole" ui/src/features/requests/RequestDetail.svelte` == 1 (same onchange side-effect: writes approveRole)
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <done>RequestDetail's Роль picker in the confirm-registration modal renders via custom Dropdown; same 3 options, same approveRole write, no native Select remains.</done>
</task>

<task type="auto">
  <name>Task 2: RequestFormModal.svelte — Категория native Select -&gt; Dropdown</name>
  <files>ui/src/features/requests/RequestFormModal.svelte</files>
  <read_first>
    - ui/src/features/requests/RequestFormModal.svelte lines 6-13 (imports — note `Select` AND
      `GroupedPrinterSelect` both imported; `GroupedPrinterSelect` is used for the Принтер field
      and is OUT OF SCOPE for this task/GAP-1's 7-site list — do not touch it, it still wraps a
      native `&lt;select&gt;` internally but is not one of the 7 listed sites), 29 (`let categoryId
      = $state&lt;number | null&gt;(null)`), 46 (`let categories = $state&lt;RequestCategoryDto[]&gt;([])`),
      234-253 (the free-form branch: `<label for="req-category">Категория</label><Select
      value={categoryId !== null ? String(categoryId) : ''} id="req-category" onchange={(v) => {
      categoryId = v ? parseInt(v, 10) : null; }}>` with a `<option value="">Без категории</option>`
      + `{#each categories as cat}` options)
    - ui/src/lib/components/Dropdown.svelte (same contract as Task 1 — note `getGroupId` returns
      `string | number`, so a `null` sentinel for "Без категории" must be encoded as a string,
      e.g. `'none'`, and decoded back to `null` in the pick handler)
    - ui/src/features/cartridges/CartridgeFormBody.svelte lines 249-281 (the «Модель» field —
      closest analog: options built from a dynamic server-provided array + a derived selected
      label, exactly like `categories`)
  </read_first>
  <action>
    Replace `import Select from '$lib/components/Select.svelte'` with `import Dropdown from
    '$lib/components/Dropdown.svelte'` (keep the `GroupedPrinterSelect` import — unrelated, out of
    scope). Define a `NONE_CATEGORY_ID = 'none'` string sentinel. Build a derived
    `categoryOptions` array: `[{ id: NONE_CATEGORY_ID, label: 'Без категории' },
    ...categories.map((c) => ({ id: String(c.id), label: c.name }))]`. Build a derived
    `selectedCategoryKey` = `categoryId !== null ? String(categoryId) : NONE_CATEGORY_ID`, and a
    derived `selectedCategoryLabel` (find in `categoryOptions` by `id === selectedCategoryKey`).
    Add a typed `noExpandCategory(): { id: string; label: string }[]` returning `[]` (same pattern
    as Task 1's `noExpandRole`). Replace the `<label for="req-category">Категория</label><Select
    ...>...</Select>` block with the CartridgeFormBody `.dropdown-label` wrapping-label pattern:
    `<Dropdown variant="select" flat={true} value={selectedCategoryLabel} placeholder="Без
    категории" searchPlaceholder="Поиск категории" loading={false} groups={categoryOptions}
    getGroupId={(o) => o.id} getGroupName={(o) => o.label} getGroupCount={() => 0}
    isGroupExpandable={() => false} isGroupSelected={(o) => o.id === selectedCategoryKey}
    onExpandGroup={noExpandCategory} getMemberId={(o) => o.id} getMemberName={(o) => o.label}
    onSearch={() => {}} onPickGroup={(o) => { categoryId = o.id === NONE_CATEGORY_ID ? null :
    parseInt(o.id, 10); }} onPickMember={() => {}} />`. The `{#if categoriesLoading &amp;&amp;
    categories.length === 0}` loading hint stays as-is (renders below the Dropdown, unchanged).
    Add the `.dropdown-label` CSS rule to this component's `<style>` block if not already present.
  </action>
  <acceptance_criteria>
    - `grep -c "import Select from" ui/src/features/requests/RequestFormModal.svelte` == 0
    - `grep -c "import GroupedPrinterSelect from" ui/src/features/requests/RequestFormModal.svelte` == 1 (printer picker untouched — out of GAP-1 scope)
    - `grep -c "import Dropdown from" ui/src/features/requests/RequestFormModal.svelte` == 1
    - `grep -c "NONE_CATEGORY_ID" ui/src/features/requests/RequestFormModal.svelte` &gt;= 3 (definition + isGroupSelected + onPickGroup usages)
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <done>RequestFormModal's Категория picker renders via custom Dropdown with the same option set (Без категории + server categories) and identical categoryId write; GroupedPrinterSelect printer field untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundary — GAP-1 is a component swap (native `Select` -> custom `Dropdown`) inside
already-authenticated admin/employee screens. No new data source, no new external input path.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-11-01 | Tampering (regression) | RequestDetail approveRole picker | mitigate | acceptance criteria greps confirm identical 3 options + identical `onPickGroup` write target; svelte-check catches type drift |
| T-28-11-02 | Tampering (regression) | RequestFormModal categoryId picker | mitigate | `NONE_CATEGORY_ID` sentinel round-trip explicitly asserted via grep; same categoryId write semantics as native Select |
| T-28-11-SC | Tampering (supply-chain) | npm installs | accept | no new packages — Dropdown is an existing in-repo primitive |

No `high`-severity threats introduced.
</threat_model>

<verification>
- `pnpm --dir ui svelte-check` exits 0
- `pnpm --dir ui lint` exits 0 (includes `check-tokens.mjs`)
- `pnpm --dir ui build` exits 0
- No `import Select from '$lib/components/Select.svelte'` remains in either file
</verification>

<success_criteria>
GAP-1 closed for RequestDetail.svelte (Роль) and RequestFormModal.svelte (Категория): both use the
custom Dropdown primitive, same options/values/side-effects, zero native Select imports remaining
in these two files.
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-11-SUMMARY.md` when done
</output>
