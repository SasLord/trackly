---
phase: 28-support-admin-windows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/requests/RequestDetail.svelte
  - ui/src/features/requests/RequestFormModal.svelte
  - ui/src/features/requests/RequestsPage.svelte
autonomous: true
requirements: [WIN-06]
must_haves:
  truths:
    - "D-01 (перенос из Фазы 27, Заявки): RequestDetail потребляет общие `DetailPanel`/`DetailSection`/`DetailField`; заголовок использует прецедент `PrinterDetail.svelte` (badges + meta-row как первый элемент children), НЕ простой строковый title `ActDetail.svelte`, т.к. заголовок Заявки несёт два бейджа (тип+статус) плюс автор/дата — не влезает в строковый title-проп"
    - "D-04 (везде): внутренности RequestFormModal ре-токенизированы целиком без остатков bespoke-классов; функция/раскладка формы не меняются"
    - "PageHeader adoption (находка пэттерн-мапера, консистентность компонентов SC #1): RequestsPage потребляет примитив `PageHeader` вместо bespoke `<header class=\"page-header\"><h1 class=\"page-title\">`, как D-07 Фазы 26 применил ActsPage/DevicesPage"
    - "SC #4: все lifecycle-кнопки заявки (Подтвердить/Принять в работу/Выполнить/Установить картридж/Отклонить/Отменить/Удалить), история, поля по типу заявки — на месте и работают как раньше"
  artifacts:
    - path: "ui/src/features/requests/RequestDetail.svelte"
      provides: "Деталь заявки на DetailPanel"
      contains: "DetailPanel"
  key_links:
    - from: "ui/src/features/requests/RequestDetail.svelte"
      to: "ui/src/lib/components/DetailPanel.svelte"
      via: "импорт общего паттерна детали (создан 27-01)"
      pattern: "import DetailPanel"
    - from: "ui/src/features/requests/RequestsPage.svelte"
      to: "ui/src/lib/components/PageHeader.svelte"
      via: "импорт PageHeader (title + actions snippet)"
      pattern: "import PageHeader"
---

<objective>
Перевести детальную панель, форму создания и шапку окна **Заявок (WIN-06)** на дизайн-систему:
`RequestDetail` на общий `DetailPanel`/`DetailSection`/`DetailField` (D-01, самый сложный перенос
фазы — заголовок несёт два бейджа + meta-row, не влезает в простой строковый `title`), внутренности
`RequestFormModal` ре-токенизированы (D-04), `RequestsPage` переходит на `PageHeader` (находка вне
явного списка CONTEXT.md — тот же пропуск, что был у ActsPage/DevicesPage до D-07 Фазы 26). **Ни одно
поле, действие, lifecycle-переход или workflow не меняется (SC #4)**.

Purpose: закрыть SC #1 (Заявки повсеместно на новых токенах/компонентах) для детали/формы/шапки.
Output: 3 файла Заявок на `DetailPanel`/`PageHeader`, без bespoke-классов в форме/шапке.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/features/acts/ActDetail.svelte
@ui/src/features/printers/PrinterDetail.svelte
@ui/src/features/acts/ActsPage.svelte
@ui/src/features/devices/DeviceFormBody.svelte
@ui/src/lib/components/DetailPanel.svelte
@ui/src/lib/components/DetailSection.svelte
@ui/src/lib/components/DetailField.svelte
@ui/src/lib/components/PageHeader.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: RequestDetail → DetailPanel/DetailSection/DetailField (D-01)</name>
  <read_first>
    - ui/src/features/requests/RequestDetail.svelte (ПРОЧИТАТЬ ЦЕЛИКОМ — 883 строки, самый большой файл фазы; ключевые блоки: loading/empty ветки строки 397–408; заголовок `.title-row` (2 Badge) + `.meta-row` (автор/дата) строки 410–424; секция «Информация» `.fields-grid` по типу заявки строки 427–468; каскад lifecycle-действий по роли/статусу строки 470–587 — admin ad_register/specialist/employee-own/terminal ветки, каждая в своей `<section class="section">`; секция «История» строки 601–621; 4 confirm-`<Modal>` вне корневого `<div>` строки 625+ — НЕ трогать, это D-04 территория, не D-01)
    - ui/src/features/printers/PrinterDetail.svelte (эталон заголовка со значком: `panelTitle` = простое строковое значение строка 204, `<DetailPanel title={panelTitle} ...>` строки 213–219, `<div class="title-badges"><Badge .../></div>` как ПЕРВЫЙ элемент `children` сразу после `{#snippet actions()}` строки 226–229 — этот паттерн, НЕ ActDetail, подходит RequestDetail: заголовок Заявки несёт 2 Badge + meta-row, не влезает в строковый `title`)
    - ui/src/features/acts/ActDetail.svelte (эталон общей структуры: empty/loading ветки строки 65–79, `{#snippet actions()}` с условными Button строки 80–97, `<DetailSection heading="...">` + `<DetailField label value>` строки 100+)
    - ui/src/lib/components/DetailPanel.svelte, DetailSection.svelte, DetailField.svelte (контракты: `title?: string`, `actions?/emptyActions?: Snippet`, `DetailField.value?: string | null` — null рендерит «—»)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Переписать `RequestDetail.svelte` на общий паттерн 27-01, по прецеденту `PrinterDetail` (не
    `ActDetail`) для заголовка. `panelTitle` = `typeLabel` (простое строковое значение — заявка не
    имеет отдельного «номера»/заголовка, `typeLabel` уже вычисляется в текущем `<script>`).
    Empty-состояние → `<DetailPanel empty emptyTitle="Выберите заявку" emptyBody="Выберите заявку
    слева, чтобы увидеть детали и историю.">` (KEEP копирайт дословно, UI-SPEC §7.1). Loading-ветка —
    сиблинг вне `DetailPanel` (как сейчас, Spinner + текст «Загрузка заявки…» — уже есть, просто
    сохранить условие). Заполненное состояние → `<DetailPanel title={panelTitle} ...>`, внутри
    `children` ПЕРВЫМ элементом — bespoke `<div class="title-row"><Badge variant="default">{typeLabel}
    </Badge><Badge variant={statusVariant}>{statusLabel}</Badge></div>` + `<div class="meta-row">`
    (автор/дата) — переносятся ДОСЛОВНО из текущих строк 410–424 (не через `title`-проп, по прецеденту
    PrinterDetail `title-badges`). Секция «Информация» (`.fields-grid` строки 427–468, поля по типу
    заявки ad_register/cartridge_replace/free_form) → `<DetailSection heading="Информация">` с сеткой
    `DetailField label value` (2-колоночный grid как в текущем `.fields-grid` — Field-grid паттерн
    27-PATTERNS.md D-01, копировать 1:1); каждое условное поле (`{#if request.requestType === ...}`)
    сохраняется как условный `DetailField`.
    Lifecycle-действия (строки 470–587, ветки admin/specialist/employee/terminal по роли и статусу):
    простые кнопки-переходы БЕЗ прикреплённого инлайн-поля ввода (Подтвердить, Принять в работу,
    Отклонить-открывает-Modal, Установить картридж-открывает-Modal, Отменить заявку-открывает-Modal,
    Удалить-открывает-Modal) — перенести в `{#snippet actions()}` DetailPanel как условные `<Button>`
    (образец — ActDetail строки 80–97), сохранив ТУ ЖЕ условную логику видимости по роли/статусу.
    Кнопку «Выполнить» (in_progress + free_form), которая submit-ит инлайн `completeNotes`-Textarea
    (строки 512–524), НЕ переносить в header actions — она часть инлайн-мини-формы, оставить вместе
    с Textarea внутри `<DetailSection>` тела (структура/поведение этой мини-формы не меняется).
    Отображение `resolutionNotes` (terminal-статусы) и одиночная кнопка «Удалить»/«Отменить заявку»
    (отдельные независимые секции строк 587–598) — обернуть в `<DetailSection>` без heading (как
    было — bespoke `.resolution`/`.actions` внутри секции) либо, если действие простое и не завязано
    на локальные поля, переместить в общий `actions`-snippet вместе с остальными lifecycle-кнопками
    (Claude's Discretion, CONTEXT.md — главное: каждая существующая кнопка/условие видимости
    сохраняется буквально, просто меняется контейнер). Секция «История» (строки 601–621) →
    `<DetailSection heading="История">` с существующей `{#each historyEntries}`-разметкой без
    изменений внутри (пустое состояние «История пуста» — KEEP, UI-SPEC §7.1). Удалить bespoke
    `.detail-header`/`.section`/`.section-heading`/`.fields-grid`/`.field`/`.field-wide` CSS-классы,
    заменённые примитивами. **4 confirm-`<Modal>` (строки 625+, вне корневого `<div>`) НЕ трогать** —
    это D-04, покрыто визуальной проверкой, не структурной правкой D-01. Ничего не переименовывать
    в `<script>`-логике — только разметка.
  </action>
  <acceptance_criteria>
    - `ui/src/features/requests/RequestDetail.svelte` содержит `import DetailPanel` И `<DetailPanel` И `import DetailSection` И `import DetailField`
    - `grep -v '^\s*//' ui/src/features/requests/RequestDetail.svelte | grep -c 'class="detail-header"'` == 0
    - `grep -c "История пуста\|Выберите заявку\|Выберите заявку слева" ui/src/features/requests/RequestDetail.svelte` &gt;= 2 (копирайт сохранён дословно)
    - Все lifecycle-кнопки присутствуют: `grep -c "Подтвердить\|Принять в работу\|Выполнить\|Установить картридж\|Отклонить\|Отменить заявку\|Удалить" ui/src/features/requests/RequestDetail.svelte` &gt;= 7
    - 4 confirm-Modal не удалены: `grep -c "<Modal" ui/src/features/requests/RequestDetail.svelte` &gt;= 4
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: открыть заявки разных типов/статусов под разными ролями (admin/specialist/employee) — заголовок с двумя бейджами+meta-row виден, каждая lifecycle-кнопка появляется по тем же условиям, что раньше, история рендерится
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть заявку каждого типа (ad_register/cartridge_replace/free_form) под admin/specialist/employee: заголовок (2 badge + автор/дата), поля «Информация», нужные lifecycle-кнопки, история — всё на месте и работает</human-check>
  </verify>
  <done>RequestDetail на DetailPanel/DetailSection/DetailField с PrinterDetail-заголовком (title-badges); bespoke detail-классы удалены; все lifecycle-переходы, поля и 4 confirm-модалки сохранены (SC #4).</done>
</task>

<task type="auto">
  <name>Task 2: RequestFormModal ре-токенизация (D-04) + RequestsPage → PageHeader</name>
  <read_first>
    - ui/src/features/requests/RequestFormModal.svelte (уже частично на `Select`/`Textarea`/`GroupedPrinterSelect` — импорты строки 6–10; проверить остаточные raw `<input>`/bespoke `.field`-стили по всему файлу 351 строк — низкий риск, largely done, финальный аудит на 100% покрытие примитивами)
    - ui/src/features/devices/DeviceFormBody.svelte (эталон field+error разметки: `<div class="field" class:has-error>` + `<label class="label">` + примитив + `{#if fieldErrors[...]}<p class="field-error">` строки 196–260) — использовать как эталон, если найдутся остаточные raw-поля
    - ui/src/features/requests/RequestsPage.svelte (bespoke `<header class="page-header"><h1 class="page-title">Заявки</h1></header>` строки 222–224, БЕЗ кнопки-действия в шапке — «Создать заявку» живёт в RequestsSearchAndTabs, не в шапке; scoped CSS `.page-header`/`.page-title` — найти по grep)
    - ui/src/features/acts/ActsPage.svelte (эталон PageHeader: `<PageHeader title="Акты">{#snippet actions()}<Button variant="primary" onclick={openCreate}>+ Создать акт</Button>{/snippet}</PageHeader>` строки 254–258)
    - ui/src/lib/components/PageHeader.svelte (контракт: `title: string`, `actions?: Snippet`; встроенный гамбургер nav-toggle)
  </read_first>
  <action>
    (D-04) В `RequestFormModal.svelte` провести финальный аудит: заменить любые оставшиеся raw
    `<input>`/`<select>`/bespoke `.field`-CSS на `Input`/`Select`/field-error паттерн из
    `DeviceFormBody`. Компонент уже в основном на примитивах (`Select`, `Textarea`,
    `GroupedPrinterSelect`) — если аудит не находит остаточного raw HTML, задача сводится к
    подтверждению 100%-покрытия (низкий риск по 28-PATTERNS.md). Валидация (`validate()`,
    `printerError`/`descError`) и `<script>`-логика НЕ меняются.
    (PageHeader) В `RequestsPage.svelte` заменить bespoke `<header class="page-header"><h1
    class="page-title">Заявки</h1></header>` на `<PageHeader title="Заявки">` БЕЗ `actions`-snippet
    (в этой шапке сейчас нет кнопки — «Создать заявку» остаётся в `RequestsSearchAndTabs`, НЕ
    дублировать её в PageHeader). Импортировать `PageHeader` из `$lib/components/PageHeader.svelte`.
    Удалить scoped CSS `.page-header`/`.page-title`. Остальная структура страницы (employee-summary
    `StatWidget`, master-detail, модалка формы) не меняется.
  </action>
  <acceptance_criteria>
    - `ui/src/features/requests/RequestFormModal.svelte` не содержит raw `<input\s` (кроме внутри `.script`-комментариев): `grep -v '^\s*//' ui/src/features/requests/RequestFormModal.svelte | grep -c '&lt;input'` == 0
    - `ui/src/features/requests/RequestsPage.svelte` содержит `import PageHeader` И `<PageHeader title="Заявки"` И `grep -v '^\s*//' ui/src/features/requests/RequestsPage.svelte | grep -c 'class="page-header"'` == 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: модалка создания заявки выглядит и работает как раньше (валидация, отправка); шапка «Заявки» рендерится через PageHeader с гамбургером на мобильной ширине
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть «Создать заявку»: форма без остаточных bespoke-полей, валидация работает; шапка страницы — PageHeader</human-check>
  </verify>
  <done>RequestFormModal на 100% примитивах ввода; RequestsPage потребляет PageHeader вместо bespoke header; поведение неизменно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. Деталь заявки — самый функционально насыщенный экран фазы (множество
lifecycle-переходов, 4 confirm-модалки, ролевая видимость действий). Главный риск этой волны —
случайно изменить условие видимости/доступности какой-либо кнопки (например, потерять
Admin-only-гейт на ad_register-действиях, T-09-21) при переносе разметки на DetailPanel/actions.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Elevation of Privilege (regression) | RequestDetail lifecycle-кнопки (ролевые/статусные условия) | mitigate | условная логика (`{#if}`/роль/статус) переносится буквально, не переписывается; acceptance criteria проверяет наличие всех 7 экшнов; human-check проходит все роли (admin/specialist/employee) × статусы |
| T-28-02-02 | Tampering (regression) | 4 confirm-Modal (Отклонить/Удалить/Отменить/подтверждение AD) | mitigate | Modal-блоки исключены из правки D-01 (остаются как есть вне DetailPanel), grep-гейт на `&lt;Modal` count &gt;= 4 |
| T-28-02-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается |

Угроз уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Human-check подтверждает: все lifecycle-переходы Заявки работают под каждой ролью, форма создания валидна, шапка на PageHeader
</verification>

<success_criteria>
Деталь, форма создания и шапка окна Заявок — на `DetailPanel`/`DetailSection`/`DetailField`/`PageHeader`
и токенах `--tr-*`, без bespoke-классов; ни один lifecycle-переход, поле или confirm-диалог не утрачен
(SC #4).
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-02-SUMMARY.md` when done
</output>
