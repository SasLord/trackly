---
phase: 28-support-admin-windows
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/settings/BackupSettings.svelte
  - ui/src/features/settings/NetworkSettings.svelte
  - ui/src/features/settings/TemplateEditor.svelte
autonomous: true
gap_closure: true
requirements: [WIN-08]
must_haves:
  truths:
    - "GAP-1 (BackupSettings.svelte:150, частота бэкапа): schedule picker uses custom Dropdown (flat + variant=\"select\"), same 3 options (Отключено/Ежедневно/Еженедельно), same schedule write, same disabled-while-no-folder behavior"
    - "GAP-1 (NetworkSettings.svelte:194, Bind-адрес): host picker uses custom Dropdown, same 2 options (0.0.0.0/127.0.0.1), same settings.host write, same disabled-while-running behavior"
    - "GAP-1 (TemplateEditor.svelte:220, тип шаблона) + D-08 boundary respected: only the kind-select control is migrated to Dropdown; the textarea/code editing area and preview mechanics are untouched"
    - "SC #3: Настройки window fields/actions/workflow unchanged — component swap only"
  artifacts:
    - path: "ui/src/features/settings/BackupSettings.svelte"
      provides: "Расписание бэкапа picker on custom Dropdown"
      contains: "Dropdown"
    - path: "ui/src/features/settings/NetworkSettings.svelte"
      provides: "Bind-адрес picker on custom Dropdown"
      contains: "Dropdown"
    - path: "ui/src/features/settings/TemplateEditor.svelte"
      provides: "Шаблон kind-select on custom Dropdown, editor/preview untouched"
      contains: "Dropdown"
  key_links:
    - from: "ui/src/features/settings/BackupSettings.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "import Dropdown, variant=\"select\" flat={true}"
      pattern: "import Dropdown"
    - from: "ui/src/features/settings/NetworkSettings.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "import Dropdown, variant=\"select\" flat={true}"
      pattern: "import Dropdown"
    - from: "ui/src/features/settings/TemplateEditor.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "import Dropdown, variant=\"select\" flat={true}"
      pattern: "import Dropdown"
---

<objective>
Close GAP-1 (28-VERIFICATION.md) for the three remaining native-`Select` sites in the
**Настройки** window: `BackupSettings.svelte:150` (частота бэкапа), `NetworkSettings.svelte:194`
(Bind-адрес), and `TemplateEditor.svelte:220` (тип шаблона — D-08 constrained, control only).
Replace all three with the custom `Dropdown.svelte` primitive (flat + `variant="select"`),
matching the Phase 27 Cartridges precedent (commit `80d0b41`). Component swap only — same
options, same selected value, same onchange side-effects (SC #3).

Purpose: recurring correction — native combobox must never remain once `Dropdown` exists for this
exact single-select need; `TemplateEditor`'s D-08 visual-only boundary (editor/preview untouched)
must hold.
Output: 3 files on `Dropdown` instead of `Select`, zero remaining `Select` imports in these files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-VERIFICATION.md
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@ui/src/lib/components/Dropdown.svelte
@ui/src/features/cartridges/CartridgeFormBody.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: BackupSettings.svelte — частота бэкапа native Select -&gt; Dropdown</name>
  <files>ui/src/features/settings/BackupSettings.svelte</files>
  <read_first>
    - ui/src/features/settings/BackupSettings.svelte lines 4 (`import Select from
      '$lib/components/Select.svelte'`), 21 (`let schedule = $state('')`), 146-164 (`<div
      class="config-row"><label class="config-label" for="backup-schedule">Расписание</label><div
      class="select-shrink"><Select id="backup-schedule" value={schedule}
      disabled={!backupFolder} onchange={(v) => (schedule = v)}><option value="">Отключено</option>
      <option value="daily">Ежедневно</option><option value="weekly">Еженедельно</option></Select>
      </div></div>`), 280-283 (`.select-shrink { width: fit-content; min-width: 180px; }` — this
      wrapper div CSS is independent of Select's internals and works unchanged around Dropdown)
    - ui/src/lib/components/Dropdown.svelte (prop contract; no `id`/`for` — implicit wrapping
      label needed, same as 28-11's pattern)
    - ui/src/features/cartridges/CartridgeFormBody.svelte lines 283-310 (the «Состояние» field —
      closest analog: 3-option flat select with a plain string/id value, no server fetch)
  </read_first>
  <action>
    Replace `import Select from '$lib/components/Select.svelte'` with `import Dropdown from
    '$lib/components/Dropdown.svelte'`. Define `SCHEDULE_OPTIONS = [{ id: '', label: 'Отключено'
    }, { id: 'daily', label: 'Ежедневно' }, { id: 'weekly', label: 'Еженедельно' }]` next to
    `schedule`'s declaration. Add a derived `scheduleLabel` (find in `SCHEDULE_OPTIONS` by `id ===
    schedule`) and a typed `noExpandSchedule(): { id: string; label: string }[]` returning `[]`.
    Replace the `<label class="config-label" for="backup-schedule">Расписание</label><div
    class="select-shrink"><Select ...>...</Select></div>` block: keep the surrounding
    `.select-shrink` div exactly as-is (wraps the field, still constrains width to
    fit-content/180px regardless of which field component is inside), but change the label to wrap
    the Dropdown (implicit-label pattern, since Dropdown has no `id`/`for`) — e.g. `<label
    class="config-label dropdown-label"><span>Расписание</span><div class="select-shrink"><Dropdown
    variant="select" flat={true} value={scheduleLabel} placeholder="Отключено"
    searchPlaceholder="Поиск" disabled={!backupFolder} loading={false} groups={SCHEDULE_OPTIONS}
    getGroupId={(o) => o.id} getGroupName={(o) => o.label} getGroupCount={() => 0}
    isGroupExpandable={() => false} isGroupSelected={(o) => o.id === schedule}
    onExpandGroup={noExpandSchedule} getMemberId={(o) => o.id} getMemberName={(o) => o.label}
    onSearch={() => {}} onPickGroup={(o) => (schedule = o.id)} onPickMember={() => {}}
    /></div></label>` (adapt exact wrapping to this file's existing `.config-row`/`.config-label`
    structure — the goal is: same visible label text, same disabled condition, same `schedule`
    write). Add a `.dropdown-label` CSS rule (flex column, small gap) to this component's
    `<style>` if not already present. The helper text below (`{#if !backupFolder}<p
    class="helper-text">...`) stays untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "import Select from" ui/src/features/settings/BackupSettings.svelte` == 0
    - `grep -c "import Dropdown from" ui/src/features/settings/BackupSettings.svelte` == 1
    - `grep -c "SCHEDULE_OPTIONS" ui/src/features/settings/BackupSettings.svelte` &gt;= 1
    - `grep -c "onPickGroup={(o) => (schedule" ui/src/features/settings/BackupSettings.svelte` == 1
    - `grep -c "disabled={!backupFolder}" ui/src/features/settings/BackupSettings.svelte` &gt;= 1 (disabled condition preserved)
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <done>BackupSettings' schedule picker renders via custom Dropdown; same 3 options, same disabled condition, same schedule write.</done>
</task>

<task type="auto">
  <name>Task 2: NetworkSettings.svelte — Bind-адрес native Select -&gt; Dropdown</name>
  <files>ui/src/features/settings/NetworkSettings.svelte</files>
  <read_first>
    - ui/src/features/settings/NetworkSettings.svelte lines 5 (`import Select from
      '$lib/components/Select.svelte'`), 192-203 (`<div class="form-field"><label
      class="form-label" for="net-host">Bind-адрес</label><Select id="net-host"
      value={settings.host} disabled={saving || serverRunning} onchange={(v) => (settings.host =
      v)}><option value="0.0.0.0">0.0.0.0 (все интерфейсы)</option><option
      value="127.0.0.1">127.0.0.1 (только localhost)</option></Select></div>`)
    - ui/src/lib/components/Dropdown.svelte (prop contract; implicit-label pattern)
    - ui/src/features/cartridges/CartridgeFormBody.svelte lines 196-224 («Что добавляем» field —
      closest analog: 2-option static flat select)
  </read_first>
  <action>
    Replace `import Select from '$lib/components/Select.svelte'` with `import Dropdown from
    '$lib/components/Dropdown.svelte'`. Define `HOST_OPTIONS = [{ id: '0.0.0.0', label: '0.0.0.0
    (все интерфейсы)' }, { id: '127.0.0.1', label: '127.0.0.1 (только localhost)' }]`. Add a
    derived `hostLabel` (find by `id === settings.host`) and a typed `noExpandHost(): { id: string;
    label: string }[]` returning `[]`. Replace the `<label class="form-label"
    for="net-host">Bind-адрес</label><Select ...>...</Select>` block with the implicit-label
    wrapping pattern: `<label class="form-label dropdown-label"><span
    class="label-text">Bind-адрес</span><Dropdown variant="select" flat={true} value={hostLabel}
    placeholder="Выберите адрес" searchPlaceholder="Поиск" disabled={saving || serverRunning}
    loading={false} groups={HOST_OPTIONS} getGroupId={(o) => o.id} getGroupName={(o) => o.label}
    getGroupCount={() => 0} isGroupExpandable={() => false} isGroupSelected={(o) => o.id ===
    settings.host} onExpandGroup={noExpandHost} getMemberId={(o) => o.id} getMemberName={(o) =>
    o.label} onSearch={() => {}} onPickGroup={(o) => (settings.host = o.id)} onPickMember={() =>
    {}} /></label>`. Preserve the existing `.form-field`/`.form-grid` layout wrapper unchanged. Add
    a `.dropdown-label` CSS rule if not already present. The «Порт»/«Путь к сертификату» sibling
    fields are untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "import Select from" ui/src/features/settings/NetworkSettings.svelte` == 0
    - `grep -c "import Dropdown from" ui/src/features/settings/NetworkSettings.svelte` == 1
    - `grep -c "HOST_OPTIONS" ui/src/features/settings/NetworkSettings.svelte` &gt;= 1
    - `grep -c "onPickGroup={(o) => (settings.host" ui/src/features/settings/NetworkSettings.svelte` == 1
    - `grep -c "disabled={saving || serverRunning}" ui/src/features/settings/NetworkSettings.svelte` &gt;= 1
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <done>NetworkSettings' Bind-адрес picker renders via custom Dropdown; same 2 options, same disabled condition, same settings.host write.</done>
</task>

<task type="auto">
  <name>Task 3: TemplateEditor.svelte — тип шаблона native Select -&gt; Dropdown (D-08 strict)</name>
  <files>ui/src/features/settings/TemplateEditor.svelte</files>
  <read_first>
    - ui/src/features/settings/TemplateEditor.svelte lines 5 (`import Select from
      '$lib/components/Select.svelte'`), 16-19 (`KIND_LABELS` map), 86-87 (`templates`/`selectedKind`
      state), 217-228 (`<div class="template-selector-row"><label class="form-label"
      for="template-kind">Шаблон</label><div class="select-shrink"><Select id="template-kind"
      value={selectedKind} onchange={(v) => (selectedKind = v)}>{#each templates as tmpl}<option
      value={tmpl.kind}>{KIND_LABELS[tmpl.kind] ?? tmpl.label ?? tmpl.kind}</option>{/each}</Select>
      </div></div>`), 331-334 (`.select-shrink { width: fit-content; min-width: 220px; }` — kept
      as-is, wraps the field regardless of which component is inside)
    - .planning/phases/28-support-admin-windows/28-CONTEXT.md section "Серая зона C — Редактор
      шаблонов" (D-08: strictly visual — chrome + form controls only; the textarea/code editing
      area and preview mechanics are OUT OF SCOPE and must not be touched by this task)
    - ui/src/lib/components/Dropdown.svelte (prop contract; implicit-label pattern)
    - ui/src/features/cartridges/CartridgeFormBody.svelte lines 249-281 («Модель» field — closest
      analog: dynamic server-provided array with a label lookup map, same shape as
      `templates`/`KIND_LABELS`)
  </read_first>
  <action>
    Replace `import Select from '$lib/components/Select.svelte'` with `import Dropdown from
    '$lib/components/Dropdown.svelte'`. Build a derived `templateOptions` array:
    `templates.map((tmpl) => ({ id: tmpl.kind, label: KIND_LABELS[tmpl.kind] ?? tmpl.label ??
    tmpl.kind }))`. Add a derived `selectedKindLabel` (find in `templateOptions` by `id ===
    selectedKind`) and a typed `noExpandKind(): { id: string; label: string }[]` returning `[]`.
    Replace ONLY the `<label class="form-label" for="template-kind">Шаблон</label><div
    class="select-shrink"><Select ...>...</Select></div>` block (lines 217-228) with the
    implicit-label wrapping pattern, keeping the `.select-shrink` div exactly as-is around the new
    field: `<label class="form-label dropdown-label"><span>Шаблон</span><div
    class="select-shrink"><Dropdown variant="select" flat={true} value={selectedKindLabel}
    placeholder="Выберите шаблон" searchPlaceholder="Поиск" loading={false}
    groups={templateOptions} getGroupId={(o) => o.id} getGroupName={(o) => o.label}
    getGroupCount={() => 0} isGroupExpandable={() => false} isGroupSelected={(o) => o.id ===
    selectedKind} onExpandGroup={noExpandKind} getMemberId={(o) => o.id} getMemberName={(o) =>
    o.label} onSearch={() => {}} onPickGroup={(o) => (selectedKind = o.id)} onPickMember={() =>
    {}} /></div></label>`. Do NOT touch: the `<details class="variables-panel">` block, the
    `.editor-wrapper`/textarea (`.script` logic around `body`/`handleSave`/`handleReset`), the PDF
    preview `<Modal>`, or any `apiCall('templates_*', ...)` calls — this task is strictly the
    kind-selector control (D-08). Add a `.dropdown-label` CSS rule if not already present.
  </action>
  <acceptance_criteria>
    - `grep -c "import Select from" ui/src/features/settings/TemplateEditor.svelte` == 0
    - `grep -c "import Dropdown from" ui/src/features/settings/TemplateEditor.svelte` == 1
    - `grep -c "templateOptions" ui/src/features/settings/TemplateEditor.svelte` &gt;= 1
    - `grep -c "onPickGroup={(o) => (selectedKind" ui/src/features/settings/TemplateEditor.svelte` == 1
    - `git diff ui/src/features/settings/TemplateEditor.svelte | grep -c "textarea\|editor-wrapper\|apiCall('templates_"` == 0 (editor/preview/API mechanics untouched — D-08)
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <done>TemplateEditor's kind-select renders via custom Dropdown; textarea/preview/API untouched per D-08; same options/labels, same selectedKind write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundary — GAP-1 is a component swap inside authenticated admin-only Настройки
screens. `TemplateEditor` writes to DB-backed templates via existing `apiCall('templates_*', ...)`
paths, unchanged by this plan.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-12-01 | Tampering (regression) | BackupSettings/NetworkSettings pickers | mitigate | acceptance criteria grep-assert identical option sets + identical write targets + preserved disabled conditions |
| T-28-12-02 | Tampering (scope creep, D-08) | TemplateEditor.svelte | mitigate | explicit grep gate asserting zero diff touching textarea/editor-wrapper/apiCall('templates_*',...) |
| T-28-12-SC | Tampering (supply-chain) | npm installs | accept | no new packages |

No `high`-severity threats introduced.
</threat_model>

<verification>
- `pnpm --dir ui svelte-check` exits 0
- `pnpm --dir ui lint` exits 0 (includes `check-tokens.mjs`)
- `pnpm --dir ui build` exits 0
- No `import Select from '$lib/components/Select.svelte'` remains in any of the 3 files
- TemplateEditor diff does not touch textarea/editor-wrapper/preview/API code (D-08)
</verification>

<success_criteria>
GAP-1 closed for BackupSettings.svelte, NetworkSettings.svelte, TemplateEditor.svelte: all three
use the custom Dropdown primitive, same options/values/side-effects, D-08 boundary intact.
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-12-SUMMARY.md` when done
</output>
