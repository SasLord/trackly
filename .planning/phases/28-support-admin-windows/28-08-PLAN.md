---
phase: 28-support-admin-windows
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/settings/TemplateEditor.svelte
autonomous: true
requirements: [WIN-08]
must_haves:
  truths:
    - "D-08 (Настройки, TemplateEditor — серая зона C, строго ограничена): только хрома и форм-контролы переведены на примитивы (kind-select → Select); область редактирования (textarea тела шаблона) и механика предпросмотра (iframe sandbox=\"\" + srcdoc) НЕ тронуты"
    - "D-08: Button/Modal уже на примитивах (подтверждено кодом, UI-SPEC §6.3) — confirm-модалка «Сбросить шаблон?» и её копирайт («Отмена»/«Сбросить») не переоткрываются"
    - "SC #4 (строгость D-08): DB-backed шаблоны (seed/upgrade-ловушка проекта) — эта правка не касается сохранения/валидации/рендера шаблона, только markup селектора"
  artifacts:
    - path: "ui/src/features/settings/TemplateEditor.svelte"
      provides: "Селектор шаблона на Select; область редактирования не тронута"
      contains: "import Select"
  key_links:
    - from: "ui/src/features/settings/TemplateEditor.svelte"
      to: "ui/src/lib/components/Select.svelte"
      via: "kind-select (Акт приёма-передачи/Акт возврата/Отчёт)"
      pattern: "import Select"
---

<objective>
Ре-токенизировать единственный raw-контрол окна **редактора Шаблонов (WIN-08, D-08)**: селектор типа
шаблона (`template-kind`) → `Select`. Это самая функционально-чувствительная панель фазы — шаблоны
живут в БД с известной seed/upgrade-ловушкой (см. `.claude memory: db_backed_templates_upgrade_trap`)
— граница D-08 строго ограничивает правку хромой/форм-контролами, **область `textarea`/код и механика
превью (`iframe sandbox=""`) НЕ трогаются**.

Purpose: закрыть D-04/D-08 для последней панели Настроек с минимально возможным риском.
Output: 1 файл, единственное изменение — `<select>` → `Select`, ноль изменений в textarea/iframe/save-логике.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-support-admin-windows/28-CONTEXT.md
@.planning/phases/28-support-admin-windows/28-UI-SPEC.md
@.planning/phases/28-support-admin-windows/28-PATTERNS.md
@ui/src/lib/components/Select.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemplateEditor kind-select → Select (D-08, строго ограничено)</name>
  <read_first>
    - ui/src/features/settings/TemplateEditor.svelte (ПРОЧИТАН ЦЕЛИКОМ; 462 строки — единственный raw-контрол: `<select id="template-kind" class="form-select" bind:value={selectedKind}>{#each templates as tmpl (tmpl.kind)}<option value={tmpl.kind}>{KIND_LABELS[tmpl.kind] ?? tmpl.label ?? tmpl.kind}</option>{/each}</select>` строки 217–224; `.form-select` CSS строка 328; `template-textarea` строки 245–250 и `iframe sandbox="" srcdoc={previewHtml}` строка 267 — **ГРАНИЦА: НЕ ТРОГАТЬ**; footer Button×3 (Проверить/Сохранить/Сбросить) уже на примитивах строки 255–261; confirm-Modal «Сбросить шаблон?» уже на Modal+Button строки 273–286 — уже на примитивах, не переоткрывать)
    - ui/src/lib/components/Select.svelte (контракт: `value: string`, `onchange?: (v:string)=>void`, `children: Snippet`)
    - .planning/phases/28-support-admin-windows/28-UI-SPEC.md §6.3 (подтверждение кодом: Button/Modal/iframe sandbox уже на месте; область правки D-08 сводится к kind-select)
  </read_first>
  <action>
    Заменить `<select id="template-kind" class="form-select" bind:value={selectedKind}>` на
    `<Select id="template-kind" value={selectedKind} onchange={(v) => (selectedKind = v)}>` с теми
    же `{#each templates as tmpl (tmpl.kind)}<option value={tmpl.kind}>{...}</option>{/each}` внутри
    (children snippet, без изменений). Удалить bespoke `.form-select` CSS (строка 328), заменённый
    примитивом. **Строго не трогать:** `.template-textarea`/`bind:value={body}` (тело шаблона),
    `<iframe sandbox="" srcdoc={previewHtml}>` (превью), `validateAndPreview`/`saveTemplate`/
    `resetTemplate`-логику, `<Modal>` confirm-диалог сброса, `<Button>`×3 footer-row. Единственное
    изменение файла — селектор типа шаблона.
  </action>
  <acceptance_criteria>
    - `ui/src/features/settings/TemplateEditor.svelte` содержит `import Select` И `&lt;Select id="template-kind"`
    - `grep -v '^\s*//' ui/src/features/settings/TemplateEditor.svelte | grep -c 'class="form-select"'` == 0
    - Область редактирования не тронута: `grep -c 'class="template-textarea"' ui/src/features/settings/TemplateEditor.svelte` == 1 И `grep -c 'sandbox=""' ui/src/features/settings/TemplateEditor.svelte` == 1
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - verify visually in light AND dark theme, behavior unchanged: смена типа шаблона в селекторе загружает соответствующий шаблон, textarea/превью/сохранение/сброс работают идентично прежнему
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Настройки → Шаблоны: переключить тип шаблона (Акт приёма-передачи/Акт возврата/Отчёт) через новый Select, отредактировать текст, «Проверить (превью)» показывает превью, «Сохранить шаблон» и «Сбросить до умолчания» работают как раньше</human-check>
  </verify>
  <done>Только template-kind селектор на Select; textarea/iframe/save/reset-механика байт-в-байт не изменены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
`TemplateEditor` — единственная панель фазы, где неаккуратная правка могла бы задеть DB-backed
шаблоны (известная seed/upgrade-ловушка проекта: правка bundled-шаблона НЕ трогает существующие БД,
если авто-upgrade не покрывает конкретное поле). D-08 сознательно сужает объём правки до kind-select,
чтобы исключить этот риск целиком.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08-01 | Tampering (высокий риск при нарушении границы) | TemplateEditor область textarea/превью/save-путь (DB-backed, seed/upgrade-ловушка) | mitigate | D-08 явно запрещает правку этой области; acceptance criteria содержит grep-гейт, подтверждающий 0 изменений в `.template-textarea`/`sandbox=""`; human-check проверяет save/reset/preview функционально |
| T-28-08-02 | Tampering (regression) | template-kind Select-адаптер | mitigate | `selectedKind` читает/пишет то же состояние, что раньше, без переименования; human-check переключает все типы шаблона |
| T-28-08-SC | Tampering (supply-chain) | npm-установки | accept | новых пакетов не устанавливается |

Единственный `high`-уровень риска этой фазы (нарушение D-08 границы) явно митигирован сужением scope
и проверочным гейтом — не принимается как остаточный риск.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- Grep-гейт подтверждает: textarea/iframe-превью не изменены
- Human-check подтверждает: выбор шаблона, редактирование, превью, сохранение и сброс работают идентично прежнему
</verification>

<success_criteria>
TemplateEditor: единственный raw-контрол (kind-select) на `Select`; область редактирования и
предпросмотра — байт-в-байт неизменна (D-08); DB-backed save/reset-путь не затронут.
</success_criteria>

<output>
Create `.planning/phases/28-support-admin-windows/28-08-SUMMARY.md` when done
</output>
