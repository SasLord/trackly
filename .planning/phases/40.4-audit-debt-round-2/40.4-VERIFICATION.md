---
phase: 40.4-audit-debt-round-2
verified: 2026-09-25T18:50:20Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 40.4: Долг аудита v1.4, раунд 2 — Verification Report

**Phase Goal:** Закрыть четыре находки повторного аудита вехи (2026-09-23): откат акта не
воскрешает очищенные поля устройства; импорт CSV обновляет дерево мест и не устраивает шторм
WS-событий; автоподстановка номера не блокирует массовое создание; номер акта в печатной форме
приходит из одного источника, а не собирается из сырого номера и суффикса.
**Verified:** 2026-09-25T18:50:20Z (backend-code and test evidence current as of HEAD; commits
fae6c9d4 / 02332498 fold in the two review-time criticals)
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | NEW-1: undo акта не воскрешает очищенные поля устройства (serial_number/model/condition/complectation/notes), inventory_number по-прежнему не восстанавливается | ✓ VERIFIED | `restore_from_snapshot_in_tx` (crates/trackly-infra/src/repos/devices_sqlite.rs:428-490) uses presence-based `CASE WHEN` for exactly the 5 named fields; `status_id`/`place_id` remain unconditional writes; `inventory_number` is absent from the UPDATE with the BE-WR-06 comment intact. Fresh run: `cargo test -p trackly-app --test acts_undo` → 6/6 passed, including the new `undo_restores_serial_number_and_complectation_cleared_after_handover` (scenario 1) and all 5 pre-existing restore-value regression tests. |
| 2 | N-3 (инвалидация): импорт CSV обновляет счётчики дерева «Места» без перехода экрана; write-site зарегистрирован в INV-7 | ✓ VERIFIED | `DevicesPage.svelte:327-333` conditionally calls `notifyPlaceContentChanged(placeIds)` from `onImported`; `DeviceImportCsvModal.svelte`'s single `handleClose()` now feeds both the «Готово» button and Escape/backdrop/× with `report.affected_place_ids` when a commit happened (CR-02 fix, commit 02332498). `node ui/scripts/check-place-tree-invalidation.mjs` → PASS, 0 violations. I independently re-mutated `handleClose()` back to the pre-fix `onClose()`-only body and reran the gate — it failed with the exact CR-02 diagnostic, then passed again after restoring (working tree confirmed clean via `git status`/`git diff`). Live UAT (40.4-HUMAN-UAT.md item 1): passed — counters update without a screen switch, unmapped-place import completes cleanly. |
| 3 | N-3 (шторм событий): импорт CSV шлёт `NumberSpaceChanged` ровно один раз на импорт, не на строку | ✓ VERIFIED | `device_service.rs`: `create_without_broadcast` (no broadcast call) is used inside `import_csv_commit`'s per-row loop; `any_number_set`/`affected_place_ids` accumulate during the loop and `broadcast_number_space_changed()` fires once after the loop (line ~1310), outside it. Fresh run: `cargo test -p trackly-app --test devices_csv_import` → 15/15 passed, including `import_commit_sends_single_broadcast_not_per_row` and `import_commit_collects_affected_place_ids`. |
| 4 | NEW-3: автоподставленный (не тронутый) номер не блокирует «Количество»; номер, введённый/изменённый вручную, по-прежнему блокирует | ✓ VERIFIED | `DeviceFormBody.svelte`: `quantityDisabled` reads `(inventoryNo.trim() !== '' && inventoryNoEditedByHand) || serialNo.trim() !== '' || ...`; the sibling `$effect` that resets `quantity = 1` was patched (commit fae6c9d4, CR-01) to use the identical `inventoryNoEditedByHand` gate, closing the silent-defeat path the reviewer found (mount-autofill race, type-switch re-peek). Bulk-create payload (`handleSubmit`, qty>1 branch) unconditionally sets `inventory_no: null`, matching the server's `bulk_create_with_printer` hard-reject guard (`bulk_create_rejects_when_inventory_no_set` regression test, confirmed present). Live UAT (item 2): passed — bulk-create works with an autofilled number, no server validation error. |
| 5 | NEW-2: печатная форма акта берёт `act.number_display` (=`ActDto.number`, тот же канонический источник, что таймлайн/отчёт «Перемещения») вместо сборки из raw+suffix; акт возврата не теряет суффикс | ✓ VERIFIED (test caveat below) | All 3 number-display sites in `act_handover.html` (`<title>`, `.subtitle`, `.appendix-mark`) read `{{ act.number_display }}` only — no `{{ act.number }}{{ act.suffix` concatenation remains in the active template (confirmed by direct grep, not just by the SUMMARY's claim). `act_service.rs::render_pdf` sets `"number_display": act.number.clone()`; `act.number` (`ActDto.number`) is produced by `format_act_number`, the same single-owner formula `act_number_display.rs::resolve_movement_act_number` uses for the timeline/report. `_legacy_defaults/v29/act_handover.html` snapshot registered so untouched installs upgrade. Fresh run: `cargo test -p trackly-app --lib html_templates` → 16/16, `cargo test -p trackly-app --test html_act_render` → 23/23 (incl. the new suffix-not-lost test and the permanent no-reconstruction gate). Live UAT (item 3): passed — user confirmed «В Перемещения и печатной форме отображаются правильные номера актов и возвратов». **Caveat:** REVIEW.md's WR-01 finding is independently confirmed true — the automated `html_return_act_prints_number_display_with_suffix_not_lost` test is vacuous for its own fixture (parent number == return `number_raw`, so the pre-fix template would render the identical string); it does not by itself prove the fix over the specific "parent renumbered after return exists" edge case. That edge case is a pre-existing, separately-scoped defect, already captured in `.planning/todos/pending/2026-09-26-return-act-suffix-numbering-rework.md` per the user's own live-UAT finding — not a gap of this phase's stated Success Criterion, which is satisfied by the direct source-level evidence (template no longer concatenates) plus live human confirmation. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/trackly-infra/src/repos/devices_sqlite.rs` | presence-based CASE WHEN for 5 clearable fields | ✓ VERIFIED | Confirmed by direct read; wired into 3 call sites (`update`, `update_return`, `undo_device_mutations_for_act` in `act_service.rs`) |
| `crates/trackly-app/tests/acts_undo.rs` | extended `DeviceSnap` + new regression test | ✓ VERIFIED | 6/6 tests pass fresh |
| `crates/trackly-app/src/dto/device.rs` | `CsvImportReport.affected_place_ids: Vec<i64>` | ✓ VERIFIED | Present with correct `#[specta(type = Vec<i32>)]` annotation (fixed same-wave deviation) |
| `crates/trackly-app/src/services/device_service.rs` | `create_without_broadcast` + single-broadcast-after-loop | ✓ VERIFIED | Confirmed via grep + passing tests |
| `ui/src/features/devices/DeviceImportCsvModal.svelte` | `onImported` carries `placeIds`; unified dismiss path | ✓ VERIFIED | CR-02 fix applied and independently mutation-tested by this verifier |
| `ui/src/features/devices/DevicesPage.svelte` | conditional `notifyPlaceContentChanged(placeIds)` | ✓ VERIFIED | Confirmed by direct read |
| `ui/scripts/check-place-tree-invalidation.mjs` | INV-7 registers `DeviceImportCsvModal`; INV-8 (CR-02) added | ✓ VERIFIED | Both registries present; gate passes; live mutation-break/restore confirms non-vacuous |
| `ui/src/lib/components/NumberTemplateField.svelte` | `onEditedByHandChange` callback | ✓ VERIFIED | Present, reuses `isValueUnedited()` |
| `ui/src/features/devices/DeviceFormBody.svelte` | `quantityDisabled` + reset-effect both edited-by-hand-gated; bulk payload unconditional null | ✓ VERIFIED | CR-01 fix applied; confirmed by direct read |
| `crates/trackly-app/src/pdf/html_templates.rs` + `templates/act_handover.html` | `act.number_display`, legacy-defaults snapshot | ✓ VERIFIED | v29 snapshot present, registered; 3 template sites use `number_display` only |
| `crates/trackly-app/src/services/act_service.rs` | print context carries `number_display` | ✓ VERIFIED | `render_pdf` sets it from `act.number.clone()` |
| `crates/trackly-app/tests/html_act_render.rs` | suffix-not-lost test + permanent no-reconstruction gate | ✓ VERIFIED (vacuity caveat, see truth 5) | 23/23 pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `act_service.rs` (update/update_return/undo) | `devices_sqlite.rs::restore_from_snapshot_in_tx` | direct call, before_json snapshot | WIRED | 3 call sites confirmed |
| `import_csv_commit` loop | `create_without_broadcast` | direct call | WIRED | Confirmed; broadcast moved outside loop |
| `DeviceImportCsvModal.onImported` | `DevicesPage.notifyPlaceContentChanged` | prop callback | WIRED | Confirmed + mutation-tested |
| `DeviceImportCsvModal`'s `<Modal onClose=...>` | same `onImported`-carrying path | unified `handleClose` | WIRED | CR-02 fix; mutation-tested by this verifier (broke → gate failed → restored → gate passed) |
| `NumberTemplateField.onEditedByHandChange` | `DeviceFormBody.inventoryNoEditedByHand` | callback prop | WIRED | Drives both `quantityDisabled` and the reset `$effect` (CR-01 fix) |
| `act_service.render_pdf` context | `act_handover.html` template | minijinja `act.number_display` | WIRED | 3 sites confirmed, no raw+suffix concatenation remains |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| acts_undo regression suite | `cargo test -p trackly-app --test acts_undo` | 6/6 passed | ✓ PASS |
| devices_csv_import (affected_place_ids + single broadcast) | `cargo test -p trackly-app --test devices_csv_import` | 15/15 passed | ✓ PASS |
| html_templates (legacy-defaults registry) | `cargo test -p trackly-app --lib html_templates` | 16/16 passed | ✓ PASS |
| html_act_render (suffix-not-lost + no-reconstruction gate) | `cargo test -p trackly-app --test html_act_render` | 23/23 passed | ✓ PASS |
| Frontend static gates incl. INV-7/INV-8 | `pnpm --dir ui lint` | full green | ✓ PASS |
| INV-8 gate liveness (mutation test) | break `handleClose()` → rerun gate → restore → rerun gate | FAIL then PASS as expected | ✓ PASS |
| No Rust changed after last green full-workspace run | `git diff --name-only 9ce60491..HEAD -- '*.rs' 'Cargo.toml'` | empty | ✓ PASS |

### Requirements Coverage

Phase declares no new REQ-IDs (`без новых REQ-ID; дефекты на стыках`). The six referenced IDs
(HST-03, PLC-06, NUM-06, NUM-08, NUM-14, NUM-16) are already marked `Complete` in
`.planning/REQUIREMENTS.md`, attributed to their originating phases (40, 39, 40.2 ×4). Phase
40.4 fixes defects at the *interfaces* of these already-complete requirements rather than
re-claiming them — consistent with the phase's own frontmatter. No orphaned requirements found.

| Requirement | Source Plan | Status | Evidence |
|-------------|-------------|--------|----------|
| HST-03 | 40.4-01 | Interface defect fixed | Truth 1 |
| PLC-06 | 40.4-02, 40.4-03 | Interface defect fixed | Truths 2, 3 |
| NUM-16 | 40.4-02 | Interface defect fixed | Truth 3 |
| NUM-06 | 40.4-04 | Interface defect fixed | Truth 4 |
| NUM-08 | 40.4-04 | Interface defect fixed | Truth 4 |
| NUM-14 | 40.4-05 | Interface defect fixed | Truth 5 |

### Anti-Patterns Found

No debt markers (`TBD`/`FIXME`/`XXX`) in any file touched by this phase (grep confirmed clean;
the one `XXX` hit in `DeviceFormBody.svelte` is the placeholder string `SN-XXXXXXXX`, not a
marker).

Two Critical findings from `40.4-REVIEW.md` were confirmed real by this verifier and are now
fixed on `main` (commits `fae6c9d4` CR-01, `02332498` CR-02) — see Truths 4 and 2 above.

14 Warning + 3 Info findings from the review remain **unfixed** on `main`. None of them
contradict the letter of the 5 roadmap Success Criteria (judged individually against each SC's
wording), so they do not block this phase's goal. Listed here for visibility, not as gaps:

- WR-01 (test vacuity, NEW-2) — discussed under Truth 5 above.
- WR-02/WR-03/WR-04 — doc-comment drift + `place_id` presence-collapse + incomplete field
  coverage in the NEW-1 regression test, all in `devices_sqlite.rs`/`acts_undo.rs`.
- WR-05/WR-06/WR-07 — `affected_place_ids` non-deterministic ordering, missing
  `number_space_broadcast_gate` registry entry, missing negative-broadcast test.
- WR-08 — bulk-create silently drops a visibly-autofilled number with no UI feedback (UX gap,
  not a regression of the SC4 contract itself).
- WR-09/WR-10/WR-11 — stale-reactivity edge case in `NumberTemplateField`, INV-7 doc-comment not
  extended, NEW-2's permanent gate is whitespace-exact.
- WR-12/WR-13 — `<title>` guard/print key mismatch (`act.number` vs `act.number_display`);
  user-customized template files are not detected/warned when they still concatenate.
- WR-14 — test-only CSV header auto-map helper diverges from the UI's `autoMapHeaders`.

These are legitimate follow-up debt. Recommend a future `/gsd-quick` or small phase to work
through them, particularly WR-12 (title/print key mismatch) and WR-13 (silent wrong-number risk
for hand-customized templates), since both touch printed legal documents.

### Human Verification Required

None outstanding. The three checkpoints that genuinely needed live observation (plans 03/04/05)
were auto-approved during execution but were then run live by the user on 2026-09-26
(`40.4-HUMAN-UAT.md`, 3/3 passed) — this satisfies the verification need; no further human
action is required for this phase's own scope.

### Gaps Summary

No gaps. All 5 roadmap Success Criteria are verified against the codebase (not against SUMMARY
claims): source-level inspection, fresh targeted `cargo test` runs (acts_undo, devices_csv_import,
html_templates, html_act_render — all green), fresh frontend static-gate runs (`pnpm --dir ui
lint`, including an independent mutation-test of the INV-8 gate this verifier performed and then
reverted), and the user's own live UAT for the three runtime-only behaviors. The two Critical
findings from code review were confirmed real and are now fixed on `main`; the 17 Warning/Info
findings are real but do not contradict any SC's literal wording and are documented above as
follow-up debt rather than phase gaps. One test-quality caveat (WR-01, NEW-2 test vacuity) is
noted against Truth 5 but does not invalidate that truth, since the underlying code change is
independently verifiable and the specific defect it fails to catch is a separately-scoped,
already-logged pre-existing issue.

---
*Verified: 2026-09-25T18:50:20Z*
*Verifier: Claude (gsd-verifier)*
