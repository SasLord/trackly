---
phase: 40.4-audit-debt-round-2
plan: 05
subsystem: printing
tags: [minijinja, html-templates, act-service, act-printing, number-formatting]

# Dependency graph
requires:
  - phase: 40.2-templates-inventory-numbers
    provides: format_act_number canonical number-formatting rule + ActDto.number, already consumed by act_number_display.rs::resolve_movement_act_number for the timeline/report
provides:
  - render_pdf's print context carries act.number_display (ActDto.number, the same canonical value the timeline and "Перемещения" report already show)
  - act_handover.html's 3 number-display sites read act.number_display instead of concatenating act.number + act.suffix
  - _legacy_defaults/v29/act_handover.html snapshot + KNOWN_LEGACY_DEFAULTS registration so untouched installs stay upgradeable
  - template_service.rs demo_context_for_kind (act branch) co-fix so the strict-undefined template-editor preview does not break
  - permanent regression gate (html_act_render.rs) against reintroducing the raw number+suffix concatenation in the active template
affects: [phase-41-АРМ-acts, any future consumer of act.number in a print/preview context]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Prefer an already-computed canonical field (ActDto.number) over reconstructing a display value from raw parts + a separately-computed suffix"
    - "Template body change → snapshot pre-change body into _legacy_defaults/vNN BEFORE editing the active file, register in KNOWN_LEGACY_DEFAULTS"
    - "Any new key referenced by a strict-undefined-rendered template MUST also be added to demo_context_for_kind or the template-editor preview breaks"

key-files:
  created:
    - crates/trackly-app/templates/_legacy_defaults/v29/act_handover.html
  modified:
    - crates/trackly-app/src/pdf/html_templates.rs
    - crates/trackly-app/src/services/act_service.rs
    - crates/trackly-app/templates/act_handover.html
    - crates/trackly-app/src/services/template_service.rs
    - ui/src/features/settings/TemplateEditor.svelte
    - crates/trackly-app/tests/html_act_render.rs

key-decisions:
  - "Added act.number_display as an ADDITIVE context key (act.number/act.number_raw/act.suffix kept unchanged) — preserves backward-compat for admin-customized file templates under D-05 (never overwritten)"
  - "compute_suffix_from_display (the fragile display.find('в') parser) was left in place, unused by the active template, since act.suffix must still be served to custom templates"

requirements-completed: ["NUM-14"]

# Metrics
duration: 39min
completed: 2026-09-24
---

# Phase 40.4 Plan 05: NEW-2 — Canonical act.number_display in print context Summary

**Печатная форма акта теперь берёт готовый `act.number_display` (=ActDto.number) из того же источника, что таймлайн и отчёт «Перемещения», вместо реконструкции из `number_raw` + хрупкого `compute_suffix_from_display` парсера.**

## Performance

- **Duration:** 39 min
- **Started:** 2026-09-24T22:32:00+07:00 (approx, first file reads)
- **Completed:** 2026-09-24T23:11:02+07:00
- **Tasks:** 3 auto + 1 checkpoint (auto-approved, see below)
- **Files modified:** 6 (5 modified + 1 new snapshot)

## Accomplishments

- `render_pdf`'s print context (`act_service.rs`) now carries `act.number_display: act.number.clone()`, additive alongside the existing `number`/`number_raw`/`suffix` keys.
- All 3 concatenation sites in `act_handover.html` (`<title>`, `.subtitle`, `.appendix-mark`) now read `{{ act.number_display }}` instead of `{{ act.number }}{{ act.suffix | default('') }}` — no longer dependent on `compute_suffix_from_display`'s literal `'в'`-anywhere-in-string fallback for arbitrary number-template masks.
- Mandatory template-versioning procedure followed correctly: `_legacy_defaults/v29/act_handover.html` snapshot captured from `git show HEAD:...` (byte-identical, verified via `diff`) and registered in `KNOWN_LEGACY_DEFAULTS` **before** the active file was edited (Task 1 committed first, Task 2 second) — `every_default_template_has_a_known_legacy_defaults_entry` stays green.
- `template_service.rs::demo_context_for_kind` (act branch) got the matching `"number_display": "42"` co-fix — `validate_preview_act_handover_returns_html_with_title_marker` (strict-undefined preview) stays green.
- `TemplateEditor.svelte`'s variable reference panel documents `act.number_display` for `act_handover`, with `act.number`/`act.suffix` descriptions clarified as raw components.
- New integration test `html_return_act_prints_number_display_with_suffix_not_lost`: creates a handover, fully returns it in one `do_return` call (`apply_to_all: true`, `sibling_return_count == 1` → suffix «в»), renders through the real `render_pdf` pipeline, and asserts the HTML contains the exact `return_act.number` (e.g. `"1в"`) — proves the return suffix is not lost.
- New permanent regression gate `active_act_handover_template_does_not_reconstruct_number_from_raw_plus_suffix`: asserts the ACTIVE template never again contains `"{{ act.number }}{{ act.suffix"`. Mutation-validated: the pre-change v29 snapshot carries this substring exactly 3 times (`grep -c` confirmed), so this test would fail against the pre-Phase-40.4 body and only passes because Task 2 actually changed the active file.

## Task Commits

Each task was committed atomically:

1. **Task 1: снепшот шаблона ДО правки + регистрация в KNOWN_LEGACY_DEFAULTS** - `280349e8` (feat)
2. **Task 2: act.number_display в контексте печати + 3 сайта шаблона + demo-контекст + справка UI** - `421dc90c` (feat)
3. **Task 3: регресс-тест на суффикс + постоянный gate против повторной сырой конкатенации** - `135f97ca` (test)

**Plan metadata:** commit pending (this SUMMARY + STATE/ROADMAP update)

## Files Created/Modified

- `crates/trackly-app/templates/_legacy_defaults/v29/act_handover.html` - byte-identical pre-Phase-40.4 snapshot of act_handover.html body
- `crates/trackly-app/src/pdf/html_templates.rs` - `KNOWN_LEGACY_DEFAULTS["act_handover.html"]` gains the v29 entry with an explaining comment
- `crates/trackly-app/src/services/act_service.rs` - `render_pdf`'s `"act"` context JSON gains `"number_display": act.number.clone()`
- `crates/trackly-app/templates/act_handover.html` - 3 sites switched to `{{ act.number_display }}`; doc-comment variable list updated
- `crates/trackly-app/src/services/template_service.rs` - `demo_context_for_kind` act branch gains `"number_display": "42"`
- `ui/src/features/settings/TemplateEditor.svelte` - `VARIABLES_BY_KIND.act_handover` documents `act.number_display`
- `crates/trackly-app/tests/html_act_render.rs` - two new tests (return-suffix proof + permanent regression gate)

## Decisions Made

- **Additive key, no removal:** `act.number`/`act.number_raw`/`act.suffix` were kept in the print context unchanged — required by D-05 (file templates are never overwritten; admins may have hand-customized copies that still read the old keys under `UndefinedBehavior::Strict`).
- **`compute_suffix_from_display` left in place, unused by the active template:** it still computes `act.suffix` for the context (backward-compat), but the active template no longer calls it via concatenation. Not removed — out of scope, and needed for custom templates.
- **Minimal doc-comment edit:** only added `act.number_display` to the context-variables list; did not restructure the surrounding comment.

## Deviations from Plan

None — plan executed exactly as written. The `v28`/`v29` numbering assumption in RESEARCH/PATTERNS was re-verified with `ls crates/trackly-app/templates/_legacy_defaults/` immediately before Task 1 (confirmed `v28` was the max, occupied by `report.html`) — no correction needed.

## Issues Encountered

- **Test-run scoping mistake (self-corrected, no code impact):** Task 1's first verification attempt used `cargo test -p trackly-app html_templates` (a bare substring filter across the whole package, not `--lib`). Because `trackly-app` has 110 integration-test binaries, this filter ran the (mostly zero-match) filter across every one of them sequentially, taking >25 minutes with no useful signal. Killed the process and re-ran scoped as `cargo test -p trackly-app --lib html_templates`, which completed in ~0.01s and gave the actual signal (16/16 passed, including `every_default_template_has_a_known_legacy_defaults_entry`). All subsequent verifications used correctly scoped commands (`--lib template_service`, `--test html_act_render`). No code changes were needed as a result — purely a wasted verification cycle, documented per the project's "SCOPE YOUR TEST RUN" guidance.

## User Setup Required

None - no external service configuration required.

## Checkpoint: Task 4 (live verification) — AUTO-APPROVED, NOT OBSERVED LIVE

Per this session's explicit instruction (project runs with `workflow.auto_advance: true`), Task 4's `checkpoint:human-verify` (live print verification in `cargo tauri dev` + LAN browser + template-editor preview) was **auto-approved without live human observation**. This is documented here plainly per the checkpoint policy, and is the same class of risk that produced BLOCKER-3 in Phase 40.3 (auto-approve without live observation missed a real defect there).

**Recommendation for the next live session:** before shipping, spot-check the 4 verification points from the plan:
1. `cargo tauri dev` → create an act, do a full return (одно «в») → print/preview the return act (desktop) → number in title/subtitle/appendix matches the timeline and "Перемещения" report exactly, suffix not lost.
2. Repeat the same return act's print in the LAN browser (server mode) — same result (per `lan_print_dom_leakage` project memory, LAN print lays out independently of the desktop path).
3. Настройки → редактор шаблонов → «Акт приёма-передачи» → confirm the preview renders without a `UndefinedError` and that `act.number_display` appears in the variables reference panel.

What automated verification DID cover (high confidence, no live observation needed): the exact HTML string containing the return act's canonical number renders correctly through the real `render_pdf` pipeline (`html_return_act_prints_number_display_with_suffix_not_lost`), the template-editor preview test passes under strict-undefined (`validate_preview_act_handover_returns_html_with_title_marker`), and the legacy-defaults upgrade path recognizes untouched installs (`every_default_template_has_a_known_legacy_defaults_entry`). The residual live-verification risk is narrower than a typical UI checkpoint — it is mostly "does this look right visually side-by-side with the timeline," which text-based tests cannot see (per `synthetic_harness_not_verification` / `act_pdf_word_fidelity` project memory).

## Next Phase Readiness

Phase 40.4 (audit-debt-round-2) now has all 5 plans (01, 02, 04, 05, and 03 if scheduled separately) closing NEW-1/N-3/NEW-3/NEW-2 from the v1.4 milestone re-audit. This plan closes NEW-2 fully at the code/test level; the only open item is the Task-4 live spot-check noted above, recommended before the next `cargo tauri dev` session touches acts/printing.

---
*Phase: 40.4-audit-debt-round-2*
*Completed: 2026-09-24*

## Self-Check: PASSED

- FOUND: crates/trackly-app/templates/_legacy_defaults/v29/act_handover.html
- FOUND: .planning/phases/40.4-audit-debt-round-2/40.4-05-SUMMARY.md
- FOUND commit 280349e8 (Task 1)
- FOUND commit 421dc90c (Task 2)
- FOUND commit 135f97ca (Task 3)
