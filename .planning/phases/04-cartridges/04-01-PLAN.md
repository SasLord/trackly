---
phase: 04-cartridges
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V016__cartridges_kind_color_settings.sql
  - crates/trackly-infra/src/test_support/test_db.rs
  - crates/trackly-app/tests/cartridges_crud.rs
  - crates/trackly-app/tests/cartridges_numbering.rs
  - crates/trackly-app/tests/cartridges_lifecycle.rs
  - crates/trackly-app/tests/cartridges_search.rs
  - crates/trackly-app/tests/cartridges_low_stock.rs
  - crates/trackly-app/tests/cartridges_history.rs
autonomous: true
requirements:
  - CART-03
  - CART-04
  - CART-05
  - CART-06
  - CART-07
  - CART-08
  - CART-09
  - CART-10
  - CART-11
  - CART-12

must_haves:
  truths:
    - "Миграция V016 выполняется без ошибок на свежей БД и на БД с существующими строками cartridge_models"
    - "PRAGMA user_version = 16 после выполнения миграций"
    - "Таблица cartridge_kinds содержит строки id=1 Картридж / id=2 Фотобарабан"
    - "app_settings содержит строку low_stock_threshold = '2'"
    - "FTS-триггеры cartridges_fts_ai / ad / au существуют и синхронизируют данные"
    - "Тестовые файлы существуют и компилируются (RED — провальные тесты до реализации)"
  artifacts:
    - path: "migrations/V016__cartridges_kind_color_settings.sql"
      provides: "DDL: cartridge_kinds lookup, ALTER cartridge_models, app_settings, FTS triggers, PRAGMA user_version=16"
      min_lines: 60
    - path: "crates/trackly-infra/src/test_support/test_db.rs"
      provides: "user_version assertion = 16"
      contains: "assert_eq!(user_version, 16)"
    - path: "crates/trackly-app/tests/cartridges_crud.rs"
      provides: "тесты create + get + soft_delete + counts"
    - path: "crates/trackly-app/tests/cartridges_numbering.rs"
      provides: "тесты авто-кода C-000001 + concurrent numbering + collision retry"
    - path: "crates/trackly-app/tests/cartridges_lifecycle.rs"
      provides: "тесты install + return_to_stock + to_refill + from_refill + write_off + audit_log"
    - path: "crates/trackly-app/tests/cartridges_search.rs"
      provides: "тесты FTS + LIKE UNION CTE search по code/model/location"
    - path: "crates/trackly-app/tests/cartridges_low_stock.rs"
      provides: "тесты low_stock query ниже порога"
    - path: "crates/trackly-app/tests/cartridges_history.rs"
      provides: "тесты audit_log history для картриджа"
  key_links:
    - from: "migrations/V016__cartridges_kind_color_settings.sql"
      to: "crates/trackly-infra/src/test_support/test_db.rs"
      via: "refinery user_version"
      pattern: "user_version.*16"
    - from: "cartridges_fts_au trigger"
      to: "cartridges_fts virtual table"
      via: "AFTER UPDATE ON cartridges"
      pattern: "cartridges_fts_au"
---

<objective>
Wave 0: Миграция V016 + обновление test_db assertion + скаффолд тестов (RED фаза TDD).

Эта волна создаёт фундамент фазы: добавляет недостающие колонки/таблицы в схему БД, исправляет
assertion теста версии схемы, и создаёт тестовые файлы со stub-имплементациями, которые компилируются
но падают на отсутствующий CartridgeService (будет создан в плане 04-03).

Purpose: Без V016 поиск по cartridges_fts работать не будет (таблица без триггеров — пустая).
Без обновления test_db.rs тесты падают сразу после применения V016.
Output: SQL-миграция + исправленный assertion + 6 тестовых файлов в RED-состоянии.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cartridges/04-CONTEXT.md
@.planning/phases/04-cartridges/04-RESEARCH.md
@.planning/phases/04-cartridges/04-PATTERNS.md
@.planning/phases/04-cartridges/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция V016 — cartridge_kinds + color + app_settings + FTS-триггеры + PRAGMA 16</name>

  <read_first>
    - migrations/V015__acts_clone_on_handover.sql (последняя существующая миграция — образец структуры, паттерн PRAGMA user_version)
    - migrations/V013__devices_fts_triggers.sql (точный образец трёх FTS-триггеров: ai/ad/au с WHEN deleted_at_utc IS NULL)
    - migrations/V012__indexes_and_fts.sql (строка с комментарием про отсутствие cartridges_fts триггеров — подтверждение задачи)
    - migrations/V005__cartridges.sql (структура cartridges таблицы: колонки code, model_id, status_id, state_id, location, holder_name, notes + deleted_at_utc)
    - migrations/V001__init_pragmas_and_lookups.sql (образец lookup-таблиц cartridge_statuses / cartridge_states — шаблон для cartridge_kinds)
    - .planning/phases/04-cartridges/04-CONTEXT.md §D-Model-Kind-01, §D-Model-Color-01, §D-LowStock-01, §D-Search-01
    - .planning/phases/04-cartridges/04-RESEARCH.md §Pattern 1 (полный SQL миграции с комментариями)
  </read_first>

  <files>migrations/V016__cartridges_kind_color_settings.sql</files>

  <action>
Создать файл migrations/V016__cartridges_kind_color_settings.sql. Структура (выполнять строго в указанном порядке, иначе FK-ошибки):

1. Создать таблицу cartridge_kinds: id INTEGER PRIMARY KEY, name TEXT NOT NULL UNIQUE. Вставить строки: (1, 'Картридж'), (2, 'Фотобарабан'). Паттерн — V001 cartridge_statuses.

2. ALTER TABLE cartridge_models ADD COLUMN kind_id INTEGER NOT NULL DEFAULT 1 REFERENCES cartridge_kinds(id). DEFAULT 1 обязателен — без него SQLite откажет при наличии существующих строк (Pitfall 2 из RESEARCH.md). ALTER TABLE cartridge_models ADD COLUMN color TEXT NULL.

3. Создать таблицу app_settings: key TEXT NOT NULL PRIMARY KEY, value TEXT NOT NULL, created_at_utc INTEGER NOT NULL, updated_at_utc INTEGER NOT NULL. Вставить строку: ('low_stock_threshold', '2', unixepoch(), unixepoch()). Это поддерживает D-LowStock-01 и CART-12.

4. Три FTS-триггера для cartridges_fts (по образцу V013 devices_fts_*). Имена: cartridges_fts_ai, cartridges_fts_ad, cartridges_fts_au. Поля FTS: code, location, holder_name (те же, что определены в V012). cartridges_fts_ai: AFTER INSERT ON cartridges, guard WHEN NEW.deleted_at_utc IS NULL, INSERT INTO cartridges_fts(rowid, code, location, holder_name). cartridges_fts_ad: AFTER DELETE ON cartridges, INSERT с первым аргументом 'delete' (FTS5 external-content protocol). cartridges_fts_au: AFTER UPDATE, сначала delete OLD, затем conditional INSERT NEW WHERE NEW.deleted_at_utc IS NULL.

5. PRAGMA user_version = 16 в самом конце файла.

КРИТИЧЕСКИ ВАЖНО: порядок — сначала cartridge_kinds, затем ALTER TABLE (FK должна ссылаться на уже существующую таблицу), затем app_settings, затем триггеры. Не использовать IF NOT EXISTS на триггерах — миграции запускаются один раз через refinery.
  </action>

  <verify>
    <automated>cargo test -p trackly-infra --test integration -- test_db_version --nocapture 2>&1 | grep -E "FAILED|passed|user_version"</automated>
  </verify>

  <done>
Файл V016 существует, синтаксически валиден (cargo check зелёный), содержит: CREATE TABLE cartridge_kinds (2 строки), ALTER TABLE cartridge_models ADD COLUMN kind_id/color, CREATE TABLE app_settings (1 строка low_stock_threshold='2'), три FTS-триггера (cartridges_fts_ai/ad/au), PRAGMA user_version = 16.
  </done>
</task>

<task type="auto">
  <name>Task 2: Обновить test_db.rs assertion 15 → 16 + создать 6 тестовых скаффолдов (RED)</name>

  <read_first>
    - crates/trackly-infra/src/test_support/test_db.rs (строка 41: assert_eq!(user_version, 15) — изменить на 16)
    - crates/trackly-app/tests/acts_crud.rs (полный образец структуры теста: make_act_service(), #[tokio::test(flavor="multi_thread")], tokio::time::timeout 30s)
    - crates/trackly-app/tests/acts_numbering.rs (образец concurrent numbering test: spawn 50 параллельных write)
    - crates/trackly-app/tests/acts_returns.rs (образец lifecycle test с audit_log проверкой)
    - crates/trackly-app/tests/acts_search.rs (образец FTS + LIKE search test)
    - crates/trackly-infra/src/test_support/mod.rs (test_writer_and_readers — публичная утилита)
    - .planning/phases/04-cartridges/04-RESEARCH.md §Wave 0 Gaps (список всех 6 тестовых файлов и их назначение)
    - .planning/phases/04-cartridges/04-PATTERNS.md §Тесты (паттерн make_cartridge_service, tokio::time::timeout)
    - .planning/phases/04-cartridges/04-VALIDATION.md §Per-Task Verification Map
  </read_first>

  <files>
    crates/trackly-infra/src/test_support/test_db.rs,
    crates/trackly-app/tests/cartridges_crud.rs,
    crates/trackly-app/tests/cartridges_numbering.rs,
    crates/trackly-app/tests/cartridges_lifecycle.rs,
    crates/trackly-app/tests/cartridges_search.rs,
    crates/trackly-app/tests/cartridges_low_stock.rs,
    crates/trackly-app/tests/cartridges_history.rs
  </files>

  <action>
1. В crates/trackly-infra/src/test_support/test_db.rs: изменить строку 41 с `assert_eq!(user_version, 15)` на `assert_eq!(user_version, 16)`. Это единственное изменение в этом файле.

2. Создать 6 тестовых файлов. Каждый скаффолд должен: (a) компилироваться, (b) иметь реальные имена тестовых функций, (c) использовать todo!() как тела — тесты будут RED (compile OK, runtime panic), но это допустимо на Wave 0. CartridgeService будет добавлен в plan 04-03, после чего тесты получат реальную реализацию.

Структура каждого файла (паттерн из acts_crud.rs):
- use trackly_infra::test_support::test_writer_and_readers
- use trackly_core::primitives::clock::SystemClock (или аналог)
- fn make_cartridge_service() с todo!() — сигнатура возвращает (CartridgeService, tempfile::TempDir)
- #[tokio::test(flavor = "multi_thread", worker_threads = 4)] на каждом тесте
- tokio::time::timeout(Duration::from_secs(30), ...) враппер

Тестовые функции по файлам:
- cartridges_crud.rs: create_cartridge_assigns_auto_code(), create_cartridge_custom_code(), get_returns_404_for_missing(), soft_delete_hides_item(), counts_by_status()
- cartridges_numbering.rs: concurrent_50_unique_codes() — spawn 50 futures через tokio::spawn, collect, assert все 50 кодов уникальны + формат C-NNNNNN; collision_retry_does_not_lose_counter()
- cartridges_lifecycle.rs: install_changes_status_to_in_use(), return_to_stock_sets_default_empty_state(), to_refill_changes_status(), from_refill_sets_default_full_state(), write_off_changes_status_to_written_off(), all_transitions_write_audit_log()
- cartridges_search.rs: search_by_code(), search_by_model_brand(), search_by_location(), empty_query_returns_all()
- cartridges_low_stock.rs: low_stock_returns_model_below_threshold(), full_stock_not_in_low_stock(), threshold_read_from_app_settings()
- cartridges_history.rs: history_returns_audit_entries_for_cartridge(), history_is_chronological()

В каждом тестовом файле: добавить `#[allow(dead_code)]` на make_cartridge_service чтобы скаффолд компилировался без предупреждений.
  </action>

  <verify>
    <automated>cargo check -p trackly-app --tests 2>&1 | grep -E "error|warning.*unused" | grep -v "todo" | head -20</automated>
  </verify>

  <done>
`crates/trackly-infra/src/test_support/test_db.rs` содержит `assert_eq!(user_version, 16)`. Все 6 тестовых файлов существуют, `cargo check -p trackly-app --tests` завершается без ошибок компиляции. `cargo test -p trackly-infra` проходит (user_version assertion теперь 16).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL файл миграции → SQLite engine | DDL исполняется в контексте refinery — SQL должен быть детерминированным, без пользовательского ввода |
| FTS trigger → cartridges table | Триггер срабатывает на мутации cartridges — должен безопасно обрабатывать NULL в holder_name/location |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | V016 migration DDL | mitigate | Миграция содержит только DDL/DML без пользовательского ввода; unixepoch() — системное время без ввода |
| T-04-01-02 | Tampering | cartridges_fts_au trigger (UPDATE path) | mitigate | Триггер работает только на UPDATE ON cartridges — мутации всегда через single-writer BEGIN IMMEDIATE |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | Plan 01 не добавляет новых зависимостей — только SQL файл и Rust тесты |
</threat_model>

<verification>
После выполнения обоих задач:
- `cargo test -p trackly-infra` зелёный (user_version assertion = 16)
- `cargo check -p trackly-app --tests` без ошибок компиляции
- `grep -c "user_version = 16" migrations/V016__cartridges_kind_color_settings.sql` == 1
- `grep -c "cartridges_fts_ai\|cartridges_fts_ad\|cartridges_fts_au" migrations/V016__cartridges_kind_color_settings.sql` == 3
- `grep -c "assert_eq!(user_version, 16)" crates/trackly-infra/src/test_support/test_db.rs` == 1
</verification>

<success_criteria>
1. Миграция V016 применяется чисто: `cargo test -p trackly-infra` зелёный.
2. test_db.rs содержит `assert_eq!(user_version, 16)` — CI не сломан.
3. Все 6 тестовых файлов компилируются: `cargo check -p trackly-app --tests` OK.
4. FTS-триггеры присутствуют: после INSERT одной строки в cartridges, `SELECT COUNT(*) FROM cartridges_fts` = 1 (проверяется в cartridges_search.rs тестах после plan 04-03).
</success_criteria>

<output>
Создать `.planning/phases/04-cartridges/04-01-SUMMARY.md` при завершении.
</output>
