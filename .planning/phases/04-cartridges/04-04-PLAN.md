---
phase: 04-cartridges
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - ui/src/features/cartridges/api.ts
  - ui/src/features/cartridges/CartridgesPage.svelte
  - ui/src/features/cartridges/CartridgesSearchAndTabs.svelte
  - ui/src/features/cartridges/CartridgesMasterDetail.svelte
  - ui/src/features/cartridges/CartridgesList.svelte
  - ui/src/features/cartridges/CartridgeListRow.svelte
  - ui/src/features/cartridges/CartridgeDetail.svelte
  - ui/src/features/cartridges/CartridgeFilters.svelte
  - ui/src/features/layout/sidebar-config.ts
autonomous: true
requirements:
  - CART-03
  - CART-04
  - CART-05
  - CART-10
  - CART-11

must_haves:
  truths:
    - "Пункт «Картриджи» в sidebar работает и открывает /cartridges"
    - "Switch-bar отображает 5 вкладок (Все/На складе/В работе/На заправке/Списано) со счётчиками"
    - "Список картриджей показывает строки с кодом C-XXXXXX, бренд+модель, статус-badge, расположение, kebab ⋮"
    - "Кликнув строку — детальная панель показывает поля картриджа + секцию истории перемещений"
    - "Поиск с debounce 250ms фильтрует список по коду/модели/расположению"
    - "Фильтры по типу (Картридж/Фотобарабан) и модели работают совместно со switch-bar"
    - "Пустые состояния отображаются согласно UI-SPEC §Пустые состояния"
  artifacts:
    - path: "ui/src/features/cartridges/api.ts"
      provides: "cartridges API объект с list/get/create/update/delete/transition/statusCounts/getHistory/lowStock/search/models*/suggest*"
    - path: "ui/src/features/cartridges/CartridgesPage.svelte"
      provides: "Корневой компонент раздела с двумя табами"
    - path: "ui/src/features/cartridges/CartridgeDetail.svelte"
      provides: "Детальная панель с полями + historySec"
    - path: "ui/src/features/layout/sidebar-config.ts"
      provides: "Placeholder /cartridges заменён реальным route phase:4"
  key_links:
    - from: "ui/src/features/cartridges/CartridgesPage.svelte"
      to: "ui/src/features/cartridges/api.ts"
      via: "import { cartridges } from './api'"
      pattern: "cartridges\\.statusCounts"
    - from: "ui/src/features/cartridges/CartridgeDetail.svelte"
      to: "ui/src/features/cartridges/api.ts"
      via: "cartridges.getHistory(id)"
      pattern: "getHistory"
---

<objective>
Wave 3 (план A из двух параллельных): Основной UI — страница, список, детали, фильтры, sidebar.

Создаём скелет раздела «Картриджи»: страницу с двумя табами (Картриджи / Модели), master-detail
для экземпляров, switch-bar статусов + дополнительные фильтры, строки списка, детальную панель
с историей перемещений. CRUD-модалки и lifecycle-модалки создаются в плане 04-05 параллельно.

Purpose: После этого плана пользователь видит раздел «Картриджи» с работающим списком и фильтрами.
Output: 8 Svelte-компонентов + api.ts + обновлённый sidebar-config.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cartridges/04-CONTEXT.md
@.planning/phases/04-cartridges/04-UI-SPEC.md
@.planning/phases/04-cartridges/04-PATTERNS.md

<interfaces>
Из ui/src/lib/api/acts.ts — образец apiCall wrapper:
- import { apiCall } from '$lib/api/client'
- export const acts = { list: (filter, pagination) => apiCall<ActListResponse>('acts_list', {filter, pagination}), ... }

Из ui/src/features/acts/ActsPage.svelte — образец страницы:
- let activeTab = $state<'acts' | 'returns' | 'archive'>('acts')
- CartridgesPage: activeTab = 'cartridges' | 'models', default 'cartridges'

Из ui/src/features/acts/ActsMasterDetail.svelte — образец master-detail:
- grid-template-columns: 35% 65%, gap: var(--space-md)
- {#snippet master()} / {#snippet detail()}

Из ui/src/features/acts/ActDetail.svelte — образец детальной панели:
- aria-live="polite" на корневом div
- Секция с heading + fields + history

Из ui/src/features/acts/ActsSearchAndTabs.svelte — образец поиска + табов:
- debounce 250ms через setTimeout
- role="tablist" / role="tab" aria-selected на кнопках вкладок

Из ui/src/features/devices/DeviceFilters.svelte — образец switch-bar:
- STATUSES = [{ id: null, label: 'Все' }, { id: 1, label: 'На складе' }, ...]
- .status-tab.active { color: var(--color-accent); border-bottom-color: var(--color-accent); font-weight: var(--font-weight-medium); }
- .count-badge.count-active { background: color-mix(...); color: var(--color-accent); }

Из ui/src/features/layout/sidebar-config.ts — placeholder:
- { path: '/cartridges', label: 'Картриджи', icon: '...', phase: 4 } → убрать phase: 4 (или оставить если конвенция другая — прочитать файл)

TypeScript типы из ui/src/bindings.ts (после плана 04-03):
- CartridgeDto: { id: number, code: string, model_id: number, model_brand: string | null, model_name: string | null, model_kind_id: number | null, status_id: number, status_name: string | null, state_id: number | null, state_name: string | null, location: string | null, holder_name: string | null, notes: string | null, created_at_utc: number, updated_at_utc: number }
- CartridgeCountsDto: { all, in_stock, in_use, at_refill, written_off: number }
- AuditEntryDto: { action: string, payload_json: string | null, created_at_utc: number }
- CartridgeFilter: { status_id: number | null, kind_id: number | null, model_id: number | null, search: string | null }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts + CartridgesPage + CartridgesSearchAndTabs + CartridgesMasterDetail + sidebar</name>

  <read_first>
    - ui/src/lib/api/acts.ts (ПОЛНЫЙ файл — паттерн apiCall wrapper для api.ts)
    - ui/src/features/acts/ActsPage.svelte (ПОЛНЫЙ файл — паттерн страницы с табами, $state activeTab)
    - ui/src/features/acts/ActsSearchAndTabs.svelte (ПОЛНЫЙ файл — поиск + таб-переключатели)
    - ui/src/features/acts/ActsMasterDetail.svelte (ПОЛНЫЙ файл — 35/65 grid со snippets)
    - ui/src/features/layout/sidebar-config.ts (прочитать всё — найти placeholder cartridges)
    - .planning/phases/04-cartridges/04-UI-SPEC.md §Навигация и заголовки, §Switch-bar статусов, §Дополнительные фильтры, §CartridgesPage, §CartridgesSearchAndTabs, §CartridgesMasterDetail
    - .planning/phases/04-cartridges/04-PATTERNS.md §api.ts, §CartridgesPage.svelte, §CartridgesSearchAndTabs.svelte, §CartridgesMasterDetail.svelte
  </read_first>

  <files>
    ui/src/features/cartridges/api.ts,
    ui/src/features/cartridges/CartridgesPage.svelte,
    ui/src/features/cartridges/CartridgesSearchAndTabs.svelte,
    ui/src/features/cartridges/CartridgesMasterDetail.svelte,
    ui/src/features/layout/sidebar-config.ts
  </files>

  <action>
1. Создать ui/src/features/cartridges/api.ts по образцу acts.ts:
   - import { apiCall } from '$lib/api/client'; import type { ... } from '../../bindings'
   - export const cartridges = { list, get, create, update, delete_, transition, statusCounts, getHistory, lowStock, search, modelsList, modelsGet, modelsCreate, modelsUpdate, modelsDelete, suggestBrand, suggestModel, suggestCompatPrinter, suggestLocation }
   - Каждая функция вызывает apiCall<T>('command_name', params)
   - cartridges_status_counts команда — без параметров
   - cartridges_get_history — { id: number }

2. Создать ui/src/features/cartridges/CartridgesPage.svelte:
   - $props(): {} (нет внешних props)
   - let activeTab = $state<'cartridges' | 'models'>('cartridges')
   - $effect: при смене activeTab сбрасывать selectedCartridgeId
   - Рендер: CartridgesSearchAndTabs (tabs + search wiring) + {#if activeTab==='cartridges'} LowStockBanner + CartridgesMasterDetail {:else} ModelsList {/if}
   - Передавать onTabChange callback в CartridgesSearchAndTabs
   - Заголовок страницы (h1): «Картриджи»

3. Создать ui/src/features/cartridges/CartridgesSearchAndTabs.svelte:
   - props: activeTab, onTabChange, onSearchChange, onStatusChange, onKindChange, onModelChange, counts: CartridgeCountsDto
   - Two tabs: кнопки с role="tab" aria-selected — «Картриджи» (activeTab==='cartridges') / «Модели» (activeTab==='models')
   - Search input: placeholder «Поиск по коду, модели, расположению», debounce 250ms, вызывает onSearchChange
   - Поиск виден только когда activeTab === 'cartridges'
   - Структура: отдельная секция для switch-bar + фильтров (только на вкладке Картриджи)
   - CartridgeFilters передаётся как дочерний компонент ниже

4. Создать ui/src/features/cartridges/CartridgesMasterDetail.svelte:
   - Копировать структуру ActsMasterDetail.svelte ТОЧНО: grid 35/65, gap var(--space-md), breakpoint <1100px
   - {#snippet master()} и {#snippet detail()} как в Acts
   - Стили: .master { background: var(--color-surface) }, .detail { background: var(--color-bg) }, border/radius/overflow

5. В ui/src/features/layout/sidebar-config.ts:
   - Найти placeholder `/cartridges` (скорее всего содержит `phase: 4` или заглушку без компонента)
   - Заменить на реальный route с импортом CartridgesPage.svelte (или добавить component: CartridgesPage по паттерну существующих разделов)
   - Прочитать файл ПЕРЕД изменением — понять существующую структуру объекта
  </action>

  <verify>
    <automated>pnpm --prefix /Users/madsas/Projects/trackly/ui svelte-check 2>&1 | grep -E "^Error" | head -10</automated>
  </verify>

  <done>
pnpm svelte-check без ошибок на новых файлах. sidebar-config.ts содержит рабочий route /cartridges с компонентом CartridgesPage.
  </done>
</task>

<task type="auto">
  <name>Task 2: CartridgeFilters + CartridgesList + CartridgeListRow + CartridgeDetail</name>

  <read_first>
    - ui/src/features/devices/DeviceFilters.svelte (ПОЛНЫЙ файл — switch-bar паттерн со счётчиками: .status-tab / .count-badge / STATUSES массив)
    - ui/src/features/acts/ActsList.svelte (ПОЛНЫЙ файл — список с loading/empty states + pagination footer)
    - ui/src/features/acts/ActListRow.svelte (ПОЛНЫЙ файл — строка списка: .row/.top/.bottom, role=button, keydown handler, Badge)
    - ui/src/features/acts/ActDetail.svelte (ПОЛНЫЙ файл — детальная панель: header + sections + кнопки size="sm")
    - ui/src/lib/components/Badge.svelte (варианты: success/accent/warning/default)
    - ui/src/lib/components/Spinner.svelte (загрузка)
    - .planning/phases/04-cartridges/04-UI-SPEC.md §Badge-цвета статусов, §CartridgeDetail, §Пустые состояния, §Accessibility
    - .planning/phases/04-cartridges/04-PATTERNS.md §CartridgeFilters.svelte, §CartridgesList.svelte, §CartridgeListRow.svelte, §CartridgeDetail.svelte
  </read_first>

  <files>
    ui/src/features/cartridges/CartridgeFilters.svelte,
    ui/src/features/cartridges/CartridgesList.svelte,
    ui/src/features/cartridges/CartridgeListRow.svelte,
    ui/src/features/cartridges/CartridgeDetail.svelte
  </files>

  <action>
1. Создать ui/src/features/cartridges/CartridgeFilters.svelte по образцу DeviceFilters.svelte:
   - props: statusId (number | null), kindId (number | null), modelId (number | null), counts: CartridgeCountsDto, onStatusChange, onKindChange, onModelChange, models: CartridgeModelDto[]
   - STATUSES = [{id: null, label:'Все'}, {id:1, label:'На складе'}, {id:2, label:'В работе'}, {id:3, label:'На заправке'}, {id:4, label:'Списано'}]
   - Счётчики из CartridgeCountsDto: null→all, 1→in_stock, 2→in_use, 3→at_refill, 4→written_off
   - Дополнительные фильтры — строка ниже switch-bar: Select «Тип» (null/1 Картридж/2 Фотобарабан) + Select «Модель» (из models prop)
   - CSS паттерн из DeviceFilters.svelte: .status-tab.active/.count-badge.count-active с var(--color-accent)

2. Создать ui/src/features/cartridges/CartridgesList.svelte по образцу ActsList.svelte:
   - props: items: CartridgeDto[], selectedId: number | null, loading: boolean, onSelect, onMenuAction (будет проброшен в CartridgeContextMenu в плане 04-05)
   - Пустые состояния (из UI-SPEC §Пустые состояния):
     - items пусто + нет фильтра: «Картриджей пока нет» / «Добавьте первый картридж...» / «+ Добавить картридж» кнопка
     - items пусто + есть фильтр: «Ничего не найдено» / «Попробуйте изменить фильтры...»
   - {#each items as c (c.id)} <CartridgeListRow ... />

3. Создать ui/src/features/cartridges/CartridgeListRow.svelte по образцу ActListRow.svelte:
   - props: cartridge: CartridgeDto, selected: boolean, onSelect: () => void, onMenuAction: (op, cartridge) => void
   - Структура: div.row class:selected role="button" tabindex="0" aria-pressed={selected} onclick onkeydown (Enter/Space)
   - .top: код с font-variant-numeric: tabular-nums, span с brand+model («{model_brand} {model_name}»), Badge
   - Badge variant по status_id: 1→'success', 2→'accent', 3→'warning', 4→'default'
   - .bottom: location ?? '—'
   - kebab ⋮: button aria-label="Действия с картриджем {code}" шириной 28px — CartridgeContextMenu будет добавлен в план 04-05; сейчас kebab рендерит простую кнопку без действий (заглушка, совместимая с 04-05)
   - Строка высотой --row-height (40px)

4. Создать ui/src/features/cartridges/CartridgeDetail.svelte по образцу ActDetail.svelte:
   - props: cartridge: CartridgeDto | null, history: AuditEntryDto[], loading: boolean
   - Если cartridge === null: пустое состояние «Выберите картридж» (текст из UI-SPEC)
   - Header: h2.detail-title с кодом (font-variant-numeric: tabular-nums, --font-size-display), badge статуса, model_brand + model_name
   - Секция «Информация»: расположение, holder_name (только если status_id=2 «В работе»), state_name, notes
   - Секция «История перемещений»: h3 + {#each history} строка высотой --row-height-dense (32px)
     - Формат строки: parseAuditEntry(entry) → «{дата} — {операция_label}; {подробности}» (дата из entry.created_at_utc через formatDate utc→display)
     - action→label маппинг: "create"/"custom:cartridge_code_override"→"Создан", "custom:install"→"Установлен в принтер", "custom:return_to_stock"→"Возвращён на склад", "custom:to_refill"→"Отправлен на заправку", "custom:from_refill"→"Получен с заправки", "custom:write_off"→"Списан", "update"→"Изменён", "delete"→"Удалён"
   - Пустая история: inline текст «История пуста» (не пустой экран)
   - aria-live="polite" на корневом div
   - Кнопки действий (size="sm") в header зависят от status_id — заглушки без onclick пока (actions wire-up в 04-05)
  </action>

  <verify>
    <automated>pnpm --prefix /Users/madsas/Projects/trackly/ui svelte-check 2>&1 | grep -E "^Error" | head -10</automated>
  </verify>

  <done>
pnpm svelte-check без ошибок. CartridgesList корректно рендерит CartridgeListRow с Badge по статусу. CartridgeDetail показывает историю из AuditEntryDto. Пустые состояния реализованы согласно UI-SPEC.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Пользовательский ввод поиска → apiCall → backend | Строка поиска передаётся через apiCall; backend выполняет parameterized SQL |
| AuditEntryDto.payload_json отображается в CartridgeDetail | JSON-строка из БД; рендерится как text, не как HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | Строка поиска в CartridgesSearchAndTabs | mitigate | Debounce 250ms; поисковая строка передаётся как текстовый параметр через apiCall — экранирование на backend в parameterized SQL |
| T-04-04-02 | Information Disclosure | AuditEntryDto в CartridgeDetail | accept | Phase 4: нет auth; данные только для авторизованных пользователей (Phase 5 добавит сессии); payload_json рендерится как text (не innerHTML) |
| T-04-04-03 | Tampering | sidebar-config route /cartridges | accept | Роут заменяет placeholder — никакого пользовательского ввода в path |
| T-04-04-SC | Tampering | npm/pip installs | accept | Нет новых зависимостей |
</threat_model>

<verification>
После выполнения обоих задач:
- `pnpm svelte-check` зелёный (без ошибок на всех новых файлах)
- `pnpm lint` зелёный
- Sidebar-config содержит работающий route /cartridges с CartridgesPage
- CartridgeListRow рендерит Badge variant "success" для status_id=1, "accent" для 2, "warning" для 3, "default" для 4
- CartridgeDetail рендерит историю с форматом «дата — метка» из AuditEntryDto.action
</verification>

<success_criteria>
1. `pnpm svelte-check && pnpm lint` зелёные.
2. Пункт «Картриджи» в sidebar открывает CartridgesPage с двумя табами.
3. Switch-bar отображает 5 вкладок со счётчиками из CartridgeCountsDto.
4. CartridgeListRow отображает Badge с правильным цветом по status_id.
5. CartridgeDetail отображает пустое состояние «Выберите картридж» и историю перемещений с human-readable форматом.
</success_criteria>

<output>
Создать `.planning/phases/04-cartridges/04-04-SUMMARY.md` при завершении.
</output>
