---
phase: 04-cartridges
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - crates/trackly-app/src/dto/cartridge.rs
  - crates/trackly-app/src/dto/mod.rs
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-app/src/services/mod.rs
  - crates/trackly-app/src/tauri_cmds/cartridges.rs
  - crates/trackly-app/src/tauri_cmds/mod.rs
  - crates/trackly-app/src/http/cartridges.rs
  - crates/trackly-app/src/http/mod.rs
  - crates/trackly-app/src/context.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/tests/cartridges_crud.rs
  - crates/trackly-app/tests/cartridges_numbering.rs
  - crates/trackly-app/tests/cartridges_lifecycle.rs
  - crates/trackly-app/tests/cartridges_search.rs
  - crates/trackly-app/tests/cartridges_low_stock.rs
  - crates/trackly-app/tests/cartridges_history.rs
autonomous: true
requirements:
  - CART-01
  - CART-02
  - CART-03
  - CART-04
  - CART-05
  - CART-06
  - CART-07
  - CART-08
  - CART-09
  - CART-10
  - CART-11
  - CART-12

must_haves:
  truths:
    - "CartridgeService.create() атомарно создаёт картридж с авто-кодом или custom-кодом в single-writer tx"
    - "CartridgeService.transition() атомарно меняет статус + пишет audit_log в одной writer-tx"
    - "CartridgeService.low_stock() возвращает модели ниже порога из app_settings"
    - "Tauri commands cartridges_list/get/create/update/delete/transition/search/status_counts/get_history/low_stock + models CRUD + suggest_* зарегистрированы в specta_export"
    - "AppCtx содержит поле cartridges: Arc<CartridgeService>"
    - "HTTP router http::cartridges::router() построен (не bind'ится до Phase 5)"
    - "Все 6 интеграционных тестов проходят (GREEN)"
  artifacts:
    - path: "crates/trackly-app/src/dto/cartridge.rs"
      provides: "CartridgeDto, CartridgeModelDto, CartridgeTransitionPayload, CartridgeCreateDto, CartridgeModelCreateDto, LowStockItemDto, AuditEntryDto, CartridgeFilter, CartridgeListResponse, CartridgeCountsDto"
    - path: "crates/trackly-app/src/services/cartridge_service.rs"
      provides: "CartridgeService с методами create/update/delete/get/list/transition/search/status_counts/get_history/low_stock/model_create/model_update/model_delete/model_list/suggest_*"
      min_lines: 250
    - path: "crates/trackly-app/src/tauri_cmds/cartridges.rs"
      provides: "build_* helpers + thin #[tauri::command] #[specta::specta] wrappers"
    - path: "crates/trackly-app/src/http/cartridges.rs"
      provides: "axum router() с /api/v1/cartridges_* routes"
  key_links:
    - from: "crates/trackly-app/src/specta_export.rs"
      to: "crates/trackly-app/src/tauri_cmds/cartridges.rs"
      via: "collect_commands! macro"
      pattern: "cartridges_list"
    - from: "crates/trackly-app/src/context.rs"
      to: "crates/trackly-app/src/services/cartridge_service.rs"
      via: "pub cartridges: Arc<CartridgeService>"
      pattern: "Arc<CartridgeService>"
---

<objective>
Wave 2: App-слой — DTO + CartridgeService + Tauri commands + HTTP router + AppCtx wire-up + GREEN тесты.

Эта волна превращает domain/infra-слои плана 04-02 в рабочий backend.
CartridgeService оркестрирует single-writer writes, CartridgeRepository reads, audit_log inserts.
Tauri commands и axum handlers — тонкие адаптеры над build_* helpers.
После этого плана все интеграционные тесты из плана 04-01 должны быть GREEN.

Purpose: После выполнения этого плана backend картриджей полностью функционален;
следующие планы (04-04, 04-05, 04-06) добавляют только UI.
Output: Полный app-слой картриджей + зелёные тесты + регенерированные bindings.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cartridges/04-CONTEXT.md
@.planning/phases/04-cartridges/04-RESEARCH.md
@.planning/phases/04-cartridges/04-PATTERNS.md

<interfaces>
Из crates/trackly-app/src/dto/act.rs — образец DTO:
- #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Type)]
- #[specta(type = i32)] на всех i64 полях — TS получает number, не bigint
- CartridgeTransitionPayload: #[serde(tag = "op")] enum

Из crates/trackly-app/src/services/act_service.rs — образец сервиса:
- pub struct ActService { writer: Arc<WriterHandle>, readers: Arc<ReaderPool>, clock: Arc<dyn Clock+Send+Sync>, repo: Arc<...>, audit_repo: Arc<...> }
- pub fn new(...) -> Self
- pub async fn create(...) — через self.writer.execute(move |conn| { tx = conn.transaction(); ... tx.commit(); Ok(id) }).await?

Из crates/trackly-app/src/tauri_cmds/acts.rs — образец команд:
- pub async fn build_acts_list(ctx: &AppCtx, ...) -> Result<...>
- #[tauri::command] #[specta::specta] pub async fn acts_list(state: tauri::State<'_, AppCtx>, ...) -> Result<...>
- ПОРЯДОК ОБЯЗАТЕЛЕН: #[tauri::command] ПЕРЕД #[specta::specta]

Из crates/trackly-app/src/http/acts.rs — образец HTTP handler:
- axum::extract::State<AppCtx>, axum::Json
- pub fn router() -> Router<AppCtx> с .route("/api/v1/...", post(handler))
- Роутер СТРОИТСЯ, но НЕ bind'ится (Phase 5/8)

Из crates/trackly-app/src/context.rs (строки 58-74):
- pub devices: Arc<DeviceService>
- pub acts: Arc<ActService>
- Добавить: pub cartridges: Arc<CartridgeService>

Из crates/trackly-app/src/specta_export.rs:
- collect_commands![ ..existing.., crate::tauri_cmds::cartridges::cartridges_list, ... ]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DTO + CartridgeService + AppCtx wire-up</name>

  <read_first>
    - crates/trackly-app/src/dto/act.rs (ПОЛНЫЙ файл — шаблон для CartridgeDto, CartridgeTransitionPayload с #[serde(tag="op")])
    - crates/trackly-app/src/dto/device.rs (шаблон #[specta(type = i32)] на i64 полях)
    - crates/trackly-app/src/services/act_service.rs (ПОЛНЫЙ файл — шаблон CartridgeService: new(), create(), transition(), list(), get(), delete() и validate_*)
    - crates/trackly-app/src/services/mod.rs (добавить pub mod cartridge_service + pub use)
    - crates/trackly-app/src/dto/mod.rs (добавить pub mod cartridge)
    - crates/trackly-app/src/context.rs (ПОЛНЫЙ файл — добавить cartridges: Arc<CartridgeService> в struct + build())
    - .planning/phases/04-cartridges/04-PATTERNS.md §dto/cartridge.rs, §services/cartridge_service.rs, §context.rs
    - .planning/phases/04-cartridges/04-CONTEXT.md §D-Op-Fields-01, §D-History-01, §D-LowStock-02
    - .planning/phases/04-cartridges/04-RESEARCH.md §Pattern 3 (transition), §Pattern 4 (low_stock)
  </read_first>

  <behavior>
    - CartridgeDto содержит все поля CartridgeRow с #[specta(type = i32)] на id/version/model_id/status_id + Option<i32> на model_kind_id/state_id + timestamp поля
    - CartridgeTransitionPayload — #[serde(tag = "op")] enum с вариантами Install/ReturnToStock/ToRefill/FromRefill/WriteOff; каждый содержит cartridge_id и version
    - CartridgeService.create(payload: CartridgeCreateDto): AppError::Validation если model_id = 0; writer.execute: assign_code_in_tx → INSERT → audit_log "create" или "custom:cartridge_code_override"
    - CartridgeService.transition(payload: CartridgeTransitionPayload): writer.execute: fetch → transition_in_tx (включая audit_log); AppError::OptimisticLockMismatch если version mismatch
    - CartridgeService.low_stock(): readers.acquire() → cartridges_sqlite::low_stock()
    - CartridgeService.model_delete(id, version): сначала count живых экземпляров, при >0 → AppError::Conflict "Нельзя удалить модель: она используется N картриджами" (per CONTEXT §Claude's Discretion)
    - CartridgeService.suggest_brand(prefix): DISTINCT brand FROM cartridge_models WHERE brand LIKE ?% ORDER BY brand LIMIT 20
    - CartridgeService.suggest_model(brand, prefix): DISTINCT model FROM cartridge_models WHERE brand=? AND model LIKE ?%
    - CartridgeService.suggest_compat_printer(field, prefix): DISTINCT printer_brand/printer_model FROM cartridge_model_compatibility WHERE ?
  </behavior>

  <files>
    crates/trackly-app/src/dto/cartridge.rs,
    crates/trackly-app/src/dto/mod.rs,
    crates/trackly-app/src/services/cartridge_service.rs,
    crates/trackly-app/src/services/mod.rs,
    crates/trackly-app/src/context.rs
  </files>

  <action>
1. Создать crates/trackly-app/src/dto/cartridge.rs:
   - CartridgeDto (из CartridgeRow, все i64 → #[specta(type=i32)])
   - CartridgeModelDto (из CartridgeModelRow)
   - CartridgeCreateDto { model_id: i64, code_override: Option<String>, state_id: Option<i64>, location: Option<String>, notes: Option<String> }
   - CartridgeModelCreateDto { brand: String, model: String, kind_id: i64, color: Option<String>, notes: Option<String>, compatibility: Vec<(String,String)> }
   - CartridgeModelPatchDto (те же поля + compatibility)
   - CartridgeTransitionPayload { #[serde(tag = "op")] enum: Install{cartridge_id, version, date_utc, given_by_name, given_to_name, location}, ReturnToStock{cartridge_id, version, state_id, location, notes}, ToRefill{...}, FromRefill{...}, WriteOff{cartridge_id, version, date_utc, notes} }
   - CartridgeFilter { status_id: Option<i32>, kind_id: Option<i32>, model_id: Option<i32>, search: Option<String> }
   - CartridgeListResponse { items: Vec<CartridgeDto>, total: i64 }
   - CartridgeCountsDto { all, in_stock, in_use, at_refill, written_off: i32 }
   - LowStockItemDto { model_id: i32, brand: String, model: String, count: i32, threshold: i32 }
   - AuditEntryDto { action: String, payload_json: Option<String>, before_json: Option<String>, after_json: Option<String>, created_at_utc: i32 }
   - Pagination (re-export или newtype из domain)
   Добавить `pub mod cartridge;` в dto/mod.rs.

2. Создать crates/trackly-app/src/services/cartridge_service.rs по образцу act_service.rs:
   - pub struct CartridgeService { writer, readers, clock, cart_repo: Arc<SqliteCartridgeRepository>, audit_repo: Arc<SqliteAuditLogRepository> }
   - pub fn new(writer, readers, clock) -> Self
   - Методы: create, update, delete, get, list, transition, search, status_counts, get_history, low_stock, model_list, model_get, model_create, model_update, model_delete, suggest_brand, suggest_model, suggest_compat_printer, suggest_location
   - В create: валидация model_id > 0; writer.execute; assign_code_in_tx; INSERT OR IGNORE locations; INSERT cartridges; audit_log
   - В transition: reader fetch_full_in_tx через writer.execute; transition_in_tx; возвращает обновлённый CartridgeDto
   - В model_delete: сначала count живых экземпляров через readers; при >0 AppError::Conflict
   - suggest_location: SELECT DISTINCT name FROM locations WHERE deleted_at_utc IS NULL ORDER BY name LIMIT 20 (общий справочник)
   Добавить `pub mod cartridge_service; pub use cartridge_service::CartridgeService;` в services/mod.rs.

3. В crates/trackly-app/src/context.rs:
   - Добавить в use: `use crate::services::CartridgeService;`
   - Добавить поле в AppCtx struct: `pub cartridges: Arc<CartridgeService>,`
   - В AppCtx::build: после создания `acts =` добавить: `let cartridges = Arc::new(CartridgeService::new(writer.clone(), readers.clone(), clock.clone()));`
   - В Self { ... } добавить: `cartridges,`
  </action>

  <verify>
    <automated>cargo check -p trackly-app 2>&1 | grep -c "^error"</automated>
  </verify>

  <done>
`cargo check -p trackly-app` возвращает 0 ошибок. CartridgeService, CartridgeDto, CartridgeTransitionPayload публично доступны. AppCtx содержит поле cartridges.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tauri commands + HTTP router + specta registration + GREEN тесты</name>

  <read_first>
    - crates/trackly-app/src/tauri_cmds/acts.rs (ПОЛНЫЙ файл — образец build_* helpers + thin commands; порядок атрибутов #[tauri::command] ПЕРЕД #[specta::specta])
    - crates/trackly-app/src/http/acts.rs (образец router() → Router<AppCtx> с POST routes)
    - crates/trackly-app/src/http/mod.rs (как зарегистрировать новый router)
    - crates/trackly-app/src/tauri_cmds/mod.rs (как добавить новый модуль команд)
    - crates/trackly-app/src/specta_export.rs (ПОЛНЫЙ файл — расширить collect_commands!)
    - crates/trackly-app/tests/acts_crud.rs (образец теста с make_act_service + tokio timeout)
    - crates/trackly-app/tests/acts_numbering.rs (образец concurrent test 50 tasks)
    - .planning/phases/04-cartridges/04-PATTERNS.md §tauri_cmds/cartridges.rs, §http/cartridges.rs, §specta_export.rs, §Тесты
    - .planning/phases/04-cartridges/04-RESEARCH.md §Pattern 7 (specta registration list)
  </read_first>

  <behavior>
    - build_cartridges_list(ctx, filter, pagination) -> CartridgeListResponse
    - build_cartridges_transition(ctx, payload: CartridgeTransitionPayload) -> CartridgeDto
    - build_cartridges_low_stock(ctx) -> Vec<LowStockItemDto>
    - build_cartridges_get_history(ctx, id) -> Vec<AuditEntryDto>
    - build_cartridge_models_create/update/delete — делегируют ctx.cartridges
    - HTTP router строится: .route("/api/v1/cartridges_list", post(handler_list)) + все остальные routes из 04-PATTERNS.md §http/cartridges.rs
    - specta collect_commands! содержит: cartridges_list, cartridges_get, cartridges_create, cartridges_update, cartridges_delete, cartridges_transition, cartridges_search, cartridges_status_counts, cartridges_get_history, cartridges_low_stock, cartridge_models_list, cartridge_models_get, cartridge_models_create, cartridge_models_update, cartridge_models_delete, cartridges_suggest_brand, cartridges_suggest_model, cartridges_suggest_compat_printer, cartridges_suggest_location
    - Тест create_cartridge_assigns_auto_code: CartridgeService.create с code_override=None → code начинается с "C-", уникален
    - Тест concurrent_50_unique_codes: spawn 50 создающих задач → все коды уникальны + формат C-[0-9]{6}
    - Тест install_changes_status_to_in_use: create → transition(Install{..}) → get().status_id == 2
    - Тест all_transitions_write_audit_log: transition → get_history().len() > 0 И action содержит "custom:"
    - Тест low_stock_returns_model_below_threshold: создать модель + 0 картриджей на складе → low_stock() возвращает эту модель
    - Тест history_returns_audit_entries_for_cartridge: create + transition → get_history() не пустой, первая запись newer
  </behavior>

  <files>
    crates/trackly-app/src/tauri_cmds/cartridges.rs,
    crates/trackly-app/src/tauri_cmds/mod.rs,
    crates/trackly-app/src/http/cartridges.rs,
    crates/trackly-app/src/http/mod.rs,
    crates/trackly-app/src/specta_export.rs,
    crates/trackly-app/tests/cartridges_crud.rs,
    crates/trackly-app/tests/cartridges_numbering.rs,
    crates/trackly-app/tests/cartridges_lifecycle.rs,
    crates/trackly-app/tests/cartridges_search.rs,
    crates/trackly-app/tests/cartridges_low_stock.rs,
    crates/trackly-app/tests/cartridges_history.rs
  </files>

  <action>
1. Создать crates/trackly-app/src/tauri_cmds/cartridges.rs:
   - Паттерн из acts.rs: build_* pub async fn (принимает &AppCtx, делегирует в ctx.cartridges.*) + thin tauri commands
   - Каждая #[tauri::command] + #[specta::specta] функция принимает state: tauri::State<'_, AppCtx>
   - ПОРЯДОК: #[tauri::command] ОБЯЗАТЕЛЬНО перед #[specta::specta]
   - Команды: cartridges_list, cartridges_get, cartridges_create, cartridges_update, cartridges_delete, cartridges_transition, cartridges_search, cartridges_status_counts, cartridges_get_history, cartridges_low_stock, cartridge_models_list, cartridge_models_get, cartridge_models_create, cartridge_models_update, cartridge_models_delete, cartridges_suggest_brand, cartridges_suggest_model, cartridges_suggest_compat_printer, cartridges_suggest_location
   Добавить `pub mod cartridges;` в tauri_cmds/mod.rs.

2. Создать crates/trackly-app/src/http/cartridges.rs:
   - Паттерн из http/acts.rs: handler_* async fn + pub fn router() -> Router<AppCtx>
   - Каждый handler вызывает соответствующий build_* helper из tauri_cmds/cartridges
   - router() строит /api/v1/cartridges_* routes через POST
   - Роутер ТОЛЬКО строится — НЕ bind'ится (заглушка до Phase 5, как http/acts.rs)
   Добавить `pub mod cartridges;` в http/mod.rs.

3. В crates/trackly-app/src/specta_export.rs — расширить collect_commands! добавив все 19 cartridges_* команд из 04-PATTERNS.md §Pattern 7 (после acts_* команд, с комментарием // Phase 4 — Cartridges).

4. Обновить тестовые файлы из плана 04-01 (заменить todo!() на реальные реализации):
   - make_cartridge_service(): let (writer, readers, dir) = test_writer_and_readers(); let clock = Arc::new(SystemClock); let svc = CartridgeService::new(writer, readers, clock); (svc, dir)
   - Реализовать все тесты по их описанию (behavior block выше)
   - Тест concurrent_50_unique_codes: tokio::spawn 50 задач, каждая вызывает svc.create(CartridgeCreateDto{model_id: model.id, code_override: None, ...}), collect results, assert uniqueness + format check

5. Запустить `cargo test -p trackly-app` для проверки что все тесты проходят.

6. Запустить `cargo test -p trackly-app --test export_bindings` чтобы регенерировать ui/src/bindings.ts с новыми cartridges_* типами.
  </action>

  <verify>
    <automated>cargo test -p trackly-app 2>&1 | tail -20</automated>
  </verify>

  <done>
`cargo test -p trackly-app` зелёный (все тесты проходят включая cartridges_crud, cartridges_numbering, cartridges_lifecycle, cartridges_search, cartridges_low_stock, cartridges_history). `ui/src/bindings.ts` содержит cartridges_list, cartridges_transition и другие cartridges_* типы.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri invoke (frontend) → AppCtx → CartridgeService | Входные параметры из DTO — проходят serde-десериализацию, затем бизнес-валидацию в сервисе |
| HTTP POST /api/v1/cartridges_* → AppCtx | Phase 4: router построен, не bind'ится; Phase 5 добавит session middleware |
| CartridgeService → WriterHandle | Единственный путь для mutations — структурная гарантия single-writer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | CartridgeCreateDto.code_override | mitigate | Валидация длины + символов кода в CartridgeService.validate_create; assign_code_in_tx проверяет UNIQUE |
| T-04-03-02 | Tampering | CartridgeTransitionPayload.version (optimistic lock bypass) | mitigate | transition_in_tx сверяет version в WHERE id=? AND version=? — без совпадения UPDATE не выполняется (0 rows → OptimisticLockMismatch) |
| T-04-03-03 | Tampering | cartridge_models_delete при наличии живых экземпляров | mitigate | CartridgeService.model_delete: COUNT живых экземпляров перед удалением; Conflict при >0 |
| T-04-03-04 | Information Disclosure | AuditEntryDto.before_json / after_json | accept | Phase 4: нет PII в картриджах (только местонахождение + ФИО в свободной форме); Phase 5 добавит авторизацию |
| T-04-03-05 | Elevation | axum router зарегистрирован в http/mod.rs | accept | Router построен но НЕ bind'ится — нет сетевого доступа до Phase 5; принято как архитектурный выбор |
| T-04-03-SC | Tampering | cargo installs | accept | Нет новых зависимостей |
</threat_model>

<verification>
После выполнения:
- `cargo test -p trackly-app` — все тесты зелёные
- `grep -v "^#" ui/src/bindings.ts | grep -c "cartridges_list"` == 1 (bindings регенерированы)
- `grep -c "cartridges: Arc<CartridgeService>" crates/trackly-app/src/context.rs` == 1
- `grep -c "cartridges_transition" crates/trackly-app/src/specta_export.rs` == 1
- `cargo test -p trackly-app --test cartridges_numbering -- concurrent_50_unique_codes` зелёный
</verification>

<success_criteria>
1. `cargo test -p trackly-app` — все 6 новых тестовых файлов (все тесты) проходят.
2. `ui/src/bindings.ts` содержит cartridges_list, cartridges_transition, CartridgeDto, CartridgeTransitionPayload.
3. AppCtx.cartridges: Arc<CartridgeService> присутствует в context.rs.
4. HTTP router cartridges.router() компилируется и построен (не bind'ится).
5. Concurrent test: 50 параллельных create → 50 уникальных кодов формата C-NNNNNN.
</success_criteria>

<output>
Создать `.planning/phases/04-cartridges/04-03-SUMMARY.md` при завершении.
</output>
