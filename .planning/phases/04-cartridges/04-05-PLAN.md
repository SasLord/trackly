---
phase: 04-cartridges
plan: "05"
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - ui/src/features/cartridges/CartridgeContextMenu.svelte
  - ui/src/features/cartridges/OperationModal.svelte
  - ui/src/features/cartridges/CartridgeFormModal.svelte
  - ui/src/features/cartridges/LowStockBanner.svelte
autonomous: true
requirements:
  - CART-06
  - CART-07
  - CART-08
  - CART-09
  - CART-12

must_haves:
  truths:
    - "CartridgeContextMenu показывает только допустимые операции для текущего status_id (нет серых disabled пунктов)"
    - "OperationModal с op='install' показывает поля Дата/Кто выдал/Кому выдал/Расположение; с op='return_to_stock' — Состояние заряда/Расположение/Примечание"
    - "После успешной операции: Toast success + модал закрыт + список обновлён через onSuccess callback"
    - "CartridgeFormModal позволяет создать/редактировать картридж; код можно оставить пустым (авто) или ввести вручную"
    - "LowStockBanner показывается если low_stock() возвращает непустой массив, скрывается если пуст"
    - "Состояние заряда по умолчанию: install — не меняется; return_to_stock/write_off — Пустой (state_id=3); from_refill — Полный (state_id=1)"
  artifacts:
    - path: "ui/src/features/cartridges/CartridgeContextMenu.svelte"
      provides: "Status-dependent kebab меню с portal + mousedown-outside close"
      contains: "use:portal"
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "Параметризованная модалка для 5 операций жизненного цикла"
    - path: "ui/src/features/cartridges/CartridgeFormModal.svelte"
      provides: "CRUD модалка создания/редактирования экземпляра"
    - path: "ui/src/features/cartridges/LowStockBanner.svelte"
      provides: "Баннер низкого остатка с warning-цветом"
  key_links:
    - from: "ui/src/features/cartridges/CartridgeContextMenu.svelte"
      to: "ui/src/features/cartridges/OperationModal.svelte"
      via: "onInstall/onReturnToStock/onToRefill/onFromRefill/onWriteOff callbacks"
      pattern: "OperationModal"
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "ui/src/features/cartridges/api.ts"
      via: "cartridges.transition(payload)"
      pattern: "transition"
---

<objective>
Wave 3 (план B из двух параллельных): Lifecycle UI — контекстное меню, модалка операций, CRUD модалка, баннер.

Создаём интерактивный слой: CartridgeContextMenu с portal и status-dependent пунктами,
единую OperationModal для 5 lifecycle-операций, CartridgeFormModal для CRUD экземпляра,
и LowStockBanner для отображения низкого остатка.

Параллелен с планом 04-04 (нет пересечения файлов).

Purpose: После этого плана пользователь может выполнять lifecycle-операции из контекстного меню.
Output: 4 Svelte-компонента.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cartridges/04-CONTEXT.md
@.planning/phases/04-cartridges/04-UI-SPEC.md
@.planning/phases/04-cartridges/04-PATTERNS.md

<interfaces>
Из ui/src/features/devices/DeviceContextMenu.svelte — точный образец:
- import { portal } from '$lib/utils/portal'
- menuX/menuY из triggerEl.getBoundingClientRect(): menuX = rect.right - 160; menuY = rect.bottom + 4
- svelte:window onmousedown={handleBodyMousedown} onscroll/onresize
- handleBodyMousedown: игнорировать клик внутри .ctx-menu-portal (target.closest('.ctx-menu-portal'))
- Escape закрывает меню
- z-index: 2000

Из ui/src/features/acts/ReturnModal.svelte — образец модалки операции:
- {#key openKey} сброс формы при открытии
- $effect(() => { if (open) { reset fields } })
- submitting $state + loading prop на Button + блокировка кнопки

Из ui/src/features/devices/DeviceFormModal.svelte — образец CRUD модалки:
- openInstanceCounter паттерн: $effect(() => { if (open && !_wasOpen) openInstanceCounter++; _wasOpen = open; })
- isEdit = target !== null

Из ui/src/lib/components/Modal.svelte:
- props: open, title, size ('md' | 'wide'), onClose
- G-1 backdrop fix: mousedown/mouseup паттерн (не onclick)

Из ui/src/lib/components/DatePicker.svelte:
- bind:value (unix seconds)

Из ui/src/lib/components/PersonAutocomplete.svelte:
- bind:value: string
- suggestFn prop или встроенный fetch

Из ui/src/lib/components/LocationAutocomplete.svelte:
- bind:value: string, suggestFn/commandName prop

CartridgeTransitionPayload TypeScript (из bindings.ts после 04-03):
- { op: "Install", cartridge_id, version, date_utc, given_by_name, given_to_name, location }
- { op: "ReturnToStock", cartridge_id, version, state_id, location, notes }
- { op: "ToRefill", ... }, { op: "FromRefill", ... }, { op: "WriteOff", cartridge_id, version, date_utc, notes }

LowStockItemDto: { model_id: number, brand: string, model: string, count: number, threshold: number }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartridgeContextMenu + LowStockBanner</name>

  <read_first>
    - ui/src/features/devices/DeviceContextMenu.svelte (ПОЛНЫЙ файл — копировать структуру: portal, menuX/menuY расчёт, handleBodyMousedown, svelte:window listeners, CSS z-index:2000 .ctx-menu-portal)
    - ui/src/lib/utils/portal.ts (или portal.svelte — проверить путь через grep; он нужен для use:portal)
    - .planning/phases/04-cartridges/04-UI-SPEC.md §Контекстное меню (status-dependent пункты), §Accessibility (aria-labels), §LowStockBanner
    - .planning/phases/04-cartridges/04-PATTERNS.md §CartridgeContextMenu.svelte (menuItems $derived.by, CSS классы)
    - .planning/phases/04-cartridges/04-CONTEXT.md §D-Op-Transitions-01 (разрешённые операции по статусу)
  </read_first>

  <files>
    ui/src/features/cartridges/CartridgeContextMenu.svelte,
    ui/src/features/cartridges/LowStockBanner.svelte
  </files>

  <action>
1. Создать ui/src/features/cartridges/CartridgeContextMenu.svelte:
   - props: cartridge: CartridgeDto, onInstall, onReturnToStock, onToRefill, onFromRefill, onWriteOff, onEdit, onDelete
   - let triggerEl: HTMLButtonElement | null = $state(null)
   - let menuOpen = $state(false), menuX = $state(0), menuY = $state(0)
   - toggleMenu: вычисляет menuX/menuY из triggerEl.getBoundingClientRect() (menuX = rect.right - 160, menuY = rect.bottom + 4) и устанавливает menuOpen
   - menuItems через $derived.by(() => { const items = []; по status_id добавлять из D-Op-Transitions-01 }):
     - status_id=1 (На складе): «Установить в принтер» (onInstall), «Отправить на заправку» (onToRefill), separator, «Списать» (onWriteOff, destructive)
     - status_id=2 (В работе): «Вернуть на склад» (onReturnToStock)
     - status_id=3 (На заправке): «Забрать с заправки» (onFromRefill)
     - Все статусы: «Редактировать» (onEdit), separator, «Удалить» (onDelete, destructive)
     - Порядок строго по UI-SPEC §Контекстное меню
   - Закрытие: handleBodyMousedown — если клик вне triggerEl и вне .ctx-menu-portal → menuOpen = false; также onkeydown Escape
   - svelte:window onmousedown={handleBodyMousedown} onscroll={closeMenu} onresize={closeMenu}
   - Kebab button: width/height 28px, aria-label="Действия с картриджем {cartridge.code}", aria-expanded={menuOpen}
   - Portal div: use:portal, position:fixed, left:{menuX}px top:{menuY}px, z-index:2000, class="ctx-menu-portal"
   - Разделитель: hr.ctx-menu-sep перед деструктивными действиями
   - CSS: скопировать .ctx-menu-portal, .ctx-menu-item, .ctx-menu-item--destructive, .ctx-menu-sep из DeviceContextMenu.svelte

2. Создать ui/src/features/cartridges/LowStockBanner.svelte:
   - props: items: LowStockItemDto[]
   - Если items.length === 0: не рендерится (display: none или {#if items.length > 0})
   - Структура: div.low-stock-banner с иконкой предупреждения (inline SVG 16×16, --color-warning), h4 «Низкий остаток картриджей», ul с li для каждого item
   - Строка каждого item: «{item.brand} {item.model} — {item.count} шт. на складе (порог: {item.threshold})»
   - CSS: background: color-mix(in srgb, var(--color-warning) 10%, transparent); border: 1px solid var(--color-warning); border-radius: var(--radius-md); padding: var(--space-md); margin-bottom: var(--space-md)
   - Иконка: inline SVG path предупреждения (треугольник с восклицательным знаком), color: var(--color-warning), width/height: 16px
  </action>

  <verify>
    <automated>pnpm --prefix /Users/madsas/Projects/trackly/ui svelte-check 2>&1 | grep -E "CartridgeContextMenu|LowStockBanner" | head -5</automated>
  </verify>

  <done>
svelte-check без ошибок на CartridgeContextMenu и LowStockBanner. CartridgeContextMenu содержит use:portal и status-dependent menuItems. LowStockBanner не рендерится при пустом массиве.
  </done>
</task>

<task type="auto">
  <name>Task 2: OperationModal + CartridgeFormModal</name>

  <read_first>
    - ui/src/features/acts/ReturnModal.svelte (ПОЛНЫЙ файл — паттерн $effect reset при open, submitting state, handleSubmit с try/catch + pushToast)
    - ui/src/features/acts/ActFormModal.svelte (ПОЛНЫЙ файл — паттерн openInstanceCounter для сброса формы, isEdit derived)
    - ui/src/features/devices/DeviceFormModal.svelte (паттерн {#key openInstanceCounter} для FormBody reset)
    - ui/src/lib/components/Modal.svelte (size prop, onClose, G-1 backdrop fix)
    - ui/src/lib/components/DatePicker.svelte (bind:value — unix seconds)
    - ui/src/lib/components/PersonAutocomplete.svelte (suggestFn prop или command-based)
    - ui/src/lib/components/LocationAutocomplete.svelte (как передавать suggestFn/commandName)
    - ui/src/lib/components/Select.svelte (options prop, bind:value)
    - ui/src/lib/stores/toast.svelte.ts (или аналог) — pushToast('message', 'success'|'error')
    - .planning/phases/04-cartridges/04-UI-SPEC.md §Заголовки OperationModal, §Поля OperationModal, §Деструктивные подтверждения, §Primary CTA, §Ошибочные состояния
    - .planning/phases/04-cartridges/04-PATTERNS.md §OperationModal.svelte, §CartridgeFormModal.svelte
    - .planning/phases/04-cartridges/04-CONTEXT.md §D-Op-Fields-01 (дефолты заряда по операции)
  </read_first>

  <files>
    ui/src/features/cartridges/OperationModal.svelte,
    ui/src/features/cartridges/CartridgeFormModal.svelte
  </files>

  <action>
1. Создать ui/src/features/cartridges/OperationModal.svelte:
   - type Op = 'install' | 'return_to_stock' | 'to_refill' | 'from_refill' | 'write_off'
   - props: open: boolean, op: Op, cartridge: CartridgeDto | null, onClose: () => void, onSuccess: () => void
   - Состояние формы: dateUtc, givenByName, givenToName, location, stateId, notes — все $state
   - $effect(() => { if (open) { dateUtc = Math.floor(Date.now() / 1000); givenByName = ''; givenToName = ''; location = ''; notes = '';
     stateId = (op === 'from_refill') ? 1 : 3; // D-Op-Fields-01: install не меняет заряд (поле не показывается), from_refill→Полный(1), остальные→Пустой(3)
     } })
   - MODAL_TITLES: объект из UI-SPEC §Заголовки OperationModal
   - CONFIRM_LABELS: из UI-SPEC §Primary CTA (Установить / Вернуть на склад / Отправить на заправку / Вернуть с заправки / Списать)
   - STATE_OPTIONS = [{value: 1, label: 'Полный'}, {value: 2, label: 'Частичный'}, {value: 3, label: 'Пустой'}]
   - Рендер полей по op:
     - 'install' | 'to_refill': DatePicker(Дата) + PersonAutocomplete(Кто выдал, required) + PersonAutocomplete(Кому выдал, required) + LocationAutocomplete(Расположение, required) + hint «Укажите рабочее место или кабинет (не склад)» (install) без hint (to_refill)
     - 'return_to_stock' | 'from_refill': Select(Состояние заряда, stateId) + LocationAutocomplete(required) + hint «Укажите склад или место хранения» (return_to_stock) + Textarea(Примечание, необязательно)
     - 'write_off': DatePicker(Дата) + Textarea(Причина/Примечание, необязательно)
   - handleSubmit: validation (required fields) → submitting = true → cartridges.transition(buildPayload()) → onSuccess() + onClose() + pushToast('...', 'success') → catch → pushToast(err.message || 'Не удалось выполнить операцию. Повторите попытку.', 'error') → submitting = false
   - buildPayload() — собирает CartridgeTransitionPayload по op: Install{cartridge_id: cartridge.id, version: cartridge.version, date_utc: dateUtc, given_by_name, given_to_name, location}, ReturnToStock{..., state_id}, и т.д.
   - PersonAutocomplete suggestFn: (prefix) => cartridges.suggestPerson(prefix) — проверить существующую команду acts_suggest_person или добавить suggestFn через acts api
   - LocationAutocomplete suggestFn: (prefix) => cartridges.suggestLocation(prefix)
   - Modal size="md"
   - Кнопка подтвердить: variant="primary", loading={submitting}; кнопка Отмена: variant="secondary"

2. Создать ui/src/features/cartridges/CartridgeFormModal.svelte:
   - props: open: boolean, target: CartridgeDto | null (null = создание), models: CartridgeModelDto[], onClose: () => void, onSuccess: (cart: CartridgeDto) => void
   - isEdit = $derived(target !== null)
   - modalTitle = $derived(isEdit ? 'Редактирование картриджа' : 'Новый картридж')
   - submitLabel = $derived(isEdit ? 'Сохранить изменения' : 'Добавить картридж')
   - Паттерн openInstanceCounter: $effect(() => { if (open && !_wasOpen) openInstanceCounter++; _wasOpen = open; })
   - {#key openInstanceCounter} CartridgeFormBody заполняется из target при isEdit {/key}
   - Поля (из UI-SPEC §CartridgeFormModal): Код (Input, placeholder «Будет присвоен автоматически», hint под полем) + Модель (Select из models prop, required) + Состояние заряда (Select, default state_id=1 Полный) + Расположение (LocationAutocomplete, optional) + Примечания (Textarea)
   - handleSubmit: если isEdit → cartridges.update(target.id, target.version, patch), иначе cartridges.create({model_id, code_override: code || null, state_id, location, notes}) → onSuccess(result) + onClose() + pushToast
   - Ошибка «Код уже занят»: проверять error.code === 'Conflict' и показывать «Картридж с кодом «{code}» уже существует. Введите другой код.» под полем кода
   - Modal size="md"
  </action>

  <verify>
    <automated>pnpm --prefix /Users/madsas/Projects/trackly/ui svelte-check 2>&1 | grep -E "^Error" | head -10</automated>
  </verify>

  <done>
svelte-check без ошибок. OperationModal рендерит правильный набор полей по op. CartridgeFormModal содержит openInstanceCounter паттерн для сброса формы. Оба модала используют Modal.svelte с size="md".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ввод пользователя в OperationModal (givenByName, givenToName, location, notes) → CartridgeTransitionPayload → backend | Текстовые поля; backend валидирует через AppError::Validation |
| CartridgeContextMenu portal (position:fixed, z-index:2000) | Меню рендерится вне нормального DOM-дерева; событие mousedown обрабатывается через svelte:window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | OperationModal.handleSubmit — payload fields | mitigate | Обязательные поля валидируются до submit; backend дополнительно валидирует через AppError::Validation |
| T-04-05-02 | Tampering | CartridgeContextMenu — status-based menu items | mitigate | menuItems генерируются на основе cartridge.status_id из backend (не из локального состояния); backend transition_in_tx также проверяет допустимость перехода |
| T-04-05-03 | Tampering | CartridgeFormModal — code_override field | mitigate | Пустая строка передаётся как null (code_override: code || null) → авто-код; backend проверяет UNIQUE на custom-code |
| T-04-05-SC | Tampering | npm installs | accept | Нет новых зависимостей |
</threat_model>

<verification>
После выполнения обоих задач:
- `pnpm svelte-check && pnpm lint` зелёные
- CartridgeContextMenu содержит use:portal (grep -c "use:portal")
- OperationModal различает поля по op prop: {#if op === 'install' || op === 'to_refill'}
- LowStockBanner не рендерится при items=[] (conditional rendering)
- stateId default: op==='from_refill' → 1 (Полный), остальные → 3 (Пустой) согласно D-Op-Fields-01
</verification>

<success_criteria>
1. `pnpm svelte-check && pnpm lint` зелёные.
2. CartridgeContextMenu показывает для status_id=1: «Установить в принтер» / «Отправить на заправку» / «Списать» / «Редактировать» / «Удалить».
3. OperationModal с op='install' рендерит DatePicker + 2x PersonAutocomplete + LocationAutocomplete.
4. OperationModal с op='return_to_stock' рендерит Select(Состояние заряда, default Пустой) + LocationAutocomplete + Textarea.
5. LowStockBanner виден только при непустом массиве items; использует --color-warning.
6. CartridgeFormModal код поле имеет hint «Будет присвоен автоматически...».
</success_criteria>

<output>
Создать `.planning/phases/04-cartridges/04-05-SUMMARY.md` при завершении.
</output>
