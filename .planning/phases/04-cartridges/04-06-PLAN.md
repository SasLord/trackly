---
phase: 04-cartridges
plan: "06"
type: execute
wave: 4
depends_on:
  - "04-04"
  - "04-05"
files_modified:
  - ui/src/features/cartridges/ModelsList.svelte
  - ui/src/features/cartridges/ModelListRow.svelte
  - ui/src/features/cartridges/ModelFormModal.svelte
  - ui/src/features/cartridges/CompatibilityEditor.svelte
  - ui/src/features/cartridges/CartridgesPage.svelte
  - ui/src/features/cartridges/CartridgesList.svelte
  - ui/src/features/cartridges/CartridgeListRow.svelte
  - ui/src/features/cartridges/CartridgeDetail.svelte
autonomous: false
requirements:
  - CART-01
  - CART-02
  - CART-03
  - CART-04
  - CART-05
  - CART-06
  - CART-07
  - CART-08
  - CART-09
  - CART-10
  - CART-11
  - CART-12

must_haves:
  truths:
    - "Пользователь создаёт модель картриджа с Брендом, Моделью, Типом, Цветом (скрыт для Фотобарабана), Примечанием и матрицей совместимых принтеров"
    - "CompatibilityEditor позволяет добавлять/удалять пары Бренд+Модель принтера с focus-open автокомплитом"
    - "Раздел «Картриджи» полностью интегрирован: контекстное меню картриджа вызывает lifecycle-операции, кнопка создания работает"
    - "CartridgesPage.svelte единый оркестратор: передаёт callbacks между всеми дочерними компонентами"
    - "После операции transition список автоматически обновляется"
    - "Баннер low_stock обновляется после операции (refetch при каждом transition)"
  artifacts:
    - path: "ui/src/features/cartridges/ModelsList.svelte"
      provides: "Список моделей с поиском и кнопкой создания"
    - path: "ui/src/features/cartridges/ModelFormModal.svelte"
      provides: "CRUD модели с CompatibilityEditor, size='wide'"
    - path: "ui/src/features/cartridges/CompatibilityEditor.svelte"
      provides: "Добавляемый список пар Бренд+Модель принтера с focus-open autocomplete"
    - path: "ui/src/features/cartridges/CartridgesPage.svelte"
      provides: "Полная интеграция всех компонентов с event routing"
  key_links:
    - from: "ui/src/features/cartridges/CartridgesPage.svelte"
      to: "ui/src/features/cartridges/CartridgeContextMenu.svelte"
      via: "onInstall/onReturnToStock/etc callbacks через CartridgeListRow → CartridgesPage → OperationModal"
      pattern: "onMenuAction"
    - from: "ui/src/features/cartridges/ModelFormModal.svelte"
      to: "ui/src/features/cartridges/CompatibilityEditor.svelte"
      via: "bind:compatibility"
      pattern: "CompatibilityEditor"
    - from: "ui/src/features/cartridges/CompatibilityEditor.svelte"
      to: "ui/src/features/cartridges/api.ts"
      via: "suggestFn props: cartridges.suggestCompatPrinter('brand', prefix) / cartridges.suggestCompatPrinter('model', prefix)"
      pattern: "suggestCompatPrinter"
---

<objective>
Wave 4: Завершение — UI моделей + финальная интеграция всех компонентов.

Создаём Models CRUD (ModelsList + ModelListRow + ModelFormModal + CompatibilityEditor),
затем финально соединяем все компоненты в CartridgesPage.svelte:
- контекстное меню → OperationModal
- кнопка «+ Добавить картридж» → CartridgeFormModal
- успех операции → обновление списка + low_stock баннера
- вкладка «Модели» → ModelsList

После этого плана раздел «Картриджи» полностью функционален end-to-end.
Завершается human-verify checkpoint.

Purpose: Последняя волна замыкает end-to-end slice.
Output: Models UI + полный wire-up CartridgesPage + human-verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cartridges/04-CONTEXT.md
@.planning/phases/04-cartridges/04-UI-SPEC.md
@.planning/phases/04-cartridges/04-PATTERNS.md

<interfaces>
Из ui/src/features/acts/ActsList.svelte — образец списка с toolbar:
- Кнопка «+ Добавить» в шапке списка

Из ui/src/features/devices/DeviceFormModal.svelte — образец CRUD модалки с reset:
- openInstanceCounter паттерн

Из ui/src/lib/components/LocationAutocomplete.svelte — образец focus-open autocomplete:
- Открывается при focus без ввода, показывает все предложения
- suggestFn prop или commandName

Из ui/src/lib/components/PersonAutocomplete.svelte — образец focus-open autocomplete с suggestFn:
- bind:value: string
- suggestFn: (prefix: string) => Promise<string[]>

CartridgeModelDto (из bindings.ts):
- { id: number, brand: string, model: string, kind_id: number, color: string | null, notes: string | null }

CompatibilityEditor получает:
- compatibility: Array<{ printer_brand: string, printer_model: string }>
- suggestBrandFn: (prefix: string) => Promise<string[]>  — реализация: (prefix) => cartridges.suggestCompatPrinter('brand', prefix)
- suggestModelFn: (prefix: string) => Promise<string[]>  — реализация: (prefix) => cartridges.suggestCompatPrinter('model', prefix)

Цвета для ModelFormModal (D-Model-Color-01):
- ['Чёрный', 'Голубой', 'Пурпурный', 'Жёлтый', 'Светло-голубой', 'Светло-пурпурный']
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Models UI — ModelsList + ModelListRow + ModelFormModal + CompatibilityEditor</name>

  <read_first>
    - ui/src/features/acts/ActsList.svelte (образец списка с шапкой toolbar + пустое состояние)
    - ui/src/features/acts/ActListRow.svelte (образец строки: .row/.top/.bottom, kebab кнопка)
    - ui/src/features/devices/DeviceFormModal.svelte (ПОЛНЫЙ файл — openInstanceCounter, isEdit, size="md")
    - ui/src/lib/components/Input.svelte (props: label, value, placeholder, bind:value, error)
    - ui/src/lib/components/Select.svelte (options: [{value, label}], bind:value)
    - ui/src/lib/components/PersonAutocomplete.svelte (ПОЛНЫЙ файл — паттерн suggestFn prop, focus-open поведение)
    - ui/src/lib/components/LocationAutocomplete.svelte (паттерн suggestFn/commandName — для понимания как передавать async suggest)
    - .planning/phases/04-cartridges/04-UI-SPEC.md §ModelFormModal (поля, размер wide, условное скрытие Цвет), §CompatibilityEditor (focus-open autocomplete из DISTINCT cartridge_model_compatibility), §ModelListRow, §Copywriting Contract (пустые состояния моделей, CTA кнопки, ошибки)
    - .planning/phases/04-cartridges/04-PATTERNS.md §ModelFormModal.svelte, §CompatibilityEditor (no analog — создать с нуля), §ModelListRow.svelte
    - .planning/phases/04-cartridges/04-CONTEXT.md §D-Model-Fields-01, §D-Model-Kind-01, §D-Model-Color-01, §D-Model-Compat-01
    - .planning/phases/04-cartridges/04-03-PLAN.md Task 2 behavior block — список зарегистрированных команд (cartridges_suggest_compat_printer экспортируется в api.ts как cartridges.suggestCompatPrinter(field, prefix))
  </read_first>

  <files>
    ui/src/features/cartridges/ModelsList.svelte,
    ui/src/features/cartridges/ModelListRow.svelte,
    ui/src/features/cartridges/ModelFormModal.svelte,
    ui/src/features/cartridges/CompatibilityEditor.svelte
  </files>

  <action>
1. Создать ui/src/features/cartridges/CompatibilityEditor.svelte (новый паттерн — нет аналога):
   - props: compatibility: Array<{printer_brand: string, printer_model: string}>, onChange: (pairs) => void, suggestBrandFn: (prefix: string) => Promise<string[]>, suggestModelFn: (prefix: string) => Promise<string[]>
   - let rows = $state([...compatibility]) при открытии (copy)
   - addRow: rows = [...rows, {printer_brand: '', printer_model: ''}]
   - removeRow(index): rows = rows.filter((_, i) => i !== index); onChange(rows)
   - При изменении любого поля: onChange(rows)
   - Рендер: {#each rows as row, i} div.compat-row {
       PersonAutocomplete или аналог с suggestFn={suggestBrandFn} bind:value={rows[i].printer_brand} label="Бренд принтера" placeholder="Бренд принтера"
       + PersonAutocomplete или аналог с suggestFn={suggestModelFn} bind:value={rows[i].printer_model} label="Модель принтера" placeholder="Модель принтера"
       + кнопка ✕ (removeRow(i), aria-label="Удалить принтер {i+1}")
     }
   - Использовать тот же autocomplete-компонент (PersonAutocomplete или LocationAutocomplete), что применяется в OperationModal (04-05) — они уже принимают suggestFn prop. Не создавать новый компонент с нуля, если подходящий уже существует.
   - suggestBrandFn передаётся из ModelFormModal: (prefix) => cartridges.suggestCompatPrinter('brand', prefix)
   - suggestModelFn передаётся из ModelFormModal: (prefix) => cartridges.suggestCompatPrinter('model', prefix)
   - Focus-open поведение: autocomplete открывается при focus (prefix='') и показывает все ранее введённые значения из cartridge_model_compatibility, без необходимости вводить символы
   - Кнопка «+ Добавить принтер» под списком (variant="secondary", size="sm")
   - CSS layout: .compat-row { display: grid; grid-template-columns: 1fr 1fr 28px; gap: var(--space-sm); align-items: end; margin-bottom: var(--space-sm); }

2. Создать ui/src/features/cartridges/ModelFormModal.svelte:
   - props: open, target: CartridgeModelDto | null, onClose, onSuccess: (model) => void
   - isEdit = $derived(target !== null)
   - modalTitle = isEdit ? 'Редактирование модели' : 'Новая модель картриджа'
   - submitLabel = isEdit ? 'Сохранить изменения' : 'Добавить модель'
   - Поля (из UI-SPEC §ModelFormModal, layout: grid 1fr 1fr для основных полей):
     - Select «Тип расходника» (kind_id: 1=Картридж, 2=Фотобарабан), default 1, required
     - Input «Бренд» + focus-open autocomplete через cartridges.suggestBrand(prefix), required
     - Input «Модель» + focus-open autocomplete через cartridges.suggestModel(brand, prefix), required
     - {#if kindId !== 2}: Select «Цвет» (COLOR_OPTIONS: Чёрный/Голубой/Пурпурный/Жёлтый/Светло-голубой/Светло-пурпурный), default 'Чёрный', НЕ обязательно
     - Textarea «Примечание»
     - CompatibilityEditor (полная ширина, bind:compatibility, suggestBrandFn={(p) => cartridges.suggestCompatPrinter('brand', p)}, suggestModelFn={(p) => cartridges.suggestCompatPrinter('model', p)})
   - Валидация brand.trim() !== '' + model.trim() !== '' перед submit
   - openInstanceCounter паттерн для сброса формы при открытии
   - handleSubmit: isEdit → cartridges.modelsUpdate(target.id, target.version, patch), иначе → cartridges.modelsCreate({brand, model, kind_id, color: kindId !== 2 ? color : null, notes, compatibility})
   - Ошибка «Бренд+Модель уже существует» из UI-SPEC §Ошибочные состояния
   - Modal size="wide" (960px)

3. Создать ui/src/features/cartridges/ModelListRow.svelte по образцу ActListRow.svelte:
   - props: model: CartridgeModelDto, instanceCount: number, onEdit, onDelete
   - .top: brand + ' ' + model, Badge тип (kind_id=1 → variant='accent' «Картридж», kind_id=2 → variant='default' «Фотобарабан'), {#if model.kind_id === 1 && model.color} Badge color.svelte цвет {/if}
   - .bottom: instanceCount + ' шт.', model.notes если есть
   - Kebab ⋮: aria-label="Действия с моделью {model.brand} {model.model}", только Редактировать / Удалить (не нужен portal — простое инлайн меню или использовать паттерн DeviceContextMenu)
   - Строка НЕ кликабельна (нет master-detail для моделей)

4. Создать ui/src/features/cartridges/ModelsList.svelte:
   - props: models: CartridgeModelDto[], loading: boolean, onCreateModel, onEditModel, onDeleteModel
   - Toolbar: заголовок «Модели картриджей» + кнопка «+ Добавить модель» (variant="primary")
   - Пустое состояние: «Моделей пока нет» / «Добавьте модель картриджа...» / кнопка «+ Добавить модель» (из UI-SPEC)
   - {#each models as m (m.id)} ModelListRow {instanceCount передать через отдельный Map или пересчитывать из пропа}
   - При удалении модели с живыми экземплярами: backend вернёт AppError::Conflict → показать Toast error «Нельзя удалить модель: она используется N картриджами» БЕЗ confirm-модала (UI-SPEC §ModelListRow)
  </action>

  <verify>
    <automated>pnpm --prefix /Users/madsas/Projects/trackly/ui svelte-check 2>&1 | grep -E "^Error" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - svelte-check без ошибок на 4 новых компонентах
    - ModelFormModal содержит {#if kindId !== 2} для поля Цвет
    - CompatibilityEditor рендерит grid 1fr/1fr/28px строки
    - CompatibilityEditor.svelte содержит ссылки на suggestCompatPrinter: grep -c "suggestCompatPrinter" ui/src/features/cartridges/CompatibilityEditor.svelte возвращает >= 1 (или grep -c "suggestBrandFn\|suggestModelFn" >= 2, если suggestCompatPrinter вызывается в ModelFormModal)
    - Поведение autocomplete (human-verify в Task 3): при фокусе на поле «Бренд принтера» в CompatibilityEditor показываются предложения из ранее введённых пар (cartridges.suggestCompatPrinter('brand', '')); при фокусе на поле «Модель принтера» — предложения из cartridges.suggestCompatPrinter('model', '')
    - ModelFormModal передаёт suggestBrandFn и suggestModelFn в CompatibilityEditor: grep -c "suggestCompatPrinter" ui/src/features/cartridges/ModelFormModal.svelte возвращает >= 2
  </acceptance_criteria>

  <done>
svelte-check без ошибок на 4 новых компонентах. ModelFormModal содержит {#if kindId !== 2} для поля Цвет. CompatibilityEditor рендерит grid 1fr/1fr/28px строки с focus-open autocomplete на полях «Бренд принтера» и «Модель принтера» через cartridges.suggestCompatPrinter('brand'/'model', prefix) — без упрощений.
  </done>
</task>

<task type="auto">
  <name>Task 2: Финальный wire-up CartridgesPage + обновление CartridgesList/ListRow/Detail</name>

  <read_first>
    - ui/src/features/cartridges/CartridgesPage.svelte (создан в плане 04-04 — прочитать полностью перед изменением)
    - ui/src/features/cartridges/CartridgesList.svelte (создан в плане 04-04 — прочитать полностью)
    - ui/src/features/cartridges/CartridgeListRow.svelte (создан в плане 04-04 — прочитать полностью; заглушка kebab)
    - ui/src/features/cartridges/CartridgeDetail.svelte (создан в плане 04-04 — прочитать полностью)
    - ui/src/features/cartridges/CartridgeContextMenu.svelte (создан в плане 04-05 — props: onInstall/onReturnToStock/...)
    - ui/src/features/cartridges/OperationModal.svelte (создан в плане 04-05 — props: op, cartridge, onSuccess)
    - ui/src/features/cartridges/CartridgeFormModal.svelte (создан в плане 04-05)
    - ui/src/features/cartridges/LowStockBanner.svelte (создан в плане 04-05)
    - .planning/phases/04-cartridges/04-UI-SPEC.md §CartridgesPage layout, §1 CartridgesPage два таба, §3 CartridgeContextMenu, §6 CartridgeDetail кнопки
  </read_first>

  <files>
    ui/src/features/cartridges/CartridgesPage.svelte,
    ui/src/features/cartridges/CartridgesList.svelte,
    ui/src/features/cartridges/CartridgeListRow.svelte,
    ui/src/features/cartridges/CartridgeDetail.svelte
  </files>

  <action>
CartridgesPage.svelte — полный wire-up всех компонентов:
1. State: selectedCartridgeId, selectedCartridgeHistory, lowStockItems, cartridgeFormTarget, operationModalOp, operationModalCartridge, showModelForm, modelFormTarget, models, counts, items, loading, activeTab, filters (status, kind, model, search)

2. Функция loadAll(): вызывает Promise.all([cartridges.list(filter, {page:1, per_page:50}), cartridges.statusCounts(), cartridges.lowStock()]) → обновляет items/counts/lowStockItems

3. $effect: вызывает loadAll() при изменении filters (status/kind/model/search)

4. При выборе картриджа (handleSelectCartridge(id)): загружает cartridges.get(id) + cartridges.getHistory(id) → selectedCartridge + selectedCartridgeHistory

5. handleMenuAction(op, cartridge): 
   - 'install'/'to_refill'/'from_refill'/'return_to_stock'/'write_off' → operationModalOp=op, operationModalCartridge=cartridge, showOperationModal=true
   - 'edit' → cartridgeFormTarget=cartridge, showCartridgeForm=true
   - 'delete' → показывает confirm-диалог (простой confirm() или inline confirm-modal)

6. handleOperationSuccess(): showOperationModal=false; loadAll(); если selectedCartridgeId → перезагрузить detail

7. Кнопка «+ Добавить картридж» (в toolbar над CartridgesList): cartridgeFormTarget=null, showCartridgeForm=true

8. Обновить CartridgeListRow: передать CartridgeContextMenu вместо заглушки kebab; props из row передать в CartridgeContextMenu

9. Обновить CartridgeDetail: кнопки в header (из UI-SPEC §CartridgeDetail): если status_id=1 → кнопки «Установить», «На заправку»; если status_id=2 → «Вернуть на склад»; если status_id=3 → «С заправки»; нажатие вызывает handleMenuAction(op, cartridge)

10. Models tab (activeTab === 'models'): loadModels() → cartridges.modelsList(); ModelsList с onCreateModel/onEditModel/onDeleteModel callbacks; ModelFormModal показывается при showModelForm=true

11. Передавать onSuccess в OperationModal, CartridgeFormModal, ModelFormModal с обновлением списков

ИТОГОВЫЙ стейт CartridgesPage должен быть единственным местом где вызываются cartridges.list/statusCounts/lowStock — дочерние компоненты получают данные через props и вызывают callbacks.
  </action>

  <verify>
    <automated>pnpm --prefix /Users/madsas/Projects/trackly/ui svelte-check 2>&1 | grep -E "^Error" | head -10</automated>
  </verify>

  <done>
svelte-check без ошибок. CartridgesPage передаёт callbacks во все дочерние компоненты. CartridgeListRow содержит CartridgeContextMenu вместо заглушки. После операции вызывается loadAll() для обновления counts и lowStockItems.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Полный раздел «Картриджи» — модели с матрицей совместимости, экземпляры с авто-кодом C-XXXXXX,
lifecycle-операции через контекстное меню, детальная панель с историей перемещений, баннер низкого остатка.
  </what-built>
  <how-to-verify>
1. Запустить `cargo test && pnpm svelte-check && pnpm lint` — должны быть зелёными.
2. Запустить приложение `cargo tauri dev`.
3. Открыть раздел «Картриджи» — убедиться что sidebar-item активен, загружается страница с двумя табами.
4. Перейти на вкладку «Модели» → нажать «+ Добавить модель» → создать «Pantum / TL-5120X», тип Картридж, цвет Чёрный. В секции «Совместимые принтеры» нажать «+ Добавить принтер» → кликнуть в поле «Бренд принтера» без ввода → убедиться что autocomplete открывается (пустой список при первом запуске, затем показывает ранее введённые бренды). Ввести «Pantum» → добавить «BM5100ADN» в поле Модель. Сохранить. Убедиться что модель появилась в списке.
5. Создать фотобарабан «Cactus / DL-5120», тип Фотобарабан. Убедиться что поле «Цвет» СКРЫТО.
6. Создать ещё одну модель с совместимостью «Pantum / BM5100ADN» → открыть любую следующую модель на редактирование → в поле «Бренд принтера» сфокусироваться: autocomplete должен предложить «Pantum» из ранее введённых.
7. Перейти на вкладку «Картриджи» → нажать «+ Добавить картридж» → выбрать модель «Pantum / TL-5120X», оставить код пустым → сохранить. Убедиться что код C-000001 присвоен автоматически.
8. Нажать ⋮ рядом с картриджем «C-000001» → меню содержит «Установить в принтер», «Отправить на заправку», «Списать», «Редактировать», «Удалить».
9. Выбрать «Установить в принтер» → заполнить Дата, Кто выдал «Иванов И.И.», Кому выдал «Петров П.П.», Расположение «Каб. 305» → нажать «Установить». Убедиться что: Toast success появился, статус изменился на «В работе», badge стал синим.
10. Открыть детальную панель картриджа — проверить что История перемещений содержит «Установлен в принтер; выдал Иванов И.И., получил Петров П.П.; Каб. 305».
11. Нажать ⋮ → «Вернуть на склад» → Состояние заряда «Пустой», Расположение «Склад» → подтвердить. Убедиться что статус «На складе», badge зелёный.
12. Добавить ещё один картридж «C-000002» — проверить авто-нумерацию.
13. Switch-bar: нажать «На складе» — список показывает только картриджи со статусом На складе. Счётчик правильный.
14. Поиск: набрать «Pantum» в строке поиска → картридж находится.
15. (если есть ≥1 модель с 0 картриджей на складе) Убедиться что баннер «Низкий остаток картриджей» отображается над списком.
  </how-to-verify>
  <resume-signal>
Написать "approved" если раздел работает корректно, или описать найденные проблемы.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CartridgesPage $state → дочерние компоненты через props | Данные из backend; никакой пользовательской логики в компонентах — только отображение и callbacks |
| CompatibilityEditor Input fields → CartridgeModelCreateDto.compatibility | Текстовые пары бренд+модель; backend валидирует на пустые значения |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | ModelFormModal brand/model input — UNIQUE constraint | mitigate | Backend возвращает AppError::Conflict при дублирующей паре brand+model; UI показывает ошибку из UI-SPEC |
| T-04-06-02 | Tampering | CompatibilityEditor — пустые пары | mitigate | Фильтровать пустые пары перед передачей в compatibility: compatibility.filter(p => p.printer_brand && p.printer_model) в handleSubmit |
| T-04-06-03 | Tampering | CartridgesPage confirm-delete — double-submit | mitigate | submitting=$state(true) при первом submit; кнопка disabled пока submitting |
| T-04-06-SC | Tampering | npm installs | accept | Нет новых зависимостей |
</threat_model>

<verification>
Финальная фазовая верификация (перед /gsd-verify-work):
- `cargo test && pnpm svelte-check && pnpm lint` — зелёные
- `cargo test -p trackly-app --test cartridges_numbering` — зелёный (concurrent 50 unique codes)
- `cargo test -p trackly-app --test cartridges_lifecycle` — зелёный (все переходы + audit_log)
- `grep -c "suggestCompatPrinter" ui/src/features/cartridges/ModelFormModal.svelte` >= 2 (brand + model)
- Human-verify checkpoint выше: «approved»
</verification>

<success_criteria>
1. `cargo test && pnpm svelte-check && pnpm lint` — все зелёные.
2. ModelFormModal с kind_id=2 (Фотобарабан) скрывает поле «Цвет».
3. CompatibilityEditor использует focus-open autocomplete через suggestCompatPrinter для полей «Бренд принтера» и «Модель принтера»; пустые пары фильтруются при submit.
4. CartridgesPage загружает list+counts+lowStock в одном Promise.all.
5. Human checkpoint: approved.
6. Все CART-01..CART-12 требования выполнены end-to-end.
</success_criteria>

<output>
Создать `.planning/phases/04-cartridges/04-06-SUMMARY.md` при завершении.
</output>
