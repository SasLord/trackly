---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-core/src/ports/ad_directory.rs
  - crates/trackly-core/src/ports/mod.rs
  - crates/trackly-infra/src/ad/directory_mock.rs
  - crates/trackly-infra/src/ad/cache.rs
  - crates/trackly-infra/src/ad/mod.rs
autonomous: true
requirements: [SSO-01, SSO-03]

must_haves:
  truths:
    - "[SSO-01] A DirectoryResult carrying a resolved display_name can be produced deterministically from a fixture (MockAdDirectory), without any live LDAP connection — this is the seam RealAdDirectory (Plan 31-02) will implement for real."
    - "[SSO-03] A DirectoryResult carrying an Option<Role> role hint can be produced deterministically from a fixture, distinguishing 'no group matched' (role: None) from 'directory unreachable' (Err(DirectoryError::Unreachable)) — never collapsed into a boolean."
    - "A TtlCache entry inserted via put() is retrievable via get() before its TTL expires, and returns None after expiry — proven without a live LDAP connection."
  artifacts:
    - path: "crates/trackly-core/src/ports/ad_directory.rs"
      provides: "AdDirectory port trait, DirectoryResult struct, DirectoryError enum (ldap3-free)"
      contains: "pub trait AdDirectory"
    - path: "crates/trackly-infra/src/ad/directory_mock.rs"
      provides: "MockAdDirectory deterministic fixtures for SSO-01/SSO-03 tests"
      contains: "pub struct MockAdDirectory"
    - path: "crates/trackly-infra/src/ad/cache.rs"
      provides: "Generic hand-rolled TtlCache<V> used by RealAdDirectory in Plan 31-02"
      contains: "pub struct TtlCache"
  key_links:
    - from: "crates/trackly-infra/src/ad/directory_mock.rs"
      to: "crates/trackly-core/src/ports/ad_directory.rs"
      via: "impl AdDirectory for MockAdDirectory"
      pattern: "impl AdDirectory for MockAdDirectory"
---

<objective>
Define the `AdDirectory` port contract (SSO-01 displayName resolve + SSO-03 role hint,
fail-closed 3-plus-state outcome) and its two foundational, dependency-free building
blocks: a deterministic `MockAdDirectory` (dev macOS / CI, no live AD needed) and a
hand-rolled `TtlCache<V>` (used later by `RealAdDirectory` to avoid hitting the DC on
every SSO login).

Purpose: This is the "define contracts first" wave — later plans (`RealAdDirectory` in
31-02, `AuthService` wiring in 31-03, the end-to-end integration test in 31-04) all
implement against or consume these three files. Writing them first, and getting them
fully unit-tested here, means downstream plans compile against a stable, already-green
interface instead of discovering gaps mid-wiring.
Output: `crates/trackly-core/src/ports/ad_directory.rs`,
`crates/trackly-infra/src/ad/directory_mock.rs`, `crates/trackly-infra/src/ad/cache.rs`,
plus the two `mod.rs` registrations — all compiling and unit-tested in isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-ad-bind-ad/31-RESEARCH.md
@.planning/phases/31-ad-bind-ad/31-PATTERNS.md

<interfaces>
<!-- Existing AdClient port this phase's AdDirectory port must structurally mirror
     (3-state / never-boolean outcome philosophy, ldap3-free hexagonal boundary). -->

From crates/trackly-core/src/ports/ad.rs (79 lines, read in full):
```rust
use async_trait::async_trait;
use crate::error::AppError;
use crate::primitives::secret::Secret;

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum AuthOutcome {
    Ok { display_name: String },
    BadCreds,
    Unreachable,
}

#[async_trait]
pub trait AdClient: Send + Sync {
    async fn authenticate(&self, login: &str, password: &Secret<String>) -> Result<AuthOutcome, AppError>;
    async fn test_connection(&self) -> Result<AuthOutcome, AppError>;
}
```
Module doc (copy this shape, renaming AdClient->AdDirectory / real->directory / mock->directory_mock):
```
//! `AdClient` port — abstraction for Active Directory authentication (USR-08/USR-12).
//!
//! Pattern: like `SnmpClient`, this trait lives in trackly-core but has NO
//! ldap3/hickory/tokio imports — I/O-free invariant enforced by `tests/no_io_deps.rs`.
//! The real impl (`RealAdClient`) lives in `trackly_infra::ad::real`.
//! The mock impl (`MockAdClient`) lives in `trackly_infra::ad::mock`.
```

From crates/trackly-core/src/auth.rs (lines 1-54, read in full) — `Role` enum this
port's `DirectoryResult.role: Option<Role>` field reuses directly (same crate, no new
import path needed beyond `crate::auth::Role`):
```rust
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum Role {
    Admin,
    Manager,
    Employee,
}
impl Role {
    pub fn from_str(s: &str) -> Result<Self, AppError> { ... }
    pub fn as_str(&self) -> &'static str {
        match self { Self::Admin => "admin", Self::Manager => "manager", Self::Employee => "employee" }
    }
}
```

From crates/trackly-infra/src/ad/mock.rs (268 lines, read in full) — `MockAdClient`
shape to mirror for `MockAdDirectory` (fixtures, `unreachable()` ctor, `lookup_key`
normalization, per-scenario `#[tokio::test]` naming convention):
```rust
pub struct MockAdClient {
    pub users: HashMap<String, AdFixture>,
    pub unreachable: bool,
}
impl MockAdClient {
    pub fn default_fixtures() -> Self { /* us100/Passw0rd!/Иванов Иван Иванович, us200/Secret123/Петрова Анна Сергеевна */ }
    pub fn unreachable() -> Self { Self { users: HashMap::new(), unreachable: true } }
    fn lookup_key(login: &str) -> &str {
        let without_upn_suffix = login.split('@').next().unwrap_or(login);
        without_upn_suffix.rsplit('\\').next().unwrap_or(without_upn_suffix)
    }
}
```

RESEARCH.md's TTL-cache skeleton (authoritative source for `cache.rs` — use a GENERIC
`TtlCache<V: Clone>` instead of the fixed-shape `DirectoryCacheEntry` shown there, so it
can back BOTH the display_name cache (`TtlCache<String>`) and the role cache
(`TtlCache<Option<Role>>>`) as two separate instances inside `RealAdDirectory` in Plan
31-02 — see RESEARCH Open Question 2 / PATTERNS.md cache.rs section, "simpler two-instance
approach matches the ... philosophy better"):
```rust
use std::collections::HashMap;
use std::sync::Mutex;
use std::time::{Duration, Instant};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdDirectory port contract (trackly-core)</name>
  <files>crates/trackly-core/src/ports/ad_directory.rs, crates/trackly-core/src/ports/mod.rs</files>
  <read_first>
    crates/trackly-core/src/ports/ad.rs (full file — the exact port shape and module-doc
    to mirror), crates/trackly-core/src/ports/mod.rs (module list to extend),
    crates/trackly-core/src/auth.rs lines 1-54 (Role enum, reused directly),
    crates/trackly-core/tests/no_io_deps.rs (the hexagonal-boundary guard this new file
    must satisfy — FORBIDDEN_CRATES list: tokio, rusqlite, tauri, axum, hyper, tower,
    reqwest, sqlx, libsqlite3-sys; ldap3/async-trait are NOT in this list so
    `async_trait` is fine here, ldap3 must NEVER be imported in this file).
  </read_first>
  <action>
    Create `crates/trackly-core/src/ports/ad_directory.rs` with: (1) a module doc
    mirroring `ports/ad.rs`'s doc block verbatim in spirit, renamed for `AdDirectory` /
    `RealAdDirectory` (`trackly_infra::ad::directory`) / `MockAdDirectory`
    (`trackly_infra::ad::directory_mock`), including the CRITICAL hexagonal-boundary
    comment ("MUST NOT import tokio, ldap3, or hickory-resolver... `async_trait` +
    `crate::auth::Role` are the only allowed dependencies here, enforced by
    `tests/no_io_deps.rs`"); (2) `use async_trait::async_trait;` and
    `use crate::auth::Role;` as the only imports; (3) a struct
    `DirectoryResult { pub display_name: String, pub role: Option<Role> }` deriving
    `Debug, Clone, PartialEq, Eq`, doc-commented per-field (`role: None` = "no configured
    AD group matched — caller must NOT elevate privilege, but this is NOT a failure");
    (4) an enum `DirectoryError` with exactly three variants: `NotConfigured` (service
    bind not set up — silent degrade per RESEARCH Pitfall 5, doc-comment says so),
    `ServiceBindFailed` (service account's OWN credentials rejected by AD — a config
    error, loggable), `Unreachable` (network/TLS/timeout reaching the DC — loggable,
    per RESEARCH Pitfall 4 this is the variant the fail-closed contract at the
    `AuthService` call site in Plan 31-03 matches on); derive `Debug, Clone, PartialEq,
    Eq` on it too; (5) a trait `AdDirectory: Send + Sync` with `#[async_trait]` and one
    method: `async fn resolve(&self, sam_account_name: &str) -> Result<DirectoryResult,
    DirectoryError>;` doc-commented explaining this single combined call resolves BOTH
    displayName and role in one conceptual round trip (mirrors RESEARCH's architecture
    diagram `directory.resolve(ad_username)` call). Do NOT add a `test_connection`-style
    method — this port has no reachability-probe use case yet. Register the new module
    in `crates/trackly-core/src/ports/mod.rs` by adding `pub mod ad_directory;`
    alphabetically after `pub mod acts;` and before `pub mod cartridges;` (mirrors the
    existing `pub mod ad;` line's position — note `ad_directory` sorts after `ad`, before
    `cartridges`).
  </action>
  <acceptance_criteria>
    `crates/trackly-core/src/ports/ad_directory.rs` exists and contains `pub trait
    AdDirectory`, `pub struct DirectoryResult`, and `pub enum DirectoryError` with
    exactly the three variants `NotConfigured`, `ServiceBindFailed`, `Unreachable`.
    `grep -c "use ldap3\|use tokio\|use rusqlite" crates/trackly-core/src/ports/ad_directory.rs`
    returns 0. `cargo build -p trackly-core` exits 0.
    `cargo test -p trackly-core --test no_io_deps` exits 0 (new port does not leak an
    I/O dependency into the core crate's dependency closure).
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p trackly-core && cargo test -p trackly-core --test no_io_deps</automated>
  </verify>
  <done>`ad_directory.rs` compiles standalone as part of `trackly-core`, is registered in `ports/mod.rs`, and the `no_io_deps` guard stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MockAdDirectory fixtures (trackly-infra)</name>
  <files>crates/trackly-infra/src/ad/directory_mock.rs, crates/trackly-infra/src/ad/mod.rs</files>
  <read_first>
    crates/trackly-infra/src/ad/mock.rs (full file — `MockAdClient`'s exact fixture/
    lookup_key/unreachable()/test-module shape to mirror), the newly-created
    crates/trackly-core/src/ports/ad_directory.rs from Task 1 (the trait this mock
    implements), crates/trackly-infra/src/ad/mod.rs (module list + doc comment showing
    the mock/real switch convention to extend).
  </read_first>
  <behavior>
    - `default_fixtures()` resolving `"us100"` returns `DirectoryResult { display_name:
      "Иванов Иван Иванович", role: Some(Role::Manager) }` — reuses the EXISTING us100
      fixture identity from `mock.rs`, now also carrying a role hint (simulates "us100 is
      a member of the AD group mapped to Manager").
    - `default_fixtures()` resolving `"us200"` returns `DirectoryResult { display_name:
      "Петрова Анна Сергеевна", role: None }` — reuses the EXISTING us200 fixture
      identity, `role: None` simulates "in no configured group" (SSO-03 regression case:
      default to `employee` downstream).
    - `default_fixtures()` resolving an unknown login (e.g. `"us999"`) falls back to
      `DirectoryResult { display_name: "us999", role: None }` — no panic, no error
      (mirrors `real.rs`'s D-Config-02 fallback-to-login-itself behavior for the
      not-found case, adapted: `AdDirectory` never returns `BadCreds`-equivalent since
      there is no password to validate here).
    - `us100@example.local` (UPN form) and `EXAMPLE\us100` (NetBIOS form) both resolve to
      the SAME us100 fixture (Pitfall 3 — reuse `lookup_key`'s exact strip-suffix logic
      from `mock.rs`, duplicated here per the project's "small independent adapters over
      premature sharing" convention already established between `mock.rs` and `real.rs`).
    - `MockAdDirectory::unreachable()` resolving ANY login returns
      `Err(DirectoryError::Unreachable)` — never `Ok` with a degraded/default result;
      the CALLER (Plan 31-03's `AuthService::sso_login`) is responsible for the
      degrade-to-login/no-elevation behavior, not this mock.
  </behavior>
  <action>
    Create `crates/trackly-infra/src/ad/directory_mock.rs` mirroring `mock.rs`'s module
    doc (D-Mock-01 convention, "used when TRACKLY_AD_MOCK env var is set or
    config.ad.use_mock = true"). Define `pub struct DirectoryFixture { pub display_name:
    &'static str, pub role: Option<Role> }` (deriving `Clone`) and `pub struct
    MockAdDirectory { pub users: HashMap<String, DirectoryFixture>, pub unreachable:
    bool }`. Implement `MockAdDirectory::default_fixtures()` with EXACTLY the two
    fixtures from the Behavior section above (reusing `us100`/`us200` identities — do NOT
    invent new fixture names, per RESEARCH/PATTERNS explicit instruction). Implement
    `MockAdDirectory::unreachable()` returning `Self { users: HashMap::new(), unreachable:
    true }`. Add a private `fn lookup_key(login: &str) -> &str` duplicating
    `mock.rs::lookup_key`'s exact strip-UPN/strip-NetBIOS logic (do not import from
    `mock.rs` — independent copy, matching the codebase's established convention of
    NOT sharing this helper between `mock.rs` and `real.rs` either). Implement `impl
    AdDirectory for MockAdDirectory` with `async fn resolve`: if `self.unreachable`,
    return `Err(DirectoryError::Unreachable)`; otherwise look up `Self::lookup_key(sam_account_name)`
    in `self.users` — `Some(fixture)` maps to `Ok(DirectoryResult { display_name:
    fixture.display_name.to_string(), role: fixture.role.clone() })`, `None` maps to
    `Ok(DirectoryResult { display_name: sam_account_name.to_string(), role: None })` (NOT
    an error — unknown-but-Kerberos-already-authenticated login degrades gracefully, it
    never happens in practice since Kerberos already proved the account exists, but the
    mock must not panic on it). Write a `#[cfg(test)] mod tests` block with one
    `#[tokio::test]` per Behavior bullet above (5 tests total), naming them in the
    established per-scenario style (e.g. `known_user_resolves_display_name_and_role`,
    `user_with_no_group_resolves_none_role`, `unknown_login_falls_back_to_login_itself`,
    `upn_and_netbios_forms_resolve_to_same_fixture`, `unreachable_fixture_returns_typed_error`).
    Register the module in `crates/trackly-infra/src/ad/mod.rs`: add `pub mod
    directory_mock;` alphabetically — insert it IMMEDIATELY BEFORE `pub mod discovery;`
    (CORRECTED per plan-checker review: `directory_mock` sorts BEFORE `discovery`
    lexicographically — 4th character `r` < `s` — so it must NOT be placed after
    `discovery`). Pre-phase order is `discovery, keytab, mock, real, sso`; after this
    task the order must read `directory_mock, discovery, keytab, mock, real, sso`
    (Plan 31-01 Task 3 then inserts `cache` as the new FIRST line, giving the
    Plan-31-01-final order `cache, directory_mock, discovery, keytab, mock, real, sso`;
    Plan 31-02 Task 2 later inserts `directory` between `cache` and `directory_mock`,
    giving the canonical cross-plan final order `cache, directory, directory_mock,
    discovery, keytab, mock, real, sso`).
  </action>
  <acceptance_criteria>
    `crates/trackly-infra/src/ad/directory_mock.rs` exists with `pub struct
    MockAdDirectory` implementing `AdDirectory`. `cargo test -p trackly-infra
    ad::directory_mock` exits 0 with 5 passing tests. `grep -c "us999\|Сидорова\|us300"
    crates/trackly-infra/src/ad/directory_mock.rs` (excluding the unknown-login test
    literal `us999`, which is intentional per the Behavior spec) shows no invented
    fixture identity beyond `us100`/`us200` in the `default_fixtures()` map itself.
    `grep -n "^pub mod" crates/trackly-infra/src/ad/mod.rs` shows `directory_mock`
    listed BEFORE `discovery` (order after this task, before Task 3 runs:
    `directory_mock, discovery, keytab, mock, real, sso`).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra ad::directory_mock</automated>
  </verify>
  <done>`MockAdDirectory` compiles, implements `AdDirectory`, all 5 unit tests pass, and `ad/mod.rs` registers the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generic TtlCache primitive</name>
  <files>crates/trackly-infra/src/ad/cache.rs, crates/trackly-infra/src/ad/mod.rs</files>
  <read_first>
    .planning/phases/31-ad-bind-ad/31-RESEARCH.md "TTL cache skeleton" code block
    (authoritative source for this file's shape — adapt to a GENERIC `TtlCache<V>`
    rather than the fixed `DirectoryCacheEntry{display_name, role}` shown there),
    crates/trackly-infra/src/db/pools.rs (the `ReaderPool` hand-rolled
    `Mutex<Vec<Connection>>` convention this file's "small hand-rolled primitive, no
    external crate" style should match — read only for convention, do NOT copy its
    checkout-API shape), crates/trackly-infra/src/ad/mod.rs (module list to extend).
  </read_first>
  <behavior>
    - `put("us100", "Иванов Иван Иванович")` then immediately `get("us100")` returns
      `Some("Иванов Иван Иванович")`.
    - `get("us200")` on an empty cache (nothing ever `put` for that key) returns `None`.
    - A `TtlCache` constructed with `Duration::from_millis(10)`, after `put`, followed by
      a `std::thread::sleep`/`tokio::time::sleep` of ~30ms, then `get()` returns `None`
      (entry expired) — proves TTL expiry without needing a production-scale sleep.
    - Two entries under different keys do not interfere: `put("a", 1)`, `put("b", 2)`,
      `get("a")` still returns `Some(1)` after `put("b", 2)`.
  </behavior>
  <action>
    Create `crates/trackly-infra/src/ad/cache.rs` with a module doc explaining this is a
    hand-rolled TTL cache (no `moka`/`cached` crate — per RESEARCH's "Don't Hand-Roll"
    table this ONE data structure IS meant to be hand-rolled, LAN/~20-user scale, no
    eviction policy needed) used by `RealAdDirectory` (Plan 31-02) as TWO separate
    instances — one for `display_name: TtlCache<String>` (long TTL, e.g. 30 min, low
    security stakes) and one for `role: TtlCache<Option<Role>>` (short TTL, e.g. 5 min,
    direct authorization impact) — cite RESEARCH Open Question 2's "simpler two-instance
    approach" recommendation in the doc comment. Define `pub struct TtlCache<V: Clone> {
    entries: Mutex<HashMap<String, (V, Instant)>>, ttl: Duration }` with `pub fn
    new(ttl: Duration) -> Self`, `pub fn get(&self, key: &str) -> Option<V>` (locks the
    mutex, looks up the key, filters on `expires_at > Instant::now()`, clones the value
    out — expired-but-still-present entries are simply not returned, no eager eviction
    needed at this scale), and `pub fn put(&self, key: String, value: V)` (locks the
    mutex, inserts `(value, Instant::now() + self.ttl)`, overwriting any prior entry for
    that key). Use `.expect("TtlCache mutex poisoned")` on the lock (matches the
    project's existing panic-on-poison convention for hand-rolled primitives, e.g.
    `ReaderPool`). Write a `#[cfg(test)] mod tests` block covering all 4 Behavior bullets
    above using `TtlCache<String>` and `TtlCache<i32>` instances (no need to instantiate
    with `Option<Role>` here — that wiring is exercised in Plan 31-02's `directory.rs`
    tests instead). Register the module in `crates/trackly-infra/src/ad/mod.rs`: add
    `pub mod cache;` as the ABSOLUTE FIRST `pub mod` line in the file — `cache` sorts
    alphabetically before EVERY other module in this file, including `directory_mock`
    (inserted by Task 2, immediately above), so it goes above `directory_mock` too, not
    just above `discovery`. Order after this task (Plan 31-01 final): `cache,
    directory_mock, discovery, keytab, mock, real, sso`.
  </action>
  <acceptance_criteria>
    `crates/trackly-infra/src/ad/cache.rs` exists with `pub struct TtlCache`. `cargo test
    -p trackly-infra ad::cache` exits 0 with all 4 tests passing, including the TTL-expiry
    test using a short injected TTL (`Duration::from_millis` — not a production-scale
    sleep; if the executor used a multi-second sleep, flag as a Nyquist violation and
    fix to milliseconds). `grep -n "^pub mod" crates/trackly-infra/src/ad/mod.rs` shows
    the exact Plan-31-01-final order `cache, directory_mock, discovery, keytab, mock,
    real, sso` (pins the module-order contradiction fix from plan-checker review).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra ad::cache</automated>
  </verify>
  <done>`TtlCache<V>` compiles generically, all 4 unit tests pass, `ad/mod.rs` registers the module, and the whole crate still builds (`cargo build -p trackly-infra`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `trackly-core` port surface | `AdDirectory`/`DirectoryResult`/`DirectoryError` are pure data types — no I/O, no secrets cross this boundary (the service-account password never appears in this crate) |
| Mock fixture data | `MockAdDirectory`'s fixtures are hardcoded, in-git placeholder identities (`us100`/`us200`) — must never be replaced with real org data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01a | Information Disclosure | `directory_mock.rs` fixtures | accept | Fixtures reuse EXISTING in-git placeholder identities (`us100`/`us200`/Cyrillic placeholder names) already established in `mock.rs` — zero new real-data risk, this task invents no new names |
| T-31-01b | Elevation of Privilege | `AdDirectory::resolve` contract shape | mitigate | Trait signature enforces `Result<DirectoryResult, DirectoryError>` (never a bare `bool`/`Ok(default)`-on-failure) at the TYPE level — `Err(Unreachable)` cannot be silently coerced to "no group matched" by a downstream caller without an explicit `match` arm, per Pitfall 4 |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new external packages introduced by this plan — `async_trait`/`std` only |
</threat_model>

<verification>
`cargo build -p trackly-core && cargo build -p trackly-infra && cargo test -p trackly-core --test no_io_deps && cargo test -p trackly-infra ad::directory_mock && cargo test -p trackly-infra ad::cache && cargo clippy -p trackly-core -p trackly-infra -- -D warnings && cargo fmt --check`
</verification>

<success_criteria>
`AdDirectory` port trait exists and is ldap3-free (no_io_deps green); `MockAdDirectory`
deterministically resolves both display_name and role for the existing us100/us200
fixtures plus an unreachable() failure mode; `TtlCache<V>` proves put/get/expiry
semantics via fast (millisecond-scale) unit tests. All three files compile as part of
the workspace and are registered in their respective `mod.rs` files.
</success_criteria>

<output>
Create `.planning/phases/31-ad-bind-ad/31-01-SUMMARY.md` when done
</output>
