---
phase: 09-ad
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - crates/trackly-app/src/dto/auth.rs
  - crates/trackly-app/src/http/auth.rs
  - crates/trackly-app/src/tauri_cmds/auth.rs
  - crates/trackly-app/src/http/requests.rs
  - crates/trackly-app/src/tauri_cmds/requests.rs
  - ui/src/lib/bindings-phase9.ts
autonomous: true
requirements: [USR-08, USR-11, SET-10, REQ-06]
user_setup: []

must_haves:
  truths:
    - "POST /api/v1/auth_login accepts the existing {req:{login,password}} plus remember and routes through ctx.auth.login (AD fallback transparent — D-UX-01)"
    - "«Запомнить меня» (remember=true) yields a persistent sliding-30d session cookie; remember=false yields a session cookie cleared on browser close (D-UX-02)"
    - "AD settings get/set endpoints exist on BOTH transports (axum + Tauri) and are ManageSettings-gated"
    - "restoration-request create and approve-with-role endpoints exist on both transports"
    - "specta/bindings are regenerated so the frontend has typed access to AdSettingsDto, the extended LoginRequest, and approve payload"
  artifacts:
    - path: "crates/trackly-app/src/dto/auth.rs"
      provides: "LoginRequest.remember + AdSettingsDto + ApproveAdRegisterDto"
      contains: "AdSettingsDto"
    - path: "crates/trackly-app/src/http/auth.rs"
      provides: "remember cookie policy + settings_get_ad/settings_set_ad routes"
      contains: "settings_set_ad"
    - path: "crates/trackly-app/src/tauri_cmds/auth.rs"
      provides: "matching tauri commands"
      contains: "settings_set_ad"
    - path: "ui/src/lib/bindings-phase9.ts"
      provides: "regenerated TS types for Phase 9 DTOs"
      contains: "AdSettingsDto"
  key_links:
    - from: "crates/trackly-app/src/http/auth.rs"
      to: "ctx.auth.login"
      via: "build_auth_login passes remember to cookie policy"
      pattern: "remember"
    - from: "crates/trackly-app/src/http/requests.rs"
      to: "RequestService.approve_ad_register"
      via: "approve route"
      pattern: "approve"
---

<objective>
Expose the Phase 9 service capabilities over both transports (one DTO, two transports): extend `LoginRequest` with `remember` and wire the cookie policy (D-UX-02), add AD settings get/set, restoration-request create, and approve-with-role endpoints to both axum and Tauri, and regenerate the TS bindings. AD login itself needs no new handler — it already flows through the shared `ctx.auth.login` (D-UX-01); only the cookie policy and the admin/settings commands are new.

Purpose: makes the plan 02/03 service logic reachable from the browser (web-only AD login) and the admin desktop (settings + approval). USR-08 cookie behavior, SET-10/USR-11 settings transport, REQ-06 approval transport.
Output: extended auth DTOs + axum routes + Tauri commands + regenerated bindings-phase9.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-ad/09-CONTEXT.md
@.planning/phases/09-ad/09-RESEARCH.md
@.planning/phases/09-ad/09-PATTERNS.md
@.planning/phases/09-ad/09-03-SUMMARY.md

<interfaces>
<!-- Existing (extend, don't rewrite): -->
LoginRequest { login: String, password: String }                     // dto/auth.rs:19-23 — ADD #[serde(default)] remember: bool
NetworkSettingsDto { ... }                                           // dto/auth.rs:108-126 — pattern for AdSettingsDto
build_auth_login(ctx, session, payload) -> UserDto                   // http/auth.rs:99-125 — flush-before-insert (T-05-SF); ADD remember cookie policy
public_router / protected_router                                     // http/auth.rs:215-226 — register new routes
build_*_tauri + #[tauri::command] #[specta::specta]                 // tauri_cmds/auth.rs:24-40

<!-- From plan 02/03 (service layer, already built): -->
AuthService: ad_enabled/ad_auto_accept + AD connection getters/setters (ManageSettings-gated)
RequestService: approve_ad_register(request_id, role, caller); restoration request create; admin-only ad_register list
// Phase 6 bindings convention: bindings-phase6.ts kept separate (not gitignored bindings.ts) — mirror with bindings-phase9.ts
// i64 -> #[specta(type = i32)] rule (dto/auth.rs:1-9)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LoginRequest.remember + cookie policy + AdSettingsDto + approve DTO</name>
  <read_first>
    - crates/trackly-app/src/dto/auth.rs:1-9 (header: i64 -> #[specta(type=i32)] rule) + :19-23 (LoginRequest) + :108-126 (NetworkSettingsDto) + :159-243 (serde roundtrip / snake_case / no-secret test templates)
    - crates/trackly-app/src/http/auth.rs:99-125 (build_auth_login flush-before-insert; cookie/session handling) + :215-226 (routers)
    - 09-RESEARCH.md §"D-UX-02" (persistent vs session) + §Security Domain V3 (session-fixation flush-before-insert)
    - 09-PATTERNS.md §"dto/auth.rs" + §"http/auth.rs / tauri_cmds/auth.rs"
    - Phase 5 session config (sliding 30d, tower-sessions) — grep for the existing session Expiry setting in http/auth.rs or server/mod.rs
  </read_first>
  <behavior>
    - Test `login_request_remember_default_false`: deserializing `{login,password}` without `remember` → remember=false (serde default).
    - Test `ad_settings_dto_roundtrip`: AdSettingsDto serde roundtrip is snake_case and contains no secret (mirror existing no-secret test template).
    - Test (if a cookie-policy unit is feasible): remember=true sets a persistent expiry, remember=false a session-scoped one — otherwise cover via the http oneshot test in task 2.
  </behavior>
  <action>
    Add `#[serde(default)] pub remember: bool` to `LoginRequest` (password stays plain in DTO; AuthService wraps in Secret).
    Add `AdSettingsDto { enabled: bool, auto_accept: bool, host: String, port (#[specta(type=i32)]), domain: String, base_dn: String, name_attr: String, no_tls_verify: bool }` mirroring NetworkSettingsDto. NEVER include any password field (AD password is never stored).
    Add `ApproveAdRegisterDto { request_id (#[specta(type=i32)]), role: String }` (role default handled service-side, default employee) and a `RestoreRequestDto`/reuse the existing request-create DTO for restoration as appropriate.
    In `build_auth_login`, after the existing `flush().await` (keep flush-BEFORE-insert — T-05-SF / Security V3) and `insert("identity", ...)`, apply the «Запомнить меня» cookie policy from `payload.remember`: persistent sliding-30d when true, session cookie (cleared on browser close) when false — use the tower-sessions Expiry API consistent with the Phase 5 session config. Add serde + no-secret tests mirroring dto/auth.rs:159-243.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app dto::auth` (or the relevant test module) green incl. remember default + AdSettingsDto roundtrip + no-secret assertion.
    - `grep -c 'remember' crates/trackly-app/src/dto/auth.rs` >= 1; `grep -c 'AdSettingsDto' crates/trackly-app/src/dto/auth.rs` >= 1.
    - No password field in AdSettingsDto (`grep -i password crates/trackly-app/src/dto/auth.rs` shows only the existing LoginRequest.password, no AD-settings password).
    - `cargo build -p trackly-app` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app dto && cargo build -p trackly-app</automated>
  </verify>
  <done>LoginRequest carries remember with serde default; cookie policy persistent vs session wired in build_auth_login preserving flush-before-insert; AdSettingsDto + approve DTO added with no secret fields; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: axum + Tauri endpoints (AD settings, restoration, approve) + bindings regen</name>
  <read_first>
    - crates/trackly-app/src/http/auth.rs:99-125 + :215-226 (build_* + public/protected router registration; oneshot test style)
    - crates/trackly-app/src/tauri_cmds/auth.rs:6-7 (header: #[specta::specta] AFTER #[tauri::command]) + :24-40 (build_*_tauri helpers)
    - crates/trackly-app/src/http/requests.rs + tauri_cmds/requests.rs (existing request transition routes — analog for approve_ad_register)
    - 09-PATTERNS.md §"One DTO, two transports" + §"http/auth.rs / tauri_cmds/auth.rs"
    - The Phase 6 bindings split decision (bindings-phase6.ts) — STATE: "Phase 6 типы вынесены в отдельный файл (не gitignored bindings.ts)"; mirror as bindings-phase9.ts
    - The existing specta export/collect call site (grep for `specta` / `export` in tauri_cmds or main.rs) to register new commands
  </read_first>
  <behavior>
    - Test `settings_set_ad_requires_manage_settings` (http oneshot or service): non-admin session → 403; admin → 200.
    - Test `ad_register_list_admin_only_http`: employee session listing requests → no ad_register; admin → includes ad_register (REQ-06 over the wire).
    - Test `login_remember_persistent_cookie` (http oneshot): login with remember=true sets a persistent cookie expiry; remember=false a session cookie.
    - Test `approve_ad_register_http`: admin approve with role → 200 and target user activated (smoke through the service).
  </behavior>
  <action>
    Add axum routes (thin adapters over the plan 02/03 service methods): `settings_get_ad` / `settings_set_ad` (protected_router, ManageSettings via service authorize), `requests_approve_ad_register` (admin), restoration-request create (the blocked-screen submit — note: the create may be unauthenticated/public since the blocked user has no session; route it appropriately — likely a public endpoint that takes the login or a pre-issued token, consistent with how the blocked outcome is returned in plan 02). Mirror each with a `build_*_tauri` helper + `#[tauri::command] #[specta::specta]` wrapper (specta AFTER tauri::command). AD login itself stays on the existing `auth_login` route (web-only, D-UX-01) — no new login handler.
    Register all new commands in the specta export/collect list and regenerate the TS bindings into `ui/src/lib/bindings-phase9.ts` (mirror the Phase 6 bindings-phase6.ts split so it is committed to git, not the gitignored bindings.ts). Add the http oneshot tests for the behaviors above (reuse the existing oneshot + programmatic session-creation pattern; GovernorLayer note from STATE — create sessions via RusqliteSessionStore::create()).
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app http::` (or the relevant route tests) green: settings_set_ad 403-for-non-admin, ad_register admin-only over HTTP, remember cookie policy, approve smoke.
    - Both transports present: `grep -c 'settings_set_ad' crates/trackly-app/src/http/auth.rs` >= 1 AND `grep -c 'settings_set_ad' crates/trackly-app/src/tauri_cmds/auth.rs` >= 1.
    - `ui/src/lib/bindings-phase9.ts` exists and contains `AdSettingsDto` and the extended `LoginRequest` (with remember).
    - `cargo clippy -p trackly-app -- -D warnings` clean; `cargo build -p trackly-app` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app http && cargo clippy -p trackly-app -- -D warnings</automated>
  </verify>
  <done>AD settings + restoration + approve endpoints exist on both axum and Tauri, ManageSettings/admin-gated; remember cookie policy verified over HTTP; bindings-phase9.ts regenerated and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → settings/approve endpoints | privileged admin operations exposed over HTTP |
| blocked user → restoration endpoint | a non-session user submits a restore request |
| session cookie issuance | fixation + persistence policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Elevation | settings_set_ad / approve over HTTP | mitigate | Service-level authorize(ManageSettings/ManageUsers) enforced server-side (USR-06); 403 test for non-admin |
| T-09-16 | Spoofing | session fixation on login | mitigate | flush() BEFORE insert("identity") preserved (T-05-SF / Security V3); covered by existing + new login test |
| T-09-17 | Info Disclosure | AdSettingsDto leaking a secret | mitigate | No password field in AdSettingsDto; AD password never in any DTO/binding (D-Sec-01); grep gate |
| T-09-18 | Info Disclosure | ad_register over the wire to non-admin | mitigate | Admin-only filter enforced in the service/list path; http test ad_register_list_admin_only |
| T-09-19 | DoS/Tampering | unauthenticated restoration endpoint abuse | accept | Low-value LAN endpoint; existing rate-limit (SRV-02) applies; request is admin-reviewed before any effect |
</threat_model>

<verification>
- `cargo test -p trackly-app http` — transport tests green (authz, admin-only, cookie policy).
- Both transports expose every new command (grep gate).
- bindings-phase9.ts present with Phase 9 types.
</verification>

<success_criteria>
Login carries remember and sets the correct cookie scope; AD settings, restoration, and approve are reachable on both transports with server-side authorize; no AD secret appears in any DTO/binding; TS bindings regenerated. UI (plan 05) can now call typed endpoints.
</success_criteria>

<output>
Create `.planning/phases/09-ad/09-04-SUMMARY.md` when done
</output>
