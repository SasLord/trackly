---
phase: 03.2-deferred-uat-gap-closure
plan: 02
type: execute
wave: 2
depends_on:
  - "03.2-01"
files_modified:
  - crates/trackly-infra/src/repos/devices_sqlite.rs
  - crates/trackly-core/src/domain/devices.rs
  - crates/trackly-app/src/services/act_service.rs
  - ui/src/features/acts/ActFormItemsTable.svelte
  - crates/trackly-app/tests/devices_grouping.rs
  - crates/trackly-app/tests/acts_clone_handover.rs
autonomous: true
requirements:
  - DEF-2B
  - DEF-3
gap_closure: true

must_haves:
  truths:
    - "Два устройства с одинаковым name+model, но разным condition, отображаются как ДВЕ отдельные группы в list_grouped"
    - "Два устройства с одинаковым name+model и одинаковым condition схлопываются в ОДНУ группу count=2"
    - "UI DeviceGroup-dropdown в ActFormItemsTable показывает condition группы (state) в tooltip или подписи"
    - "После handover-акта через location_name, devices.location_id устройств = resolved location (не NULL)"
    - "do_return корректно записывает devices.location_id обратно (resolved из bulk_location_name); не затирает в NULL молча"
    - "Существующие 35 тестов остаются зелёными; cargo clippy 0 warnings; svelte-check 0 errors"
  artifacts:
    - path: "crates/trackly-infra/src/repos/devices_sqlite.rs"
      provides: "list_grouped GROUP BY d.type_id, d.name, d.condition"
      contains: "GROUP BY d.type_id, d.name"
    - path: "crates/trackly-app/src/services/act_service.rs"
      provides: "update_status_and_location_in_tx получает resolved_location_id"
      contains: "resolved_location_id"
    - path: "ui/src/features/acts/ActFormItemsTable.svelte"
      provides: "отображение g.repr.state в dropdown-item"
      contains: "repr.state"
    - path: "crates/trackly-app/tests/devices_grouping.rs"
      provides: "тест condition_key_splits_groups"
      contains: "condition_key_splits_groups"
    - path: "crates/trackly-app/tests/acts_clone_handover.rs"
      provides: "тест handover_via_location_name_sets_device_location_id"
      contains: "handover_via_location_name_sets_device_location_id"
  key_links:
    - from: "list_grouped SQL GROUP BY"
      to: "d.condition добавлен в ключ"
      via: "GROUP BY d.type_id, d.name, d.condition"
      pattern: "d.condition"
    - from: "list_grouped location_name LEFT JOIN subquery"
      to: "repr location берётся из той же condition-группы (NULL-safe)"
      via: "AND d2.condition IS d.condition в WHERE subquery"
      pattern: "d2.condition IS d.condition"
    - from: "act_service.rs create handover loop"
      to: "update_status_and_location_in_tx(..., resolved_location_id, ...)"
      via: "замена payload.location_id → resolved_location_id на line ~424"
      pattern: "resolved_location_id"
---

<objective>
Архитектурные исправления: (1) DEF-2B — group key list_grouped включает condition (d.condition) — два устройства с разным condition = две группы; UI показывает condition группы. (2) DEF-3 — ActService::create передаёт resolved_location_id в handover-UPDATE вместо payload.location_id (который None при создании через location_name).

Purpose: Закрыть DEF-2B (корректная группировка по condition устраняет неверный выбор repr при клонировании) и DEF-3 (devices.location_id актуализируется после handover — таблица устройств показывает текущее расположение).
Output: Изменённые SQL-запрос (без миграции), исправление 1-строки в act_service.rs, UI tooltip с condition, два новых теста.
</objective>

<execution_context>
@/Users/madsas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madsas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-deferred-uat-gap-closure/03.2-CONTEXT.md
@.planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-DEFERRED-UAT-ITEMS.md
@.planning/phases/03.2-deferred-uat-gap-closure/03.2-01-SUMMARY.md
</context>

<interfaces>
<!-- Извлечено из кодовой базы для executor-а без необходимости повторного чтения. -->

Из crates/trackly-core/src/domain/devices.rs:
  pub struct DeviceGroupRow { pub repr: DeviceRow, pub ids: Vec&lt;i64&gt;, pub count: i64 }
  pub struct DeviceRow { ..., pub state: Option&lt;String&gt;, ... }  // state ↔ DB condition

Из crates/trackly-infra/src/repos/devices_sqlite.rs (list_grouped, line ~959):
  GROUP BY d.type_id, d.name  ← добавить d.condition

Из crates/trackly-app/src/services/act_service.rs (handover loop):
  resolved_location_id: Option&lt;i64&gt;  computed at line ~258 (location_name → id)
  update_status_and_location_in_tx(&amp;tx, dev_id, in_work_status_id, payload.location_id, now) at line ~424
  ← изменить payload.location_id → resolved_location_id

Из crates/trackly-infra/src/repos/devices_sqlite.rs (line ~262):
  fn update_status_and_location_in_tx(&amp;self, tx, device_id: i64, status_id: i64, location_id: Option&lt;i64&gt;, now_utc: i64) → DeviceRow
  SQL: UPDATE devices SET status_id=?1, location_id=?2 ...

Из crates/trackly-app/src/services/act_service.rs (do_return, line ~663):
  resolved_bulk_location_id: Option&lt;i64&gt;  (bulk_location_name → id)
  update_full_in_tx → записывает effective_location = per_row_loc_id.or(resolved_bulk_location_id)
  Путь возврата уже корректен — проверить что effective_location не None при наличии bulk_location_name.

Из ui/src/bindings.ts:
  export type DeviceGroup = { repr: DeviceDto; count: number; ids: number[] }
  export type DeviceDto = { ..., state: string | null, ... }

Из ui/src/features/acts/ActFormItemsTable.svelte (dropdown render, line ~202-213):
  {#each suggestionsByRow[idx] as g (g.repr.id)}
    &lt;button&gt;
      {g.repr.name}
      {g.repr.model}   // opt-model
      SN / инв. / ×count  // opt-sn / opt-inv / opt-count
    &lt;/button&gt;
  Добавить: {#if g.repr.state}&lt;span class="opt-state"&gt;{g.repr.state}&lt;/span&gt;{/if}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DEF-2B — добавить d.condition в GROUP BY list_grouped + UI tooltip</name>

  <read_first>
    - crates/trackly-infra/src/repos/devices_sqlite.rs — list_grouped function (line ~903-1064): SQL с GROUP BY d.type_id, d.name (line ~959); читать строку GroupBy и комментарий "Round 8 fix"
    - crates/trackly-app/tests/devices_grouping.rs — существующие тесты группировки: структура helper non_unique_device, make_service; примеры assert_eq!(groups.len(), N)
    - ui/src/features/acts/ActFormItemsTable.svelte — dropdown-рендер (line ~200-218): существующие span.opt-name, opt-model, opt-sn, opt-inv, opt-count
  </read_first>

  <files>
    crates/trackly-infra/src/repos/devices_sqlite.rs,
    crates/trackly-app/tests/devices_grouping.rs,
    ui/src/features/acts/ActFormItemsTable.svelte
  </files>

  <action>
    ЧАСТЬ A — SQL GROUP BY:
    В list_grouped SQL изменить строку "GROUP BY d.type_id, d.name" на "GROUP BY d.type_id, d.name, d.condition". Миграция НЕ нужна — изменение только в SQL запросе, схема БД не меняется. Обновить комментарий выше GROUP BY: убрать упоминание что condition НЕ в ключе, добавить "condition включён в ключ — groups with same name but different condition remain separate (DEF-2B)". Поле state (d.condition) уже вычитывается в row ~975 и попадает в DeviceRow.state через `from_row` — дополнительный маппинг не нужен.

КРИТИЧНО (representative location correctness — иначе DEF-2B даёт неверный repr): LEFT JOIN для location_name (line ~949-956) резолвит location через `SELECT MAX(d2.location_id) ... WHERE d2.type_id=d.type_id AND d2.name=d.name`. Этот subquery НЕ фильтрует по condition — после добавления condition в GROUP BY группа "Хорошее" получила бы location устройства "Новое" (cross-contamination repr). Обязательно расширить WHERE subquery (line ~952-956), добавив condition-matching. Использовать SQLite-оператор `IS` (НЕ `=`): `AND d2.condition IS d.condition`. (`=` дал бы FALSE при обоих NULL → location_name стал бы NULL для устройств без condition; `IS` в SQLite трактует NULL IS NULL как TRUE.)

    ЧАСТЬ B — Тест в devices_grouping.rs:
    Добавить новый тест condition_key_splits_groups в конце файла:
    - Создать два устройства: одинаковые type_id=1, name="DVD-ROM ASUS", model="BW-16D1HT", разный state: "Новое" и "Хорошее" (через DeviceNew.state = Some("Новое".into()) и Some("Хорошее".into()))
    - Вызвать svc.list_grouped(DeviceFilter::default(), page)
    - assert_eq!(groups.len(), 2, "разный condition → две группы")
    - Дополнительно assert, что repr.state двух групп различны и соответствуют ожидаемым ("Новое" и "Хорошее") — ловит cross-contamination repr (если бы JOIN не фильтровал по condition). Например: собрать states = groups.iter().map(|g| g.repr.state.clone()).collect(), проверить что содержит оба значения.
    Добавить второй тест condition_key_same_condition_collapses:
    - Создать два устройства: одинаковые name + state = "Новое"
    - assert_eq!(groups.len(), 1); assert_eq!(groups[0].count, 2)

    ЧАСТЬ C — UI tooltip в ActFormItemsTable.svelte:
    В dropdown-рендере после строки {#if g.repr.model}...{/if} (или после opt-count) добавить:
    {#if g.repr.state}
      &lt;span class="opt-state"&gt;{g.repr.state}&lt;/span&gt;
    {/if}
    В &lt;style&gt; добавить .opt-state со стилем аналогичным .opt-model: font-size: var(--font-size-label), color: var(--color-text-secondary). Не трогать логику pickGroup/handleQueryInput — только рендер.

    Важно: g.repr — это DeviceDto, поле state: string | null. Отображать только если не null.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app condition_key 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - SQL GROUP BY содержит d.condition: grep -n "GROUP BY d.type_id" crates/trackly-infra/src/repos/devices_sqlite.rs показывает "d.type_id, d.name, d.condition"
    - LEFT JOIN subquery фильтрует по condition (NULL-safe): grep -n "d2.condition IS d.condition" crates/trackly-infra/src/repos/devices_sqlite.rs → 1 совпадение (repr location берётся из той же condition-группы)
    - Тест condition_key_splits_groups: cargo test -p trackly-app condition_key_splits_groups → ok
    - Тест condition_key_splits_groups также проверяет, что repr.state групп различны (не cross-contaminated)
    - Тест condition_key_same_condition_collapses: cargo test -p trackly-app condition_key_same_condition_collapses → ok
    - ActFormItemsTable.svelte содержит "opt-state" class: grep -c "opt-state" ui/src/features/acts/ActFormItemsTable.svelte ≥ 2 (style + usage)
    - svelte-check 0 errors
    - Все существующие тесты devices_grouping.rs проходят: cargo test -p trackly-app --test devices_grouping → все ok
  </acceptance_criteria>

  <done>list_grouped возвращает две группы для одинаковых name+model с разным condition; UI показывает condition в dropdown-item; тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: DEF-3 — передавать resolved_location_id в handover-UPDATE + integration test</name>

  <read_first>
    - crates/trackly-app/src/services/act_service.rs — строки ~258-263 (resolved_location_id вычисление из location_name), строки ~420-426 (update_status_and_location_in_tx передаёт payload.location_id — БАГ), строки ~562-870 (do_return: resolved_bulk_location_id, update_full_in_tx с effective_location)
    - crates/trackly-app/tests/acts_clone_handover.rs — структура существующих тестов: make_service(), ActHandoverPayload, ActReturnPayload, svc.create(), svc.do_return()
    - crates/trackly-app/tests/devices_location_roundtrip.rs — РЕФЕРЕНС-ПАТТЕРН для теста: как создаётся location и как читается devices.location_id в тестах (writer/reader доступ); реплицировать этот подход вместо изобретения
    - crates/trackly-infra/src/repos/devices_sqlite.rs — update_status_and_location_in_tx (line ~257-280): UPDATE devices SET location_id=?2
  </read_first>

  <files>
    crates/trackly-app/src/services/act_service.rs,
    crates/trackly-app/tests/acts_clone_handover.rs
  </files>

  <action>
    ЧАСТЬ A — Fix в act_service.rs:
    Найти строку ~424: update_status_and_location_in_tx(&tx, dev_id, in_work_status_id, payload.location_id, now)
    Заменить payload.location_id на resolved_location_id.
    Переменная resolved_location_id уже вычислена выше (line ~258-262) в том же closure. Никаких новых переменных не нужно — это однострочное исправление.

    Проверить do_return на возможность silent NULL: в do_return effective_location вычисляется как per_row_loc_id.or(resolved_bulk_location_id) (line ~732). Если payload.apply_to_all=true и bulk_location_name дан, effective_location будет Some. Если apply_to_all=false и per_row_loc_id=None — effective_location=None, что означает location_id не меняется (NULL). Это приемлемое поведение (do_return НЕ обнуляет силой — update_full_in_tx пишет то что передано). Добавить комментарий в коде рядом с do_return effective_location: "// DEF-3: если effective_location=None, update_full_in_tx запишет NULL в location_id. Caller обязан передать bulk_location_name или location_name_override для восстановления расположения." — это документирует ожидаемое поведение без изменения логики.

    ЧАСТЬ B — Integration test в acts_clone_handover.rs:
    Добавить тест handover_via_location_name_sets_device_location_id в конце файла. Структура теста:
    1. make_service() → svc, _dir
    2. Создать location "Отдел кадров" через locations_repo или через helper (если test_support предоставляет), либо напрямую через writer transaction. Альтернатива: использовать svc.create handover с location_name и проверить device.location_id после.
       Если прямой API для создания location из теста нет — INSERT через conn напрямую (test_support writer доступен). Смотри существующие тесты devices_location_roundtrip.rs за паттерном.
    3. Создать одно устройство (DeviceNew с status_id=1 "на_складе").
    4. Создать handover-акт: ActHandoverPayload { location_name: Some("Отдел кадров"), location_id: None, items: [{ device_id, quantity: 1, device_ids: [] }], ... }
    5. После svc.create: получить устройство через svc_device.get(device_id) или через reader conn, проверить device.location_id == Some(location_id_of_odiel_kadrov). Это основной assert DEF-3.
    6. Проверить do_return: svc.do_return с bulk_location_name: Some("Склад"), затем assert device.location_id == Some(sklad_location_id).

    Если доступ к DeviceService из acts_clone_handover.rs требует добавления dep — проверить fixtures или make_full_service helper в test_support. Если нет прямого способа получить DeviceService — читать devices.location_id через прямой SQL на reader conn (test_support предоставляет writer/readers).

    Обернуть в tokio::time::timeout(Duration::from_secs(30), ...) как другие тесты в файле.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app handover_via_location_name 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - act_service.rs строка ~424 содержит resolved_location_id (не payload.location_id): grep -n "update_status_and_location_in_tx" crates/trackly-app/src/services/act_service.rs показывает resolved_location_id в аргументах
    - Тест handover_via_location_name_sets_device_location_id: cargo test -p trackly-app handover_via_location_name_sets_device_location_id → ok
    - Тест проверяет device.location_id после handover == resolved location (assert_eq!, не просто is_some)
    - Тест проверяет device.location_id после do_return (restore path)
    - Все существующие тесты в acts_clone_handover.rs проходят: cargo test -p trackly-app --test acts_clone_handover → все ok
    - cargo clippy -p trackly-app 0 новых warnings
  </acceptance_criteria>

  <done>ActService::create передаёт resolved_location_id в handover-UPDATE; devices.location_id = расположение акта после handover. do_return сохраняет документированный контракт effective_location. Integration test подтверждает end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP /api/v1/acts (POST) → act_service.rs | payload.location_name — user-supplied строка, резолвится в id через resolve_location_id_in_tx |
| Tauri invoke devices_list_grouped | filter.state — user-supplied строка, используется как SQL параметр через rusqlite::params! |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-02-01 | Tampering | list_grouped SQL GROUP BY + d.condition | accept | d.condition — уже существующий столбец, добавляется в GROUP BY, не в WHERE-user-input. SQL по-прежнему параметризован через rusqlite::params![status_id, limit, offset]. Инъекция невозможна. |
| T-03.2-02-02 | Tampering | act_service.rs: payload.location_name → resolved_location_id в UPDATE | mitigate | resolve_location_id_in_tx выполняет INSERT OR IGNORE + SELECT с параметризованным SQL (location_name через rusqlite::params!). Результат — i64 id, не строка — в UPDATE используется только id. Инъекции нет. |
| T-03.2-02-03 | Elevation of Privilege | Handover UPDATE devices.location_id = resolved (не None) | accept | До фикса location_id был None → не было изменений. После фикса — пишется resolved id. Это корректирует ошибку, не расширяет привилегий. Все writes уже внутри single-writer tx. |
| T-03.2-02-04 | Information Disclosure | list_grouped возвращает condition из БД в UI | accept | condition — не PII, это состояние устройства ("Хорошее", "Новое"). Доступ к list_grouped защищён сессионной аутентификацией. |
| T-03.2-02-SC | Tampering | npm/cargo install | accept | Этот план не добавляет новых пакетов — только редактирует .rs и .svelte файлы. |

Угрозы минимальные: DEF-3 fix однострочный, sql параметризован; DEF-2B расширяет только GROUP BY — структура данных не меняется.
</threat_model>

<verification>
После выполнения обеих задач:

1. Cargo test полный suite:
   `cd /Users/madsas/Projects/trackly && cargo test --workspace --quiet 2>&1 | tail -15`
   Ожидаемо: все тесты ok, 2 новых теста добавлены.

2. Новые тесты:
   `cargo test -p trackly-app condition_key 2>&1 | tail -5` → condition_key_splits_groups ok, condition_key_same_condition_collapses ok
   `cargo test -p trackly-app handover_via_location_name 2>&1 | tail -5` → ok

3. Cargo clippy:
   `cargo clippy --workspace -- -D warnings 2>&1 | tail -10`

4. Svelte-check:
   `cd /Users/madsas/Projects/trackly/ui && pnpm exec svelte-check --tsconfig tsconfig.json 2>&1 | tail -5`

5. Source-assertions:
   - `grep -n "GROUP BY d.type_id" crates/trackly-infra/src/repos/devices_sqlite.rs` → d.type_id, d.name, d.condition
   - `grep -n "update_status_and_location_in_tx" crates/trackly-app/src/services/act_service.rs | grep -v "^.*//.*update"` → содержит resolved_location_id
   - `grep -c "opt-state" ui/src/features/acts/ActFormItemsTable.svelte` → ≥2
</verification>

<success_criteria>
- GROUP BY в list_grouped включает d.condition
- Тест condition_key_splits_groups: два устройства с разным condition → two groups (assert_eq!(groups.len(), 2))
- Тест condition_key_same_condition_collapses: два устройства с одинаковым condition → одна группа count=2
- ActFormItemsTable dropdown показывает state (condition) группы через span.opt-state
- act_service.rs передаёт resolved_location_id в update_status_and_location_in_tx (строка ~424)
- Тест handover_via_location_name_sets_device_location_id проверяет device.location_id = resolved id после handover
- cargo test --workspace, cargo clippy --workspace -D warnings — все зелёные
- svelte-check 0 errors
</success_criteria>

<output>
Создать `.planning/phases/03.2-deferred-uat-gap-closure/03.2-02-SUMMARY.md` по завершении
</output>
