---
phase: 03.2-deferred-uat-gap-closure
verified: 2026-06-06T12:00:00Z
status: human_needed
score: 6/6
overrides_applied: 0
human_verification:
  - test: "DEF-1: Открыть ActFormModal, перейти в поле 'Кто сдал' — без набора символов нажать Tab или кликнуть мышью. Убедиться, что dropdown с ≥1 вариантом открывается немедленно (если в БД есть записи о сдавших)."
    expected: "Dropdown PersonAutocomplete открывается сразу при фокусе с первыми 20 suggestion-ами или сообщением 'нет вариантов'."
    why_human: "Поведение зависит от наполнения БД и DOM-события onfocus; grep подтверждает код, но не живое взаимодействие."
  - test: "DEF-1: В ActFormItemsTable ввести строку и нажать в поле 'Устройство со склада' — убедиться, что dropdown открывается немедленно при фокусе."
    expected: "DeviceAutocompleteField показывает dropdown при focus даже с пустым полем."
    why_human: "Аналогично PersonAutocomplete — требует живого теста в браузере/Tauri."
  - test: "DEF-1 edit-mode: открыть существующий акт на редактирование. Убедиться, что заполненное поле 'Кто сдал' НЕ показывает dropdown до явного клика/ввода."
    expected: "suppressDropdown=true из onMount-seed предотвращает auto-open при edit-mode pre-fill."
    why_human: "Контракт edit-mode vs. user-focus — нужен реальный тест с pre-filled значением."
  - test: "DEF-2A: Добавить строку в ActFormItemsTable, выбрать устройство (группу). Добавить вторую строку и убедиться, что та же группа отсутствует в dropdown второй строки."
    expected: "getSelectedIds фильтрует уже выбранные group_ids; дубликаты не предлагаются."
    why_human: "Требует живого взаимодействия с формой (две строки в table, UI state)."
  - test: "DEF-2B: Если в БД есть устройства с одинаковым названием, но разным condition — убедиться, что в dropdown ActFormItemsTable они показываются ДВУМЯ отдельными строками с подписью condition."
    expected: "list_grouped GROUP BY condition разделяет их; span.opt-state показывает 'Хорошее'/'Новое' в dropdown."
    why_human: "Требует живых данных в БД и визуальной проверки dropdown UI."
  - test: "DEF-3: Создать handover-акт через поле 'Расположение' (вводить название, не выбирать ID). После создания открыть карточку устройства — убедиться, что поле 'Расположение' отображает введённое название."
    expected: "devices.location_id обновлён на resolved location после handover via location_name."
    why_human: "End-to-end UI test — требует проверки значения в таблице устройств после операции."
---

# Phase 03.2: Deferred UAT Gap Closure — Verification Report

**Phase Goal:** Закрыть 3 отложенных UAT-пункта (DEF-1, DEF-2, DEF-3) из round-3 ручного UAT Phase 03.1. Включает DEF-2A (dedupe) и DEF-2B (condition grouping) как подзадачи DEF-2.

**Verified:** 2026-06-06T12:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | PersonAutocomplete содержит handleFocus, открывающий dropdown при focus с пустым полем | VERIFIED | `PersonAutocomplete.svelte:108` — функция `handleFocus` с `suppressDropdown=false` + прямой setTimeout(0) fetch в обход `$effect` v.length guard |
| 2 | DeviceAutocompleteField содержит handleFocus на обоих элементах (input и textarea) | VERIFIED | `DeviceAutocompleteField.svelte:160` — `handleFocus`; onfocus на input `:259` и textarea `:275` |
| 3 | Edit-mode pre-fill не открывает dropdown: suppressDropdown=true из onMount сохранён | VERIFIED | `PersonAutocomplete.svelte:53-57` и `DeviceAutocompleteField.svelte:85-90` — onMount-seed не изменён; handleFocus явно сбрасывает перед fetch |
| 4 | ActFormItemsTable скрывает уже выбранные device groups из dropdown (DEF-2A) | VERIFIED | `ActFormItemsTable.svelte:175` — `getSelectedIds(excludeIdx)`: 3 вхождения; `:114` фильтрация в handleQueryInput; `:217` inline-фильтр в dropdown render |
| 5 | list_grouped GROUP BY включает d.condition; два устройства с разным condition = 2 группы (DEF-2B) | VERIFIED | `devices_sqlite.rs:964` — `GROUP BY d.type_id, d.name, d.condition`; `:958` — `AND d2.condition IS d.condition` (NULL-safe) в repr-subquery; тесты `condition_key_splits_groups` и `condition_key_same_condition_collapses` — оба PASS |
| 6 | act_service.rs передаёт resolved_location_id (не payload.location_id) в handover-UPDATE devices.location_id (DEF-3) | VERIFIED | `act_service.rs:429` — аргумент `resolved_location_id` в `update_status_and_location_in_tx`; тест `handover_via_location_name_sets_device_location_id` — PASS |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `ui/src/lib/components/PersonAutocomplete.svelte` | handleFocus + onfocus binding + suppressDropdown contract | VERIFIED | `:108` `function handleFocus`, `:198` `onfocus={handleFocus}`, `:111` suppressDropdown=false |
| `ui/src/features/devices/DeviceAutocompleteField.svelte` | handleFocus + onfocus на input и textarea + bypass v.length guard | VERIFIED | `:160` `function handleFocus`, `:259` и `:275` onfocus на оба элемента |
| `ui/src/features/acts/ActFormItemsTable.svelte` | getSelectedIds + opt-state span | VERIFIED | `:175` `getSelectedIds`, `:217` inline-фильтр, `:230` `span.opt-state`, `:363` `.opt-state` стиль |
| `crates/trackly-infra/src/repos/devices_sqlite.rs` | GROUP BY d.type_id, d.name, d.condition + IS subquery | VERIFIED | `:964` GROUP BY, `:958` IS d.condition |
| `crates/trackly-app/src/services/act_service.rs` | resolved_location_id в update_status_and_location_in_tx | VERIFIED | `:429` — `resolved_location_id` как 4-й аргумент |
| `crates/trackly-app/tests/devices_grouping.rs` | тесты condition_key_splits_groups + condition_key_same_condition_collapses | VERIFIED | оба теста определены (:548, :599) и проходят |
| `crates/trackly-app/tests/acts_clone_handover.rs` | тест handover_via_location_name_sets_device_location_id | VERIFIED | `:918` — полный integration test с assert_eq на location_id после handover и do_return |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| PersonAutocomplete handleFocus | suppressDropdown=false + fetch с delay=0 | onfocus binding на input | WIRED | `:108-129` — функция полностью подключена, `:198` onfocus binding активен |
| DeviceAutocompleteField handleFocus | fetch bypass v.length < 1 guard | setTimeout(0) напрямую, не через $effect | WIRED | `:160-181` — прямой fetch с devices.autocomplete; `:166` setTimeout(0) |
| ActFormItemsTable pickGroup / dropdown render | фильтрация через getSelectedIds Set | в handleQueryInput и inline в dropdown | WIRED | `:114` в handleQueryInput; `:217` в each-фильтре |
| list_grouped SQL GROUP BY | d.condition добавлен в ключ | GROUP BY d.type_id, d.name, d.condition | WIRED | `:964` — d.condition в GROUP BY |
| list_grouped location LEFT JOIN subquery | repr location из той же condition-группы | AND d2.condition IS d.condition | WIRED | `:958` — NULL-safe IS оператор |
| act_service.rs create handover loop | resolved_location_id передаётся в UPDATE | замена payload.location_id → resolved_location_id | WIRED | `:429` — аргумент в вызове |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| ActFormItemsTable.svelte | `suggestionsByRow[idx]` | `devices.listGrouped(...)` через `handleQueryInput` | Да — API call с реальными фильтрами; результат фильтруется через getSelectedIds | FLOWING |
| PersonAutocomplete.svelte | `suggestions` в handleFocus | `acts.suggestPerson(field, value)` с delay=0 | Да — реальный API call; результат идёт в `open = suggestions.length > 0` | FLOWING |
| DeviceAutocompleteField.svelte | `suggestions` в handleFocus | `devices.autocomplete(field, value, ...)` с delay=0 | Да — реальный API call с параметрами контекста | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| condition_key_splits_groups | `cargo test -p trackly-app --test devices_grouping condition_key` | 2 passed; 0 failed | PASS |
| condition_key_same_condition_collapses | (включён в ту же команду) | ok | PASS |
| handover_via_location_name_sets_device_location_id | `cargo test -p trackly-app --test acts_clone_handover handover_via_location_name_sets_device_location_id` | 1 passed; 0 failed | PASS |
| Полный тест-сьют workspace | `cargo test --workspace --quiet` | все суиты ok; 0 failed | PASS |

---

### Probe Execution

Step 7c: SKIPPED — фаза не объявляет `probe-*.sh` файлов; изменения фронтенд-only (plan 01) и однострочный бэкенд-фикс (plan 02); проверка проведена через cargo test.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DEF-1 | 03.2-01 | Focus в autocomplete открывает dropdown сразу | SATISFIED | handleFocus в PersonAutocomplete + DeviceAutocompleteField с onfocus binding и bypass $effect guard |
| DEF-2A | 03.2-01 | Dedupe уже-выбранных device groups в ActFormItemsTable | SATISFIED | getSelectedIds(excludeIdx) с двойной фильтрацией (handleQueryInput + dropdown render) |
| DEF-2B | 03.2-02 | list_grouped GROUP BY condition; разный condition = разные группы | SATISFIED | SQL GROUP BY d.condition + NULL-safe IS subquery; 2 новых теста PASS |
| DEF-3 | 03.2-02 | devices.location_id обновляется после handover via location_name | SATISFIED | act_service.rs resolved_location_id; integration test PASS с assert_eq |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | — |

Нет `TBD`/`FIXME`/`XXX` маркеров в изменённых файлах. Нет пустых возвращаемых значений в новых функциях. Нет TODO без номера issue.

---

### Human Verification Required

Все 6 observable truths подтверждены кодом и тестами. Следующие проверки требуют живого запуска приложения — их невозможно верифицировать статическим анализом:

#### 1. DEF-1: PersonAutocomplete focus-open

**Test:** Открыть ActFormModal, поставить фокус в поле «Кто сдал» без нажатия клавиш.
**Expected:** Dropdown с ≥1 suggestion открывается немедленно (если в БД есть данные о giver/receiver).
**Why human:** Поведение onfocus зависит от DOM-событий и наполнения БД — grep подтверждает код, но не живой UX.

#### 2. DEF-1: DeviceAutocompleteField focus-open

**Test:** В ActFormItemsTable кликнуть в поле «Устройство со склада» с пустым значением.
**Expected:** Dropdown с device groups открывается немедленно.
**Why human:** Аналогично пункту 1.

#### 3. DEF-1: edit-mode — НЕ открывать dropdown

**Test:** Открыть существующий акт на редактирование, убедиться что pre-filled поля не открывают dropdown автоматически.
**Expected:** onMount-seed suppressDropdown=true предотвращает auto-open.
**Why human:** Нужно проверить real DOM-поведение при программном filled значении vs. user-focus.

#### 4. DEF-2A: dedupe в dropdown второй строки

**Test:** Добавить строку в таблицу, выбрать устройство. Добавить вторую строку, открыть dropdown.
**Expected:** Ранее выбранная группа устройств не отображается в dropdown второй строки.
**Why human:** Требует живого multi-row state в форме.

#### 5. DEF-2B: UI показывает condition в dropdown

**Test:** При наличии в БД устройств с разным condition — убедиться, что в dropdown отображаются отдельные строки с подписью condition (span.opt-state).
**Expected:** «Хорошее» и «Новое» видны как отдельные группы в dropdown.
**Why human:** Визуальная проверка UI рендера и реальных данных.

#### 6. DEF-3: devices.location_id после handover via location_name

**Test:** Создать handover акт через ввод названия расположения (не выбирая из списка). Открыть список устройств.
**Expected:** Устройство показывает расположение, соответствующее введённому в акте.
**Why human:** End-to-end UI flow — devices table рендер + актуальность данных.

---

### Gaps Summary

Нет gaps — все 6 must-have truths VERIFIED кодом и integration-тестами. Фаза переходит в `human_needed` исключительно из-за необходимости живого UAT для подтверждения UX-поведения (focus-open, edit-mode suppression, UI рендер condition).

Структурных проблем не обнаружено:
- Все 5 коммитов (3377478, 8ef17cb, f6eacbc, fccd04b, 237fa82) существуют и содержат заявленные изменения.
- Cargo test workspace: 0 failed во всех суитах.
- Нет долгового маркеров (TBD/FIXME/XXX) в изменённых файлах.

---

_Verified: 2026-06-06T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
