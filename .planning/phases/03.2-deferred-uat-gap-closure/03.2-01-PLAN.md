---
phase: 03.2-deferred-uat-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/PersonAutocomplete.svelte
  - ui/src/features/devices/DeviceAutocompleteField.svelte
  - ui/src/features/acts/ActFormItemsTable.svelte
autonomous: true
requirements:
  - DEF-1
  - DEF-2A
gap_closure: true

must_haves:
  truths:
    - "Фокус в пустое поле 'Кто сдал'/'Кто принял' сразу открывает dropdown с первыми 20 suggestion-ами"
    - "Фокус в пустое поле device-picker в ActFormItemsTable сразу открывает dropdown с первыми 20 группами"
    - "Edit-mode pre-fill в PersonAutocomplete и DeviceAutocompleteField НЕ открывает dropdown до явного focus/keystroke пользователя"
    - "Уже выбранные в других строках ActFormItemsTable группы/devices не появляются в dropdown новой строки"
    - "Существующие 35 тестов остаются зелёными; svelte-check 0 ошибок"
  artifacts:
    - path: "ui/src/lib/components/PersonAutocomplete.svelte"
      provides: "handleFocus + onfocus binding на input; ArrowDown-при-закрытом без requirementа value.length > 0"
      contains: "function handleFocus"
    - path: "ui/src/features/devices/DeviceAutocompleteField.svelte"
      provides: "handleFocus + onfocus на input и textarea; focus-triggered fetch обходит v.length < 1 guard"
      contains: "function handleFocus"
    - path: "ui/src/features/acts/ActFormItemsTable.svelte"
      provides: "dedupe уже-выбранных device IDs из всех rows при рендере dropdown"
      contains: "selectedIds"
  key_links:
    - from: "PersonAutocomplete.svelte handleFocus"
      to: "suppressDropdown = false + scheduleFetch(value, 0)"
      via: "onfocus binding на input"
      pattern: "suppressDropdown = false"
    - from: "DeviceAutocompleteField.svelte handleFocus"
      to: "fetch bypass v.length < 1 guard"
      via: "отдельный setTimeout в handleFocus, не через $effect"
      pattern: "function handleFocus"
    - from: "ActFormItemsTable pickGroup / dropdown render"
      to: "filter by selectedIds Set"
      via: "derived selectedIds из items[].group_ids"
      pattern: "selectedIds"
---

<objective>
UX-polish: привести PersonAutocomplete и DeviceAutocompleteField к поведению LocationAutocomplete (commit b2c43a5) — фокус → dropdown сразу. Добавить dedupe уже-выбранных device-групп в ActFormItemsTable.

Purpose: Закрыть DEF-1 (UAT-item: "во всех autocomplete полях фокус открывает dropdown") и DEF-2A ("в ActFormItemsTable нельзя добавить одну группу дважды через UI").
Output: Три изменённых Svelte-файла; backend не меняется (acts.suggestPerson и devices.autocomplete уже поддерживают empty prefix).
</objective>

<execution_context>
@/Users/madsas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madsas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-deferred-uat-gap-closure/03.2-CONTEXT.md
@.planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-DEFERRED-UAT-ITEMS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEF-1 — добавить handleFocus в PersonAutocomplete</name>

  <read_first>
    - ui/src/lib/components/PersonAutocomplete.svelte — целевой файл: нет onfocus, есть suppressDropdown, lastSelected, onMount-seed
    - ui/src/lib/components/LocationAutocomplete.svelte — canonical pattern (lines 59-63 handleFocus; line 130 onfocus=; lines 80-85 ArrowDown-при-закрытом)
  </read_first>

  <files>ui/src/lib/components/PersonAutocomplete.svelte</files>

  <action>
    Добавить функцию handleFocus (по образцу LocationAutocomplete lines 59-63) непосредственно после функции handleInput:

    handleFocus снимает suppressDropdown (false), обнуляет lastSelected (null), и вызывает fetch с delay 0 через существующий механизм debounce: запускает setTimeout(0) который вызывает acts.suggestPerson(field, value) и открывает dropdown при !suppressDropdown. Не использовать существующий $effect (он блокируется guard v.length < 1 и suppressDropdown), а написать прямой fetch-вызов по аналогии с scheduleFetch в LocationAutocomplete.

    Добавить onfocus={handleFocus} на input-элемент (строка с oninput={handleInput}).

    Исправить handleKeydown: существующая ветка ArrowDown имеет guard "value.length > 0" (line 109) — снять этот guard, чтобы ArrowDown при пустом поле тоже открывал dropdown (аналог LocationAutocomplete lines 80-85). Сохранить suppressDropdown = false перед fetch.

    Контракт suppressDropdown (edit-mode pre-fill) сохранить: onMount-seed (value.length > 0 → lastSelected=value, suppressDropdown=true) остаётся без изменений. handleFocus явно сбрасывает suppressDropdown=false — пользователь сознательно фокусируется в поле, это не programmatic re-render.

    Backend acts.suggestPerson уже поддерживает empty prefix (frequency DESC) — изменений в backend НЕТ.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui && pnpm exec svelte-check --tsconfig tsconfig.json 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - PersonAutocomplete.svelte содержит функцию handleFocus (grep: "function handleFocus")
    - Input-элемент имеет onfocus={handleFocus} (grep: "onfocus={handleFocus}")
    - handleFocus явно устанавливает suppressDropdown = false (grep: "suppressDropdown = false" внутри handleFocus)
    - handleKeydown ветка ArrowDown не требует value.length > 0 (grep: убедиться что guard "value.length > 0" на ArrowDown убран или value.length допускает 0)
    - svelte-check 0 errors / 0 warnings
  </acceptance_criteria>

  <done>PersonAutocomplete: фокус в пустое поле показывает ≥1 suggestion (если данные есть); edit-mode pre-fill не открывает dropdown до явного действия пользователя.</done>
</task>

<task type="auto">
  <name>Task 2: DEF-1 — добавить handleFocus в DeviceAutocompleteField</name>

  <read_first>
    - ui/src/features/devices/DeviceAutocompleteField.svelte — целевой файл: $effect guard v.length < 1 (line 102), onMount-seed (lines 85-90), inputEl ref (lines 72-73), поддержка textarea (multiline prop)
    - ui/src/lib/components/LocationAutocomplete.svelte — canonical pattern (lines 59-63 handleFocus; line 130 onfocus=)
  </read_first>

  <files>ui/src/features/devices/DeviceAutocompleteField.svelte</files>

  <action>
    Добавить функцию handleFocus после функции handleInput.

    handleFocus: устанавливает suppressDropdown = false, lastSelected = null, затем вызывает fetch с задержкой 0 мс напрямую (не через $effect, чтобы обойти guard v.length < 1). В теле fetch: вызвать devices.autocomplete(field, value, contextName, contextStatusId, statusIn), установить suggestions, если !suppressDropdown — установить open = suggestions.length > 0, activeIndex = -1. Обернуть в try/catch (suggestions = [], open = false при ошибке).

    Добавить onfocus={handleFocus} на ОБА элемента: input (line ~249) и textarea (line ~230). У обоих уже есть oninput={handleInput} — добавить onfocus рядом.

    Исправить handleKeydown: ArrowDown-ветка имеет guard "value.length > 0" (line 162) — снять его так же как в Task 1.

    Контракт suppressDropdown: onMount-seed (lines 85-90) не трогать. handleFocus сбрасывает suppressDropdown=false явно — это правильно (пользователь фокусируется намеренно).

    Backend devices.autocomplete с пустым prefix уже возвращает результаты (LIKE '%') — изменений в backend НЕТ.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui && pnpm exec svelte-check --tsconfig tsconfig.json 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - DeviceAutocompleteField.svelte содержит функцию handleFocus (grep: "function handleFocus")
    - Оба элемента (input и textarea) имеют onfocus={handleFocus}
    - handleFocus устанавливает suppressDropdown = false (grep внутри функции)
    - ArrowDown-ветка в handleKeydown не требует value.length > 0
    - svelte-check 0 errors
  </acceptance_criteria>

  <done>DeviceAutocompleteField: фокус в пустое поле запускает fetch и открывает dropdown; edit-mode pre-fill по-прежнему не открывает dropdown до явного keystroke.</done>
</task>

<task type="auto">
  <name>Task 3: DEF-2A — dedupe выбранных device groups в ActFormItemsTable</name>

  <read_first>
    - ui/src/features/acts/ActFormItemsTable.svelte — целевой файл: FormItemRow.group_ids (line 35), pickGroup (line 124), handleQueryInput + suggestionsByRow (lines 85-122), dropdown render (lines 200-218)
  </read_first>

  <files>ui/src/features/acts/ActFormItemsTable.svelte</files>

  <action>
    Вычислить Set всех device ID, уже занятых другими строками. Добавить функцию (или $derived) selectedIds: собрать все group_ids из items где picked=true, объединить в один Set&lt;number&gt;. Пример: items.filter(it => it.picked && it.group_ids).flatMap(it => it.group_ids!).

    В handleQueryInput перед установкой suggestionsByRow[idx]: отфильтровать groups — исключить группы, у которых хотя бы один ID из g.ids присутствует в selectedIds. При этом текущий row (idx) не должен исключаться из собственного selectedIds (т.е. не включать группу из row idx в selectedIds при фильтрации suggestions для idx).

    В dropdown-рендере (строки 200-218, {#each suggestionsByRow[idx] as g}) дополнительно добавить фильтр `g.ids.some(id => selectedIds(idx).has(id))` — как fallback для уже-загруженных suggestions, если selectedIds изменился после загрузки (другая строка выбрала device пока этот dropdown уже открыт).

    Конкретная механика: написать вспомогательную функцию getSelectedIds(excludeIdx: number): Set&lt;number&gt; которая итерирует items, пропускает rows[excludeIdx], собирает group_ids. Вызывать в handleQueryInput для фильтрации результатов и в рендере dropdown для inline-фильтрации.

    Не трогать pickGroup, removeRow, addRow, handleQtyInput — логика выбора/удаления строк не меняется.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui && pnpm exec svelte-check --tsconfig tsconfig.json 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - ActFormItemsTable.svelte содержит функцию getSelectedIds (grep: "getSelectedIds")
    - getSelectedIds принимает excludeIdx и возвращает Set&lt;number&gt; из group_ids всех picked-строк кроме excludeIdx
    - В handleQueryInput применяется фильтрация через getSelectedIds перед записью в suggestionsByRow[idx]
    - В dropdown-рендере применяется inline-фильтрация: группы с пересечением в selectedIds не показываются
    - svelte-check 0 errors
  </acceptance_criteria>

  <done>Добавив группу в строку 1, в dropdown строки 2 эта группа (или группы с перекрывающимися device IDs) не отображается.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI input → API call | Autocomplete prefix приходит из input-поля пользователя → передаётся на backend через apiCall |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-01-01 | Tampering | PersonAutocomplete / DeviceAutocompleteField: empty-prefix fetch | accept | Prefix "" → LIKE '%' в SQL, возвращает top-30 DISTINCT значений. Запрос уже параметризован через rusqlite::params![], SQL-инъекция невозможна (T-02-04-02). Rate-limit: debounce 0ms на focus (единственный запрос), далее 200ms на keystroke. |
| T-03.2-01-02 | Information Disclosure | Autocomplete возвращает DISTINCT field-значения из devices | accept | Только field-значения (строки типа "Мышь", "Хорошее"), без PII. Сессионная аутентификация уже покрывает /api/v1/* (tower-sessions). |
| T-03.2-01-SC | Tampering | npm install (нет новых пакетов в этом плане) | accept | Этот план не добавляет npm-зависимостей — только редактирует .svelte файлы. |

Угрозы низкие: план меняет только UI-поведение (focus → fetch). Новых API-эндпоинтов, форм авторизации или доверенных данных нет. Параметризация SQL-запросов на backend не изменяется.
</threat_model>

<verification>
После выполнения всех трёх задач:

1. svelte-check возвращает 0 errors:
   `cd /Users/madsas/Projects/trackly/ui && pnpm exec svelte-check --tsconfig tsconfig.json`

2. Rust тесты остаются зелёными (план не трогает backend):
   `cd /Users/madsas/Projects/trackly && cargo test --workspace --quiet 2>&1 | tail -10`

3. Grep-проверки source-assertions:
   - `grep -c "function handleFocus" ui/src/lib/components/PersonAutocomplete.svelte` → 1
   - `grep -c "function handleFocus" ui/src/features/devices/DeviceAutocompleteField.svelte` → 1
   - `grep -c "getSelectedIds" ui/src/features/acts/ActFormItemsTable.svelte` → ≥2 (def + usage)
   - `grep -c "onfocus={handleFocus}" ui/src/lib/components/PersonAutocomplete.svelte` → 1
   - `grep -c "onfocus={handleFocus}" ui/src/features/devices/DeviceAutocompleteField.svelte` → 2 (input + textarea)
</verification>

<success_criteria>
- PersonAutocomplete содержит handleFocus; onfocus binding присутствует на input; suppressDropdown = false внутри handleFocus
- DeviceAutocompleteField содержит handleFocus; onfocus на input и textarea; bypass v.length < 1 guard для focus-triggered fetch
- ActFormItemsTable содержит getSelectedIds(excludeIdx); suggestions в handleQueryInput фильтруются через set; dropdown-рендер применяет inline-фильтр
- svelte-check 0 errors
- cargo test --workspace проходит без новых сбоев
</success_criteria>

<output>
Создать `.planning/phases/03.2-deferred-uat-gap-closure/03.2-01-SUMMARY.md` по завершении
</output>
