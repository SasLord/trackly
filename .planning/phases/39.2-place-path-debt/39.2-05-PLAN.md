---
phase: 39.2
plan: 05
type: execute
wave: 5
depends_on: ["39.2-04"]
files_modified:
  - ui/scripts/check-path-settings-form.mjs
  - ui/scripts/check-placepath-parity.mjs
  - ui/package.json
  - ui/src/features/settings/OrgSettings.svelte
  - ui/src/lib/utils/placePath.ts
  - .planning/phases/39.1-place-path-display/39.1-UI-SPEC.md
autonomous: false
requirements: [PLC-07]
must_haves:
  truths:
    - "WR-06: кнопка «Сохранить формат пути» неактивна при заведомо невалидном разделителе — inline-ошибка больше не дублируется красным тостом после round-trip'а; `savePathDefaults` дополнительно делает ранний возврат, поэтому запрос не уходит даже при обходе `disabled`"
    - "WR-07: `<Radio>` вызывается в принятой в проекте форме `<Radio ...>Текст</Radio>` без внешнего `<label class=\"path-radio-row\">` — вложенный `<label>` (запрещённый спецификацией HTML) из разметки исчез; форма совпадает с `ThresholdSettings.svelte:76`, `ActiveDirectorySettings.svelte:127`, `FieldsSection.svelte:118`"
    - "WR-07: инвариант удерживается ПОСТОЯННЫМ структурным гейтом `ui/scripts/check-path-settings-form.mjs`, вписанным в скрипт `lint` в `ui/package.json` — вне цепочки `pnpm lint` (CI: .github/workflows/ci-full.yml:126) скрипт гейтом не является"
    - "IN-04: `monoReadout` подсвечивает не только U+0020 — табуляция, NBSP (U+00A0) и узкий неразрывный пробел (U+202F) в разделителе перестают быть невидимыми; проверка исполняющая, на реальном вызове функции"
    - "IN-04: `.field-hint` и `.field-error` под обоими полями разделителей связаны с полем через `aria-describedby` (проп у `Input.svelte` уже существует) — скринридер читает и подсказку, и ошибку"
    - "Граница фазы: формула сокращения не изменена — `previewShortenPath` и golden-фикстура `scripts/fixtures/place-path/shorten-cases.json` остаются нетронутыми, оба гейта пути зелёные"
  artifacts:
    - path: "ui/scripts/check-path-settings-form.mjs"
      provides: "структурный гейт: форма вызова <Radio> (WR-07), disabled у кнопки формата пути (WR-06), aria-describedby у полей разделителей (IN-04)"
      contains: "--src="
      min_lines: 80
    - path: "ui/package.json"
      provides: "новый гейт в цепочке lint"
      contains: "check-path-settings-form.mjs"
    - path: "ui/src/features/settings/OrgSettings.svelte"
      provides: "разметка радиогруппы без внешних label, disabled на кнопке, aria-describedby на полях"
      contains: "aria-describedby"
    - path: "ui/src/lib/utils/placePath.ts"
      provides: "monoReadout, показывающий все невидимые символы разделителя"
      contains: "monoReadout"
  key_links:
    - from: "ui/package.json (скрипт lint)"
      to: "scripts/check-path-settings-form.mjs"
      via: "&& node scripts/check-path-settings-form.mjs в конце цепочки"
      pattern: "check-path-settings-form"
    - from: "ui/src/features/settings/OrgSettings.svelte"
      to: "Input.svelte prop aria-describedby"
      via: "aria-describedby=\"path-sep-ends-hint path-sep-ends-error\" + id на span'ах"
      pattern: "aria-describedby"
---

<objective>
Экран «Формат отображения пути места» в Настройках доводится до принятых в проекте форм:
кнопка не предлагает сохранить заведомо невалидное значение (WR-06), радиогруппа перестаёт
содержать запрещённые спецификацией вложенные `<label>` (WR-07), а подсказка/ошибка под
полями разделителей становятся доступны скринридеру и показывают все невидимые символы
(IN-04).

Purpose: три находки на одном экране, и ни одну из них не удерживает ни один гейт —
`svelte-check`, `eslint` и `build` по конструкции не видят ни рантайм-реактивности рун,
ни валидности вложенности разметки (прецедент проекта: «compile gates miss Svelte runtime»).
Поэтому план сначала строит гейт, и только потом чинит код.

Output: новый структурный гейт в цепочке `pnpm lint`, исправленный `OrgSettings.svelte`,
расширенный `monoReadout` с исполняющей проверкой.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-place-path-debt/39.2-VALIDATION.md
@.planning/phases/39.1-place-path-display/39.1-UI-SPEC.md

<interfaces>
  ui/src/lib/components/Radio.svelte — Props { group?, value, disabled?, invalid?, id?,
    children? }. Компонент САМ рендерит `<label class="check-row">` и вставляет подпись
    через `{@render children?.()}`. Внешний `<label>` вокруг него запрещён.

  ui/src/lib/components/Input.svelte — Props { type?, value, placeholder?, disabled?,
    invalid?, id?, 'aria-describedby'?, autocomplete?, oninput?, iconLeft?, mono? }.
    Проп `aria-describedby` УЖЕ поддержан и пробрасывается на `<input>`.

  ui/src/lib/utils/placePath.ts
    export function previewShortenPath(fullPath, variant, sepEnds, sepLastTwo): string
      — НЕ ТРОГАТЬ: её паритет с Rust пришпилен golden-фикстурой (23 кейса).
    export function monoReadout(v: string): string  — предмет правки IN-04.

  Текущее состояние экрана (OrgSettings.svelte):
    строки 76-82   — `sepEndsErr` / `sepLastTwoErr` ($derived, `.length === 0`, НЕ `.trim()`)
    строки 121-142 — `savePathDefaults()` без проверки ошибок
    строки 459-472 — радиогруппа с тремя `<label class="path-radio-row">` и self-closing
                     `<Radio ... />`
    строки 474-503 — два поля разделителей со `<span class="field-hint tr-mono">` и
                     `<span class="field-error">` без `id` и без связи с полем
    строка  534    — `<Button variant="primary" loading={savingPath} onclick=...>` без `disabled`
    строки 686-696 — стили `.path-variant-group` (flex column, gap xs) и `.path-radio-row`
</interfaces>

Приватность (CLAUDE.md): образцы предпросмотра на экране — вымышленные («Здание А /
1 этаж / 1-05»), они не меняются; в гейте и его самотесте реальные данные не появляются.
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Структурный гейт `check-path-settings-form.mjs` + включение в `lint` + самотест</name>
  <files>ui/scripts/check-path-settings-form.mjs, ui/package.json</files>

  <read_first>
    - `ui/scripts/check-print-isolation.mjs` целиком — ОБРАЗЕЦ формы гейта, принятой в
      проекте: шапка с объяснением «почему он существует», zero-dependency импорты
      `node:fs`/`node:path`/`node:url`, аргумент `--src=`, коды выхода;
    - `ui/scripts/check-place-path-short.mjs`, строки 36-140 и 250-300 — хелперы
      `markupOnly()` (затирает комментарии/`<script>`/`<style>`, чтобы объяснение в
      комментарии не удовлетворяло проверку) и `endOfOpeningTag()` (учёт `{}` внутри
      атрибутов), а также разбор `--src=` и формат отчёта;
    - `ui/package.json`, строка 16 — текущая цепочка `lint` из 7 гейтов;
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, разделы «WR-06»
      (строки 298-320), «WR-07» (321-340), «IN-04» (408-422) — дословные наброски фиксов;
    - `ui/src/features/settings/OrgSettings.svelte`, строки 455-540 — текущая «дофиксная»
      разметка, на которой гейт обязан краснеть;
    - `ui/src/features/settings/ThresholdSettings.svelte`, строки 74-90 и
      `ui/src/features/showcase/sections/FieldsSection.svelte`, строки 116-130 — вызовы
      `<Radio>`, которые ОБЯЗАНЫ остаться зелёными (гейт не должен на них ложно срабатывать).
  </read_first>

  <action>
Создать `ui/scripts/check-path-settings-form.mjs` — zero-dependency структурный гейт
(только `node:fs`, `node:path`, `node:url`), поддерживающий `--src=<ui-dir>` для самотеста
на копии (по образцу `check-place-path-short.mjs`: `--src` задаёт корень, относительно
которого разрешаются пути вида `src/...`). Шапка обязана объяснять, ПОЧЕМУ гейт существует:
`svelte-check`/`eslint`/`build` не видят ни вложенности `<label>`, ни отсутствия `disabled`,
ни отсутствия `aria-describedby`; три находки ревью фазы 39.1 (WR-06, WR-07, IN-04) уехали
в сборку именно поэтому.

Инварианты гейта:

INV-1 (WR-07, репозиторный) — обойти ВСЕ `.svelte` в `src/` и проверить два условия для
каждого вхождения `<Radio`: (а) вызов не self-closing (нет `/>` до закрывающего `>`
открывающего тега) — подпись передаётся через children; (б) вхождение не находится внутри
блока `<label ...> ... </label>`. Разметку предварительно прогонять через аналог
`markupOnly()`, чтобы `<Radio` в комментарии не считался вызовом. При нарушении печатать
файл, строку и требуемую форму `<Radio ...>Текст</Radio>`.

INV-2 (WR-06, точечный) — в `src/features/settings/OrgSettings.svelte` найти `<Button`,
чей текст содержит «Сохранить формат пути», и проверить, что в его открывающем теге есть
атрибут `disabled=`, выражение которого упоминает и `sepEndsErr`, и `sepLastTwoErr`.
Дополнительно проверить, что тело функции `savePathDefaults` упоминает обе переменные
(ранний возврат) — по образцу хелпера `functionBody()` из `check-place-path-short.mjs`.

INV-3 (IN-04, точечный) — в том же файле для обоих полей (`id="path-sep-ends"` и
`id="path-sep-last-two"`) проверить, что открывающий тег `<Input` содержит
`aria-describedby=`, и что каждый упомянутый там идентификатор реально объявлен как `id=`
на каком-то элементе того же файла (иначе связь мертва). Пустая ссылка на несуществующий
`id` — нарушение.

Отчёт: печатать все нарушения списком с тегом `[check-path-settings-form]`, выходить
кодом 1 при любом нарушении и кодом 0 при чистом прогоне; поддержать `--help`.

Вписать гейт в конец цепочки `lint` в `ui/package.json` (после
`node scripts/check-place-path-short.mjs`), иначе он не гейт — CI прогоняет только
`pnpm lint` (`.github/workflows/ci-full.yml:126`).

САМОТЕСТ (обязателен, выполняется на КОПИЯХ, не на реальных файлах). В scratchpad-каталоге
подготовить два корня: `broken/` — копия `ui/src` в текущем (дофиксном) виде; `fixed/` —
та же копия с руками применённой целевой формой (Radio без внешних `<label>`, `disabled`
у кнопки, `aria-describedby` у полей). Убедиться, что гейт краснеет на `broken/` с тремя
типами сообщений (INV-1, INV-2, INV-3) и зеленеет на `fixed/`. Вывод обоих прогонов
приложить в SUMMARY.

На этой задаче гейт на РЕАЛЬНОМ репозитории обязан быть КРАСНЫМ (код чинится в задаче 2) —
это ожидаемое состояние, а не дефект.
  </action>

  <acceptance_criteria>
    - файл `ui/scripts/check-path-settings-form.mjs` существует, не импортирует ничего,
      кроме `node:fs`/`node:path`/`node:url`, и поддерживает `--src=`;
    - `grep -c 'check-path-settings-form.mjs' ui/package.json` → 1, и вхождение находится
      внутри значения скрипта `lint`;
    - гейт на подготовленной копии `broken/` завершается кодом 1 и печатает нарушения
      всех трёх инвариантов;
    - гейт на подготовленной копии `fixed/` завершается кодом 0;
    - гейт на `fixed/`-копии НЕ ругается на `ThresholdSettings.svelte`,
      `ActiveDirectorySettings.svelte` и `FieldsSection.svelte` — их вызовы `<Radio>` уже
      в правильной форме (ложных срабатываний нет);
    - `prettier --check` по новому файлу проходит.
  </acceptance_criteria>

  <verify>
    <automated>cd ui && node scripts/check-path-settings-form.mjs --src="$SCRATCH/fixed" && ! node scripts/check-path-settings-form.mjs --src="$SCRATCH/broken"</automated>
  </verify>

  <done>Существует постоянный гейт, доказанно падающий на дофиксной форме и вписанный
  в цепочку `pnpm lint`.</done>
</task>

<task type="auto">
  <name>Задача 2: `OrgSettings.svelte` — снять внешние `<label>`, погасить кнопку, связать hint/error с полями (WR-07, WR-06, IN-04)</name>
  <files>ui/src/features/settings/OrgSettings.svelte, .planning/phases/39.1-place-path-display/39.1-UI-SPEC.md</files>

  <read_first>
    - `ui/src/features/settings/OrgSettings.svelte`, строки 60-145 (состояние, валидаторы,
      `loadPathDefaults`, `savePathDefaults`) и 455-545 (разметка секции + `<Button>`),
      а также 645-700 (стили `.field-hint`, `.field-error`, `.path-variant-group`,
      `.path-radio-row`);
    - `ui/src/lib/components/Radio.svelte` целиком — какой `<label>` компонент рендерит сам
      и как принимает подпись;
    - `ui/src/features/settings/ThresholdSettings.svelte`, строки 74-90 — эталонная форма
      вызова `<Radio ...>Текст</Radio>` в проекте;
    - `.planning/phases/39.1-place-path-display/39.1-UI-SPEC.md`, строки 40-105 и 140-165 —
      визуальный контракт (gap между радио-строками = `--tr-space-xs`, тексты подписей,
      aria-label радиогруппы) и фрагмент skeleton-разметки, содержащий устаревшую форму
      с внешним `<label class="path-radio-row">`;
    - `ui/scripts/check-path-settings-form.mjs` — созданный в задаче 1 гейт: его сообщения
      описывают требуемую целевую форму.
  </read_first>

  <action>
WR-07: заменить три блока `<label class="path-radio-row"><Radio ... /><span>Текст</span></label>`
на `<Radio bind:group={pathVariant} value="ends" disabled={savingPath}>Крайние</Radio>`
и аналогично для `last_two` («Два последних») и `last` («Последнее»). Тексты подписей и
`aria-label="Вариант сокращения по умолчанию"` у контейнера `role="radiogroup"` сохранить
дословно — это контракт UI-SPEC. Стиль `.path-radio-row` удалить (иначе останется мёртвый
селектор); визуальный отступ между строками уже даёт `.path-variant-group { gap: var(--tr-space-xs) }` —
вертикальный ритм не меняется.

WR-06: добавить `<Button>` формата пути атрибут
`disabled={sepEndsErr !== null || sepLastTwoErr !== null}` и в начало `savePathDefaults()`
ранний возврат по тому же условию (кнопку можно обойти программно, сервер остаётся
источником истины, но лишний round-trip и дублирующий красный тост исчезают). Клиентская
валидация НЕ должна триммить: условие остаётся `.length === 0`, whitespace-only разделитель
легален (D-09/D-10) и обязан сохраняться.

IN-04 (доступность): дать `<span class="field-hint">` и `<span class="field-error">` под
каждым полем уникальные `id` (`path-sep-ends-hint` / `path-sep-ends-error` и
`path-sep-last-two-hint` / `path-sep-last-two-error`) и передать соответствующему `<Input>`
проп `aria-describedby` со ссылкой на них. Ссылка на error-span должна ставиться только
когда ошибка отрисована (иначе `aria-describedby` укажет на несуществующий узел) —
собрать значение выражением, зависящим от `sepEndsErr` / `sepLastTwoErr`.

Синхронизировать `.planning/phases/39.1-place-path-display/39.1-UI-SPEC.md`: в фрагменте
skeleton-разметки (около строки 150) заменить три `<label class="path-radio-row">`-блока
на целевую форму `<Radio ...>Текст</Radio>` и добавить однострочную пометку с датой
и ссылкой на WR-07 фазы 39.2 — спека не должна остаться единственным документом,
предписывающим невалидную вложенность.

Компоненты списков и любые Rust-файлы этим планом НЕ трогаются.
  </action>

  <acceptance_criteria>
    - `grep -c 'path-radio-row' ui/src/features/settings/OrgSettings.svelte` → 0
      (и в разметке, и в стилях);
    - `grep -c '<Radio' ui/src/features/settings/OrgSettings.svelte` → 3, и ни один вызов
      не self-closing (`grep -c '<Radio[^>]*/>' ui/src/features/settings/OrgSettings.svelte` → 0);
    - `grep -c 'aria-describedby' ui/src/features/settings/OrgSettings.svelte` → не менее 2;
    - в открывающем теге `<Button ...>Сохранить формат пути` присутствует `disabled=`
      с упоминанием `sepEndsErr` и `sepLastTwoErr`; тело `savePathDefaults` содержит
      ранний возврат по тому же условию;
    - `cd ui && node scripts/check-path-settings-form.mjs` завершается кодом 0
      (на РЕАЛЬНОМ репозитории);
    - `cd ui && pnpm lint` зелёный целиком (все 8 гейтов + eslint + prettier);
    - `cd ui && pnpm svelte-check` без новых ошибок;
    - `cd ui && pnpm build` успешен и не печатает предупреждения о неиспользуемых
      CSS-селекторах для `.path-radio-row`;
    - фрагмент skeleton в `39.1-UI-SPEC.md` больше не содержит `path-radio-row`.
  </acceptance_criteria>

  <verify>
    <automated>cd ui && node scripts/check-path-settings-form.mjs && pnpm lint && pnpm svelte-check && pnpm build</automated>
  </verify>

  <done>Разметка радиогруппы валидна и совпадает с остальными call-site'ами проекта;
  кнопка не предлагает сохранить невалидное значение; подсказка и ошибка связаны с полями.</done>
</task>

<task type="auto">
  <name>Задача 3: `monoReadout` показывает все невидимые символы + исполняющая проверка в гейте паритета (IN-04)</name>
  <files>ui/src/lib/utils/placePath.ts, ui/scripts/check-placepath-parity.mjs</files>

  <read_first>
    - `ui/src/lib/utils/placePath.ts` целиком (56 строк) — `previewShortenPath` (НЕ трогать)
      и `monoReadout` (строки 46-56, сегодня `v.replace(/ /g, '·')`);
    - `ui/scripts/check-placepath-parity.mjs`, строки 1-60 и хвост файла — шапка,
      TS-загрузчик (снятие аннотаций типов без `--experimental-strip-types`), разбор
      `--impl=`, формат отчёта и коды выхода; сюда встраивается новая проверка;
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «IN-04»
      (строки 408-422) — какие символы сегодня остаются невидимыми и почему это важно
      (whitespace-only разделитель легален);
    - `.planning/phases/39.1-place-path-display/39.1-UI-SPEC.md`, строка 64 и раздел
      «Проблема 1» — назначение readout'а и класс `.tr-mono`.
  </read_first>

  <action>
Расширить `monoReadout` в `ui/src/lib/utils/placePath.ts` так, чтобы визуально проявлялись
ВСЕ пробельные символы, которые разрешено сохранять в разделителе, а не только U+0020.
Минимальный обязательный набор: обычный пробел U+0020, табуляция U+0009, неразрывный
пробел U+00A0, узкий неразрывный пробел U+202F. Обычный пробел обязан по-прежнему
отображаться как `·` (U+00B7) — это уже видел пользователь, менять привычное представление
без нужды не надо; для остальных выбрать различимые маркеры и перечислить соответствие
в doc-комментарии функции. Ключевое проверяемое свойство: в результате `monoReadout`
не остаётся ни одного «сырого» пробельного символа.

`previewShortenPath` НЕ трогать ни одной строкой — её паритет с Rust удерживается
golden-фикстурой из 23 кейсов, а сокращение формулы вне границ фазы.

Встроить в `ui/scripts/check-placepath-parity.mjs` исполняющую проверку `monoReadout`
(гейт уже умеет загружать `.ts`-модуль — переиспользовать этот загрузчик, второй не
заводить): вызвать функцию на строке, содержащей все четыре символа, и утверждать, что
в результате не осталось ни одного символа из набора {U+0020, U+0009, U+00A0, U+202F}
(в самом гейте эти символы задавать escape-последовательностями `\u00a0`/`\u202f`, а не
вставлять литералами), а обычный пробел дал `·`.
Шапку гейта дополнить абзацем о том, что файл теперь держит два инварианта: паритет
формулы (фикстура) и полноту readout'а (IN-04). Фикстуру
`scripts/fixtures/place-path/shorten-cases.json` НЕ изменять — она вне правки.

Убедиться, что новая проверка падает на прежнем теле `monoReadout` (проверить на КОПИИ
`placePath.ts` в scratchpad через `--impl=<path>`, а не на реальном файле), и приложить
вывод в SUMMARY.
  </action>

  <acceptance_criteria>
    - `monoReadout(' ')` возвращает `'·'` (прежнее поведение обычного пробела сохранено);
    - `monoReadout` на строке из U+0009, U+00A0, U+202F не оставляет сырых пробельных
      символов — проверяется исполняющей вставкой в `check-placepath-parity.mjs`;
    - `git status --porcelain scripts/fixtures/place-path/shorten-cases.json` → пусто;
    - `previewShortenPath` не изменена (`git diff ui/src/lib/utils/placePath.ts` не
      затрагивает её тело);
    - прогон расширенного гейта на дофиксной копии через `--impl=` падает (вывод в SUMMARY);
    - `cd ui && pnpm lint` зелёный целиком;
    - регресс-гейт границы фазы зелёный: `cargo test -p trackly-core shorten_place_path_matches_golden_fixture`
      + оба `check-*` гейта пути.
  </acceptance_criteria>

  <verify>
    <automated>cd ui && node scripts/check-placepath-parity.mjs && node scripts/check-place-path-short.mjs && node scripts/check-path-settings-form.mjs && pnpm lint && cd .. && cargo test -p trackly-core shorten_place_path_matches_golden_fixture -- --test-threads=1</automated>
  </verify>

  <done>Значение разделителя читается однозначно при любом составе пробельных символов;
  свойство удерживается исполняющим гейтом.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Задача 4: Ручная проверка экрана «Формат пути» (рантайм рун, ассоциация подписи, скринридер)</name>
  <files>ui/src/features/settings/OrgSettings.svelte</files>
  <action>Остановиться и дождаться подтверждения пользователя. Автоматика по этому экрану
исчерпана: структурный гейт, исполняющая проверка `monoReadout`, `pnpm lint`, `svelte-check`
и `pnpm build` уже зелёные. Оставшиеся три свойства статикой не проверяются — реактивность
рун Svelte (гаснет ли кнопка), реальная ассоциация подписи радио с инпутом и озвучивание
`aria-describedby` скринридером. Ничего не менять до ответа пользователя; при расхождениях
исправлять и повторно прогонять `pnpm lint`.</action>
  <what-built>
    Экран «Настройки → Организация → Формат отображения пути места»:
    (1) кнопка «Сохранить формат пути» гаснет при пустом разделителе и не шлёт запрос;
    (2) радиогруппа «Вариант сокращения» перестроена на `<Radio ...>Текст</Radio>` без
        внешних `<label>` — визуально должна выглядеть так же, как до правки;
    (3) под полями разделителей подсказка и ошибка связаны с полем через `aria-describedby`,
        а readout показывает табуляцию/NBSP/узкий пробел.
    Автоматически проверено: структурный гейт формы, исполняющая проверка `monoReadout`,
    `pnpm lint`, `svelte-check`, `build`. НЕ проверяемо статикой (прецедент проекта
    «compile gates miss Svelte runtime»): рантайм-реактивность рун, реальная ассоциация
    подписи с инпутом и поведение скринридера.
  </what-built>
  <how-to-verify>
    1. `pnpm --dir ui build`, затем `cargo tauri dev`.
    2. Настройки → Организация → блок «Формат отображения пути места».
    3. Очистить поле «Разделитель «Крайние»» → кнопка «Сохранить формат пути» становится
       неактивной, красный тост НЕ появляется. Вернуть значение → кнопка снова активна.
    4. Клик по тексту «Два последних» переключает радио (ассоциация подписи жива).
       Вертикальные отступы между тремя строками радио не изменились.
    5. Ввести в поле разделителя таб/NBSP — в строке «Значение: «…»» символ виден.
    6. VoiceOver (Cmd+F5) на поле разделителя: читается подпись, затем подсказка;
       при пустом значении — ещё и текст ошибки; подпись радио читается ОДИН раз.
    7. Открыть тот же экран в LAN-браузере (сервер-мод) и сверить: поведение то же.
  </how-to-verify>
  <resume-signal>Напишите «approved» либо перечислите расхождения</resume-signal>
  <verify>
    <human-check>Пользователь подтвердил все семь пунктов из how-to-verify — в десктопе
    и в LAN-браузере; расхождений нет либо они исправлены и `pnpm lint` снова зелёный.</human-check>
  </verify>
  <done>Получен ответ «approved»; результат ручной проверки записан в SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер/webview → `settings_set_place_path_defaults` | клиентская валидация разделителя; сервер остаётся источником истины |

Новых сетевых вызовов, новых данных и новых прав план не вводит. Изменяется только
разметка одного экрана, чистая функция форматирования и набор локальных lint-скриптов.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.2-05-01 | Tampering | клиентская валидация разделителя (WR-06) | accept | `disabled` и ранний возврат — только UX: они сокращают лишний round-trip, но не являются защитой. Серверные проверки в `build_settings_set_place_path_defaults` (`.is_empty()` + `PathDisplayVariant::from_str`) остаются единственным гейтом и этим планом не трогаются |
| T-39.2-05-02 | Tampering | `ui/scripts/check-path-settings-form.mjs` | mitigate | Гейт zero-dependency: только `node:fs`/`node:path`/`node:url`, никаких новых npm-пакетов, никакой сети. Поверхность цепочки поставок не расширяется |
| T-39.2-05-03 | Information Disclosure | `monoReadout`, гейт и его самотест | mitigate | Функция форматирует только значение разделителя (символы пунктуации/пробелы), в лог ничего не пишет. Самотест выполняется на копиях в scratchpad; в репозиторий не попадают ни копии, ни реальные данные — контролируется pre-commit хуком `scripts/check-privacy.mjs` |

Установок npm-пакетов план не делает (`ui/package.json` меняется ТОЛЬКО в скрипте `lint`,
секции зависимостей не трогаются) — гейт легитимности пакетов неприменим.
</threat_model>

<verification>
- `cd ui && pnpm lint` (включая новый восьмой гейт), `pnpm svelte-check`, `pnpm build`.
- `cd ui && node scripts/check-path-settings-form.mjs` — код 0 на репозитории после задачи 2.
- Самотесты обоих гейтов на копиях в scratchpad — падение на дофиксной форме доказано.
- Регресс-гейт границы фазы: golden-фикстура + оба гейта пути, без правок фикстуры.
- Ручная проверка (checkpoint) — рантайм-реактивность, ассоциация подписи, скринридер,
  LAN-браузер.
</verification>

<success_criteria>
- Ни один `<Radio>` в кодовой базе не обёрнут внешним `<label>` и не вызывается
  self-closing; инвариант удерживается гейтом в `pnpm lint`.
- Кнопка «Сохранить формат пути» неактивна при невалидном разделителе; дублирующего
  красного тоста после round-trip'а больше нет.
- Подсказка и ошибка каждого поля разделителя доступны скринридеру; readout не скрывает
  ни одного пробельного символа.
- Формула сокращения и golden-фикстура не изменены.
</success_criteria>

<output>
Create `.planning/phases/39.2-place-path-debt/39.2-05-SUMMARY.md` when done.
В SUMMARY приложить: вывод гейта формы на `broken/` и `fixed/` копиях, вывод падающей
проверки `monoReadout` на дофиксной копии, результат ручной проверки (checkpoint).
</output>
