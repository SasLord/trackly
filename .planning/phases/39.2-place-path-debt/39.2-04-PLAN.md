---
phase: 39.2
plan: 04
type: execute
wave: 4
depends_on: ["39.2-03"]
files_modified:
  - crates/trackly-infra/src/repos/devices_sqlite.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-infra/tests/devices_place_path_short.rs
  - crates/trackly-infra/tests/cartridges_place_path_short.rs
autonomous: true
requirements: [PLC-08]
must_haves:
  truths:
    - "WR-01: список больше не теряет путь, который у него есть — при известном `full_path` и невыводимом варианте сокращения (NULL из `place_effective_variant` или нераспознанный токен) `place_path_short` деградирует к ПОЛНОМУ пути, а не к `None`, из-за которого ячейка «Место» показывала «—»"
    - "WR-01: деградация сделана в РЕПОЗИТОРИЯХ, единообразно с уже существующим прецедентом `places_sqlite.rs:275` (`unwrap_or_else(|| full_path.clone())`), а не в Svelte-компонентах — `DeviceListRow.svelte:68`, `DeviceGroupRow.svelte:156`, `CartridgeListRow.svelte:102` не изменяются вовсе"
    - "WR-01: структурный гейт `ui/scripts/check-place-path-short.mjs` остаётся зелёным без единой правки — его INV-3 запрещает рендерить полный путь в ТЕКСТЕ ячейки, и выбранное решение этого запрета не нарушает"
    - "Отсутствие места по-прежнему означает отсутствие пути: при `full_path == None` значение `place_path_short` остаётся `None` (существующие тесты `list_device_without_place_has_no_short_path` и `list_cartridge_without_place_has_no_short_path` не меняются)"
    - "Граница фазы: на исправной БД деградация недостижима, поэтому ни один видимый результат не изменился — golden-фикстура и оба гейта пути зелёные без правок"
  artifacts:
    - path: "crates/trackly-infra/src/repos/devices_sqlite.rs"
      provides: "деградация к полному пути в from_row_with_short_path и в ветке list_grouped"
      contains: "unwrap_or_else"
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "деградация к полному пути в map_row_with_short_path"
      contains: "unwrap_or_else"
    - path: "crates/trackly-infra/tests/devices_place_path_short.rs"
      provides: "тесты деградации: нераспознанный токен и отсутствующий org-ключ"
    - path: "crates/trackly-infra/tests/cartridges_place_path_short.rs"
      provides: "зеркальный тест деградации для картриджей"
  key_links:
    - from: "crates/trackly-infra/src/repos/devices_sqlite.rs::from_row_with_short_path"
      to: "DeviceRow.place_path_short"
      via: "Some(full.to_string()) вместо None при невыводимом варианте"
      pattern: "place_path_short"
    - from: "crates/trackly-infra/src/repos/cartridges_sqlite.rs::map_row_with_short_path"
      to: "CartridgeRow.place_path_short"
      via: "Some(full.to_string()) вместо None при невыводимом варианте"
      pattern: "place_path_short"
---

<objective>
WR-01: ячейка «Место» в списках устройств и картриджей перестаёт утверждать «места нет»
там, где путь известен. Деградация делается один раз в репозиториях — так, как это уже
сделано в `places_sqlite.rs:275` для содержимого места.

Purpose: у `place_path_short` есть законные `None`-сценарии (NULL из
`place_effective_variant` — WR-02; нераспознанный токен). В каждом из них рядом лежит
полный путь, и показывать вместо него «—» — потеря данных на экране.

Output: единообразная деградация в двух репозиториях + 3 теста. Ни один Svelte-компонент
и ни один фронт-гейт не трогается.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-place-path-debt/39.2-VALIDATION.md
@.planning/phases/39.2-place-path-debt/39.2-01-SUMMARY.md
@.planning/phases/39.2-place-path-debt/39.2-02-SUMMARY.md

<interfaces>
  DeviceRow.full_path: Option<String>       DeviceRow.place_path_short: Option<String>
  CartridgeRow.full_path: Option<String>    CartridgeRow.place_path_short: Option<String>

  trackly_core::domain::places::PathDisplayVariant::from_str(&str) -> Result<Self, AppError>
  trackly_core::domain::places::shorten_place_path(full, variant, sep_ends, sep_last_two) -> String
  trackly_infra::repos::place_path_settings::read_path_display_separators(conn) -> (String, String)
    (единственный владелец после плана 02)

Целевая семантика после этого плана (одна и та же в трёх точках):
  full_path == None                        -> place_path_short == None
  full_path == Some(p), вариант выводим     -> Some(shorten_place_path(p, ...))
  full_path == Some(p), вариант НЕ выводим  -> Some(p)          <-- изменение WR-01
где «вариант не выводим» = колонка `effective_variant` пришла NULL ЛИБО её токен не
распознан `PathDisplayVariant::from_str`.
</interfaces>

Приватность (CLAUDE.md): в тестах — только вымышленные названия («Здание А», «1 этаж», «1-05»).
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Деградация к полному пути в `devices_sqlite` (list, search_fts, list_grouped) + тесты</name>
  <files>crates/trackly-infra/src/repos/devices_sqlite.rs, crates/trackly-infra/tests/devices_place_path_short.rs</files>

  <read_first>
    - `crates/trackly-infra/src/repos/devices_sqlite.rs`, строки 93-116
      (`from_row_with_short_path` — обслуживает `list()` и `search_fts()`) и строки
      1285-1300 (вычисление `place_path_short` в `list_grouped`, там своя копия формулы
      с комментарием «Same formula as from_row_with_short_path»);
    - `crates/trackly-infra/src/repos/places_sqlite.rs`, окрестность строки 275
      (`list_subtree_contents_impl`) — ПРЕЦЕДЕНТ, форму которого надо повторить:
      `.unwrap_or_else(|| full_path.clone())`;
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «WR-01»
      (строки 172-189) — перечень `None`-сценариев и указание, что правка в репозиториях
      предпочтительнее правки компонентов;
    - `crates/trackly-infra/tests/devices_place_path_short.rs` целиком (5 тестов) —
      хелперы создания места/устройства и форма assert-ов; особое внимание тесту
      `list_device_without_place_has_no_short_path`, который обязан остаться зелёным;
    - `ui/scripts/check-place-path-short.mjs`, шапка (строки 1-35) — формулировка INV-3,
      объясняющая, почему деградация НЕ делается в компоненте.
  </read_first>

  <action>
Привести обе точки вычисления `place_path_short` в `devices_sqlite.rs` к целевой семантике
из `<interfaces>`:

- в `from_row_with_short_path`: если `full_path` есть, значение `place_path_short` теперь
  всегда `Some(...)` — либо сокращённый путь (когда токен пришёл и распознан), либо сам
  полный путь. Ветка `full_path == None` не меняется;
- в блоке `list_grouped` (комментарий «Same formula as from_row_with_short_path») применить
  ровно ту же семантику, чтобы плоский и сгруппированный списки не разошлись — это уже
  расходилось однажды (WR-04, коммит 8fa995e5).

Doc-комментарии обоих мест переписать: сегодня они утверждают «yields
`place_path_short: None`, mirroring `full_path`'s existing behavior» — после правки это
верно ТОЛЬКО для случая «места нет». Новый текст обязан назвать оба источника
невыводимого варианта (NULL из вью — WR-02; нераспознанный токен) и сослаться на
`places_sqlite.rs::list_subtree_contents_impl` как на источник формы.

Svelte-компоненты (`DeviceListRow.svelte`, `DeviceGroupRow.svelte`) НЕ трогать — решение
фазы; `?? '—'` в них остаётся и теперь срабатывает только при реальном отсутствии места.

Добавить в `crates/trackly-infra/tests/devices_place_path_short.rs` два теста:
  - `list_unknown_variant_token_degrades_to_full_path` — создать место с полным путём из
    трёх сегментов и устройство в нём, сырым SQL записать в `places.path_variant_override`
    неизвестный токен (`'bogus'`), вызвать `list()` и проверить, что
    `place_path_short == Some(full_path)` (а не `None` и не «—»);
  - `list_missing_org_variant_key_degrades_to_full_path` — то же дерево без override, но
    сырым SQL удалить строку `app_settings` с ключом `place_path_variant` (состояние WR-02b,
    зафиксированное планом 01), вызвать `list()` и проверить ту же деградацию.
Оба теста прогнать ДО правки репозитория, убедиться, что они падают с `None`, и приложить
вывод в SUMMARY.
  </action>

  <acceptance_criteria>
    - в `devices_sqlite.rs` обе точки вычисления `place_path_short` дают `Some(full)` при
      известном `full_path` и невыводимом варианте (проверяется двумя новыми тестами);
    - `git diff --stat` этого плана НЕ содержит файлов из `ui/` (ни компонентов, ни гейтов);
    - тесты `list_unknown_variant_token_degrades_to_full_path` и
      `list_missing_org_variant_key_degrades_to_full_path` существуют и проходят; их прогон
      до правки падал (вывод в SUMMARY);
    - `list_device_without_place_has_no_short_path` не изменён и зелёный;
    - `cargo test -p trackly-infra --test devices_place_path_short -- --test-threads=1`
      сообщает 7 passed;
    - `cargo clippy -p trackly-infra --all-targets -- -D warnings` чист.
  </acceptance_criteria>

  <verify>
    <automated>cargo test -p trackly-infra --test devices_place_path_short -- --test-threads=1 && cargo clippy -p trackly-infra --all-targets -- -D warnings</automated>
  </verify>

  <done>Плоский и сгруппированный списки устройств показывают полный путь вместо «—»
  во всех случаях, когда путь известен, а вариант вывести не удалось.</done>
</task>

<task type="auto">
  <name>Задача 2: Зеркальная деградация в `cartridges_sqlite` + тест + регресс-гейт волны</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-infra/tests/cartridges_place_path_short.rs</files>

  <read_first>
    - `crates/trackly-infra/src/repos/cartridges_sqlite.rs`, строки 118-145
      (`map_row_with_short_path` — обслуживает `list()` и `search()`);
    - `crates/trackly-infra/src/repos/devices_sqlite.rs` — уже приведённая к целевой
      семантике версия из задачи 1 (форма, которую надо повторить дословно);
    - `crates/trackly-infra/tests/cartridges_place_path_short.rs` целиком (3 теста) —
      хелперы и форма; `list_cartridge_without_place_has_no_short_path` обязан остаться
      зелёным;
    - `.planning/phases/39.2-place-path-debt/39.2-VALIDATION.md`, раздел «Ограничение фазы:
      обратная совместимость поведения» — три команды регресс-гейта.
  </read_first>

  <action>
Применить в `map_row_with_short_path` ту же целевую семантику, что в задаче 1: при
известном `full_path` и невыводимом варианте класть в `place_path_short` полный путь.
Doc-комментарий переписать симметрично устройствам (нельзя оставлять утверждение
«yields `place_path_short: None`» как описание общего случая).

Добавить в `crates/trackly-infra/tests/cartridges_place_path_short.rs` тест
`list_unknown_variant_token_degrades_to_full_path` — зеркало устройственного: место с
трёхсегментным путём, картридж в нём, `'bogus'` в `places.path_variant_override`,
`list()` возвращает `place_path_short == Some(full_path)`. Прогнать ДО правки, убедиться
в падении, приложить вывод в SUMMARY.

Компоненты `CartridgeListRow.svelte` и фронт-гейты не трогать.

После правки прогнать регресс-гейт границы фазы целиком и убедиться, что
`scripts/fixtures/place-path/shorten-cases.json` НЕ изменён (`git status` его не показывает).
Красная фикстура означает выход за границы фазы, а не повод её обновить — в этом случае
остановиться и сообщить.
  </action>

  <acceptance_criteria>
    - тест `list_unknown_variant_token_degrades_to_full_path` в
      `cartridges_place_path_short.rs` существует и проходит; до правки падал;
    - `list_cartridge_without_place_has_no_short_path` не изменён и зелёный;
    - `cargo test -p trackly-infra --test cartridges_place_path_short -- --test-threads=1`
      сообщает 4 passed;
    - `cargo test -p trackly-infra -- --test-threads=1` зелёный целиком;
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1`
      зелёный (DTO списков устройств/картриджей проходят через этот пакет);
    - `git status --porcelain scripts/fixtures/place-path/shorten-cases.json` → пусто;
    - `git status --porcelain ui/` → пусто;
    - регресс-гейт границы фазы зелёный.
  </acceptance_criteria>

  <verify>
    <automated>cargo test -p trackly-infra -- --test-threads=1 && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1 && cargo test -p trackly-core shorten_place_path_matches_golden_fixture -- --test-threads=1 && (cd ui && node scripts/check-placepath-parity.mjs && node scripts/check-place-path-short.mjs)</automated>
  </verify>

  <done>Деградация к полному пути единообразна для устройств, картриджей и содержимого
  места; фронт не тронут; фикстура не тронута.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| БД → DTO списков → UI (десктоп и LAN-браузер) | значение `full_path` уходит в ячейку «Место» и в её `title` |

Новых точек входа нет; права и маршруты не меняются. Меняется только значение уже
отдаваемого поля в одном ранее «пустом» случае.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.2-04-01 | Information Disclosure | `place_path_short` в списках | accept | В fallback-случае в поле попадает `full_path` — та же строка, которая УЖЕ уходит тому же пользователю в этом же DTO и рендерится в `title` ячейки (D-21). Новых данных получатель не видит, круг получателей не расширяется: оба поля отдаются одним и тем же запросом под теми же гейтами `Action::ReadData` |

Установок пакетов нет — гейт легитимности пакетов неприменим.
</threat_model>

<verification>
- `cargo test -p trackly-infra -- --test-threads=1`, затем (отдельным процессом)
  `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1`.
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`.
- Регресс-гейт границы фазы — зелёный; фикстура и каталог `ui/` не изменены.
</verification>

<success_criteria>
- Три точки вычисления `place_path_short` (devices list/search, devices grouped, cartridges
  list/search) имеют одинаковую семантику деградации, совпадающую с `places_sqlite`.
- «—» в колонке «Место» означает ровно одно: у объекта нет места.
- Ни один файл в `ui/` не изменён; `check-place-path-short.mjs` зелёный без правок.
</success_criteria>

<output>
Create `.planning/phases/39.2-place-path-debt/39.2-04-SUMMARY.md` when done.
В SUMMARY приложить вывод трёх падавших ДО фикса тестов.
</output>
