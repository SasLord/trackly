---
phase: 39.2
plan: 03
type: execute
wave: 3
depends_on: ["39.2-02"]
files_modified:
  - crates/trackly-app/src/tauri_cmds/settings_org.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: true
requirements: [PLC-07]
must_haves:
  truths:
    - "WR-05: три записи org-дефолтов формата пути (`place_path_variant`, `place_path_sep_ends`, `place_path_sep_last_two`) идут ОДНОЙ транзакцией — отказ на втором или третьем операторе не оставляет состояния «вариант новый, разделители старые» при возвращённом `Err`"
    - "WR-05: атомарность доказана тестом с инъекцией отказа на третьем операторе, а не заявлена в комментарии"
    - "IN-05: GET org-дефолтов прогоняет `variant` через `PathDisplayVariant::from_str(...).unwrap_or(Ends)` с `tracing::warn!` на fallback — симметрично SET-пути; мусор в БД больше не приводит к тихой перезаписи настройки на `ends` при следующем «Сохранить» без действия пользователя"
    - "IN-02: Manager → `places_set_path_variant` → 403 (`Action::MutatePlaces`, тот же гейт, что у шести других `places_*` мутаций из Case 45) и Manager → `settings_set_place_path_defaults` → 403 (`Action::ManageSettings`) покрыты матрицей ролей"
    - "Существующие 403-гейты не ослаблены: Case 49 и Case 50 (Employee) остаются в файле и зелёные"
    - "Граница фазы: наблюдаемое поведение на исправной БД не изменилось — golden-фикстура и оба гейта пути зелёные без правок"
  artifacts:
    - path: "crates/trackly-app/src/tauri_cmds/settings_org.rs"
      provides: "транзакционный SET org-дефолтов + валидирующий GET + 3 новых теста в mod place_path"
      contains: "conn.transaction()"
    - path: "crates/trackly-app/tests/role_endpoint_matrix.rs"
      provides: "Manager-кейсы для places_set_path_variant и settings_set_place_path_defaults"
      contains: "places_set_path_variant"
  key_links:
    - from: "build_settings_set_place_path_defaults"
      to: "app_settings (3 ключа)"
      via: "одна транзакция ctx.writer.execute -> conn.transaction() -> tx.commit()"
      pattern: "tx.commit"
    - from: "build_settings_get_place_path_defaults"
      to: "PathDisplayVariant::from_str"
      via: "валидация прочитанного токена с fallback на Ends + warn"
      pattern: "PathDisplayVariant::from_str"
---

<objective>
Три находки в одном файле настроек: запись org-дефолтов формата пути становится атомарной
(WR-05), чтение — симметричным записи по строгости (IN-05), а матрица ролей закрывает дырку
на двух мутациях, где Manager не был покрыт (IN-02).

Purpose: сегодня отказ на втором из трёх `conn.execute` даёт пользователю «не сохранилось»
при уже изменённой части настроек, а мусорный `variant` в БД молча переписывается на `ends`
при следующем «Сохранить». Обе ситуации — тихая потеря пользовательского намерения.

Output: транзакция, валидирующий GET, 3 новых теста в `mod place_path`, 2 новых
Manager-кейса в матрице ролей.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-place-path-debt/39.2-VALIDATION.md
@.planning/phases/39.2-place-path-debt/39.2-02-SUMMARY.md

<interfaces>
Контракты, нужные исполнителю (искать по кодовой базе не требуется):

  trackly_infra::db::writer_worker::WriterHandle::execute<F, R>(op: F) -> Result<R, AppError>
    where F: FnOnce(&mut rusqlite::Connection) -> Result<R, AppError> + Send + 'static
  — замыкание получает `&mut Connection`, поэтому `conn.transaction()` доступен напрямую.
    Образец транзакции в этом же крейте: `crates/trackly-app/src/services/device_service.rs:170`.

  trackly_core::domain::places::PathDisplayVariant::from_str(&str) -> Result<Self, AppError>
  trackly_core::domain::places::PathDisplayVariant::as_str(&self) -> &'static str

  trackly_infra::repos::place_path_settings::{DEFAULT_VARIANT, DEFAULT_SEP_ENDS,
    DEFAULT_SEP_LAST_TWO}  — созданы планом 02; литералы в этом файле использовать нельзя.

  OrgPathDisplayDto { variant: String, sep_ends: String, sep_last_two: String }

  В `crates/trackly-app/tests/role_endpoint_matrix.rs` уже существуют:
    макрос `new_app!()`, хелпер `post_with_cookie(app, path, json, Some(&cookie))`,
    куки `manager_cookie` и `employee_cookie`, Case 45 (Manager → шесть `places_*` мутаций),
    Case 49 (Employee → settings_set_place_path_defaults), Case 50 (Employee →
    places_set_path_variant).
</interfaces>

Приватность (CLAUDE.md): репозиторий публичный, в тестах — только вымышленные значения.
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Атомарная запись org-дефолтов + тест на частичный отказ (WR-05)</name>
  <files>crates/trackly-app/src/tauri_cmds/settings_org.rs</files>

  <read_first>
    - `crates/trackly-app/src/tauri_cmds/settings_org.rs`, строки 345-410 —
      `build_settings_set_place_path_defaults`: валидации ДО записи и три независимых
      `conn.execute` в autocommit;
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «WR-05» (строки 265-297) —
      дословный набросок фикса (цикл по трём парам ключ/значение внутри `tx`);
    - `crates/trackly-app/src/services/device_service.rs`, строки 165-180 — образец
      `conn.transaction()` / `tx.commit()` в этом крейте;
    - `crates/trackly-app/src/tauri_cmds/settings_org.rs`, строки 769-878 — тестовый модуль
      `mod place_path`: хелпер `make_test_ctx()` и форма четырёх существующих тестов.
  </read_first>

  <action>
Переписать тело замыкания в `ctx.writer.execute(...)` внутри
`build_settings_set_place_path_defaults`: открыть `conn.transaction()`, выполнить три
UPSERT'а по парам («place_path_variant» → `patch.variant`, «place_path_sep_ends» →
`patch.sep_ends`, «place_path_sep_last_two» → `patch.sep_last_two`) внутри одного цикла с
параметризованным ключом, затем `tx.commit()`. SQL UPSERT'а сохранить прежним по смыслу
(`INSERT ... ON CONFLICT(key) DO UPDATE SET value = ..., updated_at_utc = ...`), но ключ
передавать параметром, а не подставлять в текст запроса — конкатенации не заводить.
Валидации ДО записи (`PathDisplayVariant::from_str` для `variant`, `.is_empty()` для обоих
разделителей, именно `.is_empty()`, а НЕ `.trim().is_empty()` — D-09/D-10) оставить без
изменений: они уже корректны и покрыты тестами.

Добавить в `mod place_path` тест
`set_is_atomic_partial_failure_leaves_all_three_keys_unchanged`:
1. поднять контекст через `make_test_ctx()`;
2. через `ctx.writer.execute` создать временный триггер, который валит запись ТРЕТЬЕГО
   ключа: `CREATE TRIGGER ... BEFORE UPDATE ON app_settings WHEN NEW.key =
   'place_path_sep_last_two' BEGIN SELECT RAISE(ABORT, 'injected failure'); END;`
   (V039 засеивает все три строки, поэтому UPSERT идёт по ветке UPDATE — триггер на UPDATE
   достаточен; если исполнитель обнаружит иное, добавить и BEFORE INSERT);
3. вызвать `build_settings_set_place_path_defaults` с заведомо ДРУГИМИ значениями
   (например `variant: "last"`, `sep_ends: " @ "`, `sep_last_two: " ~ "`) и убедиться,
   что вернулся `Err`;
4. вызвать `build_settings_get_place_path_defaults` и проверить, что ВСЕ ТРИ значения
   остались прежними (`"ends"`, `" // "`, `" / "`) — то есть первые два оператора
   откатились вместе с третьим.

Тест обязан краснеть на дофиксном коде — исполнитель запускает его ДО правки функции,
фиксирует вывод (ожидаемо: `variant` уже `"last"`, `sep_ends` уже `" @ "`) и прикладывает
в SUMMARY.
  </action>

  <acceptance_criteria>
    - `grep -c 'conn.transaction()' crates/trackly-app/src/tauri_cmds/settings_org.rs` → не менее 1;
    - в `build_settings_set_place_path_defaults` ровно один `tx.commit()` и ни одного
      прямого `conn.execute` с ключом `place_path_*`;
    - тест `set_is_atomic_partial_failure_leaves_all_three_keys_unchanged` существует и
      проходит; его прогон ДО правки функции падал (вывод в SUMMARY);
    - четыре существующих теста `mod place_path` не изменены и проходят;
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app place_path -- --test-threads=1`
      сообщает не менее 5 passed.
  </acceptance_criteria>

  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app place_path -- --test-threads=1</automated>
  </verify>

  <done>Частичный отказ записи org-дефолтов невозможен: либо применены все три ключа,
  либо ни одного — доказано инъекцией отказа.</done>
</task>

<task type="auto">
  <name>Задача 2: Валидация варианта на чтении org-дефолтов + тест (IN-05)</name>
  <files>crates/trackly-app/src/tauri_cmds/settings_org.rs</files>

  <read_first>
    - `crates/trackly-app/src/tauri_cmds/settings_org.rs`, строки 300-345 —
      `build_settings_get_place_path_defaults` после правок плана 02 (дефолты уже
      константами из `place_path_settings`);
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «IN-05»
      (строки 423-437) — почему тихий сырой возврат приводит к перезаписи настройки
      без действия пользователя;
    - `ui/src/features/settings/OrgSettings.svelte`, строки 103-112 (`loadPathDefaults`) —
      клиент уже схлопывает неизвестное значение в `'ends'`, поэтому сервер обязан
      быть источником истины, а не полагаться на это схлопывание.
  </read_first>

  <action>
В `build_settings_get_place_path_defaults` прогонять прочитанное значение ключа
`place_path_variant` через `PathDisplayVariant::from_str`. При успехе класть в
`OrgPathDisplayDto.variant` канонический токен (`.as_str().to_string()`), при ошибке —
`DEFAULT_VARIANT` (то есть поведение `unwrap_or(Ends)`) и один `tracing::warn!` с самим
некорректным значением и пояснением, что настройка отдана как `ends` и будет перезаписана
при следующем сохранении.

Разделители НЕ валидировать и НЕ триммить: пустая строка запрещена только на записи,
whitespace-only разделитель легален и обязан пройти байт-в-байт (D-09) — существующий тест
`set_with_whitespace_only_sep_round_trips_untrimmed` это фиксирует и должен остаться зелёным.

Добавить в `mod place_path` тест `get_with_bogus_variant_in_db_falls_back_to_ends`:
через `ctx.writer.execute` записать в `app_settings` строку `place_path_variant` со
значением `'bogus'` (сырым UPDATE, минуя валидирующий SET), затем вызвать
`build_settings_get_place_path_defaults` и проверить, что `dto.variant == "ends"`, а
разделители не изменились. Тест обязан краснеть на дофиксном коде (вернётся `"bogus"`) —
прогнать ДО правки и приложить вывод в SUMMARY.
  </action>

  <acceptance_criteria>
    - `grep -c 'PathDisplayVariant::from_str' crates/trackly-app/src/tauri_cmds/settings_org.rs`
      → не менее 2 (SET и GET);
    - в `build_settings_get_place_path_defaults` присутствует `tracing::warn!`;
    - тест `get_with_bogus_variant_in_db_falls_back_to_ends` существует, проходит, и его
      прогон ДО правки падал с полученным значением `"bogus"` (вывод в SUMMARY);
    - `set_with_whitespace_only_sep_round_trips_untrimmed` по-прежнему зелёный —
      валидация НЕ распространилась на разделители;
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app place_path -- --test-threads=1`
      сообщает не менее 6 passed.
  </acceptance_criteria>

  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app place_path -- --test-threads=1</automated>
  </verify>

  <done>GET org-дефолтов так же строг к варианту, как SET; мусор в БД не приводит к тихой
  перезаписи настройки.</done>
</task>

<task type="auto">
  <name>Задача 3: Manager-кейсы в матрице ролей для двух мутаций формата пути (IN-02)</name>
  <files>crates/trackly-app/tests/role_endpoint_matrix.rs</files>

  <read_first>
    - `crates/trackly-app/tests/role_endpoint_matrix.rs`, строки 1520-1670 — Case 49
      (Employee → settings_set_place_path_defaults), Case 50 (Employee →
      places_set_path_variant) и Case 45 (Manager → шесть `places_*` мутаций) с точными
      формами payload'ов и assert-сообщений;
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «IN-02»
      (строки 382-394) — формулировка дырки и предписанный фикс;
    - `.planning/phases/39.2-place-path-debt/39.2-VALIDATION.md`, строка требований
      к окружению тестов `trackly-app` (env-переменные и собранный `ui/dist`).
  </read_first>

  <action>
Расширить Case 45 в `crates/trackly-app/tests/role_endpoint_matrix.rs` седьмой мутацией:
Manager-сессия → POST `/api/v1/places_set_path_variant` с payload
`{"id": 1, "pathVariantOverride": "ends", "version": 1}` → ожидается 403. Форму блока и
текст assert-сообщения повторить в точности как у шести соседних проверок Case 45
(с пояснением «D-20 Admin-only»), дополнив ссылкой на IN-02. В шапке Case 45 обновить
комментарий: «все шесть places_* мутаций» → «все семь», и указать, что седьмая добавлена
фазой 39.2 по IN-02, гейт тот же — `Action::MutatePlaces`.

Добавить рядом новый Manager-кейс для настроек: Manager-сессия → POST
`/api/v1/settings_set_place_path_defaults` с тем же payload, что в Case 49
(`{"patch": {"variant": "ends", "sep_ends": " // ", "sep_last_two": " / "}}`) → ожидается 403
(`Action::ManageSettings`). Оформить его как новый пронумерованный кейс со следующим
свободным номером (проверить по файлу — Case 50 занят), в комментарии сослаться на IN-02
и на симметрию с Case 49.

Case 49 и Case 50 (Employee) НЕ трогать — они остаются как есть.

Перед запуском теста убедиться, что `ui/dist` собран (`pnpm --dir ui build`) — сервер-мод
тесты читают его с диска; и что заданы `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1`.
  </action>

  <acceptance_criteria>
    - `grep -c 'places_set_path_variant' crates/trackly-app/tests/role_endpoint_matrix.rs`
      → не менее 2 (Case 50 + новая проверка в Case 45);
    - `grep -c 'settings_set_place_path_defaults' crates/trackly-app/tests/role_endpoint_matrix.rs`
      → не менее 2 (Case 49 + новый Manager-кейс);
    - оба новых assert-сообщения содержат `IN-02` и слово `Manager`;
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1`
      зелёный;
    - полный пакет `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1`
      зелёный;
    - `cargo fmt --check` и `cargo clippy --all-targets -- -D warnings` чисты;
    - регресс-гейт границы фазы зелёный БЕЗ правки `scripts/fixtures/place-path/shorten-cases.json`.
  </acceptance_criteria>

  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1 && cargo test -p trackly-core shorten_place_path_matches_golden_fixture -- --test-threads=1 && (cd ui && node scripts/check-placepath-parity.mjs && node scripts/check-place-path-short.mjs)</automated>
  </verify>

  <done>Матрица ролей покрывает Manager для обеих мутаций формата пути; дырка IN-02
  закрыта; существующие Employee-кейсы не ослаблены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN-браузер / Tauri-webview → `/api/v1/settings_set_place_path_defaults` | мутация организационных настроек, гейт `Action::ManageSettings` (Admin) |
| LAN-браузер / Tauri-webview → `/api/v1/places_set_path_variant` | мутация места, гейт `Action::MutatePlaces` (Admin, D-20 — Manager НЕ допущен) |
| БД → процесс приложения | `app_settings.place_path_variant` может содержать значение, записанное мимо API (правка руками, откат версии) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.2-03-01 | Elevation of Privilege | `places_set_path_variant`, `settings_set_place_path_defaults` | mitigate | Гейты (`Action::MutatePlaces` / `Action::ManageSettings`) существуют и стоят первой строкой, но роль Manager не была покрыта тестом ни для одной из двух мутаций — регрессия в гейте прошла бы незамеченной. Задача 3 добавляет Manager-кейсы в матрицу ролей (IN-02); Employee-кейсы 49/50 сохраняются |
| T-39.2-03-02 | Tampering | `build_settings_set_place_path_defaults` | mitigate | Переход на транзакцию не ослабляет валидацию: `PathDisplayVariant::from_str` и проверки `.is_empty()` остаются ДО открытия транзакции. Ключи UPSERT'ов передаются параметрами, не конкатенацией — SQL-инъекция через ключ невозможна |
| T-39.2-03-03 | Tampering | `build_settings_get_place_path_defaults` | mitigate | Значение из БД перестаёт выходить наружу непроверенным: `from_str` + fallback на `ends` + `tracing::warn!`. Это сужает, а не расширяет множество значений, доходящих до UI и до печатных форм |
| T-39.2-03-04 | Repudiation | fallback в GET | accept | Фиксация аномалии — только `tracing::warn!` в лог, отдельной записи в audit_log не заводим: событие не является действием пользователя и порождается чтением, которое может происходить часто |

Установок пакетов план не делает — гейт легитимности пакетов неприменим.
</threat_model>

<verification>
- `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1`
  (один процесс cargo; `ui/dist` предварительно собран).
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`.
- Регресс-гейт границы фазы — зелёный без правки фикстуры.
</verification>

<success_criteria>
- Отказ на любом из трёх операторов записи org-дефолтов не оставляет частично применённого
  набора (тест с инъекцией отказа).
- GET и SET одинаково строги к токену варианта.
- Manager получает 403 на обеих мутациях формата пути, и это зафиксировано матрицей ролей.
</success_criteria>

<output>
Create `.planning/phases/39.2-place-path-debt/39.2-03-SUMMARY.md` when done.
В SUMMARY приложить вывод обоих падавших ДО фикса тестов (WR-05 и IN-05).
</output>
