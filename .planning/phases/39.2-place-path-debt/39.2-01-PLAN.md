---
phase: 39.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-infra/tests/place_effective_variant.rs
  - crates/trackly-infra/tests/places_crud.rs
  - crates/trackly-infra/src/repos/places_sqlite.rs
autonomous: true
requirements: [PLC-07, PLC-08]
must_haves:
  truths:
    - "WR-02a: потомок soft-deleted места и поведение вью `place_effective_variant` в этом состоянии зафиксированы тестом — фактическое поведение задокументировано как известное ограничение, миграция НЕ заводится (решение фазы: оба состояния недостижимы штатным кодом сегодня)"
    - "WR-02b: отсутствие строки `place_path_variant` в `app_settings` перестаёт быть непокрытым состоянием — тест фиксирует, что вью отдаёт NULL, а читатели деградируют мягко"
    - "WR-02: хелпер `effective_variant()` в `place_effective_variant.rs` больше не панікует ни при отсутствии строки, ни при NULL в колонке — он возвращает `Option<String>`"
    - "IN-01: неизвестный токен в `places.path_variant_override` НЕ роняет весь список мест через `FromSqlConversionFailure` — `from_row` деградирует до `None` («как у родителя») + `tracing::warn!`, единообразно с четырьмя другими потребителями того же токена (`devices_sqlite.rs:113`, `cartridges_sqlite.rs:133`, `report_service.rs:307`, `act_service.rs:3038`)"
    - "Строгость валидации токена остаётся на пути ЗАПИСИ (`PathDisplayVariant::from_str` в `place_service`) — читатель мягкий, писатель строгий"
    - "Граница фазы: ни один кейс `scripts/fixtures/place-path/shorten-cases.json` не изменил результат, оба гейта пути зелёные без правки фикстуры"
  artifacts:
    - path: "crates/trackly-infra/tests/place_effective_variant.rs"
      provides: "2 новых теста (WR-02a/WR-02b) + хелпер effective_variant, возвращающий Option<String>"
      contains: "Option<String>"
    - path: "crates/trackly-infra/tests/places_crud.rs"
      provides: "тест на неизвестный токен в path_variant_override (IN-01)"
      contains: "path_variant_override"
    - path: "crates/trackly-infra/src/repos/places_sqlite.rs"
      provides: "мягкая деградация нераспознанного токена в from_row"
      contains: "tracing::warn!"
  key_links:
    - from: "crates/trackly-infra/src/repos/places_sqlite.rs::from_row"
      to: "PlaceRow.path_variant_override"
      via: "PathDisplayVariant::from_str(...).ok() вместо FromSqlConversionFailure"
      pattern: "path_variant_override"
    - from: "crates/trackly-infra/tests/place_effective_variant.rs"
      to: "вью place_effective_variant (V039)"
      via: "SELECT effective_variant FROM place_effective_variant WHERE place_id = ?"
      pattern: "place_effective_variant"
---

<objective>
Wave 0 фазы 39.2: зафиксировать тестами фактическое поведение вью `place_effective_variant`
на двух непокрытых состояниях БД (WR-02a/WR-02b) и убрать единственную жёсткую точку отказа
на пути чтения варианта — нераспознанный токен в `places.path_variant_override`, который
сегодня роняет дерево мест целиком (IN-01).

Purpose: тесты идут ПЕРВЫМИ в фазе — они фиксируют поведение до того, как остальные планы
тронут читателей этих же данных. Без них рефакторинг WR-08 и WR-01 будет двигать код,
поведение которого нигде не записано.

Output: 3 новых теста, `Option`-хелпер вместо панікующего, мягкая деградация в `from_row`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-place-path-debt/39.2-VALIDATION.md

Приватность (CLAUDE.md, жёсткое условие): репозиторий публичный. В тестах — только
вымышленные названия мест («Здание А», «2 этаж», «214»), никаких реальных данных.

<interfaces>
Контракты, которые исполнителю НЕ нужно искать по кодовой базе:

`trackly_core::domain::places::PathDisplayVariant`
  — варианты `Ends` / `LastTwo` / `Last`;
  — `PathDisplayVariant::from_str(&str) -> Result<Self, AppError>` (токены `ends`,
    `last_two`, `last`; всё прочее → `AppError::Validation`);
  — `PathDisplayVariant::as_str(&self) -> &'static str` (обратная операция).

`trackly_infra::test_support::test_db() -> (rusqlite::Connection, tempfile::TempDir)`
  — открывает временную БД, применяет writer-прагмы и ВСЕ миграции (включая V039).

`trackly_infra::repos::SqlitePlaceRepository` реализует
`trackly_core::ports::places::PlaceRepository` — используется в обоих тест-файлах
для создания мест (`create(&mut conn, &PlaceNew, now)`), см. существующие тесты.

Вью `place_effective_variant` (V039) отдаёт колонку `effective_variant`, которая
может быть NULL (см. WR-02b), а строка для места может вообще отсутствовать
(см. WR-02a) — поэтому чтение обязано быть двухуровнево-опциональным.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Хелпер `effective_variant` -> Option + два теста на непокрытые состояния вью (WR-02a, WR-02b)</name>
  <files>crates/trackly-infra/tests/place_effective_variant.rs</files>

  <read_first>
    - `crates/trackly-infra/tests/place_effective_variant.rs` целиком (6 существующих тестов
      и 3 хелпера — `new_place`, `set_override`, `set_org_default`, `effective_variant`);
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «WR-02» (строки 190-225) —
      там таблица репродукции на SQLite с ТОЧНЫМИ ожидаемыми строками вью для обоих состояний;
    - `migrations/V039__place_path_display.sql`, строки 60-85 — определение вью (рекурсивный
      член + терминальный предикат + COALESCE до org-дефолта).
  </read_first>

  <action>
Переделать хелпер `effective_variant(conn, place_id)` в
`crates/trackly-infra/tests/place_effective_variant.rs` так, чтобы он возвращал
`Option<String>` и не паніковал НИ в одном из двух случаев: строки для места во вью нет
И значение колонки `effective_variant` равно NULL. Практически это означает читать
`r.get::<_, Option<String>>(0)`, оборачивать запрос в `OptionalExtension::optional()` и
складывать два уровня опциональности в один (`.ok().flatten()` либо `.optional()` +
`.flatten()`). Обновить все 6 существующих вызовов хелпера — они ожидают `String`, поэтому
им дописывается распаковка вида `.expect("effective_variant присутствует")`; ожидаемые
значения существующих тестов НЕ менять.

Добавить два теста, фиксирующих ФАКТИЧЕСКОЕ поведение вью (миграцию НЕ править —
это решение фазы, см. `<objective>`):

1. `soft_deleted_ancestor_drops_descendant_from_view` (WR-02a). Дерево из трёх уровней
   («Здание А» ← «2 этаж» ← «214»), org-дефолт `ends`, ни одного override. Проверить, что
   до soft-delete все три места имеют `Some("ends")`. Затем сырым SQL пометить средний
   уровень удалённым (`UPDATE places SET deleted_at_utc = ?1 WHERE id = ?2`) и проверить,
   что живой потомок «214» ИСЧЕЗАЕТ из вью (хелпер отдаёт `None`), а корень остаётся
   `Some("ends")`. В doc-комментарии теста прямо написать: это ЗАФИКСИРОВАННОЕ известное
   ограничение, а не желаемое поведение; сегодня состояние недостижимо штатным кодом
   (места удаляются только hard-delete и только пустые), последствие для пользователя
   закрыто мягкой деградацией читателей (WR-01, план 04), а не правкой вью.

2. `missing_org_default_key_yields_null_variant` (WR-02b). Одно корневое место без
   override; сырым SQL удалить строку `DELETE FROM app_settings WHERE key = 'place_path_variant'`
   и проверить, что хелпер отдаёт `None` (колонка NULL, строка при этом существует).
   В doc-комментарии — та же формулировка: фиксируем факт, ключ пишется upsert'ом, состояние
   недостижимо штатно, последствие закрыто деградацией читателей.

Оба теста обернуть в `tokio::time::timeout(Duration::from_secs(30), ...)` и пометить
`#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` — как все остальные тесты
этого файла.

Если по ходу работы обнаружится, что любое из двух состояний ДОСТИЖИМО штатным кодом
(например, найдётся путь soft-delete места) — остановиться и сообщить: это deviation-триггер
фазы, миграцию заводить нельзя без решения пользователя.
  </action>

  <acceptance_criteria>
    - `fn effective_variant(...)` в `crates/trackly-infra/tests/place_effective_variant.rs`
      имеет возвращаемый тип `Option<String>`;
    - в файле присутствуют функции `soft_deleted_ancestor_drops_descendant_from_view`
      и `missing_org_default_key_yields_null_variant`;
    - `cargo test -p trackly-infra --test place_effective_variant -- --test-threads=1`
      сообщает `8 passed; 0 failed` (6 существующих + 2 новых);
    - ни один существующий тест файла не изменил своих ожидаемых значений
      (`git diff` по файлу не содержит правок assert-ов вне двух новых функций,
      кроме механической распаковки `Option`);
    - `migrations/V039__place_path_display.sql` НЕ изменён (`git status` его не показывает);
    - в тестах нет реальных названий организации/ФИО — только «Здание А», «2 этаж», «214».
  </acceptance_criteria>

  <verify>
    <automated>cargo test -p trackly-infra --test place_effective_variant -- --test-threads=1</automated>
  </verify>

  <done>Вью `place_effective_variant` имеет тесты на оба ранее непокрытых состояния; хелпер
  не паникует ни при отсутствии строки, ни при NULL.</done>
</task>

<task type="auto">
  <name>Задача 2: Мягкая деградация нераспознанного токена в `places_sqlite::from_row` + тест (IN-01)</name>
  <files>crates/trackly-infra/src/repos/places_sqlite.rs, crates/trackly-infra/tests/places_crud.rs</files>

  <read_first>
    - `crates/trackly-infra/src/repos/places_sqlite.rs`, строки 44-90 — текущий `from_row`
      с двумя ветками `FromSqlConversionFailure` (для `kind` и для `path_variant_override`);
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «IN-01» (строки 365-380) —
      там дословный набросок фикса и перечень четырёх потребителей, которые уже деградируют
      мягко;
    - `crates/trackly-infra/src/repos/devices_sqlite.rs`, строки 99-116 (`from_row_with_short_path`) —
      образец мягкой формы: `if let Ok(variant) = PathDisplayVariant::from_str(&token)`;
    - `crates/trackly-infra/src/config.rs`, строка 394 — образец вызова `tracing::warn!` в этом крейте;
    - `crates/trackly-infra/tests/places_crud.rs`, строки 1-60 — хелперы файла и форма
      тестов (`tokio::time::timeout`, `test_db`).
  </read_first>

  <action>
В `crates/trackly-infra/src/repos/places_sqlite.rs::from_row` заменить ветку, превращающую
нераспознанный токен `places.path_variant_override` в
`rusqlite::Error::FromSqlConversionFailure`, на мягкую деградацию: нераспознанный токен даёт
`path_variant_override: None` («как у родителя») и один `tracing::warn!` с полями `place_id`
(значение колонки 0) и самим токеном. Ветку для `places.kind` НЕ трогать — там есть CHECK
constraint в схеме, и её строгость обоснована.

Комментарий над этим местом переписать: объяснить, что столбец `path_variant_override`
добавлен ALTER'ом БЕЗ CHECK (V039), поэтому вероятность мусора в нём выше, чем у `kind`,
а строгость остаётся на пути записи (`PathDisplayVariant::from_str` в `place_service`) —
читатель мягкий, писатель строгий. Прежняя формулировка «зеркалит конвенцию `places.kind`»
должна исчезнуть, иначе она противоречит коду.

В `crates/trackly-infra/tests/places_crud.rs` добавить тест
`unknown_path_variant_override_token_degrades_to_none_and_does_not_break_list`: создать
корневое место «Здание А» и дочернее «2 этаж», сырым SQL записать в
`places.path_variant_override` дочернего заведомо неизвестный токен (например `'bogus'`),
затем через `SqlitePlaceRepository` вызвать `list_all` (или `get` — обе точки должны
пережить мусор) и проверить, что вызов возвращает `Ok`, в результате присутствуют ОБА места,
а у испорченного `path_variant_override == None`. Тест обернуть в
`tokio::time::timeout(Duration::from_secs(30), ...)`, как остальные в файле.

Точный набор методов, которые должны пережить мусор, взять из IN-01: `places_list_all`,
`list_children`, `get`. Достаточно покрыть тестом список (`list_all`) и `get` — оба идут
через один `from_row`.
  </action>

  <acceptance_criteria>
    - `crates/trackly-infra/src/repos/places_sqlite.rs` не содержит `FromSqlConversionFailure`
      с текстом `invalid places.path_variant_override` (проверяется
      `grep -c 'invalid places.path_variant_override' crates/trackly-infra/src/repos/places_sqlite.rs`
      → 0);
    - в том же файле есть `tracing::warn!` в теле `from_row`;
    - ветка `PlaceKind::from_str` с `FromSqlConversionFailure` осталась на месте
      (`grep -c 'invalid places.kind' crates/trackly-infra/src/repos/places_sqlite.rs` → 1);
    - тест `unknown_path_variant_override_token_degrades_to_none_and_does_not_break_list`
      существует и проходит; до правки `from_row` он падает (исполнитель обязан убедиться
      в этом, запустив тест ДО изменения кода репозитория, и записать результат в SUMMARY);
    - `cargo test -p trackly-infra -- --test-threads=1` зелёный целиком;
    - `cargo clippy -p trackly-infra --all-targets -- -D warnings` без предупреждений;
    - регресс-гейт границы фазы зелёный без правки `scripts/fixtures/place-path/shorten-cases.json`.
  </acceptance_criteria>

  <verify>
    <automated>cargo test -p trackly-infra -- --test-threads=1 && cargo clippy -p trackly-infra --all-targets -- -D warnings && cargo test -p trackly-core shorten_place_path_matches_golden_fixture -- --test-threads=1 && (cd ui && node scripts/check-placepath-parity.mjs && node scripts/check-place-path-short.mjs)</automated>
  </verify>

  <done>Одна испорченная строка `places.path_variant_override` больше не ломает дерево мест;
  поведение покрыто тестом; наблюдаемое поведение на исправной БД не изменилось.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| БД → процесс приложения | значения `places.path_variant_override` и `app_settings.place_path_variant` читаются из SQLite, у первого столбца НЕТ CHECK constraint (добавлен ALTER'ом в V039) — то есть это данные, чью форму схема не гарантирует |

Новых точек входа план не создаёт: изменяется только маппинг уже читаемой колонки и
добавляются тесты. HTTP/Tauri-поверхность не трогается, права не трогаются.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.2-01-01 | Denial of Service | `places_sqlite::from_row` | mitigate | Одно нераспознанное значение в столбце без CHECK сегодня делает весь раздел «Места» недоступным (отказ в обслуживании через порчу данных). Задача 2 меняет отказ на деградацию до `None` + `tracing::warn!` — раздел остаётся работоспособным, факт аномалии попадает в лог |
| T-39.2-01-02 | Tampering | путь ЗАПИСИ варианта | accept | Ослабление касается ТОЛЬКО чтения. Валидация записи (`PathDisplayVariant::from_str` в `place_service`, гейт `Action::MutatePlaces`) не изменяется — мусор по-прежнему нельзя записать через API. Мягкий читатель не создаёт способа протащить значение вне enum'а дальше в домен: непарсящийся токен становится `None`, а не произвольной строкой |

Установок пакетов (npm/pip/cargo add) план не делает — гейт легитимности пакетов
неприменим.
</threat_model>

<verification>
- `cargo test -p trackly-infra -- --test-threads=1` — зелёный целиком (один процесс cargo).
- `cargo clippy -p trackly-infra --all-targets -- -D warnings`, `cargo fmt --check`.
- Регресс-гейт границы фазы: `cargo test -p trackly-core shorten_place_path_matches_golden_fixture`
  + `node ui/scripts/check-placepath-parity.mjs` + `node ui/scripts/check-place-path-short.mjs`,
  все три зелёные БЕЗ правки `scripts/fixtures/place-path/shorten-cases.json`.
- `git status` не показывает изменений в `migrations/`.
</verification>

<success_criteria>
- 3 новых теста (2 на вью, 1 на токен) проходят; общее число тестов
  `place_effective_variant` = 8.
- Нераспознанный токен `path_variant_override` не приводит к `Err` ни в `list_all`, ни в `get`.
- Ни одно наблюдаемое на исправной БД поведение не изменилось (регресс-гейт зелёный).
</success_criteria>

<output>
Create `.planning/phases/39.2-place-path-debt/39.2-01-SUMMARY.md` when done.
В SUMMARY зафиксировать: (а) вывод падающего теста IN-01 ДО правки `from_row`;
(б) точное поведение вью в обоих состояниях WR-02 — оно станет справочным для Фазы 40.
</output>
