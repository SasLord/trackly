---
phase: 39.2
plan: 02
type: execute
wave: 2
depends_on: ["39.2-01"]
files_modified:
  - crates/trackly-infra/src/repos/place_path_settings.rs
  - crates/trackly-infra/src/repos/mod.rs
  - crates/trackly-infra/src/repos/devices_sqlite.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-infra/src/repos/places_sqlite.rs
  - crates/trackly-infra/tests/place_path_settings.rs
  - crates/trackly-app/src/services/report_service.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/tauri_cmds/settings_org.rs
autonomous: true
requirements: [PLC-07, PLC-08]
must_haves:
  truths:
    - "WR-08: `read_path_display_separators` существует в ЕДИНСТВЕННОМ экземпляре — в новом модуле `trackly_infra::repos::place_path_settings`; четыре дословные копии (`devices_sqlite.rs:77`, `cartridges_sqlite.rs:105`, `places_sqlite.rs:196`, `report_service.rs:280`) и пятая, заинлайненная в `act_service::compute_place_path_short` (`act_service.rs:3042`), заменены импортом"
    - "WR-08: строковые дефолты `\" // \"` / `\" / \"` / `\"ends\"` объявлены один раз как `DEFAULT_SEP_ENDS` / `DEFAULT_SEP_LAST_TWO` / `DEFAULT_VARIANT`; `build_settings_get_place_path_defaults` (`settings_org.rs:315-317`) использует константы, а не литералы"
    - "WR-08: у значения по умолчанию появился единственный владелец в коде — грядущий сдвиг дефолта физически не может обновить не все копии, потому что копий больше нет; доказано тестом-сканером исходников, а не обещанием в комментарии"
    - "WR-08: второй законный носитель тех же значений — сид `migrations/V039__place_path_display.sql` — связан с константами модуля ТЕСТОМ (`fresh_db_seed_matches_module_defaults` сравнивает прочитанное из свежей БД с `DEFAULT_*`), а не только doc-комментарием: расхождение константы и сида краснеет автоматически"
    - "WR-08 (граница плана): восьмое место-владелец — литералы состояния в `ui/src/features/settings/OrgSettings.svelte:67-69` — закрывается планом 05 (фронт), а не этим планом; на конец фазы владельцев дефолта остаётся ровно два: модуль `place_path_settings` и сид V039"
    - "Модуль живёт в `trackly-infra` (там уже 3 из 5 копий и там разрешён `rusqlite`), а НЕ в `trackly-core` — гейт `crates/trackly-core/tests/no_io_deps.rs` запрещает `rusqlite` в ядре"
    - "Граница фазы: рефакторинг не изменил ни одного результата на исправной БД — оба гейта пути и golden-фикстура зелёные без правок фикстуры"
  artifacts:
    - path: "crates/trackly-infra/src/repos/place_path_settings.rs"
      provides: "единственный владелец дефолтов формата пути: DEFAULT_VARIANT/DEFAULT_SEP_ENDS/DEFAULT_SEP_LAST_TWO + read_path_display_separators + read_org_default_variant_token"
      contains: "pub fn read_path_display_separators"
      min_lines: 40
    - path: "crates/trackly-infra/tests/place_path_settings.rs"
      provides: "поведенческие тесты модуля + тест-сканер, доказывающий отсутствие дублей"
      contains: "place_path_sep_ends"
  key_links:
    - from: "crates/trackly-infra/src/repos/devices_sqlite.rs"
      to: "crate::repos::place_path_settings::read_path_display_separators"
      via: "use + вызов вместо приватной копии"
      pattern: "place_path_settings"
    - from: "crates/trackly-app/src/services/act_service.rs"
      to: "trackly_infra::repos::place_path_settings"
      via: "use + вызовы read_org_default_variant_token/read_path_display_separators"
      pattern: "place_path_settings"
    - from: "crates/trackly-app/src/tauri_cmds/settings_org.rs"
      to: "place_path_settings::DEFAULT_*"
      via: "инициализация значений GET константами вместо литералов"
      pattern: "DEFAULT_SEP_ENDS"
---

<objective>
WR-08: у дефолтов формата пути места появляется ровно один владелец. Сегодня
`read_path_display_separators` скопирована дословно четыре раза, пятая копия заинлайнена
в `act_service`, а литералы `" // "` / `" / "` / `"ends"` живут ещё и в
`build_settings_get_place_path_defaults` — итого 8 мест, обязанных меняться синхронно.

Purpose: Фаза 40 (история перемещений) читает те же настройки. Пока копий пять, любой
будущий сдвиг дефолта почти наверняка обновит не все — и разойдётся то, что пользователь
видит в списках, отчётах и печатных формах.

Output: модуль `trackly_infra::repos::place_path_settings`, шесть переключённых мест,
тест-сканер, который громко падает при появлении шестой копии.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.2-place-path-debt/39.2-VALIDATION.md
@.planning/phases/39.2-place-path-debt/39.2-01-SUMMARY.md

<interfaces>
Целевой публичный контракт нового модуля (объявляется этим планом, потребляется
шестью местами — иных потребителей быть не должно):

  crates/trackly-infra/src/repos/place_path_settings.rs
    pub const DEFAULT_VARIANT: &str        // "ends"
    pub const DEFAULT_SEP_ENDS: &str       // " // "
    pub const DEFAULT_SEP_LAST_TWO: &str   // " / "
    pub fn read_path_display_separators(conn: &rusqlite::Connection) -> (String, String)
    pub fn read_org_default_variant_token(conn: &rusqlite::Connection) -> String

Ключи в `app_settings`, которые читает модуль (и больше не читает никто другой):
  'place_path_variant', 'place_path_sep_ends', 'place_path_sep_last_two'

Уже существующее (не создаётся этим планом):
  trackly_core::domain::places::PathDisplayVariant::{from_str, as_str}
  trackly_core::domain::places::shorten_place_path(full, variant, sep_ends, sep_last_two)
  trackly_infra::test_support::test_db() -> (Connection, TempDir)
</interfaces>

Приватность (CLAUDE.md): репозиторий публичный, в тестах — только вымышленные данные.
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Модуль `place_path_settings` + перевод трёх репозиториев `trackly-infra`</name>
  <files>crates/trackly-infra/src/repos/place_path_settings.rs, crates/trackly-infra/src/repos/mod.rs, crates/trackly-infra/src/repos/devices_sqlite.rs, crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-infra/src/repos/places_sqlite.rs, crates/trackly-infra/tests/place_path_settings.rs</files>

  <read_first>
    - `.planning/phases/39.1-place-path-display/39.1-REVIEW.md`, раздел «WR-08» (строки 341-362) —
      перечень всех восьми мест и дословный набросок фикса;
    - `crates/trackly-infra/src/repos/devices_sqlite.rs`, строки 70-120 — эталонная копия
      `read_path_display_separators` (её текст и есть будущее тело модуля) и её вызовы
      на строках 652, 934, 1255;
    - `crates/trackly-infra/src/repos/cartridges_sqlite.rs`, строки 100-145 и вызовы
      на строках 960, 1398;
    - `crates/trackly-infra/src/repos/places_sqlite.rs`, строки 185-215 — третья копия
      и её вызов в `list_subtree_contents_impl`;
    - `crates/trackly-infra/src/repos/mod.rs` целиком — форма объявления модулей и ре-экспортов;
    - `crates/trackly-core/tests/no_io_deps.rs`, строки 1-20 — почему модуль НЕЛЬЗЯ класть
      в `trackly-core` (`rusqlite` в списке запрещённых крейтов ядра).
  </read_first>

  <action>
Создать `crates/trackly-infra/src/repos/place_path_settings.rs` с публичным контрактом
из `<interfaces>`:

- `DEFAULT_VARIANT = "ends"`, `DEFAULT_SEP_ENDS = " // "`, `DEFAULT_SEP_LAST_TWO = " / "` —
  значения обязаны совпадать с тем, что засеивает `migrations/V039__place_path_display.sql`;
- `read_path_display_separators(conn) -> (String, String)` — перенести тело эталонной копии
  из `devices_sqlite.rs` дословно, заменив литералы на константы. Guarded-read через
  `.ok().unwrap_or_else(...)` сохранить: отсутствующая строка `app_settings` не должна быть
  ошибкой;
- `read_org_default_variant_token(conn) -> String` — читает ключ `place_path_variant`,
  при отсутствии возвращает `DEFAULT_VARIANT`; это ровно то, что сегодня заинлайнено
  в `act_service.rs:3029-3037` (внешний `unwrap_or_else`-блок).

Doc-комментарий модуля обязан объяснить: (а) это ЕДИНСТВЕННЫЙ владелец дефолтов формата
пути, любая новая копия — регрессия WR-08; (б) второй владелец тех же значений —
`migrations/V039__place_path_display.sql` (сид), и при сдвиге дефолта менять надо оба;
(в) почему модуль в `trackly-infra`, а не в `trackly-core` (`rusqlite` запрещён в ядре
гейтом `no_io_deps.rs`). Прежние комментарии в репозиториях, объявлявшие дублирование
«конвенцией фазы 39.1» («each read-path repo keeps its own private copy»), удалить —
после этого плана они ложные.

Объявить `pub mod place_path_settings;` в `crates/trackly-infra/src/repos/mod.rs`
(ре-экспорт типа `pub use` не нужен — потребители обращаются по полному пути модуля).

Удалить приватные копии `read_path_display_separators` из `devices_sqlite.rs`,
`cartridges_sqlite.rs`, `places_sqlite.rs` и переключить все их вызовы (5 вызовов:
devices — 3, cartridges — 2, places — 1) на функцию из нового модуля. Сигнатура вызова
не меняется, поэтому правка call-site'ов сводится к импорту.

Завести `crates/trackly-infra/tests/place_path_settings.rs` с поведенческими тестами
на свежей БД из `test_db()`. КРИТИЧНО: ожидаемые значения задавать КОНСТАНТАМИ модуля, а не
литералами — иначе тест сравнивает сид с самим собой и расхождение «константа ↔ сид V039»
(второй законный владелец тех же значений) не поймается ничем, кроме doc-комментария:
  - `fresh_db_seed_matches_module_defaults` —
    `assert_eq!(read_path_display_separators(&conn), (DEFAULT_SEP_ENDS.to_string(),
    DEFAULT_SEP_LAST_TWO.to_string()))` и
    `assert_eq!(read_org_default_variant_token(&conn), DEFAULT_VARIANT)`. Именно этот тест
    удерживает синхронность модуля и миграции: сдвиньте константу без правки V039 (или
    наоборот) — он покраснеет. В doc-комментарии теста это назвать прямо;
  - `read_separators_falls_back_when_rows_missing` — после `DELETE FROM app_settings
    WHERE key LIKE 'place_path_sep%'` возвращаются те же константы, без паники и без `Err`;
  - `read_org_default_variant_token_falls_back_when_key_missing` — после удаления ключа
    `place_path_variant` возвращается `DEFAULT_VARIANT`.
  </action>

  <acceptance_criteria>
    - файл `crates/trackly-infra/src/repos/place_path_settings.rs` существует и экспортирует
      все пять имён из `<interfaces>`;
    - `grep -c 'fn read_path_display_separators' crates/trackly-infra/src/repos/devices_sqlite.rs`
      → 0; то же для `cartridges_sqlite.rs` и `places_sqlite.rs`;
    - `grep -c 'fn read_path_display_separators' crates/trackly-infra/src/repos/place_path_settings.rs`
      → 1;
    - в `crates/trackly-infra/src/repos/` строка `" // "` встречается только в
      `place_path_settings.rs` (проверяется командой, исключающей строки комментариев:
      `grep -rn '" // "' crates/trackly-infra/src/repos/ | grep -v place_path_settings.rs`
      → пусто);
    - `cargo test -p trackly-infra -- --test-threads=1` зелёный целиком, включая
      `devices_place_path_short`, `cartridges_place_path_short`, `places_crud`,
      `place_effective_variant`;
    - `cargo clippy -p trackly-infra --all-targets -- -D warnings` чист.
  </acceptance_criteria>

  <verify>
    <automated>cargo test -p trackly-infra -- --test-threads=1 && cargo clippy -p trackly-infra --all-targets -- -D warnings</automated>
  </verify>

  <done>Три копии в `trackly-infra` заменены одним владельцем; поведение чтения покрыто
  тестами на свежей БД и на БД с удалёнными ключами.</done>
</task>

<task type="auto">
  <name>Задача 2: Перевод `trackly-app` (report_service, act_service, settings_org) + тест-сканер против новых дублей</name>
  <files>crates/trackly-app/src/services/report_service.rs, crates/trackly-app/src/services/act_service.rs, crates/trackly-app/src/tauri_cmds/settings_org.rs, crates/trackly-infra/tests/place_path_settings.rs</files>

  <read_first>
    - `crates/trackly-app/src/services/report_service.rs`, функция
      `read_path_display_separators` (около строки 280) и её потребитель
      `compute_place_path_short` — четвёртая копия;
    - `crates/trackly-app/src/services/act_service.rs`, функция `compute_place_path_short`
      (блок около строк 3029-3052) — пятая, заинлайненная копия: сначала чтение override
      места, затем fallback на ключ `place_path_variant`, затем локальное замыкание
      `read_sep` с теми же двумя литералами;
    - `crates/trackly-app/src/tauri_cmds/settings_org.rs`, функция
      `build_settings_get_place_path_defaults` (около строк 319-321) — три литерала-дефолта;
      ВНИМАНИЕ: номера строк в ревью фазы 39.1 сдвинуты, ориентируйся на ИМЯ функции;
    - `crates/trackly-infra/src/repos/place_path_settings.rs` — созданный в задаче 1 контракт;
    - `crates/trackly-core/tests/no_io_deps.rs` — образец теста, который проверяет
      инвариант КОДОВОЙ БАЗЫ, а не поведение (форма для тест-сканера).
  </read_first>

  <action>
Переключить три места в `trackly-app` на `trackly_infra::repos::place_path_settings`:

1. `report_service.rs` — удалить локальную `read_path_display_separators`, вызовы заменить
   на функцию из модуля. Функция `compute_place_path_short` в этом файле остаётся как есть
   (её поведение — предмет других находок, не WR-08).
2. `act_service.rs::compute_place_path_short` — заменить заинлайненный fallback-блок на
   `read_org_default_variant_token(conn)` и локальное замыкание `read_sep` на
   `read_path_display_separators(conn)`. Чтение per-place override (первый запрос,
   `places.path_variant_override`) остаётся на месте — это не дефолт организации.
   Существующая мягкая деградация `PathDisplayVariant::from_str(...).unwrap_or(Ends)`
   сохраняется без изменений.
3. `settings_org.rs::build_settings_get_place_path_defaults` — три инициализатора
   (`"ends"`, `" // "`, `" / "`) заменить на `DEFAULT_VARIANT`, `DEFAULT_SEP_ENDS`,
   `DEFAULT_SEP_LAST_TWO`. Логику функции (один `SELECT key, value ... WHERE key IN (...)`)
   НЕ переписывать — она читает три ключа одним запросом, это её право; меняются только
   значения по умолчанию. Валидация варианта в этой функции — предмет плана 03 (IN-05),
   здесь её НЕ добавлять, чтобы два плана не переписывали одни и те же строки.

Добавить в `crates/trackly-infra/tests/place_path_settings.rs` тест-сканер
`no_duplicate_path_default_owners_in_sources`: он читает исходники по путям, собранным от
`env!("CARGO_MANIFEST_DIR")`, и утверждает, что ключи `place_path_sep_ends`,
`place_path_sep_last_two` и `place_path_variant` не встречаются НИ В ОДНОМ из перечисленных
файлов, кроме `place_path_settings.rs`. Третий ключ обязателен: заинлайненный fallback на
org-дефолт варианта живёт именно в `act_service.rs` (около строки 3031), и без него сканер
не поймает повторное заинлайнивание. Единственное исключение по этому ключу —
`settings_org.rs` (см. ниже): он читает все три ключа одним `SELECT` как API настроек.
Строки-комментарии (`//`, `///`, `//!`) при сканировании ИГНОРИРОВАТЬ: упоминание имени
ключа в пояснении легально и полезно (например, doc-комментарий
`act_service.rs:3006` объясняет источник org-дефолта), дублируется именно КОД. Без этого
фильтра гейт был бы самоинвалидирующимся — ровно та ошибка, которую проект уже ловил. Список сканируемых файлов задать явным массивом (не обходом
дерева), чтобы тест был читаемым и предсказуемым:
  crates/trackly-infra/src/repos/devices_sqlite.rs
  crates/trackly-infra/src/repos/cartridges_sqlite.rs
  crates/trackly-infra/src/repos/places_sqlite.rs
  crates/trackly-app/src/services/report_service.rs
  crates/trackly-app/src/services/act_service.rs
Сообщение об ошибке обязано называть файл, строку и найденный ключ и прямо говорить,
что это регрессия WR-08 — «дефолт формата пути снова получил второго владельца».
`settings_org.rs` в список сканируемых на ключи файлов НЕ включать: он легитимно читает те
же ключи одним `SELECT` как API настроек — но проверить в нём отдельным утверждением, что
литералы `" // "` и `" / "` в нём отсутствуют (владельцем ЗНАЧЕНИЙ остаётся модуль, за
`settings_org.rs` остаётся только право знать ИМЕНА ключей). Это исключение записать в
сообщении об ошибке сканера, чтобы следующий читатель не принял его за недосмотр.

Тест обязан падать на «дофиксной» форме — исполнитель проверяет это на КОПИИ файла в
scratchpad (скопировать `act_service.rs` во временный каталог, вернуть в него старый блок
и убедиться, что сканер краснеет), а не на реальном файле.
  </action>

  <acceptance_criteria>
    - `grep -rn 'place_path_sep_ends' crates/trackly-infra/src crates/trackly-app/src | grep -v 'place_path_settings.rs' | grep -v 'settings_org.rs' | grep -vE ':[0-9]+: *//'` → пусто;
    - `grep -rn 'place_path_variant' crates/trackly-infra/src crates/trackly-app/src | grep -v 'place_path_settings.rs' | grep -v 'settings_org.rs' | grep -vE ':[0-9]+: *//'` → пусто
      (заинлайненный fallback из `act_service.rs` исчез; упоминания ключа в doc-комментариях
      легальны и отфильтрованы);
    - `grep -c '" // "' crates/trackly-app/src/tauri_cmds/settings_org.rs` → 0;
    - `grep -rn 'place_path_settings' crates/trackly-app/src | wc -l` → не менее 3 (три
      переключённых места);
    - тест `no_duplicate_path_default_owners_in_sources` присутствует и проходит;
    - самотест сканера выполнен на копии в scratchpad, вывод падения приложен в SUMMARY;
    - `cargo test -p trackly-infra --test place_path_settings -- --test-threads=1` зелёный;
    - тест `fresh_db_seed_matches_module_defaults` сравнивает прочитанное из свежей БД
      именно с `DEFAULT_SEP_ENDS`/`DEFAULT_SEP_LAST_TWO`/`DEFAULT_VARIANT` (в файле теста
      нет литералов `" // "`, `" / "`, `"ends"` в ожидаемых значениях — проверяется
      `grep -n '" // "' crates/trackly-infra/tests/place_path_settings.rs` → пусто);
    - расхождение константы и сида проверено вручную: временно изменить `DEFAULT_SEP_ENDS`
      в копии, убедиться, что тест краснеет, вернуть значение (вывод в SUMMARY);
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1` зелёный;
    - `cargo clippy --all-targets -- -D warnings` и `cargo fmt --check` чисты;
    - регресс-гейт границы фазы зелёный БЕЗ правки `scripts/fixtures/place-path/shorten-cases.json`.
  </acceptance_criteria>

  <verify>
    <automated>cargo test -p trackly-infra --test place_path_settings -- --test-threads=1 && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1 && cargo test -p trackly-core shorten_place_path_matches_golden_fixture -- --test-threads=1 && (cd ui && node scripts/check-placepath-parity.mjs && node scripts/check-place-path-short.mjs)</automated>
  </verify>

  <done>Все шесть потребителей читают дефолты из одного модуля; появление седьмой копии
  ловится тестом; ни один видимый результат не изменился.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| БД → процесс приложения | значения `app_settings.place_path_*` читаются из SQLite; форма значений схемой не гарантирована |

План чисто внутренний: ни новых команд, ни новых HTTP-маршрутов, ни изменений в
авторизации. Публичная поверхность приложения после него побайтово та же.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.2-02-01 | Tampering | `place_path_settings::read_*` | accept | Функции читают `app_settings` параметризованным запросом (`WHERE key = ?1`) и никогда не конкатенируют значения в SQL; значение используется только как разделитель при форматировании строки. Инъекция невозможна, порча значения даёт косметический эффект. Риск не изменился по сравнению с пятью копиями |
| T-39.2-02-02 | Information Disclosure | doc-комментарии и тесты нового модуля | mitigate | Модуль и тесты пишутся с вымышленными данными; примеры путей в комментариях — только «Здание А / 1 этаж / 1-05». Проверяется pre-commit хуком `scripts/check-privacy.mjs` |

Единственная реальная угроза этого плана — регрессионная, а не атакующая: молчаливое
изменение видимого поведения при переносе кода. Она закрыта регресс-гейтом границы фазы
в `<verify>` задачи 2. Установок пакетов нет — гейт легитимности неприменим.
</threat_model>

<verification>
- `cargo test -p trackly-infra -- --test-threads=1` и
  `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1`
  — по одному процессу cargo за раз.
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`.
- Регресс-гейт границы фазы (golden-фикстура + оба `check-*` гейта пути) — зелёный без
  правки фикстуры.
</verification>

<success_criteria>
- `read_path_display_separators` существует ровно в одном файле кодовой базы.
- Литералы `" // "` / `" / "` / `"ends"` как ДЕФОЛТЫ объявлены в коде один раз; второй
  законный носитель тех же значений — сид миграции V039 — связан с константами тестом
  `fresh_db_seed_matches_module_defaults`, а не только doc-комментарием.
- Восьмое место-владелец (`OrgSettings.svelte:67-69`) осознанно оставлено плану 05 —
  этот план закрывает 7 из 8, фаза целиком закрывает 8 из 8.
- Тест-сканер падает при возврате любой из пяти копий (доказано самотестом на копии).
- Наблюдаемое поведение на исправной БД не изменилось.
</success_criteria>

<output>
Create `.planning/phases/39.2-place-path-debt/39.2-02-SUMMARY.md` when done.
В SUMMARY приложить вывод самотеста сканера на «дофиксной» копии.
</output>
