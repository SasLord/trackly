# Phase 37: Приватность данных - Context

**Gathered:** 2026-08-14
**Status:** Ready for planning

> **Дисциплина этого файла (наследует 37-SPEC.md).** Репозиторий публичный. Ниже нет ни
> одного реального значения — только классы утечки, пути файлов и плейсхолдеры. PLAN,
> SUMMARY, REVIEW и VERIFICATION этой фазы обязаны соблюдать то же правило, включая
> сообщения коммитов.

<domain>
## Phase Boundary

Две последовательные работы над одним и тем же репозиторием:

1. **Чистка HEAD** (R1–R4) — обезличивание 17 текстовых файлов по классам A (реквизиты
   организации), B (ФИО), C (инфраструктурные идентификаторы) и удаление 3 бинарных
   reference-артефактов класса D.
2. **Единый durable-гейт** (R5–R10) — новый скрипт, поглощающий
   `scripts/check-privacy-requisites.sh`, работающий по SHA-256 хэшам токенов, с контролем
   бинарных расширений и исключениями только по путям; подключён в `pre-commit` (staged) и
   в `ci-fast.yml` (весь HEAD).

**Порядок жёсткий и не обсуждается:** гейт, включённый до чистки, падает на собственном
репозитории с первого запуска.

**Не в этой фазе:** переписывание истории git и перевод репозитория в private (решение
пользователя 2026-08-08, PRIV-03 в бэклоге); распаковка бинарников; морфологическая
нормализация окончаний; секреты/пароли/API-ключи; аудит утечек вне классов A–D; ротация
пароля служебной учётной записи AD.

</domain>

<spec_lock>
## Requirements (locked via SPEC.md)

**10 требований залочено.** Полные формулировки, границы и критерии приёмки — в
`.planning/phases/37-data-privacy/37-SPEC.md`. Здесь они не дублируются.

Downstream-агенты ОБЯЗАНЫ прочитать `37-SPEC.md` до планирования и реализации.

**In scope (из SPEC.md):**
- Обезличивание 17 текстовых файлов HEAD по классам A/B/C (замена на плейсхолдеры, смысл сохраняется)
- Удаление `.planning/reference/act-word-source/` из индекса + `.gitignore`
- Удаление `.planning/PHASE-BRIEF-act-pdf-word-fidelity.md`
- Новый единый durable-гейт: allowlist реквизитов + хэши токенов + контроль бинарников + исключения по путям
- Поглощение и удаление `scripts/check-privacy-requisites.sh`, перевод шага `ci-fast.yml` на новый гейт
- `pre-commit`-хук в версионируемом каталоге хуков + документация по его включению
- Файл SHA-256 хэшей токенов, коммитится в репозиторий

**Out of scope (из SPEC.md):**
- Переписывание истории git (`filter-repo`, force-push) и перевод репозитория в private
- Распаковка и сканирование содержимого бинарных файлов
- Морфологическая нормализация русских окончаний в токенах
- Секреты/пароли/API-ключи
- Подключение гейта в `pnpm --dir ui lint`
- Аудит `.planning/`-артефактов на утечки вне классов A–D
- Ротация пароля служебной учётной записи AD

**Поправка к SPEC, принятая в этом обсуждении:** R9 перечисляет каталог самой Фазы 37
среди исключаемых путей. Это противоречит Constraints того же SPEC («артефакты фазы сами
не должны содержать маркеров») и создаёт слепую зону ровно там, где агенты пишут про
утечку. Исключение снимается — см. D-13.

</spec_lock>

<decisions>
## Implementation Decisions

### Чистка HEAD и висячие ссылки (R1–R4)

- **D-01: Ссылки на удаляемые файлы переписываются во всех `.planning`-артефактах, включая
  архивные.** `act-word-source` упоминается в 10 файлах, `PHASE-BRIEF-act-pdf-word-fidelity` —
  в 8 (в том числе `.planning/ROADMAP.md`, `14-CONTEXT.md`, `15-*`, `20-PATTERNS.md`,
  `35-05-PLAN/SUMMARY`, `35-CONTEXT.md`, `36-CONTEXT.md`, `quick/260704-wxw-*`). Формулировка
  замены: «исходный образец не хранится в репозитории», окружающий технический смысл
  сохраняется. Отвергнуты: править только «живые» документы (оставляет ~12 файлов,
  указывающих в пустоту, и смягчает acceptance R4) и не править ничего.

- **D-02 (ЖЁСТКОЕ ИСКЛЮЧЕНИЕ ИЗ D-01): `migrations/V033__org_settings_requisites.sql`
  НЕ ТРОГАТЬ.** Файл содержит ссылку на удаляемый PHASE-BRIEF в комментарии (строка 5).
  refinery считает checksum SipHasher13 по `name + version + sql`, где `sql` — весь текст
  файла **вместе с комментариями**; `PartialEq for Migration` сравнивает checksum, а
  `abort_divergent` по умолчанию `true`. Правка комментария в уже применённой миграции даёт
  `Kind::DivergentVersion` при старте на **каждой установленной копии Trackly** — приложение
  не поднимется. Сама ссылка реальных данных не содержит: это имя `.planning`-файла.
  Планировщик обязан внести это в запреты плана явно.

- **D-03: Чистка выполняется разовым скриптом замены, а не ручными правками.** Mapping
  «реальное значение → плейсхолдер» живёт **вне репозитория** (scratchpad-файл или
  интерактивный ввод), скрипт применяет его машинно ко всему списку файлов и **не
  коммитится**. Результат проверяется по diff. Причина: главный риск фазы — не сама чистка,
  а вторичная утечка, когда агент-исполнитель или ревьюер цитирует прочитанное реальное
  значение в свой SUMMARY/REVIEW (зафиксированная повторяющаяся ловушка проекта). При
  машинной замене отчёт агента звучит как «файл X, класс B, N замен». Отвергнуты: ручные
  Edit-правки под протоколом (полагается на дисциплину) и полностью ручная чистка
  пользователем (весь труд по 20 файлам на пользователя).

- **D-04: В `.gitignore` добавляется весь `.planning/reference/`, а не только
  `act-word-source/`.** Локально держать образцы можно, в репозиторий они не попадут
  никогда. Узкий паттерн оставляет соседний новый каталог образцов версионируемым (его
  поймал бы только гейт R8 — то есть на одну преграду позже).

### Файл хэшей и токенизация (R6)

- **D-05: Токен = отдельное слово И скользящее окно из 2–3 слов (n-граммы).** Многословный
  маркер (полное название организации, ФИО целиком) заносится в список **целиком** и ловится
  точно, при этом общеупотребимые слова в список класть не нужно — иначе они дают ложные
  срабатывания по всему репозиторию. Цена: втрое больше хэшей на файл (десятки мс на весь
  репозиторий, SHA-256 короткой строки ~микросекунды). Отвергнуты: только отдельные слова
  (многословное название ловится лишь через самое редкое слово в нём — либо дыра, либо шум)
  и «слова + склейка без разделителей» (не ловит фразу через пробелы).

- **D-06: Нормализация перед хэшированием — `lowercase` + `ё→е` + Unicode NFC + срезание
  пунктуации по краям токена.** Стемминга и морфологии нет (отклонено SPEC) — нужные
  словоформы заносятся в список отдельными токенами. Латинская транслитерация фамилии
  (`Ivanov`-подобная) — тоже отдельный токен, автоматически не выводится. Отвергнуты:
  дополнительная «цифровая очистка» (удаление пробелов/дефисов/скобок внутри числовых
  токенов — растит риск совпасть с чужой цифровой последовательностью, ровно случай
  `Cargo.lock`) и полное отсутствие нормализации (маркер в другом регистре проходит сквозь).

- **D-07: Формат файла хэшей — текстовый, строка = `<sha256> <класс A|B|C>`,
  `#`-комментарии, шапка с инструкцией по восстановлению.** Метка класса нужна, чтобы гейт
  мог сказать «маркер класса B (ФИО)», не называя значения (см. D-12), а шапка — чтобы
  fail-closed из R7 объяснял, что делать. Строки отсортированы: diff читаемый, порядок
  стабильный. Отвергнуты: JSON (комментарии-инструкции приходится прятать в служебный ключ)
  и голый список хэшей без меток (сообщение гейта теряет диагностическую ценность).
  **Метка — только класс.** Никаких пояснений вида «фамилия такого-то» — это наводка.

- **D-08: Остаточный риск словарного восстановления хэшей принят осознанно и
  документируется в шапке файла.** SHA-256 короткого низкоэнтропийного токена (фамилия,
  домен, аббревиатура) перебирается по словарю за секунды; соль, лежащая в том же
  репозитории, от целевого словаря не спасает; KDF с растяжкой неприменим, потому что
  хэшируется каждый токен каждого проверяемого файла; секретов у CI для этой проверки нет
  (Constraint SPEC). Ключевое: сами значения **и так** лежат в истории git — PRIV-03 принят
  осознанно, — поэтому маргинальный риск близок к нулю. Файл хэшей — это список стоп-слов
  без плейнтекста, а не криптозащита; так его и надо описать. Отвергнуты: фиксированная
  соль в коде гейта (нулевая цена, но и почти нулевая польза — создаёт ложное ощущение
  защиты) и отказ от хэшей в пользу одних регулярок по форматам (переоткрывает R6 и
  оставляет ФИО и название организации без защиты вообще).

### Язык гейта, размещение, подключение (R5, R10)

- **D-09: Гейт — zero-dependency Node ESM (`.mjs`) в корневом `scripts/`.** По образцу
  `ui/scripts/check-print-isolation.mjs` (структурный гейт, самотест через аргумент-путь), но
  **не** в `ui/scripts/`: гейт repo-wide, а не фронтендовый, и в `pnpm --dir ui lint` он по
  решению SPEC не подключается. `node:crypto` даёт одинаковый SHA-256 на macOS и Linux, а
  юникод-нормализация из D-06 в bash практически неисполнима (`sha256sum` против `shasum`,
  lowercase кириллицы, `ё→е`). Отвергнуты: bash (текущий язык `check-privacy-requisites.sh`)
  и Rust-тест в `crates/` (`cargo test` на каждый `git commit` — десятки секунд, плюс в
  репозитории есть унаследованный висячий тест).

- **D-10: Хук — `.githooks/pre-commit` + односторочный `scripts/setup-hooks.sh`, который
  делает `git config core.hooksPath .githooks`.** Git не включает хуки сам; одна команда
  вместо запоминания синтаксиса. Отвергнуты: только документация без setup-скрипта и
  автоподключение через `postinstall` в `ui/package.json` (меняет git-конфиг пользователя
  без его ведома и мешает в CI).

- **D-11: `pre-commit` проверяет staged-блобы (`git show :путь`), а не файлы рабочей копии;
  при отсутствии `node` — `exit 1` с инструкцией (fail-closed).** Чтение рабочей копии
  разошлось бы с тем, что реально уйдёт в коммит: `git add -p` частичного ханка пронёс бы
  маркер мимо гейта. Мягкий пропуск без node отвергнут по той же логике, что R7 — защита не
  должна тихо исчезать. Отвергнуты: staged + мягкий пропуск и проверка рабочей копии.

- **D-12: Шаг гейта в `ci-fast.yml` переезжает наверх — сразу после `Checkout`.** Сейчас он
  стоит после `apt-get install`, `pnpm install` и сборки SPA, то есть утечка обнаруживается
  через 5–10 минут после старта. Гейту не нужны ни `setup-node`, ни `pnpm install`: на
  `ubuntu-latest` Node предустановлен. В `ci-full.yml` шаг **не** дублируется. Отвергнуты:
  оставить на нынешнем месте (минимальный diff, но обратная связь через 10 минут) и добавить
  такой же шаг в матрицу `ci-full` (три лишних прогона ради проверки кроссплатформенности,
  которую дешевле закрыть локальным запуском на macOS).

### Эксплуатация гейта (R7–R9)

- **D-13: Каталог `.planning/phases/37-data-privacy/` НЕ исключается из проверки** — вопреки
  буквальному перечислению в R9. Артефакты фазы по её же дисциплине не цитируют значений,
  значит гейт им не мешает; зато он ловит именно тот срыв, который наиболее вероятен —
  агента-исполнителя или ревьюера, процитировавшего реальное значение из истории git в
  PLAN/SUMMARY/REVIEW. Оставшиеся исключения по путям: сам файл хэшей, `node_modules`,
  `target/`, `Cargo.lock` (известное цифровое ложное срабатывание).

- **D-14: Список исключаемых путей и allowlist бинарников — константы в коде гейта**,
  рядом с комментарием «почему», как нынешний `ALLOWED` в `check-privacy-requisites.sh`.
  Расширение исключения = правка кода гейта, видимая в code review. Отдельный конфиг-файл
  отвергнут: править проще — и ослаблять гейт тоже проще.

- **D-15: Пополнение списка — интерактивный режим `--add` без эха ввода.** Скрипт
  спрашивает значение и класс, нормализует **тем же кодом**, что и сканер (D-06),
  дописывает хэш и пересортировывает файл. Значение не попадает ни в файл, ни в
  `~/.zsh_history`, ни в контекст агента. Отвергнуты: пайп из stdin (значение оседает в
  истории shell) и ручной `shasum` с копипастом (нормализацию пришлось бы повторять руками —
  ошибка в регистре или `ё` даёт хэш, который никогда не совпадёт, и гейт молча слепнет).

- **D-16: Сообщение о срабатывании — `путь:строка — маркер класса X`, без значения и без
  содержимого строки.** Логи CI публичного репозитория видны всем; нынешний скрипт печатает
  само значение (`value: "…"`) — этот приём поглощаемый гейт **не наследует**. Номер строки
  утечкой не является: при `pre-commit` файл ещё не опубликован, а в CI гейт падает как раз
  на уже видимом содержимом. Отвергнуты: только путь и класс (в большом файле маркер
  придётся искать глазами) и печать строки с маскировкой маркера (окружающий контекст сам
  может быть наводкой).

### Ограничения, вытекающие из решений (выведены, не обсуждались)

- **C-01: Самотест гейта не может использовать боевой файл хэшей.** Acceptance R6 требует
  фикстуру «с токеном, чей хэш занесён в список» — против боевого списка такая фикстура
  обязана содержать **реальное значение**, то есть тест сам стал бы утечкой. Следствие: гейт
  принимает путь к файлу хэшей аргументом (например `--hashes <path>`), а тесты гоняются
  против отдельного тестового списка с вымышленным токеном («Иванов И.И.», `example.local`).
  Тот же аргумент закрывает R7: несуществующий/пустой/нечитаемый файл → `exit 1`, и это
  проверяется передачей заведомо отсутствующего пути.

- **C-02: Регрессия старого поведения обязательна.** Поглощение
  `scripts/check-privacy-requisites.sh` не должно потерять ни одного его случая: allowlist
  литералов по ключам `inn|kpp|okpo|ogrn|phone|fax` в `*.rs`/`*.html` со всеми ~20
  утверждёнными плейсхолдерами и pattern'ом, различающим JSON-ish и Rust-struct-init формы.
  Acceptance: подставленный нефиктивный литерал `inn`/`ogrn` в `.rs` по-прежнему валит гейт.

- **C-03: Порядок коммитов внутри фазы.** Чистка (R1–R4) идёт отдельными коммитами ДО того,
  как гейт подключается в `pre-commit`/CI (R10) — иначе первый же коммит фазы блокируется
  собственным гейтом. Разработку самого гейта (R5–R9) можно вести параллельно с чисткой;
  запрещено только его **включение** до её завершения.

- **C-04: Артефакты фазы под собственным гейтом (следствие D-13).** Перед каждым коммитом
  фазы — прогон гейта по staged. Это же страхует от классической ловушки проекта:
  генерируемые агентами REVIEW/VERIFICATION цитируют реальные значения, вычитанные из
  истории git.

- **C-05: Сообщения коммитов фазы тоже не содержат маркеров.** Хук проверяет содержимое
  файлов, а не commit message — дисциплина здесь только процедурная, и планировщик обязан
  записать её в план явным запретом.

- **C-06: `.gitignore` и удаление из индекса — разные операции.** `git rm --cached` для
  `.planning/reference/act-word-source/` плюс запись в `.gitignore`; сам каталог на диске
  пользователя остаётся (образец нужен ему локально).

- **C-07:** Один `cargo test` за раз (память `cargo_no_concurrent_test`); в репозитории есть
  унаследованный висяк `login_remember_persistent_cookie` — прогонять со `--skip`
  (память `workspace_test_hangs_auth_remember_cookie`). Для этой фазы Rust-тесты почти не
  задействованы, но проверка «`ci-fast` зелёный» их затрагивает.

### Claude's Discretion

- Точное имя файла гейта и файла хэшей (например `scripts/check-privacy.mjs` и
  `scripts/privacy-tokens.sha256`), имена флагов CLI, формат шапки-комментария.
- Внутренняя структура токенизатора: регулярка границ слов, порядок обхода n-грамм,
  структура данных для O(1)-сверки (Set хэшей).
- Точный набор расширений в проверке бинарников сверх перечисленных в R8 и форма записи
  allowlist-путей (точные пути против глобов).
- Формулировки текста замены в обезличиваемых файлах, в рамках правил CLAUDE.md
  (`org.name`, «Иванов И.И.», `example.local`, `+7 495 000-00-00`).
- Разбиение фазы на планы и группировка коммитов чистки (по классам A/B/C/D против по
  каталогам) — в рамках C-03.

</decisions>

<canonical_refs>
## Canonical References

**Downstream-агенты ОБЯЗАНЫ прочитать это до планирования и реализации.**

### Требования и границы
- `.planning/phases/37-data-privacy/37-SPEC.md` — **залоченные требования** R1–R10, пофайловая
  разведка классов утечки A–D, границы, критерии приёмки. Читать первым.
- `.planning/REQUIREMENTS.md` §PRIV-01, §PRIV-02 — формулировки требований вехи; PRIV-03
  (история git / private-репозиторий) — в Backlog, вне фазы.
- `.planning/ROADMAP.md` §«Phase 37» — пять критериев успеха, включая порядок «сначала
  чистка, потом гейт».
- `CLAUDE.md` §Constraints — правило приватности целиком: что запрещено, какие плейсхолдеры
  легитимны, требование проверять ДО коммита.

### Что поглощается и заменяется
- `scripts/check-privacy-requisites.sh` — **поглощается и удаляется** (R5). Источник
  регрессионных случаев C-02: allowlist из ~20 плейсхолдеров, pattern для `inn|kpp|okpo|ogrn|phone|fax`
  в `*.rs`/`*.html`, разбор JSON-ish и Rust-struct-init форм. Из него **не** наследуется
  печать значения в сообщении об ошибке (D-16).
- `.github/workflows/ci-fast.yml` — шаг `Privacy gate (organization requisites)` переезжает
  сразу после `Checkout` и вызывает новый гейт (D-12).

### Образцы durable-гейтов (форма, а не содержание)
- `ui/scripts/check-print-isolation.mjs` — эталон структурного zero-dependency гейта:
  развёрнутая шапка «почему он существует», инварианты с привязкой к дефектам, самотест
  через аргумент-путь. Форму шапки и самотеста копируем (D-09, C-01).
- `ui/scripts/check-contrast.mjs`, `ui/scripts/check-pagedjs-csp-hash.mjs` — тот же класс
  скриптов, меньше по объёму.
- `ui/package.json` §scripts.lint — цепочка, в которую новый гейт по решению SPEC
  **не** добавляется.

### Файлы с особым режимом
- `migrations/V033__org_settings_requisites.sql:5` — **не редактировать ни при каких
  условиях** (D-02). Ссылка на удаляемый PHASE-BRIEF остаётся.
- `crates/trackly-app/src/pdf/renderer.rs:251` — единственная точка класса A вне
  `.planning/` (комментарий в коде).
- `crates/trackly-app/icons/*`, `crates/trackly-app/tests/fixtures/logo_test.png` — 18
  легитимных бинарников, обязаны проходить гейт R8.
- `.gitignore` — правится по D-04 (весь `.planning/reference/`).

### Прошлые решения, задающие плейсхолдер-конвенцию
- `.planning/phases/31-*`, `.planning/phases/32-sso-main/32-CONTEXT.md` — источник
  конвенции `example.local` / `us100` / `dc.example.local`, которую обязана использовать
  чистка класса C.
- `.planning/phases/36-act-pagination/36-CONTEXT.md` §C-08 — формулировка правила приватности
  для фикстур и артефактов, принятая в предыдущей фазе.

Внешних спецификаций и ADR у фазы нет — всё остальное зафиксировано решениями выше.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `ui/scripts/check-print-isolation.mjs` — готовая форма durable-гейта: zero-dependency ESM,
  `node:fs`/`node:path`/`node:url`, самотест через аргумент-путь, шапка с объяснением каждого
  инварианта. Новый гейт строится по этому шаблону, но в корневом `scripts/`.
- `scripts/check-privacy-requisites.sh` — источник регрессионных случаев (C-02) и уже
  выстраданный аргумент в пользу allowlist вместо эвристики «выглядит достаточно фейково»
  (см. его шапку). Аргумент переносится в новый гейт дословно по смыслу.
- `.github/workflows/ci-fast.yml` — шаг гейта уже существует, меняется только его место и
  вызываемая команда.

### Established Patterns
- Гейты проекта **структурные и durable**: они не доказывают правильность, а громко падают,
  когда выстраданное исправление молча удалили. Тот же тон нужен новому гейту.
- Сообщение об ошибке гейта в этом проекте объясняет, **что делать дальше** (см. хвост
  `check-privacy-requisites.sh`), а не просто печатает «FAILED». Новый гейт наследует это —
  но без печати самого значения (D-16).
- Node в CI появляется через `setup-node@v4` (v20) для pnpm, однако на `ubuntu-latest` он
  предустановлен — шаг после `Checkout` работает без подготовки (D-12).
- Git-хуков в проекте нет вообще: `.githooks/` отсутствует, `.git/hooks` содержит только
  `*.sample`, `core.hooksPath` не задан. `.githooks/` — новый каталог, а не переезд.
- README целиком пользовательский (установка, portable, SmartScreen/Gatekeeper, контрольные
  суммы); раздела для разработчиков нет, `CONTRIBUTING.md` тоже — поэтому инструкция по
  включению хука уходит в новый `CONTRIBUTING.md`.

### Integration Points
- **Две точки запуска одного кода:** `.githooks/pre-commit` (staged-блобы через
  `git show :путь`) и шаг в `ci-fast.yml` (весь HEAD, включая архивные
  `.planning/milestones/`). Разница только в источнике списка файлов — сканер общий.
- **Аргумент пути к файлу хэшей** (C-01) — точка, через которую подключаются тестовые
  фикстуры и проверяется fail-closed из R7.
- `migrations/V033` — интеграционная мина: правка комментария ломает старт приложения на
  установленных копиях (D-02).

</code_context>

<specifics>
## Specific Ideas

- Пользователь выбрал каждую рекомендованную опцию во всех четырёх областях — расхождений
  с предложенным направлением нет. Единственное содержательное отклонение от SPEC инициировано
  разбором противоречия и подтверждено пользователем: снятие исключения для каталога
  Фазы 37 (D-13).
- Приоритет в спорных местах — **fail-closed**: и при отсутствии `node` (D-11), и при
  отсутствии файла хэшей (R7), и в вопросе слепых зон (D-13) выбран вариант «лучше упасть,
  чем молча пропустить».

</specifics>

<deferred>
## Deferred Ideas

- **Охват стартового списка токенов шире классов A–C** (превентивно занести второй домен,
  ФИО других сотрудников, названия филиалов). Обсуждение предложено и пользователем не
  выбрано; SPEC прямо выносит «аудит утечек вне классов A–D» за границы. Пополнять список
  штатным `--add` (D-15) по мере необходимости — механизм для этого фаза и создаёт.
- **Форма allowlist бинарников** (точные пути против глобов) и процедура добавления новой
  иконки/фикстуры — оставлено на усмотрение планировщика (D-14, Claude's Discretion).
  Вернуться, если добавление легитимного бинарника окажется неудобным на практике.
- **Проверка гейта на Windows** — SPEC явно снимает требование (`Constraints`: macOS + Linux).
  Если появится Windows-разработка, гейт понадобится прогнать там отдельно.
- **PRIV-03 — очистка истории git (`filter-repo` + force-push) либо перевод репозитория
  в private.** Осознанно отложено пользователем 2026-08-08, живёт в Backlog `REQUIREMENTS.md`.
  Прямо влияет на D-08: пока история не переписана, восстановимость хэшей маргинальна,
  потому что значения и так доступны.

### Reviewed Todos (not folded)

- **`Rework act templates — shared header + handover body redesign`**
  (`.planning/todos/pending/2026-08-08-rework-act-templates-shared-header-handover-body-redesign.md`,
  score 0.6) — совпал по ключевым словам («act», «phase», «2026»), но это зонтичный todo по
  печатным формам, уже свёрнутый в Фазу 36 и закрывающийся вместе с ней. К приватности
  отношения не имеет, в Фазу 37 не сворачивается.

</deferred>

---

*Phase: 37-Приватность данных*
*Context gathered: 2026-08-14*
