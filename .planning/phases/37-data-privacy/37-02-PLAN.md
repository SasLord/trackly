---
phase: 37-data-privacy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-render-word-fidelity/15-02-PLAN.md
  - .planning/phases/15-render-word-fidelity/15-CONTEXT.md
  - .planning/phases/15-render-word-fidelity/15-RESEARCH.md
  - .planning/phases/15-render-word-fidelity/15-01-PLAN.md
  - .planning/phases/15-render-word-fidelity/15-VALIDATION.md
  - .planning/phases/15-render-word-fidelity/15-VERIFICATION.md
  - .planning/phases/14-act-data-structure/14-CONTEXT.md
  - .planning/phases/35-act-handover-body/35-05-PLAN.md
  - .planning/phases/35-act-handover-body/35-05-SUMMARY.md
  - .planning/phases/35-act-handover-body/35-CONTEXT.md
  - .planning/phases/36-act-pagination/36-CONTEXT.md
  - .planning/phases/20-print-acts-org/20-PATTERNS.md
  - .planning/quick/260704-wxw-act-pdf-word-fidelity-redesign/260704-wxw-PLAN.md
  - .planning/ROADMAP.md
  - .planning/PHASE-BRIEF-act-pdf-word-fidelity.md
  - .planning/reference/act-word-source/act-sample.docx
  - .planning/reference/act-word-source/image1.png
  - .planning/reference/act-word-source/image2.png
  - .gitignore
autonomous: true
requirements: [PRIV-01]

must_haves:
  truths:
    - "D-01 (as corrected by RESEARCH §Open Questions #3, not D-01's own prose list): the dangling-reference rewrite targets the programmatically verified 14-file union (10 files referencing act-word-source, 6 files referencing the PHASE-BRIEF-act-pdf-word-fidelity filename, overlap resolved) — verified by `git grep -l` for both marker strings after cleanup returning only migrations/V033__org_settings_requisites.sql and phase 37's own descriptive artifacts"
    - "D-02: migrations/V033__org_settings_requisites.sql is byte-identical to its pre-phase state — verified by `git diff --stat -- migrations/V033__org_settings_requisites.sql` being empty after this plan's commits"
    - "D-04 (orchestrator resolution 2026-08-14, broad-ignore + explicit negation): .gitignore ignores .planning/reference/* but negates !.planning/reference/design-system-v2/, and git rm --cached touches only act-word-source/ — verified by `git ls-files .planning/reference/act-word-source/` being empty AND `git ls-files .planning/reference/design-system-v2/ | wc -l` equal to 11"
    - "C-06: .gitignore editing and git rm --cached are performed as two distinct operations in the correct order (rm --cached first, then .gitignore) — verified by `git status --ignored` showing .planning/reference/act-word-source/ under the Ignored section, not Untracked, after both steps"
    - "R4/PRIV-01: .planning/PHASE-BRIEF-act-pdf-word-fidelity.md does not exist in HEAD after this plan; the 3 binary files under .planning/reference/act-word-source/ are untracked; every dangling reference to either is rewritten to 'исходный образец не хранится в репозитории' (or an equivalent phrase) while preserving the surrounding technical meaning of the sentence"
    - "R1/PRIV-01 (class A, overlap files): after cleanup, `git grep -I` for every real class-A marker discovered while editing 15-02-PLAN.md, 15-CONTEXT.md, 15-RESEARCH.md returns 0 matches in those 3 files"
  artifacts:
    - path: ".gitignore"
      provides: "Wildcard ignore of .planning/reference/* with an explicit negation for design-system-v2/"
  key_links:
    - from: ".gitignore"
      to: ".planning/reference/act-word-source/"
      via: "git check-ignore -v"
      pattern: "reference/\\*"
---

<objective>
Remove the class-D binary reference artifacts (`.planning/reference/act-word-source/` — the real
act sample and organization logo) from git's index, delete the file that carries the densest
class-A/B leak (`PHASE-BRIEF-act-pdf-word-fidelity.md`), rewrite every dangling reference to
either across the programmatically verified 14-file union, and additionally scrub the residual
class-A org-name markers in the 3 files of that union that also appear in the SPEC's class-A
recon table (`15-02-PLAN.md`, `15-CONTEXT.md`, `15-RESEARCH.md` — kept out of plan 37-01 to avoid
a same-wave file-ownership conflict).

Purpose: satisfies R4/PRIV-01 and the orchestrator's binding D-04 scope resolution (broad
`.gitignore` + explicit negation for the unrelated `design-system-v2/` mockup directory, which
must remain tracked).

Output: `act-word-source/` untracked with 3 files remaining on the local disk only;
`.gitignore` updated; `PHASE-BRIEF-act-pdf-word-fidelity.md` deleted; 14 files rewritten to say
the sample is not stored in the repository, 3 of them additionally scrubbed of class-A markers;
`migrations/V033__org_settings_requisites.sql` untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md

<privacy_protocol>
Same discipline as plan 37-01: real values discovered while reading the 3 class-A files
(`15-02-PLAN.md`, `15-CONTEXT.md`, `15-RESEARCH.md`) never leave the scratchpad. Approved
placeholders: `org.name`, `{{ act.giver_name }}`, «Иванов И.И.», «Петров П.П.», `example.local`,
`dc.example.local`, `us100`, `+7 495 000-00-00`.
</privacy_protocol>

<orchestrator_decision_d04>
D-04's literal text ("ignore all of `.planning/reference/`") would untrack 14 files, not 3 — the
directory also holds `design-system-v2/` (11 tracked HTML design-system mockups from milestone
v1.2, unrelated to the leak). The binding resolution: broad ignore + explicit negation.
`.gitignore` gets, in this order:

  .planning/reference/*
  !.planning/reference/design-system-v2/

`git rm --cached` touches ONLY `act-word-source/`. The bare-directory form (`.planning/reference/`
with no wildcard) silently defeats the negation — git will not descend into a directory-ignored
path to evaluate an exception under it. Use `git check-ignore -v` to confirm both halves before
committing.

Two of the 11 `design-system-v2/` files contain guillemet-quoted Cyrillic text that has not been
checked against the token list built in plan 37-03. Do not resolve that here — plan 37-04 (Wave 2)
runs the finished gate over `design-system-v2/` once the token list exists and cleans or untracks
those 2 files if the gate fires. Do not weaken the gate to make them pass.
</orchestrator_decision_d04>

<hard_prohibitions>
1. Do NOT edit `migrations/V033__org_settings_requisites.sql` under any circumstances (D-02). It
   references the PHASE-BRIEF filename in a line-5 comment; editing that comment changes the
   refinery SipHasher13 checksum and makes the app refuse to start (`Kind::DivergentVersion`,
   `abort_divergent` defaults true) on every installed copy of Trackly. It is intentionally absent
   from this plan's `files_modified`.
2. Do NOT `git rm --cached` anything under `.planning/reference/design-system-v2/` — only
   `act-word-source/`.
3. Do NOT use a bare `.planning/reference/` line in `.gitignore` — it defeats the negation (see
   orchestrator_decision_d04 above).
4. Do NOT quote a real class-A value in the SUMMARY, a commit message, or this plan's own record
   of work (C-05).
</hard_prohibitions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Untrack the binary reference artifacts, delete PHASE-BRIEF, fix .gitignore</name>
  <files>
    .planning/reference/act-word-source/act-sample.docx,
    .planning/reference/act-word-source/image1.png,
    .planning/reference/act-word-source/image2.png,
    .planning/PHASE-BRIEF-act-pdf-word-fidelity.md,
    .gitignore
  </files>
  <read_first>
    .gitignore (current content, to match the existing comment-block style before adding the new
    entry). Do not open `.planning/PHASE-BRIEF-act-pdf-word-fidelity.md` or the files under
    `act-word-source/` — they are the leak sites themselves and are being deleted/untracked
    wholesale, not edited; there is nothing in them this task needs to read.
  </read_first>
  <action>
    Run `git rm --cached -r .planning/reference/act-word-source/` (removes the 3 files from the
    index; `--cached` leaves them on local disk for the user, per C-06). Delete
    `.planning/PHASE-BRIEF-act-pdf-word-fidelity.md` with a normal `git rm` (this one should not
    survive on disk — it is the densest single leak site, and R4/Boundaries explicitly scope its
    removal). Append to `.gitignore`, matching the file's existing short-comment-then-bare-paths
    style, a new block: a comment explaining the rule ties to D-04 and that `design-system-v2/`
    predates the rule and stays tracked intentionally, then the two lines `.planning/reference/*`
    and `!.planning/reference/design-system-v2/` in that exact order. Verify with
    `git check-ignore -v .planning/reference/act-word-source/act-sample.docx` (must print a match
    against the new `.gitignore` line) and `git check-ignore` against any file directly inside
    `design-system-v2/` (must exit 1 — not ignored). Commit as "chore(37): untrack
    act-word-source binary reference artifacts, delete PHASE-BRIEF (PRIV-01)".
  </action>
  <acceptance_criteria>
    `git ls-files .planning/reference/act-word-source/` produces no output.
    `git ls-files .planning/reference/design-system-v2/ | wc -l` equals 11.
    `test ! -f .planning/PHASE-BRIEF-act-pdf-word-fidelity.md` succeeds.
    `git check-ignore -v .planning/reference/act-word-source/act-sample.docx` exits 0.
    `git status --ignored` lists `.planning/reference/act-word-source/` under "Ignored files", not
    "Untracked files".
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; test -z "$(git ls-files .planning/reference/act-word-source/)" &amp;&amp; test "$(git ls-files .planning/reference/design-system-v2/ | wc -l | tr -d ' ')" = "11" &amp;&amp; test ! -f .planning/PHASE-BRIEF-act-pdf-word-fidelity.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>act-word-source/ untracked (files remain on disk), design-system-v2/ still fully tracked (11 files), PHASE-BRIEF deleted, .gitignore has the wildcard+negation pair, committed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite dangling references across the 14-file union and scrub the 3 overlap files</name>
  <files>
    .planning/phases/15-render-word-fidelity/15-02-PLAN.md,
    .planning/phases/15-render-word-fidelity/15-CONTEXT.md,
    .planning/phases/15-render-word-fidelity/15-RESEARCH.md,
    .planning/phases/15-render-word-fidelity/15-01-PLAN.md,
    .planning/phases/15-render-word-fidelity/15-VALIDATION.md,
    .planning/phases/15-render-word-fidelity/15-VERIFICATION.md,
    .planning/phases/14-act-data-structure/14-CONTEXT.md,
    .planning/phases/35-act-handover-body/35-05-PLAN.md,
    .planning/phases/35-act-handover-body/35-05-SUMMARY.md,
    .planning/phases/35-act-handover-body/35-CONTEXT.md,
    .planning/phases/36-act-pagination/36-CONTEXT.md,
    .planning/phases/20-print-acts-org/20-PATTERNS.md,
    .planning/quick/260704-wxw-act-pdf-word-fidelity-redesign/260704-wxw-PLAN.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/phases/37-data-privacy/37-RESEARCH.md §"Сводный список файлов с висячими ссылками"
    (the exact 14-file union table with which marker each file references — act-word-source vs.
    the PHASE-BRIEF filename vs. both). Use THIS table, not D-01's prose list in 37-CONTEXT.md —
    RESEARCH's is the programmatically verified one and 37-CONTEXT.md itself says so. For the 3
    files that also carry class-A org-name markers (15-02-PLAN.md, 15-CONTEXT.md, 15-RESEARCH.md),
    read 37-SPEC.md's class-A recon table entries for those files before editing.
  </read_first>
  <action>
    For each of the 14 files: locate every mention of `act-word-source` and/or
    `PHASE-BRIEF-act-pdf-word-fidelity` (per the RESEARCH table's per-file marker column) and
    replace it with prose stating the original sample is not stored in the repository (Russian:
    "исходный образец не хранится в репозитории" or a natural inline variant), preserving the
    surrounding sentence's technical point (e.g. "структура акта была сверена с оригиналом Word
    -документа (исходный образец не хранится в репозитории)" rather than deleting the clause
    outright). `.planning/ROADMAP.md` needs this treatment only where it references the old
    PHASE-BRIEF filename in an earlier phase's history section — do not touch Phase 37's own
    entry in the same file (that is edited separately at the end of the planning workflow, not by
    this task). For the 3 overlap files, additionally apply the same class-A scrub discipline as
    plan 37-01's Task 1/2 (build a scratch mapping for whatever real organization-name markers
    appear in exactly these 3 files, replace mechanically, verify by diff and by `git grep -I`,
    delete the scratch mapping afterward — do not reuse or persist any mapping in the repo). Run
    `git grep -l 'act-word-source\|PHASE-BRIEF-act-pdf-word-fidelity'` after both edits: the only
    remaining hits must be `migrations/V033__org_settings_requisites.sql` (untouched, D-02) and
    phase 37's own artifacts (`37-CONTEXT.md`, `37-SPEC.md`, `37-DISCUSSION-LOG.md`,
    `37-RESEARCH.md`), which mention both names descriptively on purpose and are out of this
    task's file list. Commit as "chore(37): rewrite dangling references to removed reference
    artifacts, scrub residual class-A markers (PRIV-01)".
  </action>
  <acceptance_criteria>
    `git grep -l 'act-word-source\|PHASE-BRIEF-act-pdf-word-fidelity'` returns only
    `migrations/V033__org_settings_requisites.sql` and phase 37's own artifact files.
    `git diff --stat -- migrations/V033__org_settings_requisites.sql` is empty (untouched).
    `git grep -I` for the class-A markers recorded while editing the 3 overlap files returns 0
    matches. Each rewritten sentence, read by the executor, remains grammatical and keeps its
    original technical point.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; git grep -l 'act-word-source\|PHASE-BRIEF-act-pdf-word-fidelity' | grep -v -E '^(migrations/V033__org_settings_requisites\.sql|\.planning/phases/37-data-privacy/)' | wc -l | grep -qx 0 &amp;&amp; echo PASS</automated>
  </verify>
  <done>All 14 dangling references rewritten to descriptive prose; migrations/V033 untouched; 3 overlap files' class-A markers scrubbed; committed with a marker-free message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Executor agent context → committed files | Real class-A values read from the 3 overlap files must never cross into a non-scratch location |
| Git index → git working tree | `git rm --cached` must not accidentally drop `--cached` and delete the user's local copy of the reference sample |
| `.gitignore` pattern → git's directory-descent behavior | A bare directory ignore silently defeats the `design-system-v2/` negation with no error — a correctness trap, not a security one, but it directly gates D-04 compliance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-09 | Tampering | `migrations/V033__org_settings_requisites.sql` | mitigate | File absent from `files_modified`; hard prohibition #1 states the refinery-checksum boot failure explicitly; Task 2 acceptance criterion asserts an empty `git diff --stat` on it |
| T-37-08 | Information Disclosure | Executor's SUMMARY / commit messages for the 3 overlap-file class-A scrub | mitigate | Same D-03/C-05 scripted-substitution discipline as plan 37-01, restated in this plan's privacy_protocol and hard prohibition #4 |
| T-37-12 | Repudiation | `git rm --cached` used without `--cached`, silently deleting the user's local reference sample | accept (low likelihood, high specificity of the exact command in the action text) — mitigated procedurally by giving the executor the literal command string, not a paraphrase |
| T-37-13 | Tampering | Bare `.planning/reference/` `.gitignore` line silently defeating the `design-system-v2/` negation | mitigate | orchestrator_decision_d04 block gives the exact two-line form; acceptance criterion checks `git ls-files design-system-v2/` count = 11 as a functional (not just syntactic) proof the negation works |
</threat_model>

<verification>
`git ls-files .planning/reference/act-word-source/` is empty; `git ls-files
.planning/reference/design-system-v2/` still lists 11 files; `.planning/PHASE-BRIEF-act-pdf-word-fidelity.md`
is gone; `git grep -l 'act-word-source\|PHASE-BRIEF-act-pdf-word-fidelity'` outside
`migrations/V033` and phase 37's own artifacts returns nothing; `migrations/V033` diff is empty.
</verification>

<success_criteria>
Binary reference artifacts untracked without deleting the user's local copy; `.gitignore`
correctly ignores future samples in `.planning/reference/` while keeping `design-system-v2/`
tracked; every dangling reference rewritten with technical meaning preserved; the migration file
is provably untouched.
</success_criteria>

<output>
Create `.planning/phases/37-data-privacy/37-02-SUMMARY.md` when done. Report file counts and
replacement counts per class — never a real value.
</output>
