---
phase: 37-data-privacy
plan: 04
type: execute
wave: 2
depends_on: ["37-01", "37-02", "37-03"]
files_modified:
  - scripts/privacy-tokens.sha256
  - .githooks/pre-commit
  - scripts/setup-hooks.sh
  - CONTRIBUTING.md
  - .github/workflows/ci-fast.yml
  - scripts/check-privacy-requisites.sh
  - .planning/phases/37-data-privacy/37-VALIDATION.md
autonomous: true
requirements: [PRIV-02]

must_haves:
  truths:
    - "C-03: this plan's tasks run only after 37-01 and 37-02 (cleanup) have landed — encoded structurally via depends_on: [37-01, 37-02, 37-03] — because activating the gate before cleanup would fail on the phase's own repository on the first run"
    - "D-10: the hook is enabled via .githooks/pre-commit plus a one-line scripts/setup-hooks.sh that runs `git config core.hooksPath .githooks` — verified by running setup-hooks.sh in a scratch clone and confirming `git config core.hooksPath` reports `.githooks`"
    - "D-11: .githooks/pre-commit checks staged blobs (via check-privacy.mjs --staged, which reads `git show :path`, not the working tree) and exits 1 with an instructional message if `node` is not on PATH — verified by temporarily shadowing PATH in a scratch check and observing exit 1, not a silent pass"
    - "D-12: the ci-fast.yml gate step is relocated to immediately after Checkout (before Rust toolchain install, Tauri Linux deps, pnpm install, and the SPA build) and calls `node scripts/check-privacy.mjs`; ci-full.yml receives no duplicate step — verified by the step's line position in ci-fast.yml and `grep -c check-privacy .github/workflows/ci-full.yml` equal to 0"
    - "R5: scripts/check-privacy-requisites.sh is deleted and ci-fast.yml's step calls the new gate — verified by `test ! -f scripts/check-privacy-requisites.sh` and `grep check-privacy.mjs .github/workflows/ci-fast.yml`"
    - "R6/D-08: scripts/privacy-tokens.sha256 is populated with real class A/B/C tokens via the gate's --add flow (D-15, no echo, no shell-history exposure) and its header documents the same accepted dictionary-attack residual risk as the fixture header — verified by every non-comment line matching `^[0-9a-f]{64} [ABC]$` and the file containing zero plaintext"
    - "RESEARCH §Open Questions #1: Task 1 re-derives the full real-marker set for --add from the marker-shape checklists recorded in 37-01-SUMMARY.md and 37-02-SUMMARY.md (class + structural form + count per file), not from unaided recall of a possibly different session — verified by Task 1's acceptance criteria requiring every checklist line from both SUMMARYs to have a corresponding --add entry before the file is considered complete"
    - "R9: the exclusion list remains the 4 constants from plan 37-03 (hash file, node_modules, target/, Cargo.lock) — this plan does not add .planning/phases/37-data-privacy/ to it, confirming D-13 survives activation — verified by re-grepping the EXCLUDED_PATHS constant unchanged from plan 37-03's commit"
    - "R10: node scripts/check-privacy.mjs (full HEAD, production hash file) exits 0 on the post-cleanup repository, including .planning/reference/design-system-v2/'s 11 tracked files, and a scratch git commit with an injected fictional marker is blocked when core.hooksPath is set — verified by a temporary-clone test transcript in the SUMMARY (no real value quoted)"
    - "Orchestrator resolution (2026-08-14, design-system-v2 guillemet check): once scripts/privacy-tokens.sha256 exists, the gate is run explicitly over .planning/reference/design-system-v2/ — if it fires on the 2 flagged files, they are cleaned or untracked, not exempted by weakening the gate"
    - "WARNING 3 fix (token-list completeness proof, the only check that validates coverage rather than just the executor's own recall): Task 3 derives its proof set from the marker-shape checklist lines actually recorded in 37-01-SUMMARY.md and 37-02-SUMMARY.md (the files whose real markers were substituted — 14 from 37-01, 3 overlap files from 37-02 Task 3) plus .planning/PHASE-BRIEF-act-pdf-word-fidelity.md (deleted wholesale by 37-02 Task 1 with no checklist line, but per 37-SPEC.md carrying real class A/B content) — 18 files total — and explicitly EXCLUDES 37-02 Task 2's 11 dangling-reference-only files (no class A/B/C value per that task's own action), .gitignore, and the 3 untracked binary artifacts (covered by R8's binary-extension control, not token hashing); Task 3 checks out each proof-set file's pre-cleanup blob from its parent commit (still reachable in git history, no history rewrite needed) into the session scratchpad only, re-scans it with the finished gate + populated production hash list, confirms it fires, and deletes the extracted blob immediately — any proof-set file the gate fails to fire on is a hole in the token list and is closed via --add before this plan is considered complete; only a file list and pass/fail record ever appears in the SUMMARY, never a matched value, and git status --porcelain confirms no extracted blob survives in the working tree before Task 3's commit"
    - "WARNING 2 fix (closing 37-VALIDATION.md): once the full-HEAD gate run and cargo test are both green, Task 3 flips 37-VALIDATION.md's frontmatter to nyquist_compliant: true and wave_0_complete: true, checks off every item in the Validation Sign-Off list, fills the Per-Task Verification Map's Task ID column with the real task IDs (37-01 Task 2, 37-02 Task 1/2/3, 37-03 Task 1/2, 37-04 Task 1/2/3 per requirement), and sets every row's Status column to ✅"
    - "C-07: the ci-fast-equivalent local verification in this plan runs cargo test one at a time, with --skip login_remember_persistent_cookie and env TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1"
    - "C-05: this plan's own commit messages carry no markers — same discipline as 37-01/37-02"
  artifacts:
    - path: ".githooks/pre-commit"
      provides: "Staged-scope entry point, fail-closed on missing node"
    - path: "scripts/setup-hooks.sh"
      provides: "One-line core.hooksPath enabler"
    - path: "CONTRIBUTING.md"
      provides: "Developer-facing doc explaining hook activation (new file — no CONTRIBUTING.md existed before)"
    - path: "scripts/privacy-tokens.sha256"
      provides: "Production token hash list, D-07 format, populated via --add"
    - path: ".planning/phases/37-data-privacy/37-VALIDATION.md"
      provides: "Closed-out validation contract: nyquist_compliant true, Sign-Off checked, Per-Task Verification Map filled with real task IDs and ✅ status"
  key_links:
    - from: ".githooks/pre-commit"
      to: "scripts/check-privacy.mjs"
      via: "exec node .../scripts/check-privacy.mjs --staged --hashes .../scripts/privacy-tokens.sha256"
      pattern: "check-privacy\\.mjs --staged"
    - from: ".github/workflows/ci-fast.yml"
      to: "scripts/check-privacy.mjs"
      via: "run: node scripts/check-privacy.mjs, placed immediately after the Checkout step"
      pattern: "node scripts/check-privacy\\.mjs"
---

<objective>
Activate the gate built in plan 37-03 against the repository cleaned by plans 37-01/37-02:
populate the production token hash list with real class A/B/C markers via `--add` (re-derived
from 37-01/37-02's marker-shape checklists, not unaided recall), wire `.githooks/pre-commit` +
`scripts/setup-hooks.sh` + `CONTRIBUTING.md`, relocate and swap the `ci-fast.yml` step, delete the
absorbed `scripts/check-privacy-requisites.sh`, prove the token list's completeness against the
pre-cleanup blobs still in git history, prove a fully green full-HEAD run — including a live check
of `.planning/reference/design-system-v2/`'s two guillemet-flagged files — and close out
`37-VALIDATION.md`.

Purpose: completes PRIV-02 (R5, R10) and the phase's blocking C-03 ordering constraint — this is
the only plan whose tasks may not start before cleanup lands. It is also the only plan that can
close the phase's validation contract, since closing it requires the full green run this plan
produces.

Output: pre-commit hook wired, CI step relocated and swapped, old script gone, production hash
list populated and proven complete against the pre-cleanup history, full-HEAD gate run green,
`37-VALIDATION.md` closed out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.github/workflows/ci-fast.yml
@README.md

<why_this_plan_is_wave_2>
C-03 is the phase's spine: cleanup (37-01, 37-02) must be committed BEFORE the gate is switched on
in pre-commit/CI, otherwise the phase's own first commit under an active gate would need a
production hash list built from markers that, by definition, cleanup has already removed from
HEAD — and, worse, activating first would mean the gate's initial full-HEAD run fails on the
phase's own uncleaned repository. This plan also needs 37-03's finished `check-privacy.mjs --add`
flow to exist before it can populate the production hash list. Hence `depends_on: ["37-01",
"37-02", "37-03"]`.
</why_this_plan_is_wave_2>

<privacy_protocol>
Populating `scripts/privacy-tokens.sha256` requires re-deriving the real class A/B/C values. The
primary source is the marker-shape checklists recorded in `37-01-SUMMARY.md` and
`37-02-SUMMARY.md` (class + structural form + count per file, never the value itself) — read
those first (Task 1). Where a checklist entry's exact value is not already in the executor's own
memory from the same session, re-derive it via `git log -p` on the pre-cleanup commits from
37-01/37-02, still reachable in history. Every value is entered via `--add`'s no-echo stdin
prompt — never typed into an Edit/Write call, a commit message, or the SUMMARY. Only counts,
classes, and shape labels are reported.
</privacy_protocol>

<hard_prohibitions>
1. Do NOT run this plan's tasks before 37-01 and 37-02 have committed (C-03) — depends_on encodes
   this; do not attempt to skip ahead even if 37-03 alone is already done.
2. Do NOT edit `migrations/V033__org_settings_requisites.sql`.
3. Do NOT add a step to `.github/workflows/ci-full.yml` (D-12 — no duplicate).
4. Do NOT weaken `scripts/check-privacy.mjs`'s exclusions or add a per-token override to make
   `design-system-v2/` pass if the gate fires on it — clean or untrack the 2 flagged files instead.
5. Do NOT print a real value anywhere in this plan's commit messages or SUMMARY (C-05). The
   completeness-proof transcript (Task 3) records only file paths and pass/fail, never a matched
   value or a checked-out blob's content.
</hard_prohibitions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the production token hash list from the marker-shape checklists and verify the exclusion set is unchanged</name>
  <files>scripts/privacy-tokens.sha256</files>
  <read_first>
    .planning/phases/37-data-privacy/37-01-SUMMARY.md and 37-02-SUMMARY.md — specifically their
    marker-shape checklist sections (class + structural form + count per file). This is the
    primary re-derivation source (RESEARCH §Open Questions #1) — use it as a checklist to work
    through file-by-file, not unaided recall of what plans 37-01/37-02 discovered. scripts/
    check-privacy.mjs (the --add flow implemented in plan 37-03, and the EXCLUDED_PATHS constant,
    to confirm D-13 has not drifted). scripts/fixtures/privacy/tokens.fixture.sha256 (the D-07
    format to mirror in the production file's header, adapted to explain HOW to add a new real
    token via --add rather than describing the fixture).
  </read_first>
  <action>
    Build a checklist by combining every marker-shape line from `37-01-SUMMARY.md` and
    `37-02-SUMMARY.md` (one line per file/class/form/count). For each checklist line, re-derive
    the actual real value — from the executor's own memory if this session also ran 37-01/37-02,
    otherwise via `git log -p -- &lt;file&gt;` on the pre-cleanup commit for that file (still
    reachable in history, no history rewrite needed) — and run `node scripts/check-privacy.mjs
    --add --hashes scripts/privacy-tokens.sha256`, entering the value at the no-echo prompt and
    its class. Do this for every checklist line before considering the file populated; a checklist
    line with no corresponding `--add` entry is an incomplete pass. Write the file's header (D-07)
    — recovery instructions (how to add a token, R7's fail-closed behavior explained so a future
    maintainer understands why an empty file blocks everything) and the D-08 residual-risk note
    (SHA-256 of a short token is dictionary-attackable; this list is a stop-word list, not
    cryptographic protection; the same values already exist unredacted in git history since
    PRIV-03 is deferred). Confirm the file's data lines are sorted (D-07) — the `--add`
    implementation from 37-03 should already do this; if not, sort manually. Re-grep
    `scripts/check-privacy.mjs`'s `EXCLUDED_PATHS` constant and confirm it still excludes only the
    4 items from plan 37-03 (hash file, node_modules, target/, Cargo.lock) — do not add
    `.planning/phases/37-data-privacy/` (D-13). Do not commit yet — Task 3 commits everything
    together with the final green run.
  </action>
  <acceptance_criteria>
    Every non-comment, non-empty line in `scripts/privacy-tokens.sha256` matches
    `^[0-9a-f]{64} [ABC]$`. The file's header documents D-08's residual risk. `git diff --stat --
    scripts/check-privacy.mjs` (against plan 37-03's commit) shows no change to `EXCLUDED_PATHS`.
    Every marker-shape checklist line found in `37-01-SUMMARY.md` and `37-02-SUMMARY.md` has a
    corresponding `--add` entry — cross-checked by the executor line-by-line before moving to
    Task 2, and noted as done in this plan's own working notes (not necessarily the SUMMARY). As a
    cheap automated cross-check (defense in depth alongside the line-by-line pass): the count of
    checklist lines matching the `: класс [ABC], ` shape across both SUMMARYs is no greater than
    the number of non-comment, non-empty lines in `scripts/privacy-tokens.sha256` — a hash file
    that grew by fewer lines than there are checklist entries is itself evidence of a skipped
    `--add`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && grep -vE '^\s*(#.*)?$' scripts/privacy-tokens.sha256 | grep -qvE '^[0-9a-f]{64} [ABC]$' && echo FAIL || echo PASS; CHECKLIST=$(grep -hcE ': класс [ABC], ' .planning/phases/37-data-privacy/37-01-SUMMARY.md .planning/phases/37-data-privacy/37-02-SUMMARY.md | awk '{s+=$1} END{print s+0}'); HASHES=$(grep -vcE '^\s*(#.*)?$' scripts/privacy-tokens.sha256); test "$HASHES" -ge "$CHECKLIST" && echo "coverage_count_ok=true" || echo "coverage_count_ok=false"</automated>
  </verify>
  <done>Production hash list populated with real markers via --add (no echo), re-derived from the 37-01/37-02 marker-shape checklists (not unaided recall), sorted, D-07/D-08-compliant header; exclusion set unchanged from plan 37-03; hash-file line count cross-checked against checklist-line count.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the pre-commit hook and document activation</name>
  <files>.githooks/pre-commit, scripts/setup-hooks.sh, CONTRIBUTING.md</files>
  <read_first>
    scripts/check-privacy-requisites.sh (shebang/set -euo pipefail/header convention to mirror in
    setup-hooks.sh — the only existing directly-exec'd root-scripts/ file). README.md (heading list
    and prose register only — do not copy content, CONTRIBUTING.md is a deliberate audience split
    per 37-PATTERNS.md, there is no existing developer-setup section to absorb).
  </read_first>
  <action>
    Create `.githooks/pre-commit` (executable, `#!/usr/bin/env bash`, `set -euo pipefail`): check
    `command -v node`, and on failure print "pre-commit: node not found — cannot run the privacy
    gate (fail-closed, D-11)." plus a note that `git commit --no-verify` bypasses this (not
    recommended) and `exit 1`; otherwise `exec node "$(git rev-parse --show-toplevel)/scripts/check-privacy.mjs"
    --staged --hashes "$(git rev-parse --show-toplevel)/scripts/privacy-tokens.sha256"`. Create
    `scripts/setup-hooks.sh` (executable, same shebang/header convention as
    `check-privacy-requisites.sh`, a short "why" comment tied to D-10's rationale that git never
    enables hooks on its own) running `git config core.hooksPath .githooks` and printing a
    confirmation line. Create `CONTRIBUTING.md` at repo root — this is a new file, there is no
    prior developer-facing doc to extend — with one section explaining that
    `core.hooksPath` is not automatic and the one-line fix is `./scripts/setup-hooks.sh`, in
    README's imperative-Russian, numbered/fenced-code prose register.
  </action>
  <acceptance_criteria>
    `.githooks/pre-commit` and `scripts/setup-hooks.sh` are both executable (`test -x`).
    Running `scripts/setup-hooks.sh` in a scratch clone sets `core.hooksPath` to `.githooks`
    (verified via `git config core.hooksPath`). `CONTRIBUTING.md` exists and mentions
    `setup-hooks.sh`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && test -x .githooks/pre-commit && test -x scripts/setup-hooks.sh && grep -q setup-hooks.sh CONTRIBUTING.md && echo PASS</automated>
  </verify>
  <done>Hook wired, one-command activation documented, CONTRIBUTING.md created.</done>
</task>

<task type="auto">
  <name>Task 3: Swap ci-fast.yml, delete the old script, prove token-list completeness, run the full green verification, close out 37-VALIDATION.md</name>
  <files>.github/workflows/ci-fast.yml, scripts/check-privacy-requisites.sh, .planning/phases/37-data-privacy/37-VALIDATION.md</files>
  <read_first>
    .github/workflows/ci-fast.yml (current step position and comment block, to relocate and adapt
    rather than rewrite from scratch). .planning/phases/37-data-privacy/37-VALIDATION.md in full
    (§Sampling Rate, §Per-Task Verification Map, §Manual-Only Verifications, §Validation Sign-Off —
    this task both performs the checks it lists and closes out the document itself).
  </read_first>
  <action>
    In `.github/workflows/ci-fast.yml`, move the "Privacy gate" step block to immediately after
    the `Checkout` step (before "Install Rust toolchain", "Install Tauri 2 Linux system
    dependencies", "Install pnpm", "Setup Node", "pnpm install", "Seed ui/dist placeholder", and
    "Build Svelte SPA") — no `setup-node`/`pnpm install` prerequisite is needed since Node is
    preinstalled on `ubuntu-latest`. Keep the existing explanatory comment block (it already states
    D-12's rationale) and update `run:` to `node scripts/check-privacy.mjs` (production `--hashes`
    default — if 37-03's CLI requires an explicit `--hashes` flag with no built-in default, add
    `--hashes scripts/privacy-tokens.sha256` here). Confirm `.github/workflows/ci-full.yml` still
    has zero privacy-step matches (D-12 — no duplicate, should already be true, this is a
    verification not an edit). Delete `scripts/check-privacy-requisites.sh` (R5 — fully absorbed
    into mode 1 of the new gate). Run `node scripts/check-privacy.mjs` (full HEAD, production hash
    list) and confirm exit 0; explicitly re-run it scoped to
    `.planning/reference/design-system-v2/` — if either of the 2 guillemet-flagged files trips a
    violation, clean the flagged text or `git rm --cached` that specific file (do not touch the
    other 9), per the orchestrator's 2026-08-14 resolution; do not add a path exclusion or token
    override to make it pass.

    Token-list completeness proof (WARNING 3 — the only check that validates the token list's
    COVERAGE rather than just confirming the markers the executor happened to find are gone).
    Derive the proof set first — do not hard-code a file list: it is the union of every file that
    has a marker-shape checklist line (class + structural form + count) in `37-01-SUMMARY.md` or
    `37-02-SUMMARY.md` (exactly the files whose real markers were actually substituted — 14 files
    from 37-01 Task 2, plus the 3 overlap files from 37-02 Task 3) together with
    `.planning/PHASE-BRIEF-act-pdf-word-fidelity.md` (deleted wholesale by 37-02 Task 1, so it
    produced no checklist line, but 37-SPEC.md's class-A/B recon table records it as carrying real
    values — its pre-cleanup blob is still reachable via the commit immediately before 37-02
    Task 1's deletion commit). This is 18 files total. The proof set explicitly EXCLUDES: 37-02
    Task 2's 11 dangling-reference-only files (that task's own action states they carry no class
    A/B/C value — only a filename/path reference), `.gitignore` (never carried a marker), and the
    3 untracked binary artifacts under `.planning/reference/act-word-source/` (covered by R8's
    binary-extension control, not token hashing) — re-scanning any of these against the finished
    gate would legitimately exit 0 and proves nothing; do not treat a clean result on an excluded
    file as a hole.

    For every file in the derived proof set, identify its parent commit (the commit immediately
    before the cleanup commit that edited or deleted it — `git log --oneline -- &lt;file&gt;` from
    HEAD finds it; the pre-cleanup blob is still reachable, no history rewrite needed). Extract
    that pre-cleanup blob into the session scratchpad directory ONLY — the same scratch root plan
    37-01 Task 1 used (a path starting with `/private/tmp/claude-501/...` or the equivalent scratch
    root for this session; never a relative path, never anywhere under
    `/Users/madsas/Projects/trackly`): `git show &lt;parent-sha&gt;:&lt;file&gt; >
    &lt;scratchpad-dir&gt;/&lt;file-basename&gt;`. Scan it with the finished gate: `node
    scripts/check-privacy.mjs --hashes scripts/privacy-tokens.sha256
    &lt;scratchpad-dir&gt;/&lt;file-basename&gt;` (or the gate's single-file scan mode if
    Task 1/37-03 exposed one) — expect exit 1 (the gate must fire on the pre-cleanup content).
    Record in this plan's SUMMARY only a table of file path + pass/fail (fired / did not fire) —
    never the matched value, never the blob's content. Any file the gate fails to fire on is a
    hole in the token list: return to Task 1, `--add` the missing marker (re-examine that file's
    marker-shape checklist line more carefully, or re-derive from `git log -p` on the pre-cleanup
    commit), and re-run the proof for that file before continuing. Delete each extracted blob from
    the scratchpad the moment its own scan concludes — pass, fail, or hole-found — so no
    pre-cleanup content survives past its own check; a hole found is exactly when it is most
    tempting to leave the blob sitting around for the retry, and that is precisely when it must
    not. Immediately before this task's commit, run `git status --porcelain` from the repo root
    and confirm it shows nothing beyond this plan's own intended changes — zero leftover extracted
    content anywhere in the working tree.

    Run `cargo test --workspace --no-fail-fast -- --test-threads=1 --skip
    login_remember_persistent_cookie` with `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1` (C-07, one
    `cargo test` invocation at a time — do not run this in parallel with any other cargo
    invocation). In a scratch clone with `core.hooksPath` set via `setup-hooks.sh`, stage a file
    containing a fictional marker already present in `scripts/privacy-tokens.sha256`'s test
    coverage (reuse a 37-03 fixture marker, not a real value) and confirm `git commit` is blocked;
    record the transcript (command + exit code only, no real value) in the SUMMARY.

    Close out `.planning/phases/37-data-privacy/37-VALIDATION.md` (WARNING 2) once every check
    above is green: set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`; check
    off every item in the `## Validation Sign-Off` list; fill the `## Per-Task Verification Map`
    table's `Task ID`-equivalent column (add one if the existing table lacks it — the current
    columns are `Req | Behavior | Threat Ref | Test Type | Automated Command | File Exists |
    Status`, so add a `Plan/Task` column between `Req` and `Behavior`) with the real task that
    satisfies each row: R1/R2/R3 → `37-01 Task 2` (+ `37-02 Task 3` for the 3 overlap files); R4 →
    `37-02 Task 1/2/3`; R4b → `37-02 Task 1`; R5 → `37-04 Task 3`; R6 → `37-03 Task 2`; R7 → `37-03
    Task 1`; R8 → `37-03 Task 1/2`; R9 → `37-03 Task 1` / `37-04 Task 1`; R10 → `37-04 Task 2/3`;
    R16 → `37-03 Task 1/2`. Set every row's `Status` column to `✅`.

    Commit all of this plan's changes together as "chore(37): activate privacy gate — pre-commit
    hook, CI relocation, retire legacy script, close validation (PRIV-02)".
  </action>
  <acceptance_criteria>
    `node scripts/check-privacy.mjs` exits 0 on full HEAD. `grep -c check-privacy
    .github/workflows/ci-full.yml` is 0. `test ! -f scripts/check-privacy-requisites.sh` succeeds.
    `grep -q "node scripts/check-privacy.mjs" .github/workflows/ci-fast.yml` succeeds and the step
    appears before the "Install Rust toolchain" step in file order. `git ls-files
    .planning/reference/design-system-v2/ | wc -l` is still 11 (files cleaned in place, not
    removed, unless the executor judged an untrack necessary and documented why). `cargo test`
    run completed with `--test-threads=1` and the required skip/env flags. The completeness-proof
    table in the SUMMARY lists exactly the derived proof set (the files with a marker-shape
    checklist line in `37-01-SUMMARY.md`/`37-02-SUMMARY.md`, plus
    `.planning/PHASE-BRIEF-act-pdf-word-fidelity.md` — 18 files total), every row is `pass`
    (fired), no `fail` rows remain unresolved, and none of 37-02 Task 2's 11 dangling-reference-only
    files, `.gitignore`, or the 3 binary artifacts appear as proof-set rows. `git status
    --porcelain`, run immediately before this task's commit, shows no leftover extracted blob or
    scratch artifact anywhere in the working tree. `.planning/phases/37-data-privacy/
    37-VALIDATION.md` has `nyquist_compliant: true`, `wave_0_complete: true`, a fully checked
    Sign-Off list, and every Per-Task Verification Map row's Status is `✅` with a non-empty
    plan/task reference.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && node scripts/check-privacy.mjs --hashes scripts/privacy-tokens.sha256; echo "gate_exit=$?"; test ! -f scripts/check-privacy-requisites.sh && echo "old_script_gone=true"; grep -q 'nyquist_compliant: true' .planning/phases/37-data-privacy/37-VALIDATION.md && echo "validation_closed=true"; test -z "$(git status --porcelain)" && echo "worktree_clean=true"</automated>
  </verify>
  <done>ci-fast.yml calls the new gate immediately after Checkout; ci-full.yml unchanged; old script deleted; full-HEAD gate run green including design-system-v2; token-list completeness proven against the derived 18-file proof set (37-01's 14 files + 37-02's 3 overlap files + PHASE-BRIEF, excluding the 11 dangling-reference-only files, .gitignore, and the 3 binaries) with zero unresolved holes; no extracted blob survives outside the scratchpad (git status --porcelain clean before commit); cargo test green under C-07 constraints; pre-commit block demonstrated in a scratch clone; 37-VALIDATION.md closed out (nyquist_compliant true, Sign-Off checked, Per-Task map filled with real task IDs and ✅).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local `git commit --no-verify` → CI | The hook is a local, bypassable opt-in; CI is the only boundary an attacker/careless committer cannot skip without repo-admin action |
| CI job env → public log output | `ci-fast.yml`'s step output is visible to anyone who can view Actions runs on a public repo |
| Populated `scripts/privacy-tokens.sha256` → git history | Once committed, the hash list itself becomes permanent — but per D-08 it is a stop-word list, not a secret, so this is an accepted disposition |
| Pre-cleanup blobs checked out for the completeness proof → session scratchpad (outside the repo) | Task 3 extracts real leaking content from git history into the session scratchpad only, never a repo-relative or ambiguous path; those files must never be committed, quoted, or left on disk past their own scan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------|--------------|-----------------|
| T-37-05 | Repudiation / Tampering | `git commit --no-verify` bypassing the local hook | Medium | V1 Architecture | mitigate | Not solvable locally by design (git hooks are always locally bypassable) — the `ci-fast.yml` step is the backstop, documented explicitly in the kept comment block as "a backstop, not a duplication" per SPEC Constraints; this plan's Task 3 verifies the CI step actually runs and blocks on a positive fixture |
| T-37-02 | Information Disclosure | Production `scripts/privacy-tokens.sha256` | Low | V6 Cryptography | accept (D-08, restated) | Same accepted residual risk as plan 37-03's fixture; header text required by Task 1's acceptance criteria |
| T-37-14 | Elevation of Privilege (low) | `scripts/setup-hooks.sh` silently changing a developer's git config | Low | V14 Configuration | accept | D-10 explicitly rejected the auto-wiring alternative (`postinstall`) for this exact reason — `setup-hooks.sh` is opt-in, one command, documented in `CONTRIBUTING.md`, never run automatically |
| T-37-15 | Tampering | `design-system-v2/` guillemet-flagged files "fixed" by weakening the gate instead of cleaning content | Medium | V14 Configuration | mitigate | Hard prohibition #4 explicitly forbids adding an exclusion/override; Task 3's action requires cleaning or untracking the specific flagged file(s) instead |
| T-37-16 | Information Disclosure | Pre-cleanup blobs extracted to scratch during the Task 3 completeness proof | High | V8 Data Protection | mitigate | Extraction target is pinned to the session scratchpad (same scratch root as plan 37-01 Task 1, proven outside `/Users/madsas/Projects/trackly`), never a bare `/scratch/path` placeholder; each blob is deleted the moment its own scan concludes, on both the pass and hole-found paths; `git status --porcelain` is checked clean immediately before Task 3's commit; SUMMARY records only file path + pass/fail, never matched value or blob content |
</threat_model>

<verification>
`node scripts/check-privacy.mjs` (production hashes) exits 0 on full HEAD. `.githooks/pre-commit`
blocks a scratch commit containing a known fixture marker. `ci-fast.yml`'s gate step runs
immediately after Checkout; `ci-full.yml` has no privacy step. `scripts/check-privacy-requisites.sh`
is gone. `cargo test` passes under the C-07 constraints. The completeness proof shows the gate
firing on every file in the derived 18-file proof set (37-01's 14 files + 37-02's 3 overlap files
+ PHASE-BRIEF) with zero holes, and no extracted blob survives outside the session scratchpad.
`37-VALIDATION.md` is closed out (`nyquist_compliant: true`, Sign-Off checked, Per-Task map filled
and ✅).
</verification>

<success_criteria>
The gate is live in two enforcement points (local pre-commit, CI backstop), the legacy script is
fully retired with zero regression, the production token list is populated without ever exposing a
real value in a commit, message, or SUMMARY, its completeness is proven against the derived
pre-cleanup proof set rather than assumed or over-scoped to files that never carried a marker, no
extracted blob ever leaves the session scratchpad, the full HEAD — including the previously
unaudited `design-system-v2/` directory — is provably clean, and the phase's own validation
contract is closed rather than left `pending`.
</success_criteria>

<output>
Create `.planning/phases/37-data-privacy/37-04-SUMMARY.md` when done. Include the scratch-clone
pre-commit-block transcript (commands + exit codes only, zero real values) and the completeness-
proof table (file path + pass/fail only, zero real values).
</output>
</output>
