---
phase: 37-data-privacy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/phases/30-quality-a11y-platform-parity/30-09-SUMMARY.md
  - .planning/phases/34-document-header/34-REVIEW.md
  - .planning/phases/03-pdf/03-UAT.md
  - crates/trackly-app/src/pdf/renderer.rs
  - .planning/quick/260805-lrs-employee-header-full-name-must-use-avail/260805-lrs-PLAN.md
  - .planning/quick/260805-lrs-employee-header-full-name-must-use-avail/260805-lrs-SUMMARY.md
  - .planning/quick/260804-ire-ad-ldap-transport-mode/260804-ire-PLAN.md
  - .planning/quick/260805-edd-fix-lan-print-pass-stylesheets-to-paged-/260805-edd-PLAN.md
  - .planning/quick/260805-edd-fix-lan-print-pass-stylesheets-to-paged-/260805-edd-SUMMARY.md
  - .planning/quick/260805-gdz-lan-print-surface-swallowed-error-and-st/260805-gdz-PLAN.md
  - .planning/quick/260805-gdz-lan-print-surface-swallowed-error-and-st/260805-gdz-SUMMARY.md
  - .planning/quick/260805-har-lan-print-neutralize-app-body-background/260805-har-PLAN.md
  - .planning/quick/260805-har-lan-print-neutralize-app-body-background/260805-har-SUMMARY.md
autonomous: true
requirements: [PRIV-01]

must_haves:
  truths:
    - "D-03: the mapping real-value→placeholder is built and applied via a one-shot substitution script that lives OUTSIDE the repo (session scratchpad) and is never committed — verified by `git status --porcelain` showing no untracked script file and the SUMMARY reporting 'file X, class Y, N replacements' with zero quoted values"
    - "C-04: before every commit created by this plan, run `node scripts/check-privacy.mjs --staged` if the gate already exists (parallel plan 37-03 may have landed it); if it does not exist yet, run the scratch marker-list `git grep -I` check as the pre-commit substitute — this plan never commits without one of the two checks passing"
    - "C-05: commit messages produced by this plan reference file paths, leak classes and counts only — never a real requisite, ФИО or infrastructure identifier"
    - "R1/PRIV-01 (class A): after cleanup, `git grep -I` (excluding Cargo.lock) for every real class-A marker discovered during Task 1 (organization full/short name, address, phones, fax, e-mail, ОКПО, ОГРН) returns 0 matches across the files in files_modified, and each replaced site still reads as a grammatical, technically meaningful sentence"
    - "R2/PRIV-01 (class B): after cleanup, `git grep -I` for every real class-B marker (employee ФИО) discovered during Task 1 returns 0 matches; STATE.md's 2026-08-05 journal entry still explains what broke and how it was fixed, using a fictional name («Иванов И.И.») or an anonymized description («ФИО сотрудника обрезалось»)"
    - "R3/PRIV-01 (class C): after cleanup, `git grep -I` for every real class-C marker (DC name, AD domain, employee login, local org-named path) discovered during Task 1 returns 0 matches; replacements use the `example.local`/`us100`/`dc.example.local`-style convention already established in phases 31/32"
    - "RESEARCH §Open Questions #1: for every real marker replaced in Task 2, this plan's SUMMARY records a marker-shape checklist entry (class + structural form, never the value) so plan 37-04 can re-derive the full real-marker set for `--add` without unaided recall — verified by the SUMMARY containing 'класс {A|B|C}, {форма}, {N} замен'-shaped lines and `grep -E '[0-9]{4,}'` against the SUMMARY returning 0 matches"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Journal entries for 2026-08-05 with class B/C markers replaced, diagnostic meaning preserved"
    - path: "crates/trackly-app/src/pdf/renderer.rs"
      provides: "Code comment near line 251 no longer names the real organization"
  key_links:
    - from: "scratchpad substitution script (not committed)"
      to: "the 14 files in files_modified"
      via: "one mechanical text-replacement pass per file, diffed and verified before commit"
      pattern: "git diff --stat"
---

<objective>
Scrub real organization requisites (class A), real employee ФИО (class B), and real
infrastructure identifiers — DC name, AD domain, employee login, org-named local path (class C)
— from the 14 HEAD text files that the SPEC reconnaissance table identifies as carrying them,
using a scripted, one-shot substitution (D-03) so no real value is ever typed into an Edit call,
a commit message, or this plan's own SUMMARY.

Purpose: satisfies PRIV-01 for these 14 files (R1, R2, R3). The remaining 3 SPEC-listed files
that also carry class-A markers (`15-02-PLAN.md`, `15-CONTEXT.md`, `15-RESEARCH.md`) are owned by
plan 37-02 instead, because they are edited a second time in that plan for the R4 dangling-reference
rewrite — keeping them out of this plan's `files_modified` avoids a same-wave file-ownership
conflict between 37-01 and 37-02.

Output: 14 files with class A/B/C markers replaced by CLAUDE.md-approved placeholders or
anonymized prose, technical meaning preserved, committed in class-labeled commits with no
markers in the messages (C-05).

**Note on file counts (informational, do not "correct" the file list below):** 37-SPEC.md's
Goal line states "17 текстовых файлов" for the combined class A/B/C scrub. The class A/B/C
reconnaissance tables in 37-SPEC.md sum to 18 because a small number of files carry markers of
more than one class and are counted once per class row. The distinct-file count that this plan
(14 files) plus plan 37-02's overlap scrub (3 files: `15-02-PLAN.md`, `15-CONTEXT.md`,
`15-RESEARCH.md`) together cover is 17 — matching the Goal line and already matching the
recon tables' per-file coverage. Do not reduce either plan's file list to reconcile a
"17 vs 18" reading; the tables' per-file coverage is authoritative, the Goal line's distinct-file
phrasing and this plan's combined coverage already agree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<privacy_protocol>
This repository is PUBLIC. Real values discovered while executing this plan MUST NOT appear in:
any Edit/Write tool call outside the 14 target files themselves, this plan file, the executor's
SUMMARY, or any commit message. The approved fictional placeholders are: `org.name`,
`{{ act.giver_name }}`, «Иванов И.И.», «Петров П.П.», `example.local`, `dc.example.local`,
`us100`, `+7 495 000-00-00`. Everything committed stays in git history even after later deletion
— get the substitution right the first time, do not iterate by committing-then-fixing.
</privacy_protocol>

<hard_prohibitions>
1. Do NOT edit `migrations/V033__org_settings_requisites.sql` — it is not in this plan's
   `files_modified` and must stay byte-identical (D-02). Editing it (even the filename reference
   in its line-5 comment) changes its refinery checksum and makes the app refuse to start on every
   installed copy of Trackly.
2. Do NOT write the real-value→placeholder mapping, or the substitution script itself, into any
   file inside the repository working tree. Use the session scratchpad directory only. The script
   is discarded after use, not committed.
3. Do NOT quote a real value in the SUMMARY.md this plan produces. Report as "file X, class Y,
   N replacements" plus the marker-shape checklist entry (class + structural form only) — counts,
   classes and shapes, never values.
4. Do NOT touch `.planning/reference/`, `.planning/PHASE-BRIEF-act-pdf-word-fidelity.md`, or the
   `.gitignore` — that is plan 37-02's scope (R4).
</hard_prohibitions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover real markers and build the scratch substitution script</name>
  <files>(scratchpad only — no repository files written in this task)</files>
  <read_first>
    .planning/phases/37-data-privacy/37-SPEC.md (§Background, classes A/B/C recon table) —
    the file-by-file description of WHAT to look for in each of the 14 files, without ever
    quoting the leaking value itself. CLAUDE.md §Constraints for the approved placeholder list.
    Do not treat this read_first as license to copy any value found while reading the 14 target
    files into any output outside the scratchpad.
  </read_first>
  <action>
    For each of the 14 files in this plan's files_modified: open it, locate every class-A, -B, or
    -C marker per the SPEC recon table's description of that file, and record `real value ->
    placeholder` pairs in a mapping file inside the session scratchpad directory (never inside the
    repo). Use placeholders from the approved list — for organization name use a variable-shaped
    placeholder that reads naturally in Russian prose (e.g. «ООО «Организация»» for the full form,
    `org.name` only where the source is already code/template-variable-shaped, not prose);
    for phone/fax use `+7 495 000-00-00`-style numbers with an incrementing last two digits to keep
    multi-number examples distinguishable; for ОКПО/ОГРН use structurally-valid-looking but
    obviously-fake digit strings (same digit-count as the original, not copied from the existing
    `check-privacy-requisites.sh` ALLOWED list unless independently marking them fictional here
    too); for ФИО use «Иванов И.И.» / «Петров П.П.» or, where CLAUDE.md's anonymization guidance
    applies (a UAT narrative about what broke, not a demo record), an anonymized description
    («ФИО сотрудника обрезалось») that preserves the diagnostic meaning of the sentence; for AD
    domain/DC name/login use `example.local` / `dc.example.local` / `us100` per the phases 31/32
    convention; for the org-named local path in `30-09-SUMMARY.md`, generalize it to a
    structurally equivalent but organization-agnostic path. Then write a small Node or bash
    script in the scratchpad (not the repo) that reads the mapping and applies literal
    string-replacement across the 14 files, one file at a time, printing a per-file replacement
    count to stdout (counts only, never the matched text). Do not run the script yet — that is
    Task 2. While recording each mapping entry, also note its structural FORM (e.g. "кавычная
    кириллическая форма", "path-embedded форма", "латинская транслитерация", "цифровая форма без
    разделителей") next to the entry — this shape label (never the value) is what Task 2 copies
    into the SUMMARY for plan 37-04 to re-derive its production token list against.
  </action>
  <acceptance_criteria>
    A mapping file and a substitution script exist in the session scratchpad directory (path
    starts with `/private/tmp/claude-501/...` or equivalent scratch root), covering every file in
    this plan's files_modified. Neither file exists inside `/Users/madsas/Projects/trackly`.
    `git status --porcelain` at the end of this task shows no changes (script has not run yet).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && test -z "$(git status --porcelain)" && echo PASS</automated>
  </verify>
  <done>Scratch mapping and substitution script exist outside the repo, covering all 14 files; no repository file yet modified; script not yet run; each mapping entry carries a structural-form label.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the substitution, verify, commit, and record the marker-shape checklist</name>
  <files>
    .planning/STATE.md,
    .planning/phases/30-quality-a11y-platform-parity/30-09-SUMMARY.md,
    .planning/phases/34-document-header/34-REVIEW.md,
    .planning/phases/03-pdf/03-UAT.md,
    crates/trackly-app/src/pdf/renderer.rs,
    .planning/quick/260805-lrs-employee-header-full-name-must-use-avail/260805-lrs-PLAN.md,
    .planning/quick/260805-lrs-employee-header-full-name-must-use-avail/260805-lrs-SUMMARY.md,
    .planning/quick/260804-ire-ad-ldap-transport-mode/260804-ire-PLAN.md,
    .planning/quick/260805-edd-fix-lan-print-pass-stylesheets-to-paged-/260805-edd-PLAN.md,
    .planning/quick/260805-edd-fix-lan-print-pass-stylesheets-to-paged-/260805-edd-SUMMARY.md,
    .planning/quick/260805-gdz-lan-print-surface-swallowed-error-and-st/260805-gdz-PLAN.md,
    .planning/quick/260805-gdz-lan-print-surface-swallowed-error-and-st/260805-gdz-SUMMARY.md,
    .planning/quick/260805-har-lan-print-neutralize-app-body-background/260805-har-PLAN.md,
    .planning/quick/260805-har-lan-print-neutralize-app-body-background/260805-har-SUMMARY.md
  </files>
  <read_first>
    The scratchpad mapping and script built in Task 1. Do not re-open the 14 files' leaking
    content again for reading purposes beyond what the script itself does — you already have the
    mapping.
  </read_first>
  <action>
    Run the Task 1 script against the 14 files. For each file, review the diff (`git diff --
    &lt;file&gt;`) to confirm the replacement preserved sentence grammar and technical meaning
    (this is a human-judgment check the script cannot make — read the diff, not the whole file
    history). Where a diff reads awkwardly after mechanical substitution, hand-adjust the
    surrounding prose only (never re-introduce the real value). Run `git grep -I` across the
    repository for every real marker string recorded in the Task 1 scratch mapping (feed the
    scratch file to `git grep -I -f &lt;scratch-marker-file&gt;` or loop `git grep -I -F -e
    &lt;value&gt;` per marker) and confirm 0 matches outside `Cargo.lock` (known false positive,
    a phone-code-shaped digit sequence, not a real leak). If the gate built by the parallel plan
    37-03 already exists at `scripts/check-privacy.mjs`, additionally run
    `node scripts/check-privacy.mjs --staged` before committing; otherwise skip that check for
    this plan (C-03 — the gate is not required to exist yet). Delete the scratch script and
    mapping file from the scratchpad once verification passes (D-03 — the mapping does not
    outlive the cleanup). Stage and commit with three separate commits grouped by class (A, B,
    C) — or one combined commit if a file mixes classes — using a message of the shape
    "chore(37): scrub class {A|B|C} markers from N files (PRIV-01)" with zero real values or
    matched strings in the body (C-05). Before writing this plan's SUMMARY, transcribe the
    structural-form labels captured in Task 1 into a marker-shape checklist section: one line per
    file/class combination, shaped exactly like "{file}: класс {A|B|C}, {форма}, {N} замен" —
    permitted form descriptors name ONLY the structural shape (e.g. "кавычная кириллическая
    форма", "path-embedded форма", "латинская транслитерация"); forbidden content is the token
    itself, any substring of it, its length, or a masked/partial rendering of it. This checklist
    is what plan 37-04 (Wave 2) re-derives its production `--add` list against instead of relying
    on unaided recall (RESEARCH §Open Questions #1).
  </action>
  <acceptance_criteria>
    `git grep -I` (excluding `Cargo.lock`) for every marker recorded in the Task 1 scratch mapping
    returns 0 matches. `git log --format=%B -1` for each new commit contains no marker from the
    scratch mapping. The scratch mapping and script no longer exist on disk. `git diff` for each
    of the 14 files, read by the executor, still reads as grammatical Russian prose with the
    diagnostic/technical content of the original sentence intact. The produced SUMMARY.md contains
    a marker-shape checklist line for every file/class combination touched, using only
    class+form+count language (permitted example: "STATE.md: класс B, кавычная кириллическая
    форма, 2 замены"; forbidden: the token, any substring, its length, or a masked rendering).
    `grep -E '[0-9]{4,}' <path to 37-01-SUMMARY.md>` returns no matches (no digit run of length
    ≥4 anywhere in the SUMMARY — catches phone/ОКПО/ОГРН literals leaking into a shape
    description).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && git grep -c '' -- .planning/STATE.md crates/trackly-app/src/pdf/renderer.rs | grep -v ':0' | wc -l</automated>
  </verify>
  <done>All 14 files committed with class A/B/C markers replaced; scratch mapping/script deleted; no real value in any commit message or in this plan's SUMMARY; SUMMARY carries a marker-shape checklist (class+form+count, zero digit runs ≥4 chars, zero token values) for plan 37-04 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Executor agent context → committed files | The agent reads real leak values to know what to replace; those values must never cross into a committed artifact, a tool-call argument outside the 14 target files, or the SUMMARY |
| Executor agent context → session scratchpad | The only place the real-value mapping is allowed to persist, and only transiently |
| Local commit → public git history | Once committed, unrecoverable even if later reverted (CLAUDE.md constraint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|------|--------------|-----------------|
| T-37-08 | Information Disclosure | Executor's own SUMMARY.md / commit messages | High | V8 Data Protection | mitigate | D-03 scripted substitution (values never typed by the agent into a non-scratch location) + C-05 (commit-message discipline stated as an explicit prohibition) + SUMMARY template instructed to report counts/classes/shapes only |
| T-37-09 | Tampering | `migrations/V033__org_settings_requisites.sql` | High | V14 Configuration | mitigate | File is not in this plan's `files_modified`; hard prohibition #1 states the refinery-checksum consequence explicitly so the executor does not "helpfully" touch it |
| T-37-11 | Information Disclosure | Scratch mapping file surviving past task completion | Medium | V8 Data Protection | mitigate | Task 2 explicitly deletes the scratch mapping/script after verification passes |
</threat_model>

<verification>
`git grep -I` across HEAD for every class A/B/C marker recorded during Task 1 returns 0 matches
outside `Cargo.lock`. All 14 files' diffs read as grammatical, technically meaningful Russian
prose. No commit message or SUMMARY produced by this plan contains a real value. SUMMARY.md
contains a complete marker-shape checklist (class + structural form + count per file) with zero
digit runs ≥4 chars.
</verification>

<success_criteria>
The 14 files no longer carry any real organization requisite, employee ФИО, or infrastructure
identifier; replacements use CLAUDE.md-approved or phase-31/32-convention placeholders;
technical/diagnostic meaning of each edited sentence is preserved; commits are class-labeled and
marker-free; the SUMMARY hands plan 37-04 a marker-shape checklist instead of requiring unaided
recall.
</success_criteria>

<output>
Create `.planning/phases/37-data-privacy/37-01-SUMMARY.md` when done. Report file counts and
replacement counts per class, plus the marker-shape checklist (class + structural form + count,
never a value) — never a real value.
</output>
