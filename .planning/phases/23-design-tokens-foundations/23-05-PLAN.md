---
phase: 23-design-tokens-foundations
plan: 05
type: execute
wave: 4
depends_on: ["23-01", "23-02", "23-03", "23-04"]
files_modified:
  - "ui/src/**/*.svelte (все файлы, использующие --font-size-*/--font-weight-*/--line-height-*/--font-family-base — точный список через git grep, см. Task 1 action)"
  - "ui/src/**/*.scss (кроме _tokens.scss/global.scss)"
  - ui/src/features/acts/ActItemsTable.svelte
  - ui/src/features/devices/DeviceListRow.svelte
  - ui/src/features/printers/PrinterDetail.svelte
  - ui/src/features/acts/DocumentAcceptanceModal.svelte
  - ui/src/features/acts/ActFormItemsTable.svelte
  - ui/src/features/acts/ReturnModal.svelte
  - ui/src/features/acts/ActDetail.svelte
  - ui/src/features/acts/ActListRow.svelte
autonomous: true
requirements: [DS-03, QA-01]

must_haves:
  truths:
    - "Ни один .svelte/.scss файл в ui/src (кроме _tokens.scss/global.scss) не содержит var(--font-size-*), var(--font-weight-*), var(--line-height-*) или var(--font-family-base) — миграция по роли, на декомпозированные оси 1:1 (D-14)"
    - "--font-size-sm (PersonAutocomplete.svelte:312) резолвится в --tr-font-size-caption — QA-01 fix"
    - "class=\"tr-mono\" применён на 7 «чистых» in-scope call-sites: табличные ячейки/карточки-детали/dropdown-labels, где inventory_no/serial_no/act.number отображаются как данные (D-13, реальные имена полей — не inventory_number/serial_number/act_number, см. D-17)"
    - "class=\"tr-mono\" НЕ применён к полям ввода, печатным HTML-шаблонам, toast/confirm/modal-title интерполяциям номера акта (D-16) и справочнику плейсхолдеров TemplateEditor.svelte (grey area, осознанно вне scope)"
  artifacts: []
  key_links:
    - from: "ui/src/features/acts/ActItemsTable.svelte, ActDetail.svelte, ActListRow.svelte"
      to: "ui/src/styles/global.scss"
      via: "class=\"tr-mono\" ссылается на .tr-mono, определённый в global.scss планом 23-01"
      pattern: "class=\"tr-mono\""
---

<objective>
Мигрировать ВСЕ call-sites `--font-size-*`/`--font-weight-*`/`--line-height-*`/`--font-family-base`
на декомпозированные `--tr-font-size-{role}`/`--tr-font-weight-{role}`/`--tr-line-height-{role}`/
`--tr-font-family` — **по роли, 1:1, без переписывания на composite shorthand** (правило 3 из
трёх, D-14). Затем применить `.tr-mono` (уже определён планом 23-01) к конкретным 7 «чистым»
местам, где инвентарный №/серийный №/№ акта отображаются как данные (D-13/D-16).

Purpose: закрывает DS-03 (типографика на 9-уровневой шкале, идентификаторы моноширинным) и
последнюю часть QA-01 (`--font-size-sm` undefined bug, PersonAutocomplete.svelte:312).
Output: все ~350 call-sites типографики переведены на новые оси; `.tr-mono` применён на грепаемом,
явно задокументированном подмножестве.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-design-tokens-foundations/23-UI-SPEC.md
@.planning/phases/23-design-tokens-foundations/23-RESEARCH.md
@.planning/phases/23-design-tokens-foundations/23-PATTERNS.md
</context>

<interfaces>
<!-- Ordered-карта Task 1. Пункты 1-3 — full-expression rewrite (fallback-паттерн целиком, не
     просто внутренний токен) — обязаны идти ПЕРЕД пунктами 5-6, иначе generic-замена
     --font-size-heading/--font-size-body внутри fallback-выражения сломает совместимость с ещё
     не переписанным внешним var(--font-size-page-title, …). -->

1. `var(--font-size-page-title, var(--font-size-heading))` → `var(--tr-font-size-h3)` (целиком выражение, 7 сайтов: CartridgesPage.svelte:507, ActsPage.svelte:359, RequestsPage.svelte:307, PrintersPage.svelte:224, UsersPage.svelte:152, DevicesPage.svelte:335, SettingsPage.svelte:64)
2. `var(--font-size-subheading, var(--font-size-body))` → `var(--tr-font-size-body)` (целиком выражение, 2 сайта: ActDetail.svelte:179, ReturnModal.svelte:451)
3. `var(--font-size-caption, 12px)` → `var(--tr-font-size-caption)` (целиком выражение, 1 сайт: DeviceImportCsvModal.svelte:393)
4. `--font-size-sm` → `--tr-font-size-caption` (QA-01 bug fix, было undefined; 1 сайт: PersonAutocomplete.svelte:312)
5. `--font-size-label` → `--tr-font-size-label`
6. `--font-size-heading` → `--tr-font-size-h3` (оставшиеся bare-употребления вне fallback-паттерна, уже забранного пунктом 1)
7. `--font-size-display` → `--tr-font-size-display`
8. `--font-size-body` → `--tr-font-size-body` (только ПОСЛЕ пункта 2, который уже забрал subheading-fallback)
9. `--font-weight-regular` → `--tr-font-weight-regular` (400, значение не меняется)
10. `--font-weight-medium` → `--tr-font-weight-medium` (500, значение не меняется)
11. `--font-weight-semibold` → `--tr-font-weight-semibold` (600, значение не меняется)
12. `--line-height-body` → `--tr-line-height-body`
13. `--line-height-label` → `--tr-line-height-label`
14. `--line-height-heading` → `--tr-line-height-h3`
15. `--line-height-display` → `--tr-line-height-display`
16. `--font-family-base` → `--tr-font-family`

`.tr-mono`-применение (Task 2) — 7 «чистых» in-scope call-sites, по D-13 (реальные grep-термины —
`inventory_no`/`serial_no`/`.number`, НЕ `inventory_number`/`serial_number`/`act_number`, см. D-17):
`ActItemsTable.svelte:34-35` (`{item.inventory_no}`/`{item.serial_no}` в табличных ячейках);
`DeviceListRow.svelte:52-53` (`{device.inventory_no}`/`{device.serial_no}` в табличных ячейках);
`PrinterDetail.svelte:327,331` (`{deviceData?.inventory_no}`/`{deviceData?.serial_no}` в карточке
деталей); `DocumentAcceptanceModal.svelte:100-101` (`(инв. № {device.inventory_no})` — Svelte-
модалка UI, НЕ печатный HTML-шаблон); `ActFormItemsTable.svelte:240-316` (label для grouped
dropdown — `${d.name} (SN ${d.serial_no}, инв. ${d.inventory_no})` — сам `<input>` для текстового
поиска рядом НЕ трогать); `ReturnModal.svelte:104-151` (deviceLabel для dropdown-опций возврата,
та же природа); `ActDetail.svelte:69` (`<h2 class="detail-title">№{act.number} от {headerDate}
</h2>` — обернуть в `<span class="tr-mono">` ТОЛЬКО сегмент `№{act.number}`, не весь заголовок и
не `{headerDate}`); `ActListRow.svelte:62` (`<span class="number">№{act.number}</span>` — уже
изолированный span, тривиально добавить класс рядом с существующим).

НЕ применять `.tr-mono` (D-16, осознанное решение, не молчаливый пропуск): `DeviceFormBody.svelte`
(поле ввода); `PrinterCreateModal.svelte` (не рендерится); `DeviceImportCsvModal.svelte` (CSV
column-mapping select — имена полей, не значения устройства); `TemplateEditor.svelte:48,61-62`
(справочник плейсхолдеров шаблона — мета-текст, не отображение реального значения); toast/confirm/
modal-title интерполяции номера акта (`` `Редактировать акт №${initialAct?.number}` ``,
`ActFormModal.svelte:38`, `ActsPage.svelte:224-334`, `ReturnModal.svelte:178-331`) — номер
интерполирован внутри полноценного русского предложения, изоляция потребовала бы переписывать
структуру строки; `DeviceContextMenu.svelte:146` (confirm-диалог с инв.№ внутри предложения) —
grey area, оставить как есть.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Литеральный sweep типографики по ordered-карте (16 пунктов)</name>
  <files>ui/src/**/*.svelte, ui/src/**/*.scss (кроме _tokens.scss/global.scss)</files>
  <read_first>
    .planning/phases/23-design-tokens-foundations/23-UI-SPEC.md §Typography Scale (таблица 9
    ролей) и §Font-size / line-height migration table (BY ROLE); ui/src/styles/_tokens.scss
    (переписан планом 23-01 — все 16 целевых decomposed-осей должны быть в нём определены до
    начала sweep); .planning/phases/23-design-tokens-foundations/23-RESEARCH.md раздел
    «`--font-size-page-title`/`--font-size-subheading`/`--font-size-caption` — НЕ undefined-баги»
    (объясняет, почему эти 3 fallback-паттерна требуют замены ЦЕЛОГО выражения, а не только
    внутреннего токена)
  </read_first>
  <action>
    Получить точный список файлов: `git grep -l -E -- '--font-size-[a-z0-9-]+|--font-weight-
    (regular|medium|semibold)|--line-height-[a-z0-9-]+|--font-family-base' ui/src`.

    Применить 16-пунктовую ordered-карту из блока `<interfaces>` выше СТРОГО в указанном порядке:
    пункты 1-3 — замена ЦЕЛОГО выражения (`var(--font-size-X, var(--font-size-Y))` целиком, не
    только внутреннего токена) на 10 конкретных file:line сайтов, перечисленных в интерфейсе.
    Пункт 4 — QA-01 fix на единственном сайте PersonAutocomplete.svelte:312. Пункты 5-16 —
    обычная 1:1 литеральная замена токен-имени везде в оставшихся файлах.

    НЕ трогать в этой задаче: цветовые/space/radius-токены (уже сделаны в 23-03/23-04),
    `class="tr-mono"` (это Task 2 этого же плана).
  </action>
  <verify>
    <automated>git grep -c -- 'var(--font-size-' ui/src -- '*.svelte' '*.scss' | grep -v ':0'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c -- 'var(--font-size-' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто
    - `git grep -c -- 'var(--font-weight-regular)\|var(--font-weight-medium)\|var(--font-weight-semibold)' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто
    - `git grep -c -- 'var(--line-height-' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто
    - `git grep -c -- 'var(--font-family-base)' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто
    - `git grep -c -- '--tr-font-size-caption' ui/src/lib/components/PersonAutocomplete.svelte` возвращает не менее 1 (QA-01 fix подтверждён)
  </acceptance_criteria>
  <done>Все --font-size-*/--font-weight-*/--line-height-*/--font-family-base call-sites мигрированы, включая 3 fallback-паттерна как целые выражения и QA-01 font-size-sm fix.</done>
</task>

<task type="auto">
  <name>Task 2: Применить class="tr-mono" на 7 in-scope call-sites (DS-03)</name>
  <files>ui/src/features/acts/ActItemsTable.svelte, ui/src/features/devices/DeviceListRow.svelte, ui/src/features/printers/PrinterDetail.svelte, ui/src/features/acts/DocumentAcceptanceModal.svelte, ui/src/features/acts/ActFormItemsTable.svelte, ui/src/features/acts/ReturnModal.svelte, ui/src/features/acts/ActDetail.svelte, ui/src/features/acts/ActListRow.svelte</files>
  <read_first>
    ui/src/styles/global.scss (планом 23-01 уже содержит определение `.tr-mono` — использовать
    класс как есть, не переопределять); .planning/phases/23-design-tokens-foundations/23-RESEARCH.md
    §DS-03 Mono Coverage (полная категоризация call-sites — IN SCOPE / NOT IN SCOPE / GREY AREA —
    и точная построчная разбивка для всех 7 целевых файлов); каждый из 8 целевых файлов на
    строках, перечисленных в блоке `<interfaces>` выше, до внесения правки — подтвердить текущий
    номер строки не сдвинулся с момента research (счётчики строк могли измениться после Task 1
    этого же плана)
  </read_first>
  <action>
    Добавить `class="tr-mono"` (рядом с существующими классами, не заменяя их) в разметке ровно
    на 9 конкретных местах из блока `<interfaces>` выше: `ActItemsTable.svelte` — 2 ячейки
    (inventory_no, serial_no); `DeviceListRow.svelte` — 2 ячейки; `PrinterDetail.svelte` — 2
    значения в карточке деталей; `DocumentAcceptanceModal.svelte` — 1 место (интерполяция инв.№
    внутри `{#if}`-блока); `ActFormItemsTable.svelte` — dropdown-label конструкция (обернуть
    сегмент с SN/инв. № внутри итоговой строки label, не весь label); `ReturnModal.svelte` —
    аналогичный dropdown-label; `ActDetail.svelte:69` — обернуть ТОЛЬКО `№{act.number}` в новый
    `<span class="tr-mono">`, оставить `{headerDate}` вне обёртки; `ActListRow.svelte:62` —
    добавить класс на уже существующий `<span class="number">`.

    НЕ применять `.tr-mono` нигде за пределами этих 9 мест в этой задаче (полный список grey-area
    и explicitly-excluded мест — в блоке `<interfaces>` выше, решение D-16 задокументировано, не
    молчаливо пропущено).
  </action>
  <verify>
    <automated>git grep -c 'class="tr-mono"' ui/src</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c 'class="tr-mono"' ui/src` возвращает не менее 9 (9 конкретных вставок; возможно больше, если dropdown-label конструкции содержат несколько сегментов — не менее)
    - `git grep -n 'class="tr-mono"' ui/src/features/acts/ActItemsTable.svelte ui/src/features/devices/DeviceListRow.svelte ui/src/features/printers/PrinterDetail.svelte ui/src/features/acts/DocumentAcceptanceModal.svelte ui/src/features/acts/ActFormItemsTable.svelte ui/src/features/acts/ReturnModal.svelte ui/src/features/acts/ActDetail.svelte ui/src/features/acts/ActListRow.svelte` — каждый из 8 файлов даёт не менее одного совпадения
    - `git grep -c 'tr-mono' ui/src/features/acts/ActFormModal.svelte ui/src/features/acts/ActsPage.svelte ui/src/lib/components/TemplateEditor.svelte` возвращает 0 в каждом (подтверждает D-16 exclusion не пропущен по ошибке — grey-area файлы намеренно чисты)
    - `cd ui && pnpm svelte-check` остаётся на чистом baseline (0 errors)
  </acceptance_criteria>
  <done>class="tr-mono" применён на всех 7 «чистых» in-scope файлах (9 конкретных сайтах); explicitly-excluded места (toast/modal-title/TemplateEditor) подтверждённо чисты.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Нет новой границы доверия | `.tr-mono` — статическая строка класса в Svelte-шаблоне, не пользовательский ввод; не вводит новый XSS-вектор (подтверждено research §Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | Tampering | Типографические значения могут тихо сдвинуться при неверном role-mapping (напр. heading→h3 vs body) | mitigate | Ordered-карта в этом плане — точная копия UI-SPEC §Font-size/line-height migration table, включая полный список конкретных file:line для fallback-паттернов; DS-03 допускает мягкий сдвиг размера (не строгий DS-04-класс), но role-mapping всё равно детерминирован таблицей, не оставлен на усмотрение |
| T-23-05-SC | Tampering (supply chain) | npm installs | accept | Ноль новых зависимостей |
</threat_model>

<verification>
`node ui/scripts/check-tokens.mjs --rules=1` больше не находит --font-size-*/--font-weight-*/
--line-height-*/--font-family-base нарушений. `git grep 'class="tr-mono"'` подтверждает покрытие
9 конкретных сайтов. `pnpm svelte-check` чист. Визуальная иерархия «текст на правильном уровне
шкалы» — UAT-проверка (D-09), не эта задача.
</verification>

<success_criteria>
Все ~350 typography call-sites мигрированы на decomposed оси по роли; `--font-size-sm` QA-01 bug
закрыт; `.tr-mono` применён на грепаемом, явно задокументированном 9-сайтовом подмножестве без
молчаливых пропусков toast/modal-title/TemplateEditor grey-area. DS-03 (статически проверяемая
часть) и финальная часть QA-01 выполнены.
</success_criteria>

<output>
Create `.planning/phases/23-design-tokens-foundations/23-05-SUMMARY.md` when done
</output>
