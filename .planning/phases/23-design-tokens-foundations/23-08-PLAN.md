---
phase: 23-design-tokens-foundations
plan: 08
type: execute
wave: 2
depends_on: ["23-07"]
gap_closure: true
files_modified:
  - ui/src/lib/components/Modal.svelte
  - ui/src/lib/components/Button.svelte
  - ui/src/lib/components/Input.svelte
  - ui/src/lib/components/DatePicker.svelte
  - ui/src/lib/components/LocationAutocomplete.svelte
  - ui/src/lib/components/PersonAutocomplete.svelte
  - ui/src/features/devices/DeviceAutocompleteField.svelte
  - ui/src/features/acts/ActFormItemsTable.svelte
  - ui/src/features/cartridges/ModelFormModal.svelte
  - ui/src/features/dashboard/ChartWidget.svelte
  - ui/src/features/auth/LoginPage.svelte
  - ui/src/features/auth/FirstRunWizard.svelte
  - ui/src/features/auth/PendingScreen.svelte
  - ui/src/features/auth/BlockedScreen.svelte
autonomous: true
requirements: [DS-01]

must_haves:
  truths:
    - "Modal.svelte overlay-фон использует var(--tr-overlay) вместо hardcoded rgba(0, 0, 0, 0.4)/rgba(0, 0, 0, 0.6)"
    - "Все 9 сайтов дублированного 'invalid'-focus-ring rgba(220, 38, 38, ...) в 8 файлах переведены на var(--tr-danger-ring), включая Button.svelte (alpha 0.3 -> 0.2)"
    - "ChartWidget-тултип и redundant rgba-fallback в ActFormItemsTable:909 используют var(--tr-elev-*) без hardcoded rgba"
    - "4 auth-экрана используют var(--tr-elev-*) вместо hardcoded rgba(0, 0, 0, 0.08)-теней карточек"
    - "node ui/scripts/check-tokens.mjs (все 4 правила по умолчанию) завершается exit-кодом 0 на всём ui/src"
    - "pnpm lint / pnpm svelte-check / pnpm build остаются зелёными после миграции"
  artifacts:
    - path: "ui/src/lib/components/Modal.svelte"
      provides: "Оверлей на var(--tr-overlay), не hardcoded rgba"
  key_links:
    - from: "8 danger-ring файлов"
      to: "ui/src/styles/_tokens.scss --tr-danger-ring"
      via: "var(--tr-danger-ring)"
      pattern: "var\\(--tr-danger-ring\\)"
    - from: "ui/src/lib/components/Modal.svelte"
      to: "ui/src/styles/_tokens.scss --tr-overlay"
      via: "var(--tr-overlay)"
      pattern: "var\\(--tr-overlay\\)"
---

<objective>
Закрыть основной (DS-01-blocking) пробел gap-closure раунда: мигрировать 16-17 хардкоженных
rgba()-литералов в 14 файлах на --tr-* токены (--tr-overlay для Modal.svelte, новый
--tr-danger-ring из плана 23-07 для 9 focus-ring сайтов, --tr-elev-* для ChartWidget + 4
auth-экранов), и подтвердить полный check-tokens.mjs (все 4 правила) зелёным на всём дереве.

Purpose: 23-VERIFICATION.md зафиксировала это как единственный настоящий пробел (6/7 must-haves) —
буквальная формулировка DS-01 включает rgba(), а не только hex; план 23-03 сузил свою проверку до
hex-only. Wave 2, зависит от 23-07 (гейт + --tr-danger-ring должны существовать первыми).
Output: 14 файлов на --tr-*; check-tokens.mjs зелёный; pnpm lint/svelte-check/build зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-design-tokens-foundations/23-VERIFICATION.md
@.planning/phases/23-design-tokens-foundations/23-REVIEW.md
</context>

<interfaces>
Токены-цели (план 23-07, ui/src/styles/_tokens.scss): --tr-overlay (уже существовал, не
потреблялся Modal.svelte); --tr-danger-ring (новый, rgba(207,59,59,0.2) light / rgba(242,101,101,0.2)
dark); --tr-elev-1/--tr-elev-2 (уже существовали — Modal.svelte:98, PersonAutocomplete.svelte:308,
PdfPreviewModal.svelte:335, ActFormItemsTable.svelte:818 уже используют var(--tr-elev-2) для
аналогичных "карточных"/"плавающих" элементов — тот же уровень применяется здесь).

Точные до-миграционные сайты (номера строк могут сместиться — читать файл перед правкой):
Modal.svelte:83/:91 (rgba(0,0,0,0.4) / rgba(0,0,0,0.6) overlay). 9 сайтов rgba(220,38,38,0.2)
(0.3 в Button.svelte): PersonAutocomplete.svelte:281, ActFormItemsTable.svelte:985,1010,
Button.svelte:109, Input.svelte:67, DatePicker.svelte:67, LocationAutocomplete.svelte:200,
DeviceAutocompleteField.svelte:416, ModelFormModal.svelte:526. ChartWidget.svelte:398
(rgba(0,0,0,0.15) tooltip). 4×rgba(0,0,0,0.08): LoginPage.svelte:179, FirstRunWizard.svelte:194,
PendingScreen.svelte:41, BlockedScreen.svelte:129 (карточка экрана). ActFormItemsTable.svelte:909
box-shadow: var(--tr-elev-1, 0 1px 2px rgba(0,0,0,0.08)); — redundant hardcoded rgba fallback.

Button.svelte visual-touch note (WR-01, pre-approved): смена alpha 0.3 → 0.2 на danger-ring
Button.svelte — реальное, хоть и микроскопическое, визуальное изменение. CONTEXT.md резервирует
визуальный редизайн Button за фазой 24, но это конкретное изменение санкционировано через WR-01 /
verification gap (не layout, не API, не поведение). Обязательно отразить в 23-08-SUMMARY (Task 3),
чтобы планирование фазы 24 не удивилось «уже тронутому» Button.svelte.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Modal overlay + danger-ring migration (9 файлов — крупная механическая задача)</name>
  <files>ui/src/lib/components/Modal.svelte, ui/src/lib/components/PersonAutocomplete.svelte, ui/src/features/acts/ActFormItemsTable.svelte, ui/src/lib/components/Button.svelte, ui/src/lib/components/Input.svelte, ui/src/lib/components/DatePicker.svelte, ui/src/lib/components/LocationAutocomplete.svelte, ui/src/features/devices/DeviceAutocompleteField.svelte, ui/src/features/cartridges/ModelFormModal.svelte</files>
  <read_first>
    Modal.svelte строки 75-100 (оба overlay-сайта + var(--tr-elev-2) на строке 98 как образец
    theme-scoped паттерна); _tokens.scss --tr-overlay/--tr-danger-ring (план 23-07); все 9 сайтов
    rgba(220,38,38,...) перечислены в interfaces — прочитать окружающий focus-visible/.invalid
    контекст каждого перед заменой.
  </read_first>
  <action>
    ПЕЙСИНГ: эта задача трогает 9 файлов (Modal + 8 danger-ring файлов) — сознательно крупнее
    обычного, но это чисто механическая одно-значение-на-строку замена, а Task 3 прогоняет полный
    check-tokens.mjs как backstop. Обрабатывать файлы по одному, проверяя grep после каждого.

    В Modal.svelte заменить оба overlay-сайта на var(--tr-overlay). Если второй сайт —
    [data-theme='dark']-override, идентичный первому после замены (токен сам theme-scoped) —
    удалить избыточный override; если покрывает что-то ещё — заменить только значение. Заменить
    все 9 сайтов rgba(220,38,38,0.2)/0.3 на var(--tr-danger-ring) — единое каноническое значение,
    устраняя расхождение 0.2 vs 0.3 (Button.svelte:109 — единственный сайт с 0.3; замена делает
    его 0.2, это санкционированный WR-01 микро-визуальный touch, см. interfaces). Не менять другую
    логику/CSS в затронутых блоках.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && ! grep -rq "rgba(220, 38, 38" ui/src && ! grep -Eq "rgba\(0, 0, 0, 0\.(4|6)\)" ui/src/lib/components/Modal.svelte && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rq "rgba(220, 38, 38" ui/src` — истинно (0 danger-ring rgba-литералов во всём ui/src, нонзеро-exit при любом остатке)
    - `grep -c "var(--tr-danger-ring)" ui/src/features/acts/ActFormItemsTable.svelte` возвращает 2
    - `! grep -Eq "rgba\(0, 0, 0, 0\.(4|6)\)" ui/src/lib/components/Modal.svelte` — истинно (overlay-литералы убраны)
    - `grep -c "var(--tr-overlay)" ui/src/lib/components/Modal.svelte` возвращает не менее 1
  </acceptance_criteria>
  <done>Modal overlay и все 9 danger-ring сайтов на токенах; ни rgba(220,38,38…), ни hardcoded overlay rgba не остаётся. Button.svelte alpha 0.3→0.2 зафиксировано как WR-01-санкционированный touch (для SUMMARY в Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Elevation-тени — ChartWidget + 4 auth-экрана + fallback-strip</name>
  <files>ui/src/features/dashboard/ChartWidget.svelte, ui/src/features/auth/LoginPage.svelte, ui/src/features/auth/FirstRunWizard.svelte, ui/src/features/auth/PendingScreen.svelte, ui/src/features/auth/BlockedScreen.svelte, ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    ChartWidget.svelte ~398 (.chart-tooltip); 4 auth-файла (карточка box-shadow); ActFormItemsTable.svelte
    ~909 (drill-header fallback); Modal.svelte:98/PersonAutocomplete.svelte:308 как образец var(--tr-elev-2).
  </read_first>
  <action>
    ChartWidget.svelte: box-shadow: 0 2px 6px rgba(0,0,0,0.15); -> var(--tr-elev-2). 4 auth-экрана:
    box-shadow: 0 2px 12px rgba(0,0,0,0.08); -> var(--tr-elev-2) (та же роль карточки, что Modal).
    ActFormItemsTable.svelte:909: var(--tr-elev-1, 0 1px 2px rgba(0,0,0,0.08)) -> var(--tr-elev-1)
    (убрать мёртвый rgba-fallback, --tr-elev-1 всегда определён).
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && ! grep -rEq "rgba\(0, 0, 0, 0\.(15|08)\)" ui/src && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rEq "rgba\(0, 0, 0, 0\.(15|08)\)" ui/src` — истинно (0 elevation-rgba-литералов во всём ui/src, нонзеро-exit при любом остатке)
    - `grep -c "var(--tr-elev-2)" ui/src/features/dashboard/ChartWidget.svelte` возвращает 1
    - grep по 4 auth-файлам на "var(--tr-elev-2)" суммарно возвращает 4
    - `grep -c "var(--tr-elev-1);" ui/src/features/acts/ActFormItemsTable.svelte` возвращает не менее 1
  </acceptance_criteria>
  <done>Все elevation-rgba заменены на --tr-elev-*; ни один fallback rgba не остаётся в ui/src.</done>
</task>

<task type="auto">
  <name>Task 3: Финальный полный гейт + pnpm lint/svelte-check/build + SUMMARY handoff</name>
  <files>нет новых файлов кода — только запуск проверок; точечные фиксы при необходимости; SUMMARY</files>
  <read_first>
    ui/scripts/check-tokens.mjs (Правило 4 из плана 23-07 теперь в дефолте); 23-VERIFICATION.md
    Gaps (сверка после прогона); interfaces выше — Button.svelte visual-touch note для SUMMARY.
  </read_first>
  <action>
    Запустить node ui/scripts/check-tokens.mjs без флага (все 4 правила). При нарушениях — найти
    пропущенный сайт, применить ту же логику Task 1/2 (danger-ring/overlay/elevation по роли),
    повторить до чистого результата. Затем cd ui && pnpm lint и pnpm svelte-check — оба зелёные
    (baseline из 23-06 не меняется). Затем cd ui && pnpm build — успешная сборка.

    В 23-08-SUMMARY ОБЯЗАТЕЛЬНО добавить явную запись handoff для фазы 24: «Button.svelte получил
    минимальный визуальный touch в фазе 23 (danger-ring alpha 0.3→0.2 при миграции на
    --tr-danger-ring, WR-01-санкционировано); это НЕ layout/НЕ API/НЕ поведение, но фаза 24
    (визуальный редизайн Button) должна знать, что файл уже тронут в фазе токенов». Это отвечает на
    резервирование Button за фазой 24 в CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui && node scripts/check-tokens.mjs && pnpm lint && pnpm svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - node scripts/check-tokens.mjs завершается exit-кодом 0, "PASS — 0 нарушений" (все 4 правила)
    - pnpm lint завершается exit-кодом 0
    - pnpm svelte-check завершается exit-кодом 0 (0 errors, pre-existing warnings допустимы)
    - pnpm build завершается exit-кодом 0
    - 23-08-SUMMARY.md содержит явную строку о Button.svelte visual-touch handoff для фазы 24
  </acceptance_criteria>
  <done>check-tokens.mjs (все 4 правила) зелёный на всём ui/src; pnpm lint/svelte-check/build зелёные; Button.svelte touch задокументирован для фазы 24.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Нет новой границы доверия | Точечная замена CSS-литералов на уже определённые токены внутри существующих компонентов; новых endpoint'ов, пользовательского ввода или зависимостей не вводится |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08-01 | Repudiation | Молчаливо забытый rgba-сайт вне известного списка | mitigate | Task 3 запускает полный check-tokens.mjs (Правило 4) на всём дереве, а не только на 14 известных файлах — ловит пропуски на стыке; Tasks 1/2 verify используют негативный grep (нонзеро-exit при остатке), не echo-заглушку |
| T-23-08-SC | Tampering (supply chain) | npm installs | accept | Ноль новых npm/cargo-зависимостей |
</threat_model>

<verification>
node ui/scripts/check-tokens.mjs (все 4 правила) — exit 0, "PASS — 0 нарушений" на всём ui/src.
pnpm lint/svelte-check/build — зелёные. Verify-блоки Task 1/2 используют негативный grep
(`! grep -rq ...`), поэтому пропущенный сайт даёт нонзеро-exit, а не молчаливый проход.
</verification>

<success_criteria>
DS-01 буквально закрыт: захардкоженных цветов (hex И rgba/hsl) в компонентах не остаётся;
постоянный гейт фазы (check-tokens.mjs, все 4 правила) впервые проходит на 0 нарушений;
pnpm lint/svelte-check/build остаются зелёными; Button.svelte visual-touch явно передан фазе 24
через SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/23-design-tokens-foundations/23-08-SUMMARY.md` when done
</output>
