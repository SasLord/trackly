---
phase: 23-design-tokens-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/styles/_tokens.scss
  - ui/src/styles/global.scss
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04]

must_haves:
  truths:
    - "_tokens.scss не содержит ни одного custom property со старым именем (--color-*, --space-*, --radius-*, --font-size-*, --font-weight-*, --line-height-*, --shadow-*), кроме layout-констант (D-01)"
    - "Layout-константы (--sidebar-width, --header-height, --modal-max-width, --modal-max-width-wide, --touch-target-min, --row-height, --row-height-dense) присутствуют в новом файле с прежними значениями, без переименования (D-02)"
    - "--shadow-elev-2-dark отсутствует в новом файле (D-03)"
    - "global.scss ссылается только на --tr-* токены; форма и охват правила *:focus-visible не изменены, поменялось только имя custom property внутри (D-05)"
    - "Новые значения (в т.ч. инверсия поверхностей --tr-bg/--tr-surface) включены сразу, не за флагом (D-10)"
    - "Класс .tr-mono определён в global.scss рядом с .skip-link, тем же плоским паттерном (D-12)"
    - "Типографика имеет и composite shorthand (--tr-text-{role}), и decomposed оси (--tr-font-size-{role}/--tr-font-weight-{role}/--tr-line-height-{role}) для всех 9 ролей + mono (D-14)"
  artifacts:
    - path: "ui/src/styles/_tokens.scss"
      provides: "Единственный источник --tr-* токенов (цвет light+dark, spacing 11 уровней, radius 5, elevation 5, типографика 9 ролей+mono, layout-константы)"
    - path: "ui/src/styles/global.scss"
      provides: "body/focus-ring/scrollbar/.skip-link на --tr-*, плюс новый .tr-mono"
  key_links:
    - from: "ui/src/styles/global.scss"
      to: "ui/src/styles/_tokens.scss"
      via: "@use './tokens' (единственная точка подключения, не меняется)"
      pattern: "@use '\\./tokens'"
---

<objective>
Переписать `ui/src/styles/_tokens.scss` с нуля в единый слой `--tr-*` (D-01) и мигрировать
`ui/src/styles/global.scss` по карте (D-05), добавив глобальный класс `.tr-mono` (D-12). Это
фундамент фазы — все остальные планы (цвет, space/radius, типографика, гейты) зависят от этого
плана, потому что читают/сверяются с именами и значениями, которые он вводит. Разбиение всей фазы
на планы по семействам токенов (а не по каталогам/экранам) — D-06: один план = одно правило миграции
= один способ автоматической проверки.

Purpose: без этого плана ни один call-site в `ui/src` не имеет корректной цели для миграции —
это Wave 1, план 1 из 2 (второй — гейт-скрипты, план 23-02, параллельно).
Output: `_tokens.scss` (только `--tr-*` + неизменные layout-константы) и `global.scss` (мигрирован
+ `.tr-mono`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-design-tokens-foundations/23-UI-SPEC.md
@.planning/phases/23-design-tokens-foundations/23-CONTEXT.md
@.planning/phases/23-design-tokens-foundations/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать _tokens.scss в единый слой --tr-*</name>
  <files>ui/src/styles/_tokens.scss</files>
  <read_first>
    ui/src/styles/_tokens.scss (текущее состояние — 89 строк, единственный эталон старых
    значений/структуры блоков :root/[data-theme]; сносится целиком по D-01, кроме layout-констант);
    .planning/phases/23-design-tokens-foundations/23-UI-SPEC.md §Color Tokens (light+dark таблицы:
    Brand/Accent, Surfaces, Text, Border, Semantic, Table row states, Neutral ramp, Elevation),
    §Typography Scale (9 ролей + font-weight/line-height таблицы), §Spacing Scale, §Radii,
    §Layout constants — источник истины по ВСЕМ значениям, копировать дословно, не пересчитывать
  </read_first>
  <action>
    Полностью заменить содержимое файла. Новая структура (сохраняя механику `:root,
    [data-theme='light'] { … }` / `[data-theme='dark'] { … }` / `color-scheme` — не менять саму
    механику переключения темы, только содержимое):

    1. Блок `:root, [data-theme='light']` + `[data-theme='dark']` — цветовые custom properties.
    Определить в light и в dark вариантах ровно: `--tr-accent`, `--tr-accent-hover`,
    `--tr-accent-active`, `--tr-accent-soft`, `--tr-on-accent`, `--tr-focus-ring` (Brand/Accent);
    `--tr-bg`, `--tr-surface`, `--tr-surface-raised`, `--tr-surface-sunken`, `--tr-overlay`
    (Surfaces); `--tr-text-primary`, `--tr-text-secondary`, `--tr-text-tertiary`,
    `--tr-text-disabled`, `--tr-text-inverse` (Text, 5 уровней); `--tr-border`, `--tr-border-strong`
    (Border); `--tr-success`/`--tr-success-soft`/`--tr-success-text`,
    `--tr-warning`/`--tr-warning-soft`/`--tr-warning-text`,
    `--tr-danger`/`--tr-danger-soft`/`--tr-danger-text`, `--tr-info`/`--tr-info-soft`/`--tr-info-text`
    (Semantic, 4 роли × 3); `--tr-row-hover`, `--tr-row-selected` (Table row states); `--tr-n-0`
    … `--tr-n-950` (Neutral ramp, 12 шагов — значения ОДИНАКОВЫ в обеих темах, но определить их
    внутри обоих блоков light/dark, как остальные, для консистентности структуры файла).
    Значения — дословно из UI-SPEC §Color Tokens (не пересчитывать).

    2. `--tr-elev-0` … `--tr-elev-4` (5 уровней) — тоже theme-scoped (разные тени light/dark), как
    цвет. Значения дословно из UI-SPEC §Elevation. Это заменяет старые `--shadow-elev-1`,
    `--shadow-elev-2` целиком (новых имён 5 вместо старых 2). `--shadow-elev-2-dark` НЕ переносится
    (D-03, мёртвый код, 0 call sites подтверждено).

    3. Блок `:root` (не theme-scoped) — spacing: `--tr-space-3xs` (2px), `--tr-space-2xs` (4px),
    `--tr-space-xs` (8px), `--tr-space-sm` (12px), `--tr-space-md` (16px), `--tr-space-lg` (20px),
    `--tr-space-xl` (24px), `--tr-space-2xl` (32px), `--tr-space-3xl` (40px), `--tr-space-4xl`
    (48px), `--tr-space-5xl` (64px) — 11 уровней, значения из UI-SPEC §Spacing Scale (включая
    4 новых промежуточных уровня, не только value-map старых 7).

    4. Layout-константы — скопировать блок ДОСЛОВНО из текущего файла без изменений (D-02):
    `--sidebar-width: 240px`, `--header-height: 56px`, `--modal-max-width: 640px`,
    `--modal-max-width-wide: 960px`, `--touch-target-min: 36px`, `--row-height: 40px`,
    `--row-height-dense: 32px`.

    5. Radius: `--tr-radius-xs` (4px), `--tr-radius-sm` (6px), `--tr-radius-md` (8px),
    `--tr-radius-lg` (12px), `--tr-radius-full` (999px) — 5 уровней из UI-SPEC §Radii.

    6. Типографика — `--tr-font-family` (переименованный `--font-family-base`, значение без
    изменений) + `--tr-font-mono` (font stack: ui-monospace,'SF Mono','Segoe UI Mono',Menlo,Consolas,
    monospace — из UI-SPEC §Typography, строка Моноширинный). Для каждой из 9 ролей (display, h2,
    h3, subtitle, body, body-strong, label, caption, micro) + mono — ОБЕ формы: (a) composite
    shorthand `--tr-text-{role}` в формате `{weight} {size}px/{line-height} var(--tr-font-family)`
    (пример из UI-SPEC: `--tr-text-body: 400 14px/1.5 var(--tr-font-family);`); (b) decomposed оси
    `--tr-font-size-{role}`, `--tr-font-weight-{role}`, `--tr-line-height-{role}` с теми же тремя
    значениями раздельно. Значения size/weight/line-height для каждой роли — дословно из UI-SPEC
    §Typography Scale таблицы (Дисплей 30/600/1.15, H2 24/600/1.25, H3 20/600/1.3, Подзаголовок
    16/600/1.4, Текст 14/400/1.5, Текст-акцент 14/600/1.5, Подпись 13/500/1.4, Мелкий 12/500/1.35,
    Микро 11/500/1.3, Моноширинный 13/500 + `font-variant-numeric: tabular-nums` отдельно в
    `.tr-mono`-классе, не в самом токене). Также перенести (переименовать без изменения значения)
    `--font-weight-regular`→`--tr-font-weight-regular` (400), `--font-weight-medium`→
    `--tr-font-weight-medium` (500), `--font-weight-semibold`→`--tr-font-weight-semibold` (600) —
    эти три оси общие для нескольких ролей, не дублировать их под именами ролей.

    7. Обновить шапку-комментарий файла (сейчас "Design tokens — Phase 2. Full palette (light +
    dark), spacing, layout, typography. Auto-prepended into every &lt;style lang="scss"&gt; block via
    vite.config.ts prependData." — ВТОРАЯ строка сейчас фактически неверна, токены НЕ идут через
    prependData, см. vite.config.ts:11-15 и global.scss:1-6). Новый комментарий должен отражать
    актуальную механику (`@use './tokens'` из global.scss, единственная точка подключения) и
    ссылаться на `23-UI-SPEC.md` как источник значений.
  </action>
  <verify>
    <automated>grep -cE -- '--(color|space|radius|font-size|font-weight|line-height|shadow)-[a-z0-9-]+' ui/src/styles/_tokens.scss</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE -- '--(color|space|radius|font-size|font-weight|line-height|shadow)-[a-z0-9-]+' ui/src/styles/_tokens.scss` возвращает 0 (кроме layout-констант, которые не матчатся этим паттерном — подтверждено research)
    - `grep -c -- '--shadow-elev-2-dark' ui/src/styles/_tokens.scss` возвращает 0
    - `grep -c -- '--sidebar-width: 240px' ui/src/styles/_tokens.scss` возвращает 1 (layout-константа сохранена дословно)
    - `grep -c -- '--tr-bg: #eef1f6' ui/src/styles/_tokens.scss` возвращает 1 (light-тема, инверсия поверхностей включена)
    - `grep -c -- '--tr-text-mono' ui/src/styles/_tokens.scss` возвращает не менее 1 (mono роль определена)
    - `grep -cE -- '--tr-font-size-[a-z0-9-]+' ui/src/styles/_tokens.scss` возвращает не менее 9 (decomposed size-ось на каждую из 9 ролей)
  </acceptance_criteria>
  <done>Файл переписан целиком, содержит только --tr-* + неизменные layout-константы, оба варианта темы (light/dark) присутствуют для theme-scoped токенов.</done>
</task>

<task type="auto">
  <name>Task 2: Мигрировать global.scss по карте + добавить .tr-mono</name>
  <files>ui/src/styles/global.scss</files>
  <read_first>
    ui/src/styles/global.scss (текущее состояние — 103 строки, единственная точка подключения
    токенов `@use './tokens'` на строке 7, использует старые имена на строках 29-33, 42, 75-79,
    96, 100); ui/src/styles/_tokens.scss (уже переписан этим планом Task 1 — новые имена, на
    которые нужно переключить global.scss); .planning/phases/23-design-tokens-foundations/23-CONTEXT.md
    решение D-05 (точная карта имён для этого файла)
  </read_first>
  <action>
    Применить дословную карту имён из D-05 построчно: `var(--color-bg)` (строка 29) →
    `var(--tr-bg)`; `var(--color-text-primary)` (30) → `var(--tr-text-primary)`;
    `var(--font-family-base)` (31) → `var(--tr-font-family)`; `var(--font-size-body)` (32) →
    `var(--tr-font-size-body)`; `var(--line-height-body)` (33) → `var(--tr-line-height-body)`;
    `var(--color-accent-focus)` (42, внутри `*:focus-visible`) → `var(--tr-focus-ring)` — форма
    правила (`box-shadow: 0 0 0 3px …`) и охват (`*:focus-visible`) НЕ меняются, только имя токена
    внутри; `var(--space-md)` (75, внутри `.skip-link:focus`) → `var(--tr-space-md)`;
    `var(--color-accent)` (76) → `var(--tr-accent)`; `var(--color-text-inverse)` (77) →
    `var(--tr-text-inverse)`; `var(--font-size-body)` (78) → `var(--tr-font-size-body)`;
    `var(--font-weight-medium)` (79) → `var(--tr-font-weight-medium)`;
    `var(--color-border-strong)` (96, скроллбар) → `var(--tr-border-strong)`;
    `var(--color-text-muted)` (100, скроллбар hover) → `var(--tr-text-tertiary)`.

    Добавить новый глобальный класс `.tr-mono` сразу после блока `.skip-link` (строки 60-82),
    той же плоской структурой верхнего уровня (не вложенный, не scoped): свойства
    `font: var(--tr-text-mono);` и `font-variant-numeric: tabular-nums;` (D-12).

    Обновить комментарий над `@use './tokens'` (строки 1-6) — сохранить суть объяснения (почему
    НЕ `scss.prependData`: svelte хэширует `:root` в scoped-блоках), актуализировать формулировку
    под новую реальность `--tr-*`-слоя, если старая формулировка ссылается на устаревшие имена.

    Не менять: box-sizing reset, `@media (prefers-reduced-motion: reduce)` блок, структуру
    `.skip-link` (кроме имён токенов внутри), порядок подключения `@use './tokens'`.
  </action>
  <verify>
    <automated>grep -cE -- '--(color|space|radius|font-size|font-weight|line-height|shadow)-[a-z0-9-]+' ui/src/styles/global.scss</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE -- '--(color|space|radius|font-size|font-weight|line-height|shadow)-[a-z0-9-]+' ui/src/styles/global.scss` возвращает 0
    - `grep -c "@use './tokens'" ui/src/styles/global.scss` возвращает 1 (точка подключения не изменилась)
    - `grep -c '.tr-mono' ui/src/styles/global.scss` возвращает не менее 1
    - `grep -c '\*:focus-visible' ui/src/styles/global.scss` возвращает 1 (охват правила не изменён)
    - `cd ui && pnpm svelte-check` завершается без новых ошибок (0 errors baseline)
  </acceptance_criteria>
  <done>global.scss ссылается только на --tr-* токены; .tr-mono определён; svelte-check чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Нет новой границы доверия | Чисто CSS custom properties + один глобальный CSS-класс; ни пользовательский ввод, ни сетевые данные, ни auth-логика не затрагиваются |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering | `_tokens.scss` значения | accept | Все значения копируются дословно из утверждённого (`status: approved`) `23-UI-SPEC.md`; нет пользовательского ввода, нет динамического вычисления — риск фальсификации значения отсутствует |
| T-23-01-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Ноль новых зависимостей в этом плане (чистый CSS/SCSS файл) — Package Legitimacy Gate неприменим |
</threat_model>

<verification>
Оба файла (`_tokens.scss`, `global.scss`) не содержат старых имён семейств (кроме layout-констант).
`pnpm svelte-check` остаётся на чистом baseline (0 errors). Полный греп-гейт (`check-tokens.mjs`)
ещё не существует на момент этого плана (создаётся параллельным планом 23-02) — не запускать его
здесь.
</verification>

<success_criteria>
`_tokens.scss` содержит только `--tr-*` + неизменные layout-константы. `global.scss` мигрирован
по карте D-05, включает `.tr-mono`. `pnpm svelte-check` чист. Оба файла готовы как источник имён
для всех последующих sweep-планов (23-03..23-05).
</success_criteria>

<output>
Create `.planning/phases/23-design-tokens-foundations/23-01-SUMMARY.md` when done
</output>
