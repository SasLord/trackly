---
phase: 23-design-tokens-foundations
plan: 06
type: execute
wave: 5
depends_on: ["23-01", "23-02", "23-03", "23-04", "23-05"]
files_modified:
  - "ui/src/**/*.svelte, ui/src/**/*.scss (только точечные остаточные фиксы, если check-tokens.mjs находит что-то пропущенное — см. Task 1)"
autonomous: true
requirements: [DS-01, DS-02, DS-03, DS-04, QA-01]

must_haves:
  truths:
    - "node ui/scripts/check-tokens.mjs (все 3 правила, без --rules флага) завершается exit-кодом 0 на всём ui/src"
    - "node ui/scripts/verify-value-map.mjs подтверждает 0 нарушений на полном диапазоне space/radius-миграции (планы 23-04)"
    - "pnpm svelte-check остаётся на чистом baseline (0 errors)"
    - "pnpm lint (eslint+prettier+check-tokens.mjs) завершается exit-кодом 0 — впервые за фазу, весь sweep завершён и D-15 закрыт"
    - "git grep подтверждает все известные QA-01 сайты (font-size-sm, radius-lg×4, shadow-md×3) резолвятся в --tr-*, ни один старый undefined-token баг не остался"
    - "Явный чек-лист НЕ автоматически проверяемых пунктов (DS-02 визуал, DS-04 визуал, DS-03 визуальная иерархия, D-16 toast-mono grey area) зафиксирован в SUMMARY.md для передачи /gsd-verify-work — не молчаливо считается done (D-09)"
  artifacts:
    - path: ".planning/phases/23-design-tokens-foundations/23-06-SUMMARY.md"
      provides: "Финальный отчёт фазы + явный hand-off чек-лист не автоматически проверяемых пунктов для UAT"
  key_links: []
---

<objective>
Закрыть цикл проверки фазы: прогнать полный `check-tokens.mjs` (все 3 правила) и
`verify-value-map.mjs` на итоговом состоянии `ui/src`, зафиксировать любые остаточные пропуски,
подтвердить `pnpm svelte-check`/`pnpm lint` зелёными, и явно передать пользователю (через
`/gsd-verify-work`) список того, что физически недоказуемо без браузера (D-09).

Purpose: ни один из шести планов фазы по отдельности не даёт гарантии, что швы между ними (файлы,
задетые несколькими sweep-планами последовательно) не оставили пропуска — только финальный
whole-tree прогон это доказывает. Также это единственная точка, где `pnpm lint` целиком имеет
смысл использовать как gate (после D-15 в 23-02 И после того, как check-tokens.mjs больше не
находит домиграционных нарушений).
Output: подтверждённо зелёные все статические гейты фазы + явный non-automatable чек-лист в
SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-design-tokens-foundations/23-UI-SPEC.md
@.planning/phases/23-design-tokens-foundations/23-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Полный check-tokens.mjs + verify-value-map.mjs прогон, закрытие остаточных пропусков</name>
  <files>ui/src/**/*.svelte, ui/src/**/*.scss (точечные фиксы при необходимости)</files>
  <read_first>
    ui/scripts/check-tokens.mjs, ui/scripts/verify-value-map.mjs (созданы планом 23-02 — читать
    перед запуском, чтобы понимать вывод); .planning/phases/23-design-tokens-foundations/23-UI-SPEC.md
    §Verification Checklist (5 пунктов, привязанных к DS-01..04/QA-01)
  </read_first>
  <action>
    Запустить `node ui/scripts/check-tokens.mjs` без флага `--rules` (все 3 правила: old-name /
    hex-in-style / closed-world). Если найдены нарушения — это означает, что один из планов
    23-01..23-05 пропустил call-site (файл, изменённый несколькими sweep-планами последовательно,
    мог получить неполное покрытие на стыке). Для каждого найденного нарушения: определить
    корректную цель по той же логике, что использовал соответствующий sweep-план (цвет — по роли
    из UI-SPEC §Color migration table; space/radius — по значению из §Spacing Scale/§Radii;
    типографика — по роли из §Typography Scale), внести точечный фикс, повторить запуск до
    чистого результата.

    Запустить `node ui/scripts/verify-value-map.mjs <SHA-до-плана-23-04>`. **Источник SHA:**
    строка `BASE_SHA (до правок space/radius): <sha>` в `23-04-SUMMARY.md` (Task 1 плана 23-04
    обязан её записать). Если строки нет — реконструировать: SHA коммита непосредственно перед
    первым коммитом плана 23-04 (`git log --oneline --grep '23-04' | tail -1`, взять его родителя),
    и записать находку в собственный SUMMARY. Если найдены новые расхождения из-за точечных фиксов
    этой задачи — зафиксировать и устранить.

    Выполнить `git grep -n -- '--font-size-sm\|--radius-lg\b\|--shadow-md' ui/src` (без `--tr-`
    префикса — ищем именно СТАРЫЕ незамещённые имена) — ожидаемый результат: 0 совпадений. Если
    что-то найдено — это прямой QA-01 регресс, исправить немедленно.
  </action>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node ui/scripts/check-tokens.mjs` завершается exit-кодом 0, "PASS — 0 нарушений"
    - `node ui/scripts/verify-value-map.mjs <SHA-до-плана-23-04>` завершается exit-кодом 0
    - `git grep -c -- '--font-size-sm\|--radius-lg\b\|--shadow-md' ui/src | grep -v ':0'` возвращает пусто (0 файлов со старыми undefined-именами)
    - `git grep -c -- '--tr-font-size-caption' ui/src/lib/components/PersonAutocomplete.svelte` возвращает не менее 1
  </acceptance_criteria>
  <done>check-tokens.mjs полностью зелёный на всём дереве (все 3 правила); verify-value-map.mjs подтверждает 0 нарушений; все известные QA-01 баги (включая bonus-находку --shadow-md) закрыты без остатка.</done>
</task>

<task type="auto">
  <name>Task 2: pnpm lint/svelte-check зелёные + hand-off чек-лист для UAT</name>
  <files>нет новых изменений кода — только запуск проверок и составление отчёта</files>
  <read_first>
    .planning/phases/23-design-tokens-foundations/23-VALIDATION.md §Manual-Only Verifications
    (4 пункта — источник формулировок для hand-off чек-листа); .planning/phases/23-design-tokens-foundations/23-CONTEXT.md
    решение D-09 (гоча про pnpm --dir ui build перед LAN-браузер UAT) и D-16 (toast-mono grey area
    формулировка, чтобы не переизобретать текст)
  </read_first>
  <action>
    Запустить `cd ui && pnpm svelte-check` — ожидается чистый результат (0 errors, warnings
    допустимы как pre-existing baseline). Запустить `cd ui && pnpm lint` (eslint+prettier+
    check-tokens.mjs, все три части) — впервые за фазу ожидается ПОЛНОСТЬЮ зелёный результат
    (0 exit code): eslint чист благодаря D-15 (план 23-02), prettier не тронут этой фазой
    (форматирование не менялось намеренно за пределами замены токен-имён — если prettier находит
    несоответствия, прогнать `pnpm prettier --write .` и подтвердить, что изменения ограничены
    пробельным форматированием, не логикой), check-tokens.mjs чист благодаря Task 1.

    Составить явный hand-off чек-лист (4 пункта, дословно из VALIDATION.md §Manual-Only
    Verifications) и включить его в `23-06-SUMMARY.md`: (1) переключение темы без артефактов —
    обе темы, минимум 3-4 плотных экрана (Устройства, форма акта, Настройки), гоча — нужен `pnpm
    --dir ui build` перед проверкой через LAN-браузер, `cargo tauri dev` хотрелоадит только
    desktop-webview; (2) отсутствие сдвига вёрстки после space/radius миграции «на глаз» —
    инверсия поверхностей (`--tr-bg` #eef1f6, `--tr-surface` #ffffff) это НАМЕРЕННОЕ изменение
    (D-10), не дефект миграции; (3) визуальная иерархия типографики «на правильном уровне» для
    каждого текстового блока; (4) точечная потеря контраста от инверсии поверхностей
    (белое-на-белом) — чинится через `--tr-border`/`--tr-elev-1`/`--tr-surface-sunken` (D-11), не
    относится к этой фазе как обязательство, но полезно как ожидание для UAT.
  </action>
  <verify>
    <automated>cd ui && pnpm svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && pnpm svelte-check` завершается exit-кодом 0
    - `cd ui && pnpm lint` завершается exit-кодом 0
    - `.planning/phases/23-design-tokens-foundations/23-06-SUMMARY.md` содержит секцию с 4 явно перечисленными non-automatable пунктами (не менее 4 строк, каждая с конкретной инструкцией по проверке)
  </acceptance_criteria>
  <done>pnpm svelte-check и pnpm lint оба зелёные впервые за фазу; hand-off чек-лист задокументирован в SUMMARY.md для передачи /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Нет новой границы доверия | Финальная верификация — прогон существующих статических инструментов, без нового кода за пределами точечных остаточных фиксов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Repudiation | Незадокументированный «молчаливый» пропуск непроверяемых визуальных пунктов | mitigate | Явный hand-off чек-лист в SUMMARY.md — предотвращает ситуацию, когда DS-02/визуальная часть DS-03/DS-04 считается done без реальной проверки (D-09) |
| T-23-06-SC | Tampering (supply chain) | npm installs | accept | Ноль новых зависимостей во всей фазе 23 |
</threat_model>

<verification>
Все статические гейты фазы (check-tokens.mjs все 3 правила, verify-value-map.mjs, svelte-check,
pnpm lint) зелёные одновременно — первый и единственный раз за фазу, что достижимо только после
завершения всех пяти предыдущих планов. Явный hand-off чек-лист передан для UAT.
</verification>

<success_criteria>
Фаза 23 завершена: DS-01/DS-02/DS-03/DS-04/QA-01 закрыты в статически проверяемой части; ни один
из 5 требований не остался без покрытия хотя бы в одном плане; непроверяемые вручную пункты явно
переданы `/gsd-verify-work`, а не молчаливо посчитаны выполненными.
</success_criteria>

<output>
Create `.planning/phases/23-design-tokens-foundations/23-06-SUMMARY.md` when done
</output>
