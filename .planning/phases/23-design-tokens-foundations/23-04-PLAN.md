---
phase: 23-design-tokens-foundations
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-02", "23-03"]
files_modified:
  - "ui/src/**/*.svelte (все файлы, использующие --space-*/--radius-* — точный список через git grep, см. Task 1 action)"
  - "ui/src/**/*.scss (кроме _tokens.scss/global.scss)"
autonomous: true
requirements: [DS-04, QA-01]

must_haves:
  truths:
    - "Ни один .svelte/.scss файл в ui/src (кроме _tokens.scss/global.scss) не содержит var(--space-*) или var(--radius-*) — миграция строго по значению, значения НЕ меняются (D-04's DS-04 hard no-shift constraint)"
    - "Каждая замена space/radius доказана value-preserving скриптом verify-value-map.mjs (D-08), с единственным разрешённым исключением — split --radius-sm (D-07)"
    - "--radius-sm получает --tr-radius-sm (6px) ТОЛЬКО в 4 файлах allowlist (Button.svelte, Input.svelte, Select.svelte, Textarea.svelte); все остальные call-sites (включая спорные кнопкоподобные) получают --tr-radius-xs (4px), без исключений (D-07)"
    - "--radius-lg (4 сайта: LoginPage.svelte:175, BlockedScreen.svelte:125, FirstRunWizard.svelte:190, PendingScreen.svelte:37) резолвится в --tr-radius-lg (12px) — QA-01 fix"
    - "Этот план мигрирует ровно ОДНО семейство токенов (space + radius) по ОДНОМУ правилу (по значению) и проверяется одним способом (verify-value-map.mjs); цвет/типографика в тех же файлах не тронуты — они принадлежат планам 23-03/23-05 (D-06)"
    - "Базовый SHA до правок зафиксирован в 23-04-SUMMARY.md — план 23-06 читает его оттуда, а не реконструирует из git-истории"
  artifacts: []
  key_links:
    - from: "ui/src/**/*.svelte <style> блоки"
      to: "ui/src/styles/_tokens.scss"
      via: "var(--tr-space-*)/var(--tr-radius-*) ссылки"
      pattern: "var\\(--tr-(space|radius)-"
---

<objective>
Мигрировать ВСЕ call-sites `--space-*`/`--radius-*` на `--tr-space-*`/`--tr-radius-*` — **по
значению, значения НЕ меняются** (правило 2 из трёх, UI-SPEC §The Three Migration Rules), кроме
двух явных именованных исключений (radius-sm split D-07, radius-lg QA-01 fix). Это самое
рискованное семейство фазы: 651 space + 134 radius call-sites, и именно этот сдвиг REQUIREMENTS.md
называет главной ловушкой фазы («642 использования тихо поедут без единой ошибки сборки»).

Purpose: закрывает DS-04 (вёрстка не сдвигается) и часть QA-01 (`--radius-lg` undefined bug на
4 auth-экранах).
Output: все call-sites space/radius переведены на новую value-matched шкалу; `verify-value-map.mjs`
подтверждает 0 нарушений на git-диффе этого плана.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-design-tokens-foundations/23-UI-SPEC.md
@.planning/phases/23-design-tokens-foundations/23-RESEARCH.md
@.planning/phases/23-design-tokens-foundations/23-PATTERNS.md
</context>

<interfaces>
<!-- Ordered-карта Task 1 (пространственные значения — коллизий подстрок между разными суффиксами
     нет, порядок не критичен между записями 1-7, но radius-sm намеренно исключён — обрабатывается
     отдельно в Task 2). -->

1. `--space-xs` → `--tr-space-2xs` (4px)
2. `--space-sm` → `--tr-space-xs` (8px)
3. `--space-md` → `--tr-space-md` (16px, имя не меняется, но токен всё равно переименован в --tr-*-неймспейс)
4. `--space-lg` → `--tr-space-xl` (24px)
5. `--space-xl` → `--tr-space-2xl` (32px)
6. `--space-2xl` → `--tr-space-4xl` (48px)
7. `--space-3xl` → `--tr-space-5xl` (64px)
8. `--radius-md` → `--tr-radius-md` (8px, значение не меняется)
9. `--radius-lg` → `--tr-radius-lg` (12px — QA-01 fix, было undefined; 4 сайта: LoginPage.svelte:175, BlockedScreen.svelte:125, FirstRunWizard.svelte:190, PendingScreen.svelte:37)

`--radius-sm` НЕ входит в эту карту — обрабатывается отдельно в Task 2 (файл-зависимое ветвление,
D-07).

Radius-sm allowlist (ровно 4 файла → `--tr-radius-sm` 6px, по 1 сайту каждый, подтверждено прямым
grep): `ui/src/lib/components/Button.svelte:42`, `ui/src/lib/components/Input.svelte:50`,
`ui/src/lib/components/Select.svelte:69`, `ui/src/lib/components/Textarea.svelte:47`. Все
остальные ~102 сайта `var(--radius-sm)` в остальных файлах → `--tr-radius-xs` (4px) — включая
визуально похожие на кнопки/поля элементы вне этого списка (D-07/D-17 явно закрывают этот вопрос,
не открытый). `Checkbox.svelte` не существует в кодовой базе на момент этой фазы (строится Phase
24) — пункт allowlist для него пустой набор, действий не требуется.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Литеральный sweep --space-*/--radius-md/--radius-lg по value-map</name>
  <files>ui/src/**/*.svelte, ui/src/**/*.scss (кроме _tokens.scss/global.scss)</files>
  <read_first>
    .planning/phases/23-design-tokens-foundations/23-UI-SPEC.md §Spacing Scale и §Radii (значение
    таблиц — источник соответствий); .planning/phases/23-design-tokens-foundations/23-PATTERNS.md
    Категория 3 Аналог A (`ActItemsTable.svelte:46-107` — конкретный пример смешанного
    space+radius в одном &lt;style&gt;-блоке, показывает, что radius-sm ТАМ не входит в allowlist →
    идёт в --tr-radius-xs); ui/src/styles/_tokens.scss (переписан планом 23-01 — 11-уровневая
    space-шкала и 5 radius уже определены)
  </read_first>
  <action>
    Захватить базовый SHA перед началом правок этой задачи: `git rev-parse HEAD`. **Записать его
    дословно в `23-04-SUMMARY.md`** (секция Decisions Made, строка вида
    `BASE_SHA (до правок space/radius): <sha>`) — он нужен дважды: Task 2 этого плана и Task 1
    плана 23-06, который выполняется в ОТДЕЛЬНОЙ сессии и не увидит контекст этого плана. Без
    записи в SUMMARY исполнитель 23-06 будет вынужден реконструировать SHA из git-истории
    (коммит непосредственно перед первым коммитом 23-04) — это выполнимо, но хрупко.

    Получить точный список файлов на момент выполнения: `git grep -l -E -- '--space-[a-z0-9]+|
    --radius-(md|lg)\b' ui/src` (--radius-sm сюда НЕ включать этим паттерном — исключить явно,
    обрабатывается в Task 2).

    Применить 9-пунктовую ordered-карту из блока `<interfaces>` выше литеральной заменой ко всем
    найденным файлам — каждая замена ЗНАЧЕНИЕ-СОХРАНЯЮЩАЯ (кроме пункта 9, `--radius-lg`, который
    чинит QA-01 undefined-баг, а не сдвигает существующее значение). НЕ трогать `--radius-sm` в
    этой задаче ни в одном файле — это Task 2. НЕ трогать цветовые/typography-токены в тех же
    файлах (уже сделано в 23-03 / будет сделано в 23-05).

    **Чанковать применение карты пакетами по 20–30 файлов**, между пакетами прогонять
    `node ui/scripts/check-tokens.mjs --rules=1` и коммитить. Задача покрывает ~106 файлов — это
    осознанно (D-06: один план = одно правило миграции), но одним заходом её выполнять нельзя.
    Здесь цена ошибки выше, чем в 23-03/23-05: замены обязаны быть ЗНАЧЕНИЕ-СОХРАНЯЮЩИМИ, а
    несохраняющая замена не даёт ни одной ошибки сборки — она просто тихо сдвигает вёрстку.
    Промежуточные коммиты также дают `verify-value-map.mjs` осмысленные границы диффа.
  </action>
  <verify>
    <automated>git grep -c -- 'var(--space-' ui/src -- '*.svelte' '*.scss' | grep -v ':0'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c -- 'var(--space-' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто
    - `git grep -c -- 'var(--radius-md)' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто
    - `git grep -c -- 'var(--radius-lg)' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто
    - `git grep -l -- 'var(--radius-sm)' ui/src` по-прежнему находит ~102-106 сайтов (radius-sm намеренно не тронут в этой задаче)
    - `git grep -c -- '--tr-radius-lg' ui/src/features/auth/LoginPage.svelte ui/src/features/auth/BlockedScreen.svelte ui/src/features/auth/FirstRunWizard.svelte ui/src/features/auth/PendingScreen.svelte` возвращает 1 в каждом файле
    - `23-04-SUMMARY.md` содержит строку `BASE_SHA (до правок space/radius): <40-символьный sha>` — значение читается планом 23-06 из отдельной сессии
  </acceptance_criteria>
  <done>Все --space-* и --radius-md/--radius-lg call-sites мигрированы по value-map; --radius-sm намеренно нетронут, ждёт Task 2; базовый SHA зафиксирован в SUMMARY для 23-06.</done>
</task>

<task type="auto">
  <name>Task 2: Split --radius-sm по allowlist + верификация value-map</name>
  <files>ui/src/lib/components/Button.svelte, ui/src/lib/components/Input.svelte, ui/src/lib/components/Select.svelte, ui/src/lib/components/Textarea.svelte, остальные файлы с var(--radius-sm)</files>
  <read_first>
    ui/src/lib/components/Button.svelte:42, ui/src/lib/components/Input.svelte:50,
    ui/src/lib/components/Select.svelte:69, ui/src/lib/components/Textarea.svelte:47 (по одному
    сайту `var(--radius-sm)` в каждом — allowlist на 6px); .planning/phases/23-design-tokens-foundations/23-RESEARCH.md
    §Ground-truth «--radius-sm split (D-07) — конкретный allowlist» (полный список ~55 остальных
    файлов с --radius-sm вне allowlist, включая .form-input/.form-select/.btn-* похожие-но-не-
    компоненты — все они по D-07's явной формулировке идут в --tr-radius-xs без исключений)
  </read_first>
  <action>
    В ровно 4 файлах allowlist (`Button.svelte:42`, `Input.svelte:50`, `Select.svelte:69`,
    `Textarea.svelte:47`) заменить `var(--radius-sm)` на `var(--tr-radius-sm)` (6px, НАМЕРЕННЫЙ
    сдвиг 4→6 по D-07 — это единственное объявленное исключение из value-preserving правила
    семейства).

    Во ВСЕХ остальных файлах (получить актуальный список: `git grep -l -- 'var(--radius-sm)'
    ui/src`, вычесть 4 файла allowlist) заменить `var(--radius-sm)` на `var(--tr-radius-xs)` (4px,
    value-preserving) — без per-file триажа "похоже на кнопку/не похоже": D-07 явно предписывает
    единый дефолт для всего, что не входит в явный allowlist.

    Запустить `node ui/scripts/verify-value-map.mjs <SHA-из-Task-1>` (SHA, зафиксированный в
    начале Task 1 этого плана) — скрипт должен подтвердить: каждая замена в объединённом диффе
    Task 1 + Task 2 соответствует value-map, единственное расхождение (`--radius-sm` →
    `--tr-radius-sm` в 4 файлах allowlist) распознано скриптом как разрешённое исключение, а не
    нарушение.
  </action>
  <verify>
    <automated>git grep -c -- 'var(--radius-sm)' ui/src -- '*.svelte' '*.scss' | grep -v ':0'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c -- 'var(--radius-sm)' ui/src -- '*.svelte' '*.scss' | grep -v ':0'` возвращает пусто (старое имя полностью выведено)
    - `git grep -c -- 'var(--tr-radius-sm)' ui/src/lib/components/Button.svelte ui/src/lib/components/Input.svelte ui/src/lib/components/Select.svelte ui/src/lib/components/Textarea.svelte` возвращает 1 в каждом из 4 файлов
    - `node ui/scripts/verify-value-map.mjs <SHA-из-Task-1>` завершается exit-кодом 0, "PASS — 0 нарушений"
    - `node ui/scripts/check-tokens.mjs --rules=1` больше не репортит ни одного `--space-*`/`--radius-*` нарушения
  </acceptance_criteria>
  <done>--radius-sm полностью выведен из кодовой базы; split применён строго по 4-файловому allowlist; verify-value-map.mjs подтверждает 0 нарушений на всём диффе плана.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Нет новой границы доверия | Числовые CSS-значения отступов/радиусов — статический контент стилей, не пользовательский ввод |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Tampering | Замена space/radius токенов может тихо сдвинуть вёрстку (класс риска, который весь план и существует, чтобы предотвратить) | mitigate | `verify-value-map.mjs` — автоматическое доказательство value-preserving замены на самом git-диффе плана; единственное разрешённое исключение (--radius-sm split) параметризовано явным allowlist, а не эвристикой |
| T-23-04-SC | Tampering (supply chain) | npm installs | accept | Ноль новых зависимостей |
</threat_model>

<verification>
`node ui/scripts/check-tokens.mjs --rules=1` больше не находит --space-*/--radius-* нарушений.
`node ui/scripts/verify-value-map.mjs <base-sha>` подтверждает 0 нарушений на полном диффе плана.
Визуальное «вёрстка не сдвинулась» подтверждает пользователь на UAT (D-09) — этот план даёт
логическое, а не визуальное доказательство.
</verification>

<success_criteria>
Все 651+ space call-sites и 134+ radius call-sites мигрированы по значению; единственное
исключение (radius-sm split) применено точно по 4-файловому allowlist; verify-value-map.mjs
зелёный на диффе плана. DS-04 (статически проверяемая часть) и QA-01's radius-lg fix выполнены.
</success_criteria>

<output>
Create `.planning/phases/23-design-tokens-foundations/23-04-SUMMARY.md` when done
</output>
