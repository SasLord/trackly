---
phase: 10-employee-employee-ui-role-gating-read
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-core/src/auth.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: true
requirements: [D-GATE-01, D-GATE-02, D-TEST-01]
must_haves:
  truths:
    - "Action::ReadData is no longer granted to Employee in the authorize() permission matrix"
    - "The CI role×endpoint matrix can assert on response BODY content, not just status code"
    - "Case 9 of the matrix test (previously asserting Employee->devices_list->200) now asserts 403"
  artifacts:
    - path: "crates/trackly-core/src/auth.rs"
      provides: "ReadData moved out of the always-true arm into the Admin|Manager arm"
      contains: "Action::ReadData"
    - path: "crates/trackly-app/tests/role_endpoint_matrix.rs"
      provides: "post_with_cookie_json helper + flipped Case 9 + retained-access regression case"
      contains: "post_with_cookie_json"
  key_links:
    - from: "crates/trackly-core/src/auth.rs::authorize"
      to: "Action::ReadData match arm"
      via: "matches!(identity.role, Role::Admin | Role::Manager)"
      pattern: "Action::ReadData[^}]*Role::Admin \\| Role::Manager"
---

<objective>
Fix the root-cause permission-matrix bug: `Action::ReadData` is currently granted
to ALL roles including Employee (the literal catch-all that RESEARCH.md Pitfall 1
identifies as the reason ~25 read methods across 6 services are not actually
gated, even where downstream code calls `authorize(caller, &Action::ReadData)`).
This plan also lays the Wave-0 test infrastructure every later plan's automated
verification depends on: a body-aware HTTP test helper, since the existing
`post_with_cookie` only returns `StatusCode` and cannot assert "only own requests
in the list" (D-REQ-01) or "no org-wide fields in the employee dashboard"
(D-GATE-03).

Purpose: Every later backend plan (10-02, 10-03) inserts `authorize(caller,
&Action::ReadData)` calls into HTTP/Tauri read handlers. Those calls are no-ops
until this plan's matrix fix lands — Employee would still pass. This plan MUST
execute first and in isolation so the matrix flip is bisectable and the existing
unit test (`authorize_employee_read_data_ok`, currently asserting `is_ok()`) is
updated in the same commit that breaks it.

Output: Updated `authorize()` permission matrix (Employee no longer gets
`ReadData`), updated/flipped unit test, new `post_with_cookie_json` test helper,
Case 9 flipped from 200->403, one new retained-access regression case proving
Employee's OWN `requests_list` still returns 200 (since `ReadRequests` is
unaffected by this matrix change — only `ReadData` moves).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-employee-employee-ui-role-gating-read/10-CONTEXT.md
@.planning/phases/10-employee-employee-ui-role-gating-read/10-RESEARCH.md
@.planning/phases/10-employee-employee-ui-role-gating-read/10-VALIDATION.md

<interfaces>
<!-- Current authorize() permission matrix — crates/trackly-core/src/auth.rs lines 90-157.
     Executor must edit this exact match expression; do not restructure the
     Action enum or Identity struct (D-RBAC-01 invariant: zero I/O imports in
     this module, enforced by a `no_io_deps` gate on every cargo test). -->

pub enum Action {
    ManageUsers,
    ManageSettings,
    MutateDevices,
    MutateActs,
    MutateCartridges,
    ReadData,        // <-- currently in the always-true arm; MUST move
    CreateRequest,
    MutatePrinters,
    TransitionRequests,
    ReadPrinters,
    ReadRequests,     // <-- stays always-true; this is the Employee
                      //     "see only own requests" path, scoped at the
                      //     service layer in Plan 10-03, NOT here.
}

pub fn authorize(identity: &Identity, action: &Action) -> Result<(), AppError> {
    let allowed = match action {
        Action::ManageUsers | Action::ManageSettings => {
            matches!(identity.role, Role::Admin)
        }
        Action::MutateDevices
        | Action::MutateActs
        | Action::MutateCartridges
        | Action::MutatePrinters
        | Action::TransitionRequests
        | Action::ReadPrinters => {
            matches!(identity.role, Role::Admin | Role::Manager)
        }
        Action::ReadData | Action::CreateRequest | Action::ReadRequests => true,
        //        ^^^^^^^^ MOVE this out of this arm into the Admin|Manager arm above.
    };
    if allowed { Ok(()) } else { Err(AppError::Forbidden) }
}

<!-- Existing unit test that currently asserts the WRONG (pre-fix) behavior —
     crates/trackly-core/src/auth.rs lines 266-273. Must flip is_ok() -> Err(Forbidden). -->

#[test]
fn authorize_employee_read_data_ok() {
    let id = Identity { user_id: Some(3), role: Role::Employee };
    assert!(authorize(&id, &Action::ReadData).is_ok());  // <-- flip this assertion
}

<!-- Existing test helper in role_endpoint_matrix.rs (returns StatusCode only) —
     the new helper must coexist alongside this one, NOT replace it (other
     existing cases in this file still use the status-only helper). -->

async fn post_with_cookie(
    app: axum::Router,
    uri: &str,
    body: serde_json::Value,
    cookie: Option<&str>,
) -> StatusCode {
    let mut builder = Request::builder()
        .method("POST")
        .uri(uri)
        .header("content-type", "application/json");
    if let Some(c) = cookie {
        builder = builder.header("cookie", c);
    }
    let req = builder
        .body(Body::from(serde_json::to_string(&body).unwrap()))
        .unwrap();
    let res = app.oneshot(req).await.unwrap();
    res.status()
}

<!-- Case 9 as it stands today (asserts the BUG) — must flip to FORBIDDEN. -->

{
    let status = post_with_cookie(
        new_app!(),
        "/api/v1/devices_list",
        device_list_payload.clone(),
        Some(&employee_cookie),
    )
    .await;
    assert_eq!(
        status,
        StatusCode::OK,
        "Case 9: Employee -> devices_list (read) -> expected 200, got {status}"
    );
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the authorize() permission matrix — move ReadData out of the always-true arm</name>
  <files>crates/trackly-core/src/auth.rs</files>
  <read_first>crates/trackly-core/src/auth.rs (full file already in context above — lines 90-283)</read_first>
  <behavior>
    - Test 1 (flip existing): `authorize(&Identity{role: Employee}, &Action::ReadData)` must now return `Err(AppError::Forbidden)`, not `Ok(())`.
    - Test 2 (new): `authorize(&Identity{role: Manager}, &Action::ReadData)` must return `Ok(())` (Manager keeps read access — this is a regression guard since Manager's path is otherwise untouched by this matrix edit).
    - Test 3 (unchanged, must still pass): `authorize(&Identity{role: Employee}, &Action::ReadRequests)` must still return `Ok(())` — `ReadRequests` is a SEPARATE action from `ReadData` and is NOT touched by this fix; Employee's "see my own requests" path depends on `ReadRequests` staying always-true.
    - Test 4 (unchanged, must still pass): `authorize(&Identity{role: Employee}, &Action::CreateRequest)` must still return `Ok(())`.
  </behavior>
  <action>
    In the `authorize()` match expression (around line 141-149), move `Action::ReadData`
    from its current arm (`Action::ReadData | Action::CreateRequest | Action::ReadRequests => true`)
    into the `Admin | Manager` arm that currently lists `MutateDevices | MutateActs |
    MutateCartridges | MutatePrinters | TransitionRequests | ReadPrinters`. After the
    edit, the always-true arm contains only `Action::CreateRequest | Action::ReadRequests`.
    Update the doc-comment permission-matrix table above the function (lines 121-132) to
    move the `ReadData` row from "✓ ✓ ✓" to "✓ ✓ ✗", matching the new behavior. Per D-GATE-01/D-GATE-02
    (CONTEXT.md), this single match-arm move is the root-cause fix: every downstream
    service/handler that already calls `authorize(caller, &Action::ReadData)` will start
    correctly rejecting Employee once this lands — no other file in this plan touches
    business logic.
    Then flip the existing test `authorize_employee_read_data_ok` (line 266-273): rename
    it to `authorize_employee_read_data_forbidden` and change the assertion from
    `assert!(authorize(&id, &Action::ReadData).is_ok())` to
    `assert!(matches!(authorize(&id, &Action::ReadData), Err(AppError::Forbidden)))`.
    Add a new test `authorize_manager_read_data_ok` asserting Manager still gets `Ok(())`
    for `Action::ReadData` (regression guard — Manager's read path must remain unaffected).
    Confirm (do not modify, just verify by reading) that `authorize_employee_manage_users_forbidden`
    and the `ReadRequests`/`CreateRequest` paths are untouched.
  </action>
  <verify>
    <automated>cargo test -p trackly-core auth:: -- --test-threads=1</automated>
  </verify>
  <done>authorize_employee_read_data_forbidden passes (Employee+ReadData=Forbidden); authorize_manager_read_data_ok passes (Manager+ReadData=Ok); no_io_deps gate still passes; all pre-existing auth.rs unit tests still green.</done>
  <acceptance_criteria>
    - `cargo test -p trackly-core` exits 0 with zero failures.
    - The permission-matrix doc comment table reflects ReadData=✗ for Employee.
    - No change to `Action` enum variants, `Identity` struct, or `Role` enum — only the match-arm grouping and the doc comment moved.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add body-aware HTTP test helper + flip Case 9 + add retained-access regression case</name>
  <files>crates/trackly-app/tests/role_endpoint_matrix.rs</files>
  <read_first>crates/trackly-app/tests/role_endpoint_matrix.rs (full file already in context above)</read_first>
  <behavior>
    - Test 1 (new helper, exercised via Case 9 below): `post_with_cookie_json()` returns `(StatusCode, serde_json::Value)` — the status AND the parsed JSON body (empty `json!({})` if the body fails to parse, e.g. on a 403 with an empty body).
    - Test 2 (flip Case 9): Employee session -> POST `/api/v1/devices_list` -> now expects `StatusCode::FORBIDDEN` (this is the test that currently asserts the bug — it MUST fail before Task 1's auth.rs fix lands, and pass after, proving Task 1 and Task 2 are correctly ordered/dependent within this same plan).
    - Test 3 (new regression case, Case 10): Employee session -> POST `/api/v1/requests_list` with an empty filter -> expects `StatusCode::OK` (NOT 403) — proves the `ReadData` matrix fix did not collaterally break Employee's own-requests read path, which depends on the separate `ReadRequests` action plus ownership-filtering wired in Plan 10-03 (not yet present in this plan — only the "not 403" status is assertable here; body-content assertion for "only own requests" is Plan 10-03's job once ownership scoping exists).
  </behavior>
  <action>
    Add a new helper function `post_with_cookie_json` alongside the existing
    `post_with_cookie` (do not remove or modify `post_with_cookie` — other existing
    cases in this file keep using it). Signature: takes the same four parameters
    (`app: axum::Router, uri: &str, body: serde_json::Value, cookie: Option<&str>`),
    returns `(StatusCode, serde_json::Value)`. Implementation: builds and sends the
    request identically to `post_with_cookie`, then reads the response body via
    `axum::body::to_bytes` (import from `http_body_util` or the existing axum body
    re-export already used elsewhere in this crate — check `Cargo.toml`/other test
    files for the exact import path already in use before adding a new dependency),
    parses it with `serde_json::from_slice` falling back to `serde_json::json!({})`
    on parse failure, and returns the `(status, value)` tuple.
    Update the module doc-comment test matrix list (lines 6-15) to add: "10. Employee
    session -> POST /api/v1/requests_list -> 200 OK (own-requests read retained)".
    Flip Case 9's body: change the assertion from `StatusCode::OK` to
    `StatusCode::FORBIDDEN`, and update the comment/message string from "(reads
    allowed)" to "(reads now gated — D-GATE-01/02)". Add Case 10 immediately after
    Case 9 inside the same test function `role_endpoint_matrix_test`, asserting
    Employee -> `/api/v1/requests_list` (empty filter, same `Pagination` shape as
    `device_list_payload`) -> `StatusCode::OK`, using the plain `post_with_cookie`
    helper (no body assertion needed yet in this plan).
  </action>
  <verify>
    <automated>cargo test --test role_endpoint_matrix -- --test-threads=1</automated>
  </verify>
  <done>role_endpoint_matrix_test passes with Case 9 now asserting FORBIDDEN and Case 10 asserting OK for Employee's own requests_list; post_with_cookie_json compiles and is available for Plan 10-02/10-03 to import.</done>
  <acceptance_criteria>
    - `cargo test --test role_endpoint_matrix -- --test-threads=1` exits 0.
    - Running this test BEFORE Task 1's auth.rs change (if checked out independently) would fail Case 9 — confirms the matrix fix is load-bearing, not redundant.
    - `post_with_cookie_json` is `pub(crate)` or otherwise visible for reuse if Plan 10-02/10-03 need to import it (tests in the same file reuse local fns directly; if 10-02/10-03 add cases to THIS file rather than a new file, no visibility change needed — confirm during execution which approach the later plans take and keep the helper at module scope either way).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| HTTP client -> axum handler | Untrusted LAN browser session cookie; role claim lives server-side in the session store, not client-supplied |
| Tauri webview -> Tauri command | Desktop context; identity resolved server-side via `resolve_tauri_identity` (trusted unless desktop_lock_enabled) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege (API5 BFLA) | `authorize()` permission matrix | mitigate | This plan's Task 1: move `Action::ReadData` out of the always-true arm so every downstream call-site that already invokes `authorize(caller, &Action::ReadData)` starts correctly rejecting Employee |
| T-10-02 | Tampering | CI test fixture (`role_endpoint_matrix.rs`) | accept | Test-only code; not shipped; no production attack surface |
| T-10-SC | Tampering | cargo dependency installs | accept | No new crates added in this plan — `axum::body::to_bytes` (or equivalent) is already a transitive dependency exercised elsewhere in the codebase; if a new import path requires a Cargo.toml change, treat as [ASSUMED] and verify against crates.io before adding |

All threats in this plan's scope are either mitigated by Task 1 or accepted as test-only/no-new-dependency. No high-severity threat is left unmitigated.
</threat_model>

<verification>
Run, in this exact order (never concurrently — project rule):
1. `cargo test -p trackly-core auth:: -- --test-threads=1`
2. `cargo test --test role_endpoint_matrix -- --test-threads=1`
Both must exit 0 before this plan is considered complete. Manually diff the
`authorize()` doc-comment table against the function body to confirm they agree.
</verification>

<success_criteria>
- `Action::ReadData` requires `Role::Admin | Role::Manager`; Employee is rejected.
- `Action::ReadRequests` and `Action::CreateRequest` remain always-true (Employee's request-related access is untouched by this matrix edit).
- `post_with_cookie_json` exists and compiles, ready for Plan 10-02/10-03 body assertions.
- Case 9 in the CI matrix asserts 403 (was 200 — the bug is now caught by CI).
- Case 10 proves Employee's own `requests_list` read survives the matrix fix.
</success_criteria>

<output>
Create `.planning/phases/10-employee-employee-ui-role-gating-read/10-01-SUMMARY.md` when done
</output>
