---
phase: 02-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/package.json
  - ui/pnpm-lock.yaml
  - ui/index.html
  - ui/src/App.svelte
  - ui/src/main.ts
  - ui/src/routes.ts
  - ui/src/styles/_tokens.scss
  - ui/src/styles/global.scss
  - ui/src/lib/api/client.ts
  - ui/src/lib/api/errors.ts
  - ui/src/lib/api/index.ts
  - ui/src/lib/stores/theme.svelte.ts
  - ui/src/lib/stores/toast.svelte.ts
  - ui/src/lib/stores/transport.svelte.ts
  - ui/src/lib/components/Button.svelte
  - ui/src/lib/components/Input.svelte
  - ui/src/lib/components/Select.svelte
  - ui/src/lib/components/Textarea.svelte
  - ui/src/lib/components/Modal.svelte
  - ui/src/lib/components/Toast.svelte
  - ui/src/lib/components/ToastHost.svelte
  - ui/src/lib/components/ThemeSwitcher.svelte
  - ui/src/lib/components/Placeholder.svelte
  - ui/src/lib/components/Spinner.svelte
  - ui/src/lib/components/Badge.svelte
  - ui/src/lib/utils/date.ts
  - ui/src/features/layout/Layout.svelte
  - ui/src/features/layout/Sidebar.svelte
  - ui/src/features/layout/sidebar-config.ts
  - ui/src/pages/Dashboard.svelte
  - ui/src/pages/MapPage.svelte
  - ui/src/pages/ActsPage.svelte
  - ui/src/pages/PrintersPage.svelte
  - ui/src/pages/CartridgesPage.svelte
  - ui/src/pages/RequestsPage.svelte
  - ui/src/pages/ReportsPage.svelte
  - ui/src/pages/UsersPage.svelte
  - ui/src/pages/SettingsPage.svelte
  - ui/src/pages/NotFound.svelte
  - .github/workflows/ci-fast.yml
  - .github/workflows/ci-full.yml
  - .planning/phases/01-foundation/deferred-items.md
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
tags:
  - ui
  - shell
  - theme
  - sidebar
  - transport
user_setup: []

must_haves:
  truths:
    - "`ui/package.json` объявляет в `dependencies` (НЕ devDependencies) три новых рантайм-зависимости: `@tauri-apps/api ^2.11`, `@tauri-apps/plugin-dialog ^2.7`, `svelte-spa-router ^5.1` (D-Cleanup-01). `pnpm install --frozen-lockfile` устанавливается без ошибок, `pnpm-lock.yaml` обновлён и закоммичен."
    - "`ui/index.html` содержит inline `<script>...</script>` без атрибута `src`, размещённый внутри `<head>` ДО `<script type=\"module\" src=\"/src/main.ts\">`. Skрипт читает `localStorage.getItem('trackly:theme')` + `matchMedia('(prefers-color-scheme: dark)')` и выставляет `document.documentElement.dataset.theme` синхронно (RESEARCH §Pattern 5)."
    - "`ui/src/styles/_tokens.scss` определяет CSS custom properties в `:root, [data-theme=\"light\"]` и `[data-theme=\"dark\"]` для всех ролей из UI-SPEC.md §Color: `--color-bg`, `--color-surface`, `--color-surface-raised`, `--color-surface-sunken`, `--color-accent`, `--color-accent-hover`, `--color-accent-focus`, `--color-destructive`, `--color-success`, `--color-warning`, `--color-text-primary`, `--color-text-secondary`, `--color-text-muted`, `--color-text-inverse`, `--color-border`, `--color-border-strong`, плюс `--space-xs..3xl`, `--sidebar-width: 240px`, `--header-height: 56px`, `--row-height: 40px`, `--radius-sm: 4px`, `--radius-md: 8px`, `--shadow-elev-1`, `--shadow-elev-2`, font-tokens (`--font-family-base`, sizes/weights)."
    - "`ui/src/lib/api/client.ts` экспортирует `export async function apiCall<R>(name: string, args?: Record<string, unknown>): Promise<R>` с runtime-детектом `const isTauri = typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window;` (RESEARCH §Pattern 8). В Tauri-ветке — lazy `await import('@tauri-apps/api/core')`, в browser-ветке — `fetch('/api/v1/' + name, { method: 'POST', body: JSON.stringify(args), headers: { 'content-type': 'application/json' } })`. Catch → `parseAppError(e)`."
    - "`ui/src/lib/stores/theme.svelte.ts` — module-level `export const themeStore = $state({ preference: 'system', resolved: 'light' });` плюс `initTheme()`, `setTheme(pref)`, listener на `matchMedia('(prefers-color-scheme: dark)') change` активен ТОЛЬКО когда `preference === 'system'`. Storage key — `'trackly:theme'`."
    - "`ui/src/lib/stores/toast.svelte.ts` — `toastStore = $state({ items: [...] })` + `pushToast(kind, message)` с auto-remove по TTL (error 6000ms, warning 5000ms, success/info 4000ms)."
    - "Sidebar displays 10 items in this order (Дашборд, Карта, Устройства, Акты, Принтеры, Картриджи, Заявки, Отчёты, Пользователи, Настройки) grouped into 4 visual sections separated by horizontal dividers (после Карта, после Акты, после Заявки, после Пользователи) — per UI-SPEC.md §Copywriting Sidebar таблица (lines 222-238) / D-UI-Sidebar-01. Source of truth — UI-SPEC §Copywriting Sidebar (10 items + 4 dividers = 14 entries в массиве `SIDEBAR_ITEMS`)."
    - "`ui/src/features/layout/sidebar-config.ts` экспортирует массив `SIDEBAR_ITEMS` РОВНО из 14 entries: 10 items + 4 dividers, в порядке UI-SPEC §Copywriting Sidebar (lines 222-238). Distribution: items в позициях 1,2,4,5,7,8,9,11,12,14; dividers в позициях 3,6,10,13."
    - "`ui/src/features/layout/Sidebar.svelte` рендерит 10 nav items + 4 dividers + footer-блок `<ThemeSwitcher/>` per UI-SPEC §Layout / Sidebar. Active link определяется через `use:active` action из `svelte-spa-router/active`."
    - "Hash routing работает: `svelte-spa-router` подключён в `App.svelte`; маршруты `/`, `/map`, `/devices`, `/acts`, `/printers`, `/cartridges`, `/requests`, `/reports`, `/users`, `/settings` объявлены в `routes.ts`; не-определённый путь → `NotFound.svelte`."
    - "9 placeholder-страниц (`pages/Dashboard.svelte`, `MapPage`, `ActsPage`, `PrintersPage`, `CartridgesPage`, `RequestsPage`, `ReportsPage`, `UsersPage`, `SettingsPage`) — каждый ≤10 LoC, рендерит `<Placeholder section=\"<имя из UI-SPEC>\" phase=\"<номер фазы>\"/>`."
    - "Все hand-rolled primitives созданы (`Button`, `Input`, `Select`, `Textarea`, `Modal`, `Toast`, `ToastHost`, `ThemeSwitcher`, `Placeholder`, `Spinner`, `Badge`) с props/states из UI-SPEC §Component Inventory. Toast TTL и kind colors сматчены."
    - "100% Russian UI invariant: `grep -REn '[A-Za-z]{4,}' ui/src/features ui/src/lib/components --include='*.svelte'` НЕ показывает user-facing английский текст (английские идентификаторы в `<script>` блоках, имена событий — OK)."
    - "CI cleanup (D-Cleanup-01): `.github/workflows/ci-fast.yml` и `.github/workflows/ci-full.yml` — удалена `continue-on-error: true` со step `pnpm svelte-check`. `.planning/phases/01-foundation/deferred-items.md` помечает `@tauri-apps/api runtime dependency` как `✅ Resolved in Phase 2, Plan 02-02`."
    - "`pnpm svelte-check` зелёный (без `continue-on-error`)."
    - "`pnpm lint` зелёный."
    - "`pnpm build` собирает SPA-бандл; `dist/index.html` содержит inline no-flash script verbatim."
    - "Manual smoke: `pnpm tauri dev` запускает desktop-приложение, sidebar отображается с 10 пунктами, переключение темы работает с no-flash при reload."
    - "Scope note (W1): этот план намеренно поставляет UI shell как единый вертикальный slice (~38 файлов) — splitting привёл бы к перетасовке wave-зависимостей; вместо split — каждый task имеет узкий `read_first` чтобы executor загружал только реально редактируемые файлы (Task 1: 5 файлов; Task 2: ~17 файлов компонентов + 4 stores; Task 3: ~13 файлов pages/routes/layout; Task 4: 3 CI/docs файла). Каждый task держится в context-budget ~30%."
  artifacts:
    - path: "ui/index.html"
      provides: "Inline no-flash theme bootstrap script в `<head>` ДО Vite-module entry."
      contains: "trackly:theme"
    - path: "ui/src/lib/api/client.ts"
      provides: "`apiCall<R>(name, args)` функция с transport-detect (Tauri invoke vs HTTP fetch)."
      contains: "__TAURI_INTERNALS__"
    - path: "ui/src/lib/stores/theme.svelte.ts"
      provides: "`themeStore`, `initTheme()`, `setTheme(pref)` с localStorage-persistence."
    - path: "ui/src/features/layout/Sidebar.svelte"
      provides: "Sidebar component с 10 nav items, 4 dividers, ThemeSwitcher в footer."
    - path: "ui/src/styles/_tokens.scss"
      provides: "Полный палитра + spacing + typography tokens; light + dark themes."
    - path: "ui/src/App.svelte"
      provides: "Router shell + ToastHost + initial theme apply."
    - path: ".github/workflows/ci-fast.yml"
      provides: "Step `pnpm svelte-check` без `continue-on-error: true` (blocking)."
  key_links:
    - from: "ui/src/lib/api/client.ts"
      to: "@tauri-apps/api/core"
      via: "lazy dynamic import in isTauri branch"
      pattern: "import.*@tauri-apps/api/core"
    - from: "ui/index.html"
      to: "ui/src/styles/_tokens.scss"
      via: "[data-theme] attribute set by inline script читается через CSS variables"
      pattern: "data-theme"
    - from: "ui/src/features/layout/Sidebar.svelte"
      to: "ui/src/features/layout/sidebar-config.ts"
      via: "import SIDEBAR_ITEMS"
      pattern: "SIDEBAR_ITEMS"
---

<objective>
Поставить навигационный каркас приложения: hash-routing, layout с sidebar + theme switcher, transport-detect API client, toast-инфраструктуру, и все hand-rolled UI-примитивы — БЕЗ конкретного content'а Устройств (это Plan 03).

Также закрыть Phase 1 deferred-item: добавить `@tauri-apps/api` в runtime-deps и снять `continue-on-error: true` со step `pnpm svelte-check` в CI.

Purpose: после этого плана можно открыть `pnpm tauri dev`, увидеть sidebar, переключить тему без вспышки, открыть placeholder для любого раздела. UI-01..UI-06 — все из списка фазы — выполнены layout-уровнево; Plan 03+ наполняют активный раздел «Устройства» функционалом.

Output: компилирующийся, ESLint+svelte-check-зелёный UI-бандл с полной навигацией и темой. Backend остаётся таким, каким его оставил Plan 01 (scaffold).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ui/02-CONTEXT.md
@.planning/phases/02-ui/02-UI-SPEC.md
@.planning/phases/02-ui/02-RESEARCH.md
@.planning/phases/02-ui/02-PATTERNS.md
@.planning/phases/02-ui/02-VALIDATION.md
@.planning/phases/01-foundation/deferred-items.md
@ui/src/App.svelte
@ui/index.html
@ui/package.json
@ui/src/bindings.ts
@.github/workflows/ci-fast.yml
@.github/workflows/ci-full.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Установить JS-зависимости + index.html + design tokens + global styles</name>
  <files>
    ui/package.json,
    ui/pnpm-lock.yaml,
    ui/index.html,
    ui/src/styles/_tokens.scss,
    ui/src/styles/global.scss
  </files>
  <read_first>
    ui/package.json (текущее содержание — нужно сохранить scripts и devDependencies, только расширить dependencies),
    ui/index.html (текущие 13 строк — расширяем),
    ui/src/styles/_tokens.scss (текущий placeholder с `$placeholder: true`),
    .planning/phases/02-ui/02-UI-SPEC.md §Spacing Scale (tokens table),
    .planning/phases/02-ui/02-UI-SPEC.md §Typography (token mapping ~lines 89-103),
    .planning/phases/02-ui/02-UI-SPEC.md §Color §Semantic tokens (полный SCSS блок ~lines 162-201),
    .planning/phases/02-ui/02-UI-SPEC.md §Interaction Patterns §Theme application (inline script ~lines 547-559),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 5 (Vite + inline head script правила).
  </read_first>
  <action>
    Шаг A — `ui/package.json` (D-Cleanup-01): добавить в `"dependencies"` объект три записи. **НЕ удалять** существующее `"svelte": "^5.55.0"` (или текущая версия в package.json). Финал:
    ```
    "dependencies": {
      "@tauri-apps/api": "^2.11.0",
      "@tauri-apps/plugin-dialog": "^2.7.1",
      "svelte": "<existing version>",
      "svelte-spa-router": "^5.1.0"
    }
    ```

    Шаг B — выполнить `pnpm install` в `ui/`, чтобы обновить `pnpm-lock.yaml`. Закоммитить lockfile.

    Шаг C — `ui/index.html`: добавить внутрь `<head>` ПЕРЕД `<script type="module" src="/src/main.ts">` inline-script per UI-SPEC §Theme application (lines 547-559). Без `src`-атрибута, без `type="module"` — иначе Vite его не оставит inline. Содержимое:
    ```html
    <script>
      (function(){
        try {
          var t = localStorage.getItem('trackly:theme');
          var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
          var resolved = (t==='light'||t==='dark') ? t : (prefersDark?'dark':'light');
          document.documentElement.dataset.theme = resolved;
        } catch(e){}
      })();
    </script>
    ```

    Шаг D — переписать `ui/src/styles/_tokens.scss`. Удалить `$placeholder: true;`. Создать:
    - `:root, [data-theme="light"] { ...full UI-SPEC §Color light palette... }` — копировать значения как есть.
    - `[data-theme="dark"] { ...full UI-SPEC §Color dark palette... }`.
    - Под палитрой — spacing tokens (`--space-xs: 4px;` … `--space-3xl: 64px;`).
    - Layout constants (`--sidebar-width: 240px;` … `--touch-target-min: 36px;` … `--row-height: 40px;` … `--radius-sm: 4px;` … `--shadow-elev-1`/`elev-2`/`elev-2-dark`).
    - Font tokens (per UI-SPEC §Typography Token mapping: `--font-family-base`, sizes/weights/line-heights).

    Шаг E — создать `ui/src/styles/global.scss`. Содержание:
    - CSS reset (минимальный — box-sizing border-box, margin 0 на html/body, font-family inheritance).
    - `html { color-scheme: light dark }` (выбирается auto по `[data-theme]`).
    - `body { background: var(--color-bg); color: var(--color-text-primary); font-family: var(--font-family-base); font-size: var(--font-size-body); line-height: var(--line-height-body); }`.
    - Focus-ring policy: `*:focus-visible { outline: none; box-shadow: 0 0 0 3px var(--color-accent-focus); }`.
    - Кастом-скроллбары (опционально-по-возможностям, не критично).
    - `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; } }` (UI-SPEC §Motion).
    - Skip-link styles: `.skip-link { position: absolute; left: -9999px; ...; &:focus { left: 0; top: 0; padding: var(--space-md); background: var(--color-accent); color: var(--color-text-inverse); } }`.

    Импортировать `global.scss` из `ui/src/main.ts` (`import './styles/global.scss';` ДО `mount`).

    Verify: `vite.config.ts` уже настроен на `_tokens.scss` autoprepend (Phase 1) — никаких изменений конфига не нужно.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm install --frozen-lockfile 2&gt;&amp;1 | tail -5</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm svelte-check 2&gt;&amp;1 | tail -10</automated>
    <automated>grep -c "@tauri-apps/api" ui/package.json  # ожидание: 1 (только в dependencies)</automated>
    <automated>grep -c "svelte-spa-router" ui/package.json  # ожидание: 1</automated>
    <automated>grep -c "trackly:theme" ui/index.html  # ожидание: 1</automated>
    <automated>grep -E "data-theme=\"dark\"|data-theme=\"light\"" ui/src/styles/_tokens.scss | wc -l  # ожидание: &gt;= 2</automated>
    <automated>grep -c "color-mix|--color-accent|--space-md" ui/src/styles/_tokens.scss  # ожидание: &gt;= 2</automated>
  </verify>
  <done>
    `pnpm install` зелёный, lockfile обновлён. `index.html` содержит inline no-flash script (без `src`/`type=module`). `_tokens.scss` определяет полную палитру для light + dark тем и все spacing/layout/font tokens из UI-SPEC. `global.scss` подключает базовые стили, reduced-motion, focus-ring, skip-link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать stores (theme/toast/transport) + api client + hand-rolled primitives</name>
  <files>
    ui/src/lib/api/client.ts,
    ui/src/lib/api/errors.ts,
    ui/src/lib/api/index.ts,
    ui/src/lib/stores/theme.svelte.ts,
    ui/src/lib/stores/toast.svelte.ts,
    ui/src/lib/stores/transport.svelte.ts,
    ui/src/lib/utils/date.ts,
    ui/src/lib/components/Button.svelte,
    ui/src/lib/components/Input.svelte,
    ui/src/lib/components/Select.svelte,
    ui/src/lib/components/Textarea.svelte,
    ui/src/lib/components/Modal.svelte,
    ui/src/lib/components/Toast.svelte,
    ui/src/lib/components/ToastHost.svelte,
    ui/src/lib/components/ThemeSwitcher.svelte,
    ui/src/lib/components/Placeholder.svelte,
    ui/src/lib/components/Spinner.svelte,
    ui/src/lib/components/Badge.svelte
  </files>
  <read_first>
    ui/src/bindings.ts (увидеть как генерируется TAURI_INVOKE и AppError shape — нужно для errors.ts типа),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 8 (apiCall полный листинг ~lines 683-707),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 9 (theme store полный листинг ~lines 716-755),
    .planning/phases/02-ui/02-UI-SPEC.md §Component Inventory §Primitives (Button/Input/Select/Textarea/Modal/Toast/ToastHost/ThemeSwitcher/Placeholder/Spinner/Badge — все props/states/sizes),
    .planning/phases/02-ui/02-UI-SPEC.md §Copywriting §Theme switcher (label tex + aria-label),
    .planning/phases/02-ui/02-UI-SPEC.md §Interaction Patterns §Error rendering (apiClient try/catch policy).
  </read_first>
  <action>
    Шаг A — `ui/src/lib/api/errors.ts`: экспортирует тип `AppError { code: string; message: string; details?: Record<string, unknown> }` и функцию `parseAppError(e: unknown): AppError`. parseAppError проверяет: если `e` — объект с `code` и `message` string, вернуть как есть; иначе вернуть `{ code: 'UnknownError', message: 'Не удалось связаться с приложением. Попробуйте перезапустить.' }` (UI-SPEC §Error rendering network-error копи).

    Шаг B — `ui/src/lib/api/client.ts`: ровно как в RESEARCH §Pattern 8. `const isTauri = typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window;`. `export async function apiCall<R>(name: string, args: Record<string, unknown> = {}): Promise<R>` — две ветки: tauri (lazy `await import('@tauri-apps/api/core')`, `invoke<R>(name, args)`) и fetch (POST `/api/v1/${name}`, JSON body). На error — `throw parseAppError(e)` (импорт из `./errors`).

    Шаг C — `ui/src/lib/api/index.ts`: `export { apiCall } from './client'; export { parseAppError, type AppError } from './errors';` (`devices.ts` придёт в Plan 03).

    Шаг D — `ui/src/lib/stores/transport.svelte.ts`: `export const isTauri = typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window;` (single boolean, не rune — Tauri vs browser определяется на load).

    Шаг E — `ui/src/lib/stores/theme.svelte.ts` — полный листинг из RESEARCH §Pattern 9. Storage key — `'trackly:theme'`. Exports: `themeStore`, `initTheme()`, `setTheme(p)`. mql change-listener активен только при `preference === 'system'` (per UI-SPEC §Theme application).

    Шаг F — `ui/src/lib/stores/toast.svelte.ts`: `type ToastKind = 'success'|'error'|'info'|'warning';` `interface ToastItem { id: string; kind: ToastKind; message: string; }` `export const toastStore = $state({ items: [] as ToastItem[] });` `export function pushToast(kind, message)` — добавляет item с UUID-id, ставит `setTimeout(() => toastStore.items = toastStore.items.filter(...), TTL[kind])`. TTL per UI-SPEC: error 6000ms, warning 5000ms, success 4000ms, info 4000ms. Удобные хелперы: `success(msg)`, `error(msg)`, `warning(msg)`, `info(msg)`.

    Шаг G — `ui/src/lib/utils/date.ts`: `export function formatUnixSeconds(seconds: number): string` — форматирует через `new Date(seconds * 1000).toLocaleString('ru-RU', { year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute: '2-digit' })`. Для display-only; backend хранит UTC.

    Шаг H — UI-примитивы (все в `ui/src/lib/components/`). Каждый — Svelte 5 (`<script lang="ts">` + runes-style props через `$props()`):
    
    - `Button.svelte`: props `{ variant?: 'primary'|'secondary'|'destructive'|'ghost'|'link', size?: 'sm'|'md', loading?: boolean, disabled?: boolean, type?: 'button'|'submit', onclick?: () => void }`, default `variant='primary'`, `size='md'`, `type='button'`. CSS — все variants per UI-SPEC §Button matrix. Loading state: показывает `<Spinner size="sm"/>` слева; pointer-events: none. Slot для label.
    
    - `Input.svelte`: props `{ type?: 'text'|'number'|'search', value: string, placeholder?: string, disabled?: boolean, invalid?: boolean, id?: string, onInput?: (v: string) => void }`. Размеры/colors per UI-SPEC. `:focus-visible` ring через global.scss + override border-color на accent. Invalid → destructive border + ring.
    
    - `Select.svelte`: native `<select>` styled. Same dimensions. Caret-icon — inline SVG (можно простой data-uri `<svg ...>`).
    
    - `Textarea.svelte`: same metrics, min-height 80px, resize vertical.
    
    - `Modal.svelte`: props `{ open: boolean, title: string, size?: 'md'|'wide', onClose: () => void }`. Backdrop + container + header + body + footer-slot. Escape closes. Body-overflow hidden when open. Mount через `{#if open}` (Svelte 5; портал — можно через `<svelte:head>` или просто условный `<div class="modal-backdrop">` под `<svelte:body class="modal-open">`). Focus-trap: при `open=true` фокус на close-button или первое поле формы; tab-loop через event listener (упрощённо — на Esc закрыть).
    
    - `Toast.svelte`: props `{ kind: ToastKind, message: string, onClose: () => void }`. Left-border 4px по kind-color (`--color-success` / `--color-destructive` / `--color-accent` / `--color-warning`). Aria-role per kind (info/success = `status`, error/warning = `alert`).
    
    - `ToastHost.svelte`: fixed bottom-right (per UI-SPEC). Iterates `toastStore.items`. Внутри — `Toast` с onClose, который убирает по id.
    
    - `ThemeSwitcher.svelte`: 3 segmented buttons «Светлая | Тёмная | Системная» (UI-SPEC §Copywriting Theme switcher). aria-labels per таблица. onClick → `setTheme(key)`. Selected segment — visually `var(--color-surface-raised)`.
    
    - `Placeholder.svelte`: props `{ section: string, phase: number | string }`. Centered (`display: flex; align-items: center; justify-content: center; padding: var(--space-2xl);`). Heading 20/600 — `{section}`. Body 14/regular gray — «Раздел в разработке». Sub-line — «Появится в Phase {phase}» (для `phase='v2'` — «Появится в v2»; для `section='Карта'` — «В разработке (запланирована на v2)»).
    
    - `Spinner.svelte`: inline circular SVG (12/16/24 px по prop `size`). CSS-only `@keyframes spin { from { transform: rotate(0); } to { transform: rotate(360deg); } }`. Color: `currentColor` (наследует).
    
    - `Badge.svelte`: props `{ variant?: 'default'|'accent'|'success'|'warning'|'destructive', size?: 'sm'|'md' }`. Дефолт `'default'` (фон `--color-surface-sunken`, text primary). Высота 20px, font 12px/500, radius 10px.

    Все компоненты используют tokens из `_tokens.scss` (auto-prepended). Все user-facing строки — на русском. Все идентификаторы (имена компонентов, props) — на английском.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm svelte-check 2&gt;&amp;1 | tail -10</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm lint 2&gt;&amp;1 | tail -10</automated>
    <automated>grep -c "__TAURI_INTERNALS__" ui/src/lib/api/client.ts  # ожидание: 1</automated>
    <automated>grep -c "trackly:theme" ui/src/lib/stores/theme.svelte.ts  # ожидание: &gt;= 1</automated>
    <automated>ls ui/src/lib/components/*.svelte | wc -l  # ожидание: &gt;= 11</automated>
    <automated>grep -REn '[a-zA-Z]{5,}' ui/src/lib/components --include='*.svelte' | grep -vE '(class|style|let|const|export|import|from|return|function|true|false|null|undefined|number|string|boolean|onClose|onInput|onclick|aria|role|type|kind|size|variant|disabled|invalid|loading|preference|resolved|currentColor|tabular)' | grep -E '^[^:]*:[^<]*[А-Яа-я]' | head -5  # должна быть minimum 5 русских строк</automated>
  </verify>
  <done>
    Все stores, api/client, 11 примитивов созданы. `pnpm svelte-check` зелёный. `pnpm lint` зелёный. `apiCall` использует runtime-детект `__TAURI_INTERNALS__`. ThemeSwitcher отображает русские labels.
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать Layout/Sidebar + sidebar-config + routes + 9 placeholder pages + переписать App.svelte/main.ts</name>
  <files>
    ui/src/features/layout/Layout.svelte,
    ui/src/features/layout/Sidebar.svelte,
    ui/src/features/layout/sidebar-config.ts,
    ui/src/routes.ts,
    ui/src/pages/Dashboard.svelte,
    ui/src/pages/MapPage.svelte,
    ui/src/pages/ActsPage.svelte,
    ui/src/pages/PrintersPage.svelte,
    ui/src/pages/CartridgesPage.svelte,
    ui/src/pages/RequestsPage.svelte,
    ui/src/pages/ReportsPage.svelte,
    ui/src/pages/UsersPage.svelte,
    ui/src/pages/SettingsPage.svelte,
    ui/src/pages/NotFound.svelte,
    ui/src/App.svelte,
    ui/src/main.ts
  </files>
  <read_first>
    ui/src/App.svelte (текущий placeholder — переписываем),
    ui/src/main.ts (текущий — расширяем initTheme + global.scss import),
    .planning/phases/02-ui/02-UI-SPEC.md §Copywriting Sidebar таблица (10 items + 4 dividers = 14 entries; точные labels, routes, phase-номера, dividers после поз. Карта/Акты/Заявки/Пользователи),
    .planning/phases/02-ui/02-UI-SPEC.md §Component Inventory §Layout (Layout, Sidebar specs),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 10 (Sidebar w/ use:link + use:active),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 5 + §Open Question 5 (init theme в main.ts ДО mount).
  </read_first>
  <action>
    Шаг A — `ui/src/features/layout/sidebar-config.ts`:
    ```ts
    export type SidebarItem = { kind: 'item'; route: string; label: string; phase?: number | string };
    export type SidebarDivider = { kind: 'divider' };
    export type SidebarEntry = SidebarItem | SidebarDivider;
    export const SIDEBAR_ITEMS: SidebarEntry[] = [
      { kind: 'item', route: '/', label: 'Дашборд', phase: 7 },
      { kind: 'item', route: '/map', label: 'Карта', phase: 'v2' },
      { kind: 'divider' },
      { kind: 'item', route: '/devices', label: 'Устройства' },     // активный в Phase 2
      { kind: 'item', route: '/acts', label: 'Акты', phase: 3 },
      { kind: 'divider' },
      { kind: 'item', route: '/printers', label: 'Принтеры', phase: 6 },
      { kind: 'item', route: '/cartridges', label: 'Картриджи', phase: 4 },
      { kind: 'item', route: '/requests', label: 'Заявки', phase: 6 },
      { kind: 'divider' },
      { kind: 'item', route: '/reports', label: 'Отчёты', phase: 7 },
      { kind: 'item', route: '/users', label: 'Пользователи', phase: 5 },
      { kind: 'divider' },
      { kind: 'item', route: '/settings', label: 'Настройки', phase: 7 },
    ];
    ```
    (PINNED: 10 items + 4 dividers = 14 entries — точно как в UI-SPEC §Copywriting Sidebar таблица lines 222-238. Dividers — ровно 4, размещены после: Карта (поз. 3), Акты (поз. 6), Заявки (поз. 10), Пользователи (поз. 13). Это source of truth; не пересчитывать.)

    Шаг B — `ui/src/features/layout/Sidebar.svelte`. Структура per UI-SPEC §Layout Sidebar + RESEARCH §Pattern 10:
    ```svelte
    <script lang="ts">
      import { link } from 'svelte-spa-router';
      import active from 'svelte-spa-router/active';
      import { SIDEBAR_ITEMS } from './sidebar-config';
      import ThemeSwitcher from '$lib/components/ThemeSwitcher.svelte';
    </script>
    <nav class="sidebar">
      <ul class="nav-list">
        {#each SIDEBAR_ITEMS as item, i}
          {#if item.kind === 'divider'}
            <li class="divider" aria-hidden="true"></li>
          {:else}
            <li>
              <a href={item.route} use:link use:active={{ path: item.route, className: 'is-active' }}>
                {item.label}
              </a>
            </li>
          {/if}
        {/each}
      </ul>
      <div class="sidebar-footer">
        <span class="theme-label">Тема</span>
        <ThemeSwitcher />
      </div>
    </nav>
    ```
    Стили (within `<style lang="scss">`): per UI-SPEC §Sidebar. Active item: 3px left-border accent, background `color-mix(in srgb, var(--color-accent) 10%, transparent)`. Hover inactive item — background `color-mix(in srgb, var(--color-text-primary) 5%, transparent)`. Width `var(--sidebar-width)`. flex column, footer внизу через `margin-top: auto`.

    Шаг C — `ui/src/features/layout/Layout.svelte`: per UI-SPEC §Layout. CSS Grid `grid-template-columns: var(--sidebar-width) 1fr; min-height: 100vh`. `<aside>` для Sidebar, `<main class="content">` для slot. `<main>`: overflow auto, padding `var(--space-lg)`. Skip-link сверху (`<a href="#main" class="skip-link">Перейти к основному содержимому</a>`). В `<script>` — `import { onMount } from 'svelte'; import { initTheme } from '$lib/stores/theme.svelte';` и `onMount(initTheme);` ИЛИ инициализировать в `main.ts` ДО `mount` (см. §Open Question 5 — рекомендация: в `main.ts`).

    Шаг D — `ui/src/routes.ts`:
    ```ts
    import type { RouteDefinition } from 'svelte-spa-router';
    import Layout from './features/layout/Layout.svelte';
    // NOTE: svelte-spa-router doesn't natively wrap routes in a layout — обычно Layout импортируется в App.svelte и оборачивает <Router>.
    // Маршруты — компоненты страниц:
    import Dashboard from './pages/Dashboard.svelte';
    import MapPage from './pages/MapPage.svelte';
    // ... import all 9 placeholder pages + DevicesPage placeholder import will come in Plan 03 ...
    import NotFound from './pages/NotFound.svelte';
    
    export const routes = {
      '/': Dashboard,
      '/map': MapPage,
      '/devices': DevicesPlaceholder,    // ← в этом плане — placeholder; Plan 03 заменит на DevicesPage.svelte
      '/acts': ActsPage,
      '/printers': PrintersPage,
      '/cartridges': CartridgesPage,
      '/requests': RequestsPage,
      '/reports': ReportsPage,
      '/users': UsersPage,
      '/settings': SettingsPage,
      '*': NotFound,
    } as const;
    ```
    Для маршрута `/devices` в Plan 02 — временный placeholder `pages/DevicesPagePlaceholder.svelte` (показывает «Раздел будет реализован в этом же плане» — потом Plan 03 правит routes.ts чтобы импортировать `features/devices/DevicesPage.svelte`). Или просто на этом этапе указать на тот же `Placeholder`-компонент с `section="Устройства" phase="2"` и пометкой что Plan 03 заменит — executor вправе создать `pages/DevicesPlaceholder.svelte`.

    Шаг E — Создать 9 placeholder pages в `ui/src/pages/`. Каждый ≤10 LoC:
    ```svelte
    <script lang="ts">
      import Placeholder from '$lib/components/Placeholder.svelte';
    </script>
    <Placeholder section="Дашборд" phase={7} />
    ```
    Section и phase — по UI-SPEC §Copywriting Sidebar таблица:
    - Dashboard: section="Дашборд", phase=7
    - MapPage: section="Карта", phase="v2"
    - ActsPage: section="Акты", phase=3
    - PrintersPage: section="Принтеры", phase=6
    - CartridgesPage: section="Картриджи", phase=4
    - RequestsPage: section="Заявки", phase=6
    - ReportsPage: section="Отчёты", phase=7
    - UsersPage: section="Пользователи", phase=5
    - SettingsPage: section="Настройки", phase=7
    
    `NotFound.svelte`: заголовок «Страница не найдена», текст «Раздел `{location.hash}` не существует. Откройте навигацию слева.» + button «На главную» (на `/`).

    Шаг F — `ui/src/App.svelte` (REWRITE). Структура:
    ```svelte
    <script lang="ts">
      import Router from 'svelte-spa-router';
      import { routes } from './routes';
      import Layout from './features/layout/Layout.svelte';
      import ToastHost from '$lib/components/ToastHost.svelte';
    </script>
    <Layout>
      <Router {routes} />
    </Layout>
    <ToastHost />
    ```
    
    Шаг G — `ui/src/main.ts`: import `'./styles/global.scss';` сверху, затем `import { initTheme } from './lib/stores/theme.svelte';`, `initTheme();` ДО `mount(App, ...)`. Сохранить mount-логику Phase 1.

    Шаг H — обновить `vite.config.ts` aliases если нужно для `$lib` — Phase 1 уже мог настроить; если нет, добавить `resolve.alias = { $lib: path.resolve(__dirname, 'src/lib') }`. Executor проверяет в первую очередь, есть ли уже alias.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm svelte-check 2&gt;&amp;1 | tail -8</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm lint 2&gt;&amp;1 | tail -8</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm build 2&gt;&amp;1 | tail -6</automated>
    <automated>ls ui/src/pages/*.svelte | wc -l  # ожидание: &gt;= 10 (9 placeholders + NotFound)</automated>
    <automated>grep -c "SIDEBAR_ITEMS" ui/src/features/layout/sidebar-config.ts  # ожидание: 1</automated>
    <automated>grep -cE "Дашборд|Карта|Устройства|Акты|Принтеры|Картриджи|Заявки|Отчёты|Пользователи|Настройки" ui/src/features/layout/sidebar-config.ts  # ожидание: 10</automated>
    <automated>grep -cE "kind:\s*'divider'" ui/src/features/layout/sidebar-config.ts  # ожидание: 4 (per UI-SPEC §Copywriting Sidebar)</automated>
    <automated>grep -cE "kind:\s*'item'" ui/src/features/layout/sidebar-config.ts  # ожидание: 10</automated>
    <automated>grep -c "initTheme" ui/src/main.ts  # ожидание: 1</automated>
    <automated>grep -c "trackly:theme" ui/dist/index.html  # после pnpm build — inline script сохранён verbatim</automated>
  </verify>
  <done>
    `pnpm build` собирается. `pnpm svelte-check` зелёный без `continue-on-error`. Sidebar содержит 10 items в правильном порядке. 9 placeholder pages + NotFound существуют. App.svelte монтирует Router + Layout + ToastHost. main.ts вызывает initTheme() ДО mount. inline no-flash script сохранён в `dist/index.html` после Vite-сборки.
  </done>
</task>

<task type="auto">
  <name>Task 4: CI cleanup — снять continue-on-error со svelte-check + закрыть deferred-item</name>
  <files>
    .github/workflows/ci-fast.yml,
    .github/workflows/ci-full.yml,
    .planning/phases/01-foundation/deferred-items.md
  </files>
  <read_first>
    .github/workflows/ci-fast.yml (найти `continue-on-error: true` на step `pnpm svelte-check`),
    .github/workflows/ci-full.yml (то же),
    .planning/phases/01-foundation/deferred-items.md (найти запись о @tauri-apps/api / svelte-check).
  </read_first>
  <action>
    Шаг A — В обоих workflows YAML-файлах найти step, который выполняет `pnpm svelte-check` (вероятно содержит `run: pnpm svelte-check` или `run: cd ui && pnpm svelte-check`). Удалить из этого step строку `continue-on-error: true`. Обновить inline-comment, если есть (например, было `# remove when bindings.ts resolves — see deferred-items.md`).

    Шаг B — В `.planning/phases/01-foundation/deferred-items.md` найти запись для `@tauri-apps/api` runtime dependency / svelte-check unblock и добавить пометку:
    ```
    **Status:** ✅ Resolved in Phase 2 (Plan 02-02, Task 4 — 2026-MM-DD).
    `@tauri-apps/api ^2.11.0`, `@tauri-apps/plugin-dialog ^2.7.1`, `svelte-spa-router ^5.1.0` добавлены в `ui/package.json` dependencies; `continue-on-error: true` снят со step `pnpm svelte-check` в `ci-fast.yml` + `ci-full.yml`.
    ```
    НЕ закрывать запись про `export_bindings` Windows skip — это deferred остаётся (per CONTEXT D-Cleanup-01).
  </action>
  <verify>
    <automated>grep -c "continue-on-error" .github/workflows/ci-fast.yml || true  # ожидание: 0 или строки не относятся к svelte-check (нужно глазами)</automated>
    <automated>grep -B1 -A2 "svelte-check" .github/workflows/ci-fast.yml | grep -c "continue-on-error"  # ожидание: 0</automated>
    <automated>grep -B1 -A2 "svelte-check" .github/workflows/ci-full.yml | grep -c "continue-on-error"  # ожидание: 0</automated>
    <automated>grep -c "Resolved in Phase 2" .planning/phases/01-foundation/deferred-items.md  # ожидание: &gt;= 1</automated>
  </verify>
  <done>
    `continue-on-error: true` снят со step pnpm svelte-check в обоих workflow files. deferred-items.md помечает запись закрытой со ссылкой на Plan 02-02.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: Checkpoint — UI shell + theme + sidebar manual smoke</name>
  <what-built>
    Полный UI shell: layout с sidebar, hash routing на 10 разделов, theme switcher с no-flash при reload, toast-хост, transport-detect API client, 11 hand-rolled примитивов. Все JS-deps установлены, CI svelte-check unblocked.
  </what-built>
  <how-to-verify>
    1. `cd ui && pnpm build` — сборка без ошибок; `dist/index.html` содержит inline `trackly:theme` script.
    2. `cd ui && pnpm svelte-check` — зелёный, нет фатальных ошибок.
    3. `pnpm tauri dev` (или `cargo tauri dev` из репо) — открывается окно. Подтвердить:
       - Sidebar слева, 10 пунктов в правильном порядке, между группами — горизонтальные dividers.
       - Click на «Дашборд» — открывается placeholder «Раздел в разработке / Появится в Phase 7».
       - Click на «Устройства» — placeholder с пометкой что Plan 03 заполнит.
       - Footer sidebar: ThemeSwitcher с тремя кнопками «Светлая / Тёмная / Системная».
       - Click «Тёмная» → тема меняется мгновенно (без transition); фон становится `#0F1419`.
       - Перезагрузка приложения (close + reopen) → загружается сразу в Тёмной без вспышки светлой темы.
       - localStorage в DevTools (если откроется) содержит `trackly:theme: "dark"`.
    4. CI runs (GitHub Actions, опционально): следующий push в main / PR — `pnpm svelte-check` теперь blocking-step; зелёный → подтверждено отсутствие unresolved `bindings.ts` импортов.
  </how-to-verify>
  <resume-signal>Напишите "approved" или опишите проблемы (no-flash, активная подсветка, переключение темы, рендер placeholder).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → ui bundle | 3 новых пакета (@tauri-apps/api, @tauri-apps/plugin-dialog, svelte-spa-router) попадают в runtime UI |
| localStorage → DOM | inline-script читает theme value и применяет к `<html data-theme>` |
| webview ↔ Rust | apiCall использует invoke (Tauri) или fetch (HTTP browser в Phase 5+) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-SC | Tampering | npm install of @tauri-apps/api, @tauri-apps/plugin-dialog, svelte-spa-router | mitigate | Все 3 пакета verified в RESEARCH.md §Package Legitimacy Audit (официальные tauri-apps publishers + svelte-spa-router 5.1.0 Microsoft-engineer-maintained). pinning ^2.11/^2.7/^5.1 в `package.json`. |
| T-02-02-02 | Tampering | localStorage theme value (XSS / user-tampered) | mitigate | inline-script проверяет `(t==='light'\|\|t==='dark')` — любое неожиданное значение fallback на `prefersDark ? 'dark' : 'light'`. data-theme на `<html>` принимает только из 2 значений после validation. CSS не выполняет JS из tokens. |
| T-02-02-03 | Information Disclosure | apiCall error messages могут утечь internals в toast | mitigate | parseAppError проверяет shape `{code, message}` — backend AppError.message уже sanitized (Phase 1 invariant); unknown error fallback на жёстко закодированную русскую копи без deeper info. |
| T-02-02-04 | Tampering | Hash router accepts any client-side hash | accept | Phase 2 has no auth; routing — pure UI affordance. Unknown route → NotFound page (no leak). Phase 5+ authorize() будет accessing-control'ить через Rust. |
| T-02-02-05 | DoS | Toast spam (many pushToast calls) растёт array | mitigate | TTL auto-remove (4-6s); limit array length к 10 в pushToast — если больше, сдвинуть оldest (executor реализует или принимает риск). |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile` зелёный.
- `pnpm svelte-check` зелёный (без `continue-on-error`).
- `pnpm lint` зелёный.
- `pnpm build` собирает без ошибок.
- Manual: `pnpm tauri dev` — UI работает per UI-SPEC.
</verification>

<success_criteria>
- 3 новых dependencies зарегистрированы в `package.json` под `dependencies`, lockfile обновлён.
- inline no-flash script в `<head>` `index.html` ДО Vite-module entry; работает в production-build (`dist/index.html`).
- `_tokens.scss` определяет полную палитру + spacing + typography для light + dark тем.
- 11 hand-rolled primitives + 3 stores + apiCall client созданы, без svelte-check ошибок.
- Sidebar отображает 10 nav items в порядке UI-01 / D-UI-Sidebar-01 с правильными dividers, theme switcher в footer.
- Hash routing работает: все 10 разделов открываются placeholder'ом «Раздел в разработке».
- Theme persistence: reload не даёт вспышки светлой темы при сохранённой Тёмной.
- 100% Russian: user-facing strings в `.svelte` файлах — на русском (UI-03).
- CI: `pnpm svelte-check` теперь blocking; Phase 1 deferred-item закрыт в `deferred-items.md`.
</success_criteria>

<output>
Create `.planning/phases/02-ui/02-02-SUMMARY.md` при завершении.
</output>
