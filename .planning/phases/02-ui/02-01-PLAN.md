---
phase: 02-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/trackly-app/Cargo.toml
  - crates/trackly-infra/src/db/migrations.rs
  - migrations/V013__devices_fts_triggers.sql
  - crates/trackly-app/src/tauri_cmds/health.rs
  - crates/trackly-app/src/http/health.rs
  - crates/trackly-app/tests/health_smoke.rs
  - crates/trackly-core/src/lib.rs
  - crates/trackly-core/src/ports/mod.rs
  - crates/trackly-core/src/ports/devices.rs
  - crates/trackly-core/src/domain/mod.rs
  - crates/trackly-core/src/domain/devices.rs
  - crates/trackly-infra/src/lib.rs
  - crates/trackly-infra/src/repos/mod.rs
  - crates/trackly-infra/src/repos/devices_sqlite.rs
  - crates/trackly-app/src/services/mod.rs
  - crates/trackly-app/src/services/device_service.rs
  - crates/trackly-app/src/dto/device.rs
  - crates/trackly-app/src/dto/mod.rs
  - crates/trackly-app/src/tauri_cmds/devices.rs
  - crates/trackly-app/src/tauri_cmds/mod.rs
  - crates/trackly-app/src/http/devices.rs
  - crates/trackly-app/src/http/mod.rs
  - crates/trackly-app/src/csv/mod.rs
  - crates/trackly-app/src/csv/sniff.rs
  - crates/trackly-app/src/csv/decode.rs
  - crates/trackly-app/src/csv/parse.rs
  - crates/trackly-app/src/csv/session_store.rs
  - crates/trackly-app/src/context.rs
  - crates/trackly-app/src/lib.rs
autonomous: false
requirements:
  - DEV-04
  - DEV-05
tags:
  - foundation
  - scaffolding
  - migration
user_setup: []

must_haves:
  truths:
    - "Workspace `Cargo.toml` declares pinned versions for `chardetng = \"0.1\"`, `encoding_rs = \"0.8\"`, `csv = \"1.3\"`, `uuid = { version = \"1\", features = [\"v4\", \"serde\"] }`, `tauri-plugin-dialog = \"2\"`, `tauri-plugin-single-instance = \"2\"` in `[workspace.dependencies]` and `cargo metadata --format-version=1 --no-deps` succeeds."
    - "`crates/trackly-app/Cargo.toml` adds the same six crates as `{ workspace = true }` dependencies under `[dependencies]` (`chardetng`, `encoding_rs`, `csv`, `uuid`, `tauri-plugin-dialog`, `tauri-plugin-single-instance`)."
    - "`migrations/V013__devices_fts_triggers.sql` exists, ends with `PRAGMA user_version = 13;`, creates three FTS triggers (`devices_fts_ai`, `devices_fts_ad`, `devices_fts_au`) using V003 column names (`inventory_number`, `serial_number`, `model`) and five partial indexes (`idx_devices_autocomplete_name`, `_model`, `_location`, `_condition`, `_complectation`) each with `WHERE deleted_at_utc IS NULL` predicate."
    - "`crates/trackly-infra/src/db/migrations.rs::max_known_version()` returns `13`; all callsites that previously hardcoded `12` (tauri_cmds/health.rs minimal_ctx, http/health.rs test minimal_ctx + smoke assert, tests/health_smoke.rs assert, migrations.rs tests) are updated to `13`."
    - "`cargo run -p trackly-app -- --self-test` against a fresh tempdir creates `trackly.db` and `PRAGMA user_version` reports `13`."
    - "Module scaffolds compile (empty/stub bodies only — real impl lands in Plans 03-05): `trackly-core::{ports::devices, domain::devices}`, `trackly-infra::repos::{mod, devices_sqlite}`, `trackly-app::{services::{mod, device_service}, dto::device, tauri_cmds::devices, http::devices, csv::{mod, sniff, decode, parse, session_store}}`."
    - "`AppCtx` in `crates/trackly-app/src/context.rs` gains `pub devices: Arc<DeviceService>` field; `AppCtx::build` constructs `DeviceService::new(writer.clone(), readers.clone(), clock.clone())` after reader pool init."
    - "`crates/trackly-core/tests/no_io_deps.rs` still passes after `domain/` and `ports/` modules are added (proves core has no rusqlite/tokio leak)."
    - "`cargo clippy --workspace --all-targets -- -D warnings` green; `cargo fmt --check` green; `cargo test --workspace --no-fail-fast` green (existing Phase 1 tests still pass, plus migration runner picks up V013)."
  artifacts:
    - path: "migrations/V013__devices_fts_triggers.sql"
      provides: "FTS5 sync triggers + 5 partial autocomplete indexes; final line `PRAGMA user_version = 13;`."
      contains: "CREATE TRIGGER devices_fts_ai"
    - path: "crates/trackly-core/src/ports/devices.rs"
      provides: "`pub trait DeviceRepository` declaration (associated `type Conn` per PATTERNS.md option-2 to keep rusqlite out of core)."
    - path: "crates/trackly-core/src/domain/devices.rs"
      provides: "`DeviceNew`, `DevicePatch`, `DeviceFilter`, `Pagination`, `DeviceRow`, `DeviceGroupRow` pure-value structs."
    - path: "crates/trackly-infra/src/repos/devices_sqlite.rs"
      provides: "`pub struct SqliteDeviceRepository` (no methods yet; methods land in Plan 03)."
    - path: "crates/trackly-app/src/services/device_service.rs"
      provides: "`pub struct DeviceService { writer, readers, clock, repo, csv_sessions }` + `impl Clone` + `pub fn new(...)` constructor only; CRUD methods stubbed `todo!()` (filled in Plan 03)."
    - path: "crates/trackly-app/src/dto/device.rs"
      provides: "Empty module with `pub const STATE_HINTS: &[&str]` (6 entries per DEV-10) only; DTO structs land in Plan 03."
    - path: "crates/trackly-app/src/csv/session_store.rs"
      provides: "`ImportSessionStore` with `new`, `put`, `take` methods (full impl per RESEARCH §Pattern 7 — used by Plan 05)."
  key_links:
    - from: "crates/trackly-app/src/context.rs"
      to: "crates/trackly-app/src/services/device_service.rs"
      via: "Arc<DeviceService> field on AppCtx + new() call in AppCtx::build"
      pattern: "devices: Arc::new\\(DeviceService::new"
    - from: "crates/trackly-infra/src/db/migrations.rs"
      to: "migrations/V013__devices_fts_triggers.sql"
      via: "embed_migrations! macro picks up V013 file"
      pattern: "max_known_version.*13"
---

<objective>
Подготовить весь backend-scaffold для Phase 2 + накатить миграцию V013 + закрыть workspace-зависимости.
Это Wave 0 — после неё все последующие планы строят на готовой структуре модулей.

Purpose:
- Регистрация crate-зависимостей в Cargo (chardetng / encoding_rs / csv / uuid / tauri-plugin-dialog / tauri-plugin-single-instance).
- V013-миграция: FTS5 sync triggers + 5 partial indexes для autocomplete (D-Schema-Phase2-01, D-Autocomplete-01).
- Пустые модули `ports/`, `domain/`, `repos/`, `services/`, `csv/` + ImportSessionStore (полный) + STATE_HINTS const.
- `AppCtx` расширяется полем `devices: Arc<DeviceService>`.
- Schema-version bump 12 → 13 во всех 5 hardcoded callsites.

Output:
- Workspace компилируется. Все Phase 1 тесты зелёные. `--self-test` создаёт V013-схему. Никаких новых behaviour'ов — только структура и DDL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ui/02-CONTEXT.md
@.planning/phases/02-ui/02-RESEARCH.md
@.planning/phases/02-ui/02-PATTERNS.md
@.planning/phases/02-ui/02-VALIDATION.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@migrations/V003__devices.sql
@migrations/V012__indexes_and_fts.sql

<interfaces>
Ключевые типы из Phase 1, к которым этот план привязывается:

From crates/trackly-infra/src/db/writer_worker.rs:
- pub struct WriterHandle (Arc-clonable handle)
- impl WriterHandle { pub async fn execute<F, R>(&self, op: F) -> Result<R, AppError> where F: FnOnce(&mut Connection) -> Result<R, AppError> + Send + 'static, R: Send + 'static }

From crates/trackly-infra/src/db/pools.rs:
- pub struct ReaderPool
- impl ReaderPool { pub fn new(db_path: &Path, size: usize) -> Result<Self, AppError>; pub fn acquire(&self) -> ReaderHandle }
- pub struct ReaderHandle (RAII; Deref<Target = Connection>)

From crates/trackly-infra/src/db/migrations.rs:
- pub fn run(conn: &mut Connection) -> Result<MigrationReport, AppError>
- pub fn max_known_version() -> u32  // returns 12 today; bump to 13

From crates/trackly-infra/src/error_conversions.rs:
- pub fn map_rusqlite(e: rusqlite::Error) -> AppError
- pub fn map_refinery(e: refinery::Error) -> AppError

From crates/trackly-core/src/primitives/clock.rs:
- pub trait Clock: Send + Sync { fn now(&self) -> OffsetDateTime; fn unix_seconds(&self) -> i64 { ... } }

From crates/trackly-core/src/error.rs:
- pub enum AppError { Validation { field, message }, OptimisticLockMismatch { entity, id, expected, actual }, Conflict, Internal { source_chain }, NotFound, WriteQueueBusy, ... }  // 9 variants total

From crates/trackly-app/src/context.rs:
- pub struct AppCtx { writer: Arc<WriterHandle>, readers: Arc<ReaderPool>, paths: Arc<Paths>, config: Arc<AppConfig>, clock: Arc<dyn Clock + Send + Sync>, shutdown: CancellationToken, log_guard: Arc<WorkerGuard>, schema_version: u32 }
- impl AppCtx { pub async fn build(...) -> Result<Self, AppError>; }
- AppCtx: Clone
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Объявить workspace + crate deps и накатить V013-миграцию</name>
  <files>
    Cargo.toml,
    crates/trackly-app/Cargo.toml,
    migrations/V013__devices_fts_triggers.sql,
    crates/trackly-infra/src/db/migrations.rs,
    crates/trackly-app/src/tauri_cmds/health.rs,
    crates/trackly-app/src/http/health.rs,
    crates/trackly-app/tests/health_smoke.rs
  </files>
  <read_first>
    Cargo.toml (workspace; найти секцию `[workspace.dependencies]`),
    crates/trackly-app/Cargo.toml (найти `[dependencies]`),
    migrations/V012__indexes_and_fts.sql (полностью — это analog для V013, см. PATTERNS.md),
    migrations/V003__devices.sql (для подтверждения фактических имён колонок: `inventory_number`, `serial_number`, `condition`, `complectation`, `notes`),
    crates/trackly-infra/src/db/migrations.rs (поиск `max_known_version` и тестов с числом `12`),
    crates/trackly-app/src/tauri_cmds/health.rs (поиск `12` в test minimal_ctx, ~line 69),
    crates/trackly-app/src/http/health.rs (поиск `12`, ~lines 53 и 82),
    crates/trackly-app/tests/health_smoke.rs (поиск `12`, ~line 34-35),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 4 "FTS5 trigger sync (V013 SQL)"  (готовый SQL — используем как референс),
    .planning/phases/02-ui/02-PATTERNS.md §`migrations/V013__devices_fts_triggers.sql` Pattern Assignment (column-name reconciliation).
  </read_first>
  <action>
    Шаг A. В workspace `Cargo.toml` под `[workspace.dependencies]` добавить (НЕ модифицировать существующие pinned `=...` версии specta/tauri-specta/specta-typescript): `chardetng = "0.1"`, `encoding_rs = "0.8"`, `csv = "1.3"`, `uuid = { version = "1", features = ["v4", "serde"] }`, `tauri-plugin-dialog = "2"`, `tauri-plugin-single-instance = "2"`.

    Шаг B. В `crates/trackly-app/Cargo.toml` под `[dependencies]` добавить пять строк `chardetng = { workspace = true }`, `encoding_rs = { workspace = true }`, `csv = { workspace = true }`, `uuid = { workspace = true }`, `tauri-plugin-dialog = { workspace = true }`, `tauri-plugin-single-instance = { workspace = true }`. (Регистрация плагинов в `tauri::Builder` — НЕ в этом плане; план 03 wire'ит Tauri Builder и подключает оба плагина.)

    Шаг C. Создать `migrations/V013__devices_fts_triggers.sql`. Содержимое: comment-блок описывает purpose (FTS sync + autocomplete indexes per D-Schema-Phase2-01), затем три триггера на таблицу `devices`:
    - `CREATE TRIGGER devices_fts_ai AFTER INSERT ON devices WHEN NEW.deleted_at_utc IS NULL BEGIN INSERT INTO devices_fts(rowid, name, inventory_number, serial_number, model) VALUES (NEW.id, NEW.name, NEW.inventory_number, NEW.serial_number, NEW.model); END;`
    - `CREATE TRIGGER devices_fts_ad AFTER DELETE ON devices BEGIN INSERT INTO devices_fts(devices_fts, rowid, name, inventory_number, serial_number, model) VALUES ('delete', OLD.id, OLD.name, OLD.inventory_number, OLD.serial_number, OLD.model); END;`
    - `CREATE TRIGGER devices_fts_au AFTER UPDATE ON devices BEGIN INSERT INTO devices_fts(devices_fts, rowid, name, inventory_number, serial_number, model) VALUES ('delete', OLD.id, OLD.name, OLD.inventory_number, OLD.serial_number, OLD.model); INSERT INTO devices_fts(rowid, name, inventory_number, serial_number, model) SELECT NEW.id, NEW.name, NEW.inventory_number, NEW.serial_number, NEW.model WHERE NEW.deleted_at_utc IS NULL; END;`

    Затем 5 partial indexes (per D-Autocomplete-01 — используем фактические V003 column names):
    - `CREATE INDEX idx_devices_autocomplete_name ON devices(name) WHERE deleted_at_utc IS NULL;`
    - `CREATE INDEX idx_devices_autocomplete_name_model ON devices(name, model) WHERE deleted_at_utc IS NULL AND model IS NOT NULL;`
    - `CREATE INDEX idx_devices_autocomplete_name_location ON devices(name, location_id) WHERE deleted_at_utc IS NULL AND location_id IS NOT NULL;`
    - `CREATE INDEX idx_devices_autocomplete_name_condition ON devices(name, condition) WHERE deleted_at_utc IS NULL AND condition IS NOT NULL;`
    - `CREATE INDEX idx_devices_autocomplete_name_complectation ON devices(name, complectation) WHERE deleted_at_utc IS NULL AND complectation IS NOT NULL;`

    Финал файла — строка `PRAGMA user_version = 13;`.

    ВАЖНО: ИСПОЛЬЗОВАТЬ фактические V003 column names (`inventory_number`, `serial_number`, `condition`, `complectation`) — НЕ доменные имена (`inventory_no`, `state`, `kit`). Это решение Path B из PATTERNS.md (DTO маппит UI-имена в repo-`from_row`; SQL колонки остаются V003).

    НЕ переименовывать `notes` → `specs` колонкой в SQL — Path B оставляет колонку как есть и маппит в DTO. RESEARCH §Open Question 1 рекомендовал ALTER COLUMN, но PATTERNS.md выбрал DTO-mapping; следуем PATTERNS.md.

    Шаг D. В `crates/trackly-infra/src/db/migrations.rs` обновить `max_known_version()` чтобы возвращал `13` (per D-04 паттерн). Поправить все тесты в файле, которые ассертят `12` или `applied_count == 12`, на `13` (включая `run_applies_all_twelve_migrations_on_fresh_db` — оставить имя теста или переименовать на `run_applies_all_known_migrations`, executor решает).

    Шаг E. Bump callsites где `12` zahkardcoded: `crates/trackly-app/src/tauri_cmds/health.rs` (test `minimal_ctx`, line ~69 — `schema_version: 12` → `13`), `crates/trackly-app/src/http/health.rs` (test `minimal_ctx` ~53; test assert ~82), `crates/trackly-app/tests/health_smoke.rs` (assert на schema_version ~34-35).

    Pattern source: PATTERNS.md §Pattern 7 "Schema-version bump propagates to all callsites".
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo metadata --format-version=1 --no-deps &gt; /dev/null &amp;&amp; cargo build --workspace 2&gt;&amp;1 | tail -5</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-infra --lib db::migrations 2&gt;&amp;1 | grep -E "test result|PRAGMA|schema_version|13"</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app --test health_smoke 2&gt;&amp;1 | grep -E "test result"</automated>
    <automated>grep -c "PRAGMA user_version = 13" migrations/V013__devices_fts_triggers.sql  # ожидание: 1</automated>
    <automated>grep -E "tauri-plugin-single-instance" Cargo.toml crates/trackly-app/Cargo.toml | wc -l  # ожидание: &gt;= 2 (workspace + crate dep)</automated>
    <automated>grep -E "schema_version.*12|== 12" crates/trackly-app/src/tauri_cmds/health.rs crates/trackly-app/src/http/health.rs crates/trackly-app/tests/health_smoke.rs crates/trackly-infra/src/db/migrations.rs | grep -v '^[^:]*:[[:space:]]*//' | wc -l  # ожидание: 0 (после grep'a комментарии исключены)</automated>
  </verify>
  <done>
    `cargo build --workspace` succeeds; `cargo test --workspace --no-fail-fast` green; `migrations/V013__devices_fts_triggers.sql` существует с 3 триггерами + 5 partial indexes + final `PRAGMA user_version = 13;`; `max_known_version()` возвращает 13; нет hardcoded `12` в обновлённых callsites (вне комментариев).
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать пустые scaffolds для ports/domain/repos/services/csv + STATE_HINTS + AppCtx-extension</name>
  <files>
    crates/trackly-core/src/lib.rs,
    crates/trackly-core/src/ports/mod.rs,
    crates/trackly-core/src/ports/devices.rs,
    crates/trackly-core/src/domain/mod.rs,
    crates/trackly-core/src/domain/devices.rs,
    crates/trackly-infra/src/lib.rs,
    crates/trackly-infra/src/repos/mod.rs,
    crates/trackly-infra/src/repos/devices_sqlite.rs,
    crates/trackly-app/src/services/mod.rs,
    crates/trackly-app/src/services/device_service.rs,
    crates/trackly-app/src/dto/device.rs,
    crates/trackly-app/src/dto/mod.rs,
    crates/trackly-app/src/tauri_cmds/devices.rs,
    crates/trackly-app/src/tauri_cmds/mod.rs,
    crates/trackly-app/src/http/devices.rs,
    crates/trackly-app/src/http/mod.rs,
    crates/trackly-app/src/csv/mod.rs,
    crates/trackly-app/src/csv/sniff.rs,
    crates/trackly-app/src/csv/decode.rs,
    crates/trackly-app/src/csv/parse.rs,
    crates/trackly-app/src/csv/session_store.rs,
    crates/trackly-app/src/context.rs,
    crates/trackly-app/src/lib.rs
  </files>
  <read_first>
    crates/trackly-core/src/lib.rs (увидеть как объявляются модули `primitives`, `error`),
    crates/trackly-core/src/primitives/mod.rs (analog для domain/ports module wiring),
    crates/trackly-core/src/primitives/clock.rs (analog для pure trait — нужен паттерн `trait X: Send + Sync`),
    crates/trackly-core/src/primitives/secret.rs (analog для pure value type),
    crates/trackly-core/tests/no_io_deps.rs (тест который ДОЛЖЕН остаться зелёным; смотреть какие зависимости запрещены),
    crates/trackly-infra/src/lib.rs (увидеть текущие `pub mod` объявления),
    crates/trackly-infra/src/db/mod.rs (analog для module wiring),
    crates/trackly-app/src/lib.rs (текущие `pub mod` объявления),
    crates/trackly-app/src/context.rs (полностью — расширяем `AppCtx` + `AppCtx::build`),
    crates/trackly-app/src/dto/mod.rs (analog — добавляем `pub mod device;`),
    crates/trackly-app/src/dto/health.rs (analog для STATE_HINTS const стиля + специально для понимания serde derive),
    crates/trackly-app/src/tauri_cmds/mod.rs (analog),
    crates/trackly-app/src/http/mod.rs (analog),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 7 "CSV preview-then-commit token store" (полный код ImportSessionStore),
    .planning/phases/02-ui/02-CONTEXT.md §D-DeviceHints-01 (6 пресетов состояния).
  </read_first>
  <action>
    Шаг A. `trackly-core` — добавить модули `domain` и `ports`. В `crates/trackly-core/src/lib.rs` добавить `pub mod domain;` и `pub mod ports;`. Создать `domain/mod.rs` с `pub mod devices;`. Создать `domain/devices.rs`: pub struct'ы `DeviceNew`, `DevicePatch`, `DeviceFilter`, `Pagination`, `DeviceRow`, `DeviceGroupRow` с полями (см. RESEARCH §Architecture для имён полей: `type_id: i64`, `name: String`, `inventory_no: Option<String>`, `serial_no: Option<String>`, `model: Option<String>`, `specs: Option<String>`, `kit: Option<String>`, `state: Option<String>`, `location_id: Option<i64>`, `status_id: i64`). НИКАКИХ derive `serde::Serialize` / `specta::Type` в core (это для DTO). `#[derive(Debug, Clone, PartialEq, Eq)]` достаточно. Доменные типы используют UI-имена (`inventory_no`, `state`, `kit`, `specs`) — Path B из PATTERNS.md.

    Создать `ports/mod.rs` с `pub mod devices;`. Создать `ports/devices.rs` с trait `DeviceRepository` с associated type для соединения (PATTERNS.md §"Объявить trait в core" вариант 2 — `type Conn`; конкретный rusqlite::Connection задаётся в impl в infra). Метод-сигнатуры пустые/`todo!()` пока — это scaffold. Минимум: `fn create(&self, conn: &mut Self::Conn, new: &DeviceNew, now_utc: i64) -> Result<i64, AppError>`, `fn get(&self, conn: &Self::Conn, id: i64) -> Result<DeviceRow, AppError>`, `fn list(&self, conn: &Self::Conn, filter: &DeviceFilter, page: &Pagination) -> Result<(Vec<DeviceRow>, u64), AppError>`, `fn update(&self, conn: &mut Self::Conn, id: i64, version: i64, patch: &DevicePatch, now_utc: i64) -> Result<DeviceRow, AppError>`, `fn delete_soft(&self, conn: &mut Self::Conn, id: i64, version: i64, now_utc: i64) -> Result<(), AppError>`, `fn search_fts(&self, conn: &Self::Conn, fts_query: &str, page: &Pagination) -> Result<Vec<DeviceRow>, AppError>`, `fn autocomplete(&self, conn: &Self::Conn, field: &str, prefix: &str, ctx_name: Option<&str>) -> Result<Vec<String>, AppError>`, `fn list_grouped(&self, conn: &Self::Conn, filter: &DeviceFilter, page: &Pagination) -> Result<Vec<DeviceGroupRow>, AppError>`.

    Шаг B. `trackly-infra` — `pub mod repos;` в `lib.rs`. Создать `repos/mod.rs` с `pub mod devices_sqlite;` и `pub use devices_sqlite::SqliteDeviceRepository;`. Создать `repos/devices_sqlite.rs` с `pub struct SqliteDeviceRepository;` (unit struct, derive Default + Clone). НЕ реализовывать методы trait в этом плане — это сделает Plan 03 (CRUD) и Plan 04 (search/autocomplete/grouping). Опционально: чтобы scaffolding не падал на `unused_imports`, можно либо НЕ импортировать trait здесь, либо добавить полностью пустой `impl DeviceRepository for SqliteDeviceRepository { type Conn = rusqlite::Connection; fn create(...) { todo!() } ... }` — executor выбирает; рекомендация: пустой `impl` со всеми методами в `todo!()` чтобы компилятор подтверждал signatures rано.

    Шаг C. `trackly-app/dto/device.rs` — НЕ создавать DTO struct'ы (это Plan 03). Создать ТОЛЬКО `pub const STATE_HINTS: &[&str] = &["Новое", "Новый в заводской упаковке, не вскрытый", "Новый в заводской упаковке, вскрытый, настроенное рабочее окружение (ОС)", "Хорошее", "Среднее", "Б/У"];` (6 строк per DEV-10 / D-DeviceHints-01). В `dto/mod.rs` добавить `pub mod device;`.

    Шаг D. `trackly-app/services/mod.rs` + `services/device_service.rs`. В `lib.rs` добавить `pub mod services;`. `services/mod.rs`: `pub mod device_service; pub use device_service::DeviceService;`. `services/device_service.rs`: `pub struct DeviceService { writer: Arc<WriterHandle>, readers: Arc<ReaderPool>, clock: Arc<dyn Clock + Send + Sync>, repo: Arc<SqliteDeviceRepository>, csv_sessions: Arc<ImportSessionStore> }` + `impl Clone` (через derive — Arc клонится дёшево) + `pub fn new(writer: Arc<WriterHandle>, readers: Arc<ReaderPool>, clock: Arc<dyn Clock + Send + Sync>) -> Self { Self { writer, readers, clock, repo: Arc::new(SqliteDeviceRepository), csv_sessions: Arc::new(ImportSessionStore::new()) } }`. НИКАКИХ CRUD методов — они приедут в Plan 03/04/05.

    Шаг E. `trackly-app/csv/` — создать модули. `csv/mod.rs`: `pub mod sniff; pub mod decode; pub mod parse; pub mod session_store;`. `csv/sniff.rs`, `csv/decode.rs`, `csv/parse.rs` — оставить пустыми (либо `// TODO: Plan 05`-комментарий + минимальная заглушка чтобы компилировалось без warnings, например `#![allow(dead_code)]` в каждом). `csv/session_store.rs` — ПОЛНЫЙ impl из RESEARCH §Pattern 7 (lines 631-672): `ImportSession`, `TTL: Duration = Duration::from_secs(5 * 60)`, `ImportSessionStore { inner: Mutex<HashMap<Uuid, ImportSession>> }`, методы `new()`, `put(session) -> Uuid`, `take(token) -> Option<ImportSession>` с lazy sweep на put. Это нужно сейчас, потому что `DeviceService::new` ссылается на `ImportSessionStore::new()`.

    В `lib.rs` (trackly-app): добавить `pub mod csv;` (но имя `csv` — НЕ конфликт со внешним crate `csv` потому что pub mod — внутренний namespace; компилятор это допускает).

    Шаг F. `trackly-app/tauri_cmds/devices.rs` + `http/devices.rs` — создать пустыми (модуль-заглушки). Объявить `pub mod devices;` в `tauri_cmds/mod.rs` и `http/mod.rs`.

    Шаг G. `crates/trackly-app/src/context.rs` — добавить поле `pub devices: Arc<DeviceService>` в struct `AppCtx` (после `schema_version`). В `AppCtx::build` после `let readers = Arc::new(ReaderPool::new(...));` и `let clock = ...` (PATTERNS.md §`context.rs` modification, lines 254-264) добавить `let devices = Arc::new(DeviceService::new(writer.clone(), readers.clone(), clock.clone()));` и положить в struct-construction.

    Шаг H. Проверить что `crates/trackly-core/tests/no_io_deps.rs` всё ещё проходит — domain/ports не должны тянуть rusqlite/tokio/serde-в-зависимости core.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo build --workspace 2&gt;&amp;1 | tail -5</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-core --test no_io_deps 2&gt;&amp;1 | grep -E "test result"</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test --workspace --no-fail-fast 2&gt;&amp;1 | grep -E "test result.*ok|FAIL" | tail -10</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2&gt;&amp;1 | tail -8</automated>
    <automated>grep -c "STATE_HINTS" crates/trackly-app/src/dto/device.rs  # ожидание: &gt;= 1</automated>
    <automated>grep -c "ImportSessionStore" crates/trackly-app/src/csv/session_store.rs  # ожидание: &gt;= 2 (struct + impl)</automated>
    <automated>grep -c "devices: Arc&lt;DeviceService&gt;" crates/trackly-app/src/context.rs  # ожидание: 1</automated>
  </verify>
  <done>
    Все модули созданы. `cargo build --workspace` зелёный. `cargo test --workspace --no-fail-fast` зелёный (Phase 1 тесты + no_io_deps). `cargo clippy -- -D warnings` без предупреждений. `DeviceService` и `ImportSessionStore` инстанцируются в `AppCtx::build`. STATE_HINTS const содержит 6 правильных русских строк (per D-DeviceHints-01). `AppCtx: Clone` всё ещё работает (Arc-обёртки дёшевы).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — Phase 1 cleanup + scheme-version push</name>
  <what-built>
    Шаги Task 1–2 завершены: workspace зарегистрировал 5 новых crate-deps; V013 миграция накатана; все 5 callsites schema_version обновлены 12→13; scaffold-модули для ports/domain/repos/services/csv созданы и собираются; AppCtx расширен полем devices.
  </what-built>
  <how-to-verify>
    1. Запустить `cargo run -p trackly-app -- --self-test` в свежем tempdir (или из репо). На stderr — `self-test OK: schema_version=13`. БД-файл создан рядом с .exe (или target/...).
    2. Открыть созданную БД через `sqlite3 trackly.db` и выполнить `PRAGMA user_version;` — ожидается `13`.
    3. `SELECT name FROM sqlite_master WHERE type='trigger' AND tbl_name='devices';` — ожидаются 3 строки: `devices_fts_ai`, `devices_fts_ad`, `devices_fts_au`.
    4. `SELECT name FROM sqlite_master WHERE type='index' AND name LIKE 'idx_devices_autocomplete%';` — ожидаются 5 строк.
    5. `cargo clippy --workspace --all-targets -- -D warnings` зелёный.
    6. `cargo test --workspace --no-fail-fast` зелёный (включая `health_smoke`, `concurrent_writes`, `no_io_deps`, `tests::run_applies_all_known_migrations`).
  </how-to-verify>
  <resume-signal>Напишите "approved" или опишите проблему.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package-manager → workspace | crates.io packages cross into the build; legitimacy audited in RESEARCH §Package Legitimacy Audit |
| migration runner → SQLite file | DDL runs inside trusted writer task; no untrusted input reaches V013 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | cargo install of chardetng / encoding_rs / csv / uuid / tauri-plugin-dialog / tauri-plugin-single-instance | mitigate | Все 6 crates audited в RESEARCH.md §Package Legitimacy Audit — все [VERIFIED] (Mozilla / BurntSushi / tauri-apps официальные authors); slopcheck недоступен в research env — приемлемо при cross-check к CLAUDE.md и официальным docs. Версии pinned в workspace `Cargo.toml`. |
| T-02-01-02 | Tampering | V013 trigger SQL — fixed strings, no user input | mitigate | DDL — статическая SQL-строка, embed_migrations! читает .sql из репо в compile-time; не существует attack vector кроме compromise репо. |
| T-02-01-03 | Information Disclosure | Migration log lines не должны утечь content БД | accept | `tracing::info!("migration applied", version=...)` — только версия миграции; no payload. Phase 1 invariant. |
| T-02-01-04 | DoS | Partial indexes на `WHERE deleted_at_utc IS NULL` могут раздуть БД | mitigate | Размер индекса пропорционален живым строкам; для 10k устройств — &lt;5MB total. Verification — `EXPLAIN QUERY PLAN` после Plan 04. |
</threat_model>

<verification>
- `cargo build --workspace` зелёный.
- `cargo clippy --workspace --all-targets -- -D warnings` зелёный.
- `cargo test --workspace --no-fail-fast` зелёный.
- Manual: SQLite intospection (Task 3 checkpoint).
</verification>

<success_criteria>
- V013 миграция создана, накатывается на свежей БД; `PRAGMA user_version` = 13.
- 3 триггера + 5 partial indexes для devices зарегистрированы в `sqlite_master`.
- Все scaffold-модули скомпилированы без warnings.
- `AppCtx` содержит поле `devices: Arc<DeviceService>`, заполняется в `AppCtx::build`.
- `crates/trackly-core/tests/no_io_deps.rs` зелёный (core по-прежнему без I/O deps).
- Все Phase 1 тесты остаются зелёными.
</success_criteria>

<output>
Create `.planning/phases/02-ui/02-01-SUMMARY.md` при завершении.
</output>
