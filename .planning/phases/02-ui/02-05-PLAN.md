---
phase: 02-ui
plan: 05
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - crates/trackly-app/src/csv/sniff.rs
  - crates/trackly-app/src/csv/decode.rs
  - crates/trackly-app/src/csv/parse.rs
  - crates/trackly-app/src/csv/session_store.rs
  - crates/trackly-app/src/csv/mod.rs
  - crates/trackly-app/src/dto/device.rs
  - crates/trackly-app/src/services/device_service.rs
  - crates/trackly-app/src/tauri_cmds/devices.rs
  - crates/trackly-app/src/tauri_cmds/fs_helpers.rs
  - crates/trackly-app/src/tauri_cmds/mod.rs
  - crates/trackly-app/src/http/devices.rs
  - crates/trackly-app/src/http/fs_helpers.rs
  - crates/trackly-app/src/http/mod.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/tests/devices_csv_import.rs
  - crates/trackly-app/tests/devices_csv_export.rs
  - crates/trackly-app/tests/devices_csv_session.rs
  - crates/trackly-app/tests/export_bindings.rs
  - crates/trackly-app/tests/fixtures/devices/utf8.csv
  - crates/trackly-app/tests/fixtures/devices/utf8_bom.csv
  - crates/trackly-app/tests/fixtures/devices/cp1251_comma.csv
  - crates/trackly-app/tests/fixtures/devices/cp1251_semicolon.csv
  - crates/trackly-app/tests/fixtures/devices/malformed_mixed_rows.csv
  - crates/trackly-app/tests/fixtures/devices/README.md
  - ui/src/lib/api/devices.ts
  - ui/src/features/devices/DeviceImportCsvModal.svelte
  - ui/src/features/devices/DevicesPage.svelte
autonomous: false
requirements:
  - DEV-12
  - DEV-13
tags:
  - vertical-slice
  - devices
  - csv
  - mvp-slice-3
user_setup: []

must_haves:
  truths:
    - "Пользователь нажимает «Импорт CSV» на странице Устройства → открывается file picker (tauri-plugin-dialog) → выбирает файл .csv → backend определяет кодировку (UTF-8 / UTF-8 BOM / CP1251) и делимитер (`,` или `;`) → 4-step wizard показывает preview первых 5 строк + автомаппинг колонок → пользователь подтверждает → backend импортирует все строки в одной транзакции writer-channel → toast с количеством успешных/ошибочных строк."
    - "Backend `DeviceService::import_csv_preview(bytes: Vec<u8>) -> Result<CsvImportPreviewResponse, AppError>` — sniff encoding (chardetng + BOM check, UTF-8 если BOM), sniff delimiter (count `,` vs `;` в первой непустой строке вне quote), decode (encoding_rs), parse (csv crate ReaderBuilder flexible=true), вернуть `{ token: Uuid, encoding: \"UTF-8\"|\"WINDOWS_1251\", delimiter: \",\"|\";\", headers: Vec<String>, preview_rows: Vec<Vec<String>>, total_rows: u64, had_replacements: bool }` (Pitfall #7 has-replacements warning). Сессия сохраняется в `csv_sessions.put(...)` с 5-min TTL."
    - "Backend `DeviceService::import_csv_commit(token: Uuid, mapping: HashMap<String, String>) -> Result<CsvImportReport, AppError>` — `csv_sessions.take(token)` возвращает сессию или AppError::Validation { field: \"token\", message: \"expired\" }. Затем `writer.execute(move |conn| { let tx = conn.transaction(); for row in session.all_rows { build DeviceNew from mapping; validate; if ok — repo.create + audit_log; else — accumulate RowError; } tx.commit(); ... })`. Возвращает `CsvImportReport { inserted: u64, failed: Vec<RowError { row_index: u64, error: AppError }> }`."
    - "Backend `DeviceService::export_csv(filter: DeviceFilter) -> Result<String, AppError>` — spawn_blocking + reader.acquire + repo.list(filter, Pagination { offset: 0, limit: u64::MAX }) (или streaming в Phase 7) → формирует CSV string: BOM `\\xEF\\xBB\\xBF` + headers «Тип;Наименование;Инвентарный №;Серийный №;Модель;Технические характеристики;Комплектация;Состояние;Расположение;Статус» + строки (CSV crate WriterBuilder delimiter=`b';'`). Возвращает строку — UI сохраняет через tauri-plugin-dialog save-dialog."
    - "CSV pipeline (csv/sniff.rs / decode.rs / parse.rs): полная реализация per RESEARCH §Pattern 6. `detect(bytes) -> CsvProfile { encoding, delimiter }`; `decode_to_string(bytes, encoding) -> (String, had_replacements)`; `parse_rows(text, delimiter) -> Result<(headers, rows), csv::Error>`. Все функции — pure, тестируемые независимо."
    - "Fixtures (crates/trackly-app/tests/fixtures/devices/): 5 файлов с реальной строкой `«Сидоров-Петроградский Иван Александрович (ё) №42»` (Phase 1 fixture string). `utf8.csv` — plain UTF-8 без BOM, делимитер `,`. `utf8_bom.csv` — UTF-8 с 3-byte BOM, delim `,`. `cp1251_comma.csv` — binary, CP1251 bytes, delim `,`. `cp1251_semicolon.csv` — binary, CP1251 bytes, delim `;`. `malformed_mixed_rows.csv` — UTF-8 с 1-2 строками с пустым обязательным полем (имя). README.md в той же папке описывает iconv-команду для регенерации."
    - "3 Tauri commands добавлены: `devices_import_csv_preview(bytes: Vec<u8>) -> CsvImportPreviewResponse`, `devices_import_csv_commit(token: String, mapping: HashMap<String, String>) -> CsvImportReport`, `devices_export_csv(filter: DeviceFilter) -> String`. axum routes на тех же путях. Specta_export расширен."
    - "FS helper commands (B2 pinned strategy — backend helpers с path validation, НЕ tauri-plugin-fs): `read_file_bytes(path: String) -> Vec<u8>` и `write_file_bytes(path: String, content: String) -> ()` — оба объявлены в `crates/trackly-app/src/tauri_cmds/devices.rs` (или отдельный модуль `tauri_cmds/fs_helpers.rs` — executor выбирает; рекомендуется отдельный модуль). Path validation per T-02-05-02: (a) `Path::new(&path).canonicalize()?`; (b) reject если canonical path содержит `..`; (c) reject UNC paths (`\\\\?`); (d) `metadata()?.len() <= 50 * 1024 * 1024` (50 MB cap); (e) extension whitelist `[\"csv\"]` для read и write. На любое нарушение — `AppError::Validation { field: \"path\", message: \"<russian text>\" }`. Оба добавлены в `specta_export::builder()::collect_commands![...]` и в `tests/export_bindings.rs` substring asserts (`read_file_bytes`, `write_file_bytes`). axum routes для browser-mode (Phase 5+): `POST /api/v1/read_file_bytes` и `POST /api/v1/write_file_bytes` — в Phase 2 routes регистрируются, но не используются (Phase 2 browser-mode не активен)."
    - "Frontend DeviceImportCsvModal — 4-step wizard per UI-SPEC §Component Inventory DeviceImportCsvModal: file pick → preview → mapping confirm → result. Modal `size=\"wide\"` (960px). Step indicator сверху (dots или text «Шаг N из 4»). Step 2 показывает «Определена кодировка: {encoding}, разделитель: «{delim}»». Step 3 — таблица «Колонка CSV / Поле устройства» с `<Select>` per колонка (auto-populate по совпадению header). Step 4 — «Импортировано: X. Пропущено с ошибками: Y.» + expandable список ошибок."
    - "Frontend Export CSV: при click `«Экспорт CSV»` button — `await devices.exportCsv(currentFilter)` → bytes → tauri-plugin-dialog save dialog (default filename `устройства_{YYYY-MM-DD}.csv`) → toast «Экспортировано {count} устройств»."
    - "Frontend: file pick через `@tauri-apps/plugin-dialog` `open({ filters: [{ name: 'CSV', extensions: ['csv'] }] })`; полученный path → `std::fs::read` через Tauri-команду (или альтернатива: read через `@tauri-apps/api/fs` если ACL разрешает; рекомендация: сделать helper-command `read_file_bytes(path: String) -> Vec<u8>` в backend для централизации; ИЛИ пускать pickup через HTML `<input type='file'>` чтобы не зависеть от fs-permissions — executor выбирает, рекомендация: native dialog + helper command для path-resolved read)."
    - "Tests integration: `devices_csv_import.rs` (4 fixture encodings/delimiters round-trip with cyrillic string), `devices_csv_export.rs` (BOM check, `;` delimiter, Russian headers, opens-in-Excel manual note), `devices_csv_session.rs` (token expires after 5min via mock-clock or sleep-skip; double-take returns expired). Each test — 30s timeout."
    - "validation: `cargo test -p trackly-app --test devices_csv_import` зелёный; `cargo test -p trackly-app --test devices_csv_export` зелёный; manual smoke — export откроется в RU-locale Excel без mojibake (тест документирован но не автоматизирован)."
  artifacts:
    - path: "crates/trackly-app/src/csv/sniff.rs"
      provides: "detect(bytes) -> CsvProfile с BOM + chardetng + delimiter sniff."
    - path: "crates/trackly-app/src/csv/decode.rs"
      provides: "decode_to_string(bytes, encoding) -> (String, had_replacements)."
    - path: "crates/trackly-app/src/csv/parse.rs"
      provides: "parse_rows(text, delimiter) -> (headers, rows) с csv::ReaderBuilder flexible=true."
    - path: "crates/trackly-app/src/csv/session_store.rs"
      provides: "ImportSessionStore с 5-min TTL (полный impl уже из Plan 01)."
    - path: "crates/trackly-app/tests/fixtures/devices/utf8.csv"
      provides: "Test fixture с cyrillic + UTF-8 + comma delimiter."
    - path: "crates/trackly-app/tests/fixtures/devices/cp1251_semicolon.csv"
      provides: "Test fixture с cyrillic + CP1251 binary + semicolon delimiter."
    - path: "ui/src/features/devices/DeviceImportCsvModal.svelte"
      provides: "4-step CSV import wizard."
      min_lines: 200
  key_links:
    - from: "crates/trackly-app/src/services/device_service.rs"
      to: "crates/trackly-app/src/csv/{sniff,decode,parse,session_store}.rs"
      via: "import_csv_preview/commit использует pipeline"
      pattern: "csv::sniff::detect|csv::parse::parse_rows|csv_sessions"
    - from: "ui/src/features/devices/DeviceImportCsvModal.svelte"
      to: "@tauri-apps/plugin-dialog"
      via: "open({ filters: [csv] }) для file pick"
      pattern: "plugin-dialog"
---

<objective>
Vertical slice #3: CSV import + export.

Пользователь импортирует устройства из CSV (4 encoding+delimiter варианта, per-row error accumulation, 5-min TTL preview→commit token) и экспортирует список в Excel-friendly UTF-8 BOM + `;`-делимитер. Cyrillic корректно round-trip'ится во всех вариантах.

Output: DEV-12 и DEV-13 выполнены. Phase 2 success criteria #4 ассертирован.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ui/02-CONTEXT.md
@.planning/phases/02-ui/02-RESEARCH.md
@.planning/phases/02-ui/02-PATTERNS.md
@.planning/phases/02-ui/02-UI-SPEC.md
@.planning/phases/02-ui/02-VALIDATION.md
@.planning/phases/02-ui/02-01-PLAN.md
@.planning/phases/02-ui/02-03-PLAN.md

<interfaces>
After Plan 03:

From crates/trackly-app/src/services/device_service.rs:
- DeviceService { writer, readers, clock, repo, csv_sessions: Arc<ImportSessionStore> }
- async fn create / get / list / update / delete_soft (готово)

From crates/trackly-app/src/csv/session_store.rs (Plan 01):
- ImportSession { encoding, delimiter, headers, all_rows, created }
- ImportSessionStore { new(), put(session) -> Uuid, take(token: Uuid) -> Option<ImportSession> }
- TTL = 5 min

Crate deps (Plan 01 registered):
- chardetng = "0.1"
- encoding_rs = "0.8"  
- csv = "1.3"
- uuid = "1"
- tauri-plugin-dialog = "2"

Frontend:
- ui/src/lib/api/client.ts — apiCall<R>(name, args)
- ui/src/lib/components/Modal.svelte (size: 'md'|'wide')
- @tauri-apps/plugin-dialog (Plan 02-02 dependency)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSV pipeline (sniff/decode/parse) + fixtures + unit tests</name>
  <files>
    crates/trackly-app/src/csv/sniff.rs,
    crates/trackly-app/src/csv/decode.rs,
    crates/trackly-app/src/csv/parse.rs,
    crates/trackly-app/src/csv/mod.rs,
    crates/trackly-app/tests/fixtures/devices/utf8.csv,
    crates/trackly-app/tests/fixtures/devices/utf8_bom.csv,
    crates/trackly-app/tests/fixtures/devices/cp1251_comma.csv,
    crates/trackly-app/tests/fixtures/devices/cp1251_semicolon.csv,
    crates/trackly-app/tests/fixtures/devices/malformed_mixed_rows.csv,
    crates/trackly-app/tests/fixtures/devices/README.md,
    crates/trackly-app/tests/devices_csv_import.rs
  </files>
  <read_first>
    crates/trackly-app/src/csv/session_store.rs (Plan 01 — полный, для понимания ImportSession shape),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 6 (полные листинги sniff/decode/parse),
    .planning/phases/02-ui/02-RESEARCH.md §Pitfall 7 (mixed-encoding had_replacements warning),
    .planning/phases/02-ui/02-RESEARCH.md §Open Question 4 (commit binary fixtures, не build.rs),
    .planning/phases/02-ui/02-CONTEXT.md §D-CSV-01 (encoding/delimiter discovery rules),
    .planning/phases/02-ui/02-CONTEXT.md §D-CSV-02 (export rules — UTF-8 BOM + `;`).
  </read_first>
  <behavior>
    Unit tests (внутри `csv/sniff.rs` / `csv/decode.rs` / `csv/parse.rs` через `#[cfg(test)]`):
    - sniff::detect on bytes starting with 0xEF 0xBB 0xBF → encoding=UTF_8.
    - sniff::detect on plain UTF-8 cyrillic bytes → encoding=UTF_8 (chardetng guess).
    - sniff::detect on CP1251 cyrillic bytes (e.g., "А" = 0xC0) → encoding=WINDOWS_1251.
    - sniff::detect with semicolon-delimited first line → delimiter=b';'.
    - sniff::detect with comma-delimited first line → delimiter=b','.
    - decode::decode_to_string на UTF-8 cyrillic → exact round-trip, had_replacements=false.
    - decode::decode_to_string на CP1251 bytes for "Ёлка" → string "Ёлка", had_replacements=false.
    - parse::parse_rows на CSV «Имя,Тип\nЛlevox,Принтер\n» → headers=["Имя","Тип"], rows=[["Lenovo","Принтер"]].
    - parse::parse_rows с flexible row (ragged) — НЕ паникует.
    
    Integration tests (tests/devices_csv_import.rs):
    - `import_utf8_no_bom` — read `tests/fixtures/devices/utf8.csv`, call `service.import_csv_preview(bytes).await`. Assert preview.encoding == "UTF-8", delimiter == ",", preview_rows.len() == 5 (или <total), preview_rows[0] contains «Сидоров-Петроградский».
    - `import_utf8_with_bom` — same но с `utf8_bom.csv` — encoding должно быть "UTF-8" (BOM-detected).
    - `import_cp1251_semicolon` — `cp1251_semicolon.csv` — encoding "WINDOWS_1251", delimiter ";".
    - `import_cp1251_comma` — `cp1251_comma.csv` — encoding "WINDOWS_1251", delimiter ",".
    - `import_cyrillic_round_trip` — после `import_csv_commit` устройство в БД имеет name == «Сидоров-Петроградский Иван Александрович (ё) №42».
    - `import_per_row_errors` — `malformed_mixed_rows.csv` — commit returns CsvImportReport { inserted: N, failed: vec![RowError { row_index: K, error: AppError::Validation }] }.
    - `import_session_expired` — preview, манипулировать clock на +6min, commit с тем же token → AppError::Validation { field: "token", message содержит "expired" } (тест может пропускаться или использовать explicit advance — executor решает; рекомендация: добавить #[cfg(test)] helper для force-expire в session_store).
  </behavior>
  <action>
    Шаг A — `csv/sniff.rs`: per RESEARCH §Pattern 6 sniff.rs. Импорт `chardetng::EncodingDetector`, `encoding_rs::{Encoding, UTF_8, WINDOWS_1251}`. `pub struct CsvProfile { pub encoding: &'static Encoding, pub delimiter: u8 }`. `pub fn detect(bytes: &[u8]) -> CsvProfile { ... }` — BOM-check первой, потом chardetng `feed + guess`, потом decode для delimiter sniff (count `,` vs `;` в первой строке).

    Шаг B — `csv/decode.rs`: per RESEARCH §Pattern 6. `pub fn decode_to_string(bytes: &[u8], encoding: &'static Encoding) -> (String, bool)`. Возвращает (decoded_owned, had_replacements).

    Шаг C — `csv/parse.rs`: per RESEARCH §Pattern 6. `pub fn parse_rows(text: &str, delimiter: u8) -> Result<(Vec<String>, Vec<Vec<String>>), csv::Error>`. ReaderBuilder с `flexible(true)` (tolerate ragged rows).

    Шаг D — `csv/mod.rs`: re-export `pub use sniff::{detect, CsvProfile}; pub use decode::decode_to_string; pub use parse::parse_rows; pub use session_store::{ImportSessionStore, ImportSession};`.

    Шаг E — Fixtures. Создать 5 файлов в `crates/trackly-app/tests/fixtures/devices/`. Содержимое каждого — реальные строки с тестовой fixture string:
    
    `utf8.csv` (plain UTF-8, no BOM, comma):
    ```
    Тип,Наименование,Инвентарный №,Серийный №,Модель,Расположение,Статус
    Устройство,Сидоров-Петроградский Иван Александрович (ё) №42,,,Acme X,Кабинет 305,На складе
    Устройство,Ноутбук Lenovo,INV-001,SN-XYZ-001,X1 Carbon,Кабинет 101,На складе
    Устройство,Принтер HP,INV-002,,LaserJet 1020,Кабинет 102,В работе
    Расходник,Бумага A4,,,,Кладовая,На складе
    ```
    
    `utf8_bom.csv` — точно те же байты, но с 3-byte BOM `\xEF\xBB\xBF` в начале.
    
    `cp1251_comma.csv` — те же логические данные, но закодировано в CP1251 (executor использует `iconv -f UTF-8 -t WINDOWS-1251 utf8.csv > cp1251_comma.csv` либо аналогично Python: `text.encode('cp1251')`).
    
    `cp1251_semicolon.csv` — те же данные в CP1251 с `;` вместо `,`. Executor regenerirует utf8 source с `;` сначала, потом iconv.
    
    `malformed_mixed_rows.csv` — UTF-8, comma. 5 строк, из которых 2 имеют пустое поле Наименование (что нарушает DEV-02 required) — должны вернуть RowError при commit.
    
    `README.md` — описание как сгенерировать fixtures: `# CSV fixtures для тестов devices_csv_import` + iconv команды + объяснение что cp1251_*.csv хранятся как binary blobs (НЕ редактировать вручную; regenerate from source UTF-8 + iconv).

    Шаг F — `crates/trackly-app/tests/devices_csv_import.rs` — поведения per `<behavior>` (preview-only тесты в этом задаче; commit-related — в Task 2 после реализации DeviceService::import_csv_commit). Каждый — 30s timeout.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app csv:: 2&gt;&amp;1 | grep -E "test result"</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app --test devices_csv_import 2&gt;&amp;1 | grep -E "test result"  # only preview-tests pass в этом этапе</automated>
    <automated>ls crates/trackly-app/tests/fixtures/devices/*.csv | wc -l  # ожидание: 5</automated>
    <automated>file crates/trackly-app/tests/fixtures/devices/cp1251_comma.csv | grep -E "data|ASCII|Non-ISO|ISO-8859" || true  # ожидание: НЕ UTF-8 (binary blob)</automated>
    <automated>head -c 3 crates/trackly-app/tests/fixtures/devices/utf8_bom.csv | xxd | grep "efbb bf"  # ожидание: 3-byte BOM</automated>
  </verify>
  <done>
    sniff/decode/parse — все 3 module созданы с unit-тестами зелёными. 5 CSV fixtures committed. devices_csv_import.rs preview-тесты зелёные (4 encoding варианта). Cyrillic round-trip подтверждён.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeviceService::import_csv_preview/commit + export_csv + Tauri commands + tests</name>
  <files>
    crates/trackly-app/src/dto/device.rs,
    crates/trackly-app/src/services/device_service.rs,
    crates/trackly-app/src/tauri_cmds/devices.rs,
    crates/trackly-app/src/http/devices.rs,
    crates/trackly-app/src/specta_export.rs,
    crates/trackly-app/tests/devices_csv_import.rs,
    crates/trackly-app/tests/devices_csv_export.rs,
    crates/trackly-app/tests/devices_csv_session.rs,
    crates/trackly-app/tests/export_bindings.rs
  </files>
  <read_first>
    crates/trackly-app/src/services/device_service.rs (Plans 03+04 — расширяем),
    crates/trackly-app/src/csv/session_store.rs (ImportSessionStore API),
    crates/trackly-app/src/csv/mod.rs (sniff/decode/parse re-exports),
    .planning/phases/02-ui/02-RESEARCH.md §Pattern 6 + §Pattern 7,
    .planning/phases/02-ui/02-RESEARCH.md §Pitfall 7 (had_replacements warning),
    .planning/phases/02-ui/02-CONTEXT.md §D-CSV-01 (preview-then-commit, per-row errors accumulation),
    .planning/phases/02-ui/02-CONTEXT.md §D-CSV-02 (UTF-8 BOM + `;` export rules),
    .planning/phases/02-ui/02-UI-SPEC.md §Copywriting §CSV import modal + §CSV export.
  </read_first>
  <behavior>
    Integration tests (devices_csv_import.rs extended):
    - `import_commit_inserts_devices` — preview utf8.csv → token → commit с auto-mapping → CsvImportReport { inserted: 4, failed: [] }. DB query shows 4 devices.
    - `import_commit_per_row_errors` — preview malformed_mixed_rows.csv → commit → inserted=3, failed.len()=2; failed[0].error.code == "Validation"; failed[0].error.message содержит «Наименование».
    - `import_commit_with_double_take_fails` — preview → take token first (success) → call commit again with same token → AppError::Validation { field: "token", message contains "expired" or "not found" }.
    - `import_commit_records_audit_log` — import 3 devices → audit_log получает 3 записи action='create' / entity_type='device' (одна транзакция per row inside writer.execute).
    
    Tests (devices_csv_export.rs):
    - `export_starts_with_bom` — `service.export_csv(DeviceFilter default).await` → string.bytes().take(3).collect::<Vec<_>>() == [0xEF, 0xBB, 0xBF].
    - `export_uses_semicolon_delimiter` — created device + export → returned string contains `;` between columns, не `,`.
    - `export_headers_russian` — first line содержит `Тип;Наименование;Инвентарный №`.
    - `export_cyrillic_roundtrip` — create device с name «Сидоров-Петроградский ... (ё) №42» → export → строка содержит exact name в UTF-8 после BOM.
    
    Tests (devices_csv_session.rs):
    - `session_put_returns_token_and_take_retrieves` — basic flow.
    - `session_expired_returns_none` — put → manipulate created Instant via test helper или sleep>5min (опционально skip с long-running flag), take → None.
    - `session_double_take_returns_none` — put → take (Some) → take (None).
  </behavior>
  <action>
    Шаг A — `dto/device.rs`: добавить
    - `#[derive(...)] pub struct CsvImportPreviewResponse { pub token: String, pub encoding: String, pub delimiter: String, pub headers: Vec<String>, pub preview_rows: Vec<Vec<String>>, pub total_rows: u64, pub had_replacements: bool }`. (`token: String` — Uuid serializ via Hyphenated; specta::Type Vec<u8>/Uuid имеют ограничения, использовать String.)
    - `pub struct CsvImportReport { pub inserted: u64, pub failed: Vec<RowError> }`.
    - `pub struct RowError { pub row_index: u64, pub error: AppError }` (или `pub error_code: String, pub error_message: String` — depending на specta::Type ability with nested AppError; executor выбирает; рекомендация: flatten в `RowError { row_index, code, message }` для проще JSON-shape).

    Шаг B — `services/device_service.rs`: добавить methods:
    
    - `pub async fn import_csv_preview(&self, bytes: Vec<u8>) -> Result<CsvImportPreviewResponse, AppError>`:
      - Cap size at 50MB (RESEARCH §Security V12): `if bytes.len() > 50 * 1024 * 1024 { return Err(AppError::Validation { field: "file", message: "Файл больше 50 МБ".into() }); }`.
      - `let profile = csv::sniff::detect(&bytes);`
      - `let (text, had_replacements) = csv::decode::decode_to_string(&bytes, profile.encoding);`
      - `let (headers, all_rows) = csv::parse::parse_rows(&text, profile.delimiter).map_err(|e| AppError::Validation { field: "file", message: format!("Не удалось разобрать CSV: {e}") })?;`
      - Construct `ImportSession { encoding: profile.encoding, delimiter: profile.delimiter, headers: headers.clone(), all_rows: all_rows.clone(), created: Instant::now() }`.
      - `let token = self.csv_sessions.put(session);`
      - preview_rows = first 5 of all_rows.
      - Return `CsvImportPreviewResponse { token: token.to_string(), encoding: profile.encoding.name().into(), delimiter: (profile.delimiter as char).to_string(), headers, preview_rows, total_rows: all_rows.len() as u64, had_replacements }`.

    - `pub async fn import_csv_commit(&self, token: String, mapping: HashMap<String, String>) -> Result<CsvImportReport, AppError>`:
      - `let uuid = Uuid::parse_str(&token).map_err(|_| AppError::Validation { field: "token", message: "Некорректный токен".into() })?;`
      - `let session = self.csv_sessions.take(uuid).ok_or_else(|| AppError::Validation { field: "token", message: "Сессия истекла или уже использована".into() })?;`
      - Map headers index by name: `let header_idx: HashMap<String, usize> = session.headers.iter().enumerate().map(|(i, h)| (h.clone(), i)).collect();`.
      - `let mut report = CsvImportReport { inserted: 0, failed: Vec::new() };`
      - Iterate `session.all_rows` with row_index (1-based for user display):
        - Build DeviceNew from mapping. Resolve field-to-CSV-column через mapping (e.g., mapping["Наименование"] = "Lenovo X1" → DeviceNew.name = "Lenovo X1"). Required field absence → push RowError, continue.
        - Lookup type_id и status_id по text label (resolve через seed lookups, либо принять что mapping содержит already-resolved IDs; рекомендация: backend сам делает lookup `device_types.name -> id` и `device_statuses.name -> id` через тёплые reader.acquire; для location — same).
        - Через `self.writer.execute(move |conn| { ... })` — каждая строка в отдельной транзакции (или batch в одной — executor решает; CONTEXT D-CSV-01 говорит «вставляет по одной строке через writer-channel; ошибки на каждую строку аккумулируются»). Рекомендация: per-row writer.execute → меньше batch-time, проще per-row error handling; либо single writer.execute с цикл и individual try-catch — executor wybirает; рекомендуется second (RESEARCH §Pitfall 7 предлагает one-tx per-row accumulation).
      - На каждый row.create_success — `report.inserted += 1`. На validation/conflict error — `report.failed.push(RowError { row_index, code, message })`.
      - На non-recoverable (DB busy, internal) — propagate; уже committed rows остаются.
      - Return report.

    - `pub async fn export_csv(&self, filter: DeviceFilter) -> Result<String, AppError>`:
      - `let response = self.list(filter, Pagination { offset: 0, limit: 1_000_000 }).await?;` (cap большой; для Phase 2 LAN-scale ok; streaming — Phase 7).
      - `let mut output = String::new(); output.push_str("\u{FEFF}");` (UTF-8 BOM via `\u{FEFF}` или явные bytes `output.push_str("\u{ef}\u{bb}\u{bf}")` — лучше `\u{FEFF}` чтобы Rust сам кодировал в UTF-8).
      - `let mut wtr = csv::WriterBuilder::new().delimiter(b';').from_writer(vec![]);`
      - Headers: `wtr.write_record(&["Тип", "Наименование", "Инвентарный №", "Серийный №", "Модель", "Технические характеристики", "Комплектация", "Состояние", "Расположение", "Статус"]).map_err(|e| AppError::Internal { source_chain: format!("csv writer: {e}") })?;`
      - For each DeviceDto: resolve type_id → name (через reader.acquire seed lookup или cached), same для status_id/location_id; write_record с строковыми значениями (Option<String> → empty string).
      - `let inner = wtr.into_inner().map_err(...)?; let body = String::from_utf8(inner).map_err(...)?; output.push_str(&body);`
      - Return output.

    Шаг C — Tauri commands + axum handlers + specta export.

    **CSV-related (3 commands):**
    - `devices_import_csv_preview(bytes: Vec<u8>) -> CsvImportPreviewResponse`. Note: Vec<u8> через invoke — Tauri 2 supports binary args; specta::Type для Vec<u8> works (генерит `number[]` или `Uint8Array` depending config).
    - `devices_import_csv_commit(token: String, mapping: HashMap<String, String>) -> CsvImportReport`.
    - `devices_export_csv(filter: DeviceFilter) -> String`.

    **FS helpers (2 commands — B2 pinned strategy):**

    Создать `crates/trackly-app/src/tauri_cmds/fs_helpers.rs`:

    - `pub async fn build_read_file_bytes(_ctx: &AppCtx, path: String) -> Result<Vec<u8>, AppError>` — Path validation:
      1. `let p = Path::new(&path);`
      2. `let canon = p.canonicalize().map_err(|e| AppError::Validation { field: \"path\".into(), message: format!(\"Не удалось распознать путь: {e}\") })?;`
      3. Reject если `canon.components().any(|c| matches!(c, Component::ParentDir))` — `AppError::Validation { field: \"path\", message: \"Путь не должен содержать `..`\" }`.
      4. Reject UNC: на Windows — `if path.starts_with(\"\\\\\\\\\")` → Validation.
      5. Extension check: `canon.extension().and_then(|e| e.to_str()) == Some(\"csv\")` иначе Validation.
      6. Metadata size cap: `let meta = std::fs::metadata(&canon)?; if meta.len() > 50 * 1024 * 1024 { return Validation \"Файл больше 50 МБ\" }`.
      7. `std::fs::read(&canon).map_err(...)?` → Ok(bytes).
    - `#[tauri::command] #[specta::specta] pub async fn read_file_bytes(state: tauri::State<'_, AppCtx>, path: String) -> Result<Vec<u8>, AppError> { build_read_file_bytes(state.inner(), path).await }`.
    - `pub async fn build_write_file_bytes(_ctx: &AppCtx, path: String, content: String) -> Result<(), AppError>` — same path validation steps 1-5 (extension `.csv` required); НЕ требует pre-existing file (write создаёт); cap content.len() <= 50MB; `std::fs::write(&path_canonical_parent_join_filename, content.as_bytes())?`. NOTE: для write canonicalize не работает на не-существующем файле — executor canonicalize'ит `parent_dir` и затем join'ит `filename` обратно (separate logic).
    - `#[tauri::command] #[specta::specta] pub async fn write_file_bytes(state: tauri::State<'_, AppCtx>, path: String, content: String) -> Result<(), AppError> { build_write_file_bytes(state.inner(), path, content).await }`.

    **axum routes (Phase 2 регистрирует, Phase 5+ активирует bind):**
    - `POST /api/v1/devices_import_csv_preview` (binary body)
    - `POST /api/v1/devices_import_csv_commit`
    - `POST /api/v1/devices_export_csv`
    - `POST /api/v1/read_file_bytes` (через `crates/trackly-app/src/http/fs_helpers.rs::router()`)
    - `POST /api/v1/write_file_bytes`

    `mod.rs` файлы (`tauri_cmds/mod.rs`, `http/mod.rs`) — добавить `pub mod fs_helpers;`.

    **specta_export.rs** — `collect_commands![..., devices_import_csv_preview, devices_import_csv_commit, devices_export_csv, read_file_bytes, write_file_bytes]`.

    **tests/export_bindings.rs** substring asserts: `CsvImportPreviewResponse`, `CsvImportReport`, `RowError`, `devices_import_csv_preview`, `devices_import_csv_commit`, `devices_export_csv`, `read_file_bytes`, `write_file_bytes`.

    **NOTE on capabilities (B2 + B4 cross-ref):** Helpers — это custom Tauri commands (НЕ tauri-plugin-fs), они автоматически разрешены через `core:default` permission уже declared в `capabilities/main.json` (Plan 03). НЕ нужно добавлять `fs:default` — это была альтернативная стратегия, которую отвергли в B2.

    Шаг D — Tests `devices_csv_import.rs` (extended): commit-тесты per `<behavior>`. `devices_csv_export.rs` и `devices_csv_session.rs` — новые файлы с поведениями.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app --test devices_csv_import 2&gt;&amp;1 | grep -E "test result"</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app --test devices_csv_export 2&gt;&amp;1 | grep -E "test result"</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app --test devices_csv_session 2&gt;&amp;1 | grep -E "test result"</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app --test export_bindings 2&gt;&amp;1 | grep -E "test result"</automated>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo clippy -p trackly-app --all-targets -- -D warnings 2&gt;&amp;1 | tail -8</automated>
    <automated>grep -c "import_csv_preview\|import_csv_commit\|export_csv" crates/trackly-app/src/services/device_service.rs  # ожидание: &gt;= 3</automated>
    <automated>grep -c "import_csv_preview\|import_csv_commit\|export_csv" crates/trackly-app/src/specta_export.rs  # ожидание: &gt;= 3</automated>
    <automated>grep -c "read_file_bytes\|write_file_bytes" crates/trackly-app/src/specta_export.rs  # ожидание: &gt;= 2 (B2 fix — FS helpers зарегистрированы)</automated>
    <automated>test -f crates/trackly-app/src/tauri_cmds/fs_helpers.rs &amp;&amp; grep -c "build_read_file_bytes\|build_write_file_bytes" crates/trackly-app/src/tauri_cmds/fs_helpers.rs  # ожидание: &gt;= 2</automated>
    <automated>grep -c "read_file_bytes\|write_file_bytes" crates/trackly-app/tests/export_bindings.rs  # ожидание: &gt;= 2 (bindings.ts substring asserts)</automated>
  </verify>
  <done>
    Полный CSV pipeline на backend: preview через chardetng+encoding_rs+csv, commit с writer.execute и per-row error accumulation, 5-min TTL session_store. Export даёт UTF-8 BOM + `;` + Russian headers. 3 integration test файла зелёные. bindings.ts содержит все типы и команды.
  </done>
</task>

<task type="auto">
  <name>Task 3: Frontend DeviceImportCsvModal (4-step wizard) + Export CSV кнопка + интеграция в DevicesPage</name>
  <files>
    ui/src/lib/api/devices.ts,
    ui/src/features/devices/DeviceImportCsvModal.svelte,
    ui/src/features/devices/DevicesPage.svelte
  </files>
  <read_first>
    ui/src/features/devices/DevicesPage.svelte (Plans 03+04 — расширяем для интеграции import/export buttons),
    ui/src/lib/components/Modal.svelte (size: 'md' | 'wide'),
    ui/src/lib/components/Button.svelte,
    ui/src/lib/components/Select.svelte,
    ui/src/bindings.ts (после Task 2 — содержит CsvImportPreviewResponse / CsvImportReport / RowError),
    .planning/phases/02-ui/02-UI-SPEC.md §Component Inventory §DeviceImportCsvModal (полная спека),
    .planning/phases/02-ui/02-UI-SPEC.md §Copywriting §CSV import modal (все 4 step headings + button copies + error format),
    .planning/phases/02-ui/02-UI-SPEC.md §Copywriting §CSV export (filename pattern + toasts).
  </read_first>
  <action>
    Шаг A — Расширить `ui/src/lib/api/devices.ts`:
    ```ts
    importCsvPreview: (bytes: number[]) => apiCall<CsvImportPreviewResponse>('devices_import_csv_preview', { bytes }),
    importCsvCommit: (token: string, mapping: Record<string, string>) =>
      apiCall<CsvImportReport>('devices_import_csv_commit', { token, mapping }),
    exportCsv: (filter: DeviceFilter) => apiCall<string>('devices_export_csv', { filter }),
    ```

    Шаг B — `ui/src/features/devices/DeviceImportCsvModal.svelte`. Modal size="wide". 4 шага:

    State (Svelte 5 runes):
    ```ts
    let step = $state<1|2|3|4>(1);
    let fileBytes = $state<Uint8Array | null>(null);
    let preview = $state<CsvImportPreviewResponse | null>(null);
    let mapping = $state<Record<string, string>>({}); // header → device field
    let report = $state<CsvImportReport | null>(null);
    let loading = $state(false);
    ```

    Step 1 (file pick): `<Button onclick={openFilePicker}>Выбрать файл…</Button>`. `openFilePicker`:
    ```ts
    import { open as openDialog } from '@tauri-apps/plugin-dialog';
    import { isTauri } from '$lib/stores/transport.svelte';
    
    async function openFilePicker() {
      if (isTauri) {
        const path = await openDialog({ filters: [{ name: 'CSV', extensions: ['csv'] }] });
        if (!path || Array.isArray(path)) return;
        // read file bytes — option A: backend helper command; option B: @tauri-apps/plugin-fs if capability allows; option C: just call backend with path and let it read.
        // RECOMMENDED: add helper Tauri command `read_file_bytes(path: String) -> Vec<u8>` in backend, OR use plugin-fs.
        // Executor выбирает; для простоты Plan 05 — backend reads via path:
        const bytes = await apiCall<number[]>('read_file_bytes', { path });
        fileBytes = new Uint8Array(bytes);
      } else {
        // browser fallback: HTML <input type="file">
        // Phase 5 will wire HTTP upload; Phase 2 browser-mode не активен, оставить TODO.
      }
      if (!fileBytes) return;
      loading = true;
      try {
        preview = await devices.importCsvPreview(Array.from(fileBytes));
        // auto-mapping: для каждой preview.headers — попытка совпадения с device DTO field labels (Тип/Наименование/Инвентарный №/...)
        mapping = autoMapHeaders(preview.headers);
        step = 2;
      } catch (e: any) { pushToast('error', e.message); } finally { loading = false; }
    }
    ```
    
    NOTE (B2 RESOLVED): `read_file_bytes(path: String) -> Vec<u8>` объявлен в Plan 05 Task 2 Step C — backend helper command в `crates/trackly-app/src/tauri_cmds/fs_helpers.rs` с path validation per T-02-05-02. Frontend просто вызывает `apiCall<number[]>('read_file_bytes', { path })` — типизация через bindings.ts.

    Step 2 (preview): показывает «Определена кодировка: **{preview.encoding}**, разделитель: «**{preview.delimiter}**». Показаны первые 5 строк». Если `preview.had_replacements` — warning banner «Внимание: возможные ошибки декодирования». Таблица первых 5 строк sticky-header. Кнопки [Отмена] [Далее].

    Step 3 (mapping): двухколонная таблица «Колонка CSV / Поле устройства». Per row — `<Select>` с опциями {«— не импортировать —», «Тип», «Наименование», ...10 полей}. Auto-populated по совпадению header label. Кнопки [Назад] [Импортировать].

    Step 4 (result): «Импортировано: **{report.inserted}**. Пропущено с ошибками: **{report.failed.length}**». Если failed > 0 — expander «Показать ошибки ({n})» который раскрывает list `Строка {row_index}: {message}`. Кнопка [Готово] закрывает modal и triggers refresh DeviceList.

    Step transitions: Click Импортировать → loading → `report = await devices.importCsvCommit(preview.token, mapping); step = 4;` На error → toast + остаться на step 3.

    Шаг C — Update `DevicesPage.svelte`:
    - Переменная `csvModalOpen = $state(false)`.
    - Header-action кнопка `<Button onclick={() => csvModalOpen = true}>Импорт CSV</Button>` (теперь активна).
    - Header-action кнопка `<Button onclick={exportCsv}>Экспорт CSV</Button>` с handler:
    ```ts
    import { save as saveDialog } from '@tauri-apps/plugin-dialog';
    async function exportCsv() {
      try {
        const csv = await devices.exportCsv({ status_id: statusFilter, type_id: null, include_deleted: false });
        const today = new Date().toISOString().slice(0, 10);
        const defaultPath = `устройства_${today}.csv`;
        if (isTauri) {
          const path = await saveDialog({ defaultPath, filters: [{ name: 'CSV', extensions: ['csv'] }] });
          if (!path) return;
          // write bytes — option: backend `write_file_bytes(path, content)` helper OR plugin-fs.
          await apiCall<void>('write_file_bytes', { path, content: csv });
          pushToast('success', `Экспортировано ${items.length} устройств.`);
        } else {
          // browser: trigger <a download>
          const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' });
          const url = URL.createObjectURL(blob);
          // ... <a href={url} download={defaultPath} click />
        }
      } catch (e: any) { pushToast('error', e.message); }
    }
    ```
    NOTE (B2 RESOLVED): `write_file_bytes` объявлен в Plan 05 Task 2 Step C; вызов через `apiCall<void>('write_file_bytes', { path, content })`.
    - Render `<DeviceImportCsvModal open={csvModalOpen} onClose={() => csvModalOpen = false} onImported={() => { csvModalOpen = false; refresh(); pushToast('success', 'Импорт завершён'); }} />`.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; cargo test -p trackly-app --test export_bindings 2&gt;&amp;1 | grep -E "test result"  # regenerate bindings.ts</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm svelte-check 2&gt;&amp;1 | tail -8</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm lint 2&gt;&amp;1 | tail -5</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm build 2&gt;&amp;1 | tail -5</automated>
    <automated>grep -c "importCsvPreview\|importCsvCommit\|exportCsv" ui/src/lib/api/devices.ts  # ожидание: 3</automated>
    <automated>grep -cE "Шаг|Импорт CSV|Импортировать|Выбрать файл|Сопоставление|Импорт завершён" ui/src/features/devices/DeviceImportCsvModal.svelte  # ожидание: &gt;= 4 русских строк</automated>
  </verify>
  <done>
    DeviceImportCsvModal 4-step wizard работает. Export CSV button открывает save-dialog. file pick — через tauri-plugin-dialog. Все строки на русском. bindings.ts типизирован.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Checkpoint — CSV import/export manual smoke (включая RU-locale Excel)</name>
  <what-built>
    Полный CSV slice: import wizard (4 шага, encoding+delimiter sniff, per-row errors, 5-min TTL), export UTF-8 BOM + `;` + Russian headers.
  </what-built>
  <how-to-verify>
    1. `pnpm tauri dev`. На странице Устройства — кликнуть «Импорт CSV». Open dialog появляется → выбрать `crates/trackly-app/tests/fixtures/devices/utf8.csv` или CP1251 файл.
    2. Step 2 показывает «Определена кодировка: UTF-8/WINDOWS_1251, разделитель: «,»/«;»» и первые 5 строк. Cyrillic строка «Сидоров-Петроградский...» отображается без mojibake.
    3. Step 3 — auto-mapping (Наименование → name, etc). Можно ручно изменить «не импортировать» для какой-то колонки.
    4. Click «Импортировать» → step 4 «Импортировано: 4. Пропущено с ошибками: 0.». Click «Готово» → DeviceList обновлён, 4 новых устройства в списке.
    5. Repeat с `malformed_mixed_rows.csv` → step 4 показывает inserted=3, failed=2; expander «Показать ошибки» — `Строка 2: Наименование обязательно`.
    6. Export: click «Экспорт CSV» → save-dialog с предложенным именем `устройства_2026-MM-DD.csv` → save → toast success.
    7. Открыть экспортированный файл в Russian Excel (manual, на Windows-машине): открывается корректно, столбцы разделены, Cyrillic читается без mojibake, заголовки на русском. Проверить через `xxd <file> | head -1`: первые 3 байта — `EFBBBF`.
    8. Проверить TTL session: открыть import wizard, выбрать файл (step 2), подождать 5+ минут (или triggered через test helper), нажать «Импортировать» → toast «Сессия истекла или уже использована».
  </how-to-verify>
  <resume-signal>Напишите "approved" или опишите проблему (encoding wrong / mojibake / per-row errors not shown / Excel can't open).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → Tauri command | Untrusted file bytes pass into backend; encoding sniff + size cap |
| User browser → backend (Phase 5) | Future HTTP upload — same controls + multipart limits |
| CSV import → INSERT INTO devices | Per-row validation; failures accumulated, не rollback |
| CSV cell → Excel formula (export) | Formula injection (=cmd, +1+1, -1+1, @SUM) — must prefix or quote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | DoS | Huge CSV upload memory exhaustion | mitigate | `service.import_csv_preview` checks `bytes.len() > 50 * 1024 * 1024` → AppError::Validation. Per-session memory bound by 50MB. |
| T-02-05-02 | Tampering | Path traversal через `read_file_bytes` / `write_file_bytes` helpers | mitigate | Реализация — `crates/trackly-app/src/tauri_cmds/fs_helpers.rs::build_read_file_bytes` / `build_write_file_bytes` (B2 pinned). Steps: canonicalize → reject `..` components → reject UNC → extension whitelist `[\".csv\"]` → metadata().len() ≤ 50MB. Любое нарушение → `AppError::Validation { field: \"path\", message: <ru> }`. |
| T-02-05-03 | Tampering | Excel formula injection в CSV export | mitigate | На export — prefix cells starting with `=`, `+`, `-`, `@` с `'` (Excel-safe). Применяется в `repos/devices_sqlite.rs export` helper или в DeviceService::export_csv. Executor добавляет sanitize-функцию `csv_safe(value: &str) -> String` per OWASP CSV injection guidance. |
| T-02-05-04 | Tampering | Malformed CSV (mixed encoding, ragged rows) → partial DB state | mitigate | Per-row errors accumulated в RowError (CONTEXT D-CSV-01); никакого rollback по validation failures (per-row OK). Non-recoverable DB errors → propagate to commit failure, partial commit OK на этом уровне (audit_log записи позволяют traceability). |
| T-02-05-05 | Information Disclosure | Encoding detection логирует пользовательские данные | mitigate | `tracing::debug!("encoding detected", encoding=...)` без content; no body-logging. |
| T-02-05-06 | DoS | ImportSessionStore memory leak (failed sessions) | mitigate | Lazy sweep on `put` (existing in Plan 01 impl); plus single-use `take`. Map size bound by concurrent users × few sessions. |
| T-02-05-07 | Tampering | UUID v4 token enumeration | accept | 122-bit entropy; не enumerable в реальном времени; 5-min TTL limits replay window. |
| T-02-05-08 | Tampering | mapping HashMap с unknown keys → SQL injection | mitigate | Backend resolves header key → enum DeviceField (whitelisted: type/name/inventory_no/serial_no/model/specs/kit/state/location/status); unknown keys ignored. DeviceField is enum, not string interpolation. |
| T-02-05-SC | Tampering | uuid, csv, chardetng, encoding_rs install | mitigate | Package legitimacy verified Plan 01 (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `cargo test --workspace --no-fail-fast` зелёный, 3 новых CSV test файла + 5 fixtures.
- `cargo clippy -- -D warnings` зелёный.
- `pnpm svelte-check && pnpm build` зелёный.
- Manual smoke (Task 4) — import 4 кодировок + export в RU Excel.
</verification>

<success_criteria>
- DEV-12: CSV import с детектом UTF-8 / UTF-8 BOM / CP1251; разделители `,` и `;`; 5-row preview; per-row errors accumulated; 5-min TTL token.
- DEV-13: CSV export UTF-8 BOM + `;` + русские заголовки; открывается в RU Excel без mojibake.
- Cyrillic строка «Сидоров-Петроградский... (ё) №42» round-trip через все 4 encoding варианта.
- ImportSessionStore TTL работает; double-take возвращает None.
- Excel formula injection mitigated на export.
- Path traversal через read_file_bytes mitigated.
- Phase 2 Success Criteria #4 (CSV import + export) — выполнен.
</success_criteria>

<output>
Create `.planning/phases/02-ui/02-05-SUMMARY.md` при завершении.
</output>
