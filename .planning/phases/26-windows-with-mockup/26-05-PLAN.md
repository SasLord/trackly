---
phase: 26-windows-with-mockup
plan: 05
type: execute
wave: 2
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - ui/src/features/devices/DeviceFilters.svelte
autonomous: true
requirements: [WIN-02]

must_haves:
  truths:
    - "Поиск, переключение статуса и группировка ведут себя ИДЕНТИЧНО поведению до правки: 250мс debounce на поиске, пять статусов в порядке «Все · На складе · В работе · На ремонте · Списано», счётчик «Все» = сумма остальных, сигнатуры onSearchChange/onStatusChange/onGroupedChange не меняются (D-10 — жёсткий контракт, не деталь)."
    - "Поисковое поле визуально мигрирует на примитив `Input` с иконкой-лупой слева (12px) через новый `iconLeft` проп (D-10, §3.13)."
    - "Табы статуса визуально мигрируют на примитив `Tabs` (variant=\"underline\"), сохраняя пять статусов и активный счётчик soft-фона (§3.13)."
    - "Чекбокс группировки визуально мигрирует на примитив `Checkbox`, сохраняя обработчик onGroupedChange (§3.13)."
  artifacts:
    - path: "ui/src/features/devices/DeviceFilters.svelte"
      provides: "поиск/статус/группировка на примитивах Input/Tabs/Checkbox, поведение фильтров без изменений"
      min_lines: 150
  key_links:
    - from: "ui/src/features/devices/DeviceFilters.svelte"
      to: "ui/src/lib/components/Input.svelte"
      via: "<Input iconLeft={searchIcon} value={localSearch} oninput={handleSearchInput}>"
      pattern: "<Input[^>]*iconLeft"
    - from: "ui/src/features/devices/DeviceFilters.svelte"
      to: "ui/src/lib/components/Tabs.svelte"
      via: "<Tabs variant=\"underline\" tabs={...} active={...} onchange={onStatusChange}>"
      pattern: "<Tabs[^>]*variant=\"underline\""
    - from: "ui/src/features/devices/DeviceFilters.svelte"
      to: "ui/src/lib/components/Checkbox.svelte"
      via: "<Checkbox checked={grouped} onchange={onGroupedChange}>"
      pattern: "<Checkbox[^>]*onchange={onGroupedChange}"
---

<objective>
Migrate `DeviceFilters.svelte`'s hand-rolled search input, status switch-bar, and group checkbox onto
the Phase 24/25 primitives (`Input` with the new `iconLeft` slot, `Tabs` variant="underline",
`Checkbox`) per D-10 — WITHOUT changing any filter behavior. This is the single highest-risk task in
the phase per 26-CONTEXT.md: "поведение фильтров (FTS-поиск, переключение статуса, группировка) не
должно измениться — это контракт, а не деталь."

Purpose: `DeviceFilters` already matches the mockup structurally (search-with-icon, tabs-with-counts,
group checkbox) — this plan is a pure primitive substitution, isolated in its own plan (separate from
Plan 26-04's lower-risk header/table-frame work) so the risky behavior-preservation work gets full
attention and a dedicated verification pass.
Output: `DeviceFilters.svelte` renders identically-behaving filters on the new primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-windows-with-mockup/26-UI-SPEC.md
@.planning/phases/26-windows-with-mockup/26-CONTEXT.md
@.planning/phases/26-windows-with-mockup/26-PATTERNS.md
</context>

<interfaces>
From Plan 26-03's Input.svelte (contract, must exist before this plan runs):
```ts
iconLeft?: Snippet;  // 12px left icon, padding-left:34px on the input when passed
value: string;
oninput?: (_value: string) => void;
```

From ui/src/lib/components/Tabs.svelte (existing, unchanged this phase):
```ts
interface Tab { key: string; label: string; count?: number; disabled?: boolean; }
interface Props { variant?: 'underline' | 'segmented'; tabs: Tab[]; active: string; onchange?: (_key: string) => void; ariaLabel?: string; }
```

From ui/src/lib/components/Checkbox.svelte (existing, unchanged this phase):
```ts
interface Props { checked?: boolean; disabled?: boolean; invalid?: boolean; id?: string; onchange?: (_checked: boolean) => void; children?: Snippet; }
```
</interfaces>

<code_context>
`DeviceFilters.svelte` (current, full 244 lines). The debounce logic (`localSearch`, `debounceTimer`,
`handleSearchInput`, lines 26-39), the `STATUSES` array (lines 41-47), `totalCount`/`getCount`
derivations (lines 50-55), and the three prop callbacks (`onSearchChange`/`onStatusChange`/`onGroupedChange`)
are ALL hard-contract, verbatim-preserve per D-10. Only the rendering markup/CSS around them changes.

`Tabs` uses `key: string` for its `Tab` type, but `STATUSES` uses `id: number | null` — the adapter
must derive `tabs: Tab[]` with `key: String(s.id)` and compare `active={String(statusFilter)}`,
converting back to `number | null` in the `onchange` callback before calling `onStatusChange`.
</code_context>

<tasks>

<task type="auto">
  <name>Task 1: Search input — migrate to Input with iconLeft, preserve debounce</name>
  <files>ui/src/features/devices/DeviceFilters.svelte</files>
  <read_first>ui/src/features/devices/DeviceFilters.svelte (current, full 244 lines, especially lines 26-39 debounce logic and lines 58-81 current search markup), ui/src/lib/components/Input.svelte (final shape from Plan 26-03)</read_first>
  <action>
    Import `Input` from `$lib/components/Input.svelte`. Replace the current
    `<div class="search-wrapper"><span class="search-icon">...inline SVG...</span><input class="search-input" .../></div>`
    block with `<Input type="search" value={localSearch} oninput={handleSearchInput} placeholder="Поиск по наименованию, инвентарному, серийному, модели" aria-describedby={undefined}>{#snippet iconLeft()}`
    wrapping the EXACT SAME inline 16×16 magnifier SVG markup currently at lines 62-71 (circle
    cx=6.5 cy=6.5 r=4.5 + path M10 10L14 14, `stroke="currentColor"`) `{/snippet}</Input>`.

    Do NOT modify `localSearch` (state), `debounceTimer`, the `$effect` syncing `localSearch = searchQuery`,
    or `handleSearchInput` (lines 26-39) — `Input`'s `oninput` callback signature
    (`(_value: string) => void`) already matches `handleSearchInput`'s parameter shape exactly, so it
    passes through unchanged. Note: `Input`'s native element does not currently expose an
    `aria-label` prop — if `svelte-check` flags this, add a visually-hidden `<label for={...}>Поиск устройств</label>`
    instead of relying on a prop `Input` doesn't have; do not add new props to `Input.svelte` in this
    plan (that file belongs to Plan 26-03, already closed).

    Remove the now-unused `.search-wrapper`, `.search-icon`, `.search-input` style rules.
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '<Input' ui/src/features/devices/DeviceFilters.svelte` matches with `iconLeft` used as a snippet
    - `grep -n 'oninput={handleSearchInput}' ui/src/features/devices/DeviceFilters.svelte` matches (unchanged handler reference)
    - `grep -n 'setTimeout(() => {' ui/src/features/devices/DeviceFilters.svelte` still matches inside `handleSearchInput`, with `250` as the debounce delay unchanged
    - `grep -c 'class="search-input"' ui/src/features/devices/DeviceFilters.svelte` returns 0 (old markup removed)
    - `pnpm --dir ui run svelte-check` exits 0
  </acceptance_criteria>
  <done>Search input renders via Input+iconLeft with the exact same 250ms-debounced onSearchChange wiring as before.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Status tabs → Tabs primitive, group checkbox → Checkbox primitive (D-10 hard contract)</name>
  <files>ui/src/features/devices/DeviceFilters.svelte</files>
  <read_first>ui/src/features/devices/DeviceFilters.svelte (current, full 244 lines, especially lines 41-56 STATUSES/counts and lines 83-112 current tabs/checkbox markup), ui/src/lib/components/Tabs.svelte (full 167 lines), ui/src/lib/components/Checkbox.svelte (full 120 lines)</read_first>
  <behavior>
    - Given `statusFilter = null` and `counts` containing entries for statuses 1-4, the derived
      `tabs: Tab[]` must render exactly 5 tabs labeled "Все · На складе · В работе · На ремонте ·
      Списано" in that exact order, with the "Все" tab's count equal to the sum of the other four.
    - Given a click/keyboard-activate on any tab, `onStatusChange` must be called with the EXACT
      same `number | null` value `STATUSES[i].id` that the pre-migration `.status-tab` `onclick`
      handler used to pass — no signature or value-mapping change visible to `DevicesPage.svelte`.
    - Given a click on the group checkbox, `onGroupedChange` must be called with the new boolean
      checked state, identical to the pre-migration native-checkbox `onchange` handler.
  </behavior>
  <action>
    Import `Tabs` from `$lib/components/Tabs.svelte` and `Checkbox` from `$lib/components/Checkbox.svelte`.

    Derive `const tabItems = $derived<Tab[]>(STATUSES.map((s) => ({ key: String(s.id), label: s.label, count: getCount(s.id) })));`
    (keep `STATUSES` and `getCount` exactly as they are — lines 41-55 untouched). Render
    `<Tabs variant="underline" tabs={tabItems} active={String(statusFilter)} ariaLabel="Фильтр по статусу" onchange={(key) => onStatusChange(key === 'null' ? null : Number(key))} />`
    replacing the current `<div class="status-bar" role="tablist">...</div>` block (lines 85-101).
    Note the `key === 'null'` check: `STATUSES[0].id` is the literal `null`, and `String(null)` is
    the string `'null'` — this is the exact inverse mapping needed to recover `number | null` from
    `Tabs`'s `string`-keyed contract; verify this round-trips correctly for all 5 entries, not just
    the non-null ones.

    Replace `<label class="group-toggle"><input type="checkbox" class="group-checkbox" .../><span class="group-label">Группировать похожие</span></label>`
    (lines 103-111) with `<Checkbox checked={grouped} onchange={onGroupedChange}>Группировать похожие</Checkbox>`.

    Update `.filters-row` container styles per §3.13: `gap: var(--tr-space-md)` → keep as-is if
    already 16px-equivalent (confirm against `--tr-space-md` = 16px, matches Д spec `gap:16px` — no
    change needed there); container padding/gap for `.device-filters` root: `gap: var(--tr-space-xs)` (8px) → `12px`;
    `padding-bottom: var(--tr-space-xs)` → `12px`; `margin-bottom: var(--tr-space-md)` (16px) → `14px`.

    Remove the now-unused `.status-bar`, `.status-tab`, `.count-badge`, `.group-toggle`,
    `.group-checkbox`, `.group-label` style rules — `Tabs`/`Checkbox` own their own styling.
  </behavior>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'const STATUSES' ui/src/features/devices/DeviceFilters.svelte` still matches verbatim with the same 5 entries in the same order
    - `grep -n '<Tabs variant="underline"' ui/src/features/devices/DeviceFilters.svelte` matches
    - `grep -n "key === 'null' ? null : Number(key)" ui/src/features/devices/DeviceFilters.svelte` matches (round-trip mapping present)
    - `grep -n '<Checkbox checked={grouped} onchange={onGroupedChange}>' ui/src/features/devices/DeviceFilters.svelte` matches
    - `grep -c 'class="status-tab"' ui/src/features/devices/DeviceFilters.svelte` returns 0 (old markup removed)
    - `pnpm --dir ui run svelte-check` exits 0
    - Manual: click each of the 5 status tabs in the running app and confirm the device list re-filters exactly as before; toggle "Группировать похожие" and confirm grouping toggles as before
  </acceptance_criteria>
  <done>Status filter and group toggle render on Tabs/Checkbox primitives with byte-identical STATUSES order, counts, and callback signatures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-typed search query → FTS5 query | `searchQuery` (from this component's debounced input) flows into `devices.search()` → backend FTS5 query — unchanged code path, only the input markup changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Tampering | Search input value → FTS query | accept | The value still flows through the exact same `handleSearchInput`/`onSearchChange` path into the existing (unchanged) `devices.search()` API call — this plan does not touch query construction or sanitization. |
| T-26-05-02 | Repudiation | Status-filter round-trip mapping (`String(id)` ↔ `Number(key)`) | mitigate | Task 2's acceptance criteria explicitly grep-verify the `'null'` round-trip case, since a silent off-by-one here would silently mis-filter the device list (a data-integrity bug, not classic STRIDE, but the highest-risk item in this plan per D-10). |
| T-26-05-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies introduced. |
</threat_model>

<verification>
1. `pnpm --dir ui run svelte-check` passes.
2. Manual click-through: all 5 status tabs, search debounce (type and wait 250ms), group toggle — confirm identical behavior to pre-migration, per Plan 26-08's UAT checklist.
</verification>

<success_criteria>
- DeviceFilters renders on Input/Tabs/Checkbox primitives with zero behavioral drift (D-10).
- STATUSES order, counts, and all three callback signatures are unchanged.
</success_criteria>

<output>
Create `.planning/phases/26-windows-with-mockup/26-05-SUMMARY.md` when done
</output>
