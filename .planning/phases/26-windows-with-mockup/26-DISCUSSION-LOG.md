# Phase 26: Окна с готовым макетом - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-19
**Phase:** 26-windows-with-mockup
**Areas discussed:** Виджеты дашборда без данных, Границы app shell, Объём WIN-02 после Фазы 25, Что считать «точным соответствием», Побочный эффект инверсии фонов, Состояния загрузки и ошибок, Адаптивность и узкий экран, Порядок работ и волны

---

## Виджеты дашборда без данных

| Option | Description | Selected |
|--------|-------------|----------|
| Только раскладка, панели — позже | Фаза остаётся чисто визуальной; три панели и кнопка в бэклог | ✓ |
| Гибрид — что есть в данных | Строим панели, покрытые существующим DTO | |
| Строим всё по макету | Расширяем DashboardWidgetDto, запросы, тесты | |

**User's choice:** Только раскладка, панели — позже
**Notes:** Подтверждено доказательством из кода — `DashboardWidgetDto` (`crates/trackly-app/src/dto/reports.rs:138`) не содержит процентов остатка, списка заявок и уровней тонера.

| Option | Description | Selected |
|--------|-------------|----------|
| График во всю ширину | Ряд из 4 карточек, под ним график на всю ширину | ✓ |
| Держать 1.7fr/1fr сразу | Геометрия по макету, правая колонка пуста | |
| Решай сам | На усмотрение планировщика | |

**User's choice:** График во всю ширину

| Option | Description | Selected |
|--------|-------------|----------|
| Кнопку «+ Создать акт» не добавлять | Стили шапки и селектов — по макету, новая точка входа не заводится | ✓ |
| Добавить кнопку | Ведёт на существующую форму создания акта | |

**User's choice:** Кнопку не добавлять

| Option | Description | Selected |
|--------|-------------|----------|
| Сохранить warningItems | Предупреждение о низком остатке остаётся, оформляется токенами warning | ✓ |
| Убрать, как в макете | Карточка строго по макету | |

**User's choice:** Сохранить warningItems
**Notes:** Удаление было бы регрессом функциональности против SC #3.

---

## Границы app shell

| Option | Description | Selected |
|--------|-------------|----------|
| Sidebar — в Фазе 26 | Оболочка делается один раз, Фазы 27-28 наследуют | ✓ |
| Только контент, shell позже | Трогаем только область main | |
| Shell в 26, но только стили | Геометрия и токены без изменения состава | |

**User's choice:** Sidebar — в Фазе 26
**Notes:** Выбор против «минимального» варианта; отдельной фазы под shell в роадмапе нет.

| Option | Description | Selected |
|--------|-------------|----------|
| Принять инверсию макета | Сайдбар `--tr-bg`, контент `--tr-surface` | ✓ |
| Отложить инверсию | Оставить текущие фоны до Фазы 28 | |

**User's choice:** Принять инверсию макета

| Option | Description | Selected |
|--------|-------------|----------|
| Общий PageHeader-компонент | Новый примитив + скроллящееся тело; Layout снимает padding | ✓ |
| Локально в двух страницах | Правим .page-header в DashboardPage и DevicesPage | |

**User's choice:** Общий PageHeader-компонент

| Option | Description | Selected |
|--------|-------------|----------|
| Стили макета, содержание приложения | Логотип добавляем, «Тема»→«Оформление», «Выйти» и ролевая навигация остаются | ✓ |
| Строго по макету | Убрать кнопку «Выйти» | |

**User's choice:** Стили макета, содержание приложения

| Option | Description | Selected |
|--------|-------------|----------|
| EmployeeLayout вне Фазы 26 | Остаётся Фазе 29 (WIN-11) | ✓ |
| Захватить заодно | Привести и EmployeeLayout | |

**User's choice:** Вне Фазы 26

---

## Объём WIN-02 после Фазы 25

| Option | Description | Selected |
|--------|-------------|----------|
| Перевести DeviceFilters на Tabs/Input/Checkbox | Самописные элементы заменяются примитивами CMP-02/CMP-04 | ✓ |
| Только подравнять стили | Оставить самописную разметку | |

**User's choice:** Перевести на Tabs/Input/Checkbox

| Option | Description | Selected |
|--------|-------------|----------|
| Обёртка таблицы — в компонент Table | Рамка, radius, тень, скролл-контейнер туда же, где футер и пустые состояния | ✓ |
| Локально в DeviceList | Table не трогается | |

**User's choice:** В компонент Table

| Option | Description | Selected |
|--------|-------------|----------|
| Сверить групповые строки с макетом окна | Разрешать расхождения с TableRows.dc в пользу окна | |
| Не трогать строки | Строки закрыты Фазой 25; Фаза 26 — только вокруг таблицы | ✓ |

**User's choice:** Не трогать строки

---

## Что считать «точным соответствием»

| Option | Description | Selected |
|--------|-------------|----------|
| Чек-лист значений + глазами | Таблица значений из .dc, построчная сверка, затем визуальный UAT | ✓ |
| Скриншоты рядом | Сравнение глазами без формального чек-листа | |
| Добавить playwright-снимки | Новая инфраструктура визуального регресса | |

**User's choice:** Чек-лист значений + глазами
**Notes:** Мотивировано ложным `[VERIFIED]` Фазы 24.

| Option | Description | Selected |
|--------|-------------|----------|
| Переформулировать SC #1 | Явное указание правки в ROADMAP, как D-01 Фазы 25 | ✓ |
| Оставить как есть | Верификатор разберётся по CONTEXT.md | |

**User's choice:** Переформулировать SC

| Option | Description | Selected |
|--------|-------------|----------|
| Обе темы в границах фазы | Светлая и тёмная проверяются обе | ✓ |
| Только светлая | Тёмную сверить в Фазе 30 | |

**User's choice:** Обе темы

---

## Побочный эффект инверсии фонов

| Option | Description | Selected |
|--------|-------------|----------|
| Принять регресс до Фаз 27-28 | Карточки семи окон временно сольются с фоном | ✓ |
| Временный шим для старых окон | Класс-обёртка с прежним фоном, снимается в Фазе 28 | |
| Сразу поднять карточки везде | Перевести все окна на `--tr-surface-raised` | |

**User's choice:** Принять регресс до Фаз 27-28
**Notes:** Релиза в середине v1.2 не будет.

---

## Состояния загрузки и ошибок

| Option | Description | Selected |
|--------|-------------|----------|
| Сохранить поведение, перекрасить | Логика loading/error не меняется, высота карточки сохраняется | ✓ |
| Спроектировать skeleton | Новый примитив для всей системы | |

**User's choice:** Сохранить текущее поведение, перекрасить

---

## Адаптивность и узкий экран

| Option | Description | Selected |
|--------|-------------|----------|
| Минимум по макету | auto-fit сетки, горизонтальный скролл таблицы, сайдбар не трогаем | |
| Фиксированно по макету | 4 колонки всегда | |
| Полноценная адаптивность | Брейкпоинты, сворачиваемый сайдбар, мобильный вид | ✓ |

**User's choice:** Полноценная адаптивность

**Follow-up — насколько далеко:**

| Option | Description | Selected |
|--------|-------------|----------|
| Сетки + брейкпоинты, сайдбар позже | Два окна перестраиваются, сайдбар фиксирован | |
| Включая сворачиваемый сайдбар | Сайдбар схлопывается/выезжает | |
| Включая мобильный вид | Полный мобильный layout для LAN-браузера с телефона | ✓ |

**User's choice:** Включая мобильный вид
**Notes:** Claude явно предупредил, что это выходит за формулировку WIN-01/WIN-02 и требует правки требований; пользователь подтвердил выбор.

**Follow-up — источник истины для мобильного:**

| Option | Description | Selected |
|--------|-------------|----------|
| Вывести из токенов и компонентов | Тот же приём, что запланирован для Фаз 27-28 (окна без макета) | ✓ |
| Сначала сделать макет | Прогнать /gsd-sketch или /gsd-ui-phase 26 перед планированием | |
| Только чтобы не ломалось | Без отдельного мобильного дизайна | |

**User's choice:** Вывести из токенов и компонентов

---

## Порядок работ и волны

| Option | Description | Selected |
|--------|-------------|----------|
| Зафиксировать порядок явно | Волна 1 — оболочка + обёртка Table; Волна 2 — два окна параллельно | ✓ |
| На усмотрение планировщика | Как в Фазе 25 | |

**User's choice:** Зафиксировать порядок явно

---

## Claude's Discretion

- Форма API `PageHeader` (пропы vs snippets, где живёт slot действий).
- Конкретные брейкпоинты и механика сворачивания сайдбара.
- Во что превращается строка таблицы на узкой ширине.
- Дробление волн на планы внутри зафиксированного порядка.
- Способ встраивания обёртки в `Table` без поломки витрины и `ActFormItemsTable`.

## Deferred Ideas

- Панели дашборда «Низкий остаток» (с %), «Последние заявки», «Мониторинг картриджей» — требуют расширения `DashboardWidgetDto`.
- Кнопка «+ Создать акт» в шапке дашборда.
- Возврат сетки 1.7fr/1fr на дашборде после появления панелей.
- `EmployeeLayout` по новой системе — Фаза 29, WIN-11.
- Перевод карточек семи оставшихся окон на `--tr-surface-raised` — Фазы 27-28.
- Механика выделения строк в Устройствах.
- Сортировка колонок и липкая шапка таблицы.
- Грep-гейт на `:global(` в plain `.scss` (WR-15 Фазы 24).
