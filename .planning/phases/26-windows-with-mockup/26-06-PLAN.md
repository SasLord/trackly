---
phase: 26-windows-with-mockup
plan: 06
type: execute
wave: 2
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - ui/src/features/dashboard/DashboardPage.svelte
autonomous: true
requirements: [WIN-01, WIN-12]

must_haves:
  truths:
    - "Шапка Дашборда использует общий `PageHeader` (variant=\"fixed\") вместо самописной разметки; кнопка «+ Создать акт» НЕ добавляется — макет её показывает, но это новая точка входа вне границ фазы (D-03)."
    - "Три панели макета без данных в DTO («Низкий остаток» с %, «Последние заявки», «Мониторинг картриджей») НЕ строятся — фаза остаётся чисто визуальной (D-01)."
    - "Раскладка меняется на ряд из 4 карточек-статистик (repeat(4,1fr), gap 16px) + график во всю ширину ниже, вместо сетки 1.7fr/1fr/3fr-2fr — правая колонка пуста без панелей (D-02)."
    - "На ширине < 1280px (`$bp-xl`) ряд статистик сжимается в repeat(2,1fr); на < 560px (`$bp-sm`) — в 1fr (D-15/D-16, WIN-12)."
    - "Селекты месяц/год визуально приведены к макету (height 32px, radius 6px, border-strong, background surface, gap 10px между контролами) без изменения обработчиков reloadWidgets (SC #3)."
  artifacts:
    - path: "ui/src/features/dashboard/DashboardPage.svelte"
      provides: "PageHeader(variant=fixed), stat-row repeat(4,1fr) + full-width chart, responsive collapse"
      min_lines: 260
  key_links:
    - from: "ui/src/features/dashboard/DashboardPage.svelte"
      to: "ui/src/lib/components/PageHeader.svelte"
      via: "<PageHeader title=\"Дашборд\" variant=\"fixed\">"
      pattern: "<PageHeader[^>]*variant=\"fixed\""
    - from: "ui/src/features/dashboard/DashboardPage.svelte"
      to: "ui/src/styles/_breakpoints.scss"
      via: "@use for $bp-xl/$bp-sm media queries"
      pattern: "@use '\\.\\./\\.\\./styles/_breakpoints'"
---

<objective>
Migrate `DashboardPage.svelte`'s header to `PageHeader` and restructure its grid per D-02 (4-card
stat row + full-width chart below, since the mockup's right-hand panel column has no data source
yet — D-01). Add the two responsive breakpoints for the stat row (WIN-12).

Purpose: closes WIN-01's shell/layout gap without inventing the three data-less mockup panels or a
new "+ Создать акт" entry point — both are explicitly out of scope (D-01, D-03). Data-loading logic
(`loadWidgets`/`loadChart`, period-select handlers) is untouched (D-14, SC #3).
Output: `DashboardPage.svelte` renders through `PageHeader` with the new grid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-windows-with-mockup/26-UI-SPEC.md
@.planning/phases/26-windows-with-mockup/26-CONTEXT.md
@.planning/phases/26-windows-with-mockup/26-PATTERNS.md
</context>

<interfaces>
From Plan 26-01's PageHeader.svelte (contract, must exist before this plan runs):
```ts
interface Props { title: string; variant?: 'fixed' | 'wrap'; actions?: Snippet; }
```

From Plan 26-01's _breakpoints.scss (contract, must exist before this plan runs):
```scss
$bp-xl: 1280px; $bp-lg: 1024px; $bp-md: 768px; $bp-sm: 560px;
```
</interfaces>

<code_context>
`DashboardPage.svelte` (current, full 311 lines) — `loadWidgets`/`loadChart` (lines 76-134),
`reloadWidgets`/`handleWindowChange` and all 4 `<StatWidget>` + `<ChartWidget>` call sites
(lines 165-240) are UNCHANGED by this plan; only the `<header class="page-header">` block
(lines 137-163) and `.dashboard-grid`/`.dashboard-col` styles (lines 290-311) change.

Current grid: `.dashboard-grid { grid-template-columns: 3fr 2fr; }` with `.dashboard-col--main`
(devices, cartridges, chart) and `.dashboard-col--side` (requests, printers) — this two-column split
is REPLACED by a single stat-row of all 4 `StatWidget`s followed by `ChartWidget` full-width below
(D-02). The 4 `StatWidget` components themselves (title/mainNumber/breakdown/warningItems props) are
NOT reordered in meaning — just re-laid-out from 2 columns into 1 row + full-width chart.
</code_context>

<tasks>

<task type="auto">
  <name>Task 1: Header migration to PageHeader(variant=fixed) + period-select restyle</name>
  <files>ui/src/features/dashboard/DashboardPage.svelte</files>
  <read_first>ui/src/features/dashboard/DashboardPage.svelte (current, full 311 lines, lines 137-163 header block and lines 268-288 period-select styles), ui/src/lib/components/PageHeader.svelte (final shape from Plan 26-01)</read_first>
  <action>
    Import `PageHeader` from `$lib/components/PageHeader.svelte`. Replace
    `<header class="page-header"><h1 class="page-title">Дашборд</h1><div class="period-selector">...2 selects...</div></header>`
    with `<PageHeader title="Дашборд" variant="fixed">{#snippet actions()}` wrapping the SAME two
    `<select>` elements verbatim (month/year, `bind:value={periodMonth}`/`bind:value={periodYear}`,
    `onchange={reloadWidgets}`) `{/snippet}</PageHeader>`. Do NOT add a "+ Создать акт" button or any
    new action (D-03 — out of scope).

    Restyle `.period-selector`: `gap: var(--tr-space-xs)` (8px) → `gap: 10px` (Д:69). Restyle
    `.period-select`: `border-radius: var(--tr-radius-xs)` (4px) → `border-radius: 6px`;
    `border: 1px solid var(--tr-border)` → `border: 1px solid var(--tr-border-strong)`;
    `background: var(--tr-bg)` → `background: var(--tr-surface)`. `height: 32px` and
    `padding: 0 var(--tr-space-xs)` (8px) are already correct per §3.9 — leave unchanged.

    Remove the now-unused `.page-header`, `.page-title` style rules.
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '<PageHeader title="Дашборд" variant="fixed">' ui/src/features/dashboard/DashboardPage.svelte` matches
    - `grep -c 'class="page-header"' ui/src/features/dashboard/DashboardPage.svelte` returns 0
    - `grep -n 'gap: 10px' ui/src/features/dashboard/DashboardPage.svelte` matches inside `.period-selector`
    - `grep -n 'border: 1px solid var(--tr-border-strong)' ui/src/features/dashboard/DashboardPage.svelte` matches inside `.period-select`
    - `grep -c 'Создать акт' ui/src/features/dashboard/DashboardPage.svelte` returns 0 (D-03 — no new entry point)
    - `grep -n 'bind:value={periodMonth}' ui/src/features/dashboard/DashboardPage.svelte` still matches unchanged
    - `pnpm --dir ui build` exits 0
  </acceptance_criteria>
  <done>Dashboard header renders through PageHeader(variant=fixed) with restyled selects and unchanged reloadWidgets wiring; no new "+ Создать акт" button.</done>
</task>

<task type="auto">
  <name>Task 2: Grid restructure — stat row (repeat(4,1fr)) + full-width chart, responsive</name>
  <files>ui/src/features/dashboard/DashboardPage.svelte</files>
  <read_first>ui/src/features/dashboard/DashboardPage.svelte (current, full 311 lines, lines 165-241 widget markup and lines 290-311 grid styles), ui/src/styles/_breakpoints.scss (created in Plan 26-01)</read_first>
  <action>
    Restructure the markup inside `.dashboard-grid`: replace the two `.dashboard-col--main`/`.dashboard-col--side`
    divs with a new `<div class="stat-row">` containing all 4 `<StatWidget>` calls in their CURRENT
    prop order (devices, cartridges, requests, printers — do not reorder or change any prop), followed
    by the single `<ChartWidget>` call as a direct sibling (full width, no wrapping column div).

    `.dashboard-grid`: change from `grid-template-columns: 3fr 2fr; gap: var(--tr-space-2xl);` to
    `display:flex; flex-direction:column; gap:16px;` (D-02 — no more 2-column grid; keep
    `flex:1; overflow:auto; padding:24px` as the body-container rule per §3.8, replacing the old
    `padding: var(--tr-space-2xl)` value of 32px with 24px). `.stat-row`: `display:grid;
    grid-template-columns: repeat(4, 1fr); gap: 16px;`.

    Add responsive overrides using `@use '../../styles/_breakpoints' as bp;` at the top of the
    `<style lang="scss">` block: `@media (max-width: bp.$bp-xl) { .stat-row { grid-template-columns: repeat(2, 1fr); } }`
    and `@media (max-width: bp.$bp-sm) { .stat-row { grid-template-columns: 1fr; } .dashboard-grid { padding: 16px; } }`
    (§6.2 thresholds). Remove the old `@media (max-width: 1280px) { .dashboard-grid { grid-template-columns: 1fr; } }`
    block (superseded).

    Remove now-unused `.dashboard-col`, `.dashboard-col--main`, `.dashboard-col--side` style rules.
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'class="stat-row"' ui/src/features/dashboard/DashboardPage.svelte` matches
    - `grep -n 'grid-template-columns: repeat(4, 1fr)' ui/src/features/dashboard/DashboardPage.svelte` matches
    - `grep -n "@use '../../styles/_breakpoints' as bp" ui/src/features/dashboard/DashboardPage.svelte` matches
    - `grep -n 'bp.\$bp-xl' ui/src/features/dashboard/DashboardPage.svelte` and `grep -n 'bp.\$bp-sm' ui/src/features/dashboard/DashboardPage.svelte` both match
    - `grep -c 'dashboard-col--main' ui/src/features/dashboard/DashboardPage.svelte` returns 0
    - `grep -n '<StatWidget' ui/src/features/dashboard/DashboardPage.svelte | wc -l` returns 4 (all 4 widgets preserved)
    - `pnpm --dir ui build` exits 0
  </acceptance_criteria>
  <done>Dashboard renders a responsive 4-card stat row followed by a full-width chart, collapsing to 2 then 1 column at the documented breakpoints, with all StatWidget/ChartWidget props unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-provided widget/chart data → template | `DashboardWidgetDto`/`ConsumptionPoint` fields flow into `StatWidget`/`ChartWidget` props — unchanged data path, only surrounding grid/header markup changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Information Disclosure | Dashboard scope reduction (D-01) | accept | The 3 data-less mockup panels are intentionally NOT built rather than stubbed with fabricated data — no information-disclosure or data-integrity risk from a half-built feature. |
| T-26-06-02 | Tampering (XSS) | `title="Дашборд"` / static labels | accept | All new markup uses static string literals, no user input rendered in the header. |
| T-26-06-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies introduced. |
</threat_model>

<verification>
1. `pnpm --dir ui build` passes.
2. Manual: resize the browser through 1280px and 560px and confirm the stat row collapses 4→2→1 columns; change month/year selects and confirm `reloadWidgets()` still refetches (SC #3), deferred to Plan 26-08.
</verification>

<success_criteria>
- Dashboard header and grid match UI-SPEC §3.6/§3.9 and D-02's row+chart layout.
- No new "+ Создать акт" button and no fabricated data for the 3 missing panels (D-01/D-03).
</success_criteria>

<output>
Create `.planning/phases/26-windows-with-mockup/26-06-SUMMARY.md` when done
</output>
