---
phase: 26-windows-with-mockup
plan: 04
type: execute
wave: 2
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - ui/src/features/devices/DevicesPage.svelte
  - ui/src/features/devices/DeviceList.svelte
autonomous: true
requirements: [WIN-02]

must_haves:
  truths:
    - "Шапка окна Устройств использует общий `PageHeader` (variant=\"wrap\") вместо самописной разметки — тот же титул, те же три кнопки (D-07)."
    - "Тело страницы Устройств получает `padding:16px 24px` вместо `24px 32px` (§3.7/§3.8)."
    - "Таблица устройств получает рамку макета (border+radius8+elev-1) через `Table`'s `framed` prop, а итоговая строка «Показано N из M» / «Групп: N» переезжает внутрь рамки как `footer`-снип, БЕЗ изменения условия рендера, текстов или `$derived`-логики (D-11, D-12, §6.5)."
    - "DeviceListRow/DeviceGroupRow — не тронуты (D-12); строки/групповые строки продолжают рендериться как раньше."
    - "CRUD (создание/редактирование/удаление устройства), печать акта приёма и CSV import/export работают без изменения поведения (SC #3)."
  artifacts:
    - path: "ui/src/features/devices/DevicesPage.svelte"
      provides: "шапка через PageHeader(variant=wrap), page-content padding 16px 24px"
      min_lines: 300
    - path: "ui/src/features/devices/DeviceList.svelte"
      provides: "footer-снип передан в Table, framed по умолчанию true, .device-list-wrapper удалён"
      min_lines: 140
  key_links:
    - from: "ui/src/features/devices/DevicesPage.svelte"
      to: "ui/src/lib/components/PageHeader.svelte"
      via: "<PageHeader title=\"Устройства\" variant=\"wrap\">"
      pattern: "<PageHeader[^>]*variant=\"wrap\""
    - from: "ui/src/features/devices/DeviceList.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "footer snippet passed to <Table footer={footer}>"
      pattern: "\\{footer\\}"
---

<objective>
Migrate `DevicesPage.svelte`'s header to the shared `PageHeader` primitive and move
`DeviceList.svelte`'s footer/frame into `Table`'s new `framed`/`footer` props (from Plan 26-03),
without touching row rendering, CRUD handlers, CSV import/export, or the acceptance-print flow.

Purpose: closes WIN-02's shell-level gap — the Devices window's chrome (header, body padding, table
frame) must match the mockup while every existing behavior (filters wiring stays in Plan 26-05,
CRUD/CSV/print stays here unchanged) continues to work exactly as before (SC #3).
Output: `DevicesPage.svelte` header migrated to `PageHeader`; `DeviceList.svelte`'s footer relocated
inside `Table`'s frame.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-windows-with-mockup/26-UI-SPEC.md
@.planning/phases/26-windows-with-mockup/26-CONTEXT.md
@.planning/phases/26-windows-with-mockup/26-PATTERNS.md
</context>

<interfaces>
From Plan 26-01's PageHeader.svelte (contract, must exist before this plan runs):
```ts
interface Props {
  title: string;
  variant?: 'fixed' | 'wrap';
  actions?: Snippet;
}
```

From Plan 26-03's Table.svelte (contract, must exist before this plan runs):
```ts
/** default true — border+radius8+overflow:hidden+elev-1 frame. */
framed?: boolean;
/** default undefined — footer bar rendered inside the frame, below the scroller. */
footer?: Snippet;
```
</interfaces>

<code_context>
`DevicesPage.svelte` (current, full 353 lines) — header block (lines 228-236) and `.page-content`
(lines 238, 348-352) are the ONLY parts changing. `openCreate`/`exportCsv`/`handleSearchChange`/etc.
(lines 66-225) and all modal wiring (lines 277-313) are UNCHANGED.

`DeviceList.svelte` (current, full 155 lines) — footer block (lines 107-117) and the
`.device-list-wrapper` div (lines 73, 120-124) are the ONLY parts changing. The `{#if !skeletonLoading && !isEmpty}`
condition, `emptyMessage`/`emptySubtext` derivations (lines 52-59), and all `$derived` (lines 41-59)
are FORBIDDEN to touch (D-12 hard constraint, UI-SPEC §6.5).
</code_context>

<tasks>

<task type="auto">
  <name>Task 1: DevicesPage.svelte — header migration to PageHeader + body padding</name>
  <files>ui/src/features/devices/DevicesPage.svelte</files>
  <read_first>ui/src/features/devices/DevicesPage.svelte (current, full 353 lines), ui/src/lib/components/PageHeader.svelte (created in Plan 26-01 — read its actual final Props shape before wiring)</read_first>
  <action>
    Replace the current `<header class="page-header"><h1 class="page-title">Устройства</h1><div class="header-actions">...3 Buttons...</div></header>`
    block with `<PageHeader title="Устройства" variant="wrap">{#snippet actions()}` wrapping the
    SAME three `<Button>` calls verbatim (`variant="primary" onclick={openCreate}` "+ Создать
    устройство", `variant="secondary" onclick={() => (csvModalOpen = true)}` "Импорт CSV",
    `variant="secondary" onclick={exportCsv}` "Экспорт CSV") `{/snippet}</PageHeader>`. Import
    `PageHeader` from `$lib/components/PageHeader.svelte`.

    Remove the now-unused `.page-header`, `.page-title`, `.header-actions` style rules from the
    `<style>` block (PageHeader owns this styling now).

    Change `.page-content`'s `padding: var(--tr-space-xl) var(--tr-space-2xl)` (24px 32px) to
    `padding: 16px 24px` (§3.7/§3.8).

    Do NOT modify `openCreate`, `openEdit`, `onSaved`, `exportCsv`, `handleSearchChange`,
    `handleStatusChange`, `handleGroupedChange`, `handlePrintAcceptance`, `handleAcceptanceSubmit`,
    or any of the modal components below the closing `</div>` of `.devices-page` — only the header
    markup and `.page-content` padding change.
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '<PageHeader title="Устройства" variant="wrap">' ui/src/features/devices/DevicesPage.svelte` matches
    - `grep -c 'class="page-header"' ui/src/features/devices/DevicesPage.svelte` returns 0 (old header markup removed)
    - `grep -n 'padding: 16px 24px' ui/src/features/devices/DevicesPage.svelte` matches inside `.page-content`
    - `grep -n 'onclick={openCreate}' ui/src/features/devices/DevicesPage.svelte` still matches (handler wiring preserved)
    - `grep -n 'function exportCsv' ui/src/features/devices/DevicesPage.svelte` still matches unchanged
    - `pnpm --dir ui build` exits 0
  </acceptance_criteria>
  <done>DevicesPage renders its header through PageHeader(variant=wrap) with unchanged button handlers, and page-content uses 16px 24px padding.</done>
</task>

<task type="auto">
  <name>Task 2: DeviceList.svelte — footer snippet + framed table, D-12-safe</name>
  <files>ui/src/features/devices/DeviceList.svelte</files>
  <read_first>ui/src/features/devices/DeviceList.svelte (current, full 155 lines), ui/src/lib/components/Table.svelte (created in Plan 26-03 — read final framed/footer prop shape)</read_first>
  <action>
    Wrap the existing footer block EXACTLY as-is —
    `{#if !skeletonLoading && !isEmpty}<footer class="list-footer">...unchanged inner content...</footer>{/if}` —
    inside a new `{#snippet footer()}...{/snippet}` block, and pass `footer={footer}` as a prop to
    the `<Table ...>` call (alongside the existing `columns`/`loading`/`empty`/`emptyTitle`/`emptyBody`/`head` props).
    Do not pass `framed` explicitly — the default `true` from Plan 26-03 is correct here.

    Delete the outer `<div class="device-list-wrapper">` wrapper (its `overflow-x: auto` role is now
    owned by `Table`'s internal `.tr-table-wrapper`) — the component's root becomes the `<Table>` call
    itself (plus the `{#snippet tableHead()}` definition and the `<DeviceFormModal>`-adjacent markup
    stays as before, unaffected). Remove the now-unused `.device-list-wrapper` style rule.

    FORBIDDEN per D-12: do not touch the `{#if !skeletonLoading && !isEmpty}` condition, the
    `Показано {items.length} из {total}` / `Групп: {groups.length}` strings, `emptyMessage`/`emptySubtext`
    (lines 52-59), or any `$derived` declaration (lines 41-59). If wrapping the footer in a
    `{#snippet}` proves structurally impossible without touching this logic, STOP and use the
    documented fallback instead: pass only `framed` (implicit default), leave the footer exactly
    where it is today (outside `<Table>`), and record this as an accepted deviation from У:104 in
    the plan's SUMMARY.
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '{#snippet footer()}' ui/src/features/devices/DeviceList.svelte` matches (or, if the fallback was used, the SUMMARY explicitly documents the deviation)
    - `grep -n 'footer={footer}' ui/src/features/devices/DeviceList.svelte` matches (unless fallback documented)
    - `grep -n 'skeletonLoading && !isEmpty' ui/src/features/devices/DeviceList.svelte` still matches verbatim (condition untouched)
    - `grep -n 'Показано {items.length} из {total}' ui/src/features/devices/DeviceList.svelte` still matches verbatim
    - `grep -c 'class="device-list-wrapper"' ui/src/features/devices/DeviceList.svelte` returns 0 (unless fallback documented, in which case it may remain)
    - `pnpm --dir ui build` exits 0
  </acceptance_criteria>
  <done>DeviceList's footer renders inside Table's framed border via the footer snippet (or the documented fallback), with zero change to the empty/loading condition, copy, or derived state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client renders server-provided device data | `DeviceDto`/`DeviceGroup` fields (name, inventory/serial numbers, location, model) flow from the Rust backend into Svelte templates — unchanged data path, only wrapping markup changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering (XSS) | Device fields rendered inside `PageHeader`'s title/actions and `Table`'s footer | accept | Svelte's default text-interpolation escaping is untouched by this plan; no `{@html}` introduced; `title="Устройства"` is a static string literal, not user input. |
| T-26-04-02 | Repudiation | CSV export / device CRUD | accept | Handler functions (`exportCsv`, `openCreate`, `onSaved`, etc.) are byte-for-byte unchanged by this plan — no new audit-trail risk introduced. |
| T-26-04-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies introduced. |
</threat_model>

<verification>
1. `pnpm --dir ui build` passes.
2. Manual: create/edit/delete a device, run CSV import and export, print an acceptance document — all must behave identically to pre-plan behavior (SC #3), deferred to Plan 26-08's UAT checklist.
</verification>

<success_criteria>
- DevicesPage header and table frame match UI-SPEC §3.7/§3.8/§3.14/§6.5.
- No CRUD/CSV/print behavior regression (D-12, SC #3).
</success_criteria>

<output>
Create `.planning/phases/26-windows-with-mockup/26-04-SUMMARY.md` when done
</output>
