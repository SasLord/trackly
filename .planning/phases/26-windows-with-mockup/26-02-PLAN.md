---
phase: 26-windows-with-mockup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/layout/Sidebar.svelte
  - ui/src/lib/components/ThemeSwitcher.svelte
autonomous: true
requirements: [WIN-01, WIN-02]

must_haves:
  truths:
    - "Сайдбар визуально инвертирован по макету: фон `--tr-bg` (было `--tr-surface`), контент-область станет `--tr-surface` в Плане 26-01 (D-06)."
    - "Сайдбар получает шапку с логотипом «Trackly» (квадрат 11px `--tr-accent`, radius 3px) — новый структурный блок, которого не было (D-08)."
    - "Подпись переключателя тем меняется с «Тема» на «Оформление»; кнопка «Выйти» и ролевая навигация (`sidebar-config.ts`) остаются нетронутыми — их отсутствие в макете не читается как повод для удаления (D-08)."
    - "Активный пункт навигации переезжает с `border-left` + `color-mix` на `box-shadow:inset 3px 0 0 var(--tr-accent)` + `--tr-accent-soft` + `--tr-accent-text` + вес 600, сохраняя `:global(.nav-link.is-active)` как scoped-компонентный `:global()` (это не запрещённый plain-`.scss` `:global()` — не «чинить»)."
    - "ThemeSwitcher переходит на sunken-подложку с `gap:2px` и box-shadow на активном сегменте, тройка кнопок в порядке «Светлая · Системная · Тёмная»."
  artifacts:
    - path: "ui/src/features/layout/Sidebar.svelte"
      provides: "сайдбар: шапка-логотип NEW, инверсия фона D-06, навигация/футер CHG по значениям, логика логаута/nav-config нетронута"
      min_lines: 200
    - path: "ui/src/lib/components/ThemeSwitcher.svelte"
      provides: "сегмент-контрол темы, отревьюженный по значениям Д:214-216"
      min_lines: 60
  key_links:
    - from: "ui/src/features/layout/Sidebar.svelte"
      to: "ui/src/features/layout/sidebar-config.ts"
      via: "getVisibleItems(role) — состав пунктов не меняется"
      pattern: "getVisibleItems\\("
    - from: "ui/src/features/layout/Sidebar.svelte"
      to: "ui/src/stores/auth.svelte"
      via: "authStore.user — logout/role display unchanged"
      pattern: "authStore\\.user"
---

<objective>
Перекрасить `Sidebar.svelte` и `ThemeSwitcher.svelte` по значениям из `Окно · Дашборд.dc.html`
(шапка-логотип, инверсия фона, навигация, футер, сегмент-контрол темы) без изменения ролевой
навигации, логики логаута или состава пунктов меню. Часть Волны 1 (D-19) — параллельна Плану 26-01
(без пересечения файлов) и Плану 26-03.

Purpose: сайдбар — общий для всех 11 окон приложения элемент; его стилевой контракт должен быть
готов до того, как Дашборд/Устройства (Волна 2) визуально проверяются на соответствие макету.
Purpose (D-08): «макет задаёт форму, приложение задаёт содержание» — тот же принцип, что и в D-04/D-10
Фазы 25, применённый четвёртый раз подряд в этой фазе.
Output: перекрашенный `Sidebar.svelte` (+ новая шапка-логотип) и `ThemeSwitcher.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-windows-with-mockup/26-UI-SPEC.md
@.planning/phases/26-windows-with-mockup/26-CONTEXT.md
@.planning/phases/26-windows-with-mockup/26-PATTERNS.md
</context>

<interfaces>
<!-- Tabs.svelte's segmented-variant styling is the styling TARGET for ThemeSwitcher — not a
     component swap, just borrowed CSS values (ThemeSwitcher keeps its own markup/role/ARIA). -->

From ui/src/lib/components/Tabs.svelte (lines 131-165, `.tabs-segmented` block):
```scss
.tabs-segmented {
  display: inline-flex;
  gap: 3px;
  padding: 3px;
  background: var(--tr-surface-sunken);
  border-radius: 7px;
  .tab {
    ...
    &.active { background: var(--tr-surface); color: var(--tr-accent-text); box-shadow: var(--tr-elev-1); }
    &:focus-visible { box-shadow: 0 0 0 3px var(--tr-focus-ring); }
  }
}
```
</interfaces>

<code_context>
`Sidebar.svelte` (current, full 216 lines) imports `getVisibleItems` from `./sidebar-config` and
`authStore`/`apiCall` for logout — **none of this changes**. Only the `<style>` block and the new
logo-header markup change. The active-link selector `:global(.nav-link.is-active)` (line 127) stays
as a `:global()` wrapper — this is a SCOPED-component `:global()` (works fine, confirmed in
24-LEARNINGS.md and UI-SPEC §3.3), NOT the plain-`.scss` `:global()` anti-pattern.

`ThemeSwitcher.svelte` (current, full 71 lines) imports `themeStore, setTheme` from
`$lib/stores/theme.svelte` — unchanged. `options` array order changes from
Светлая/Тёмная/Системная → Светлая/Системная/Тёмная (D-08).
</code_context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar.svelte — logo header (NEW), background inversion, nav/footer restyle</name>
  <files>ui/src/features/layout/Sidebar.svelte</files>
  <read_first>ui/src/features/layout/Sidebar.svelte (current, full 216 lines)</read_first>
  <action>
    Add a new logo-header block as the FIRST child of the `<nav class="sidebar">` root (before the
    `<ul class="nav-list">`): `<div class="sidebar-logo" aria-hidden="false">` containing a
    `<span class="logo-mark" aria-hidden="true"></span>` (the decorative square) and
    `<span class="logo-text">Trackly</span>`. Container: `display:flex; align-items:center; gap:9px;
    height:56px; padding:0 16px; border-bottom:1px solid var(--tr-border)`. `.logo-mark`:
    `width:11px; height:11px; border-radius:3px; background:var(--tr-accent)`. `.logo-text`:
    `font-size:16px; font-weight:600` (color inherits `--tr-text-primary`).

    `.sidebar` root: change `background: var(--tr-surface)` → `background: var(--tr-bg)` (D-06
    inversion). Keep `width: var(--sidebar-width)` (now 236px via Plan 26-01's token edit — no
    change needed here since it already references the variable).

    `.nav-list`: `padding: var(--tr-space-xs) 0` → `padding: 8px; display:flex; flex-direction:column;
    gap: 1px` (Д:45). `.divider`: `margin: var(--tr-space-2xs) var(--tr-space-md)` → `margin: 6px 8px`
    (Д:47). `.nav-link`: `height: var(--row-height)` (currently 40px) → `height: 38px; line-height: 38px`
    (Д:280); `padding: 0 var(--tr-space-md)` → `padding: 0 12px`; add `border-radius: var(--tr-radius-sm)`;
    remove `border-left: 3px solid transparent` (replaced by the active-state box-shadow below); keep
    the existing `&:hover` rule (macet is static, hover stays per O-10).

    `:global(.nav-link.is-active)` (line 127): change declarations to
    `box-shadow: inset 3px 0 0 var(--tr-accent); background: var(--tr-accent-soft); color: var(--tr-accent-text); font-weight: var(--tr-font-weight-medium);`
    where `--tr-font-weight-medium` resolves to 600 in this scale — DO NOT change the selector
    wrapper itself (`:global(.nav-link.is-active)` stays exactly as-is; it is a scoped-component
    `:global()`, confirmed working, not the plain-`.scss` anti-pattern from 24-LEARNINGS.md).

    `.sidebar-footer`: `padding: var(--tr-space-md)` → `padding: 14px 16px`. `.user-block`:
    `padding-bottom: var(--tr-space-xs); margin-bottom: var(--tr-space-2xs)` → `padding-bottom: 10px; margin-bottom: 10px`.
    `.user-name`: `font-weight: var(--tr-font-weight-medium)` → `font-weight: 600`. `.user-role`:
    `font-size: var(--tr-font-size-label)` (13px) → `font-size: 12px`. `.theme-row`: `gap: var(--tr-space-2xs)` → `gap: 7px`.
    `.theme-label`: `font-size: var(--tr-font-size-label)` → `12px`; `color: var(--tr-text-tertiary)` → `var(--tr-text-secondary)`;
    text content `Тема` → `Оформление` (D-08). Do NOT remove the `.logout-btn` block or its markup — D-08 keeps it.
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'class="sidebar-logo"' ui/src/features/layout/Sidebar.svelte` matches
    - `grep -n '>Trackly<' ui/src/features/layout/Sidebar.svelte` matches
    - `grep -n 'background: var(--tr-bg)' ui/src/features/layout/Sidebar.svelte` matches inside `.sidebar` rule
    - `grep -n 'box-shadow: inset 3px 0 0 var(--tr-accent)' ui/src/features/layout/Sidebar.svelte` matches
    - `grep -n '>Оформление<' ui/src/features/layout/Sidebar.svelte` matches; `grep -c '>Тема<' ui/src/features/layout/Sidebar.svelte` returns 0
    - `grep -n 'Выйти' ui/src/features/layout/Sidebar.svelte` still matches (logout button preserved, D-08)
    - `grep -n "getVisibleItems" ui/src/features/layout/Sidebar.svelte` still matches (nav-config import untouched)
    - `pnpm --dir ui run svelte-check` exits 0
  </acceptance_criteria>
  <done>Sidebar shows the new logo header, inverted background, restyled nav/footer per §3.1-3.4, "Оформление" copy, and unchanged logout/nav-config wiring.</done>
</task>

<task type="auto">
  <name>Task 2: ThemeSwitcher.svelte — sunken-track restyle + button reorder</name>
  <files>ui/src/lib/components/ThemeSwitcher.svelte</files>
  <read_first>ui/src/lib/components/ThemeSwitcher.svelte (current, full 71 lines), ui/src/lib/components/Tabs.svelte (lines 131-165, `.tabs-segmented` styling target)</read_first>
  <action>
    Reorder the `options` array (lines 4-8) to Светлая · Системная · Тёмная (D-08) — keep each
    entry's `key`/`label`/`ariaLabel` values unchanged, only the array order.

    `.theme-switcher`: replace `height:32px; background:var(--tr-surface); border:1px solid var(--tr-border); border-radius:var(--tr-radius-xs); overflow:hidden` with
    `display:flex; padding:2px; gap:2px; background:var(--tr-surface-sunken); border:1px solid var(--tr-border); border-radius:8px`
    (Д:214). `.segment`: remove `border-right: 1px solid var(--tr-border)` and the `&:last-child { border-right: none; }`
    rule entirely (gap replaces dividers per §3.5 note); set `flex:1; height:26px; border:none;
    border-radius:6px; padding:0 4px; font-size:12px; font-weight:600`. `.segment.active`:
    `color:var(--tr-text-primary); background:var(--tr-surface-raised); box-shadow:var(--tr-elev-1);`.
    `.segment` non-active state: `color:var(--tr-text-tertiary); background:transparent; box-shadow:none;`.
    Change `transition: none;` (line 48) to `transition: background 0.12s, color 0.12s;` (matches the
    project-wide Phase 24 micro-transition convention already used in `Button.svelte`/`Tabs.svelte`).
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "key: 'light'" ui/src/lib/components/ThemeSwitcher.svelte` appears before `grep -n "key: 'system'"` and before `grep -n "key: 'dark'"` in file line order (Светлая · Системная · Тёмная)
    - `grep -n 'background: var(--tr-surface-sunken)' ui/src/lib/components/ThemeSwitcher.svelte` matches inside `.theme-switcher`
    - `grep -n 'border-radius: 8px' ui/src/lib/components/ThemeSwitcher.svelte` matches
    - `grep -n 'box-shadow: var(--tr-elev-1)' ui/src/lib/components/ThemeSwitcher.svelte` matches inside `.segment.active`
    - `grep -c 'border-right: 1px solid var(--tr-border)' ui/src/lib/components/ThemeSwitcher.svelte` returns 0 (dividers removed)
    - `grep -n 'transition:' ui/src/lib/components/ThemeSwitcher.svelte` shows `background 0.12s, color 0.12s` (not `none`)
    - `pnpm --dir ui run svelte-check` exits 0
  </acceptance_criteria>
  <done>ThemeSwitcher renders Светлая·Системная·Тёмная on a sunken track with a shadowed active segment and .12s transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side only | Pure visual restyle; logout button still calls the existing `auth_logout` Tauri/HTTP command through the untouched `logout()` handler — no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Spoofing | `Sidebar.svelte` logout button | accept | Logout call path (`apiCall('auth_logout', {})`, session clear on failure) is unchanged by this plan — no new risk introduced. |
| T-26-02-02 | Information Disclosure | `.user-name`/`.user-role` display | accept | Same fields rendered as before (`authStore.user.fullName`/`role`), only font-size/weight change — no new data exposed. |
| T-26-02-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies introduced by this plan. |
</threat_model>

<verification>
1. `pnpm --dir ui run svelte-check` passes for both files.
2. Manual: confirm logout button still works and role-based nav items are unchanged for admin/manager/employee (grep-check `getVisibleItems` call signature unchanged).
</verification>

<success_criteria>
- Sidebar and ThemeSwitcher match UI-SPEC §3.1-3.5 values in both themes.
- Logout, role-based nav, and theme-switching logic are unchanged (D-08).
</success_criteria>

<output>
Create `.planning/phases/26-windows-with-mockup/26-02-SUMMARY.md` when done
</output>
