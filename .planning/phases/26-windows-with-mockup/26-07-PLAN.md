---
phase: 26-windows-with-mockup
plan: 07
type: execute
wave: 2
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - ui/src/features/dashboard/StatWidget.svelte
  - ui/src/features/dashboard/ChartWidget.svelte
  - ui/src/features/dashboard/PeriodToggle.svelte
autonomous: true
requirements: [WIN-01]

must_haves:
  truths:
    - "Карточка статистики визуально соответствует макету: padding 16px, box-shadow elev-1, min-width:0, число 30px/700/lh1/tabular-nums на одной базовой линии с единицей (Д:84-92)."
    - "Разбивка (breakdown) рендерится пилюлями локальной разметкой StatWidget (не через Badge — Badge не поддерживает пару label+strong), а `warningItems` сохраняется как работающая функциональность, только перекрашивается токенами `--tr-warning-*` без сплошной рамки (D-04)."
    - "Строка ошибки виджета и графика меняется ОДНОВРЕМЕННО в обоих файлах на «Не удалось загрузить. Смените период или обновите страницу.» — расхождение между StatWidget и ChartWidget недопустимо (§9)."
    - "Высота карточки в состоянии загрузки не меняется (min-height:120px сохраняется) — сетка не должна прыгать при переходе loading→loaded (D-14)."
    - "Палитра рядов графика — согласованное исключение из токен-слоя: #3b6fe0/#1a9d5f/#d8820e литералами в обеих темах, не через --tr-* токены (§7)."
    - "PeriodToggle остаётся `role=\"group\"`/`aria-label=\"Период графика\"` (НЕ табы) — только CSS-значения заимствуются у макета, семантика не меняется."
  artifacts:
    - path: "ui/src/features/dashboard/StatWidget.svelte"
      provides: "карточка по §3.10-3.11, error string CHG, warningItems retone D-04"
      min_lines: 130
    - path: "ui/src/features/dashboard/ChartWidget.svelte"
      provides: "карточка/оси/легенда по §3.12, литеральная палитра, error string CHG (paired with StatWidget)"
      min_lines: 400
    - path: "ui/src/features/dashboard/PeriodToggle.svelte"
      provides: "pStyle-значения Д:254, role=group сохранён"
      min_lines: 50
  key_links:
    - from: "ui/src/features/dashboard/StatWidget.svelte"
      to: "ui/src/features/dashboard/ChartWidget.svelte"
      via: "identical error string in both files"
      pattern: "Не удалось загрузить\\. Смените период или обновите страницу\\."
---

<objective>
Restyle `StatWidget.svelte`, `ChartWidget.svelte`, and `PeriodToggle.svelte` to the mockup's values
(§3.10-3.12) while preserving all loading/error behavior (D-14) and the `warningItems` low-stock
feature (D-04). Change the widget error string in BOTH `StatWidget` and `ChartWidget` together
(§9's explicitly-flagged two-file trap).

Purpose: closes the remaining WIN-01 visual gap (the two data-bearing widget components + the chart)
without touching `loadWidgets`/`loadChart`/`reloadWidgets` logic — this is a pure retone/relayout pass.
Output: three restyled widget components, still driven by the exact same `DashboardPage.svelte` props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-windows-with-mockup/26-UI-SPEC.md
@.planning/phases/26-windows-with-mockup/26-CONTEXT.md
@.planning/phases/26-windows-with-mockup/26-PATTERNS.md
</context>

<code_context>
`StatWidget.svelte` (current, full 143 lines) — `Props` interface, `loading`/`error` branches
(lines 36-62) are UNCHANGED in structure; only their inner markup/CSS values and the error string
change. `ChartWidget.svelte` (current, full 453 lines) — `barLayout`/`yTicks`/`seriesData` derivations
(lines 80-211) are UNCHANGED; only card/header/axis/legend CSS values, the `COLORS` array (line 24),
and the error string (line 225) change. `PeriodToggle.svelte` (current, full 65 lines) — `role="group"`/
`aria-label="Период графика"` (line 13) stays; only `.toggle-btn` CSS values change.
</code_context>

<tasks>

<task type="auto">
  <name>Task 1: StatWidget.svelte — card shell, pill row, warningItems retone, error string</name>
  <files>ui/src/features/dashboard/StatWidget.svelte</files>
  <read_first>ui/src/features/dashboard/StatWidget.svelte (current, full 143 lines)</read_first>
  <action>
    `.stat-widget`: `padding: var(--tr-space-xl)` (24px) → `padding: 16px`; add `box-shadow: var(--tr-elev-1);
    min-width: 0;`; keep `min-height: 120px` unchanged (D-14 — grid must not jump on load).

    `.widget-title`: `font-size: var(--tr-font-size-body)` (14px) → `font-size: 13px`; `font-weight:
    var(--tr-font-weight-semibold)` → remove (unbolded); `color: var(--tr-text-primary)` →
    `color: var(--tr-text-secondary)`.

    Replace the separate `<p class="stat-number">`/`<p class="stat-label">` block with a single
    `<div class="stat-value-row">` containing `<span class="stat-number">{mainNumber ?? '—'}</span><span class="stat-unit">{mainLabel}</span>`.
    `.stat-value-row`: `display:flex; align-items:baseline; gap:8px; margin-top:6px;`. `.stat-number`:
    `font-size:30px; font-weight:700; line-height:1; font-variant-numeric:tabular-nums;` (color inherits
    `--tr-text-primary`). `.stat-unit`: `font-size:13px; color:var(--tr-text-tertiary);`.

    Replace `<ul class="breakdown-list">{#each breakdown as row}<li>{row.label}: {row.count}</li>{/each}</ul>`
    with a pill row per §3.10 — `<div class="pill-row">{#each breakdown as row}<span class="pill">{row.label}: <strong>{row.count}</strong></span>{/each}</div>`.
    Do NOT use `Badge.svelte` (confirmed in UI-SPEC §3.10 it doesn't support this label+strong pair
    shape). `.pill-row`: `display:flex; flex-wrap:wrap; gap:6px; margin-top:14px;`. `.pill`:
    `display:inline-flex; align-items:center; gap:5px; padding:3px 9px; border-radius:11px;
    background:var(--tr-surface-sunken); font-size:12px; color:var(--tr-text-secondary);
    white-space:nowrap;`. `.pill strong`: `color:var(--tr-text-primary); font-variant-numeric:tabular-nums;`.

    `.widget-warning` (D-04 — keep the container/copy, retone only): `background: color-mix(in srgb,
    var(--tr-warning) 10%, transparent)` → `background: var(--tr-warning-soft)`; REMOVE the implicit
    `border: 1px solid var(--tr-warning)`-equivalent styling if present, replacing with
    `border: 1px solid transparent`; header `span` color → `color: var(--tr-warning-text); font-weight:600;`.
    Keep the "Низкий остаток:" copy and the `{#each warningItems as m}<li>{m}</li>{/each}` list exactly
    as-is (SC #3 — working functionality, do not touch the text or the `warningItems` prop).

    Change `<div class="widget-error">Ошибка загрузки</div>` (line 41) to
    `<div class="widget-error">Не удалось загрузить. Смените период или обновите страницу.</div>` —
    this string change must land in the SAME plan/commit as Task 2's identical change in
    `ChartWidget.svelte` (§9 — do not ship one without the other).
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'padding: 16px' ui/src/features/dashboard/StatWidget.svelte` matches inside `.stat-widget`
    - `grep -n 'box-shadow: var(--tr-elev-1)' ui/src/features/dashboard/StatWidget.svelte` matches
    - `grep -n 'min-height: 120px' ui/src/features/dashboard/StatWidget.svelte` still matches (D-14 preserved)
    - `grep -n 'font-variant-numeric: tabular-nums' ui/src/features/dashboard/StatWidget.svelte` matches at least twice (stat-number + pill strong)
    - `grep -n 'class="pill-row"' ui/src/features/dashboard/StatWidget.svelte` matches; `grep -c 'class="breakdown-list"' ui/src/features/dashboard/StatWidget.svelte` returns 0
    - `grep -n 'background: var(--tr-warning-soft)' ui/src/features/dashboard/StatWidget.svelte` matches
    - `grep -n 'Низкий остаток:' ui/src/features/dashboard/StatWidget.svelte` still matches verbatim
    - `grep -n 'Не удалось загрузить. Смените период или обновите страницу.' ui/src/features/dashboard/StatWidget.svelte` matches
    - `pnpm --dir ui build` exits 0
  </acceptance_criteria>
  <done>StatWidget matches §3.10-3.11 values, keeps warningItems functionality retoned, and shares the new error string with ChartWidget.</done>
</task>

<task type="auto">
  <name>Task 2: ChartWidget.svelte — card/axis/legend restyle, literal palette, paired error string</name>
  <files>ui/src/features/dashboard/ChartWidget.svelte</files>
  <read_first>ui/src/features/dashboard/ChartWidget.svelte (current, full 453 lines)</read_first>
  <action>
    `.chart-widget`: `padding: var(--tr-space-xl)` (24px) → `padding: 18px`; add
    `box-shadow: var(--tr-elev-1); min-width: 0;`. `.chart-header`: add `margin-bottom: 20px` (was
    implicit via `.widget-title` margin). `.widget-title` (h2, "Динамика расхода картриджей"):
    `font-size: var(--tr-font-size-body)` (14px) → `font-size: 16px`.

    Change `const COLORS = ['var(--tr-accent)', 'var(--tr-success)', 'var(--tr-warning)'];` (line 24)
    to `const COLORS = ['#3b6fe0', '#1a9d5f', '#d8820e'];` — a documented exception from the `--tr-*`
    token gate (data-viz series colors, identical in both themes, `check-tokens.mjs` does not scan
    JS string literals so no allow-list change is needed).

    Y-axis tick labels (SVG `<text>` at lines ~247-253): `font-size="9"` stays (already matches §3.12's
    9px axis-label value — do not confuse with the value-label-over-bar 9px, which is the SEPARATE
    conditional item below). Grid lines: `stroke="var(--tr-border)"` stays.

    Value-label-over-bar (`<text>` at lines ~286-294, `font-size="8"`): change to `font-size="9"`
    (§3.12/§5 — this is the conditional-acceptance 9px value; do NOT round up to 11px preemptively,
    that is ONLY a fallback if visual UAT in Plan 26-08 finds it illegible in dark theme — см. O-11).

    `.chart-legend`: `gap: var(--tr-space-md)` (16px) → keep 16px if already matching, add
    `padding-top: 14px; border-top: 1px solid var(--tr-border);` (currently absent). `.legend-item`:
    `gap: var(--tr-space-2xs)` → `gap: 7px`. `.legend-dot`: keep `width:10px; height:10px; border-radius:50%`
    (already matches Д:137).

    Change `<div class="chart-state chart-error">Ошибка загрузки</div>` (line 225) to
    `<div class="chart-state chart-error">Не удалось загрузить. Смените период или обновите страницу.</div>` —
    MUST be identical byte-for-byte to Task 1's `StatWidget.svelte` string (§9).

    Do not modify `barLayout`/`yTicks`/`seriesData`/`monthKeyToLabel` derivations or the tooltip
    mouseenter/mouseleave handlers.
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "const COLORS = \['#3b6fe0', '#1a9d5f', '#d8820e'\]" ui/src/features/dashboard/ChartWidget.svelte` matches
    - `grep -n 'padding: 18px' ui/src/features/dashboard/ChartWidget.svelte` matches inside `.chart-widget`
    - `grep -n 'box-shadow: var(--tr-elev-1)' ui/src/features/dashboard/ChartWidget.svelte` matches
    - `grep -n 'font-size="9"' ui/src/features/dashboard/ChartWidget.svelte` matches for the value-label-over-bar text element (was `font-size="8"`)
    - `grep -n 'Не удалось загрузить. Смените период или обновите страницу.' ui/src/features/dashboard/ChartWidget.svelte` matches, IDENTICAL to StatWidget.svelte's string (diff the two grep outputs — must be equal)
    - `grep -n 'border-top: 1px solid var(--tr-border)' ui/src/features/dashboard/ChartWidget.svelte` matches inside `.chart-legend`
    - `grep -n 'const barLayout' ui/src/features/dashboard/ChartWidget.svelte` still matches unchanged
    - `pnpm --dir ui build` exits 0
  </acceptance_criteria>
  <done>ChartWidget matches §3.12 values with the literal hex palette and shares the identical error string with StatWidget; all data derivations remain untouched.</done>
</task>

<task type="auto">
  <name>Task 3: PeriodToggle.svelte — pStyle restyle, role unchanged</name>
  <files>ui/src/features/dashboard/PeriodToggle.svelte</files>
  <read_first>ui/src/features/dashboard/PeriodToggle.svelte (current, full 65 lines)</read_first>
  <action>
    Keep `role="group"` and `aria-label="Период графика"` on the container (line 13) EXACTLY as-is —
    do not convert this to `Tabs.svelte`'s underline variant wholesale; `Tabs` uses `role="tablist"`/`role="tab"`,
    which is semantically wrong for a toggle-group (UI-SPEC §3.12 explicit constraint).

    `.period-toggle`: `gap: 2px` stays. `.toggle-btn`: `padding: var(--tr-space-2xs) var(--tr-space-xs)`
    (4px 8px) → `padding: 2px 1px 5px;`. Active state: `color: var(--tr-accent)` →
    `color: var(--tr-accent-text); font-weight: var(--tr-font-weight-medium)` → `font-weight: 600;
    border-bottom-color: var(--tr-accent)` (unchanged). Non-active state (default, currently only via
    `color: var(--tr-text-secondary)`): explicitly add `font-weight: 500;` alongside the existing
    `color: var(--tr-text-secondary)` (Д:254's inactive-state weight).
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'role="group"' ui/src/features/dashboard/PeriodToggle.svelte` still matches; `grep -c 'role="tablist"' ui/src/features/dashboard/PeriodToggle.svelte` returns 0
    - `grep -n 'aria-label="Период графика"' ui/src/features/dashboard/PeriodToggle.svelte` still matches
    - `grep -n 'padding: 2px 1px 5px' ui/src/features/dashboard/PeriodToggle.svelte` matches
    - `grep -n 'color: var(--tr-accent-text)' ui/src/features/dashboard/PeriodToggle.svelte` matches inside `.active`
    - `grep -n 'font-weight: 600' ui/src/features/dashboard/PeriodToggle.svelte` matches inside `.active`
    - `pnpm --dir ui build` exits 0
  </acceptance_criteria>
  <done>PeriodToggle keeps its group/toggle semantics while matching the mockup's active/inactive weight and padding values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-provided chart/widget data → SVG rendering | `ConsumptionPoint`/`StatusCount` fields flow into SVG `<text>`/`<rect>` elements — unchanged data path; only CSS values, error-string copy, and color literals change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Tampering (XSS) | SVG `<text>` content (month labels, install counts) | accept | Values are numeric/derived-string, rendered via Svelte's default escaping (no `{@html}`); unchanged from before this plan. |
| T-26-07-02 | Information Disclosure | Error string copy change | accept | The new string ("Не удалось загрузить. Смените период или обновите страницу.") reveals no new information — it only clarifies an existing, already-working retry path (`reloadWidgets()` / `windowMonths` `$effect`), per §9's rationale. |
| T-26-07-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependencies introduced. |
</threat_model>

<verification>
1. `pnpm --dir ui build` passes.
2. Manual (deferred to Plan 26-08): trigger the error state (stop backend / disconnect network) and confirm the identical string appears in both StatWidget and ChartWidget; check 9px value-label legibility in dark theme.
</verification>

<success_criteria>
- StatWidget/ChartWidget/PeriodToggle match UI-SPEC §3.10-3.12 values.
- Error string is identical across both widget files (§9); loading-state height and warningItems behavior are unchanged (D-14/D-04).
</success_criteria>

<output>
Create `.planning/phases/26-windows-with-mockup/26-07-SUMMARY.md` when done
</output>
