---
phase: 35-act-handover-body
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - crates/trackly-app/templates/act_handover.html
autonomous: true
requirements: [DOC-07, DOC-08, DOC-09]

must_haves:
  truths:
    - "Текст акта Word-образца сохраняется дословно — интро-фраза «Настоящим актом утверждаю, что мною: …» и лейблы полей (Инвентарный номер/Серийный номер/Модель/Комплектация/Технические характеристики/Состояние/Сроком до) не переписаны — D-01."
    - "При N > 1 устройствах выводится одна строка «были получены устройства:» и список имён без повтора лейбла на каждое устройство; при N = 1 — «было получено устройство: name» как в образце — D-02."
    - "Строка «Сроком до:» выводится безусловно; при пустом act.deadline_utc под значением остаётся полоска для рукописного заполнения — D-03."
    - "Организация и act.location_name в теле по-прежнему не выводятся; act.notes в контекст не добавлен — D-04."
    - "Должности подписантов по-прежнему не выводятся — в контексте их нет, в тело они не добавлены — D-05."
    - "Блок подписей — горизонтальный, одна строка на подписанта (Выдал: / Получил:), с полоской и напечатанным act.giver_name / act.receiver_name — D-06."
    - "Под каждой полоской подписи остаётся только пояснение Подпись; пояснение ФИО под напечатанным именем убрано — D-07."
    - "В строке подписанта нет даты подписания — D-08."
    - "border-bottom снят с базового .field-row .value; полоска остаётся ровно в двух местах — под подписью и под пустым Сроком до — D-10."
    - "Метка и значение каждого поля выводятся сплошным текстом через пробел, без выравнивания в колонку и без жирного лейбла — D-11."
    - "Пустая строка-заглушка (.field-row с nbsp, бывшие строки 119-121) удалена целиком — D-12."
    - "Doc-комментарий в шапке файла (было: строки 2-36) описывает новую структуру (сплошной текст, без underline; горизонтальные подписи) и перечисляет act.giver_name в списке ключей контекста — C-02."
  artifacts:
    - path: "crates/trackly-app/templates/act_handover.html"
      provides: "Переработанное тело акта приёма-передачи (D-01..D-12)"
      contains: "act.giver_name"
  key_links:
    - from: "crates/trackly-app/templates/act_handover.html"
      to: "crates/trackly-app/src/services/act_service.rs"
      via: "MiniJinja-интерполяция act.giver_name читает ключ, уже собранный act_service::render_pdf (act_service.rs:2639) — бэкенд не меняется"
      pattern: "act\\.giver_name"
    - from: "crates/trackly-app/templates/act_handover.html"
      to: "crates/trackly-app/src/services/template_service.rs"
      via: "demo_context_for_kind (Plan 01) уже содержит act.giver_name — синхронизация под UndefinedBehavior::Strict"
      pattern: "act\\.giver_name"
---

<objective>
Переработать тело act_handover.html (интро, поля устройства, «Сроком до», блок подписей) по
решениям D-01..D-12 из CONTEXT.md: убрать подчёркивания под автоподставляемым текстом (DOC-07),
сделать блок подписей горизонтальным с напечатанными ФИО (DOC-08), сохранить текст образца
дословно (DOC-09).

Purpose: DOC-07/DOC-08 — единственные оставшиеся незакрытые критерии фазы; DOC-09 закрыт
согласованием (D-01), эта правка лишь переносит согласованный текст в новую вёрстку без
изменения формулировок.
Output: act_handover.html с обновлёнными doc-комментарием, CSS и разметкой тела; production
рендер act_service::render_pdf не меняется (не трогается в этом плане).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-act-handover-body/35-CONTEXT.md
@.planning/phases/35-act-handover-body/35-RESEARCH.md
@.planning/phases/35-act-handover-body/35-PATTERNS.md
</context>

<interfaces>
Целевая структура блока подписей (используется идентично в act_acceptance.html, План 03, для
согласованности между обоими шаблонами — класс-имена ниже финальны, не менять):

Контейнер .signatures — тот же, что сейчас (теряет display:grid / grid-template-columns).
На подписанта — один div.signature-row, внутри три элемента: span.signature-label (текст
"Выдал:" или "Получил:"); span.signature-field — обёртка, внутри span.signature-line (полоска,
border-bottom) и span.signature-sublabel (текст "Подпись", мелкий кегль, единственное оставшееся
пояснение); span.signature-name (act.giver_name или act.receiver_name).

CSS-правила: .signature-row { display:flex; align-items:flex-end; gap:10pt; margin:16pt 0 0 0; }
.signature-row .signature-label { white-space:nowrap; } .signature-field { display:inline-flex;
flex-direction:column; align-items:center; flex:0 0 160pt; } .signature-field .signature-line {
display:block; width:100%; border-bottom:1px solid #000; min-height:1.2em; } .signature-field
.signature-sublabel { font-size:8pt; margin-top:2pt; } .signature-row .signature-name {
white-space:nowrap; }

Целевой класс для пустой полоски «Сроком до» (D-03/D-10) — новый класс .value-blank:
display:inline-block; min-width:140pt; border-bottom:1px solid #000; min-height:1.1em; —
единственное дополнительное место (кроме .signature-line), где border-bottom остаётся.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Doc-комментарий и CSS — снять подчёркивания, подготовить классы для подписей и пустого «Сроком до»</name>
  <files>crates/trackly-app/templates/act_handover.html</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (текущий HEAD, целиком — редактируется на месте)
    .planning/phases/35-act-handover-body/35-PATTERNS.md (секция act_handover.html, doc-комментарий и CSS .field-row/.signatures)
    crates/trackly-app/templates/act_acceptance.html строки 2-27 (стилистический образец doc-комментария соседнего шаблона)
  </read_first>
  <action>
    Переписать doc-комментарий (текущий HEAD, строки 2-36): заменить фразу про "label | underlined
    value" на описание сплошного текста без подчёркивания, и "two-line signatures Выдал/Получил" —
    на описание горизонтальных строк подписи с напечатанным ФИО из act.giver_name/act.receiver_name.
    В списке ключей контекста (после act.receiver_name) добавить act.giver_name. Затем переписать
    CSS: у .field-row (строки 64-78, текущий HEAD) убрать вложенные селекторы .field-row .label и
    .field-row .value целиком (в частности border-bottom:1px solid #000 — D-10); .field-row
    сохранить как блочный элемент с вертикальным отступом margin:4pt 0, без display:flex/
    align-items/gap — метка и значение больше не отдельные span, а сплошной текст (D-11, реализуется
    в Task 2). Добавить новый класс .value-blank (правила см. в блоке interfaces выше — единственное
    дополнительное место, где остаётся border-bottom, для пустого «Сроком до»). Заменить блок
    .signatures/.signature .line/.signature .sublabel (строки 89-105, текущий HEAD) на классы
    .signatures, .signature-row, .signature-row .signature-label, .signature-field, .signature-field
    .signature-line, .signature-field .signature-sublabel, .signature-row .signature-name — все
    точные правила описаны в блоке interfaces выше. .signatures сохраняет margin-top:30pt;
    page-break-inside:avoid, но теряет display:grid/grid-template-columns/gap:24pt.
  </action>
  <verify>
    <automated>grep -c "border-bottom" crates/trackly-app/templates/act_handover.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "border-bottom" в файле выводит 2 (только .value-blank и .signature-field .signature-line)
    - grep -c "display: flex" в CSS-блоке файла не находит .field-row среди совпадений (визуально проверить)
    - grep -c "act.giver_name" в файле выводит не менее 1 (упомянуто в doc-комментарии)
    - grep -c "display: grid" в файле выводит 0 (старая сетка .signatures удалена)
  </acceptance_criteria>
  <done>Doc-комментарий описывает новую структуру и упоминает act.giver_name; CSS больше не содержит border-bottom на .field-row .value и не содержит display:grid для .signatures; добавлены классы .value-blank и .signature-*.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Тело — интро без заглушки, поля устройств сплошным текстом, множественное число, безусловное «Сроком до»</name>
  <files>crates/trackly-app/templates/act_handover.html</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (после Task 1 — читать актуальное состояние файла)
    .planning/phases/35-act-handover-body/35-PATTERNS.md (секция act_handover.html: интро+заглушка, цикл по устройствам, «Сроком до», множественное число)
    crates/trackly-app/templates/report.html строка 117 (рабочий пример фильтра length в этом же движке)
  </read_first>
  <action>
    Правки в теле (текущий HEAD до Task 2, строки 115-178): 1) Удалить вторую пустую
    div.field-row (текущие строки 119-121, содержимое span.value с nbsp) целиком — D-12.
    2) Интро (текущие строки 115-118) — оставить текст «Настоящим актом утверждаю, что мною:
    {{ act.receiver_name }}» БЕЗ изменения слов (D-01), но перевести разметку на один
    div.field-row со сплошным текстом (label и значение через пробел в одном узле, не в двух span).
    3) Перед циклом {% for item in act.items %} (текущая строка 123) добавить условие
    {% if act.items | length > 1 %}: внутри — один div.field-row с текстом «были получены
    устройства:» и следом список имён устройств (по одному ul/li на каждый item.name, БЕЗ повтора
    лейбла на каждый пункт) — D-02. 4) Внутри самого цикла — лейбл «было получено устройство:
    {{ item.name }}» оставить, но обернуть в {% if act.items | length == 1 %} (рендерится только
    при одном устройстве — при N>1 имя уже выведено списком в шаге 3, повторно не выводится).
    5) Шесть полей внутри цикла (inventory_no/serial_no/model/kit/specs/condition, текущие строки
    129-164) перевести с двух span (label+value) на один div.field-row со сплошным текстом
    «Лейбл: {{ значение }}» — текст лейблов не менять (D-01), условия {% if item.X %} сохранить
    как есть. 6) Блок «Сроком до» (текущие строки 168-178, сейчас {% if act.deadline_human %}…
    {% elif act.deadline %}…{% endif %} вокруг ВСЕГО field-row) заменить на безусловный
    div.field-row с текстом «Сроком до: », за которым — либо {{ act.deadline_human }} (если есть),
    либо {{ act.deadline }} (если есть, но нет deadline_human), либо, если оба пусты,
    span.value-blank (пустая полоска для руки) — D-03. Родительский блок act.parent (строки
    180-184) не трогать.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test pdf_render_act render_handover_act_contains_d09_intro_phrase -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - render_handover_act_contains_d09_intro_phrase остаётся зелёным (D-01: интро-текст и act.receiver_name не изменились)
    - grep -c "nbsp" в файле выводит 0 (заглушка D-12 удалена)
    - grep -c "act.items | length" в файле выводит не менее 2 (используется и для сводной строки N>1, и для условия N=1)
    - grep -c "Сроком до" в файле выводит 1, и это вхождение находится ВНЕ {% if %}/{% elif %} блока (безусловный рендер, D-03)
    - grep -c "value-blank" в файле выводит 1 (пустая полоска только в ветке пустого deadline)
  </acceptance_criteria>
  <done>Интро без изменения текста, заглушка удалена, поля устройства — сплошной текст, множественное число реализовано length-фильтром, «Сроком до» рендерится всегда с полоской при пустом значении.</done>
</task>

<task type="auto">
  <name>Task 3: Горизонтальный блок подписей с напечатанными ФИО</name>
  <files>crates/trackly-app/templates/act_handover.html</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (после Task 1/2 — читать актуальное состояние файла)
    .planning/phases/35-act-handover-body/35-PATTERNS.md (секция «Блок подписей», целевая разметка D-06/D-07/D-08)
    crates/trackly-app/src/services/act_service.rs строки 2629-2661 (подтверждение: act.giver_name и act.receiver_name уже в контексте)
  </read_first>
  <action>
    Заменить блок div.signatures (текущий HEAD ДО Task 1, строки 186-201 — с двумя div.signature,
    каждый с двумя div.line и div.sublabel "Подпись"/"ФИО") на два div.signature-row внутри
    div.signatures, используя классы из Task 1 (signature-row/signature-label/signature-field/
    signature-line/signature-sublabel/signature-name), как описано в блоке interfaces этого файла:
    первая строка — span.signature-label с текстом "Выдал:", span.signature-field (пустой
    span.signature-line + span.signature-sublabel с текстом "Подпись"), span.signature-name с
    {{ act.giver_name }}; вторая строка — то же с "Получил:" и {{ act.receiver_name }}. Никакого
    span/div с текстом "ФИО" не остаётся (D-07). Дат в строке нет (D-08). {{ act.giver_name }} и
    {{ act.receiver_name }} — обычная интерполяция под AutoEscape::Html, БЕЗ фильтра | safe
    (T-16-01, тот же паттерн, что уже используется для act.receiver_name в интро).
  </action>
  <verify>
    <automated>grep -c "ФИО" crates/trackly-app/templates/act_handover.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "ФИО" в файле выводит 0 (сублейбл убран — D-07)
    - grep -c "act.giver_name" в файле выводит не менее 2 (упоминание в doc-комментарии Task 1 + интерполяция в подписи)
    - grep -c "signature-name" в файле выводит 2 (по одному на подписанта)
    - grep -c "| safe" рядом с giver_name/receiver_name отсутствует (0 вхождений safe рядом с этими двумя переменными)
    - файл НЕ содержит "___" или дублирующей второй полоски в строке подписанта (одна span.signature-line на подписанта)
  </acceptance_criteria>
  <done>Блок подписей — два горизонтальных div.signature-row, каждый с полоской, "Подпись" и напечатанным ФИО; никаких упоминаний "ФИО"-сублейбла и второй полоски не осталось.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| act.giver_name/act.receiver_name (значения из БД, введены пользователем через форму создания акта) → HTML-шаблон | Новое МЕСТО интерполяции уже существующего проверенного sink'а (T-16-01) — не новый sink |
| Пользователь-администратор редактирует act_handover.html на диске → MiniJinja render | Файл читается с диска на каждый рендер; движок настроен с AutoEscape::Html и без файлового загрузчика для include (T-16-02) — не меняется этим планом |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | Tampering (stored XSS) | `{{ act.giver_name }}` / `{{ act.receiver_name }}` в блоке подписей | mitigate | Обычная `{{ var }}`-интерполяция под `AutoEscape::Html`; НЕ добавлять `\| safe` — проверяется Task 3 acceptance_criteria |
| T-35-04 | Information Disclosure | Doc-комментарий и текст шаблона | accept | Правки ограничены структурными/CSS изменениями; текст интро и лейблов не меняется (D-01) — риска внесения реальных данных нет, т.к. значения полей всегда приходят из БД, не из литералов шаблона |
</threat_model>

<verification>
После Task 3: TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test pdf_render_act render_handover_act_contains_d09_intro_phrase render_handover_act_produces_cyrillic_pdf -- --test-threads=1 — оба зелёные (D-01 не тронут; general rendering sanity). Тесты signature_renders_two_line_labels и html_handover_contains_required_blocks_and_logo ОЖИДАЕМО красные после Task 3 (проверяют старый "ФИО"-сублейбл) — это плановый дрейф C-03, переписывается в Плане 04, НЕ регрессия этого плана.
</verification>

<success_criteria>
- act_handover.html не содержит border-bottom нигде, кроме .value-blank и .signature-line.
- Текст интро и лейблов полей идентичен тексту до правки (D-01), подтверждено зелёным render_handover_act_contains_d09_intro_phrase.
- Множественное число (D-02) и безусловное «Сроком до» (D-03) реализованы через length-фильтр и удаление внешнего if/elif.
- Блок подписей горизонтальный, без "ФИО"-сублейбла, с напечатанными act.giver_name/act.receiver_name (D-06/D-07/D-08).
</success_criteria>

<output>
Create `.planning/phases/35-act-handover-body/35-02-SUMMARY.md` when done
</output>
