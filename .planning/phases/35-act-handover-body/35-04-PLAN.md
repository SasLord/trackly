---
phase: 35-act-handover-body
plan: 04
type: execute
wave: 3
depends_on: ["35-02", "35-03"]
files_modified:
  - crates/trackly-app/tests/pdf_render_act.rs
  - crates/trackly-app/tests/html_act_render.rs
  - crates/trackly-app/tests/acts_e2e_smoke.rs
  - crates/trackly-app/tests/html_field_row_underline_gate.rs
autonomous: true
requirements: [DOC-07, DOC-08, DOC-09]

must_haves:
  truths:
    - "pdf_render_act.rs::signature_renders_two_line_labels переписан под D-06/D-07: больше не проверяет наличие сублейбла ФИО, вместо этого проверяет присутствие напечатанного act.giver_name в блоке подписей — C-03."
    - "pdf_render_act.rs::render_handover_act_produces_cyrillic_pdf — комментарий обновлён и больше не описывает поведение Фазы 15 D-09 (giver_name отсутствует в теле); ассерции не менялись, т.к. были технически совместимы — C-03."
    - "pdf_render_act.rs::render_handover_act_contains_d09_intro_phrase НЕ тронут — D-01 не переоткрывается."
    - "html_act_render.rs::html_handover_contains_required_blocks_and_logo больше не проверяет строку ФИО в списке ожидаемых меток и проверяет присутствие напечатанного имени подписанта — C-03/Pitfall 3 (найдено research, не было в CONTEXT.md)."
    - "html_act_render.rs::html_acceptance_contains_required_blocks расширен ассерцией отсутствия строки «Кто передал» в HTML — подтверждает дедупликацию D-09."
    - "acts_e2e_smoke.rs::handover_pdf_render_within_e2e — устаревший комментарий про D-09 Фазы 15 обновлён под текущее поведение D-06 — C-03."
    - "Новый структурный тест html_field_row_underline_gate.rs подтверждает отсутствие border-bottom в диапазоне между include _header.html и .signatures в act_handover.html — дешёвый durable-гейт для DOC-07 (RESEARCH.md Open Question 1)."
  artifacts:
    - path: "crates/trackly-app/tests/pdf_render_act.rs"
      provides: "Переписанные ассерции под D-06/D-07, актуальные комментарии"
    - path: "crates/trackly-app/tests/html_act_render.rs"
      provides: "Ассерции без ФИО-сублейбла, расширенная проверка дедупликации акта приёмки"
    - path: "crates/trackly-app/tests/acts_e2e_smoke.rs"
      provides: "Актуальные комментарии, ассерции не менялись"
    - path: "crates/trackly-app/tests/html_field_row_underline_gate.rs"
      provides: "Структурный regex-гейт DOC-07 (border-bottom отсутствует в теле handover)"
  key_links:
    - from: "crates/trackly-app/tests/pdf_render_act.rs"
      to: "crates/trackly-app/templates/act_handover.html"
      via: "render_pdf возвращает HTML, тесты проверяют присутствие/отсутствие строк по markup из Плана 02"
      pattern: "html\\.contains"
    - from: "crates/trackly-app/tests/html_field_row_underline_gate.rs"
      to: "crates/trackly-app/templates/act_handover.html"
      via: "include_str! (compile-time) читает файл, regex извлекает диапазон между include _header.html и .signatures"
      pattern: "include_str!\\(\"\\.\\./templates/act_handover\\.html\"\\)"
---

<objective>
Обновить три существующих тестовых файла под новый markup `act_handover.html`/`act_acceptance.html`
из Планов 02/03 (плановый дрейф C-03, шире заявленного в CONTEXT.md — RESEARCH.md нашёл третий
файл, `html_act_render.rs`), и добавить дешёвый структурный regex-гейт против регрессии DOC-07.

Purpose: без этой правки `cargo test -p trackly-app` красный после Планов 02/03 — это ожидаемое,
не регрессионное поведение (тесты писались вокруг Фазы 15 D-09, которую Фаза 35 D-06 частично
отменяет), но плановое, а не забытое.
Output: три обновлённых теста + один новый структурный тест; полный сьют зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-act-handover-body/35-CONTEXT.md
@.planning/phases/35-act-handover-body/35-RESEARCH.md
@.planning/phases/35-act-handover-body/35-PATTERNS.md
@.planning/phases/35-act-handover-body/35-VALIDATION.md
</context>

<interfaces>
Образец для нового структурного гейта — `crates/trackly-app/tests/html_page_parity.rs` (49 строк,
читается целиком в Task 3's read_first): `const ... = include_str!("../templates/act_handover.html")`
на верхнем уровне файла + функция-хелпер, извлекающая регексом нужный диапазон текста, + `#[test]`
с `assert!`/`assert_eq!` и информативным сообщением при провале. Тот же приём копируется для нового
гейта, меняется только извлекаемый диапазон и условие (отсутствие подстроки вместо равенства двух
блоков).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: tests/pdf_render_act.rs — переписать под D-06/D-07</name>
  <files>crates/trackly-app/tests/pdf_render_act.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_render_act.rs строки 120-294 (create_handover_with_giver, render_handover_act_produces_cyrillic_pdf, render_handover_act_contains_d09_intro_phrase, signature_renders_two_line_labels, render_handover_multi_device_wraps_long_fields)
    crates/trackly-app/templates/act_handover.html (после Планов 02 — актуальная разметка блока подписей и полей)
    .planning/phases/35-act-handover-body/35-PATTERNS.md (секция tests/pdf_render_act.rs)
  </read_first>
  <action>
    В render_handover_act_produces_cyrillic_pdf (строки 157-206, текущий HEAD ДО этого плана) —
    обновить doc-комментарий над функцией (строки 157-168): убрать формулировку "D-09 removed
    giver_name from the rendered body — it now only appears via the bare Выдал signature label" и
    заменить описанием, что act.giver_name (Фаза 35 D-06) теперь печатается в блоке подписей вместе
    с receiver_name в интро; сама ассерция html.contains("Петров") НЕ меняется (D-01 не тронут).
    В signature_renders_two_line_labels (строки 233-252) — переименовать функцию в
    signature_renders_giver_name_horizontal_block; заменить цикл проверки
    ["Выдал","Получил","Подпись","ФИО"] на ["Выдал","Получил","Подпись"] (без "ФИО" — D-07);
    добавить отдельную assert!(html.contains("Иванов И.И.")) — это giver, переданный
    create_handover_with_giver в этом тесте, подтверждающий присутствие напечатанного ФИО в блоке
    подписей (D-06). Обновить doc-комментарий функции: заменить "Two-line signature sublabels (D-07)"
    на описание горизонтального блока с напечатанным ФИО (D-06/D-07). render_handover_multi_device_wraps_long_fields
    (строки 262+) — перечитать после Плана 02 и подтвердить отсутствие завязки на
    <span class="value">-селектор; если такой завязки нет (ожидаемо — тест проверяет только
    html.contains подстрок), правка не требуется. render_handover_act_contains_d09_intro_phrase
    (строки 211-231) — НЕ трогать.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test pdf_render_act -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Все тесты в pdf_render_act.rs зелёные, включая переименованный signature_renders_giver_name_horizontal_block
    - signature_renders_giver_name_horizontal_block не содержит строки "ФИО" в списке ожидаемых меток
    - signature_renders_giver_name_horizontal_block содержит отдельную ассерцию на "Иванов И.И."
    - render_handover_act_contains_d09_intro_phrase байт-в-байт не изменён (git diff показывает 0 изменений в теле этой функции)
  </acceptance_criteria>
  <done>pdf_render_act.rs зелёный целиком; сигнатурный тест переименован и проверяет новое поведение D-06/D-07; D-09-тест не тронут.</done>
</task>

<task type="auto">
  <name>Task 2: tests/html_act_render.rs — убрать ассерцию ФИО, расширить проверку дедупликации акта приёмки</name>
  <files>crates/trackly-app/tests/html_act_render.rs</files>
  <read_first>
    crates/trackly-app/tests/html_act_render.rs строки 160-241 (html_handover_contains_required_blocks_and_logo, html_acceptance_contains_required_blocks)
    crates/trackly-app/templates/act_handover.html и act_acceptance.html (после Планов 02/03)
    .planning/phases/35-act-handover-body/35-PATTERNS.md (секция tests/html_act_render.rs, Pitfall 3 в RESEARCH.md)
  </read_first>
  <action>
    В html_handover_contains_required_blocks_and_logo (строки 188-195, текущий HEAD ДО этого плана)
    — убрать "ФИО" из списка ["Акт приема-передачи","Выдал","Получил","Подпись","ФИО"] (список
    становится ["Акт приема-передачи","Выдал","Получил","Подпись"]); добавить отдельную
    assert!(html.contains("Выдалов В.В.")) сразу после цикла — это giver, переданный
    create_handover(&p.acts, &[device_id], "Выдалов В.В.", "Получилов П.П.") на строке 184,
    подтверждающий присутствие напечатанного ФИО в блоке подписей (D-06). В
    html_acceptance_contains_required_blocks (строки 212-240+) — после существующих ассерций на
    "Отдалов О.О."/"Принялов П.П." добавить assert!(!html.contains("Кто передал")) — подтверждает,
    что дублирующая строка таблицы удалена (D-09), а не просто переехала под другим текстом.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_act_render -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Оба теста в html_act_render.rs зелёные
    - html_handover_contains_required_blocks_and_logo не содержит "ФИО" в списке ожидаемых меток и содержит отдельную ассерцию на "Выдалов В.В."
    - html_acceptance_contains_required_blocks содержит assert отсутствия "Кто передал"
  </acceptance_criteria>
  <done>html_act_render.rs зелёный целиком; handover-тест не проверяет ФИО-сублейбл, acceptance-тест подтверждает дедупликацию.</done>
</task>

<task type="auto">
  <name>Task 3: tests/acts_e2e_smoke.rs комментарий + новый структурный гейт html_field_row_underline_gate.rs + полный сьют</name>
  <files>crates/trackly-app/tests/acts_e2e_smoke.rs, crates/trackly-app/tests/html_field_row_underline_gate.rs</files>
  <read_first>
    crates/trackly-app/tests/acts_e2e_smoke.rs строки 255-321 (handover_pdf_render_within_e2e, acceptance_pdf_render_smoke)
    crates/trackly-app/tests/html_page_parity.rs (49 строк, целиком — образец для копирования приёма include_str! + regex-извлечение + assert)
    crates/trackly-app/templates/act_handover.html (после Плана 02 — актуальная структура для regex-диапазона)
  </read_first>
  <action>
    В handover_pdf_render_within_e2e (строки 288-291, текущий HEAD ДО этого плана) — заменить
    комментарий "D-09 (Phase 15 plan 02) removed giver_name from the rendered body — it now only
    appears via the bare Выдал signature label" на описание текущего поведения: giver_name (Фаза 35
    D-06) теперь печатается в блоке подписей; assert!(html.contains("Петров")) не менять.
    acceptance_pdf_render_smoke — не трогать (ассерции уже совместимы, комментарий не устарел).
    Создать новый файл crates/trackly-app/tests/html_field_row_underline_gate.rs по образцу
    html_page_parity.rs: константа ACT_HANDOVER_HTML = include_str!("../templates/act_handover.html");
    функция-хелпер извлекает regex'ом диапазон текста между маркером {% include "_header.html" %} и
    маркером <div class="signatures"> (оба маркера гарантированно присутствуют в файле после Плана
    02); тест #[test] fn handover_body_has_no_underline_outside_signature_and_blank_deadline()
    проверяет, что извлечённый диапазон содержит РОВНО ОДНО вхождение "border-bottom" (класс
    .value-blank для пустого «Сроком до», D-03/D-10) — если ноль или больше одного, паника с
    информативным сообщением о количестве найденных вхождений. Диапазон намеренно заканчивается
    ПЕРЕД .signatures, чтобы не включать легитимный border-bottom строки подписи (D-06).
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - acts_e2e_smoke.rs: комментарий над handover_pdf_render_within_e2e обновлён, assert!(html.contains("Петров")) не изменён
    - html_field_row_underline_gate.rs существует, компилируется, тест зелёный
    - Полный сьют `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1` зелёный (требует предварительного `pnpm --dir ui build` — выполнить перед запуском, если ui/dist ещё placeholder)
  </acceptance_criteria>
  <done>acts_e2e_smoke.rs комментарий актуален; новый структурный гейт DOC-07 существует и проходит; весь `cargo test -p trackly-app` зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Тестовый код → публичный git-репозиторий | Все ФИО в новых/изменённых ассерциях — уже существующие в кодовой базе вымышленные значения ("Иванов И.И.", "Выдалов В.В.") — новых литералов с потенциально реальными данными не вводится |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | html_field_row_underline_gate.rs (новый файл, только чтение) | accept | Тест только читает `act_handover.html` через `include_str!` (compile-time), не модифицирует ничего — риска нет по построению, аналогично `html_page_parity.rs` |
| T-35-08 | Repudiation | Ослабленные/удалённые ассерции (например, снятие "ФИО" из списка меток) | mitigate | Каждая удалённая ассерция заменяется явной новой (отдельный assert на напечатанное ФИО, assert на отсутствие "Кто передал") — тестовое покрытие не уменьшается, а перенаправляется на актуальное поведение |
</threat_model>

<verification>
Финальная проверка плана: `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1` — весь сьют зелёный (требует `pnpm --dir ui build` заранее). Не запускать `cargo test --workspace` (виснет на auth_remember_cookie) и не запускать два `cargo test` параллельно.
</verification>

<success_criteria>
- Все три перечисленных тестовых файла отражают markup из Планов 02/03, не старое поведение Фазы 15 D-09.
- Новый структурный гейт DOC-07 существует и проходит.
- `cargo test -p trackly-app -- --test-threads=1` зелёный целиком.
</success_criteria>

<output>
Create `.planning/phases/35-act-handover-body/35-04-SUMMARY.md` when done
</output>
