---
phase: 35-act-handover-body
plan: 05
type: execute
wave: 4
depends_on: ["35-04"]
files_modified: []
autonomous: false
requirements: [DOC-07, DOC-08, DOC-09]

must_haves:
  truths:
    - "Приватность проверена ДО коммита фазы: diff всех изменённых файлов не содержит реальных ФИО сотрудников или реквизитов организации, только вымышленные значения — C-07."
    - "target/debug/templates/ удалён перед UAT, чтобы cargo tauri dev материализовал свежий embedded-default из отредактированных шаблонов — Pitfall 2."
    - "pnpm --dir ui build выполнен, чтобы LAN-браузерный транспорт раздавал актуальный ui/dist, а не устаревший placeholder — C-05."
    - "Полный `cargo test -p trackly-app -- --test-threads=1` зелёный как финальный phase gate — VALIDATION.md."
    - "Рендер настоящего PDF/превью подтверждает вёрстку на ОБОИХ транспортах (десктоп + LAN-браузер), а не текстовое сравнение — критерий #5 фазы, C-05."
    - "durable-гейт ui/scripts/check-print-isolation.mjs (pnpm --dir ui lint) остаётся зелёным — C-06."
  artifacts: []
  key_links: []
---

<objective>
Закрыть фазу: privacy-грep перед коммитом (C-07), операционная подготовка окружения перед UAT
(удаление устаревшего build-артефакта, сборка ui/dist), финальный автоматический phase gate, и
обязательная ручная визуальная проверка вёрстки тела акта на обоих транспортах — единственный
способ подтвердить DOC-07/DOC-08 (text-extraction тесты не видят CSS/геометрию).

Purpose: критерий #5 фазы («рендер настоящего PDF/превью подтверждает вёрстку на обоих
транспортах») и C-07 (жёсткое условие приватности) — оба требуют человеческого подтверждения,
не автоматизируются.
Output: privacy-грep выполнен и чист; окружение готово к UAT; полный сьют зелёный; фаза
подтверждена визуально на десктопе и в LAN-браузере.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-act-handover-body/35-CONTEXT.md
@.planning/phases/35-act-handover-body/35-RESEARCH.md
@.planning/phases/35-act-handover-body/35-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Privacy-грep, операционная подготовка, финальный phase gate</name>
  <files>Нет изменяемых файлов — операционные действия (grep/build/test) над файлами, изменёнными в Планах 01-04</files>
  <read_first>
    CLAUDE.md (раздел Constraints — жёсткое условие приватности)
    scripts/check-privacy-requisites.sh (существующий гейт org-реквизитов — не покрывает ФИО, только inn/kpp/okpo/ogrn/phone/fax)
    .planning/phases/35-act-handover-body/35-CONTEXT.md (раздел C-07)
  </read_first>
  <action>
    Выполнить `git status`/`git diff` по всем файлам, изменённым в Планах 01-04 этой фазы
    (act_handover.html, act_acceptance.html, _legacy_defaults/v22/*, html_templates.rs,
    template_service.rs, tests/pdf_render_act.rs, tests/html_act_render.rs, tests/acts_e2e_smoke.rs,
    tests/html_field_row_underline_gate.rs), и вручную просмотреть diff на предмет реальных ФИО или
    реквизитов организации — допустимы только уже утверждённые вымышленные значения из этих файлов
    ("Иванов И.И.", "Петров П.П.", "Выдалов В.В.", "Получилов П.П.", "Отдалов О.О.",
    "Принялов П.П.", "Сидоров-Петроградский Иван Александрович", "Кузнецов К.К."). Запустить
    `./scripts/check-privacy-requisites.sh` (существующий гейт org-реквизитов) и убедиться, что он
    зелёный — он не покрывает ФИО, но подтверждает отсутствие новых org-реквизитных литералов.
    Убедиться, что `.planning/reference/act-word-source/act-sample.docx` (содержит реальные ФИО и
    реквизиты) не процитирован дословно ни в одном изменённом файле, коммите или SUMMARY. Затем
    удалить каталог `target/debug/templates/` (материализован с датой Фазы 34, содержит тела ДО
    правок этой фазы — Pitfall 2 из RESEARCH.md), выполнить `pnpm --dir ui build` (обновляет
    `ui/dist` для LAN-транспорта — без этого `security_headers`-тест и живой LAN-предпросмотр видят
    устаревшую сборку), и выполнить финальный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test
    -p trackly-app -- --test-threads=1` (не запускать `cargo test --workspace` — виснет на
    auth_remember_cookie; не запускать параллельно с другим cargo test). Также выполнить
    `pnpm --dir ui lint` и подтвердить, что `check-print-isolation.mjs` остаётся зелёным (C-06,
    durable-гейт не должен пострадать от правок этой фазы — она их не касается, но проверка дешёвая).
  </action>
  <verify>
    <automated>./scripts/check-privacy-requisites.sh && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/check-privacy-requisites.sh` завершается с кодом 0
    - Ручной просмотр diff не находит строк, похожих на реальные ФИО/реквизиты, вне утверждённого списка вымышленных значений
    - `target/debug/templates/` отсутствует ИЛИ пересоздан после удаления и содержит акт актуальной (пост-Фаза-35) версии
    - `pnpm --dir ui build` завершается без ошибок, `ui/dist` содержит свежую сборку
    - Полный `cargo test -p trackly-app -- --test-threads=1` зелёный (0 failed)
    - `pnpm --dir ui lint` зелёный, включая `check-print-isolation.mjs`
  </acceptance_criteria>
  <done>Privacy-грep чист, окружение готово к UAT, полный автоматический сьют и lint зелёные.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ручная визуальная UAT-проверка тела акта на обоих транспортах</name>
  <action>Провести блокирующую человеческую проверку DOC-07/DOC-08 (полоски, горизонтальный блок подписей, множественное число, дедупликация акта приёмки) на десктопе и в LAN-браузере по инструкции ниже; не авто-одобрять (gate=blocking).</action>
  <what-built>
    Переработанное тело акта приёма-передачи (act_handover.html) и акта приёмки
    (act_acceptance.html): без полосок-подчёркиваний под автоподставляемым текстом (кроме подписи
    и пустого «Сроком до»), горизонтальный блок подписей с напечатанными ФИО, множественное число
    при N>1 устройств, доставка через legacy-defaults v22 в уже установленные копии.
  </what-built>
  <how-to-verify>
    Десктоп: удалить `target/debug/templates/` (если ещё не сделано в Task 1), запустить
    `cargo tauri dev`, создать акт приёма-передачи на одно устройство, открыть предпросмотр —
    убедиться, что (1) под полями «Инвентарный номер», «Серийный номер», «Модель», «Комплектация»,
    «Технические характеристики», «Состояние» НЕТ полоски-подчёркивания, значение идёт сплошным
    текстом сразу после метки; (2) строка «Сроком до:» присутствует всегда — если срок не указан,
    под ней полоска для руки; (3) блок подписей — горизонтальный, две строки «Выдал: ⟨полоска⟩
    ⟨ФИО⟩» и «Получил: ⟨полоска⟩ ⟨ФИО⟩», под полоской только «Подпись», без второй подписи «ФИО»
    и без даты. Создать второй акт на 2+ устройства — убедиться, что вместо повторяющегося
    «было получено устройство:» на каждое устройство выводится одна строка «были получены
    устройства:» и список имён. Открыть предпросмотр «Документ приёма устройства на склад» —
    убедиться, что ФИО встречается один раз (в блоке подписей, не в таблице), блок подписей того
    же горизонтального вида. Открыть Settings → Шаблоны → выбрать `act_handover` → «Предпросмотр»
    без изменений — убедиться, что редактор НЕ падает с ошибкой. Повторить то же для
    `act_acceptance`.

    LAN-браузер: запустить сервер-режим (после `pnpm --dir ui build` из Task 1), открыть
    предпросмотр того же акта приёма-передачи в браузере из локальной сети — повторить визуальные
    проверки (1)-(3) выше.

    🔒 При описании результата НЕ указывать реальные ФИО/реквизиты — обезличивать («ФИО получателя
    отображается корректно», а не само ФИО).
  </how-to-verify>
  <resume-signal>Опишите результат: "approved" (все пункты подтверждены на обоих транспортах) или опишите конкретные расхождения — обезличенно, без реальных ФИО/реквизитов.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Diff фазы → публичный git-репозиторий | Последняя точка контроля перед коммитом — единственный durable способ не допустить утечку реальных данных, т.к. закоммиченное остаётся в истории git даже после удаления из HEAD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-SC | Information Disclosure | Diff всех файлов фазы перед коммитом | mitigate | Ручной privacy-грep + `check-privacy-requisites.sh` + явная сверка со списком утверждённых вымышленных ФИО (Task 1) — blocking-проверка перед коммитом |
| T-35-09 | Information Disclosure | Ручной UAT-отчёт (Task 2 resume-signal) | mitigate | Инструкция явно требует обезличенного описания результата — реальные ФИО/реквизиты не должны попасть в текст ответа пользователя или в SUMMARY |
</threat_model>

<verification>
Полный `cargo test -p trackly-app -- --test-threads=1` зелёный (Task 1) + `pnpm --dir ui lint` зелёный + ручное подтверждение пользователя (Task 2) на обоих транспортах — это финальный gate фазы 35 перед `/gsd-verify-work`.
</verification>

<success_criteria>
- Privacy-грep чист, ни одного реального ФИО/реквизита не найдено в diff фазы.
- `target/debug/templates/` пересоздан из актуальных шаблонов; `ui/dist` пересобран.
- Полный автоматический сьют и `check-print-isolation.mjs` зелёные.
- Пользователь подтвердил вёрстку визуально на десктопе и в LAN-браузере (criteria #3/#4/#5 фазы).
</success_criteria>

<output>
Create `.planning/phases/35-act-handover-body/35-05-SUMMARY.md` when done
</output>
