---
phase: 35-act-handover-body
plan: 07
type: execute
wave: 6
depends_on: ["35-06"]
files_modified:
  - crates/trackly-app/templates/act_handover.html
  - crates/trackly-app/templates/act_acceptance.html
  - crates/trackly-app/templates/_legacy_defaults/v23/act_handover.html
  - crates/trackly-app/templates/_legacy_defaults/v23/act_acceptance.html
  - crates/trackly-app/src/pdf/html_templates.rs
  - crates/trackly-app/tests/html_field_row_underline_gate.rs
  - crates/trackly-app/tests/pdf_render_act.rs
autonomous: false
requirements: [DOC-08]

must_haves:
  truths:
    - "VERIFICATION.md (2026-08-12, status gaps_found 4/5) Success Criterion #4 / DOC-08 закрыт: `.signature-row .signature-name` в ОБОИХ act_handover.html и act_acceptance.html больше не `white-space: nowrap` без `min-width: 0` — правило разрешает перенос (`min-width: 0; white-space: normal; overflow-wrap: break-word;`), поэтому длинное кириллическое ФИО (двойная фамилия + отчество, >= 50 символов) переносится внутри доступной ширины строки подписи вместо ухода за печатную область."
    - "missing item 1 из VERIFICATION.md закрыт структурным CSS-гейтом (не текстовым `html.contains`) в crates/trackly-app/tests/html_field_row_underline_gate.rs, который читает <style>-блок по селектору и проверяет отсутствие голого `white-space: nowrap` в правиле `.signature-row .signature-name` для обоих шаблонов."
    - "missing item 2 из VERIFICATION.md закрыт двумя уровнями доказательства: (a) автоматический тест полного пайплайна рендера (render_pdf/render_acceptance_pdf) с вымышленным длинным ФИО (>= 50 кириллических символов) подтверждает, что имя доходит до <span class=\"signature-name\"> целиком, без обрезания — это необходимое, но не достаточное условие; (b) настоящая ручная печать/предпросмотр на десктопе И в LAN-браузере (Task 3, checkpoint:human-verify, gate=blocking) — единственный путь увидеть реальную геометрию для HTML+Paged.js-пайплайна этой фазы (RESEARCH.md Pitfall 5: автоматизированный Rust-side PDF-рендер для этого шаблона не существует с Фазы 16 — text-extraction тесты структурно не видят перенос/overflow, память act-pdf-word-fidelity)."
    - "Бэкенд не тронут: git diff по crates/trackly-app/src/services/ за весь план пуст — фикс полностью в шаблонах, тестах и legacy-defaults инфраструктуре."
    - "Ловушка db-backed-templates-upgrade-trap закрыта для этого изменения тела: новый срез _legacy_defaults/v23/ (ПРЕ-фикс-состояние обоих шаблонов, зафиксированное ДО применения CSS-правки) зарегистрирован в KNOWN_LEGACY_DEFAULTS как четвёртый элемент слайсов act_handover.html/act_acceptance.html и покрыт регрессионным тестом upgrade_replaces_v23_legacy_default_with_current_bundled_body (bodies.get(3)) — иначе установленные копии Trackly на пред-этим-планом теле не получат CSS-фикс автоматически."
  artifacts:
    - path: "crates/trackly-app/templates/act_handover.html"
      provides: "`.signature-row .signature-name` разрешает перенос длинного ФИО (min-width:0/white-space:normal/overflow-wrap:break-word), без изменений разметки/текста тела"
    - path: "crates/trackly-app/templates/act_acceptance.html"
      provides: "идентичный CSS-фикс в дублирующей копии блока подписей (REVIEW.md WR-08 — фикс применён в обеих копиях, рефакторинг общего партиала НЕ делается в этом плане)"
    - path: "crates/trackly-app/templates/_legacy_defaults/v23/act_handover.html"
      provides: "снимок тела act_handover.html ДО CSS-фикса этого плана (пост-35-06, с уже снятым гейтом length==1, но ещё с nowrap-дефектом WR-02)"
    - path: "crates/trackly-app/templates/_legacy_defaults/v23/act_acceptance.html"
      provides: "снимок тела act_acceptance.html ДО CSS-фикса этого плана"
    - path: "crates/trackly-app/src/pdf/html_templates.rs"
      provides: "v23 зарегистрирован в KNOWN_LEGACY_DEFAULTS (index 3, оба филенейма); новый тест upgrade_replaces_v23_legacy_default_with_current_bundled_body"
    - path: "crates/trackly-app/tests/html_field_row_underline_gate.rs"
      provides: "структурный CSS-гейт: .signature-row .signature-name разрешает перенос в обоих шаблонах, без голого nowrap"
    - path: "crates/trackly-app/tests/pdf_render_act.rs"
      provides: "два новых теста полного пайплайна (handover + acceptance) с вымышленным длинным ФИО-фикстурой, подтверждающих целостность имени в <span class=\"signature-name\">"
  key_links:
    - from: "crates/trackly-app/tests/html_field_row_underline_gate.rs (новый тест)"
      to: "crates/trackly-app/templates/act_handover.html + act_acceptance.html"
      via: "include_str! (compile-time) -> extract_style_block -> extract_rule_body(\".signature-row .signature-name\")"
      pattern: "extract_rule_body\\(&style, \"\\.signature-row \\.signature-name\"\\)"
    - from: "crates/trackly-app/tests/pdf_render_act.rs (новые тесты)"
      to: "ActService::render_pdf / render_acceptance_pdf"
      via: "полный пайплайн: seed_devices -> create_handover_with_giver(giver=длинное ФИО) -> render_pdf -> assert полное имя внутри signature-name span"
      pattern: "signature-name\\\">.*Александрович"
    - from: "crates/trackly-app/src/pdf/html_templates.rs::upgrade_replaces_v23_legacy_default_with_current_bundled_body"
      to: "crates/trackly-app/templates/_legacy_defaults/v23/act_handover.html + act_acceptance.html"
      via: "bodies.get(3) индексирует четвёртый элемент KNOWN_LEGACY_DEFAULTS-слайса, include_str! компилируется в бинарник"
      pattern: "bodies\\.get\\(3\\)"
---

<objective>
Закрыть единственный оставшийся гэп Фазы 35 из VERIFICATION.md (status: gaps_found, 4/5,
2026-08-12): Success Criterion #4 / DOC-08 — `.signature-row .signature-name` в
`act_handover.html` и `act_acceptance.html` объявлен `white-space: nowrap` без `min-width: 0`
на flex-элементе, из-за чего длинное кириллическое ФИО (двойная фамилия + отчество) не может
сжаться/перенестись и уходит за печатную ширину страницы — дефект WR-02 из 35-REVIEW.md,
подтверждённый независимо верификатором прямым чтением CSS.

Purpose: замкнуть DOC-08 (Success Criterion #4) без переоткрытия структуры/данных блока
подписей (D-06..D-09 остаются в силе — меняется только перенос текста ФИО, не разметка и не
источник данных) и без изменений бэкенда.
Output: `.signature-row .signature-name` в обоих шаблонах разрешает перенос длинного текста;
структурный CSS-гейт и тест полного рендер-пайплайна с длинным ФИО-фикстурой; новый срез
`_legacy_defaults/v23/` доставляет фикс установленным копиям; ручное подтверждение печати на
обоих транспортах с длинным ФИО.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-act-handover-body/35-CONTEXT.md
@.planning/phases/35-act-handover-body/35-VERIFICATION.md
@.planning/phases/35-act-handover-body/35-REVIEW.md
@.planning/phases/35-act-handover-body/35-RESEARCH.md
@.planning/phases/35-act-handover-body/35-06-PLAN.md
@.planning/phases/35-act-handover-body/35-06-SUMMARY.md
</context>

<interfaces>
**Current (pre-fix) `.signature-name` CSS — IDENTICAL in both templates:**

`act_handover.html:117-119`:
```
  .signature-row .signature-name {
    white-space: nowrap;
  }
```
`act_acceptance.html:103-105`: byte-identical rule (duplicated block, REVIEW.md WR-08 — not
refactored to a shared partial in this plan, both copies get the same fix independently).

Both rules sit inside a `.signature-row { display: flex; align-items: flex-end; gap: 10pt; }`
parent. Sibling `.signature-field { flex: 0 0 160pt; }` is fixed-width and NOT touched by this
plan. `.signature-name` currently has no explicit `flex` value (defaults to the CSS initial
`flex: 0 1 auto` — flex-shrink is already 1, but `white-space: nowrap` forces its min-content
width to the full unbroken text width, which `min-width: 0` alone would not fix without also
permitting the text itself to wrap).

**`html_templates.rs` — `KNOWN_LEGACY_DEFAULTS` (current lines 75-90, exact text this plan
extends):**
```rust
pub const KNOWN_LEGACY_DEFAULTS: &[(&str, &[&str])] = &[
    (
        "act_handover.html",
        &[
            include_str!("../../templates/_legacy_defaults/v20/act_handover.html"),
            include_str!("../../templates/_legacy_defaults/v21/act_handover.html"),
            include_str!("../../templates/_legacy_defaults/v22/act_handover.html"),
        ],
    ),
    (
        "act_acceptance.html",
        &[
            include_str!("../../templates/_legacy_defaults/v20/act_acceptance.html"),
            include_str!("../../templates/_legacy_defaults/v21/act_acceptance.html"),
            include_str!("../../templates/_legacy_defaults/v22/act_acceptance.html"),
        ],
    ),
    ...
```
v20/v21/v22 are the ONLY existing legacy slices for these two filenames — v22 (added by Plan
35-01) is the pre-Phase-35 body. This plan's v23 becomes the FOURTH element (index 3) of each
slice — the pre-THIS-PLAN body (i.e., post-35-06: `length==1` gate already removed, but the
`nowrap` CSS defect still present).

**`upgrade_replaces_v22_legacy_default_with_current_bundled_body`
(`html_templates.rs:477-525`, the exact structural skeleton the new v23 test mirrors):** locks
`ENV_GUARD`, builds a tempdir, iterates `DEFAULT_HTML_TEMPLATES`, looks up each filename's slice
in `KNOWN_LEGACY_DEFAULTS`, pulls `bodies.get(2)` (skipping via `continue` when absent — safely
skips `report.html`'s 2-element slice and `_header.html`'s empty slice), asserts
`assert_ne!(v22_body, current, ...)` as a precondition guard, writes the v22 body to the
tempdir, calls `upgrade_untouched_defaults_on_startup(dir.path())`, re-reads each file and
`assert_eq!` against `current`, gated by `bodies.len() > 2`. The new v23 test is a structural
sibling: `bodies.get(3)` instead of `bodies.get(2)`, `bodies.len() > 3` instead of `> 2`, a
locally-scoped `has_v23` variable (do not rename `has_v22` inside the existing function).

**`html_field_row_underline_gate.rs` helpers `extract_style_block`/`extract_rule_body`
(lines 27-49, free functions, reused as-is):** `extract_style_block(text)` returns the raw
`<style>...</style>` inner text; `extract_rule_body(css, selector)` returns the `{ ... }` body
of the first rule matching `selector` exactly. Both already used by the existing
`field_row_css_has_no_border_bottom_and_only_two_legit_exceptions_remain` and
`acceptance_signature_line_css_has_exactly_one_legitimate_border_bottom` tests — no duplication
needed, the new test in this plan reuses them against a different selector.

**`pdf_render_act.rs` helpers (lines 42-155, reused as-is):** `make_full_pipeline()`,
`seed_devices(&p.writer, count)`, `create_handover_with_giver(&p.acts, &device_ids, giver: &str)`
(sets `receiver_name` to the fixed fictional `"Петров П.П."`), and the direct
`p.acts.render_acceptance_pdf(device_id, giver, receiver, timestamp)` call pattern used by
`render_acceptance_pdf_for_device_works` (lines 621-645).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Снять фикс CSS ФИО-переноса, снять v23-снимок ДО правки, зарегистрировать в KNOWN_LEGACY_DEFAULTS</name>
  <files>crates/trackly-app/templates/act_handover.html, crates/trackly-app/templates/act_acceptance.html, crates/trackly-app/templates/_legacy_defaults/v23/act_handover.html, crates/trackly-app/templates/_legacy_defaults/v23/act_acceptance.html, crates/trackly-app/src/pdf/html_templates.rs</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html строки 92-120 (CSS `.signature-row`/`.signature-field`/`.signature-name`) и 172-189 (разметка блока подписей — НЕ трогать)
    crates/trackly-app/templates/act_acceptance.html строки 78-105 (идентичный CSS-блок) и 129-146 (разметка — НЕ трогать)
    crates/trackly-app/src/pdf/html_templates.rs строки 42-96 (DEFAULT_HTML_TEMPLATES, KNOWN_LEGACY_DEFAULTS, doc-комментарий "Extension point") и строки 467-525 (upgrade_replaces_v22_legacy_default_with_current_bundled_body — образец, который зеркалится под v23)
    crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html и .../v22/act_acceptance.html (образец оформления слайса — НЕ читать целиком, только подтвердить формат файла: сырой HTML, без доп. метаданных)
    .planning/phases/35-act-handover-body/35-VERIFICATION.md секция gaps -> missing (два пункта, закрываемые этим планом)
  </read_first>
  <action>
    ПОРЯДОК ОПЕРАЦИЙ ВАЖЕН (снимок обязан быть снят ДО правки, иначе он будет байт-в-байт
    идентичен текущему бандлу и precondition-гард `assert_ne!` в новом тесте провалится
    тривиально — тот же Pitfall 5, что уже задокументирован в v21/v22-тестах):

    1. Создать каталог `crates/trackly-app/templates/_legacy_defaults/v23/` и скопировать в него
       ТЕКУЩЕЕ (пред-правкой) содержимое `crates/trackly-app/templates/act_handover.html` и
       `crates/trackly-app/templates/act_acceptance.html` дословно, байт-в-байт (простое
       копирование файла, без редактирования) — это состояние ПОСЛЕ Плана 35-06 (гейт
       `length==1` уже снят), но ДО CSS-фикса этого плана (`white-space: nowrap` без
       `min-width: 0` в `.signature-row .signature-name` всё ещё присутствует).

    2. ТОЛЬКО ПОСЛЕ шага 1: в `crates/trackly-app/templates/act_handover.html` заменить тело
       правила `.signature-row .signature-name` (текущие строки 117-119, `white-space: nowrap;`)
       на три объявления: `min-width: 0; white-space: normal; overflow-wrap: break-word;` —
       никаких других правок в CSS-блоке (строки 46-120) не делать, `.signature-field`/
       `.signature-line`/`.signature-label` НЕ трогать. Идентичную замену выполнить в
       `crates/trackly-app/templates/act_acceptance.html` (текущие строки 103-105). Разметку
       (`<div class="signatures">`...) в ОБОИХ файлах НЕ трогать — это чисто CSS-правка одного
       правила в двух копиях.

    3. В `crates/trackly-app/src/pdf/html_templates.rs`, в массиве `KNOWN_LEGACY_DEFAULTS`
       (текущие строки 75-90), добавить ЧЕТВЁРТЫЙ элемент в слайс `"act_handover.html"` (после
       строки с `v22/act_handover.html`) —
       `include_str!("../../templates/_legacy_defaults/v23/act_handover.html")` — и аналогично
       четвёртый элемент в слайс `"act_acceptance.html"` —
       `include_str!("../../templates/_legacy_defaults/v23/act_acceptance.html")`. Слайсы
       `"report.html"` и `"_header.html"` НЕ трогать.

    4. Сразу после существующего теста `upgrade_replaces_v22_legacy_default_with_current_bundled_body`
       (заканчивается на текущей строке 525, перед `upgrade_leaves_user_customized_file_untouched`
       на строке 531), добавить структурный сиблинг-тест
       `upgrade_replaces_v23_legacy_default_with_current_bundled_body`, дословно зеркалящий
       скелет v22-теста (тот же `ENV_GUARD.lock()`, тот же `tempfile::tempdir()`, тот же цикл по
       `DEFAULT_HTML_TEMPLATES`, тот же поиск слайса, тот же вызов
       `upgrade_untouched_defaults_on_startup`), но с тремя точечными заменами: (a)
       `bodies.get(2)` → `bodies.get(3)` при извлечении `v23_body`; (b) precondition guard
       `assert_ne!(v23_body, current, ...)` с сообщением, называющим v23; (c) финальная проверка
       гейтится через `bodies.len() > 3` (локальная переменная `has_v23`, НЕ переименовывать
       `has_v22` внутри существующей функции). Добавить короткий doc-комментарий над новой
       функцией: закрывает доставку CSS-фикса Фазы 35 План 07 (DOC-08/SC#4, WR-02) установленным
       копиям на пред-этим-планом теле — тот же класс регрессии, что v21/v22-тесты предотвращают
       для своих версий. НЕ менять существующие v21/v22-функции, НЕ менять
       `every_default_template_has_a_known_legacy_defaults_entry`.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib pdf:: -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `diff crates/trackly-app/templates/_legacy_defaults/v23/act_handover.html crates/trackly-app/templates/act_handover.html` показывает РОВНО отличие в CSS-теле `.signature-row .signature-name` (снимок — старый `white-space: nowrap;`, текущий файл — новые три объявления) и НИГДЕ больше
    - `grep -c "min-width: 0" crates/trackly-app/templates/act_handover.html` возвращает 1, аналогично для `act_acceptance.html`
    - `grep -A2 "signature-row .signature-name" crates/trackly-app/templates/act_handover.html | grep -c "white-space: nowrap"` возвращает 0 (nowrap полностью убран из этого правила) в обоих файлах
    - `grep -c "v23/act_handover.html" crates/trackly-app/src/pdf/html_templates.rs` возвращает 1, `grep -c "v23/act_acceptance.html"` возвращает 1
    - `grep -c "fn upgrade_replaces_v23_legacy_default_with_current_bundled_body" crates/trackly-app/src/pdf/html_templates.rs` возвращает 1
    - `cargo test -p trackly-app --lib upgrade_replaces_v23_legacy_default_with_current_bundled_body -- --test-threads=1` завершается кодом 0 (precondition guard проходит НЕ тривиально — снимок снят до правки)
    - `cargo test -p trackly-app --lib upgrade_replaces_v22_legacy_default_with_current_bundled_body -- --test-threads=1` остаётся зелёным (v22-тест не задет)
    - `git diff --stat -- crates/trackly-app/src/services/` пуст
  </acceptance_criteria>
  <done>Оба шаблона разрешают перенос длинного ФИО в блоке подписей; новый срез v23 фиксирует пред-фикс состояние и доставляется установленным копиям через KNOWN_LEGACY_DEFAULTS + доказывающий тест; существующие v20/v21/v22-тесты не тронуты.</done>
</task>

<task type="auto">
  <name>Task 2: Структурный CSS-гейт + тест полного пайплайна с длинным вымышленным ФИО</name>
  <files>crates/trackly-app/tests/html_field_row_underline_gate.rs, crates/trackly-app/tests/pdf_render_act.rs</files>
  <read_first>
    crates/trackly-app/tests/html_field_row_underline_gate.rs (целиком, ~114 строк — extract_style_block/extract_rule_body хелперы, обе константы ACT_HANDOVER_HTML/ACT_ACCEPTANCE_HTML, существующие два теста как образец)
    crates/trackly-app/tests/pdf_render_act.rs строки 108-220 (seed_devices, create_handover_with_giver, render_handover_act_produces_cyrillic_pdf — уже использует "Сидоров-Петроградский Иван Александрович" как giver, 40 символов) и строки 621-645 (render_acceptance_pdf_for_device_works — вызов render_acceptance_pdf напрямую)
    .planning/phases/35-act-handover-body/35-RESEARCH.md раздел Pitfall 5 (почему для этого HTML+Paged.js-пайплайна автоматический тест НЕ может доказать отсутствие визуального переполнения — только структуру/содержание; настоящее доказательство = Task 3)
  </read_first>
  <action>
    В `crates/trackly-app/tests/html_field_row_underline_gate.rs` добавить новый тест
    `signature_name_css_permits_wrap_for_long_names`, размещённый после существующего
    `acceptance_signature_line_css_has_exactly_one_legitimate_border_bottom`. Тест итерируется по
    массиву `[("act_handover.html", ACT_HANDOVER_HTML), ("act_acceptance.html",
    ACT_ACCEPTANCE_HTML)]`; для каждого элемента: `let style = extract_style_block(html);`, `let
    body = extract_rule_body(&style, ".signature-row .signature-name");`, затем три ассерции —
    `assert!(body.contains("min-width: 0"))`, `assert!(body.contains("white-space: normal"))`,
    `assert!(body.contains("overflow-wrap: break-word"))`, и `assert!(!body.contains("nowrap"),
    "..name} must not force nowrap — long Cyrillic ФИО (DOC-08/SC#4, VERIFICATION.md gap) needs
    to wrap, not overflow")`. Сообщение паники должно называть filename и включать `body` целиком
    для диагностики. Doc-комментарий над тестом: закрывает VERIFICATION.md missing item 1
    (2026-08-12 re-verification) структурным CSS-гейтом, а не текстовым `html.contains`.

    В `crates/trackly-app/tests/pdf_render_act.rs`, добавить фикстуру-константу
    `const LONG_GIVER_NAME_FICTIONAL: &str = "Сидоров-Петроградский-Константинов Иван
    Александрович";` (53 символа — вымышленное расширение уже используемой в этом файле
    вымышленной фамилии «Сидоров-Петроградский» дополнительным дефисным компонентом; НЕ реальное
    ФИО, согласуется с приватность-константой CLAUDE.md) рядом с существующими module-level
    константами/хелперами (после `create_handover_with_giver`, перед первым тестом). Добавить два
    новых теста, каждый в стандартной обёртке `tokio::time::timeout(Duration::from_secs(60),
    async { ... }).await.expect("timeout")`:

    (1) `render_handover_with_long_giver_name_preserves_full_name_in_signature_block` —
    `make_full_pipeline()`, `seed_devices(&p.writer, 1)`, `create_handover_with_giver(&p.acts,
    &device_ids, LONG_GIVER_NAME_FICTIONAL)`, `p.acts.render_pdf(act.id).await`, затем: (a)
    `assert!(html.contains(LONG_GIVER_NAME_FICTIONAL))` — имя дошло до вывода целиком, без
    обрезания; (b) извлечь подстроку между `<span class="signature-name">` и первым `</span>`
    после позиции найденного `giver`-вхождения (простой `.find`/срез, без regex) и
    `assert_eq!` её содержимого точно `LONG_GIVER_NAME_FICTIONAL` — доказывает, что имя попало
    именно в `.signature-name`, а не куда-то ещё в документ. Комментарий над тестом: это
    доказывает целостность данных через полный пайплайн (DB -> ActService -> MiniJinja), НЕ
    доказывает отсутствие визуального переполнения печатной ширины — та проверка
    структурно невозможна из Rust-теста для этого HTML+Paged.js-шаблона (RESEARCH.md Pitfall 5) и
    выполняется человеком в Task 3.

    (2) `render_acceptance_with_long_giver_name_preserves_full_name_in_signature_block` — тот же
    приём через `p.acts.render_acceptance_pdf(device_ids[0], LONG_GIVER_NAME_FICTIONAL.to_string(),
    "Получилов П.П.".to_string(), 1_700_000_000).await` (сигнатура и вымышленное имя получателя —
    по образцу `render_acceptance_pdf_for_device_works`), те же две ассерции (полное вхождение +
    точное содержимое `.signature-name` span). Тот же комментарий про пределы доказательства.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_field_row_underline_gate --test pdf_render_act -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test html_field_row_underline_gate signature_name_css_permits_wrap_for_long_names -- --test-threads=1` завершается кодом 0
    - `cargo test -p trackly-app --test pdf_render_act render_handover_with_long_giver_name_preserves_full_name_in_signature_block -- --test-threads=1` завершается кодом 0
    - `cargo test -p trackly-app --test pdf_render_act render_acceptance_with_long_giver_name_preserves_full_name_in_signature_block -- --test-threads=1` завершается кодом 0
    - `grep -c "Сидоров-Петроградский-Константинов" crates/trackly-app/tests/pdf_render_act.rs` возвращает не менее 1 (фикстура объявлена и вымышлена, не совпадает ни с одним реальным ФИО)
    - Оба существующих теста в `html_field_row_underline_gate.rs` остаются зелёными (regression-check)
    - Полный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test pdf_render_act -- --test-threads=1` зелёный (весь файл, не только новые тесты)
  </acceptance_criteria>
  <done>Структурный CSS-гейт доказывает разрешение переноса в обоих шаблонах без голого nowrap; два новых теста полного пайплайна доказывают, что вымышленное длинное ФИО доходит целиком до <span class="signature-name"> для act_handover и act_acceptance — automated-evidence портит закрыт, визуальное доказательство остаётся за Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ручная проверка печати длинного ФИО на обоих транспортах</name>
  <read_first>
    .planning/phases/35-act-handover-body/35-06-PLAN.md Task 4 (образец формата UAT-чекпоинта этой же фазы — desktop + LAN-browser, target/debug/templates/ housekeeping)
    .planning/phases/35-act-handover-body/35-RESEARCH.md раздел Pitfall 5 (почему для HTML+Paged.js-пайплайна проверка = живой предпросмотр приложения, а не Rust-тест на PDF-байтах)
    .planning/phases/35-act-handover-body/35-VERIFICATION.md секция gaps -> missing item 2 (требование: рендер/печать на обоих транспортах, не текстовый assert)
  </read_first>
  <action>
    Перед запросом подтверждения у пользователя: выполнить `./scripts/check-privacy-requisites.sh`
    и вручную просмотреть diff всех семи файлов, изменённых Задачами 1-2 этого плана, на предмет
    реальных ФИО/реквизитов — допустимо только вымышленное значение, введённое этим планом
    («Сидоров-Петроградский-Константинов Иван Александрович», «Получилов П.П.») плюс уже
    утверждённые ранее в фазе вымышленные значения, встречающиеся в контексте (например
    «Петров П.П.» из `create_handover_with_giver`). Если в `target/debug/templates/` осталась
    материализованная копия шаблонов с прошлого раунда UAT (Плана 35-06) — удалить её (иначе
    следующий `cargo tauri dev` покажет старое, дефектное CSS-поведение и эта проверка ничего не
    докажет). Затем провести блокирующую человеческую проверку переноса длинного ФИО на десктопе
    и в LAN-браузере по инструкции ниже; не авто-одобрять (gate=blocking).
  </action>
  <what-built>
    `.signature-row .signature-name` в `act_handover.html` и `act_acceptance.html` больше не
    `white-space: nowrap` без права на перенос — правило теперь `min-width: 0; white-space:
    normal; overflow-wrap: break-word;`, поэтому длинное напечатанное ФИО переносится внутри
    доступной ширины строки подписи вместо ухода за печатную область страницы. Плюс: структурный
    CSS-гейт, два теста полного пайплайна с вымышленной длинной ФИО-фикстурой, срез
    `_legacy_defaults/v23/` доставляет фикс установленным копиям.
  </what-built>
  <how-to-verify>
    Десктоп: если `target/debug/templates/` не удалён с прошлого раунда — удалить, запустить
    `cargo tauri dev`. Создать акт приёма-передачи на любое устройство, указав в поле «Сдал»
    заведомо длинное вымышленное ФИО, например «Сидоров-Петроградский-Константинов Иван
    Александрович» (или аналогичное по длине вымышленное ФИО с двойной фамилией и отчеством, НЕ
    реальное ФИО сотрудника). Открыть предпросмотр акта — убедиться, что строка «Выдал:» с
    длинным ФИО ПОЛНОСТЬЮ видна в пределах печатной ширины страницы (переносится на вторую строку
    внутри своей колонки, если не помещается на одной, но НЕ обрезается и НЕ уходит за правый
    край листа). Повторить для акта приёмки (Документ приёма устройства на склад) с тем же
    вымышленным длинным ФИО в поле «Выдал».

    LAN-браузер: если фронтенд не пересобирался в рамках этой правки (эта правка — только
    HTML-шаблоны и Rust-тесты, `ui/dist` не затронут) — пересборка `pnpm --dir ui build` НЕ
    требуется для проверки самих актов; тем не менее если `ui/dist` уже был placeholder/устаревшим
    по другой причине, выполнить `pnpm --dir ui build`. Запустить сервер-режим, открыть
    предпросмотр обоих актов (приёма-передачи и приёмки) с тем же длинным вымышленным ФИО в
    браузере из локальной сети — повторить проверку выше.

    🔒 При описании результата НЕ указывать реальные ФИО/реквизиты — используйте только
    вымышленное значение, введённое этим планом, или опишите обезличенно («длинное вымышленное
    ФИО переносится корректно на обоих транспортах»).
  </how-to-verify>
  <resume-signal>Опишите результат обезличенно: "approved" (длинное вымышленное ФИО полностью видно в пределах печатной ширины на ОБОИХ транспортах для ОБОИХ актов — приёма-передачи и приёмки) или опишите конкретные расхождения — без реальных ФИО/реквизитов.</resume-signal>
  <acceptance_criteria>
    - `./scripts/check-privacy-requisites.sh` завершается с кодом 0 перед запросом подтверждения
    - Ручной просмотр diff семи изменённых файлов не находит строк, похожих на реальные ФИО/реквизиты, вне утверждённого списка
    - Ответ пользователя однозначно подтверждает отсутствие обрезания/переполнения длинного ФИО на ОБОИХ транспортах для ОБОИХ актов (приёма-передачи и приёмки)
    - Ответ пользователя не содержит реальных ФИО/реквизитов
  </acceptance_criteria>
  <done>Privacy-грep чист; пользователь подтвердил визуально на обоих транспортах, что длинное ФИО в блоке подписей переносится в пределах печатной ширины страницы, не обрезается и не выходит за край листа, для обоих печатных актов.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MiniJinja render → печатный документ | `act.giver_name`/`document.giver_name`/`act.receiver_name` интерполируются в `<span class="signature-name">` через autoescape (build_safe_html_env, T-16-01); этот план меняет только CSS-правило, обёртывающее уже существующий безопасный sink, новых точек интерполяции не добавляет |
| Diff этого плана → публичный git-репозиторий | Новая вымышленная фикстура «Сидоров-Петроградский-Константинов Иван Александрович» должна остаться вымышленной — последняя точка контроля перед коммитом (Task 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-16 | Information Disclosure | CSS-only правка `.signature-row .signature-name` в двух шаблонах | accept | Тот же render-sink, что уже безопасно используется с Фазы 35 Плана 06 (autoescape ON) — правка меняет только `white-space`/`min-width`/`overflow-wrap`, не структуру интерполяции, новой поверхности нет |
| T-35-17 | Tampering | Правка act_handover.html/act_acceptance.html (Task 1) могла бы случайно затронуть разметку блока подписей или CSS других правил | mitigate | Acceptance criteria Task 1 требуют diff строго внутри тела `.signature-row .signature-name` (через `diff` со снимком v23) — разметка `<div class="signatures">` явно вне scope правки |
| T-35-SC | Information Disclosure | Diff всех семи файлов этого плана (включая новую фикстуру в тестах) → публичный git-репозиторий | mitigate | Ручной privacy-грep + `check-privacy-requisites.sh` + явная сверка со списком утверждённых вымышленных значений (Task 3) — blocking-проверка перед коммитом |
| T-35-18 | Repudiation | Legacy-defaults v23-снимок (Task 1) мог бы быть снят ПОСЛЕ CSS-правки, обесценив upgrade-гарантию для установленных копий | mitigate | Task 1's action явно предписывает порядок «снимок сначала, правка потом»; acceptance criteria включают precondition-guard тест `upgrade_replaces_v23_legacy_default_with_current_bundled_body`, который проваливается тривиально, если снимок идентичен текущему бандлу |

</threat_model>

<verification>
`TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1 --skip
login_remember_persistent_cookie` зелёный (полный сьют, после Задач 1-2) + `./scripts/
check-privacy-requisites.sh` зелёный + ручное подтверждение пользователя (Task 3) на обоих
транспортах для обоих актов с длинным вымышленным ФИО — это финальный gate закрытия
единственного оставшегося гэпа Фазы 35 перед повторным `/gsd-verify-work`.
</verification>

<success_criteria>
- VERIFICATION.md missing item 1 закрыт: `.signature-row .signature-name` разрешает перенос
  (min-width:0/white-space:normal/overflow-wrap:break-word) в ОБОИХ act_handover.html и
  act_acceptance.html, покрыто структурным CSS-гейтом.
- VERIFICATION.md missing item 2 закрыт: тест полного рендер-пайплайна с длинной вымышленной
  ФИО-фикстурой (automated evidence) + ручная печать на десктопе И в LAN-браузере для обоих
  актов (real render evidence, Task 3, gate=blocking).
- Ловушка db-backed-templates-upgrade-trap закрыта: срез `_legacy_defaults/v23/` доставляет фикс
  установленным копиям, доказано регрессионным тестом.
- Бэкенд не тронут (`crates/trackly-app/src/services/` без изменений).
- Полный автоматический сьют зелёный; пользователь подтвердил отсутствие обрезания ФИО визуально
  на обоих транспортах для обоих актов.
</success_criteria>

<output>
Create `.planning/phases/35-act-handover-body/35-07-SUMMARY.md` when done
</output>
