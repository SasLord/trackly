---
phase: 35-act-handover-body
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - crates/trackly-app/templates/act_acceptance.html
autonomous: true
requirements: [DOC-08]

must_haves:
  truths:
    - "act_acceptance.html приведён к тому же виду блока подписей, что act_handover.html: дублирующие строки «Кто передал»/«Кто принял» удалены из таблицы, ФИО остаются только в блоке подписей — D-09."
    - "Блок подписей act_acceptance.html — горизонтальный, одна строка на подписанта (Выдал: / Получил:), с полоской и напечатанным document.giver_name / document.receiver_name — D-06."
    - "Под каждой полоской подписи в act_acceptance.html остаётся только пояснение Подпись, без пояснения ФИО — D-07."
    - "В строке подписанта act_acceptance.html нет даты подписания — D-08."
  artifacts:
    - path: "crates/trackly-app/templates/act_acceptance.html"
      provides: "Дедуплицированная таблица + горизонтальный блок подписей (D-09/D-06/D-07)"
      contains: "document.giver_name"
  key_links:
    - from: "crates/trackly-app/templates/act_acceptance.html"
      to: "crates/trackly-app/src/services/act_service.rs"
      via: "MiniJinja-интерполяция document.giver_name/document.receiver_name — контекст render_acceptance_pdf уже содержит эти ключи, бэкенд не меняется"
      pattern: "document\\.giver_name"
---

<objective>
Привести `act_acceptance.html` к тому же виду блока подписей, что `act_handover.html` (План 02):
убрать дублирующие строки «Кто передал»/«Кто принял» из таблицы (D-09) и переверстать `.signature`
в горизонтальный блок с полоской, пояснением «Подпись» и напечатанным ФИО (D-06/D-07/D-08).

Purpose: DOC-08 требует одинакового горизонтального вида блока подписей во всех документах,
использующих этот паттерн; CONTEXT.md D-09 явно распространяет решения D-06/D-07 на акт приёмки.
Output: `act_acceptance.html` без дублирования ФИО в таблице, с новым `.signature-row`-блоком.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-act-handover-body/35-CONTEXT.md
@.planning/phases/35-act-handover-body/35-RESEARCH.md
@.planning/phases/35-act-handover-body/35-PATTERNS.md
</context>

<interfaces>
Целевая структура блока подписей — идентична act_handover.html (План 02), класс-имена совпадают
для консистентности между обоими шаблонами (не переименовывать):

Контейнер div.signatures, внутри два div.signature-row (по одному на "Выдал:"/"Получил:"), каждый
содержит span.signature-label (текст "Выдал:" / "Получил:"), span.signature-field (обёртка вокруг
span.signature-line — пустая полоска с border-bottom — и span.signature-sublabel с текстом
"Подпись"), span.signature-name (document.giver_name / document.receiver_name).

CSS-правила (идентичны act_handover.html Task 1): .signatures { margin-top: 30pt;
page-break-inside: avoid; } .signature-row { display:flex; align-items:flex-end; gap:10pt;
margin:16pt 0 0 0; } .signature-row .signature-label { white-space:nowrap; } .signature-field {
display:inline-flex; flex-direction:column; align-items:center; flex:0 0 160pt; } .signature-field
.signature-line { display:block; width:100%; border-bottom:1px solid #000; min-height:1.2em; }
.signature-field .signature-sublabel { font-size:8pt; margin-top:2pt; } .signature-row
.signature-name { white-space:nowrap; }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Удалить дублирующие строки ФИО из таблицы</name>
  <files>crates/trackly-app/templates/act_acceptance.html</files>
  <read_first>
    crates/trackly-app/templates/act_acceptance.html (текущий HEAD, целиком)
    .planning/phases/35-act-handover-body/35-PATTERNS.md (секция act_acceptance.html — таблица с дублем ФИО)
  </read_first>
  <action>
    В table.kv (текущий HEAD, строки 86-90) удалить две строки tr: "Кто передал" (со значением
    {{ document.giver_name }}) и "Кто принял" (со значением {{ document.receiver_name }}) — D-09.
    Строку tr "Дата" ({{ document.date_human }}) оставить без изменений. Обновить doc-комментарий
    (строки 11-15, текущий HEAD): фраза "-> heading -> date/giver/receiver key-value rows -> device
    key-value rows -> single signature block" заменяется на описание, где ключевая строка таблицы —
    только дата, а giver_name/receiver_name выводятся исключительно в горизонтальном блоке подписей
    (реализуется в Task 2 этого плана). Список ключей в перечне context строк 13-15 не меняется —
    document.giver_name/receiver_name остаются в контексте, просто описание МЕСТА их вывода
    обновляется.
  </action>
  <verify>
    <automated>grep -c "Кто передал\|Кто принял" crates/trackly-app/templates/act_acceptance.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Кто передал\|Кто принял" в файле выводит 0 (строки таблицы удалены)
    - grep -c "document.date_human" в файле выводит не менее 1 (строка "Дата" осталась)
    - grep -c "document.giver_name" в файле выводит ровно 1 (единственное оставшееся вхождение — в блоке подписей, ещё не тронутом в этой задаче; итоговая проверка после Task 2)
  </acceptance_criteria>
  <done>Таблица содержит только строку "Дата"; строки "Кто передал"/"Кто принял" удалены.</done>
</task>

<task type="auto">
  <name>Task 2: Горизонтальный блок подписей (D-06/D-07/D-08)</name>
  <files>crates/trackly-app/templates/act_acceptance.html</files>
  <read_first>
    crates/trackly-app/templates/act_acceptance.html (после Task 1 — читать актуальное состояние файла)
    .planning/phases/35-act-handover-body/35-02-PLAN.md (Task 1 и Task 3 — идентичные CSS-правила и разметка для консистентности между шаблонами)
    crates/trackly-app/src/services/act_service.rs (render_acceptance_pdf — подтверждение: document.giver_name/receiver_name уже в контексте)
  </read_first>
  <action>
    Заменить CSS .signature/.signature .line (текущий HEAD, строки 71-77 — margin:10pt 0 на .line,
    текст вписан прямо в div.line) на классы .signatures/.signature-row/.signature-row
    .signature-label/.signature-field/.signature-field .signature-line/.signature-field
    .signature-sublabel/.signature-row .signature-name — точные правила см. в блоке interfaces этого
    файла (идентичны act_handover.html). Заменить markup div.signature (текущий HEAD, строки 103-106
    — два div.line с текстом "Кто передал: {{ document.giver_name }}" / "Кто принял:
    {{ document.receiver_name }}") на div.signatures с двумя div.signature-row: первая — "Выдал:" +
    пустая полоска с пояснением "Подпись" + {{ document.giver_name }}; вторая — "Получил:" + пустая
    полоска с пояснением "Подпись" + {{ document.receiver_name }}. Никакого текста "ФИО" как
    отдельного пояснения не добавлять (D-07); даты в строке нет (D-08). Интерполяция без | safe
    (тот же паттерн T-16-01, что уже используется в этом файле для device.name и других полей).
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_act_render html_acceptance_contains_required_blocks -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - html_acceptance_contains_required_blocks остаётся зелёным (giver/receiver имена по-прежнему присутствуют в HTML, теперь только в блоке подписей)
    - grep -c "document.giver_name" в файле выводит ровно 1 (единственное оставшееся вхождение — в блоке подписей)
    - grep -c "document.receiver_name" в файле выводит ровно 1
    - grep -c "signature-row" в файле выводит 2 (по одному на подписанта)
    - grep -c "ФИО" в файле выводит 0
    - grep -c "border-bottom" в файле выводит 1 (только .signature-field .signature-line — в act_acceptance.html нет аналога «Сроком до»/value-blank)
  </acceptance_criteria>
  <done>Блок подписей — два горизонтальных div.signature-row с полоской, "Подпись" и напечатанным ФИО; giver_name/receiver_name встречаются в файле ровно по одному разу каждый.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document.giver_name/document.receiver_name (значения из БД) → HTML-шаблон | Новое МЕСТО интерполяции уже существующего проверенного sink'а (T-16-01) — не новый sink, значения не меняются, только markup вокруг них |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering (stored XSS) | `{{ document.giver_name }}` / `{{ document.receiver_name }}` в переверстанном блоке подписей | mitigate | Обычная `{{ var }}`-интерполяция под `AutoEscape::Html`; НЕ добавлять `\| safe` — проверяется Task 2 acceptance_criteria |
| T-35-06 | Information Disclosure | Дедупликация ФИО в таблице | accept | Удаление строк — чисто структурная правка, не меняет источник данных и не расширяет поверхность утечки |
</threat_model>

<verification>
После Task 2: TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_act_render html_acceptance_contains_required_blocks --test acts_e2e_smoke acceptance_pdf_render_smoke -- --test-threads=1 — оба зелёные (существующие ассерции присутствия ФИО остаются истинными после переезда из таблицы в блок подписей).
</verification>

<success_criteria>
- act_acceptance.html не содержит строк "Кто передал"/"Кто принял" в таблице.
- Блок подписей — горизонтальный, две строки, giver_name/receiver_name встречаются ровно по одному разу каждый во всём файле.
- html_acceptance_contains_required_blocks и acceptance_pdf_render_smoke остаются зелёными без изменений в этих тестах.
</success_criteria>

<output>
Create `.planning/phases/35-act-handover-body/35-03-SUMMARY.md` when done
</output>
