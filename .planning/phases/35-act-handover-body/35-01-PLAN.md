---
phase: 35-act-handover-body
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html
  - crates/trackly-app/templates/_legacy_defaults/v22/act_acceptance.html
  - crates/trackly-app/src/pdf/html_templates.rs
  - crates/trackly-app/src/services/template_service.rs
autonomous: true
requirements: [DOC-08]

must_haves:
  truths:
    - "Срез `_legacy_defaults/v22/` содержит `act_handover.html` и `act_acceptance.html` байт-в-байт в состоянии ДО правок этой фазы (текущий HEAD, пост-Фаза-34) — C-01."
    - "`act_acceptance.html` включён в срез v22 наравне с `act_handover.html`, потому что D-09 меняет и его — C-04."
    - "`KNOWN_LEGACY_DEFAULTS` содержит новый элемент `v22` в обоих слайсах (`act_handover.html`, `act_acceptance.html`), поэтому установленные копии Trackly получат новое тело акта через `upgrade_untouched_defaults_on_startup`, а не останутся на старом (регрессия класса Фазы 34 D-15)."
    - "`demo_context_for_kind`'s ветка `_` (act_handover) содержит `act.giver_name`, поэтому предпросмотр редактора шаблонов (`Settings → Шаблоны`) не упадёт под `UndefinedBehavior::Strict`, как только `act_handover.html` начнёт читать этот ключ в Плане 02."
  artifacts:
    - path: "crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html"
      provides: "Снимок тела акта приёма-передачи ДО правок Фазы 35"
    - path: "crates/trackly-app/templates/_legacy_defaults/v22/act_acceptance.html"
      provides: "Снимок акта приёмки ДО правок Фазы 35"
    - path: "crates/trackly-app/src/pdf/html_templates.rs"
      provides: "Реестр `KNOWN_LEGACY_DEFAULTS` с новым элементом v22 в обоих слайсах"
      contains: "_legacy_defaults/v22"
    - path: "crates/trackly-app/src/services/template_service.rs"
      provides: "`demo_context_for_kind`'s ветка `_` содержит `act.giver_name`"
      contains: "giver_name"
  key_links:
    - from: "crates/trackly-app/src/pdf/html_templates.rs"
      to: "crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html"
      via: "include_str! в KNOWN_LEGACY_DEFAULTS"
      pattern: "_legacy_defaults/v22/act_handover\\.html"
    - from: "crates/trackly-app/src/services/template_service.rs"
      to: "crates/trackly-app/src/services/act_service.rs"
      via: "demo_context_for_kind должен зеркалить набор ключей act_service::render_pdf's ctx под UndefinedBehavior::Strict"
      pattern: "\"giver_name\""
---

<objective>
Подготовить инфраструктуру доставки и предпросмотра ДО правки тела шаблонов актов: снять
байт-в-байт снимок текущего (пост-Фаза-34) состояния `act_handover.html`/`act_acceptance.html` в
новый срез `_legacy_defaults/v22/`, зарегистрировать его в `KNOWN_LEGACY_DEFAULTS`, и добавить
`act.giver_name` в демо-контекст живого редактора шаблонов.

Purpose: без снимка «до» установленные копии Trackly не получат новое тело акта после апгрейда
(регрессия того же класса, что билась в Фазе 34 D-15, quick `260704-uw3`); без правки демо-контекста
живой редактор шаблонов (Settings → Шаблоны) упадёт под `UndefinedBehavior::Strict`, как только
`act_handover.html` начнёт использовать `act.giver_name` в Плане 02 — этот план идёт ПЕРВЫМ, чтобы
оба факта были верны ДО того, как тела шаблонов изменятся.
Output: новый срез `_legacy_defaults/v22/{act_handover,act_acceptance}.html`, обновлённый
`KNOWN_LEGACY_DEFAULTS`, обновлённая ветка `_` в `demo_context_for_kind`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-act-handover-body/35-CONTEXT.md
@.planning/phases/35-act-handover-body/35-RESEARCH.md
@.planning/phases/35-act-handover-body/35-PATTERNS.md
</context>

<interfaces>
Текущее состояние `KNOWN_LEGACY_DEFAULTS` (`crates/trackly-app/src/pdf/html_templates.rs:75-103`,
целевая точка правки Task 1): два релевантных слайса `"act_handover.html"` и `"act_acceptance.html"`,
каждый — массив из двух `include_str!` (`v20`, `v21`). Задача добавить третий элемент `v22` в конец
каждого из этих двух слайсов; слайсы `"report.html"` и `"_header.html"` не трогать.

Текущее состояние `demo_context_for_kind`'s ветка `_` (`template_service.rs:504-529`, целевая точка
правки Task 2): объект `"act"` содержит `"receiver_name": "Петров П.П."`, но НЕ содержит
`"giver_name"`. Соседняя ветка `"act_acceptance"` (`template_service.rs:467-481`) уже содержит
`"document": { "giver_name": "Иванов И.И.", "receiver_name": "Петров П.П.", ... }` — тот же
вымышленный формат ФИО переносится буквально в ветку `_`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Снять срез `_legacy_defaults/v22/` и зарегистрировать его в KNOWN_LEGACY_DEFAULTS</name>
  <files>crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html, crates/trackly-app/templates/_legacy_defaults/v22/act_acceptance.html, crates/trackly-app/src/pdf/html_templates.rs</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (текущий HEAD — источник снимка)
    crates/trackly-app/templates/act_acceptance.html (текущий HEAD — источник снимка)
    crates/trackly-app/src/pdf/html_templates.rs:46-103 (KNOWN_LEGACY_DEFAULTS + doc-комментарий с инструкцией по срезу)
    crates/trackly-app/templates/_legacy_defaults/v21/act_handover.html (образец оформления предыдущего среза)
  </read_first>
  <action>
    Создать каталог `crates/trackly-app/templates/_legacy_defaults/v22/` и скопировать в него текущий
    (пост-Фаза-34, ДО правок этой фазы) байт-в-байт контент `crates/trackly-app/templates/act_handover.html`
    и `crates/trackly-app/templates/act_acceptance.html` командой `cp` (не переписывать руками —
    байтовое сравнение в `upgrade_untouched_defaults_on_startup` чувствительно к любому расхождению,
    включая пробелы/переносы строк). `report.html` и `_header.html` в `v22/` НЕ копировать — этой фазой
    не меняются, слайс v22 содержит только эти два файла (расхождение с v20/v21, которые содержат все
    три файла, — ожидаемо и допустимо, см. PATTERNS.md). Затем в
    `crates/trackly-app/src/pdf/html_templates.rs` добавить в `KNOWN_LEGACY_DEFAULTS` (строки 75-103,
    текущий HEAD) третий `include_str!("../../templates/_legacy_defaults/v22/act_handover.html")`
    элементом в конец слайса `"act_handover.html"` (после `v21`), и аналогично третий
    `include_str!("../../templates/_legacy_defaults/v22/act_acceptance.html")` в конец слайса
    `"act_acceptance.html"`. Слайсы `"report.html"` и `"_header.html"` НЕ менять.
  </action>
  <verify>
    <automated>diff -q crates/trackly-app/templates/act_handover.html crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html && diff -q crates/trackly-app/templates/act_acceptance.html crates/trackly-app/templates/_legacy_defaults/v22/act_acceptance.html && grep -c "_legacy_defaults/v22" crates/trackly-app/src/pdf/html_templates.rs</automated>
  </verify>
  <acceptance_criteria>
    - `diff -q` для обоих файлов не выводит ничего (снимок байт-в-байт идентичен текущему HEAD)
    - `grep -c "_legacy_defaults/v22" crates/trackly-app/src/pdf/html_templates.rs` выводит `2` (по одному `include_str!` на слайс `act_handover.html`/`act_acceptance.html`)
    - визуально после grep подтверждено, что затронутые строки принадлежат только слайсам `act_handover.html`/`act_acceptance.html` — слайсы `report.html`/`_header.html` не упоминают `v22`
    - `cargo test -p trackly-app --lib every_default_template_has_a_known_legacy_defaults_entry` зелёный (существующий WR-06 гейт, не должен сломаться от добавления элементов)
  </acceptance_criteria>
  <done>Каталог `_legacy_defaults/v22/` существует с двумя файлами, байт-в-байт равными текущему HEAD; оба слайса `KNOWN_LEGACY_DEFAULTS` содержат третий элемент `v22`.</done>
</task>

<task type="auto">
  <name>Task 2: Добавить `act.giver_name` в демо-контекст предпросмотра шаблонов</name>
  <files>crates/trackly-app/src/services/template_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/template_service.rs:434-530 (demo_context_for_kind целиком, обе релевантные ветки)
    crates/trackly-app/src/services/act_service.rs:2629-2661 (боевой контекст render_pdf — источник истины для набора ключей `act.*`)
  </read_first>
  <action>
    В `crates/trackly-app/src/services/template_service.rs`, функция `demo_context_for_kind`, ветка
    `_` (текущий HEAD, строки 504-529, объект `"act"`) — добавить ключ `"giver_name": "Иванов И.И."`
    рядом с существующим `"receiver_name": "Петров П.П."` (та же вымышленная форма ФИО, что уже
    используется в соседней ветке `"act_acceptance"`'s `document.giver_name`, строка 477). Не менять
    никакие другие ключи ветки `_` и не трогать ветку `"act_acceptance"` (она уже содержит
    `giver_name`) — это единственная правка, синхронизирующая демо-контекст с боевым контекстом
    `act_service::render_pdf`, где `act.giver_name` уже присутствует (act_service.rs:2639).
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib validate_preview_act_handover_returns_html_with_title_marker -- --test-threads=1 && grep -c '"giver_name": "Иванов И.И."' crates/trackly-app/src/services/template_service.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"giver_name": "Иванов И.И."' crates/trackly-app/src/services/template_service.rs` выводит `2` (одно вхождение в ветке `"act_acceptance"`, одно новое в ветке `_`)
    - `cargo test -p trackly-app --lib validate_preview_act_handover_returns_html_with_title_marker` зелёный (регрессия не внесена; этот тест станет прямым гейтом против Pitfall 1, как только `act_handover.html` начнёт читать `act.giver_name` в Плане 02)
    - ветка `"act_acceptance"` (строки 467-481) не изменена
  </acceptance_criteria>
  <done>`demo_context_for_kind`'s ветка `_` содержит `"giver_name": "Иванов И.И."`; существующий unit-тест предпросмотра остаётся зелёным.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust-код фазы → публичный git-репозиторий | Любой литерал, добавленный в `demo_context_for_kind`/`KNOWN_LEGACY_DEFAULTS`, попадает в историю git навсегда — реальные ФИО/реквизиты недопустимы |
| Файловый снимок `_legacy_defaults/v22/` → бинарь (compile-time `include_str!`) | Снимок должен быть байт-в-байт равен текущему HEAD; расхождение (случайная правка при копировании) сломает структурное сравнение в `upgrade_untouched_defaults_on_startup`, не угроза безопасности, но угроза корректности доставки |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information Disclosure | `demo_context_for_kind` (новый литерал `"giver_name"`) | mitigate | Использовать только уже утверждённое вымышленное ФИО «Иванов И.И.» (существующий прецедент в соседней ветке), не вводить новое имя — C-07 |
| T-35-02 | Tampering | `_legacy_defaults/v22/*.html` (новые файлы) | accept | Снимок делается механическим `cp` без ручного редактирования контента — риск внесения постороннего кода в снимок отсутствует по построению задачи |
</threat_model>

<verification>
После обеих задач: `cargo test -p trackly-app --lib every_default_template_has_a_known_legacy_defaults_entry validate_preview_act_handover_returns_html_with_title_marker -- --test-threads=1` — оба теста зелёные. Полный сьют (`TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1`) откладывается до финальной проверки после всех волн (Plan 05) — на этом этапе он ещё не отражает правки тела шаблонов.
</verification>

<success_criteria>
- Срез `_legacy_defaults/v22/` существует и байт-в-байт равен телам `act_handover.html`/`act_acceptance.html` ДО правок этой фазы.
- `KNOWN_LEGACY_DEFAULTS` регистрирует v22 в обоих релевантных слайсах.
- `demo_context_for_kind`'s ветка `_` содержит `act.giver_name`, синхронизировав демо-контекст с боевым контекстом заранее.
</success_criteria>

<output>
Create `.planning/phases/35-act-handover-body/35-01-SUMMARY.md` when done
</output>
