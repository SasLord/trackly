---
phase: 35-act-handover-body
plan: 06
type: execute
wave: 5
depends_on: ["35-05"]
files_modified:
  - crates/trackly-app/templates/act_handover.html
  - crates/trackly-app/tests/pdf_render_act.rs
  - crates/trackly-app/src/pdf/html_templates.rs
  - crates/trackly-app/tests/html_act_render.rs
  - crates/trackly-app/tests/html_field_row_underline_gate.rs
autonomous: false
requirements: [DOC-07, DOC-08, DOC-09]

must_haves:
  truths:
    - "Roadmap Success Criterion #2 (согласованный текст сверен с каноном Word-образца, изменения не ломают то, ради чего делались Фазы 15/16) восстановлено: для N>1 каждый .device-block в act_handover.html содержит собственное имя устройства — CR-01 закрыт."
    - "D-02a (поправка к D-02, 2026-08-11): гейт {%- if act.items | length == 1 %} вокруг строки «было получено устройство: {{ item.name }}» снят полностью; строка печатается внутри каждого .device-block независимо от количества устройств. D-02 остаётся в силе для формулировки текста (не переоткрыт) — только его следствие «без повтора метки на каждое устройство» отменено."
    - "Верхний перечень «были получены устройства:» + <ul> при N>1 сохранён как сводка (D-02a — оба блока приняты осознанно, избыточность не устраняется)."
    - "Новый тест render_handover_multi_device_fields_attributable_to_own_device в tests/pdf_render_act.rs подтверждает для N=3 устройств с разным набором опциональных полей (включая устройство без единого опционального поля), что каждый .device-block содержит только имя своего устройства и свои значения полей, и что тест провалился бы против гейтированного шаблона до этого плана — закрывает VERIFICATION.md missing item 2."
    - "WR-01 закрыт: новый тест upgrade_replaces_v22_legacy_default_with_current_bundled_body в html_templates.rs (по образцу v21-версии, index bodies.get(2)) доказывает, что срез v22 отличается от текущего бандла и реально драйвит upgrade_untouched_defaults_on_startup для установленных копий на пред-Фазой-35 теле."
    - "WR-02 закрыт: html_act_render.rs::html_handover_contains_required_blocks_and_logo проверяет метки «Выдал:»/«Получил:» с двоеточием (не «Выдал»/«Получил» без него), которые не коллидируют с ФИО фикстуры «Выдалов В.В.»/«Получилов П.П.» как префиксом."
    - "IN-01 закрыт: html_field_row_underline_gate.rs содержит эквивалентный структурный гейт для act_acceptance.html — ровно один легитимный border-bottom во всём <style>-блоке, принадлежащий правилу .signature-field .signature-line."
    - "DOC-07/DOC-08/DOC-09 переподтверждены: полный TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1 --skip login_remember_persistent_cookie зелёный после правок G-01..G-04, включая уже существующий гейт field_row_css_has_no_border_bottom_and_only_two_legit_exceptions_remain (не ослаблен)."
    - "Рендер настоящего предпросмотра приложения (десктоп + LAN-браузер, RESEARCH.md Pitfall 5: для HTML+Paged.js-пайплайна с Фазы 16 это единственный путь проверки геометрии, не Rust qlmanage-тест) подтверждает per-block атрибуцию имени устройства для акта на 3 устройства с разным набором полей — human checkpoint, gate=blocking."
  artifacts:
    - path: "crates/trackly-app/templates/act_handover.html"
      provides: "Безусловная строка «было получено устройство: {{ item.name }}» внутри каждого .device-block (D-02a/CR-01 fix), верхний <ul>-перечень при N>1 сохранён"
    - path: "crates/trackly-app/tests/pdf_render_act.rs"
      provides: "Новый регрессионный тест render_handover_multi_device_fields_attributable_to_own_device (co-location device name ↔ its own field block, N=3, differing field counts)"
    - path: "crates/trackly-app/src/pdf/html_templates.rs"
      provides: "Новый тест upgrade_replaces_v22_legacy_default_with_current_bundled_body (WR-01) — v22-индексный сиблинг существующего v21-теста"
    - path: "crates/trackly-app/tests/html_act_render.rs"
      provides: "html_handover_contains_required_blocks_and_logo проверяет «Выдал:»/«Получил:» с двоеточием, не коллидирующие с ФИО фикстуры (WR-02)"
    - path: "crates/trackly-app/tests/html_field_row_underline_gate.rs"
      provides: "Новый тест acceptance_signature_line_css_has_exactly_one_legitimate_border_bottom для act_acceptance.html (IN-01), существующий handover-гейт не ослаблен"
  key_links:
    - from: "crates/trackly-app/tests/pdf_render_act.rs::render_handover_multi_device_fields_attributable_to_own_device"
      to: "crates/trackly-app/templates/act_handover.html"
      via: "render_pdf возвращает HTML; тест разбивает его по литеральному маркеру <div class=\"device-block\"> и проверяет co-location имени и полей внутри каждого блока"
      pattern: "html\\.split\\(\"<div class=\\\\\"device-block\\\\\">\"\\)"
    - from: "crates/trackly-app/src/pdf/html_templates.rs::upgrade_replaces_v22_legacy_default_with_current_bundled_body"
      to: "crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html"
      via: "bodies.get(2) индексирует третий элемент KNOWN_LEGACY_DEFAULTS-слайса, include_str! компилируется в бинарник"
      pattern: "bodies\\.get\\(2\\)"
    - from: "crates/trackly-app/tests/html_field_row_underline_gate.rs::acceptance_signature_line_css_has_exactly_one_legitimate_border_bottom"
      to: "crates/trackly-app/templates/act_acceptance.html"
      via: "include_str! (compile-time) читает файл целиком, extract_style_block + extract_rule_body извлекают тело CSS-правила по селектору"
      pattern: "include_str!\\(\"\\.\\./templates/act_acceptance\\.html\"\\)"
---

<objective>
Закрыть Фазу 35 GAP CLOSURE: четыре подтверждённых пробела из VERIFICATION.md (status
gaps_found, Success Criterion #2 = FAILED) и 35-REVIEW.md (CR-01 blocker, WR-01/WR-02 warnings,
IN-01 info) — все four уже подтверждены как открытые в рабочем дереве на момент планирования.
G-01 (CR-01/SC#2): для многоустройственных актов приёма-передачи каждый `.device-block` теряет
привязку к имени устройства — читатель напечатанного акта не может сопоставить Инвентарный/
Серийный номер с конкретным устройством. G-02 (WR-01): у нового среза `_legacy_defaults/v22/`
нет регрессионного теста, доказывающего реальный upgrade. G-03 (WR-02): тест
`html_handover_contains_required_blocks_and_logo` может пройти вхолостую — метки «Выдал»/
«Получил» без двоеточия совпадают как префикс с ФИО фикстуры. G-04 (IN-01): структурный гейт
DOC-07 покрывает только `act_handover.html`, у `act_acceptance.html` (та же D-09-переверстка
подписей) нет аналогичной защиты.

Purpose: замкнуть аудиторский след VERIFICATION.md → REVIEW.md → пользовательское решение D-02a
без переоткрытия D-01..D-12 или посягательства на область Фазы 36 (пагинация, «Приложение №1»).
Output: `act_handover.html` без анонимных device-block'ов при N>1; четыре новых/исправленных
регрессионных теста; полный `cargo test -p trackly-app` зелёный; ручное подтверждение per-block
атрибуции на обоих транспортах для акта на 3 устройства с разным набором полей.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-act-handover-body/35-CONTEXT.md
@.planning/phases/35-act-handover-body/35-VERIFICATION.md
@.planning/phases/35-act-handover-body/35-REVIEW.md
@.planning/phases/35-act-handover-body/35-RESEARCH.md
@.planning/phases/35-act-handover-body/35-VALIDATION.md
</context>

<interfaces>
**act_handover.html device-block loop, CURRENT state (post-35-02, the gap this plan closes) —
lines 140-164:**

`{%- for item in act.items %}` opens at line 140; `<div class="device-block">` at line 141; the
gated name line is lines 142-144 (`{%- if act.items | length == 1 %}` / the field-row / `{%-
endif %}`); the six optional `{%- if item.X %}...{%- endif %}` field-rows (inventory_no,
serial_no, model, kit, specs, condition) follow at lines 145-162 unconditionally per-item
already (they are NOT part of this gap — only the name line at 142-144 is gated). The top-level
N>1 summary (`были получены устройства:` + `<ul>`) is a SEPARATE block at lines 131-138, before
the loop — D-02a keeps it untouched.

**`_legacy_defaults/v22/act_handover.html` (the pre-Phase-35 canon this gap restores) — lines
123-128:** the equivalent name line has NO conditional wrapper at all — `{%- for item in
act.items %}` immediately followed by `<div class="device-block">` then unconditionally
`<div class="field-row"><span class="label">было получено устройство:</span><span
class="value">{{ item.name }}</span></div>`. This confirms D-02a's instruction ("restore the
pre-Phase-35 behaviour") is exactly "remove the `length == 1` wrapper", not a markup redesign —
the current post-35-02 markup already uses the plain-text `field-row` form (D-11), which stays.

**`html_templates.rs::upgrade_replaces_v21_legacy_default_with_current_bundled_body`
(lines 415-465, the exact pattern Task 2 mirrors):** locks the `ENV_GUARD` mutex, builds a
tempdir, iterates `DEFAULT_HTML_TEMPLATES`, looks up each filename's slice in
`KNOWN_LEGACY_DEFAULTS`, pulls `bodies.get(1)` (skipping via `continue` when absent — this is
how `_header.html`'s empty slice and any filename without a v21 element are safely skipped),
asserts `assert_ne!(v21_body, current, ...)` as a precondition guard, writes the v21 body to the
tempdir, calls `upgrade_untouched_defaults_on_startup(dir.path())`, then re-reads each file and
`assert_eq!` against `current` — gated by a `has_v21` check (`bodies.len() > 1`). Task 2's new
test is a structural sibling: same skeleton, `bodies.get(2)` instead of `bodies.get(1)`,
`has_v22` gated by `bodies.len() > 2`. For `report.html` (2-element slice: v20, v21) and
`_header.html` (empty slice), `bodies.get(2)` is `None` → `continue` → correctly skipped, so the
new test only exercises `act_handover.html`/`act_acceptance.html`, which is exactly right (only
those two got a v22 snapshot).

**`html_field_row_underline_gate.rs` helpers `extract_style_block`/`extract_rule_body`
(lines 27-49, read in full during Task 3's read_first):** both are free functions at module
top level, not methods — Task 3 reuses them as-is against a second constant, no duplication.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: G-01 — снять гейт length==1, добавить регрессионный тест атрибуции</name>
  <files>crates/trackly-app/templates/act_handover.html, crates/trackly-app/tests/pdf_render_act.rs</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (целиком, 195 строк — текущий HEAD, особенно строки 129-164)
    crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html строки 108-166 (канон, который восстанавливает D-02a)
    .planning/phases/35-act-handover-body/35-CONTEXT.md раздел D-02a (поправка, закрытие CR-01)
    crates/trackly-app/tests/pdf_render_act.rs строки 108-333 (seed_devices, create_handover_with_giver, render_handover_multi_device_wraps_long_fields, render_handover_default_template_uses_field_rows_not_device_card — идиомы UPDATE devices / UPDATE act_items и паттерн ассерций)
  </read_first>
  <action>
    В `crates/trackly-app/templates/act_handover.html`, внутри цикла `{%- for item in act.items
    %}` (текущая строка 140), удалить условную обёртку `{%- if act.items | length == 1 %}`
    (строка 142) и парную `{%- endif %}` (строка 144), которые гейтируют строку
    `<div class="field-row">было получено устройство: {{ item.name }}</div>` (строка 143) — по
    D-02a эта строка печатается безусловно внутри каждого `.device-block` при любом количестве
    устройств. Текст строки НЕ менять, нумерацию НЕ добавлять, верхний
    `{%- if act.items | length > 1 %}` перечень (текущие строки 131-138) НЕ трогать (D-02a
    сохраняет оба блока), шесть `{%- if item.X %}...{%- endif %}` блоков ниже (инвентарный/
    серийный/модель/комплектация/теххарактеристики/состояние, строки 145-162) НЕ трогать —
    они уже безусловны per-item и не входят в этот гэп. Doc-комментарий (строки 2-41) и CSS
    (строки 46-120) НЕ трогать — это markup-only правка одного условного блока.

    В `crates/trackly-app/tests/pdf_render_act.rs`, добавить новую функцию
    `render_handover_multi_device_fields_attributable_to_own_device` (разместить после
    `render_handover_multi_device_wraps_long_fields`, перед
    `render_handover_default_template_uses_field_rows_not_device_card`), в той же обёртке
    `tokio::time::timeout(Duration::from_secs(60), async { ... }).await.expect("timeout")`, что
    и все остальные тесты файла. Шаги: (1) `make_full_pipeline()`; (2) `seed_devices(&p.writer,
    3)` — даёт устройства «Ноутбук-0»/«Ноутбук-1»/«Ноутбук-2»; (3) `UPDATE devices SET
    inventory_number = 'ИНВ-АТРИБУЦИЯ-0' WHERE id = ?1` ТОЛЬКО для `device_ids[0]` (идиома
    `p.writer.execute` из `render_handover_default_template_uses_field_rows_not_device_card`);
    (4) `create_handover_with_giver(&p.acts, &device_ids, "Морозов М.М.")`; (5) `UPDATE act_items
    SET complectation_at_time = 'КОМПЛЕКТ-АТРИБУЦИЯ-1' WHERE act_id = ?1 AND device_id = ?2`
    ТОЛЬКО для `act.items[1].device_id` (идиома из `render_handover_multi_device_wraps_long_fields`'s
    `.iter().take(2)` loop, но здесь ровно один элемент, индекс 1) — устройство с индексом 2
    (`act.items[2]`) НЕ получает ни одного опционального поля, намеренно покрывая CR-01's «блок
    без единого опционального поля рендерится пустым»; (6) `p.acts.render_pdf(act.id).await`;
    (7) разбить `html` по литеральному маркеру `<div class="device-block">` через
    `.split("<div class=\"device-block\">").collect::<Vec<&str>>()`, `assert_eq!` длину ровно 4
    (индекс 0 — преамбула до первого device-block, индексы 1-3 — по одному на устройство в
    порядке `act.items`, то же допущение о сохранении порядка, что уже используется
    `render_handover_default_template_uses_field_rows_not_device_card`); (8) для каждого индекса
    i в 0..3 — `assert!` что `parts[i+1]` содержит `format!("Ноутбук-{i}")` и НЕ содержит имена
    двух других устройств; (9) `assert!` что `parts[1]` содержит `"ИНВ-АТРИБУЦИЯ-0"`, а
    `parts[2]`/`parts[3]` — нет; (10) `assert!` что `parts[2]` содержит
    `"КОМПЛЕКТ-АТРИБУЦИЯ-1"`, а `parts[1]`/`parts[3]` — нет; (11) `assert!` что `parts[3]`
    (устройство 2, без опциональных полей) всё равно содержит `"Ноутбук-2"` — доказывает, что
    дефект CR-01 «пустой блок = ноль идентифицируемого контента» исправлен. Каждое сообщение
    паники должно включать `html.chars().take(800).collect::<String>()` — как в остальных
    тестах файла. Тест ОБЯЗАН проваливаться против гейтированного (до этой правки) шаблона:
    при `length == 1`-гейте ни один из трёх блоков не содержал бы имени устройства вовсе — не
    реализовывать проверку так, чтобы она могла пройти вхолостую против старой разметки.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test pdf_render_act -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "act.items | length == 1" crates/trackly-app/templates/act_handover.html` возвращает 0
    - `grep -c "было получено устройство: {{ item.name }}" crates/trackly-app/templates/act_handover.html` возвращает 1 (строка сохранена, теперь безусловна)
    - `grep -c "были получены устройства:" crates/trackly-app/templates/act_handover.html` возвращает 1 (верхний перечень D-02a сохранён)
    - `git diff crates/trackly-app/templates/act_handover.html` затрагивает только строки в диапазоне 140-145 (без CSS/doc-comment правок)
    - `cargo test -p trackly-app --test pdf_render_act render_handover_multi_device_fields_attributable_to_own_device -- --test-threads=1` завершается с кодом 0
    - Все существующие тесты в pdf_render_act.rs остаются зелёными (полный прогон файла)
  </acceptance_criteria>
  <done>Гейт length==1 снят; имя устройства печатается в каждом .device-block независимо от количества устройств; верхний перечень при N>1 сохранён; новый тест доказывает co-location имени и полей и провалился бы против старой разметки.</done>
</task>

<task type="auto">
  <name>Task 2: G-02 — регрессионный гейт для среза v22 (WR-01)</name>
  <files>crates/trackly-app/src/pdf/html_templates.rs</files>
  <read_first>
    crates/trackly-app/src/pdf/html_templates.rs строки 75-105 (KNOWN_LEGACY_DEFAULTS — v22 уже зарегистрирован третьим элементом обоих слайсов) и строки 396-465 (every_default_template_has_a_known_legacy_defaults_entry + upgrade_replaces_v21_legacy_default_with_current_bundled_body — образец, который зеркалится)
    crates/trackly-app/templates/_legacy_defaults/v22/act_handover.html и .../v22/act_acceptance.html (подтвердить, что оба существуют и отличаются от текущего бандла после Task 1's правки)
  </read_first>
  <action>
    В `crates/trackly-app/src/pdf/html_templates.rs`, в тестовом модуле, сразу после
    `upgrade_replaces_v21_legacy_default_with_current_bundled_body` (заканчивается на текущей
    строке 465), добавить структурный сиблинг-тест
    `upgrade_replaces_v22_legacy_default_with_current_bundled_body`, дословно зеркалящий скелет
    v21-теста (тот же `ENV_GUARD.lock()`, тот же `tempfile::tempdir()`, тот же цикл по
    `DEFAULT_HTML_TEMPLATES`, тот же поиск слайса в `KNOWN_LEGACY_DEFAULTS`, тот же вызов
    `upgrade_untouched_defaults_on_startup(dir.path())`), но с тремя точечными заменами: (a)
    `bodies.get(1)` → `bodies.get(2)` при извлечении `v22_body` (для `report.html`
    двухэлементного слайса и пустого слайса `_header.html` это корректно даёт `None` → `continue`
    — тест автоматически ограничивается только `act_handover.html`/`act_acceptance.html`); (b)
    precondition guard `assert_ne!(v22_body, current, ...)` с сообщением, называющим v22 (тот же
    Pitfall-5-guard, что у v21-версии — защита от «срез снят ПОСЛЕ правки»); (c) финальная
    проверка гейтится через `bodies.len() > 2` (переименовать локальную переменную `has_v21` →
    `has_v22` внутри новой функции, не трогая одноимённую переменную в существующей v21-функции).
    Добавить короткий doc-комментарий над новой функцией: закрывает Phase 35 WR-01 — без этого
    теста ничего не доказывает, что v22-срез реально отличается от текущего бандла и реально
    драйвит upgrade для установленных копий на пред-Фазой-35 теле (тот же класс регрессии, что
    v21-тест предотвращает для v21). НЕ менять существующую v21-функцию, НЕ менять
    `every_default_template_has_a_known_legacy_defaults_entry`, НЕ менять регистрацию в
    `KNOWN_LEGACY_DEFAULTS` (уже корректна — она была добавлена в рамках уже отгруженного Плана
    01, это только тестовое покрытие).
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib pdf:: -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn upgrade_replaces_v22_legacy_default_with_current_bundled_body" crates/trackly-app/src/pdf/html_templates.rs` возвращает 1
    - `grep -c "bodies.get(2)" crates/trackly-app/src/pdf/html_templates.rs` возвращает не менее 1 (новая функция использует индекс 2)
    - `cargo test -p trackly-app --lib upgrade_replaces_v22_legacy_default_with_current_bundled_body -- --test-threads=1` завершается с кодом 0
    - `git diff crates/trackly-app/src/pdf/html_templates.rs` НЕ затрагивает тело существующей `upgrade_replaces_v21_legacy_default_with_current_bundled_body` (0 изменённых строк внутри неё)
    - `every_default_template_has_a_known_legacy_defaults_entry` остаётся зелёным
  </acceptance_criteria>
  <done>Новый v22-индексный тест доказывает, что срез отличается от текущего бандла и реально драйвит upgrade для act_handover.html и act_acceptance.html; существующий v21-тест не тронут.</done>
</task>

<task type="auto">
  <name>Task 3: G-03 + G-04 — точные ассерции меток подписи и структурный гейт для act_acceptance.html</name>
  <files>crates/trackly-app/tests/html_act_render.rs, crates/trackly-app/tests/html_field_row_underline_gate.rs</files>
  <read_first>
    crates/trackly-app/tests/html_act_render.rs строки 160-214 (html_handover_contains_required_blocks_and_logo целиком)
    crates/trackly-app/tests/html_field_row_underline_gate.rs (целиком, 87 строк — extract_style_block/extract_rule_body хелперы и существующий handover-тест, образец для нового теста)
    crates/trackly-app/templates/act_acceptance.html строки 36-106 (<style>-блок — ровно одно объявление border-bottom, строка 96, правило .signature-field .signature-line)
  </read_first>
  <action>
    В `crates/trackly-app/tests/html_act_render.rs`, в `html_handover_contains_required_blocks_and_logo`
    (текущие строки 188-194), заменить массив `["Акт приема-передачи", "Выдал", "Получил",
    "Подпись"]` на `["Акт приема-передачи", "Выдал:", "Получил:", "Подпись"]` — добавить
    двоеточие ТОЛЬКО к «Выдал»/«Получил» (единственные два элемента, коллидирующие как префикс с
    ФИО фикстуры «Выдалов В.В.»/«Получилов П.П.», заданными на строке 184 через
    `create_handover(&p.acts, &[device_id], "Выдалов В.В.", "Получилов П.П.")`); «Акт
    приема-передачи» и «Подпись» не коллидируют ни с чем и остаются без изменений. Отдельную
    ассерцию `assert!(html.contains("Выдалов В.В."))` (текущие строки ~198-202, уже точная) НЕ
    трогать.

    В `crates/trackly-app/tests/html_field_row_underline_gate.rs`, добавить новую константу
    `ACT_ACCEPTANCE_HTML: &str = include_str!("../templates/act_acceptance.html")` рядом с
    существующей `ACT_HANDOVER_HTML`, и новый тест
    `acceptance_signature_line_css_has_exactly_one_legitimate_border_bottom`, переиспользующий
    БЕЗ дублирования уже существующие функции `extract_style_block`/`extract_rule_body`: (1)
    `let style = extract_style_block(ACT_ACCEPTANCE_HTML);`; (2) `assert_eq!(style.matches(
    "border-bottom").count(), 1, ...)` — у `act_acceptance.html` нет `.value-blank`-эквивалента
    (таблица `table.kv`, не `.field-row`), поэтому легитимен ровно один источник; (3)
    `let signature_line_body = extract_rule_body(&style, ".signature-field .signature-line");
    assert!(signature_line_body.contains("border-bottom"), ...)` — подтверждает, что
    единственное вхождение принадлежит именно этому правилу, а не случайному совпадению. Дописать
    к doc-комментарию в шапке файла (строки 1-21) одно предложение о том, что файл теперь также
    покрывает `act_acceptance.html` — не переписывать существующее handover-ориентированное
    объяснение целиком. Существующий тест
    `field_row_css_has_no_border_bottom_and_only_two_legit_exceptions_remain` (act_handover.html)
    НЕ трогать.

    После обеих правок выполнить полный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p
    trackly-app -- --test-threads=1` (требует актуального `ui/dist` — выполнить `pnpm --dir ui
    build` заранее, если он ещё placeholder) как wave-boundary-гейт, подтверждающий, что G-01..
    G-04 совместно не сломали ничего в остальном сьюте.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1 --skip login_remember_persistent_cookie  # wave-boundary gate, не per-commit — см. VALIDATION.md §Sampling Rate. `--skip` обязателен: login_remember_persistent_cookie висит и с `-p trackly-app` (pre-existing, память `workspace-test-hangs-auth-remember-cookie`; в Планах 04/05 исполнитель уже дважды импровизировал этот обход). ОДИН `cargo test` за раз — параллельные прогоны дерутся за target/-lock и выглядят как многоминутный ханг.</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"Выдал:", "Получил:"' crates/trackly-app/tests/html_act_render.rs` возвращает 1 (либо эквивалентная форма массива с обоими двоеточиями на одной строке — подтвердить фактическую форматировку после rustfmt)
    - `crates/trackly-app/tests/html_field_row_underline_gate.rs`: `grep -c "ACT_ACCEPTANCE_HTML" ...` возвращает не менее 2 (объявление константы + использование в новом тесте)
    - `cargo test -p trackly-app --test html_act_render html_handover_contains_required_blocks_and_logo -- --test-threads=1` зелёный
    - `cargo test -p trackly-app --test html_field_row_underline_gate -- --test-threads=1` зелёный, включая оба теста (handover — не тронут, acceptance — новый)
    - Полный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1 --skip login_remember_persistent_cookie` зелёный (0 failed)
  </acceptance_criteria>
  <done>html_handover_contains_required_blocks_and_logo больше не может пройти вхолостую на ФИО-префиксе; act_acceptance.html получил структурный DOC-07-эквивалентный гейт; полный сьют зелёный.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Ручная UAT-проверка per-block атрибуции на обоих транспортах</name>
  <read_first>
    .planning/phases/35-act-handover-body/35-CONTEXT.md раздел D-02a
    .planning/phases/35-act-handover-body/35-05-PLAN.md Task 2 (образец формата UAT-чекпоинта этой же фазы)
    .planning/phases/35-act-handover-body/35-RESEARCH.md Pitfall 5 (почему для этого HTML+Paged.js-пайплайна проверка = живой предпросмотр приложения, а не Rust qlmanage-тест на PDF-байтах)
    .planning/phases/35-act-handover-body/35-VALIDATION.md раздел Manual-Only
  </read_first>
  <action>
    Перед запросом подтверждения у пользователя: выполнить `./scripts/check-privacy-requisites.sh`
    и вручную просмотреть diff всех пяти файлов, изменённых Задачами 1-3 этого плана, на предмет
    реальных ФИО/реквизитов — допустимы только уже утверждённые вымышленные значения из фазы
    («Иванов И.И.», «Петров П.П.», «Выдалов В.В.», «Получилов П.П.», «Отдалов О.О.», «Принялов
    П.П.», «Сидоров-Петроградский Иван Александрович», «Кузнецов К.К.», «Волков В.В.») плюс новые
    вымышленные значения, введённые этим планом («Морозов М.М.», «ИНВ-АТРИБУЦИЯ-0»,
    «КОМПЛЕКТ-АТРИБУЦИЯ-1»). Затем провести блокирующую человеческую проверку per-block атрибуции
    (закрытие CR-01/SC#2) на десктопе и в LAN-браузере по инструкции ниже; не авто-одобрять
    (gate=blocking).
  </action>
  <what-built>
    `act_handover.html` больше не анонимизирует device-block'и для акта на несколько устройств:
    строка «было получено устройство: ⟨имя⟩» теперь печатается в КАЖДОМ блоке независимо от
    количества устройств (было — только когда устройство ровно одно), верхний перечень «были
    получены устройства:» + список имён при N>1 сохранён как сводка. Плюс: новый регрессионный
    тест атрибуции полей к своему устройству, новый гейт доставки v22-среза в установленные
    копии, точные (не коллидирующие с ФИО) ассерции меток подписи, структурный гейт
    подчёркиваний для акта приёмки.
  </what-built>
  <how-to-verify>
    Десктоп: если `target/debug/templates/` ещё не удалён с прошлого раунда UAT этой фазы —
    удалить, запустить `cargo tauri dev`. Создать акт приёма-передачи на 3 устройства, где
    устройство A имеет заполненные ВСЕ опциональные поля (инвентарный номер, серийный номер,
    модель, комплектация, теххарактеристики, состояние), устройство B — только ОДНО опциональное
    поле, устройство C — НИ ОДНОГО опционального поля. Открыть предпросмотр — убедиться, что: (1)
    над списком блоков по-прежнему выводится сводная строка «были получены устройства:» со
    списком всех трёх имён; (2) каждый из трёх блоков ниже начинается со своей собственной строки
    «было получено устройство: ⟨имя⟩» — то есть по напечатанному документу однозначно видно, к
    какому устройству относится каждый Инвентарный/Серийный номер/Модель и т.д.; (3) блок
    устройства C (без опциональных полей) не выглядит пустым/потерянным — его имя присутствует,
    даже если под ним больше ничего нет. Открыть Settings → Шаблоны → `act_handover` →
    «Предпросмотр» без изменений — убедиться, что редактор не падает с ошибкой (демо-контекст
    синхронизирован).

    LAN-браузер: после `pnpm --dir ui build` (если ещё не выполнен в рамках этого раунда),
    запустить сервер-режим, открыть предпросмотр того же трёхустройственного акта в браузере из
    локальной сети — повторить проверки (1)-(3) выше.

    🔒 При описании результата НЕ указывать реальные ФИО/реквизиты — обезличивать («имя третьего
    устройства видно в его собственном блоке», а не реальные инвентарные данные, если вы
    случайно ввели что-то нефиктивное при ручном тестировании — используйте только вымышленные
    значения при создании тестового акта).
  </how-to-verify>
  <resume-signal>Опишите результат обезличенно: "approved" (per-block атрибуция подтверждена на обоих транспортах для акта на 3 устройства с разным набором полей, редактор шаблонов не падает) или опишите конкретные расхождения — без реальных ФИО/реквизитов.</resume-signal>
  <acceptance_criteria>
    - `./scripts/check-privacy-requisites.sh` завершается с кодом 0 перед запросом подтверждения
    - Ручной просмотр diff пяти изменённых файлов не находит строк, похожих на реальные ФИО/реквизиты, вне утверждённого списка (включая новые значения этого плана)
    - Ответ пользователя однозначно подтверждает per-block атрибуцию на ОБОИХ транспортах (десктоп + LAN-браузер) для акта на 3 устройства с разным набором опциональных полей, включая устройство без единого опционального поля
    - Ответ пользователя не содержит реальных ФИО/реквизитов
  </acceptance_criteria>
  <done>Privacy-грep чист; пользователь подтвердил визуально на обоих транспортах, что каждый device-block самоидентифицируется именем устройства независимо от количества устройств и набора заполненных полей.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MiniJinja render → печатный документ | `item.name`/`act.giver_name` интерполируются в HTML; G-01 расширяет МЕСТО, где `{{ item.name }}` печатается (теперь безусловно на каждый блок), но не вводит новый sink |
| Diff этого плана → публичный git-репозиторий | Последняя точка контроля перед коммитом — новые вымышленные значения («Морозов М.М.», «ИНВ-АТРИБУЦИЯ-0», «КОМПЛЕКТ-АТРИБУЦИЯ-1») должны остаться вымышленными |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-13 | Information Disclosure | `{{ item.name }}` теперь печатается безусловно в каждом `.device-block` (было условно) | accept | Тот же sink, что уже использовался в N=1-ветке до этого плана (Фаза 16 T-16-01, `AutoEscape::Html`) — G-01 убирает только Jinja-условие вокруг уже существующей безопасной интерполяции, не добавляет новую |
| T-35-14 | Tampering | Правка `act_handover.html` (Task 1) могла бы случайно добавить `\| safe` вокруг `item.name` | mitigate | Acceptance criteria Task 1 требуют `git diff` только в диапазоне строк 140-145 (markup-only, без CSS/doc-comment) — ревью диффа перед коммитом (Task 4) подтверждает отсутствие нового `\| safe` |
| T-35-SC | Information Disclosure | Diff всех пяти файлов этого плана → публичный git-репозиторий | mitigate | Ручной privacy-грep + `check-privacy-requisites.sh` + явная сверка со списком утверждённых вымышленных значений (Task 4) — blocking-проверка перед коммитом |
| T-35-15 | Repudiation | Ослабление тестовых ассерций (G-03: снятие «ФИО»-подобных строк, замена меток) | mitigate | WR-02-фикс СТРОЖЕ исходной ассерции (двоеточие исключает ложное совпадение с ФИО-префиксом), а не слабее — покрытие не уменьшается |

</threat_model>

<verification>
Полный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1
--skip login_remember_persistent_cookie`
зелёный (Task 3) + `./scripts/check-privacy-requisites.sh` зелёный + ручное подтверждение
пользователя (Task 4) на обоих транспортах для акта на 3 устройства с разным набором полей —
это финальный gate закрытия гэпов Фазы 35 перед повторным `/gsd-verify-work`.
</verification>

<success_criteria>
- G-01 (CR-01/SC#2): каждый `.device-block` в `act_handover.html` самоидентифицируется именем
  устройства независимо от количества устройств; верхний N>1-перечень сохранён.
- G-02 (WR-01): срез v22 имеет регрессионный тест, доказывающий реальный upgrade установленных
  копий.
- G-03 (WR-02): ассерции меток подписи в `html_act_render.rs` не могут пройти вхолостую на
  ФИО-префиксе.
- G-04 (IN-01): `act_acceptance.html` получил структурный DOC-07-эквивалентный гейт подчёркиваний.
- Полный автоматический сьют зелёный; пользователь подтвердил per-block атрибуцию визуально на
  обоих транспортах.
</success_criteria>

<output>
Create `.planning/phases/35-act-handover-body/35-06-SUMMARY.md` when done
</output>
