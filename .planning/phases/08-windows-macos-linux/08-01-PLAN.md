---
phase: 08-windows-macos-linux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/tauri.conf.json
  - README.md
  - README-portable.md
  - trackly.config.toml.example
autonomous: true
requirements:
  - BLD-04
  - BLD-05

must_haves:
  decisions: "Реализует решения CONTEXT.md D-04, D-06, D-07, D-08, D-09, D-10, D-14, D-16"
  truths:
    - "Bundle config позволяет tauri build выдавать NSIS/dmg/AppImage/deb артефакты"
    - "portable ZIP staging файлы присутствуют в корне репо и готовы для включения в ZIP"
    - "Корневой README.md на русском с инструкциями по каждой ОС существует"
  artifacts:
    - path: "crates/trackly-app/tauri.conf.json"
      provides: "Bundle configuration с active:true, полным списком иконок, macOS signingIdentity"
      contains: "\"active\": true"
    - path: "README.md"
      provides: "Корневой README на русском (BLD-05)"
      min_lines: 80
    - path: "README-portable.md"
      provides: "Краткая инструкция для portable ZIP"
    - path: "trackly.config.toml.example"
      provides: "Закомментированный шаблон конфигурации для portable ZIP"
  key_links:
    - from: "crates/trackly-app/tauri.conf.json"
      to: "crates/trackly-app/icons/"
      via: "bundle.icon array"
      pattern: "icons/icon.ico"
    - from: "release.yml (plan 02)"
      to: "README-portable.md"
      via: "Copy-Item в portable ZIP stage"
      pattern: "README-portable.md"
---

<objective>
Подготовить статические артефакты, необходимые для работы release-пайплайна: включить Tauri bundler (`bundle.active: true`), расширить `bundle.icon` на полный набор форматов, добавить `macOS.signingIdentity` для ad-hoc подписи, и создать файлы-сопутники для portable ZIP (README-portable.md, trackly.config.toml.example) плюс корневой README.md.

Purpose: После этого плана `tauri build` начинает выдавать дистрибутивы (NSIS, dmg, AppImage, deb); portable ZIP staging файлы готовы к упаковке в CI; README.md выполняет требование BLD-05.

Output: Обновлённый tauri.conf.json, README.md, README-portable.md, trackly.config.toml.example.
</objective>

<execution_context>
@/Users/madsas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madsas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-windows-macos-linux/08-CONTEXT.md
@.planning/phases/08-windows-macos-linux/08-RESEARCH.md
@.planning/phases/08-windows-macos-linux/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Включить Tauri bundler и расширить bundle.icon в tauri.conf.json</name>
  <files>crates/trackly-app/tauri.conf.json</files>
  <read_first>
    crates/trackly-app/tauri.conf.json — текущее состояние: bundle.active=false, bundle.icon=["icons/icon.png"], нет macOS секции.
    .planning/phases/08-windows-macos-linux/08-PATTERNS.md — §"crates/trackly-app/tauri.conf.json" — целевой bundle блок и подтверждённый список иконок.
    .planning/phases/08-windows-macos-linux/08-RESEARCH.md — §"tauri.conf.json — обновлённый bundle блок" и §"Pitfall 3: tauri.conf.json bundle.icon".
    .planning/phases/08-windows-macos-linux/08-CONTEXT.md — D-04 (ad-hoc, без Apple Developer ID), D-09 (без updater), D-14 (bundle.active true).
  </read_first>
  <action>
    Изменить только блок `"bundle"` в crates/trackly-app/tauri.conf.json (остальные поля productName, identifier, version, build, app, plugins — не трогать):

    - Установить `"active": true` (сейчас `false`) — per D-14.
    - Оставить `"targets": "all"` без изменений.
    - Заменить `"icon"` с текущего `["icons/icon.png"]` на полный список: `["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.icns", "icons/icon.ico"]`. Все эти файлы подтверждены в `crates/trackly-app/icons/` — per 08-PATTERNS.md.
    - Добавить вложенный объект `"macOS": { "signingIdentity": "-" }` — per D-04 (ad-hoc без Apple Developer ID).

    НЕ добавлять секцию updater — per D-09 (tauri-plugin-updater не используется в portable mode).
    НЕ менять `"version": "0.1.0"` — он будет подставляться из тега через jq в Plan 02.
    НЕ добавлять `webviewInstallMode` — per D-07 (системный Evergreen WebView2).
  </action>
  <verify>
    <automated>jq '.bundle.active, (.bundle.icon | length), .bundle.macOS.signingIdentity' crates/trackly-app/tauri.conf.json</automated>
  </verify>
  <done>
    jq возвращает: `true`, `5`, `"-"`. bundle.active=true, 5 иконок в массиве, signingIdentity="-".
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать portable ZIP staging файлы (README-portable.md + trackly.config.toml.example)</name>
  <files>README-portable.md, trackly.config.toml.example</files>
  <read_first>
    .planning/phases/08-windows-macos-linux/08-CONTEXT.md — D-08 (содержимое portable ZIP: trackly.exe + portable.txt + краткий README + закомментированный trackly.config.toml).
    .planning/phases/08-windows-macos-linux/08-PATTERNS.md — §"README-portable.md" и §"trackly.config.toml.example" — структура и пример содержимого.
    .planning/phases/01-foundation/01-CONTEXT.md (или STATE.md) — логика portable-маркера: наличие portable.txt ИЛИ trackly.config.toml рядом с .exe → portable mode через trackly_infra::paths.
  </read_first>
  <action>
    Создать два файла в корне репозитория (рядом с Cargo.toml):

    README-portable.md — краткая инструкция на русском (10-20 строк):
    - Что такое portable-режим и почему данные хранятся рядом с .exe.
    - Как запустить: распаковать ZIP в любую папку, запустить trackly.exe.
    - portable.txt (пустой файл) активирует portable-режим — не удалять.
    - Данные (trackly.db, логи) создаются рядом с trackly.exe.
    - Для указания другого пути к БД: переименовать trackly.config.toml.example в trackly.config.toml и раскомментировать поле db_path.
    - Требование: Windows 10 x64, WebView2 (обычно уже установлен на Windows 10).

    trackly.config.toml.example — закомментированный шаблон конфигурации на русском:
    - Заголовок: "Trackly — шаблон конфигурации portable-режима. Переименуйте в trackly.config.toml."
    - Секция [storage] с закомментированным db_path — пример: "D:\\trackly-data\\trackly.db".
    - Секция [server] с закомментированными port (8443) и bind ("0.0.0.0").
    - Все поля закомментированы через `#`. Сам файл НЕ является trackly.config.toml — это образец.

    Язык комментариев и текста: русский (per CLAUDE.md).
    НЕ создавать реальный trackly.config.toml — только .example.
  </action>
  <verify>
    <automated>ls -1 README-portable.md trackly.config.toml.example</automated>
  </verify>
  <done>
    Оба файла существуют. README-portable.md содержит "portable.txt". trackly.config.toml.example содержит закомментированный "db_path".
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать корневой README.md на русском (BLD-05)</name>
  <files>README.md</files>
  <read_first>
    .planning/phases/08-windows-macos-linux/08-CONTEXT.md — D-06 (обход SmartScreen/Gatekeeper), D-07 (WebView2 Evergreen), D-08 (portable ZIP содержимое), D-16 (полный README на русском).
    .planning/phases/08-windows-macos-linux/08-PATTERNS.md — §"README.md (корневой, новый)" — структура разделов.
    .planning/ROADMAP.md — §"Phase 8 Success Criteria" п.2 — что должен содержать README.
  </read_first>
  <action>
    Создать README.md в корне репозитория (рядом с Cargo.toml). Язык: русский (per CLAUDE.md). Структура:

    1. Заголовок + краткое описание: «Trackly — приложение для учёта техники, принтеров и картриджей».
    2. Раздел «Установка и запуск» с подразделами по ОС:
       - Windows 10 x64 — два варианта: NSIS-installer (файл Trackly_N.N.N_x64-setup.exe, запустить, следовать мастеру) и portable ZIP (trackly-vN.N.N-windows-x64-portable.zip, распаковать, запустить trackly.exe).
       - macOS aarch64 — .dmg: смонтировать, перетащить в Applications.
       - Linux x86_64 — .AppImage: `chmod +x *.AppImage && ./*.AppImage`; .deb: `sudo dpkg -i *.deb`.
    3. Раздел «Portable-режим (Windows)»: portable.txt активирует portable mode, данные рядом с .exe, настройка через trackly.config.toml.example.
    4. Раздел «Требования»: Windows — WebView2 (включён в Windows 10 по умолчанию через Evergreen). macOS — macOS 11+. Linux — GTK3, WebKitGTK.
    5. Раздел «Серверный режим»: включить в Настройки → Сеть, порт 8443 (HTTPS), self-signed сертификат генерируется при первом включении. Как добавить доверие self-signed сертификату в браузере LAN.
    6. Раздел «Предупреждения безопасности (SmartScreen / Gatekeeper)»: объяснение почему появляются (нет code-signing); Windows: правая кнопка → Свойства → Разблокировать / «Подробнее» → «Выполнить в любом случае»; macOS: Ctrl+клик → Открыть / Системные настройки → Конфиденциальность и безопасность → Всё равно открыть.
    7. Раздел «Проверка контрольных сумм»: для каждого артефакта есть SHA256SUMS; команды проверки (sha256sum -c / certutil -hashfile на Windows).

    НЕ добавлять маркеры эмодзи (per CLAUDE.md).
    НЕ писать инструкции по сборке из исходного кода — только запуск готовых артефактов.
  </action>
  <verify>
    <automated>grep -c "SmartScreen\|Gatekeeper\|WebView2\|portable\|серверный" README.md</automated>
  </verify>
  <done>
    README.md существует, grep возвращает >= 4 (присутствуют SmartScreen, Gatekeeper, WebView2, portable, серверный). Файл >= 80 строк.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| репо → пользователь | README.md и README-portable.md содержат инструкции; нет исполняемого кода |
| tauri.conf.json → tauri bundler | Конфиг определяет что подписывается (signingIdentity="-" = ad-hoc) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | tauri.conf.json bundle.active | mitigate | bundle.active:true является намеренным; updater не добавляется (D-09 — uploadUpdaterJson:false в Plan 02) — нет attack surface auto-update |
| T-08-01-02 | Information Disclosure | trackly.config.toml.example | accept | файл содержит только закомментированные поля-примеры, нет секретов; users invited to copy-edit it |
| T-08-01-03 | Repudiation | README.md SmartScreen/Gatekeeper обходы | accept | артефакты неподписаны (D-04); README честно объясняет риск; SHA256SUMS закрывает integrity (Plan 02) |
</threat_model>

<verification>
После выполнения всех задач плана:

1. `jq '.bundle.active, (.bundle.icon | length), .bundle.macOS.signingIdentity' crates/trackly-app/tauri.conf.json` — должно вернуть `true`, `5`, `"-"`.
2. `ls -1 README.md README-portable.md trackly.config.toml.example` — все три файла присутствуют.
3. `grep "SmartScreen\|Gatekeeper\|WebView2\|portable" README.md | wc -l` — >= 4 строк.
4. `grep "db_path" trackly.config.toml.example` — присутствует закомментированная строка.
5. `grep "portable.txt" README-portable.md` — упоминание маркера portable.txt.
</verification>

<success_criteria>
- bundle.active: true в tauri.conf.json (D-14)
- bundle.icon содержит 5 файлов: 32x32.png, 128x128.png, 128x128@2x.png, icon.icns, icon.ico (Pitfall 3 устранён)
- bundle.macOS.signingIdentity: "-" для ad-hoc подписи (D-04)
- README.md существует в корне, содержит инструкции по Windows/macOS/Linux, portable-режим, WebView2, серверный режим self-signed TLS, SmartScreen/Gatekeeper обходы (BLD-05)
- README-portable.md и trackly.config.toml.example существуют и готовы для включения в portable ZIP (D-08)
</success_criteria>

<output>
Create `.planning/phases/08-windows-macos-linux/08-01-SUMMARY.md` when done
</output>
