---
phase: 40-movement-history
plan: 15
type: execute
wave: 4
depends_on: ["40-10"]
files_modified:
  - ui/src/lib/components/MovementTimeline.svelte
  - ui/src/features/acts/ActsPage.svelte
  - ui/src/bindings.ts
autonomous: true
requirements: [HST-02]
must_haves:
  truths:
    - "Timeline row anatomy matches the canonical format from CONTEXT.md/UI-SPEC.md exactly: date — from → to · actor · reason, with the two place segments and the act number rendered as real focusable buttons, not spans or anchors (D-17, D-19)"
    - "Path segments show the server-shortened snapshot with the full stored path in a native title tooltip — no JS mirror of the shortening formula is introduced anywhere in this component (D-18, Don't Hand-Roll)"
    - "ActsPage now accepts a ?id= hash query exactly like Devices/Cartridges/Printers/Places already do, so the act-number link has somewhere real to navigate to (D-19, UI-SPEC discretion #6)"
  artifacts:
    - path: "ui/src/lib/components/MovementTimeline.svelte"
      provides: "Shared timeline row component consumed by PlaceEntityViewModal (40-16), CartridgeDetail and PrinterDetail (40-17)"
  key_links:
    - from: "ui/src/features/acts/ActsPage.svelte"
      to: "svelte-spa-router parseIdFromHash"
      via: "?id= query consumption, mirroring DevicesPage/CartridgesPage"
      pattern: "parseIdFromHash"
---

<objective>
Build the ONE shared `MovementTimeline.svelte` component (the "3 consumers, 1 formula" architecture
note from RESEARCH.md) that renders the canonical timeline row anatomy, and extend `ActsPage.svelte` to
accept the `?id=` hash query so the timeline's act-number link (D-19) has a real navigation target. This
plan builds the reusable pieces BEFORE Plans 40-16/40-17 wire them into the three actual consumer
screens, so those two plans are pure integration work.

Purpose: give Plans 40-16 (device/printer) and 40-17 (cartridge/printer) a single, already-correct
component to import — avoiding three independent, slowly-diverging copies of the same row markup.
Output: `MovementTimeline.svelte` (states: loading/empty/error/populated, per UI-SPEC), `ActsPage.svelte`
hash-query support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-UI-SPEC.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
Backend DTO consumed (Plan 40-10): `MovementEntryDto { id, entity_type, entity_id, from_place_id,
from_place_path, from_place_path_short, to_place_id, to_place_path, to_place_path_short, actor_display,
source, note, act_id, act_number, created_at_utc }` — generated into
`ui/src/bindings.ts` by the project's specta-export step (this plan's Task 1 should run/verify that
generation step, or hand-author the matching TS type if bindings.ts is not auto-regenerated as part of
this plan's build step — check the project's existing `pnpm` script for a bindings-generation command).

`.crumb` link CSS to copy-and-modify ONE property (`PlaceContents.svelte`, full block already read):
```scss
.crumb {
  padding: 0; background: none; border: none; font: inherit; color: inherit; cursor: pointer;
  &:hover { color: var(--tr-text-primary); text-decoration: underline; }
  &:focus-visible { outline: none; box-shadow: 0 0 0 3px var(--tr-focus-ring); border-radius: var(--tr-radius-xs); }
}
```
UI-SPEC's exact required change: replace `color: inherit` with `color: var(--tr-accent-text)` — nothing
else. Row format contract (UI-SPEC): `01.09 — Здание А / 2 эт. / 214 → Склад · Иванов И.И. · актом
№123`. Element rules: date via manual `padStart` (`CartridgeDetail.svelte`'s existing `formatDate`
helper, no `Intl`), from/to as real `<button type="button">` elements with `title=` carrying the full
stored path, arrow `→` plain text `--tr-text-secondary`, `·` middle-dot separator, actor plain text
`--tr-text-primary` never a link, reason composed client-side from the DTO's raw `source`/`note`/
`act_id`/`act_number` fields per: `source=='act'` → `«актом №{act_number}»` (act_number itself a
clickable button navigating to `#/acts?id={act_id}` via `await push(...)` then any close callback, D-19)
`source=='manual'` + empty note → `«вручную»`; `source=='manual'` + non-empty note → `«вручную ·
{note}»`; any other/unrecognized `source` → a safe fallback label, never a crash (Pitfall 6 applies to
the frontend too — do not let an unrecognized enum value throw).
`PlaceEntityViewModal.svelte`'s existing `handleGoTo` sequencing (`await push(target); onClose();`) is
the exact pattern this component's own navigation handlers must mirror when a `onNavigated`/`onClose`
callback prop is provided by the parent (Plans 40-16/40-17 wire this).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MovementTimeline.svelte shared component</name>
  <files>ui/src/lib/components/MovementTimeline.svelte, ui/src/bindings.ts</files>
  <read_first>
    ui/src/features/places/PlaceContents.svelte lines 221-229, 345-375 (`.crumb` markup + CSS block, the
    exact link style to clone-and-modify), ui/src/features/cartridges/CartridgeDetail.svelte (locate
    `formatDate` — the manual padStart date formatter to reuse, and the existing `.history-row`/
    `.history-empty` styling for row-height/empty-state visual parity per UI-SPEC's Spacing Scale table:
    `--row-height-dense` 32px), ui/src/features/places/PlaceEntityViewModal.svelte lines 128-135
    (`handleGoTo`'s await-push-then-close sequencing to mirror)
  </read_first>
  <behavior>
    - Given an array of `MovementEntryDto` with 3 entries, renders exactly 3 rows in the given order
      (server already returns newest-first, D-20 — component does NOT re-sort)
    - Given an empty array, renders the empty state: heading «Перемещений ещё не было», body «Место
      изменится — здесь появится запись: откуда, куда, когда, кем и почему.»
    - Given a row with `source: "manual"`, `note: null`, renders the reason text «вручную» (no trailing
      middle-dot, no empty note fragment)
    - Given a row with `source: "manual"`, `note: "уточнение"`, renders «вручную · уточнение»
    - Given a row with `source: "act"`, `act_id: 42`, `act_number: "123"`, renders «актом №123» where
      "123" is itself a `<button type="button">` — clicking it calls the component's own navigation
      handler (a prop callback, e.g. `onNavigateToAct?: (actId: number) => void`) rather than hardcoding
      a hash write inside this shared component (keeps the component reusable across the three different
      parent contexts, each of which may need slightly different close-then-navigate sequencing)
    - Given a row with an unrecognized `source` value (e.g. "garbage"), does not throw — renders a safe
      fallback reason string
    - The from/to place buttons carry a `title` attribute equal to the DTO's full, unshortened
      `from_place_path`/`to_place_path` snapshot string (Plan 40-10 ships both the full snapshot AND
      the shortened display string on the same DTO specifically for this purpose, D-17) — the visible
      button TEXT uses `from_place_path_short`/`to_place_path_short`, never the full string, and this
      component never re-derives or re-fetches either value client-side
  </behavior>
  <action>
    Create `ui/src/lib/components/MovementTimeline.svelte` accepting props `{ entries:
    MovementEntryDto[], loading: boolean, loadError: boolean, onNavigateToPlace?: (placeId: number) =>
    Promise<void> | void, onNavigateToAct?: (actId: number) => Promise<void> | void }`. Render, per row:
    date (formatted via a local copy of the manual `padStart` DD.MM.YYYY logic, OR import
    `formatDate` from `CartridgeDetail.svelte` if it is exported — check whether it is a
    module-level export or a local closure and adapt accordingly), an em-dash, the from-place button
    (`.crumb`-derived class with `color: var(--tr-accent-text)`, `title={entry.from_place_path}` (the
    full snapshot, per D-17), visible text `entry.from_place_path_short ?? entry.from_place_path`,
    calling `onNavigateToPlace?.(entry.from_place_id)` on click, literal `→`, the to-place button
    (same shape, using the `to_*` fields and `entry.to_place_id`), middle-dot, `actor_display` as plain text, middle-dot, the composed reason (per
    `<interfaces>`'s exact composition rules), with the act-number segment as its own button when
    `source=='act'`. Implement the loading/empty/error states exactly per UI-SPEC's States table (loading
    reuses the PARENT's spinner per D-29's "part of the same fetch" rule — this component itself does NOT
    render its own independent spinner; it only renders empty/error/populated based on props the parent
    already resolved). Do NOT implement any path-shortening logic in this component — it only displays
    already-shortened strings from the DTO (D-18, Don't Hand-Roll).
  </action>
  <acceptance_criteria>
    - `pnpm --dir ui svelte-check` passes with no new errors attributable to this file
    - `grep -c "shorten\|substring.*path\|split('/ ')" ui/src/lib/components/MovementTimeline.svelte`
      is 0 (confirms no JS mirror of the shortening formula was introduced)
    - Component renders all 6 behavior cases correctly when exercised via a Storybook-style showcase
      entry or an ad-hoc dev harness (mirror however this codebase already showcases new shared
      components, per Phase 39's `PlacePicker.svelte + showcase` precedent)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Component renders the canonical row format correctly for all documented cases, with zero client-side path-shortening logic.</done>
</task>

<task type="auto">
  <name>Task 2: ActsPage.svelte accepts ?id= hash query (D-19 discretion #6)</name>
  <files>ui/src/features/acts/ActsPage.svelte</files>
  <read_first>
    ui/src/features/devices/DevicesPage.svelte or ui/src/features/cartridges/CartridgesPage.svelte
    (whichever most simply demonstrates `parseIdFromHash` consumption — read the full pattern: how the
    hash query is parsed on mount/route-change, how the matching row/detail view is opened/highlighted),
    ui/src/features/acts/ActsPage.svelte (current state — confirm it genuinely has NO existing
    `parseIdFromHash` consumption before adding one)
  </read_first>
  <action>
    Add the same `parseIdFromHash`-based hash-query consumption already used by
    `DevicesPage.svelte`/`CartridgesPage.svelte`/`PrintersPage.svelte`/`PlacesPage.svelte` to
    `ActsPage.svelte`: on mount (and on relevant route-hash changes), parse an `?id=` query parameter and
    open/focus/scroll-to the matching act row or its detail view, mirroring whichever exact UX the sibling
    pages use (row highlight, or opening a detail modal — match the ACTS page's own existing detail-view
    mechanism, do not invent a new one).
  </action>
  <acceptance_criteria>
    - `grep -n "parseIdFromHash" ui/src/features/acts/ActsPage.svelte` returns at least one line
    - `pnpm --dir ui svelte-check` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Navigating to #/acts?id=N opens/focuses act N, matching the established sibling-page convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MovementTimeline props → rendered DOM | Server-provided strings only (actor_display, shortened paths) — no client-side construction of sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-29 | Tampering | A JS mirror of the path-shortening formula being introduced here, silently drifting from the Rust formula (the exact WR-03 bug class) | mitigate | Task 1's acceptance criteria explicitly greps for shortening-related string manipulation and requires zero matches; the component only displays server-shortened strings |
| T-40-30 | Information Disclosure | Unrecognized `source` enum value crashing the whole timeline render (frontend equivalent of Pitfall 6/IN-01) | mitigate | Explicit behavior case requires a safe fallback string for any unrecognized `source`, never a thrown error |
</threat_model>

<verification>
`pnpm --dir ui svelte-check` and `pnpm --dir ui lint` both green.
</verification>

<success_criteria>
`MovementTimeline.svelte` exists, correctly renders every documented row/state case with zero
client-side path-shortening logic; `ActsPage.svelte` accepts `?id=` hash navigation, ready for the
act-number link built by this component.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-15-SUMMARY.md` when done
</output>
