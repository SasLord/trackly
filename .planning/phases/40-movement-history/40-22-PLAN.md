---
phase: 40-movement-history
plan: 22
type: execute
wave: 2
depends_on: ["40-21"]
files_modified:
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/tests/cartridges_lifecycle.rs
autonomous: true
gap_closure: true
requirements: [HST-01]

must_haves:
  truths:
    - "Когда авто-возврат предыдущего картриджа при установке нового не получает явного previous_cartridge_place_id, картриджу подставляется его последнее известное складское место (из place_movements), а не NULL"
    - "Если у картриджа нет истории перемещений в складское место, поведение остаётся прежним (место очищается в NULL) — регрессии для картриджей без истории нет"
  artifacts:
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "last_known_storage_place_in_tx — поиск последнего known складского места картриджа по place_movements"
  key_links:
    - from: "crates/trackly-infra/src/repos/cartridges_sqlite.rs (auto-return branch, ~line 614)"
      to: "place_movements JOIN places WHERE is_storage=1"
      via: "last_known_storage_place_in_tx(tx, cartridge_id)"
      pattern: "last_known_storage_place_in_tx"
---

<objective>
Закрыть gap UAT-40 «return-to-stock-empty-place-field» (test 16,
`.planning/debug/return-to-stock-empty-place-field.md`) — реализовать решение пользователя:
АВТОПОДСТАНОВКА прежнего складского места картриджа из `place_movements`, когда поле «Место
(предыдущий картридж)» оставлено пустым при авто-возврате (установка нового картриджа в
принтер, где уже стоял другой). Молчаливое стирание места в NULL устраняется для картриджей,
у которых есть история.

Purpose: сегодня пустое поле пишет `place_id = NULL` безусловно (`cartridges_sqlite.rs:614`,
нет fallback в отличие от соседнего поля состояния заряда), и D-06 гейт молчит на переходе
место→NULL — операция выглядит «съевшей» перемещение. Пользователь ожидает автоматического
возврата на последнее известное складское место.
Output: `transition_in_tx`'s auto-return branch подставляет последнее складское место из
`place_movements`, когда `previous_cartridge_place_id` не передан явно; при отсутствии
истории — прежнее поведение (NULL) сохраняется.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/debug/return-to-stock-empty-place-field.md

@crates/trackly-infra/src/repos/cartridges_sqlite.rs
</context>

<interfaces>
Схема `places` (migrations/V037__places.sql:11): `is_storage INTEGER NOT NULL DEFAULT 0` —
множественный флаг, не единственное «место по умолчанию».

Схема `place_movements` (migrations/V040): `entity_type TEXT` ('device'|'cartridge'),
`entity_id INTEGER`, `to_place_id INTEGER NOT NULL REFERENCES places(id)`, `created_at_utc
INTEGER`. Индекс `idx_place_movements_entity ON place_movements(entity_type, entity_id,
created_at_utc DESC)` — уже покрывает запрос "последнее перемещение этой сущности" без
доп. индекса.

Ветка авто-возврата в `transition_in_tx` (`cartridges_sqlite.rs:605-740`, см. Plan 40-21
Task 2 для соседнего изменения в ЭТОЙ ЖЕ функции чуть выше по коду — не конфликтует,
разные строки): текущая строка 614 — `let resolved_place_id = *previous_cartridge_place_id;`
(прямое присваивание без fallback). Соседнее поле `resolved_state_id` (строка ~636) ИМЕЕТ
`unwrap_or_else` fallback — используй симметричный паттерн.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fallback последнего складского места в auto-return ветке</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs</files>
  <action>
    Добавь приватную функцию (или инстанс-метод `SqliteCartridgeRepository`)
    `last_known_storage_place_in_tx(tx: &Transaction, cartridge_id: i64) ->
    Result&lt;Option&lt;i64&gt;, AppError&gt;`, выполняющую: `SELECT pm.to_place_id FROM
    place_movements pm JOIN places p ON p.id = pm.to_place_id WHERE pm.entity_type =
    'cartridge' AND pm.entity_id = ?1 AND p.is_storage = 1 ORDER BY pm.created_at_utc DESC,
    pm.id DESC LIMIT 1` через `tx.query_row(...).optional()`.

    В ветке авто-возврата замени строку 614 (`let resolved_place_id =
    *previous_cartridge_place_id;`) на: если `previous_cartridge_place_id` — `Some(x)`,
    использовать его как есть (явный оверрайд оператора не трогаем — существующее
    поведение теста `install_auto_return_uses_previous_cartridge_overrides_when_present`
    не меняется); если `None`, вызвать `self.last_known_storage_place_in_tx(tx, prev_id)?`
    (обрати внимание: `prev_id` — id ВОЗВРАЩАЕМОГО картриджа, известен на этой строке —
    `previous.id` из `previous: Option<(i64, i64)>` чуть ниже; при необходимости передвинь
    вычисление `resolved_place_id` НИЖЕ строки, где `prev_id` уже извлечён из `previous`,
    сохранив остальную логику без изменений). Если fallback вернул `None` (нет истории
    складского перемещения у этого картриджа) — поведение остаётся прежним: `place_id`
    очищается в NULL, как сегодня.

    Обнови doc-комментарий над функцией/веткой (сейчас утверждает «kind-aware default»
    только для state) — добавь фразу о новом fallback для place, включая ссылку на
    решение пользователя (см. `.planning/phases/40-movement-history/40-CONTEXT.md`
    scope-раздел этого gap'а в 40-UAT.md), чтобы следующий читатель не принял это за
    случайную мутацию.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test cartridges_lifecycle -- --test-threads=1</automated>
  </verify>
  <done>Авто-возврат картриджа С историей складского перемещения без явного previous_cartridge_place_id получает последнее складское место, а не NULL; авто-возврат картриджа БЕЗ такой истории по-прежнему получает NULL.</done>
</task>

<task type="auto">
  <name>Task 2: Уточнить существующие тесты-контракты и добавить тест на новый fallback</name>
  <files>crates/trackly-app/tests/cartridges_lifecycle.rs</files>
  <action>
    Уточни (не меняя итоговый assert, только формулировку сообщения/doc-комментария) тест
    `install_auto_returns_previous_cartridge_in_same_printer` (строки ~734-799) и
    doc-comment теста `install_auto_return_falls_back_to_defaults_when_overrides_absent`
    (строки ~1114-1117): текущая формулировка звучит как безусловный контракт «place_id
    must be cleared to NULL» — замени на явное «...when the cartridge has no prior
    storage-place movement history (create_stock_cartridge seeds place_id=None and the
    printer in this test also has no place, so there is nothing for the new fallback to
    derive — see Plan 40-22)». Сам assert (`a_place_id, None`) НЕ меняется — в обоих этих
    тестах у картриджа A действительно нет истории складского места, значит поведение
    после фикса идентично прежнему.

    Добавь НОВЫЙ тест `install_auto_return_falls_back_to_last_known_storage_place`: засей
    реальное место через `seed_place` с `is_storage=1` (расширь `seed_place`
    сигнатурой/вторым хелпером — сейчас функция всегда пишет `is_storage=0`, добавь
    `seed_storage_place(svc, name)` рядом, аналогичный `seed_place`, но с `is_storage=1`),
    создай картридж A, выполни ПРЯМОЙ SQL `INSERT INTO place_movements` (entity_type=
    'cartridge', entity_id=A.id, from_place_id/to_place_id=storage_place_id и произвольное
    другое валидное место, source='manual', created_at_utc — раньше времени install) —
    имитируя, что A когда-то уже лежал на складе. Установи A в принтер (обычный Install,
    `previous_cartridge_place_id: None`), затем установи B в тот же принтер (auto-return A,
    снова `previous_cartridge_place_id: None`). Прочитай A: ожидай
    `a_place_id == Some(storage_place_id)`, а не `None`.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test cartridges_lifecycle -- --test-threads=1</automated>
  </verify>
  <done>Новый тест `install_auto_return_falls_back_to_last_known_storage_place` проходит; оба уточнённых старых теста остаются зелёными с обновлёнными сообщениями/doc-комментариями.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client → `cartridge_service::transition` (auto-return branch) | `previous_cartridge_place_id: None` от клиента теперь триггерит серверный lookup по БД (не по клиентскому вводу) — клиент не может подделать «последнее складское место» |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-22-01 | Tampering | `last_known_storage_place_in_tx` | mitigate | Запрос читает `place_movements`/`places` по `cartridge_id`, извлечённому сервером из уже провалидированного `previous.id` — никакого клиентского текста в SQL |
| T-40-22-02 | Information Disclosure | Fallback раскрывает прежнее место картриджа оператору | accept | Оператор уже имеет доступ Admin/Manager к `place_movements` (D-12 Фазы 40); данные не новые, только автоматизирован уже доступный лукап |
</threat_model>

<verification>
`cargo test -p trackly-app --test cartridges_lifecycle -- --test-threads=1` — все тесты зелёные, включая новый и два уточнённых.
`cargo clippy --workspace -- -D warnings` — без новых предупреждений.
</verification>

<success_criteria>
- Авто-возврат без явного места подставляет последнее складское место картриджа, когда оно выводимо из истории.
- Авто-возврат без явного места и без истории оставляет прежнее поведение (NULL) — не регрессия.
- Существующие тесты-контракты явно документируют edge case «нет истории», а не читаются как безусловное правило.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-22-SUMMARY.md` when done
</output>
