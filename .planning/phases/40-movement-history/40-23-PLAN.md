---
phase: 40-movement-history
plan: 23
type: execute
wave: 3
depends_on: ["40-21", "40-22"]
files_modified:
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: true
gap_closure: true
requirements: [HST-01, HST-02]

must_haves:
  truths:
    - "Установка картриджа в принтер без указания места клиентом не блокируется валидацией формы, когда выбран принтер (место резолвится сервером из принтера либо остаётся пустым, а не требуется под угрозой ошибки формы)"
    - "Оператор видит явное пояснение, что пустое поле «Место (предыдущий картридж)» приведёт к автоподстановке последнего складского места, а не к молчаливому стиранию"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "validate() больше не требует placeId для install, когда выбран принтер (effectivePrinterId !== undefined); подсказка у previousCartridgePlaceId"
  key_links:
    - from: "ui/src/features/cartridges/OperationModal.svelte::validate"
      to: "effectivePrinterId"
      via: "op === 'install' && effectivePrinterId === undefined — единственный случай, когда placeId обязателен"
      pattern: "effectivePrinterId"
---

<objective>
Закрыть UI-часть двух gap'ов фазы 40: «cartridge-does-not-follow-printer» (test 5, item 3
missing — «сделать поле Место при установке необязательным») и «return-to-stock-empty-place-
field» (test 16, item 3 missing — «переписать подпись/хинт поля»). Бэкенд (Plan 40-21/40-22)
уже реализует cascade + fallback; эта плана убирает фронтенд-блокировку и делает поведение
формы понятным оператору.

Purpose: сегодня `validate()` требует `placeId` для `op==='install'` БЕЗУСЛОВНО — даже когда
выбран принтер без места (сервер и так корректно оставит место пустым или подставит из
принтера, D-13). Поле «Место (предыдущий картридж)» не имеет подсказки о новом fallback-
поведении из Plan 40-22 — оператор не знает, что пустое поле теперь безопасно.
Output: `validate()` требует место при install только для legacy cartridge-centric пути без
принтера (D-08); подсказка на previousCartridgePlaceId объясняет автоподстановку.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/debug/cartridge-does-not-follow-printer.md
@.planning/debug/return-to-stock-empty-place-field.md

@ui/src/features/cartridges/OperationModal.svelte
</context>

<interfaces>
`OperationModal.svelte` уже определяет (не менять контракт, только читать):
- `effectivePrinterId = $derived(preFillPrinterId ?? selectedPrinterId)` (строка 178) —
  `undefined`, когда установка НЕ целится в конкретный принтер (D-08 legacy
  cartridge-centric путь, PlacePicker в этом случае — единственный источник места).
- `validate()` (строки 547-575): текущий блок `if (op === 'install' || op === 'to_refill')
  { ...; if (placeId === null) { placeError = 'Заполните это поле'; valid = false; } }`
  требует место ОДИНАКОВО для install и to_refill — раздели их: `to_refill` не связан с
  принтером вообще (нет `printer_device_id` в его payload), там место остаётся
  обязательным без изменений; `install` — обязательным ТОЛЬКО когда
  `effectivePrinterId === undefined`.
- Поле `previousCartridgePlaceId` рендерится в блоке `{#if previousCartridge}`
  (строки ~702-718), `label` "Место (предыдущий картридж)" на `op-prev-place`, без
  `field-hint` (в отличие от главного поля «Место», у которого есть `field-hint` для
  `op === 'install'`, строки 772-774).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Место необязательно при установке в принтер + подсказка автоподстановки</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <action>
    В `validate()` раздели текущую объединённую ветку `op === 'install' || op ===
    'to_refill'` на две проверки placeId: для `to_refill` место остаётся обязательным
    без изменений (`if (placeId === null) { placeError = ...; valid = false; }`); для
    `install` требуй место ТОЛЬКО когда `effectivePrinterId === undefined` — то есть
    условие становится `if (op === 'install' && effectivePrinterId === undefined &&
    placeId === null)`. `givenByName`/`givenToName`-проверки для обоих op не трогай.

    Под `PlacePicker` для `op-prev-place` (previousCartridgePlaceId, строки ~712-717)
    добавь `<span class="field-hint">` (тот же CSS-класс, что уже используют другие
    подсказки формы, например строка 773) с текстом: «Если оставить пустым — картриджу
    подставится его последнее известное складское место; если истории нет, место
    останется не указано». Подсказка отображается всегда (нет состояния ошибки для этого
    поля — в отличие от `placeError` у основного поля «Место»).

    В блоке основного поля «Место» (строки ~756-775), дополни существующий
    `{:else if op === 'install'}` хинт: когда `effectivePrinterId !== undefined` (выбран
    принтер) И `placeId === null` (место ещё не подставлено — принтер без места), замени
    текст на «Необязательно: у принтера пока не указано место. Если укажете здесь — оно
    будет проставлено и принтеру» (Plan 40-21 Task 2 делает обратную запись). Когда
    `effectivePrinterId === undefined` (legacy путь без принтера), оставь исходный текст
    «Укажите рабочее место или кабинет (не склад)» без изменений — эта ветка
    по-прежнему обязательна.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Install с выбранным принтером и пустым «Место» проходит клиентскую валидацию (submit не блокируется field-error'ом); install без принтера с пустым «Место» по-прежнему блокируется; to_refill без места по-прежнему блокируется; previousCartridgePlaceId показывает подсказку об автоподстановке.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Клиентская валидация (`validate()`) → сервер | Клиентская валидация — только UX-удобство; сервер (Plan 40-21/40-22) уже принимает `place_id: None` независимо от того, что делает форма — снятие клиентского блока не открывает новую поверхность, оно лишь синхронизирует форму с уже принимаемым сервером контрактом |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-23-01 | Tampering | Ослабление клиентской валидации `placeId` | accept | Сервер — источник истины (D-13 auto-resolve + Plan 40-21 reverse-write); клиентская проверка была UX-удобством, не гейтом безопасности |
</threat_model>

<verification>
`pnpm --dir ui svelte-check` — 0 новых ошибок.
`pnpm --dir ui lint` — все 9 check-*.mjs гейтов зелёные (правка не затрагивает `place_path_short`/print-isolation контракты).
Ручная проверка (после мержа с 40-21/40-22): установка картриджа в принтер без места сохраняется без ошибки формы; форма показывает новую подсказку.
</verification>

<success_criteria>
- Форма установки картриджа не требует место, когда выбран принтер.
- Подсказка у поля «Место (предыдущий картридж)» объясняет автоподстановку.
- `to_refill` и legacy install-без-принтера сохраняют обязательность места (не регрессия).
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-23-SUMMARY.md` when done
</output>
