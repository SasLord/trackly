---
phase: 40-movement-history
plan: 14
type: execute
wave: 4
depends_on: ["40-10", "40-12", "40-13"]
files_modified:
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: true
requirements: [HST-01, HST-02, HST-03, HST-04]
must_haves:
  truths:
    - "Every new endpoint from this phase (timeline read, movements report list/export, bulk-move mutation) has a Manager-allow AND an Employee-deny case on BOTH transports — the exact four-part shape that Cases 45-48 established, per the IN-02 lesson that a case covering only one transport is a real, previously-shipped gap in this codebase (D-12)"
  artifacts:
    - path: "crates/trackly-app/tests/role_endpoint_matrix.rs"
      provides: "New Cases covering timeline read, movements report, and bulk-move — HTTP Manager-allow, HTTP Employee-deny, Tauri Manager/Employee variants"
  key_links:
    - from: "role_endpoint_matrix.rs new Cases"
      to: "Action::ReadPlaces gate on the timeline and report endpoints"
      via: "Manager allowed, Employee 403, both transports"
      pattern: "place_movements_get_timeline|reports_list_movements"
---

<objective>
Close out the phase's access-control regression coverage by adding role-matrix Cases for every new
endpoint introduced in Plans 40-10 (timeline read), 40-12 (movements report list + export), and 40-13
(bulk-move mutation). This plan runs LAST among the backend plans specifically because it needs all
three endpoints to already exist.

Purpose: this codebase has previously shipped a real, undetected bug where a gate existed on only one
transport (IN-02) — every new Phase 40 endpoint gets the full four-part treatment from day one, not
added later as an afterthought.
Output: new Cases in `role_endpoint_matrix.rs`, each citing D-12/IN-02 in its own comment per the
project's established convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
The exact four-part shape to clone (Cases 45-48, already read in full):
```rust
// Case 46-style (HTTP, Manager, READ allowed):
let status = post_with_cookie(new_app!(), "/api/v1/places_list_all", json!({...}), Some(&manager_cookie)).await;
assert_eq!(status, StatusCode::OK, "...");
// Case 47-style (HTTP, Employee, READ denied):
let status = post_with_cookie(new_app!(), "/api/v1/places_list_all", json!({...}), Some(&employee_cookie)).await;
assert_eq!(status, StatusCode::FORBIDDEN, "...");
// Case 48-style (Tauri, direct build_* call, Forbidden variant match):
let manager_id = Identity { user_id: Some(manager_dto.id), role: Role::Manager };
let result = build_places_create(&ctx, &manager_id, new_place).await;
assert!(matches!(result, Err(AppError::Forbidden)), "...");
```
Cases must be numbered sequentially after the last existing case in the file (grep the highest `Case
NN` comment number before choosing new numbers).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Role-matrix Cases — timeline read, movements report, bulk-move</name>
  <files>crates/trackly-app/tests/role_endpoint_matrix.rs</files>
  <read_first>
    crates/trackly-app/tests/role_endpoint_matrix.rs (full Cases 45-51 block, already read — clone their
    exact structure, comment style, and `new_app!()`/`post_with_cookie`/`Identity`/`build_*` harness
    calls; also grep the file's tail for the highest existing case number to continue sequentially),
    crates/trackly-app/src/tauri_cmds/place_movements.rs (Plan 40-10's exact Tauri command name and
    payload shape), crates/trackly-app/src/tauri_cmds/reports.rs (Plan 40-12's
    `build_reports_list_movements` exact signature), crates/trackly-app/src/tauri_cmds/places.rs (Plan
    40-13's `build_places_move_subtree_contents` exact signature)
  </read_first>
  <behavior>
    - Manager HTTP POST to the timeline-read endpoint returns 200 (or not 401/403); Employee HTTP POST
      returns 403
    - Manager Tauri call to the timeline `build_*` function does not return `Err(AppError::Forbidden)`;
      Employee Tauri call DOES return `Err(AppError::Forbidden)`
    - Manager HTTP POST to `reports_list_movements` returns 200; Employee HTTP POST returns 403
    - Manager Tauri call to `build_reports_list_movements` succeeds; Employee call returns
      `Err(AppError::Forbidden)`
    - Manager HTTP POST to the CSV/PDF export endpoints with `report_type: "movements"` returns 200;
      Employee returns 403
    - Manager Tauri/HTTP call to the bulk-move endpoint is allowed (per D-13's reused
      MutateDevices/MutateCartridges tier, both Admin|Manager); Employee call on both transports returns
      403/Forbidden
  </behavior>
  <action>
    Add a new numbered block of Cases immediately after the highest-numbered existing case in
    `role_endpoint_matrix.rs`, following the file's own established comment convention (cite "Phase 40
    Plan 14", the specific decision D-12, and the IN-02 lesson by name, exactly as Case 45's comment
    cites D-20/IN-02). Cover, in order: (1) HTTP Manager-allow + HTTP Employee-deny for the timeline-read
    endpoint; (2) Tauri Manager-allow + Tauri Employee-deny for the timeline-read `build_*` function; (3)
    HTTP Manager-allow + HTTP Employee-deny for `reports_list_movements`; (4) Tauri Manager-allow +
    Tauri Employee-deny for `build_reports_list_movements` — explicitly assert this is `Action::ReadPlaces`
    behavior (Manager allowed), NOT the `Action::ReadData` behavior every OTHER report type exhibits,
    making the divergence visible in the test itself; (5) HTTP + Tauri Manager-allow / Employee-deny for
    the movements report's CSV/PDF export with `report_type: "movements"`; (6) HTTP + Tauri Manager-allow
    / Employee-deny for the bulk-move mutation endpoint.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app role_endpoint_matrix -- --test-threads=1` passes in full (including all
      pre-existing Cases — this is a shared file, a syntax error anywhere breaks the whole test binary)
    - The new movements-report Cases include an explicit comment noting the `Action::ReadPlaces`
      divergence from the other 12 `Action::ReadData`-gated reports
    - Every new Case has both an HTTP variant and a Tauri variant (no case covers only one transport,
      per the IN-02 lesson this plan exists to prevent)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app role_endpoint_matrix -- --test-threads=1</automated>
  </verify>
  <done>All new Cases pass; the full pre-existing role_endpoint_matrix.rs suite is unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → both transports | This plan's entire purpose is proving the trust boundary (Employee vs Manager/Admin) holds identically on both transports for every new Phase 40 endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-28 | Elevation of Privilege | Any Phase 40 endpoint gated on only one transport, undetected until a real exploit (the exact IN-02 shape) | mitigate | This plan's sole purpose — every new endpoint gets both an HTTP and a Tauri case, both Manager-allow and Employee-deny |
</threat_model>

<verification>
`cargo test -p trackly-app role_endpoint_matrix -- --test-threads=1` fully green.
</verification>

<success_criteria>
Every endpoint introduced by Phase 40 has regression-tested, symmetric role gating on both transports.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-14-SUMMARY.md` when done
</output>
