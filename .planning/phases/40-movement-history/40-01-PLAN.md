---
phase: 40-movement-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V040__place_movements.sql
  - crates/trackly-core/src/domain/place_movements.rs
  - crates/trackly-core/src/domain/mod.rs
  - crates/trackly-infra/tests/place_movements_migration.rs
autonomous: true
requirements: [HST-01]
must_haves:
  truths:
    - "place_movements существует как отдельная таблица, а не выборка из audit_log (D-01)"
    - "Миграция не бэкфиллит историю из audit_log — таблица пуста сразу после апгрейда (D-02)"
    - "source хранится как незакрытый CHECK-ом TEXT-токен из четырёх значений manual/act/map/workstation, включая map и workstation, которые сегодня никто не пишет (D-07)"
    - "note — необязательное поле, NULL допустим (D-08)"
  artifacts:
    - path: "migrations/V040__place_movements.sql"
      provides: "place_movements table + 5 indexes + PRAGMA user_version=40"
    - path: "crates/trackly-core/src/domain/place_movements.rs"
      provides: "MovementSource, MovementEntityKind, is_reportable_place_change (pure, no I/O)"
    - path: "crates/trackly-infra/tests/place_movements_migration.rs"
      provides: "migration idempotency + fresh-DB seed test for V040"
  key_links:
    - from: "crates/trackly-core/src/domain/mod.rs"
      to: "crates/trackly-core/src/domain/place_movements.rs"
      via: "pub mod place_movements;"
      pattern: "pub mod place_movements"
---

<objective>
Add the `place_movements` schema (migration V040) and the pure domain types (`MovementSource`,
`MovementEntityKind`, the D-04/D-06 "is this a reportable place change" guard) that every later
plan in this phase depends on. This is the ONLY plan that touches the schema — every write-site,
repo, and read-side plan downstream assumes this table and these types already exist.

Purpose: give the phase a single, tested foundation (schema + pure business rule) before any
service-layer signature surgery begins.
Output: `migrations/V040__place_movements.sql`, `crates/trackly-core/src/domain/place_movements.rs`,
a passing migration idempotency test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration V040 — place_movements schema</name>
  <files>migrations/V040__place_movements.sql</files>
  <read_first>
    migrations/V039__place_path_display.sql (header doc-comment convention + unconstrained-enum-token
    precedent), migrations/V008__audit_log.sql (append-only shape: no deleted_at_utc, no version —
    this table copies that shape, not the standard4 soft-delete convention), migrations/V037__places.sql
    (places table + FK style)
  </read_first>
  <action>
    Create migration `migrations/V040__place_movements.sql`. Open with a header doc-comment in the
    established style (see V039) citing: D-01 (own table, not audit_log — reason: HST-04's two-filter
    report must not JSON-parse), D-06/D-07/D-09/D-10 (column shapes), and explicitly naming
    `migrations/V008__audit_log.sql` as the append-only-shape precedent (no `deleted_at_utc`, no
    `version` — this is a journal, not an editable entity). Create table `place_movements` with columns:
    `id INTEGER PRIMARY KEY AUTOINCREMENT`, `entity_type TEXT NOT NULL` (comment: 'device'|'cartridge',
    D-21 — printer is 'device'), `entity_id INTEGER NOT NULL`, `from_place_id INTEGER NOT NULL
    REFERENCES places(id) ON DELETE RESTRICT`, `from_place_path TEXT NOT NULL`, `to_place_id INTEGER
    NOT NULL REFERENCES places(id) ON DELETE RESTRICT`, `to_place_path TEXT NOT NULL`, `source TEXT
    NOT NULL` (comment: 'manual'|'act'|'map'|'workstation', D-07 — bare TEXT, no CHECK, mirrors
    `places.kind`/`path_variant_override` precedent: Rust-side `MovementSource::from_str_lenient` is
    the single parse point, never a SQL CHECK), `note TEXT NULL` (D-08, optional), `act_id INTEGER NULL
    REFERENCES acts(id) ON DELETE SET NULL`, `user_id INTEGER NULL REFERENCES users(id) ON DELETE SET
    NULL` (comment: NULL = система, D-09/D-11), `actor_name_snapshot TEXT NULL` (comment: ФИО at write
    time, login-reuse-safe, D-09), `created_at_utc INTEGER NOT NULL`. Add five indexes exactly:
    `idx_place_movements_entity ON place_movements(entity_type, entity_id, created_at_utc DESC)` (HST-02
    timeline query), `idx_place_movements_created ON place_movements(created_at_utc)` (HST-04 period
    filter), `idx_place_movements_from_place ON place_movements(from_place_id)` (HST-04 «Откуда»),
    `idx_place_movements_to_place ON place_movements(to_place_id)` (HST-04 «Куда»),
    `idx_place_movements_act ON place_movements(act_id) WHERE act_id IS NOT NULL` (D-03: `DELETE FROM
    place_movements WHERE act_id = ?` during undo). End with `PRAGMA user_version = 40;`. Do NOT add a
    `CHECK` constraint on `source` or `entity_type` — validation is Rust-side only (Pitfall 6 in
    RESEARCH.md: a strict SQL/Rust parse on this column crashed a whole screen once already, IN-01).
    Do NOT write any `INSERT`/backfill statement — D-02 requires the table start empty.
  </action>
  <acceptance_criteria>
    - `grep -c "CREATE TABLE place_movements" migrations/V040__place_movements.sql` equals 1
    - File contains no `INSERT INTO place_movements` statement (D-02 — no backfill)
    - File contains no `CHECK` clause referencing `source` or `entity_type`
    - File contains exactly 5 `CREATE INDEX` statements, one of which has a `WHERE act_id IS NOT NULL` clause
    - File ends with `PRAGMA user_version = 40;`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra migration -- --test-threads=1</automated>
  </verify>
  <done>Migration file exists, applies cleanly on a fresh DB (verified by Task 2's test), matches the append-only shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Domain types — MovementSource, MovementEntityKind, is_reportable_place_change</name>
  <files>crates/trackly-core/src/domain/place_movements.rs, crates/trackly-core/src/domain/mod.rs</files>
  <read_first>
    crates/trackly-core/src/domain/places.rs (PathDisplayVariant::from_str/as_str — exact pattern to
    mirror: `from_str` returns `Result<Self, AppError>` for STRICT write-time parsing where the value
    is server-controlled and always valid; add a SEPARATE `from_str_lenient` for read-side soft
    degradation per Pitfall 6 — do not reuse the strict form for reads), crates/trackly-core/src/domain/mod.rs
    (module registration list), crates/trackly-core/tests/no_io_deps.rs (forbidden-crate list — confirms
    this file may have zero `rusqlite`/`tokio` imports)
  </read_first>
  <behavior>
    - `MovementSource::as_str()` returns "manual"/"act"/"map"/"workstation" for each variant
    - `MovementSource::from_str_lenient("manual")` returns `Some(MovementSource::Manual)`; same for
      "act"/"map"/"workstation"
    - `MovementSource::from_str_lenient("garbage")` returns `None` (never panics, never errors — this
      is the soft-degradation entry point every read site must call, Pitfall 6/IN-01)
    - `MovementEntityKind::as_str()` returns "device"/"cartridge"; `label_ru()` returns
      "Устройство"/"Картридж" (HST-04 report «Тип» column)
    - `MovementEntityKind::from_str_lenient("printer")` returns `None` (printer is never a stored
      entity_type per D-21 — only 'device'/'cartridge' are valid tokens)
    - `is_reportable_place_change(Some(1), Some(2))` returns `true` (both Some, differ)
    - `is_reportable_place_change(Some(1), Some(1))` returns `false` (D-04 — no real change)
    - `is_reportable_place_change(None, Some(2))` returns `false` (D-06 — first assignment, NULL→place)
    - `is_reportable_place_change(Some(1), None)` returns `false` (D-06 — place cleared, place→NULL)
    - `is_reportable_place_change(None, None)` returns `false`
  </behavior>
  <action>
    Create `crates/trackly-core/src/domain/place_movements.rs` with a module doc-comment explaining this
    is Phase 40's pure domain layer (no I/O, mirrors `no_io_deps.rs` boundary). Define
    `#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum MovementSource { Manual, Act, Map, Workstation }`
    with `pub fn as_str(&self) -> &'static str` and `pub fn from_str_lenient(s: &str) -> Option<Self>`
    (returns `None` on unknown input — NEVER `Result`/`AppError` here, this is the read-side
    soft-degrade entry point, unlike `PlaceKind::from_str`'s strict `Result` form used at write time).
    Define `#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum MovementEntityKind { Device, Cartridge }`
    with `as_str()`, `from_str_lenient()`, and `pub fn label_ru(&self) -> &'static str` returning
    "Устройство"/"Картридж". Define `pub fn is_reportable_place_change(before: Option<i64>, after:
    Option<i64>) -> bool` implementing exactly: `matches!((before, after), (Some(b), Some(a)) if b != a)`.
    Register the module in `crates/trackly-core/src/domain/mod.rs` with `pub mod place_movements;`
    alongside the existing `pub mod places;` line.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-core place_movements -- --test-threads=1` passes, covering all 10 behavior
      cases above as `#[test]` functions in an inline `#[cfg(test)] mod tests` block
    - `cargo test -p trackly-core no_io_deps` still passes (file adds zero forbidden imports)
    - `grep -n "pub mod place_movements" crates/trackly-core/src/domain/mod.rs` returns one line
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-core place_movements -- --test-threads=1 && cargo test -p trackly-core no_io_deps -- --test-threads=1</automated>
  </verify>
  <done>Module compiles, exported from domain::mod, all 10 behavior cases pass, no_io_deps gate stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 migration idempotency test</name>
  <files>crates/trackly-infra/tests/place_movements_migration.rs</files>
  <read_first>
    crates/trackly-infra/tests/migration_idempotency.rs (full file — the exact pattern for a fresh-DB
    apply-and-inspect test: line ~63 "Phase 39 Plan 01: places tree schema-only migration correctness"
    is the closest analog; clone its harness setup, not its assertions)
  </read_first>
  <action>
    Create `crates/trackly-infra/tests/place_movements_migration.rs` (new file, following
    `migration_idempotency.rs`'s harness pattern: open an in-memory or tempfile SQLite connection, run
    all embedded refinery migrations, then assert on the resulting schema). Write test
    `place_movements_v040_creates_table_and_indexes` that: applies migrations to a fresh DB, queries
    `sqlite_master` for `type='table' AND name='place_movements'` (assert exists), queries
    `sqlite_master` for `type='index' AND tbl_name='place_movements'` (assert count == 5), asserts
    `PRAGMA user_version` reports at least 40. Write test `place_movements_starts_empty` that applies
    migrations then asserts `SELECT COUNT(*) FROM place_movements` returns 0 (D-02, no backfill). Write
    test `place_movements_migration_reruns_idempotently` that applies the full migration set twice in a
    row against the same connection (or two independent fresh applies) and asserts no error and the
    same final `user_version` — mirrors the existing idempotency pattern for V037-V039.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-infra place_movements_v040_creates_table_and_indexes -- --test-threads=1` passes
    - `cargo test -p trackly-infra place_movements_starts_empty -- --test-threads=1` passes
    - `cargo test -p trackly-infra place_movements_migration_reruns_idempotently -- --test-threads=1` passes
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra place_movements -- --test-threads=1</automated>
  </verify>
  <done>All three tests pass; running the full trackly-infra test suite does not regress (`cargo test -p trackly-infra -- --test-threads=1`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → SQLite schema | Static, developer-authored SQL, no runtime user input crosses this boundary |
| Domain module → callers | Pure functions, no I/O; inputs are already-validated `Option<i64>`/`&str` from trusted internal callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | `source`/`entity_type` columns (unconstrained TEXT) | accept | No SQL CHECK by design (mirrors `places.kind` precedent); values are always server-set at write time (never client-supplied, see Phase 40 RESEARCH V5), and every read site is required (Plan 40-05/40-10/40-11) to use `from_str_lenient` — an unrecognized value degrades to a safe fallback label, it cannot corrupt query results or crash a screen (IN-01 lesson) |
| T-40-02 | Information Disclosure | `actor_name_snapshot` (new ФИО-bearing column) | mitigate | Table itself carries no access control (schema-only plan); read-side gating (`Action::ReadPlaces`, Admin+Manager only) is enforced in Plan 40-10/40-12, not here — flagged so downstream plans do not skip it |
| T-40-SC | Tampering | none — this plan installs no new npm/pip/cargo packages | accept | Package Legitimacy Gate not applicable (RESEARCH.md confirms zero new external dependencies for this phase) |
</threat_model>

<verification>
`cargo test -p trackly-core place_movements -- --test-threads=1` and `cargo test -p trackly-infra place_movements -- --test-threads=1` both green; `cargo test -p trackly-core no_io_deps -- --test-threads=1` stays green (no regression).
</verification>

<success_criteria>
Migration V040 applies cleanly and idempotently on a fresh DB, creates an empty `place_movements`
table with 5 indexes, and the pure domain types (`MovementSource`, `MovementEntityKind`,
`is_reportable_place_change`) exist in `trackly-core` with zero I/O dependencies and full test coverage
of the D-04/D-06 guard logic every later write-site plan will call.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-01-SUMMARY.md` when done
</output>
