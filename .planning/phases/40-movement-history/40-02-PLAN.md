---
phase: 40-movement-history
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/src/services/place_path_display.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/services/mod.rs
autonomous: true
requirements: [HST-02, HST-04]
must_haves:
  truths:
    - "compute_place_path_short has exactly one owner in the codebase after this plan — act_service.rs no longer defines its own copy (D-18, avoids the WR-08 duplication bug)"
    - "Shortening still resolves in the same order: snapshot present -> place_id's CURRENT effective_variant -> org default -> DEFAULT_VARIANT constant, never .expect()/.unwrap()/`?` on this path (D-18)"
    - "Existing act rendering behavior is byte-for-byte unchanged after the move (regression, not a rewrite)"
  artifacts:
    - path: "crates/trackly-app/src/services/place_path_display.rs"
      provides: "pub fn compute_place_path_short(readers, place_id, snapshot) -> Option<String>, promoted out of act_service.rs"
  key_links:
    - from: "crates/trackly-app/src/services/act_service.rs"
      to: "crates/trackly-app/src/services/place_path_display.rs"
      via: "use crate::services::place_path_display::compute_place_path_short;"
      pattern: "place_path_display::compute_place_path_short"
---

<objective>
Promote the private `compute_place_path_short` function out of `act_service.rs` into a new shared
module, `place_path_display.rs`, so that Phase 40's timeline (Plan 40-10) and report (Plan 40-11) can
import the ONE existing, battle-tested path-shortening algorithm instead of copying it a second time —
this is the exact WR-08 anti-pattern (5 duplicated copies) Phase 39.2 just finished eliminating for org
defaults, and RESEARCH.md's Open Question 3 explicitly names this as required prep work.

Purpose: give every later plan that needs to render a shortened `from`/`to` place path (timeline rows,
report cells) a single import target, decided and built before those plans start, per Claude's
Discretion recommendation in RESEARCH.md.
Output: `place_path_display.rs` with the promoted function; `act_service.rs` calls it instead of
defining it; zero behavior change to existing act rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
Current private function to promote verbatim (act_service.rs:3015-3038):
```rust
fn compute_place_path_short(readers: &ReaderPool, place_id: Option<i64>, snapshot: Option<String>) -> Option<String> {
    let snapshot = snapshot?;
    let conn = readers.acquire();
    let variant_token: String = place_id
        .and_then(|pid| conn.query_row(
            "SELECT effective_variant FROM place_effective_variant WHERE place_id = ?1",
            params![pid], |r| r.get::<_, String>(0),
        ).ok())
        .unwrap_or_else(|| read_org_default_variant_token(&conn));
    let variant = PathDisplayVariant::from_str(&variant_token).unwrap_or(PathDisplayVariant::Ends);
    let (sep_ends, sep_last_two) = read_path_display_separators(&conn);
    Some(shorten_place_path(&snapshot, variant, &sep_ends, &sep_last_two))
}
```
`read_org_default_variant_token` and `read_path_display_separators` are `pub fn(&Connection)` already
in `crates/trackly-infra/src/repos/place_path_settings.rs` (Phase 39.2, WR-08's single-owner module) —
import them from there, do not re-derive. `PathDisplayVariant`/`shorten_place_path` live in
`trackly_core::domain::places` — import from there, unchanged.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote compute_place_path_short into place_path_display.rs</name>
  <files>crates/trackly-app/src/services/place_path_display.rs, crates/trackly-app/src/services/act_service.rs, crates/trackly-app/src/services/mod.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs lines 3007-3045 (compute_place_path_short and its
    call sites — grep `compute_place_path_short` in this file to find every call site before deleting
    the private fn), crates/trackly-infra/src/repos/place_path_settings.rs (full file — its own
    doc-comment explains WHY it lives in trackly-infra and stays narrowly scoped to settings-reads; this
    new module does NOT go inside place_path_settings.rs — RESEARCH's Open Question 3 recommendation is
    a NEW small trackly-app-level module because compute_place_path_short takes `&ReaderPool`, an
    app-level type, not a bare `&Connection`), crates/trackly-app/src/services/mod.rs (module
    registration list, to add `pub mod place_path_display;`)
  </read_first>
  <behavior>
    - Given a place_id with an explicit `path_variant_override` and a stored snapshot string,
      `compute_place_path_short` returns the snapshot shortened per that place's variant (unchanged
      behavior vs. the pre-move function)
    - Given a place_id with no override anywhere in its ancestor chain, falls back to the org default
      variant token (unchanged behavior)
    - Given `snapshot: None`, returns `None` immediately without querying the DB (unchanged — early
      `snapshot?` return)
    - Given an unparseable variant token in the DB, falls back to `PathDisplayVariant::Ends` via
      `.unwrap_or(...)`, never panics (unchanged — soft-degrade precedent)
  </behavior>
  <action>
    Create `crates/trackly-app/src/services/place_path_display.rs` with a module doc-comment stating
    this is the SINGLE owner of `compute_place_path_short` for Phase 40's timeline (Plan 40-10) and
    report (Plan 40-11), promoted out of `act_service.rs` per RESEARCH.md Open Question 3 to avoid the
    WR-08 duplication anti-pattern. Move the function body verbatim from `act_service.rs` (see
    `<interfaces>` above), changing visibility to `pub fn compute_place_path_short`. Import
    `place_path_settings::read_org_default_variant_token` and
    `place_path_settings::read_path_display_separators` from `trackly_infra::repos::place_path_settings`
    (or the crate's existing import alias for that module), and
    `trackly_core::domain::places::{PathDisplayVariant, shorten_place_path}`. In `act_service.rs`: delete
    the private `fn compute_place_path_short` definition entirely, add
    `use crate::services::place_path_display::compute_place_path_short;` at the top, and leave every
    existing call site (`compute_place_path_short(&readers, ...)`) unchanged — the call signature does
    not change, only where the function lives. Register the new module in
    `crates/trackly-app/src/services/mod.rs` with `pub mod place_path_display;`.
  </action>
  <acceptance_criteria>
    - `grep -c "fn compute_place_path_short" crates/trackly-app/src/services/act_service.rs` equals 0
      (function fully removed from this file)
    - `grep -c "pub fn compute_place_path_short" crates/trackly-app/src/services/place_path_display.rs` equals 1
    - `grep -n "use crate::services::place_path_display::compute_place_path_short" crates/trackly-app/src/services/act_service.rs` returns one line
    - Every pre-existing call site of `compute_place_path_short(` inside `act_service.rs` still compiles unmodified (no call-site argument changes)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app act_service -- --test-threads=1</automated>
  </verify>
  <done>Function lives in one place (`place_path_display.rs`), act_service.rs imports and calls it with zero call-site changes, full existing act_service test suite stays green (regression proof).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader pool → path-shortening function | Internal, no untrusted input crosses this boundary (place_id/snapshot are already server-resolved values) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | Tampering | Duplicated formula risk (if a second copy were introduced) | mitigate | This plan's entire purpose is to prevent a second copy from ever existing — single `pub fn` in `place_path_display.rs`, imported (not re-derived) by every downstream consumer, verified via the acceptance-criteria grep above |
| T-40-SC | Tampering | none — no new packages | accept | Not applicable |
</threat_model>

<verification>
`cargo test -p trackly-app act_service -- --test-threads=1` green (regression proof — no behavior change);
`grep -rc "fn compute_place_path_short" crates/trackly-app/src/services/*.rs` shows exactly one
occurrence total, in `place_path_display.rs`.
</verification>

<success_criteria>
`compute_place_path_short` has exactly one definition in the codebase, importable by Plan 40-10
(timeline) and Plan 40-11 (report) without touching `act_service.rs` again; existing act rendering is
provably unchanged.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-02-SUMMARY.md` when done
</output>
