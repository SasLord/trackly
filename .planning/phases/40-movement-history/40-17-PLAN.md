---
phase: 40-movement-history
plan: 17
type: execute
wave: 5
depends_on: ["40-15"]
files_modified:
  - ui/src/features/cartridges/CartridgeDetail.svelte
  - ui/src/features/printers/PrinterDetail.svelte
autonomous: true
requirements: [HST-02]
must_haves:
  truths:
    - "The existing История перемещений section in CartridgeDetail is renamed to Журнал операций, its content/format left completely untouched — including its known numeric place_id display bug, which this phase explicitly does not fix (D-16)"
    - "A NEW, separate Перемещения section is added directly below it, rendering the new backend-formatted timeline via the shared MovementTimeline component (D-16)"
    - "PrinterDetail reads the identical timeline data as its underlying device, by the device's own id — no separate UI variant, no double-accounting (D-21)"
  artifacts:
    - path: "ui/src/features/cartridges/CartridgeDetail.svelte"
      provides: "Renamed Журнал операций section (unchanged content) + new Перемещения section (MovementTimeline)"
  key_links:
    - from: "ui/src/features/printers/PrinterDetail.svelte"
      to: "ui/src/lib/components/MovementTimeline.svelte"
      via: "fetches the timeline by the printer's underlying device id, same code path as any device"
      pattern: "MovementTimeline"
---

<objective>
Wire Plan 40-15's `MovementTimeline` component into `CartridgeDetail.svelte` (rename existing section +
add new one, D-16 — "ничего не теряем") and `PrinterDetail.svelte` (reuse by device id, D-21 — "тот же
таймлайн"). This is the second of the two parallel UI-integration plans in Wave 5, touching entirely
disjoint files from Plan 40-16.

Purpose: complete HST-02's cartridge and printer surfaces, being careful to preserve the existing
«Журнал операций» content exactly as-is (it shows заправки/установки/списания that the NEW timeline
deliberately excludes per D-05).
Output: cartridge detail shows both sections; printer detail shows the same timeline as its device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-UI-SPEC.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
`CartridgeDetail.svelte`'s existing section to rename, verbatim current markup (already read):
```svelte
<DetailSection heading="История перемещений">
  {#if history.length === 0}
    <p class="history-empty">История пуста</p>
  {:else}
    <ul class="history-list">
      {#each history as entry (entry.id)}
        <li class="history-row">{formatHistoryEntry(entry)}</li>
      {/each}
    </ul>
  {/if}
</DetailSection>
```
Required change: `heading="История перемещений"` → `heading="Журнал операций"` — nothing else in this
block changes, including `formatHistoryEntry`'s numeric-`place_id` display bug (explicitly NOT fixed by
this phase, D-16). A NEW, separate `<DetailSection heading="Перемещения">` is added directly below it,
rendering `MovementTimeline` fed by a NEW fetch (`entity_type: 'cartridge'`) — this is a sibling block,
not a modification of the renamed one. `formatDate` (same file) is the manual-padStart date formatter
Plan 40-15's `MovementTimeline` may already import from here, or duplicate locally if it is not
exported — confirm which by reading the file.
`PrinterDetail.svelte` needs to fetch the timeline by its underlying device's id (`entity_type:
'device'`) — same code path as any plain device, per D-21's "PrinterDetail.svelte показывает тот же
таймлайн, читая его по id устройства."
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CartridgeDetail — rename + new Перемещения section</name>
  <files>ui/src/features/cartridges/CartridgeDetail.svelte</files>
  <read_first>
    ui/src/features/cartridges/CartridgeDetail.svelte lines 75-205 (full section — `formatHistoryEntry`,
    `formatDate`, the existing `DetailSection heading="История перемещений"` block, and wherever this
    component's own data-fetch effect lives, to add the new timeline fetch alongside it)
  </read_first>
  <behavior>
    - The existing section now reads «Журнал операций» in its heading; its list content (заправки/
      установки/списания entries, including the known numeric-`place_id` line) is byte-for-byte
      unchanged from before this plan
    - A new «Перемещения» section appears directly below it, rendering the cartridge's movement timeline
      via `MovementTimeline` (entries newest-first, correct empty/error states)
    - Opening a cartridge with BOTH some `audit_log` operation history AND some `place_movements` rows
      shows BOTH sections populated correctly, neither one hiding or replacing the other
  </behavior>
  <action>
    Change the existing `<DetailSection heading="История перемещений">`'s heading string to
    `"Журнал операций"` — do not touch anything else inside that block. Add a new fetch (in whatever
    effect/lifecycle hook this component already uses for its `history` array) for the cartridge's
    movement timeline (`entity_type: 'cartridge'`, `entity_id: cartridge.id`), stored in a new `let
    movements = $state<MovementEntryDto[]>([]);`. Add a new `<DetailSection heading="Перемещения">`
    immediately after the renamed section, rendering `<MovementTimeline entries={movements}
    loading={...} loadError={...} onNavigateToPlace={...} onNavigateToAct={...} />` — reuse this
    component's existing `loading`/error-state variables if the new fetch is folded into the same
    effect as the existing history fetch (preferred, per D-29's single-fetch-on-open convention), or add
    a second minimal loading flag if the two fetches are genuinely independent in this file's existing
    structure.
  </action>
  <acceptance_criteria>
    - `grep -n "heading=\"Журнал операций\"" ui/src/features/cartridges/CartridgeDetail.svelte` returns one line
    - `grep -n "heading=\"Перемещения\"" ui/src/features/cartridges/CartridgeDetail.svelte` returns one line
    - `grep -c "heading=\"История перемещений\"" ui/src/features/cartridges/CartridgeDetail.svelte` is 0
      (the OLD heading string no longer exists anywhere in this file)
    - `grep -n "formatHistoryEntry" ui/src/features/cartridges/CartridgeDetail.svelte` is unchanged in
      count from before this plan (the function itself is not modified, D-16)
    - `pnpm --dir ui svelte-check` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Both sections coexist correctly; the renamed section's content is provably untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrinterDetail — reuse timeline by device id</name>
  <files>ui/src/features/printers/PrinterDetail.svelte</files>
  <read_first>
    ui/src/features/printers/PrinterDetail.svelte (full existing structure — locate its own
    device-data-fetch effect and the section that would host the new timeline, mirroring how
    PlaceEntityViewModal's own device fetch was extended in Plan 40-16 for consistency)
  </read_first>
  <behavior>
    - Opening a printer's detail view shows a «История перемещений» section (same heading as the device
      version in Plan 40-16, since D-21 treats a printer's movement history as its underlying device's
      history verbatim) fetched by `entity_type: 'device'`, `entity_id: printer.device_id` (or whatever
      field this component uses to reference its underlying device row)
    - No separate "printer" branch or entity_type is ever passed to the timeline fetch
  </behavior>
  <action>
    Add a fetch for the timeline (`entity_type: 'device'`, using the printer's own underlying device id
    — confirm the exact field name by reading this file's existing device-data access pattern) alongside
    this component's existing device-data fetch. Add a `<DetailSection heading="История перемещений">`
    rendering `<MovementTimeline entries={...} loading={...} loadError={...} onNavigateToPlace={...}
    onNavigateToAct={...} />`, matching the same section heading and behavior as the device version in
    Plan 40-16 (D-21 — this is genuinely the same data, not a printer-specific variant).
  </action>
  <acceptance_criteria>
    - `grep -n "MovementTimeline" ui/src/features/printers/PrinterDetail.svelte` returns at least 2 lines
      (import + usage)
    - `grep -c "entity_type.*printer\|'printer'" ui/src/features/printers/PrinterDetail.svelte` inside
      the new timeline-fetch code is 0 (confirms no separate printer entity_type is ever used, D-21)
    - `pnpm --dir ui svelte-check` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Printer detail shows the exact same timeline data as its underlying device, with no double-accounting or separate entity_type.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cartridge/printer detail → timeline fetch | Gated server-side by Plan 40-10's `Action::ReadPlaces`, unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-34 | Information Disclosure | Accidentally reintroducing the numeric-place_id leak bug into the NEW «Перемещения» section while touching the file | mitigate | Task 1's acceptance criteria explicitly verifies `formatHistoryEntry` (the function carrying that known bug) is untouched in line-count, and the new section renders exclusively through `MovementTimeline`, which never displays a raw place_id (Plan 40-15's component only shows pre-formatted server strings) |
</threat_model>

<verification>
`pnpm --dir ui svelte-check` and `pnpm --dir ui lint` both green; `pnpm --dir ui build` succeeds.
</verification>

<success_criteria>
Cartridge detail shows both the untouched legacy section and the new timeline section; printer detail
correctly reuses the device timeline with no separate code path.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-17-SUMMARY.md` when done
</output>
