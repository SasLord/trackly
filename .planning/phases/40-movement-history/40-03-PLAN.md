---
phase: 40-movement-history
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/src/services/device_service.rs
  - crates/trackly-app/src/tauri_cmds/devices.rs
autonomous: true
requirements: [HST-01]
must_haves:
  truths:
    - "device_service::update receives a real caller identity end-to-end from both transports instead of hard-coding user_id_opt = None (Pitfall 1)"
    - "No movement-recording logic is added in this plan — this is signature plumbing only, done BEFORE the movement-insert logic (Plan 40-07), per RESEARCH's explicit instruction"
  artifacts:
    - path: "crates/trackly-app/src/services/device_service.rs"
      provides: "update(&self, caller: &Identity, id, version, patch) — caller.user_id flows into audit_log.user_id"
  key_links:
    - from: "crates/trackly-app/src/tauri_cmds/devices.rs::build_devices_update"
      to: "crates/trackly-app/src/services/device_service.rs::update"
      via: "ctx.devices.update(caller, id, version, patch).await"
      pattern: "ctx\\.devices\\.update\\(caller"
---

<objective>
Thread a real `caller: &Identity` into `device_service::update` end-to-end (both Tauri and HTTP
transports), replacing the hard-coded `let user_id_opt: Option<i64> = None;`. This is Pitfall 1 from
RESEARCH.md, isolated to its own plan/wave exactly as instructed: "Make 'thread caller: &Identity into
all six write-site methods' its own explicit task/wave, done BEFORE the movement-insert logic is
added." No `place_movements` INSERT is added here — this plan only makes the real actor identity
reachable inside the transaction closure so Plan 40-07 can use it.

Purpose: close the plumbing gap that would otherwise make every manual device move show "система" in
the timeline even when a real Manager performed it (D-09/D-11).
Output: `device_service::update` takes `caller: &Identity`; `build_devices_update` passes it through
instead of dropping it after `authorize()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
Analog pattern already used end-to-end by `place_service.rs` (Phase 39) — every method there already
takes `caller: &Identity` and extracts `user_id` BEFORE moving into the `'static` writer closure
(`Identity` is not itself moved into the closure):
```rust
pub async fn create(&self, caller: &Identity, new: PlaceNew) -> Result<PlaceDto, AppError> {
    authorize(caller, &Action::MutatePlaces)?;
    let now = self.clock.unix_seconds();
    let user_id = caller.user_id;          // extracted BEFORE moving into the writer closure
    let repo = self.repo.clone();
    let row: PlaceRow = self.writer.execute(move |conn| { /* user_id used here */ }).await?;
    Ok(PlaceDto::from(row))
}
```
Current gap (`crates/trackly-app/src/tauri_cmds/devices.rs:57-66`):
```rust
pub async fn build_devices_update(
    ctx: &AppCtx, caller: &Identity, id: i64, version: i64, patch: DevicePatch,
) -> Result<DeviceDto, AppError> {
    authorize(caller, &Action::MutateDevices)?;
    ctx.devices.update(id, version, patch).await    // <-- caller dropped here
}
```
HTTP handler (`crates/trackly-app/src/http/devices.rs::handler_update`) needs ZERO changes — it already
resolves `session_identity` and passes `&identity` into `build_devices_update`; only
`build_devices_update`'s own call to `ctx.devices.update(...)` and `DeviceService::update`'s
signature/body change.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread caller into device_service::update + build_devices_update</name>
  <files>crates/trackly-app/src/services/device_service.rs, crates/trackly-app/src/tauri_cmds/devices.rs</files>
  <read_first>
    crates/trackly-app/src/services/device_service.rs lines 257-309 (current `update` body — the
    before/after diff already exists here, only the `user_id_opt` line and signature change),
    crates/trackly-app/src/tauri_cmds/devices.rs lines 47-66 (`build_devices_update` — current call
    site), crates/trackly-app/src/http/devices.rs lines 176-189 (`handler_update` — confirm it already
    passes `&identity`, no change needed here), crates/trackly-app/src/services/place_service.rs lines
    148-160 (`create` — the exact `caller: &Identity` + `user_id` extraction shape to copy),
    crates/trackly-core/src/auth.rs (`Identity` struct definition — confirm field name is `user_id`)
  </read_first>
  <behavior>
    - Calling `device_service.update(&manager_identity, id, version, patch)` where the place actually
      changes results in the eventual `audit_log` row's `user_id` column equal to
      `manager_identity.user_id` (not `NULL`) — provable via a targeted integration test reading the
      row back after the call
    - Calling with an `Identity` whose `user_id` is `None` (a session with no resolved user id, if that
      case exists in this codebase) still succeeds and stores `NULL`, matching current behavior
    - `authorize(caller, &Action::MutateDevices)` still gates the mutation exactly as before — no
      change to who is allowed to call this method
  </behavior>
  <action>
    In `device_service.rs`: change `pub async fn update(&self, id: i64, version: i64, patch:
    DevicePatch)` to `pub async fn update(&self, caller: &Identity, id: i64, version: i64, patch:
    DevicePatch)`. Add `use trackly_core::auth::Identity;` if not already imported. Replace `let
    user_id_opt: Option<i64> = None;` with `let user_id_opt: Option<i64> = caller.user_id;` — extract
    this BEFORE the `self.writer.execute(move |conn| ...)` closure (matching `place_service::create`'s
    pattern), since `user_id_opt: Option<i64>` is already `Copy`-moved into the closure exactly as
    today, only its VALUE source changes. Do NOT call `authorize()` inside `device_service::update`
    itself — that gate stays exclusively in `build_devices_update` (mirrors the existing division of
    responsibility; do not duplicate the check). In `tauri_cmds/devices.rs::build_devices_update`,
    change the delegating call from `ctx.devices.update(id, version, patch).await` to
    `ctx.devices.update(caller, id, version, patch).await`. Do not modify
    `http/devices.rs::handler_update` — it already forwards `&identity` into `build_devices_update`
    unchanged. Do not add any `place_movements` logic in this task — that is explicitly Plan 40-07's job.
  </action>
  <acceptance_criteria>
    - `grep -n "pub async fn update" crates/trackly-app/src/services/device_service.rs` shows the
      signature includes `caller: &Identity` as the first parameter after `&self`
    - `grep -c "let user_id_opt: Option<i64> = None;" crates/trackly-app/src/services/device_service.rs`
      within the `update` method body is 0 (the hard-coded `None` line is gone from this method — other
      methods like `delete_soft` may still have their own copy, untouched by this plan)
    - `grep -n "ctx.devices.update(caller" crates/trackly-app/src/tauri_cmds/devices.rs` returns one line
    - `cargo build -p trackly-app` succeeds (no orphaned call sites left calling the old 3-arg signature)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app device_service -- --test-threads=1</automated>
  </verify>
  <done>device_service::update takes a real caller, audit_log.user_id reflects the real actor on manual device edits, existing device_service test suite passes unmodified in intent (only signature call-sites at test call-sites updated to pass a caller).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP session / Tauri invoke → device_service | `caller: &Identity` is resolved server-side (session cookie or Tauri's own identity resolution) before this call — never client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Spoofing | `device_service::update`'s new `caller` parameter | mitigate | `caller` is always the server-resolved `Identity` from `resolve_tauri_identity`/`session_identity` — never deserialized from the request body; `build_devices_update`'s existing `authorize()` call is unchanged and still gates the mutation |
| T-40-05 | Elevation of Privilege | Transport asymmetry (only one of Tauri/HTTP forwarding caller correctly) | mitigate | `http/devices.rs::handler_update` requires NO code change (already forwards `&identity`); acceptance criteria's `cargo build -p trackly-app` catches any missed call site at compile time since the signature is no longer optional |
</threat_model>

<verification>
`cargo test -p trackly-app device_service -- --test-threads=1` green; `cargo build -p trackly-app`
succeeds with zero orphaned 3-argument `ctx.devices.update(...)` call sites remaining anywhere in the
crate (compiler enforces this).
</verification>

<success_criteria>
Every manual device-place edit (via either transport) has the real actor's `user_id` available inside
`device_service::update`'s transaction, ready for Plan 40-07 to use — no `user_id_opt: None` remains
in this specific method.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-03-SUMMARY.md` when done
</output>
