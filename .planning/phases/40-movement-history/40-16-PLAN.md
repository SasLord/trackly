---
phase: 40-movement-history
plan: 16
type: execute
wave: 5
depends_on: ["40-15"]
files_modified:
  - ui/src/features/places/PlaceEntityViewModal.svelte
  - ui/src/features/devices/DeviceContextMenu.svelte
  - ui/src/features/devices/DeviceListRow.svelte
autonomous: true
requirements: [HST-02]
must_haves:
  truths:
    - "PlaceEntityViewModal gains one new DetailSection, «История перемещений», below the existing read-only field body, for both kind='device' and kind='printer' rows — no new fields, no new actions beyond this (D-14, D-15, D-21)"
    - "The modal now also opens from the device list itself, not only from PlaceContents — a new «Просмотр» context-menu item, first in the kebab menu, above «Редактировать» (D-14)"
    - "The timeline section shares the modal's single existing loading/error state — it does not get its own independent spinner (D-29, UI-SPEC States table)"
  artifacts:
    - path: "ui/src/features/places/PlaceEntityViewModal.svelte"
      provides: "New DetailSection rendering MovementTimeline, fetched alongside the existing device/cartridge fetch in the same $effect"
    - path: "ui/src/features/devices/DeviceContextMenu.svelte"
      provides: "New «Просмотр» menu item, first, above «Редактировать»"
  key_links:
    - from: "PlaceEntityViewModal.svelte"
      to: "ui/src/lib/components/MovementTimeline.svelte"
      via: "Plan 40-15's shared component, fed by the timeline fetch"
      pattern: "MovementTimeline"
    - from: "DeviceContextMenu.svelte"
      to: "PlaceEntityViewModal.svelte"
      via: "opens the modal with a minimal PlaceContentDto-shaped row constructed from the device list row"
      pattern: "kind: 'device'"
---

<objective>
Wire Plan 40-15's `MovementTimeline` component into `PlaceEntityViewModal.svelte` (covering both
`kind='device'` and `kind='printer'` rows per D-21) and add the modal's first-ever entry point from the
device list itself via a new «Просмотр» context-menu item (D-14). This is pure integration — no new
component logic, only wiring already-built pieces together.

Purpose: complete HST-02's device/printer surface.
Output: device and printer detail views show a working movement timeline; the modal is reachable
directly from `DevicesPage`'s row menu, not only from `PlaceContents`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-UI-SPEC.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
`PlaceEntityViewModal.svelte`'s exact current structure (full file already read, 226 lines):
`$effect` block does `Promise.all`-free sequential loads today (`cartridges.get`/`cartridges.modelsList`
via `Promise.all`, OR a single `devices.get` call) — this plan adds ONE more fetch (the timeline) to the
SAME async IIFE, so `loading`/`loadError` cover all three fetches together, not a separate spinner.
`row.kind === 'printer'` and `row.kind === 'device'` both resolve `deviceDto = await devices.get(r.id)`
today — the timeline fetch for either kind uses `entity_type: 'device'`, `entity_id: r.id` (D-21 — no
separate printer branch anywhere, including in this fetch).
`PlaceContentDto` shape (from `ui/src/bindings.ts`, already confirmed): `{ kind: string; id: number;
name: string; inventory_or_code: string | null; full_path: string; place_path_short: string;
status_name: string | null }` — `DeviceContextMenu`'s new «Просмотр» entry must construct an object of
this exact shape from the device list row's own available fields (device name, inventory number, place
path, place path short, status name) to open `PlaceEntityViewModal` with `row={...}`.
`DeviceContextMenu.svelte`'s existing kebab-menu item pattern (ctx-menu-item + the existing
delete-confirm Modal precedent) — «Просмотр» is a new item using the SAME `ctx-menu-item` class, calling
a new open-modal handler instead of a confirm dialog.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlaceEntityViewModal — new История перемещений DetailSection</name>
  <files>ui/src/features/places/PlaceEntityViewModal.svelte</files>
  <read_first>
    ui/src/features/places/PlaceEntityViewModal.svelte (full file, already read in this session — the
    `$effect` async IIFE at lines 83-107, the `loading`/`loadFailed` derived state, the `<Modal>` body
    markup after the existing `CartridgeFormBody`/`DeviceFormBody` readonly render), ui/src/lib/components/MovementTimeline.svelte
    (Plan 40-15's exact prop contract: `{ entries, loading, loadError, onNavigateToPlace,
    onNavigateToAct }`)
  </read_first>
  <behavior>
    - Opening the modal for a device/printer with 2 existing movements shows both the existing read-only
      field body AND, below it, a new «История перемещений» section rendering both movements via
      `MovementTimeline`
    - The timeline's loading state is visually identical to (governed by the same `loading` variable as)
      the rest of the modal — there is no second, independent spinner anywhere in this file after this
      change
    - Clicking a place segment inside the timeline navigates to `#/places?id={placeId}` (mirroring
      `handleGoTo`'s existing await-push-then-close sequencing) and closes the modal; clicking an act
      number navigates to `#/acts?id={actId}` (now supported per Plan 40-15's `ActsPage.svelte` change)
      and closes the modal
  </behavior>
  <action>
    Inside the existing `$effect`'s async IIFE, add a call to fetch the timeline (via whatever
    Tauri-invoke wrapper Plan 40-10 exposed, e.g. `placeMovements.getTimeline('device', r.id)`) alongside
    the existing `devices.get(r.id)`/`cartridges.get(r.id)` calls — for `isCartridge` rows, ALSO fetch
    the cartridge's own timeline with `entity_type: 'cartridge'`; for device AND printer rows (D-21),
    fetch with `entity_type: 'device'`. Store the result in a new `let timelineEntries =
    $state<MovementEntryDto[]>([]);`. Add a new `<DetailSection heading="История перемещений">` block
    (mirroring the existing `DetailSection` usage conventions in this codebase — check whether
    `DetailSection` is already imported here or needs a new import) immediately after the existing
    `CartridgeFormBody`/`DeviceFormBody` readonly render, rendering `<MovementTimeline
    entries={timelineEntries} loading={loading} loadError={loadError} onNavigateToPlace={async (id) =>
    { await push(\`#/places?id=\${id}\`); onClose(); }} onNavigateToAct={async (id) => { await
    push(\`#/acts?id=\${id}\`); onClose(); }} />`. Do NOT add a separate spinner/loading branch for this
    section — it participates in the modal's single existing `loading`/`loadFailed` state.
  </action>
  <acceptance_criteria>
    - `grep -n "MovementTimeline" ui/src/features/places/PlaceEntityViewModal.svelte` returns at least 2
      lines (import + usage)
    - `grep -c "Spinner" ui/src/features/places/PlaceEntityViewModal.svelte` is unchanged from before
      this plan (no NEW spinner instance added — confirms D-29/UI-SPEC's single-loading-state rule)
    - `pnpm --dir ui svelte-check` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Device and printer views both show a working, correctly-gated timeline section sharing the modal's existing loading/error state.</done>
</task>

<task type="auto">
  <name>Task 2: DeviceContextMenu «Просмотр» entry + DeviceListRow wiring</name>
  <files>ui/src/features/devices/DeviceContextMenu.svelte, ui/src/features/devices/DeviceListRow.svelte</files>
  <read_first>
    ui/src/features/devices/DeviceContextMenu.svelte (full existing kebab-menu structure — locate the
    current first item, «Редактировать», and its existing click-handler wiring pattern; also the
    existing delete-confirm `Modal` block for structural reference only, not reuse), ui/src/features/devices/DeviceListRow.svelte
    (how the row currently constructs/passes device data to `DeviceContextMenu`, to know what fields are
    available for constructing a `PlaceContentDto`-shaped object)
  </read_first>
  <action>
    Add a new first item «Просмотр» to `DeviceContextMenu`'s kebab menu, above the existing
    «Редактировать» (D-14 entry-point decision, UI-SPEC Claude's Discretion #1). Its click handler opens
    `PlaceEntityViewModal` with a `row` prop constructed as `{ kind: 'device', id: device.id, name:
    device.name, inventory_or_code: device.inventory_no ?? null, full_path: device.place_path ?? '',
    place_path_short: device.place_path_short ?? '', status_name: device.status_name ?? null }` (field
    names on the RIGHT must match whatever `DeviceDto`/list-row type actually exposes — read
    `DeviceListRow.svelte`'s current props to confirm exact field names before writing this mapping,
    they may differ slightly from these guesses). Wire the modal's `onClose`/`onChanged` callbacks the
    same way `PlaceContents.svelte` already does when it opens this same modal (reload the list on
    change, close on cancel).
  </action>
  <acceptance_criteria>
    - `grep -n "Просмотр" ui/src/features/devices/DeviceContextMenu.svelte` returns one line, appearing
      BEFORE the line containing «Редактировать» in the same file
    - `pnpm --dir ui svelte-check` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>«Просмотр» opens PlaceEntityViewModal directly from the device list, first time this modal has had an entry point outside PlaceContents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device list → PlaceEntityViewModal → timeline fetch | The timeline fetch itself is gated server-side (Plan 40-10's `Action::ReadPlaces`) — an Employee opening this modal (if the menu item were ever shown to them, which it should not be per role-based UI hiding) would still be denied server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-33 | Elevation of Privilege | Employee UI showing the «Просмотр» menu item or timeline section when the role should not see it | accept | Per D-12/UI-SPEC's Accessibility notes, UI hiding is cosmetic; the real gate is server-side (already covered by Plans 40-10/40-14) — an Employee session hitting this UI path would see a load error, not real data |
</threat_model>

<verification>
`pnpm --dir ui svelte-check` and `pnpm --dir ui lint` both green; `pnpm --dir ui build` succeeds.
</verification>

<success_criteria>
Both consumers (PlaceEntityViewModal's device/printer path, DeviceContextMenu's new entry point) work
correctly against the already-tested backend and shared component.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-16-SUMMARY.md` when done
</output>
