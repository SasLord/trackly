---
phase: 40-movement-history
plan: 19
type: execute
wave: 4
depends_on: ["40-13"]
files_modified:
  - ui/src/features/places/PlaceContents.svelte
autonomous: true
requirements: [HST-01]
must_haves:
  truths:
    - "Перенести всё содержимое в… is a new action on the place content panel, gated behind a confirm dialog that states the consequence in plain language and names the movement-history side effect (D-28)"
    - "The confirm button is variant=primary, not variant=destructive — nothing is deleted, this is a place change (UI-SPEC's explicit Claude's Discretion #4)"
  artifacts:
    - path: "ui/src/features/places/PlaceContents.svelte"
      provides: "Bulk-move button + confirm Modal, wired to build_places_move_subtree_contents"
  key_links:
    - from: "ui/src/features/places/PlaceContents.svelte"
      to: "crates/trackly-app/src/tauri_cmds/places.rs::build_places_move_subtree_contents"
      via: "Tauri invoke on confirm"
      pattern: "move_subtree_contents"
---

<objective>
Wire D-28's "Перенести всё содержимое в…" bulk-move action onto `PlaceContents.svelte`, against Plan
40-13's already-working, already-atomic backend. This is the UI half of the "cabinet relocation"
scenario — a target-place picker plus a confirm dialog stating the consequence in plain language.

Purpose: complete HST-01's bulk-move surface so a place's entire subtree contents can be relocated in
one user action instead of one-by-one manual edits.
Output: a new button + confirm `Modal` on the content panel, using only existing `Modal`/`Button`/
`PlacePicker` primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-UI-SPEC.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
Confirm-dialog analog to clone (`DeviceContextMenu.svelte`'s delete-confirm `Modal`, full block already
read):
```svelte
<Modal open={confirmOpen} title="Удалить устройство?" onClose={() => (confirmOpen = false)}>
  <p class="confirm-body">«{device.name}» ... будет помечено как удалённое. ...</p>
  {#snippet footer()}
    <Button variant="secondary" onclick={() => (confirmOpen = false)}>Отмена</Button>
    <Button variant="destructive" loading={deleting} onclick={handleDelete}>Удалить</Button>
  {/snippet}
</Modal>
```
UI-SPEC's exact required copy (Copywriting Contract table):
- Button: «Перенести всё содержимое в…»
- Confirm title: «Перенести содержимое в другое место?»
- Confirm body: «Место изменится у {N} предметов в этом разделе и всех вложенных местах. Для каждого
  появится запись в истории перемещений с причиной «вручную».»
- Buttons: «Отмена» (secondary) / «Перенести» (primary — NOT destructive)
- Failure toast: «Не удалось перенести содержимое. Попробуйте ещё раз.» (mirrors existing
  `pushToast('error', …)` convention)
`PlaceContents.svelte`'s existing `.crumb` CSS/markup pattern (for consistency, not directly reused
here since this is a button+modal, not a link).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bulk-move button + confirm modal + target-place picker</name>
  <files>ui/src/features/places/PlaceContents.svelte</files>
  <read_first>
    ui/src/features/places/PlaceContents.svelte (full file — locate the content panel's toolbar/action
    area where a new button belongs, and the existing subtree-contents item count already available to
    this component for the confirm body's {N} interpolation), ui/src/features/devices/DeviceContextMenu.svelte
    lines 186-216 (delete-confirm Modal — exact structural analog), ui/src/lib/components/PlacePicker.svelte
    (props/usage convention for the target-place selector inside the confirm dialog — this dialog needs
    a PlacePicker to CHOOSE the target place, not just confirm; UI-SPEC does not show a picker in the
    dialog body copy, so add it above the body text with its own label, e.g. «Новое место»),
    ui/src/features/places/api.ts or wherever Tauri invoke wrappers for places are defined (to add the
    `move_subtree_contents` call)
  </read_first>
  <behavior>
    - Clicking «Перенести всё содержимое в…» opens the confirm modal with a `PlacePicker` for the target
      place, initially unselected, and the primary «Перенести» button disabled until a target is chosen
    - Confirming with a valid target and N items in the current place's subtree calls the backend, and
      on success closes the modal, shows a success toast (or silently refreshes the content list,
      matching whatever success-feedback convention this screen already uses for its other mutations),
      and the content list reflects the new state
    - A backend failure shows the exact toast copy «Не удалось перенести содержимое. Попробуйте ещё
      раз.» and leaves the modal open (or closes it — match whichever failure-UX convention
      `DeviceContextMenu`'s own error handling already uses)
    - The confirm button uses `variant="primary"`, never `variant="destructive"`
  </behavior>
  <action>
    Add a «Перенести всё содержимое в…» button to the content panel's toolbar/action area (locate the
    existing toolbar per `read_first`). Clicking it opens a `Modal` titled «Перенести содержимое в
    другое место?» containing a `PlacePicker` labeled «Новое место» (bound to a local
    `let targetPlaceId = $state<number | null>(null);`) followed by the body paragraph «Место изменится
    у {N} предметов в этом разделе и всех вложенных местах. Для каждого появится запись в истории
    перемещений с причиной «вручную».» with `{N}` interpolated from the already-available subtree
    item count. Footer buttons: «Отмена» (`variant="secondary"`, closes without action) and «Перенести»
    (`variant="primary"`, `disabled={targetPlaceId === null}`, `loading={moving}`) which calls the
    Tauri invoke wrapper for `move_subtree_contents(rootId, targetPlaceId, null)` (add this wrapper to
    the places API module if it does not already exist), closes the modal and refreshes the content
    list on success, and shows the toast «Не удалось перенести содержимое. Попробуйте ещё раз.» via the
    existing `pushToast('error', …)` convention on failure.
  </action>
  <acceptance_criteria>
    - `grep -n "Перенести всё содержимое в" ui/src/features/places/PlaceContents.svelte` returns one line
    - `grep -n "variant=\"destructive\"" ui/src/features/places/PlaceContents.svelte` inside the new
      bulk-move block returns zero matches (the confirm button must be `variant="primary"`)
    - `pnpm --dir ui svelte-check` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Bulk-move action works end-to-end from the UI, using existing primitives only, with the correct non-destructive styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI confirm dialog → backend mutation | The real access-control gate is server-side (Plan 40-13's `MutateDevices`/`MutateCartridges` checks); this dialog is a UX confirmation, not a security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-32 | Repudiation | User unaware that a bulk move creates individual movement history entries | mitigate | UI-SPEC's exact confirm-body copy explicitly states this consequence in plain language before the user commits |
</threat_model>

<verification>
`pnpm --dir ui svelte-check` and `pnpm --dir ui lint` both green.
</verification>

<success_criteria>
D-28's bulk-move UI is fully wired, non-destructive in styling, and matches UI-SPEC's exact copy.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-19-SUMMARY.md` when done
</output>
