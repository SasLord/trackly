---
phase: 40-movement-history
plan: 07
type: execute
wave: 3
depends_on: ["40-03", "40-05"]
files_modified:
  - crates/trackly-app/src/services/device_service.rs
  - crates/trackly-app/tests/place_movements_write_sites_devices.rs
autonomous: true
requirements: [HST-01]
must_haves:
  truths:
    - "Manual device place edits create exactly one place_movements row with source='manual' and an empty note (D-27 — no new UI, existing PlacePicker-then-save flow)"
    - "A device edit that changes status but not place creates zero rows (D-04)"
    - "A first-time place assignment (NULL -> place) creates zero rows (D-06)"
  artifacts:
    - path: "crates/trackly-app/tests/place_movements_write_sites_devices.rs"
      provides: "place_movements_manual_device, place_movements_manual_device_status_only_noop, place_movements_manual_device_first_assignment_noop"
  key_links:
    - from: "device_service.rs::update"
      to: "SqlitePlaceMovementsRepository::record_movement_if_applicable"
      via: "called inside the same self.writer.execute closure, after repo.update_in_tx"
      pattern: "record_movement_if_applicable"
---

<objective>
Wire the device manual-edit write site (Plan 40-03's caller-threaded `device_service::update`) to
Plan 40-05's `record_movement_if_applicable` helper, and create the first slice of the
`place_movements_write_sites_*` Wave 0 test coverage (device family). This is the first of the three
parallel movement-insert wiring plans (device/cartridge/act) that consume the repo helper built in
Wave 2.

Purpose: make HST-01 real for the simplest write site first — device manual edits already have a
working before/after diff (per RESEARCH, this is "the cleanest insertion point").
Output: `device_service::update` records a movement when a real place→place change occurs; three new
integration tests proving D-27 (manual source), D-04 (status-only no-op), and D-06 (first-assignment no-op).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
@.planning/phases/40-movement-history/40-VALIDATION.md
</context>

<interfaces>
From Plan 40-03: `device_service::update(&self, caller: &Identity, id: i64, version: i64, patch:
DevicePatch)` already extracts `let user_id_opt: Option<i64> = caller.user_id;` and has `before`/`after`
device rows both available inside the transaction (`let before = repo.get_in_tx(&tx, id).ok();` /
`let after = repo.update_in_tx(&tx, id, version, &domain_patch, now)?;`).
From Plan 40-05: `SqlitePlaceMovementsRepository::record_movement_if_applicable(&self, tx:
&Transaction<'_>, places_repo: &dyn PlaceRepository<Conn = Connection>, entity_type:
MovementEntityKind, entity_id: i64, before_place_id: Option<i64>, after_place_id: Option<i64>, source:
MovementSource, note: Option<&str>, act_id: Option<i64>, user_id: Option<i64>, now_utc: i64) ->
Result<(), AppError>` — already implements the D-04/D-06 guard internally; callers do NOT need to
re-check `before != after` or `both Some` themselves.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire record_movement_if_applicable into device_service::update</name>
  <files>crates/trackly-app/src/services/device_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/device_service.rs lines 257-309 (current `update` body, post Plan
    40-03 — `before`/`after`/`user_id_opt` all already in scope), crates/trackly-infra/src/repos/place_movements_sqlite.rs
    (Plan 40-05's exact `record_movement_if_applicable` signature and `MovementEntityKind`/
    `MovementSource` import paths), crates/trackly-app/src/services/place_service.rs (confirm how
    `places_repo`/`PlaceRepository` trait objects are typically injected into a service struct — this
    service needs access to a `PlaceRepository` implementation to pass into the helper; check whether
    `DeviceService` already holds one or needs a new field/constructor argument)
  </read_first>
  <behavior>
    - Editing a device's place from A to B (both real places) via `update` produces exactly one
      `place_movements` row: `entity_type='device'`, `entity_id=<device id>`, `from_place_id=A`,
      `to_place_id=B`, `source='manual'`, `note=NULL`, `act_id=NULL`, `user_id=<caller's id>`
    - Editing only the device's `notes`/`status` field with `place_id` unchanged in the patch produces
      zero new `place_movements` rows
    - Editing a device that previously had no place, assigning it a place for the first time, produces
      zero new `place_movements` rows (D-06)
  </behavior>
  <action>
    Inside `device_service.rs::update`'s writer closure, immediately after `let after =
    repo.update_in_tx(&tx, id, version, &domain_patch, now)?;` and before the existing
    `audit_log` INSERT (or after it — either ordering is fine since both go in the same `tx`, but keep
    them adjacent for readability), call `self.place_movements_repo.record_movement_if_applicable(&tx,
    self.places_repo.as_ref(), MovementEntityKind::Device, id, before.as_ref().and_then(|b| b.place_id),
    after.place_id, MovementSource::Manual, None, None, user_id_opt, now)?;` — adjust field/method names
    to match whatever `DeviceService`'s actual struct fields are named after grepping for how
    `printer_repo`/other injected repos are stored and cloned in this same method (mirror that exact
    pattern for a newly added `place_movements_repo`/`places_repo` field; if `DeviceService` does not
    yet hold a `PlaceRepository` reference, add one following the same `Arc<dyn ...>`-clone-into-closure
    convention already used for `printer_repo` in this exact method). Import
    `trackly_core::domain::place_movements::{MovementEntityKind, MovementSource}` at the top of the
    file.
  </action>
  <acceptance_criteria>
    - `grep -n "record_movement_if_applicable" crates/trackly-app/src/services/device_service.rs`
      returns at least one line inside `update`'s writer closure
    - `cargo build -p trackly-app` succeeds (confirms any new struct field/constructor wiring compiles
      workspace-wide, including wherever `DeviceService::new(...)` is constructed in `context.rs`/`main.rs`)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app place_movements_manual_device -- --test-threads=1</automated>
  </verify>
  <done>device_service::update calls record_movement_if_applicable with the correct before/after/source/user_id, guarded correctly for D-04/D-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 test file — device family write-site coverage</name>
  <files>crates/trackly-app/tests/place_movements_write_sites_devices.rs</files>
  <read_first>
    crates/trackly-app/tests/role_endpoint_matrix.rs lines 1-140 (test harness setup pattern — how a
    test app/DB is constructed, how a test user/Identity is seeded — clone this harness style, not its
    role-matrix content), .planning/phases/40-movement-history/40-VALIDATION.md (Per-Task Verification
    Map — the exact three test function names this file must contain)
  </read_first>
  <behavior>
    - `place_movements_manual_device`: seed two places A and B, a device at place A, call
      `device_service.update` (or the equivalent test harness call) changing its place to B — assert
      exactly one `place_movements` row exists with `source='manual'`, `from_place_id=A`,
      `to_place_id=B`, `user_id` matching the test caller
    - `place_movements_manual_device_status_only_noop`: seed a device at place A, call `update` changing
      only `notes`/status fields with `place_id` unchanged — assert zero `place_movements` rows
    - `place_movements_manual_device_first_assignment_noop`: seed a device with `place_id = NULL`, call
      `update` assigning it to place A for the first time — assert zero `place_movements` rows
  </behavior>
  <action>
    Create `crates/trackly-app/tests/place_movements_write_sites_devices.rs` with the three test
    functions named exactly as in `<behavior>` above (matching VALIDATION.md's
    `place_movements_manual_device` substring exactly, since `cargo test -p trackly-app
    place_movements_manual_device` must match it). Use the same test-DB/AppCtx-construction harness
    pattern already established in `role_endpoint_matrix.rs` or a similar existing integration test file
    (grep for the most minimal existing device-service integration test as an additional harness
    reference if `role_endpoint_matrix.rs`'s setup is too heavy for this file's needs).
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app place_movements_manual_device -- --test-threads=1` passes (covers both
      the primary case and the `_status_only_noop`/`_first_assignment_noop` variants via substring match)
    - All three tests use invented device names/places only, no real organization data (privacy gate)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app place_movements_write_sites_devices -- --test-threads=1</automated>
  </verify>
  <done>All three tests pass; file matches the Wave 0 requirement named in VALIDATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device_service::update → place_movements | Internal — source is hard-coded `MovementSource::Manual` by the write site itself, never client-supplied (V5 Input Validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-15 | Spoofing | Client attempting to set `source` via the device update payload | mitigate | `DevicePatch` (the client-facing patch DTO) has no `source` field at all — the server hard-codes `MovementSource::Manual` for this write site, never reads a client value |
| T-40-16 | Tampering | A patch that changes `place_id` to the SAME value (client resubmission) being misrecorded as a move | mitigate | `record_movement_if_applicable`'s D-04 guard (Plan 40-05) already rejects `before == after`, tested at the repo layer and re-verified here at the service-integration layer |
</threat_model>

<verification>
`cargo test -p trackly-app place_movements_write_sites_devices -- --test-threads=1` green.
</verification>

<success_criteria>
Manual device place changes are observable in `place_movements` with the correct source/actor; no-op
and first-assignment cases correctly produce zero rows.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-07-SUMMARY.md` when done
</output>
