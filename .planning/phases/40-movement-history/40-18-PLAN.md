---
phase: 40-movement-history
plan: 18
type: execute
wave: 4
depends_on: ["40-12"]
files_modified:
  - ui/src/features/reports/ReportSubNav.svelte
  - ui/src/features/reports/ReportFilters.svelte
  - ui/src/features/reports/ReportTable.svelte
autonomous: true
requirements: [HST-04]
must_haves:
  truths:
    - "Перемещения is its own 4th ReportSubNav domain (not nested under Устройства/Картриджи) with a single report-type tab «Все перемещения» — entity-type split happens via a row-level filter, not separate report keys (D-22)"
    - "The report filter bar shows two independent PlacePicker instances, Откуда and Куда, both subtree-inclusive (D-24)"
    - "Soft-deleted report rows show an Удалено badge rather than silently vanishing (D-25)"
  artifacts:
    - path: "ui/src/features/reports/ReportSubNav.svelte"
      provides: "4th DOMAINS entry, key='movements', label='Перемещения', single-entry MOVEMENT_REPORTS array"
  key_links:
    - from: "ui/src/features/reports/ReportFilters.svelte"
      to: "ui/src/lib/components/PlacePicker.svelte"
      via: "two independent PlacePicker instances bound to from_place_id/to_place_id"
      pattern: "PlacePicker"
---

<objective>
Wire the movements report's frontend: a new 4th `ReportSubNav` domain, two `PlacePicker` filter
instances in `ReportFilters.svelte`, and the new D-23 columns plus the D-25 soft-deleted badge in
`ReportTable.svelte`. This is pure UI wiring against Plan 40-12's already-working, already-gated
backend endpoint — no new business logic.

Purpose: complete HST-04's user-facing surface.
Output: «Перемещения» selectable as its own report domain with a working filter bar and table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-UI-SPEC.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
`ReportSubNav.svelte`'s existing `DOMAINS`/`ReportConfig` shape (full read already done):
```ts
type DomainKey = 'devices' | 'cartridges' | 'requests';   // ADD 'movements'
interface ReportConfig { key: string; label: string; temporal: boolean; cmd: string; }
const DOMAINS = [
  { key: 'devices' as DomainKey, label: 'Устройства' },
  { key: 'cartridges' as DomainKey, label: 'Картриджи' },
  { key: 'requests' as DomainKey, label: 'Заявки' },
  // ADD: { key: 'movements' as DomainKey, label: 'Перемещения' },
];
```
New single-entry array (mirrors `REQUEST_REPORTS`'s naming convention, UI-SPEC discretion #2):
```ts
const MOVEMENT_REPORTS: ReportConfig[] = [
  { key: 'all', label: 'Все перемещения', temporal: true, cmd: 'reports_list_movements' },
];
```
`ReportFilters.svelte`'s existing single place-filter block (full read already done):
```svelte
<div class="place-filter-group">
  <label class="filter-label" for="report-place-filter"><span class="filter-name">Место</span></label>
  <div class="place-filter">
    <PlacePicker id="report-place-filter" value={placeId} onChange={(id) => onFilterChange?.({ place_id: id })} />
  </div>
</div>
```
```scss
.place-filter-group { display: flex; align-items: center; gap: var(--tr-space-2xs); }
.place-filter { display: flex; flex-direction: column; width: 220px; max-width: 100%; }
```
Clone this block TWICE for D-24 («Откуда»/«Куда»), branching on `activeDomain === 'movements'` — this
report does NOT use the existing single `place_id`/`is_storage` filter pair, it fully replaces them for
its own domain, per UI-SPEC's Component Reuse Map. `ReportTable.svelte`'s existing `place_path`/
`place_path_short` + `title=` full-path cell convention is cloned for the new "Откуда"/"Куда" columns.
Soft-deleted badge: `<Badge variant="default">Удалено</Badge>`, clones `PlaceContents.svelte:234`'s
`<Badge variant="default">Архив</Badge>` pattern exactly (component + variant, different label text).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ReportSubNav 4th domain</name>
  <files>ui/src/features/reports/ReportSubNav.svelte</files>
  <read_first>ui/src/features/reports/ReportSubNav.svelte (full file — DOMAINS array, REQUEST_REPORTS naming convention)</read_first>
  <action>
    Add `'movements'` to the `DomainKey` union type. Add `{ key: 'movements' as DomainKey, label:
    'Перемещения' }` as the 4th entry in `DOMAINS` (after 'requests', per D-22's "своя группа" — not
    nested under devices/cartridges). Define `const MOVEMENT_REPORTS: ReportConfig[] = [{ key: 'all',
    label: 'Все перемещения', temporal: true, cmd: 'reports_list_movements' }];` and wire it into
    whatever mapping/lookup this file uses to resolve a domain key to its `ReportConfig[]` array
    (mirror `REQUEST_REPORTS`'s own wiring exactly).
  </action>
  <acceptance_criteria>
    - `grep -n "movements" ui/src/features/reports/ReportSubNav.svelte` shows both the `DOMAINS` entry
      and the `MOVEMENT_REPORTS` array
    - `pnpm --dir ui svelte-check` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>«Перемещения» appears as its own top-level report domain with one report type under it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReportFilters two PlacePickers (D-24) + ReportTable columns/badge (D-23/D-25)</name>
  <files>ui/src/features/reports/ReportFilters.svelte, ui/src/features/reports/ReportTable.svelte</files>
  <read_first>
    ui/src/features/reports/ReportFilters.svelte (full existing place-filter block + whatever
    `activeDomain`/branching mechanism already exists for domain-specific filter sets), ui/src/features/reports/ReportTable.svelte
    (existing place_path/place_path_short cell rendering + title= convention), ui/src/features/places/PlaceContents.svelte
    line ~234 (`<Badge variant="default">Архив</Badge>` — exact pattern to clone for «Удалено»)
  </read_first>
  <behavior>
    - When `activeDomain === 'movements'`, the filter bar shows two `PlacePicker` instances labeled
      «Откуда»/«Куда», each independently subtree-inclusive, NOT the single existing «Место» filter
    - Setting BOTH «Откуда» and «Куда» produces a filter payload with both `from_place_id`/`to_place_id`
      populated (AND semantics resolved entirely on the backend per Plan 40-11 — this UI just passes
      both values through)
    - A report row with `is_deleted: true` renders an «Удалено» badge next to its «Предмет» cell value
  </behavior>
  <action>
    In `ReportFilters.svelte`, add a branch (mirroring however this file already conditionally renders
    domain-specific filter sets) that, when the active domain is `'movements'`, renders TWO
    `.place-filter-group`/`.place-filter` blocks side by side (per UI-SPEC's `--tr-space-sm` gap between
    them) labeled «Откуда» and «Куда», each with its own `PlacePicker` bound to `from_place_id`/
    `to_place_id` respectively, calling `onFilterChange?.({ from_place_id: id })` /
    `onFilterChange?.({ to_place_id: id })` — do NOT render the existing single «Место»/`is_storage`
    filter pair for this domain (UI-SPEC: "полностью заменяет их для своего домена"). In
    `ReportTable.svelte`, add column rendering for the 7 D-23 columns
    (Дата/Предмет/Тип/Откуда/Куда/Кем/Причина) when the active report type is `"movements"`, cloning the
    existing `place_path`/`place_path_short` + `title=` cell convention for the «Откуда»/«Куда» columns
    (report cells stay plain text per UI-SPEC — no accent color, no click-through, unlike the timeline's
    own links). Add the «Удалено» `<Badge variant="default">` immediately after the «Предмет» cell's
    value when `row.is_deleted` is true.
  </action>
  <acceptance_criteria>
    - `pnpm --dir ui svelte-check` passes
    - `grep -n "from_place_id\|to_place_id" ui/src/features/reports/ReportFilters.svelte` shows both wired
    - `grep -n "Удалено" ui/src/features/reports/ReportTable.svelte` returns at least one line
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Movements report filter bar and table render correctly with both place filters and the soft-deleted marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI report-domain visibility | Cosmetic only — the Employee role never sees the «Перемещения» tab, but the real security boundary is the backend `Action::ReadPlaces` gate (Plan 40-12), already tested by Plan 40-14 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-31 | Elevation of Privilege | Relying on UI-only hiding of the report domain as an access-control mechanism | accept | Per the project's established 999.1 backlog item and UI-SPEC's Accessibility notes — this plan does no route-gating work beyond hiding the tab visually; the real gate already exists server-side (Plan 40-12/40-14) |
</threat_model>

<verification>
`pnpm --dir ui svelte-check` and `pnpm --dir ui lint` both green; `pnpm --dir ui build` succeeds
(rebuild `ui/dist` before any manual LAN-browser check per VALIDATION.md's hazard note).
</verification>

<success_criteria>
The movements report is fully usable end-to-end from the UI: domain tab, dual place filters, correct
columns, and the soft-deleted marker.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-18-SUMMARY.md` when done
</output>
