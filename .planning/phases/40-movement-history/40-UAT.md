---
status: complete
phase: 40-movement-history
source: [40-01-SUMMARY.md, 40-02-SUMMARY.md, 40-03-SUMMARY.md, 40-04-SUMMARY.md, 40-05-SUMMARY.md, 40-06-SUMMARY.md, 40-07-SUMMARY.md, 40-08-SUMMARY.md, 40-09-SUMMARY.md, 40-10-SUMMARY.md, 40-11-SUMMARY.md, 40-12-SUMMARY.md, 40-13-SUMMARY.md, 40-14-SUMMARY.md, 40-15-SUMMARY.md, 40-16-SUMMARY.md, 40-17-SUMMARY.md, 40-18-SUMMARY.md, 40-19-SUMMARY.md, 40-20-SUMMARY.md]
started: 2026-09-02T15:37:51Z
updated: 2026-09-03T18:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Холодный старт и миграция V040
expected: Закрыть запущенное приложение. Запустить заново на существующей БД. Приложение стартует без ошибок, миграция V040 применяется молча, логи в ./logs/ не содержат ошибок миграции, главный экран (список устройств) грузится с живыми данными.
result: pass
note: |
  Лог: «current version: 39 → applying migration: V40__place_movements», далее чистый старт
  без ошибок. Пустая витрина — НЕ дефект: `cargo tauri dev` работает с dev-БД
  `target/debug/trackly.db`, которая создана с нуля 28.08 (V1..V39 применены одним пакетом
  2026-08-28T13:42:20) и в которой `audit_log` = 0 строк, т.е. мутаций там никогда не было.
  Заполненная рабочая БД — отдельный файл portable-установки, на схеме V38, сегодня не
  открывалась. V040 состоит только из CREATE TABLE + 5 индексов и ничего не удаляет.
  Потери данных нет.
  Дополнительно проверено на реальных данных: dev-БД заменена консистентной
  копией рабочей БД (схема V38, содержимое обезличено не цитируется), после чего
  старт применил V39__place_path_display и V40__place_movements подряд, без ошибок,
  на реальном дереве мест — тот же путь миграции, что пройдёт у пользователей на Windows.
  Сборка UI дала только пре-существующие предупреждения state_referenced_locally, новых нет.

### 2. История перемещений в карточке устройства (D-14)
expected: В списке устройств вызвать контекстное меню строки — первым пунктом «Просмотр». Открывается модалка просмотра устройства с новой секцией «История перемещений». Консоль webview чистая (нет ошибок рун Svelte 5). Строки таймлайна читаются как «ДД.ММ — откуда → куда · Кем · причина».
result: pass
note: |
  «Всё отображается и на первый взгляд работает правильно». Смежный дефект, найденный
  в ходе этой же проверки (картридж не следует за принтером), отнесён к тесту 5 — сама
  секция D-14 в карточке устройства/принтера рендерится корректно.

### 3. Ручное изменение места устройства пишет запись «вручную» (D-27)
expected: Отредактировать устройство и сменить место на другое, сохранить. Снова открыть «Просмотр» — в истории появилась новая верхняя запись: старое место → новое место, сегодняшняя дата, ваше ФИО в «Кем», причина «вручную». Изменение только статуса (без смены места) новой записи НЕ создаёт.
result: pass

### 4. Карточка картриджа: обе секции сохранены (D-16)
expected: Открыть карточку картриджа, у которого есть и операции, и перемещения. Видны ОБЕ секции одновременно: «Журнал операций» (прежний вид, с числовым place_id — сознательный долг) и ниже новая «Перемещения» с читаемыми путями мест. Ни одна секция не потеряна.
result: pass

### 5. Операции с картриджем пишут перемещение с причиной операции (D-05)
expected: Выполнить над картриджем операцию со сменой места (установка в принтер / возврат на склад / отправка на заправку). В секции «Перемещения» появляется запись с осмысленной причиной по-русски (не «вручную»). Если операция место не меняет — новой записи нет.
result: issue
reported: |
  Если я например, установлю картридж в принтер, после этого принетру поменяю место. То
  тогда у принтера появится Перемещение, а у картриджа вообще никаких перемещений не
  появляется, даже при установке. А должен ещё переместиться с принтером.
  Ещё в разделе Картриджи:
  При установке картриджа в принтер, его Место указывается только тогда, когда картридж
  устанавливается без привязки к принтеру. Если привязывается к принтеру, то берётся Место
  принтера и в случае перемещения принтера, Перемещение картриджа тоже должно фиксироваться.
  Если у привязанного принтера нет указанного Места, то тогда должно быть активное поле
  Место, для возможности указать это Место, если будет необходимость (не обязательное поле),
  но если указать это Место, то это место надо добавить и Принтеру (определить его на место).
  В списке картриджей отображается Место для картриджа старое, если принтер к которому
  привязан этот картридж, переместили.
severity: major
found_during: test 2

### 6. История перемещений в карточке принтера (D-21)
expected: Открыть карточку принтера. Есть секция «История перемещений» с тем же таймлайном, что и у устройств (принтер использует id своего устройства — отдельного типа сущности нет).
result: pass

### 7. Акт приёма-передачи меняет место и ставит ссылку на акт (HST-03, D-19)
expected: Создать акт приёма-передачи с указанием нового места для устройства. Открыть «Просмотр» этого устройства — в истории новая запись с причиной, содержащей номер акта. Клик по номеру акта открывает раздел «Акты» на этом акте.
result: issue
reported: |
  Всё хорошо, но есть одна не до работка. Устройство лежало на складе и актом было
  передано, ссылка на Акт №20 — работала правильно и перекидывала в Акты выделяя сам акт.
  Когда я сделал возврат на склад, то появилась снова ссылка на акт, а не на Возврат.
  Надо при переходе переносить в Акты, подраздел Возвраты и выделять возврат. И когда акт
  при возврате ушёл в Архив и я пробую по ссылке в таймлайне перейти на Акт №20, то
  перемещаюсь в подраздел Акты, а не Архив, но сам Акт №20 в блоке с детальной
  информацией — открывается, только в списке не выделяется. Остальное всё хорошо.
severity: major
note: |
  Запись перемещения, смена места и сам факт ссылки — корректны. Сломана адресация
  перехода: подраздел Актов не выбирается по типу/состоянию документа.
  Отдельно проверено и НЕ является дефектом: «Кем» = «система» во всех записях, потому что
  desktop_lock_enabled=0 и десктоп работает как Identity::trusted_admin() с user_id=None
  (D-Desktop-01); D-11 корректно маппит NULL → «система». ФИО ожидается под реальным
  логином — будет проверено в тестах 17-18.

### 8. Удаление акта убирает записи истории именно этого акта (D-03)
expected: Удалить (отменить) акт из теста 7. Записи истории, созданные ЭТИМ актом, исчезают из таймлайна устройства; записи от других актов и ручные записи остаются нетронутыми. Место устройства откатывается как раньше.
result: pass

### 9. Сокращённый путь и tooltip с полным путём (D-17/D-18)
expected: Найти в таймлайне запись с длинным путём места (несколько уровней дерева). В строке путь отображается в сокращённом виде и остаётся читаемым — строка не расползается по ширине модалки. При наведении курсора tooltip показывает полный сохранённый путь.
result: pass

### 10. Переход по месту из таймлайна (D-19)
expected: Клик по названию места («откуда» или «куда») в строке таймлайна открывает раздел «Места» с выделенным этим местом.
result: pass

### 11. Отчёт «Перемещения»: вкладка и счётчик (D-22, EX-01)
expected: В разделе «Отчёты» появилась отдельная верхнеуровневая группа «Перемещения» (не вложенная в существующие). На вкладке счётчик показывает реальное число записей за период, а не 0. Таблица содержит колонки: Дата, Предмет, Тип, Откуда, Куда, Кем, Причина.
result: pass

### 12. Два независимых фильтра места «Откуда»/«Куда» (D-24)
expected: В фильтрах отчёта «Перемещения» два отдельных выбора места — «Откуда» и «Куда» (вместо одного «Место»). Каждый включает поддерево (выбор родителя ловит записи по дочерним местам). Заданные оба фильтра работают по И: остаются только перемещения из поддерева «Откуда» в поддерево «Куда».
result: pass

### 13. Маркер «удалено» для мягко удалённых предметов (D-25, WR-01)
expected: Отчёт «Перемещения» продолжает показывать записи по удалённому (мягко) устройству/картриджу — с бейджем «Удалено» рядом с названием предмета в таблице. В выгрузке CSV и в PDF у такого предмета в колонке «Предмет» стоит суффикс «(удалено)».
result: issue
reported: |
  В Отчёте при печати/экспорт PDF — помечается, что «(удалено)», в таблице Отчёта в
  приложении — не отображается.
  Ещё обнаружил проблемы: в разделе Устройства, при группировке списка: Место можно
  отображать у группы, если у всех Устройств в этой группе оно одинаковое. Поле Место не
  отображается в группе устройств, всегда отображается прочерк. Получилась инверсия: у
  самой группы должно Место отображаться, как прочерк — отображается реальное Место.
  В группе должны отображаться Места у устройств, а отображается прочерк.
severity: major
note: |
  Экспортная половина D-25 (WR-01) подтверждена рабочей; провалена живая таблица.
  Вторая проблема — регрессия отображения места в сгруппированном списке устройств,
  найдена попутно, к D-25 отношения не имеет, вынесена отдельным gap'ом.

### 14. Экспорт отчёта «Перемещения» в CSV и PDF (D-26)
expected: Экспорт в CSV: файл открывается, заголовки — те же 7 колонок D-23, кириллица не искажена. Экспорт в PDF: файл открывается, кириллица корректная, шрифт не «квадратики», многостраничный отчёт разбивается по страницам без наложения и обрезки текста.
result: pass

### 15. Редактор шаблонов не сломан (риск strict-undefined)
expected: Открыть настройки → редактор шаблонов документов, открыть предпросмотр любого шаблона (акт приёма-передачи, акт возврата). Предпросмотр по-прежнему рендерится, ошибки «undefined» нет.
result: pass

### 16. Массовый перенос содержимого места (D-28)
expected: Открыть место с содержимым, нажать «Перенести всё содержимое в…». Диалог показывает количество переносимых предметов и выбор целевого места. После подтверждения все устройства/принтеры/картриджи оказываются в новом месте, и у каждого в истории появляется своя запись перемещения. Отмена диалога ничего не меняет.
result: issue
reported: |
  Массовый перенос: «Да вроде работает и переносит всё с фиксированием в таймлайне».
  Дальше пользователь поставил серию установок картриджей в один и тот же принтер с
  заменой предыдущего: «Какие-то события пропадают и не фиксируются в Перемещении».
  (Исходный отчёт содержал реальные ФИО и названия мест — здесь приведена обезличенная
  реконструкция, см. analysis.)
severity: major
note: |
  Сам D-28 (bulk move) пользователь подтвердил рабочим. Провал — смежный, по картриджам.
analysis: |
  Сверка «Журнала операций» с секцией «Перемещения» у двух картриджей дала ровно три
  пропуска, двух разных природ:

  (1) БАГ — «Возвращён на склад» не меняет место.
      У картриджа A операция «Возвращён на склад» не породила записи перемещения, и по
      следующей операции видно почему: последующее ручное «Изменён» стартовало из МЕСТА
      ПРИНТЕРА, а не со склада. То есть возврат на склад оставил место прежним, поэтому
      guard is_reportable_place_change корректно пропустил запись. Ошибка не в журнале
      перемещений, а в том, что операция возврата не проставила складское место.
      Контроль: другая операция «Возвращён на склад» у того же картриджа место поменяла
      и перемещение дала — то есть путь воспроизводится не всегда.

  (2) СПОРНОЕ РЕШЕНИЕ D-06 — первичное размещение не попадает в историю.
      У картриджа A самая старая «Установлен в принтер» и у картриджа B единственная
      «Установлен в принтер» не дали записей: до них у картриджа не было места (NULL),
      а D-06 намеренно пропускает переходы с пустой стороны. Следствие: первая в жизни
      установка картриджа не наблюдаема в истории никогда. Для пользователя это читается
      как «события пропадают».

### 17. Сотрудник не видит историю и отчёт «Перемещения» (D-12)
expected: Зайти под ролью «Сотрудник» (LAN-браузер). Секции истории перемещений в карточках нет, группы отчётов «Перемещения» нет, прямой вызов не отдаёт данные (403). Менеджер и админ видят всё.
result: [pending]

### 18. LAN-браузер: те же проверки после сборки UI (D-29)
expected: Выполнить `pnpm --dir ui build`, открыть приложение в браузере из локальной сети под менеджером. Таймлайн в карточках устройства/картриджа/принтера и отчёт «Перемещения» работают так же, как в десктопе. Печать отчёта из браузера не ломает вёрстку страницы приложения (нет протечки print-каскада в DOM).
result: issue
reported: |
  Таймлайн отображается правильно. Но есть ошибка при печати Отчёта перемещений из
  браузера, при работе по lan. Выбрал в отчётах Перемещения, нажал Печать/Экспорт PDF,
  в предпросмотре отображается правильно на 2 страницы. При попытке отправить на Принтер
  или сохранить в PDF, появляется третий лист (дубликат первого листа). Если пытаться
  распечатать из приложения — всё правильно работает.
severity: major
note: |
  Таймлайн в LAN-браузере подтверждён рабочим. Провал — только печать отчёта.
  Совпадает с известным паттерном проекта lan_print_dom_leakage: печать в LAN верстает
  в DOM приложения, и асимметрия «десктоп печатает верно / браузер добавляет лист»
  указывает на протечку каскада, а не на дефект самого отчёта.

## Summary

total: 18
passed: 13
issues: 5
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Установка картриджа в принтер и последующее перемещение принтера фиксируются в истории перемещений картриджа; место картриджа следует за местом привязанного принтера"
  status: failed
  reason: |
    User reported: установка картриджа в принтер не создаёт ни одной записи перемещения
    у картриджа; при последующей смене места принтера запись появляется только у принтера,
    у картриджа — нет. В списке картриджей место показывается старое (место на момент
    установки), не следует за перемещённым принтером. Отдельно: поле «Место» в форме
    картриджа активно только при установке без привязки к принтеру — при привязке к
    принтеру без указанного места ввести место негде, и указанное место не проставляется
    самому принтеру.
  severity: major
  test: 5
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Клик по ссылке документа в таймлайне открывает нужный подраздел Актов (Акты / Возвраты / Архив) и выделяет нужную строку в списке"
  status: failed
  reason: |
    User reported: у записи о возврате на склад ссылка ведёт на исходный акт передачи,
    а не на документ возврата — надо открывать подраздел «Возвраты» и выделять возврат.
    Отдельно: когда акт после возврата ушёл в «Архив», переход открывает подраздел «Акты»,
    а не «Архив»; детальная карточка акта открывается, но строка в списке не выделяется.
  severity: major
  test: 7
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Бейдж «Удалено» (D-25) виден в живой таблице отчёта «Перемещения» в приложении, а не только в экспорте"
  status: failed
  reason: |
    User reported: в экспорте PDF/печати суффикс «(удалено)» проставляется корректно
    (WR-01 подтверждён), но в таблице отчёта внутри приложения бейдж не отображается.
    ReportTable.svelte::showDeletedBadge заявлен в 40-VERIFICATION.md как реализованный —
    значит либо флаг is_deleted не доезжает до живой таблицы, либо бейдж не отрисовывается.
  severity: major
  test: 13
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Операция «Возвращён на склад» проставляет картриджу складское место, и это перемещение фиксируется в истории"
  status: failed
  reason: |
    User reported: часть операций с картриджем не попадает в «Перемещения». Разбор показал,
    что одна из операций «Возвращён на склад» оставила место картриджа прежним (местом
    принтера) — видно по тому, что следующая ручная правка стартовала из места принтера,
    а не со склада. Guard is_reportable_place_change отработал верно; дефект в самой
    операции возврата, которая не проставила складское место. Другая операция «Возвращён
    на склад» у того же картриджа место поменяла — путь воспроизводится не всегда.

    Уточнение пользователя после теста 17: при той замене он СОЗНАТЕЛЬНО не заполнил поле
    места для снимаемого картриджа, ожидая, что тот вернётся на своё прежнее складское
    место сам. Цитата: «Ввело в заблуждение сообщение в скобочках у слова Место, либо
    просто не правильно работает». Поле в OperationModal.svelte:712 подписано
    «Место (предыдущий картридж)» — скобки уточняют, к какому из двух картриджей относится
    поле, а не то, что оно необязательное или подставится автоматически. Оставленное
    пустым, оно не меняет место, поэтому и перемещения нет.
    Итого требуется продуктовое решение из двух: либо авто-подстановка прежнего складского
    места при пустом поле, либо явная подпись/валидация, что без заполнения место не
    изменится.
  severity: major
  test: 16
  root_cause: "Пустое поле «Место (предыдущий картридж)» не меняет место; подпись поля не сообщает об этом"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      issue: "label «Место (предыдущий картридж)» (стр. 712) не объясняет поведение при пустом значении"
  
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Первая установка картриджа в принтер наблюдаема в истории перемещений"
  status: failed
  reason: |
    User reported (та же серия): установка картриджа в принтер не даёт записи, когда до
    неё у картриджа не было проставлено место. Причина — D-06, который намеренно
    пропускает переходы с NULL на одной стороне. Следствие: первичное размещение предмета
    не попадает в историю никогда, и для пользователя это читается как «события пропадают».
    Это не поломка реализации, а последствие зафиксированного решения — требуется
    продуктовое решение: либо писать псевдо-переход «(не указано) → место», либо принять
    и объяснить в UI.
  severity: minor
  test: 16
  root_cause: "D-06 by design — требуется пересмотр решения, а не исправление кода"
  artifacts: []
  missing: []
  debug_session: ""

- truth: "В сгруппированном списке устройств колонка «Место» показывает место у каждого устройства, а у строки-группы — только когда оно одинаково у всех её устройств"
  status: failed
  reason: |
    User reported: инверсия. У строки-группы в колонке «Место» показывается реальный путь,
    а у всех устройств внутри этой группы — прочерк. Должно быть наоборот: место всегда
    видно у устройств, а у группы — только при совпадении у всех.
    Найдено попутно при проверке теста 13; к D-25 отношения не имеет, вероятная регрессия
    отображения места в сгруппированном списке (фазы 39/39.1).
  severity: major
  test: 13
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Печать отчёта «Перемещения» из LAN-браузера даёт тот же результат, что и из десктопа"
  status: failed
  reason: |
    User reported: предпросмотр показывает корректные 2 страницы, но при отправке на принтер
    или сохранении в PDF появляется третий лист — дубликат первого. Из десктопного
    приложения та же печать работает правильно. Асимметрия «десктоп верно / браузер лишний
    лист» — известный паттерн проекта lan_print_dom_leakage (печать в LAN верстает в DOM
    приложения, протечка print-каскада), а не дефект содержимого отчёта.
  severity: major
  test: 18
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
