---
phase: 40-movement-history
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/src/tauri_cmds/cartridges.rs
autonomous: true
requirements: [HST-01]
must_haves:
  truths:
    - "cartridge_service::update receives a real caller identity AND gains a before-fetch it did not have (Pitfall 2 — no preceding SELECT exists today)"
    - "cartridge_service::transition / transition_in_tx receive a real caller identity threaded down to the repository layer, ready for Plan 40-08 to use for BOTH the main mutation and the nested auto-return call site (Pitfall 3)"
    - "No movement-recording logic is added in this plan — signature plumbing only, done BEFORE Plan 40-08"
  artifacts:
    - path: "crates/trackly-app/src/services/cartridge_service.rs"
      provides: "update(&self, caller, id, version, place_id, notes) with a before-fetch; transition(&self, caller, payload)"
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "transition_in_tx(&self, tx, cartridge_id, version, op, now_utc, caller_user_id: Option<i64>)"
  key_links:
    - from: "crates/trackly-app/src/tauri_cmds/cartridges.rs::build_cartridges_update"
      to: "crates/trackly-app/src/services/cartridge_service.rs::update"
      via: "ctx.cartridges.update(caller, id, version, place_id, notes).await"
      pattern: "ctx\\.cartridges\\.update\\(caller"
---

<objective>
Thread a real caller identity into BOTH cartridge write-site methods (`update` and `transition`),
fixing Pitfall 2 (no before-fetch exists on `update` today) along the way, so Plan 40-08 can add
movement-recording without also having to do signature surgery. This is Pitfall 1's cartridge half,
isolated to its own plan/wave per RESEARCH's explicit instruction to do threading BEFORE
movement-insert logic.

Purpose: `update`'s bare `UPDATE cartridges SET place_id=?1 ...` with no preceding `SELECT` means there
is no "before" state to diff — this plan adds the missing before-fetch AND the caller threading in one
pass, since both are prerequisite plumbing for the same method.
Output: `cartridge_service::update` gains a before-fetch and a `caller` parameter;
`cartridge_service::transition` and `SqliteCartridgeRepository::transition_in_tx` gain a
`caller_user_id: Option<i64>` parameter reaching BOTH mutation call sites inside `transition_in_tx`
(main + nested auto-return).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
Current gaps (verified by direct read):
```rust
// cartridge_service.rs:187-207 (update) — NO before-SELECT exists today
pub async fn update(&self, id: i64, version: i64, place_id: Option<i64>, notes: Option<String>)
    -> Result<CartridgeDto, AppError> {
    ...
    let tx = conn.transaction()...;
    let affected = tx.execute("UPDATE cartridges SET place_id=?1, notes=?2, ... WHERE id=?4 AND version=?5", ...)?;
    // no SELECT before this UPDATE anywhere
}
```
```rust
// cartridges_sqlite.rs:458 transition_in_tx signature — no caller param today
pub fn transition_in_tx(&self, tx: &Transaction<'_>, cartridge_id: i64, version: i64, op: &CartridgeTransitionOp, now_utc: i64) -> Result<(), AppError>
```
Note: repos in this codebase never take the `Identity` type directly (grep confirms zero matches across
`crates/trackly-infra/src/repos/*.rs`) — services extract `caller.user_id` and pass the plain
`Option<i64>` down, exactly like `place_service.rs` does. Do the same here: `transition_in_tx` gains a
plain `caller_user_id: Option<i64>` parameter, not an `Identity`.

Current tauri_cmds gaps (`crates/trackly-app/src/tauri_cmds/cartridges.rs:58-89`):
```rust
pub async fn build_cartridges_update(ctx: &AppCtx, caller: &Identity, id: i64, version: i64, place_id: Option<i64>, notes: Option<String>) -> Result<CartridgeDto, AppError> {
    authorize(caller, &Action::MutateCartridges)?;
    ctx.cartridges.update(id, version, place_id, notes).await   // <-- caller dropped
}
pub async fn build_cartridges_transition(ctx: &AppCtx, caller: &Identity, payload: CartridgeTransitionPayload) -> Result<CartridgeDto, AppError> {
    authorize(caller, &Action::MutateCartridges)?;
    ctx.cartridges.transition(payload).await   // <-- caller dropped
}
```
`http/cartridges.rs::handler_update`/`handler_transition` need ZERO changes — both already resolve
`session_identity` and forward `&identity` to the `build_*` functions.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cartridge_service::update — add before-fetch + caller threading</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs, crates/trackly-app/src/tauri_cmds/cartridges.rs</files>
  <read_first>
    crates/trackly-app/src/services/cartridge_service.rs lines 183-253 (`update` — full current body,
    including the audit_repo.insert call whose `user_id: None` also needs fixing),
    crates/trackly-app/src/services/device_service.rs lines 257-309 (the ALREADY-CORRECT before-fetch
    shape to mirror: `let before = repo.get_in_tx(&tx, id).ok();` — but note cartridge's repo trait may
    expose a differently-named getter; check `crates/trackly-core/src/ports/cartridges.rs` for the
    exact method name), crates/trackly-app/src/tauri_cmds/cartridges.rs lines 57-68
    (`build_cartridges_update` call site)
  </read_first>
  <behavior>
    - Calling `update(&caller, id, version, Some(new_place_id), notes)` when the cartridge's CURRENT
      `place_id` differs from `new_place_id` results in a before-state (old `place_id`) being available
      inside the same transaction as the UPDATE — provable by asserting the fetched before-value in a
      test that seeds a known place_id first
    - The resulting `audit_log` row's `user_id` reflects `caller.user_id`, not `NULL`
    - Optimistic-lock failure behavior (affected == 0 branch) is unchanged
  </behavior>
  <action>
    In `cartridge_service.rs::update`: change signature to `pub async fn update(&self, caller:
    &Identity, id: i64, version: i64, place_id: Option<i64>, notes: Option<String>)`. Extract `let
    user_id = caller.user_id;` before the `self.writer.execute(move |conn| ...)` closure. Inside the
    closure, BEFORE the existing `tx.execute("UPDATE cartridges SET place_id=?1, ...")` call, add a
    `SELECT place_id FROM cartridges WHERE id = ?1` (via `tx.query_row`, `.optional()` since the row
    might not exist — mirror the existing `affected == 0` error-handling style already in this method
    for consistency) to capture the pre-update `place_id` into a local variable. Replace the
    `audit_repo.insert(&tx, AuditEntry { ..., user_id: None, ... })` call's `user_id: None` with
    `user_id`. Do NOT add any `place_movements` INSERT in this task — Plan 40-08 does that, using the
    before-value this task now makes available. In `tauri_cmds/cartridges.rs::build_cartridges_update`,
    change `ctx.cartridges.update(id, version, place_id, notes).await` to
    `ctx.cartridges.update(caller, id, version, place_id, notes).await`.
  </action>
  <acceptance_criteria>
    - `grep -n "pub async fn update" crates/trackly-app/src/services/cartridge_service.rs` shows
      `caller: &Identity` as the first parameter
    - `grep -c "SELECT place_id FROM cartridges" crates/trackly-app/src/services/cartridge_service.rs`
      is at least 1 inside the `update` method (the new before-fetch)
    - `grep -n "ctx.cartridges.update(caller" crates/trackly-app/src/tauri_cmds/cartridges.rs` returns one line
    - `cargo build -p trackly-app` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app cartridge_service::update -- --test-threads=1</automated>
  </verify>
  <done>update() has both a real caller and a working before-fetch; audit_log.user_id reflects the real actor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cartridge_service::transition + transition_in_tx — caller threading, both call sites</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs, crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-app/src/tauri_cmds/cartridges.rs</files>
  <read_first>
    crates/trackly-app/src/services/cartridge_service.rs lines 383-436 (`transition` — full current
    body), crates/trackly-infra/src/repos/cartridges_sqlite.rs lines 458-689 (`transition_in_tx` — full
    body including BOTH mutation sites: the main `UPDATE cartridges` around line 539 and the nested
    auto-return branch around line 612 with its own `audit_repo.insert(..., user_id: None, ...)` at
    line ~674-687), crates/trackly-app/src/tauri_cmds/cartridges.rs lines 81-89
    (`build_cartridges_transition`)
  </read_first>
  <behavior>
    - `transition_in_tx(&tx, id, version, &op, now, Some(7))` on a plain Install/ReturnToStock/ToRefill/
      FromRefill (no auto-return branch triggered) stores `user_id: Some(7)` on its own `audit_log` row,
      not `NULL`
    - `transition_in_tx` on an Install that DOES trigger the nested auto-return of a previously
      installed cartridge stores `user_id: Some(7)` on BOTH audit_log rows — the main mutation's row AND
      the auto-returned cartridge's row (Pitfall 3 — two separate entities, two separate call sites,
      both need the caller)
  </behavior>
  <action>
    In `cartridges_sqlite.rs`, change `transition_in_tx`'s signature to add a trailing parameter:
    `pub fn transition_in_tx(&self, tx: &Transaction<'_>, cartridge_id: i64, version: i64, op:
    &CartridgeTransitionOp, now_utc: i64, caller_user_id: Option<i64>) -> Result<(), AppError>`. This
    function currently has NO `audit_repo.insert` call for the MAIN mutation visible in the excerpt read
    (only the nested auto-return branch inserts explicitly) — locate where/if the main mutation's own
    audit row is written (search the full function body past line 689 if the excerpt in `read_first`
    doesn't show it) and set its `user_id` field to `caller_user_id` if such a call exists; if the main
    mutation's audit insert lives in the CALLING method (`cartridge_service::transition`) instead, wire
    `caller_user_id` there. For the CONFIRMED nested auto-return `audit_repo.insert(tx, AuditEntry {
    entity_type: "cartridge", entity_id: prev_id, ..., user_id: None, ... })` call (~line 674-687),
    change `user_id: None` to `user_id: caller_user_id`. In `cartridge_service.rs::transition`: change
    signature to `pub async fn transition(&self, caller: &Identity, mut payload:
    CartridgeTransitionPayload)`. Extract `let user_id = caller.user_id;` before the writer closure, and
    pass it as the new trailing argument to `cart_repo.transition_in_tx(&tx, cartridge_id, version, &op,
    now, user_id)`. In `tauri_cmds/cartridges.rs::build_cartridges_transition`, change
    `ctx.cartridges.transition(payload).await` to `ctx.cartridges.transition(caller, payload).await`. Do
    NOT add any `place_movements` INSERT logic in this task — Plan 40-08 does that using the
    before/after values already local to this function (`current.place_id`/`new_place_id` for the main
    mutation, `prev_current.place_id`/`resolved_place_id` for the nested auto-return).
  </action>
  <acceptance_criteria>
    - `grep -n "pub fn transition_in_tx" crates/trackly-infra/src/repos/cartridges_sqlite.rs` shows a
      trailing `caller_user_id: Option<i64>` parameter
    - `grep -n "pub async fn transition" crates/trackly-app/src/services/cartridge_service.rs` shows
      `caller: &Identity` as the first parameter
    - `grep -c "user_id: None" crates/trackly-infra/src/repos/cartridges_sqlite.rs` inside
      `transition_in_tx`'s body is 0 for the nested auto-return's own audit insert (it now reads
      `user_id: caller_user_id`)
    - `grep -n "ctx.cartridges.transition(caller" crates/trackly-app/src/tauri_cmds/cartridges.rs` returns one line
    - `cargo build -p trackly-app` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app cartridge_service::transition -- --test-threads=1</automated>
  </verify>
  <done>Both the main transition mutation and the nested auto-return branch record the real caller's user_id in their own audit_log rows; transition_in_tx's new parameter is used at both sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP session / Tauri invoke → cartridge_service | `caller`/`caller_user_id` always server-resolved, never client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Spoofing | `cartridge_service::update`/`transition`'s new caller parameters | mitigate | Always the server-resolved `Identity`; `authorize(caller, &Action::MutateCartridges)` in `build_cartridges_*` is unchanged and still the sole gate |
| T-40-07 | Elevation of Privilege | Nested auto-return's separate entity (Pitfall 3) attributed to the wrong actor if `caller_user_id` is dropped at that specific call site | mitigate | Acceptance criteria explicitly greps for `user_id: None` remaining inside `transition_in_tx` after this plan — must be zero |
</threat_model>

<verification>
`cargo test -p trackly-app cartridge_service -- --test-threads=1` green; `cargo build -p trackly-app`
succeeds with no orphaned old-signature call sites.
</verification>

<success_criteria>
Both cartridge write-site methods (`update`, `transition` incl. its nested auto-return) have a real
caller identity reaching the transaction, with `update` also gaining the before-fetch it lacked —
Plan 40-08 can now add movement-recording using values already in scope.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-04-SUMMARY.md` when done
</output>
