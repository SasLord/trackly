---
status: partial
phase: 40-movement-history
source: [40-VERIFICATION.md]
started: 2026-09-03T07:05:00Z
updated: 2026-09-03T07:05:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Повторный прогон живого UAT после гап-клозура (40-21..40-29)

Собрать `pnpm --dir ui build`, затем повторно пройти тесты 5, 7, 13, 16, 18 из `40-UAT.md`
в запущенном приложении — и в десктопном окне, и в LAN-браузере.

expected: Ни одна из ранее сообщённых проблем не воспроизводится живьём. Особое
внимание тесту 16 (возврат картриджа на склад с пустым полем места): код-уровнево
закрыт правкой CR-02, но живого прогона после коммитов 40-28/40-29 не было.
result: [pending]

### 2. Конкурентная LAN-нагрузка на чтение

Открыть таймлайн перемещений и отчёт «Перемещения» одновременно из нескольких
браузерных вкладок/сессий; понаблюдать за отзывчивостью остальных операций.

expected: Ни одно чтение не виснет; БД остаётся доступной для прочих операций
под нагрузкой порядка 20 одновременных пользователей (проектное ограничение).
result: [pending]

### 3. Очистка места у принтера с прикреплёнными картриджами

expected: На сегодня сценарий НЕДОСТИЖИМ через штатный UI — `canSubmit` в
`DeviceFormBody.svelte` требует `placeId !== null`, форма не даёт отправить очистку
места. Бэкенд-фикс CR-03 — defense-in-depth на случай прямого вызова API или будущей
UI-фичи. Если/когда появится UI-путь очистки места принтера, проверить вручную:
место картриджей не меняется, версия не бампится, данные не теряются.
result: [pending]

## Summary

3 пункта ожидают живой проверки. Код-уровневых пробелов не осталось — верификация
дала 16/16 must-haves, все четыре гэпа предыдущего раунда закрыты и подтверждены
независимым откатом каждой правки с перепрогоном соответствующего теста.
