---
phase: 40-movement-history
plan: 30
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-app/src/tauri_cmds/cartridges.rs
  - crates/trackly-app/src/http/cartridges.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
  - crates/trackly-app/tests/cartridges_lifecycle.rs
autonomous: true
requirements: [HST-01]

must_haves:
  truths:
    - "Сервер может вычислить дефолт места для диалога «Отправка на заправку» — самое частое историческое место назначения по ВСЕЙ истории (без окна времени, без разбивки по пользователю/модели), с детерминированным tie-break, и None если истории нет"
    - "Сервер может вычислить дефолт места для диалога «Получение с заправки» — место, на котором картридж находился ДО отправки на заправку — переиспользуя last_known_storage_place_in_tx (Plan 40-28/CR-02), а не второй параллельный резолвер"
    - "Employee получает 403 при попытке вызвать новый эндпоинт дефолта места; Admin/Manager получают осмысленный ответ"
  artifacts:
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "pub fn last_known_storage_place_in_tx(&self, conn: &Connection, cartridge_id: i64) — обобщена с &Transaction на &Connection, не переименована; pub fn most_common_to_refill_destination(&self, conn: &Connection) -> Result<Option<i64>, AppError>; const TO_REFILL_MOVEMENT_NOTE — единственный источник литерала note, используемый и на записи (transition_in_tx), и на чтении (новая агрегатная функция)"
    - path: "crates/trackly-app/src/services/cartridge_service.rs"
      provides: "CartridgeService::operation_default_place(&self, op: &str, cartridge_id: Option<i64>) -> Result<Option<i64>, AppError>"
    - path: "crates/trackly-app/src/tauri_cmds/cartridges.rs"
      provides: "build_cartridges_operation_default_place helper + cartridges_operation_default_place tauri command, gated Action::ReadData"
    - path: "crates/trackly-app/src/http/cartridges.rs"
      provides: "POST /api/v1/cartridges_operation_default_place, OperationDefaultPlacePayload{op, cartridge_id}"
  key_links:
    - from: "crates/trackly-app/src/services/cartridge_service.rs::operation_default_place"
      to: "crates/trackly-infra/src/repos/cartridges_sqlite.rs::last_known_storage_place_in_tx"
      via: "прямой вызов того же резолвера, что и авто-возврат при установке нового картриджа (CR-02, план 40-28) — единственный владелец формулы 'последнее известное складское место'"
      pattern: "last_known_storage_place_in_tx"
    - from: "crates/trackly-infra/src/repos/cartridges_sqlite.rs::transition_in_tx"
      to: "crates/trackly-infra/src/repos/cartridges_sqlite.rs::most_common_to_refill_destination"
      via: "общая константа TO_REFILL_MOVEMENT_NOTE — единственный литерал, которым помечается движение 'отправлено на заправку', используется и на записи, и на чтении"
      pattern: "TO_REFILL_MOVEMENT_NOTE"
---

<objective>
Закрыть UAT3-01 (backend-часть): дать серверу возможность вычислить два осмысленных
дефолта места для диалогов картриджа — «Отправка на заправку» (самое частое историческое
место назначения, глобально) и «Получение с заправки» (место ДО отправки на заправку,
переиспользуя уже написанный в плане 40-28 резолвер `last_known_storage_place_in_tx`,
а не изобретая второй параллельный источник правды).

Purpose: OperationModal.svelte (фронтенд-план 40-31) не может подставить осмысленный
дефолт без нового источника данных — сегодня поле «Место» всегда пустое при открытии
обоих диалогов (UAT3-01, `40-HUMAN-UAT.md`).

Output: новый read-only эндпоинт на обоих транспортах (`cartridges_operation_default_place`),
две backend-функции резолва (одна переиспользована, одна новая), покрытие role-матрицей
и интеграционным тестом через реальный поток `CartridgeService`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-28-SUMMARY.md
@crates/trackly-infra/src/repos/cartridges_sqlite.rs
@crates/trackly-infra/src/repos/place_movements_sqlite.rs
@crates/trackly-app/src/services/cartridge_service.rs
@crates/trackly-app/src/tauri_cmds/cartridges.rs
@crates/trackly-app/src/http/cartridges.rs
</context>

<interfaces>
<!-- Extracted from the current codebase — executor should use these directly. -->

From `crates/trackly-infra/src/repos/cartridges_sqlite.rs` (current, private, tx-only —
Task 1 widens and publicizes this):
```
fn last_known_storage_place_in_tx(
    &self,
    tx: &Transaction<'_>,
    cartridge_id: i64,
) -> Result<Option<i64>, AppError>
```
Body only calls `tx.query_row(...)` twice (never `.execute`) — already read-only.
`Transaction<'_>` implements `Deref<Target = Connection>`, so its ONE existing call site
(`cartridges_sqlite.rs:694`, inside `transition_in_tx`, `tx: &Transaction<'_>` in scope)
keeps compiling unchanged via deref coercion once the parameter type becomes `&Connection`.

From `crates/trackly-infra/src/repos/cartridges_sqlite.rs:578-598` (`transition_in_tx`,
current write-site note literals — Task 1 promotes the `ToRefill` arm to a shared const):
```
let note: &str = match op {
    CartridgeTransitionOp::Install { .. } => "автоматически при установке в принтер",
    CartridgeTransitionOp::ReturnToStock { .. } => "автоматически при возврате на склад",
    CartridgeTransitionOp::ToRefill { .. } => "автоматически при отправке на заправку",
    CartridgeTransitionOp::FromRefill { .. } => "автоматически при возврате с заправки",
    CartridgeTransitionOp::WriteOff { .. } => "",
};
```

From `crates/trackly-infra/src/repos/place_movements_sqlite.rs` (schema/column names —
target of the new aggregate query, Task 1):
```
pub struct MovementRow {
    pub id: i64,
    pub entity_type: String,   // 'device' | 'cartridge'
    pub entity_id: i64,
    pub from_place_id: i64,
    pub from_place_path: String,
    pub to_place_id: i64,
    pub to_place_path: String,
    pub source: String,
    pub note: Option<String>,
    pub act_id: Option<i64>,
    pub user_id: Option<i64>,
    pub actor_name_snapshot: Option<String>,
    pub created_at_utc: i64,
}
```
`places` columns referenced by the existing `last_known_storage_place_in_tx` query
(reuse the same active-place filter): `is_storage`, `archived_at_utc`, `deleted_at_utc`.

From `crates/trackly-app/src/services/cartridge_service.rs:933-946` (pattern to mirror —
Task 2's new service method):
```
pub struct CartridgeService {
    pub writer: Arc<WriterHandle>,
    pub readers: Arc<ReaderPool>,
    pub(crate) cart_repo: Arc<SqliteCartridgeRepository>,
    ...
}

pub async fn storage_place_ids(&self) -> Result<Vec<i64>, AppError> {
    let readers = self.readers.clone();
    let place_repo = self.place_repo.clone();
    tokio::task::spawn_blocking(move || -> Result<Vec<i64>, AppError> {
        use trackly_core::ports::places::PlaceRepository;
        let conn = readers.acquire();
        place_repo.list_storage_place_ids(&conn)
    })
    .await
    .map_err(|e| AppError::Internal { source_chain: format!("spawn_blocking: {e}") })?
}
```

From `crates/trackly-app/src/tauri_cmds/cartridges.rs:204-219, 413-421` (pattern to mirror —
Task 2's `build_*` helper + Tauri wrapper; note the i64→i32 boundary conversion convention):
```
pub async fn build_cartridge_storage_place_ids(
    ctx: &AppCtx,
    caller: &Identity,
) -> Result<Vec<i64>, AppError> {
    authorize(caller, &Action::ReadData)?;
    ctx.cartridges.storage_place_ids().await
}

#[tauri::command]
#[specta::specta]
pub async fn cartridge_storage_place_ids(
    state: tauri::State<'_, AppCtx>,
) -> Result<Vec<i32>, AppError> {
    let caller = resolve_tauri_identity(state.inner()).await?;
    let ids = build_cartridge_storage_place_ids(state.inner(), &caller).await?;
    Ok(ids.into_iter().map(|v| v as i32).collect())
}
```

From `crates/trackly-app/src/http/cartridges.rs:112-120, 392-402, 431-452` (payload struct +
handler + router pattern to mirror — Task 2):
```
#[derive(serde::Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SuggestCompatPayload {
    pub prefix: String,
}

pub async fn handler_cartridge_storage_place_ids(
    State(ctx): State<AppCtx>,
    session: Session,
) -> Result<Json<Vec<i32>>, AppErrorResponse> {
    let identity = session_identity(&session).await.map_err(AppErrorResponse::from)?;
    let ids = build_cartridge_storage_place_ids(&ctx, &identity).await.map_err(AppErrorResponse::from)?;
    Ok(Json(ids.into_iter().map(|v| v as i32).collect()))
}
```
Router: `.route("/api/v1/cartridge_storage_place_ids", post(handler_cartridge_storage_place_ids))`.

From `crates/trackly-app/src/specta_export.rs:70-83` (registration list — Task 2 appends one
line):
```
crate::tauri_cmds::cartridges::cartridges_transition,
crate::tauri_cmds::cartridges::cartridges_get_history,
crate::tauri_cmds::cartridges::cartridge_storage_place_ids,
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Обобщить last_known_storage_place_in_tx + новая агрегатная функция + общая константа</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs</files>
  <read_first>
    - crates/trackly-infra/src/repos/cartridges_sqlite.rs (строки ~454-600 `transition_in_tx`
      и его вызов `last_known_storage_place_in_tx(tx, prev_id)` на строке ~694; строки
      ~925-983 сама функция; инлайн-тестовый модуль `#[cfg(test)] mod tests` начиная со
      строки ~1782 — `fresh_conn()`, `seed_place()`, `seed_model()`, `seed_device()` как
      образец для новых тестов)
    - crates/trackly-infra/src/repos/place_movements_sqlite.rs (полная сигнатура
      `MovementRow`/`get_history`/`insert_in_tx` — точные имена колонок `place_movements`:
      `entity_type`, `entity_id`, `from_place_id`, `to_place_id`, `note`, `created_at_utc`)
    - migrations/V040__place_movements.sql (типы колонок; `places.is_storage` /
      `archived_at_utc` / `deleted_at_utc` — тот же фильтр активного места, что уже
      применяет `last_known_storage_place_in_tx`)
  </read_first>
  <action>
    1. Объявить модульную константу `pub(crate) const TO_REFILL_MOVEMENT_NOTE: &str =
       "автоматически при отправке на заправку";` рядом с началом файла (после `use`-блока).
       Заменить инлайн-литерал в match-выражении `transition_in_tx` (арм
       `CartridgeTransitionOp::ToRefill { .. } => "..."`) на `TO_REFILL_MOVEMENT_NOTE` —
       единственный источник этой строки, используемый и на записи, и на чтении (задача 2
       ниже читает ту же константу).
    2. Обобщить `last_known_storage_place_in_tx`: изменить сигнатуру первого параметра с
       `tx: &Transaction<'_>` на `conn: &Connection`, добавить `pub` (сейчас функция
       приватна для модуля — вызов из `trackly-app` в задаче 2 требует полной видимости
       через границу крейта), переименовать внутренние обращения `tx.query_row(...)` на
       `conn.query_row(...)`. НЕ переименовывать саму функцию (единственный вызывающий на
       строке ~694 передаёт `tx: &Transaction<'_>` — компилируется без изменений через
       deref-coercion `Transaction: Deref<Target = Connection>`). Обновить doc-комментарий:
       функция теперь читаема и через транзакцию (существующие auto-return вызовы), и через
       обычное reader-pool соединение (новый вызов из задачи 2) — тело только читает,
       никогда не пишет.
    3. Добавить новую функцию `pub fn most_common_to_refill_destination(&self, conn:
       &Connection) -> Result<Option<i64>, AppError>` рядом с
       `last_known_storage_place_in_tx`. SQL: `SELECT pm.to_place_id FROM place_movements pm
       JOIN places p ON p.id = pm.to_place_id AND p.archived_at_utc IS NULL AND
       p.deleted_at_utc IS NULL WHERE pm.entity_type = 'cartridge' AND pm.note = ?1 GROUP BY
       pm.to_place_id ORDER BY COUNT(*) DESC, pm.to_place_id ASC LIMIT 1`, параметр
       `TO_REFILL_MOVEMENT_NOTE`, `.optional().map_err(map_rusqlite)`. Пользовательское
       решение (зафиксировано в 40-HUMAN-UAT.md): без окна времени, без разбивки по
       пользователю/модели — вся история, один агрегат. Tie-break `to_place_id ASC` —
       детерминированность для тестов, а не "как повезёт GROUP BY".
    4. Добавить в инлайн-тестовый модуль: (a) прямой тест, вызывающий
       `last_known_storage_place_in_tx` через `&Connection` из `fresh_conn()` (не через
       транзакцию) — доказывает, что обобщение сигнатуры реально работает вне
       write-транзакции; (b) 3-4 теста на `most_common_to_refill_destination`: пусто →
       `None`; один кандидат с двумя движениями в место A и одним в B → возвращает A;
       архивное место с наибольшим числом движений исключается, побеждает следующее по
       частоте активное; ничья по частоте — побеждает меньший `to_place_id`. Место с
       `is_storage=1`/`0` не имеет значения для этой функции (в отличие от
       `last_known_storage_place_in_tx`) — цель здесь чисто "куда чаще всего отправляли",
       без ограничения на тип места. Сидировать `place_movements` напрямую через
       `conn.execute("INSERT INTO place_movements (...) VALUES (...)", ...)` внутри теста
       (не через `record_movement_if_applicable` — тестируется ТОЛЬКО SQL-агрегат, не
       write-путь; write-путь через реальный сервис проверяется в Task 3 плана).
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-infra --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub fn last_known_storage_place_in_tx" crates/trackly-infra/src/repos/cartridges_sqlite.rs`
      показывает параметр `conn: &Connection` (не `&Transaction`)
    - `grep -n "pub fn most_common_to_refill_destination" crates/trackly-infra/src/repos/cartridges_sqlite.rs`
      присутствует
    - `grep -c "TO_REFILL_MOVEMENT_NOTE" crates/trackly-infra/src/repos/cartridges_sqlite.rs`
      ≥ 3 (объявление константы + арм в `transition_in_tx` + параметр новой функции)
    - `cargo test -p trackly-infra --lib` — зелено, включая новые тесты в
      `cartridges_sqlite::tests` (запускать строго последовательно, один `cargo test` за раз)
    - `cargo clippy -p trackly-infra -- -D warnings` чисто
    - `cargo fmt --all --check` чисто
  </acceptance_criteria>
  <done>Функция обобщена и публична, новая агрегатная функция существует, общая константа
  устраняет дублирование литерала между записью и чтением, все новые и существующие тесты
  модуля `cartridges_sqlite` зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Сервисный метод + оба транспорта (Tauri + HTTP) + регистрация specta</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs, crates/trackly-app/src/tauri_cmds/cartridges.rs, crates/trackly-app/src/http/cartridges.rs, crates/trackly-app/src/specta_export.rs</files>
  <read_first>
    - crates/trackly-app/src/services/cartridge_service.rs (поля `readers`/`cart_repo`
      структуры `CartridgeService`, строки ~39-52; метод `storage_place_ids`, строки
      ~933-946, как образец для копирования формы)
    - crates/trackly-app/src/tauri_cmds/cartridges.rs (`build_cartridge_storage_place_ids` +
      обёртка `cartridge_storage_place_ids`, строки ~204-219 и ~413-421; список импортов в
      начале файла)
    - crates/trackly-app/src/http/cartridges.rs (`SuggestCompatPayload`,
      `handler_cartridge_storage_place_ids`, `router()`, строки ~112-120, ~392-452)
    - crates/trackly-app/src/specta_export.rs (строки ~70-83 — список регистрации команд)
    - crates/trackly-infra/src/repos/cartridges_sqlite.rs (Task 1: точные новые/обобщённые
      сигнатуры `last_known_storage_place_in_tx` и `most_common_to_refill_destination`)
  </read_first>
  <action>
    1. В `cartridge_service.rs` добавить `pub async fn operation_default_place(&self, op: &str,
       cartridge_id: Option<i64>) -> Result<Option<i64>, AppError>`. Ветвление по `op`:
       `"to_refill"` → `spawn_blocking` + `readers.acquire()` +
       `cart_repo.most_common_to_refill_destination(&conn)` (cartridge_id игнорируется);
       `"from_refill"` → требует `cartridge_id.ok_or_else(|| AppError::Validation { field:
       "cartridge_id".to_string(), message: "cartridge_id обязателен для from_refill"
       .to_string() })?`, затем `cart_repo.last_known_storage_place_in_tx(&conn,
       cartridge_id)` — ЭТО и есть переиспользование резолвера из плана 40-28/CR-02,
       применённое к id САМОГО картриджа (не предыдущего/чужого) — задокументировать
       doc-комментарием ПОЧЕМУ это корректно: последнее движение картриджа, касающееся
       складского места, ЕСТЬ движение ToRefill, потому что ничто другое не двигает место
       картриджа, пока он находится в статусе «На заправке». Любой другой `op` (включая
       пустую строку) → `Err(AppError::Validation { field: "op".to_string(), message:
       format!("Неизвестная операция «{other}» для дефолта места") })`.
    2. В `tauri_cmds/cartridges.rs`: `build_cartridges_operation_default_place(ctx, caller,
       op: String, cartridge_id: Option<i64>) -> Result<Option<i64>, AppError>` — `authorize(caller,
       &Action::ReadData)?` (тот же гейт, что и у `storage_place_ids` — тот же круг
       пользователей, что и у мутации `transition`, т.к. это read-side поддержка того же
       диалога). Затем тонкая Tauri-обёртка `#[tauri::command] #[specta::specta] pub async
       fn cartridges_operation_default_place(state: tauri::State<'_, AppCtx>, op: String,
       cartridge_id: Option<i32>) -> Result<Option<i32>, AppError>` с конвертацией i32↔i64 на
       границе (тот же паттерн, что `cartridge_storage_place_ids`).
    3. В `http/cartridges.rs`: `#[derive(serde::Deserialize)] #[serde(rename_all =
       "camelCase")] pub struct OperationDefaultPlacePayload { pub op: String, pub
       cartridge_id: Option<i64> }`, обработчик `handler_operation_default_place` (session →
       identity → `build_cartridges_operation_default_place` → `Json(Option<i64>)`), маршрут
       `.route("/api/v1/cartridges_operation_default_place",
       post(handler_operation_default_place))` в `router()`.
    4. В `specta_export.rs` добавить строку
       `crate::tauri_cmds::cartridges::cartridges_operation_default_place,` в список рядом с
       остальными командами `cartridges::*`.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --test export_bindings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cartridges_operation_default_place" crates/trackly-app/src/specta_export.rs`
      присутствует
    - `cargo test -p trackly-app --test export_bindings` зелено (перегенерирует
      `ui/src/bindings.ts`)
    - `grep -n "cartridges_operation_default_place" ui/src/bindings.ts` присутствует после
      прогона export_bindings — доказывает, что Tauri-обёртка реально зарегистрирована и
      типобезопасна (несовпадение сигнатуры между `build_*` и обёрткой сломало бы этот тест
      компиляции, а не молча сгенерировало неверный биндинг)
    - `cargo build -p trackly-app` компилируется без ошибок
    - `cargo clippy -p trackly-app -- -D warnings` чисто
    - `cargo fmt --all --check` чисто
  </acceptance_criteria>
  <done>Оба транспорта (Tauri invoke + axum POST) отдают дефолт места через один и тот же
  `build_cartridges_operation_default_place`, зарегистрированы в specta_export.rs, компиляция
  и bindings-тест зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Role-матрица + интеграционный тест через реальный поток CartridgeService</name>
  <files>crates/trackly-app/tests/role_endpoint_matrix.rs, crates/trackly-app/tests/cartridges_lifecycle.rs</files>
  <read_first>
    - crates/trackly-app/tests/role_endpoint_matrix.rs (Case 13/14 `cartridges_list`, строки
      ~759-793 — образец пары «Employee 403 / Manager не-403»; текущий максимальный номер
      кейса — перепроверить через `grep -oE "Case [0-9]+" crates/trackly-app/tests/role_endpoint_matrix.rs
      | grep -oE "[0-9]+" | sort -n | tail -1` перед нумерацией новых, на момент планирования
      максимум был 59)
    - crates/trackly-app/tests/cartridges_lifecycle.rs (хелперы `seed_printer_device_with_place`
      строки ~92-110, `seed_place` строки ~116+; тест
      `install_auto_return_falls_back_via_real_service_flow_no_hand_seed` из плана 40-28 —
      ОБРАЗЕЦ "реальный поток, без ручного посева" для нового теста)
    - crates/trackly-app/src/tauri_cmds/cartridges.rs (Task 2: точная сигнатура
      `build_cartridges_operation_default_place`)
  </read_first>
  <action>
    1. В `role_endpoint_matrix.rs` добавить два новых кейса (следующие свободные номера после
       текущего максимума, см. read_first): «Employee session → POST
       /api/v1/cartridges_operation_default_place с `{"op":"to_refill","cartridgeId":null}` →
       403 Forbidden» и «Manager session → тот же payload → not 401/403 (200)» — точное
       зеркало пары Case 13/14, включая имя переменной payload
       (`operation_default_place_payload`) и формат ассертов/сообщений.
    2. В `cartridges_lifecycle.rs` добавить интеграционный тест (без ручного посева
       `place_movements` — только реальные вызовы `CartridgeService`): создать складское место
       A и картридж на месте A (`seed_place` + прямой INSERT картриджа с `place_id = A,
       status_id = 1`), выполнить `svc.transition(&caller, CartridgeTransitionPayload::ToRefill
       { ..., place_id: Some(refill_place_id) })` (создать отдельное НЕ-складское место для
       заправки), затем вызвать `svc.operation_default_place("from_refill",
       Some(cartridge_id))` и проверить, что результат `== Some(A)`. Отдельным тестом (или тем
       же файлом, новой функцией) — создать 2 картриджа, отправить оба на заправку с целевым
       местом B, третий — с целевым местом C, вызвать
       `svc.operation_default_place("to_refill", None)` и проверить `== Some(B)` (2 > 1). Оба
       теста называть с префиксом `operation_default_place_` для группируемого запуска.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --test role_endpoint_matrix && cargo test -p trackly-app --test cartridges_lifecycle -- operation_default_place</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test role_endpoint_matrix` — зелено, включая 2 новых кейса
    - `cargo test -p trackly-app --test cartridges_lifecycle -- operation_default_place`
      — зелено, минимум 2 новых теста
    - `grep -c "operation_default_place" crates/trackly-app/tests/cartridges_lifecycle.rs`
      ≥ 2
    - Полный прогон пакета: `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --
      --skip login_remember_persistent_cookie` — без регрессий (один `cargo test` за раз,
      таймаут до 600000 мс)
  </acceptance_criteria>
  <done>Employee получает 403, Manager — нет; оба сценария дефолта места (to_refill/from_refill)
  проверены через реальный поток CartridgeService, без ручного посева place_movements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| LAN-браузер (untrusted client) → axum `/api/v1/cartridges_operation_default_place` | клиент передаёт `op` (строка) и `cartridge_id` (опциональное число) |
| Десктоп-webview (Tauri invoke) → `cartridges_operation_default_place` | тот же payload, другой транспорт, тот же `build_*` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-40-30-01 | Information Disclosure | `cartridges_operation_default_place` (оба транспорта) | mitigate | `authorize(caller, &Action::ReadData)` — тот же гейт, что у `get_history`/`storage_place_ids`, Admin\|Manager, проверяется ДО любого запроса к БД |
| T-40-30-02 | Tampering | параметр `op` (произвольная строка от клиента) | mitigate | Rust `match` на точные литералы `"to_refill"`/`"from_refill"`; любое другое значение → `AppError::Validation`, нет "default"-ветки, молча выполняющей один из запросов |
| T-40-30-03 | Denial of Service | `most_common_to_refill_destination` (GROUP BY по `place_movements`) | accept | таблица ограничена масштабом одной LAN-организации (сотни, не миллионы строк — подтверждённый масштаб Фазы 39: ≤20 кабинетов, 100–150 устройств/этаж); нет пользовательского фильтра, расширяющего сканирование |
| T-40-30-04 | Elevation of Privilege / BOLA | `cartridge_id` в `from_refill`-ветке — произвольный id от клиента | accept | Admin/Manager в этой системе не имеют per-item ownership-скоупинга ни на одном существующем чтении картриджей (`get_history`, `get` — та же модель); дефолт места не раскрывает ничего, что уже не видно через `get_history` того же картриджа |
| T-40-30-SC | Tampering (supply chain) | новые зависимости | accept | план не добавляет ни одного нового crate |
</threat_model>

<verification>
- `cargo test -p trackly-infra --lib` — зелено (Task 1).
- `cargo test -p trackly-app --test export_bindings` — зелено, `ui/src/bindings.ts`
  содержит `cartridges_operation_default_place` (Task 2).
- `cargo test -p trackly-app --test role_endpoint_matrix` и `--test cartridges_lifecycle`
  — зелено, включая новые кейсы/тесты (Task 3).
- `cargo clippy --workspace -- -D warnings` и `cargo fmt --all --check` чисты.
- `node scripts/check-privacy.mjs` — 0 нарушений (новых ФИО/реквизитов план не вводит).
</verification>

<success_criteria>
Новый read-only эндпоинт `cartridges_operation_default_place` работает на обоих
транспортах, гейт совпадает с существующими cartridge-чтениями, `from_refill`-дефолт
переиспользует резолвер CR-02 без второго параллельного источника правды, `to_refill`-дефолт
— новый детерминированный агрегат по всей истории. Готово к потреблению фронтендом
(план 40-31).
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-30-SUMMARY.md` when done
</output>
