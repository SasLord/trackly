---
phase: 40-movement-history
plan: 11
type: execute
wave: 2
depends_on: ["40-01", "40-02"]
files_modified:
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/src/services/report_service.rs
autonomous: true
requirements: [HST-04]
must_haves:
  truths:
    - "ReportFilter gains two independent place fields, from_place_id and to_place_id, both subtree-inclusive, combined with AND semantics when both are set (D-24, RESEARCH Open Question 1 resolved AND per CONTEXT.md's own \"со склада в Здание Б\" example)"
    - "ReportRow gains genuinely new from_place_path/from_place_path_short fields rather than overloading an unrelated existing field for the \"from\" side (Pitfall 7)"
    - "Soft-deleted items still appear in the movements report, marked, rather than silently vanishing (D-25)"
  artifacts:
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "ReportFilter.{from_place_id,to_place_id}; ReportRow.{from_place_path,from_place_path_short,actor_name,reason,is_deleted}"
    - path: "crates/trackly-app/src/services/report_service.rs"
      provides: "list_movements (13th list_*) + query_movements_inner + row_field new match arms"
  key_links:
    - from: "report_service.rs::query_movements_inner"
      to: "place_movements table"
      via: "raw SQL SELECT with dual WITH RECURSIVE subtree CTEs (from_subtree, to_subtree)"
      pattern: "WITH RECURSIVE"
    - from: "report_service.rs::row_field"
      to: "place_path_display::compute_place_path_short"
      via: "shortening the from/to snapshot for PDF/print output (D-18)"
      pattern: "compute_place_path_short"
---

<objective>
Add the movements report's backend query layer: DTO field additions (`ReportFilter`'s two new place
filters, `ReportRow`'s new from-side fields) and the 13th `list_*` method (`list_movements`) with its
`query_movements_inner` free function implementing D-24's dual subtree-inclusive AND filter and D-25's
soft-deleted marker. This plan does NOT touch the Tauri/HTTP adapter layer or the `Action::ReadPlaces`
gate divergence — that is Plan 40-12, sequenced after this one specifically because the gate mistake
(copy-pasting `Action::ReadData` like the other 12 reports) is the single highest-risk spot in this
whole report clone and deserves its own focused plan.

Purpose: give Plan 40-12 a working query layer to wire into both transports.
Output: `ReportFilter`/`ReportRow` extended; `list_movements`/`query_movements_inner`/`row_field` new
arms implemented and unit-tested for filter semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
`ReportFilter` (full struct at `crates/trackly-app/src/dto/reports.rs:22-61`) already has
`date_from_utc`, `date_to_utc`, `place_id` (existing single-place subtree-inclusive filter, unused by
this report), `status_id`, `type_id`, `act_type`, `model_id`, `color`, `search`,
`request_category_filter`, `is_storage` — all `#[derive(Debug, Clone, Default, Serialize, Deserialize,
Type)]`. `ReportRow` (same file, ~line 69-113) is sparse — only fields relevant to a given report_type
are populated. Existing subtree-inclusive CTE clause to clone TWICE
(`report_service.rs::query_acts_inner`, ~lines 1140-1153):
```rust
if let Some(place_id) = filter.place_id {
    let idx = next_idx(&owned_params);
    owned_params.push(Box::new(place_id));
    with_prefix.push_str(&format!(
        "WITH RECURSIVE subtree(id) AS ( \
             SELECT id FROM places WHERE id = ?{idx} AND deleted_at_utc IS NULL \
             UNION ALL \
             SELECT p.id FROM places p JOIN subtree s ON p.parent_id = s.id \
             WHERE p.deleted_at_utc IS NULL \
         ) "
    ));
    clauses.push("a.place_id IN (SELECT id FROM subtree)".to_string());
}
```
SQLite supports multiple named CTEs in one `WITH RECURSIVE a AS (...), b AS (...)` clause — use
`from_subtree`/`to_subtree` as the two names, each parameterized independently, joined by `AND` in the
`WHERE` clause (D-24 AND semantics).
`list_device_acts`'s exact shape to clone for `list_movements` (report_service.rs:428-436):
```rust
pub async fn list_device_acts(&self, filter: ReportFilter, period: PeriodDto) -> Result<ReportResponse, AppError> {
    let tz = self.get_tz_offset();
    let (ts_from, ts_to) = compute_period_utc(&period, tz);
    let readers = self.readers.clone();
    tokio::task::spawn_blocking(move || {
        let conn = readers.acquire();
        query_acts_inner(&conn, &filter, ts_from, ts_to, "handover")
    }).await.map_err(|e| AppError::Internal { source_chain: format!("spawn_blocking list_device_acts: {e}") })?
}
```
`row_field`'s existing match shape (report_service.rs:1048-1082):
```rust
fn row_field(row: &ReportRow, col: &str, tz: UtcOffset, shorten: bool) -> String {
    match col {
        "place_path" => if shorten { row.place_path_short.as_deref() } else { row.place_path.as_deref() }.unwrap_or("").to_string(),
        _ => String::new(),
    }
}
```
From Plan 40-02: `place_path_display::compute_place_path_short(readers, place_id, snapshot) ->
Option<String>` — used here to shorten the `place_movements.from_place_path`/`to_place_path` STORED
snapshots (D-18: shorten based on CURRENT settings, not settings at write time — same treatment as the
timeline).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DTO additions — ReportFilter + ReportRow</name>
  <files>crates/trackly-app/src/dto/reports.rs</files>
  <read_first>
    crates/trackly-app/src/dto/reports.rs lines 1-120 (full ReportFilter + ReportRow structs — read
    completely before adding fields, to match the exact `#[specta(type = Option<i32>)]` annotation
    convention used on every `i64` field)
  </read_first>
  <behavior>
    - `ReportFilter::default()` has `from_place_id: None, to_place_id: None` (Default derive covers this
      automatically — behavior is that adding the fields does not break any existing `ReportFilter { ..
      Default::default() }` construction elsewhere in the codebase)
    - `ReportRow` with only `from_place_path`/`from_place_path_short` populated and all other new fields
      `None` serializes without error (sparse-struct convention preserved)
  </behavior>
  <action>
    In `ReportFilter`, add two fields immediately after the existing `place_id` field: `#[specta(type =
    Option<i32>)] pub from_place_id: Option<i64>,` and `#[specta(type = Option<i32>)] pub to_place_id:
    Option<i64>,` with a doc-comment citing D-24 ("два независимых subtree-inclusive фильтра — «Откуда»
    и «Куда», AND-семантика при обоих заданных"). In `ReportRow`, add: `pub from_place_path:
    Option<String>,` and `pub from_place_path_short: Option<String>,` (doc-comment: "«Откуда» для отчёта
    о перемещениях — «Куда» переиспользует существующие place_path/place_path_short, D-23"); `pub
    actor_name: Option<String>,` (doc-comment: "«Кем» — ФИО/логин/«система», D-11"); `pub reason:
    Option<String>,` (doc-comment: "«Причина» — готовая строка source+note или «актом №N», D-23,
    сформирована на бэкенде, не собирается из частей на фронте"); `pub is_deleted: Option<bool>,`
    (doc-comment: "D-25 — предмет мягко удалён после перемещения, строка остаётся с пометкой").
  </action>
  <acceptance_criteria>
    - `cargo build -p trackly-app` succeeds (Default/Serialize/Deserialize/Type derives all still apply cleanly)
    - `grep -n "from_place_id\|to_place_id" crates/trackly-app/src/dto/reports.rs` shows both new
      `ReportFilter` fields
    - `grep -n "from_place_path\|actor_name\|is_deleted" crates/trackly-app/src/dto/reports.rs` shows all
      new `ReportRow` fields
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p trackly-app</automated>
  </verify>
  <done>Both structs compile with the new fields, all additive (no existing report type's behavior changes).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: list_movements + query_movements_inner + row_field arms</name>
  <files>crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/report_service.rs lines 428-447 (list_device_acts — shape to clone),
    lines 1120-1160 (query_acts_inner's subtree CTE — shape to clone TWICE), lines 1048-1082 (row_field
    — existing match arms), migrations/V040__place_movements.sql (Plan 40-01's exact place_movements
    columns — this query reads from that table directly, joined to `devices`/`cartridges` for the
    display name and soft-delete status)
  </read_first>
  <behavior>
    - `query_movements_inner` with `from_place_id: Some(A), to_place_id: Some(B)` where A/B are
      subtree-parent nodes returns only rows whose movement's `from_place_id` is A-or-a-descendant AND
      whose `to_place_id` is B-or-a-descendant (D-24 AND semantics, subtree-inclusive both sides)
    - With only `from_place_id` set, `to_place_id` unset, returns all rows matching the from-side
      subtree regardless of destination (confirms independent, not coupled, filters)
    - A movement row whose underlying device/cartridge has since been soft-deleted (`deleted_at_utc IS
      NOT NULL`) still appears in the result set, with `is_deleted: Some(true)` (D-25) — NOT filtered out
    - `row_field(row, "reason", ..)` returns a non-empty string composed from `source`+`note` OR
      `"актом №{n}"` when `act_id` is set — never raw enum tokens leaking through
  </behavior>
  <action>
    Add `pub async fn list_movements(&self, filter: ReportFilter, period: PeriodDto) ->
    Result<ReportResponse, AppError>` to `report_service.rs`, cloning `list_device_acts`'s exact shape
    (tz resolve, period bounds, `spawn_blocking`, `readers.acquire()`), but — unlike `list_device_acts`
    — also cloning `self.readers.clone()` into the `spawn_blocking` closure alongside the acquired
    `conn`, so it can pass `&readers` into `query_movements_inner`. Implement `fn query_movements_inner(
    conn: &Connection, readers: &ReaderPool, filter: &ReportFilter, ts_from: i64, ts_to: i64) ->
    Result<ReportResponse, AppError>` that: builds a `WITH
    RECURSIVE from_subtree(id) AS (...), to_subtree(id) AS (...)` clause (only including whichever of
    `from_subtree`/`to_subtree` is actually needed based on which filter fields are `Some`, to avoid an
    unused CTE), joins `place_movements pm` to `devices d ON pm.entity_type='device' AND pm.entity_id =
    d.id` and to `cartridges c ON pm.entity_type='cartridge' AND pm.entity_id = c.id` (LEFT JOINs, since
    a row is exactly one or the other) to resolve the display name (`d.name` or `c.code`) and
    `is_deleted` (`d.deleted_at_utc IS NOT NULL OR c.deleted_at_utc IS NOT NULL`), filters on
    `pm.created_at_utc BETWEEN ts_from AND ts_to`, applies `pm.from_place_id IN (SELECT id FROM
    from_subtree)` when `filter.from_place_id` is set and `pm.to_place_id IN (SELECT id FROM
    to_subtree)` when `filter.to_place_id` is set (both present = AND, matching D-24's own worked
    example "со склада в Здание Б"), applies `filter.type_id` against the underlying device's
    `type_id` when `entity_type='device'` (D-04 requirement "тип устройства"), and maps each row into
    `ReportRow` with `from_place_path`/`from_place_path_short` set from `pm.from_place_path` (shortened
    via `place_path_display::compute_place_path_short`, D-18's single-owner formula. This function
    requires `&ReaderPool`, not `&Connection` — `query_movements_inner` MUST gain an additional
    `readers: &ReaderPool` parameter alongside `conn` and pass it straight through to
    `compute_place_path_short` for both the "from" and "to" snapshots, exactly matching how
    `act_service.rs` already calls it from code that has both a `conn` and a `readers` handle
    available. Do NOT resolve the variant/separators directly against `conn` inline inside this
    function under any circumstance — that would create a second copy of the shortening formula,
    precisely the WR-03/WR-08 duplication bug Plan 40-02 exists to eliminate; `list_movements` (this
    same task) must pass `self.readers.clone()` into `query_movements_inner` alongside the acquired
    `conn`), `place_path`/`place_path_short` reused for the "to" side, `reason`
    composed as `"актом №{act_number}"` when `act_id` is `Some` (resolve `act_number` via a join to
    `acts.number`) else `"вручную" + (" · " + note if note non-empty)` else the soft-degraded
    `MovementSource::from_str_lenient` label for any other source token, `actor_name` from
    `actor_name_snapshot` else the `users.login` for `user_id` else `"система"`, `month_key` from
    `created_at_utc` (existing helper reused, grep for how other `list_*` compute it), `is_deleted` as
    derived above. Add new `match` arms to `row_field`: `"from_place_path" =>` mirroring the existing
    `"place_path"` arm's `if shorten {...} else {...}` shape but reading `from_place_path`/
    `from_place_path_short`; `"actor_name" => row.actor_name.as_deref().unwrap_or("").to_string()`;
    `"reason" => row.reason.as_deref().unwrap_or("").to_string()`; `"entity_type_label" =>` a
    small label derived from context (this may require adding an `entity_type_label: Option<String>`
    field to `ReportRow` in Task 1's scope if not already covered — if so, note the addition in this
    task's diff and update Task 1's acceptance criteria mentally, but do not re-edit Task 1's file
    list). Do not touch `columns_for`/`column_labels_for`/`report_display_name`/the `Action::ReadPlaces`
    gate/HTTP handler — that is exclusively Plan 40-12.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app report_movements_place_filters -- --test-threads=1` passes (new test,
      seeds movements across at least 3 different from/to place pairs, asserts AND-filtered result count)
    - `cargo test -p trackly-app report_movements_deleted_item_marker -- --test-threads=1` passes (seeds
      a movement then soft-deletes the underlying device, asserts the row still appears with
      `is_deleted: Some(true)`)
    - `grep -c "WITH RECURSIVE" crates/trackly-app/src/services/report_service.rs` includes at least one
      new occurrence for the movements query (in addition to the existing `query_acts_inner` one)
    - `grep -n "fn query_movements_inner" crates/trackly-app/src/services/report_service.rs` shows a
      `readers: &ReaderPool` parameter in the signature (confirms the ReaderPool-threading approach was
      used, not an inline second copy of the shortening formula, per D-18/WR-08)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app report_movements -- --test-threads=1</automated>
  </verify>
  <done>list_movements returns correctly filtered, subtree-inclusive, soft-delete-inclusive rows; row_field renders all new columns without leaking raw enum tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReportFilter (client-supplied) → SQL WHERE clause | `from_place_id`/`to_place_id` are plain integers bound via parameterized queries (`params![]`), never string-interpolated — same discipline as the existing `place_id` filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-13 | Tampering | SQL injection via filter place ids | mitigate | All CTE parameters bound via `rusqlite::params!`, mirroring the existing `query_acts_inner` pattern verbatim — no string interpolation of user-controlled values |
| T-40-14 | Information Disclosure | This plan does NOT add the access gate (Plan 40-12 does) | accept | Explicitly deferred — `list_movements` is unreachable from any transport until Plan 40-12 wires `build_reports_list_movements`/`handler_list_movements`; no route exists yet that could leak data prematurely |
</threat_model>

<verification>
`cargo test -p trackly-app report_movements -- --test-threads=1` green, covering AND-semantics subtree
filtering and the soft-deleted marker.
</verification>

<success_criteria>
`list_movements`/`query_movements_inner`/`row_field` are fully implemented and unit-tested; Plan 40-12
can wire the adapter layer without touching any query logic.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-11-SUMMARY.md` when done
</output>
