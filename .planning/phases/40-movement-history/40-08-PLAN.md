---
phase: 40-movement-history
plan: 08
type: execute
wave: 3
depends_on: ["40-04", "40-05"]
files_modified:
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/tests/place_movements_write_sites_cartridges.rs
autonomous: true
requirements: [HST-01]
must_haves:
  truths:
    - "Cartridge manual place edits (update) create exactly one movement row with source='manual' (D-27)"
    - "Cartridge transitions (Install/ReturnToStock/ToRefill/FromRefill) that change place create exactly one movement row per entity, with a source reflecting D-05's \"meaningful reason\" requirement, not a generic 'manual' label"
    - "The nested auto-return branch inside transition_in_tx creates a SECOND, separate movement row for the auto-returned cartridge — this is the single easiest write site to miss in the whole phase (Pitfall 3)"
  artifacts:
    - path: "crates/trackly-app/tests/place_movements_write_sites_cartridges.rs"
      provides: "place_movements_cartridge_transition, covering both the main mutation and the nested auto-return second row"
  key_links:
    - from: "cartridges_sqlite.rs::transition_in_tx"
      to: "SqlitePlaceMovementsRepository::record_movement_if_applicable"
      via: "called twice — once for the main mutation, once for the nested auto-return branch"
      pattern: "record_movement_if_applicable"
---

<objective>
Wire BOTH cartridge write sites (manual `update` and `transition`/`transition_in_tx`, including its
easy-to-miss nested auto-return branch) to Plan 40-05's `record_movement_if_applicable` helper, using
the caller threading and before-fetch built in Plan 40-04. This plan is where Pitfall 3 (RESEARCH.md's
"second, easy-to-miss write site") gets its dedicated test.

Purpose: make HST-01/D-05 real for cartridge operations — installs/returns/refills only produce a
movement row when the operation ACTUALLY changes `place_id` (e.g. installing into a printer at a
different place), never as a side effect of the status transition itself.
Output: `cartridge_service::update` and `cartridges_sqlite::transition_in_tx` (both call sites) record
movements; a dedicated test proving the nested auto-return produces its own separate row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
@.planning/phases/40-movement-history/40-VALIDATION.md
</context>

<interfaces>
From Plan 40-04: `cartridge_service::update` now has a before-fetch (`SELECT place_id FROM cartridges
WHERE id = ?1`) and `caller: &Identity`/`user_id` in scope. `transition_in_tx` now takes a trailing
`caller_user_id: Option<i64>` parameter and has `current.place_id` (before)/`new_place_id` (after) as
local variables for the main mutation, and `prev_current.place_id` (before)/`resolved_place_id` (after)
for the nested auto-return branch — both pairs already exist as local variables per RESEARCH, no new
diff logic needed, only the movement-insert call.
From Plan 40-05: `record_movement_if_applicable(...)` signature as documented in Plan 40-07's
`<interfaces>` block.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire record_movement_if_applicable into cartridge_service::update</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/cartridge_service.rs (post-Plan-40-04 `update` body — the new
    before-fetch and `user_id` are already there; locate exactly where to insert the movement-record
    call, after the `UPDATE cartridges` execute succeeds and before `tx.commit()`)
  </read_first>
  <behavior>
    - Editing a cartridge's place from A to B via `update` produces exactly one `place_movements` row
      with `entity_type='cartridge'`, `source='manual'`, correct `from`/`to`/`user_id`
    - Editing only `notes` with `place_id` unchanged produces zero rows
  </behavior>
  <action>
    Inside `cartridge_service.rs::update`'s writer closure, after the `UPDATE cartridges ...` succeeds
    (the existing `affected == 0` optimistic-lock branch already returns early on failure, so this call
    only runs on success) and before `tx.commit()`, call
    `self.place_movements_repo.record_movement_if_applicable(&tx, self.places_repo.as_ref(),
    MovementEntityKind::Cartridge, id, before_place_id, place_id, MovementSource::Manual, None, None,
    user_id, now)?;` using the before-fetch variable added in Plan 40-04 and the `place_id` parameter
    already passed into `update`. Follow the same `Arc`-clone-into-closure wiring pattern established
    for `device_service.rs` in Plan 40-07 (grep that file's finished diff for the exact field-naming
    convention if `CartridgeService` needs a new `place_movements_repo`/`places_repo` field added).
  </action>
  <acceptance_criteria>
    - `grep -n "record_movement_if_applicable" crates/trackly-app/src/services/cartridge_service.rs`
      returns at least one line inside `update`
    - `cargo build -p trackly-app` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app cartridge_service::update -- --test-threads=1</automated>
  </verify>
  <done>Manual cartridge place edits record a movement row correctly; no-op edits do not.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire record_movement_if_applicable into transition_in_tx — BOTH call sites</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/cartridges_sqlite.rs lines 458-689 (full `transition_in_tx` — main
    mutation around line 539 with `current`/`new_place_id` locals, nested auto-return around line
    568-688 with `prev_current`/`resolved_place_id` locals and its own `audit_repo.insert` call at
    ~674-687 that Plan 40-04 already updated to use `caller_user_id`), crates/trackly-infra/src/repos/place_movements_sqlite.rs
    (Plan 40-05's repo — note this repo function lives in `trackly-infra`, same crate as
    `place_movements_sqlite.rs`, so no cross-crate trait-object indirection is needed here; a plain
    `SqlitePlaceMovementsRepository` value/reference can be constructed or injected directly, mirroring
    how `let audit_repo = SqliteAuditLogRepository;` is already constructed inline inside this exact
    function)
  </read_first>
  <behavior>
    - A plain Install (no printer occupied by another cartridge) that changes place from A to B produces
      exactly ONE movement row for the installed cartridge
    - An Install into a printer that already holds another cartridge "В работе" produces TWO movement
      rows in the SAME transaction: one for the newly-installed cartridge (A→B) and a SEPARATE one for
      the auto-returned previous cartridge (its own before place → its own resolved stock/refill place)
      — Pitfall 3's exact scenario
    - A ReturnToStock/ToRefill/FromRefill that does not actually change `place_id` (e.g. staying in the
      same storage room) produces zero rows for that entity
  </behavior>
  <action>
    Immediately after the main `UPDATE cartridges SET status_id=?1, ... WHERE id=?7 AND version=?8`
    succeeds (the `affected == 0` optimistic-lock check already guards this), construct
    `let place_movements_repo = crate::repos::place_movements_sqlite::SqlitePlaceMovementsRepository;`
    (mirroring the existing inline `let audit_repo = SqliteAuditLogRepository;` construction pattern a
    few lines below) and call `place_movements_repo.record_movement_if_applicable(tx,
    &self.place_repo /* or however this repo already references a PlaceRepository impl — check the
    struct's existing fields */, MovementEntityKind::Cartridge, cartridge_id, current.place_id,
    new_place_id, MovementSource::Manual, None, None, caller_user_id, now_utc)?;` — note D-05 says
    cartridge lifecycle operations are NOT WebSocket-worthy "movement events" on their own, but when they
    DO change place, the recorded `source` should still read as `'manual'` (the four-value enum has no
    dedicated "cartridge-op" token; D-07 confirms exactly four values: manual/act/map/workstation, and a
    cartridge-driven place change is a manual-category action from the history's point of view — do NOT
    invent a fifth source value). Then, inside the nested auto-return `if let Some((prev_id,
    prev_version)) = previous { ... }` block, immediately after its own `UPDATE cartridges SET
    status_id=1, ...` succeeds and before its `audit_repo.insert(...)` call, add a SECOND call:
    `place_movements_repo.record_movement_if_applicable(tx, &self.place_repo, MovementEntityKind::Cartridge,
    prev_id, prev_current.place_id, resolved_place_id, MovementSource::Manual, None, None,
    caller_user_id, now_utc)?;` — using `prev_id` (NOT `cartridge_id`) as the entity, per Pitfall 3's
    explicit warning that this is a second, separate entity.
  </action>
  <acceptance_criteria>
    - `grep -c "record_movement_if_applicable" crates/trackly-infra/src/repos/cartridges_sqlite.rs`
      returns exactly 2 (main mutation call site + nested auto-return call site)
    - The nested auto-return call site uses `prev_id` as its `entity_id` argument, not `cartridge_id`
      (verify by reading the surrounding 5 lines of context around each call)
    - `cargo build -p trackly-infra` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app place_movements_cartridge_transition -- --test-threads=1</automated>
  </verify>
  <done>Both call sites record movements correctly; the nested auto-return scenario produces two distinct rows for two distinct entities in one transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 test file — cartridge family write-site coverage</name>
  <files>crates/trackly-app/tests/place_movements_write_sites_cartridges.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/cartridges_sqlite.rs lines 568-689 (nested auto-return trigger
    condition — a second cartridge already "В работе" in the SAME target printer, `status_id = 2`),
    .planning/phases/40-movement-history/40-VALIDATION.md (exact test function name:
    `place_movements_cartridge_transition`)
  </read_first>
  <behavior>
    - `place_movements_cartridge_transition`: seed two places (stock room, printer's place), install a
      cartridge into a printer at a different place than its current stock location — assert exactly one
      `place_movements` row for that cartridge
    - Same test extends: install a SECOND cartridge into the SAME printer (triggering the nested
      auto-return of the first) — assert a SECOND `place_movements` row now exists for the FIRST
      cartridge (the auto-returned one), with `entity_id` matching the first cartridge's id, not the
      second's
  </behavior>
  <action>
    Create `crates/trackly-app/tests/place_movements_write_sites_cartridges.rs` with test function
    `place_movements_cartridge_transition` implementing both scenarios in `<behavior>` above as two
    assertions within one test (or two separate `#[tokio::test]` functions both matching the
    `place_movements_cartridge_transition` substring, e.g.
    `place_movements_cartridge_transition_install` and
    `place_movements_cartridge_transition_nested_auto_return`).
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app place_movements_cartridge_transition -- --test-threads=1` passes
    - Test explicitly asserts the auto-returned cartridge's row has `entity_id` equal to the FIRST
      cartridge's id, catching the exact regression named in Pitfall 3's "Warning signs"
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app place_movements_write_sites_cartridges -- --test-threads=1</automated>
  </verify>
  <done>Both scenarios pass, proving the nested auto-return is not silently dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cartridge_service/cartridges_sqlite → place_movements | Internal — `source` is always hard-coded `MovementSource::Manual` by the write site, never client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-17 | Elevation of Privilege / Information Disclosure | Nested auto-return's movement row silently missing (Pitfall 3 recurrence) would under-report a real place change | mitigate | Task 3's test explicitly asserts on the SECOND row's `entity_id`, catching exactly this regression class |
| T-40-18 | Spoofing | Client attempting to influence `source` via the transition payload | mitigate | `CartridgeTransitionOp` has no `source` field; the server hard-codes `MovementSource::Manual` for both call sites |
</threat_model>

<verification>
`cargo test -p trackly-app place_movements_write_sites_cartridges -- --test-threads=1` green.
</verification>

<success_criteria>
Cartridge manual edits and transitions (including the nested auto-return) all correctly record
movements in `place_movements`, with the auto-return case producing two distinct, correctly-attributed
rows.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-08-SUMMARY.md` when done
</output>
