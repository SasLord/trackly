---
phase: 40-movement-history
plan: 12
type: execute
wave: 3
depends_on: ["40-11"]
files_modified:
  - crates/trackly-app/src/tauri_cmds/reports.rs
  - crates/trackly-app/src/http/reports.rs
  - crates/trackly-app/tests/report_movements.rs
autonomous: true
requirements: [HST-04]
must_haves:
  truths:
    - "The movements report gates on Action::ReadPlaces, NOT Action::ReadData like the other 12 reports — this is the single highest-risk copy-paste spot in the whole report clone (D-12, PATTERNS.md's own explicit warning)"
    - "columns_for/column_labels_for/report_display_name all gain a matching movements arm, index-aligned, extending the existing regression test rather than duplicating it (D-23 columns: Дата/Предмет/Тип/Откуда/Куда/Кем/Причина)"
    - "CSV and PDF export work for the movements report via the existing export_csv/export_pdf pipeline with zero new export code (D-26)"
  artifacts:
    - path: "crates/trackly-app/src/tauri_cmds/reports.rs"
      provides: "\"movements\" arm in columns_for/column_labels_for/report_display_name + build_reports_list_movements gated by Action::ReadPlaces"
    - path: "crates/trackly-app/src/http/reports.rs"
      provides: "handler_list_movements"
    - path: "crates/trackly-app/tests/report_movements.rs"
      provides: "report_movements_place_filters, report_movements_deleted_item_marker, CSV/PDF export smoke tests"
  key_links:
    - from: "tauri_cmds/reports.rs::build_reports_list_movements"
      to: "crates/trackly-core/src/auth.rs::Action::ReadPlaces"
      via: "authorize(caller, &Action::ReadPlaces)? — NOT Action::ReadData"
      pattern: "build_reports_list_movements.*ReadPlaces"
---

<objective>
Wire the movements report's adapter layer: the three parallel `columns_for`/`column_labels_for`/
`report_display_name` matches (extending the existing index-alignment regression test), the
`build_reports_list_movements` Tauri command (gated by `Action::ReadPlaces`, diverging deliberately
from the other 12 reports' `Action::ReadData`), and `handler_list_movements` on the HTTP side. This
plan is sequenced strictly after Plan 40-11 (the query layer) so the adapter work is a mechanical,
low-risk wiring pass — the ONE risky decision (which `Action` to gate on) is called out explicitly in
every task.

Purpose: complete HST-04's backend surface so Plan 40-18 (report UI) has a working, correctly-gated
endpoint to wire the filter bar and table into.
Output: `"movements"` report type fully registered on both transports with the correct gate; CSV/PDF
export proven to work with zero new export code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-movement-history/40-CONTEXT.md
@.planning/phases/40-movement-history/40-RESEARCH.md
@.planning/phases/40-movement-history/40-PATTERNS.md
</context>

<interfaces>
Existing `columns_for`/`column_labels_for`/`report_display_name` (`tauri_cmds/reports.rs:20-101`, full
match arms already read):
```rust
fn columns_for(report_type: &str) -> Vec<&'static str> {
    match report_type {
        "device_acts" | "device_returns" => vec!["number", "device_name", "giver_name", "receiver_name", "place_path"],
        // ADD: "movements" => vec!["created_at_utc", "device_name", "entity_type_label", "from_place_path", "place_path", "actor_name", "reason"],
        _ => vec!["id"],
    }
}
fn column_labels_for(report_type: &str) -> Vec<&'static str> {
    match report_type {
        "device_acts" | "device_returns" => vec!["Номер", "Устройства", "Сдал", "Принял", "Место"],
        // ADD: "movements" => vec!["Дата", "Предмет", "Тип", "Откуда", "Куда", "Кем", "Причина"],  // D-23, index-aligned with columns_for
        _ => vec!["ID"],
    }
}
```
Existing regression test to EXTEND, not duplicate (`tauri_cmds/reports.rs:~609`,
`column_labels_for_is_index_aligned_with_columns_for`) — add `"movements"` to its report-type list.
Gate divergence — every existing report uses `Action::ReadData`:
```rust
pub async fn build_reports_list_device_acts(...) -> Result<ReportResponse, AppError> {
    authorize(caller, &Action::ReadData)?;
    // ...
}
```
The movements report MUST use `Action::ReadPlaces` instead (D-12) — this is PATTERNS.md's own explicit
"single most copy-paste-error-prone spot" warning; do not let an editor's find-replace propagate
`Action::ReadData` into this one new arm.
`handler_list_device_acts` shape to clone exactly for `handler_list_movements`:
```rust
pub async fn handler_list_device_acts(State(ctx), session, Json(p)) -> Result<Json<ReportResponse>, AppErrorResponse> {
    let identity = session_identity(&session).await.map_err(AppErrorResponse::from)?;
    Ok(Json(build_reports_list_device_acts(&ctx, &identity, p.filter, p.period).await.map_err(AppErrorResponse::from)?))
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: columns_for/column_labels_for/report_display_name + build_reports_list_movements (ReadPlaces gate)</name>
  <files>crates/trackly-app/src/tauri_cmds/reports.rs</files>
  <read_first>
    crates/trackly-app/src/tauri_cmds/reports.rs lines 20-101 (all three matches, full read), lines
    107-260 (the block of `build_reports_list_*` functions — read at least 2 full examples to see the
    exact `authorize`+delegate shape), line ~609 (`column_labels_for_is_index_aligned_with_columns_for`
    — full test body, to extend its report-type list)
  </read_first>
  <behavior>
    - `columns_for("movements")` returns exactly 7 entries; `column_labels_for("movements")` returns
      exactly 7 entries in the SAME order (Дата·Предмет·Тип·Откуда·Куда·Кем·Причина, D-23)
    - `report_display_name("movements")` returns a non-generic, Russian label (e.g. "Перемещения")
    - `build_reports_list_movements` called with a Manager identity succeeds; called with an Employee
      identity returns `Err(AppError::Forbidden)` — proving the `Action::ReadPlaces` gate, distinct from
      every other report type's `Action::ReadData`
  </behavior>
  <action>
    Add `"movements" => vec!["created_at_utc", "device_name", "entity_type_label", "from_place_path",
    "place_path", "actor_name", "reason"]` to `columns_for`'s match (column KEYS must correspond to
    `ReportRow`/`row_field` fields from Plan 40-11 — if `entity_type_label` was not added to `ReportRow`
    in Plan 40-11, add it now as a small follow-up `pub entity_type_label: Option<String>` field on
    `ReportRow` in `dto/reports.rs` plus its own `row_field` arm delegating to
    `MovementEntityKind::from_str_lenient(&row_entity_type).map(|k| k.label_ru())`). Add `"movements" =>
    vec!["Дата", "Предмет", "Тип", "Откуда", "Куда", "Кем", "Причина"]` to `column_labels_for`'s match,
    index-aligned with the 7 keys above. Add `"movements" => "Перемещения"` to `report_display_name`'s
    match. Extend `column_labels_for_is_index_aligned_with_columns_for`'s report-type list to include
    `"movements"` (do NOT write a second, duplicate test function). Add `pub async fn
    build_reports_list_movements(ctx: &AppCtx, caller: &Identity, filter: ReportFilter, period:
    PeriodDto) -> Result<ReportResponse, AppError>` that calls `authorize(caller, &Action::ReadPlaces)?`
    — NOT `Action::ReadData` — then delegates to `ctx.reports.list_movements(filter, period).await`.
  </action>
  <acceptance_criteria>
    - `grep -n "\"movements\" =>" crates/trackly-app/src/tauri_cmds/reports.rs` returns at least 3 lines
      (one per match: columns_for, column_labels_for, report_display_name)
    - `grep -A2 "fn build_reports_list_movements" crates/trackly-app/src/tauri_cmds/reports.rs | grep -c "Action::ReadPlaces"`
      equals 1
    - `grep -A2 "fn build_reports_list_movements" crates/trackly-app/src/tauri_cmds/reports.rs | grep -c "Action::ReadData"`
      equals 0 (the exact copy-paste error this task must NOT make)
    - `cargo test -p trackly-app column_labels_for_is_index_aligned_with_columns_for -- --test-threads=1` passes
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app column_labels_for_is_index_aligned_with_columns_for -- --test-threads=1</automated>
  </verify>
  <done>All three matches include a correctly index-aligned "movements" arm; the gate is provably Action::ReadPlaces, not Action::ReadData.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handler_list_movements (HTTP) + report_movements.rs test suite</name>
  <files>crates/trackly-app/src/http/reports.rs, crates/trackly-app/tests/report_movements.rs</files>
  <read_first>
    crates/trackly-app/src/http/reports.rs (handler_list_device_acts — full function, exact clone
    target), crates/trackly-app/src/services/report_service.rs (export_csv/export_pdf — confirm these
    are genuinely report-type-agnostic and require zero changes, per RESEARCH's claim, by reading their
    full bodies once)
  </read_first>
  <behavior>
    - HTTP POST to the movements report endpoint with a Manager session cookie returns 200 with a valid
      `ReportResponse` body
    - Same request with an Employee session cookie returns 403
    - Calling `export_csv`/`export_pdf` with `report_type: "movements"` produces valid output containing
      the 7 D-23 column labels as headers, using the SAME existing functions as every other report
      (zero new export code)
  </behavior>
  <action>
    Add `pub async fn handler_list_movements(State(ctx): State<AppCtx>, session: Session, Json(p):
    Json<ListMovementsPayload>) -> Result<Json<ReportResponse>, AppErrorResponse>` to `http/reports.rs`,
    cloning `handler_list_device_acts`'s exact shape (resolve `session_identity`, delegate to
    `build_reports_list_movements(&ctx, &identity, p.filter, p.period)`, wrap in `Json`/map error).
    Register the route in this file's router-wiring section (mirror an existing report route's path
    convention, e.g. `/api/v1/reports_list_movements`). Create
    `crates/trackly-app/tests/report_movements.rs` with test `report_movements_place_filters` (seeds
    movements across at least 3 from/to place combinations, calls `list_movements` or the HTTP endpoint
    with both `from_place_id`/`to_place_id` set, asserts AND-filtered subtree-inclusive results per
    D-24's own "со склада в Здание Б" example), `report_movements_deleted_item_marker` (seeds a
    movement, soft-deletes the underlying device, asserts the report row still appears with the D-25
    deleted marker populated), and a CSV/PDF export smoke test calling `export_csv`/`export_pdf` with
    `report_type: "movements"` and asserting the 7 column headers appear in the output.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app report_movements_place_filters -- --test-threads=1` passes
    - `cargo test -p trackly-app report_movements_deleted_item_marker -- --test-threads=1` passes
    - `grep -n "fn export_csv\|fn export_pdf" crates/trackly-app/src/services/report_service.rs` shows
      these functions were NOT modified by this plan (`files_modified` frontmatter for this plan does not
      list `report_service.rs` — confirms zero new export code, D-26)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app report_movements -- --test-threads=1</automated>
  </verify>
  <done>HTTP transport works identically to Tauri; CSV/PDF export produces correct output with zero new export-pipeline code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Employee session → movements report endpoint (both transports) | Must be rejected — this is the single spot in the whole 13-report family where the gate action differs from the other 12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-24 | Elevation of Privilege | Copy-paste of `Action::ReadData` instead of `Action::ReadPlaces` (PATTERNS.md's own explicit named risk) | mitigate | Task 1's acceptance criteria greps specifically for the absence of `Action::ReadData` and the presence of `Action::ReadPlaces` on `build_reports_list_movements`; Plan 40-14 adds the role-matrix regression test on top |
| T-40-25 | Elevation of Privilege | Transport asymmetry — HTTP handler missing the identity resolve or the gate | mitigate | `handler_list_movements` routes through the SAME `build_reports_list_movements` as the Tauri command — the gate lives once, in the shared function |
</threat_model>

<verification>
`cargo test -p trackly-app report_movements -- --test-threads=1` green;
`cargo test -p trackly-app column_labels_for_is_index_aligned_with_columns_for -- --test-threads=1` green.
</verification>

<success_criteria>
The movements report is registered on both transports, gated correctly by `Action::ReadPlaces`, and
exports via the existing CSV/PDF pipeline with zero new export code.
</success_criteria>

<output>
Create `.planning/phases/40-movement-history/40-12-SUMMARY.md` when done
</output>
