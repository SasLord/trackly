---
phase: 03-pdf
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - crates/trackly-core/src/domain/acts.rs
  - crates/trackly-app/src/dto/act.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/tauri_cmds/acts.rs
  - crates/trackly-app/src/http/acts.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-infra/src/repos/acts_sqlite.rs
  - crates/trackly-app/tests/acts_search.rs
  - crates/trackly-app/tests/acts_e2e_smoke.rs
  - ui/src/lib/api/acts.ts
  - ui/src/features/acts/ActsSearchAndTabs.svelte
  - ui/src/features/acts/ActsPage.svelte
  - ui/src/features/acts/DocumentAcceptanceModal.svelte
  - ui/src/features/devices/DeviceContextMenu.svelte
  - ui/src/features/devices/DevicesPage.svelte
autonomous: true
requirements:
  - ACT-04
  - DEV-14
threat_model_severity: medium
tags:
  - phase-3
  - search
  - acceptance-doc
  - polish
  - vertical-slice

must_haves:
  truths:
    - "Поиск по актам через FTS работает: по номеру акта (LIKE), ФИО Сдал/Принял (LIKE), наименованию устройства (FTS5 через devices_fts JOIN на act_items.device_id). Возвращает merged + ranked results (ACT-04)"
    - "Search input в ActsSearchAndTabs с debounce 250ms триггерит acts_search command + обновляет list"
    - "DEV-14 UI: контекстное меню устройства имеет пункт «Печать документа приёма» (после «Редактировать», перед «Удалить»); открывает intermediate-модал с полями «Кто передал», «Кто принял», Дата → submit → PdfPreviewModal mode='acceptance'"
    - "Backend devices_render_acceptance_pdf (готов в plan 04) теперь вызывается через UI; добавлен smoke-test"
    - "E2E smoke: создать handover → return → archive → undo handover (через service.delete) → все devices в исходном состоянии; счётчики корректны"
  artifacts:
    - path: "crates/trackly-infra/src/repos/acts_sqlite.rs (extended)"
      provides: "search_acts(conn, fts_query: &str, plain_query: &str, filter, page) -> (Vec<ActRow>, u64) — UNION CTE acts text + devices_fts JOIN per RESEARCH §FTS search"
    - path: "crates/trackly-app/src/services/act_service.rs (extended)"
      provides: "search(query: String, filter: ActFilter, pagination: Pagination) -> ActListResponse"
    - path: "crates/trackly-app/tests/acts_search.rs"
      provides: "search_by_act_number + search_by_giver_name + search_by_device_name + search_returns_only_handover_when_filter + search_empty_query_falls_back_to_list"
    - path: "ui/src/features/acts/DocumentAcceptanceModal.svelte"
      provides: "intermediate modal — Кто передал / Кто принял / Дата → submit → triggers PdfPreviewModal mode=acceptance"
    - path: "ui/src/features/devices/DeviceContextMenu.svelte (extended)"
      provides: "новый пункт «Печать документа приёма»"
    - path: "crates/trackly-app/tests/acts_e2e_smoke.rs"
      provides: "end-to-end: handover → return → delete → state restored"
  key_links:
    - from: "ui/src/features/acts/ActsSearchAndTabs.svelte"
      to: "ui/src/lib/api/acts.ts::acts.search"
      via: "debounce 250ms → apiCall<ActListResponse>"
    - from: "ui/src/features/devices/DeviceContextMenu.svelte"
      to: "ui/src/features/acts/DocumentAcceptanceModal.svelte"
      via: "context-menu callback → modal open"
    - from: "ui/src/features/acts/DocumentAcceptanceModal.svelte"
      to: "ui/src/features/acts/PdfPreviewModal.svelte"
      via: "после submit → switch to preview mode='acceptance' с deviceId+giver+receiver+date"
---

## Phase Goal

**As a** сотрудник склада, **I want to** (а) искать акты по номеру/ФИО/наименованию устройства через единый search-инпут с debounce и (б) печатать «Документ приёма товара на склад» прямо из списка устройств через контекстное меню, **so that** оставшиеся требования Phase 3 (ACT-04 поиск + DEV-14 UI документа приёма) закрыты и фаза готова к /gsd-verify-work.

> **Vertical slice (MVP mode):** этот план достроит две оставшиеся возможности — поиск актов и UI-точку для DEV-14 — и завершит фазу зелёным e2e smoke тестом.

<objective>
Acts search (FTS + LIKE merge) + DEV-14 UI flow (intermediate-modal → preview-modal reuse) + e2e smoke. После него Phase 3 закрывает все 16 требований (ACT-01..14 + DEV-14/15) и проходит /gsd-verify-work без gaps.

Purpose: Закрыть ACT-04 (поиск) и DEV-14 (UI документ приёма + использование backend из plan 04).

Output: Search-input в ActsSearchAndTabs работает (поиск по номеру/ФИО/устройству); правый клик / kebab на устройстве в DevicesPage → «Печать документа приёма» → modal с полями «Кто передал/Кто принял/Дата» → submit → PdfPreviewModal с готовым PDF документа приёма; e2e smoke зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-pdf/03-CONTEXT.md
@.planning/phases/03-pdf/03-RESEARCH.md
@.planning/phases/03-pdf/03-PATTERNS.md
@.planning/phases/03-pdf/03-UI-SPEC.md
@.planning/phases/03-pdf/03-VALIDATION.md
@.planning/phases/03-pdf/03-04-SUMMARY.md
@migrations/V012__indexes_and_fts.sql
@migrations/V013__devices_fts_triggers.sql
@crates/trackly-infra/src/repos/devices_sqlite.rs
@crates/trackly-app/src/services/device_service.rs
@crates/trackly-app/src/services/act_service.rs
@crates/trackly-app/src/dto/act.rs
@crates/trackly-app/tests/devices_search.rs
@crates/trackly-app/tests/acts_crud.rs
@ui/src/features/devices/DeviceContextMenu.svelte
@ui/src/features/devices/DevicesPage.svelte
@ui/src/features/acts/PdfPreviewModal.svelte
@ui/src/features/acts/ActsSearchAndTabs.svelte
@ui/src/lib/api/acts.ts

<interfaces>
ActRepository extension (Optional — может остаться чистая function вне trait):
- pub fn search(&self, conn: &Connection, plain_query: &str, fts_query: &str, filter: &ActFilter, page: &Pagination) -> Result<(Vec<ActRow>, u64), AppError>

acts_sqlite.rs::search_acts: per RESEARCH §«FTS search across acts joining act_items + devices_fts» — WITH act_text_hits AS (LIKE по acts.number/giver_name/receiver_name) + device_text_hits AS (FTS5 MATCH через devices_fts JOIN act_items) + outer SELECT по UNION + filter + LIMIT/OFFSET + COUNT для total. Per Open Q3 — выбран простой LIKE+UNION путь (acts_fts создание отложено до Phase 7).

ActService::search:
- pub async fn search(&self, query: String, filter: ActFilter, pagination: Pagination) -> Result<ActListResponse, AppError>
  Body:
  1. trimmed = query.trim();
  2. if trimmed.is_empty() → return self.list(filter, pagination).await   — fallback на обычный list
  3. plain_query = format!("%{}%", trimmed.replace('%', r"\%").replace('_', r"\_"))   — escape для LIKE
  4. fts_query = build_fts_query(trimmed)   — переиспользуется helper из Phase 2 D-Search-01
  5. acts_repo.search(...) → Vec<ActRow> + total → map to Vec<ActDto> + ActListResponse

Tauri command:
- pub async fn build_acts_search(ctx: &AppCtx, query: String, filter: ActFilter, pagination: Pagination) -> Result<ActListResponse, AppError>
  ctx.acts.search(query, filter, pagination).await
- #[tauri::command] async fn acts_search(...) — thin wrapper

UI api:
- ui/src/lib/api/acts.ts: уже имеет `search` (из plan 02 skeleton); проверить — если возвращает proper ActListResponse, ничего не меняем; если method отсутствует — добавить.

UI ActsSearchAndTabs delta:
- debounce 250ms через `let searchTimer: ReturnType<typeof setTimeout> | null = null; function handleInput(e) { if (searchTimer) clearTimeout(searchTimer); searchTimer = setTimeout(() => { props.onSearchChange(searchInput); }, 250); }`
- props.onSearchChange triggers refresh в ActsPage.

UI DocumentAcceptanceModal (новый):
- Props: open, device: DeviceDto (нужен только id + name; берётся из DeviceContextMenu trigger), onClose, onSuccess(deviceId, giver, receiver, dateUtc).
- State runes: giverName = $state(""), receiverName = $state(""), dateUtc = $state(now()).
- Validation: giver+receiver обязательны; submit blocked если пустые.
- Submit: вызывает onSuccess(...) — в parent (DevicesPage) свитчит из intermediate в PdfPreviewModal mode='acceptance' с этими значениями.
- НЕ зовёт acts.renderAcceptancePdf — это делает PdfPreviewModal сам.
- Copy per UI-SPEC §«Document приёма entry point» lines 305-316.

UI PdfPreviewModal extension:
- Расширить props: `mode: 'handover' | 'acceptance'`, `acceptancePayload?: { deviceId, giverName, receiverName, dateUtc }`.
- При mode='acceptance': в $effect loadPdf использовать `acts.renderAcceptancePdf(payload.deviceId, payload.giverName, payload.receiverName, payload.dateUtc)` вместо `acts.renderPdf`.
- Header title и saved filename различаются per UI-SPEC.

UI DeviceContextMenu extension:
- Добавить пункт «Печать документа приёма» — между «Редактировать» и «Удалить», с separator. Trigger: `onPrintAcceptance(device)` callback.

UI DevicesPage delta:
- State: `acceptanceModalOpen = $state(false)`, `acceptanceDevice = $state<DeviceDto|null>(null)`, `acceptancePreviewOpen = $state(false)`, `acceptancePayload = $state<{deviceId,giverName,receiverName,dateUtc}|null>(null)`.
- onPrintAcceptance callback opens intermediate modal.
- onAcceptanceSubmit closes intermediate, opens preview modal с payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acts search backend + integration tests + UI search wiring</name>
  <files>
    crates/trackly-infra/src/repos/acts_sqlite.rs,
    crates/trackly-app/src/services/act_service.rs,
    crates/trackly-app/src/tauri_cmds/acts.rs,
    crates/trackly-app/src/http/acts.rs,
    crates/trackly-app/src/specta_export.rs,
    crates/trackly-app/src/dto/act.rs,
    crates/trackly-app/tests/acts_search.rs,
    ui/src/lib/api/acts.ts,
    ui/src/features/acts/ActsSearchAndTabs.svelte,
    ui/src/features/acts/ActsPage.svelte
  </files>
  <read_first>
    - .planning/phases/03-pdf/03-RESEARCH.md §«FTS search across acts joining act_items + devices_fts» — точное SQL CTE + UNION + параметры
    - .planning/phases/03-pdf/03-RESEARCH.md §Open Questions Q3 — выбран простой LIKE+UNION путь
    - .planning/phases/03-pdf/03-PATTERNS.md §B «build_fts_query 1-к-1 из Phase 2» (lines 290-300) + §S-1/S-2/S-3 cross-cutting
    - .planning/phases/02-ui/02-04-SUMMARY.md — Phase 2 FTS5 build_fts_query pattern (или существующий helper в device_service.rs)
    - crates/trackly-infra/src/repos/devices_sqlite.rs — текущая form build_fts_query + search_fts pattern
    - crates/trackly-app/src/services/device_service.rs — текущий search (input → trim → fts_query → repo.search_fts)
    - crates/trackly-app/tests/devices_search.rs — pattern integration test FTS5
    - migrations/V012__indexes_and_fts.sql + V013__devices_fts_triggers.sql — schema devices_fts
    - .planning/phases/03-pdf/03-UI-SPEC.md §«Search behaviour (Phase 2 D-Search-01 reuse)» (lines 689-694)
    - ui/src/features/acts/ActsSearchAndTabs.svelte (plan 02 skeleton) — где добавить debounce
    - ui/src/features/acts/ActsPage.svelte (plan 02 skeleton) — где принимать onSearchChange callback и switching между list vs search
  </read_first>
  <behavior>
    - Test 1 (`acts_search.rs::search_by_act_number`): seed 5 handovers с numbers 1..5 → search("3") → returns 1 result с number=3.
    - Test 2 (`search_by_giver_name`): seed handovers с giver «Иванов», «Петров», «Сидоров» → search("Иван") → returns 1 (только «Иванов»).
    - Test 3 (`search_by_device_name`): seed device «Ноутбук Lenovo» → handover с этим device → search("Lenovo") → returns 1 handover.
    - Test 4 (`search_filters_by_tab`): search("Иван") с filter.act_type=Handover → возвращает только handover, не return.
    - Test 5 (`search_empty_query_falls_back_to_list`): search("") → equivalent to list (5 results).
    - Test 6 (`search_handles_special_chars`): search("О'Брайен") (apostrophe) → no panic, returns 0 results (нет таких в seed).
    - Test 7 (UI svelte-check + lint).
  </behavior>
  <action>
    1. **Расширить acts_sqlite.rs** методом `search_acts`:
       - Pattern точно по RESEARCH §«FTS search across acts»:
         ```
         WITH act_text_hits AS (
           SELECT a.id FROM acts a
            WHERE a.deleted_at_utc IS NULL
              AND (CAST(a.number AS TEXT) LIKE ?1
                   OR a.giver_name LIKE ?1
                   OR a.receiver_name LIKE ?1)
         ),
         device_text_hits AS (
           SELECT DISTINCT ai.act_id AS id
             FROM act_items ai
             JOIN devices_fts f ON f.rowid = ai.device_id
            WHERE devices_fts MATCH ?2
         )
         SELECT a.*, l.name AS location_name, p.number AS parent_number, ...sibling_return_count
           FROM acts a
           LEFT JOIN ... (same as SELECT_ACTS const)
          WHERE a.id IN (SELECT id FROM act_text_hits UNION SELECT id FROM device_text_hits)
            AND a.deleted_at_utc IS NULL
            AND (?3 IS NULL OR a.act_type = ?3)
            AND (?4 IS NULL OR a.archived = ?4)
          ORDER BY a.created_at_utc DESC
          LIMIT ?5 OFFSET ?6
         ```
       - Параметры: ?1 = plain LIKE pattern `%term%`, ?2 = build_fts_query(term) для devices_fts MATCH, ?3 = act_type filter (Option<String>), ?4 = archived filter (Option<i64>), ?5 = limit, ?6 = offset.
       - Также написать parallel COUNT query для total (same WHERE без LIMIT/OFFSET).
       - `pub fn search_acts(&self, conn: &Connection, plain_query: &str, fts_query: &str, filter: &ActFilter, page: &Pagination) -> Result<(Vec<ActRow>, u64), AppError>`.
       - **Edge:** если build_fts_query вернул empty string (term только из спец-символов) — fts MATCH "" panics; fallback на `OR 1=0` или skip device_text_hits CTE (if fts_query.is_empty() → SELECT id FROM acts WHERE 0 в device_text_hits).
    2. **build_fts_query helper** — переиспользовать из Phase 2 (есть в device_service.rs или в utils). Если нет общего helper в trackly-app — заимствовать pattern PATTERNS §B lines 290-300:
       ```
       fn build_fts_query(user_input: &str) -> String {
         user_input.split_whitespace()
           .map(|t| t.replace('\0', "").replace('"', "\"\""))
           .filter(|t| !t.is_empty())
           .map(|t| format!("\"{t}\"*"))
           .collect::<Vec<_>>().join(" ")
       }
       ```
       Поместить как `pub(crate) fn build_fts_query` в `crates/trackly-app/src/services/act_service.rs` (или в shared utility module если есть). **Если уже импортируется из device_service.rs — переиспользовать.**
    3. **Расширить ActService::search** (если plan 02 stub-метод существует — заменить на полную реализацию; если нет — добавить):
       - Тримать query.
       - Empty → fallback на `self.list(filter, pagination).await`.
       - else: plain_query escape для LIKE → `format!("%{}%", trimmed.replace('%', "\\%").replace('_', "\\_"))`. fts_query через build_fts_query.
       - `acts_repo.search_acts(...)` через spawn_blocking + readers.
       - Map ActRow → ActDto (включая items load, как в `get` / `list`).
    4. **build_acts_search** + tauri wrapper + axum POST /api/v1/acts_search (если уже есть из plan 02 — проверить и обновить body).
    5. **Регистрировать в specta_export** (вероятно уже из plan 02).
    6. **Создать `crates/trackly-app/tests/acts_search.rs`** с 6 integration tests из Behavior list. tokio timeout 30s. Helpers из plan 02 acts_crud.
    7. **UI: ActsSearchAndTabs.svelte** — добавить debounce 250ms на search-input. Trigger props.onSearchChange(value) при истечении timer'а. Cleanup timer при unmount через onDestroy.
    8. **UI: ActsPage.svelte** — добавить state `searchQuery = $state("")`. В $effect (зависящем от searchQuery + activeTab + pagination): если searchQuery.trim().length === 0 → call acts.list(filter, pagination); else call acts.search(searchQuery, filter, pagination). pushToast на error.
  </action>
  <acceptance_criteria>
    - `crates/trackly-infra/src/repos/acts_sqlite.rs` содержит `search_acts` И `WITH act_text_hits AS` И `devices_fts MATCH`.
    - `crates/trackly-app/src/services/act_service.rs::search` содержит `build_fts_query` И fallback `self.list` для empty query.
    - `cargo test -p trackly-app --test acts_search` exits 0 (6 тестов).
    - `cargo test -p trackly-app --test acts_crud --test acts_returns --test acts_undo --test pdf_render_act --test templates_seed --test organization_io` остаются exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `pnpm -C ui svelte-check` exits 0.
    - `pnpm -C ui lint` exits 0.
    - `ui/src/features/acts/ActsSearchAndTabs.svelte` содержит `setTimeout` И `250` (debounce).
    - `ui/src/features/acts/ActsPage.svelte` содержит `acts.search` И `searchQuery`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test acts_search 2>&amp;1 | tail -15 &amp;&amp; cargo test -p trackly-app --test acts_crud --test acts_returns --test acts_undo 2>&amp;1 | tail -10 &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; pnpm -C ui svelte-check 2>&amp;1 | tail -5 &amp;&amp; pnpm -C ui lint 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Acts search backend (LIKE + FTS UNION) + UI debounce search active; integration tests cover number/ФИО/устройство paths; existing тесты не сломались.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DEV-14 UI flow (DocumentAcceptanceModal + DeviceContextMenu item + PdfPreviewModal mode='acceptance') + Phase 3 e2e smoke test</name>
  <files>
    ui/src/features/acts/DocumentAcceptanceModal.svelte,
    ui/src/features/acts/PdfPreviewModal.svelte,
    ui/src/features/devices/DeviceContextMenu.svelte,
    ui/src/features/devices/DevicesPage.svelte,
    crates/trackly-app/tests/acts_e2e_smoke.rs
  </files>
  <read_first>
    - .planning/phases/03-pdf/03-UI-SPEC.md §«Document приёма entry point (DEV-14, DEV-15)» (lines 301-316), §PdfPreviewModal (для расширения mode prop)
    - .planning/phases/03-pdf/03-PATTERNS.md §I `ui/src/features/devices/DeviceContextMenu.svelte` (модификация — добавить пункт «Печать документа приёма»)
    - .planning/phases/03-pdf/03-CONTEXT.md §«Composition» — DEV-14 backend готов в plan 04 (build_devices_render_acceptance_pdf, devices_render_acceptance_pdf tauri command)
    - ui/src/features/acts/PdfPreviewModal.svelte (plan 04 result) — текущая реализация чтобы расширить для mode prop
    - ui/src/features/devices/DeviceContextMenu.svelte — текущая структура items
    - ui/src/features/devices/DevicesPage.svelte — где интегрируется DocumentAcceptanceModal + PdfPreviewModal
    - .planning/phases/03-pdf/03-VALIDATION.md §Manual-Only Verifications — Cyrillic glyphs visual check (этот acceptance modal в этот цикл)
    - existing Phase 2 PATTERNS — context menu pattern (DeviceContextMenu добавляется пунктами через массив)
  </read_first>
  <behavior>
    - Test 1 (UI svelte-check + lint): зелёные с новым DocumentAcceptanceModal и расширенным PdfPreviewModal.
    - Test 2 (`acts_e2e_smoke.rs::full_lifecycle_then_undo`): сценарий: seed 3 devices «На складе» → create handover (counter=1) → create partial return (2 devices) → handover.archived=false; counts {handover_active:1, returns:1, archived:0} → create return на оставшийся device → handover.archived=true; counts {handover_active:0, returns:2, archived:1} → delete handover (cascade undo) → все devices снова «На складе»; counts {handover_active:0, returns:0, archived:0}.
    - Test 3 (`acts_e2e_smoke.rs::handover_pdf_render_within_e2e`): на середине scenario вызвать render_pdf на handover → returns bytes > 1000 with Cyrillic.
    - Test 4 (`acts_e2e_smoke.rs::acceptance_pdf_render_smoke`): render_acceptance_pdf(device_id, "Иван", "Пётр", date) → bytes > 1000 + extracted text contains «Иван» И «Пётр».
    - Test 5 (manual per VALIDATION): в DevicesPage правый клик на устройстве → «Печать документа приёма» → modal открывается → заполнить «Иван»/«Пётр» → Submit → PdfPreviewModal с реальным PDF документа приёма.
  </behavior>
  <action>
    1. **Создать `ui/src/features/acts/DocumentAcceptanceModal.svelte`**:
       - Props: `open: boolean, device: DeviceDto, onClose: () => void, onSubmit: (payload: { deviceId: number; giverName: string; receiverName: string; dateUtc: number }) => void`.
       - Modal size='md' (640px стандарт).
       - Header «Документ приёма устройства».
       - Body: 3 поля (Input «Кто передал», Input «Кто принял», Date input «Дата» дефолт сегодня (MSK).
       - Validation: giverName+receiverName обязательны.
       - **Timezone handling (W-9):** date input возвращает local date string `YYYY-MM-DD`. Convert to UTC unix seconds **assuming Europe/Moscow (UTC+3, no DST)** — захардкожено до Phase 7 SET-*. Backend hook (act_service::render_acceptance_pdf) делает inverse: получает unix-seconds → формирует «28 мая 2026 г.» через offset +3h. На UI: `function dateLocalToUtcSeconds(dateStr: string): number { const [y,m,d] = dateStr.split('-').map(Number); const utcMs = Date.UTC(y, m-1, d, 0, 0, 0) - 3 * 3600 * 1000; return Math.floor(utcMs / 1000); }` — кодирует «полночь MSK выбранного дня» как UTC seconds. Backend в render_acceptance_pdf использует `time::OffsetDateTime::from_unix_timestamp(dateUtc).to_offset(time::macros::offset!(+3))` чтобы вернуть тот же календарный день MSK для формирования «28 мая 2026 г.». **Принцип:** UI работает в MSK ⇒ UTC seconds ⇒ backend в MSK — never UTC display, потому что Phase 3 ru-only single-tz приложение.
       - Primary CTA «Сформировать PDF» → onSubmit(payload) → onClose.
       - Secondary «Отмена» → onClose.
       - Все copy строки из UI-SPEC §«Document приёма entry point» lines 305-316.
    2. **Расширить `ui/src/features/acts/PdfPreviewModal.svelte`** для mode prop:
       - Props добавить: `mode?: 'handover' | 'acceptance' = 'handover'`, `acceptancePayload?: { deviceId: number; giverName: string; receiverName: string; dateUtc: number }`.
       - В loadPdf: if mode==='acceptance' → call `acts.renderAcceptancePdf(acceptancePayload!.deviceId, ...)` иначе `acts.renderPdf(actId!)`.
       - Title и saved filename различаются per UI-SPEC §PdfPreviewModal — для acceptance: «Печать документа приёма» / `Документ_приёма_{device.name}_{YYYY-MM-DD}.pdf`.
    3. **Расширить `ui/src/features/devices/DeviceContextMenu.svelte`**:
       - Добавить новый item между «Редактировать» и «Удалить» (с separator перед «Удалить»):
         - label: «Печать документа приёма»
         - icon: printer (reuse Phase 2 PrinterIcon если есть, или inline SVG).
         - onclick: props.onPrintAcceptance(device).
       - Расширить ContextMenuProps типа добавив `onPrintAcceptance?: (device: DeviceDto) => void`.
    4. **Расширить `ui/src/features/devices/DevicesPage.svelte`**:
       - Импортировать DocumentAcceptanceModal + PdfPreviewModal.
       - State: `acceptanceDevice = $state<DeviceDto|null>(null)`, `acceptancePayload = $state<typeof acceptancePayload | null>(null)`.
       - handler `onPrintAcceptance(device)` → setAcceptanceDevice(device).
       - DocumentAcceptanceModal: open={acceptanceDevice !== null} device={acceptanceDevice} onClose={() => acceptanceDevice = null} onSubmit={(payload) => { acceptancePayload = payload; acceptanceDevice = null; }}.
       - PdfPreviewModal: open={acceptancePayload !== null} mode="acceptance" acceptancePayload={acceptancePayload} onClose={() => acceptancePayload = null}.
       - В DeviceContextMenu instances — передать onPrintAcceptance={onPrintAcceptance}.
    5. **Создать `crates/trackly-app/tests/acts_e2e_smoke.rs`** с 3 tests из Behavior:
       - Использовать helper make_acts_service / make_full_ctx (или AppCtx::build_for_test) — нужно, чтобы pdf, organization, templates все были подключены (вытекает из plan 04).
       - **CRITICAL для acceptance test:** перед render_acceptance_pdf нужно seed org.json placeholder + templates seed run. Если AppCtx::build_for_test уже это делает — отлично; если нет — расширить helper.
       - tokio timeout 30s каждый.
    6. Финальный run всего workspace tests + clippy + lint.
  </action>
  <acceptance_criteria>
    - `ui/src/features/acts/DocumentAcceptanceModal.svelte` существует и содержит поля «Кто передал» И «Кто принял», а также функцию `dateLocalToUtcSeconds` с явным `* 3600 * 1000` (W-9 MSK conversion).
    - **W-9 acceptance:** новый integration-test `document_acceptance_pdf_renders_correct_calendar_date_for_same_day_msk_selection` (добавить в acts_e2e_smoke.rs или templates_seed.rs scope, по выбору executor'а): прогнать `render_acceptance_pdf(device_id, "Иван", "Пётр", date_utc_msk_2026_05_29_midnight)` → extracted text PDF содержит «29 мая 2026» (НЕ «28 мая 2026» — отрицательное смещение из MSK к UTC не должно сдвигать день).
    - `ui/src/features/acts/PdfPreviewModal.svelte` содержит `mode` (prop) И `'acceptance'`.
    - `ui/src/features/devices/DeviceContextMenu.svelte` содержит «Печать документа приёма».
    - `ui/src/features/devices/DevicesPage.svelte` содержит `acceptanceDevice` И `acceptancePayload` И `onPrintAcceptance`.
    - `cargo test -p trackly-app --test acts_e2e_smoke` exits 0 (3 теста).
    - `cargo test -p trackly-app --test acts_search` остаётся exits 0.
    - `cargo test --workspace` зелёный (полный).
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo fmt --all -- --check` exits 0.
    - `pnpm -C ui svelte-check` exits 0.
    - `pnpm -C ui lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test acts_e2e_smoke 2>&amp;1 | tail -15 &amp;&amp; cargo test --workspace 2>&amp;1 | tail -10 &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; cargo fmt --all -- --check 2>&amp;1 | tail -5 &amp;&amp; pnpm -C ui svelte-check 2>&amp;1 | tail -5 &amp;&amp; pnpm -C ui lint 2>&amp;1 | tail -5</automated>
  </verify>
  <done>DEV-14 UI замкнут (context menu → intermediate modal → preview modal); e2e smoke зелёный; Phase 3 готова к /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search query string → SQL LIKE pattern | escape `%` and `_` → no LIKE injection |
| Search query string → FTS5 MATCH | build_fts_query sanitizes; empty fallback case |
| DocumentAcceptanceModal input → backend render_acceptance_pdf | giver/receiver values → passed as MiniJinja ctx variables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | SQL LIKE injection через % / _ wildcards | mitigate | escape в `build_search_like_pattern` (replace '%' и '_'); параметризованный query |
| T-03-05-02 | DoS | большой search query → slow full scan | accept | LAN scale; пагинация LIMIT 50 |
| T-03-05-03 | Information Disclosure | DocumentAcceptanceModal не валидирует device_id | accept | UI пробрасывает device из контекстного меню (текущий список под admin'ом); Phase 5 добавит RBAC |
| T-03-05-04 | Tampering | acceptance template render с user input giver/receiver → XSS? | accept | krilla рендерит как plain text (нет HTML escape vector); MiniJinja AutoEscape::None — нет HTML rendering; JSON output → DocSpec strict deserialization |
| T-03-05-05 | Repudiation | DEV-14 PDF не пишет audit_log | accept | Document приёма — не персистируемое событие (это печатная форма, не запись в систему); audit_log пишется только при изменении state |

</threat_model>

<verification>
- `cargo test --workspace` зелёный (включая acts_search, acts_e2e_smoke и всю Phase 3).
- `cargo clippy --workspace --all-targets -- -D warnings` зелёный.
- `cargo fmt --all -- --check` зелёный.
- `pnpm -C ui svelte-check` зелёный.
- `pnpm -C ui lint` зелёный.
- `pnpm tauri dev` — все Phase 3 flows работают: создание/возврат/архив/удаление актов + печать акта + поиск + печать документа приёма из устройств.
- ProcMon тест (FOUND-11) остаётся зелёным — Phase 3 не записывает в %APPDATA% или подобные пути.
</verification>

<success_criteria>
- ACT-04 (поиск по актам): integration-test acts_search + UI debounce работают.
- DEV-14 (печать документа приёма UI): context-menu item + intermediate modal + preview работают.
- DEV-15 (шаблон в БД для документа приёма): seeded в plan 04, используется через UI в этом плане.
- e2e smoke полный lifecycle handover → return → archive → undo доказывает ACT-13 transactional guarantee.
- Phase 3 закрыла все 16 требований: ACT-01..14 + DEV-14, DEV-15.
- Phase 3 готова к /gsd-verify-work и /gsd-ship.
</success_criteria>

<output>
Создать `.planning/phases/03-pdf/03-05-SUMMARY.md` с:
- Phase 3 итог: какие из 16 требований закрыты, ссылки на integration tests.
- Полный список созданных файлов фазы.
- Performance notes: LIKE pattern может быть slow at >10000 actов — отложено Phase 7.
- Phase 4 ready-state: Cartridges фаза может использовать тот же counter pattern (V009 уже имеет cartridge_seq), audit_log undo pattern, FTS search pattern.
</output>
</content>
</invoke>