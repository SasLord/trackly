---
phase: 03-pdf
plan: 06
type: execute
wave: 6
depends_on:
  - 03-05
files_modified:
  - crates/trackly-app/src/pdf/renderer.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/tests/pdf_logo.rs
  - crates/trackly-app/tests/acts_returns.rs
  - crates/trackly-app/tests/fixtures/act_42.sha256
  - crates/trackly-app/tests/fixtures/logo_test.png
autonomous: true
requirements:
  - ACT-11
  - ACT-13
threat_model_severity: medium
tags:
  - phase-3
  - gap-closure
  - pdf
  - acts-returns
  - data-integrity

must_haves:
  truths:
    - "ACT-11 (logo): renderer::render_docspec вызывает surface.draw_image(...) когда spec.header.logo_path = Some(path) и файл существует и валиден; PDF-байты содержат /XObject поток"
    - "ACT-11 (logo absent): когда spec.header.logo_path = None или файл не существует — рендеринг продолжается без ошибки (graceful) и без draw_image"
    - "ACT-13 (CR-02 status guard): do_return проверяет before.status_id == Some(in_work_status_id) для каждого item; иначе AppError::Conflict «Устройство id=N уже не в работе»"
    - "ACT-13 (CR-03 dedup act_item_id): validate_return отвергает payload с дублирующимся act_item_id → AppError::Validation field=items[N].act_item_id"
    - "ACT-13 (CR-03 dedup device_id): validate_return отвергает payload с дублирующимся device_id → AppError::Validation field=items[N].device_id"
    - "ACT-13 (CR-04 quantity bound): do_return подтягивает handover_qty + already_returned per item; payload.item.quantity + already_returned > handover_qty → AppError::Validation"
    - "Определённость PDF: pinned SHA256 фикстуры act_42 совпадает с фактическим выводом (logo_path=null в фикстуре → no draw_image → hash НЕ изменяется; если меняется — bump осознанно с пояснением в commit)"
  artifacts:
    - path: "crates/trackly-app/src/pdf/renderer.rs"
      provides: "render_docspec теперь читает spec.header.logo_path через std::fs::read и вызывает surface.draw_image после Image::from_png_bytes/from_jpeg_bytes"
    - path: "crates/trackly-app/src/services/act_service.rs"
      provides: "validate_return с HashSet dedup; do_return с status guard + quantity-bound check через SELECT quantity FROM act_items + SUM(rai.quantity) FROM return chains"
    - path: "crates/trackly-app/tests/pdf_logo.rs"
      provides: "act_with_logo_renders_image_in_pdf — assert /XObject или /Image в PDF-байтах"
    - path: "crates/trackly-app/tests/acts_returns.rs (extended)"
      provides: "4 новых теста: return_twice_same_device_rejected, return_with_duplicate_device_id_rejected, return_with_duplicate_act_item_id_rejected, return_quantity_exceeds_handover_rejected"
    - path: "crates/trackly-app/tests/fixtures/logo_test.png"
      provides: "Маленький валидный PNG (например 4x4 px) для теста логотипа — embedded через include_bytes! или временный файл в тесте"
  key_links:
    - from: "crates/trackly-app/src/pdf/renderer.rs::render_docspec"
      to: "krilla::image::Image::from_png_bytes / from_jpeg_bytes"
      via: "std::fs::read(logo_path) → определить mime по extension → Image::from_* → surface.draw_image(Point, image, width, height)"
      pattern: "surface\\.draw_image"
    - from: "crates/trackly-app/src/services/act_service.rs::validate_return"
      to: "std::collections::HashSet"
      via: "Два независимых HashSet<i64> для act_item_id и device_id; .insert() возвращает false → AppError::Validation"
      pattern: "HashSet::<i64>::new"
    - from: "crates/trackly-app/src/services/act_service.rs::do_return per-item loop"
      to: "act_items.quantity + return_acts SUM"
      via: "SELECT quantity FROM act_items WHERE id=?1 AND act_id=?2 + SUM(rai.quantity) FROM act_items rai JOIN acts ra ON ra.id=rai.act_id WHERE ra.parent_act_id=?1 AND rai.device_id=?2 AND ra.deleted_at_utc IS NULL"
      pattern: "SUM\\(rai\\.quantity\\)"
    - from: "crates/trackly-app/src/services/act_service.rs::do_return per-item loop"
      to: "AppError::Conflict (status guard)"
      via: "if before.status_id != Some(in_work_status_id) → Err(AppError::Conflict { reason: «Устройство id=N сейчас не в работе ...» })"
      pattern: "уже не в работе"
---

## Phase Goal

**As a** сотрудник, **I want to** получать PDF акта с реальным логотипом организации в шапке, и **I want** систему отказывать в некорректном возврате (двойной возврат, дубль device_id, превышение количества) с понятным сообщением, **so that** учётный документ соответствует UX-обещанию ACT-11 (шапка с логотипом), а целостность данных при возврате защищена от cross-tx и intra-payload ошибок (ACT-13).

> **Gap closure (Phase 3 post-VERIFICATION):** этот план НЕ переписывает Phase 3 — закрывает строго два failed-requirement из 03-VERIFICATION.md (ACT-11, ACT-13) через четыре BLOCKER-уровня правки из 03-REVIEW.md (CR-01..CR-04) + 5 интеграционных тестов. Все правки локальные, без schema-changes.

<objective>
1) Реализовать logo rendering в `pdf/renderer.rs::render_docspec` через `std::fs::read` + `krilla::image::Image::from_png_bytes` / `from_jpeg_bytes` + `surface.draw_image(...)` (CR-01 path A). Никакого option B (drop logo) — рендерим логотип. 2) Усилить return-tx integrity в `services/act_service.rs`: status guard (CR-02), HashSet dedup для act_item_id и device_id (CR-03), quantity-bound check против handover_qty минус already_returned (CR-04). 3) Поставить 5 интеграционных тестов (1 logo + 4 returns invariants), доказывающих закрытие гэпов.

Purpose: Закрыть ACT-11 + ACT-13 с автоматическим verification и не сломать pinned SHA256 фикстуры act_42 (logo_path=null в фикстуре → renderer должен no-op'ить когда None или path не существует).

Output: ACT-11 verified — `cargo test --test pdf_logo` зелёный + grep `surface.draw_image` в renderer.rs ≥1. ACT-13 verified — `cargo test --test acts_returns` зелёный с 4 новыми тестами + grep `HashSet::<i64>::new` и `уже не в работе` и `SUM(rai.quantity)` в act_service.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-pdf/03-CONTEXT.md
@.planning/phases/03-pdf/03-RESEARCH.md
@.planning/phases/03-pdf/03-VERIFICATION.md
@.planning/phases/03-pdf/03-REVIEW.md
@.planning/phases/03-pdf/03-04-SUMMARY.md
@.planning/phases/03-pdf/03-03-SUMMARY.md
@crates/trackly-app/src/pdf/renderer.rs
@crates/trackly-app/src/pdf/docspec.rs
@crates/trackly-app/src/services/act_service.rs
@crates/trackly-app/src/services/organization_service.rs
@crates/trackly-app/tests/pdf_render_act.rs
@crates/trackly-app/tests/pdf_determinism.rs
@crates/trackly-app/tests/acts_returns.rs
@crates/trackly-app/tests/fixtures/act_42.json
@crates/trackly-app/tests/fixtures/act_42.sha256

<interfaces>
<!-- Контракт правок. -->

PdfRenderer::render_docspec (renderer.rs, текущий метод lines 88-178):
- Сразу после рендеринга header-band текста (после `y += BODY_SIZE_PT + 16.0;` для date_label, line ~162, ПЕРЕД walk DocSpec sections) — добавить блок:
  ```
  if let Some(logo_path_str) = &spec.header.logo_path {
      // Не падать если файла нет — это допустимо (orgs без логотипа). Лог tracing::warn.
      if let Ok(bytes) = std::fs::read(logo_path_str) {
          let lower = logo_path_str.to_lowercase();
          let image_opt: Option<krilla::image::Image> = if lower.ends_with(".png") {
              krilla::image::Image::from_png(&bytes, ...) // см. krilla 0.7 API; конкретный конструктор уточнить
          } else if lower.ends_with(".jpg") || lower.ends_with(".jpeg") {
              krilla::image::Image::from_jpeg(&bytes, ...)
          } else {
              None  // unsupported mime — пропускаем
          };
          if let Some(image) = image_opt {
              // Top-right corner: x = A4_WIDTH_PT - MARGIN_PT - 100.0, y = MARGIN_PT
              // Размер 100x50 pt (правка в action — выбрать sane defaults)
              surface.draw_image(
                  Point::from_xy(A4_WIDTH_PT - MARGIN_PT - 100.0, MARGIN_PT),
                  &image,
                  krilla::geom::Size::from_wh(100.0, 50.0)?,
              );
          } else {
              tracing::warn!(path = %logo_path_str, "Logo не валиден или unsupported mime — пропускаем");
          }
      } else {
          tracing::warn!(path = %logo_path_str, "Logo файл не доступен для чтения — пропускаем");
      }
  }
  ```
- **ВАЖНО krilla 0.7 API:** конкретные сигнатуры `Image::from_png_bytes`/`from_jpeg_bytes` и `surface.draw_image` уточнить через `cargo doc --open -p krilla` ИЛИ через `grep -rn "fn from_png\|fn from_jpeg\|fn draw_image" ~/.cargo/registry/src/*/krilla-0.7*` ИЛИ через https://docs.rs/krilla/0.7/krilla/. В REVIEW указано «krilla 0.7 поддерживает Image::from_png_bytes/from_jpeg_bytes». Executor проверит и подстроится под фактический API.
- **None branch:** spec.header.logo_path = None → НИЧЕГО не делать (не лог, не error). Pinned SHA256 фикстуры act_42 (logo_path=null) останется неизменным, потому что ни один draw_image не выполняется.

ActService::validate_return (act_service.rs:328-360 — заменить тело):
```rust
fn validate_return(payload: &ActReturnDto) -> Result<(), AppError> {
    if payload.items.is_empty() {
        return Err(AppError::Validation { field: "items".into(), message: "Добавьте хотя бы одну позицию к возврату".into() });
    }
    let mut seen_act_items: std::collections::HashSet<i64> = std::collections::HashSet::new();
    let mut seen_device_ids: std::collections::HashSet<i64> = std::collections::HashSet::new();
    for (idx, it) in payload.items.iter().enumerate() {
        if !seen_act_items.insert(it.act_item_id) {
            return Err(AppError::Validation {
                field: format!("items[{idx}].act_item_id"),
                message: format!("act_item_id={} продублирован в возврате", it.act_item_id),
            });
        }
        if !seen_device_ids.insert(it.device_id) {
            return Err(AppError::Validation {
                field: format!("items[{idx}].device_id"),
                message: format!("device_id={} продублирован в возврате", it.device_id),
            });
        }
        if it.quantity < 1 {
            return Err(AppError::Validation { field: format!("items[{idx}].quantity"), message: "Количество должно быть ≥ 1".into() });
        }
        if !payload.apply_to_all {
            if it.condition_override.is_none() {
                return Err(AppError::Validation { field: format!("items[{idx}].condition_override"), message: "Состояние обязательно (apply_to_all = false)".into() });
            }
            if it.location_id_override.is_none() && it.location_name_override.is_none() {
                return Err(AppError::Validation { field: format!("items[{idx}].location_id_override"), message: "Расположение обязательно (apply_to_all = false)".into() });
            }
        }
    }
    Ok(())
}
```

ActService::do_return — два дополнения в per-item loop (lines 472-546):
- **CR-02 (status guard)** — СРАЗУ после `let before = devices_repo.get_in_tx(&tx, item.device_id)?;` (после snapshot, ДО update_full_in_tx):
  ```rust
  // CR-02: проверить, что устройство сейчас «в работе» — иначе двойной возврат
  let in_work_status_id: i64 = tx.query_row(
      "SELECT id FROM device_statuses WHERE code = 'в_работе'",
      [], |r| r.get(0),
  ).map_err(map_rusqlite)?;
  if before.status_id != Some(in_work_status_id) {
      return Err(AppError::Conflict {
          reason: format!("Устройство id={} уже не в работе — возможно, оно уже возвращено", item.device_id),
      });
  }
  ```
  Подсказка: `in_work_status_id` уже резолвится выше в коде (line 156) для handover-path; для return — нужно отдельно резолвить ИЛИ вынести в начало do_return до payload-loop (рекомендую второе — один раз перед циклом).
- **CR-04 (quantity bound)** — ДО `acts_repo.insert_act_item_in_tx(...)`:
  ```rust
  // CR-04: quantity-bound — нельзя вернуть больше выданного
  let handover_qty: i64 = tx.query_row(
      "SELECT quantity FROM act_items WHERE id = ?1 AND act_id = ?2",
      params![item.act_item_id, act_id], |r| r.get(0),
  ).map_err(map_rusqlite)?;
  let already_returned: i64 = tx.query_row(
      "SELECT COALESCE(SUM(rai.quantity), 0) \
       FROM act_items rai JOIN acts ra ON ra.id = rai.act_id \
       WHERE ra.parent_act_id = ?1 AND rai.device_id = ?2 AND ra.deleted_at_utc IS NULL",
      params![act_id, item.device_id], |r| r.get(0),
  ).map_err(map_rusqlite)?;
  if item.quantity + already_returned > handover_qty {
      return Err(AppError::Validation {
          field: "items".into(),
          message: format!(
              "Возврат превышает выданное количество для устройства id={}: уже возвращено {} + текущее {} > выдано {}",
              item.device_id, already_returned, item.quantity, handover_qty,
          ),
      });
  }
  ```
  ПРИМЕЧАНИЕ: текущая БД структура (V014): `act_items.quantity` существует; `acts.deleted_at_utc` существует. Запрос корректен.

Integration tests:
- `tests/pdf_logo.rs::act_with_logo_renders_image_in_pdf`: создать DocSpec с logo_path указывающим на временный PNG (например через tempfile::NamedTempFile::new().path().to_str() + std::fs::write валидного PNG из include_bytes!) → render_docspec → проверить, что pdf bytes содержат подстроку `/XObject` ИЛИ `/Image` (через `bytes.windows(8).any(|w| w == b"/XObject")` или regex). Минимальный валидный PNG: 4x4 px monochrome — embedded через `include_bytes!("fixtures/logo_test.png")`.
- `tests/acts_returns.rs::return_twice_same_device_rejected` (CR-02): create handover (device A) → first do_return на A (success) → second do_return на A → Err(AppError::Conflict с подстрокой «уже не в работе»).
- `tests/acts_returns.rs::return_with_duplicate_act_item_id_rejected` (CR-03): create handover (device A, act_item_id=X) → do_return с items=[{act_item_id:X, device_id:A}, {act_item_id:X, device_id:A}] → Err(AppError::Validation field содержит "act_item_id").
- `tests/acts_returns.rs::return_with_duplicate_device_id_rejected` (CR-03): create handover (device A с двумя разными act_items? — В реальности device_id уникален per act_item, но тест нужен для интра-payload защиты) → do_return с двумя items разных act_item_id но одинаковым device_id → Err(AppError::Validation field содержит "device_id").
- `tests/acts_returns.rs::return_quantity_exceeds_handover_rejected` (CR-04): create handover с quantity=1 для device A → do_return с item.quantity=100 → Err(AppError::Validation сообщение содержит "превышает выданное").

Pinned SHA256 hash:
- `crates/trackly-app/tests/fixtures/act_42.sha256` — текущий: `88df7f9d69c5db10a4685f0aa5d390caec90b045067e35cc1caba33efdd15d1f`. Фикстура `act_42.json` имеет `logo_path: null` → renderer НИЧЕГО не рисует → байты PDF не меняются → hash остаётся прежним. ОЖИДАЕМО: hash совпадает. ЕСЛИ executor видит несовпадение — это значит, что добавленный код для None-branch как-то затронул output (например, изменение порядка surface ops при insertion check). В таком случае: 1) обновить `act_42.sha256` файл новым значением, 2) в commit message чётко указать «hash bump intentional: ACT-11 logo rendering branch added; fixture has logo_path=null so render path is unchanged structurally — verify by visual diff of PDF dump», 3) подтвердить, что byte-diff не содержит lossy/random data.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PDF renderer logo rendering (CR-01 / ACT-11)</name>
  <files>
    crates/trackly-app/src/pdf/renderer.rs,
    crates/trackly-app/tests/pdf_logo.rs,
    crates/trackly-app/tests/fixtures/logo_test.png
  </files>
  <read_first>
    - .planning/phases/03-pdf/03-REVIEW.md §«CR-01: PDF логотип не рендерится» (lines 77-110) — точный fix pattern
    - .planning/phases/03-pdf/03-VERIFICATION.md frontmatter gaps[0] — точное «missing» определение
    - crates/trackly-app/src/pdf/renderer.rs (lines 88-178) — render_docspec, header-band drawing, где встроить logo
    - crates/trackly-app/src/pdf/docspec.rs (lines 28-39) — HeaderBlock.logo_path: Option<String>
    - crates/trackly-app/src/services/organization_service.rs lines ~110-142 — safe_logo_canonical контракт (path абсолютный, гарантированно внутри exe_dir, существует если Some)
    - crates/trackly-app/tests/pdf_determinism.rs — pattern для smoke-теста рендеринга
    - crates/trackly-app/tests/pdf_render_act.rs — pattern для интеграционного теста через ActService
    - crates/trackly-app/tests/fixtures/act_42.json (logo_path=null) — фикстура определённости
    - https://docs.rs/krilla/0.7/krilla/image/ — фактический API Image::from_png/from_jpeg + surface.draw_image (executor проверит)
  </read_first>
  <behavior>
    - Test 1 (`pdf_logo.rs::act_with_logo_renders_image_in_pdf`): построить DocSpec с минимальным header (org_name, inn, kpp, address, act_label, date_label, logo_path = Some(path_to_temp_png)). Записать `include_bytes!("fixtures/logo_test.png")` во временный путь (либо через tempfile, либо в `paths.exe_dir().join("test_logo.png")`). render_docspec → проверить, что `pdf_bytes.windows(8).any(|w| w == b"/XObject")` ИЛИ `pdf_bytes.windows(6).any(|w| w == b"/Image")` ИЛИ regex `/XObject|/Image\b` находит match.
    - Test 2 (`pdf_logo.rs::logo_path_none_does_not_call_draw_image_smoke`): DocSpec с logo_path=None → render_docspec succeeds → output байты НЕ содержат `/XObject` для image (sanity check, не строгий — header может ещё содержать /XObject для других целей; основная цель — не paniko).
    - Test 3 (`pdf_logo.rs::logo_path_missing_file_is_graceful`): logo_path = Some("/tmp/nonexistent_logo_12345.png") → render_docspec не падает с panic; возвращает Ok(_) (warning в tracing).
    - Test 4 (`pdf_determinism::fixture_act_42_renders_to_known_hash` — RE-RUN): фикстура logo_path=null → hash остаётся `88df7f9d69c5db10a4685f0aa5d390caec90b045067e35cc1caba33efdd15d1f`. ЕСЛИ изменился — обновить `act_42.sha256` с пояснением в commit message.
  </behavior>
  <action>
    1. **Создать `crates/trackly-app/tests/fixtures/logo_test.png`** — минимальный валидный PNG (например 4x4 px). Простейший способ: записать base64-декодированный 67-byte PNG из stdlib (или скопировать любой маленький PNG; ESLINTed pixel art не нужен). Альтернатива — генерировать в тесте через `image` crate (но это добавляет зависимость; лучше include_bytes).
    2. **Расширить `crates/trackly-app/src/pdf/renderer.rs::render_docspec`:**
       - Импортировать `krilla::image::Image` (актуальное имя — проверить через docs.rs/krilla/0.7).
       - Сразу ПОСЛЕ header-band drawing (после строки `y += BODY_SIZE_PT + 16.0;` для `date_label` ~line 162) и ПЕРЕД `for section in &spec.sections`:
         - if `let Some(logo_path_str) = &spec.header.logo_path` → попытаться `std::fs::read(logo_path_str)`.
         - Если read fail → `tracing::warn!(path = %logo_path_str, "logo not readable — skipping")` + продолжить.
         - Иначе: определить mime по extension (lowercase ends_with .png → PNG; .jpg/.jpeg → JPEG; иначе skip + tracing::warn про unsupported mime).
         - Сконструировать krilla `Image` через соответствующий API (executor подбирает: `Image::from_png_bytes(&bytes)`, `Image::from_jpeg_bytes(&bytes)`, или с другой сигнатурой — критично использовать ту, что есть в krilla 0.7).
         - Если конструктор image вернул `Some`/`Ok` → вызвать `surface.draw_image(point, image, w, h)` в правом-верхнем углу. Координаты: `Point::from_xy(A4_WIDTH_PT - MARGIN_PT - 100.0, MARGIN_PT)`, размер 100.0×50.0 pt (можно tune; задокументировать в комментарии).
         - Если конструктор image вернул None/Err → tracing::warn + продолжить.
       - **None branch:** если `spec.header.logo_path == None` — НИЧЕГО не делать. Это критично для сохранения pinned SHA256 фикстуры act_42 (logo_path=null).
    3. **Создать `crates/trackly-app/tests/pdf_logo.rs`** с тремя тестами per Behavior. Использовать `PdfRenderer::new()` напрямую (без ActService — этот тест проверяет именно renderer). DocSpec можно сконструировать вручную с минимальными полями.
    4. **Запустить `cargo test -p trackly-app --test pdf_determinism`** — если hash совпадает (ожидание: совпадает), оставить sha256 файл как есть. ЕСЛИ не совпадает: 1) обновить `crates/trackly-app/tests/fixtures/act_42.sha256` новым значением, 2) подготовить explanatory commit message «test(03-06): bump act_42.sha256 — logo rendering branch added; fixture has logo_path=null, byte-diff is structural; reviewed via xxd».
    5. **Запустить `cargo test -p trackly-app --test pdf_logo --test pdf_determinism --test pdf_render_act --test pdf_text_extract`** — все должны быть зелёными.
    6. **Запустить `cargo clippy --workspace --all-targets -- -D warnings`** — зелёный.
  </action>
  <acceptance_criteria>
    - `grep -c "surface.draw_image" crates/trackly-app/src/pdf/renderer.rs` ≥ 1
    - `grep -c "std::fs::read" crates/trackly-app/src/pdf/renderer.rs` ≥ 1
    - `grep -E "Image::from_(png|jpeg)" crates/trackly-app/src/pdf/renderer.rs` находит как минимум один match (PNG или JPEG, обычно оба)
    - `crates/trackly-app/tests/fixtures/logo_test.png` существует, размер > 50 байт (минимальный PNG)
    - `crates/trackly-app/tests/pdf_logo.rs` существует и содержит 3 #[test] функции; имена включают `act_with_logo_renders_image_in_pdf`
    - `cargo test -p trackly-app --test pdf_logo` exits 0 (3 теста)
    - `cargo test -p trackly-app --test pdf_determinism --test pdf_text_extract --test pdf_render_act` exits 0 (без regression)
    - SHA256 в `crates/trackly-app/tests/fixtures/act_42.sha256` соответствует фактическому выводу `pdf_determinism::fixture_act_42_renders_to_known_hash` (либо неизменён `88df7f9d69c5db10a4685f0aa5d390caec90b045067e35cc1caba33efdd15d1f`, либо bumped с explanatory commit)
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test pdf_logo --test pdf_determinism --test pdf_text_extract --test pdf_render_act 2>&amp;1 | tail -20 &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; grep -c "surface.draw_image" crates/trackly-app/src/pdf/renderer.rs &amp;&amp; grep -c "std::fs::read" crates/trackly-app/src/pdf/renderer.rs</automated>
  </verify>
  <done>renderer.rs читает spec.header.logo_path и вызывает surface.draw_image после krilla Image::from_png/from_jpeg; integration test pdf_logo проходит; determinism-hash либо неизменён, либо bumped с пояснением; ACT-11 закрыт.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return-tx integrity invariants — status guard + dedup + quantity bound (CR-02/03/04 / ACT-13)</name>
  <files>
    crates/trackly-app/src/services/act_service.rs,
    crates/trackly-app/tests/acts_returns.rs
  </files>
  <read_first>
    - .planning/phases/03-pdf/03-REVIEW.md §«CR-02: Возврат не валидирует …» (lines 112-145), §«CR-03: Дублирование device_id …» (lines 149-189), §«CR-04: Возврат не проверяет quantity …» (lines 192-231) — все три fix patterns с примерными SQL+Rust
    - .planning/phases/03-pdf/03-VERIFICATION.md frontmatter gaps[1] — точное «missing» определение
    - crates/trackly-app/src/services/act_service.rs (lines 328-360) — validate_return (CR-03 в нём)
    - crates/trackly-app/src/services/act_service.rs (lines 362-584) — do_return per-item loop (CR-02 + CR-04 в нём)
    - crates/trackly-app/tests/acts_returns.rs (текущий) — pattern для интеграционных тестов: AppCtx::build_for_test, seed devices через writer, create handover через service.create, do_return через service.do_return, assertions через get
    - migrations/V014__acts_indexes_and_status_codes.sql — подтверждает existence of `device_statuses.code='в_работе'` И `act_items.quantity` column
    - crates/trackly-core/src/error.rs — AppError variants (Conflict, Validation, NotFound) для понимания pattern matching в тестах
  </read_first>
  <behavior>
    - Test 1 (`acts_returns.rs::return_twice_same_device_rejected` — CR-02): seed device A в state «в_работе» через create handover → first do_return(act_id, [device A]) → Ok; **second** do_return(act_id, [device A]) → Err(AppError::Conflict { reason: содержит «уже не в работе» AND device_id }).
    - Test 2 (`return_with_duplicate_act_item_id_rejected` — CR-03): create handover с одним device A (act_item_id = X) → do_return с payload.items = [(act_item_id=X, device_id=A, qty=1), (act_item_id=X, device_id=A, qty=1)] → Err(AppError::Validation { field: содержит «act_item_id», message: содержит «продублирован» }).
    - Test 3 (`return_with_duplicate_device_id_rejected` — CR-03): create handover с двумя act_items для одного device_id? — В практике невозможно (act_items уникальны per device per act), но валидация всё равно должна сработать на интра-payload. **Реализация:** создать handover с device A (act_item_id=X) И device A (act_item_id=Y) — два разных act_items на один device. ЕСЛИ это запрещено схемой — заменить тест на: payload с двумя items разных act_item_id (X, Y) но обе с device_id=A. **DECISION:** проверить через query — если `INSERT INTO act_items` допускает два device_id=A в одном act_id, использовать payload (X, A), (Y, A); если schema запрещает (UNIQUE constraint) — использовать другой подход: создать ДВА handover'а (act_1 с device A, act_2 с device A — невозможно если device-в-работе блокирует) ИЛИ просто синтезировать payload вручную (создать handover нормально, в payload вручную указать [(act_item_X, device_A), (act_item_Y_fake, device_A)] — второй act_item проверится по validate_return ДО проверки existence в БД, потому что HashSet dedup в validate_return). **Простейший путь:** payload = [(act_item_id=X, device_id=A, qty=1), (act_item_id=999, device_id=A, qty=1)] — HashSet device_id catch'ит дубль ДО SQL проверки existence. → Err(AppError::Validation { field: содержит «device_id», message: содержит «продублирован» }).
    - Test 4 (`return_quantity_exceeds_handover_rejected` — CR-04): create handover для device A с quantity=1 → do_return с payload.items=[(act_item_X, device_A, qty=100)] → Err(AppError::Validation { message: содержит «превышает выданное» }).
    - **Все 4 теста**: проверить, что после неудачного do_return state БД неизменён (device.status_id остался в_работе, return-акт НЕ создан). Можно через дополнительный assert: `acts.list_by_filter(...).len()` до и после Err равен.
    - Regression: все существующие acts_returns тесты (8 шт) остаются зелёными.
  </behavior>
  <action>
    1. **Расширить `crates/trackly-app/src/services/act_service.rs::validate_return`** (lines 328-360) — заменить на версию с HashSet dedup per Interfaces section. Импорт `std::collections::HashSet` (либо inline через `std::collections::HashSet`). Сохранить существующие проверки (items.is_empty, quantity ≥ 1, apply_to_all=false требует overrides).
    2. **Расширить `crates/trackly-app/src/services/act_service.rs::do_return`** (lines 362-584) — две вставки в per-item loop:
       - **Перед `for item in &payload.items`** (один раз, чтобы не SELECT в каждой итерации) — резолвить `in_work_status_id` через `SELECT id FROM device_statuses WHERE code = 'в_работе'`. Подсказка: этот резолв уже есть в handover-path act_service.rs lines 156-167; вынести в shared helper или продублировать (минимальный путь — продублировать).
       - **Внутри loop, СРАЗУ после `let before = devices_repo.get_in_tx(&tx, item.device_id)?;` (line 474):** добавить status guard:
         ```rust
         if before.status_id != Some(in_work_status_id) {
             return Err(AppError::Conflict {
                 reason: format!(
                     "Устройство id={} уже не в работе — возможно, оно уже возвращено",
                     item.device_id
                 ),
             });
         }
         ```
       - **Внутри loop, ДО `acts_repo.insert_act_item_in_tx(...)` (line 505):** добавить quantity-bound check:
         ```rust
         let handover_qty: i64 = tx.query_row(
             "SELECT quantity FROM act_items WHERE id = ?1 AND act_id = ?2",
             params![item.act_item_id, act_id],
             |r| r.get(0),
         ).map_err(map_rusqlite)?;
         let already_returned: i64 = tx.query_row(
             "SELECT COALESCE(SUM(rai.quantity), 0) \
              FROM act_items rai JOIN acts ra ON ra.id = rai.act_id \
              WHERE ra.parent_act_id = ?1 AND rai.device_id = ?2 AND ra.deleted_at_utc IS NULL",
             params![act_id, item.device_id],
             |r| r.get(0),
         ).map_err(map_rusqlite)?;
         if item.quantity + already_returned > handover_qty {
             return Err(AppError::Validation {
                 field: "items".into(),
                 message: format!(
                     "Возврат превышает выданное количество для устройства id={}: уже возвращено {} + текущее {} > выдано {}",
                     item.device_id, already_returned, item.quantity, handover_qty,
                 ),
             });
         }
         ```
    3. **Расширить `crates/trackly-app/tests/acts_returns.rs`** добавив 4 теста per Behavior. Использовать существующие helpers (make_acts_service / AppCtx::build_for_test pattern из текущего файла). Каждый тест wrap в tokio::time::timeout(Duration::from_secs(30)). Assertion patterns:
       - Conflict: `matches!(err, AppError::Conflict { ref reason } if reason.contains("уже не в работе"))`.
       - Validation: `matches!(err, AppError::Validation { ref field, ref message } if field.contains("act_item_id") && message.contains("продублирован"))`.
    4. **Запустить `cargo test -p trackly-app --test acts_returns`** — должны быть зелёные ВСЕ существующие 8 тестов + 4 новых. Тесты включают новые:
       - `return_twice_same_device_rejected`
       - `return_with_duplicate_act_item_id_rejected`
       - `return_with_duplicate_device_id_rejected`
       - `return_quantity_exceeds_handover_rejected`
    5. **Запустить полный Phase 3 regression:** `cargo test -p trackly-app --test acts_crud --test acts_returns --test acts_undo --test acts_display_rule --test acts_e2e_smoke --test acts_search --test acts_http_smoke --test acts_numbering` — все зелёные. Ключ: новые проверки не должны сломать существующий happy path (нет dup, нет double-return, нет quantity exceed).
    6. **Запустить `cargo clippy --workspace --all-targets -- -D warnings`** — зелёный.
  </action>
  <acceptance_criteria>
    - `grep -c "HashSet::<i64>::new\|HashSet::new" crates/trackly-app/src/services/act_service.rs` ≥ 2 (для act_item_id и device_id)
    - `grep -c "уже не в работе" crates/trackly-app/src/services/act_service.rs` ≥ 1
    - `grep -c "SUM(rai.quantity)" crates/trackly-app/src/services/act_service.rs` ≥ 1
    - `grep -c "превышает выданное" crates/trackly-app/src/services/act_service.rs` ≥ 1
    - `grep -E "fn (return_twice_same_device_rejected|return_with_duplicate_act_item_id_rejected|return_with_duplicate_device_id_rejected|return_quantity_exceeds_handover_rejected)" crates/trackly-app/tests/acts_returns.rs` находит ВСЕ 4 функции
    - `cargo test -p trackly-app --test acts_returns` exits 0 с count ≥ 12 (8 existing + 4 new)
    - `cargo test -p trackly-app --test acts_crud --test acts_undo --test acts_display_rule --test acts_e2e_smoke --test acts_search --test acts_http_smoke --test acts_numbering` exits 0 (no regression)
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test acts_returns 2>&amp;1 | tail -20 &amp;&amp; cargo test -p trackly-app --test acts_crud --test acts_undo --test acts_display_rule --test acts_e2e_smoke --test acts_search --test acts_http_smoke --test acts_numbering 2>&amp;1 | tail -15 &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings 2>&amp;1 | tail -5 &amp;&amp; grep -c "HashSet" crates/trackly-app/src/services/act_service.rs &amp;&amp; grep -c "SUM(rai.quantity)" crates/trackly-app/src/services/act_service.rs</automated>
  </verify>
  <done>validate_return отвергает payload с дублированиями (act_item_id или device_id); do_return per-item проверяет before.status_id == in_work_status_id и quantity + already_returned ≤ handover_qty; 4 интеграционных теста зелёные; regression на остальные Phase 3 acts тесты отсутствует; ACT-13 закрыт.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logo file (file system) → krilla Image | path уже canonical через safe_logo_canonical (Phase 3 plan 04); но fs::read может вернуть corrupted bytes |
| return payload (UI/HTTP) → validate_return | unsanitized act_item_id / device_id / quantity; intra-payload dedup нужен |
| return payload → do_return SQL | quantity-bound check предотвращает overflow возврата |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | invalid PNG/JPEG bytes в logo file → krilla Image::from_* fails | mitigate | tracing::warn + skip; render продолжается без логотипа (graceful degradation per CR-01 fix pattern) |
| T-03-06-02 | Data Integrity | двойной возврат тех же device_id (cross-tx) | mitigate | CR-02 status guard в per-item loop: before.status_id != in_work_status_id → AppError::Conflict; integration test return_twice_same_device_rejected |
| T-03-06-03 | Data Integrity | intra-payload duplicate device_id → broken undo chain | mitigate | CR-03 HashSet dedup в validate_return; integration test return_with_duplicate_device_id_rejected |
| T-03-06-04 | Data Integrity | intra-payload duplicate act_item_id → broken audit refs | mitigate | CR-03 HashSet dedup; integration test return_with_duplicate_act_item_id_rejected |
| T-03-06-05 | Data Integrity | quantity overflow возврат (вернуть больше выданного) | mitigate | CR-04 SELECT quantity + SUM(rai.quantity) cross-check; integration test return_quantity_exceeds_handover_rejected |
| T-03-06-06 | Information Disclosure | logo path traversal | accept | mitigated в Phase 3 plan 04 через safe_logo_canonical; этот план не меняет path-resolution layer |
| T-03-06-07 | DoS | очень большой logo file → fs::read больших байтов | accept | path canonical через safe_logo_canonical (внутри exe_dir); admin контролирует |

</threat_model>

<verification>
- `cargo test -p trackly-app --test pdf_logo --test pdf_determinism --test pdf_text_extract --test pdf_render_act --test acts_returns` зелёный
- Все existing Phase 3 тесты остаются зелёными (acts_crud, acts_undo, acts_display_rule, acts_e2e_smoke, acts_search, acts_http_smoke, acts_numbering, templates_seed, organization_io, export_bindings)
- `cargo clippy --workspace --all-targets -- -D warnings` зелёный
- `cargo fmt --all -- --check` зелёный
- grep гейты per acceptance_criteria выполнены
- SHA256 фикстуры act_42 либо неизменён (ожидаемо), либо обновлён с explanatory commit
</verification>

<success_criteria>
- ACT-11 (PDF логотип): renderer.rs читает spec.header.logo_path и вызывает surface.draw_image; integration test pdf_logo доказывает /XObject в PDF-байтах; VERIFICATION.md gap[0] закрыт.
- ACT-13 (return-tx integrity): три cross-validating инварианта реализованы (status guard CR-02, HashSet dedup CR-03, quantity bound CR-04); 4 integration теста доказывают, что invalid payloads отвергаются с правильными AppError variants; VERIFICATION.md gap[1] закрыт.
- Никаких regression: 27 existing backend тестов Phase 3 остаются зелёными.
- pdf_determinism hash либо stable (ожидаемо для logo_path=null фикстуры), либо bumped с explanatory commit message.
</success_criteria>

<output>
Создать `.planning/phases/03-pdf/03-06-SUMMARY.md` с:
- Финальный API krilla 0.7 use для logo (`Image::from_png_bytes` vs `from_png`, точная сигнатура `surface.draw_image`).
- Дополнительные SQL queries добавленные в do_return (handover_qty + already_returned).
- Решение, изменился ли pinned SHA256 hash для act_42 (yes/no + причина).
- 5 новых интеграционных тестов (1 logo + 4 returns) с маппингом на ACT-11/ACT-13.
- Note: WR-01..WR-11 и IN-01..IN-06 из 03-REVIEW.md НЕ закрыты — это backlog для Phase 7 или follow-up gap-closure (в этом плане только BLOCKER).
</output>
