---
phase: 25-dropdown
plan: 04
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - ui/src/features/showcase/sections/TableSection.svelte
  - ui/src/features/showcase/ShowcasePage.svelte
autonomous: true
requirements: [CMP-06]

must_haves:
  truths:
    - "The showcase's new table section demonstrates all three TableRow states (normal/hover/selected) side by side, closing CMP-06's SC #1 visually (D-11)."
    - "The showcase's new table section demonstrates a collapsed AND an expanded group row with a count-pill badge and visible nested device rows, closing CMP-06's SC #2 visually."
    - "The showcase demonstrates all four Badge status tones (neutral/accent/warning/danger) inside table cells, mono identifiers, and a last row with no bottom border, per the UI-SPEC Showcase Contract."
    - "TableSection is wired into ShowcasePage.svelte following the exact 5-existing-sections precedent (import + <section class=\"showcase-block\">)."
  artifacts:
    - path: "ui/src/features/showcase/sections/TableSection.svelte"
      provides: "Static-data showcase gallery for Table/TableRow (CMP-06)"
      contains: "TableRow"
    - path: "ui/src/features/showcase/ShowcasePage.svelte"
      provides: "Wires TableSection as the 6th showcase section"
      contains: "TableSection"
  key_links:
    - from: "ui/src/features/showcase/ShowcasePage.svelte"
      to: "ui/src/features/showcase/sections/TableSection.svelte"
      via: "static import + <section class=\"showcase-block\"><TableSection /></section>"
      pattern: "TableSection"
    - from: "ui/src/features/showcase/sections/TableSection.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "static demo data rendered through Table + TableRow, mirroring TabsSection.svelte's static-array pattern"
      pattern: "import Table from"
---

<objective>
Build the CMP-06 showcase gallery: a new `TableSection.svelte` demonstrating every required `Table`/
`TableRow` state and variant from Plan 25-01 using static demo data (no live API calls, mirroring the
existing 5 showcase sections' pattern), wired into `ShowcasePage.svelte` as the 6th section.

Purpose: This is the visual-UAT surface for CMP-06 — the phase's SC #1 and #2 are proven here before (and
independently of) the `DeviceList` pilot (Plan 25-05), matching D-04's "showcase + pilot, not just one or the
other" decision.
Output: `ui/src/features/showcase/sections/TableSection.svelte`, updated `ShowcasePage.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-dropdown/25-CONTEXT.md
@.planning/phases/25-dropdown/25-UI-SPEC.md
@.planning/phases/25-dropdown/25-PATTERNS.md
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TableSection.svelte with static demo data</name>
  <files>ui/src/features/showcase/sections/TableSection.svelte</files>
  <read_first>
    - ui/src/features/showcase/sections/TabsSection.svelte (full file, 62 lines) — the exact structural
      template: `<script>` with local `$state`/static arrays only (no API imports), `<section>` -> `h2` ->
      one `.variant-block` per demo -> `.variant-label` (uppercase, `--tr-space-label`-styled) -> the
      component under demonstration. `<style lang="scss">` block: `.tabs-section { display: flex;
      flex-direction: column; gap: var(--tr-space-lg); }`, `.variant-block { display: flex; flex-direction:
      column; align-items: flex-start; gap: var(--tr-space-sm); }` — copy this exact nesting/spacing
      structure for `.table-section`.
    - ui/src/lib/components/Badge.svelte (full file, already read this session — `variant`/`size`/
      `appearance` props) — used for both the status-tone cells and the group count-pill inside this demo.
    - ui/src/lib/components/Table.svelte and ui/src/lib/components/TableRow.svelte (Plan 25-01's output) —
      the `head`/`children` snippet props and `selected`/`indent`/`last`/`group`/`groupExpanded`/`groupName`/
      `groupColspan`/`onToggleGroup` prop contract this task consumes.
    - .planning/phases/25-dropdown/25-UI-SPEC.md "Showcase Contract" section — the explicit checklist: normal/
      hover/selected row states, collapsed+expanded group row with count-pill and nested rows, all 4 badge
      tones, mono identifiers, last row without bottom border — "Обе секции — в обеих темах" (theme switcher
      already lives on the showcase page, no extra work needed here beyond token-only styling).
  </read_first>
  <action>
    Create `ui/src/features/showcase/sections/TableSection.svelte` with local static demo data (plain arrays
    of literal objects — inventory number, serial number, name, model, location, condition, a `status_id`
    1-4 mapped through the same `STATUS_LABELS`/`STATUS_VARIANTS` shape `DeviceListRow.svelte` already uses,
    for the 4-tone badge demonstration) and local `$state` for a demo group's `expanded` boolean (toggled by
    the group row's `onToggleGroup`, no API call). Structure as 3 `.variant-block`s: (1) "Состояния строки" —
    one small `<Table columns={...}>` with 3 `<TableRow>`s demonstrating normal / a CSS-only `:hover`-
    triggered state (real mouse hover, not simulated) / `selected` (one row rendered with `selected={true}`
    so the accent border + background are visible without interaction) — the LAST row in this block has
    `last={true}` to demonstrate the no-bottom-border rule; (2) "Строка-группа" — a `<Table>` with one
    `<TableRow group groupExpanded={demoExpanded} groupName="..." groupColspan={4}
    onToggleGroup={() => demoExpanded = !demoExpanded}>` whose trailing cells include a
    `<Badge variant="accent" appearance="count">{count} шт.</Badge>` count-pill, PLUS (when `demoExpanded`)
    2-3 nested plain `<TableRow indent>` rows underneath rendering demo device data; (3) "Бейджи статуса и
    моно-идентификаторы" — 4 plain rows, one per status tone, each showing a `tr-mono`-classed inventory/
    serial number cell (reuse the existing global `.tr-mono` class, do not redefine it locally) alongside its
    `<Badge variant={...}>{label}</Badge>`. Column headers passed via `Table`'s `head` snippet match the real
    pilot's copy: Наименование / Инвентарный № / Серийный № / Модель / Расположение / Состояние / Статус /
    Действия (D-10 — content/copy from the real screen, not invented). Zero hex/rgba literals in the
    `<style>` block.
  </action>
  <acceptance_criteria>
    - `grep -c "selected={true}\|selected\b" ui/src/features/showcase/sections/TableSection.svelte | grep -v '^0$'` confirms the selected-state demo exists.
    - `grep -c "group\b" ui/src/features/showcase/sections/TableSection.svelte | grep -v '^0$'` confirms the group-row demo exists.
    - `grep -c "appearance=\"count\"" ui/src/features/showcase/sections/TableSection.svelte` returns at least 1 (count-pill via Badge, not hand-rolled).
    - `grep -c "variant=\"default\"\|variant=\"accent\"\|variant=\"warning\"\|variant=\"destructive\"" ui/src/features/showcase/sections/TableSection.svelte` returns 4 (all four badge tones demonstrated).
    - `grep -c "last={true}\|last\b" ui/src/features/showcase/sections/TableSection.svelte | grep -v '^0$'` confirms the no-border-last-row demo exists.
    - `node ui/scripts/check-tokens.mjs` exits 0.
    - `pnpm --dir ui svelte-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>TableSection.svelte demonstrates all 3 row states, collapsed+expanded group row with count-pill and
  nested rows, all 4 badge tones, mono identifiers, and the no-border last row — using static demo data only.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TableSection into ShowcasePage.svelte</name>
  <files>ui/src/features/showcase/ShowcasePage.svelte</files>
  <read_first>
    - ui/src/features/showcase/ShowcasePage.svelte (full file, 60 lines, already read this session) — the
      exact wiring pattern: one `import` line per section, one `<section class="showcase-block">` block per
      section, in order, inside `.showcase-page`.
  </read_first>
  <action>
    In `ui/src/features/showcase/ShowcasePage.svelte`: add `import TableSection from
    './sections/TableSection.svelte';` after the existing `ModalSection` import, and add
    `<section class="showcase-block"><TableSection /></section>` after the existing `ModalSection` block
    (6th section, appended — do not reorder the existing 5). No other changes to this file — the CSS
    (`.showcase-block`) already applies generically to any section.
  </action>
  <acceptance_criteria>
    - `grep -c "TableSection" ui/src/features/showcase/ShowcasePage.svelte` returns 2 (import + usage).
    - `pnpm --dir ui lint` exits 0.
    - `pnpm --dir ui svelte-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>ShowcasePage.svelte renders TableSection as its 6th section, after ModalSection, unchanged ordering
  of the existing 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Static demo data only, zero API calls, admin-gated route unchanged from Phase 24 (this plan does not touch routing/sidebar gating). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-25-04-01 | Tampering (XSS) | Static demo strings | accept | Hardcoded literal Russian demo strings compiled into the bundle, rendered via default-escaped Svelte interpolation — no `{@html}`, no live data source. |
| T-25-04-02 | Elevation of Privilege | `/showcase` route gate | accept | Unchanged from Phase 24's already-accepted `roles: ['admin']` sidebar-filter gate (T-24-07-01) — this plan adds a section to an existing gated page, it does not touch the gate itself. |
| T-25-04-SC | Tampering | npm installs | accept | No new npm packages. |
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs` all exit 0.
`pnpm --dir ui build` succeeds. Visual confirmation happens live via the `/showcase` route (admin role,
`pnpm --dir ui build` required before LAN-browser UAT per the project's known gotcha).
</verification>

<success_criteria>
`/showcase` renders a 6th "Таблицы" section demonstrating all CMP-06 SC #1/#2 states and variants with static
demo data, matching TableRows.dc pixel values via the Plan 25-01 primitives.
</success_criteria>

<output>
Create `.planning/phases/25-dropdown/25-04-SUMMARY.md` when done
</output>
</content>
