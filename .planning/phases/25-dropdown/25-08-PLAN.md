---
phase: 25-dropdown
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/Dropdown.svelte
autonomous: true
requirements: [CMP-07]
gap_closure: true

must_haves:
  truths:
    - "Roadmap SC #4 ('Dropdown корректно отображает список с группами через drill-in ... а не заголовками секций') holds across a full session, not just the first drill-in: closing the panel after a manual drill-in without picking (Escape/Tab/click-outside), then reopening (focus/click), shows the current groups list — not the previously-drilled-in group's stale member list (WR-02, openPanel() reset)."
    - "Tab on an expandable group in groups-view never both starts an async drillInto and synchronously closes the panel — the pick is never silently lost, and the component is never left primed to show a stale drilled-in list on next open (WR-01, Tab branch guard)."
    - "Plan 25-03's must_have — 'Dropdown adds the full combobox ARIA pattern ... plus member-mode keyboard navigation' and 'does not regress any of the pre-existing keyboard/ARIA behaviors' — now holds for BOTH field variants, including the select-variant's in-panel search input, which owns its own onkeydown={handleKeydown}, aria-activedescendant, and aria-controls once it holds focus (WR-06)."
    - "CR-01 (handleInput sets open = true, reopening the panel on typed input) and CR-02 (expandSeq generation token guarding both the AUTO-05 auto-flatten effect and drillInto against stale async writes) remain intact and are not regressed by the WR-01/WR-02/WR-06 fixes — openPanel() becomes a third participant in the same expandSeq counter rather than a parallel, uncoordinated reset."
  artifacts:
    - path: "ui/src/lib/components/Dropdown.svelte"
      provides: "openPanel() fully resets drill-in state and cancels stale in-flight drill-ins; groups-view Tab never drills into an expandable group; select-variant search input is inside the D-12 keyboard/ARIA layer"
      contains: "expandSeq++"
  key_links:
    - from: "ui/src/lib/components/Dropdown.svelte#openPanel"
      to: "ui/src/lib/components/Dropdown.svelte (AUTO-05 effect / drillInto)"
      via: "openPanel() increments the shared expandSeq counter before resetting viewMode/activeGroup/members/showBack, so any drillInto promise still in flight from before the panel closed is dropped by the existing `seq !== expandSeq` guard instead of force-writing over the reset"
      pattern: "expandSeq++"
    - from: "ui/src/lib/components/Dropdown.svelte select-variant search input"
      to: "ui/src/lib/components/Dropdown.svelte#handleKeydown"
      via: "onkeydown={handleKeydown} wired onto the in-panel search input, the same shared handler already used by the combobox input and the select-variant trigger button"
      pattern: "onkeydown={handleKeydown}"
---

<objective>
Close the three blocking gaps `25-VERIFICATION.md` found in `ui/src/lib/components/Dropdown.svelte` — all confirmed by `25-REVIEW.md` with concrete suggested diffs, all left unfixed by the user's explicit "blockers-only" scope decision after code review (only the two criticals CR-01/CR-02 were fixed at the time):

1. **WR-02** — `openPanel()` never resets `viewMode`/`activeGroup`/`members`/`showBack`, so reopening the panel after a manual drill-in (without picking) can render a stale member list.
2. **WR-01** — the groups-view `Tab` branch drills into an expandable group asynchronously but closes the panel synchronously, silently losing the pick and feeding directly into the WR-02 defect.
3. **WR-06** — the select-variant's in-panel search input has no `onkeydown`, so once it holds focus the entire D-12 keyboard layer (Escape/Arrows/Home/End/Enter/Tab) is dead.

Purpose: These three defects falsify Roadmap SC #4 (grouped drill-in) and Plan 25-03's own "full combobox ARIA pattern... plus member-mode keyboard navigation" must_have for the select variant — both are load-bearing claims CMP-07 depends on, and both are reachable through ordinary interaction in the phase's one production consumer (`ActFormItemsTable.svelte`), not contrived edge cases.

Out of scope (explicit user "blockers-only" decision, do not touch): WR-05 (listbox/`<li>` role nesting), WR-09 (`DeviceGroupRow` retry-forever), IN-01 (dead `.hint-warn` CSS), IN-06 (showcase force-open + flat checkmark demo self-contradiction). None of these get tasks in this plan.

Output: `ui/src/lib/components/Dropdown.svelte` with WR-01/WR-02/WR-06 fixed, CR-01/CR-02 unregressed, portal/anchor files (`portal.ts`, `dropdownAnchor.ts`) untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-dropdown/25-CONTEXT.md
@.planning/phases/25-dropdown/25-VERIFICATION.md
@.planning/phases/25-dropdown/25-REVIEW.md
@ui/src/lib/components/Dropdown.svelte
</context>

<interfaces>
<!-- Current (pre-fix) state of the three hunks this plan touches, read in full this session. -->

Current `openPanel` (lines 282-290):
```
function openPanel(query: string) {
  if (searchDebounce) clearTimeout(searchDebounce);
  open = true;
  activeIndex = -1;
  onSearch(query);
}
```

Current groups-view `Tab` branch inside `handleKeydown` (lines 384-389):
```
} else if (e.key === 'Tab') {
  if (activeIndex >= 0 && activeIndex < groups.length) {
    handleOptionClick(groups[activeIndex]);
  }
  open = false;
}
```

Current select-variant in-panel search input (lines 509-520):
```
<li class="tr-dropdown-search">
  <span class="tr-dropdown-search-box">
    <span class="tr-dropdown-search-icon" aria-hidden="true">⌕</span>
    <input
      type="text"
      class="tr-dropdown-search-input"
      aria-label="Поиск"
      placeholder={searchPlaceholder}
      oninput={handleInput}
    />
  </span>
</li>
```

Confirmed baselines in the current file (grep counts to diff against post-fix):
- `expandSeq` (any occurrence, declaration + effect + drillInto): 6 lines (160, 167, 172, 183, 199, 201).
- `onkeydown={handleKeydown}` (combobox input line 471, select-variant button line 489): 2.
- `aria-activedescendant={activeOptionId()}` (combobox input line 468, select-variant button line 487): 2.
- `aria-controls={panelId}` (combobox input line 466, select-variant button line 485): 2.
- `onmousedown={(e) => e.preventDefault()}` (back button, member option, group option rows): 3 — must NOT change in this plan.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix WR-02 (openPanel drill-in reset) and WR-01 (Tab branch guard)</name>
  <files>ui/src/lib/components/Dropdown.svelte</files>
  <read_first>
    - ui/src/lib/components/Dropdown.svelte — `openPanel()` (lines 285-290), the groups-view `Tab` branch
      inside `handleKeydown` (lines 384-389), and the full `expandSeq`/`drillInto`/AUTO-05-effect mechanics
      (lines 147-219) that both fixes must stay coordinated with — already read in full this session, see
      `<interfaces>` above for the exact current bodies.
    - .planning/phases/25-dropdown/25-REVIEW.md WR-01 section (Tab-loses-pick) and WR-02 section
      (openPanel-doesn't-reset) — the suggested diffs this task implements, adapted to also cancel any
      in-flight `drillInto` via `expandSeq` (REVIEW.md's own diff resets state but does not increment
      `expandSeq`; this task closes that residual race so a slow, still-in-flight `drillInto` promise from
      before the panel closed cannot force-write over the just-reset state after reopen).
  </read_first>
  <action>
    In `openPanel(query: string)`: immediately after the existing `activeIndex = -1;` line and before the
    existing `onSearch(query);` call, insert `expandSeq++;` followed by four resets —
    `viewMode = 'groups';`, `activeGroup = null;`, `members = [];`, `showBack = false;` — in that order.
    The `expandSeq++` must come first (mirrors the AUTO-05 effect's own else-branch, which cancels the
    in-flight expand via `expandSeq++` before resetting view state at lines 183-188): this makes `openPanel`
    a third call site sharing the one `expandSeq` counter alongside the AUTO-05 effect and `drillInto`, so a
    `drillInto` promise still awaiting `onExpandGroup` from before the panel was closed gets dropped by the
    existing `if (seq !== expandSeq) return;` guard instead of overwriting the freshly-reset state once it
    resolves. Do not add a second, parallel invalidation mechanism — reuse `expandSeq` exactly as it already
    works.

    In `handleKeydown`'s groups-view `Tab` branch: replace the `handleOptionClick(groups[activeIndex])` call
    with a direct synchronous commit that never enters the async `drillInto` path — bind
    `const g = groups[activeIndex];` and call `onPickGroup(g)` directly only when BOTH (a) `g` is truthy and
    (b) the group is NOT an expandable group in non-flat mode (i.e. skip the call when `!flat &&
    isGroupExpandable(g)` is true). The truthiness check on `g` is NOT optional and must be evaluated BEFORE
    `isGroupExpandable(g)` — this preserves, in a new shape, the bounds check the current code performs via
    `if (activeIndex >= 0 && activeIndex < groups.length)`, which makes Tab-with-no-active-option a safe
    no-op. Dropping it is a reachable crash, not a theoretical one: focusing the field runs AUTO-02 →
    `openPanel()` → `activeIndex = -1`, so pressing Tab immediately (the ordinary "skip this field"
    interaction, no arrow key first) evaluates `groups[-1]` → `undefined`, and both production consumers
    dereference their argument unconditionally — `ActFormItemsTable.svelte:176` (`isExpandable`) reads
    `g.ids.length`, and the showcase combobox demo `DropdownSection.svelte:121` reads `g.expandable`. Match
    25-REVIEW.md's WR-01 diff shape, which keeps this guard (`if (g && !(!flat && isGroupExpandable(g)))
    onPickGroup(g);`). Either the `g &&` conjunct or an equivalent retained numeric bounds check on
    `activeIndex` before the array read satisfies this — what must not happen is any path that reaches
    `isGroupExpandable(g)` with `g` possibly `undefined`. For an expandable group (and for no-active-option),
    `Tab` now closes the panel (`open = false;`, unchanged) without picking and without starting a drill-in —
    matching the existing groups-view `Escape` behavior of "closing wins, no partial navigation state left
    behind." Leave the member-view `Tab` branch (lines 425-430) untouched — members are always leaf picks with
    no drill-in path, so WR-01 does not apply there.
  </action>
  <acceptance_criteria>
    - `grep -n -A 8 "function openPanel" ui/src/lib/components/Dropdown.svelte` shows, in order after
      `activeIndex = -1;`: `expandSeq++;`, `viewMode = 'groups';`, `activeGroup = null;`, `members = [];`,
      `showBack = false;`, then `onSearch(query);`.
    - `grep -no "expandSeq" ui/src/lib/components/Dropdown.svelte | wc -l` returns 7 (up from the confirmed
      pre-fix baseline of 6 — confirms `openPanel` now participates in the shared counter, not a new one).
    - `grep -n -B2 -A6 "e.key === 'Tab'" ui/src/lib/components/Dropdown.svelte` — the FIRST occurrence
      (groups-view branch) no longer contains `handleOptionClick`, and instead shows a conditional
      `onPickGroup(g)` guarded by `isGroupExpandable`.
    - `grep -c "handleOptionClick(groups\[activeIndex\])" ui/src/lib/components/Dropdown.svelte` returns 0
      (the old unguarded Tab call site is gone; `handleOptionClick` itself, called from `onclick`/`Enter`,
      is untouched and still present elsewhere).
    - Null/bounds guard retained: `grep -n -A6 "e.key === 'Tab'" ui/src/lib/components/Dropdown.svelte` —
      the FIRST occurrence (groups-view branch) contains either a `g &&` conjunct or a retained
      `activeIndex >= 0 && activeIndex < groups.length` bounds check, positioned so it is evaluated BEFORE
      any `isGroupExpandable(g)` call on that branch. A Tab branch that calls `isGroupExpandable(g)` with no
      preceding guard on `g`/`activeIndex` FAILS this criterion.
    - Non-regression: `grep -n "open = true;" ui/src/lib/components/Dropdown.svelte` still shows `handleInput`
      (CR-01, ~line 276) setting `open = true` on every keystroke — untouched by this task.
    - `pnpm --dir ui svelte-check` exits 0.
    - `node ui/scripts/check-tokens.mjs` exits 0.
    - Human repro (25-VERIFICATION.md "Human Verification Required" item 2): in the Acts form's device
      picker, drill into a multi-device group, close the panel without picking (Escape or click outside),
      then reopen it (refocus the field) — the panel must show the current groups list, not the previously
      drilled-in group's members.
    - Human repro (WR-01, Tab): in the same picker, arrow down to an expandable group in groups-view and
      press Tab — the panel closes immediately with no visible flash of a member list and no device picked;
      reopening afterward still shows the fresh groups list (confirms the WR-01 fix does not itself leave a
      stale-reopen path, satisfying the same truth as item 2).
    - Human repro (Tab with no active option — exercises `activeIndex === -1`, which the repro above never
      reaches because it arrows down first): in the same picker, focus the device field so the panel opens
      via AUTO-02 but press NO arrow key, then press Tab immediately — the panel closes, focus moves to the
      next field, no device is picked, and the browser/Tauri devtools console shows NO error (specifically no
      `Cannot read properties of undefined (reading 'ids')` from `ActFormItemsTable`'s `isExpandable`).
      Repeat the same focus-then-Tab-immediately sequence on the Showcase → Dropdown → combobox demo block,
      whose `isGroupExpandable` reads `g.expandable` and would throw identically.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs</automated>
  </verify>
  <done>openPanel() fully resets the drill-in state machine and cancels any in-flight drillInto via the
  shared expandSeq counter before reopening; Tab in groups-view never drills into an expandable group, only
  directly commits non-expandable picks, then always closes. Reopening after a drill-without-pick shows the
  current groups list, never a stale member list.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-06 — wire the select-variant in-panel search input into the D-12 keyboard/ARIA layer</name>
  <files>ui/src/lib/components/Dropdown.svelte</files>
  <read_first>
    - ui/src/lib/components/Dropdown.svelte lines 452-472 (the combobox-variant `<input>`, showing the
      existing `role="combobox"`/`aria-expanded`/`aria-controls`/`aria-haspopup`/`aria-activedescendant`/
      `onkeydown={handleKeydown}` wiring pattern to mirror) and lines 506-520 (the select-variant in-panel
      search `<input class="tr-dropdown-search-input">` this task edits — see `<interfaces>` above for its
      exact current body).
    - .planning/phases/25-dropdown/25-REVIEW.md WR-06 section — "Add `onkeydown={handleKeydown}` to the
      search input (and mirror `aria-activedescendant`/`aria-controls` onto it, since it now owns focus)."
  </read_first>
  <action>
    On the select-variant in-panel search `<input class="tr-dropdown-search-input">`, add three attributes:
    `onkeydown={handleKeydown}` (the same shared handler function already wired to the combobox `<input>`
    and the select-variant trigger `<button>` — do not create a second handler), `aria-activedescendant={activeOptionId()}`,
    and `aria-controls={panelId}`. Once a user clicks into this box it — not the trigger button — holds DOM
    focus, so it is the element a screen reader consults for these live ARIA relationships; mirroring them
    here closes the gap without touching the trigger button's own copies. Do NOT add `onmousedown`
    preventDefault to this input: unlike the option-row buttons and the "← Назад" button (which
    `preventDefault` their `mousedown` specifically to keep DOM focus on the field/trigger while clicking an
    option), this search input is meant to receive real focus on click — that is how typing into it works at
    all. Do not touch `oninput={handleInput}` on this input, and do not add `role="combobox"` or
    `aria-expanded`/`aria-haspopup` to it — those stay out of scope (WR-06's fix list is onkeydown +
    aria-activedescendant + aria-controls only; broader ARIA re-architecture of this input is not requested).
  </action>
  <acceptance_criteria>
    - `grep -c "onkeydown={handleKeydown}" ui/src/lib/components/Dropdown.svelte` returns 3 (was 2 pre-fix:
      combobox input + select-variant trigger button; the in-panel search input is now the third).
    - `grep -c "aria-activedescendant={activeOptionId()}" ui/src/lib/components/Dropdown.svelte` returns 3
      (was 2 pre-fix).
    - `grep -c "aria-controls={panelId}" ui/src/lib/components/Dropdown.svelte` returns 3 (was 2 pre-fix).
    - `grep -n -B4 "tr-dropdown-search-input" ui/src/lib/components/Dropdown.svelte` shows `onkeydown={handleKeydown}`,
      `aria-activedescendant={activeOptionId()}`, and `aria-controls={panelId}` all present on the same
      `<input>` element that still carries `oninput={handleInput}` and `class="tr-dropdown-search-input"`.
    - Non-regression: `grep -c "onmousedown={(e) => e.preventDefault()}" ui/src/lib/components/Dropdown.svelte`
      still returns 3 (unchanged from pre-fix baseline — confirms no preventDefault was added to the search
      input, and none was removed from the existing back-button/option-row rows).
    - `pnpm --dir ui svelte-check` exits 0.
    - `pnpm --dir ui lint` exits 0 (this script already chains `node scripts/check-tokens.mjs` internally per
      `ui/package.json`, so the token gate is covered here — do not invoke it a second time standalone).
    - `pnpm --dir ui build` exits 0.
    - Human repro (25-VERIFICATION.md "Human Verification Required" item 3): `pnpm --dir ui build`, then open
      Showcase → Dropdown section, "Плоский селект" block (select-variant); click to open, click into the
      in-panel search box, type to filter, then try Escape / ArrowUp / ArrowDown / Home / End / Enter / Tab —
      all must behave exactly as when focus is on the combobox-variant field or the select trigger button
      (Escape closes, arrows navigate with visible active-row highlight + scrollIntoView, Enter picks the
      active option, Tab commits-then-closes per Task 1's guard for expandable rows).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check && pnpm --dir ui lint && pnpm --dir ui build</automated>
  </verify>
  <done>The select-variant's in-panel search input is fully inside the D-12 keyboard/ARIA layer: onkeydown={handleKeydown}
  plus its own aria-activedescendant/aria-controls are wired, with zero change to the onmousedown-preventDefault
  convention on option/back-button rows and zero change to handleInput's existing behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan is a pure client-side UI state-machine and keyboard-wiring fix inside an existing, already-reviewed presentation component. No new inputs, no new data source, no new network call, no persistence. The three edits touch internal `$state` reset logic (`openPanel`), an internal keydown branch (`Tab`), and DOM attributes on an existing `<input>` that already renders caller-supplied text via default-escaped Svelte interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08-01 | Tampering (XSS) | Select-variant search input, option rows (unchanged rendering) | accept | No `{@html}` before or after this plan (continues Plan 25-02/25-03's default-escaped-text-only discipline); this plan adds zero new render paths for untrusted strings. |
| T-25-08-02 | Denial of Service (client-side) | `expandSeq` counter used as a cancellation token | accept | Plain incrementing `let` counter, not reactive, bounded by user interaction rate — cannot overflow or be driven by an attacker faster than the existing AUTO-05/drillInto call sites already allow. |
| T-25-08-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs — this plan edits one existing `.svelte` file only. |
</threat_model>

<verification>
`pnpm --dir ui svelte-check && pnpm --dir ui lint && node ui/scripts/check-tokens.mjs && pnpm --dir ui build`
all exit 0. Then the two live human repros from `25-VERIFICATION.md` ("Human Verification Required" items 2
and 3): (2) drill-in → close-without-picking → reopen shows fresh groups, not stale members; (3) select-variant
in-panel search box has full keyboard/ARIA parity with the combobox field. `pnpm --dir ui build` is required
before either live check (server-mode/LAN-browser serves `ui/dist`, which goes stale otherwise — project
gotcha).
</verification>

<success_criteria>
Roadmap SC #4 (grouped drill-in) and Plan 25-03's "full combobox ARIA pattern ... plus member-mode keyboard
navigation" must_have both hold under a full interaction session (drill-in → close → reopen; Tab on an
expandable group; typing in the select-variant's in-panel search box) — not just the first-use path already
covered by the two previously-fixed criticals (CR-01, CR-02), which remain unregressed.
</success_criteria>

<output>
Create `.planning/phases/25-dropdown/25-08-SUMMARY.md` when done
</output>
