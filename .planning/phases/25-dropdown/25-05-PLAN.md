---
phase: 25-dropdown
plan: 05
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - ui/src/features/devices/DeviceListRow.svelte
  - ui/src/features/devices/DeviceGroupRow.svelte
  - ui/src/features/devices/DeviceList.svelte
autonomous: true
requirements: [CMP-06]

must_haves:
  truths:
    - "The Devices table (DeviceList.svelte) renders through Table/TableRow — its rows and header, not window chrome/filters/layout (D-04, D-05, D-06 — WIN-02 stays Phase 26)."
    - "DeviceGroupRow's group-row visuals now use the --tr-group token and Badge appearance=count for the count-pill (replacing the pre-existing color-mix background and hand-rolled .count-pill span), and the chevron rotates 90deg on expand (not the pre-existing 180deg) — a spec correction, not a new feature."
    - "DeviceGroupRow's pre-existing 'strong divider on group-end' behavior (isLastInGroup on the last nested DeviceListRow) is preserved pixel-for-pixel via TableRow's class passthrough, not silently dropped during the migration."
    - "No row-selection mechanic is introduced in Devices (D-11) — TableRow's selected prop is never set to true anywhere in this pilot; only normal/hover states are exercised by real usage."
    - "DeviceList/DeviceListRow/DeviceGroupRow is the ONLY table migrated in this phase — UsersList, ReportTable, DiscoveryResultsTable, CartridgeListRow, and ModelListRow are untouched, deferred to phases 26-28, per D-08."
  artifacts:
    - path: "ui/src/features/devices/DeviceListRow.svelte"
      provides: "Consumes TableRow for row background/hover states"
      contains: "TableRow"
    - path: "ui/src/features/devices/DeviceGroupRow.svelte"
      provides: "Consumes TableRow's group mode + Badge appearance=count"
      contains: "appearance=\"count\""
    - path: "ui/src/features/devices/DeviceList.svelte"
      provides: "Consumes Table shell for header/loading-skeleton/empty-state"
      contains: "import Table from"
  key_links:
    - from: "ui/src/features/devices/DeviceList.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "head/children snippet props"
      pattern: "import Table from"
    - from: "ui/src/features/devices/DeviceListRow.svelte"
      to: "ui/src/lib/components/TableRow.svelte"
      via: "row-state wrapper around existing <td> cells"
      pattern: "import TableRow from"
    - from: "ui/src/features/devices/DeviceGroupRow.svelte"
      to: "ui/src/lib/components/TableRow.svelte"
      via: "group-mode wrapper (group, groupExpanded, groupName, groupColspan, onToggleGroup)"
      pattern: "onToggleGroup=\\{toggleExpand\\}"
---

<objective>
Migrate the Devices screen's table — `DeviceList.svelte` (shell), `DeviceListRow.svelte` (row), and
`DeviceGroupRow.svelte` (group row) — onto the `Table`/`TableRow` primitives from Plan 25-01. Business logic
(expand/collapse fetching, status mapping, group stable keys, edit/delete callbacks) is UNCHANGED — only the
markup/styling responsibility moves onto the new primitives, per D-04/D-05's "pilot proves no regression"
requirement and D-10's "content from the app, styles from the primitive" rule.

Purpose: This is the second half of CMP-06's proof surface (alongside Plan 25-04's showcase) and the only
in-scope real-screen migration for Phase 25 (D-08 — the other 5 tables move in phases 26-28).
Output: `DeviceListRow.svelte`, `DeviceGroupRow.svelte`, `DeviceList.svelte` all consuming the new primitives,
visually unchanged except where UI-SPEC's `.dc`-sourced values differ from the pre-existing implementation
(hover color, group background, count-pill shape, chevron rotation angle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-dropdown/25-CONTEXT.md
@.planning/phases/25-dropdown/25-UI-SPEC.md
@.planning/phases/25-dropdown/25-PATTERNS.md
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DeviceListRow.svelte and DeviceGroupRow.svelte to TableRow</name>
  <files>ui/src/features/devices/DeviceListRow.svelte, ui/src/features/devices/DeviceGroupRow.svelte</files>
  <read_first>
    - ui/src/features/devices/DeviceListRow.svelte (full file, 117 lines, already read this session) — the
      `<tr class="device-row" class:group-last-child={isLastInGroup}>` wrapper and `.device-row`/`.cell`
      CSS this task replaces; the `<td>` cell content (name/inv/serial/model/location/state/[status]/
      actions) and the `Badge` status-tone usage (lines 61-64) stay UNCHANGED — only the outer `<tr>` wrapper
      changes.
    - ui/src/features/devices/DeviceGroupRow.svelte (full file, 308 lines, already read this session) — the
      `<tr class="group-row" onclick={toggleExpand}>` wrapper (lines 144-187), the hand-rolled chevron SVG
      button (lines 148-173, `180deg` rotation) and `.count-pill` span (line 185) this task replaces with
      `TableRow`'s built-in group-mode chevron and a `Badge appearance="count"` respectively; the
      `groupStableKey`/`toggleExpand`/`$effect` auto-load-children logic (lines 17-141) and the nested
      `DeviceListRow` rendering loop (lines 189-206) stay UNCHANGED.
    - ui/src/lib/components/TableRow.svelte (Plan 25-01's output) — the exact prop names (`selected`,
      `indent`, `last`, `group`, `groupExpanded`, `groupName`, `groupColspan`, `onToggleGroup`, `class`,
      `children`) this task wires against.
    - .planning/phases/25-dropdown/25-UI-SPEC.md "Строка-группа (CMP-06, SC #2)" section — confirms the
      `90deg` chevron rotation (correcting the current file's `180deg`) and the Badge-reuse rule for the
      count-pill.
  </read_first>
  <action>
    In `DeviceListRow.svelte`: replace the `<tr class="device-row" class:group-last-child={isLastInGroup}>`
    wrapper with `<TableRow class={isLastInGroup ? 'group-last-child' : undefined}>` (passthrough class
    preserves the file's own local `:global(tr.group-last-child > td) { border-bottom: 2px solid
    var(--tr-border-strong); }` override, kept in this file's `<style>` block unchanged — this is a distinct,
    pre-existing "group-end divider" concept, NOT `TableRow`'s `last` prop, per Plan 25-01's `<interfaces>`
    note). Do not set `selected` (D-11 — no selection mechanic exists in Devices). Move the existing `<td>`
    cells inside `TableRow`'s default slot/children unchanged. Remove the ENTIRE `.device-row` rule from this file's
    `<style>` block — both its `height: var(--row-height, 40px)` and its `&:hover { background:
    var(--tr-surface); }` are now owned by `TableRow` (and this migration FIXES two pre-existing spec
    deviations in the process: the old hover used `--tr-surface` where UI-SPEC requires `--tr-row-hover`, and
    the row height now comes from `TableRow`'s base `<td>` rule at the reference's 40px).

    From the `.cell` rule, DELETE the three declarations `TableRow`'s base `<td>` rule now owns —
    `padding: 0 var(--tr-space-xs)` (the old 8px value; the reference and `TableRow` use `0 10px`),
    `border-bottom: 1px solid var(--tr-border)`, and `vertical-align: middle`. KEEP the remaining `.cell`
    declarations, which are content-specific and have no `TableRow` equivalent: `font-size`, `color`,
    `white-space: nowrap`, `overflow: hidden`, `text-overflow: ellipsis`, `max-width: 0` (the table-cell
    ellipsis trick). Keep `.cell-name`/`.cell-numeric`/`.cell-status`/`.cell-actions` entirely (column widths,
    tabular-nums, per-column overflow overrides) — those are column-specific and stay local to this file.

    Do NOT re-add `padding`, `height`, or `border-bottom` anywhere in this file's cell styling — if the
    migrated rows look wrong, the fix belongs in `TableRow.svelte`, not in a local override here. A local
    override would silently re-diverge this pilot from the primitive that phases 26-28's five remaining
    tables will consume.

    In `DeviceGroupRow.svelte`: replace the `<tr class="group-row" onclick={toggleExpand}>` + hand-rolled
    chevron `<button>` + name text with `<TableRow group groupExpanded={expanded} groupName={group.repr.name}
    groupColspan={4} onToggleGroup={toggleExpand}>`, keeping the row's REMAINING cells (location, condition
    via `conditionDisplay`, status `Badge`, and the count-pill) as `TableRow`'s children — replace the
    hand-rolled `<span class="count-pill">{group.count} шт.</span>` with `<Badge variant="accent"
    appearance="count">{group.count} шт.</Badge>`. Remove the now-redundant `.group-row` (`color-mix`
    background), `.chevron-btn` (`180deg` rotation), and `.count-pill` (`color-mix` border/background) rules
    from this file's `<style>` block — `TableRow`'s group mode now owns all three. Keep `.cell-truncate`/
    `.cell-name-wide`/`.cell-status`/`.cell-actions`/`.children-loading*` (column-specific, unchanged).
  </action>
  <acceptance_criteria>
    - `grep -c "import TableRow from" ui/src/features/devices/DeviceListRow.svelte` returns 1.
    - `grep -c "import TableRow from" ui/src/features/devices/DeviceGroupRow.svelte` returns 1.
    - `grep -c "color-mix" ui/src/features/devices/DeviceGroupRow.svelte` returns 0 (both color-mix usages —
      group background and count-pill border/background — removed, superseded by --tr-group and Badge).
    - `grep -c "appearance=\"count\"" ui/src/features/devices/DeviceGroupRow.svelte` returns 1.
    - `grep -c "rotate(180deg)" ui/src/features/devices/DeviceGroupRow.svelte` returns 0 (chevron rotation
      now owned by TableRow at 90deg, the old 180deg rule is gone).
    - `grep -c "group-last-child" ui/src/features/devices/DeviceListRow.svelte` returns at least 1 (passthrough
      class preserved).
    - Superseded base-cell declarations are GONE from the pilot (D-10 — TableRow is now the sole owner):
      `grep -c "padding: 0 var(--tr-space-xs)" ui/src/features/devices/DeviceListRow.svelte` returns 0,
      `grep -c "border-bottom: 1px solid var(--tr-border)" ui/src/features/devices/DeviceListRow.svelte` returns 0,
      and `grep -c "\.device-row" ui/src/features/devices/DeviceListRow.svelte` returns 0 (rule fully removed).
    - Content-specific cell styling SURVIVED the deletion (guards against over-deleting):
      `grep -c "max-width: 0" ui/src/features/devices/DeviceListRow.svelte` returns at least 1, and
      `grep -c "cell-numeric\|cell-status\|cell-actions" ui/src/features/devices/DeviceListRow.svelte | grep -v '^0$'`
      confirms the column-width classes remain.
    - `node ui/scripts/check-tokens.mjs` exits 0.
    - `pnpm --dir ui svelte-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>DeviceListRow.svelte and DeviceGroupRow.svelte both consume TableRow; the pre-existing color-mix
  chevron/count-pill hand-rolling is gone, replaced by --tr-group + Badge appearance=count; the group-end
  strong-divider behavior is preserved via class passthrough; no selection mechanic was added.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate DeviceList.svelte to the Table shell</name>
  <files>ui/src/features/devices/DeviceList.svelte</files>
  <read_first>
    - ui/src/features/devices/DeviceList.svelte (full file, 251 lines, already read this session) — the
      skeleton branch (lines 58-84), empty-state branch (lines 85-89), real-table branch (lines 90-146), and
      `<style>` block (lines 149-251, largely superseded by `Table.svelte`'s own styling except the
      column-width rules `.th-name`/`.th-numeric`/`.th-condition`/`.th-status`/`.th-actions` and
      `.list-footer`/`.pagination-info`, which stay local).
    - ui/src/lib/components/Table.svelte (Plan 25-01's output) — the `columns`/`loading`/`empty`/
      `emptyTitle`/`emptyBody`/`skeletonRows`/`head`/`children` prop contract this task wires against.
    - .planning/phases/25-dropdown/25-CONTEXT.md D-06 — reconfirms this task touches ONLY the `<table>`/
      `<thead>`/skeleton/empty markup inside `DeviceList.svelte`, not the surrounding window (filters,
      layout, page header) — those stay Phase 26 (WIN-02), untouched by this plan.
  </read_first>
  <action>
    Replace `DeviceList.svelte`'s three-way `{#if loading...}{:else if isEmpty}{:else}` `<table>` branches
    with a single `<Table columns={showStatus ? 8 : 7} loading={loading && items.length === 0 && groups.length
    === 0} empty={isEmpty} emptyTitle={emptyMessage} emptyBody={emptySubtext}>` call. Pass the existing header
    row (`Наименование`/`Инвентарный №`/`Серийный №`/`Модель`/`Расположение`/`Состояние`/`[Статус]`/
    `Действия`, unchanged copy and conditional `showStatus` column) as the `head` snippet — keep the
    `.th-name`/`.th-numeric`/`.th-condition`/`.th-status`/`.th-actions` width classes on each `<th>`, they
    stay local to this file since `Table.svelte` doesn't own per-column widths. Pass the existing
    `showGroups`/`groups`/`items` render loop (the `DeviceGroupRow`/`DeviceListRow` calls, completely
    unchanged) as the `children` snippet. Keep `<footer class="list-footer">` OUTSIDE the `<Table>` call,
    exactly where it is today (it is not part of the table shell contract). Remove the now-redundant
    `.device-table`/`.header-row`/`.th` (non-width parts)/`.empty-state`/`.empty-title`/`.empty-body`/
    `.skeleton-row`/`.skeleton-cell`/`.skeleton-block`/`@keyframes pulse` rules from this file's `<style>`
    block — `Table.svelte` now owns all of them.
  </action>
  <acceptance_criteria>
    - `grep -c "import Table from" ui/src/features/devices/DeviceList.svelte` returns 1.
    - `grep -c "<table" ui/src/features/devices/DeviceList.svelte` returns 0 (no more locally-owned `<table>`
      element — it now lives inside `Table.svelte`).
    - `grep -c "@keyframes pulse" ui/src/features/devices/DeviceList.svelte` returns 0 (moved into
      Table.svelte, no local duplicate).
    - `grep -c "list-footer" ui/src/features/devices/DeviceList.svelte` returns at least 1 (footer stays
      outside the Table shell, unmoved).
    - `node ui/scripts/check-tokens.mjs` exits 0.
    - `pnpm --dir ui svelte-check` exits 0.
    - `pnpm --dir ui build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check && pnpm --dir ui build</automated>
  </verify>
  <done>DeviceList.svelte renders through the Table shell for header/skeleton/empty-state; the pagination
  footer stays outside the shell; the showGroups/DeviceGroupRow/DeviceListRow business logic is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Devices data rendering | Unchanged — this plan only swaps markup/styling responsibility onto new primitives; all data (device.name, inventory_no, etc.) already flows through this same code path today, already authorized server-side before reaching these components. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-25-05-01 | Tampering (XSS) | Device field rendering (name/model/location/state) | accept | No change to how these fields are rendered — still default-escaped Svelte text interpolation before and after this migration; no `{@html}` anywhere in the 3 touched files. |
| T-25-05-02 | Elevation of Privilege | N/A | accept | No new selection/action mechanic is added (D-11 explicitly out of scope); existing edit/delete/print callbacks are unchanged, still gated by the pre-existing role checks in the parent `DevicesPage`. |
| T-25-05-SC | Tampering | npm installs | accept | No new npm packages. |
</threat_model>

<verification>
`node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check && pnpm --dir ui build` all exit 0.
`pnpm --dir ui build` required before live LAN-browser verification (project gotcha — server mode serves
`ui/dist`, not live source). Visual/behavioral confirmation: Devices screen renders identically in structure
(same columns, same expand/collapse behavior, same edit/delete/print actions) with the corrected `.dc`-exact
hover/group/chevron/count-pill visuals.
</verification>

<success_criteria>
DeviceList/DeviceListRow/DeviceGroupRow all consume Table/TableRow; group-row visuals match TableRows.dc
exactly (--tr-group background, Badge count-pill, 90deg chevron); the pre-existing group-end strong-divider
and all edit/delete/expand/collapse behavior are unregressed; no row-selection mechanic was introduced.
</success_criteria>

<output>
Create `.planning/phases/25-dropdown/25-05-SUMMARY.md` when done
</output>
</content>
