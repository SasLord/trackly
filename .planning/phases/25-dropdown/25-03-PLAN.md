---
phase: 25-dropdown
plan: 03
type: execute
wave: 2
depends_on: ["25-02"]
files_modified:
  - ui/src/lib/components/Dropdown.svelte
autonomous: true
requirements: [CMP-07]

must_haves:
  truths:
    - "Dropdown's select variant (value display + arrow, in-panel search box) matches Dropdown.dc pixel values (D-03) and has no direct codebase precedent — this sub-variant is built from the .dc reference, not extracted from existing code (PATTERNS.md 'No Analog Found')."
    - "Dropdown's flat-list mode (SC #3) shows a checkmark on the selected option instead of a group's x{N} count, matching UI-SPEC's flat-vs-grouped option-row contract."
    - "Dropdown does not regress any of the 8 pre-existing keyboard/ARIA behaviors ActFormItemsTable.svelte already has (role=listbox/option, aria-autocomplete=list, aria-selected, ArrowDown-opens-closed-panel, cyclic ArrowUp/Down in groups-mode, Enter selects in groups-mode, Enter suppressed in member-mode (WR-02), Escape closes+stopPropagation, Tab commits+closes) — D-12 regression floor."
    - "Dropdown adds the full combobox ARIA pattern (role=combobox, aria-expanded, aria-controls, aria-haspopup=listbox, aria-activedescendant) plus member-mode keyboard navigation, Home/End, two-stage Escape in member-mode, and scrollIntoView on keyboard nav — all net-new per D-12, since grep confirmed ActFormItemsTable.svelte never had role=combobox despite CONTEXT.md's initial assumption that it did."
  artifacts:
    - path: "ui/src/lib/components/Dropdown.svelte"
      provides: "Completed CMP-07 primitive — both field variants, full keyboard/ARIA contract"
      contains: "role=\"combobox\""
  key_links:
    - from: "ui/src/lib/components/Dropdown.svelte"
      to: "ui/src/lib/components/PersonAutocomplete.svelte"
      via: "aria-activedescendant id pattern precedent (id={uid}-option-{i}, the only existing codebase precedent for this attribute)"
      pattern: "aria-activedescendant"
---

<objective>
Complete the CMP-07 `Dropdown.svelte` primitive built by Plan 25-02: implement the `variant === 'select'`
field branch (value display + arrow trigger, in-panel search box — the one sub-variant with no direct
codebase precedent, built straight from `Dropdown.dc.html`'s pixel values per UI-SPEC), the flat-list
checkmark mode, and the full keyboard/ARIA layer (regression floor + net-new combobox pattern per D-12).

Purpose: This is the component-completeness gate before either downstream consumer plan (25-06 showcase,
25-07 `ActFormItemsTable` pilot) can proceed — both need the finished component, and both edit different
files than this plan, so this plan runs alone in Wave 2 ahead of them.
Output: `ui/src/lib/components/Dropdown.svelte` — feature-complete, both variants, full D-12 keyboard/ARIA
contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-dropdown/25-CONTEXT.md
@.planning/phases/25-dropdown/25-UI-SPEC.md
@.planning/phases/25-dropdown/25-PATTERNS.md
@ui/src/lib/components/Dropdown.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the select field variant and flat-list checkmark mode</name>
  <files>ui/src/lib/components/Dropdown.svelte</files>
  <read_first>
    - ui/src/lib/components/Dropdown.svelte (the file produced by Plan 25-02 — read in full to see the
      exact `// TODO Plan 25-03` placeholder left in the `variant === 'select'` branch, and the existing
      `flat` prop already declared but not yet branched on for the checkmark option-row shape).
    - .planning/phases/25-dropdown/25-UI-SPEC.md "Dropdown — две формы (D-03, SC #3/#4)" table (field/search/
      scroll values per form) and the "No Analog Found" entry in 25-PATTERNS.md ("Flat-select form of
      Dropdown... this sub-variant is genuinely new; UI-SPEC's own pixel values... are the only source of
      truth"). Field (select trigger): `<span>` showing selected value + `▼`. Search box (inside panel,
      select-variant only): h=30px, `--tr-surface-sunken`, radius 5px, icon `⌕`, wrapper padding `6px 12px` +
      `border-bottom`, placeholder `Поиск`.
    - .planning/phases/25-dropdown/25-UI-SPEC.md "Строка опции" section — plain-list checkmark row: `✓`
      `--tr-accent` 14px/700 replaces the `×N` count in flat mode; UI-SPEC's own "Checker Sign-Off" flags a
      recommendation to drop this to 600 to avoid a 4th font-weight — apply that recommendation (use
      `--tr-font-weight-semibold`, 600, not a new 700 weight token) since it does not conflict with any
      locked decision and keeps the typography scale closed.
  </read_first>
  <action>
    Implement `variant === 'select'`'s field: a `<button type="button" class="tr-dropdown-field-button"
    role="combobox">` (WAI-ARIA "select-only combobox" pattern — a button-based trigger is a valid ARIA 1.2
    combobox host when the field is not directly editable) showing `value || placeholder` plus a trailing
    `▼` glyph, `onclick` toggling `open` and calling `onSearch('')` when opening. When open, the FIRST child
    of the panel (before any option rows or the drill-in header) is an in-panel search `<input
    aria-label="Поиск" placeholder={searchPlaceholder}>` (h=30px, `--tr-surface-sunken` background, radius
    5px, `⌕` icon, wrapper padding `6px 12px` + `border-bottom: 1px solid var(--tr-border)`) whose `oninput`
    drives the same `onQueryInput`/debounced-`onSearch` path Task 1 of Plan 25-02 built for the combobox
    variant's field input (reuse that internal debounce function, don't duplicate it).

    Implement `flat`-mode option rows: replace the `×{count}` + chevron-slot markup with a trailing `✓`
    (`color: var(--tr-accent)`, `font-size: 14px`, `font-weight: var(--tr-font-weight-semibold)`) rendered
    only when `isGroupSelected?.(g)` is true; omit the chevron slot entirely in flat mode (no drill-in exists
    in this mode — `flat=true` never calls `onExpandGroup`).
  </action>
  <acceptance_criteria>
    - `grep -c "TODO Plan 25-03" ui/src/lib/components/Dropdown.svelte` returns 0 (placeholder resolved).
    - `grep -c "role=\"combobox\"" ui/src/lib/components/Dropdown.svelte` returns at least 2 (both the
      combobox-variant input and the select-variant button carry this role).
    - `grep -c "searchPlaceholder\|isGroupSelected" ui/src/lib/components/Dropdown.svelte | grep -v '^0$'`
      confirms both new branches reference their driving props.
    - `node ui/scripts/check-tokens.mjs` exits 0.
    - `pnpm --dir ui svelte-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>Both Dropdown variants render: combobox (type-in-field) and select (value-display + in-panel search).
  Flat mode shows a checkmark on the selected option instead of a group count/chevron.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full keyboard/ARIA layer — regression floor plus D-12 net-new additions</name>
  <files>ui/src/lib/components/Dropdown.svelte</files>
  <read_first>
    - ui/src/features/acts/ActFormItemsTable.svelte lines 366-422 (`handleRowKeydown` — the exact regression
      floor to reproduce without loss: `Escape` closes + `stopPropagation`; `ArrowDown` on a closed panel
      opens it; in member-mode, `Enter` is suppressed via `preventDefault`+`stopPropagation` (WR-02, protects
      the host form's native submit) and all other keys are no-ops (member-mode navigation is currently
      mouse-only — this task ADDS keyboard nav here, it is not already present, per D-12's correction of
      CONTEXT.md); in groups-mode, `ArrowDown`/`ArrowUp` cycle `activeIndexByRow` with wraparound, `Enter`
      picks the active item, `Tab` commits the active item then closes).
    - ui/src/lib/components/PersonAutocomplete.svelte lines 210-211, 232-244 — the ONLY existing
      `aria-activedescendant` precedent in the codebase: `aria-activedescendant={activeIndex >= 0 ?
      \`person-autocomplete-item-${activeIndex}\` : undefined}` on the input, paired with `id="person-
      autocomplete-item-{i}"` on each option. Use `$props.id()` (Svelte 5.20+, confirmed available at
      svelte@5.55.9 in this repo's `ui/node_modules`) to generate a stable per-instance id prefix instead of
      a hardcoded string, since Dropdown is a reusable primitive with potentially many simultaneous instances
      (unlike `PersonAutocomplete`, which is typically single-instance per screen).
    - .planning/phases/25-dropdown/25-UI-SPEC.md "Keyboard & ARIA Contract (D-12)" section in full — both the
      "Обязательный минимум (регресс запрещён)" table and the "Добавляется в этой фазе" table are the
      authoritative checklists for this task.
  </read_first>
  <action>
    Add to the combobox-variant `<input>` and the select-variant `<button>`: `role="combobox"`,
    `aria-expanded={open}`, `aria-controls={panelId}` (panel gets `id={panelId}`, generated via
    `$props.id()`), `aria-haspopup="listbox"`, `aria-activedescendant` referencing the currently active
    group/member's generated id (`` `${uid}-opt-${activeId}` ``, using each row's `getGroupId`/`getMemberId`
    result — NOT array index, since group/member lists can reorder). Each option row gets a matching `id`.
    Retain `aria-autocomplete="list"` (combobox variant only) and `aria-selected` on the active option — do
    not remove either, they are the pre-existing regression floor.

    Extend `handleKeydown` (the function driving both variants' keydown handlers) with: `ArrowUp`/`ArrowDown`
    cyclic navigation now ALSO active in member-view (currently mouse-only, per D-12), updating `activeIndex`
    and calling `scrollIntoView({ block: 'nearest' })` on the newly-active option element after each move;
    `Home`/`End` jump `activeIndex` to the first/last item of whichever list (`groups` or `members`) is
    currently visible; `Escape` in member-view is two-stage — first press calls `backToGroups()` (does NOT
    close the panel), second press (panel already back in groups-view) closes it — in groups-view, `Escape`
    closes immediately as before (regression floor, unchanged); `Enter` in member-view now picks the active
    member (calling `onPickMember`) instead of being unconditionally suppressed — but KEEP the
    `preventDefault`+`stopPropagation` call before doing so, since WR-02's form-submit-protection concern
    (Enter must never bubble to a host `<form>`) is orthogonal to whether Enter now has a selection action;
    `Tab` in member-view commits the active member then closes, mirroring the existing groups-mode `Tab`
    behavior exactly.
  </action>
  <acceptance_criteria>
    - `grep -c "aria-expanded\|aria-controls\|aria-haspopup\|aria-activedescendant" ui/src/lib/components/Dropdown.svelte | grep -v '^0$'` confirms all four new ARIA attributes are present.
    - `grep -c "aria-autocomplete=\"list\"\|aria-selected" ui/src/lib/components/Dropdown.svelte | grep -v '^0$'` confirms the regression-floor attributes were not removed.
    - `grep -c "scrollIntoView" ui/src/lib/components/Dropdown.svelte` returns at least 1.
    - `grep -c "backToGroups" ui/src/lib/components/Dropdown.svelte | grep -v '^0$'` confirms the two-stage Escape path calls the existing function from Plan 25-02, not a duplicate.
    - `pnpm --dir ui svelte-check` exits 0.
    - `node ui/scripts/check-tokens.mjs` exits 0.
    - `pnpm --dir ui build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs && pnpm --dir ui build</automated>
  </verify>
  <done>Dropdown.svelte carries the full combobox ARIA pattern (role/expanded/controls/haspopup/
  activedescendant) on both field variants without losing any of the 8 pre-existing keyboard/ARIA behaviors;
  member-mode keyboard navigation, Home/End, two-stage Escape, and scrollIntoView are all implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | Continues Plan 25-02's scope: a presentation-only component with zero data-fetching. Keyboard/ARIA additions are accessibility-layer only, no new data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-25-03-01 | Tampering (XSS) | Select-variant search input, option rows | accept | Continues Plan 25-02's default-escaped-text-only rendering discipline; no `{@html}` introduced (verified via `grep -c "@html" ui/src/lib/components/Dropdown.svelte` returning 0). |
| T-25-03-02 | Repudiation / Information Disclosure | `aria-activedescendant` id generation | accept | Generated ids (`${uid}-opt-${id}`) are DOM-scoped, non-sequential-guessable-secret material — they carry no security meaning, only accessibility wiring. |
| T-25-03-SC | Tampering | npm installs | accept | No new npm packages — `$props.id()` is a built-in Svelte 5.55 rune already present in the pinned `svelte` dependency, not a new package. |
</threat_model>

<verification>
`pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs && pnpm --dir ui build` all exit 0. Full
visual/interactive UAT happens in Plan 25-06 (showcase) and 25-07 (ActFormItemsTable pilot), which are the
first real consumers of this completed component.
</verification>

<success_criteria>
Dropdown.svelte is feature-complete: both field variants (combobox, select), both list modes (grouped
drill-in, flat checkmark), and the full D-12 keyboard/ARIA contract (regression floor preserved, net-new
combobox pattern + member-mode navigation + Home/End + two-stage Escape + scrollIntoView added).
</success_criteria>

<output>
Create `.planning/phases/25-dropdown/25-03-SUMMARY.md` when done
</output>
</content>
