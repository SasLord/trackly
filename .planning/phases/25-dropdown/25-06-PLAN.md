---
phase: 25-dropdown
plan: 06
type: execute
wave: 3
depends_on: ["25-03", "25-04"]
files_modified:
  - ui/src/features/showcase/sections/DropdownSection.svelte
  - ui/src/features/showcase/ShowcasePage.svelte
autonomous: true
requirements: [CMP-07]

must_haves:
  truths:
    - "The showcase's new dropdown section demonstrates the combobox-with-groups variant expanded, including a drill-in state with the '<- Назад' header, closing CMP-07's SC #4 visually."
    - "The showcase's new dropdown section demonstrates the plain select variant with its in-panel search box and a checkmark on the selected option, closing CMP-07's SC #3 visually."
    - "The showcase demonstrates both the empty ('Ничего не найдено') and loading ('Загрузка…' + Spinner) panel states as static, always-visible variant-blocks (D-13) — not just reachable via live interaction, since phases 26-28 need a visible reference for these states in six future consumers."
    - "DropdownSection is wired into ShowcasePage.svelte after TableSection (Plan 25-04), as the 7th section, without disturbing the 6 existing sections' order."
  artifacts:
    - path: "ui/src/features/showcase/sections/DropdownSection.svelte"
      provides: "Static-demo showcase gallery for Dropdown (CMP-07) — both variants, both list modes, empty/loading states"
      contains: "Dropdown"
    - path: "ui/src/features/showcase/ShowcasePage.svelte"
      provides: "Wires DropdownSection as the 7th showcase section"
      contains: "DropdownSection"
  key_links:
    - from: "ui/src/features/showcase/ShowcasePage.svelte"
      to: "ui/src/features/showcase/sections/DropdownSection.svelte"
      via: "static import + <section class=\"showcase-block\"><DropdownSection /></section>, appended after TableSection"
      pattern: "DropdownSection"
    - from: "ui/src/features/showcase/sections/DropdownSection.svelte"
      to: "ui/src/lib/components/Dropdown.svelte"
      via: "static demo groups/members arrays wired to local $state handlers, no live API calls"
      pattern: "import Dropdown from"
---

<objective>
Build the CMP-07 showcase gallery: a new `DropdownSection.svelte` demonstrating every required `Dropdown`
variant and state from Plans 25-02/25-03 using static demo data (fake groups/members, fake async delay for
loading demonstration), wired into `ShowcasePage.svelte` as the 7th section, appended after
`TableSection.svelte` (Plan 25-04) — this plan depends on both 25-03 (finished component) and 25-04 (which
already wrote to `ShowcasePage.svelte`, making this an in-place sequential edit of the same file).

Purpose: This is the visual-UAT surface for CMP-07's SC #3/#4, and — per D-13 — the reference implementation
phases 26-28 copy their empty/loading states from, since `Dropdown.dc.html` itself does not depict them.
Output: `ui/src/features/showcase/sections/DropdownSection.svelte`, updated `ShowcasePage.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-dropdown/25-CONTEXT.md
@.planning/phases/25-dropdown/25-UI-SPEC.md
@.planning/phases/25-dropdown/25-PATTERNS.md
@ui/src/lib/components/Dropdown.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DropdownSection.svelte with static demo data</name>
  <files>ui/src/features/showcase/sections/DropdownSection.svelte</files>
  <read_first>
    - ui/src/features/showcase/sections/FieldsSection.svelte lines 1-60 — the `.field-row` -> `.state-group`
      -> `.state-cell` with `.state-tag` labels ("Обычное"/"Ошибка"/"Отключено") side-by-side state-matrix
      layout pattern this task adapts for laying out 4+ Dropdown variant-blocks (expanded-grouped-with-
      drill-in / flat-select-with-search / empty / loading) legibly next to their labels.
    - ui/src/features/showcase/sections/TabsSection.svelte (full file) — the outer `<section>` -> `h2` ->
      `.variant-block` -> `.variant-label` nesting/spacing convention (`--tr-space-lg`/`--tr-space-sm`) that
      wraps whichever inner layout this task uses.
    - ui/src/lib/components/Dropdown.svelte (Plans 25-02/25-03's output) — the full generic prop contract
      (`variant`, `flat`, `groups`, `getGroupId`/`getGroupName`/`getGroupMeta`/`getGroupSub`/`getGroupCount`/
      `isGroupExpandable`/`isGroupSelected`, `onExpandGroup`, `getMemberId`/`getMemberName`/`getMemberMeta`/
      `getMemberSub`, `onSearch`, `onPickGroup`, `onPickMember`, `onQueryInput`, `loading`) this task
      instantiates with static demo data instead of a live `devices` API call.
    - .planning/phases/25-dropdown/25-UI-SPEC.md "Showcase Contract" section — the explicit dropdown
      checklist: combobox-with-groups (expanded, including drill-in state with '← Назад' header), plain
      select with search box + checkmark, empty state, loading state — "Обе секции — в обеих темах".
  </read_first>
  <action>
    Create `ui/src/features/showcase/sections/DropdownSection.svelte` with local static demo arrays: 2-3
    fake groups (e.g. a multi-instance "Мышь Logitech M185" group with a fake `getGroupCount` of 5 and
    `isGroupExpandable` true, a singleton "Монитор Samsung S24" with count 1 and not expandable) and 2-3 fake
    members for the expandable group's drill-in demo, all literal local objects (no `DeviceGroup`/`DeviceDto`
    import — this section proves the primitive works with ANY shape, using its own minimal demo type). Wire
    `onExpandGroup`/`onSearch`/`onPickGroup`/`onPickMember` to local no-op or `$state`-mutating handlers (no
    API calls). Structure as 4 `.variant-block`s: (1) "Комбобокс с группами (drill-in)" — a `Dropdown
    variant="combobox"` instance forced into a permanently-open, already-drilled-in visual state (so the
    reviewer sees the '← Назад' header without needing to interact — set the demo's initial `$state` so
    `viewMode`/`open` start in the drilled-in state, OR — if `Dropdown`'s `open`/`viewMode` are fully
    internal per Plan 25-02's design and not settable from outside — trigger the drill-in via a
    programmatic `onMount` that simulates a focus + click sequence on the rendered instance, whichever is
    simpler given `Dropdown.svelte`'s actual final prop surface; read the component's props/exports before
    choosing); (2) "Плоский селект" — a `Dropdown variant="select" flat={true}` instance with its in-panel
    search box visible and one option pre-marked `isGroupSelected` (checkmark visible); (3) "Пустое
    состояние" — a `Dropdown` instance whose `groups` array is empty and `loading={false}`, forced open, so
    "Ничего не найдено" is statically visible; (4) "Загрузка" — a `Dropdown` instance with `loading={true}`
    forced open, so "Загрузка…" + `Spinner` is statically visible. Zero hex/rgba literals in the `<style>`
    block; column/field labels in Russian per D-13's canonical copy.
  </action>
  <acceptance_criteria>
    - `grep -c "variant=\"combobox\"" ui/src/features/showcase/sections/DropdownSection.svelte` returns at least 1.
    - `grep -c "variant=\"select\"" ui/src/features/showcase/sections/DropdownSection.svelte` returns at least 1.
    - `grep -c "flat={true}\|flat\b" ui/src/features/showcase/sections/DropdownSection.svelte | grep -v '^0$'` confirms the flat-mode demo exists.
    - `grep -c "loading={true}\|loading\b" ui/src/features/showcase/sections/DropdownSection.svelte | grep -v '^0$'` confirms the loading-state demo exists.
    - `grep -c "← Назад\|Назад" ui/src/features/showcase/sections/DropdownSection.svelte` returns at least 1 OR the drill-in demo is visibly reachable per the task's chosen approach (verified during human/visual pass, not grep-gated if programmatic simulation is used — see task action).
    - `node ui/scripts/check-tokens.mjs` exits 0.
    - `pnpm --dir ui svelte-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>DropdownSection.svelte demonstrates the grouped/drill-in combobox, the flat select with search +
  checkmark, the empty state, and the loading state — all with static demo data, no live API calls.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DropdownSection into ShowcasePage.svelte</name>
  <files>ui/src/features/showcase/ShowcasePage.svelte</files>
  <read_first>
    - ui/src/features/showcase/ShowcasePage.svelte (as left by Plan 25-04 — 6 sections, `TableSection` last)
      — confirm the exact current state of this file before editing, since Plan 25-04 already modified it in
      an earlier wave.
  </read_first>
  <action>
    In `ui/src/features/showcase/ShowcasePage.svelte`: add `import DropdownSection from
    './sections/DropdownSection.svelte';` after the `TableSection` import (added by Plan 25-04), and add
    `<section class="showcase-block"><DropdownSection /></section>` after the `TableSection` block — 7th
    section, appended, no reordering of the existing 6.
  </action>
  <acceptance_criteria>
    - `grep -c "DropdownSection" ui/src/features/showcase/ShowcasePage.svelte` returns 2 (import + usage).
    - `grep -n "TableSection\|DropdownSection" ui/src/features/showcase/ShowcasePage.svelte` shows
      `TableSection` occurrences before `DropdownSection` occurrences (order preserved).
    - `pnpm --dir ui lint` exits 0.
    - `pnpm --dir ui svelte-check` exits 0.
    - `pnpm --dir ui build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check && pnpm --dir ui build</automated>
  </verify>
  <done>ShowcasePage.svelte renders DropdownSection as its 7th section, after TableSection, with all 5
  original Phase 24 sections and TableSection unchanged in order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Static demo data only, zero API calls, admin-gated route unchanged from Phase 24. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-25-06-01 | Tampering (XSS) | Static demo group/member names | accept | Hardcoded literal Russian demo strings, default-escaped Svelte interpolation, no `{@html}`, no live data source. |
| T-25-06-02 | Elevation of Privilege | `/showcase` route gate | accept | Unchanged from Phase 24's already-accepted `roles: ['admin']` sidebar-filter gate — this plan adds a section, does not touch the gate. |
| T-25-06-SC | Tampering | npm installs | accept | No new npm packages. |
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs && pnpm --dir ui build`
all exit 0. Visual/interactive confirmation happens live via the `/showcase` route (admin role, `pnpm --dir
ui build` required before LAN-browser UAT).
</verification>

<success_criteria>
`/showcase` renders a 7th "Dropdown" section demonstrating both variants (combobox-grouped-with-drill-in,
select-flat-with-search), plus static empty and loading states, matching Dropdown.dc pixel values via the
Plan 25-02/25-03 primitive.
</success_criteria>

<output>
Create `.planning/phases/25-dropdown/25-06-SUMMARY.md` when done
</output>
</content>
