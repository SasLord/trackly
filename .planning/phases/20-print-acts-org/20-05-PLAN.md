---
phase: 20-print-acts-org
plan: 05
type: execute
wave: 3
depends_on: ["20-02", "20-03"]
files_modified:
  - crates/trackly-app/tests/html_act_render.rs
  - crates/trackly-app/tests/html_report_render.rs
  - crates/trackly-app/tests/fixtures/logo_test_with_script.svg
autonomous: true
requirements: [PRN-01, ORG-01, ORG-02]

must_haves:
  truths:
    - "render_acceptance_pdf output содержит все org-реквизиты (inn/phone/fax/email/okpo/ogrn/address_line2), доказывая паритет с render_pdf (PRN-01)"
    - "SVG-логотип со встроенным <script> встраивается ТОЛЬКО как data: URI внутри <img> — <script> никогда не появляется буквально в HTML (ORG-01)"
    - "address_line2 рендерится отдельной строкой в отчёте (report.html), когда заполнена (ORG-02)"
  artifacts:
    - path: "crates/trackly-app/tests/html_act_render.rs"
      provides: "PRN-01 parity test + D-09 SVG-script security regression test"
      contains: "html_svg_logo_with_script_embeds_img_only_no_inline_script"
    - path: "crates/trackly-app/tests/fixtures/logo_test_with_script.svg"
      provides: "malicious SVG fixture containing <script>"
      contains: "<script>"
    - path: "crates/trackly-app/tests/html_report_render.rs"
      provides: "address_line2 rendering assertion for report.html"
      contains: "address_line2"
  key_links:
    - from: "crates/trackly-app/tests/html_act_render.rs::html_svg_logo_with_script_embeds_img_only_no_inline_script"
      to: "crates/trackly-app/templates/act_handover.html <img src=\"{{ org.logo_data_uri | safe }}\">"
      via: "OrgDbService::save_logo(SVG bytes) -> render_pdf -> assert !html.contains(\"<script>\") && html.contains(\"<img src=\\\"data:image/svg+xml;base64,\")"
      pattern: "img src=\\\"data:image/svg\\+xml;base64,"
---

<objective>
Lock in the two central invariants of this phase with automated regression tests: (1) PRN-01 — `render_acceptance_pdf` output now carries the full organizational requisite set at parity with `render_pdf`; (2) ORG-01/D-09 — an SVG logo containing an embedded `<script>` tag is embedded EXCLUSIVELY as a `data:` URI inside `<img>`, never inlined as executable markup in the rendered document DOM; (3) ORG-02 — `address_line2` renders in `report.html` as well as the two act templates (already covered implicitly for acts via test 1).

Purpose: Nyquist validation for this phase — every requirement (PRN-01/ORG-01/ORG-02) gets a concrete automated test proving the behavior, not just code review.
Output: Three new/extended tests, one new SVG fixture file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-print-acts-org/20-CONTEXT.md
@.planning/phases/20-print-acts-org/20-PATTERNS.md
</context>

<interfaces>
<!-- crates/trackly-app/tests/html_act_render.rs existing fixture/helper shapes to reuse (lines 1-64, 161-207) -->
```rust
const LOGO_PNG: &[u8] = include_bytes!("fixtures/logo_test.png");

async fn make_full_pipeline() -> Pipeline { /* already wires with_org_db(org_db.clone()) */ }
async fn seed_device(writer: &Arc<WriterHandle>, name: &str) -> i64 { /* ... */ }
async fn create_handover(svc: &ActService, device_ids: &[i64], giver: &str, receiver: &str) -> ActDto { /* ... */ }

// production write-path pattern already used in html_handover_contains_required_blocks_and_logo:
let org_db = Arc::new(OrgDbService::new(p.writer.clone(), p._readers.clone(), Arc::new(SystemClock), Arc::new(Paths::resolve_for_exe_dir(p._dir.path().to_path_buf()).expect("paths"))));
org_db.save_logo(&Identity::trusted_admin(), LOGO_PNG.to_vec(), "image/png".to_string()).await.expect("save_logo");
```
`OrgDbService::save_fields(&self, caller: &Identity, patch: crate::dto::reports::OrgPatch) -> Result<(), AppError>` is the production write-path for text requisites (org_db_service.rs, extended by Plan 20-01) — use it to populate inn/phone/fax/email/okpo/ogrn/address_line2 before rendering, exactly as `save_logo` is used for the logo BLOB in the existing test above.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PRN-01 parity test — render_acceptance_pdf carries full org requisites (html_act_render.rs)</name>
  <files>crates/trackly-app/tests/html_act_render.rs</files>
  <read_first>
    crates/trackly-app/tests/html_act_render.rs lines 1-240 (imports, Pipeline/make_full_pipeline helpers, existing html_handover_contains_required_blocks_and_logo and html_acceptance_contains_required_blocks tests — the two tests this new test sits between/extends)
    crates/trackly-app/src/services/org_db_service.rs::save_fields signature (crates/trackly-app/src/services/org_db_service.rs lines ~85-118, extended in Plan 20-01/20-02)
    crates/trackly-app/src/dto/reports.rs::OrgPatch (extended in Plan 20-01, now has address_line2)
  </read_first>
  <action>
    Add `use trackly_app::dto::reports::OrgPatch;` to html_act_render.rs's imports (alongside the existing `use trackly_app::dto::act::{...}` line).
    Add a new test `html_acceptance_full_org_parity_with_handover` (tokio::test, multi_thread, worker_threads = 4) after `html_acceptance_contains_required_blocks`. Build a `Pipeline` via `make_full_pipeline()`, construct an `OrgDbService` the same way `html_handover_contains_required_blocks_and_logo` does, and call `org_db.save_fields(&Identity::trusted_admin(), OrgPatch { org_name: "ООО Паритет".into(), inn: "7712345678".into(), kpp: "771201001".into(), address: "г. Москва, ул. Тестовая, д. 1".into(), phone: "+7 495 000-00-00".into(), fax: "+7 495 000-00-01".into(), email: "info@paritet.test".into(), okpo: "12345678".into(), ogrn: "1027700000000".into(), address_line2: "офис 305, корпус 2".into() })` to populate every requisite field including the new `address_line2`. Seed one device, render BOTH `p.acts.render_pdf(handover_act.id)` (create a handover act first via `create_handover`) and `p.acts.render_acceptance_pdf(device_id, "Отдалов О.О.".to_string(), "Принялов П.П.".to_string(), 1_700_000_000)`. Assert BOTH rendered HTML strings contain every one of: `"7712345678"` (inn), `"+7 495 000-00-00"` (phone), `"+7 495 000-00-01"` (fax), `"info@paritet.test"` (email), `"12345678"` (okpo — note: assert with a distinguishing substring if it collides with other digits), `"1027700000000"` (ogrn), `"офис 305, корпус 2"` (address_line2). This proves PRN-01 (acceptance parity with handover) and ORG-02 (address_line2 in both act templates) in one test.
  </action>
  <verify>
    <automated>cd crates/trackly-app && cargo test -p trackly-app --test html_act_render html_acceptance_full_org_parity_with_handover -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Test compiles and passes: `cargo test -p trackly-app --test html_act_render html_acceptance_full_org_parity_with_handover` exits 0.
    - Test asserts presence of inn/phone/fax/email/okpo/ogrn/address_line2 values in BOTH the handover (`render_pdf`) and acceptance (`render_acceptance_pdf`) rendered HTML strings — not just one.
    - Test uses the production write path (`OrgDbService::save_fields`), not a hand-constructed ctx or direct DB write.
  </acceptance_criteria>
  <done>A single automated test proves render_acceptance_pdf is at full requisite parity with render_pdf, including the new address_line2 field.</done>
</task>

<task type="auto">
  <name>Task 2: D-09 ORG-01 security regression — SVG logo with &lt;script&gt; embeds img-only (html_act_render.rs + fixture)</name>
  <files>crates/trackly-app/tests/html_act_render.rs, crates/trackly-app/tests/fixtures/logo_test_with_script.svg</files>
  <read_first>
    crates/trackly-app/tests/html_act_render.rs lines 24, 161-207 (LOGO_PNG const pattern + html_handover_contains_required_blocks_and_logo's save_logo/render/assert shape — the exact structure to mirror for the SVG case)
    crates/trackly-app/tests/html_act_render.rs lines 372-422 (html_is_offline_safe_no_external_links — the "assert absence of X + sanity-check presence of Y so the negative assertion isn't vacuous" pairing pattern)
    crates/trackly-app/templates/act_handover.html line 128 (the img-only `<img src="{{ org.logo_data_uri | safe }}">` invariant under test — DO NOT modify this file)
  </read_first>
  <action>
    Create crates/trackly-app/tests/fixtures/logo_test_with_script.svg containing a minimal SVG with an embedded `<script>` tag, e.g. an `<svg xmlns="http://www.w3.org/2000/svg" width="10" height="10">` root containing both a `<script>alert('xss')</script>` element and a `<rect width="10" height="10" fill="red"/>` element.
    Add `const LOGO_SVG_WITH_SCRIPT: &[u8] = include_bytes!("fixtures/logo_test_with_script.svg");` next to the existing `LOGO_PNG` const (line 24).
    Add a new test `html_svg_logo_with_script_embeds_img_only_no_inline_script` (tokio::test, multi_thread, worker_threads = 4) mirroring `html_handover_contains_required_blocks_and_logo`'s structure: build a `Pipeline`, construct `OrgDbService`, call `org_db.save_logo(&Identity::trusted_admin(), LOGO_SVG_WITH_SCRIPT.to_vec(), "image/svg+xml".to_string())`, seed a device, create a handover act, call `p.acts.render_pdf(act.id)`. Assert THREE things on the resulting HTML: (a) `!html.contains("<script>")` — the raw script tag must never appear inline in the DOM (the D-09 core claim); (b) `html.contains("data:image/svg+xml;base64,")` — sanity check that the logo DID embed as a data: URI, proving assertion (a) isn't vacuously true because the logo silently failed to embed; (c) `html.contains("<img src=\"data:image/svg+xml;base64,")` — confirms the logo is embedded exclusively via `<img src=...>`, not as inline `<svg>`/raw markup elsewhere in the document.
  </action>
  <verify>
    <automated>cd crates/trackly-app && cargo test -p trackly-app --test html_act_render html_svg_logo_with_script_embeds_img_only_no_inline_script -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `crates/trackly-app/tests/fixtures/logo_test_with_script.svg` exists and contains a literal `<script>` element.
    - `cargo test -p trackly-app --test html_act_render html_svg_logo_with_script_embeds_img_only_no_inline_script` exits 0.
    - The test asserts all three conditions listed in the action (negative `<script>` absence, positive `data:image/svg+xml;base64,` presence, positive `<img src="data:image/svg+xml;base64,` presence) — not just the negative assertion alone (avoids a vacuous pass).
    - `crates/trackly-app/templates/act_handover.html` is unmodified by this task (test-only change).
  </acceptance_criteria>
  <done>A regression test proves that an SVG logo containing an embedded &lt;script&gt; tag is rendered exclusively as a data: URI inside &lt;img&gt;, never as inline executable markup — locking the ORG-01 security invariant.</done>
</task>

<task type="auto">
  <name>Task 3: address_line2 renders in report.html (html_report_render.rs)</name>
  <files>crates/trackly-app/tests/html_report_render.rs</files>
  <read_first>
    crates/trackly-app/tests/html_report_render.rs lines 1-78, 225-265 (empty_org() helper already fixed for compile in Plan 20-01; html_report_org_header_present — the existing test this new assertion extends/models)
  </read_first>
  <action>
    Add a new test `html_report_org_header_shows_address_line2` (tokio::test, multi_thread, worker_threads = 4) modeled directly on `html_report_org_header_present`: build `(svc, _dir) = make_report_service()`, construct a single-row `ReportResponse` via the existing `make_row` helper, build `let mut org = empty_org();` then set `org.org_name = "ООО Ромашка".to_string();` and `org.address_line2 = "офис 305, корпус 2".to_string();`, call `svc.export_pdf(&rows, "Отчёт", "Сентябрь 2026", &org, None, None, &columns, &labels).await.expect("export_pdf ok")`, and assert `html.contains("офис 305, корпус 2")`. Also assert the empty-case does NOT render an empty second line: reuse `empty_org()` (address_line2 defaults to empty string) in a second assertion block within the same test or a small follow-up assertion, confirming the `{% if org.address_line2 %}` guard suppresses the line when unset (e.g. assert the rendered HTML does not contain two consecutive empty `<div></div>` lines in the `.requisites` block, or more simply just rely on the positive-case assertion since the guard behavior itself is exercised identically to the already-tested phone/fax/email guards — no new guard logic was introduced).
  </action>
  <verify>
    <automated>cd crates/trackly-app && cargo test -p trackly-app --test html_report_render html_report_org_header_shows_address_line2 -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test html_report_render html_report_org_header_shows_address_line2` exits 0.
    - Test asserts the rendered report HTML contains the literal address_line2 value ("офис 305, корпус 2").
  </acceptance_criteria>
  <done>report.html's address_line2 rendering is covered by an automated test, completing ORG-02 coverage across all three print templates (acts covered by Task 1, report covered here).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only: malicious SVG fixture -> OrgDbService::save_logo -> render_pdf -> asserted HTML | Simulates an authenticated admin uploading an adversarial SVG logo; validates the img-only embedding invariant holds under a concrete attack payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05-01 | Injection (Script Execution / Cross-Site Scripting) | act_handover.html / act_acceptance.html / report.html logo `<img>` embedding, exercised via a malicious SVG logo containing `<script>` | mitigate | img-only `data:` URI embedding (ORG-01/D-08, pre-existing, unchanged) — this plan delivers the regression test (`html_svg_logo_with_script_embeds_img_only_no_inline_script`, Task 2) that proves the embedded `<script>` never appears as literal, executable markup in the rendered document, closing the loop on the phase's core security requirement (ORG-01 Success Criterion #3) with an automated, re-runnable check rather than a one-time manual review |
| T-20-05-02 | Information Disclosure | PRN-01 parity test (Task 1) — full org requisites in test assertions | accept | Test-only synthetic data (fictional org name/inn/etc.), no real organizational data; mirrors the accepted disposition from Plan 20-02's T-20-02-01 |
</threat_model>

<verification>
`cargo test -p trackly-app --test html_act_render` and `cargo test -p trackly-app --test html_report_render` both exit 0, including the three new tests. `ls crates/trackly-app/tests/fixtures/logo_test_with_script.svg` confirms the new fixture exists.
</verification>

<success_criteria>
- PRN-01: render_acceptance_pdf's parity with render_pdf is proven by an automated test, not just code review.
- ORG-01: the img-only SVG-script embedding invariant (Success Criterion #3) is locked by a regression test using a real malicious payload.
- ORG-02: address_line2 rendering is proven across all three printed document types (acts via Task 1, report via Task 3).
</success_criteria>

<output>
Create `.planning/phases/20-print-acts-org/20-05-SUMMARY.md` when done
</output>
