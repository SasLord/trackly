---
phase: 20-print-acts-org
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - ui/src/features/settings/OrgSettings.svelte
  - ui/src/bindings.ts
autonomous: true
requirements: [ORG-02]

must_haves:
  truths:
    - "Настройки организации содержат поле «Адрес (2-я строка)», сразу под полем «Адрес» (D-05)"
    - "Значение поля загружается при открытии Настроек и сохраняется по нажатию «Сохранить настройки организации» (D-07)"
    - "bindings.ts содержит address_line2 на OrgSettingsDto/OrgPatch (TypeScript видит актуальный Rust-контракт)"
  artifacts:
    - path: "ui/src/features/settings/OrgSettings.svelte"
      provides: "address_line2 field: interface, state, load, save, markup"
      contains: "addressLine2"
    - path: "ui/src/bindings.ts"
      provides: "regenerated TS bindings with address_line2"
      contains: "address_line2"
  key_links:
    - from: "ui/src/features/settings/OrgSettings.svelte::saveOrg"
      to: "settings_save_org_fields Tauri/HTTP command -> OrgPatch.address_line2"
      via: "apiCall('settings_save_org_fields', { patch: { ..., address_line2: addressLine2 } })"
      pattern: "address_line2: addressLine2"
---

<objective>
Добавить поле «Адрес (2-я строка)» в форму настроек организации (`OrgSettings.svelte`) — D-05: полноширинное поле сразу под «Адрес», тот же паттерн загрузки/сохранения, что и остальные реквизиты. Регенерировать `ui/src/bindings.ts`, чтобы TypeScript видел `address_line2` на `OrgSettingsDto`/`OrgPatch` (Plan 20-01's DTO change).

Purpose: ORG-02 — пользователь должен иметь возможность ввести и сохранить вторую строку адреса через UI.
Output: Рабочее поле в Настройках; свежий `bindings.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-print-acts-org/20-CONTEXT.md
@.planning/phases/20-print-acts-org/20-PATTERNS.md
</context>

<interfaces>
<!-- crates/trackly-app/src/dto/reports.rs (already updated by Plan 20-01) -->
```rust
pub struct OrgPatch {
    pub org_name: String, pub inn: String, pub kpp: String, pub address: String,
    pub phone: String, pub fax: String, pub email: String, pub okpo: String, pub ogrn: String,
    pub address_line2: String,
}
pub struct OrgSettingsDto {
    pub org_name: String, pub inn: String, pub kpp: String, pub address: String,
    pub has_logo: bool,
    pub phone: String, pub fax: String, pub email: String, pub okpo: String, pub ogrn: String,
    pub address_line2: String,
}
```
The Tauri/HTTP adapters (`tauri_cmds/settings_org.rs`, `http/settings_org.rs`) are pure pass-through (`ctx.org_db.get().await`, `ctx.org_db.save_fields(caller, patch).await`) — no code change needed there; they forward whatever fields exist on the DTO/patch types.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OrgSettings.svelte — add address_line2 field (D-05)</name>
  <files>ui/src/features/settings/OrgSettings.svelte</files>
  <read_first>
    ui/src/features/settings/OrgSettings.svelte (full file, 483 lines) — specifically: TS `interface OrgSettingsDto` (lines 8-19), `$state` declarations (lines 21-30), `loadOrg()` assignment block (lines 42-51), `saveOrg()` patch object (lines 91-101), and the existing `address` field markup (lines 247-256, `.form-field.form-field--full`)
  </read_first>
  <action>
    Add `address_line2: string;` to the `interface OrgSettingsDto` (after `address: string;`, line 12).
    Add `let addressLine2 = $state('');` (after `let address = $state('');`, line 24).
    In `loadOrg()`, add `addressLine2 = dto.address_line2;` (after `address = dto.address;`, line 45).
    In `saveOrg()`'s patch object passed to `apiCall('settings_save_org_fields', { patch: {...} })`, add `address_line2: addressLine2,` (after `address,`, line 95).
    In the markup, insert a new `<div class="form-field form-field--full">` block immediately after the existing address field (after line 256's closing `</div>`), copying its exact shape: `<label class="form-label" for="org-address-line2">Адрес (2-я строка)</label>` and `<input id="org-address-line2" class="form-input" type="text" bind:value={addressLine2} placeholder="офис 305, корпус 2" />`. Label text must be EXACTLY «Адрес (2-я строка)» per 20-CONTEXT Specific Ideas. No new SCSS needed (`.form-field--full` already exists and is reused).
  </action>
  <verify>
    <automated>grep -c "addressLine2" ui/src/features/settings/OrgSettings.svelte && grep -c "Адрес (2-я строка)" ui/src/features/settings/OrgSettings.svelte</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addressLine2" ui/src/features/settings/OrgSettings.svelte` returns >= 5 (interface not counted since it uses snake_case `address_line2`; state decl, loadOrg assignment, saveOrg payload key value, bind:value all use camelCase `addressLine2`).
    - `grep -c "address_line2: string;" ui/src/features/settings/OrgSettings.svelte` returns 1 (TS interface field).
    - `grep -c "address_line2: addressLine2" ui/src/features/settings/OrgSettings.svelte` returns 1 (saveOrg payload).
    - Label text is exactly `Адрес (2-я строка)` (verified via grep above), input id is `org-address-line2`, wrapped in `form-field form-field--full`.
  </acceptance_criteria>
  <done>Settings form has a working address_line2 field: loads on mount, binds to input, saves on button click, positioned directly under «Адрес».</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate ui/src/bindings.ts (address_line2 on OrgSettingsDto/OrgPatch)</name>
  <files>ui/src/bindings.ts</files>
  <read_first>
    crates/trackly-app/tests/export_bindings.rs (the regeneration mechanism — `cargo test -p trackly-app --test export_bindings`; note Windows-skip guard `#![cfg(not(target_os = "windows"))]`, irrelevant on this macOS dev machine)
  </read_first>
  <action>
    Run the existing binding-export test to regenerate ui/src/bindings.ts from the current Rust DTOs (which already carry address_line2 after Plan 20-01). This is not new code — it is running the established mechanism (canonical_refs Integration Points in 20-CONTEXT.md), not inventing a new one. No manual edits to bindings.ts.
  </action>
  <verify>
    <automated>cd crates/trackly-app && cargo test -p trackly-app --test export_bindings 2>&1 | tail -20 && grep -c "address_line2" ../../ui/src/bindings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test export_bindings` exits 0.
    - `grep -c "address_line2" ui/src/bindings.ts` returns >= 2 (one occurrence in the `OrgPatch` type alias, one in `OrgSettingsDto`).
  </acceptance_criteria>
  <done>ui/src/bindings.ts reflects the current Rust OrgSettingsDto/OrgPatch shape, including address_line2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| OrgSettings.svelte form input -> settings_save_org_fields (Tauri/HTTP) | Free-text address_line2 value crosses from browser/webview input into the authenticated write path (unchanged authorization: Action::ManageSettings, enforced server-side in org_db_service.rs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Tampering | OrgSettings.svelte address_line2 input | accept | Client-side value is just UI state; the actual trust boundary (authorization + validation) is enforced server-side in org_db_service.rs::save_fields (Plan 20-01, unchanged `authorize(caller, &Action::ManageSettings)` gate) — no new client-side trust is granted |
| T-20-04-02 | Information Disclosure | ui/src/bindings.ts regeneration | accept | Regenerating exposes only type shapes (field names/types), not data — bindings.ts is a build artifact already regenerated routinely; no new sensitive surface |
</threat_model>

<verification>
`grep` confirms addressLine2/address_line2 present in OrgSettings.svelte at all 4 wiring points (interface, state, load, save) plus markup; `cargo test -p trackly-app --test export_bindings` exits 0 and bindings.ts contains address_line2.
</verification>

<success_criteria>
- Settings form exposes and persists address_line2 (ORG-02 Success Criterion #4, UI half).
- bindings.ts is in sync with the Rust DTO contract — no TypeScript/Rust drift.
</success_criteria>

<output>
Create `.planning/phases/20-print-acts-org/20-04-SUMMARY.md` when done
</output>
