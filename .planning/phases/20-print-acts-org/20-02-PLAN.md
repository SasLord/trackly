---
phase: 20-print-acts-org
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/services/report_service.rs
autonomous: true
requirements: [PRN-01, ORG-02]

must_haves:
  truths:
    - "render_acceptance_pdf выводит полный org-контекст (логотип из БД, название, ИНН/КПП, адрес, вторая строка адреса, телефон, факс, email, ОКПО, ОГРН) — как render_pdf"
    - "render_acceptance_pdf больше не читает pipeline.organization/org.json/read_logo_bytes — источник истины только org_db.get_for_pdf()"
    - "render_pdf и report_service::export_pdf прокидывают address_line2 в ctx"
  artifacts:
    - path: "crates/trackly-app/src/services/act_service.rs"
      provides: "render_acceptance_pdf rewritten to org_db.get_for_pdf() parity with render_pdf"
      contains: "org_db.get_for_pdf().await?"
    - path: "crates/trackly-app/src/services/report_service.rs"
      provides: "export_pdf ctx includes address_line2"
      contains: "\"address_line2\": org.address_line2"
  key_links:
    - from: "crates/trackly-app/src/services/act_service.rs::render_acceptance_pdf"
      to: "crates/trackly-app/src/services/org_db_service.rs::get_for_pdf"
      via: "match pipeline.org_db { Some(org_db) => org_db.get_for_pdf().await? ... }"
      pattern: "org_db\\.get_for_pdf\\(\\)"
    - from: "crates/trackly-app/src/services/act_service.rs::render_acceptance_pdf ctx"
      to: "crates/trackly-app/templates/act_acceptance.html"
      via: "serde_json::json! org object with 10 fields (name/inn/kpp/address/address_line2/phone/fax/email/okpo/ogrn/logo_data_uri)"
      pattern: "\"address_line2\": org_dto\\.address_line2"
---

<objective>
Центральный фикс PRN-01: переписать `render_acceptance_pdf` в `act_service.rs` так, чтобы он использовал тот же путь данных, что и `render_pdf` (D-02) — `org_db.get_for_pdf()` вместо legacy `pipeline.organization.read()`/`read_logo_bytes` (org.json) — и строил полный `org`-контекст (D-01/D-03: name/inn/kpp/address/address_line2/phone/fax/email/okpo/ogrn/logo_data_uri). Параллельно добавить `address_line2` в ctx обоих остальных рендер-путей (`render_pdf`, `report_service::export_pdf`) — D-07.

Purpose: Это корень бага PRN-01 — BLOB-логотип и полные реквизиты из БД (загружаемые через Настройки) начинают появляться в печати «Документ приёма» так же, как уже появляются в акте приёма-передачи и отчётах.
Output: `render_acceptance_pdf` при том же входе (device_id/giver/receiver/date) выдаёт HTML с полной org-шапкой из БД; `render_pdf`/`export_pdf` дополнительно несут `address_line2` в контексте (значение шаблоны используют начиная с плана 20-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-print-acts-org/20-CONTEXT.md
@.planning/phases/20-print-acts-org/20-PATTERNS.md
</context>

<interfaces>
<!-- Gold-standard org-context construction from render_pdf (act_service.rs lines 2545-2578) — the exact shape render_acceptance_pdf must mirror. -->
```rust
let org_legacy = pipeline.organization.read().await?;
let (org_dto, logo_bytes, logo_mime) = match pipeline.org_db {
    Some(org_db) => {
        let (dto, logo_bytes, logo_mime) = org_db.get_for_pdf().await?;
        (dto, logo_bytes, logo_mime)
    }
    None => (
        crate::dto::reports::OrgSettingsDto {
            org_name: org_legacy.name.clone(),
            inn: org_legacy.inn.clone(),
            kpp: org_legacy.kpp.clone(),
            address: org_legacy.address.clone(),
            has_logo: false,
            phone: String::new(),
            fax: String::new(),
            email: String::new(),
            okpo: String::new(),
            ogrn: String::new(),
            address_line2: String::new(),
        },
        None,
        None,
    ),
};
let logo_data_uri: Option<String> = logo_bytes.map(|bytes| {
    use base64::Engine;
    let mime = logo_mime.as_deref().unwrap_or("image/png");
    format!("data:{mime};base64,{}", base64::engine::general_purpose::STANDARD.encode(bytes))
});
```
<!-- Gold-standard ctx["org"] object from render_pdf (lines 2629-2641) — the full field set render_acceptance_pdf's ctx must be expanded to (plus address_line2, per this plan). -->
```rust
"org": {
    "name": org_dto.org_name,
    "inn": org_dto.inn,
    "kpp": org_dto.kpp,
    "address": org_dto.address,
    "address_line2": org_dto.address_line2,
    "phone": org_dto.phone,
    "fax": org_dto.fax,
    "email": org_dto.email,
    "okpo": org_dto.okpo,
    "ogrn": org_dto.ogrn,
    "logo_data_uri": logo_data_uri,
},
```
Note: after Task 3 of Plan 20-01, `org_dto.address_line2` already exists on `OrgSettingsDto` and `org_db.get_for_pdf()` already returns it populated — this plan only needs to READ it into the ctx json, no further data-layer work.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite render_acceptance_pdf to org_db.get_for_pdf() parity with render_pdf (PRN-01/D-02/D-03/D-11)</name>
  <files>crates/trackly-app/src/services/act_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs lines 2536-2593 (render_pdf — full gold-standard org_db match + logo_data_uri construction + template-load boilerplate, to copy)
    crates/trackly-app/src/services/act_service.rs lines 2678-2784 (render_acceptance_pdf — current deficient implementation to replace)
    crates/trackly-app/src/services/act_service.rs lines 2629-2641 (render_pdf's full ctx["org"] object — target shape for render_acceptance_pdf's ctx, plus address_line2 per this plan)
  </read_first>
  <action>
    In render_acceptance_pdf (currently lines ~2678-2784): remove the two lines `let org = pipeline.organization.read().await?;` and the subsequent `let logo_data_uri = pipeline.organization.read_logo_bytes(&org).await?...` block entirely (D-11: no org.json fallback for acceptance's logo/requisites data path). Replace with the exact org_db match pattern shown in this plan's `<interfaces>` block (copied from render_pdf lines 2545-2578, using `org_legacy` only as the structural None-branch degrade default — `pipeline.organization.read()` is still called ONCE for this fallback purpose only, mirroring render_pdf's own defensive shape, not for logo bytes). Keep the template-loading boilerplate (lines 2702-2716, file-first `act_acceptance.html` load with embedded fallback) unchanged. Replace the `ctx`'s `"org"` object (currently `{name, inn, kpp, address, logo_data_uri}`) with the full 11-field set: `name, inn, kpp, address, address_line2, phone, fax, email, okpo, ogrn, logo_data_uri` — sourced from `org_dto.*` and the newly-constructed `logo_data_uri`, exactly matching render_pdf's ctx shape (D-01/D-03) plus `address_line2` (D-07). The `device` and `document` parts of the ctx are unchanged.
  </action>
  <verify>
    <automated>cd crates/trackly-app && cargo check -p trackly-app 2>&1 | tail -30 && grep -A2 "pub async fn render_acceptance_pdf" src/services/act_service.rs | head -5 && grep -c "org_db.get_for_pdf" src/services/act_service.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p trackly-app` exits 0.
    - `grep -c "org_db.get_for_pdf" crates/trackly-app/src/services/act_service.rs` returns 2 (one in render_pdf, one in the rewritten render_acceptance_pdf).
    - `grep -c "pipeline.organization.read_logo_bytes" crates/trackly-app/src/services/act_service.rs` returns 0 (legacy logo-bytes read path fully removed from render_acceptance_pdf, and render_pdf never used it either).
    - render_acceptance_pdf's ctx `"org"` object contains all of: `name`, `inn`, `kpp`, `address`, `address_line2`, `phone`, `fax`, `email`, `okpo`, `ogrn`, `logo_data_uri` (11 keys, matching render_pdf's set plus address_line2).
  </acceptance_criteria>
  <done>render_acceptance_pdf uses org_db.get_for_pdf() as its sole org-data source (with the same None-branch degrade shape as render_pdf), never reads logo bytes from org.json, and its ctx carries the full requisites set.</done>
</task>

<task type="auto">
  <name>Task 2: Add address_line2 to render_pdf's ctx and report_service.rs's export_pdf ctx (D-07)</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs lines 2629-2641 (render_pdf's current ctx["org"] object — 9 fields, no address_line2 yet)
    crates/trackly-app/src/services/report_service.rs lines 637-649 (export_pdf's current ctx["org"] object — same 9-field shape, no address_line2 yet)
  </read_first>
  <action>
    In act_service.rs's render_pdf ctx (line ~2634, immediately after `"address": org_dto.address,`), add `"address_line2": org_dto.address_line2,`.
    In report_service.rs's export_pdf ctx (line ~642, immediately after `"address": org.address,`), add `"address_line2": org.address_line2,`.
    Do not touch report_service.rs's `empty_org()` unit-test helper further (already fixed for compile in Plan 20-01 Task 3) — no value assertion is required in this plan.
  </action>
  <verify>
    <automated>cd crates/trackly-app && cargo check -p trackly-app 2>&1 | tail -20 && grep -c '"address_line2": org_dto.address_line2' src/services/act_service.rs && grep -c '"address_line2": org.address_line2' src/services/report_service.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p trackly-app` exits 0.
    - `grep -c "\"address_line2\": org_dto.address_line2" crates/trackly-app/src/services/act_service.rs` returns 1 (render_pdf's ctx).
    - `grep -c "\"address_line2\": org.address_line2" crates/trackly-app/src/services/report_service.rs` returns 1 (export_pdf's ctx).
  </acceptance_criteria>
  <done>Both render_pdf and report_service::export_pdf pass address_line2 through to their template ctx, alongside render_acceptance_pdf (Task 1) — all three render paths now uniformly carry the field.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| org_settings (DB) -> render ctx -> HTML template string | Full organizational requisites (incl. logo BLOB as data: URI) now flow into the acceptance document, matching handover/report |
| pipeline.org_db == None (test fixtures only) -> render_acceptance_pdf | Degrade-safe fallback path — must never be reachable in production (AppCtx always wires org_db) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-01 | Information Disclosure | act_service.rs::render_acceptance_pdf ctx | accept | Full org requisites (name/inn/kpp/address/phone/fax/email/okpo/ogrn/logo) appearing in the acceptance print is the INTENDED behavior per PRN-01 — this is organizational business information already visible in Настройки to any authenticated admin, not end-user PII; identical exposure already exists in handover/report documents (Phase 14/16/17), no new disclosure class introduced |
| T-20-02-02 | Tampering | act_service.rs::render_acceptance_pdf (legacy org.json removal) | mitigate | D-11: `pipeline.organization.read_logo_bytes` call fully removed — no stale/dead code path that could silently re-introduce a second, un-authenticated logo source; verified by `grep -c read_logo_bytes` == 0 in Task 1's acceptance criteria |
| T-20-02-03 | Tampering | act_service.rs::render_acceptance_pdf logo_data_uri construction | accept | `logo_bytes`/`logo_mime` originate exclusively from `org_db.get_for_pdf()` (org_settings BLOB, written only via authenticated `Action::ManageSettings`-gated Settings UI) — never from request-supplied bytes; identical mitigation shape already documented at T-16-05 for render_pdf, now applies symmetrically to render_acceptance_pdf |
</threat_model>

<verification>
`cargo check -p trackly-app` exits 0. `grep` confirms `org_db.get_for_pdf()` appears exactly twice (render_pdf + render_acceptance_pdf), `read_logo_bytes` appears zero times, and `address_line2` is present in all three ctx-building sites (render_pdf, render_acceptance_pdf, report_service::export_pdf).
</verification>

<success_criteria>
- render_acceptance_pdf's org context is at full parity with render_pdf (PRN-01 Success Criterion #1).
- No legacy org.json/read_logo_bytes code path remains reachable from render_acceptance_pdf (D-11).
- address_line2 flows into ctx for all three render paths (ORG-02 Success Criterion #4 prerequisite — template display lands in Plan 20-03).
</success_criteria>

<output>
Create `.planning/phases/20-print-acts-org/20-02-SUMMARY.md` when done
</output>
