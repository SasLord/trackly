---
phase: 20-print-acts-org
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V035__org_settings_address_line2.sql
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/src/services/org_db_service.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/services/report_service.rs
  - crates/trackly-app/tests/html_report_render.rs
autonomous: true
requirements: [ORG-02]

must_haves:
  truths:
    - "org_settings хранит address_line2 (пустая строка по умолчанию для существующих строк)"
    - "OrgSettingsDto/OrgPatch несут address_line2 через весь Rust-контракт (компилируется без ошибок)"
  artifacts:
    - path: "migrations/V035__org_settings_address_line2.sql"
      provides: "ALTER TABLE org_settings ADD COLUMN address_line2"
      contains: "ADD COLUMN address_line2 TEXT NOT NULL DEFAULT ''"
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "OrgPatch.address_line2 + OrgSettingsDto.address_line2"
      contains: "pub address_line2: String"
    - path: "crates/trackly-app/src/services/org_db_service.rs"
      provides: "get/save_fields/get_for_pdf read+write address_line2"
      contains: "address_line2"
  key_links:
    - from: "crates/trackly-app/src/services/org_db_service.rs::get_for_pdf"
      to: "crates/trackly-app/src/dto/reports.rs::OrgSettingsDto"
      via: "SELECT ... address_line2 ... -> struct field r.get(12)"
      pattern: "address_line2"
---

<objective>
Заложить фундамент ORG-02 (вторая строка адреса организации): новая колонка `org_settings.address_line2` через refinery-миграцию V035, проброс поля через `OrgSettingsDto`/`OrgPatch`, и все три SQL-сайта в `org_db_service.rs` (`get`/`save_fields`/`get_for_pdf`). Также чинит все существующие места конструирования `OrgSettingsDto` литералом (`act_service.rs`'s `render_pdf` None-branch fallback, `report_service.rs`'s `empty_org()` тестовый хелпер, `tests/html_report_render.rs`'s `empty_org()`), которые сломают компиляцию крейта в момент добавления обязательного поля в структуру.

Purpose: Это D-04/D-10/D-07 (частично) из 20-CONTEXT.md — единая точка входа для нового поля, на которую опираются все downstream-планы этой фазы (сервисы рендера, шаблоны, UI, тесты).
Output: Компилируемый крейт `trackly-app` (lib + все test-таргеты) с `address_line2` на всех слоях данных; UI/шаблоны/рендер-логика ещё НЕ используют новое поле (это в 20-02/20-03/20-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-print-acts-org/20-CONTEXT.md
@.planning/phases/20-print-acts-org/20-PATTERNS.md
</context>

<interfaces>
<!-- Current OrgSettingsDto/OrgPatch (crates/trackly-app/src/dto/reports.rs) -->
```rust
pub struct OrgPatch {
    pub org_name: String,
    pub inn: String,
    pub kpp: String,
    pub address: String,
    pub phone: String,
    pub fax: String,
    pub email: String,
    pub okpo: String,
    pub ogrn: String,
}

pub struct OrgSettingsDto {
    pub org_name: String,
    pub inn: String,
    pub kpp: String,
    pub address: String,
    pub has_logo: bool,
    pub phone: String,
    pub fax: String,
    pub email: String,
    pub okpo: String,
    pub ogrn: String,
}
```
Task 1 appends `pub address_line2: String` (with doc-comment `/// Second address line (ORG-02, Phase 20). Empty string = not filled in.`) to BOTH structs, after `address` conceptually but positionally at the end of the field list (mirrors how phone/fax/email/okpo/ogrn were appended in Phase 14 — field declaration order in the struct does not need to match SQL column order).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration V035 + DTO extension (OrgPatch/OrgSettingsDto.address_line2)</name>
  <files>migrations/V035__org_settings_address_line2.sql, crates/trackly-app/src/dto/reports.rs</files>
  <read_first>
    migrations/V033__org_settings_requisites.sql (exact analog — doc-comment shape, DEFAULT '' rationale, PRAGMA user_version convention)
    migrations/V034__return_handover_date_backfill.sql (confirms V035 is the next free number)
    crates/trackly-app/src/dto/reports.rs lines 176-221 (OrgPatch + OrgSettingsDto current field lists, exact struct definitions to extend)
  </read_first>
  <action>
    Create migrations/V035__org_settings_address_line2.sql modeled on V033's doc-comment style: header comment references ORG-02 and 20-CONTEXT D-04/D-10, states the DEFAULT '' rationale (historic rows degrade to "no second line shown" via the D-06 `{% if %}` guard, never a misleading placeholder), states new columns are appended last (existing SELECT/UPDATE ordinal positions in org_db_service.rs unaffected). Body: `ALTER TABLE org_settings ADD COLUMN address_line2 TEXT NOT NULL DEFAULT '';` followed by `PRAGMA user_version = 35;`. Filename and PRAGMA value MUST be 35 (confirmed next-free by pattern-mapper against V034).
    In crates/trackly-app/src/dto/reports.rs: add `pub address_line2: String,` with a doc-comment `/// Second address line (ORG-02, Phase 20). Empty string = not filled in.` to both the `OrgPatch` struct (after line 187's `pub ogrn: String,`) and the `OrgSettingsDto` struct (after line 220's `pub ogrn: String,`). Field name `address_line2` (snake_case, matches file convention).
  </action>
  <verify>
    <automated>ls migrations/V035__org_settings_address_line2.sql && grep -c "ADD COLUMN address_line2 TEXT NOT NULL DEFAULT ''" migrations/V035__org_settings_address_line2.sql && grep -c "PRAGMA user_version = 35" migrations/V035__org_settings_address_line2.sql</automated>
  </verify>
  <acceptance_criteria>
    - migrations/V035__org_settings_address_line2.sql exists, contains exactly one `ALTER TABLE org_settings ADD COLUMN address_line2 TEXT NOT NULL DEFAULT ''` and `PRAGMA user_version = 35`.
    - `grep -c "pub address_line2: String" crates/trackly-app/src/dto/reports.rs` returns 2 (one in OrgPatch, one in OrgSettingsDto).
    - No other existing field in either struct was renamed or removed (diff is additive-only).
  </acceptance_criteria>
  <done>Migration file created with correct next-sequential number (V035) and PRAGMA; OrgPatch and OrgSettingsDto both carry address_line2.</done>
</task>

<task type="auto">
  <name>Task 2: org_db_service.rs — extend get()/save_fields()/get_for_pdf() SQL for address_line2</name>
  <files>crates/trackly-app/src/services/org_db_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/org_db_service.rs lines 51-118 (get, save_fields — current SELECT/UPDATE column lists and struct/params construction)
    crates/trackly-app/src/services/org_db_service.rs lines 363-401 (get_for_pdf — current SELECT + tuple construction, ordinal positions for logo_blob/logo_mime at 5/6)
  </read_first>
  <action>
    In `get()` (line ~56): append `, address_line2` to the SELECT column list (after `ogrn`), add `address_line2: r.get(10)?,` to the `OrgSettingsDto` struct literal (ordinal 10, after `ogrn` at ordinal 9).
    In `save_fields()` (line ~243): add `address_line2=?11` to the SET clause (after `ogrn=?10`), shift `updated_at_utc`/`version` params if needed or append `address_line2` param before `now` in the `params![...]` list — keep placeholder numbers (`?N`) and the params array in sync. Add `patch.address_line2` to the params list.
    In `get_for_pdf()` (line ~371): append `, address_line2` to the SELECT column list (after `ogrn`, i.e. after ordinal 11 which is `ogrn`), add `address_line2: r.get(12)?,` to the `dto` struct literal inside the row-mapping closure (mind that `logo_blob`/`logo_mime` occupy ordinals 5/6 — `address_line2` becomes ordinal 12, the new last column).
    Do NOT modify `migrate_from_org_json()` — legacy org.json never had a second address line; no migration-from-legacy handling needed for this field (per 20-CONTEXT D-11 and pattern-mapper note).
  </action>
  <verify>
    <automated>cd crates/trackly-app && grep -c "address_line2" src/services/org_db_service.rs</automated>
  </verify>
  <acceptance_criteria>
    - `get()`'s SELECT string and struct literal both reference `address_line2` (SQL column + `r.get(10)?`).
    - `save_fields()`'s UPDATE SET clause and `params![...]` array both reference `address_line2`/`patch.address_line2`, with placeholder numbering internally consistent (compiles — verified in Task 3's `cargo check`).
    - `get_for_pdf()`'s SELECT string and `dto` struct literal both reference `address_line2` (SQL column + `r.get(12)?`).
    - `migrate_from_org_json()` is unchanged (no address_line2 references added there).
  </acceptance_criteria>
  <done>All three org_settings SQL sites (get/save_fields/get_for_pdf) read and write address_line2 consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Fix all OrgSettingsDto literal construction sites broken by the new required field</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/src/services/report_service.rs, crates/trackly-app/tests/html_report_render.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs lines 2546-2567 (render_pdf's org_db-None-branch OrgSettingsDto literal — the fallback used when pipeline.org_db isn't wired, e.g. test fixtures without with_org_db)
    crates/trackly-app/src/services/report_service.rs lines 1407-1420 (empty_org() unit-test helper, module-private OrgSettingsDto literal)
    crates/trackly-app/tests/html_report_render.rs lines 64-77 (empty_org() integration-test helper, separate OrgSettingsDto literal in a different compilation unit)
  </read_first>
  <action>
    Add `address_line2: String::new(),` to each of the three OrgSettingsDto struct literals: act_service.rs's render_pdf None-branch (after the `ogrn: String::new(),` line, ~line 2562), report_service.rs's `empty_org()` test helper (after `ogrn: String::new(),`, ~line 1418), and tests/html_report_render.rs's `empty_org()` helper (after `ogrn: String::new(),`, ~line 75). This is a pure compile-fix — no behavioral change, since these are exactly the sites that construct OrgSettingsDto without going through org_db_service.rs's now-complete SQL sites (Task 2). Do not touch any other logic in these three files (the substantive render_acceptance_pdf rewrite and report_service.rs ctx wiring are out of scope for this plan — they land in the next wave).
  </action>
  <verify>
    <automated>cd crates/trackly-app && cargo check -p trackly-app --all-targets 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p trackly-app --all-targets` exits 0 (lib + every integration test binary in crates/trackly-app/tests/ compiles cleanly with the new required field present everywhere).
    - `grep -c "address_line2: String::new()" crates/trackly-app/src/services/act_service.rs` returns at least 1.
    - `grep -c "address_line2: String::new()" crates/trackly-app/src/services/report_service.rs` returns at least 1.
    - `grep -c "address_line2: String::new()" crates/trackly-app/tests/html_report_render.rs` returns 1.
  </acceptance_criteria>
  <done>Whole trackly-app crate (lib + all test targets) compiles cleanly with address_line2 present on OrgSettingsDto/OrgPatch everywhere it's constructed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| HTTP/Tauri client -> OrgDbService::save_fields | Authenticated settings-write path; caller-supplied OrgPatch (including new address_line2 free-text field) crosses into SQL UPDATE |
| org_settings row -> render/report ctx (downstream plans) | address_line2 stored value later flows into HTML templates (mitigated in Plan 20-03, not this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Tampering | org_db_service.rs::save_fields (address_line2 param) | accept | Free-text TEXT column, identical shape to phone/fax/email/okpo/ogrn (Phase 14) — no new validation needed; authorize(caller, &Action::ManageSettings) already gates the write (unchanged, existing mitigation) |
| T-20-01-02 | Elevation of Privilege | org_db_service.rs::save_fields | accept | Authorization check (`authorize(caller, &Action::ManageSettings)`) is pre-existing and untouched by this plan — no new privilege surface introduced |
| T-20-01-03 | Tampering | migrations/V035 (schema change) | accept | Additive-only ALTER TABLE ADD COLUMN with NOT NULL DEFAULT '' — cannot corrupt existing rows; refinery's embedded-migration mechanism (single transaction, applied once) is the established safe pattern from V026/V033 |
</threat_model>

<verification>
`cargo check -p trackly-app --all-targets` exits 0. `grep` confirms address_line2 present in migration, DTO structs, all three org_db_service.rs SQL sites, and all three compile-fix literal sites.
</verification>

<success_criteria>
- New migration V035 applies cleanly (additive ALTER TABLE, no manual SQL step).
- OrgSettingsDto/OrgPatch carry address_line2 end-to-end at the Rust type level.
- org_db_service.rs's three SQL sites (get/save_fields/get_for_pdf) read and write the new column.
- Whole crate compiles (lib + tests) — no downstream plan starts from a broken build.
</success_criteria>

<output>
Create `.planning/phases/20-print-acts-org/20-01-SUMMARY.md` when done
</output>
