# Phase 20: Печать актов и организация - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-13
**Phase:** 20-print-acts-org
**Areas discussed:** Шапка acceptance-печати, Вторая строка адреса, Безопасность SVG, Позиция address_line2, Миграция БД, Легаси org.json

---

## Шапка acceptance-печати (PRN-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Полный паритет | Тот же блок реквизитов, что в акте приёма-передачи: логотип + название + ИНН/КПП + адрес + тел/факс/email/ОКПО/ОГРН | ✓ |
| Только базовые | Логотип + название + ИНН/КПП + адрес, источник переключаем на БД (появится логотип), без тел/факс/email/ОКПО/ОГРН | |

**User's choice:** Полный паритет
**Notes:** Соответствует «и прочие реквизиты» в PRN-01. Как следствие — источник переключается на `get_for_pdf()` (БД), что заодно возвращает пропавший логотип.

---

## Вторая строка адреса — охват (ORG-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Все формы | Оба акта (handover + acceptance) И отчёты (report.html) | ✓ |
| Только акты | Только handover + acceptance | |

**User's choice:** Все формы
**Notes:** report.html уже читает полную org-шапку из БД, поэтому единообразие — минимальная доп. работа.

---

## Вторая строка адреса — метка поля (ORG-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Адрес (2-я строка) | Основное поле «Адрес», новое — «Адрес (2-я строка)» | ✓ |
| Адрес (продолжение) | Новое поле — «Адрес (продолжение)» | |
| Вторая строка адреса | Отдельное поле «Вторая строка адреса» (формулировка из REQUIREMENTS/ROADMAP) | |

**User's choice:** Адрес (2-я строка)

---

## Безопасность SVG (ORG-01)

| Option | Description | Selected |
|--------|-------------|----------|
| img-only + тест | Опереться на текущий `<img src=data:...>` (скрипты инертны) + regression-тест с `<script>`-в-SVG | ✓ |
| img-only + санитизация | Оставить img-рендер И добавить серверную санитизацию SVG как defense-in-depth | |

**User's choice:** img-only + тест
**Notes:** Требование 3 уже выполнено текущей реализацией; санитизация отклонена как избыточная.

---

## Позиция address_line2 в шапке

| Option | Description | Selected |
|--------|-------------|----------|
| Отдельной строкой | Новый `<div>` сразу под основным адресом, только если заполнена, единообразно в 3 шаблонах | ✓ |
| В той же строке | Адрес + line2 в одном `<div>` через разделитель | |

**User's choice:** Отдельной строкой

---

## Миграция старых БД

| Option | Description | Selected |
|--------|-------------|----------|
| ALTER + DEFAULT '' | Новая refinery-миграция: `ALTER TABLE org_settings ADD COLUMN address_line2 TEXT NOT NULL DEFAULT ''` | ✓ |
| Вы решаете | Форма миграции на усмотрение планировщика по паттерну V026 | |

**User's choice:** ALTER + DEFAULT ''

---

## Статус легаси org.json (в acceptance-рендере)

| Option | Description | Selected |
|--------|-------------|----------|
| Полностью на БД | acceptance читает всё (реквизиты + логотип) из `get_for_pdf()`; legacy org.json не используется | ✓ |
| БД + fallback | Читать из БД, но при отсутствии логотипа подхватывать старый из org.json | |

**User's choice:** Полностью на БД
**Notes:** БД — единый источник истины после one-time V026-миграции org.json→БД.

---

## Claude's Discretion

- Форма рефакторинга сборки `org` ctx (общий helper для трёх рендер-путей).
- Точный номер/имя файла новой миграции.
- Обновление doc-комментариев в шаблонах под новое поле и расширенную acceptance-шапку.

## Deferred Ideas

- Серверная санитизация SVG как defense-in-depth — рассмотрено и отклонено для этой фазы.
