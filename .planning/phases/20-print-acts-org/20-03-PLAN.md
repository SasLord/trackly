---
phase: 20-print-acts-org
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - crates/trackly-app/templates/act_acceptance.html
  - crates/trackly-app/templates/act_handover.html
  - crates/trackly-app/templates/report.html
autonomous: true
requirements: [PRN-01, ORG-02]

must_haves:
  truths:
    - "act_acceptance.html .requisites-блок выводит те же поля, что act_handover.html (name/inn+kpp/address/address_line2/phone/fax/email/okpo/ogrn) (D-01)"
    - "address_line2 выводится отдельной строкой (если заполнена) во всех трёх печатных шаблонах: act_handover.html, act_acceptance.html, report.html (D-06)"
    - "Логотип во всех трёх шаблонах остаётся img-only (без изменений — уже безопасен, ORG-01/D-08)"
  artifacts:
    - path: "crates/trackly-app/templates/act_acceptance.html"
      provides: "full .requisites parity block matching act_handover.html"
      contains: "org.phone"
    - path: "crates/trackly-app/templates/act_handover.html"
      provides: "address_line2 line in .requisites"
      contains: "org.address_line2"
    - path: "crates/trackly-app/templates/report.html"
      provides: "address_line2 line in .requisites"
      contains: "org.address_line2"
  key_links:
    - from: "crates/trackly-app/templates/act_acceptance.html"
      to: "crates/trackly-app/src/services/act_service.rs::render_acceptance_pdf ctx (Plan 20-02)"
      via: "{{ org.phone }}/{{ org.fax }}/{{ org.email }}/{{ org.okpo }}/{{ org.ogrn }}/{{ org.address_line2 }}"
      pattern: "org\\.(phone|fax|email|okpo|ogrn|address_line2)"
---

<objective>
Довести шапку `.requisites` в `act_acceptance.html` до полного паритета с `act_handover.html` (D-01, PRN-01) и добавить строку `address_line2` во все три печатных шаблона (`act_handover.html`, `act_acceptance.html`, `report.html`) по единому `{% if %}`-идиому (D-06, ORG-02).

Purpose: Это визуальная сторона PRN-01/ORG-02 — контекст уже несёт полный набор org-полей (Plan 20-02), эта работа делает их видимыми в HTML.
Output: Все три шаблона рендерят одинаковый набор org-реквизитов (с точностью до заголовка документа), включая вторую строку адреса, когда она заполнена.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-print-acts-org/20-CONTEXT.md
@.planning/phases/20-print-acts-org/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: act_acceptance.html — full .requisites parity block + address_line2 (D-01/D-06)</name>
  <files>crates/trackly-app/templates/act_acceptance.html</files>
  <read_first>
    crates/trackly-app/templates/act_acceptance.html (full file, 135 lines — current deficient `.requisites` block at lines 103-107, doc-comment context list at lines 10-13)
    crates/trackly-app/templates/act_handover.html lines 1-30, 131-140 (эталон — doc-comment context-var list shape + full `.requisites` block to copy verbatim)
  </read_first>
  <action>
    Replace act_acceptance.html's `.requisites` div (currently lines 103-107, containing only `org.name`/`org.inn`+`org.kpp`/`org.address`) with the full block copied from act_handover.html's `.requisites` div (lines 131-140), in this exact order: `org.name`, `org.inn`+`org.kpp`, `org.address`, then a NEW line `{%- if org.address_line2 %}<div>{{ org.address_line2 }}</div>{%- endif %}` immediately after `org.address` (D-06 idiom), then `org.phone` ("Тел.: "), `org.fax` ("Факс: "), `org.email` ("E-mail: "), `org.okpo` ("ОКПО "), `org.ogrn` ("ОГРН "). All lines use the same `{%- if org.X %}<div>...</div>{%- endif %}` guard idiom already established in act_handover.html — copy verbatim, do not invent new markup or CSS classes.
    Update the doc-comment context-variable list (currently lines 10-13, stating "Context (smaller than handover...): org.{name,inn,kpp,address,logo_data_uri}") to read: "Context (full org header, brought to parity with act_handover.html per PRN-01/D-01/D-02/D-03 — Phase 20): org.{name,inn,kpp,address,address_line2,logo_data_uri,phone,fax,email,okpo,ogrn}" followed by the unchanged device.{...} and document.{...} lines.
    Do NOT touch the `<img src="{{ org.logo_data_uri | safe }}">` line (line 100) or its surrounding security comment — img-only embedding is already correct (ORG-01/D-08) and out of scope for this task.
  </action>
  <verify>
    <automated>grep -c "org.phone" crates/trackly-app/templates/act_acceptance.html && grep -c "org.address_line2" crates/trackly-app/templates/act_acceptance.html && grep -c '<img src="{{ org.logo_data_uri | safe }}"' crates/trackly-app/templates/act_acceptance.html</automated>
  </verify>
  <acceptance_criteria>
    - `act_acceptance.html`'s `.requisites` block contains all of: `org.name`, `org.inn`, `org.kpp`, `org.address`, `org.address_line2`, `org.phone`, `org.fax`, `org.email`, `org.okpo`, `org.ogrn` (grep each returns >= 1).
    - `address_line2` line appears immediately after the `org.address` line and before `org.phone` (field order matches act_handover.html).
    - The `<img src="{{ org.logo_data_uri | safe }}">` line is byte-identical to before this task (unchanged).
    - Doc-comment header (top of file) no longer says "Context (smaller than handover...)" — updated to reflect full parity.
  </acceptance_criteria>
  <done>act_acceptance.html's requisites block is at full field-parity with act_handover.html, including address_line2, with the img-only logo embedding untouched.</done>
</task>

<task type="auto">
  <name>Task 2: act_handover.html + report.html — insert address_line2 line (D-06)</name>
  <files>crates/trackly-app/templates/act_handover.html, crates/trackly-app/templates/report.html</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html lines 11-20, 131-140 (current doc-comment context list + `.requisites` block)
    crates/trackly-app/templates/report.html lines 11-20, 135-144 (current doc-comment context list + `.requisites` block — byte-identical to act_handover.html's per report.html's own doc-comment, "copied verbatim from act_handover.html")
  </read_first>
  <action>
    In act_handover.html's `.requisites` block (lines 131-140), insert `{%- if org.address_line2 %}<div>{{ org.address_line2 }}</div>{%- endif %}` immediately after the `org.address` line (line 134) and before the `org.phone` line (line 135). Update the doc-comment context-variable list (lines 13-14, currently `org.name, org.inn, org.kpp, org.address, org.logo_data_uri, org.phone, org.fax, org.email, org.okpo, org.ogrn`) to insert `org.address_line2` after `org.address`.
    Apply the identical one-line insertion to report.html's `.requisites` block (lines 135-144, insert after line 138's `org.address` line, before line 139's `org.phone` line) and update its doc-comment context list (lines 12-13, same insertion point after `org.address`).
  </action>
  <verify>
    <automated>grep -c "org.address_line2" crates/trackly-app/templates/act_handover.html && grep -c "org.address_line2" crates/trackly-app/templates/report.html</automated>
  </verify>
  <acceptance_criteria>
    - `act_handover.html` contains exactly one `{%- if org.address_line2 %}<div>{{ org.address_line2 }}</div>{%- endif %}` line, positioned between the `org.address` and `org.phone` lines in `.requisites`.
    - `report.html` contains the identical line in the identical position within its own `.requisites` block.
    - Both files' doc-comment context-variable lists mention `org.address_line2`.
    - No other line in either file's `.requisites` block was reordered or altered.
  </acceptance_criteria>
  <done>act_handover.html and report.html both render address_line2 as a guarded line directly under the main address, matching act_acceptance.html's new block from Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| org_settings text fields -> HTML template interpolation | address_line2/phone/fax/email/okpo/ogrn are free-text fields written by an authenticated admin, rendered into all three print templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Injection (XSS) | act_acceptance.html/act_handover.html/report.html `{{ org.address_line2 }}` interpolation | mitigate | `build_safe_html_env` has autoescape ON (T-16-01/T-17-01-01, pre-existing) — plain `{{ var }}` interpolation is HTML-entity-encoded by construction; `address_line2` is interpolated WITHOUT `| safe` (unlike `logo_data_uri`), so any HTML/script content in the field is rendered as inert text, not markup |
| T-20-03-02 | Injection (XSS) | act_acceptance.html logo `<img src="{{ org.logo_data_uri | safe }}">` | accept (pre-existing, unchanged) | img-only data:-URI embedding (ORG-01/D-08) — untouched by this plan; regression-locked by Plan 20-05's D-09 test |
</threat_model>

<verification>
`grep` confirms all three templates contain `org.address_line2` in their `.requisites` block, positioned identically (after `org.address`, before `org.phone`), and act_acceptance.html now additionally carries `org.phone`/`org.fax`/`org.email`/`org.okpo`/`org.ogrn`.
</verification>

<success_criteria>
- act_acceptance.html's header is visually and structurally at parity with act_handover.html (PRN-01 Success Criterion #1).
- All three printed document types show a second address line when address_line2 is filled in (ORG-02 Success Criterion #4).
- Logo img-only embedding remains untouched in all three templates.
</success_criteria>

<output>
Create `.planning/phases/20-print-acts-org/20-03-SUMMARY.md` when done
</output>
