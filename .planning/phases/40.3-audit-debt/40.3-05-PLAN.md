---
phase: 40.3-audit-debt
plan: 05
type: execute
wave: 2
depends_on: ["40.3-04"]
files_modified:
  - ui/src/features/devices/DeviceFormBody.svelte
  - ui/src/features/devices/DeviceFormModal.svelte
  - ui/src/features/printers/PrintersPage.svelte
  - ui/src/features/printers/PrinterCreateModal.svelte
  - ui/src/lib/api/devices.ts
autonomous: false
requirements: ["NUM-06", "NUM-08", "PLC-06"]

must_haves:
  truths:
    - "«Завести принтер» на экране «Принтеры» открывает DeviceFormModal с типом «Принтер» по умолчанию — NumberTemplateField с автоподстановкой, кнопка «Вставка», цепочка попапов occupied→mismatch→script-mix работают так же, как на экране «Устройства» (ROADMAP Phase 40.3 SC3, D-02, NUM-06/NUM-08)"
    - "PrinterCreateModal.svelte удалён — обходного пути мимо шаблонов номеров и реестра INV-7 больше не существует (D-02)"
    - "После создания принтера notifyPlaceContentChanged вызывается для затронутого места — счётчики дерева обновляются без ручной перезагрузки (PLC-06; DeviceFormModal уже зарегистрирован в MUTATING_COMPONENTS реестра INV-7 — pnpm lint подтверждает это автоматически, без ручных правок реестра)"
    - "IP-адрес и SNMP community видны и сохраняются в ТОЙ ЖЕ транзакции, что и устройство, ТОЛЬКО в create-режиме при типе «Принтер» — режим правки принтера эти поля здесь не показывает (D-03)"
    - "devices.create() TS-обёртка удалена вместе со своим единственным вызывающим (PrinterCreateModal) — несоответствие типа DeviceDto/DeviceSaveOutcome устранено удалением, а не молчаливым несоответствием (D-04)"
  artifacts:
    - path: "ui/src/features/devices/DeviceFormBody.svelte"
      provides: "IP/community поля, видимые при !isEdit && typeId===PRINTER_TYPE_ID, передаются в createSingleWithNumberCheck"
      contains: "printerBlock"
    - path: "ui/src/features/devices/DeviceFormModal.svelte"
      provides: "initialTypeId prop для предзаданного типа при создании"
      contains: "initialTypeId"
    - path: "ui/src/features/printers/PrintersPage.svelte"
      provides: "DeviceFormModal вместо PrinterCreateModal, onSaved с notifyPlaceContentChanged"
      contains: "notifyPlaceContentChanged"
    - path: "ui/src/lib/api/devices.ts"
      provides: "createSingleWithNumberCheck(printer) + create() удалён"
  key_links:
    - from: "ui/src/features/printers/PrintersPage.svelte"
      to: "ui/src/features/devices/DeviceFormModal.svelte"
      via: "initialTypeId={2}, target={null}"
      pattern: "initialTypeId=\\{2\\}"
    - from: "ui/src/features/devices/DeviceFormBody.svelte"
      to: "ui/scripts/check-place-tree-invalidation.mjs (MUTATING_COMPONENTS)"
      via: "onSaved forwards placeId — DeviceFormModal уже в реестре, PrintersPage.onSaved обязан звать notifyPlaceContentChanged"
      pattern: "notifyPlaceContentChanged"
---

<objective>
Backend-контракт для printer IP/SNMP в той же транзакции готов (план 40.3-04). Этот план
подключает UI: заменяет `PrinterCreateModal` (двухшаговый, мимо шаблонов номеров и INV-7)
на существующий `DeviceFormModal` в режиме «Принтер», добавляет опциональный IP/community
блок в `DeviceFormBody` (create-режим, только для принтера), и подключает инвалидацию
счётчиков дерева мест через уже существующий в реестре INV-7 путь.

Purpose: единственный путь создания принтера — через шаблоны номеров и INV-7, без
обходных путей (D-02).
Output: `DeviceFormBody.svelte`/`DeviceFormModal.svelte`/`PrintersPage.svelte` обновлены,
`PrinterCreateModal.svelte` удалён, `devices.ts` очищен от мёртвого/некорректно
типизированного кода.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40.3-audit-debt/40.3-RESEARCH.md
@.planning/phases/40.3-audit-debt/40.3-PATTERNS.md
@.planning/phases/40.3-audit-debt/40.3-04-SUMMARY.md

<interfaces>
Backend-контракт из 40.3-04 (генерируется в `ui/src/bindings.ts` — gitignored,
регенерируется `pnpm --dir ui exec vitest` НЕ регенерирует; регенерируется
`cargo test -p trackly-app --test export_bindings` ИЛИ автоматически через `pnpm --dir ui
build`'s `prebuild` hook, `"prebuild": "cargo test -p trackly-app --test export_bindings"`
в `ui/package.json`) — ПЕРЕД правкой TS-файлов в этом плане ОБЯЗАТЕЛЬНО прогнать
`cargo test -p trackly-app --test export_bindings` (или `pnpm --dir ui build`), иначе
`PrinterCreateBlockDto`/расширенный `createSingleWithNumberCheck` не будут видны
`svelte-check`. Сгенерированный тип (camelCase, `#[serde(rename_all = "camelCase")]`):
```typescript
export type PrinterCreateBlockDto = { ipAddress: string | null; community: string | null };
```

ui/src/features/devices/DeviceFormBody.svelte — текущий `submitSingleCreate()` (строки
536-588):
```typescript
async function submitSingleCreate() {
  const confirms = confirmsFor(pendingConfirm, inventoryNo);
  const newDevice: DeviceNew = { type_id: typeId, name: name.trim(), inventory_no: null, /* ... */ };
  const numberInput: NumberFieldInput = { value: inventoryNo, templateId: selectedTemplateId, /* ... */ };
  let outcome: DeviceSaveOutcome;
  try {
    outcome = await devices.createSingleWithNumberCheck(newDevice, numberInput, numberContext);
  } catch (e) { /* ... */ }
  if (outcome.outcome === 'created') { /* ... onSaved(placeId) ... */ }
  /* ... */
}
```
`typeId`/`isEdit`/`PRINTER_TYPE_ID` — уже существующие в этом файле (строки 173, 182, 261).
`isEdit = $derived(target !== null)`.

Образец SNMP-полей из УДАЛЯЕМОГО `PrinterCreateModal.svelte` (для копирования UX, НЕ
структуры компонента):
```svelte
<div class="snmp-section">
  <p class="section-hint">SNMP / Сеть — заполните, если принтер подключён по сети (IP). Оставьте пустым для USB/локального принтера.</p>
  <div class="field">
    <label class="label" for="pc-ip">IP-адрес</label>
    <Input id="pc-ip" value={ipAddress} placeholder="192.168.1.100 (необязательно)" oninput={(v) => (ipAddress = v)} />
  </div>
  {#if ipAddress.trim()}
    <div class="field">
      <label class="label" for="pc-community">SNMP community</label>
      <Input id="pc-community" value={community} placeholder="public" oninput={(v) => (community = v)} />
    </div>
  {/if}
</div>
```

ui/src/features/devices/DeviceFormModal.svelte — текущий `$effect` (строки 96-104):
```typescript
$effect(() => {
  const isOpen = open;
  if (isOpen && !_wasOpen) {
    openInstanceCounter += 1;
    typeId = target?.type_id ?? DEVICE_TYPE_ID;
    confirmOpen = false;
  }
  _wasOpen = isOpen;
});
```
Props interface (строки 48-65) — добавить `initialTypeId?: number;` рядом с `target`.

ui/src/features/devices/DevicesPage.svelte — эталонный `onSaved` (строки 166-183, уже
подключён к `notifyPlaceContentChanged` из `$lib/stores/placeContentEvents.svelte`):
```typescript
function onSaved(result?: { typeId: number; placeId: number | null }) {
  const oldPlaceId = editTarget?.place_id ?? null;
  const newPlaceId = result?.placeId ?? null;
  const changedPlaceIds = Array.from(new Set([oldPlaceId, newPlaceId].filter((id): id is number => id !== null)));
  if (changedPlaceIds.length > 0) notifyPlaceContentChanged(changedPlaceIds);
  modalOpen = false;
  refresh();
  refreshCounts();
}
```
Для `PrintersPage.svelte` (create-режим, нет `editTarget`) — `oldPlaceId` всегда `null`.

ui/src/features/printers/PrintersPage.svelte — текущий рендер (строки 247-254):
```svelte
<PrinterCreateModal open={createOpen} onClose={() => (createOpen = false)} onSuccess={() => { createOpen = false; void refresh(); }} />
```
Кнопка «Завести принтер» (строка 157) открывает `createOpen = true` — остаётся без
изменений.

`ui/scripts/check-place-tree-invalidation.mjs` — `MUTATING_COMPONENTS` (строки ~706-711)
УЖЕ содержит `{ component: 'DeviceFormModal', props: ['onSaved'] }` — новых строк в
реестре НЕ требуется; гейт применится автоматически, как только `PrintersPage.svelte`'s
`onSaved` реально вызывает `notifyPlaceContentChanged`.

ui/src/lib/api/devices.ts — текущие (строки 24-57):
```typescript
create: (newDevice: DeviceNew) => apiCall<DeviceDto>('devices_create', { device: newDevice }),
createSingleWithNumberCheck: (newDevice: DeviceNew, numberInput: NumberFieldInput, context: TemplateContextDto) =>
  apiCall<DeviceSaveOutcome>('devices_create_single_with_number_check', { device: newDevice, numberInput, context }),
```
`create()` имеет единственного вызывающего — `PrinterCreateModal.svelte:59` — удаляемого
этим планом целиком.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IP/community поля в DeviceFormBody + devices.ts</name>
  <files>ui/src/features/devices/DeviceFormBody.svelte, ui/src/lib/api/devices.ts</files>
  <read_first>
    ui/src/features/devices/DeviceFormBody.svelte (весь файл — уже прочитан на этапе
    планирования; исполнителю читать целиком перед правкой, форма большая и связная)
    ui/src/lib/api/devices.ts (весь файл, 98 строк)
  </read_first>
  <behavior>
    - При `typeId === PRINTER_TYPE_ID && !isEdit` — блок «IP-адрес»/«SNMP community»
      рендерится (по образцу удаляемого PrinterCreateModal, см. interfaces); community
      видна только когда IP непустой.
    - При `typeId === DEVICE_TYPE_ID` ИЛИ `isEdit === true` — блок НЕ рендерится вовсе (не
      просто скрыт CSS — DOM-элемент отсутствует, D-03 «только при создании принтера»).
    - `submitSingleCreate()` при `typeId === PRINTER_TYPE_ID && ipAddress.trim() !== ''`
      строит `printer = { ipAddress: ipAddress.trim(), community: community.trim() || null
      }` и передаёт 4-м аргументом в `devices.createSingleWithNumberCheck(...)`; иначе
      передаёт `null` — поведение для «Устройство» и для «Принтер без IP» не меняется
      (дефолты сервера как раньше).
  </behavior>
  <action>
    В `ui/src/features/devices/DeviceFormBody.svelte` добавить `let ipAddress =
    $state('');` и `let community = $state('public');` рядом с существующими полями формы
    (не требуют явного reset-эффекта — компонент уже полностью remount'ится через
    `{#key openInstanceCounter}` в родителе на каждое открытие). Вставить новый `<div
    class="field">`-блок после поля «Место» (#9) и перед «Состояние» (#10), условно
    отрендеренный `{#if typeId === PRINTER_TYPE_ID && !isEdit}` — IP-адрес (label
    «IP-адрес», placeholder «192.168.1.100 (необязательно)») и, только когда
    `ipAddress.trim()` непусто, community (label «SNMP community», placeholder «public») —
    оба через существующий `Input` компонент, по образцу удаляемого
    `PrinterCreateModal.svelte`.

    Изменить `submitSingleCreate()`: после построения `numberInput`, вычислить
    `printerBlock` по правилу из `<behavior>` (тип `PrinterCreateBlockDto | null`, импорт
    из `'../../bindings'`), передать 4-м аргументом в
    `devices.createSingleWithNumberCheck(newDevice, numberInput, numberContext,
    printerBlock)`.

    В `ui/src/lib/api/devices.ts` расширить `createSingleWithNumberCheck` сигнатуру 4-м
    параметром `printer: PrinterCreateBlockDto | null = null`, добавить `printer` в объект
    аргументов `apiCall(...)`; добавить `PrinterCreateBlockDto` в импорт типов из
    `'../../bindings'`.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --test export_bindings 2>&1 | tail -20 && pnpm --dir ui exec svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -30</automated>
  </verify>
  <done>IP/community поля видны только create+Принтер, printerBlock корректно вычисляется и прокидывается, svelte-check зелёный.</done>
</task>

<task type="auto">
  <name>Task 2: DeviceFormModal initialTypeId + замена в PrintersPage + удаление PrinterCreateModal</name>
  <files>ui/src/features/devices/DeviceFormModal.svelte, ui/src/features/printers/PrintersPage.svelte, ui/src/features/printers/PrinterCreateModal.svelte, ui/src/lib/api/devices.ts</files>
  <read_first>
    ui/src/features/devices/DeviceFormModal.svelte (весь файл, 270 строк — Props interface
    и $effect на открытие)
    ui/src/features/printers/PrintersPage.svelte (весь файл, 276 строк)
  </read_first>
  <action>
    В `ui/src/features/devices/DeviceFormModal.svelte`: добавить в `Props` interface
    `initialTypeId?: number;` (с кратким doc-комментарием: «предзаданный тип для
    create-режима, например вызов с экрана «Принтеры» — игнорируется, когда `target !==
    null` (правка всегда использует `target.type_id`)»), деструктурировать в `const {
    open, target, onClose, onSaved, initialTypeId }: Props = $props();`. В `$effect`
    открытия заменить `typeId = target?.type_id ?? DEVICE_TYPE_ID;` на `typeId =
    target?.type_id ?? initialTypeId ?? DEVICE_TYPE_ID;` — существующие вызывающие
    (`DevicesPage.svelte`, не передающие `initialTypeId`) не меняют поведение (`undefined ??
    DEVICE_TYPE_ID` = `DEVICE_TYPE_ID`).

    В `ui/src/features/printers/PrintersPage.svelte`: удалить `import PrinterCreateModal
    from './PrinterCreateModal.svelte';`, добавить `import DeviceFormModal from
    '../devices/DeviceFormModal.svelte';` и `import { notifyPlaceContentChanged } from
    '$lib/stores/placeContentEvents.svelte';`. Заменить рендер `<PrinterCreateModal
    open={createOpen} onClose={...} onSuccess={...} />` на:
    ```svelte
    <DeviceFormModal
      open={createOpen}
      target={null}
      initialTypeId={2}
      onClose={() => (createOpen = false)}
      onSaved={(result) => {
        createOpen = false;
        const newPlaceId = result?.placeId ?? null;
        if (newPlaceId !== null) notifyPlaceContentChanged([newPlaceId]);
        void refresh();
      }}
    />
    ```
    (create-режим — `oldPlaceId` всегда `null`, по образцу `DevicesPage.svelte::onSaved`).

    Удалить файл `ui/src/features/printers/PrinterCreateModal.svelte` целиком.

    В `ui/src/lib/api/devices.ts` удалить метод `create` (строки 24-33, включая
    объясняющий doc-комментарий про `DeviceSaveOutcome`/`PrinterCreateModal` — комментарий
    теряет предмет вместе с удаляемым вызывающим) и убрать `DeviceDto`/`DeviceNew` из
    импорта типов, ЕСЛИ они после удаления `create` больше нигде в файле не используются
    (свериться grep'ом перед удалением импортов — `DeviceNew` используется и
    `createSingleWithNumberCheck`/`bulkCreate`, скорее всего останется нужен;
    `DeviceListResponse`/`DeviceDto` тоже используются в `list`/`get` — вероятно, только
    сам метод `create` удаляется, импорты не трогать без проверки).
  </action>
  <verify>
    <automated>test ! -f ui/src/features/printers/PrinterCreateModal.svelte && echo "deleted" && pnpm --dir ui exec svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -30 && pnpm --dir ui lint 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn "PrinterCreateModal" ui/src` возвращает 0 строк.
    `grep -n "devices\.create(" ui/src/features/printers/PrintersPage.svelte ui/src/lib/api/devices.ts`
    возвращает 0 строк (метод и его единственный вызов удалены).
    `pnpm --dir ui lint` зелёный, включая `check-place-tree-invalidation.mjs`/INV-7 гейт для
    нового `PrintersPage.svelte::onSaved`.
  </acceptance_criteria>
  <done>«Завести принтер» открывает DeviceFormModal(initialTypeId=2); PrinterCreateModal.svelte удалён; devices.create() удалён; pnpm lint и svelte-check зелёные.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: живая проверка «Завести принтер» через DeviceFormModal</name>
  <what-built>
    «Завести принтер» на экране «Принтеры» теперь использует тот же диалог, что и
    «Устройства» (типа «Принтер») — шаблоны номеров, попапы occupied/mismatch/script-mix,
    IP/SNMP в create-режиме, инвалидация счётчиков дерева мест.
  </what-built>
  <files></files>
  <action>Автоматического действия нет — эта задача целиком ручная проверка живого приложения, см. <how-to-verify> ниже.</action>
  <how-to-verify>
    1. `pnpm --dir ui build` (LAN/HTTP-режим требует актуальную сборку), затем
       `cargo tauri dev`.
    2. Настройки → Организация → Шаблоны номеров: убедиться, что для типа «Устройства и
       принтеры» есть контекст «Новый принтер» с активным шаблоном (или создать
       временный).
    3. Экран «Принтеры» → «Завести принтер». Проверить: поле номера показывает
       автоподстановку «первый свободный» по шаблону; кнопка «Вставка» открывает меню
       шаблонов; ниже поля «Место» появляется блок «IP-адрес»/«SNMP community» (community
       появляется только при непустом IP).
    4. Ввести номер, ЗАНЯТЫЙ другим устройством — сохранить — должен появиться попап
       «Номер занят».
    5. Ввести свободный номер + IP + место — сохранить. Проверить: принтер появился в
       списке; счётчик занятости выбранного места в дереве «Места» обновился БЕЗ
       перезагрузки страницы; открыть карточку принтера — IP-адрес сохранён.
    6. Открыть созданный принтер на правку (тип «Принтер», DeviceFormModal в edit-режиме)
       — блок IP/community НЕ должен отображаться в этой форме (правка IP остаётся через
       существующую карточку принтера/PrinterDetail).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify><human-check>Подтверждено пользователем в ответ на resume-signal.</human-check></verify>
  <done>Пользователь подтвердил "approved" либо перечисленные расхождения устранены и проверка повторена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Tauri webview форма → devices_create_single_with_number_check | Пользовательский ввод IP/community/номера пересекает границу процесса — untrusted input, уже валидируется на бэкенде (40.3-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.3-11 | Tampering | Двойная точка входа создания принтера (обход D-01 цепочки/INV-7) | mitigate | `PrinterCreateModal` физически удалён — единственный путь создания принтера теперь `DeviceFormModal` → `create_single_with_number_check_with_printer`, тот же путь, что и «Устройства» (устраняет class бага «альтернативный UI-путь с иным уровнем проверки», уже отмеченный в 40.3-RESEARCH Known Threat Patterns) |
| T-40.3-12 | Information Disclosure | IP/community видны в форме создания принтера | accept | Тот же уровень доступа, что уже был у `PrinterCreateModal` (Admin/Manager, гейт на кнопку «Завести принтер» в `PrintersPage.svelte`) — RBAC-поверхность не расширяется, переиспользуется существующая `MutateDevices` проверка |
</threat_model>

<verification>
```bash
cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --test export_bindings
pnpm --dir ui exec svelte-check --tsconfig ./tsconfig.json
pnpm --dir ui lint
pnpm --dir ui build
```
</verification>

<success_criteria>
«Завести принтер» использует DeviceFormModal с шаблонами номеров, INV-7-инвалидацией и
IP/SNMP в той же транзакции; PrinterCreateModal.svelte удалён; devices.create() удалён;
живая проверка в cargo tauri dev подтверждена.
</success_criteria>

<output>
Create `.planning/phases/40.3-audit-debt/40.3-05-SUMMARY.md` when done
</output>
