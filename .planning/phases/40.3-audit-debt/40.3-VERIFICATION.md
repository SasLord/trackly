---
phase: 40.3-audit-debt
verified: 2026-09-23T15:48:22Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 8/13
  gaps_closed:
    - "Домен, DTO и оба SQL write-пути используют один и тот же двойной-Option паттерн, что и inventory_no/place_id (план 40.3-01, must_have truth) — закрыто планом 40.3-06 (place_id + UserPatch.email)"
    - "«Завести принтер» атомарно создаёт устройство и printers-строку с введёнными IP/community в одной транзакции (ROADMAP SC3, N-2) — закрыто планом 40.3-07 (quantityDisabled + bulk_create_with_printer guard)"
    - "В ui/src не остаётся кода, который считает номер акта начинающимся с цифр — проверено структурным гейтом (план 40.3-03, must_have truth, D-05) — закрыто планом 40.3-08 (ReturnModal preview + двойной гейт с selftest)"
  gaps_remaining: []
  regressions: []
---

# Phase 40.3: Долг аудита v1.4 Verification Report (раунд 2, после закрытия пробелов)

**Phase Goal:** Закрыть четыре находки повторного аудита вехи (2026-09-22), которые дёшевы
сейчас и лежат под фундаментом Фазы 41: пользователь может стереть необязательные поля
устройства, тост возврата показывает номер акта по шаблону без искажения, «Завести принтер»
на экране «Принтеры» идёт через шаблоны номеров и инвалидацию дерева мест, а проверка
целостности FK после миграций не пропускается на повторном старте.
**Verified:** 2026-09-23T15:48:22Z
**Status:** passed
**Re-verification:** Да — после раунда закрытия пробелов (планы 40.3-06..40.3-09), закрывающего
3 BLOCKER'а и 2 WARNING'а первичной верификации (см. `re_verification.gaps_closed` в frontmatter)

## Goal Achievement

Эта верификация — повторный проход после первичной верификации (`gaps_found`, 8/13,
2026-09-22T23:20:23Z, полный текст сохранён в git-истории этого файла). Каждый из трёх
BLOCKER'ов и обоих WARNING'ов проверен заново прямым чтением кода и независимым запуском
тестов/гейтов в этой сессии (не только цитированием SUMMARY.md или `40.3-REVIEW.md` — раунд-2
код-ревью использован как вход, но каждая его находка перепроверена самостоятельно). Ранее
VERIFIED истины (не бывшие в списке гэпов) проверены регрессионно (существование + беглая
проверка, без полного повторного анализа).

### Observable Truths

| # | Truth | Source | Status | Evidence |
|---|-------|--------|--------|----------|
| 1 | SC1 (WR-03): очистка serial_no/model/specs/kit/state в форме правки реально сохраняет пустое значение (оба транспорта); незатронутое поле не меняется | ROADMAP SC1 | ✓ VERIFIED (regression, не изменялось в раунде 2) | Не тронуто планами 06-09; сохраняется структура 5 полей с serde double_option в `dto/device.rs` |
| 2 | place_id (DevicePatch) и email (UserPatch) несут serde double_option — очистка «Места» и email реально сохраняется как NULL, не молча игнорируется (BLOCKER-1/CR-01, план 40.3-06) | PLAN 40.3-06 / gap 1 | ✓ VERIFIED | Прочитан код: `dto/device.rs:201-213` (place_id — атрибут перед полем), `dto/auth.rs:77-79` (email — атрибут перед полем). Оба новых JSON-boundary теста запущены независимо: `cargo test -p trackly-app --test devices_crud place_id` — 2/2 ok; `cargo test -p trackly-app --test users_crud email` — 3/3 ok (включая старый литеральный тест) |
| 3 | Регресс-тесты на place_id/email проходят через РЕАЛЬНУЮ serde-границу (serde_json::from_str), не Rust-литерал | PLAN 40.3-06 | ✓ VERIFIED | Прочитан код тестов `devices_crud.rs:873-990`, `users_crud.rs:734-825` — оба используют `serde_json::from_str`, не литерал |
| 4 | SC2 (N-1): тост возврата показывает `ActDto.number` как есть, без склейки number_raw+regex | ROADMAP SC2 | ✓ VERIFIED (regression) | `ReturnModal.svelte:327`: `pushToast('success', \`Создан акт возврата №${ret.number}. ...\`)` — не изменялось в раунде 2 |
| 5 | ReturnModal.svelte больше не вычисляет parentNumber/predictedSubNumber из number_raw — предпросмотр не показывает номер, которого сервер не создаст (BLOCKER-3/WR-08, план 40.3-08) | PLAN 40.3-08 / gap 3a | ✓ VERIFIED | `grep -c "number_raw\|parentNumber\|predictedSubNumber" ReturnModal.svelte` = 0; строка 366: `<p class="subheading">Создаст акт возврата</p>` — без интерполяции |
| 6 | Структурный гейт ловит все 5 известных форм дефекта (WR-07), доказано встроенным `--selftest`, а не одной буквальной строкой (план 40.3-08) | PLAN 40.3-08 / gap 3b | ✓ VERIFIED | Запущено независимо: `node ui/scripts/check-act-number-no-regex-split.mjs --selftest` → 7/7 PASS, exit=0; реальное сканирование репозитория → `PASS — 0 нарушений`, exit=0 |
| 7 | В ui/src единственное оставшееся вхождение number_raw (ActFormBody.svelte:178, легитимный префилл) явно занесено в whitelist гейта | PLAN 40.3-08 | ✓ VERIFIED | `ALLOWED_NUMBER_RAW_FILES = new Set(['src/features/acts/ActFormBody.svelte'])` в коде гейта (прочитано); selftest-фикстура `whitelisted number_raw` — PASS |
| 8 | SC3 (N-2): «Завести принтер» даёт шаблоны номеров/попапы/автоподстановку как на «Устройствах»; счётчики дерева мест обновляются; точка входа в реестре INV-7; тип ответа devices.create соответствует серверному | ROADMAP SC3 | ✓ VERIFIED (regression + human-observed, см. ниже) | `PrintersPage.svelte` → `DeviceFormModal(initialTypeId=2)`, в `MUTATING_COMPONENTS` (`check-place-tree-invalidation.mjs:709`); `onSaved` → `notifyPlaceContentChanged`; `grep -rn "devices.create(" ui/src` — 0 совпадений (не считая `createSingleWithNumberCheck`/`bulkCreate`) |
| 9 | При typeId===PRINTER_TYPE_ID и непустом IP «Количество» принудительно заблокировано на 1 — printerBlock ВСЕГДА уходит через createSingleWithNumberCheck, никогда через bulkCreate (BLOCKER-2/CR-02, план 40.3-07) | PLAN 40.3-07 / gap 2 | ✓ VERIFIED | `DeviceFormBody.svelte:297-302`: `quantityDisabled = $derived(isEdit \|\| inventoryNo.trim() !== '' \|\| serialNo.trim() !== '' \|\| (typeId === PRINTER_TYPE_ID && ipAddress.trim() !== ''))` — прочитано напрямую |
| 10 | `bulk_create_with_printer` явно отклоняет непустой printer-блок через AppError::Validation{field:"printer"} — defense-in-depth; старый `bulk_create` остаётся тонкой обёрткой, существующие вызывающие не меняются | PLAN 40.3-07 | ✓ VERIFIED | `device_service.rs:1532` (метод) и `:1548-1555` (guard ДО открытия транзакции) прочитаны; `bulk_create` (:1662) — тонкая обёртка `self.bulk_create_with_printer(new, count, None)`. Тест `bulk_create_with_printer_rejects_non_empty_printer_block` запущен независимо в составе полного файла: `cargo test -p trackly-app --test devices_bulk_create` — 11/11 ok |
| 11 | SC4 (N-4): новое FK-нарушение после миграций блокирует старт на КАЖДОМ следующем запуске; предсуществующие нарушения не блокируют | ROADMAP SC4 | ✓ VERIFIED (regression) | `run_to` в `migrations.rs` — структура из плана 40.3-02 не менялась, только внутренности сравнения (см. #12/#13) |
| 12 | persist_fk_baseline на запись — guarded best-effort (tracing::warn! + продолжить), симметрично read; run_up_to(<16) не падает при отсутствии app_settings (WARNING-1/WR-03, план 40.3-09) | PLAN 40.3-09 / gap 4a | ✓ VERIFIED | `migrations.rs:419-423`: `if let Err(e) = persist_fk_baseline(conn, &after) { tracing::warn!(...); }` — прочитано напрямую, старый `?`-проброс отсутствует |
| 13 | Сравнение бейзлайна ключуется по устойчивой сигнатуре (table, parent, fkid) с учётом кратности, не по нестабильному rowid — перестройка таблицы не создаёт ложных блокировок и не маскирует новые нарушения (WARNING-2/WR-01, план 40.3-09) | PLAN 40.3-09 / gap 4b | ✓ VERIFIED | `FkSignature`/`signature_counts`/`violations_with_grown_signature` прочитаны в коде (`migrations.rs:86-131`); `run_to` использует `violations_with_grown_signature(&after, &baseline)` (:391), `after.difference(&baseline)` в продакшн-коде отсутствует (0 совпадений grep) |

**Score:** 13/13 истин подтверждены полностью — все три BLOCKER'а и оба WARNING'а первичной
верификации закрыты и независимо перепроверены в этой сессии.

### Independently Re-run Tests (this session, not trusted from SUMMARY/REVIEW)

| Command | Result |
|---------|--------|
| `cargo test -p trackly-infra --lib db::migrations -- --skip login_remember_persistent_cookie` | 10/10 ok (включает 4 новых теста плана 09 + оба существующих N-4 теста без регрессии) |
| `cargo test -p trackly-app --test devices_crud place_id -- --skip login_remember_persistent_cookie` | 2/2 ok |
| `cargo test -p trackly-app --test users_crud email -- --skip login_remember_persistent_cookie` | 3/3 ok |
| `cargo test -p trackly-app --test devices_bulk_create -- --skip login_remember_persistent_cookie` | 11/11 ok |
| `node ui/scripts/check-act-number-no-regex-split.mjs --selftest` | 7/7 PASS, exit=0 |
| `node ui/scripts/check-act-number-no-regex-split.mjs` (реальное сканирование репо) | PASS — 0 нарушений, exit=0 |
| `pnpm --dir ui exec svelte-check --tsconfig ./tsconfig.json` | 0 ошибок, 68 warnings (все pre-existing/тот же класс, `291 FILES 0 ERRORS`) |
| `pnpm --dir ui lint` (полная цепочка, 15+ гейтов включая eslint/prettier/check-act-number-no-regex-split) | PASS по всем шагам |

Полный `cargo test --workspace -- --skip login_remember_persistent_cookie` в этой сессии
повторно не запускался (уже прогнан после последнего коммита `cc340890` с 0 упавших — см.
project-instructions/important_context; конкурентный `cargo test` запрещён project memory).
Вместо этого — таргетированные прогоны выше, покрывающие ровно изменённые в раунде 2 файлы.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/trackly-app/src/dto/device.rs` | serde double_option на ВСЕХ 6 double-Option полях (5+place_id) | ✓ VERIFIED | `grep -c "with = \"serde_with::rust::double_option\""` = 6 |
| `crates/trackly-app/src/dto/auth.rs` | serde double_option на UserPatch.email | ✓ VERIFIED | Атрибут присутствует перед `pub email: Option<Option<String>>` |
| `crates/trackly-app/tests/devices_crud.rs` | JSON-boundary regression на place_id | ✓ VERIFIED | 2 новых теста, зелёные |
| `crates/trackly-app/tests/users_crud.rs` | JSON-boundary regression на email | ✓ VERIFIED | 2 новых теста + старый литеральный, все зелёные |
| `ui/src/features/devices/DeviceFormBody.svelte` | quantityDisabled учитывает IP для Принтера | ✓ VERIFIED | 4-е условие `(typeId === PRINTER_TYPE_ID && ipAddress.trim() !== '')` присутствует |
| `crates/trackly-app/src/services/device_service.rs` | `bulk_create_with_printer` с явным guard'ом; `bulk_create` — тонкая обёртка | ✓ VERIFIED | Оба метода прочитаны, guard ДО транзакции |
| `crates/trackly-app/tests/devices_bulk_create.rs` | regression на отклонение printer-блока в bulk-пути | ✓ VERIFIED | 11/11 тестов зелёные |
| `ui/src/features/acts/ReturnModal.svelte` | без клиентского предсказания номера в предпросмотре | ✓ VERIFIED | 0 вхождений number_raw/parentNumber/predictedSubNumber |
| `ui/scripts/check-act-number-no-regex-split.mjs` | двойной инвариант + `--selftest` | ✓ VERIFIED | 7/7 selftest PASS, реальное сканирование PASS |
| `crates/trackly-infra/src/db/migrations.rs` | guarded persist + rowid-независимая сигнатура | ✓ VERIFIED | Оба фикса прочитаны и покрыты тестами (10/10) |
| `ui/src/lib/components/NumberTemplateField.svelte` | `templateActionsDisabled` prop (внеплановый UAT-фикс плана 07) | ✓ VERIFIED (in-scope deviation) | Проп присутствует, применяется только к кнопке/стрелке через `actionsDisabled = disabled \|\| templateActionsDisabled`, Input остаётся с чистым `disabled` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `dto/device.rs::DevicePatch.place_id` | `domain::DevicePatch.place_id` | `impl From` — конверсия не менялась, была недостижима только из-за serde-схлопывания | ✓ WIRED | Конверсия корректна и теперь достижима — serde больше не схлопывает `{"place_id": null}` |
| `dto/auth.rs::UserPatch.email` | `services/auth.rs::AuthService::update_user` SQL CASE WHEN | serde-граница | ✓ WIRED | SQL-слой не менялся (был уже корректен), serde больше не блокирует достижимость |
| `DeviceFormBody.svelte::quantityDisabled` | `handleSubmit` `qty` computation | `quantityDisabled ? 1 : ...` | ✓ WIRED | При заполненном IP для Принтера `qty` структурно равен 1 → только `submitSingleCreate()` |
| `device_service.rs::bulk_create` | `bulk_create_with_printer` | тонкая обёртка, `printer=None` | ✓ WIRED | Существующие вызывающие (devices_bulk_create.rs/devices_type_conversion.rs/devices_numbering.rs/транспорты) не изменены, компилируются и проходят |
| `ReturnModal.svelte` тост | `ActDto.number` | `ret.number` напрямую | ✓ WIRED | Не менялось в раунде 2 |
| `ReturnModal.svelte` предпросмотр | (нет реконструкции) | текст без интерполяции | ✓ WIRED (исправлено) | Ранее NOT_WIRED из-за клиентской реконструкции — теперь просто не показывает номер |
| `check-act-number-no-regex-split.mjs` | `ui/package.json` lint chain | `node scripts/check-act-number-no-regex-split.mjs` внутри `lint` | ✓ WIRED (реальное сканирование) / ⚠️ `--selftest` НЕ подключён в lint/CI (WR-13, non-blocking, см. Anti-Patterns) | `pnpm lint` запускает гейт без `--selftest` |
| `migrations.rs::run_to` | `persist_fk_baseline` | `if let Err(e) = ... { tracing::warn!(...) }` | ✓ WIRED | Guarded, симметрично чтению |
| `migrations.rs::run_to` | `violations_with_grown_signature` | заменяет `after.difference(&baseline)` | ✓ WIRED | Единственный вызов в продакшн-коде |

### Requirements Coverage

Фаза не вводит новых REQ-ID («без новых REQ-ID» в ROADMAP) — подтверждено: `grep -n "40\.3"
.planning/REQUIREMENTS.md` не находит ни одного упоминания фазы 40.3 как источника REQ-ID.
Все формальные ID (WR-03, N-1, N-2, N-4 — обозначения аудит-находок, не REQ-ID из
трейсинг-таблицы) и REQ-ID, объявленные в frontmatter планов (NUM-06, NUM-08, NUM-14, HST-03,
PLC-06), уже отмечены Complete в `REQUIREMENTS.md` за более ранними фазами.

| Requirement | Source Plan | Ранее закрыто в | Статус в этой фазе (раунд 2) | Evidence |
|-------------|------------|------------------|-------------------------------|----------|
| WR-03 (аудит-находка, не REQ-ID) | 40.3-01, 40.3-06 | — | ✓ SATISFIED полностью (все 6 полей DevicePatch + email UserPatch) | Truths 1-3 |
| N-1 (аудит-находка) | 40.3-03, 40.3-08 | — | ✓ SATISFIED структурно (тост + предпросмотр + гейт с self-test) | Truths 4-7 |
| N-2 (аудит-находка) | 40.3-04, 40.3-05, 40.3-07 | — | ✓ SATISFIED (атомарность держится при любом qty) | Truths 8-10 |
| N-4 (аудит-находка) | 40.3-02, 40.3-09 | — | ✓ SATISFIED (безусловность + устойчивость к rebuild + guarded persist) | Truths 11-13 |
| NUM-06, NUM-08 | 40.3-04, 40.3-05, 40.3-07 | Phase 40.2 | ✓ SATISFIED | Truths 8-10 |
| NUM-14, HST-03 | 40.3-03, 40.3-08 | Phase 40.2 / Phase 40 | ✓ SATISFIED | Truths 4-7 |
| PLC-06 | 40.3-05 | Phase 39 | ✓ SATISFIED | Truth 8 |

Орфанных REQ-ID не найдено — фаза сознательно не вводит новых ID.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `ui/package.json` / `ui/scripts/check-act-number-no-regex-split.mjs` | `lint` script | `--selftest` не вызывается из `pnpm lint`/CI (WR-13, round-2 review) | ⚠️ WARNING (non-blocking, carried forward) | Само доказательство полноты гейта (7 фикстур) не защищено от будущего незаметного ослабления регулярок; не блокирует текущее закрытие BLOCKER-3, но требует внимания при следующем касании файла |
| `ui/scripts/check-act-number-no-regex-split.mjs:67` | `DIGIT_PREFIX_REGEX_LITERAL_RE` | Широкий текстовый матч, не привязан к контексту regex-литерала (WR-14, round-2 review) | ⚠️ WARNING (non-blocking, carried forward) | Теоретический будущий false-positive (например, гипотетический `/^data:/`-паттерн в другом файле); сегодня 0 нарушений в реальном репо, не влияет на текущую корректность |

Debt-маркеры (`TBD`/`FIXME`/`XXX`) в изменённых файлах не найдены (единственное совпадение —
`"SN-XXXXXXXX"` placeholder-текст поля серийного номера, не debt-маркер).

Приватность: grep изменённых в раунде 2 файлов на email/ИНН/ОГРН/КПП/ОКПО/реальные ФИО —
чисто; тестовые данные вымышленные (`petrov`/«Петров П.П.»/`petrov@example.com`), тестовые IP —
RFC 5737 (`192.0.2.0/24`).

### Deliberately Out-of-Scope (confirmed intact, not a gap)

IP/SNMP-блок в `DeviceFormBody.svelte` по-прежнему рендерится только при `typeId ===
PRINTER_TYPE_ID && !isEdit` (строка 967, не менялась) — редактирование сетевых параметров
принтера явно отложено пользователем в отдельную будущую фазу, поскольку у бэкенда нет пути
обновления этих параметров вообще (`PrinterRepo` без update-метода). Отслежено в
`.planning/todos/pending/2026-09-23-printer-network-params-editable.md` (подтверждено на
диске).

### In-Scope Deviation: edit-mode «Вставка» для Инвентарный №

Найдено и исправлено на живом чекпоинте плана 40.3-07 (не входило в исходные must_haves):
`NumberTemplateField` получил узкий проп `templateActionsDisabled` (гасит только
кнопку «Вставка» + стрелку ↑/↓, сам `Input` остаётся редактируемым через обычный `disabled`).
Прочитан код `DeviceFormBody.svelte:810-825`: `inventoryTemplateActionsDisabled = savedInventoryNo
!== '' && inventoryNo.trim() === savedInventoryNo` — кнопка неактивна, пока живое значение
совпадает с исходно сохранённым; активна сразу, если исходное значение было пустым; активна
после очистки/правки. Поведение соответствует правилу пользователя, зафиксированному в
задании этой верификации, и подтверждено живым наблюдением человека (раунд 2 чекпоинта плана
07, см. ниже).

### Human Verification — Closed This Round (genuine live observation, not auto-approve)

В отличие от первичной верификации (оба чекпоинта были закрыты auto-approve БЕЗ наблюдения —
BLOCKER-3 и часть human_verification round 1), в этом раунде **два чекпоинта закрыты реальным
наблюдением человека в запущенном `cargo tauri dev`**, подтверждено коммитами от имени
реального пользователя (`Alexander Platov`, git log: `6f41b079`, `162681e9`) и явным текстом
SUMMARY.md ("Чекпоинт ЭТОГО плана был явно пройден через ДВА раунда РЕАЛЬНОГО наблюдения
человека").

1. **Тост возврата с шаблонным номером акта + предпросмотр без предсказания (перенесено из
   40.3-03, закрыто в плане 40.3-08).** Подтверждено: предпросмотр показывает ровно «Создаст
   акт возврата» без номера; тост после сохранения показывает корректный шаблонный номер без
   дублирования.
2. **«Завести принтер» через DeviceFormModal + сценарий CR-02 (IP + Количество), перенесено из
   40.3-05, закрыто в плане 40.3-07.** Первый проход выявил UAT-дефект (edit-mode «Вставка»
   отсутствовала) — координатор НЕ одобрил чекпоинт, вернул на фикс. Второй проход (после
   `1bd215d6`) — подтверждено APPROVED по всем пунктам: автоподстановка по шаблону, попап
   «Номер занят», IP/SNMP-блок только в create-режиме, обновление счётчика дерева мест без
   перезагрузки, сохранение IP, «Количество» остаётся заблокированным на 1 при заполненном IP,
   реально сохранённый (не NULL) IP у созданного принтера, корректное поведение edit-mode
   «Вставка».

Открытых пунктов, требующих ручной проверки, не осталось — human_verification секция пуста,
поэтому status не human_needed.

### Gaps Summary

Ни одного открытого гэпа. Все 3 BLOCKER'а (CR-01/place_id-email, CR-02/bulk-path IP loss,
WR-07+WR-08/act-number gate+preview) и оба WARNING'а (WR-03/unguarded persist,
WR-01/rowid-instability) первичной верификации закрыты кодом планов 40.3-06..40.3-09 и
независимо перепроверены в этой сессии прямым чтением кода и повторным запуском
тестов/гейтов — не только цитированием SUMMARY.md.

Раунд-2 код-ревью (`40.3-REVIEW.md`) нашёл 0 BLOCKER'ов в новом коде и 2 non-blocking WARNING
(WR-13 — `--selftest` не подключён к `pnpm lint`/CI; WR-14 — детектор digit-prefix regex —
широкий текстовый матч) плюс 2 INFO. Оба WARNING относятся к будущей устойчивости самого гейта
(не к текущей корректности закрытых дефектов) и по явному указанию несут статус carried-forward
non-blocking, а не gap этой фазы.

Явно отложенный (пользователем, задокументированный) долг вне объёма фазы 40.3 — не
переоткрывается здесь: WR-05, WR-10 (план 06); WR-06, WR-09, WR-11, WR-12, IN-01, IN-02,
IN-04, IN-05, IN-06 (план 07); WR-02, WR-04 (план 09); IP/SNMP edit-mode (отдельная будущая
фаза, todo зафиксирован на диске).

Цель фазы достигнута полностью: пользователь может стереть необязательные поля устройства
(включая «Место») и email пользователя; тост и предпросмотр возврата показывают номер акта без
искажения, структурно защищено self-test'ируемым гейтом; «Завести принтер» атомарно создаёт
устройство+принтер с IP при любом значении «Количество», через шаблоны номеров и с
инвалидацией дерева мест; FK-проверка после миграций безусловна на каждом старте и устойчива к
перестройке таблиц, которая уже является нормой проекта (V042-V044) и предстоит в Фазах 41/43.

---

_Verified: 2026-09-23T15:48:22Z_
_Verifier: Claude (gsd-verifier)_
