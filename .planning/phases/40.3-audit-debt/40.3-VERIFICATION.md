---
phase: 40.3-audit-debt
verified: 2026-09-22T23:20:23Z
status: gaps_found
score: 8/13 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Домен, DTO и оба SQL write-пути используют один и тот же двойной-Option паттерн, что и inventory_no/place_id (план 40.3-01, must_have truth)"
    status: failed
    reason: "place_id — единственное поле с идентичной семантикой Option<Option<i64>> — НЕ получило #[serde(default, with = \"serde_with::rust::double_option\")] в crates/trackly-app/src/dto/device.rs:212-213. На JSON-границе {\"place_id\": null} схлопывается во внешний None, если let в impl From не срабатывает, значение не долетает до SQL, «Место» не очищается. Это прямо противоречит общей формулировке цели фазы («пользователь может стереть необязательные поля устройства») — place_id такое же необязательное поле, как и 5 починенных. Подтверждено кодом (CR-01 в 40.3-REVIEW.md) и изолированным serde-репро."
    artifacts:
      - path: "crates/trackly-app/src/dto/device.rs"
        issue: "поле place_id (строка ~213) без serde-атрибута double_option, в отличие от 5 соседних полей"
    missing:
      - "Добавить #[serde(default, with = \"serde_with::rust::double_option\")] над place_id"
      - "Регресс-тест на place_id по образцу device_patch_serde_double_option_distinguishes_absent_vs_null, через serde_json::from_str, не литералом"
      - "Проверить тот же паттерн в crates/trackly-app/src/dto/auth.rs:78 (email: Option<Option<String>> без атрибута) — тот же класс бага"
  - truth: "«Завести принтер» атомарно создаёт устройство и printers-строку с введёнными IP/community в одной транзакции (ROADMAP SC3, N-2)"
    status: failed
    reason: "IP/SNMP-блок в DeviceFormBody.svelte рендерится независимо от количества (строка 907: {#if typeId === PRINTER_TYPE_ID && !isEdit}), а quantityDisabled (строка 282) не учитывает заполненный IP. При Количество > 1 (типовой сценарий для принтера без введённых номеров) handleSubmit уходит в devices.bulkCreate(), которая printer-блок не принимает вовсе (device_service.rs::bulk_create вызывает sync_printer_row_in_tx(..., None)). Пользователь вводит IP/community, жмёт «Создать», получает тост «Создано N устройств», а в БД у всех N принтеров ip_address=NULL, community='public' — без единой ошибки/предупреждения. Подтверждено кодом (CR-02 в 40.3-REVIEW.md)."
    artifacts:
      - path: "ui/src/features/devices/DeviceFormBody.svelte"
        issue: "quantityDisabled (строка 282) не блокирует qty>1 при заполненном IP для типа «Принтер»; IP/SNMP-блок виден независимо от qty"
      - path: "crates/trackly-app/src/services/device_service.rs"
        issue: "bulk_create не имеет defense-in-depth валидации против непустого printer-блока (в отличие от create_single_with_number_check_with_printer, T-40.3-09)"
    missing:
      - "Заблокировать qty>1 при typeId===PRINTER_TYPE_ID && ipAddress непуст, ЛИБО скрывать IP-блок при qty>1"
      - "Server-side defense-in-depth: bulk_create должен явно отклонять непустой printer-блок, а не молча игнорировать (если он вообще туда попадёт)"
  - truth: "В ui/src не остаётся кода, который считает номер акта начинающимся с цифр — проверено структурным гейтом (план 40.3-03, must_have truth, D-05)"
    status: failed
    reason: "Гейт check-act-number-no-regex-split.mjs ловит ровно один буквальный паттерн /\\.replace\\(\\s*\\/\\^\\\\d/ и пропускает (PASS) четыре очевидные вариации того же дефекта: группу /^(\\d+)/, класс /^[0-9]+/, вынесенную regex-константу, и — что показательно — сам исходный дефект N-1 в форме шаблонной конкатенации `${number_raw}в${sub}` без .replace вовсе. В ReturnModal.svelte:222-223,372 предпросмотр возврата по-прежнему вычисляет `parentNumber = act?.number_raw ?? 0` и `predictedSubNumber = return_ids.length + 1`, показывая «№42в1» для первого возврата, тогда как сервер (format_act_number, dto/act.rs:36-41) вернёт «42в» — тот же класс искажения, что и исправленный тост, в том же файле, не пойманный новым гейтом. Подтверждено кодом (WR-07, WR-08 в 40.3-REVIEW.md)."
    artifacts:
      - path: "ui/scripts/check-act-number-no-regex-split.mjs"
        issue: "паттерн детекции слишком узкий (одна буквальная последовательность байт), самотест на позитивных фикстурах отсутствует"
      - path: "ui/src/features/acts/ReturnModal.svelte"
        issue: "строки 222-223 (parentNumber/predictedSubNumber) и 372 (предпросмотр) реконструируют номер на клиенте тем же способом, что и исправленный баг тоста"
    missing:
      - "Расширить гейт до семантического инварианта («number_raw не участвует в отображаемой строке» + whitelist), с самотестом на позитивных фикстурах"
      - "Убрать клиентское предсказание номера в предпросмотре возврата (ReturnModal.svelte:222-223,372) — либо не показывать номер до сохранения, либо получать peek с сервера"
human_verification:
  - test: "Живая проверка тоста возврата с шаблонным номером акта (40.3-03, task 3)"
    expected: "После оформления возврата по акту с нецифровым шаблонным номером (например «2026/09-1») тост показывает «№2026/09-1в» без дублирования"
    why_human: "Чекпоинт закрыт авто-approve (AUTO_MODE) координатором, БЕЗ фактического наблюдения в cargo tauri dev — сам SUMMARY.md фиксирует статус UNVERIFIED (deferred to human UAT). Компиляционные гейты (svelte-check, pnpm lint) не ловят Svelte-рантайм-поведение (project memory compile_gates_miss_svelte_runtime)."
  - test: "Живая проверка «Завести принтер» через DeviceFormModal (40.3-05, task 3)"
    expected: "Поле номера с автоподстановкой/шаблонами, попап «Номер занят» при конфликте, блок IP/SNMP только в create-режиме для принтера, счётчик дерева мест обновляется без перезагрузки, IP сохраняется, блок IP/SNMP отсутствует в edit-режиме"
    why_human: "Чекпоинт закрыт авто-approve (AUTO_MODE), БЕЗ живого наблюдения — SUMMARY.md фиксирует статус UNVERIFIED (deferred to human UAT). Ни агент, ни человек не открывали cargo tauri dev в рамках этого плана."
---

# Phase 40.3: Долг аудита v1.4 Verification Report

**Phase Goal:** Закрыть четыре находки повторного аудита вехи (2026-09-22): пользователь может
стереть необязательные поля устройства, тост возврата показывает номер акта по шаблону без
искажения, «Завести принтер» на экране «Принтеры» идёт через шаблоны номеров и инвалидацию
дерева мест, а проверка целостности FK после миграций не пропускается на повторном старте.
**Verified:** 2026-09-22T23:20:23Z
**Status:** gaps_found
**Re-verification:** Нет — первичная верификация

## Goal Achievement

Верификация опирается на код-ревью `.planning/phases/40.3-audit-debt/40.3-REVIEW.md`
(закоммичен, независимо перепроверен оркестратором чтением кода) как на установленный вход —
находки CR-01/CR-02/WR-07/WR-08 не переоткрывались с нуля, а сверялись против must_haves
плана и роадмапа. Остальные должные-истины проверены самостоятельно чтением кода.

### Observable Truths

| # | Truth | Source | Status | Evidence |
|---|-------|--------|--------|----------|
| 1 | SC1 (WR-03): очистка serial_no/model/specs/kit/state в форме правки реально сохраняет пустое значение (оба транспорта); незатронутое поле не меняется | ROADMAP SC1 | ✓ VERIFIED | `domain::DevicePatch`/`dto::DevicePatch` — все 5 полей `Option<Option<String>>` + serde double_option; `devices_sqlite.rs` — CASE WHEN в обеих write-функциях; 2 новых теста через реальную serde-границу, мутационно провалидированы (40.3-01-SUMMARY.md) |
| 2 | Домен/DTO/SQL используют ТОТ ЖЕ паттерн, что inventory_no/place_id (план 40.3-01 must_have) | PLAN 40.3-01 | ✗ FAILED | `place_id` в `dto/device.rs:212-213` без serde-атрибута — очистка «Места» по-прежнему молча не срабатывает (CR-01, независимо подтверждено grep'ом) |
| 3 | Регресс-тест различает «не передано» и «явный null» через реальную serde-границу | PLAN 40.3-01 | ✓ VERIFIED | `devices_crud.rs::device_patch_serde_double_option_distinguishes_absent_vs_null` идёт через `serde_json::from_str`, мутационно проверен (SUMMARY фиксирует ручной откат → красный) |
| 4 | SC2 (N-1): тост возврата показывает `ActDto.number` как есть, без склейки number_raw+regex | ROADMAP SC2 | ✓ VERIFIED | `ReturnModal.svelte`: `pushToast('success', \`Создан акт возврата №${ret.number}. ...\`)`, мёртвая `suffix`-переменная удалена (commit f1b9072a) |
| 5 | В ui/src не остаётся кода, считающего номер акта начинающимся с цифр — закрыто структурным гейтом (план 40.3-03 must_have) | PLAN 40.3-03 | ✗ FAILED | Гейт ловит один буквальный паттерн, пропускает 4 вариации (WR-07); `ReturnModal.svelte:222-223,372` по-прежнему реконструирует номер на клиенте и ошибается на первом возврате («№42в1» вместо «42в») (WR-08) |
| 6 | SC3 (N-2): «Завести принтер» даёт шаблоны номеров/попапы/автоподстановку как на «Устройствах»; счётчики дерева мест обновляются; точка входа в реестре INV-7 | ROADMAP SC3 | ✓ VERIFIED (для базового пути) | `PrintersPage.svelte` → `DeviceFormModal(initialTypeId=2)`; `DeviceFormModal` уже в `MUTATING_COMPONENTS` (`check-place-tree-invalidation.mjs:709`); `onSaved` вызывает `notifyPlaceContentChanged`; `PrinterCreateModal.svelte` удалён |
| 7 | printer-блок (IP/SNMP) создаётся в ТОЙ ЖЕ транзакции, что и устройство — единый атомарный запрос (ROADMAP SC3 formulation, план 40.3-04/05 must_have) | ROADMAP SC3 / PLAN | ✗ FAILED | При Количество>1 IP/SNMP-блок остаётся видимым и заполняемым, но `handleSubmit` уходит в `devices.bulkCreate()`, который printer-блок не принимает — введённые данные молча теряются (CR-02, независимо подтверждено grep'ом `quantityDisabled`) |
| 8 | printer-блок отклоняется явной ошибкой валидации при type_id ≠ Принтер, а не тихо игнорируется | PLAN 40.3-04 | ✓ VERIFIED | `create_single_with_number_check_with_printer` возвращает `AppError::Validation{field:"printer"}` сразу после `validate_new`; покрыто тестом 3 в `devices_printer_create_ip.rs` |
| 9 | Существующие вызывающие `create_single_with_number_check` (Rust-тесты) не требуют изменений | PLAN 40.3-04 | ✓ VERIFIED | Старая 3-арг сигнатура — тонкая обёртка (`..., None)`); `devices_numbering.rs`/`role_endpoint_matrix.rs` компилируются и проходят без правок (SUMMARY + REVIEW подтверждают) |
| 10 | devices.create() TS-обёртка удалена вместе со своим единственным вызывающим (D-04) | PLAN 40.3-05 | ✓ VERIFIED | `grep -rn "devices.create(" ui/src` → 0 строк; `PrinterCreateModal.svelte` удалён (git diff подтверждает -204 строки) |
| 11 | SC4 (N-4): новое FK-нарушение после миграций блокирует старт на КАЖДОМ следующем запуске, не только в первый раз; предсуществующие нарушения по-прежнему не блокируют | ROADMAP SC4 | ✓ VERIFIED | `run_to` в `migrations.rs:272-365` читает бейзлайн БЕЗУСЛОВНО (`read_persisted_fk_baseline` вне `if pending`), считает `after` и сравнивает БЕЗУСЛОВНО в обеих ветках, персистит только когда чисто (self-heal). Тесты `run_blocks_every_subsequent_start_after_new_fk_violation_not_only_first` и `run_self_heals_baseline_after_violation_is_fixed` присутствуют в файле и, по SUMMARY, мутационно провалидированы (ручной откат → красный) |
| 12 | Персистентный бейзлайн — guarded single-key read/upsert (план 40.3-02 must_have) | PLAN 40.3-02 | ⚠️ UNCERTAIN | Чтение (`read_persisted_fk_baseline`) действительно guarded (`.ok()` на любую ошибку). Запись (`persist_fk_baseline(conn, &after)?` на строке 349) НЕ guarded — пробрасывает ошибку наружу и валит `AppCtx::build` при `run_up_to<16` (до создания `app_settings`) или на read-only носителе (WR-03 в REVIEW). Публичная `run_up_to` приглашает к такому сценарию, хотя сегодня все реальные вызовы — на полную версию. Не переоткрывалось с нуля — доверяю разбору REVIEW, помечаю UNCERTAIN/WARNING, а не BLOCKER, т.к. основной сценарий (полный `run`) не задет |
| 13 | Идентификация FK-нарушений устойчива к перестройке таблиц (rowid-based) | Derived (forward risk F2 из ROADMAP-источника аудита) | ⚠️ UNCERTAIN | `FkViolation = (table, rowid, parent, fkid)` — `rowid` переназначается при `CREATE TABLE new → INSERT SELECT → DROP → RENAME`, что в проекте уже норма (V042-V044). REVIEW (WR-01) показывает как ложные срабатывания после апгрейда, так и маскировку новых нарушений при переиспользовании rowid. Не переоткрывалось с нуля, доверяю разбору REVIEW — это forward-риск именно для Фазы 41/43 (перестройка `places`), которую этот план прямо называет своей причиной существования |

**Score:** 8/13 истин подтверждены полностью (2 — UNCERTAIN/warning-уровня, 3 — FAILED/BLOCKER)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/trackly-core/src/domain/devices.rs` | double-Option на serial_no/model/specs/kit/state | ✓ VERIFIED | 5 полей `Option<Option<String>>` + doc-комментарии |
| `crates/trackly-app/src/dto/device.rs` | serde double_option на 5 полях + корректная конверсия | ✓ VERIFIED (для 5 полей) / ✗ STUB на place_id | 5 новых атрибутов присутствуют; `place_id` — без атрибута (CR-01) |
| `crates/trackly-infra/src/repos/devices_sqlite.rs` | CASE WHEN в обеих write-функциях | ✓ VERIFIED | Подтверждено SUMMARY grep-метриками и независимым чтением кода run_to рядом |
| `crates/trackly-app/tests/devices_crud.rs` | JSON-boundary regression-тест | ✓ VERIFIED | 2 новых теста, мутационно проверены |
| `crates/trackly-infra/src/db/migrations.rs` | безусловная персистентная FK-проверка | ✓ VERIFIED (осн. путь) / ⚠️ ORPHANED-риск на edge-путях | `run_to` переписан как описано; `persist_fk_baseline` не guarded (WR-03), rowid нестабилен при rebuild (WR-01) |
| `ui/src/features/acts/ReturnModal.svelte` | тост использует `ret.number` напрямую | ✓ VERIFIED (тост) / ✗ residual bug (предпросмотр) | Тост исправлен; строки 222-223/372 (предпросмотр) — не исправлены (WR-08) |
| `ui/scripts/check-act-number-no-regex-split.mjs` | структурный гейт против regex-склейки, подключён в pnpm lint | ✓ EXISTS, подключён / ✗ недостаточен | Подключён в `lint`; ловит 1 из 5 реалистичных вариаций дефекта (WR-07) |
| `crates/trackly-app/src/dto/printer.rs` | `PrinterCreateBlockDto` | ✓ VERIFIED | camelCase DTO присутствует |
| `crates/trackly-app/src/services/device_service.rs` | `create_single_with_number_check_with_printer` + printer override в той же транзакции | ✓ VERIFIED | Метод существует, обёртка тонкая, тесты `devices_printer_create_ip.rs` (3 сценария) |
| `crates/trackly-app/tests/devices_printer_create_ip.rs` | regression на printer-блок | ✓ VERIFIED (частично) | 3 теста зелёные, но не доказывают атомарность отказа (IN-01, info-уровень, не блокер) |
| `ui/src/features/devices/DeviceFormBody.svelte` | IP/community поля, printerBlock прокидывается | ✓ WIRED, но ⚠️ HOLLOW при qty>1 | Поля рендерятся и передаются в `createSingleWithNumberCheck` только при одиночном создании; при `qty>1` printerBlock не достигает бэкенда вовсе (CR-02) |
| `ui/src/features/devices/DeviceFormModal.svelte` | `initialTypeId` prop | ✓ VERIFIED | Проп добавлен, используется в `$effect` открытия |
| `ui/src/features/printers/PrintersPage.svelte` | DeviceFormModal вместо PrinterCreateModal, notifyPlaceContentChanged | ✓ VERIFIED | Импорт/рендер/onSaved подтверждены чтением файла |
| `ui/src/lib/api/devices.ts` | createSingleWithNumberCheck(printer) + create() удалён | ✓ VERIFIED | `grep` подтверждает 0 вхождений `devices.create(` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `DeviceFormBody.svelte` printer-блок | `printerBlock` (ipAddress/community) | `submitSingleCreate()` → `devices.createSingleWithNumberCheck(..., printerBlock)` | Да, при qty=1 | ✓ FLOWING (qty=1) / ✗ DISCONNECTED (qty>1, уходит в `bulkCreate` без printer-параметра) |
| `PrintersPage.svelte` → `notifyPlaceContentChanged` | `result?.placeId` из `DeviceSaveOutcome` | `onSaved` callback `DeviceFormModal` | Да | ✓ FLOWING |
| `migrations.rs::run_to` FK-бейзлайн | `after` (`fk_violations(conn)`) | `PRAGMA foreign_key_check` — реальный запрос к БД | Да | ✓ FLOWING (см. оговорки в Truth 12/13 про guarded-запись и rowid-стабильность) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `dto/device.rs::DevicePatch` | `domain::DevicePatch` | `impl From` — `p.field = Some(inner)` | ✓ WIRED (5 полей) / ✗ NOT_WIRED (place_id, serde схлопывает раньше конверсии) | Конверсия корректна для всех 6 double-Option полей, но JSON-граница не долетает до неё для `place_id` |
| `tauri_cmds/devices.rs` / `http/devices.rs` | `device_service.rs::create_single_with_number_check_with_printer` | printer параметр прокидывается насквозь | ✓ WIRED | Оба транспорта передают `printer: Option<PrinterCreateBlockDto>` в сервис |
| `DeviceFormBody.svelte` submitSingleCreate | `devices.ts::createSingleWithNumberCheck` | 4-й аргумент `printerBlock` | ✓ WIRED (qty=1) / ✗ NOT_WIRED (qty>1, другая ветка кода) | Раздвоение пути на `createSingleWithNumberCheck` vs `bulkCreate` — только первая несёт printer-блок |
| `PrintersPage.svelte::onSaved` | `placeContentEvents.svelte::notifyPlaceContentChanged` | вызов с `newPlaceId` | ✓ WIRED | Подтверждено чтением файла и реестром INV-7 |
| `ReturnModal.svelte` тост | `ActDto.number` | `ret.number` напрямую | ✓ WIRED | Строка тоста подтверждена |
| `ReturnModal.svelte` предпросмотр | `ActDto.number` (peek) | — | ✗ NOT_WIRED | Предпросмотр НЕ использует серверное значение, реконструирует на клиенте (WR-08) |

### Requirements Coverage

Фаза не вводит новых REQ-ID («без новых REQ-ID» в ROADMAP). Все ID, объявленные в frontmatter
планов, уже отмечены Complete в `REQUIREMENTS.md` за более ранними фазами — задача 40.3 состояла
в починке дефектов на стыках этих требований, а не в первичном закрытии.

| Requirement | Source Plan | Ранее закрыто в | Статус в этой фазе | Evidence |
|-------------|------------|------------------|---------------------|----------|
| WR-03 (не формальный REQ-ID, находка аудита) | 40.3-01 | — | Частично: SUMMARY фиксирует WR-03 как ID находки, не как REQ-ID из трейсинг-таблицы; `requirements.mark-complete` вернул `not_found` — ожидаемо | 5/6 семантически связанных полей исправлены, place_id — нет (Truth 2) |
| N-4 (аудит-находка) | 40.3-02 | — | ✓ SATISFIED | Truth 11 |
| NUM-14, HST-03 | 40.3-03 | Phase 40.2 / Phase 40 | Дефект на стыке частично устранён | Truth 4 VERIFIED (тост), Truth 5 FAILED (гейт/предпросмотр) |
| NUM-06, NUM-08 | 40.3-04, 40.3-05 | Phase 40.2 | Дефект на стыке частично устранён | Truth 6/8/9 VERIFIED, Truth 7 FAILED (bulk-путь) |
| PLC-06 | 40.3-05 | Phase 39 | ✓ SATISFIED | notifyPlaceContentChanged подтверждён (см. выше) |

Орфанных REQ-ID (заявленных в `REQUIREMENTS.md` для фазы 40.3, но не покрытых ни одним планом)
не найдено — фаза сознательно не вводит новых ID.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `crates/trackly-app/src/dto/device.rs` | 212-213 | double-Option без serde-атрибута (place_id) | 🛑 BLOCKER | Очистка «Места» молча не работает — прямое противоречие цели фазы |
| `ui/src/features/devices/DeviceFormBody.svelte` | 282, 907 | Условие видимости IP-блока не согласовано с условием разрешённости `qty>1` | 🛑 BLOCKER | Данные пользователя (IP/community) теряются молча при массовом создании принтера |
| `ui/scripts/check-act-number-no-regex-split.mjs` | 60 | Слишком узкий детектирующий паттерн, без самотеста на позитивных фикстурах | 🛑 BLOCKER (нарушает собственный must_have плана) | Класс бага (N-1) не закрыт структурно, несмотря на заявленную цель гейта |
| `ui/src/features/acts/ReturnModal.svelte` | 222-223, 372 | Клиентская реконструкция номера акта (`number_raw` + суффикс), тот же класс, что и исправленный тост | 🛑 BLOCKER (тот же дефект, не покрытый гейтом) | Предпросмотр возврата показывает неверный номер на первом возврате |
| `crates/trackly-infra/src/db/migrations.rs` | 349 | `persist_fk_baseline(...)?` не guarded (в отличие от чтения) | ⚠️ WARNING | Потенциальный крах старта на read-only БД / `run_up_to<16` |
| `crates/trackly-infra/src/db/migrations.rs` | 48, 328 | Бейзлайн ключуется по нестабильному `rowid` | ⚠️ WARNING | Ложные блокировки/маскировка после перестройки таблиц (актуально для Фаз 41/43) |
| `crates/trackly-app/src/services/device_service.rs` | ~337-341, ~411-416 | Доккомментарии ссылаются на удалённый в этой же фазе `PrinterCreateModal.svelte` | ℹ️ INFO | Не блокирует функциональность, но вводит в заблуждение |

Debt-маркеры (`TBD`/`FIXME`/`XXX`) в изменённых файлах не найдены (grep по списку файлов review
и diff чист).

### Human Verification Required

Оба чекпоинта фазы закрыты авто-approve координатором (`AUTO_MODE=true`), НЕ живым наблюдением —
это явно зафиксировано в обоих SUMMARY.md как **UNVERIFIED (deferred to human UAT)**. Ниже —
перенесённые пункты, без изменений в их процедуре.

#### 1. Тост возврата с шаблонным номером акта (40.3-03)

**Test:** Активировать нецифровой шаблон номеров актов, оформить акт передачи, затем возврат по
нему; прочитать текст тоста.
**Expected:** «№2026/09-1в» (без дублирования вида «№2026/09-1/09-1в»).
**Why human:** Чекпоинт закрыт без запуска `cargo tauri dev`; компиляционные гейты не ловят
Svelte-рантайм.

#### 2. «Завести принтер» через DeviceFormModal (40.3-05)

**Test:** Открыть «Принтеры» → «Завести принтер», проверить автоподстановку номера, попап «Номер
занят», видимость IP/SNMP-блока только в create-режиме, обновление счётчика дерева мест без
перезагрузки, сохранение IP, отсутствие IP-блока в edit-режиме.
**Expected:** Все перечисленные поведения работают как в «Устройствах».
**Why human:** Чекпоинт закрыт без запуска `cargo tauri dev`; ни агент, ни человек не наблюдали
поведение в реально работающем приложении в рамках этого плана.

**Дополнительно, вне формального чекпоинта:** учитывая находку CR-02 (потеря IP при
Количество > 1), при живой проверке рекомендуется отдельно воспроизвести именно этот сценарий
(ввести IP, увеличить количество, сохранить, проверить БД) — он не входит в шаги чекпоинта
40.3-05, но напрямую относится к цели фазы.

### Gaps Summary

Фаза механически аккуратна (SQL-параметризация, атомарность транзакции устройство+принтер,
удаление обходного модала, безусловность FK-проверки — всё подтверждено чтением кода), но не
достигает заявленной цели в трёх независимо воспроизводимых местах:

1. **Очистка «Места» не работает** — то же поле, что фаза 40-28 объявляла закрытым, снова (точнее,
   всё ещё) не очищается через JSON-границу. Общая формулировка цели фазы («пользователь может
   стереть необязательные поля устройства») не выполнена полностью, хотя буквальный список из
   SC1 (serial_no/model/specs/kit/state) починен корректно.
2. **Атомарность N-2 обходится количеством > 1** — ключевая гарантия «принтер заводится вместе с
   IP в одной транзакции» не держится в самом типовом сценарии для принтера без предзаданных
   номеров (Количество остаётся редактируемым).
3. **Класс бага N-1 не закрыт структурно** — тост починен точечно, но постоянный гейт, который
   должен был предотвратить рецидив (в том числе для будущих актов АРМ, Фаза 41), пропускает
   4 из 5 реалистичных вариаций, и рецидив уже присутствует в том же файле строкой выше (не в
   тосте, а в предпросмотре).

Ни один из трёх пунктов не является гипотетическим — все три подтверждены прямым чтением кода
(grep/файл), не только цитированием REVIEW. N-4 (FK-целостность) реализован корректно по своей
буквальной формулировке ROADMAP SC4; отмеченные в REVIEW риски (нестабильность rowid при
перестройке таблиц, негвардированная запись бейзлайна) — реальные, но не опровергают сам факт
достижения SC4 как написано, и вынесены как WARNING, а не BLOCKER.

Оба UI-плана (40.3-03, 40.3-05) также несут неподтверждённые живые UAT — координатор закрыл их
авто-approve без наблюдения в `cargo tauri dev`, что сами SUMMARY.md фиксируют явно.

---

_Verified: 2026-09-22T23:20:23Z_
_Verifier: Claude (gsd-verifier)_
