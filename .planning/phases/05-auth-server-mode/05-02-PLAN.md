---
phase: 05-auth-server-mode
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/trackly-app/src/services/auth.rs
  - crates/trackly-app/src/services/mod.rs
  - crates/trackly-app/src/server/mod.rs
  - crates/trackly-app/src/server/tls.rs
  - crates/trackly-app/src/server/rusqlite_session_store.rs
  - crates/trackly-app/src/context.rs
  - crates/trackly-app/src/lib.rs
  - crates/trackly-app/tests/auth_smoke.rs
  - crates/trackly-app/tests/users_crud.rs
  - crates/trackly-app/tests/session_survives_restart.rs
  - crates/trackly-app/tests/tls_server_smoke.rs
  - crates/trackly-app/tests/server_hot_toggle.rs
  - crates/trackly-app/tests/graceful_shutdown_drain.rs
autonomous: true
requirements:
  - USR-01
  - USR-03
  - USR-04
  - USR-05
  - USR-07
  - SRV-01
  - SRV-03
  - SRV-04
  - SRV-05

must_haves:
  truths:
    - "AuthService::needs_bootstrap() returns true on empty users table, false after admin created"
    - "AuthService::create_user() stores argon2id hash (never plaintext), login() verifies correctly"
    - "RusqliteSessionStore create/save/load/delete work against sessions table V010; session survives store recreation from same DB"
    - "generate_self_signed() produces TlsBundle with non-empty fingerprint_hex and valid TlsAcceptor"
    - "start_server() binds TcpListener, serves HTTPS connections, exits on CancellationToken cancel within 30s"
    - "AppCtx has auth: Arc<AuthService> and server_ctl: Arc<Mutex<Option<ServerHandle>>> fields"
    - "D-Desktop-01 audit attribution: desktop_identity() returns Identity with user_id=Some(id) when exactly one admin, None otherwise"
  artifacts:
    - path: "crates/trackly-app/src/services/auth.rs"
      provides: "AuthService: needs_bootstrap, login, create_user, update_user, delete_user, list_users, get_user_by_id, desktop_identity, hash_password, verify_password"
      exports: ["AuthService"]
    - path: "crates/trackly-app/src/server/rusqlite_session_store.rs"
      provides: "RusqliteSessionStore: impl SessionStore (create, save, load, delete)"
      exports: ["RusqliteSessionStore"]
    - path: "crates/trackly-app/src/server/tls.rs"
      provides: "TlsBundle + generate_self_signed() + load_from_pem()"
      exports: ["TlsBundle", "generate_self_signed", "load_from_pem"]
    - path: "crates/trackly-app/src/server/mod.rs"
      provides: "start_server() + ServerHandle struct"
      exports: ["start_server", "ServerHandle"]
    - path: "crates/trackly-app/src/context.rs"
      provides: "AppCtx with auth + server_ctl fields"
      contains: "server_ctl"
  key_links:
    - from: "crates/trackly-app/src/services/auth.rs"
      to: "crates/trackly-infra/src/db/writer_worker.rs"
      via: "argon2 hash in spawn_blocking; writes through writer.execute()"
      pattern: "spawn_blocking.*hash_password"
    - from: "crates/trackly-app/src/server/rusqlite_session_store.rs"
      to: "migrations/V010__sessions.sql"
      via: "sessions table id BLOB + data BLOB + expiry_date INTEGER"
      pattern: "sessions.*id.*data.*expiry_date"
    - from: "crates/trackly-app/src/server/tls.rs"
      to: "crates/trackly-app/src/server/mod.rs"
      via: "TlsAcceptor from TlsBundle used in accept loop"
      pattern: "TlsAcceptor.*accept"
---

## Phase Goal

**As a** system administrator, **I want to** have the auth backend (user CRUD, session store, TLS server) fully implemented, **so that** the HTTP routes and UI can plug in to a working auth infrastructure in Plans 03–05.

<objective>
Implement the auth service backend, rusqlite session store, TLS utilities, server lifecycle, and wire them into AppCtx.

Purpose: This plan makes the auth backbone live — Plans 03 (HTTP routes) and 04 (RBAC enforcement) can only be built once AuthService + RusqliteSessionStore + start_server() exist.
Output: AuthService, RusqliteSessionStore, TlsBundle/start_server, AppCtx wired, 6 integration tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-auth-server-mode/05-CONTEXT.md
@.planning/phases/05-auth-server-mode/05-RESEARCH.md
@.planning/phases/05-auth-server-mode/05-PATTERNS.md
@.planning/phases/05-auth-server-mode/05-01-SUMMARY.md

Key existing files (read before touching):
- crates/trackly-app/src/context.rs — AppCtx struct and build() to extend
- crates/trackly-app/src/services/device_service.rs — exact service pattern (writer.execute, spawn_blocking readers, clock)
- crates/trackly-app/src/services/mod.rs — module export pattern
- crates/trackly-app/src/lib.rs — crate pub module declarations
- crates/trackly-app/src/shutdown.rs — CancellationToken lifecycle pattern for server/mod.rs
- crates/trackly-infra/src/db/writer_worker.rs — WriterHandle::execute() signature
- crates/trackly-infra/src/db/pools.rs — ReaderPool::acquire() RAII pattern
- migrations/V010__sessions.sql — sessions table schema (id BLOB, data BLOB, expiry_date INTEGER)
- crates/trackly-app/tests/devices_crud.rs — make_service() + timeout(30s) test pattern
</context>

<interfaces>
From crates/trackly-core/src/auth.rs (Plan 01 output):
```rust
pub struct Identity { pub user_id: Option<i64>, pub role: Role }
impl Identity { pub fn trusted_admin() -> Self }
pub enum Role { Admin, Manager, Employee }
impl Role { pub fn from_str(s: &str) -> Result<Self, AppError>; pub fn as_str(&self) -> &'static str }
pub enum Action { ManageUsers, ManageSettings, MutateDevices, MutateActs, MutateCartridges, ReadData, CreateRequest }
pub fn authorize(identity: &Identity, action: &Action) -> Result<(), AppError>
```

From crates/trackly-app/src/dto/auth.rs (Plan 01 output):
```rust
pub struct LoginRequest { pub login: String, pub password: String }
pub struct UserDto { pub id: i64, pub version: i64, pub login: String, pub full_name: String,
                     pub role: String, pub email: Option<String>, pub is_active: bool,
                     pub created_at_utc: i64, pub updated_at_utc: i64 }
pub struct UserNew { pub login: String, pub full_name: String, pub password: String,
                     pub role: String, pub email: Option<String> }
pub struct UserPatch { pub full_name: Option<String>, pub role: Option<String>,
                       pub email: Option<Option<String>>, pub is_active: Option<bool> }
pub struct ChangePasswordRequest { pub old_password: String, pub new_password: String }
pub struct AuthStatusDto { pub needs_bootstrap: bool, pub user: Option<UserDto> }
pub struct UserFilter { pub search: Option<String> }
pub struct UserListResponse { pub items: Vec<UserDto>, pub total: i64 }
pub struct NetworkSettingsDto { pub enabled: bool, pub host: String, pub port: i64,
                                 pub cert_path: String, pub server_url: Option<String>,
                                 pub fingerprint: Option<String> }
pub struct ServerStatusDto { pub running: bool, pub url: Option<String>, pub fingerprint: Option<String> }
```

From crates/trackly-app/src/context.rs (existing):
```rust
pub struct AppCtx {
    pub writer: Arc<WriterHandle>,
    pub readers: Arc<ReaderPool>,
    pub clock: Arc<dyn Clock + Send + Sync>,
    pub shutdown: CancellationToken,
    pub cartridges: Arc<CartridgeService>,
    // ... other fields
}
```

From tower_sessions crate (RESEARCH Pattern 2):
```rust
// SessionStore trait methods: create(&mut Record), save(&Record), load(&Id) -> Option<Record>, delete(&Id)
// Record serialized via rmp_serde::to_vec() / from_slice()
// Id is i128 — serialize as id.0.to_le_bytes().to_vec() for BLOB storage (Pitfall 3)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthService — argon2id hash/verify, user CRUD, needs_bootstrap, desktop_identity</name>
  <files>
    crates/trackly-app/src/services/auth.rs,
    crates/trackly-app/src/services/mod.rs,
    crates/trackly-app/tests/auth_smoke.rs,
    crates/trackly-app/tests/users_crud.rs
  </files>
  <read_first>
    - crates/trackly-app/src/services/device_service.rs — full pattern: struct fields (writer, readers, clock), spawn_blocking reads, writer.execute writes, map_rusqlite, audit_log INSERT pattern
    - crates/trackly-app/src/services/mod.rs — current exports to extend
    - crates/trackly-infra/src/error_conversions.rs — map_rusqlite function signature
    - migrations/V002__core_entities.sql — users table columns: login, full_name, password_hash, role, ad_user, email, notes, created_at_utc, updated_at_utc, deleted_at_utc, version
    - migrations/V008__audit_log.sql — audit_log columns: entity, entity_id, op, user_id, payload_json, created_at_utc
    - crates/trackly-app/tests/devices_crud.rs — test setup make_service() + 30s timeout pattern
    - crates/trackly-core/src/primitives/secret.rs — Secret<T>::new(), expose() usage
  </read_first>
  <behavior>
    - needs_bootstrap(): empty users table → true; after create_user with role='admin' → false
    - login("alice", Secret::new("correct_pw")) → Ok(UserDto); login("alice", Secret::new("wrong")) → Err(AppError::Unauthorized)
    - create_user(UserNew { role: "admin", ... }, &Identity::trusted_admin()) → Ok(UserDto); password_hash stored in DB (not plaintext); UserDto.id > 0
    - get_user_by_id(id) → Ok(UserDto) matching created user
    - list_users(UserFilter { search: None }, Pagination { offset:0, limit:50 }) → UserListResponse.items contains created user
    - update_user(id, version, UserPatch { is_active: Some(false), ..Default }, &admin_identity) → Ok(UserDto with is_active=false)
    - delete_user(id, version, &admin_identity) → Ok(()); get_user_by_id(id) → Err(AppError::NotFound)
    - desktop_identity() with exactly 1 admin in DB → Identity { user_id: Some(admin_id), role: Role::Admin } (D-Desktop-01)
    - desktop_identity() with 0 or 2+ admins → Identity { user_id: None, role: Role::Admin }
    - create_user with role="employee" by employee caller → Err(AppError::Forbidden) (authorize() check)
    - create_user with login shorter than 3 chars → Err(AppError::Validation)
    - create_user with password shorter than 8 chars → Err(AppError::Validation) (D-Auth-01)
  </behavior>
  <action>
    Create crates/trackly-app/src/services/auth.rs:

    Struct AuthService { writer: Arc<WriterHandle>, readers: Arc<ReaderPool>, clock: Arc<dyn Clock + Send + Sync> }
    impl AuthService:
    - new(writer, readers, clock) -> Self
    - async needs_bootstrap(&self) -> Result<bool, AppError>: spawn_blocking, query COUNT(*) FROM users WHERE deleted_at_utc IS NULL AND role='admin', return count == 0
    - async get_password_hash(&self, login: &str) -> Result<String, AppError>: spawn_blocking, SELECT password_hash FROM users WHERE login=? AND deleted_at_utc IS NULL AND is_active=1, return AppError::Unauthorized if not found
    - async login(&self, req: LoginRequest) -> Result<UserDto, AppError>: call get_password_hash, then spawn_blocking verify_password (argon2 CPU-bound — NEVER inline in async), return Unauthorized on mismatch
    - async create_user(&self, new: UserNew, caller: &Identity) -> Result<UserDto, AppError>: call authorize(caller, &Action::ManageUsers)?, validate (login >= 3 chars, password >= 8 chars, role in admin/manager/employee), spawn_blocking hash_password using argon2::Argon2::new(Algorithm::Argon2id, Version::V0x13, Params::new(19456, 2, 1, None)?) with SaltString::generate(&mut OsRng) (use password_hash::rand_core::OsRng to avoid rand 0.8/0.9 conflict — Pitfall 2), INSERT via writer.execute with transaction, INSERT INTO audit_log (entity='user', op='create', user_id=caller.user_id, payload_json=login)
    - async get_user_by_id(&self, id: i64) -> Result<UserDto, AppError>: spawn_blocking reader query
    - async get_by_login(&self, login: &str) -> Result<UserDto, AppError>
    - async list_users(&self, filter: UserFilter, pagination: Pagination) -> Result<UserListResponse, AppError>: spawn_blocking, SELECT with optional LIKE search on login/full_name, ORDER BY created_at_utc DESC
    - async update_user(&self, id: i64, version: i64, patch: UserPatch, caller: &Identity) -> Result<UserDto, AppError>: authorize ManageUsers, writer.execute with optimistic version check
    - async delete_user(&self, id: i64, version: i64, caller: &Identity) -> Result<(), AppError>: authorize ManageUsers, soft-delete (SET deleted_at_utc=unixepoch(), version=version+1)
    - async change_password(&self, user_id: i64, req: ChangePasswordRequest) -> Result<(), AppError>: load current hash, verify old_password, validate new_password >= 8 chars, hash new, update via writer
    - async reset_password(&self, user_id: i64, new_password: String, caller: &Identity) -> Result<(), AppError>: authorize ManageUsers, hash new_password, update
    - async desktop_identity(&self) -> Identity: spawn_blocking, SELECT id FROM users WHERE role='admin' AND deleted_at_utc IS NULL AND is_active=1 LIMIT 2; if exactly 1 row → Identity { user_id: Some(id), role: Role::Admin }; otherwise → Identity::trusted_admin() (user_id: None per D-Desktop-01)

    Free functions (module-level):
    - fn hash_password(password: &Secret<String>) -> Result<String, AppError>: argon2id with OWASP params m=19456 KiB, t=2, p=1
    - fn verify_password(password: &Secret<String>, hash: &str) -> bool: Argon2::default().verify_password()

    Add `pub mod auth; pub use auth::AuthService;` to crates/trackly-app/src/services/mod.rs.

    Fill in auth_smoke.rs and users_crud.rs tests (replacing todo!() scaffolds from Plan 01):
    - auth_smoke.rs: make_auth_service() helper using test_writer_and_readers(); test bootstrap_creates_admin() calls needs_bootstrap() → true, create_user, needs_bootstrap() → false; test login_success_and_failure(); test desktop_identity_attribution() (1 admin → Some(id), 0 admins → None, 2 admins → None)
    - users_crud.rs: test users_create_read_update_delete() full lifecycle; test users_password_validation(); test users_role_enforcement()
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test auth_smoke --test users_crud 2>&1 | tail -20</automated>
  </verify>
  <done>
    auth_smoke and users_crud tests GREEN. cargo test -p trackly-app --test auth_smoke -- --nocapture shows no FAILED lines. Argon2 params verified: grep -n "19456" crates/trackly-app/src/services/auth.rs returns non-empty.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RusqliteSessionStore + TLS + server/mod.rs + AppCtx wiring</name>
  <files>
    crates/trackly-app/src/server/mod.rs,
    crates/trackly-app/src/server/tls.rs,
    crates/trackly-app/src/server/rusqlite_session_store.rs,
    crates/trackly-app/src/context.rs,
    crates/trackly-app/src/lib.rs,
    crates/trackly-app/tests/session_survives_restart.rs,
    crates/trackly-app/tests/tls_server_smoke.rs,
    crates/trackly-app/tests/server_hot_toggle.rs,
    crates/trackly-app/tests/graceful_shutdown_drain.rs
  </files>
  <read_first>
    - crates/trackly-app/src/context.rs — full AppCtx struct and build() to add auth + server_ctl fields
    - crates/trackly-app/src/shutdown.rs — CancellationToken install pattern (child_token() anti-pattern note)
    - crates/trackly-app/src/lib.rs — current pub mod declarations; add `pub mod server;`
    - crates/trackly-infra/src/db/writer_worker.rs — WriterHandle type and execute() signature
    - crates/trackly-infra/src/db/pools.rs — ReaderPool::acquire() returning RAII ConnGuard
    - migrations/V010__sessions.sql — sessions table: id BLOB NOT NULL PRIMARY KEY, data BLOB NOT NULL, expiry_date INTEGER NOT NULL
    - crates/trackly-app/tests/health_smoke.rs — existing test that uses build router pattern
  </read_first>
  <behavior>
    Session store (session_survives_restart):
    - RusqliteSessionStore::create() inserts row to sessions table; load() by same Id returns Some(record) with same data
    - After dropping store and creating new RusqliteSessionStore from same DB files, load() still returns record (per D-Session-01 survives restart)
    - delete() removes session; subsequent load() returns None
    - Expired sessions (expiry_date < unixepoch()) not returned by load()

    TLS (tls_server_smoke):
    - generate_self_signed("localhost") returns TlsBundle where fingerprint_hex is 95-char hex string (32 bytes * 3 - 1 colons)
    - TlsAcceptor can accept TLS connections (tested by connecting with tokio-rustls client with danger_accept_invalid_certs)
    - load_from_pem() called with PEM strings of the generated cert+key creates equivalent TlsBundle

    Server lifecycle (server_hot_toggle, graceful_shutdown_drain):
    - start_server() binds random port, accepts TLS connections, exits within 2s of CancellationToken cancel
    - After cancel + task join, TcpStream::connect to that port → ConnectionRefused
  </behavior>
  <action>
    Create crates/trackly-app/src/server/ directory structure with mod.rs, tls.rs, rusqlite_session_store.rs.

    server/tls.rs (from RESEARCH Pattern 3 + PATTERNS.md §server/tls.rs):
    - pub struct TlsBundle { pub acceptor: TlsAcceptor, pub fingerprint_hex: String, pub cert_pem: String, pub key_pem: String }
    - pub fn generate_self_signed(host: &str) -> anyhow::Result<TlsBundle>: use rcgen::generate_simple_self_signed(vec![host.to_string(), "localhost".to_string()]); rcgen 0.14 API returns CertifiedKey { cert, signing_key }; compute SHA-256 fingerprint as XX:XX:... colon-hex from cert.der().to_vec() using sha2::Sha256::digest; build rustls::ServerConfig with ServerConfig::builder().with_no_client_auth().with_single_cert(certs, key)?; wrap in TlsAcceptor::from(Arc::new(config))
    - pub fn load_from_pem(cert_pem: &str, key_pem: &str) -> anyhow::Result<TlsBundle>: use rustls_pemfile to parse certs + private key; compute fingerprint from first cert DER; same ServerConfig builder pattern

    server/rusqlite_session_store.rs (from RESEARCH Pattern 2 + PATTERNS.md §rusqlite_session_store):
    - pub struct RusqliteSessionStore { pub(crate) writer: Arc<WriterHandle>, pub(crate) readers: Arc<ReaderPool> }
    - impl RusqliteSessionStore { pub fn new(writer, readers) -> Self }
    - #[async_trait] impl SessionStore for RusqliteSessionStore with all 4 methods:
      * create(): serialize record via rmp_serde::to_vec(&record), store id as record.id.0.to_le_bytes().to_vec() (BLOB, Pitfall 3), expiry as record.expiry_date.unix_timestamp(); INSERT OR IGNORE via writer.execute; map errors to session_store::Error::Backend
      * save(): same serialization, INSERT OR REPLACE via writer.execute
      * load(): spawn_blocking readers.acquire(), SELECT data FROM sessions WHERE id=? AND expiry_date > unixepoch(); deserialize via rmp_serde::from_slice; map session_store::Error::Decode on deser error
      * delete(): writer.execute DELETE FROM sessions WHERE id=?
    - Add background_cleanup fn (NOT async task — called at server build time as a one-shot DELETE WHERE expiry_date < unixepoch()) per Pitfall 5 mitigation

    server/mod.rs (from RESEARCH Pattern 3/4 + PATTERNS.md §server/mod.rs):
    - pub struct ServerHandle { pub cancel: CancellationToken, pub task: tokio::task::JoinHandle<()> }
    - pub async fn start_server(app: Router, addr: SocketAddr, tls: TlsAcceptor, shutdown: CancellationToken) -> anyhow::Result<()>:
      bind TcpListener::bind(addr).await?, enter tokio::select! loop: on listener.accept() spawn tokio task that calls tls.accept(stream), on success wraps in hyper_util::rt::TokioIo, serves via hyper::server::conn::http1::Builder::new().serve_connection(io, service_fn(move |req| app.clone().call(req))); on shutdown.cancelled() → break
    - Note: use hyper 1.x API from RESEARCH Pattern 3; tower::ServiceExt::oneshot or tower::Service::call for serving

    Extend crates/trackly-app/src/context.rs:
    - Add `use crate::server::ServerHandle;` and `use crate::services::auth::AuthService;`
    - Add to AppCtx struct: `pub auth: Arc<AuthService>`, `pub server_ctl: Arc<tokio::sync::Mutex<Option<ServerHandle>>>`
    - In AppCtx::build(): after cartridges init, add: `let auth = Arc::new(AuthService::new(writer.clone(), readers.clone(), clock.clone()));`; add auth + server_ctl to Ok(Self { ... })

    Add `pub mod server;` to crates/trackly-app/src/lib.rs (after existing pub mod lines).

    Fill in the 4 test stubs (replacing todo!() from Plan 01):
    - session_survives_restart.rs: make_session_store() helper using test_writer_and_readers(); test session_persists_after_store_recreation(); test expired_session_not_returned()
    - tls_server_smoke.rs: test tls_bundle_fingerprint_format() verifies fingerprint_hex length and XX:XX colon pattern; test tls_server_accepts_connection() starts server on random port, connects with tokio-rustls acceptor (danger_accept_invalid_certs via ClientConfig::builder().dangerous()), verifies HTTP/1.1 200 from /api/v1/health-like route
    - server_hot_toggle.rs: test server_starts_stops_port_freed() starts server on random available port, verifies TcpStream connects, cancels token, awaits task, verifies TcpStream::connect returns error
    - graceful_shutdown_drain.rs: test graceful_shutdown_cancels_accept_loop() — starts server, cancels token, awaits join handle completes within 5s
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test session_survives_restart --test tls_server_smoke --test server_hot_toggle --test graceful_shutdown_drain 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 4 tests GREEN. cargo build -p trackly-app succeeds (AppCtx builds with auth + server_ctl). Verify: grep -n "server_ctl" crates/trackly-app/src/context.rs returns 2+ lines (struct field + build init).
    Verify argon2 params: grep "19456" crates/trackly-app/src/services/auth.rs non-empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| password→hash | Plain password enters AuthService::login/create_user, exits only as argon2id hash |
| session cookie→server | Session ID from client resolves to user identity via rusqlite store |
| cert private key→memory | TLS private key lives only in TlsAcceptor (TlsBundle.key_pem dropped after server build) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Info Disclosure | argon2 in async context | mitigate | ALL argon2 calls (hash + verify) wrapped in spawn_blocking — blocks checked in auth_smoke test |
| T-05-04 | Tampering | session ID collision | mitigate | INSERT OR IGNORE in create() + tower-sessions collision-safe Id generation (128-bit OsRng) |
| T-05-05 | Elevation of Privilege | session fixation | mitigate | New session created after login (flush old + session.insert() creates new record); session.flush() in logout |
| T-05-06 | Info Disclosure | TLS private key on disk | mitigate | key_pem written to exe_dir (portable path), permissions not set in Phase 5 — Phase 7 settings hardening; cert_pem saved separately |
| T-05-07 | Elevation of Privilege | desktop_identity attribution | mitigate | Exactly-1-admin query result (LIMIT 2) prevents misattribution when multiple admins; NULL fallback logs as system per D-Desktop-01 |
| T-05-08 | Info Disclosure | stale sessions in sessions table | mitigate | delete() on logout (Pitfall 5); background_cleanup() at server build time removes expired rows |
| T-05-SC | Tampering | cargo installs (argon2, tower-sessions, tokio-rustls, rcgen, rmp-serde, hyper, hyper-util, sha2) | mitigate | All [APPROVED] in RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] packages in this plan |
</threat_model>

<verification>
1. `cargo test -p trackly-app --test auth_smoke --test users_crud` — all tests GREEN
2. `cargo test -p trackly-app --test session_survives_restart` — GREEN
3. `cargo test -p trackly-app --test tls_server_smoke` — GREEN
4. `cargo test -p trackly-app --test server_hot_toggle --test graceful_shutdown_drain` — GREEN
5. `cargo build -p trackly-app` — clean build
6. `grep -n "19456" crates/trackly-app/src/services/auth.rs` — returns argon2 m=19456 param
7. `grep -n "server_ctl" crates/trackly-app/src/context.rs` — returns struct field + build init
8. `cargo test -p trackly-app 2>&1 | grep -E "FAILED" | wc -l` — should be 2 (only role_endpoint_matrix and security_headers remain RED; those need Plan 03)
</verification>

<success_criteria>
- AuthService compiles with all 12 methods; login/verify use argon2id m=19456/t=2/p=1 in spawn_blocking
- desktop_identity() implements D-Desktop-01 exactly-1-admin rule
- RusqliteSessionStore passes create/save/load/delete/restart tests
- TlsBundle generated with valid TlsAcceptor and non-empty colon-hex fingerprint
- start_server() exits within 5s of CancellationToken cancel (no port leak)
- AppCtx.auth and AppCtx.server_ctl present; cargo build clean
- All 6 new integration tests GREEN; existing tests still green
</success_criteria>

<output>
Create `.planning/phases/05-auth-server-mode/05-02-SUMMARY.md` when done
</output>
