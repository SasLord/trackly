---
phase: 05-auth-server-mode
plan: 06
type: execute
wave: 1
depends_on: ["05-05"]
files_modified:
  - crates/trackly-app/src/http/settings.rs
  - crates/trackly-app/src/tauri_cmds/auth.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-infra/tests/migration_idempotency.rs
autonomous: true
requirements:
  - SET-08

must_haves:
  truths:
    - "POST /api/v1/settings_set_network маршрут зарегистрирован в router() из http/settings.rs"
    - "Команда settings_set_network зарегистрирована в specta_export.rs как 14-я команда Phase 5"
    - "build_settings_set_network вызывает authorize(&caller, &Action::ManageSettings) перед записью — неаутентифицированный или не-admin caller получает 401/403"
    - "saveSettings() в NetworkSettings.svelte (apiCall('settings_set_network', { patch: { host, port, cert_path } })) успешно выполняется — port и bind-address сохраняются"
    - "cargo test -p trackly-app компилируется без ошибок после изменений в http/settings.rs и tauri_cmds/auth.rs"
    - "cargo test -p trackly-infra --test migration_idempotency проходит (значения 19, не 17)"
  artifacts:
    - path: "crates/trackly-app/src/http/settings.rs"
      provides: "build_settings_set_network helper + handler_set_network + SetNetworkPayload + маршрут /api/v1/settings_set_network в router()"
      contains: "settings_set_network"
    - path: "crates/trackly-app/src/tauri_cmds/auth.rs"
      provides: "pub async fn settings_set_network Tauri command с #[tauri::command] #[specta::specta]"
      contains: "settings_set_network"
    - path: "crates/trackly-app/src/specta_export.rs"
      provides: "14-я команда Phase 5: crate::tauri_cmds::auth::settings_set_network в collect_commands!"
      contains: "settings_set_network"
    - path: "crates/trackly-infra/tests/migration_idempotency.rs"
      provides: "assertion == 19 на всех строках (17, 22-23, 28, 43)"
      contains: "19"
  key_links:
    - from: "ui/src/features/settings/NetworkSettings.svelte saveSettings()"
      to: "tauri_cmds/auth::settings_set_network (Tauri) / POST /api/v1/settings_set_network (HTTP)"
      via: "apiCall('settings_set_network', { patch: { host, port, cert_path } })"
      pattern: "settings_set_network"
    - from: "http/settings.rs build_settings_set_network"
      to: "trackly_core::auth::authorize"
      via: "authorize(&caller, &Action::ManageSettings)"
      pattern: "authorize"
    - from: "tauri_cmds/auth.rs build_settings_set_network_tauri"
      to: "tauri_cmds/users::resolve_tauri_identity"
      via: "resolve_tauri_identity(ctx).await?"
      pattern: "resolve_tauri_identity"
    - from: "http/settings.rs build_settings_set_network"
      to: "app_settings таблица в SQLite"
      via: "ctx.writer.execute (upsert server_host / server_port / server_cert_path)"
      pattern: "server_host|server_port|server_cert_path"
---

<objective>
Закрыть два BLOCKER-gap'а из VERIFICATION.md (score 12/14):

1. Gap 1: реализовать `settings_set_network` — HTTP-маршрут и Tauri-команду для сохранения port, host, cert_path. UI уже вызывает `apiCall('settings_set_network', { patch: { host, port, cert_path } })` в `saveSettings()`, но бэкенд не реализован.
2. Gap 2: подтвердить и (если нужно) исправить `migration_idempotency.rs` — assertion'ы должны быть `== 19`. Коммит 7c26288 заявил об исправлении; задача — верифицировать тестом и исправить, если остались артефакты `== 17`.

Purpose: фаза 5 не может считаться завершённой, пока кнопка «Сохранить настройки» в NetworkSettings.svelte бросает «command not found» в рантайме, и пока `cargo test -p trackly-infra` красный.

Output: `build_settings_set_network()` в http/settings.rs, Tauri-команда `settings_set_network` в tauri_cmds/auth.rs, регистрация в specta_export.rs, зелёные тесты trackly-infra.
</objective>

<execution_context>
@/Users/madsas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madsas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-auth-server-mode/05-CONTEXT.md

<!-- Ключевые паттерны из уже выполненных планов -->
@.planning/phases/05-auth-server-mode/05-03-SUMMARY.md
@.planning/phases/05-auth-server-mode/05-05-SUMMARY.md

<!-- Основные файлы реализации -->
@crates/trackly-app/src/http/settings.rs
@crates/trackly-app/src/tauri_cmds/auth.rs
@crates/trackly-app/src/specta_export.rs
@ui/src/features/settings/NetworkSettings.svelte
@crates/trackly-infra/tests/migration_idempotency.rs

<interfaces>
<!-- Из crates/trackly-app/src/http/settings.rs — существующие build_* helpers для аналогии: -->

Паттерн HTTP-хелпера с авторизацией (аналог для settings_set_network):
  pub async fn build_desktop_set_lock_http(ctx: &AppCtx, session: &Session, enabled: bool) -> Result<(), AppError> {
      let caller = session_identity(session).await?;
      ctx.auth.set_desktop_lock_enabled(enabled, &caller).await
  }

Паттерн build_settings_get_network (для понимания существующей структуры):
  pub async fn build_settings_get_network(ctx: &AppCtx, session: &Session) -> Result<NetworkSettingsDto, AppError> {
      let caller = session_identity(session).await?;
      trackly_core::auth::authorize(&caller, &Action::ManageSettings)?;
      ...
  }

router() — текущие маршруты (нужно добавить settings_set_network):
  Router::new()
      .route("/api/v1/settings_get_network", post(handler_get_network))
      .route("/api/v1/server_toggle", post(handler_server_toggle))
      .route("/api/v1/server_status", post(handler_server_status))
      .route("/api/v1/desktop_set_lock", post(handler_desktop_set_lock))
  // Добавить: .route("/api/v1/settings_set_network", post(handler_set_network))

<!-- Из crates/trackly-app/src/tauri_cmds/auth.rs — паттерн Tauri-команды (аналог): -->
  #[tauri::command]
  #[specta::specta]
  pub async fn desktop_set_lock(state: tauri::State<'_, AppCtx>, enabled: bool) -> Result<(), AppError> {
      build_desktop_set_lock_tauri(state.inner(), enabled).await
  }
  // build_desktop_set_lock_tauri использует resolve_tauri_identity из tauri_cmds/users.rs:
  pub async fn resolve_tauri_identity(ctx: &AppCtx) -> Result<Identity, AppError>

<!-- Из crates/trackly-app/src/tauri_cmds/users.rs — resolve_tauri_identity: -->
  pub async fn resolve_tauri_identity(ctx: &AppCtx) -> Result<Identity, AppError> {
      let lock = ctx.auth.get_desktop_lock_enabled().await?;
      if lock { Ok(ctx.auth.desktop_identity().await) } else { Ok(Identity::trusted_admin()) }
  }

<!-- Из ui/src/features/settings/NetworkSettings.svelte — точные поля payload: -->
  await apiCall<void>('settings_set_network', {
      patch: {
          host: settings.host,        // String: "0.0.0.0" | "127.0.0.1"
          port: settings.port,        // number (u16 на бэкенде)
          cert_path: settings.cert_path,  // String (пусто = self-signed)
      },
  });

<!-- Из trackly_core::auth — Action::ManageSettings (используется в build_settings_get_network): -->
  trackly_core::auth::authorize(&caller, &Action::ManageSettings)?;

<!-- Из crates/trackly-infra/src/config.rs — ServerConfig поля: -->
  pub struct ServerConfig {
      pub enabled: bool,
      pub host: String,
      pub port: u16,
      pub cert_path: String,
      pub key_path: String,
  }

<!-- AppCtx.writer — паттерн upsert через writer.execute (аналог set_desktop_lock_enabled): -->
  self.writer.execute(move |conn| {
      conn.execute(
          "INSERT INTO app_settings (key, value, created_at_utc, updated_at_utc)
           VALUES (?1, ?2, ?3, ?3) ON CONFLICT(key) DO UPDATE SET value = ?2, updated_at_utc = ?3",
          rusqlite::params![key, value, now],
      ).map(|_| ()).map_err(map_rusqlite)
  }).await
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Реализовать settings_set_network в HTTP-слое и Tauri-команду; зарегистрировать в specta_export</name>

  <files>
    crates/trackly-app/src/http/settings.rs,
    crates/trackly-app/src/tauri_cmds/auth.rs,
    crates/trackly-app/src/specta_export.rs
  </files>

  <read_first>
    - crates/trackly-app/src/http/settings.rs — полный файл (уже в контексте); образец для SetNetworkPayload, handler, router() chain
    - crates/trackly-app/src/tauri_cmds/auth.rs — полный файл; образец build_desktop_set_lock_tauri + #[tauri::command] #[specta::specta] wrapper
    - crates/trackly-app/src/tauri_cmds/users.rs — resolve_tauri_identity (строки 33-40); импортировать в auth.rs уже есть как crate::tauri_cmds::users::resolve_tauri_identity
    - crates/trackly-app/src/specta_export.rs — полный файл; добавить строку после server_status (строка 97)
    - ui/src/features/settings/NetworkSettings.svelte — строки 61-81 (saveSettings); подтвердить поля patch.host / patch.port / patch.cert_path
    - crates/trackly-infra/src/config.rs — ServerConfig (строки 40-55); понять типы u16/String
    - crates/trackly-infra/src/db/writer_worker.rs (или аналог) — WriterHandle::execute сигнатура для upsert паттерна
    - migrations/V018__auth_settings.sql — upsert паттерн для app_settings (идентичный для новых ключей)
  </read_first>

  <action>
    Стратегия персистирования: сохранять host, port, cert_path в таблице app_settings (как desktop_lock_enabled) под ключами 'server_host', 'server_port', 'server_cert_path'. Это позволяет runtime-override без мутирования Arc&lt;AppConfig&gt; и не требует добавления Serialize к AppConfig. Значения из app_settings читаются при следующем запросе settings_get_network (опционально — расширение build_settings_get_network, если нужно).

    Примечание: читать сохранённые значения из app_settings в build_settings_get_network НЕ требуется для этого gap-closure — достаточно сохранить и вернуть (). build_settings_get_network уже читает из ctx.config.server (файл); сохранение в app_settings обеспечивает персистирование для будущего прочтения. Изменение build_settings_get_network (чтобы читать override из app_settings вместо config) — опционально и в рамках данного плана не обязательно.

    В crates/trackly-app/src/http/settings.rs:

    1. Добавить struct SetNetworkPayload с полем patch: NetworkPatch (или inline-struct), где NetworkPatch содержит host: String, port: u16 (или i64 в соответствии с тем, что шлёт UI — UI шлёт number, поэтому i64 с #[specta(type = i32)]), cert_path: String. Поле называется patch (именно так шлёт saveSettings()).

    2. Добавить pub async fn build_settings_set_network(ctx: &AppCtx, session: &Session, patch: NetworkPatch) -> Result&lt;(), AppError&gt;:
       - Вызвать session_identity(session).await? чтобы получить caller
       - Вызвать trackly_core::auth::authorize(&caller, &Action::ManageSettings)? — это admin-only мутация (ManageSettings, аналогично build_settings_get_network и build_desktop_set_lock_http)
       - Валидировать patch.port: диапазон 1..=65535, иначе AppError::Validation { field: "port", message: ... }
       - Вызвать ctx.writer.execute с тремя отдельными upsert-запросами (или одним batch) в app_settings:
         * key='server_host', value=patch.host
         * key='server_port', value=patch.port.to_string()
         * key='server_cert_path', value=patch.cert_path
         Каждый upsert: "INSERT INTO app_settings (key, value, created_at_utc, updated_at_utc) VALUES (?1, ?2, ?3, ?3) ON CONFLICT(key) DO UPDATE SET value = ?2, updated_at_utc = ?3" — идентично паттерну set_desktop_lock_enabled в services/auth.rs (строки 808-813). Время — ctx.clock.unix_seconds() или unixepoch() через SQL.
       - Вернуть Ok(())

    3. Добавить handler_set_network:
       pub async fn handler_set_network(
           State(ctx): State&lt;AppCtx&gt;,
           session: Session,
           Json(payload): Json&lt;SetNetworkPayload&gt;,
       ) -> Result&lt;Json&lt;()&gt;, AppErrorResponse&gt;

    4. В router() добавить .route("/api/v1/settings_set_network", post(handler_set_network)) — удалить TODO-комментарий из строки 5 module doc.

    В crates/trackly-app/src/tauri_cmds/auth.rs:

    5. Добавить pub async fn build_settings_set_network_tauri(ctx: &AppCtx, patch: NetworkPatch) -> Result&lt;(), AppError&gt;:
       - let caller = crate::tauri_cmds::users::resolve_tauri_identity(ctx).await?
       - Валидировать patch.port (диапазон 1..=65535)
       - Три upsert через ctx.writer.execute в app_settings (идентично HTTP-хелперу)
       - Вернуть Ok(())
       Примечание: NetworkPatch должен быть объявлен в http/settings.rs и переиспользован через pub use или переопределён — лучше pub в http/settings.rs и импортировать в tauri_cmds/auth.rs (use crate::http::settings::NetworkPatch).

    6. Добавить Tauri-команду:
       #[tauri::command]
       #[specta::specta]
       pub async fn settings_set_network(state: tauri::State&lt;'_, AppCtx&gt;, patch: NetworkPatch) -> Result&lt;(), AppError&gt; {
           build_settings_set_network_tauri(state.inner(), patch).await
       }
       Порядок атрибутов: #[tauri::command] ПЕРЕД #[specta::specta] — обязательное требование tauri-specta v2 rc.21 (doc-comment в specta_export.rs строка 7).

    В crates/trackly-app/src/specta_export.rs:

    7. В collect_commands! после строки с server_status добавить:
       crate::tauri_cmds::auth::settings_set_network,
       Это делает settings_set_network 14-й командой Phase 5 (per plan 05-03 must_have).

    Импорты: NetworkPatch нужно derivе Debug, Clone, Serialize, Deserialize, Type (аналогично другим DTO) — он используется как аргумент Tauri-команды через specta.

    Если удобнее разместить NetworkPatch в dto/auth.rs (рядом с NetworkSettingsDto) — допустимо; тогда импортировать из crate::dto::auth::NetworkPatch в оба модуля.
  </action>

  <verify>
    <automated>cargo check -p trackly-app 2>&amp;1 | tail -5</automated>
    <automated>grep -c "settings_set_network" /Users/madsas/Projects/trackly/crates/trackly-app/src/specta_export.rs</automated>
    <automated>grep -c "settings_set_network" /Users/madsas/Projects/trackly/crates/trackly-app/src/http/settings.rs</automated>
    <automated>grep -c "settings_set_network" /Users/madsas/Projects/trackly/crates/trackly-app/src/tauri_cmds/auth.rs</automated>
    <automated>grep -n "settings_set_network" /Users/madsas/Projects/trackly/crates/trackly-app/src/http/settings.rs | grep "route\|router"</automated>
    <automated>grep -n "authorize.*ManageSettings\|ManageSettings.*authorize" /Users/madsas/Projects/trackly/crates/trackly-app/src/http/settings.rs</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "settings_set_network" crates/trackly-app/src/specta_export.rs возвращает >= 1
    - grep на http/settings.rs показывает: '.route("/api/v1/settings_set_network"' присутствует в router()
    - grep на http/settings.rs показывает: 'build_settings_set_network' и 'handler_set_network' присутствуют
    - grep на tauri_cmds/auth.rs показывает: 'pub async fn settings_set_network' с атрибутами #[tauri::command] и #[specta::specta]
    - grep на http/settings.rs показывает 'authorize(&caller, &Action::ManageSettings)' внутри build_settings_set_network (а не только в build_settings_get_network) — гарантирует, что неаутентифицированный caller получает 403
    - cargo check -p trackly-app завершается успешно (exit 0)
    - Комментарий-TODO "// TODO: Phase 5+" в строке 5 doc-comment settings.rs обновлён/удалён
  </acceptance_criteria>

  <done>
    HTTP-маршрут POST /api/v1/settings_set_network существует и защищён authorize(ManageSettings). Tauri-команда settings_set_network зарегистрирована как 14-я команда Phase 5 в specta_export.rs. cargo check -p trackly-app зелёный.
  </done>
</task>

<task type="auto">
  <name>Task 2: Верифицировать migration_idempotency тест и при необходимости исправить артефакты == 17</name>

  <files>
    crates/trackly-infra/tests/migration_idempotency.rs
  </files>

  <read_first>
    - crates/trackly-infra/tests/migration_idempotency.rs — полный файл (уже в контексте); проверить что все assertion'ы содержат 19
    - crates/trackly-infra/src/test_support/test_db.rs — образец исправления (коммит 04579bd; user_version assertion == 19)
  </read_first>

  <action>
    Запустить cargo test -p trackly-infra --test migration_idempotency и проверить результат.

    Если тест проходит — задача выполнена (коммит 7c26288 уже содержит исправление), зафиксировать это как подтверждение.

    Если тест падает (left: 19 right: 17) — значит в файле остались неисправленные строки. Прочитать файл целиком и заменить каждое вхождение == 17 на == 19 и "all 17" → "all 19" в строке сообщения об ошибке. Затем повторить cargo test -p trackly-infra --test migration_idempotency.

    Конкретные строки для проверки (per VERIFICATION.md):
    - Строка 17: assert_eq!(report.applied_count, ?, ...) — ожидаемое значение 19
    - Строки 22-23: applied_count и schema_version после второго run
    - Строка 28: applied_count == 0 (это no-op assertion — НЕ менять на 19!)
    - Строка 43: schema_version на reopened DB — ожидаемое значение 19

    ВАЖНО: строка 28 (second run should be no-op) должна оставаться == 0, не 19. Менять нужно только applied_count из первого run (строка 17) и schema_version на строках 22-23, 43.
  </action>

  <verify>
    <automated>cargo test -p trackly-infra --test migration_idempotency 2>&amp;1</automated>
    <automated>grep -v "no-op\|== 0" /Users/madsas/Projects/trackly/crates/trackly-infra/tests/migration_idempotency.rs | grep -c "== 17" || echo "0 stale assertions found"</automated>
  </verify>

  <acceptance_criteria>
    - cargo test -p trackly-infra --test migration_idempotency завершается с exit 0 ("test result: ok")
    - grep на migration_idempotency.rs не содержит "== 17" за пределами no-op строк (applied_count == 0 — это ожидаемое правильное значение для no-op run, не трогать)
    - Строки с applied_count первого run и schema_version содержат 19, не 17
  </acceptance_criteria>

  <done>
    cargo test -p trackly-infra --test migration_idempotency green. Все assertion'ы для V018+V019 используют значение 19.
  </done>
</task>

<task type="auto">
  <name>Task 3: Финальная проверка — полный тестовый прогон и bindings export</name>

  <files>
    ui/src/bindings.ts
  </files>

  <read_first>
    - crates/trackly-app/tests/export_bindings.rs — проверить, что тест экспорта вызовет specta_export::builder() с новой командой
    - ui/src/bindings.ts — перед прогоном (для сравнения до/после)
  </read_first>

  <action>
    1. Запустить полный тестовый прогон trackly-app:
       cargo test -p trackly-app 2>&1

    2. Запустить cargo test -p trackly-infra:
       cargo test -p trackly-infra 2>&1

    3. Запустить export_bindings тест для обновления ui/src/bindings.ts:
       cargo test -p trackly-app --test export_bindings 2>&1

    4. Проверить, что bindings.ts содержит settings_set_network:
       grep "settings_set_network" ui/src/bindings.ts

    5. Запустить pnpm svelte-check (в директории ui/) для подтверждения 0 ошибок TypeScript после обновления bindings.ts.

    Если export_bindings или svelte-check выдают ошибки — исправить (например, если NetworkPatch не impl Type из specta, добавить derive Type).
  </action>

  <verify>
    <automated>cargo test -p trackly-app 2>&amp;1 | tail -10</automated>
    <automated>cargo test -p trackly-infra 2>&amp;1 | tail -5</automated>
    <automated>grep -c "settings_set_network" /Users/madsas/Projects/trackly/ui/src/bindings.ts</automated>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm svelte-check 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - cargo test -p trackly-app: "test result: ok" (все тесты зелёные)
    - cargo test -p trackly-infra: "test result: ok" (migration_idempotency включён)
    - grep -c "settings_set_network" ui/src/bindings.ts возвращает >= 1 (команда появилась в TypeScript-биндингах)
    - pnpm svelte-check: "0 errors" (не считая pre-existing warnings из Phase 4)
    - Весь Phase 5 тестовый набор: role_endpoint_matrix, auth_smoke, users_crud, security_headers, session_survives_restart, tls_server_smoke, server_hot_toggle, graceful_shutdown_drain — все зелёные
  </acceptance_criteria>

  <done>
    Все тесты зелёные. ui/src/bindings.ts содержит settings_set_network. svelte-check: 0 ошибок. Gap 1 и Gap 2 закрыты.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/LAN → POST /api/v1/settings_set_network | Неаутентифицированный или не-admin пользователь не должен иметь возможность изменить port/host/cert_path сервера |
| Tauri webview → settings_set_network invoke | Десктопный вызов через resolve_tauri_identity — в unlocked-режиме yielded trusted_admin, в locked-режиме — desktop_identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SN-01 | Tampering | POST /api/v1/settings_set_network | mitigate | build_settings_set_network вызывает authorize(&caller, &Action::ManageSettings) перед любой записью — реализует то же ограничение, что и build_settings_get_network; non-admin caller получает AppError::Forbidden (→ 403 через AppErrorResponse); session_identity возвращает Err если сессии нет (→ 401) |
| T-05-SN-02 | Elevation of Privilege | Tauri settings_set_network без auth в unlocked desktop | accept | В unlocked-режиме (D-Desktop-01) любой локальный пользователь является admin через trusted_admin() — это архитектурное решение (D-Desktop-01), не баг; в locked-режиме resolve_tauri_identity возвращает desktop_identity с реальным user_id и ролью |
| T-05-SN-03 | Tampering | patch.port: диапазон | mitigate | Валидация порта 1..=65535 в build_settings_set_network и build_settings_set_network_tauri перед записью в БД; некорректные значения → AppError::Validation |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Этот план не добавляет новых зависимостей; все изменения — в существующих файлах |
</threat_model>

<verification>
После выполнения плана запустить следующие команды для итоговой проверки:

1. cargo test -p trackly-app — все тесты зелёные
2. cargo test -p trackly-infra — migration_idempotency зелёный
3. grep "settings_set_network" crates/trackly-app/src/specta_export.rs — присутствует
4. grep '/api/v1/settings_set_network' crates/trackly-app/src/http/settings.rs — присутствует в router()
5. grep 'settings_set_network' ui/src/bindings.ts — присутствует (после export_bindings)
6. cd ui && pnpm svelte-check — 0 errors

Эти проверки закрывают обе строки в VERIFICATION.md:
- Truth #13: "settings_set_network Tauri command registered in specta_export" → VERIFIED
- Truth #14: "trackly-infra migration test suite fully green after V018/V019" → VERIFIED
</verification>

<success_criteria>
- POST /api/v1/settings_set_network маршрут существует в settings.rs router() и вызывает authorize(ManageSettings) перед записью
- Tauri-команда settings_set_network зарегистрирована в specta_export.rs (14-я команда Phase 5)
- ui/src/bindings.ts содержит settings_set_network (regenerated через export_bindings тест)
- cargo test -p trackly-app: OK
- cargo test -p trackly-infra: OK (включая migration_idempotency с assertion == 19)
- pnpm svelte-check: 0 errors
- Итоговый score Phase 5 verification: 14/14 (was 12/14)
</success_criteria>

<output>
Создать `.planning/phases/05-auth-server-mode/05-06-SUMMARY.md` по завершении.
</output>
