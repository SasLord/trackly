---
phase: 11-requests-employee-ux-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-infra/src/repos/requests_sqlite.rs
  - crates/trackly-core/src/domain/requests.rs
  - crates/trackly-app/src/dto/request.rs
  - ui/src/features/requests/RequestDetail.svelte
autonomous: true
requirements: [D-CAT-01]
must_haves:
  truths:
    - "Открыв деталь заявки, пользователь видит название категории (напр. «Программное обеспечение»), а не число."
    - "Заявка без категории (free_form) показывает прочерк или не рисует блок категории — никогда не «null»/число."
  artifacts:
    - path: crates/trackly-infra/src/repos/requests_sqlite.rs
      provides: "LEFT JOIN request_categories + category_name как последний столбец SELECT_REQUESTS (idx 18)"
      contains: "LEFT JOIN request_categories"
    - path: crates/trackly-core/src/domain/requests.rs
      provides: "RequestRow.category_name: Option<String>"
      contains: "category_name"
    - path: crates/trackly-app/src/dto/request.rs
      provides: "RequestDto.category_name: Option<String> + строка в From<RequestRow>"
      contains: "category_name"
    - path: ui/src/features/requests/RequestDetail.svelte
      provides: "рендер categoryName вместо categoryId"
      contains: "categoryName"
  key_links:
    - from: "requests_sqlite.rs SELECT_REQUESTS"
      to: "request_categories.name"
      via: "LEFT JOIN request_categories rc ON rc.id = r.category_id"
      pattern: "LEFT JOIN request_categories"
    - from: "RequestDetail.svelte"
      to: "RequestDto.categoryName"
      via: "render"
      pattern: "categoryName"
---

<objective>
D-CAT-01: категория заявки должна отображаться текстовым названием из `request_categories.name`, а не числовым `category_id`.

Purpose: UAT-находка после Phase 9/10 — деталь заявки рисует число «3» вместо «Программное обеспечение». Имя категории — производное от FK, считается на сервере (как `requester_name`), один read-путь покрывает все запросы заявок.
Output: `category_name` прокинут через `RequestRow` → `RequestDto` → `RequestDetail.svelte`; LEFT JOIN добавлен последним столбцом без сдвига индексов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-requests-employee-ux-gaps/11-CONTEXT.md
@.planning/phases/11-requests-employee-ux-gaps/11-PATTERNS.md

<interfaces>
<!-- Текущий SELECT_REQUESTS / map_row_request (requests_sqlite.rs) — последний столбец r.ad_subtype = idx 17.
     Добавляемый category_name = idx 18 (последним). НЕ вставлять в середину — сдвинет row.get(n). -->

RequestRow (trackly-core/src/domain/requests.rs) уже несёт requester_name: Option<String> (idx 11),
printer_name: Option<String> (idx 12) — образец для category_name.

RequestDto (trackly-app/src/dto/request.rs) #[serde(rename_all = "camelCase")] →
category_name на проводе становится categoryName. From<RequestRow> копирует поле в поле.

request_categories (V024) засеяна 4 RU-именами; FK requests.category_id → request_categories.id.
id «3» = «Программное обеспечение».
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — прокинуть category_name через JOIN, RequestRow, RequestDto</name>
  <files>crates/trackly-infra/src/repos/requests_sqlite.rs, crates/trackly-core/src/domain/requests.rs, crates/trackly-app/src/dto/request.rs</files>
  <read_first>
    - crates/trackly-infra/src/repos/requests_sqlite.rs (const SELECT_REQUESTS, fn map_row_request, тест test_request_repo_create ~строки 435-477, helpers fresh_conn/seed_user)
    - crates/trackly-core/src/domain/requests.rs (struct RequestRow — поля requester_name/printer_name)
    - crates/trackly-app/src/dto/request.rs (struct RequestDto + impl From<RequestRow>)
  </read_first>
  <behavior>
    - Тест (расширить test_request_repo_create в requests_sqlite.rs): засеять заявку с category_id = Some(3) → прочитанный RequestRow.category_name == Some("Программное обеспечение").
    - Тест: засеять заявку с category_id = None → RequestRow.category_name == None.
    - Существующие индексы (requester_name idx 11, printer_name idx 12, ad_subtype idx 17) не меняются.
  </behavior>
  <action>
    В `requests_sqlite.rs` const `SELECT_REQUESTS`: добавить `rc.name AS category_name` ПОСЛЕДНИМ столбцом списка (сразу после `r.ad_subtype`) и добавить `LEFT JOIN request_categories rc ON rc.id = r.category_id` после существующих джойнов `users`/`devices`. В `map_row_request` добавить `category_name: row.get(18)?` как последнее поле — НЕ менять row.get(0..17). В `trackly-core/src/domain/requests.rs` `RequestRow`: добавить `pub category_name: Option<String>,` рядом с `printer_name` (write-путь `RequestNew` не трогается — нет churn в конструкторах). В `dto/request.rs` `RequestDto`: добавить `pub category_name: Option<String>,` рядом с `printer_name`; в `impl From<RequestRow>` добавить `category_name: r.category_name,`. Расширить тест `test_request_repo_create` двумя ассертами (category_id Some(3) → Some("Программное обеспечение"), None → None) — использовать существующие `fresh_conn()`/`seed_user()`.
  </action>
  <verify>
    <automated>cargo test -p trackly-infra requests_sqlite</automated>
  </verify>
  <acceptance_criteria>
    - `requests_sqlite.rs` содержит строку `LEFT JOIN request_categories`.
    - `requests_sqlite.rs` содержит `category_name: row.get(18)?`.
    - `domain/requests.rs` RequestRow содержит `category_name`.
    - `dto/request.rs` RequestDto содержит `category_name` И строку `category_name: r.category_name` в From.
    - `cargo test -p trackly-infra requests_sqlite` выходит с кодом 0; новые ассерты (Some/None category_name) зелёные.
    - `cargo build` без ошибок (все вызовы map_row_request компилируются с новым полем).
  </acceptance_criteria>
  <done>Read-путь заявок отдаёт category_name; тест репозитория подтверждает Some("Программное обеспечение") и None.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — RequestDetail рендерит categoryName, регенерация bindings</name>
  <files>ui/src/features/requests/RequestDetail.svelte</files>
  <read_first>
    - ui/src/features/requests/RequestDetail.svelte (блок field-label/field-value ~строки 376-391, где сейчас рендерится {request.categoryId}; идиома `printerName ?? '—'` строка ~378)
  </read_first>
  <action>
    В `RequestDetail.svelte` заменить блок, рендерящий `{request.categoryId}` (~строки 387-391), на условный рендер по `request.categoryName`: при наличии — показать field-row «Категория» со значением `{request.categoryName}`; при отсутствии (free_form) — либо не рисовать блок, либо показать прочерк `'—'` (discretion, выбрать прочерк для консистентности с `printerName ?? '—'`). Svelte автоэкранирует текст (XSS V5). Затем регенерировать TS-биндинги, чтобы `categoryName` появился в типе `RequestDto`: `cargo test -p trackly-app export_bindings` (запускает tests/export_bindings.rs). После правок фронта собрать LAN-бандл: `pnpm --dir ui build`.
  </action>
  <verify>
    <automated>cargo test -p trackly-app export_bindings && pnpm --dir ui exec svelte-check --threshold error</automated>
  </verify>
  <acceptance_criteria>
    - `RequestDetail.svelte` содержит `categoryName` и НЕ рендерит `categoryId` как видимое значение категории.
    - `ui/src/bindings.ts` (или bindings-phase6.ts) после регенерации содержит `categoryName` в RequestDto-типе (grep: `categoryName`).
    - `cargo test -p trackly-app export_bindings` выходит 0 (нет дрейфа биндингов).
    - `pnpm --dir ui exec svelte-check --threshold error` — 0 ошибок.
    - MANUAL: открыть деталь заявки категории «Программное обеспечение» в desktop И в LAN-браузере (после `pnpm --dir ui build`) → видно текст «Программное обеспечение», не «3»; free_form-заявка → прочерк/без блока.
  </acceptance_criteria>
  <done>Деталь заявки показывает русское имя категории; биндинги содержат categoryName; svelte-check чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → read-DTO | `category_name` производится JOIN-ом на сервере; клиент получает уже готовую строку. |
| read-DTO → webview render | Имя категории рендерится в Svelte. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-I | Information Disclosure | SELECT_REQUESTS JOIN | accept | JOIN добавляет только публичное имя категории (4 фикс. RU-имени из V024) — не PII, не чувствительные данные. |
| T-11-01-T | Tampering (XSS) | RequestDetail.svelte рендер category_name | mitigate | Svelte автоэкранирует текстовые интерполяции; `{request.categoryName}` рендерится как текст, не HTML. |
| T-11-01-SC | Tampering | npm/cargo installs | mitigate | Новых пакетов не вводится (RESEARCH §Package Legitimacy: N/A) — install-задач нет, чекпоинт не требуется. |
</threat_model>

<verification>
- `cargo test -p trackly-infra requests_sqlite` зелёный (Some/None category_name).
- `cargo test -p trackly-app export_bindings` зелёный (биндинги синхронны).
- `pnpm --dir ui exec svelte-check --threshold error` — 0 ошибок.
- MANUAL (desktop + LAN после `pnpm --dir ui build`): деталь заявки показывает имя категории текстом; free_form → прочерк/без блока.
</verification>

<success_criteria>
- D-CAT-01 закрыт: категория отображается текстом во всех путях чтения заявок (один JOIN покрывает get/list/fetch_in_tx).
- Индексы map_row_request не сдвинуты (category_name = idx 18, последний).
- Полный `cargo build` + затронутые тесты зелёные; биндинги без дрейфа.
</success_criteria>

<output>
Create `.planning/phases/11-requests-employee-ux-gaps/11-01-SUMMARY.md` when done
</output>
