---
status: partial
phase: 11-requests-employee-ux-gaps
source: [11-VERIFICATION.md]
started: 2026-06-22T00:25:00Z
updated: 2026-06-22T00:25:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Категория заявки рендерится текстом (desktop + LAN-браузер)
expected: В детальной карточке заявки показано русское название категории (напр. «Программное обеспечение»), а не число «3»; free_form-заявка без категории не показывает блок категории (не «—», не «null», не «0»).
result: [pending]

### 2. Дропдаун формы создания заполняется с сервера и шлёт корректный category_id
expected: Дропдаун показывает все сидовые категории; выбор и отправка создают заявку, чья карточка позже показывает соответствующее название категории.
result: [pending]

### 3. Сотрудник видит непустой сгруппированный дропдаун принтеров и может создать заявку на замену картриджа
expected: При роли employee в LAN-браузере, форма «Замена картриджа» — дропдаун принтеров непустой, сгруппирован по Расположению с серыми заголовками групп, отсортирован по локации; выбор принтера и отправка проходят успешно.
result: [pending]

### 4. Realtime тост/нотификация доставляется только автору заявки
expected: Две сессии сотрудников (A и B). A создаёт заявку. Админ меняет статус заявки A при активной вкладке A (ожидается тост) и при скрытой вкладке A с granted Notification на secure-context https://...:8443 (ожидается системная нотификация). B не получает ничего по заявке A.
result: [pending]

### 5. Деликатный тайминг запроса permission
expected: Свежая сессия employee (permission === 'default'): на загрузке страницы запроса prompt НЕ появляется; ровно один prompt появляется сразу после первой успешной отправки заявки; на повторных отправках не появляется.
result: [pending]

### 6. Graceful-degrade на HTTP без secure-context
expected: Доступ к приложению по plain HTTP (first-run, без HTTPS-сертификата) как employee; админ меняет статус. Нет ошибок в консоли; нотификация деградирует до тоста, т.к. window.isSecureContext === false.
result: [pending]

### 7. Подтверждение смены семантики ошибки WR-03
expected: Ни одна часть системы (frontend error handling, другие тесты) не зависела от получения OptimisticLockMismatch (а не NotFound) при конкурентном soft-delete заявки во время transition. Нет регресса в пользовательских сообщениях об ошибке для этой редкой гонки.
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps
