# Phase 40.2: Шаблоны инвентарных номеров - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-18
**Phase:** 40.2-inventory-number-templates
**Areas discussed:** Цепочка проверок при сохранении, Номер акта как текст, Блок шаблонов в настройках, Миграции и counters, Поле номера (доп. раунд)

---

## Цепочка проверок при сохранении

| Вопрос | Варианты | Выбрано |
|--------|----------|---------|
| Порядок попапов | Занят → шаблон → кир/лат; один общий попап предупреждений; шаблон → кир/лат → занят | Занят → шаблон → кир/лат |
| Предупреждать о занятом до сохранения | Только при сохранении; подсказка при вводе + попап | Подсказка при вводе + попап |
| Где проверять кир/лат и двойника | Сервер (ответ-предупреждение); клиент + запрос двойника | Сервер |
| Предупреждения в формах правки | Занят + кир/лат; только занят | Занят + кир/лат |
| Содержимое живой подсказки | Только «занят: вид + наименование»; + мягкая строка кир/лат | Только «занят» |

## Номер акта как текст

| Вопрос | Варианты | Выбрано |
|--------|----------|---------|
| Акт передачи «42в» при возврате «42в» | Запретить (по отображаемым номерам); разрешить | Запретить |
| Дата токенов для актов | Сегодня на сервере (SPEC); дата передачи из формы | Сегодня на сервере |
| Хранение номера | Trim, регистр как ввели; trim + UPPER; как ввели | Trim, регистр как ввели |

## Блок шаблонов в настройках

| Вопрос | Варианты | Выбрано |
|--------|----------|---------|
| Вид блока | Таблица + модалка; inline-правка | Таблица + модалка |
| Смена типа | Нет, только маска; да, со сбросом контекстов | Нет |
| Меню «Вставка» | Маска + следующий номер, галочка, «Без шаблона» внизу; только маски | Маска + следующий номер |
| Удаление | Подтверждение с перечнем контекстов; простое подтверждение | С перечнем контекстов |

## Миграции и counters

| Вопрос | Варианты | Выбрано |
|--------|----------|---------|
| Таблица counters | DROP; оставить неиспользуемой | DROP |
| Засев в пустой БД | Да; только для БД с данными | Да |
| Правка маски запомненного шаблона | Контекст следует за шаблоном; сброс в «без шаблона» | Следует за шаблоном |
| Аудит ДУБЛЬ-… | user_id NULL + отдельный action; обычный update | Система + отдельный action |

## Поле номера (доп. раунд)

| Вопрос | Варианты | Выбрано |
|--------|----------|---------|
| Номер занят другим при открытом попапе | Тихо пересчитать, если не трогали; ничего | Тихо пересчитать |
| Нет шаблонов типа | Кнопка + подсказка в меню; скрыть; disabled | Кнопка + подсказка |
| Вид ↑/↓ | Иконка + тултип; стрелка + подпись | Иконка + тултип |
| Иконка «Вставка» | Из текущего набора; своя | Из текущего набора |

## Claude's Discretion

Схема таблиц и индексов, SQL/Rust-миграции, таблица гомоглифов, debounce, форма ответа-предупреждения, подсказка в формах правки.

## Deferred Ideas

Нет. Todo «Rework act templates…» рассмотрен и не включён (не относится к фазе).
