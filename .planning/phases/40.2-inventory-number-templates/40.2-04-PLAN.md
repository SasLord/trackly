---
phase: 40.2-inventory-number-templates
plan: 04
type: execute
wave: 3
depends_on: ["40.2-03"]
files_modified:
  - crates/trackly-app/src/dto/number_template.rs
  - crates/trackly-app/src/dto/mod.rs
  - crates/trackly-app/src/services/number_template_service.rs
  - crates/trackly-app/src/services/mod.rs
  - crates/trackly-app/tests/number_templates_crud.rs
  - crates/trackly-app/tests/number_templates_compute_next.rs
autonomous: true
requirements: [NUM-01, NUM-03, NUM-08, NUM-09, NUM-10, NUM-11, NUM-12]

must_haves:
  truths:
    - "Сохранение без флага подтверждения возвращает структурированное предупреждение, а не ошибку — клиент отличает 'нужно подтверждение' от 'сохранение провалилось' (D-04)"
    - "Тип шаблона неизменяем после создания на уровне сервиса — update() физически не принимает template_type (D-10)"
    - "Память контекста хранит id шаблона (не маску) и переходит в NULL при удалении шаблона через существующий FK ON DELETE SET NULL (D-17)"
  artifacts:
    - path: "crates/trackly-app/src/dto/number_template.rs"
      provides: "NumberTemplateDto, NextNumberDto, NumberWarningDto, OccupyingRecordDto, NumberCheckOutcome<T> — общий контракт для планов 05-15"
    - path: "crates/trackly-app/src/services/number_template_service.rs"
      provides: "NumberTemplateService — CRUD, peek_next, context memory, is_occupied, detect_warnings"
  key_links:
    - from: "crates/trackly-app/src/services/number_template_service.rs"
      to: "crates/trackly-infra/src/repos/number_templates_sqlite.rs"
      via: "SqliteNumberTemplateRepository (план 03)"
      pattern: "compute_next_for_template|fetch_matching_values"
---

<objective>
Построить `NumberTemplateService` — единственного владельца бизнес-логики шаблонов номеров:
CRUD с пред-проверкой дубля и аудитом (по образцу `cartridge_service.rs::model_create`), память
последнего шаблона на 5 контекстов, проверку занятости номера в пространстве и обнаружение
предупреждений (смешение алфавитов / двойник). Ввести общий DTO-контракт
`NumberCheckOutcome<T>`, которым в волне 5 (планы 06/07/08) воспользуются `act_service` /
`cartridge_service` / `device_service`, чтобы реализовать цепочку D-01 (занят → шаблон → кир/лат)
единообразно, без трёх разных изобретённых форм ответа.

Purpose: закрыть Pitfall 7 из RESEARCH.md (нужна структура «успех / нужно подтверждение», а не
переиспользование `AppError::Conflict`) одним решением для всей фазы.
Output: `trackly-app::dto::number_template`, `trackly-app::services::number_template_service`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.2-inventory-number-templates/40.2-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-CONTEXT.md
@.planning/phases/40.2-inventory-number-templates/40.2-RESEARCH.md
@.planning/phases/40.2-inventory-number-templates/40.2-PATTERNS.md

<interfaces>
Из плана 01/02/03 (уже реализовано, читать напрямую, не пересказывать): `TemplateType`,
`TemplateContext`, `NumberTemplateRow`, `NextNumberResult`, `mask::render_number`,
`homoglyphs::{has_script_mix,to_latin_skeleton}`, `SqliteNumberTemplateRepository` (CRUD in-tx +
`compute_next_for_template` + `fetch_matching_values` + `set_context_in_tx`).

Из `crates/trackly-app/src/dto/device.rs:311-333` (`CsvImportReport`/`RowError`) — пример
"плоской структуры без вложенного AppError для простого specta::Type/JSON shape" — тот же принцип
применить к `NumberWarningDto`/`NumberCheckOutcome`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Контракты — dto/number_template.rs</name>
  <files>crates/trackly-app/src/dto/number_template.rs, crates/trackly-app/src/dto/mod.rs</files>
  <read_first>
    crates/trackly-app/src/dto/cartridge.rs,
    crates/trackly-app/src/dto/device.rs,
    crates/trackly-core/src/domain/number_templates.rs,
    crates/trackly-core/src/error.rs
  </read_first>
  <action>
    Определить (все — `#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, specta::Type)]`,
    `#[serde(rename_all = "camelCase")]`, `i64`-поля с `#[specta(type = i32)]` по конвенции
    остальных DTO этого проекта):
    - `TemplateTypeDto` — enum-зеркало `TemplateType` (4 варианта, `#[serde(rename_all =
      "snake_case")]` строки `device_inventory`/`act_number`/`cartridge_code`/`drum_code`) +
      `From<TemplateType>`/`TryFrom<TemplateTypeDto> for TemplateType` (или `impl From` в обе
      стороны, раз `TemplateType` не содержит невалидных состояний).
    - `TemplateContextDto` — enum-зеркало `TemplateContext` (5 вариантов, те же строки, что
      `TemplateContext::as_str()`), с конверсиями аналогично.
    - `NumberTemplateDto { id: i64, template_type: TemplateTypeDto, mask: String, next_first_free:
      String, next_max_plus_one: String, has_gap: bool, overflowed: bool, version: i64 }` —
      `next_first_free`/`next_max_plus_one` уже ОТРЕНДЕРЕНЫ через `mask::render_number` (клиент
      никогда не рендерит номер сам — Anti-Pattern из RESEARCH).
    - `NextNumberDto { rendered: String, alt_rendered: Option<String>, has_gap: bool, overflowed:
      bool }` — `alt_rendered = Some(...)` только когда `has_gap && !overflowed &&
      max_plus_one_fits_width` (переключатель ↑/↓, NUM-07); ответ команды `peek_next` (план 05).
    - `OccupyingRecordDto { kind: String, title: String, subtitle: Option<String>, place: Option
      <String>, status: Option<String> }` — `kind` ∈ `device|printer|cartridge|drum|act`
      (Copywriting Contract §«Номер занят», карточка «Вид/Наименование/Модель/Место/Статус» —
      уместить в 4 плоских поля, `title`=наименование/модель, `subtitle`=доп.поле для акта типа
      "Передал/Принял" сериализовать как `subtitle` в формате "Передал: {X} · Принял: {Y}" —
      решение planner'а ради единой плоской структуры вместо enum-варианта на каждый вид записи;
      задокументировать этот выбор doc-комментарием).
    - `NumberWarningKind` enum: `ScriptMix`, `Mismatch` (`#[serde(rename_all = "snake_case")]`).
    - `NumberWarningDto { kind: NumberWarningKind, message: String, doppelganger: Option
      <OccupyingRecordDto> }` — `message` формируется сервисом (план 04 Task 3) дословно по
      Copywriting Contract текстам «Проверьте буквы в номере» / «Не соответствует шаблону»,
      клиент НЕ собирает текст сам.
    - `NumberCheckOutcome<T>` — НЕ generic enum с `specta::Type` (specta плохо работает с
      generics в этом проекте, см. `error.rs`'s ручной impl) — вместо этого сделать КОНКРЕТНЫЕ
      неgeneric enum'ы на каждый потребитель волны 5, каждый по форме:
      `#[serde(tag = "outcome", rename_all = "snake_case")] enum XxxSaveOutcome { Created(XxxDto),
      NeedsConfirmation(NumberWarningDto) }` (например `DeviceSaveOutcome`,
      `ActSaveOutcome`, `CartridgeSaveOutcome`) — САМИ эти три enum'а НЕ создавать в этом плане
      (это ответственность планов 06/07/08, у которых уже есть `DeviceDto`/`ActDto`/
      `CartridgeDto`), но задокументировать здесь, в doc-комментарии модуля
      `number_template.rs`, ЭТУ КОНВЕНЦИЮ как обязательную для волны 5 — иначе три плана
      изобретут три разные формы.
    - Общий входной под-контракт для create/update запросов сущностей волны 5:
      `NumberFieldInput { value: String, template_id: Option<i64>, confirm_mismatch: bool,
      confirm_script_mix: bool }` — планы 06/07/08 встраивают его как поле `number_input:
      NumberFieldInput` в свои `*CreateDto`/`*PatchDto` вместо текущих `number_override`/
      `code_override: Option<i64/String>`.
    Зарегистрировать `pub mod number_template;` в `dto/mod.rs`.
  </action>
  <acceptance_criteria>
    - `cargo check -p trackly-app` проходит.
    - `cargo test -p trackly-app number_template_dto` — serde round-trip тест для каждого нового
      DTO (по образцу существующих `serde_round_trip_*` тестов в `dto/device.rs`).
    - `grep -n "camelCase" crates/trackly-app/src/dto/number_template.rs` находит атрибут на
      каждой публичной структуре (соответствие существующей конвенции проекта).
  </acceptance_criteria>
  <done>Все контракты определены, компилируются, конвенция NumberCheckOutcome задокументирована для планов 06-08.</done>
</task>

<task type="auto">
  <name>Task 2: NumberTemplateService — CRUD, аудит, peek_next</name>
  <files>crates/trackly-app/src/services/number_template_service.rs, crates/trackly-app/src/services/mod.rs, crates/trackly-app/tests/number_templates_crud.rs</files>
  <read_first>
    crates/trackly-app/src/services/cartridge_service.rs,
    crates/trackly-app/src/dto/number_template.rs,
    crates/trackly-infra/src/repos/number_templates_sqlite.rs
  </read_first>
  <action>
    Создать `NumberTemplateService` (поля: `writer: WriterHandle`, `readers: ReaderPool`,
    `repo: SqliteNumberTemplateRepository`, `audit_repo`, `clock` — сигнатура конструктора по
    образцу `CartridgeService::new`) с методами:
    - `async fn create(&self, template_type: TemplateTypeDto, mask: String) ->
      Result<NumberTemplateDto, AppError>` — валидация: `mask::validate_mask(&mask)` (план 02,
      текст ошибки уже готов там); пред-проверка дубля `(type, mask)` внутри той же транзакции
      (`SELECT EXISTS(...)`, текст ошибки дословно из UI-SPEC «Шаблон с такой маской для типа
      «{Тип}» уже есть.» — использовать `TemplateType::display_name_ru()` из плана 01 для `{Тип}`)
      → `repo.insert_in_tx` → `audit_repo.insert(entity_type:"number_template", action:"create")`
      по образцу `cartridge_service.rs:224-234` → вернуть `self.get(id)`.
    - `async fn update_mask(&self, id: i64, mask: String, version: i64) ->
      Result<NumberTemplateDto, AppError>` — та же валидация маски + дубль-проверка (искать дубль
      среди ДРУГИХ id того же типа), `repo.update_mask_in_tx`, аудит `action:"update"`. НЕ
      принимает `template_type` параметром вообще (D-10 на уровне сигнатуры).
    - `async fn delete(&self, id: i64) -> Result<(), AppError>` — `repo.delete_in_tx` (физическое
      удаление) + аудит `action:"delete"` (entity_id — тот же `id`, `payload_json` можно оставить
      `None`, шаблон необратимо исчезнет вместе со своими данными, что нормально при физическом
      удалении).
    - `async fn list(&self, template_type: Option<TemplateTypeDto>) ->
      Result<Vec<NumberTemplateDto>, AppError>` — для каждого шаблона вызывает
      `repo.compute_next_for_template` (сегодняшняя дата — `self.clock.unix_seconds()`), рендерит
      оба числа через `mask::render_number`, собирает `NumberTemplateDto`.
    - `async fn get(&self, id: i64) -> Result<NumberTemplateDto, AppError>` — то же для одного id.
    - `async fn peek_next(&self, template_id: i64) -> Result<NextNumberDto, AppError>` —
      `compute_next_for_template` + маппинг в `NextNumberDto` (см. правило `alt_rendered` из
      Task 1).
    - `async fn preview_mask(&self, template_type: TemplateTypeDto, mask: String, today_utc: i64)
      -> Result<NextNumberDto, AppError>` — предпросмотр БЕЗ сохранения строки в
      `number_templates` (нужен модалке создания шаблона, план 10, где маска ещё не сохранена):
      `mask::validate_mask(&mask)` → при ошибке вернуть `AppError::Validation` (модалка покажет
      текст под полем «Маска», предпросмотр — «—»); при успехе — `repo.fetch_matching_values`
      напрямую по `template_type` + `mask::parse_and_expand`/`extract_digits`/`sequence::
      compute_next` (тот же путь, что `compute_next_for_template`, но без предварительно
      сохранённой `NumberTemplateRow` — извлечь общую внутреннюю функцию `compute_next_raw
      (template_type, mask, today_utc)`, которую используют И `preview_mask`, И
      `compute_next_for_template`/`list`/`get`, чтобы не дублировать логику дважды).
    Все write-методы — внутри `self.writer.execute(move |conn| { let tx = conn.transaction()...
    })` по образцу `cartridge_service.rs:208-234` (единственный писатель, Dual access path).
  </action>
  <acceptance_criteria>
    - `crates/trackly-app/tests/number_templates_crud.rs`: создать шаблон типа «Акты» с маской
      `[YYYY]/[MM]-[X]`; он появляется в `list()` с корректным превью; редактировать маску на
      `[YYYY]-[X]`; удалить — `get()` после удаления → `NotFound`. Второй шаблон с той же маской
      и типом → `AppError::Validation`/`Conflict` с текстом, содержащим «уже есть». Попытка
      `update_mask` с чужим `version` → `OptimisticLockMismatch`.
    - `preview_mask(ActNumber, "ОРГ-[YYYY]", today)` (маска без `[X…]`) → `AppError::Validation`
      с текстом «В маске нужен порядковый номер...» — доказывает переиспользование
      `mask::validate_mask` без сохранения строки в БД (`list()` до и после вызова возвращает
      одинаковое количество шаблонов).
    <automated>cargo test -p trackly-app --test number_templates_crud</automated>
  </acceptance_criteria>
  <done>CRUD полностью рабочий, дубль отклоняется человекочитаемой ошибкой, тип неизменяем, аудит пишется на create/update/delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Память контекста, is_occupied, обнаружение предупреждений</name>
  <files>crates/trackly-app/src/services/number_template_service.rs, crates/trackly-app/tests/number_templates_compute_next.rs</files>
  <read_first>
    crates/trackly-core/src/text/homoglyphs.rs,
    crates/trackly-app/src/dto/number_template.rs
  </read_first>
  <behavior>
    - `remember_context(context, template_id: Option<i64>)` затем `get_context(context)` —
      round-trip возвращает то же значение; удаление шаблона, на который ссылался контекст,
      переводит его в `None` (проверить через реальный `delete()`, не мок).
    - `is_occupied(pool: TemplateTypeDto-подобный "какое пространство" параметр, candidate:
      &str, exclude_id: Option<i64>) -> Option<OccupyingRecordDto>` — совпадение после
      `.trim()` и без учёта регистра (SPEC NUM-09: `"орг-00-000001 "` == `"ОРГ-00-000001"`).
      `exclude_id` — чтобы правка своей же записи не считалась конфликтом сама с собой.
    - `detect_warnings(candidate: &str, pool, exclude_id) -> Option<NumberWarningDto>` —
      `homoglyphs::has_script_mix(candidate)` → `Some(NumberWarningDto{kind:ScriptMix, message:
      "В номере «{candidate}» смешаны русские и латинские буквы...", doppelganger: None})`
      (текст дословно из UI-SPEC); ЕСЛИ дополнительно среди живых записей того же пространства
      находится запись, чей `to_latin_skeleton` совпадает с `to_latin_skeleton(candidate)` — тот
      же `NumberWarningDto`, но `doppelganger: Some(...)` и текст дополняется абзацем про двойника
      (дословно из UI-SPEC). Мismatch (`kind:Mismatch`) — отдельная функция
      `check_mismatch(template: &NumberTemplateRow, candidate: &str, today_utc: i64) -> bool`,
      использующая `mask::extract_digits` (если `None` — не совпадает с маской → mismatch=true).
  </behavior>
  <action>
    Реализовать `remember_context`/`get_context` как тонкие обёртки над
    `repo.set_context_in_tx`/`repo.get_context_template_id`. Реализовать `is_occupied` и
    `detect_warnings` как описано в `<behavior>`, используя пространство-специфичные SQL-запросы
    (для `is_occupied` по устройствам/картриджам — простой `SELECT ... WHERE LOWER(TRIM(col)) =
    LOWER(TRIM(?1)) AND deleted_at_utc IS NULL AND id != ?2`; для актов — см. план 06, где D-06
    расширяет это правило на ОТОБРАЖАЕМЫЕ номера возвратов — здесь, в плане 04, реализовать
    только БАЗОВЫЙ случай по `acts.number` напрямую, план 06 обёртывает/расширяет вызовом
    отдельной функции `is_act_number_occupied_including_returns`, которая живёт в `act_service.rs`
    волны 5, а не здесь, т.к. ей нужен `format_act_number` — специфика домена актов). Пометить
    doc-комментарием в этом файле, что план 06 расширит проверку для актов.
  </action>
    <acceptance_criteria>
    См. `<behavior>` выше — каждый перечисленный сценарий соответствует отдельному тесту.
    <automated>cargo test -p trackly-app --test number_templates_compute_next</automated>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test number_templates_compute_next</automated>
  </verify>
  <done>Память контекста, occupied-проверка (trim+без регистра) и оба вида предупреждений реализованы и покрыты тестами; текст предупреждений дословно совпадает с UI-SPEC Copywriting Contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri/HTTP запрос (ещё не подключён — план 05) → NumberTemplateService | Все параметры уже прошли через `authorize()` на уровне транспорта в плане 05; этот сервис сам НЕ проверяет права — предположение, которое план 05 обязан не нарушить |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-07 | Elevation of Privilege | `NumberTemplateService` методы вызываются без встроенной проверки прав | mitigate | Каждый публичный метод сервиса вызывается ИСКЛЮЧИТЕЛЬНО из `build_*` функций плана 05, которые сами делают `authorize()` до вызова — задокументировано doc-комментарием на уровне модуля сервиса, чтобы будущий вызывающий код не забыл гейт |
| T-40.2-08 | Tampering | `is_occupied`/`detect_warnings` — сравнение case-insensitive/trim | mitigate | SQL использует `LOWER(TRIM(...))` на обеих сторонах сравнения — гарантирует, что визуально идентичные, но байтово разные номера не проскакивают мимо проверки (закрывает Pitfall 4 из RESEARCH — `normalize_str` НЕ используется здесь, применяется отдельный явный `.trim()`) |
</threat_model>

<verification>
`cargo test -p trackly-app number_template` (все три тестовых файла) зелёные. `cargo clippy -p
trackly-app -- -D warnings` чист.
</verification>

<success_criteria>
`NumberTemplateService` — полный владелец бизнес-логики шаблонов: CRUD с аудитом, память 5
контекстов, occupied-проверка и оба вида предупреждений (script-mix/mismatch), с
задокументированной конвенцией `*SaveOutcome` для волны 5.
</success_criteria>

<output>
Create `.planning/phases/40.2-inventory-number-templates/40.2-04-SUMMARY.md` when done
</output>
