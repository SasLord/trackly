---
phase: 40.2-inventory-number-templates
plan: 15
subsystem: ui
tags: [svelte5, number-templates, cartridges, drums, modal]

# Dependency graph
requires:
  - phase: 40.2-07
    provides: "CartridgeService::create fully on NumberTemplateService (occupied->mismatch->script-mix chain already implemented server-side), CartridgeSaveOutcome::{Created, NeedsConfirmation}, cartridge_create/drum_create share ONE physical code space (NUM-09 'в') but separate NUM-08 context memory"
  - phase: 40.2-11
    provides: "NumberTakenPopup/NumberMismatchPopup/NumberScriptWarningPopup (presentational, D-01 chain)"
  - phase: 40.2-12
    provides: "NumberTemplateField.svelte (autofill, ↑/↓, «Вставка» menu, live «Занят» hint, focus(), onSelectedTemplateChange(id, mask), and — critically for this plan — contextAlwaysReplaces('cartridge_create'|'drum_create') already returning true, so a kindId-driven context switch unconditionally replaces the field's value with no extra plumbing needed)"
  - phase: 40.2-13
    provides: "onPopupChange-to-parent-shell pattern for D-01 popup wiring (DeviceFormBody/DeviceFormModal precedent, backdrop-filter containing-block trap workaround)"
provides:
  - "CartridgeFormBody.svelte create mode on NumberTemplateField, context reactive to kindId (cartridge_create<->drum_create), full occupied->mismatch->script-mix chain via cartridges.create's CartridgeSaveOutcome (NUM-06/07/08/10/11); switching «Что добавляем» unconditionally replaces the code with the new kind's first free number (NUM-08 Discretion #8), verified to already be handled entirely by NumberTemplateField's own Plan-12 contextAlwaysReplaces() logic — no new prop or {#key} remount needed"
  - "CartridgeFormModal.svelte renders NumberTakenPopup/NumberMismatchPopup/NumberScriptWarningPopup as top-level Modal siblings (4th application of the onPopupChange-to-parent-shell split, same backdrop-filter containing-block trap as Device/Act FormModal)"
  - "ui/src/features/cartridges/api.ts create() widened to CartridgeSaveOutcome (real backend return type since Plan 07, previously loosely cast to CartridgeDto by Plan 07's own compat shim)"
  - "Phase 40.2 fully closed: all 16 NUM-* requirements now Complete in REQUIREMENTS.md (NUM-06/07/08/10/11/14 marked complete by this plan's own closing pass — see key-decisions)"
affects: []

tech-stack:
  added: []
  patterns:
    - "Fourth and final application of the onPopupChange-to-parent-shell split (Plan 13 devices/printers, Plan 14 acts, this plan cartridges/drums) — proven across every entity family the phase touches"

key-files:
  created: []
  modified:
    - ui/src/features/cartridges/CartridgeFormBody.svelte
    - ui/src/features/cartridges/CartridgeFormModal.svelte
    - ui/src/features/cartridges/api.ts

key-decisions:
  - "Task 1's own <action> text anticipated needing a new `forceRefillOnContextChange` prop on NumberTemplateField OR wrapping it in `{#key context}` to force the unconditional-replace-on-kind-switch behaviour (NUM-08 Discretion #8). Neither was needed: reading NumberTemplateField.svelte (Plan 12) showed its own `$effect` already calls `applyContextDefault(ctx, !contextAlwaysReplaces(ctx))` on every context change, and `contextAlwaysReplaces()` already returns `true` for exactly `'cartridge_create'`/`'drum_create'` (Plan 12's own Discretion #8 implementation, built in anticipation of this exact plan). Passing a `$derived` `context` prop reactive to `kindId` was sufficient — verified this claim by reading the component source rather than assuming the plan's own two suggested options were required."
  - "CartridgeFormModal.svelte was edited even though the plan's own `files_modified` only named CartridgeFormBody.svelte + api.ts — same Rule-1 rationale as Plans 13/14 (DeviceFormModal.svelte/ActFormModal.svelte): nesting NumberTakenPopup/NumberMismatchPopup/NumberScriptWarningPopup's `<Modal>` inside CartridgeFormBody would place their `position: fixed` backdrop inside the OUTER create/edit Modal's `.modal-backdrop` (`backdrop-filter: blur(2px)`, a documented containing-block trap). CartridgeFormBody now owns D-01 orchestration and reports it via a new `onPopupChange` callback; CartridgeFormModal renders the popups as top-level `Modal` siblings, identical to the Device/Act precedent."
  - "Tasks 1 and 2 were implemented and committed together in a single commit, mirroring Plans 12/13/14's own documented rationale — both share the same `activePopup`/`pendingConfirm`/`selectedTemplateId` state and the same `handleSubmit`/`submitCreate` control flow; splitting would produce a temporarily non-compiling intermediate state with no verification value."
  - "`submitEdit()` is UNCHANGED from before this plan — code stays a disabled plain `Input` in edit mode, no D-01/D-05 chain added to it, per the plan's own `must_haves.truths` (\"Правка картриджа/фотобарабана не меняется — код не редактируется, out of scope\"). This differs from Plan 13/14's edit paths, which DO get an occupied+script-mix chain on submit (D-05) — cartridges/drums have no editable-number D-05 concept at all, because the code field is fully disabled in edit (Plan 07's own decision, unchanged by this plan)."
  - "T-40.2-25 (threat model, disposition `accept`): switching `kindId` after the user has already confirmed a mismatch/script-mix warning for the OLD code does not explicitly reset `pendingConfirm.{mismatch,scriptMix}` in `handleKindChange()`. This mirrors the exact same (also unaddressed) situation already present in `DeviceFormBody.svelte`/`ActFormBody.svelte` for a manual value edit after a confirmed warning — neither prior plan resets `pendingConfirm` on value change either. Accepted per the threat model's own disposition: `confirmMismatch`/`confirmScriptMix` only ever skip RE-SHOWING an already-approved *informational* warning (never the `occupied` check, which is unconditional and DB-enforced via `idx_cartridges_code_live`), so a stale flag's worst case is silently skipping a mismatch/script-mix popup that would have fired for the NEW code — a UX gap, not a data-integrity or security bypass. Fixing this uniformly across all three forms is out of this plan's scope (not caused by this plan, pre-existing pattern)."
  - "Marked NUM-06/07/08/10/11/14 complete in REQUIREMENTS.md as part of this plan's closing pass (Phase 40.2's last plan), per this session's explicit project_rules instruction — NOT a literal copy of this plan's own frontmatter `requirements` list (which does not mention NUM-14 at all). NUM-06/07/08/10/11 each explicitly name all four create-popup families (devices/printers, acts, cartridges/drums) or all 5 NUM-08 contexts; devices/printers were completed by Plan 13, acts by Plan 14, cartridges/drums by this plan — the last surface for each is now delivered. NUM-14 only names acts (\"Номер акта передачи...\") and has NO cartridge/drum surface to wait for at all; it was already fully deliverable after Plan 14 but Plan 14's own SUMMARY conservatively deferred it to \"until Plan 15\" without re-checking whether its wording actually depended on cartridges. Re-reading NUM-14's literal text confirmed it does not, so it is closed now, in this session, as instructed."

patterns-established: []

requirements-completed: [NUM-06, NUM-07, NUM-08, NUM-10, NUM-11, NUM-14]

duration: ~1h
completed: 2026-09-22
---

# Phase 40.2 Plan 15: Cartridges/Drums on NumberTemplateField + D-01 popup chain Summary

**`CartridgeFormBody.svelte`'s create mode now uses `NumberTemplateField` (context reactive to `kindId`, unconditional value replacement on cartridge<->drum switch already built into the field from Plan 12) plus `cartridges.create`'s full occupied→mismatch→script-mix chain (already implemented server-side since Plan 07) — the last of the three wiring plans, closing all 16 NUM-\* requirements of Phase 40.2.**

## Performance

- **Duration:** ~1h (incl. an extended wait for a full-workspace `cargo test -p trackly-app` due-diligence run — not required by this plan's own `<verification>`, since no Rust files were touched, but run anyway for confidence)
- **Completed:** 2026-09-22
- **Tasks:** 2/3 completed autonomously (Task 3 — `checkpoint:human-verify`, gate="blocking" — deferred, see below, per project checkpoint policy)
- **Files modified:** 3 (0 created)

## Accomplishments

- `ui/src/features/cartridges/CartridgeFormBody.svelte`: create-mode «Код» is now `NumberTemplateField` (`context` = `$derived(kindId === 2 ? 'drum_create' : 'cartridge_create')`, reactive to the «Что добавляем» toggle); edit mode keeps the pre-existing disabled plain `Input` (code is not editable via this form — SPEC boundary, unchanged). `handleSubmit` split into `submitCreate()` (occupied → mismatch → script-mix → success, via `cartridges.create`'s `CartridgeSaveOutcome`) and `submitEdit()` (unchanged `cartridges.update` call), sharing one `activePopup`/`pendingConfirm` state machine — identical structure to `DeviceFormBody.svelte`'s Plan 13 / `ActFormBody.svelte`'s Plan 14 orchestration.
- Switching «Что добавляем» (Картридж↔Фотобарабан) unconditionally replaces the code with the new kind's first free number — this is `NumberTemplateField`'s OWN pre-existing behaviour (`contextAlwaysReplaces()`, Plan 12), triggered automatically by the reactive `context` prop; `handleKindChange()` needed no new code for the number field itself (documented in Decisions Made — the plan's own two suggested implementation options, a new prop or `{#key context}`, were both unnecessary).
- `ui/src/features/cartridges/CartridgeFormModal.svelte`: renders `NumberTakenPopup`/`NumberMismatchPopup`/`NumberScriptWarningPopup` as top-level `Modal` siblings driven by a new `onPopupChange` callback from `CartridgeFormBody` — same pattern as `DeviceFormModal.svelte`/`ActFormModal.svelte` (Plans 13/14), avoiding the same documented `backdrop-filter` containing-block trap.
- `ui/src/lib/api/cartridges.ts` (`ui/src/features/cartridges/api.ts`): `create()` retyped `CartridgeSaveOutcome` (its real backend return type since Plan 07).
- Full D-01 chain (occupied → mismatch → script-mix) now wired for the create popup, mirroring the cartridge/drum shared-code-space-separate-context-memory design from Plan 07 (`toTakenRecordSummary`'s fallback kind is `kindId`-derived; `mismatchContextLabel` computed from the CURRENT `kindId` at save-attempt time per the plan's own `must_haves.truths`).
- All 16 NUM-\* requirements of Phase 40.2 are now `[x]` Complete in `REQUIREMENTS.md` (see key-decisions for the NUM-14 rationale).

## Task Commits

Tasks 1-2 (NumberTemplateField wiring + create-chain orchestration) were implemented and committed together in a single commit — they share the same `activePopup`/`pendingConfirm`/`selectedTemplateId` state and the same `handleSubmit`/`submitCreate` control flow, mirroring Plans 12/13/14's own documented rationale for combining equivalent tasks:

1. **Tasks 1-2: NumberTemplateField + D-01 popup chain (create only, edit unchanged)** - `6d2af9bb` (feat)

**Plan metadata:** committed alongside this SUMMARY (see final commit below)

## Files Created/Modified

- `ui/src/features/cartridges/CartridgeFormBody.svelte` — `<script module>` export of `CartridgeScriptWarningDoppelganger`/`CartridgeNumberPopupState`; `numberContext` derived from `kindId`; popup orchestration state + helpers (`isConflictError`, `toTakenRecordSummary`, `openTakenPopup`, `closeTakenPopup`, `handleTakeNext`, `openMismatchPopup`, `closeMismatchPopup`, `continueMismatch`, `openScriptWarningPopup`, `closeScriptWarningPopup`, `continueScriptWarning`, `focusNumberField`); `submitCreate`/`submitEdit`; create-mode «Код» field swapped to `NumberTemplateField`; removed the now-inaccurate "введите код вручную" hint text (`NumberTemplateField`'s own status line replaces it)
- `ui/src/features/cartridges/CartridgeFormModal.svelte` — `numberPopup` state, `onPopupChange` wiring, three new top-level `{#if}` blocks rendering `NumberTakenPopup`/`NumberMismatchPopup`/`NumberScriptWarningPopup`
- `ui/src/features/cartridges/api.ts` — `create()` retyped `CartridgeSaveOutcome`

## Decisions Made

See `key-decisions` in frontmatter above (5 decisions): NumberTemplateField's own Plan-12 `contextAlwaysReplaces()` already handling the unconditional-replace-on-kind-switch requirement (no new prop/`{#key}` needed), extending file scope to `CartridgeFormModal.svelte` for the backdrop-filter trap (Rule 1, same precedent as Plans 13/14), combining Tasks 1-2 into one commit, `submitEdit()` staying fully unchanged (SPEC boundary), the `pendingConfirm`-not-reset-on-kind-switch acceptance (T-40.2-25, mirrors an existing unaddressed pattern in Device/ActFormBody), and the NUM-06/07/08/10/11/14 REQUIREMENTS.md closing pass (including NUM-14, which this plan's own frontmatter does not list, per this session's explicit instruction).

## Deviations from Plan

None from Rules 1-3 — implementation followed the plan's `<action>` text and `must_haves.truths` exactly. The one departure from the plan's own suggested implementation approach (a new `forceRefillOnContextChange` prop or `{#key context}` for Task 1) was a **simplification**, not a deviation requiring a rule: reading `NumberTemplateField.svelte`'s existing source showed the unconditional-replace behaviour was already built in by Plan 12 specifically for this scenario — using it directly is following the plan's own `must_haves.truths` more precisely than either of its two suggested (and unnecessary) implementation paths. Extending file scope to `CartridgeFormModal.svelte` is documented as a decision above (Rule 1 — avoiding a real, previously-diagnosed positioning bug), following the exact precedent Plans 13/14 already established for this same codebase.

**Total deviations:** 0 auto-fixed (Rules 1-3 sense) — 1 documented Rule-1-precedent file-scope extension (`CartridgeFormModal.svelte`), consistent with Plans 13/14.
**Impact on plan:** No scope creep, no architecture invented — the file-scope extension follows an already-established codebase pattern verbatim.

## Known Stubs

None. `submitEdit()`'s code field stays disabled/non-editable exactly as before this plan (SPEC boundary, not a stub — the plan explicitly excludes edit-mode code changes from scope).

## Threat Flags

None beyond the plan's own `<threat_model>` (T-40.2-25, `accept` disposition — see Decisions Made). No new network endpoints, auth paths, or schema changes were introduced; this plan wires already-existing, already-tested server-side D-01 chain endpoints (Plan 07) into a fourth UI form.

## Issues Encountered

None. Verification (`pnpm --dir ui lint`, `pnpm --dir ui exec svelte-check`, `pnpm --dir ui build`, `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test cartridges_numbering`) all passed cleanly on the first attempt. A full-workspace `cargo test -p trackly-app -- --skip login_remember_persistent_cookie` due-diligence run was also kicked off (not required by this plan's `<verification>`, since zero Rust files were touched) — it ran to completion, exit code 0 (no failures); the last visible tests (`ws_upgrade_serve_connection`'s 2/2, the `trackly_app` doc-test) all passed. The run's full pass/fail count line was not captured (piped through `tail -30`, same truncation pitfall Plan 14's SUMMARY warned about — but unlike that case, exit code 0 alone is sufficient here since this plan touched zero Rust files and could not have introduced a Rust regression).

## User Setup Required

None - no external service configuration required.

## Deferred UAT (UNVERIFIED)

Task 3 (`checkpoint:human-verify`, gate="blocking") requires live verification in the running app (`cargo tauri dev`) — per project rule (checkpoint_policy) this session does not auto-approve UI-behavior checkpoints or use a synthetic harness, regardless of `auto_advance`/`yolo` config flags. The plan is otherwise complete; live verification remains outstanding, and — since this is the phase's last plan — should be combined with Plans 13/14's own still-outstanding deferred UAT into one live pass. Exact steps from this plan:

1. Открыть «Новый картридж» — код подставляется по шаблону «Картриджи» (например `C-0006`, если есть `C-0001..C-0005`).
2. Переключить «Что добавляем» на «Фотобарабан» — код МЕНЯЕТСЯ на первое свободное шаблона «Фотобарабаны» (например `D-0002`), даже если вы до этого вручную поправили код картриджа.
3. Провести полную цепочку попапов (занят/не по шаблону/кир-лат), как в предыдущих планах.
4. Убедиться, что существующая БД с картриджами `C-0001..C-0042` (если доступна тестовая среда с такими данными) предлагает `C-0043` — континуитет NUM-13.

Also carried over from Plans 13/14's own still-outstanding deferred UAT (not re-verified by this plan, listed here for the combined pass):
- Devices/printers (Plan 13): template CRUD, autofill/«Вставка», occupied/mismatch/script-mix popups, quantity>1 field hide, printer template sharing, edit-mode occupied+script-mix-only (no mismatch/no template picker).
- Acts (Plan 14): `[YYYY]/[MM]-[X]` mask format, full popup chain, edit-mode occupied-without-take-next, return-number (`Nв`) blocking a new act with the same number (D-06).
- All three plans' own note: `NumberTakenPopup`/`NumberMismatchPopup`/`NumberScriptWarningPopup` must be confirmed to render correctly centered/full-viewport on top of each create/edit Modal, not visually clipped by the outer Modal's `backdrop-filter` (compile gates cannot see this — `compile_gates_miss_svelte_runtime`).

## Next Phase Readiness

- Phase 40.2 (inventory-number-templates) is now fully implemented end-to-end across all four entity families (devices, printers, acts, cartridges, drums) — all 16 NUM-\* requirements are `[x]` Complete in `REQUIREMENTS.md`.
- The one remaining gate before the phase can be considered fully verified (not just fully implemented) is the combined live UAT pass across Plans 13/14/15 listed above.
- No blockers for closing the phase pending that UAT.

---
*Phase: 40.2-inventory-number-templates*
*Completed: 2026-09-22*

## Self-Check: PASSED

- `[ -f ui/src/features/cartridges/CartridgeFormBody.svelte ]` → FOUND (modified)
- `[ -f ui/src/features/cartridges/CartridgeFormModal.svelte ]` → FOUND (modified)
- `[ -f ui/src/features/cartridges/api.ts ]` → FOUND (modified)
- `git log --oneline --all | grep 6d2af9bb` → FOUND
- `pnpm --dir ui exec svelte-check` → 0 errors, 67 pre-existing-pattern warnings (up from 64 at Plan 12; the 3 new ones in `CartridgeFormBody.svelte` are the same `state_referenced_locally` pattern already present in `DeviceFormBody.svelte`/`ActFormBody.svelte`, not new categories of warning)
- `pnpm --dir ui lint` → clean (eslint, prettier, all `check-*.mjs` privacy/token/contrast/focus gates PASS)
- `pnpm --dir ui build` → clean, 0 new warnings from the changed files
- `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test cartridges_numbering` → 6/6 passed (Plan 07's regression suite unaffected — no Rust files touched by this plan)
- `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie` (full workspace, due diligence only) → exit code 0, no failures
- `git commit` privacy pre-commit hook (`[check-privacy]`) → PASS on the feature commit
