---
phase: 40.2-inventory-number-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V041__number_templates.sql
  - crates/trackly-core/src/domain/number_templates.rs
  - crates/trackly-core/src/domain/mod.rs
  - crates/trackly-core/src/ports/number_templates.rs
  - crates/trackly-core/src/ports/mod.rs
  - crates/trackly-infra/tests/number_templates_migration.rs
autonomous: true
requirements: [NUM-01, NUM-13]

must_haves:
  truths:
    - "Таблица counters удалена в той же миграции, что вводит шаблоны — не отдельным шагом (D-15)"
    - "Свежая БД и обновлённая БД одинаково засевают шаблоны [X] (Акты), C-[XXXX] (Картриджи), D-[XXXX] (Фотобарабаны) шириной 4, без переписывания существующих кодов (D-16)"
    - "Память контекста хранит id шаблона через FK ON DELETE SET NULL, а не копию маски (D-17)"
  artifacts:
    - path: "migrations/V041__number_templates.sql"
      provides: "number_templates + number_template_contexts таблицы, засев 3 шаблонов и 5 контекстов, DROP TABLE counters"
    - path: "crates/trackly-core/src/domain/number_templates.rs"
      provides: "TemplateType, TemplateContext, NumberTemplateRow, NextNumberResult — контракты для всей фазы"
    - path: "crates/trackly-core/src/ports/number_templates.rs"
      provides: "NumberTemplateRepository trait (CRUD + fetch_matching_values + context get/set)"
  key_links:
    - from: "migrations/V041__number_templates.sql"
      to: "crates/trackly-core/src/domain/number_templates.rs"
      via: "TemplateType::as_str()/from_str() значения совпадают с CHECK(type IN (...)) в SQL"
      pattern: "device_inventory|act_number|cartridge_code|drum_code"
---

<objective>
Заложить фундамент всей фазы 40.2: таблицы `number_templates` + `number_template_contexts`,
засев трёх шаблонов, воспроизводящих текущую нумерацию (D-16), отказ от `counters` (D-15), и
Rust-контракты (`TemplateType`, `TemplateContext`, `NumberTemplateRow`, `NextNumberResult`,
`NumberTemplateRepository` trait), на которые будут ссылаться все последующие 14 планов фазы.

Purpose: интерфейс-контракты вперёд реализации — ни один следующий план не должен изобретать
форму данных заново.
Output: миграция V041, `trackly-core::domain::number_templates`, `trackly-core::ports::number_templates`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40.2-inventory-number-templates/40.2-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-CONTEXT.md
@.planning/phases/40.2-inventory-number-templates/40.2-RESEARCH.md
@.planning/phases/40.2-inventory-number-templates/40.2-PATTERNS.md

<interfaces>
Существующие аналоги, которые нужно повторить формой (не копировать код):
- `crates/trackly-core/src/auth.rs::Role` — паттерн `enum` + `as_str()`/`from_str()` возвращающий
  `AppError::Validation` на неизвестное значение. Повторить один в один для `TemplateType` и
  `TemplateContext`.
- `crates/trackly-core/src/ports/acts.rs` и `ports/cartridges.rs` — форма port-трейта:
  `type Conn;` + методы, принимающие `&Self::Conn`. Никаких tokio/rusqlite/serde импортов в
  `trackly-core` (гейт `no_io_deps`).
- `migrations/V030__printers_drop_connectivity_check.sql` — эталон 12-шагового rebuild (не
  нужен в этом плане — он для V042/V043 других планов волны 5 — но `PRAGMA foreign_keys`
  дисциплина и комментарий-преамбула стоит повторить в V041 тоже, для единообразия).
- `migrations/V038__places_migrate_devices_acts_cartridges.sql` — пример `ALTER TABLE ... ADD
  COLUMN ... REFERENCES ... ON DELETE ...` в этом же стиле для новых FK.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rust-контракты — TemplateType, TemplateContext, NumberTemplateRow, NextNumberResult, порт</name>
  <files>
    crates/trackly-core/src/domain/number_templates.rs,
    crates/trackly-core/src/domain/mod.rs,
    crates/trackly-core/src/ports/number_templates.rs,
    crates/trackly-core/src/ports/mod.rs
  </files>
  <read_first>
    crates/trackly-core/src/auth.rs,
    crates/trackly-core/src/ports/acts.rs,
    crates/trackly-core/src/ports/cartridges.rs,
    crates/trackly-core/src/domain/mod.rs,
    crates/trackly-core/src/ports/mod.rs,
    crates/trackly-core/src/error.rs
  </read_first>
  <action>
    В `domain/number_templates.rs` определить:
    - `TemplateType` enum с ровно 4 вариантами: `DeviceInventory`, `ActNumber`, `CartridgeCode`,
      `DrumCode`. `as_str()` возвращает `"device_inventory"`, `"act_number"`, `"cartridge_code"`,
      `"drum_code"` — эти строки станут значениями `CHECK(type IN (...))` в V041 (Task 2), менять
      их отдельно от миграции нельзя. `from_str(s: &str) -> Result<Self, AppError>` по образцу
      `Role::from_str` (`AppError::Validation{field:"template_type"}` на неизвестное значение).
      Добавить `display_name_ru(&self) -> &'static str` — «Устройства и принтеры» / «Акты» /
      «Картриджи» / «Фотобарабаны» (Copywriting Contract UI-SPEC §Copywriting) — фронтенд и
      Rust-тесты не должны хардкодить эти строки в двух местах порознь.
    - `TemplateContext` enum с ровно 5 вариантами: `DeviceCreate`, `PrinterCreate`, `ActCreate`,
      `CartridgeCreate`, `DrumCreate`. `as_str()` → `"device_create"` / `"printer_create"` /
      `"act_create"` / `"cartridge_create"` / `"drum_create"` (совпадают с
      `CHECK(context IN (...))` в V041). `from_str` аналогично. `template_type(&self) ->
      TemplateType` — `DeviceCreate`/`PrinterCreate` → `DeviceInventory`, `ActCreate` →
      `ActNumber`, `CartridgeCreate` → `CartridgeCode`, `DrumCreate` → `DrumCode` (эта функция —
      единственный источник истины «какой тип шаблонов показывать в меню „Вставка“ для данного
      попапа», без неё Task 3+ в других планах будут дублировать матрицу).
    - `NumberTemplateRow { id: i64, template_type: TemplateType, mask: String, created_at_utc:
      i64, updated_at_utc: i64, version: i64 }`.
    - `NextNumberResult { first_free: u64, max_plus_one: u64, has_gap: bool, overflowed: bool }`
      — `has_gap = first_free != max_plus_one`; `overflowed = true` только когда в разрядности
      `[X…N]` не осталось ни одного свободного числа (для безразмерного `[X]` `overflowed`
      всегда `false`, разрядность не ограничена). Задокументировать это правило в doc-комментарии
      — от него зависят NUM-04/NUM-05 во всех последующих планах.
    Все три структуры/enum'а — `#[derive(Debug, Clone, PartialEq, Eq)]`, без `serde`/`specta`
    (это `trackly-core`, DTO с `specta::Type` будут в `trackly-app::dto::number_template`, план 04).
    Зарегистрировать `pub mod number_templates;` в `domain/mod.rs`.

    В `ports/number_templates.rs` определить `NumberTemplateRepository` trait (форма — как
    `ActRepository`/`CartridgeRepository`, `type Conn;` + read-методы напрямую, write — как
    `*_in_tx` без обязательства быть в трейте если это упростит вызов из транзакции сервиса —
    оставить на усмотрение плана 03, здесь фиксируем ЧИТАЮЩУЮ часть трейта, т.к. write-часть
    зависит от деталей транзакции, которые решает план 03):
    - `fn get(&self, conn: &Self::Conn, id: i64) -> Result<NumberTemplateRow, AppError>` —
      `NotFound` если нет/удалён (но шаблоны физически удаляются, D-12 — значит просто "нет
      строки").
    - `fn list(&self, conn: &Self::Conn, template_type: Option<TemplateType>) ->
      Result<Vec<NumberTemplateRow>, AppError>` — без пагинации (шаблонов всегда мало).
    - `fn get_context_template_id(&self, conn: &Self::Conn, context: TemplateContext) ->
      Result<Option<i64>, AppError>`.
    - `fn fetch_matching_values(&self, conn: &Self::Conn, template_type: TemplateType, today_utc:
      i64) -> Result<Vec<String>, AppError>` — возвращает СЫРЫЕ текстовые значения живых записей
      соответствующего пространства (devices.inventory_number для `DeviceInventory`, acts.number
      ТОЛЬКО `act_type='handover'` для `ActNumber` — возвраты в последовательность шаблона не
      входят, SPEC NUM-14 — cartridges.code для `CartridgeCode`/`DrumCode`, БЕЗ фильтра по
      kind_id — обе колонки читают весь столбец `cartridges.code`, различие типов существует
      только для CRUD/UI-группировки, а не для физического разделения данных: два шаблона с
      совпадающими масками у «Картриджи» и «Фотобарабаны» закономерно увидят одну и ту же
      последовательность, т.к. делят одну колонку — это ожидаемое поведение, не баг). Реализация
      метода — план 03; здесь только сигнатура и doc-комментарий с этим объяснением, т.к. это
      неочевидное архитектурное решение, которое иначе будет переизобретено неправильно.
    Зарегистрировать `pub mod number_templates;` в `ports/mod.rs`. Прогнать `cargo check -p
    trackly-core` — компилируется, `no_io_deps` гейт не нарушен (никаких `use rusqlite` /
    `use tokio` / `use serde` в этих двух новых файлах).
  </action>
  <acceptance_criteria>
    - `cargo check -p trackly-core` проходит без предупреждений о неиспользуемом коде (добавить
      `#[allow(dead_code)]` временно недопустимо — использовать `pub` и полагаться на то, что
      следующие планы потребят API; если clippy ругается на unused, добавить минимальный unit-тест
      ниже, который использует все публичные функции хотя бы раз).
    - `grep -rn "use rusqlite\|use tokio\|use serde" crates/trackly-core/src/domain/number_templates.rs crates/trackly-core/src/ports/number_templates.rs` возвращает пусто.
    - Юнит-тесты в `domain/number_templates.rs` (модуль `#[cfg(test)] mod tests`): round-trip
      `TemplateType::from_str(t.as_str()) == Ok(t)` для всех 4 вариантов; то же для
      `TemplateContext` (5 вариантов); `TemplateContext::template_type()` возвращает верное
      сопоставление для всех 5; `NextNumberResult` с `first_free==max_plus_one` даёт
      `has_gap==false`.
  </acceptance_criteria>
  <done>Контракты существуют, компилируются, покрыты unit-тестами на round-trip и сопоставление контекст→тип.</done>
</task>

<task type="auto">
  <name>Task 2: Миграция V041 — таблицы шаблонов, засев, DROP counters</name>
  <files>migrations/V041__number_templates.sql</files>
  <read_first>
    migrations/V009__counters.sql,
    migrations/V017__drum_states_and_counter.sql,
    migrations/V030__printers_drop_connectivity_check.sql,
    migrations/V038__places_migrate_devices_acts_cartridges.sql,
    crates/trackly-core/src/domain/number_templates.rs
  </read_first>
  <action>
    Создать `migrations/V041__number_templates.sql` (преамбула-комментарий по стилю V030/V038,
    объясняющая D-15/D-16/D-17 одной фразой каждое):
    1. `CREATE TABLE number_templates (id INTEGER PRIMARY KEY AUTOINCREMENT, type TEXT NOT NULL
       CHECK (type IN ('device_inventory','act_number','cartridge_code','drum_code')), mask TEXT
       NOT NULL, created_at_utc INTEGER NOT NULL, updated_at_utc INTEGER NOT NULL, version
       INTEGER NOT NULL DEFAULT 1, UNIQUE(type, mask));` — значения `type` должны буквально
       совпадать с `TemplateType::as_str()` из Task 1.
    2. `CREATE TABLE number_template_contexts (context TEXT PRIMARY KEY CHECK (context IN
       ('device_create','printer_create','act_create','cartridge_create','drum_create')),
       template_id INTEGER NULL REFERENCES number_templates(id) ON DELETE SET NULL,
       updated_at_utc INTEGER NOT NULL);` — `ON DELETE SET NULL` реализует D-17 буквально: при
       физическом удалении шаблона контекст сам обнуляется без кода сервиса.
    3. `INSERT INTO number_template_contexts (context, template_id, updated_at_utc) VALUES` — все
       5 контекстов, `template_id = NULL` для `device_create`/`printer_create` (D-13: для
       устройств/принтеров шаблона по умолчанию нет), реальные id проставляются шагом 5 ниже для
       остальных трёх.
    4. Засев трёх шаблонов (D-16, ширина 4 — НЕ 6, зафиксировано решением 2026-09-18, см.
       `40.2-CONTEXT.md` D-16 "Уточнение"): `('act_number', '[X]', ...)`,
       `('cartridge_code', 'C-[XXXX]', ...)`, `('drum_code', 'D-[XXXX]', ...)`. Использовать
       текущий unix-время нельзя (миграция не имеет доступа к функции приложения) — использовать
       `strftime('%s','now')` в SQL для `created_at_utc`/`updated_at_utc` (существующий приём в
       этом кодовой базе — проверить `grep -n "strftime" migrations/*.sql` на реальный пример
       перед использованием; если приёма нет, взять литеральную секундную метку, соответствующую
       дате написания миграции — оба варианта приемлемы, миграция запускается один раз).
    5. `UPDATE number_template_contexts SET template_id = (SELECT id FROM number_templates WHERE
       type='act_number' AND mask='[X]'), updated_at_utc = ... WHERE context='act_create';` и
       аналогично для `cartridge_create`→`cartridge_code`/`C-[XXXX]`,
       `drum_create`→`drum_code`/`D-[XXXX]`.
    6. `DROP TABLE counters;` (D-15 — в этой же миграции, не отдельной).
    7. `PRAGMA user_version = 41;`
    Важно: шаги 1–6 должны отработать одинаково и на пустой БД (свежая установка), и на БД с
    существующими картриджами/актами — засев НЕ зависит от наличия данных в `devices`/`acts`/
    `cartridges` (в отличие от продолжения нумерации, которое посчитает `compute_next` позже по
    факту существующих строк, а не во время этой миграции).
  </action>
  <acceptance_criteria>
    - `sqlite3 :memory: < migrations/V041__number_templates.sql` (после ручного `ATTACH`
      предыдущих 40 миграций, либо через тест из Task 3) не падает.
    - `SELECT type, mask FROM number_templates ORDER BY type` после миграции даёт ровно 3 строки:
      `act_number|[X]`, `cartridge_code|C-[XXXX]`, `drum_code|D-[XXXX]`.
    - `SELECT context, template_id FROM number_template_contexts ORDER BY context` даёт 5 строк;
      `device_create`/`printer_create` → `template_id IS NULL`; остальные три → NOT NULL,
      указывают на соответствующую строку из предыдущего пункта.
    - `SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name='counters'` == 0.
  </acceptance_criteria>
  <done>V041 применяется идемпотентно, засевает ровно предписанные данные, `counters` больше не существует как таблица.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Миграционный тест V041 (схема + засев + идемпотентность)</name>
  <files>crates/trackly-infra/tests/number_templates_migration.rs</files>
  <read_first>
    crates/trackly-infra/tests/place_movements_migration.rs,
    crates/trackly-infra/tests/migration_idempotency.rs,
    crates/trackly-infra/src/db/migrations.rs,
    migrations/V041__number_templates.sql
  </read_first>
  <behavior>
    - Тест 1: свежая БД → `number_templates` содержит ровно 3 строки с ожидаемыми (type, mask);
      `number_template_contexts` содержит ровно 5 строк с ожидаемым сопоставлением контекст→маска
      (`act_create`→`[X]`, `cartridge_create`→`C-[XXXX]`, `drum_create`→`D-[XXXX]`,
      `device_create`/`printer_create`→NULL).
    - Тест 2: таблица `counters` отсутствует в `sqlite_master` после миграции.
    - Тест 3: `number_template_contexts.template_id` действительно `ON DELETE SET NULL` —
      `DELETE FROM number_templates WHERE type='act_number'` → `SELECT template_id FROM
      number_template_contexts WHERE context='act_create'` возвращает `NULL` (доказывает D-17 на
      уровне схемы, а не только по чтению DDL).
    - Тест 4: повторный прогон `migrations::run` на той же БД — 0 применённых миграций,
      `user_version` не меняется (мирроринг `place_movements_migration.rs`'s
      `place_movements_migration_reruns_idempotently`).
  </behavior>
  <action>
    Написать `crates/trackly-infra/tests/number_templates_migration.rs`, копируя каркас
    `fresh_migrated_db` из `place_movements_migration.rs` (тот же `TempDir` + `pragmas::
    apply_writer_pragmas` + `migrations::run`) — не изобретать новый харнесс. Четыре теста из
    `<behavior>` выше как отдельные `#[test] fn`.
  </action>
    <acceptance_criteria>
    См. `<behavior>` выше — каждый перечисленный сценарий соответствует отдельному тесту.
    <automated>cargo test -p trackly-infra --test number_templates_migration</automated>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra --test number_templates_migration</automated>
  </verify>
  <done>4 теста зелёные; тест на ON DELETE SET NULL реально удаляет строку и проверяет каскад, а не просто читает DDL текстом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → SQLite | Миграция выполняется один раз с правами процесса-владельца БД, вход не приходит от пользователя — низкий риск |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-01 | Tampering | `number_template_contexts.template_id` FK | mitigate | `ON DELETE SET NULL` гарантирует, что осиротевший id никогда не останется в памяти контекста — реализовано на уровне схемы (Task 2), доказано тестом (Task 3, Тест 3), а не полагается на дисциплину сервисного слоя |
| T-40.2-02 | Tampering | `DROP TABLE counters` | accept | Необратимая операция, но `counters` не хранит ничего кроме монотонных счётчиков, которые эта фаза полностью замещает; откат миграции не предусмотрен нигде в проекте (нет прецедента downgrade) |
</threat_model>

<verification>
`cargo test -p trackly-core` (Task 1 unit-тесты) и `cargo test -p trackly-infra --test
number_templates_migration` (Task 3) зелёные. `cargo check --workspace` не падает (новые модули
зарегистрированы в `mod.rs`, ничего не сломано у существующих потребителей `domain`/`ports`).
</verification>

<success_criteria>
Контракты (`TemplateType`, `TemplateContext`, `NumberTemplateRow`, `NextNumberResult`,
`NumberTemplateRepository`) существуют и компилируются в `trackly-core` без I/O-зависимостей.
Миграция V041 создаёт схему, засевает ровно предписанные шаблоны/контексты и удаляет `counters`
— идентично на пустой и на существующей БД.
</success_criteria>

<output>
Create `.planning/phases/40.2-inventory-number-templates/40.2-01-SUMMARY.md` when done
</output>
