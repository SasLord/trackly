---
phase: 40.2-inventory-number-templates
plan: 07
type: execute
wave: 5
depends_on: ["40.2-05"]
files_modified:
  - migrations/V043__cartridges_code_unique_live.sql
  - crates/trackly-infra/tests/cartridges_code_unique_migration.rs
  - crates/trackly-core/src/ports/cartridges.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-app/src/dto/cartridge.rs
  - crates/trackly-app/tests/cartridges_numbering.rs
autonomous: true
requirements: [NUM-09, NUM-13]

must_haves:
  truths:
    - "cartridges.code уникален только среди живых записей (частичный индекс), мягко удалённый код можно переиспользовать — NUM-09 пространство (в)"
    - "Существующие коды (включая нестандартной длины) не переписываются миграцией — только правило уникальности меняется"
    - "После обновления БД с существующими C-0001..C-0042 следующий предложенный код — C-0043, а не C-0001 (континуитет NUM-13, засеянная маска C-[XXXX] шириной 4 из плана 01)"
  artifacts:
    - path: "migrations/V043__cartridges_code_unique_live.sql"
      provides: "rebuild cartridges: снятие column-level UNIQUE(code), партиционный UNIQUE INDEX WHERE deleted_at_utc IS NULL"
    - path: "crates/trackly-app/src/services/cartridge_service.rs"
      provides: "create() без assign_code_in_tx counter-инкремента, occupied/warnings через NumberTemplateService"
  key_links:
    - from: "crates/trackly-app/src/services/cartridge_service.rs"
      to: "crates/trackly-app/src/services/number_template_service.rs"
      via: "occupied/detect_warnings/remember_context для CartridgeCreate|DrumCreate по kind_id"
      pattern: "NumberTemplateService"
---

<objective>
Снять глобальную (в т.ч. по мягко удалённым) уникальность `cartridges.code` в пользу частичного
индекса «только среди живых» (NUM-09), и заменить `assign_code_in_tx`'s инкремент
`cartridge_seq`/`drum_seq` на обязательный ввод + шаблон (NUM-13). Ключевой риск (RESEARCH
Pitfall 1) уже снят решением D-16: засеянные маски `C-[XXXX]`/`D-[XXXX]` имеют ФИКСИРОВАННУЮ
ширину 4, совпадающую с текущим форматом `{seq:04}` — континуитет проверяется напрямую тестом на
миграции, без вычисления динамической ширины.

Purpose: последний из трёх «rebuild+counter-removal» планов волны 5, разблокирует финальную
зачистку в плане 15.
Output: миграция V043, `cartridge_service.rs`/`cartridges_sqlite.rs` на шаблонах.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.2-inventory-number-templates/40.2-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-CONTEXT.md
@.planning/phases/40.2-inventory-number-templates/40.2-RESEARCH.md
@.planning/phases/40.2-inventory-number-templates/40.2-PATTERNS.md

<interfaces>
Из плана 04: `NumberTemplateService::{is_occupied,detect_warnings,check_mismatch,get_context,
remember_context}` + DTO `NumberFieldInput`/`NumberWarningDto`/конвенция `*SaveOutcome`.
`kind_id` картриджа определяется через `SqliteCartridgeRepository::model_kind_in_tx(&tx,
model_id)` — существующая функция, НЕ трогать; `kind_id == 2` → фотобарабан → `TemplateContext::
DrumCreate`, иначе → `TemplateContext::CartridgeCreate`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция V043 — cartridges.code UNIQUE(глобально) → UNIQUE(среди живых)</name>
  <files>migrations/V043__cartridges_code_unique_live.sql, crates/trackly-infra/tests/cartridges_code_unique_migration.rs</files>
  <read_first>
    migrations/V005__cartridges.sql,
    migrations/V016__cartridges_kind_color_settings.sql,
    migrations/V025__cartridge_printer_link.sql,
    migrations/V038__places_migrate_devices_acts_cartridges.sql,
    migrations/V030__printers_drop_connectivity_check.sql
  </read_first>
  <action>
    Как в плане 06 Task 1 — ПЕРЕД написанием SQL получить фактический полный список колонок
    `cartridges` (`PRAGMA table_info(cartridges)` на полностью мигрированной временной БД), а не
    собирать его по разрозненным ALTER-миграциям вручную. Затем `migrations/
    V043__cartridges_code_unique_live.sql` по 12-шаговому рецепту V030:
    1. `PRAGMA foreign_keys = OFF;`
    2. `CREATE TABLE cartridges_new (...)` — все текущие колонки, `code` теряет `UNIQUE` (просто
       `code TEXT NOT NULL`).
    3. `INSERT INTO cartridges_new (<явный список>) SELECT <тот же список> FROM cartridges;` (без
       CAST — тип `code` не меняется, только constraint).
    4. `DROP TABLE cartridges; ALTER TABLE cartridges_new RENAME TO cartridges;`
    5. `CREATE UNIQUE INDEX idx_cartridges_code_live ON cartridges(code) WHERE deleted_at_utc IS
       NULL;` — НЕ `LOWER(TRIM(code))` в индексе (SPEC D-08: хранится с сохранённым регистром;
       регистронезависимое сравнение — обязанность прикладного кода при `is_occupied`, не индекса
       — индекс защищает race по байтовому совпадению; регистронезависимая часть проверяется
       ПЕРЕД записью в транзакции сервиса, план ниже, Task 3).
    6. Восстановить прочие существующие индексы на `cartridges` (`idx_cartridges_place` и любые
       другие, найденные в `PRAGMA index_list(cartridges)` до миграции, кроме автоиндекса из
       снятого `UNIQUE`) — не потерять `idx_cartridges_place` (`WHERE deleted_at_utc IS NULL AND
       place_id IS NOT NULL`, из V038).
    7. `PRAGMA foreign_keys = ON; PRAGMA user_version = 43;`

    Написать `crates/trackly-infra/tests/cartridges_code_unique_migration.rs` по образцу
    `v032_data_transform_drops_empty_and_preserves_populated` (V005-образная мини-схема +
    реальный SQL из V043 через `include_str!`) — тесты:
    1. Живой картридж с кодом `C-0042`, soft-deleted картридж с тем же кодом `C-0042` (созданный
       ДО миграции — этого не должна была допускать старая схема, но раз миграция снимает старый
       constraint, тест намеренно создаёт этот сценарий средствами прямого INSERT в стенд-схему
       ДО применения V043, чтобы доказать НОВОЕ правило после) — после V043 создать ЕЩЁ ОДИН
       живой картридж с кодом `C-0042` — обязан УСПЕШНО вставиться (частичный индекс не видит
       soft-deleted строку).
    2. Два живых картриджа с одинаковым `code` — вставка второго ПАДАЕТ (индекс жив и работает).
    3. Континуитет: засеять живые `C-0001..C-0042`, применить V043 (не меняет данные, только
       constraint) — тест НЕ вызывает `compute_next_for_template` здесь (это репозиторий из
       плана 03, недоступен в `trackly-infra/tests` migration-тесте без полного AppCtx) — вместо
       этого зафиксировать словами в комментарии теста, что фактическая проверка «следующий код —
       C-0043» лежит в Task 3 этого плана (`cartridges_numbering.rs`, уровень `trackly-app`, где
       `NumberTemplateService` доступен), а этот тест лишь доказывает, что коды `C-0001..C-0042`
       физически пережили миграцию неизменными (`SELECT code FROM cartridges ORDER BY id` даёт
       ровно исходный список).
  </action>
  <acceptance_criteria>
    <automated>cargo test -p trackly-infra --test cartridges_code_unique_migration</automated>
  </acceptance_criteria>
  <done>Частичный уникальный индекс работает по новому правилу (живые — уникальны, мягко удалённые — не блокируют), существующие коды не изменены.</done>
</task>

<task type="auto">
  <name>Task 2: Удаление port-метода peek_next_code + assign_code_in_tx counter-пути</name>
  <files>crates/trackly-core/src/ports/cartridges.rs, crates/trackly-infra/src/repos/cartridges_sqlite.rs</files>
  <read_first>
    crates/trackly-core/src/ports/cartridges.rs,
    crates/trackly-infra/src/repos/cartridges_sqlite.rs
  </read_first>
  <action>
    Удалить `fn peek_next_code(&self, conn: &Self::Conn) -> Result<i64, AppError>;` из
    `CartridgeRepository` trait и его реализацию в `cartridges_sqlite.rs` (метод НИКЕМ не
    вызывается уже сегодня — подтверждено `grep -rn "peek_next_code" crates/` при
    планировании — прямое удаление, без слоя обратной совместимости, в отличие от актов).
    В `assign_code_in_tx` (`cartridges_sqlite.rs:188+`) удалить ветку авто-генерации через
    `counters`/`cartridge_seq`/`drum_seq` (строки после текущего `code_override` early-return) —
    оставить ТОЛЬКО путь явного кода: если `code_override` не передан, вернуть
    `AppError::Validation{field:"code", message:"Введите код или выберите шаблон."}` (текст
    дословно из UI-SPEC). Переименовать параметр/поведение функции по необходимости — вызывающий
    код (`cartridge_service.rs`) обновляется в Task 3. Задача этого шага — физическое отсутствие
    строки `"cartridge_seq"`/`"drum_seq"` в кодовой базе Rust (не только в SQL-миграциях,
    которые остаются как исторический артефакт).
  </action>
  <acceptance_criteria>
    - `grep -rn "cartridge_seq\|drum_seq" crates/trackly-core crates/trackly-infra/src crates/trackly-app/src` — пусто (миграции `migrations/*.sql` НЕ проверяются этим grep — историю не переписываем).
    - `cargo check --workspace` проходит.
  </acceptance_criteria>
  <done>Ни один Rust-файл сервисов/репозиториев не упоминает cartridge_seq/drum_seq/peek_next_code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cartridge_service — occupied/warnings по kind_id, континуитет теста</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs, crates/trackly-app/src/dto/cartridge.rs, crates/trackly-app/tests/cartridges_numbering.rs</files>
  <read_first>
    crates/trackly-app/src/services/cartridge_service.rs,
    crates/trackly-app/src/dto/cartridge.rs,
    crates/trackly-app/tests/cartridges_numbering.rs
  </read_first>
  <behavior>
    - `create()` с непустым `number_input.value` и БЕЗ `template_id` → создаётся с этим кодом
      как есть (ручной ввод, обязателен, NUM-13).
    - `create()` с ПУСТЫМ `number_input.value` → `AppError::Validation` (никакой авто-генерации).
    - `create()` с кодом, занятым живым картриджем (после `.trim()`, без учёта регистра) →
      отклонено; с кодом, занятым МЯГКО УДАЛЁННЫМ картриджем — успешно (частичный индекс,
      доказывает интеграцию Task 1 + сервисный слой).
    - Континуитет (ЯДРО NUM-13 acceptance criterion): БД с живыми `C-0001..C-0042` (засеяны
      напрямую SQL до вызова сервиса, кодами по СТАРОМУ формату `{seq:04}`) → запрос следующего
      кода для шаблона «Картриджи» (засеянный `C-[XXXX]`, план 01) через
      `NumberTemplateService::peek_next` даёт `C-0043` — БЕЗ какого-либо специального кода в
      этом плане, просто потому что маска и данные совпадают по ширине (D-16 уже это
      гарантировал). Тест явно проверяет именно этот сценарий как smoke-test, поскольку это
      единственный жёсткий acceptance-критерий NUM-13 для картриджей.
    - Смена `kind_id` (картридж vs фотобарабан) при создании ведёт к разным пространствам
      контекста (`CartridgeCreate` vs `DrumCreate`) — оба читают один physical column
      `cartridges.code`, но с разными шаблонами по умолчанию (`C-[XXXX]` vs `D-[XXXX]`).
  </behavior>
  <action>
    В `dto/cartridge.rs`: заменить `CartridgeCreateDto.code_override: Option<String>` на
    `number_input: NumberFieldInput` (план 04). Добавить `CartridgeSaveOutcome{Created
    (CartridgeDto), NeedsConfirmation(NumberWarningDto)}` enum (конвенция плана 04 Task 1).

    В `cartridge_service.rs::create()`: убрать вызов `assign_code_in_tx` целиком (Task 2 уже
    убрала его auto-путь; здесь убрать САМ ВЫЗОВ и заменить прямой присвоением `let code =
    payload.number_input.value.trim().to_string();` + валидация непустоты (текст из UI-SPEC).
    Определить `template_context = if kind_id == 2 { TemplateContext::DrumCreate } else {
    TemplateContext::CartridgeCreate };` (используя уже существующий `model_kind_in_tx` вызов,
    который остаётся). Вызвать occupied-проверку (`NumberTemplateService::is_occupied(space=
    CartridgeCode-объединяющее-оба-kind, candidate=&code, exclude_id=None)` — ПРОСТРАНСТВО
    ОДНО для обоих kind, см. план 01/03 doc-комментарий) → занято → `Err(Conflict)`. Если
    `!confirm_mismatch` и `template_id` задан — проверить mismatch, вернуть `NeedsConfirmation`
    при несовпадении. Если `!confirm_script_mix` — `detect_warnings` → `NeedsConfirmation` при
    срабатывании. Иначе — вставить как раньше (`cart_repo.insert_cartridge_in_tx`), аудит
    `action` остаётся `"create"` (ручной override теперь ЕДИНСТВЕННЫЙ путь, поэтому ветка
    `"custom:cartridge_code_override"` для create() больше не нужна — но ПРОВЕРИТЬ, используется
    ли `"custom:cartridge_code_override"` где-то ещё, например при правке — если да, оставить
    константу для того call site, просто убрать из `create()`). После успеха —
    `remember_context(template_context, ...)` по правилам NUM-08, `ctx.ws_broadcast
    (WsEvent::NumberSpaceChanged{space:"cartridge_code".into()})`.

    Переписать `cartridges_numbering.rs` — убрать тесты на старый auto-generate формат
    (`assert_eq!(code1, "C-0001")` через `assign_code_in_tx`), добавить сценарии из
    `<behavior>` выше, включая continuity smoke-test.
  </action>
    <acceptance_criteria>
    См. `<behavior>` выше — каждый перечисленный сценарий соответствует отдельному тесту.
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test cartridges_numbering</automated>
  </acceptance_criteria>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test cartridges_numbering</automated>
  </verify>
  <done>Картриджи/фотобарабаны создаются только с обязательным кодом (ручным или по шаблону), occupied-проверка учитывает частичный индекс, континуитет C-0042→C-0043 доказан тестом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Клиент → cartridge_service::create | `number_input`/`confirm_*` флаги непроверены — DB partial unique index остаётся финальным арбитром при гонке |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-16 | Tampering | Гонка двух пользователей на один код картриджа | mitigate | `idx_cartridges_code_live` (Task 1) — race-тест в `cartridges_numbering.rs` (Task 3, по образцу `concurrent_50_unique_codes`) |
| T-40.2-17 | Elevation of Privilege | Смешение пространств «Картриджи»/«Фотобарабаны» позволило бы обойти уникальность через смену kind | accept | Уникальность (NUM-09 пространство в) намеренно ОБЩАЯ для обоих kind — это не эскалация, а прямое следствие того, что оба живут в одной колонке `cartridges.code`, задокументировано в плане 01/03 |
</threat_model>

<verification>
`cargo test -p trackly-app --test cartridges_numbering` и `cargo test -p trackly-infra --test
cartridges_code_unique_migration` зелёные. `grep -rn "cartridge_seq\|drum_seq" crates/*/src`
пусто.
</verification>

<success_criteria>
Картриджи/фотобарабаны полностью на обязательном вводе + шаблонах, частичная уникальность
работает, континуитет с существующими кодами доказан тестом.
</success_criteria>

<output>
Create `.planning/phases/40.2-inventory-number-templates/40.2-07-SUMMARY.md` when done
</output>
