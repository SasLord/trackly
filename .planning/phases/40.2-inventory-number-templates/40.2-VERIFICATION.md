---
phase: 40.2-inventory-number-templates
verified: 2026-09-22T06:27:17Z
status: human_needed
score: 16/16 requirement truths verified in code + tests; 0 code gaps; UI runtime behavior unverified
overrides_applied: 0
human_verification:
  - test: "Escape в открытом меню «Вставка» внутри «Новое устройство/акт/картридж» и в переключателе типа устройства"
    expected: "Закрывает только меню «Вставка», фокус — на кнопке «Вставка», форма и введённые данные остаются; повторный Escape закрывает форму. Escape в Dropdown/PlacePicker/etc. внутри форм по-прежнему не закрывает форму (FE-CR-03/FE-WR-11)."
    why_human: "Порядок всплытия события keydown между вложенными компонентами Svelte — рантайм-поведение, статические гейты (svelte-check/lint/build) его не видят."
  - test: "Цепочка подтверждений привязана к конкретному номеру: номер не по шаблону → «Продолжить» → сервер вернул кир/лат → «Поправлю» → ввести ДРУГОЙ номер не по шаблону → «Создать»"
    expected: "Попап «Не соответствует шаблону» должен появиться заново для нового номера, а не молча сохраниться с confirmMismatch=true (FE-CR-01)"
    why_human: "Многошаговый пользовательский сценарий с состоянием формы между попапами."
  - test: "Сохранение акта, чья позиция параллельно ушла на другое устройство («устройство больше не на складе»)"
    expected: "Toast с реальным серверным текстом ошибки, НЕ попап «Номер занят» (FE-CR-02)"
    why_human: "Требует гонки/параллельного изменения данных — не проверяется в статическом анализе."
  - test: "«Новое устройство»: стереть автоподставленный номер, поставить Количество=5, в другой сессии/вкладке создать запись, вызывающую number_space_changed"
    expected: "Номер НЕ появляется в очищенном поле, Количество остаётся 5 (не сбрасывается до 1) — FE-CR-04"
    why_human: "WS-событие от другой сессии + проверка тихого недействия — рантайм с двумя параллельными клиентами."
  - test: "Быстрое переключение «Картридж ↔ Фотобарабан» несколько раз подряд (особенно через LAN-браузер, где задержка сети выше)"
    expected: "Код и текст «Шаблон: …» соответствуют ПОСЛЕДНЕМУ выбранному виду, а не более медленно ответившему запросу; выбор шаблона в меню сразу после открытия формы не перетирается автоподстановкой (FE-CR-05/FE-WR-04)"
    why_human: "Гонка сетевых ответов — воспроизводимо только вручную с реальной задержкой сети (LAN HTTP)."
  - test: "Фокус в попапах «Не соответствует шаблону» / «Проверьте буквы в номере»: открыть попап, проверить положение фокуса; нажать «Поправлю»/«Закрыть»/«Взять следующий свободный»/«Без шаблона»"
    expected: "Фокус по умолчанию на «Поправлю»; после закрытия любого попапа — фокус возвращается в поле номера, а не на кнопку «Вставка» (FE-WR-10)"
    why_human: "Фокус-менеджмент между размонтированием вложенных Modal — требует реального WKWebView/браузера (project rule: compile_gates_miss_svelte_runtime)."
  - test: "Ссылка «Настройки / Организация» из пустого меню «Вставка» (когда шаблонов своего типа нет)"
    expected: "Первый клик показывает предупреждение о потере формы + пункт перехода; второй клик открывает раздел «Организация» (а не «Сеть»), при холодном переходе и когда «Настройки» уже открыты (FE-WR-13)"
    why_human: "Навигация между разделами SPA + состояние открытой модалки — рантайм-сценарий."
  - test: "Витрина Buttons/ActionMenu: сравнить `Button variant=\"ghost\" size=\"md\" iconOnly`  и `ActionMenu variant=\"ghost-md\"` в состояниях normal/hover/focus-visible/disabled, светлая и тёмная тема"
    expected: "Визуально неотличимы (общий класс `.tr-btn-ghost-icon`, UI-SPEC §10) — FE-WR-14"
    why_human: "Визуальное сравнение пикселей/оттенков — не проверяется статическими гейтами."
  - test: "Подсказка «Занят: …» — очистить поле; создать конфликтующую запись в другой сессии, пока шаблон выбран в текущей форме; удалить выбранный шаблон в Настройках из другой сессии; выбрать стрелкой max+1 и дождаться WS-обновления"
    expected: "Подсказка «Занят» пропадает при очистке поля; список «Вставка» обновляет «→ следующий номер» после чужого создания; выбор снимается, если шаблон удалён; выбор max+1 переживает WS-обновление (FE-WR-03/05/06)"
    why_human: "Многосессионный WS-сценарий."
  - test: "Кир/лат попап: ввести кириллическую «С-0005» при существующей латинской «C-0005»"
    expected: "Текст «Номер «С-0005» выглядит так же, как существующий номер «C-0005» (картридж «…»)…», без абзаца про смешение алфавитов; оба номера моноширинным шрифтом (FE-IN-01)"
    why_human: "Требует реальных данных в БД и визуальной проверки текста попапа."
  - test: "Правка шаблона номера, изменённого в другой сессии между открытием модалки и сохранением (оптимистическая блокировка)"
    expected: "Русский toast «Шаблон изменён другим пользователем…», модалка закрывается, список обновлён без ложного success-тоста (FE-WR-15)"
    why_human: "Конкурентная правка из двух сессий."
  - test: "Многократное открытие/закрытие попапов создания устройства/акта/картриджа (и в Tauri, и в LAN-браузере)"
    expected: "Не растёт число WS-подключений, не появляются лишние тосты (FE-WR-09)"
    why_human: "Утечка ресурсов проявляется только при длительном интерактивном использовании."
  - test: "Полный сквозной прогон NUM-06..NUM-12 по всем 4 сущностям в РЕАЛЬНОМ cargo tauri dev, ПОСЛЕ пересоздания dev-БД (V042–V044 правились in place — старая БД не пройдёт refinery checksum) и после `pnpm --dir ui build` (для проверки LAN-браузера)"
    expected: "Автоподстановка, кнопка «Вставка», переключатель ↑/↓, все три попапа цепочки D-01, поведение форм правки (D-05), формат `[YYYY]/[MM]-[X]` для акта, континуитет `C-0043` после `C-0001..C-0042`, блокировка отображаемых номеров возвратов (D-06) — ни один из этих сценариев не был проверен в запущенном приложении ни разу за всю фазу (все 4 плана-хоста явно задокументировали Deferred UAT)"
    why_human: "Project rule (`synthetic_harness_not_verification`): Playwright/Chromium-харнесс не считается верификацией UI; проверка обязана быть в реальном WKWebView/браузере. Также требуется свежая БД — старая упадёт на checksum refinery."
---

# Phase 40.2: Шаблоны инвентарных номеров — Verification Report

**Phase Goal:** Администратор ведёт в «Настройки / Организация» шаблоны номеров с токенами
([YYYY], [YY], [MM], [DD], [X], [XXXX…]); в попапах «Новое устройство», «Новый принтер», «Новый
акт» и «Новый картридж/фотобарабан» поле номера автоподставляет следующий номер по последнему
использованному в этом попапе шаблону (с переключателем ↑/↓ между «первым разрывом» и «max+1»),
выбор шаблона через кнопку «Вставка» с контекстным меню, запрет дубликатов с попапом о занявшей
номер записи и подтверждение номера, не совпадающего с шаблоном.

**Verified:** 2026-09-22T06:27:17Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Итог одним абзацем

Код фазы 40.2 реализует все 16 требований NUM-01…NUM-16 сквозным путём (миграции → домен →
репозиторий → сервис → Tauri/HTTP → фронтенд), и это подтверждено не только чтением кода, но и
целевым прогоном тестов (миграции через настоящий refinery-раннер с populated FK-таблицами,
concurrency-тесты, CRUD, детектор двойников, CSV-импорт). Полный code review нашёл 10 critical +
26 warning находок (в основном реальные баги: потеря `act_items` при апгрейде, гонки в UI,
пропущенные предупреждения); все backend-находки и почти все frontend-находки закрыты
задокументированными коммитами, подтверждёнными тестами и статическими гейтами. Но НИ ОДНА
UI-функция фазы ни разу не была проверена в реально запущенном приложении (`cargo tauri dev` /
LAN-браузер) — все четыре plan-хоста (12, 13, 14, 15) и итоговый REVIEW-FIX-frontend прямо
документируют это как «Deferred UAT» / «Не проверено в рантайме», согласно проектному правилу не
принимать Playwright/Chromium-харнесс как верификацию UI. Поэтому статус — `human_needed`, а не
`passed`: код готов, но необходим один живой прогон в приложении на СВЕЖЕЙ dev-БД (старая БД
упадёт на проверке контрольных сумм refinery, т.к. V042–V044 правились «на месте» уже после
исходного тега).

## Goal Achievement — Observable Truths (NUM-01…NUM-16)

| # | Truth (REQ) | Status | Evidence |
|---|-------------|--------|----------|
| 1 | NUM-01: CRUD шаблонов 4 типов в Настройки/Организация, уникальность маски в пределах типа, «следующий номер»/«переполнен», справка по токенам | ✓ VERIFIED (код+тесты) / UI не проверена вживую | `ui/src/features/settings/OrgSettings.svelte:794-870` блок «Шаблоны для инвентарных номеров»; `NumberTemplateModal.svelte`, `NumberMaskHelp.svelte`; тесты `number_templates_crud.rs` (9/9 passed, включая `create_rejects_duplicate_type_and_mask`, `preview_mask_validates_without_persisting`) |
| 2 | NUM-02: маска — только `[YYYY][YY][MM][DD]` + ровно один `[X…]`, иначе не сохраняется | ✓ VERIFIED | `crates/trackly-core/src/text/mask.rs` (`parse_mask/validate_mask`); тест `create_rejects_overlong_mask_and_too_wide_ordinal` |
| 3 | NUM-03: живой предпросмотр следующего номера в форме шаблона | ✓ VERIFIED | `preview_mask` command + `NumberTemplateModal.svelte` вызывает предпросмотр на сервере |
| 4 | NUM-04: сервер вычисляет «первый свободный»/«max+1» по живым записям, без хранимого счётчика, мягко удалённые не занимают номер | ✓ VERIFIED | `crates/trackly-core/src/text/sequence.rs::compute_next`; repo-тесты `number_templates_repo.rs` (`soft_deleted_cartridge_frees_its_number`, `gap_in_the_middle_17_and_21`, `month_prefix_isolates_independent_sequences`, `dense_tail_500_to_510_and_returns_excluded` — все ok) |
| 5 | NUM-05: переполненный шаблон недоступен в меню «Вставка», помечен «переполнен»; разрыв без места для max+1 — без переключателя | ✓ VERIFIED | `NumberTemplateField.svelte:707-731` (`aria-disabled`, серый текст для `t.overflowed`); `sequence.rs`/`compute_next` возвращает `max_plus_one_fits_width` |
| 6 | NUM-06: автоподстановка «первого свободного» по запомненному шаблону + кнопка «Вставка» с меню (включая «Без шаблона») в 4 попапах создания | ✓ VERIFIED (код) | `NumberTemplateField.svelte` подключён в `DeviceFormBody`, `ActFormBody`, `CartridgeFormBody` (create-режим); меню сортирует по маске, «Без шаблона» — последний пункт |
| 7 | NUM-07: переключатель ↑/↓ виден только пока значение = одному из двух предложенных | ✓ VERIFIED | `NumberTemplateField.svelte` — сравнение через чистый `normalize()`, ручная правка скрывает стрелку (D-19) |
| 8 | NUM-08: сервер хранит последний шаблон/«без шаблона» для 5 контекстов, общий для всех/всех режимов; несоответствие/удаление → «без шаблона» | ✓ VERIFIED | V041 таблица `number_template_contexts` (5 строк); `remember_context`/`set_context_in_tx` (BE-WR-01 fix — тип шаблона теперь сверяется с контекстом); тест `context_memory_and_peek_reject_template_of_another_type`, `remember_and_get_context_round_trip_then_clears_on_delete` |
| 9 | NUM-09: запрет сохранения занятого номера (3 пространства), Tauri+HTTP, атомарно при гонке | ✓ VERIFIED | `ensure_number_free_in_tx` внутри writer-транзакции (BE-WR-04 fix); concurrency-тесты `concurrent_creates_same_number_exactly_one_succeeds` (acts), `concurrent_creates_same_code_exactly_one_succeeds`/`concurrent_case_variant_creates_never_both_succeed` (cartridges) — все ok |
| 10 | NUM-10: попап «Номер занят» со сведениями о записи + «Взять следующий свободный» (при шаблоне) | ✓ VERIFIED | `NumberTakenPopup.svelte`; `takeNext()` экспортирован из `NumberTemplateField` (FE-WR-07 fix) |
| 11 | NUM-11: попап «Поправлю/Продолжить» при несоответствии шаблону; «Продолжить» → «без шаблона» | ✓ VERIFIED | `NumberMismatchPopup.svelte`; `check_mismatch`; тест `check_mismatch_true_when_candidate_does_not_fit_mask` |
| 12 | NUM-12: предупреждение о смешении кир/лат ИЛИ визуальном двойнике | ✓ VERIFIED | `detect_warnings` теперь ищет двойника всегда, не только при смешении (BE-CR-03 fix); тесты `detect_warnings_doppelganger_without_script_mix`, `detect_warnings_script_mix_with_doppelganger`, `detect_warnings_script_mix_without_doppelganger` |
| 13 | NUM-13: счётчики `act_number`/`cartridge_seq`/`drum_seq` не используются; засев `[X]`/`C-[XXXX]`/`D-[XXXX]` | ✓ VERIFIED | `DROP TABLE counters` в V041; `assign_code_in_tx` больше не инкрементирует, требует явный код (grep: `current_value = current_value` не встречается вне архивных миграций V009/V017) |
| 14 | NUM-14: номер акта — TEXT без потерь; возврат наследует номер с «в»/«вN», не в последовательности | ✓ VERIFIED | V042 (12-шаговый rebuild, тест `migrates_integer_number_to_text_preserving_ids_and_unique_index`); D-06 в обе стороны — `display_number_space_covers_returns_in_both_directions` (BE-WR-03 fix) |
| 15 | NUM-15: дубли инвентарных номеров → `ДУБЛЬ-NNNNNN (исходный)`, сквозная нумерация, аудит, самая ранняя запись не переименовывается | ✓ VERIFIED | `duplicate_group_renamed_per_spec_num_15_example`, `each_rename_writes_one_audit_log_row_with_null_user` (V044 migration test) |
| 16 | NUM-16: CSV-импорт различает «занят в БД» и «повтор строки M» | ✓ VERIFIED | `import_commit_num16_db_duplicate_vs_in_file_duplicate` (devices_csv_import.rs) — passed; in-file dedup регистрируется только после успешного `create` (BE-IN-03 fix) |

**Score:** 16/16 требований подтверждены в коде и целевых тестах. 0 гэпов на уровне бэкенда.
Единственный открытый пункт — фактическое поведение в запущенном UI (см. Human Verification).

## Что проверялось помимо чтения кода (не доверяя SUMMARY)

- Прогнаны настоящие тесты, а не переиспользован отчёт исполнителя:
  - `cargo test -p trackly-infra --test number_templates_migration --test number_templates_repo --test acts_number_text_migration --test cartridges_code_unique_migration --test devices_inventory_dedup_migration` → **27/27 passed**, включая `upgrade_through_real_runner_keeps_every_act_child_row_and_link` и `upgrade_succeeds_with_completed_request_and_keeps_its_cartridge_link` — это прямая регрессионная проверка того самого BE-CR-01/BE-CR-02 бага (потеря `act_items`/падение на `completed_cartridge_id`), а не пересказ фиксового отчёта.
  - `cargo test -p trackly-app --test number_space_broadcast_gate --test number_templates_crud --test number_templates_compute_next --test acts_numbering --test cartridges_numbering --test devices_csv_import --test role_endpoint_matrix --test export_bindings` → **все зелёные** (44 теста, включая concurrency и permission gates).
  - `pnpm --dir ui build` в `ui/` → зелёная сборка (683 модуля, только пред-существующие предупреждения о `CompatibilityEditor`/`.hint-warn`, не относящиеся к фазе).
- Прочитаны исходники, а не только грепнуты имена файлов: `V041__number_templates.sql` целиком (засев 5 контекстов + 3 шаблона, DROP counters — соответствует D-15/D-16/D-17 дословно); `restore_from_snapshot_in_tx` в `devices_sqlite.rs` (подтверждён комментарий BE-WR-06 — акт никогда не меняет `inventory_number`, поэтому не восстанавливать его безопасно, а не регрессия); `assign_code_in_tx` в `cartridges_sqlite.rs` (подтверждено отсутствие авто-инкремента); `saveChain.ts` (реальная логика привязки подтверждения к номеру, а не пересказ); `ActFormBody.svelte` (подтверждено, что режим правки использует голый `Input`, а НЕ `NumberTemplateField` — D-05).
- Инверсия/disconfirmation pass: искал контрпримеры к «всё готово» — нашёл, что миграции V009/V017 (архивная история) по-прежнему содержат текст `counters`/`cartridge_seq` — это ОЖИДАЕМО (история миграций неизменяема) и НЕ является живым использованием; отдельно подтвердил grep-ом, что ни один Rust-файл вне тестов/архивных SQL не ссылается на `counters`/`cartridge_seq`/`drum_seq` как на активный путь.

## Требуемые артефакты (выборка, все существуют и подключены)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `migrations/V041__number_templates.sql` … `V044__devices_inventory_number_dedup_unique.sql` | таблицы шаблонов, rebuild acts/cartridges/devices, dedup | ✓ VERIFIED | все 4 файла существуют; настоящий-раннер тесты проходят на данных с FK (act_items/place_movements/completed_cartridge_id) |
| `crates/trackly-core/src/text/{mask,homoglyphs,sequence,number_value}.rs` | грамматика маски, гомоглифы, математика последовательности, валидация длины | ✓ VERIFIED | все файлы существуют, задействованы сервисом |
| `crates/trackly-app/src/services/number_template_service.rs` | CRUD, peek_next, context memory, is_occupied, detect_warnings | ✓ VERIFIED | подключён `ws_tx` (BE-CR-04), сверка типа/контекста (BE-WR-01) |
| `crates/trackly-app/tests/number_space_broadcast_gate.rs` | реестровый гейт: каждая мутация номера шлёт `WsEvent::NumberSpaceChanged` | ✓ VERIFIED | тест зелёный, по заголовку — обязателен к расширению для новых мутаций |
| `ui/src/lib/components/NumberTemplateField.svelte` | единая точка автоподстановки | ✓ VERIFIED, WIRED | импортирован и используется в 3 формах создания |
| `ui/src/lib/components/{NumberTakenPopup,NumberMismatchPopup,NumberScriptWarningPopup}.svelte` | 3 попапа цепочки D-01 | ✓ VERIFIED, WIRED | подключены как top-level `Modal`-siblings через `onPopupChange` во всех трёх `*FormModal.svelte` |
| `ui/src/features/acts/ActNumberField.svelte` | должен быть УДАЛЁН (план 14 финальная зачистка) | ✓ VERIFIED — файл отсутствует | `ls` → not found; `acts_peek_next_number` тоже отсутствует во всём дереве |
| `ui/src/lib/numbering/saveChain.ts` | общая логика привязки подтверждения к номеру + occupied-разрешение (FE-IN-03 частично) | ✓ VERIFIED | существует, используется тремя формами; сама машина состояний попапов осознанно НЕ вынесена (см. Gaps/Info ниже) |

## Data-Flow / Wiring — ключевые связи

| From | To | Via | Status |
|------|----|----|--------|
| `NumberTemplateField.svelte` | `number_templates_list_by_context/_peek_next/_is_occupied/contexts_get` | apiCall | ✓ WIRED |
| `DeviceFormBody`/`ActFormBody`/`CartridgeFormBody` | `saveChain.ts` | `confirmsFor/withConfirm/occupyingRecordOrRethrow` | ✓ WIRED |
| `device_service.rs`/`act_service.rs`/`cartridge_service.rs` | `NumberTemplateService` | `ensure_number_free_in_tx`, `detect_warnings`, `remember_context_in_tx` | ✓ WIRED, внутри writer-транзакции (BE-WR-02/BE-WR-04 fix) |
| Мутации номера (create/update/delete/do_return/template CRUD/contexts_set) | `WsEvent::NumberSpaceChanged` | `with_ws_tx` + broadcast | ✓ WIRED, закреплено гейтом `number_space_broadcast_gate.rs` |
| `tauri_cmds/number_templates.rs` | `http/number_templates.rs` | общие `build_*` функции | ✓ WIRED (dual access path соблюдён) |

## Требования (NUM-01…NUM-16) — соответствие REQUIREMENTS.md

REQUIREMENTS.md отмечает все 16 NUM как `Complete`. По итогам этой проверки — **согласен со всеми
16**, но с оговоркой: отметка "Complete" в REQUIREMENTS.md для UI-зависимых пунктов (NUM-01,
NUM-03, NUM-06…NUM-12) описывает готовность КОДА, а не подтверждённое ПОВЕДЕНИЕ в приложении —
этот разрыв не является ошибкой отметки (весь код и тесты действительно готовы), но должен быть
закрыт живым UAT, а не молчаливо приравнен к «фаза полностью проверена». Ни одна из 16 меток не
скорректирована в сторону понижения — для всех найдены прямые доказательства в коде и тестах.

| Requirement | Source Plan | Статус в REQUIREMENTS.md | Проверка кода | Комментарий |
|---|---|---|---|---|
| NUM-01 | Plans 01, 05, 10 | Complete | ✓ подтверждено | — |
| NUM-02 | Plan 02 | Complete | ✓ подтверждено | — |
| NUM-03 | Plans 03, 05, 10 | Complete | ✓ подтверждено | — |
| NUM-04 | Plans 02, 03 | Complete | ✓ подтверждено | — |
| NUM-05 | Plans 02, 03, 12 | Complete | ✓ подтверждено | — |
| NUM-06 | Plans 05, 09, 12, 13, 14, 15 | Complete | ✓ подтверждено (код), UI не проверена вживую | см. human_verification |
| NUM-07 | Plans 04, 12, 13, 14, 15 | Complete | ✓ подтверждено (код) | см. human_verification |
| NUM-08 | Plans 04, 05, 12, 13, 14, 15 | Complete | ✓ подтверждено | — |
| NUM-09 | Plans 04-08, 12-15 | Complete | ✓ подтверждено | concurrency-тестами |
| NUM-10 | Plans 04, 05, 11, 13-15 | Complete | ✓ подтверждено (код), UI не проверена вживую | см. human_verification |
| NUM-11 | Plans 04, 11, 13-15 | Complete | ✓ подтверждено (код), UI не проверена вживую | см. human_verification |
| NUM-12 | Plans 02, 04, 05, 11-15 | Complete | ✓ подтверждено | BE-CR-03 закрыт |
| NUM-13 | Plans 01, 06-08, 14 | Complete | ✓ подтверждено | grep-гейт чист |
| NUM-14 | Plans 06, 14 | Complete | ✓ подтверждено | — |
| NUM-15 | Plan 08 | Complete | ✓ подтверждено | — |
| NUM-16 | Plan 08 | Complete | ✓ подтверждено | — |

Orphaned requirements: нет — все 16 ID из REQUIREMENTS.md присутствуют в `requirements:` фронтматтере какого-либо из 15 планов фазы.

## Anti-Patterns Found

Полный `40.2-REVIEW.md` (44 находки: 10 critical, 26 warning, 8 info) уже прогнан через два
раунда исправлений (`40.2-REVIEW-FIX-backend.md` — 19/20 исправлено, 1 no-change-needed;
`40.2-REVIEW-FIX-frontend.md` — 22/24 исправлено, 1 частично, 1 no-change-needed). Заново
проверено выборочно (не поверил отчёту на слово):

| File | Проверка | Severity | Impact |
|------|----------|----------|--------|
| `crates/trackly-infra/src/db/migrations.rs` | BE-CR-01/02 fix — FK off вне транзакции, `pragma_foreign_key_check` до включения обратно | — | ПОДТВЕРЖДЕНО кодом и реальным-раннер тестом, не только текстом отчёта |
| `crates/trackly-app/src/services/number_template_service.rs:400-416` | BE-WR-01 fix — сверка типа шаблона с контекстом | — | ПОДТВЕРЖДЕНО тестом `context_memory_and_peek_reject_template_of_another_type` |
| `ui/src/lib/numbering/saveChain.ts`, `NumberTemplateField.svelte` | FE-CR-01/02/04/05 fixes | — | ПОДТВЕРЖДЕНО чтением исходников (см. код-сниппеты выше) — статические гейты зелёные, но РАНТАЙМ не проверен (см. human_verification) |
| FE-IN-03 (дублирование ~200 строк оркестрации попапов в 3 формах) | ℹ️ INFO, осознанно оставлено частично | INFO | Не блокер — задокументированная trade-off: перенос машины состояний в рантайм-риск выше пользы, при этом ключевая логика (`confirmsFor`, occupied-разрешение, фокус) уже вынесена в `saveChain.ts` |
| Побочно найдено в `40.2-REVIEW-FIX-frontend.md` (не исправлялось, не было в исходном review) | `applyPeekResult` может показать пустую маску «» в попапе «Не соответствует шаблону», если `peek_next` пришёл раньше списка шаблонов нового контекста | ⚠️ WARNING (не BLOCKER — редкая гонка, существовала и до правок этой фазы) | Не создаёт гэп по must-haves фазы, но стоит зафиксировать как известный мелкий дефект для будущего quick-фикса |

Debt-marker gate (TBD/FIXME/XXX): не обнаружено в изменённых файлах фазы (`grep -rn "TBD\|FIXME\|XXX"` по списку файлов из планов — чисто).

## Human Verification Required

Подробный список — во frontmatter (`human_verification:`, 13 пунктов), консолидирован из
Deferred UAT планов 12/13/14/15 и раздела «Не проверено в рантайме» `40.2-REVIEW-FIX-frontend.md`,
с дедупликацией. Коротко по категориям:

1. Полный сквозной UAT автоподстановки/цепочки попапов D-01/D-05 по всем 4 сущностям — **ни разу не выполнялся** за всю фазу.
2. Все 5 critical + часть warning фронтенд-фиксов (гонки контекста, Escape, фокус, WS-инвалидация, утечка WS-подключений, ссылка в Настройки) — исправлены в коде и проходят статические гейты, но НЕ проверены в WKWebView/браузере.
3. Перед прогоном: пересоздать dev-БД (V042–V044 правились in place, старая БД не пройдёт checksum refinery) и выполнить `pnpm --dir ui build` для проверки LAN-браузерного пути.

## Gaps Summary

Гэпов на уровне кода/тестов не найдено — все 16 NUM-требований подтверждены прямыми доказательствами
(существующий код + пройденные тесты, включая регрессионные тесты на исходные critical-баги code
review). Единственная причина статуса `human_needed` — отсутствие живой проверки UI-поведения,
что по проектному правилу (`synthetic_harness_not_verification`) не может быть закрыто иначе, чем
реальным прогоном в приложении. Рекомендация: один сквозной UAT-проход по списку в
`human_verification`, затем повторная верификация только для отметки `passed`.

---

_Verified: 2026-09-22T06:27:17Z_
_Verifier: Claude (gsd-verifier)_
