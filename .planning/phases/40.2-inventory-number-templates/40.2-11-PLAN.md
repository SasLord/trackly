---
phase: 40.2-inventory-number-templates
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/NumberTakenPopup.svelte
  - ui/src/lib/components/NumberMismatchPopup.svelte
  - ui/src/lib/components/NumberScriptWarningPopup.svelte
autonomous: true
requirements: [NUM-10, NUM-11, NUM-12]

must_haves:
  truths:
    - "Три попапа — presentational-компоненты с локальными TS-интерфейсами пропсов, не импортируют сгенерированные bindings.ts — независимы от бэкенда, чтобы попасть в Wave 1"
    - "Кнопка «Продолжить» — variant destructive в обоих мягких попапах (несоответствие шаблону и смешение алфавитов), фокус по умолчанию на «Поправлю» (UI-SPEC Discretion #5)"
    - "«Номер занят» без активного шаблона (форма правки, D-05) показывает только кнопку «Закрыть» — без «Взять следующий свободный»"
  artifacts:
    - path: "ui/src/lib/components/NumberTakenPopup.svelte"
      provides: "попап 'Номер занят' с карточкой записи + опциональной кнопкой 'Взять следующий свободный'"
    - path: "ui/src/lib/components/NumberMismatchPopup.svelte"
      provides: "попап 'Не соответствует шаблону' — Поправлю/Продолжить"
    - path: "ui/src/lib/components/NumberScriptWarningPopup.svelte"
      provides: "попап 'Проверьте буквы в номере' — смешение алфавитов + опциональный двойник"
---

<objective>
Построить три презентационных попапа цепочки D-01 (занят → не по шаблону → смешение алфавитов),
каждый — вложенный `Modal size="md"` поверх формы создания. Компоненты принимают уже готовые
данные через пропсы (локальные TS-интерфейсы, не сгенерированные bindings) — оркестрация цепочки
(какой попап открыть, когда, с какими данными) — ответственность планов 13/14/15 (волна 6),
которые подключат эти попапы к реальным вызовам API.

Purpose: строится независимо от бэкенда и от `NumberTemplateField` — чистые UI-компоненты,
поэтому можно закончить в первой волне, не дожидаясь остальной фазы.
Output: `NumberTakenPopup.svelte`, `NumberMismatchPopup.svelte`, `NumberScriptWarningPopup.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.2-inventory-number-templates/40.2-UI-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NumberTakenPopup.svelte</name>
  <files>ui/src/lib/components/NumberTakenPopup.svelte</files>
  <read_first>
    ui/src/lib/components/Modal.svelte,
    ui/src/lib/components/Button.svelte
  </read_first>
  <action>
    Локальный интерфейс пропсов: `number: string`, `record: { kind: 'device'|'printer'|
    'cartridge'|'drum'|'act'; title: string; subtitle: string | null; place: string | null;
    status: string | null }`, `canTakeNext: boolean` (false в формах правки/без активного
    шаблона, D-05), `loadingTakeNext?: boolean`, `onTakeNext?: () => void`, `onClose: () => void`.
    Разметка — `Modal size="md"`, заголовок «Номер «{number}» уже занят» (Copywriting Contract),
    вводная строка «Этот номер уже есть у другой записи. Сохранить с ним нельзя.», карточка —
    `<dl>` сетка `120px 1fr`, зазор 8px по вертикали, подписи 13px `--tr-text-secondary`, значения
    14px `--tr-text-primary`; поля карточки зависят от `record.kind`: `device`/`printer` →
    Вид/Наименование/Модель(=title)/Место/Статус; `cartridge`/`drum` → Вид/Модель/Место/Состояние;
    `act` → Вид («Акт передачи»/«Акт возврата»)/Дата/Передал/Принял (использовать `subtitle` для
    двух последних, формат "Передал: {X} · Принял: {Y}" разобрать на два `<dt>/<dd>` парсингом
    по " · " либо — проще и надёжнее — принять `record.subtitle` как уже готовую HTML-безопасную
    строку и вывести одним `<dd>` без попытки её разбирать на фронтенде; пустое значение поля —
    «—». Место — короткий путь текстом, полный — в `title` атрибуте элемента. Кнопки: если
    `canTakeNext` — «Закрыть» (secondary) + «Взять следующий свободный» (primary, `loading=
    {loadingTakeNext}`, `onclick={onTakeNext}`); иначе — только «Закрыть» (D-05).
  </action>
  <acceptance_criteria>
    <automated>pnpm --dir ui lint</automated>
  </acceptance_criteria>
    <verify>
    <automated>pnpm --dir ui lint</automated>
  </verify>
<done>Компонент рендерит карточку по всем 3 видам record.kind-групп, кнопка «Взять следующий свободный» скрыта при canTakeNext=false.</done>
</task>

<task type="auto">
  <name>Task 2: NumberMismatchPopup.svelte + NumberScriptWarningPopup.svelte</name>
  <files>ui/src/lib/components/NumberMismatchPopup.svelte, ui/src/lib/components/NumberScriptWarningPopup.svelte</files>
  <read_first>ui/src/lib/components/Modal.svelte, ui/src/lib/components/Button.svelte</read_first>
  <action>
    `NumberMismatchPopup.svelte` — пропсы: `number: string`, `mask: string`, `contextLabel:
    string` (название попапа-контекста, например «Новый акт», для текста «Следующее окно
    «{контекст}» откроется без шаблона.»), `onFix: () => void` (Поправлю), `onContinue: () =>
    void` (Продолжить). Заголовок «Номер не соответствует шаблону», тело дословно из Copywriting
    Contract «Номер «{number}» не подходит под шаблон «{mask}». Сохранить как есть? Следующее
    окно «{contextLabel}» откроется без шаблона.» (номер и маска — `.tr-mono`). Кнопки: «Поправлю»
    (secondary, автофокус) · «Продолжить» (destructive).

    `NumberScriptWarningPopup.svelte` — пропсы: `number: string`, `doppelganger:
    {number: string; record: {kind: string; title: string}} | null`, `onFix: () => void`,
    `onContinue: () => void`. Заголовок «Проверьте буквы в номере», тело — первый абзац дословно
    «В номере «{number}» смешаны русские и латинские буквы. Внешне одинаковые буквы, например «О»
    и «O», считаются разными — такой номер легко перепутать.», второй абзац ТОЛЬКО если
    `doppelganger !== null`: «Он выглядит так же, как существующий номер «{doppelganger.number}»
    ({doppelganger.record.kind} «{doppelganger.record.title}»), но записан другими буквами.»
    (номер и номер-двойник — `.tr-mono`). Кнопки: «Поправлю» (secondary, автофокус) · «Продолжить»
    (destructive) — по образцу `NumberMismatchPopup`.

    Оба компонента — `Modal size="md"`, порядок кнопок в подвале: вторичная слева, destructive
    справа (существующая конвенция проекта).
  </action>
  <acceptance_criteria>
    <automated>pnpm --dir ui lint</automated>
  </acceptance_criteria>
    <verify>
    <automated>pnpm --dir ui lint</automated>
  </verify>
<done>Оба попапа рендерят текст дословно по Copywriting Contract, включая условный абзац про двойника; фокус по умолчанию на «Поправлю» в обоих.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Чисто презентационные компоненты — все данные приходят готовыми через пропсы от вызывающего кода (планы 13-15), который отвечает за их происхождение |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-13 | Information Disclosure | `NumberTakenPopup` карточка занявшей записи | accept | Карточка показывает те же поля (наименование/место/статус), что Admin/Manager и так видят в списках устройств/актов/картриджей через `Action::ReadData` — новой утечки не возникает (RESEARCH Security Domain, тот же вывод) |
</threat_model>

<verification>
`pnpm --dir ui lint` и `pnpm --dir ui build` зелёные для всех трёх файлов.
</verification>

<success_criteria>
Три попапа готовы к подключению из планов 13-15, текст дословно совпадает с UI-SPEC Copywriting
Contract, ни один не зависит от ещё не существующих бэкенд-привязок.
</success_criteria>

<output>
Create `.planning/phases/40.2-inventory-number-templates/40.2-11-SUMMARY.md` when done
</output>
