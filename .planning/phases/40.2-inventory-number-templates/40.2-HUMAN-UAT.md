---
status: partial
phase: 40.2-inventory-number-templates
source: [40.2-VERIFICATION.md]
started: 2026-09-22T06:30:01Z
updated: 2026-09-22T06:30:01Z
---

## Current Test

[awaiting human testing]

## Preconditions

- Пересоздать dev-БД перед `cargo tauri dev` (V042–V044 правились на месте — старая БД упадёт на checksum refinery).
- `pnpm --dir ui build` перед проверкой в LAN-браузере.

## Tests

### 1. Escape в открытом меню «Вставка» внутри «Новое устройство/акт/картридж» и в переключателе типа устройства
expected: Закрывает только меню «Вставка», фокус — на кнопке «Вставка», форма и введённые данные остаются; повторный Escape закрывает форму. Escape в Dropdown/PlacePicker/etc. внутри форм по-прежнему не закрывает форму (FE-CR-03/FE-WR-11).
result: [pending]

### 2. Цепочка подтверждений привязана к конкретному номеру: номер не по шаблону → «Продолжить» → сервер вернул кир/лат → «Поправлю» → ввести ДРУГОЙ номер не по шаблону → «Создать»
expected: Попап «Не соответствует шаблону» должен появиться заново для нового номера, а не молча сохраниться с confirmMismatch=true (FE-CR-01)
result: [pending]

### 3. Сохранение акта, чья позиция параллельно ушла на другое устройство («устройство больше не на складе»)
expected: Toast с реальным серверным текстом ошибки, НЕ попап «Номер занят» (FE-CR-02)
result: [pending]

### 4. «Новое устройство»: стереть автоподставленный номер, поставить Количество=5, в другой сессии/вкладке создать запись, вызывающую number_space_changed
expected: Номер НЕ появляется в очищенном поле, Количество остаётся 5 (не сбрасывается до 1) — FE-CR-04
result: [pending]

### 5. Быстрое переключение «Картридж ↔ Фотобарабан» несколько раз подряд (особенно через LAN-браузер, где задержка сети выше)
expected: Код и текст «Шаблон: …» соответствуют ПОСЛЕДНЕМУ выбранному виду, а не более медленно ответившему запросу; выбор шаблона в меню сразу после открытия формы не перетирается автоподстановкой (FE-CR-05/FE-WR-04)
result: [pending]

### 6. Фокус в попапах «Не соответствует шаблону» / «Проверьте буквы в номере»: открыть попап, проверить положение фокуса; нажать «Поправлю»/«Закрыть»/«Взять следующий свободный»/«Без шаблона»
expected: Фокус по умолчанию на «Поправлю»; после закрытия любого попапа — фокус возвращается в поле номера, а не на кнопку «Вставка» (FE-WR-10)
result: [pending]

### 7. Ссылка «Настройки / Организация» из пустого меню «Вставка» (когда шаблонов своего типа нет)
expected: Первый клик показывает предупреждение о потере формы + пункт перехода; второй клик открывает раздел «Организация» (а не «Сеть»), при холодном переходе и когда «Настройки» уже открыты (FE-WR-13)
result: [pending]

### 8. Витрина Buttons/ActionMenu: сравнить `Button variant="ghost" size="md" iconOnly`  и `ActionMenu variant="ghost-md"` в состояниях normal/hover/focus-visible/disabled, светлая и тёмная тема
expected: Визуально неотличимы (общий класс `.tr-btn-ghost-icon`, UI-SPEC §10) — FE-WR-14
result: [pending]

### 9. Подсказка «Занят: …» — очистить поле; создать конфликтующую запись в другой сессии, пока шаблон выбран в текущей форме; удалить выбранный шаблон в Настройках из другой сессии; выбрать стрелкой max+1 и дождаться WS-обновления
expected: Подсказка «Занят» пропадает при очистке поля; список «Вставка» обновляет «→ следующий номер» после чужого создания; выбор снимается, если шаблон удалён; выбор max+1 переживает WS-обновление (FE-WR-03/05/06)
result: [pending]

### 10. Кир/лат попап: ввести кириллическую «С-0005» при существующей латинской «C-0005»
expected: Текст «Номер «С-0005» выглядит так же, как существующий номер «C-0005» (картридж «…»)…», без абзаца про смешение алфавитов; оба номера моноширинным шрифтом (FE-IN-01)
result: [pending]

### 11. Правка шаблона номера, изменённого в другой сессии между открытием модалки и сохранением (оптимистическая блокировка)
expected: Русский toast «Шаблон изменён другим пользователем…», модалка закрывается, список обновлён без ложного success-тоста (FE-WR-15)
result: [pending]

### 12. Многократное открытие/закрытие попапов создания устройства/акта/картриджа (и в Tauri, и в LAN-браузере)
expected: Не растёт число WS-подключений, не появляются лишние тосты (FE-WR-09)
result: [pending]

### 13. Полный сквозной прогон NUM-06..NUM-12 по всем 4 сущностям в РЕАЛЬНОМ cargo tauri dev, ПОСЛЕ пересоздания dev-БД (V042–V044 правились in place — старая БД не пройдёт refinery checksum) и после `pnpm --dir ui build` (для проверки LAN-браузера)
expected: Автоподстановка, кнопка «Вставка», переключатель ↑/↓, все три попапа цепочки D-01, поведение форм правки (D-05), формат `[YYYY]/[MM]-[X]` для акта, континуитет `C-0043` после `C-0001..C-0042`, блокировка отображаемых номеров возвратов (D-06) — ни один из этих сценариев не был проверен в запущенном приложении ни разу за всю фазу (все 4 плана-хоста явно задокументировали Deferred UAT)
result: [pending]

## Summary

total: 13
passed: 0
issues: 0
pending: 13
skipped: 0
blocked: 0

## Gaps
