---
phase: 40.2-inventory-number-templates
plan: 14
type: execute
wave: 6
depends_on: ["40.2-06", "40.2-12", "40.2-11"]
files_modified:
  - ui/src/features/acts/ActFormBody.svelte
  - ui/src/features/acts/ActNumberField.svelte
  - ui/src/features/acts/api.ts
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/tauri_cmds/acts.rs
  - crates/trackly-app/src/http/acts.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-core/src/ports/acts.rs
  - crates/trackly-app/tests/export_bindings.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: false
requirements: [NUM-06, NUM-07, NUM-08, NUM-09, NUM-10, NUM-11, NUM-12, NUM-14]

must_haves:
  truths:
    - "ActNumberField.svelte и acts_peek_next_number удалены целиком после переключения на общий компонент — не остаются мёртвым кодом (финальная зачистка временной обратной совместимости плана 06)"
    - "Правка акта (D-05) показывает 'Номер занят' без кнопки 'Взять следующий свободный' и предупреждение кир/лат — 'Не соответствует шаблону' в правке не бывает"
    - "Цепочка попапов при создании акта идёт строго занят → не по шаблону → кир/лат (D-01)"
  artifacts:
    - path: "ui/src/features/acts/ActFormBody.svelte"
      provides: "поле № → NumberTemplateField в создании; правка — обычный Input + occupied/warning без mismatch"
---

<objective>
Подключить `NumberTemplateField`/попапы к попапу «Новый акт», И финально зачистить временную
обратную совместимость плана 06: удалить `ActNumberField.svelte`, `acts_peek_next_number`
(команда + `build_acts_peek_next_number` + порт-метод, если что-то из этого пережило план 06 как
шим) — план 06 намеренно оставил их работающими через `NumberTemplateService`, чтобы не сломать
волну 5; теперь, когда фронтенд переключается на общий компонент, старые имена становятся мёртвым
кодом и должны исчезнуть, что и есть NUM-13's grep-гейт в его финальном виде для актов.

Purpose: последний штрих NUM-13 для актов + первое подключение шаблонов к форме ПРАВКИ (D-05,
единственная форма правки в фазе, которая получает occupied+warning без mismatch).
Output: `ActFormBody.svelte` на `NumberTemplateField`, `ActNumberField.svelte` удалён.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.2-inventory-number-templates/40.2-UI-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-CONTEXT.md

<interfaces>
`NumberTemplateField`/`NumberTakenPopup`/`NumberMismatchPopup`/`NumberScriptWarningPopup` — как в
плане 13. `act_service.rs::create()` уже возвращает `ActSaveOutcome` (план 06) — этот план
подключает к нему реальный UI. `act_service.rs::update()` нужно РАСШИРИТЬ (план 06 сделал это для
`create()`; `update()` для occupied/script-mix — если план 06 уже это сделало, здесь только
подключить UI; если план 06 ограничился `create()` — доделать `update()` здесь, читать план 06
SUMMARY перед началом, чтобы не дублировать).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NumberTemplateField в создании акта, occupied/warning в правке (D-05)</name>
  <files>ui/src/features/acts/ActFormBody.svelte, ui/src/features/acts/api.ts</files>
  <read_first>
    ui/src/features/acts/ActFormBody.svelte,
    ui/src/features/acts/ActNumberField.svelte,
    ui/src/lib/components/NumberTemplateField.svelte,
    crates/trackly-app/src/services/act_service.rs
  </read_first>
  <action>
    В `ActFormBody.svelte`: в РЕЖИМЕ СОЗДАНИЯ заменить `<ActNumberField>` (строка ~269) на
    `<NumberTemplateField context="act_create" value={numberValue} onSelectedTemplateChange={...}
    invalid={!!fieldErrors['number']} errorMessage={fieldErrors['number']} />`; убрать переменную
    `numberOverride`/логику "auto vs override" (`payload.number_override` заменяется на
    `payload.number_input` — план 06 уже поменяло DTO, здесь только клиентская сторона); `.grid-3`
    ячейка номера занимает первую колонку, `max-width:200px` у поля снимается (UI-SPEC §1). В
    РЕЖИМЕ ПРАВКИ — обычный `Input type="text"` (номер — уже TEXT после плана 06), БЕЗ
    `NumberTemplateField`/шаблонов (out of scope), но С occupied+script-mix проверкой при
    сохранении (D-05): при отправке правки, если сервер вернёт `ActSaveOutcome::
    NeedsConfirmation(warning)` с `kind:'script_mix'` — показать `NumberScriptWarningPopup`;
    `kind:'mismatch'` НЕ должен возникать в правке (сервер не проверяет mismatch без
    `template_id`, которого в payload правки нет) — если всё же придёт, обработать как no-op
    ошибку с toast (defensive, не должно происходить). Occupied-конфликт в правке →
    `NumberTakenPopup canTakeNext={false}` (D-05 — без «Взять следующий свободный»).
    Обновить `ui/src/features/acts/api.ts` под новую сигнатуру `create`/`update` (принимают
    `number_input`, возвращают `ActSaveOutcome`).
  </action>
  <acceptance_criteria>
    <automated>pnpm --dir ui lint</automated>
  </acceptance_criteria>
  <done>Создание акта использует общий компонент, правка получила occupied+script-mix без mismatch и без "взять следующий свободный", как того требует D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Оркестрация D-01 для акта + финальная зачистка ActNumberField/acts_peek_next_number</name>
  <files>
    ui/src/features/acts/ActFormBody.svelte,
    ui/src/features/acts/ActNumberField.svelte,
    crates/trackly-app/src/services/act_service.rs,
    crates/trackly-app/src/tauri_cmds/acts.rs,
    crates/trackly-app/src/http/acts.rs,
    crates/trackly-app/src/specta_export.rs,
    crates/trackly-core/src/ports/acts.rs,
    crates/trackly-app/tests/export_bindings.rs,
    crates/trackly-app/tests/role_endpoint_matrix.rs
  </files>
  <read_first>
    ui/src/features/acts/ActFormBody.svelte,
    crates/trackly-app/src/tauri_cmds/acts.rs,
    crates/trackly-app/src/http/acts.rs,
    crates/trackly-app/src/specta_export.rs,
    crates/trackly-app/tests/export_bindings.rs,
    crates/trackly-app/tests/role_endpoint_matrix.rs
  </read_first>
  <action>
    Реализовать в `ActFormBody.svelte::handleSubmit` (режим создания) ту же оркестрацию D-01, что
    план 13 Task 2 сделал для устройств (occupied→mismatch→script-mix, `activePopup` state
    machine, «Продолжить» переустанавливает confirm-флаг и повторяет вызов) — использовать
    ИДЕНТИЧНУЮ структуру состояния ради консистентности между тремя формами (не изобретать
    другой паттерн). `NumberTakenPopup` карточка для акта заполняется по правилу
    Copywriting Contract: «Вид» («Акт передачи»/«Акт возврата»), «Дата», «Передал», «Принял» —
    маппинг из `OccupyingRecordDto` (план 04: `subtitle` несёт «Передал: X · Принял: Y» одной
    строкой — компонент `NumberTakenPopup`, план 11, уже принимает это как готовую `subtitle`
    строку, ничего разбирать не нужно).

    Финальная зачистка (NUM-13 для актов, теперь безопасна — фронтенд больше не вызывает старое
    имя): удалить файл `ActNumberField.svelte`; удалить `acts_peek_next_number`/
    `build_acts_peek_next_number` (tauri_cmds/acts.rs, http/acts.rs, `router()` маршрут,
    `specta_export.rs` регистрацию, `tests/export_bindings.rs` ассерт) и
    `act_service.rs::peek_next_number()` (если план 06 оставил её как временный шим — читать план
    06 SUMMARY перед удалением, чтобы понять точное состояние). Убрать соответствующий Case из
    `role_endpoint_matrix.rs`, если там был кейс именно на это имя команды (не путать с новыми
    кейсами группы B из плана 05, которые НЕ трогать).
  </action>
  <acceptance_criteria>
    - `grep -rn "ActNumberField\|acts_peek_next_number\|build_acts_peek_next_number" crates/ ui/src` — пусто.
    - `cargo test -p trackly-app --test export_bindings --test role_endpoint_matrix` зелёный.
    <automated>pnpm --dir ui lint && pnpm --dir ui build</automated>
  </acceptance_criteria>
  <done>Полная цепочка D-01 работает для акта, старый компонент и команда физически удалены, ничего в кодовой базе на них не ссылается.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Живая проверка попапа «Новый акт» + правки акта</name>
  <what-built>
    Автоподстановка/цепочка попапов в «Новый акт»; occupied+script-mix (без mismatch, без «Взять
    следующий свободный») в правке существующего акта.
  </what-built>
  <files></files>
  <action>Автоматического действия нет — эта задача целиком ручная проверка живого приложения, см. <how-to-verify> ниже.</action>
  <how-to-verify>
    1. Создать шаблон типа «Акты» с маской `[YYYY]/[MM]-[X]` — открыть «Новый акт», убедиться
       номер подставляется в формате `2026/09-N`.
    2. Провести акт через всю цепочку попапов (занят/не по шаблону/кир-лат), как в плане 13
       Task 3, шаги 3-5.
    3. Открыть правку существующего акта, ввести номер уже занятого другого акта — «Номер занят»
       БЕЗ кнопки «Взять следующий свободный».
    4. Создать акт, вернуть его — убедиться отображаемый номер возврата (`Nв`) блокирует создание
       НОВОГО акта передачи с таким же номером (D-06).
  </how-to-verify>
  <resume-signal>Опишите результат: "approved" либо перечислите расхождения по шагам 1-4.</resume-signal>
  <verify><human-check>Подтверждено пользователем в ответ на resume-signal.</human-check></verify>
  <done>Пользователь подтвердил "approved" либо перечисленные расхождения устранены и проверка повторена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Форма → act_service::create/update | Как в плане 06 — confirm-флаги непроверены, DB unique index — финальный арбитр |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-24 | Elevation of Privilege | Удаление `acts_peek_next_number` могло бы оставить осиротевший маршрут в `router()`, доступный без проверки, если удаление сделано частично | mitigate | Grep-проверка в acceptance criteria покрывает все 4 места (tauri_cmds, http, specta_export, export_bindings) — частичное удаление даёт non-zero grep, гейт не проходит |
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui build`, `cargo test -p trackly-app --test export_bindings
--test role_endpoint_matrix`, `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app
--test acts_numbering` — все зелёные. Живая проверка (Task 3) обязательна.
</verification>

<success_criteria>
Попап «Новый акт» и форма правки полностью реализуют свою часть NUM-06..14, старый компонент и
команда удалены без остатка.
</success_criteria>

<output>
Create `.planning/phases/40.2-inventory-number-templates/40.2-14-SUMMARY.md` when done
</output>
