---
phase: 40.2-inventory-number-templates
plan: 13
type: execute
wave: 6
depends_on: ["40.2-08", "40.2-12", "40.2-11"]
files_modified:
  - ui/src/features/devices/DeviceFormBody.svelte
  - ui/src/features/devices/api.ts
autonomous: false
requirements: [NUM-06, NUM-07, NUM-08, NUM-09, NUM-10, NUM-11, NUM-12]

must_haves:
  truths:
    - "Цепочка попапов при сохранении идёт строго в порядке занят → не по шаблону → кир/лат, максимум один попап открыт одновременно (D-01)"
    - "При qty>1 (массовое создание) поле номера отключено как и раньше — цепочка подтверждений применяется только к одиночному созданию (qty=1)"
    - "Правка устройства/принтера (D-05) проверяет occupied+script-mix при сохранении номера — БЕЗ mismatch-попапа, БЕЗ кнопки «Взять следующий свободный», БЕЗ запоминания шаблона (память контекста существует только для create)"
  artifacts:
    - path: "ui/src/features/devices/DeviceFormBody.svelte"
      provides: "поле Инвентарный № → NumberTemplateField в режиме создания (D-01 через devices_create_single_with_number_check); режим правки — occupied+script-mix без mismatch (D-05) через devices_update"
---

<objective>
Подключить `NumberTemplateField` (план 12) и попапы цепочки D-01/D-05 (план 11) к попапу
«Новое устройство»/«Новый принтер» И к форме ПРАВКИ устройства/принтера
(`DeviceFormBody.svelte`). Создание — единственная из трёх create-форм волны 6, где массовое
создание (qty>1) уже существует и должно остаться незатронутым — поле номера участвует в
цепочке подтверждений ТОЛЬКО при qty=1. Правка — D-05 ЛОКИРОВАН и покрывает формы правки
устройства/принтера: `device_service.rs::update()` (план 08 Task 2) уже возвращает
`DeviceSaveOutcome` с occupied+script-mix, БЕЗ mismatch и БЕЗ «взять следующий свободный» —
этот план подключает к нему реальный UI, зеркаля то, что план 14 Task 1 делает для акта.

Purpose: первое реальное end-to-end использование всей фазы для одной из четырёх сущностей, и
единственная из трёх сущностей волны 6, где отдельная задача нужна специально для правки (акт —
план 14 Task 1 совмещает создание+правку в одной задаче; устройство здесь разделено на
отдельную задачу из-за qty-логики в create-ветке, усложняющей совмещение).
Output: рабочий попап создания устройства/принтера с автоподстановкой номера и полной цепочкой
подтверждений; рабочая форма правки с occupied+script-mix проверкой номера (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.2-inventory-number-templates/40.2-UI-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-CONTEXT.md

<interfaces>
`NumberTemplateField` (план 12): пропсы `context`, `value` (bindable), `placeholder`, `invalid`,
`errorMessage`, `disabled`, `onSelectedTemplateChange`. `NumberTakenPopup`/`NumberMismatchPopup`/
`NumberScriptWarningPopup` (план 11): пропсы описаны в их файлах — читать напрямую.
`devices_create_single_with_number_check(new: DeviceNew, number_input: NumberFieldInput,
context: TemplateContextDto) -> Result<DeviceSaveOutcome, AppError>` (план 08 Task 2, новый
эндпоинт, `MutateDevices`). `devices.bulkCreate` (существующий) остаётся для `qty>1`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NumberTemplateField вместо Input в режиме создания</name>
  <files>ui/src/features/devices/DeviceFormBody.svelte, ui/src/features/devices/api.ts</files>
  <read_first>
    ui/src/features/devices/DeviceFormBody.svelte,
    ui/src/lib/components/NumberTemplateField.svelte,
    crates/trackly-app/src/dto/device.rs
  </read_first>
  <action>
    В `ui/src/features/devices/api.ts` добавить обёртку `devices.createSingleWithNumberCheck(...)`
    вокруг сгенерированной привязки `devices_create_single_with_number_check` (по образцу
    существующих обёрток `devices.bulkCreate`/`devices.update` в этом же файле).

    В `DeviceFormBody.svelte`, В РЕЖИМЕ СОЗДАНИЯ (`!isEdit`), заменить слот «Инвентарный №»
    (строка ~320-329, `Input` с `id="f-inv"`) на `NumberTemplateField` с `context={typeId ===
    PRINTER_TYPE_ID ? 'printer_create' : 'device_create'}` (реактивно — `$derived`, меняется
    сразу при переключении типа устройства/принтера в форме, если такое переключение доступно в
    этой форме — читать текущий код на предмет того, есть ли переключатель типа ВНУТРИ формы
    создания или он выбирается ДО открытия попапа; в любом случае `context` должен быть
    реактивным `$derived` от `typeId`, не статичным на момент монтирования). `disabled={!
    quantityDisabled}` — ПРОВЕРИТЬ по существующей переменной `quantityDisabled`: поле номера
    должно быть недоступно/скрыто, когда `quantity > 1` (текущее поведение — количество
    disabled, если инв/серийный заполнен; обратное правило теперь тоже нужно: при quantity>1
    поле номера показывать `disabled` состоянием `NumberTemplateField`, т.к. массовое создание
    не поддерживает шаблонный номер — читать текущую логику `quantityDisabled`/`quantity` в этом
    файле, чтобы не сломать существующее правило, только ДОБАВИТЬ симметричное). В `f-inv` ряду
    сохранить `flex: 2 1 0` для слота инвентарного номера (UI-SPEC Discretion #12), `1 1 0` для
    серийного номера и количества — не менять их. В режиме ПРАВКИ (`isEdit`) поле «Инвентарный №»
    ОСТАЁТСЯ обычным `Input` (НЕ `NumberTemplateField` — шаблоны/«Вставка»/↑↓ вне scope правки,
    SPEC исключает шаблоны из форм правки) — но occupied+script-mix проверка ПРИ СОХРАНЕНИИ
    ДОБАВЛЯЕТСЯ (D-05, ЛОКИРОВАНО — устройство/принтер НЕ исключение из D-05, только шаблон/
    автоподстановка исключены) — реализация оркестрации самой проверки перенесена в Task 3
    этого плана, не сюда (это задача 1 — только разметка полей).
  </action>
  <acceptance_criteria>
    <automated>pnpm --dir ui lint</automated>
  </acceptance_criteria>
    <verify>
    <automated>pnpm --dir ui lint</automated>
  </verify>
<done>Поле номера в режиме создания — NumberTemplateField с реактивным context, режим правки не тронут, поведение quantity-блокировки симметрично распространено на новое поле.</done>
</task>

<task type="auto">
  <name>Task 2: Оркестрация D-01 в handleSubmit — occupied → mismatch → script-mix</name>
  <files>ui/src/features/devices/DeviceFormBody.svelte</files>
  <read_first>ui/src/features/devices/DeviceFormBody.svelte</read_first>
  <action>
    Ввести состояние попапов: `activePopup: 'taken' | 'mismatch' | 'scriptWarning' | null`,
    `pendingConfirm: {mismatch: boolean; scriptMix: boolean}`, `selectedTemplateId: number |
    null` (из `onSelectedTemplateChange`). Переписать ветку создания (`qty===1`) в
    `handleSubmit()`: вызвать `devices.createSingleWithNumberCheck(newDevice, {value:
    inventoryNo, templateId: selectedTemplateId, confirmMismatch: pendingConfirm.mismatch,
    confirmScriptMix: pendingConfirm.scriptMix}, context)`. Обработать ответ:
    - `{outcome:'created', ...}` → как раньше (toast успеха, `onSaved`, сброс `pendingConfirm`).
    - `{outcome:'needs_confirmation', kind:'mismatch', ...}` → `activePopup='mismatch'`
      (`NumberMismatchPopup` с `number=inventoryNo`, `mask=<маска выбранного шаблона — взять из
      уже загруженного списка шаблонов поля, либо передать через ещё один колбэк с поля>`,
      `contextLabel='Новое устройство'`/`'Новый принтер'`).
    - `kind:'script_mix'` → `activePopup='scriptWarning'` (`NumberScriptWarningPopup`).
    Ошибка `AppError::Conflict` (occupied, из финальной транзакции — гонка ИЛИ первая попытка
    без пред-проверки) → `activePopup='taken'` (`NumberTakenPopup canTakeNext={selectedTemplateId
    !== null}` — данные карточки занявшей записи получить ОТДЕЛЬНЫМ вызовом
    `number_templates_is_occupied(context, inventoryNo, null)` ПЕРЕД показом попапа — RESEARCH
    Security Domain: сама ошибка создания не обязана нести карточку, достаточно факта отказа).
    «Поправлю»/«Закрыть» во всех трёх попапах → `activePopup=null`, `pendingConfirm` сброшен,
    фокус в поле номера (компонент `NumberTemplateField` должен экспортировать способ вернуть
    фокус — добавить `export function focus()` или `bind:this` — минимальное дополнение к плану
    12, если там не было явного метода фокуса). «Продолжить» в mismatch/script-warning →
    `pendingConfirm.mismatch=true`/`pendingConfirm.scriptMix=true`, `activePopup=null`, повторный
    вызов `handleSubmit()` (та же функция, теперь сервер видит confirm-флаг и либо переходит к
    следующей проверке цепочки, либо создаёт запись). «Взять следующий свободный» в
    `NumberTakenPopup` → запросить `number_templates_peek_next(selectedTemplateId, context)`,
    `inventoryNo = rendered`, `activePopup=null`, форма НЕ сохраняется автоматически (пользователь
    жмёт «Создать» повторно, per UI-SPEC §6).
  </action>
  <acceptance_criteria>
    <automated>pnpm --dir ui lint</automated>
  </acceptance_criteria>
    <verify>
    <automated>pnpm --dir ui lint</automated>
  </verify>
<done>Полная цепочка occupied→mismatch→script-mix работает через реальный backend-вызов, не более одного попапа открыто одновременно, «Продолжить» корректно продвигает цепочку дальше, а не повторяет тот же попап.</done>
</task>

<task type="auto">
  <name>Task 3: Правка устройства/принтера — occupied+script-mix без mismatch, без «взять следующий свободный» (D-05)</name>
  <files>ui/src/features/devices/DeviceFormBody.svelte, ui/src/features/devices/api.ts</files>
  <read_first>
    ui/src/features/devices/DeviceFormBody.svelte,
    ui/src/lib/components/NumberTakenPopup.svelte,
    ui/src/lib/components/NumberScriptWarningPopup.svelte,
    crates/trackly-app/src/services/device_service.rs
  </read_first>
  <action>
    В `ui/src/features/devices/api.ts` обновить сигнатуру обёртки `devices.update(...)` — теперь
    возвращает `DeviceSaveOutcome` (`{outcome:'created', device: DeviceDto}` — да, поле называется
    `created` и в ответе на update тоже, по конвенции плана 04 Task 1, это не переименовывается
    под "updated" — сверить точное имя поля с реальным сгенерированным типом при исполнении —
    `{outcome:'needs_confirmation', warning: NumberWarningDto}`), а не голый `DeviceDto`.

    В `DeviceFormBody.svelte::handleSubmit()`, ветка `isEdit && target` (там, где сегодня
    `const updated = await devices.update(target.id, currentVersion, patch); currentVersion =
    updated.version; pushToast('success', 'Устройство сохранено');`): ввести то же состояние
    попапов, что Task 2 завело для создания — переиспользовать ОДНО И ТО ЖЕ состояние
    `activePopup`/`pendingConfirm` (не заводить второй набор переменных для edit-ветки; `
    selectedTemplateId` в edit-ветке не используется вовсе, т.к. шаблонов в правке нет — D-05).
    `patch.inventory_no` заворачивается в `number_input: {value: inventoryNo, templateId: null,
    confirmMismatch: false, confirmScriptMix: pendingConfirm.scriptMix}` (`templateId` ВСЕГДА
    `null` в правке — нет понятия выбранного шаблона; `confirmMismatch` физически не используется
    сервером в `update()`, но поле присутствует в `NumberFieldInput` структуре — передавать
    `false` константой). Обработать ответ:
    - `{outcome:'created', device}` (т.е. успех) → как раньше (`currentVersion = device.version`,
      toast «Устройство сохранено», `onSaved`).
    - `{outcome:'needs_confirmation', warning}` → `warning.kind` в правке может быть ТОЛЬКО
      `'script_mix'` (сервер `update()` по плану 08 Task 2 никогда не проверяет mismatch — если
      всё же придёт `'mismatch'`, это баг сервера, обработать defensively: показать generic toast
      «Не удалось сохранить» и НЕ показывать `NumberMismatchPopup`, т.к. в правке для него нет
      осмысленного `contextLabel`/маски) → `activePopup='scriptWarning'`
      (`NumberScriptWarningPopup`, те же пропсы, что в Task 2, `number=inventoryNo`).
    - Ошибка `AppError::Conflict` (occupied) → `activePopup='taken'` с `NumberTakenPopup
      canTakeNext={false}` (D-05 — правка НИКОГДА не показывает «Взять следующий свободный»,
      т.к. в правке нет активного шаблона, из которого можно предложить число) — карточка
      занявшей записи получить через `number_templates_is_occupied('device_create'-или-
      'printer_create' в зависимости от `typeId`, inventoryNo, exclude_id=target.id)` ПЕРЕД
      показом попапа (тот же `context` параметр здесь используется ТОЛЬКО ради выбора правильного
      физического пространства для карточки — `is_occupied` внутри читает по `TemplateType`,
      которую `context` определяет, а не потому что в правке есть контекст-память).
    - «Закрыть» в `NumberTakenPopup` (единственная кнопка, `canTakeNext=false`) → `activePopup=
      null`, фокус в поле «Инвентарный №», поле помечается `invalid` с текстом «Номер уже занят.»
      до следующей правки (тот же паттерн, что и в create-ветке).
    - «Поправлю»/«Продолжить» в `NumberScriptWarningPopup` → `activePopup=null`;
      «Продолжить» → `pendingConfirm.scriptMix=true`, повторный вызов `handleSubmit()`.
    НЕ вызывать `remember_context`/не передавать `template_id` — правка НЕ участвует в NUM-08
    (память контекста существует только для 5 create-контекстов).
  </action>
  <acceptance_criteria>
    Правка устройства на номер, занятый другим живым устройством, открывает `NumberTakenPopup`
    БЕЗ кнопки «Взять следующий свободный»; правка на номер со смешением алфавитов открывает
    `NumberScriptWarningPopup`; `NumberMismatchPopup` никогда не открывается из правки; успешная
    правка без изменения номера ведёт себя как раньше (без лишних popup-проверок, план 08 Task 2
    Task 2 пропускает occupied/warning-цепочку, если номер не изменился).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint</automated>
  </verify>
  <done>Правка устройства/принтера проверяет occupied+script-mix при сохранении номера, никогда не показывает mismatch-попап или «взять следующий свободный», не запоминает шаблон (D-05 полностью реализован в UI).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Живая проверка попапа «Новое устройство»/«Новый принтер» + правки</name>
  <what-built>
    Автоподстановка номера, кнопка «Вставка», переключатель ↑/↓ и полная цепочка попапов при
    сохранении в реальном попапе создания устройства/принтера. Плюс: occupied+script-mix
    проверка номера при ПРАВКЕ устройства/принтера (D-05) — без mismatch, без «взять следующий
    свободный», без запоминания шаблона.
  </what-built>
  <files></files>
  <action>Автоматического действия нет — эта задача целиком ручная проверка живого приложения, см. <how-to-verify> ниже.</action>
  <how-to-verify>
    1. Создать шаблон типа «Устройства и принтеры» через Настройки (если ещё не создан).
    2. Открыть «Новое устройство» — поле номера подставляет первое свободное число, кнопка
       «Вставка» показывает список шаблонов.
    3. Ввести занятый номер существующего устройства — сохранение открывает «Номер занят» с
       карточкой этого устройства.
    4. Ввести номер, не соответствующий выбранному шаблону — сохранение открывает «Не
       соответствует шаблону»; «Продолжить» сохраняет как есть.
    5. Ввести номер со смешением кириллицы/латиницы — сохранение открывает «Проверьте буквы в
       номере»; «Продолжить» сохраняет.
    6. Увеличить «Количество» — поле номера скрывается/блокируется, как раньше.
    7. Открыть «Новый принтер» — меню «Вставка» показывает шаблоны ТОЛЬКО типа «Устройства и
       принтеры» (общий с устройствами).
    8. Открыть ПРАВКУ существующего устройства, ввести номер, уже занятый другим живым
       устройством — «Номер занят» открывается БЕЗ кнопки «Взять следующий свободный» (в отличие
       от шага 3, где кнопка есть).
    9. В той же правке ввести номер со смешением кириллицы/латиницы — открывается «Проверьте
       буквы в номере»; «Продолжить» сохраняет правку.
    10. Убедиться, что в правке НЕТ кнопки «Вставка» и НЕТ попапа «Не соответствует шаблону» ни
        при каком вводе (шаблоны полностью исключены из правки, D-05).
  </how-to-verify>
  <resume-signal>Опишите результат: "approved" либо перечислите расхождения по шагам 1-10.</resume-signal>
  <verify><human-check>Подтверждено пользователем в ответ на resume-signal.</human-check></verify>
  <done>Пользователь подтвердил "approved" либо перечисленные расхождения устранены и проверка повторена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Форма → devices_create_single_with_number_check | confirm-флаги от клиента непроверены — сервер (план 08) обязан заново вычислить occupied/script-mix независимо от присланных флагов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-23 | Tampering | Клиент мог бы отправить `confirmScriptMix: true` без реального показа попапа пользователю | accept | Подтверждение — не гейт безопасности, а UX-подтверждение неоднозначного, но РАЗРЕШЁННОГО ввода (SPEC явно допускает сохранение с предупреждением) — нет security-инварианта, который это нарушает |
</threat_model>

<verification>
`pnpm --dir ui lint` и `pnpm --dir ui build` зелёные. Живая проверка (Task 4) обязательна перед
закрытием плана.
</verification>

<success_criteria>
Попап «Новое устройство»/«Новый принтер» полностью реализует NUM-06/07/08/09/10/11/12 для этой
сущности; форма правки устройства/принтера полностью реализует D-05 (occupied+script-mix, без
mismatch, без «взять следующий свободный», без памяти шаблона); всё живьём проверено.
</success_criteria>

<output>
Create `.planning/phases/40.2-inventory-number-templates/40.2-13-SUMMARY.md` when done
</output>
