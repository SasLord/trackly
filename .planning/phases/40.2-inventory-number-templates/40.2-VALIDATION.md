---
phase: 40.2
slug: inventory-number-templates
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-09-18
---

# Phase 40.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust: `cargo test`; UI: `svelte-check` + `eslint` + `prettier --check` + `node scripts/check-*.mjs` via `pnpm --dir ui lint` |
| **Config file** | `crates/*/Cargo.toml`; `ui/package.json` (`lint` script) |
| **Quick run command** | `cargo test -p <crate> <module>` (одна cargo-сессия за раз) / `pnpm --dir ui lint` |
| **Full suite command** | `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test --workspace -- --skip login_remember_persistent_cookie` + `pnpm --dir ui build && pnpm --dir ui lint` |
| **Estimated runtime** | ~300 seconds |

---

## Sampling Rate

- **After every task commit:** targeted `cargo test -p <crate> <module>` for touched files, or `pnpm --dir ui lint` for UI tasks
- **After every plan wave:** full suite command
- **Before `/gsd-verify-work`:** Full suite must be green + manual UAT items below
- **Max feedback latency:** 300 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled by planner/executor) | | | NUM-01..16 | | | | | | ⬜ pending |

Reference map (from RESEARCH.md § Validation Architecture):

| Req | Test Type | Command |
|-----|-----------|---------|
| NUM-01 | integration | `cargo test -p trackly-app number_template_crud` |
| NUM-02 | unit | `cargo test -p trackly-core mask` |
| NUM-03/04/05 | unit/integration (table-driven from SPEC acceptance numbers) | `cargo test -p trackly-app compute_next` |
| NUM-06/07 | manual (Svelte runtime) | live UAT |
| NUM-08 | integration | `cargo test -p trackly-app template_context_memory` |
| NUM-09 | integration (race) | `cargo test -p trackly-app number_uniqueness` |
| NUM-10/11/12 | unit (DTO) + manual (UI) | `cargo test -p trackly-app number_warnings` |
| NUM-13/14/15 | migration integration + grep gate | `cargo test -p trackly-infra migration` |
| NUM-16 | integration | `cargo test -p trackly-app devices_csv_import` |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/trackly-core` mask parse/validate module + unit tests — NUM-02
- [ ] homoglyph/script-mix detection + unit tests — NUM-12
- [ ] `crates/trackly-app/tests/number_templates_crud.rs` — NUM-01
- [ ] `crates/trackly-app/tests/number_templates_compute_next.rs` — NUM-03/04/05
- [ ] `crates/trackly-app/tests/number_uniqueness.rs` — NUM-09..12
- [ ] rewrite `acts_numbering.rs`, `cartridges_numbering.rs` — NUM-13/14
- [ ] extend `devices_csv_import.rs` — NUM-16
- [ ] migration integration test — NUM-13/14/15
- [ ] `role_endpoint_matrix.rs` cases for template CRUD vs use (both transports)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Автоподстановка номера в 4 попапах | NUM-06 | Svelte runtime не покрыт статическими гейтами | Открыть каждый попап в запущенном приложении, проверить подставленный номер |
| Переключатель ↑/↓ | NUM-07 | Svelte runtime | Создать разрыв, переключить, проверить значение |
| Цепочка попапов занят/несовпадение/смешение алфавитов | NUM-10/11/12 | UI-поток | Ввести занятый / не по шаблону / с кириллицей номер |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 300s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
