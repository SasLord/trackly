---
phase: 40.2-inventory-number-templates
plan: 15
type: execute
wave: 6
depends_on: ["40.2-07", "40.2-12", "40.2-11"]
files_modified:
  - ui/src/features/cartridges/CartridgeFormBody.svelte
  - ui/src/features/cartridges/api.ts
autonomous: false
requirements: [NUM-06, NUM-07, NUM-08, NUM-09, NUM-10, NUM-11, NUM-12]

must_haves:
  truths:
    - "Смена 'Что добавляем' (kindId 1↔2) переключает context между cartridge_create/drum_create и подставляет первое свободное запомненного шаблона нового вида — значение поля заменяется ВСЕГДА, не только когда не правили вручную (SPEC п.8 приёмка, UI-SPEC Discretion #8)"
    - "Цепочка попапов при создании картриджа/фотобарабана идёт строго занят → не по шаблону → кир/лат (D-01)"
    - "Правка картриджа/фотобарабана не меняется — код не редактируется, out of scope (SPEC boundaries)"
  artifacts:
    - path: "ui/src/features/cartridges/CartridgeFormBody.svelte"
      provides: "поле 'Код' → NumberTemplateField, реактивный context по kindId, оркестрация D-01"
---

<objective>
Подключить `NumberTemplateField`/попапы к попапу «Новый картридж/фотобарабан», с особым
вниманием к NUM-08's самому нетривиальному правилу фазы: смена переключателя «Что добавляем»
(`kindId` 1↔2) обязана переключить `context` поля номера И ЗАМЕНИТЬ значение на первое свободное
НОВОГО контекста БЕЗУСЛОВНО (в отличие от общего правила D-14 «заменять только если не
правили вручную») — SPEC явно требует именно замену при смене вида, т.к. коды двух видов живут в
разных последовательностях.

Purpose: последний из трёх wiring-планов, завершает end-to-end реализацию всех 16 требований
фазы.
Output: рабочий попап «Новый картридж/фотобарабан» с полной автоподстановкой и цепочкой D-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40.2-inventory-number-templates/40.2-UI-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-SPEC.md
@.planning/phases/40.2-inventory-number-templates/40.2-CONTEXT.md

<interfaces>
Как в планах 13/14 — `NumberTemplateField`, три попапа плана 11, `CartridgeSaveOutcome` (план 07)
через `cartridges.create(...)` (обновить `ui/src/features/cartridges/api.ts` под новую сигнатуру
`number_input`/`CartridgeSaveOutcome`, по образцу того, что план 13/14 уже сделали для своих
API-обёрток — держать структуру идентичной).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NumberTemplateField с реактивным context по kindId</name>
  <files>ui/src/features/cartridges/CartridgeFormBody.svelte, ui/src/features/cartridges/api.ts</files>
  <read_first>
    ui/src/features/cartridges/CartridgeFormBody.svelte,
    ui/src/lib/components/NumberTemplateField.svelte
  </read_first>
  <action>
    В `CartridgeFormBody.svelte`, поле «Код» (заменяет текущий `Input`/`code` state, строка
    ~173-region) → `NumberTemplateField context={kindId === 2 ? 'drum_create' :
    'cartridge_create'} value={code} placeholder={codePlaceholder} onSelectedTemplateChange=
    {...} invalid={!!fieldErrors['code']} errorMessage={fieldErrors['code']}`. `context` —
    `$derived` от `kindId`, реактивно меняется при `handleKindChange`. Убрать текст-подсказку
    «Будет присвоен автоматически…», если он ещё присутствует где-то в этом файле (UI-SPEC
    «Удаляемые строки» — автогенерации больше нет). В `handleKindChange(v)`, ПОСЛЕ смены
    `kindId`, ЯВНО запросить у `NumberTemplateField` смену контекста и БЕЗУСЛОВНУЮ замену
    значения на первое свободное нового контекста (это ОТЛИЧАЕТСЯ от общего правила D-14 плана
    12, где замена условна на "не правили вручную" — здесь замена ВСЕГДА, т.к. смена вида — явное
    намеренное действие пользователя, а не фоновое событие инвалидации; реализовать либо через
    доп. проп `NumberTemplateField`'а `forceRefillOnContextChange?: boolean = false`
    (устанавливается `true` в этом вызове), либо простым пересозданием компонента через `{#key
    context}` вокруг `<NumberTemplateField>` — `{#key}` проще и не требует доп. пропа в плане 12,
    предпочесть его, если поведение при пересоздании компонента (полный ремонт autofill-эффекта
    из плана 12 Task 2) даёт тот же результат, что и «первое свободное нового контекста»). Обновить
    `cartridges/api.ts` под `code`→`number_input`/`CartridgeSaveOutcome`.
  </action>
  <acceptance_criteria>
    <automated>pnpm --dir ui lint</automated>
  </acceptance_criteria>
    <verify>
    <automated>pnpm --dir ui lint</automated>
  </verify>
<done>Поле «Код» переключает контекст при смене kindId и безусловно подставляет первое свободное нового вида; правка не тронута.</done>
</task>

<task type="auto">
  <name>Task 2: Оркестрация D-01 для картриджа/фотобарабана</name>
  <files>ui/src/features/cartridges/CartridgeFormBody.svelte</files>
  <read_first>ui/src/features/cartridges/CartridgeFormBody.svelte</read_first>
  <action>
    Та же структура state machine, что в планах 13/14 Task 2 (occupied→mismatch→script-mix,
    «Продолжить» устанавливает confirm-флаг и повторяет `handleSubmit`). `NumberTakenPopup`
    карточка для картриджа/фотобарабана — «Вид»/«Модель»/«Место»/«Состояние» из
    `OccupyingRecordDto` (план 04 doc: `kind` ∈ `cartridge|drum`, `title`=модель, `place`,
    `status`=состояние). Убедиться, что `contextLabel` в `NumberMismatchPopup` показывает
    «Новый картридж» или «Новый фотобарабан» в зависимости от ТЕКУЩЕГО `kindId` на момент
    попытки сохранения (не зависит от того, менялся ли вид после последней подстановки).
  </action>
  <acceptance_criteria>
    <automated>pnpm --dir ui lint</automated>
  </acceptance_criteria>
    <verify>
    <automated>pnpm --dir ui lint</automated>
  </verify>
<done>Полная цепочка D-01 работает для обоих видов расходников, карточка занявшей записи корректна для обоих kind.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Живая проверка попапа «Новый картридж/фотобарабан»</name>
  <what-built>
    Автоподстановка/цепочка попапов + переключение контекста по «Что добавляем».
  </what-built>
  <files></files>
  <action>Автоматического действия нет — эта задача целиком ручная проверка живого приложения, см. <how-to-verify> ниже.</action>
  <how-to-verify>
    1. Открыть «Новый картридж» — код подставляется по шаблону «Картриджи» (например
       `C-0006`, если есть `C-0001..C-0005`).
    2. Переключить «Что добавляем» на «Фотобарабан» — код МЕНЯЕТСЯ на первое свободное шаблона
       «Фотобарабаны» (например `D-0002`), даже если вы до этого вручную поправили код
       картриджа.
    3. Провести полную цепочку попапов (занят/не по шаблону/кир-лат), как в предыдущих планах.
    4. Убедиться, что существующая БД с картриджами `C-0001..C-0042` (если доступна тестовая
       среда с такими данными) предлагает `C-0043` — континуитет NUM-13.
  </how-to-verify>
  <resume-signal>Опишите результат: "approved" либо перечислите расхождения по шагам 1-4.</resume-signal>
  <verify><human-check>Подтверждено пользователем в ответ на resume-signal.</human-check></verify>
  <done>Пользователь подтвердил "approved" либо перечисленные расхождения устранены и проверка повторена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Форма → cartridge_service::create | Как в планах 13/14 — confirm-флаги непроверены, DB partial unique index — финальный арбитр |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.2-25 | Tampering | Переключение kindId после того, как пользователь уже подтвердил script-mix/mismatch для СТАРОГО кода | accept | Смена вида безусловно заменяет значение поля (Task 1) — любой ранее подтверждённый confirm-флаг относится к уже замененному значению и сбрасывается вместе с ним на клиенте; сервер в любом случае перепроверяет всё заново при сохранении |
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui build` зелёные. `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1
cargo test -p trackly-app --test cartridges_numbering` зелёный (регрессия от плана 07 не
нарушена). Живая проверка (Task 3) обязательна.
</verification>

<success_criteria>
Попап «Новый картридж/фотобарабан» полностью реализует свою часть NUM-06..12, переключение вида
работает безусловно, континуитет NUM-13 виден живьём.
</success_criteria>

<output>
Create `.planning/phases/40.2-inventory-number-templates/40.2-15-SUMMARY.md` when done
</output>
