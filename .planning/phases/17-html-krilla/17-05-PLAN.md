---
phase: 17-html-krilla
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/src/tauri_cmds/reports.rs
  - crates/trackly-app/src/services/report_service.rs
  - crates/trackly-app/tests/html_report_render.rs
autonomous: true
requirements: [Req-1]
must_haves:
  truths:
    - "Отчёт (экспорт/печать) показывает русские подписи колонок (Номер, Устройства, Сдал, Принял, Локация и т.д.), а не сырые snake_case-ключи (device_name, giver_name...)"
    - "Значения ячеек по-прежнему корректно вычисляются через исходные ключи колонок (row_field не сломан)"
    - "logo_mime валидируется по allowlist ПЕРЕД интерполяцией в data:-URI логотипа — несовпадающий mime отбрасывает логотип, а не встраивает произвольную строку в URI"
  artifacts:
    - path: "crates/trackly-app/src/tauri_cmds/reports.rs"
      provides: "column_labels_for(report_type) — русские подписи, индекс-выровненные с columns_for()"
      contains: "fn column_labels_for"
    - path: "crates/trackly-app/src/services/report_service.rs"
      provides: "export_pdf принимает column_labels; ctx[\"columns\"] строится из labels, row_field — из исходных ключей; logo_mime allowlist перед data:-URI"
      contains: "column_labels"
    - path: "crates/trackly-app/tests/html_report_render.rs"
      provides: "регрессионный тест на русскую подпись заголовка + обновлённые вызовы export_pdf(...) с новым аргументом"
      min_lines: 40
  key_links:
    - from: "crates/trackly-app/src/tauri_cmds/reports.rs::build_reports_export_pdf"
      to: "crates/trackly-app/src/services/report_service.rs::export_pdf"
      via: "column_labels_for(&report_type) передаётся отдельным аргументом column_labels, index-aligned с cols"
      pattern: "column_labels_for\\(&report_type\\)"
    - from: "crates/trackly-app/src/services/report_service.rs::export_pdf"
      to: "crates/trackly-app/templates/report.html"
      via: "ctx[\"columns\"] = column_labels (не сырые ключи)"
      pattern: "\"columns\":\\s*column_labels"
---

<objective>
Закрыть BLOCKER-дефект D-03/CR-01 из 17-VERIFICATION.md: `columns_for(report_type)` в
`tauri_cmds/reports.rs` возвращает snake_case-КЛЮЧИ (`device_name`, `giver_name`, ...), которые
сегодня используются дважды — правильно как аксессоры `row_field(row, col)` для значений ячеек, и
НЕПРАВИЛЬНО как данные для `ctx["columns"]`, которые `report.html` рендерит буквально как
`<th>{{ col }}</th>`. В итоге каждый экспортированный/напечатанный отчёт показывает английские
заголовки колонок вместо русских. Заодно закрывается WR-05 (логотип: `logo_mime` читается из БД и
интерполируется в `data:`-URI с `| safe` без реальной проверки allowlist, хотя комментарии в коде
утверждают обратное) — правка находится в той же функции `export_pdf`, рядом с местом, где
дефект D-03 исправляется.

Purpose: Русский-only v1 UI (CLAUDE.md) требует, чтобы КАЖДЫЙ печатный/экспортный артефакт был на
русском — это не косметика, это функциональный регресс, найденный независимо верификатором и
код-ревьюером (CR-01). WR-05 — defense-in-depth: `save_logo` уже проверяет allowlist при загрузке,
но комментарий в `report.html`/`report_service.rs` утверждает, что allowlist проверяется ЗДЕСЬ,
на чтении — а он не проверяется нигде на этом пути. Правка делает комментарий правдой.

Output: `column_labels_for(report_type)` в `tauri_cmds/reports.rs`; обновлённая сигнатура
`ReportService::export_pdf` с новым параметром `column_labels: &[&str]`, чьи значения (не ключи)
попадают в `ctx["columns"]`; enforced mime-allowlist перед построением `logo_data_uri`; обновлённый
и расширенный `tests/html_report_render.rs` с регрессионным тестом на русскую подпись заголовка.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-html-krilla/17-VERIFICATION.md
@.planning/phases/17-html-krilla/17-REVIEW.md
@.planning/phases/17-html-krilla/17-CONTEXT.md

<interfaces>
Текущая (дефектная) сигнатура `ReportService::export_pdf`
(`crates/trackly-app/src/services/report_service.rs:534-543`) — этот план меняет её:

pub async fn export_pdf(
    &self,
    rows: &ReportResponse,
    report_name: &str,
    period_label: &str,
    org: &OrgSettingsDto,
    logo_bytes: Option<Vec<u8>>,
    logo_mime: Option<String>,
    columns: &[&str],
) -> Result<String, AppError>

Целевая сигнатура (добавлен один параметр в конец — минимизирует диф):

pub async fn export_pdf(
    &self,
    rows: &ReportResponse,
    report_name: &str,
    period_label: &str,
    org: &OrgSettingsDto,
    logo_bytes: Option<Vec<u8>>,
    logo_mime: Option<String>,
    columns: &[&str],        // ключи — по-прежнему источник row_field(row, col)
    column_labels: &[&str],  // NEW: русские подписи — источник ctx["columns"]
) -> Result<String, AppError>

Текущий `columns_for(report_type)` (`crates/trackly-app/src/tauri_cmds/reports.rs:19-41`) —
не меняется, остаётся источником ключей:

fn columns_for(report_type: &str) -> Vec<&'static str> {
    match report_type {
        "device_acts" | "device_returns" =>
            vec!["number", "device_name", "giver_name", "receiver_name", "location_name"],
        "device_in_use" | "device_in_stock" =>
            vec!["device_name", "status_name", "location_name"],
        "cartridge_consumption" | "cartridge_refills" =>
            vec!["code", "model_label", "status_name", "location_name"],
        "cartridge_in_use" | "cartridge_in_stock" =>
            vec!["code", "model_label", "status_name", "location_name"],
        _ => vec!["id"],
    }
}

Источник истины для русских подписей — `ui/src/features/reports/ReportsPage.svelte`'s
`COLUMNS_MAP` (строки 104-155), ключи по каждому типу отчёта (не порядок массива, а
пары key→label):
  acts/returns: number→"Номер", giver_name→"Сдал", receiver_name→"Принял",
    location_name→"Локация", device_name→"Устройства"
  in_use/in_stock: device_name→"Наименование", location_name→"Расположение", status_name→"Статус"
  consumption/refills: code→"Код картриджа", model_label→"Модель", location_name→"Локация"
    (status_name не имеет своей записи в этих двух табах COLUMNS_MAP — берём "Статус" из
    cartridge_in_use/in_stock, см. ниже)
  cartridge_in_use/in_stock: code→"Код", model_label→"Модель", location_name→"Расположение",
    status_name→"Статус"

Существующий вызов `build_reports_export_pdf` (`crates/trackly-app/src/tauri_cmds/reports.rs:181-198`)
— этот план добавляет вызов `column_labels_for` и один новый аргумент в конце
`ctx.reports.export_pdf(...)`.

Место для WR-05 fix — `crates/trackly-app/src/services/report_service.rs:554-561`
(текущий код, УЖЕ читал план):

let logo_data_uri: Option<String> = logo_bytes.map(|bytes| {
    use base64::Engine;
    let mime = logo_mime.as_deref().unwrap_or("image/png");
    format!("data:{mime};base64,{}", base64::engine::general_purpose::STANDARD.encode(bytes))
});

Существующий allowlist на записи (`crates/trackly-app/src/services/org_db_service.rs:139-151`,
`OrgDbService::save_logo`) — источник разрешённых mime-типов, которые нужно повторно
enforce'ить на чтении:

let mime_lower = logo_mime.to_lowercase();
if !matches!(mime_lower.as_str(), "image/png" | "image/jpeg" | "image/svg+xml") {
    return Err(AppError::Validation { field: "logo_mime".to_string(), message: ... });
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: column_labels_for() + export_pdf теперь строит заголовки из русских подписей, не ключей (D-03/CR-01)</name>
  <files>crates/trackly-app/src/tauri_cmds/reports.rs, crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/tauri_cmds/reports.rs строки 19-41 (columns_for — не меняется, только
    источник ключей) и 152-198 (build_reports_export_pdf — точка вызова, куда добавляется
    column_labels_for + новый аргумент). crates/trackly-app/src/services/report_service.rs
    строки 525-635 (export_pdf целиком — текущая сигнатура, месячная группировка через
    row_field(row, col) по ключам, финальный ctx с "columns": columns). ui/src/features/reports/ReportsPage.svelte
    строки 104-155 (COLUMNS_MAP — источник истины для точных русских строк-подписей по каждому
    ключу и типу отчёта, см. <interfaces> выше).
  </read_first>
  <action>
    В tauri_cmds/reports.rs добавить функцию `column_labels_for(report_type: &str) -> Vec&lt;&'static str&gt;`
    сразу после `columns_for` (после строки 41), зеркалящую её match-арматуру 1:1 по report_type,
    но возвращающую русские подписи в ТОМ ЖЕ порядке, что и соответствующий `columns_for` arm:
    "device_acts" | "device_returns" → vec!["Номер", "Устройства", "Сдал", "Принял", "Локация"];
    "device_in_use" | "device_in_stock" → vec!["Наименование", "Статус", "Расположение"];
    "cartridge_consumption" | "cartridge_refills" → vec!["Код картриджа", "Модель", "Статус", "Локация"];
    "cartridge_in_use" | "cartridge_in_stock" → vec!["Код", "Модель", "Статус", "Расположение"];
    _ → vec!["ID"]. Каждый arm должен иметь ТУ ЖЕ длину, что и соответствующий columns_for arm —
    это инвариант, который проверяется юнит-тестом в Task 3. В build_reports_export_pdf (после
    строки, где вычисляется `let cols = columns_for(&report_type);`) добавить
    `let labels = column_labels_for(&report_type);` и передать `&labels` последним аргументом в
    вызов `ctx.reports.export_pdf(...)`. В report_service.rs::export_pdf добавить параметр
    `column_labels: &[&str]` в конец сигнатуры (после `columns: &[&str]`); оставить
    `columns.iter().map(|col| row_field(row, col))` без изменений (ключи по-прежнему управляют
    ячейками); в финальном `serde_json::json!({...})` заменить `"columns": columns` на
    `"columns": column_labels` — единственная строка, которая фактически исправляет дефект
    (шаблон report.html не меняется: он как читал переменную контекста `columns`, так и продолжает,
    просто теперь туда попадают лейблы, а не ключи). Обновить doc-комментарий над `export_pdf`
    (строки 529-533), упомянув, что `column_labels` — новый источник заголовков (D-03), а
    `columns` остаётся источником ячеек через `row_field`.
  </action>
  <verify>
    <automated>cargo build -p trackly-app</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p trackly-app` компилируется без ошибок после сигнатурного изменения
    - `grep -n "fn column_labels_for" crates/trackly-app/src/tauri_cmds/reports.rs` находит функцию
    - `grep -n "\"columns\": column_labels" crates/trackly-app/src/services/report_service.rs` находит новую строку (не `"columns": columns`)
    - `grep -c "\"columns\": columns," crates/trackly-app/src/services/report_service.rs` возвращает 0 (старая строка полностью заменена, не задвоена)
  </acceptance_criteria>
  <done>column_labels_for существует и index-aligned с columns_for; export_pdf строит ctx["columns"] из русских подписей, row_field по-прежнему использует ключи для значений ячеек.</done>
</task>

<task type="auto">
  <name>Task 2: WR-05 — реально enforce'ить mime-allowlist логотипа перед data:-URI</name>
  <files>crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/report_service.rs строки 550-562 (текущее построение
    logo_data_uri — T-17-01-01 комментарий уже объясняет происхождение байт, но НЕ проверяет mime).
    crates/trackly-app/src/services/org_db_service.rs строки 120-151 (`OrgDbService::save_logo` —
    точный allowlist на запись: "image/png" | "image/jpeg" | "image/svg+xml" — используем ТЕ ЖЕ
    три строки, чтобы поведение совпадало с тем, что реально принимает upload-путь).
    crates/trackly-app/templates/report.html строки 22-27, 124-133 (doc-комментарии, которые
    УЖЕ утверждают про "hardcoded mime whitelist (report_service.rs)" — после этой правки
    утверждение становится верным, редактировать сам report.html не требуется).
  </read_first>
  <action>
    В export_pdf, непосредственно перед построением `logo_data_uri`, добавить константу или
    inline-проверку с ТЕМ ЖЕ набором из 3 mime, что в `OrgDbService::save_logo`
    ("image/png", "image/jpeg", "image/svg+xml", регистронезависимо через `.to_lowercase()`).
    Отфильтровать `logo_mime` через этот allowlist: если `logo_mime` не проходит проверку (включая
    случай `None` при непустых `logo_bytes` — тогда default "image/png" применять только когда
    allowlist-проверка самого None-случая пройдена, т.е. текущий fallback "image/png" остаётся, но
    ЯВНО заданный disallowed mime должен приводить к полному отбрасыванию логотипа), устанавливать
    `logo_data_uri = None` целиком (не просто менять mime на дефолтный) — это самый безопасный
    вариант: неизвестный/неразрешённый mime не должен интерполироваться в URI ни в каком виде, а
    сами байты неизвестного формата не стоит выдавать за image/png. Реализация: построить
    `let logo_mime_ok = logo_mime.as_deref().map(|m| matches!(m.to_lowercase().as_str(), "image/png" | "image/jpeg" | "image/svg+xml")).unwrap_or(true);`
    (случай None остаётся "ok" — тогда действует существующий default "image/png" для байт без
    указанного mime, это НЕ регресс, т.к. байты в этом случае гарантированно приходят из
    OrgDbService), затем `let logo_bytes = if logo_mime_ok { logo_bytes } else { None };` ПЕРЕД
    текущим `.map(...)` построением URI. Добавить комментарий на месте изменения, поясняющий, что
    это фактическая реализация allowlist, ранее только заявленная в комментариях report.html/report_service.rs (WR-05).
  </action>
  <verify>
    <automated>cargo build -p trackly-app</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p trackly-app` компилируется
    - `grep -n "image/svg+xml" crates/trackly-app/src/services/report_service.rs` находит новую проверку allowlist в export_pdf (не только в org_db_service.rs)
    - Новый юнит/интеграционный тест (Task 3) подтверждает: недопустимый `logo_mime` → HTML не содержит `<img src="data:` вовсе (лого отброшено, не встроено с чужим mime)
  </acceptance_criteria>
  <done>logo_mime валидируется по тому же allowlist, что и upload-путь (save_logo), перед интерполяцией в data:-URI; disallowed mime полностью отбрасывает логотип вместо встраивания непроверенной строки.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Регрессионные тесты — русские заголовки колонок + mime-allowlist</name>
  <files>crates/trackly-app/tests/html_report_render.rs, crates/trackly-app/src/tauri_cmds/reports.rs</files>
  <read_first>
    crates/trackly-app/tests/html_report_render.rs (файл целиком, уже прочитан в ходе планирования —
    5 существующих тестов вызывают `svc.export_pdf(&rows, report_name, period_label, &org,
    logo_bytes, logo_mime, &columns)` с 7 аргументами; после Task 1 сигнатура требует 8-й
    аргумент `&labels`).
  </read_first>
  <behavior>
    - Обновить все 5 существующих тестов: добавить `let labels = [...]` массив, index-aligned с
      уже используемым в каждом тесте `columns` массивом (использовать РЕАЛЬНЫЕ русские подписи
      из <interfaces>/COLUMNS_MAP, например для columns = ["number", "device_name", "giver_name",
      "receiver_name", "location_name"] → labels = ["Номер", "Устройства", "Сдал", "Принял",
      "Локация"]; для columns = ["device_name"] → labels = ["Устройства"]), и передать `&labels`
      последним аргументом в каждый вызов `.export_pdf(...)`.
    - Новый тест html_report_header_uses_russian_labels_not_raw_keys: columns = ["number",
      "device_name", "giver_name", "receiver_name", "location_name"], labels = ["Номер",
      "Устройства", "Сдал", "Принял", "Локация"]; после export_pdf assert html.contains("<th>Сдал</th>")
      И assert !html.contains("giver_name") (сырой ключ не должен просочиться в вывод как текст
      заголовка — это именно тот регресс, который CR-01 описывает).
    - Новый тест html_report_disallowed_logo_mime_drops_logo: вызвать export_pdf с
      logo_bytes = Some(vec![1,2,3]) и logo_mime = Some("text/html".to_string()) (disallowed);
      assert !html.contains("<img src=\"data:") — лого полностью отброшено, а не встроено с
      произвольным mime.
    - Новый юнит-тест (в tauri_cmds/reports.rs, добавить #[cfg(test)] mod tests если его ещё нет):
      column_labels_for_is_index_aligned_with_columns_for — для каждого из 8 известных
      report_type-строк ("device_acts", "device_returns", "device_in_use", "device_in_stock",
      "cartridge_consumption", "cartridge_refills", "cartridge_in_use", "cartridge_in_stock")
      assert_eq!(columns_for(rt).len(), column_labels_for(rt).len()).
  </behavior>
  <action>
    Внести описанные выше изменения. В html_report_render.rs для теста
    html_report_disallowed_logo_mime_drops_logo используйте `empty_org()` фикстуру (as в других
    тестах файла) и любой непустой `rows`/`columns` набор (например columns = ["device_name"],
    labels = ["Устройства"]) — суть теста только в проверке логотипа, остальное можно взять из
    существующего single-row фикстуры этого файла.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test html_report_render</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test html_report_render` — все тесты (5 обновлённых + 2 новых) проходят
    - `cargo test -p trackly-app --lib tauri_cmds::reports` (или эквивалентный `--lib` фильтр, если модуль тестов встроен) проходит, включая column_labels_for_is_index_aligned_with_columns_for
    - `grep -n "html.contains(\"&lt;th&gt;Сдал&lt;/th&gt;\")" crates/trackly-app/tests/html_report_render.rs` (буквально `<th>Сдал</th>`) присутствует
  </acceptance_criteria>
  <done>Регрессионный тест ловит именно тот класс дефекта, что CR-01/D-03 описывают (сырые ключи вместо русских подписей); mime-allowlist тест ловит WR-05; параллельный юнит-тест страхует от будущего рассинхрона длин между columns_for/column_labels_for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org_settings.logo_mime (DB) → report.html `data:` URI | `logo_mime` — данные из БД (записаны ранее через ManageSettings-гейтед `save_logo`), интерполируются в `data:{mime};base64,...` с `| safe` в report.html — autoescape намеренно обойдён для этого одного значения |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Tampering | report_service.rs::export_pdf logo_data_uri construction | mitigate | Enforce тот же mime allowlist ("image/png"/"image/jpeg"/"image/svg+xml"), что уже используется на записи (`OrgDbService::save_logo`) — disallowed mime отбрасывает логотип целиком перед интерполяцией в `data:`-URI (WR-05) |
| T-17-05-02 | Information Disclosure | Report headers (column_labels_for) | accept | Русские подписи колонок — статичные строковые константы, без пользовательского ввода; риск отсутствует, это чисто функциональный (не security) дефект |
| T-17-05-SC | Tampering | Нет новых установок npm/pip/cargo в этом плане | accept | План не добавляет зависимостей — только Rust-логика и тесты |
</threat_model>

<verification>
- `cargo test -p trackly-app --test html_report_render` — зелёный, включая 2 новых теста
- `cargo build -p trackly-app` — компилируется
- `grep -n "\"columns\": column_labels" crates/trackly-app/src/services/report_service.rs` находит новую строку
- `grep -n "fn column_labels_for" crates/trackly-app/src/tauri_cmds/reports.rs` находит функцию
- `cargo clippy -p trackly-app -- -D warnings` чист
- `cargo fmt --check` чист
</verification>

<success_criteria>
- Экспорт/печать любого отчёта показывает русские заголовки колонок вместо snake_case-ключей (D-03 закрыт)
- row_field по-прежнему корректно резолвит значения ячеек по исходным ключам (не сломан)
- logo_mime проверяется по allowlist перед интерполяцией в data:-URI (WR-05 закрыт)
- Регрессионный тест защищает от повторного появления этого класса дефекта
</success_criteria>

<output>
Create `.planning/phases/17-html-krilla/17-05-SUMMARY.md` when done
</output>
</content>
