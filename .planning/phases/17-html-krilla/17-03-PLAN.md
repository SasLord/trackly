---
phase: 17-html-krilla
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - ui/src/features/acts/PdfPreviewModal.svelte
  - ui/src/features/reports/ReportsPage.svelte
  - ui/src/features/settings/TemplateEditor.svelte
  - ui/src/bindings.ts
autonomous: true
requirements: [Req-4, Req-5]
must_haves:
  truths:
    - "Кнопка «Экспорт PDF» в Отчётах открывает превью+печать вместо скачивания PDF-файла"
    - "Печать Отчётов работает в desktop (Tauri webview) и в LAN-браузере через PdfPreviewModal"
    - "Редактор Шаблонов показывает report как выбираемый kind и его собственный набор переменных"
    - "Превью в редакторе Шаблонов рендерится как HTML (iframe srcdoc), не как blob PDF"
  artifacts:
    - path: "ui/src/features/acts/PdfPreviewModal.svelte"
      provides: "mode='report' branch added to renderCall(), reportParams prop"
      contains: "reportParams"
    - path: "ui/src/features/settings/TemplateEditor.svelte"
      provides: "per-kind variables panel + HTML srcdoc preview + report kind in KIND_LABELS"
      contains: "report:"
  key_links:
    - from: "ui/src/features/reports/ReportsPage.svelte"
      to: "ui/src/features/acts/PdfPreviewModal.svelte"
      via: "PdfPreviewModal mode=\"report\" reportParams={{...}}"
      pattern: "mode=\"report\""
    - from: "ui/src/features/settings/TemplateEditor.svelte"
      to: "backend templates_validate_preview"
      via: "apiCall<string>('templates_validate_preview', { kind, body })"
      pattern: "apiCall<string>\\('templates_validate_preview'"
---

<objective>
Wire the frontend onto the HTML-returning backend contracts shipped in Plans 17-01
and 17-02: extend PdfPreviewModal.svelte with a third mode='report' (self-fetch
branch calling reports_export_pdf), replace ReportsPage.svelte's blob-download
exportPdf()/printReport() with opening that modal, and retarget
TemplateEditor.svelte's preview (blob/PDF iframe -> HTML srcdoc iframe),
kind list (add report), and variables panel (per-kind, not one static hardcoded
block) to the file-backed HTML editor contract.

Purpose: Completes the user-visible half of Req 4/Req 5 — closes the live bug
16-HUMAN-UAT 2b ("Ошибка при создании PDF") by removing the failing blob/
tauri-plugin-fs download path entirely, and makes the Settings template editor
edit what actually gets printed (the HTML files, not a dead DocSpec path).

Output: PdfPreviewModal.svelte (mode='report'), ReportsPage.svelte (modal-driven
export+print), TemplateEditor.svelte (file-backed HTML editor UI), regenerated
ui/src/bindings.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-html-krilla/17-SPEC.md
@.planning/phases/17-html-krilla/17-CONTEXT.md
@.planning/phases/17-html-krilla/17-PATTERNS.md

<interfaces>
Tauri commands (unchanged names, new return types from 17-01/17-02):

reports_export_pdf(reportType: string, filter: ReportFilter, period?: PeriodDto): Promise<string>  // HTML
templates_list_for_editor(): Promise<TemplateEditorItem[]>  // { id, kind, body, is_default }
templates_update_body({ kind: string, body: string }): Promise<void>
templates_reset_to_default({ kind: string }): Promise<void>
templates_validate_preview({ kind: string, body: string }): Promise<string>  // HTML

From ui/src/lib/api/client.ts (unchanged, already routes text/html correctly per Phase 16):

apiCall<R>(cmd: string, args?: object): Promise<R>
// HTTP transport: content-type text/html -> res.text(); Tauri transport: invoke() returns the raw value

From ui/src/features/acts/PdfPreviewModal.svelte (current file, being extended — read in full below):

interface Props { open: boolean; actId: number | null; title: string; onClose: () => void;
  mode?: 'handover' | 'acceptance'; acceptancePayload?: AcceptancePayload | null; }
function renderCall(): Promise<string>  // dispatches on mode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate bindings.ts against the Plan 17-01/17-02 backend changes</name>
  <files>ui/src/bindings.ts</files>
  <read_first>
    crates/trackly-app/tests/export_bindings.rs (the test that regenerates bindings.ts) and
    crates/trackly-app/tests/specta_roundtrip.rs (confirms Tauri/HTTP DTO shape parity) — run these,
    do not hand-edit bindings.ts. This file is gitignored per project convention (bindings.ts is
    regenerated via "cargo test --test export_bindings", NOT committed).
  </read_first>
  <action>
    Run "cargo test -p trackly-app --test export_bindings" from the repo root to regenerate
    ui/src/bindings.ts reflecting the new return types from Plans 17-01 (reports_export_pdf ->
    string) and 17-02 (templates_validate_preview -> string, now taking kind non-discarded — the
    signature already included kind as a param before this phase, only the return type changes).
    Confirm the regenerated file is NOT staged for commit (it is gitignored) — this task exists
    purely so Task 2/3 of this plan can type-check against the updated bindings during
    "pnpm svelte-check".
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test export_bindings</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p trackly-app --test export_bindings exits 0
    - grep -n "reports_export_pdf" ui/src/bindings.ts shows a string return type, not number[]
    - grep -n "templates_validate_preview" ui/src/bindings.ts shows a string return type, not number[]
    - git status ui/src/bindings.ts shows it is ignored (not tracked/staged)
  </acceptance_criteria>
  <done>ui/src/bindings.ts regenerated in place with string return types for reports_export_pdf and
  templates_validate_preview, confirmed gitignored (not committed).</done>
</task>

<task type="auto">
  <name>Task 2: Extend PdfPreviewModal with mode='report' and rewire ReportsPage export/print to the modal</name>
  <files>ui/src/features/acts/PdfPreviewModal.svelte, ui/src/features/reports/ReportsPage.svelte</files>
  <read_first>
    ui/src/features/acts/PdfPreviewModal.svelte (full file, already read in planning context above —
    especially renderCall() lines 79-95, the Props/ready derivation lines 54-99, and the UNTOUCHED
    print machinery printViaSystemBrowser/printViaTopLevel/handlePrint lines 154-244 which this
    task must NOT modify). ui/src/features/acts/ActsPage.svelte lines 252-260 (the PdfPreviewModal
    invocation pattern with open/actId/title/onClose props) as the wiring convention to replicate
    for Reports. Current ReportsPage.svelte lines 356-463 (exportCsv/exportPdf/printReport —
    exportPdf/printReport are being replaced; exportCsv stays untouched) and lines 500-545 (the
    ReportFilters onExportPdf={exportPdf} onPrint={printReport} wiring being repointed).
  </read_first>
  <action>
    In PdfPreviewModal.svelte: add an exported ReportParams interface with fields reportType
    (string), filter (unknown), and an optional period (unknown), and extend the Props interface's
    mode union to 'handover' | 'acceptance' | 'report', adding an optional reportParams prop typed
    ReportParams | null (default null, destructured alongside the existing acceptancePayload
    default). Extend renderCall() with a new branch: if mode === 'report', reject with an Error if
    reportParams is null, otherwise call apiCall<string>('reports_export_pdf', { reportType:
    reportParams.reportType, filter: reportParams.filter, period: reportParams.period }) (import
    apiCall from '$lib/api/client' at the top of the file — it is not currently imported since the
    component only used the acts wrapper before). Extend the ready $derived to a 3-way branch:
    mode === 'acceptance' checks acceptancePayload !== null, mode === 'report' checks
    reportParams !== null, otherwise falls back to actId !== null. Do not touch anything else in
    the file — printViaSystemBrowser, printViaTopLevel, handlePrint, the $effect that calls
    renderCall(), the iframe/srcdoc markup, and all styling are mode-agnostic already and require
    zero changes (this is a pure extension per D-09, not a rewrite). In ReportsPage.svelte: import
    PdfPreviewModal from '../acts/PdfPreviewModal.svelte' and add "let reportModalOpen =
    $state(false);" alongside the existing csvExporting/pdfExporting state. Replace the ENTIRE body
    of exportPdf() (current lines 380-430 — the apiCall<number[]>/Blob/tauri-plugin-fs/download
    logic) with a single-statement function that sets reportModalOpen = true (D-10 — the modal
    self-fetches on open via its own $effect, no pre-fetch needed here). Delete printReport()
    entirely (current lines 432-463 — its duplicate PDF-generation + save-dialog logic becomes dead
    code once print goes through the modal) and point the ReportFilters onPrint prop at the SAME
    trigger as onExportPdf (i.e. reuse exportPdf, or an equivalent inline arrow function setting
    reportModalOpen = true) per D-10 ("кнопка полностью заменить печатью" — both buttons now open
    the same preview+print flow). Render a PdfPreviewModal instance near the bottom of the template
    markup with open={reportModalOpen}, mode="report", actId={null} (required since Props.actId is
    a non-optional number | null field unrelated to report mode), title="Печать отчёта",
    reportParams={{ reportType: reportTypeKey(), filter, period: isSnapshot() ? undefined : period }},
    and onClose setting reportModalOpen back to false — mirroring ActsPage.svelte's placement at
    line 252. Leave exportCsv() and its ReportFilters onExportCsv={exportCsv} wiring completely
    untouched (D-1 CSV boundary, out of scope).
  </action>
  <verify>
    <automated>pnpm --dir ui exec svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "mode === 'report'" ui/src/features/acts/PdfPreviewModal.svelte returns at least 1 match inside renderCall
    - grep -c "tauri-plugin-fs\|writeFile\|@tauri-apps/plugin-dialog" ui/src/features/reports/ReportsPage.svelte returns 0 (blob/save-dialog download path fully removed)
    - grep -n "function printReport" ui/src/features/reports/ReportsPage.svelte returns 0 matches (function deleted)
    - grep -n "PdfPreviewModal" ui/src/features/reports/ReportsPage.svelte shows the component imported and rendered with mode="report"
    - pnpm --dir ui exec svelte-check reports 0 errors for both files
  </acceptance_criteria>
  <done>PdfPreviewModal supports mode='report' via a pure additive extension; ReportsPage's
  exportPdf/onPrint both open the modal (self-fetching reports_export_pdf), the old blob/download/
  tauri-plugin-fs path and printReport() are deleted, exportCsv is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget TemplateEditor.svelte to the file-backed HTML editor contract (report kind, srcdoc preview, per-kind variables)</name>
  <files>ui/src/features/settings/TemplateEditor.svelte</files>
  <read_first>
    ui/src/features/settings/TemplateEditor.svelte (full file, already read in planning context
    above — KIND_LABELS lines 15-18, validateAndPreview lines 90-112, the static
    .variables-panel/.var-col markup lines 173-195, the blobUrl state + cleanup lines 24, 61-69,
    82-86, and the preview iframe markup lines 222-225). ui/src/features/acts/PdfPreviewModal.svelte's
    iframe using srcdoc={htmlContent} (line 261) as the srcdoc convention to replicate here
    (without the print-button footer — this is a validate-preview surface, not a print flow).
    crates/trackly-app/templates/act_acceptance.html's doc-comment (read this file directly — it is
    not excerpted in planning context) for the exact device/document/org variable list used by the
    act_acceptance kind entry below.
  </read_first>
  <action>
    Add a report entry mapped to the Russian label "Отчёт" to the KIND_LABELS record (alongside the
    existing act_handover/act_acceptance entries) so the kind select (unchanged markup, line 164)
    offers all 3 kinds once templates_list_for_editor returns 3 items (after Plan 17-02 Task 1's
    file-backed rewrite). Replace the blobUrl state var with a previewHtml state var typed
    string | null; remove the URL.revokeObjectURL(blobUrl) cleanup call from onMount's teardown
    (line ~65-67) since there is no object URL lifecycle anymore. In validateAndPreview(), replace
    the apiCall<number[]>('templates_validate_preview', ...) + Blob/URL.createObjectURL chain with
    a single assignment: previewHtml = await apiCall<string>('templates_validate_preview', { kind:
    selectedKind, body }) (still inside the existing try/catch/finally — only the response-handling
    body changes). In the $effect that clears the preview on kind change (lines 72-88), replace the
    blobUrl revoke-and-null logic with simply setting previewHtml = null (no revoke needed for a
    plain string). Replace the preview markup (the blobUrl-conditional iframe block, lines 222-225)
    with an equivalent previewHtml-conditional block rendering an iframe with srcdoc={previewHtml},
    title "Превью", and the existing pdf-iframe class — keep the same preview-wrapper/pdf-iframe
    class names (styles already defined, lines 400-411, need no changes). Replace the static
    hardcoded variables-grid block (lines 176-193, two var-col divs describing DocSpec fields
    org_name/inn/kpp/act_number/giver_name/etc.) with a per-kind data-driven render: add a
    VARIABLES_BY_KIND constant (Record<string, {code, desc}[]>) with 3 entries — act_handover:
    org.name/org.inn/org.kpp/org.address/org.phone/org.fax/org.email/org.okpo/org.ogrn/
    org.logo_data_uri, act.number/act.suffix/act.date_human/act.receiver_name/act.deadline_human/
    act.location_name/act.items[] (name/inventory_no/serial_no/model/specs/kit/condition/quantity)
    — sourced from act_handover.html's own doc-comment (already read in planning context);
    act_acceptance: the equivalent device/document/org variable set (read
    crates/trackly-app/templates/act_acceptance.html's doc-comment for the exact list before
    writing this entry — do not guess); report: org.* (same as act_handover), report_name,
    period_label, columns, groups[].month_label, groups[].rows[] (sourced from report.html's
    doc-comment, Plan 17-01 Task 1). Add a currentVariables $derived reading
    VARIABLES_BY_KIND[selectedKind] with an empty-array fallback, and render it via an each-block
    producing a var-item paragraph per entry (code element + description) inside the existing
    variables-grid/details-panel wrapper (keep the wrapper markup and its styles, lines 174-195 and
    301-341, only the inner static divs become dynamic).
  </action>
  <verify>
    <automated>pnpm --dir ui exec svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "report:" ui/src/features/settings/TemplateEditor.svelte shows the new KIND_LABELS entry
    - grep -c "blobUrl\|createObjectURL\|revokeObjectURL" ui/src/features/settings/TemplateEditor.svelte returns 0
    - grep -n "srcdoc={previewHtml}" ui/src/features/settings/TemplateEditor.svelte returns at least 1 match
    - grep -n "VARIABLES_BY_KIND" ui/src/features/settings/TemplateEditor.svelte shows 3 kind entries (act_handover, act_acceptance, report)
    - pnpm --dir ui exec svelte-check reports 0 errors for this file
  </acceptance_criteria>
  <done>TemplateEditor.svelte lists report as a selectable kind, previews via HTML srcdoc (no blob/
  PDF object URLs), and shows a per-kind variables panel matching each HTML template's actual
  context (act_handover/act_acceptance/report).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Backend HTML string -> iframe srcdoc | reports_export_pdf/templates_validate_preview HTML strings are rendered client-side inside an iframe via srcdoc — same sandboxing model already proven safe for acts in Phase 16 (server-side autoescape via build_safe_html_env, iframe has no elevated privileges over the parent app) |
| ReportFilters export/print buttons -> reportParams | reportType/filter/period values are passed from ReportsPage.svelte's existing reactive state into the modal's self-fetch call — same values already sent to reports_export_csv/the old reports_export_pdf, no new user-input surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | PdfPreviewModal iframe srcdoc (mode='report') | accept (unchanged from Phase 16 T-07-04-02/T-16-02) | The HTML string is server-rendered with autoescape ON before reaching the browser; the iframe itself has no access to parent-window cookies/session storage (different origin under srcdoc), same as the existing handover/acceptance modes |
| T-17-03-02 | Information Disclosure | reportParams (filter/period) sent to reports_export_pdf | accept | Identical values already flow through the pre-existing reports_export_csv/reports_list_* calls on the same page — no new data exposed, only the export transport changes (HTML instead of PDF bytes) |
| T-17-03-03 | Tampering | TemplateEditor "Сохранить" body -> update_body | accept (server-side mitigation from Plan 17-02) | Path-traversal and MiniJinja-syntax validation both happen server-side (Plan 17-02 T-17-02-01/T-17-02-03); this plan's UI changes are purely presentational (no new client-side validation bypass introduced) |
| T-17-03-SC | Tampering | No new package-manager installs in this plan | accept | Plan adds zero new npm dependencies; deletes a dependency on @tauri-apps/plugin-dialog/@tauri-apps/plugin-fs/@tauri-apps/plugin-shell imports from ReportsPage.svelte specifically (those plugins remain used elsewhere, e.g. PdfPreviewModal's own desktop print path) |
</threat_model>

<verification>
- pnpm --dir ui exec svelte-check reports 0 errors
- pnpm --dir ui build succeeds (ui/dist rebuilt for LAN-browser testing per dev convention)
- cargo test -p trackly-app --test export_bindings exits 0 (bindings regenerated, confirms Rust side compiles)
- Manual: open Reports page, click "Экспорт PDF" — preview modal opens showing HTML (not a download); click "Печать" — desktop opens system browser print dialog, LAN browser shows native print dialog
- Manual: open Settings > Шаблоны, select "Отчёт" — variables panel shows report-specific vars, "Проверить" shows an HTML iframe (not a PDF blob)
</verification>

<success_criteria>
- PdfPreviewModal.svelte supports mode='report' as a pure additive extension (no changes to
  print machinery)
- ReportsPage.svelte's export/print buttons both open the modal; the old blob/tauri-plugin-fs
  download path and printReport() are deleted; exportCsv() untouched
- TemplateEditor.svelte lists report as a selectable kind, previews via HTML srcdoc, and shows
  a per-kind variables panel
- ui/src/bindings.ts regenerated (gitignored, not committed) reflecting string return types
</success_criteria>

<output>
Create `.planning/phases/17-html-krilla/17-03-SUMMARY.md` when done
</output>
