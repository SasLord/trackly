---
phase: 17-html-krilla
plan: 04
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified:
  - crates/trackly-app/tests/template_edit.rs
  - crates/trackly-app/tests/html_report_render.rs
  - crates/trackly-app/tests/pdf_determinism.rs
  - crates/trackly-app/src/services/report_service.rs
  - crates/trackly-app/src/services/template_service.rs
autonomous: true
requirements: [Req-6, Req-7]
must_haves:
  truths:
    - "cargo test -p trackly-app зелёный после миграции Отчётов/Шаблонов"
    - "Есть тест на HTML-рендер отчёта: 1 строка, N строк с помесячной группировкой, пустой набор"
    - "template_edit.rs проверяет file-backed list_all_for_editor/update_body/reset_to_default, не DB"
    - "Все krilla-специфичные тесты (render_docspec-based) помечены #[ignore] с D-13-стиль комментарием"
  artifacts:
    - path: "crates/trackly-app/tests/html_report_render.rs"
      provides: "HTML-render regression suite for ReportService::export_pdf (1/N rows, month grouping, empty)"
      min_lines: 40
    - path: "crates/trackly-app/tests/template_edit.rs"
      provides: "Rewritten to test file-backed editor methods (templates/*.html), not document_templates DB rows"
      contains: "resolve_templates_dir"
  key_links:
    - from: "crates/trackly-app/tests/html_report_render.rs"
      to: "crates/trackly-app/src/services/report_service.rs"
      via: "ReportService::export_pdf(...).await returns Result<String, AppError>"
      pattern: "export_pdf\\("
    - from: "crates/trackly-app/tests/template_edit.rs"
      to: "crates/trackly-app/src/pdf/html_templates.rs"
      via: "assertions against on-disk templates/*.html files, not document_templates rows"
      pattern: "templates_dir\\.join"
---

<objective>
Migrate the test suite off PDF-byte assertions onto HTML-string assertions for the
two consumers Plans 17-01/17-02 changed, and complete the krilla #[ignore]
hygiene sweep (D-13 pattern) across any remaining active-path krilla test that
Plans 17-01/17-02 orphaned. This is the verification-closing plan for the whole
phase — Req 7 requires cargo test -p trackly-app green with new HTML-render
coverage, and Req 6 requires every krilla-touching test that is no longer on an
active path to be explicitly frozen (not silently broken or deleted).

Purpose: Proves the migration is complete and regression-safe — a reviewer running
cargo test after this plan sees green, with the new report/template HTML tests
as the durable proof that Req 1/2/5 actually work end-to-end.

Output: New tests/html_report_render.rs (report HTML-render coverage), rewritten
tests/template_edit.rs (file-backed editor coverage), any #[ignore] additions
needed in pdf_determinism.rs or elsewhere if this phase's changes orphaned a
previously-active krilla call site, and updated unit tests inside
report_service.rs/template_service.rs for the behavior specified in Plans
17-01 Task 2 and 17-02 Task 2 (if not already delivered as part of those plans'
TDD cycles — this plan is the safety net that confirms full-suite green, not a
duplicate of that work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-html-krilla/17-SPEC.md
@.planning/phases/17-html-krilla/17-CONTEXT.md
@.planning/phases/17-html-krilla/17-PATTERNS.md

<interfaces>
From crates/trackly-app/src/services/report_service.rs (shipped by Plan 17-01 — this plan reads,
does not modify the signature, only adds/updates tests against it):

pub struct ReportService { pub writer: ..., pub readers: ..., pub clock: ..., pub config: ...,
  pub pdf: Arc<PdfRenderer>, pub(crate) organization: Option<Arc<OrganizationService>> }
impl ReportService {
  pub fn with_organization(mut self, organization: Arc<OrganizationService>) -> Self;
  pub async fn export_pdf(&self, rows: &ReportResponse, report_name: &str, period_label: &str,
    org: &OrgSettingsDto, logo_bytes: Option<Vec<u8>>, logo_mime: Option<String>,
    columns: &[&str]) -> Result<String, AppError>;
}

From crates/trackly-app/src/services/template_service.rs (shipped by Plan 17-02):

impl TemplateService {
  pub fn with_organization(mut self, organization: Arc<OrganizationService>) -> Self;
  pub async fn list_all_for_editor(&self) -> Result<Vec<TemplateEditorItem>, AppError>;
  pub async fn update_body(&self, caller: &Identity, kind: &str, body: String) -> Result<(), AppError>;
  pub async fn reset_to_default(&self, caller: &Identity, kind: &str) -> Result<(), AppError>;
  pub async fn validate_preview(&self, kind: &str, body: &str) -> Result<String, AppError>;
  // FROZEN, unchanged: seed_defaults_on_startup, get_active, DEFAULT_TEMPLATES
}

From crates/trackly-app/tests/html_act_render.rs (Phase 16 analog — full file already read in
planning context; this plan's new html_report_render.rs mirrors its make_full_pipeline-style
fixture setup and assertion style, adapted for ReportService instead of ActService).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/html_report_render.rs covering 1-row, N-row month-grouped, and empty report HTML render</name>
  <files>crates/trackly-app/tests/html_report_render.rs</files>
  <read_first>
    crates/trackly-app/tests/html_act_render.rs (full file, already in planning context — fixture
    setup style make_full_pipeline/seed_device/seed_devices and assertion conventions
    html.contains(...)/!html.contains(...)) as the structural template for this new file, adapted
    to ReportService instead of ActService. crates/trackly-app/tests/report_csv_export.rs lines
    1-50 (the ReportService::new(writer, readers, clock, config, pdf) construction pattern used in
    this crate's tests — this plan's fixture additionally calls .with_organization(...)).
    crates/trackly-app/src/services/report_service.rs::export_pdf (as shipped by Plan 17-01) and
    row_field/month_key_to_russian (unchanged helpers) for the exact ReportRow/ReportResponse
    shape to construct fixtures against.
  </read_first>
  <behavior>
    - Test html_report_single_row_renders_columns_and_month: build a ReportResponse with 1 row
      (month_key: Some("2026-09"), populated number/device_name/giver_name/receiver_name/
      location_name), call export_pdf with columns = ["number", "device_name", "giver_name",
      "receiver_name", "location_name"], assert the returned HTML contains "Сентябрь 2026" and
      every one of the row's field values.
    - Test html_report_multi_month_groups_render_separately: build a ReportResponse with rows
      spanning 2 distinct month_key values (e.g. "2026-08" and "2026-09"), assert the HTML contains
      BOTH Russian month labels ("Август 2026" and "Сентябрь 2026") and that each month's row values
      appear in the output.
    - Test html_report_empty_response_shows_no_data_message: build a ReportResponse with an empty
      rows vec, call export_pdf, assert the HTML contains "Нет данных за указанный период."
    - Test html_report_org_header_present: call export_pdf with a populated OrgSettingsDto
      (non-empty org_name/inn), assert the HTML contains the org name string.
    - Test html_report_no_krilla_artifacts: for any of the above fixtures, assert the returned HTML
      does NOT start with "%PDF" (proves the output is HTML text, not accidentally still PDF bytes
      encoded as a string).
  </behavior>
  <action>
    Create crates/trackly-app/tests/html_report_render.rs with a make_report_service() fixture
    helper mirroring report_csv_export.rs's construction (test_writer_and_readers() or the manual
    tempfile+migrations pattern already used in that file — check which helper is already
    imported/available in this crate's trackly_infra::test_support module and prefer it) that
    builds a ReportService::new(writer, readers, clock, config, pdf) and chains
    .with_organization(Arc::new(OrganizationService::new(paths))) so export_pdf's
    resolve_templates_dir call succeeds. Write the 5 behavior tests above as tokio::test functions
    with a multi-thread flavor and a small worker-thread count (matching this crate's existing
    convention), constructing ReportRow/ReportResponse/OrgSettingsDto fixtures directly (all 3 are
    plain public structs from trackly_app::dto::reports, no DB seeding required — export_pdf takes
    a pre-built ReportResponse, it does not query the DB itself). Do not call
    materialize_defaults_on_startup in the fixture — rely on the embedded-default fallback path
    (same as html_act_render.rs's html_falls_back_to_embedded_default_when_file_absent test),
    proving report.html's embedded default alone is sufficient for these assertions.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test html_report_render</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p trackly-app --test html_report_render — all 5 tests pass
    - grep -c "render_docspec\|DocSpec" crates/trackly-app/tests/html_report_render.rs returns 0
  </acceptance_criteria>
  <done>html_report_render.rs exists with 5 passing tests proving export_pdf's HTML output covers
  single-row, multi-month-grouped, empty, org-header, and non-PDF-artifact assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite tests/template_edit.rs for the file-backed editor contract</name>
  <files>crates/trackly-app/tests/template_edit.rs</files>
  <read_first>
    crates/trackly-app/tests/template_edit.rs (current file, already read in planning context above
    — the 3 existing tests template_update_and_render_uses_new_body/template_invalid_syntax_rejected/
    template_reset_to_default_restores_builtin all call svc.get_active(...) and
    svc.list_all_for_editor()/svc.update_body(...)/svc.reset_to_default(...) — get_active reads the
    FROZEN DB path (D-13, unchanged) while the other 3 methods are now file-backed after Plan
    17-02, so these tests currently assert cross-path consistency that no longer holds:
    update_body writes a file, get_active reads the DB row — they will diverge). Read
    crates/trackly-app/tests/html_act_render.rs's html_uses_file_when_present_and_edit_changes_output
    test (lines ~296-334) for the on-disk-file assertion pattern to reuse here
    (Paths::resolve_for_exe_dir, templates_dir.join(...), std::fs::write/std::fs::read_to_string
    direct filesystem checks).
  </read_first>
  <behavior>
    - Test update_body_writes_file_to_disk: call svc.update_body(&admin, "act_handover",
      new_body.clone()), then directly read templates_dir.join("act_handover.html") from disk via
      std::fs::read_to_string and assert it equals new_body (not svc.get_active(...), which now
      reads the frozen DB path).
    - Test list_all_for_editor_reflects_disk_state: write a known body directly to
      templates_dir.join("report.html") via std::fs::write, call svc.list_all_for_editor(),
      assert the returned Vec<TemplateEditorItem> contains an item with kind == "report" and
      body equal to what was written, is_default == false.
    - Test reset_to_default_restores_embedded_body: call update_body to change "act_acceptance",
      then reset_to_default(&admin, "act_acceptance"), assert the on-disk file now equals the
      embedded default from DEFAULT_HTML_TEMPLATES.
    - Test update_body_unknown_kind_returns_not_found: KEEP this existing test as-is (still valid —
      the allowlist-check contract from Plan 17-02 Task 1 must still return NotFound for an
      unrecognized kind string, same assertion as before).
    - Test update_body_rejects_invalid_minijinja_syntax: adapt the existing
      template_invalid_syntax_rejected test to assert AppError::Validation { field: "body", .. }
      is returned AND the on-disk file is untouched (read it before/after, assert unchanged) —
      replaces the old "DB row unchanged" assertion with a "file unchanged" assertion.
  </behavior>
  <action>
    Rewrite template_edit.rs's fixture helper make_template_service() to additionally construct an
    Arc<OrganizationService> from the same TempDir's resolved Paths and chain
    .with_organization(organization) onto the TemplateService::new(...) call (mirrors the pattern
    already used in html_act_render.rs::make_full_pipeline). Keep svc.seed_defaults_on_startup()
    calls where the test still exercises the frozen DB path incidentally (it is harmless and
    already idempotent) but do NOT rely on it for any NEW assertion — the new/rewritten tests
    above assert against Paths::resolve_for_exe_dir(dir.path().to_path_buf())'s templates_dir()
    file contents and list_all_for_editor()'s return value, never get_active(). Delete or adapt
    every existing assertion that calls svc.get_active(...) and expects it to reflect an
    update_body/reset_to_default call (those are now DIFFERENT storage paths post Plan 17-02 —
    asserting they match would be testing a false invariant). Keep the 2 tests noted as
    "KEEP"/"adapt" above, replace the rest per the behavior block.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test template_edit</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p trackly-app --test template_edit — all tests pass
    - grep -c "svc.get_active" crates/trackly-app/tests/template_edit.rs returns 0 (no test asserts cross-path DB/file consistency)
    - grep -n "templates_dir\|std::fs::read_to_string\|std::fs::write" crates/trackly-app/tests/template_edit.rs shows direct filesystem assertions in at least 3 tests
  </acceptance_criteria>
  <done>template_edit.rs tests the file-backed list_all_for_editor/update_body/reset_to_default
  contract directly against on-disk templates/*.html files, with no lingering assertions against the
  now-decoupled DB-backed get_active path.</done>
</task>

<task type="auto">
  <name>Task 3: krilla #[ignore] hygiene sweep — confirm no active path constructs PdfRenderer::render_docspec, tighten struct-field comments</name>
  <files>crates/trackly-app/tests/pdf_determinism.rs, crates/trackly-app/src/services/report_service.rs, crates/trackly-app/src/services/template_service.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_determinism.rs (full file — both tests already carry #[ignore]
    with a "D-13 (Phase 16): frozen krilla path" comment from Phase 16; confirm this phase did not
    orphan any NEW krilla call site that needs the same treatment). crates/trackly-app/src/tauri_cmds/health.rs
    and crates/trackly-app/src/http/health.rs (both construct a PdfRenderer::new() instance purely
    as a structural constructor argument for ActService::with_pdf_pipeline/ReportService::new's pdf
    param — confirm via grep that .render_docspec( is never called on these instances; this is
    expected and requires NO change, only verification).
  </read_first>
  <action>
    Run a repo-wide grep for "render_docspec" and "PdfRenderer::new" across crates/trackly-app/src
    and crates/trackly-app/tests, and manually classify every hit: (a) pdf_determinism.rs's 2
    #[ignore]d tests — already correctly frozen, no change; (b) report_service.rs/template_service.rs
    — after Plans 17-01/17-02, these files must have ZERO render_docspec calls in non-test code
    (verify; if any residual call exists, this task removes it as a Rule-1-style fix, since finding
    one here means an earlier plan's task was incomplete); (c) health.rs/tauri_cmds/health.rs/
    various test fixture builders (html_act_render.rs, pdf_render_act.rs, pdf_column_overflow.rs,
    pdf_logo.rs, acts_e2e_smoke.rs) — these construct PdfRenderer::new() and pass it into
    ActService::with_pdf_pipeline/ReportService::new/TemplateService's constructor purely as a
    required-but-now-mostly-unused struct field; confirm NONE of them call .render_docspec( on that
    instance (act rendering already migrated to HTML in Phase 16; report rendering migrates in this
    phase) — no source changes needed here, this is a verification-only sub-step. If step (b) finds
    a residual call, delete it and replace with the HTML-render call path established in Plan 17-01
    Task 2 / Plan 17-02 Task 2 (do not re-derive the fix from scratch — cross-reference those plans'
    action blocks for the exact replacement code). Add a one-line doc comment above the
    "pdf: Arc<PdfRenderer>" field declaration in both ReportService and TemplateService structs
    noting that the krilla renderer handle is frozen/unused since Phase 17 (D-13-style) and kept
    only because the struct's constructor signature is used by roughly 10 existing call sites, so
    future readers understand why the field persists despite being dead weight.
  </action>
  <verify>
    <automated>cargo test -p trackly-app</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "render_docspec" crates/trackly-app/src/services/report_service.rs crates/trackly-app/src/services/template_service.rs crates/trackly-app/src/tauri_cmds/reports.rs crates/trackly-app/src/http/reports.rs crates/trackly-app/src/tauri_cmds/settings_org.rs crates/trackly-app/src/http/settings_org.rs returns exactly 0 matches (Req 6 gate, matches SPEC Acceptance Criteria's own grep command)
    - cargo test -p trackly-app full suite green (all binaries)
    - cargo clippy -p trackly-app --all-targets -- -D warnings clean
    - cargo fmt --check clean
  </acceptance_criteria>
  <done>Zero render_docspec calls remain in report_service.rs/template_service.rs/their Tauri+HTTP
  adapters; pdf: Arc<PdfRenderer> fields carry a doc comment explaining their frozen status; full
  cargo test/clippy/fmt suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test fixtures -> production code paths | This plan's new/rewritten tests exercise the exact same export_pdf/list_all_for_editor/update_body/reset_to_default/validate_preview code paths real users hit — no test-only bypass of the allowlist/autoescape mitigations from Plans 17-01/17-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Repudiation | Test suite completeness | mitigate | New html_report_render.rs + rewritten template_edit.rs are the durable regression proof that Req 1/2/5's autoescape/allowlist mitigations (T-17-01-01, T-17-02-01, T-17-02-02) keep working across future refactors — a regression in either would fail these tests before reaching production |
| T-17-04-02 | Tampering | krilla #[ignore] test hygiene | accept | pdf_determinism.rs's frozen tests remain #[ignore]d and require explicit "cargo test -- --ignored" to run — this is intentional (D-13), not a gap; they exist to let a developer manually re-verify the frozen krilla path still compiles/runs if ever needed |
| T-17-04-SC | Tampering | No new package-manager installs in this plan | accept | Plan adds zero new Cargo/npm dependencies — pure test/hygiene work |
</threat_model>

<verification>
- cargo test -p trackly-app — full suite green (all test binaries, no #[ignore] regressions introduced)
- cargo test -p trackly-app --test html_report_render --test template_edit — both new/rewritten files green in isolation
- cargo clippy -p trackly-app --all-targets -- -D warnings clean
- cargo fmt --check clean
- grep -rn "render_docspec" crates/trackly-app/src/services/report_service.rs crates/trackly-app/src/services/template_service.rs crates/trackly-app/src/tauri_cmds/reports.rs crates/trackly-app/src/http/reports.rs crates/trackly-app/src/tauri_cmds/settings_org.rs crates/trackly-app/src/http/settings_org.rs returns 0 matches
</verification>

<success_criteria>
- cargo test -p trackly-app full suite green after the Reports/Templates HTML migration
- tests/html_report_render.rs proves 1-row, multi-month-grouped, empty, and org-header HTML
  rendering for export_pdf
- tests/template_edit.rs proves the file-backed editor contract (disk reads/writes), with no
  lingering assertions against the now-decoupled DB-backed get_active path
- Zero render_docspec calls remain in the Reports/Templates service layer or their Tauri/HTTP
  adapters (Req 6 grep gate satisfied)
</success_criteria>

<output>
Create `.planning/phases/17-html-krilla/17-04-SUMMARY.md` when done
</output>
