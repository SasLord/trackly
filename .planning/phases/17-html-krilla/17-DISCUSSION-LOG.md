# Phase 17: html-krilla - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-06
**Phase:** 17-html-krilla
**Areas discussed:** Структура report.html, Контекст/переменные report.html, Превью-модалка Отчётов, Миграция редактора Шаблонов

Требования зафиксированы 17-SPEC.md (ambiguity 0.15) — обсуждались только реализационные (КАК) решения.

---

## Структура report.html

**Layout**

| Option | Description | Selected |
|--------|-------------|----------|
| Порт DocSpec: h3 + `<table>` | Прямой перенос текущей структуры, минимальный риск | |
| Новый дизайн таблицы | Переработать визуал (zebra, отступы, шрифты) | ✓ |

**Что главное в новом дизайне**

| Option | Description | Selected |
|--------|-------------|----------|
| Чистый современный вид | Zebra-строки, лёгкие границы, выделенный `<thead>`, помесячные разделители, A4 | ✓ |
| Компактный плотный | Максимум строк на странице, мелкий шрифт | |
| Опишу сам | Freeform | |

**Шапка организации**

| Option | Description | Selected |
|--------|-------------|----------|
| Как в актах | 2-колоночный блок логотип+реквизиты из act_handover.html | ✓ |
| Упрощённая | Только название + логотип | |

**Метки колонок**

| Option | Description | Selected |
|--------|-------------|----------|
| Rust передаёт метки | Шаблон генеричен, итерирует columns | ✓ |
| Метки в шаблоне | Хардкод меток с ветвлением по типу отчёта | |

**Notes:** Новый визуал — стилистическая переработка того же набора данных; новых колонок/полей нет (границы SPEC соблюдены).

---

## Контекст/переменные report.html

**Группировка по месяцам**

| Option | Description | Selected |
|--------|-------------|----------|
| На стороне Rust | groups = [{month_label, rows[]}]; шаблон итерирует | ✓ |
| Плоские строки + month_key | Группировка в MiniJinja | |

**Форма строки**

| Option | Description | Selected |
|--------|-------------|----------|
| Ячейки по порядку колонок | row = list значений; `{% for cell in row %}` | ✓ |
| Объекты с именованными полями | row = {number, giver_name, ...}; ветвление по типам | |

**Формат дат**

| Option | Description | Selected |
|--------|-------------|----------|
| МСК, формат в Rust | dd.mm.yyyy как date_human в актах | |
| Оставить как есть | Не трогать текущее форматирование (только миграция движка) | ✓ |

**Notes:** Даты сохраняют текущее поведение row_field (то же, что CSV сегодня); форматирование отложено, чтобы не расширять скоуп.

---

## Превью-модалка Отчётов

**Как подать HTML**

| Option | Description | Selected |
|--------|-------------|----------|
| mode='report' в PdfPreviewModal | Модалка сама зовёт reports_export_pdf по параметрам | ✓ |
| Обобщённый renderCall-callback | ReportsPage тянет HTML и передаёт | |

**Кнопка «Скачать PDF»**

| Option | Description | Selected |
|--------|-------------|----------|
| Полностью заменить печатью | Печать + Save-as-PDF браузера; CSV остаётся | ✓ |
| Оставить отдельное сохранение | Отдельная кнопка сохранения файла | |

---

## Миграция редактора Шаблонов

**Данные превью**

| Option | Description | Selected |
|--------|-------------|----------|
| Встроенные sample-данные | Детерминированно, работает на пустой БД | ✓ |
| Реальная запись | Последний акт / текущий отчёт | |

**Панель переменных**

| Option | Description | Selected |
|--------|-------------|----------|
| По выбранному шаблону | Переменные только текущего kind | ✓ |
| Общий список | Все переменные всех шаблонов | |

**Старый DB-путь шаблонов**

| Option | Description | Selected |
|--------|-------------|----------|
| Заморозить, не трогать | Таблица + seed остаются, редактор не указывает на них | ✓ |
| Отключить seed | Перестать сидить DB-шаблоны на старте | |

---

## Claude's Discretion

- Имена команд (`templates_*` reuse vs new), точные сигнатуры адаптеров, маппинг kind→filename.
- Регистрация `report.html` в `DEFAULT_HTML_TEMPLATES` + материализация на старте.
- Конкретная структура sample-данных editor-превью.
- Конкретный CSS «чистого современного вида» таблицы отчёта.

## Deferred Ideas

- Полное удаление krilla/DocSpec/`pdf/*.rs` — будущая уборка.
- Форматирование/локализация дат отчёта (МСК dd.mm.yyyy).
- WYSIWYG-редактор (DOC-01), импорт/экспорт .docx (DOC-02), прочие документы (DOC-03) — вне milestone.
