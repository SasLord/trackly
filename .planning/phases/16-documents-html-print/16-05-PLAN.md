---
phase: 16-documents-html-print
plan: 05
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - crates/trackly-app/tests/pdf_render_act.rs
  - crates/trackly-app/tests/pdf_column_overflow.rs
  - crates/trackly-app/tests/pdf_logo.rs
  - crates/trackly-app/tests/acts_e2e_smoke.rs
  - crates/trackly-app/tests/html_act_render.rs
  - crates/trackly-app/tests/pdf_determinism.rs
autonomous: true
requirements: [SPEC-Req3, SPEC-Req8]

must_haves:
  truths:
    - "cargo test -p trackly-app compiles and passes after the Vec<u8>->String change (no test file left asserting PDF-magic-header/pdf-extract behavior against ActService::render_pdf output)"
    - "A new test module asserts both acts' HTML output contains all required blocks/fields plus the logo data: URI (D-14 item 1)"
    - "A new test asserts 1-device vs N-device acts both render all item rows without truncation (D-14 item 2)"
    - "A new test asserts fallback-to-embedded-default vs read-from-templates-file behavior, including that editing the file changes the output (D-14 item 3)"
    - "A new test asserts the rendered HTML contains no http(s):// references in src/href attributes (D-14 item 4, self-contained/offline guarantee)"
    - "Heavy/slow krilla-path tests (pdf_determinism, full DocSpec-render tests) that call PdfRenderer::render_docspec directly (frozen path) remain green and untouched — only ActService-mediated full-pipeline tests are migrated"
    - "Decisions implemented in this plan: D-13, D-14"
  artifacts:
    - path: "crates/trackly-app/tests/html_act_render.rs"
      provides: "New HTML-generation test suite covering D-14 items 1-4 for both acts"
      contains: "fn html_"
  key_links:
    - from: "crates/trackly-app/tests/html_act_render.rs"
      to: "crates/trackly-app/src/services/act_service.rs"
      via: "ActService::render_pdf / render_acceptance_pdf returning HTML asserted via .contains(...)"
      pattern: "render_pdf|render_acceptance_pdf"
    - from: "crates/trackly-app/tests/html_act_render.rs"
      to: "crates/trackly-infra/src/paths.rs"
      via: "Paths::resolve_for_exe_dir(dir.path()) test seam, same as pdf_render_act.rs's make_full_pipeline"
      pattern: "resolve_for_exe_dir"
---

<objective>
Migrate every existing test that calls `ActService::render_pdf`/
`render_acceptance_pdf` (which now return HTML strings, not PDF bytes) away
from `pdf-extract`/PDF-magic-header assertions and onto HTML-string
assertions; add a new dedicated test module covering the four D-14
requirements (block/field presence + logo, 1-vs-N devices, fallback-vs-file,
offline/no-CDN); and confirm the frozen krilla-path tests that call
`PdfRenderer::render_docspec` directly (not through `ActService`) remain
untouched and green as bit-rot guards per D-13.

Purpose: `cargo test -p trackly-app` must stay green end-to-end (Req 8's
acceptance criterion) — this plan is where every test broken by Plan 16-02's
return-type change gets fixed, and where the phase's core new-behavior
guarantees (D-14) get automated coverage.
Output: All test files compile and pass; new `html_act_render.rs` proves the
phase's must-have behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-documents-html-print/16-SPEC.md
@.planning/phases/16-documents-html-print/16-CONTEXT.md
@.planning/phases/16-documents-html-print/16-PATTERNS.md
@.planning/phases/16-documents-html-print/16-02-SUMMARY.md
</context>

<interfaces>
<!-- Test-seam pattern already established (pdf_render_act.rs, already read in
     planning) — reuse verbatim for the new test file and for fixing existing
     ones. -->
```rust
let (writer, readers, dir) = test_writer_and_readers();
let clock: Arc<dyn Clock + Send + Sync> = Arc::new(SystemClock);
let paths = Arc::new(Paths::resolve_for_exe_dir(dir.path().to_path_buf()).expect("paths"));
let organization = Arc::new(OrganizationService::new(paths.clone()));
let templates = Arc::new(TemplateService::new(writer.clone(), readers.clone(), clock.clone()));
let pdf = Arc::new(PdfRenderer::new());
templates.seed_defaults_on_startup().await.expect("seed");
let acts = ActService::new(writer.clone(), readers.clone(), clock.clone())
    .with_pdf_pipeline(templates.clone(), organization.clone(), pdf.clone());
    // .with_org_db(org_db.clone()) if org requisites/logo needed
```
Note: after Plan 16-02, `ActService` may require a `.with_paths(paths.clone())`
call (if that builder method was added) for `render_pdf`/`render_acceptance_pdf`
to resolve the HTML templates directory — check Plan 16-02's SUMMARY for the
exact builder-method name/shape before writing new pipeline-construction code
in this plan's tests; every `make_full_pipeline`-style helper in the existing
test files that call `render_pdf`/`render_acceptance_pdf` will need the same
addition.

For the "templates dir override" tests (D-14 item 3), use
`TRACKLY_TEMPLATES_DIR` (set to a `TempDir` subpath) OR — if Plan 16-02's
`resolve_templates_dir` takes `&Paths` directly — construct `Paths::resolve_for_exe_dir(dir.path())`
pointing at a temp exe_dir and write/omit `templates/act_handover.html` inside
`dir.path().join("templates")` before calling `render_pdf`, matching the
existing per-test `TempDir` isolation convention.

<!-- pdf_column_overflow.rs's full-pipeline test to migrate (already read in
     planning, lines 115-200ish) — the assertion to replace: -->
```rust
let bytes = acts.render_pdf(act.id).await.expect("render_pdf");
let text = pdf_extract::extract_text_from_mem(&bytes).expect("extract");
assert!(!text.contains('…'), ...);
```
Becomes (conceptually): `let html = acts.render_pdf(act.id).await.expect("render_pdf");
assert!(!html.contains('…'), "long field must wrap, not truncate, in HTML output");`
— word-wrap in HTML happens via CSS at print time, not at generation time, so
the assertion shifts from "no ellipsis in extracted PDF text" to "no ellipsis
in the raw HTML string" (the long value should appear in full, since CSS
handles wrapping visually, not the generator).

<!-- pdf_logo.rs's full-pipeline test (blob_logo_via_full_pipeline_renders_in_act_pdf,
     already read in planning, lines 191-265ish) — assertion to replace: -->
```rust
let bytes = acts.render_pdf(act.id).await.expect("render_pdf");
assert_eq!(&bytes[..4], b"%PDF", ...);
assert!(bytes_contain(&bytes, b"/Subtype /Image") || bytes_contain(&bytes, b"/XObject"), ...);
```
Becomes (conceptually): `let html = acts.render_pdf(act.id).await.expect("render_pdf");
assert!(html.contains("data:image/png;base64,"), "logo must appear as a data: URI in HTML output");`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate existing ActService-mediated full-pipeline tests off PDF assertions</name>
  <files>crates/trackly-app/tests/pdf_render_act.rs, crates/trackly-app/tests/pdf_column_overflow.rs, crates/trackly-app/tests/pdf_logo.rs, crates/trackly-app/tests/acts_e2e_smoke.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_render_act.rs (full file — every test calling p.acts.render_pdf/render_acceptance_pdf, lines 161-690ish, already read in planning for structure)
    crates/trackly-app/tests/pdf_column_overflow.rs (device_card_long_field_wraps_instead_of_truncating, lines 115-205, already read in planning)
    crates/trackly-app/tests/pdf_logo.rs (blob_logo_via_full_pipeline_renders_in_act_pdf, lines 191-270, already read in planning)
    crates/trackly-app/tests/acts_e2e_smoke.rs (handover_pdf_render_within_e2e-style tests + acceptance_pdf_render_smoke + document_acceptance_pdf_renders_correct_calendar_date_for_same_day_msk_selection, lines 260-360, already read in planning)
    crates/trackly-app/src/services/act_service.rs (post-Plan-16-02 signatures + any new with_paths builder)
  </read_first>
  <behavior>
    - Every existing test in these 4 files that calls `.render_pdf(...)` or
      `.render_acceptance_pdf(...)` and asserts on the return value as bytes
      (`bytes[..4] == b"%PDF"`, `pdf_extract::extract_text_from_mem(&bytes)`,
      `bytes_contain(&bytes, ...)`, `bytes.len() > N`) must have its assertion
      rewritten to operate on the HTML string directly: replace `pdf_extract::
      extract_text_from_mem(&bytes).expect("extract")` call sites with the raw
      `html` variable itself (Cyrillic text assertions like `text.contains("Петров")`
      become `html.contains("Петров")` — HTML-escaping does not change plain
      Cyrillic text content, only special characters like `&`/`<`/`>`/`"`, none
      of which appear in the existing fixture names/values); replace `&bytes[..4]
      == b"%PDF"` checks with a check that the string starts with `<!DOCTYPE
      html` or contains `<html` (case-insensitive); replace
      `bytes_contain(&bytes, b"/Subtype /Image")` with `html.contains("data:image/")`.
      Every pipeline-construction helper (`make_full_pipeline`,
      `make_full_pipeline_with_org_db` in pdf_render_act.rs; the inline
      construction in pdf_column_overflow.rs/pdf_logo.rs/acts_e2e_smoke.rs)
      must add whatever new builder call Plan 16-02 introduced (e.g.
      `.with_paths(paths.clone())`) so `render_pdf`/`render_acceptance_pdf` can
      resolve the templates directory — check the exact method name in
      16-02-SUMMARY.md before editing.
    - `render_with_missing_template_returns_notfound` /
      `render_with_broken_template_returns_validation` in pdf_render_act.rs: these
      test the DB-backed `TemplateService::get_active` NotFound/Validation
      paths — since `render_pdf` no longer calls `pipeline.templates.get_active`
      (Plan 16-02 replaced it with `html_templates::load_template`, which
      NEVER returns an error — it falls back to the embedded default per
      D-06), these two tests are now testing dead code paths. Rewrite them to
      assert the NEW fallback behavior instead: deleting/never-creating
      `templates/act_handover.html` in the test's temp dir must NOT raise
      `NotFound`/`Validation` — it must successfully render using the embedded
      default (assert `render_pdf(...)` returns `Ok(html)` and `html` contains
      an expected default-template marker string, e.g. "Акт приема-передачи").
      Rename them accordingly (e.g. `render_falls_back_to_embedded_default_when_template_file_missing`)
      since asserting an error is no longer the correct behavior for this
      code path.
    - `signature_renders_two_line_labels`, `render_handover_multi_device_wraps_long_fields`,
      `render_handover_multi_device_paginates_when_overflowing_one_page`,
      `render_handover_default_template_uses_field_rows_not_device_card` in
      pdf_render_act.rs: these test krilla-specific rendering behavior
      (page-count via a krilla page counter, DocSpec section-type discrimination)
      that no longer applies once `render_pdf` stops producing a
      krilla-rendered PDF. Since browser pagination (CSS `@page`/`page-break-inside`)
      cannot be asserted from a Rust unit test against a raw HTML string,
      DELETE these krilla-behavior-specific tests from `pdf_render_act.rs`
      entirely (they tested Phase 15 krilla internals that Plan 16-02 replaced)
      — their successor coverage (multi-device HTML correctness) is added
      fresh in Task 2 of this plan (`html_act_render.rs`), which asserts
      HTML-appropriate equivalents (all item rows present, `page-break-inside:
      avoid` present in the `<style>` block) instead of krilla page-count
      internals.
  </behavior>
  <action>
    Apply the rewrites/deletions described in `<behavior>` above to all four
    files. Keep test function names for tests whose assertions are merely
    translated (bytes -> html string), rename or delete tests whose entire
    premise (krilla error paths, krilla page-count internals) no longer
    applies per the specific guidance per test above. Do not touch any test in
    these files that calls `PdfRenderer::render_docspec` directly (not through
    `ActService`) — those are the frozen krilla-path bit-rot guards per D-13
    and are explicitly out of this task's scope (there are none of those in
    these 4 files per the grep performed during planning — `pdf_column_overflow.rs`'s
    `truncate_to_width_*`/`long_name_truncated_does_not_overlap_inv_no` tests
    and `pdf_logo.rs`'s `act_with_logo_renders_image_in_pdf`/`logo_path_none_renders_without_panic`/
    `logo_path_missing_file_is_graceful`/`logo_bytes_blob_renders_image_in_pdf`/
    `logo_bytes_takes_priority_over_logo_path` all call `PdfRenderer::render_docspec`
    directly via a `base_spec(...)` helper, NOT through `ActService` — leave
    every one of those completely unchanged).
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test pdf_render_act --test pdf_column_overflow --test pdf_logo --test acts_e2e_smoke</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test pdf_render_act --test pdf_column_overflow --test pdf_logo --test acts_e2e_smoke` exits 0
    - `grep -rn "pdf_extract::extract_text_from_mem" crates/trackly-app/tests/pdf_render_act.rs crates/trackly-app/tests/pdf_column_overflow.rs crates/trackly-app/tests/acts_e2e_smoke.rs` returns 0 matches for any call site that follows a `.render_pdf(` or `.render_acceptance_pdf(` call (manually verify — pdf_logo.rs's direct-renderer tests are exempt and may retain their own separate pdf_extract usage if present)
    - `grep -n "b\"%PDF\"" crates/trackly-app/tests/pdf_render_act.rs crates/trackly-app/tests/acts_e2e_smoke.rs` returns 0 matches following an `ActService::render_pdf`/`render_acceptance_pdf` call site
    - Manual confirm: `render_with_missing_template_returns_notfound`/`render_with_broken_template_returns_validation` are renamed/rewritten to assert successful fallback, not error
  </acceptance_criteria>
  <done>All 4 test files compile and pass against the new String-returning ActService API; every assertion that used to inspect PDF bytes now inspects the HTML string; krilla-specific tests with no HTML equivalent are removed with justification; direct-renderer (frozen-path) tests in pdf_logo.rs/pdf_column_overflow.rs are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New html_act_render.rs — D-14 items 1-4 coverage for both acts</name>
  <files>crates/trackly-app/tests/html_act_render.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_render_act.rs (test-seam helpers, post-Task-1 state — reuse the same make_full_pipeline shape)
    crates/trackly-app/src/pdf/html_templates.rs (DEFAULT_HTML_TEMPLATES, resolve_templates_dir, load_template — from Plan 16-01)
    crates/trackly-app/templates/act_handover.html, crates/trackly-app/templates/act_acceptance.html (from Plan 16-01 — know the exact block markers to assert on)
  </read_first>
  <behavior>
    - Test `html_handover_contains_required_blocks_and_logo`: build a full
      pipeline with org logo saved via `OrgDbService::save_logo` (mirrors
      `pdf_logo.rs`'s `blob_logo_via_full_pipeline_renders_in_act_pdf` setup),
      create a 1-device handover act, call `render_pdf`, assert the returned
      HTML `.contains(...)` each of: "Акт приема-передачи" (title), the
      act number, "Выдал", "Получил", "Подпись", "ФИО" (signature block), and
      `"data:image/png;base64,"` (logo).
    - Test `html_acceptance_contains_required_blocks`: build a pipeline,
      create a device, call `render_acceptance_pdf`, assert the HTML contains
      "Документ приёма устройства" and the giver/receiver names passed in.
    - Test `html_handover_multi_device_all_items_present_no_truncation`: create
      a handover act with 2+ devices, one of which has a long
      `complectation_at_time` value (reuse the >150-char fixture string
      pattern from `pdf_column_overflow.rs`), call `render_pdf`, assert the
      HTML contains EACH device's name/inventory_no AND the full long value
      verbatim (no `…` truncation marker anywhere in the output).
    - Test `html_falls_back_to_embedded_default_when_file_absent`: construct a
      pipeline pointed at a `TempDir` where `templates/act_handover.html` is
      NEVER created (or is deleted after `materialize_defaults_on_startup` ran),
      call `render_pdf`, assert it succeeds (`Ok`) and the HTML matches the
      embedded-default marker content.
    - Test `html_uses_file_when_present_and_edit_changes_output`: write a
      custom marker string (e.g. a unique sentinel HTML comment
      `<!-- CUSTOM-TEST-MARKER-{unique} -->`) into
      `templates_dir.join("act_handover.html")` before calling `render_pdf`
      (using the SAME ctx-shaped template — either reuse the real default body
      with the marker appended, or a minimal valid MiniJinja HTML template
      using only variables the real ctx provides), assert the returned HTML
      contains the sentinel marker, proving the on-disk file (not the embedded
      default) was used.
    - Test `html_is_offline_safe_no_external_links`: for both `render_pdf` and
      `render_acceptance_pdf` output (reuse the fixtures from the first two
      tests), assert the HTML string does NOT contain the substring `"http://"`
      or `"https://"` (case-insensitive) — the only external-looking scheme
      permitted is `data:`, which is unaffected by this check since `"http"` is
      not a substring of `"data:"`.
  </behavior>
  <action>
    Create `crates/trackly-app/tests/html_act_render.rs` as a new integration
    test file implementing all 6 behaviors above, following the exact
    `test_writer_and_readers()` + `Paths::resolve_for_exe_dir(dir.path())` +
    `ActService::new(...).with_pdf_pipeline(...)` construction pattern from
    `pdf_render_act.rs`'s `make_full_pipeline` (plus whatever new `with_paths`
    builder Plan 16-02 introduced), and the `OrgDbService::save_logo` setup
    pattern from `pdf_logo.rs`'s `blob_logo_via_full_pipeline_renders_in_act_pdf`
    for the logo-bearing tests. Use `#[tokio::test(flavor = "multi_thread",
    worker_threads = 4)]` matching the existing convention in these files (the
    writer/reader pool spawns blocking tasks that need a multi-thread runtime).
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test html_act_render</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test html_act_render` exits 0 with all 6 tests passing
    - `grep -c "^async fn html_" crates/trackly-app/tests/html_act_render.rs` returns 6
    - `grep -n "data:image" crates/trackly-app/tests/html_act_render.rs` returns at least 1 match
    - `grep -n "http://\|https://" crates/trackly-app/tests/html_act_render.rs` — only appears inside string-literal assertions checking ABSENCE (e.g. `!html.contains("http://")`), never as an actual URL being embedded in a fixture
  </acceptance_criteria>
  <done>html_act_render.rs exists with 6 passing tests covering block/field+logo presence, multi-device no-truncation, fallback-vs-file (including live edit-changes-output), and offline/no-CDN safety for both acts.</done>
</task>

<task type="auto">
  <name>Task 3: D-13 krilla test hygiene — mark heavy direct-renderer tests #[ignore]</name>
  <files>crates/trackly-app/tests/pdf_determinism.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_determinism.rs (full file, 64 lines — already read in planning)
  </read_first>
  <action>
    Per D-13 ("тяжёлые/медленные (`pdf_determinism`, рендер реального PDF) —
    `#[ignore]`"), add `#[ignore]` immediately above the existing `#[test]`
    attribute on both `fixture_act_42_renders_to_known_hash` and
    `rendering_twice_yields_identical_bytes` in `pdf_determinism.rs`, each with
    a short comment explaining why (e.g. `// D-13 (Phase 16): frozen krilla
    path, ignored by default — run explicitly with \`cargo test -- --ignored\`
    to verify byte-determinism after a deliberate renderer change.`). Do NOT
    touch `pdf_column_overflow.rs`'s `truncate_to_width_*`/
    `long_name_truncated_does_not_overlap_inv_no` tests or `pdf_logo.rs`'s
    direct-renderer tests (`act_with_logo_renders_image_in_pdf`,
    `logo_path_none_renders_without_panic`, `logo_path_missing_file_is_graceful`,
    `logo_bytes_blob_renders_image_in_pdf`, `logo_bytes_takes_priority_over_logo_path`)
    — these are the FAST krilla bit-rot guards D-13 explicitly says must stay
    green and un-ignored (single-digit-millisecond `PdfRenderer::render_docspec`
    calls against small in-memory `DocSpec` fixtures, not slow).
  </action>
  <verify>
    <automated>grep -c "#\[ignore\]" crates/trackly-app/tests/pdf_determinism.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#\[ignore\]" crates/trackly-app/tests/pdf_determinism.rs` returns 2
    - `cargo test -p trackly-app --test pdf_determinism` exits 0 with 0 tests run (both ignored, none failed)
    - `cargo test -p trackly-app --test pdf_determinism -- --ignored` exits 0 with 2 tests passing (proves the ignored tests still work when explicitly requested)
    - `grep -c "#\[ignore\]" crates/trackly-app/tests/pdf_logo.rs crates/trackly-app/tests/pdf_column_overflow.rs` returns 0 for both files (fast bit-rot guards remain un-ignored)
  </acceptance_criteria>
  <done>pdf_determinism.rs's two tests are marked #[ignore] with an explanatory comment per D-13, still pass when run explicitly via --ignored, and no other krilla direct-renderer test file gains an #[ignore] it shouldn't have.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test fixtures -> render_pdf/render_acceptance_pdf | Test-only, no production trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering (regression) | Deleted krilla-behavior tests (page-count, DocSpec section-type) | accept | These tests validated krilla-internal behavior (Phase 15) that is structurally inapplicable once `render_pdf` stops producing krilla output; their removal is a scope-appropriate consequence of the pivot, not a coverage gap — HTML-equivalent coverage (multi-device completeness, no truncation) is added fresh in Task 2 |
| T-16-14 | Repudiation (silent regression) | `render_with_missing_template_returns_notfound`/`render_with_broken_template_returns_validation` semantic flip | mitigate | Explicitly renamed and reasserted (Task 1) to prove the NEW correct behavior (graceful fallback, not error) rather than silently deleted — prevents a future reader from assuming missing-template handling was simply dropped |

</threat_model>

<verification>
- `cargo test -p trackly-app` exits 0 (full suite green, including the new html_act_render.rs and all migrated tests)
- `cargo clippy -p trackly-app --tests -- -D warnings` exits 0
- `cargo fmt --check` exits 0
- `cargo test -p trackly-app --test pdf_determinism --test pdf_column_overflow --test pdf_logo -- --ignored` — confirm D-13's heavy/ignored krilla tests still exist and are marked `#[ignore]` where PATTERNS.md specifies (pdf_determinism), and that fast krilla bit-rot guards (pdf_logo's direct-renderer tests, pdf_column_overflow's truncate_to_width unit tests) remain un-ignored and green
</verification>

<success_criteria>
- `cargo test -p trackly-app` is fully green after the Vec<u8>->String migration
- New `html_act_render.rs` proves all 4 D-14 requirements for both acts
- Frozen krilla-path direct-renderer tests are untouched and still pass
- No test silently asserts against dead/unreachable code paths
</success_criteria>

<output>
Create `.planning/phases/16-documents-html-print/16-05-SUMMARY.md` when done
</output>
