---
phase: 39-place-tree
plan: 21
type: execute
wave: 10
depends_on: ["39-15", "39-16", "39-17", "39-18", "39-20"]
files_modified:
  - ui/src/lib/components/LocationAutocomplete.svelte
autonomous: false
requirements: [PLC-04]

must_haves:
  truths:
    - "LocationAutocomplete.svelte больше не существует в дереве файлов — ни один компонент приложения его не импортирует (D-17, PLC-04)"
    - "Ни один файл в crates/ или ui/src не содержит location_id, bulk_location_id, location_id_override, location_name, locations_autocomplete, resolve_location_id_in_tx, upsert_location_in_tx — весь словарь свободнотекстового размещения удалён из кода (PLC-04)"
    - "Полный набор тестов (cargo test с обязательным skip и mock-переменными) зелёный; cargo clippy, svelte-check и pnpm build проходят без ошибок — фаза не оставляет красный CI (урок вехи v1.3.3)"
    - "Существующая (пред-Фазой-39) БД открывается портативным приложением после миграции без падения; значения размещения не переносятся — обнуляются, как подтверждено пользователем дважды в CONTEXT.md — это ожидаемое поведение, а не дефект"
  artifacts:
    - path: "ui/src/lib/components/LocationAutocomplete.svelte"
      provides: "DELETED — file must not exist after this plan"
---

<objective>
Delete `LocationAutocomplete.svelte`, sweep the entire codebase for any remaining reference to the
pre-Phase-39 location vocabulary, run the full verification gate (tests, clippy, svelte-check,
build), and manually confirm an existing (pre-Phase-39) portable DB upgrades safely — without data
preservation, per the locked "no migration" decision, but without crashing either.

Purpose: this is the phase-closing plan. Every prior plan's `<action>` targeted specific files;
this plan is the safety net that catches anything the file-by-file enumeration in RESEARCH.md/
PATTERNS.md missed (the CartridgeFormBody/OperationModal/ActFormBody/ReturnModal/
ReturnItemsTable/PrinterCreateModal consumers of `LocationAutocomplete` were found by this planner's
own grep sweep, not enumerated in the upstream research artifacts — this plan's grep-based
acceptance criteria are the final backstop against any similarly missed reference).
Output: `LocationAutocomplete.svelte` deleted; zero remaining references to the old vocabulary;
green CI; a verified-safe DB-upgrade path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep-verify zero remaining references; delete LocationAutocomplete.svelte</name>
  <files>ui/src/lib/components/LocationAutocomplete.svelte</files>
  <read_first>
    None — this task is a repository-wide sweep, not a single-file read. Use grep across `crates/`
    and `ui/src/` as the primary tool.
  </read_first>
  <action>
    Run `grep -rn "LocationAutocomplete" ui/src` — every remaining match must be either (a) inside
    `LocationAutocomplete.svelte` itself (about to be deleted) or (b) a stale comment referencing the
    filename as a historical precedent (e.g. `CompatibilityEditor.svelte`/`ModelFormModal.svelte`'s
    doc comments noting "now matches Input.svelte/LocationAutocomplete.svelte/PersonAutocomplete.svelte"
    conventions) — for case (b), update the comment to reference `PlacePicker.svelte` instead if it
    is describing a still-relevant pattern, or remove the reference if it is purely historical. Run
    `grep -rn "location_id\|bulk_location_id\|location_id_override\|location_name\b" crates/ ui/src`
    and `grep -rn "locations_autocomplete\|resolve_location_id_in_tx\|upsert_location_in_tx" crates/
    ui/src` — every match found here is a gap in a prior plan (05 through 19) that this plan must
    close directly (edit the offending file in place; do not defer). Once both sweeps return zero
    unexplained matches, delete `ui/src/lib/components/LocationAutocomplete.svelte`.
  </action>
  <verify>
    <automated>grep -rc "LocationAutocomplete" ui/src | awk -F: '{s+=$2} END {print s+0}' && test -f ui/src/lib/components/LocationAutocomplete.svelte && grep -rc "location_id\|bulk_location_id\|location_id_override\|locations_autocomplete\|resolve_location_id_in_tx\|upsert_location_in_tx" crates/ ui/src | awk -F: '{s+=$2} END {print s+0}'</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -rc "LocationAutocomplete" ui/src | awk -F: '{s+=$2} END {print s+0}'` returns 0
    - `test -f ui/src/lib/components/LocationAutocomplete.svelte` fails (file does not exist)
    - `grep -rc "location_id\|bulk_location_id\|location_id_override\|locations_autocomplete\|resolve_location_id_in_tx\|upsert_location_in_tx" crates/ ui/src | awk -F: '{s+=$2} END {print s+0}'`
      returns 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full verification gate</name>
  <files>none (verification-only task; fixes any residual compile/lint errors surfaced by Task 1's sweep, in whichever files they occur)</files>
  <read_first>
    39-VALIDATION.md (Known Test Constraints — mandatory `--skip login_remember_persistent_cookie`,
    `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1`, never run two `cargo test` invocations concurrently).
  </read_first>
  <action>
    Run, in this exact sequence (never concurrently): `cargo clippy --all-targets -- -D warnings`;
    `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip
    login_remember_persistent_cookie --test-threads=1`; `cargo test -p trackly-infra`; `pnpm --dir ui
    exec svelte-check`; `pnpm --dir ui exec eslint .` (or whatever the project's existing lint-check
    npm script is named — grep `package.json` scripts first); `pnpm --dir ui build`. Fix any error
    surfaced by these gates that traces back to this phase's changes (do not fix unrelated
    pre-existing failures outside this phase's scope — if one is found, note it in the SUMMARY
    instead of silently absorbing scope creep).
  </action>
  <verify>
    <automated>cargo clippy --all-targets -- -D warnings && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1 && cargo test -p trackly-infra && pnpm --dir ui exec svelte-check && pnpm --dir ui build</automated>
  </verify>
  <done>Clippy, the full backend test suite (with the documented skip/mocks), svelte-check, and the
  frontend build all pass with zero errors attributable to this phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: The completed Phase 39 place-tree migration: schema, backend, and every UI surfa</name>
  <action>No code changes in this task — see &lt;how-to-verify&gt; for the manual verification steps to perform.</action>
  <what-built>The completed Phase 39 place-tree migration: schema, backend, and every UI surface,
  with `locations`/`LocationAutocomplete` fully removed.</what-built>
  <how-to-verify>
    1. Locate or create a scratch pre-Phase-39 database fixture (a portable DB file with the V001-
       V036 schema applied, containing ONLY invented data — e.g. a fictional "Здание А" location row
       and a couple of test devices — never real organization data, per the project's hard privacy
       rule). If no such fixture exists, create one by checking out the commit immediately before
       this phase's migrations landed, running the app once to seed a fresh DB with a couple of
       invented rows, then reverting to this branch.
    2. Copy that DB file next to the current build's executable (or point the app at it via the
       existing "choose database location" setting).
    3. Launch the built app (`cargo tauri dev` or a release build) against that DB file. Confirm it
       opens without error or crash.
    4. Open a device that previously had a location value assigned. Confirm its place field now
       shows empty/unset (place is optional, D-07) rather than the app crashing or showing a broken
       value — this is the expected, locked behavior (data zeroed out, not migrated, confirmed twice
       in CONTEXT.md).
    5. Confirm the app is otherwise fully functional against this upgraded DB: create a place, assign
       it to that device, create an act referencing it, print the act, confirm the place path
       appears correctly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full codebase sweep (Task 1) | No new trust boundary — this task removes dead/legacy surface area, reducing the attack surface (one fewer place-entry component, one fewer auto-create code path) |
| Existing portable DB → V037/V038 migration on first launch | An operator's real DB file, containing potentially years of data in every OTHER table, must survive this phase's schema migration intact — only the location-related columns/table are affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-21-01 | Denial of Service | Migration failure on a real operator's existing DB, corrupting or blocking access to unrelated data (users, other entities) | mitigate | Plan 01's `migration_idempotency.rs` already proves schema-level correctness in CI; this plan's manual checkpoint proves it end-to-end against a realistic (if invented-data) fixture before the phase is considered closed |
| T-39-21-02 | Tampering | A leftover reference to the deleted `locations_autocomplete`/`resolve_location_id_in_tx` surface silently reintroducing the D-18 auto-create gap | mitigate | Task 1's grep sweep is the explicit backstop for this — any survivor is treated as a plan gap to close, not an acceptable residue |
</threat_model>

<verification>
Task 2's automated gate green. Task 3's manual checkpoint approved by the user against a real
(invented-data) pre-Phase-39 DB fixture.
</verification>

<success_criteria>
Zero references to the pre-Phase-39 location vocabulary remain anywhere in the codebase;
`LocationAutocomplete.svelte` is deleted; full CI (clippy/tests/svelte-check/build) is green; an
existing portable DB upgrades safely (schema-only, no data preservation, as locked) without
crashing.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-21-SUMMARY.md` when done
</output>
