---
phase: 39-place-tree
plan: 10
type: execute
wave: 3
depends_on: ["39-04", "39-03"]
files_modified:
  - crates/trackly-app/src/services/report_service.rs
  - crates/trackly-app/src/services/request_service.rs
  - crates/trackly-infra/src/repos/requests_sqlite.rs
  - crates/trackly-infra/src/repos/printers_sqlite.rs
  - crates/trackly-app/src/dto/printer.rs
  - crates/trackly-app/src/dto/request.rs
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/tests/request_printer_options.rs
autonomous: true
requirements: [PLC-04]

must_haves:
  truths:
    - "Три отчёта (акты, устройства, картриджи/принтеры) больше не JOIN-ят таблицу locations — все читают место через place_full_paths; выходные колонки location_name/location_id переименованы в place_path/place_id"
    - "Фильтр отчётов по месту захватывает поддерево целиком (WITH RECURSIVE subtree), а не только точное совпадение place_id — прямое следствие D-28"
    - "Список принтеров для формы заявки сортируется и отображается по разрешённому пути места, а не по location — тот же контракт, что и раньше (место без указания сортируется последним), без переосмысления бизнес-правила (объём Фазы 42 не трогается)"
    - "dto/printer.rs (PrinterDto.device_place) и dto/request.rs (RequestDto.printer_place, RequestPrinterOptionDto.place) переименованы вслед за доменными полями device_place/printer_place (План 03) — иначе impl From<PrinterRow> for PrinterDto и impl From<RequestRow> for RequestDto не компилируются после доменного переименования"
    - "PrinterDto дополнительно несёт device_place_id: Option<i64> (числовой id места принтера, не только отображаемая строка device_place) — нужен фронтенду (План 16, OperationModal.svelte) для программного выбора значения в PlacePicker при выборе принтера в операции Install"
    - "Отчёты (акты/устройства/картриджи/принтеры) поддерживают быстрый фильтр ReportFilter.is_storage — геометрический признак (place_id входит в поддерево is_storage-узла по D-11.4 через recursive-CTE по предкам), НЕ смешанный со статусом предмета «на складе» (D-11.2, D-11.5)"
  artifacts:
    - path: "crates/trackly-app/src/services/report_service.rs"
      provides: "3 report queries + filter clauses on place_id/place_full_paths instead of locations"
      contains: "place_full_paths"
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "ReportFilter.place_id/is_storage, ReportRow.place_path"
      contains: "is_storage"
  key_links:
    - from: "crates/trackly-app/src/services/report_service.rs"
      to: "migrations/V037__places.sql"
      via: "LEFT JOIN place_full_paths pfp ON pfp.place_id = ..."
      pattern: "place_full_paths"
---

<objective>
Migrate the remaining read-path SQL that still joins the (now-dropped) `locations` table: three
report queries in `report_service.rs`, the printer-options query in `request_service.rs`, and the
supporting repo joins in `requests_sqlite.rs`/`printers_sqlite.rs`. All mechanical — replace `LEFT
JOIN locations` with `LEFT JOIN place_full_paths`, rename output columns, no business-logic change.

Purpose: closes the last `locations`-table references outside the entity CRUD paths already
covered by Plans 06/07/09, so `DROP TABLE locations` (Plan 01) does not leave any dangling query.
Output: `report_service.rs`, `request_service.rs`, `requests_sqlite.rs`, `printers_sqlite.rs`
migrated onto `place_full_paths`/`place_id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-RESEARCH.md
@.planning/phases/39-place-tree/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: report_service.rs — acts + devices report queries onto place_full_paths</name>
  <files>crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/report_service.rs (grep-confirmed line ranges: 1007
    `"location_name" => row.location_name...` sort-key match arm; 1054-1055 acts-report filter
    clause `filter.location_id` / `a.location_id = ?`; 1079-1105 acts-report SELECT with `LEFT JOIN
    locations l ON a.location_id = l.id` and `location_name` output mapping; 1146-1179 devices-report
    filter clause + SELECT with the same join shape; 1529-1550 and 1582-1594 two more filter-clause +
    JOIN occurrences for related report variants).
  </read_first>
  <action>
    For every one of the five acts/devices-report occurrences listed in `<read_first>`: replace the
    filter clause — **D-28 requires subtree-inclusive filtering, not an exact match** (choosing a
    place in the filter must capture that place and everything nested under it, matching the same
    "Здание А" → whole building semantics the Places content screen and PlacePicker filters use
    elsewhere in this phase). Do NOT write `a.place_id = ?`/`d.place_id = ?` — instead add a
    parameterized `WITH RECURSIVE subtree(id) AS (SELECT id FROM places WHERE id = ?{n} AND
    deleted_at_utc IS NULL UNION ALL SELECT p.id FROM places p JOIN subtree s ON p.parent_id = s.id
    WHERE p.deleted_at_utc IS NULL)` CTE (mirrors 39-RESEARCH.md Pattern 2, already used by Plan 04's
    `places_sqlite.rs::subtree_stats`/`list_subtree_contents`) ahead of each report query, and filter
    `a.place_id IN (SELECT id FROM subtree)` / `d.place_id IN (SELECT id FROM subtree)` — only when
    `filter.place_id` is `Some`; when `None`, skip the clause entirely as before. Replace `LEFT JOIN
    locations l ON a.location_id = l.id` / `... ON d.location_id = l.id` with `LEFT JOIN
    place_full_paths pfp ON pfp.place_id = a.place_id` / `... ON pfp.place_id = d.place_id`; replace
    the selected `l.name AS location_name` with `pfp.full_path AS place_path`; update every
    `row.get(N)` mapping and the corresponding DTO field from `location_name` to `place_path`. Update
    the sort-key match arm at line 1007 (`"location_name" => row.location_name...`) to `"place_path"
    => row.place_path...`.
  </action>
  <verify>
    <automated>grep -c "LEFT JOIN locations\|location_id\|location_name" crates/trackly-app/src/services/report_service.rs && grep -c "place_full_paths\|place_path\|place_id" crates/trackly-app/src/services/report_service.rs && grep -c "WITH RECURSIVE subtree" crates/trackly-app/src/services/report_service.rs</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "LEFT JOIN locations\|location_id\|location_name" crates/trackly-app/src/services/report_service.rs`
      in the acts/devices report functions (lines ~1000-1600 per the read_first ranges) returns 0
    - `grep -c "place_full_paths\|place_path\|place_id" crates/trackly-app/src/services/report_service.rs`
      returns at least 8 in that same region
    - `grep -c "WITH RECURSIVE subtree" crates/trackly-app/src/services/report_service.rs` returns at
      least 2 (acts report + devices report each carry their own subtree CTE — D-28 subtree-inclusive
      filtering, not an exact place_id match)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: report_service.rs — cartridges + printer report queries, combine_printer_and_place rename</name>
  <files>crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/report_service.rs (grep-confirmed: lines 275-284
    `combine_printer_and_location` helper + doc comment; lines 1245-1274 cartridges report — comment
    "cartridges has no location_id FK — uses freeform text `location` column" is now FALSE post-Plan-09
    [cartridges DO have `place_id` now], `c.location as location_name` SELECT; lines 1325-1349 a
    second cartridges-report query with the same shape; lines 1408-1435 printer report — `LEFT JOIN
    locations dl ON dl.id = d.location_id`, `dl.name AS printer_location`,
    `combine_printer_and_location(printer_name, printer_location)`; test fixtures at lines 1921-1939
    and 2140 asserting on `combine_printer_and_location`/`location_name`).
  </read_first>
  <action>
    Rename `combine_printer_and_location` to `combine_printer_and_place` (same signature shape,
    `Option&lt;String&gt;` printer place path in, formatted "Принтер / Место" string out — no logic
    change, only the parameter's semantic source changes from a joined `locations.name` to a joined
    `place_full_paths.full_path`). Update its doc comment accordingly. In the cartridges-report
    queries (~1245-1274, ~1325-1349): replace `c.location as location_name` with a
    `LEFT JOIN place_full_paths pfp ON pfp.place_id = c.place_id` + `pfp.full_path AS place_path`
    (cartridges now have a real `place_id` FK post-Plan-09 — delete the stale "no FK" comments at
    lines 1245/1325 entirely, they describe a state this phase eliminates). In the printer report
    (~1408-1435): replace `LEFT JOIN locations dl ON dl.id = d.location_id` /
    `dl.name AS printer_location` with `LEFT JOIN place_full_paths pfp ON pfp.place_id = d.place_id`
    / `pfp.full_path AS printer_place`, and update the `combine_printer_and_place(printer_name,
    printer_place)` call site. Update the test fixtures (~1921-1939, ~2140) to use the renamed
    function/field names, keeping their existing assertion values (only names change, not expected
    output strings, unless the printer-place formatting logic itself changed — it did not).
  </action>
  <verify>
    <automated>grep -c "combine_printer_and_location\|location_name\|LEFT JOIN locations\|no location_id FK\|uses freeform text"
      crates/trackly-app/src/services/report_service.rs && grep -c "combine_printer_and_place\|place_path\|place_full_paths" crates/trackly-app/src/services/report_service.rs && cargo test -p trackly-app --lib combine_printer_and_place -- --skip login_remember_persistent_cookie</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "combine_printer_and_location\|location_name\|LEFT JOIN locations\|no location_id FK\|uses freeform text"
      crates/trackly-app/src/services/report_service.rs` returns 0
    - `grep -c "combine_printer_and_place\|place_path\|place_full_paths" crates/trackly-app/src/services/report_service.rs`
      returns at least 6
    - `cargo test -p trackly-app --lib combine_printer_and_place -- --skip login_remember_persistent_cookie`
      passes (unit tests for the renamed helper)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: request_service.rs printer_options + requests_sqlite.rs + printers_sqlite.rs + dto/printer.rs + dto/request.rs</name>
  <files>crates/trackly-app/src/services/request_service.rs, crates/trackly-infra/src/repos/requests_sqlite.rs, crates/trackly-infra/src/repos/printers_sqlite.rs, crates/trackly-app/src/dto/printer.rs, crates/trackly-app/src/dto/request.rs, crates/trackly-app/tests/request_printer_options.rs</files>
  <read_first>
    crates/trackly-app/src/services/request_service.rs (lines ~236-271 — `printer_options` inline
    query: `SELECT d.id, d.name, l.name AS location FROM devices d LEFT JOIN locations l ON
    d.location_id = l.id ... ORDER BY l.name IS NULL, l.name, d.name`; `RequestPrinterOptionDto`
    construction with `location: row.get(2)`), crates/trackly-infra/src/repos/requests_sqlite.rs
    (lines ~26-75 — `dl.name AS printer_location` join + `printer_location: row.get(19)`),
    crates/trackly-infra/src/repos/printers_sqlite.rs (lines ~26-50 — `l.name AS device_location`
    join + `device_location: row.get(9)`), crates/trackly-core/src/domain/printers.rs and
    domain/requests.rs (already renamed to `device_place`/`printer_place` by Plan 03 — this task's
    job is wiring the SQL to match those already-renamed fields), crates/trackly-app/src/dto/printer.rs
    (grep-confirmed: `PrinterDto.device_location: Option&lt;String&gt;` at ~line 33 + `impl
    From&lt;PrinterRow&gt; for PrinterDto` reading `r.device_location` at ~line 68 — now nonexistent
    after `domain/printers.rs`'s `device_place` rename [Plan 03]; this DTO file is otherwise unowned
    by any Phase 39 plan and would leave `trackly-app` non-compiling if skipped), crates/trackly-app/src/dto/request.rs
    (grep-confirmed: `RequestDto.printer_location: Option&lt;String&gt;` at ~line 41 + `impl
    From&lt;RequestRow&gt; for RequestDto` reading `r.printer_location` at ~line 73 — now nonexistent
    after `domain/requests.rs`'s `printer_place` rename [Plan 03]; also `RequestPrinterOptionDto.location:
    Option&lt;String&gt;` at ~line 112, the struct this task's `printer_options` action already
    references below).
  </read_first>
  <action>
    In `request_service.rs::printer_options`, replace `LEFT JOIN locations l ON d.location_id = l.id`
    with `LEFT JOIN place_full_paths pfp ON pfp.place_id = d.place_id`, replace `l.name AS location`
    with `pfp.full_path AS place`, keep the exact same `ORDER BY pfp.full_path IS NULL,
    pfp.full_path, d.name` sort contract (places without a location sort last — literal preservation
    of the existing behavior, do not reinterpret ranking, that is Phase 42's job), and rename
    `RequestPrinterOptionDto.location` to `RequestPrinterOptionDto.place` (update the struct
    definition wherever it lives — likely `dto/request.rs`, check via grep if not co-located). In
    `requests_sqlite.rs`, replace the `LEFT JOIN locations dl ON dl.id = d.location_id` /
    `dl.name AS printer_location` pair with `LEFT JOIN place_full_paths pfp ON pfp.place_id =
    d.place_id` / `pfp.full_path AS printer_place`, matching `domain/requests.rs`'s already-renamed
    `printer_place` field (Plan 03). In `printers_sqlite.rs`, replace `LEFT JOIN locations l ON l.id =
    d.location_id` / `l.name AS device_location` with `LEFT JOIN place_full_paths pfp ON pfp.place_id
    = d.place_id` / `pfp.full_path AS device_place`, matching `domain/printers.rs`'s `device_place`
    field, and ADD `d.place_id AS device_place_id` to the same SELECT's column list (no join needed
    beyond what already exists — `d.place_id` is the same devices-table column the join predicate
    already references), mapping it to `PrinterRow.device_place_id` (Plan 03's new field) in
    `from_row`. In `dto/printer.rs`, rename `PrinterDto.device_location: Option&lt;String&gt;` to
    `device_place: Option&lt;String&gt;`, ADD a new field `device_place_id: Option&lt;i64&gt;`, and
    update `impl From&lt;PrinterRow&gt; for PrinterDto` to read `r.device_place`/`r.device_place_id`
    (the id field is what Plan 16's `OperationModal.svelte` binds `PlacePicker` to — a display
    string alone cannot drive a numeric-id picker). In `dto/request.rs`, rename `RequestDto.printer_location:
    Option&lt;String&gt;` to `printer_place: Option&lt;String&gt;` (update its doc comment to
    reference `place_full_paths`/`devices.place_id`), update `impl From&lt;RequestRow&gt; for
    RequestDto` to read `r.printer_place`, and rename `RequestPrinterOptionDto.location:
    Option&lt;String&gt;` to `place: Option&lt;String&gt;` (already referenced above in this same
    task's `request_service.rs::printer_options` edit — this is the struct-definition half of that
    change, confirmed co-located in `dto/request.rs`). Update `request_printer_options.rs` test
    assertions to match the renamed field/output shape (values unchanged, only the field name
    callers read).
  </action>
  <verify>
    <automated>grep -c "LEFT JOIN locations\|location_id\|AS location\b" crates/trackly-app/src/services/request_service.rs
      crates/trackly-infra/src/repos/requests_sqlite.rs crates/trackly-infra/src/repos/printers_sqlite.rs && grep -c "place_full_paths" crates/trackly-app/src/services/request_service.rs
      crates/trackly-infra/src/repos/requests_sqlite.rs crates/trackly-infra/src/repos/printers_sqlite.rs && grep -c "device_location\|printer_location" crates/trackly-app/src/dto/printer.rs crates/trackly-app/src/dto/request.rs && grep -c "device_place\|printer_place\|place" crates/trackly-app/src/dto/printer.rs crates/trackly-app/src/dto/request.rs && cargo build -p trackly-app</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "LEFT JOIN locations\|location_id\|AS location\b" crates/trackly-app/src/services/request_service.rs
      crates/trackly-infra/src/repos/requests_sqlite.rs crates/trackly-infra/src/repos/printers_sqlite.rs`
      returns 0 across all three files combined
    - `grep -c "place_full_paths" crates/trackly-app/src/services/request_service.rs
      crates/trackly-infra/src/repos/requests_sqlite.rs crates/trackly-infra/src/repos/printers_sqlite.rs`
      returns at least 3 (one per file)
    - `grep -c "device_location\|printer_location" crates/trackly-app/src/dto/printer.rs crates/trackly-app/src/dto/request.rs`
      returns 0
    - `cargo build -p trackly-app` succeeds — the wave-3 crate-boundary gate covering every file
      this plan owns (report_service.rs, request_service.rs, requests_sqlite.rs, printers_sqlite.rs,
      dto/printer.rs, dto/request.rs)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: dto/reports.rs place rename + is_storage quick filter (D-11.2)</name>
  <files>crates/trackly-app/src/dto/reports.rs, crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/dto/reports.rs (grep-confirmed: `ReportFilter.location_id: Option&lt;i64&gt;`
    at ~line 31 — read directly by Tasks 1/2's `filter.place_id`/`filter.location_id` clauses in
    `report_service.rs`; this DTO file is NOT in any Phase 39 plan's `files_modified` today, so Tasks
    1/2's own `filter.place_id` reads do not compile until this task lands — this is the same "shared
    struct owned by no plan" gap class already found and fixed elsewhere in this revision round, not
    a new discovery specific to D-11.2; `ReportRow.location_name: Option&lt;String&gt;` at ~line 80,
    read/written by Tasks 1/2's `place_path`-aliased SELECT columns for the identical reason),
    crates/trackly-app/src/services/report_service.rs (Task 1's five acts/devices-report
    `clauses.push(...)`/`owned_params.push(...)`/`next_idx(&owned_params)` occurrences and Task 2's
    cartridges/printer-report occurrences — this task's `is_storage` clause follows the exact same
    dynamic-WHERE-building idiom, added as one more conditional `clauses.push(...)` per query
    function), 39-CONTEXT.md D-11.4 (ancestor-inclusive `is_storage`: a place counts as storage if it
    OR any ancestor has `is_storage = 1` — the recursive CTE walks UP via `parent_id`, distinct from
    Task 1's subtree-descendant CTE which walks DOWN) and D-11.5 (this is a SEPARATE dimension from
    item status "на складе" — `device_statuses.code`/`cartridge` status columns are untouched by this
    task; do not merge the two filters into one clause or one UI control).
  </read_first>
  <action>
    In `dto/reports.rs`: rename `ReportFilter.location_id: Option&lt;i64&gt;` to `place_id:
    Option&lt;i64&gt;` (update its doc comment from "Filter by location ID" to reference the place
    tree); rename `ReportRow.location_name: Option&lt;String&gt;` to `place_path: Option&lt;String&gt;`
    (doc comment likewise); add a new `ReportFilter` field `pub is_storage: Option&lt;bool&gt;` with a
    doc comment citing D-11.2/D-11.4 ("`Some(true)` = geographically в складском месте (себя или
    любого предка); `Some(false)` = в эксплуатации (не в складском месте); `None` = без ограничения.
    Отдельное измерение от `status_id`/item-статуса «на складе», см. D-11.5.") — no `#[specta(type =
    ...)]` override needed, `bool` maps natively.
    In `report_service.rs`, in EVERY report query function Task 1/Task 2 already touch (the five
    acts/devices occurrences, the cartridges/printer occurrences): when `filter.is_storage` is
    `Some(want_storage)`, add a `storage_ids(id) AS (SELECT id FROM places WHERE is_storage = 1 AND
    deleted_at_utc IS NULL UNION ALL SELECT p.id FROM places p JOIN storage_ids s ON p.parent_id =
    s.id WHERE p.deleted_at_utc IS NULL)` CTE member alongside Task 1's `subtree` CTE (SQLite allows
    multiple comma-separated members inside one `WITH RECURSIVE` prefix; add it as a second member
    only when `filter.is_storage.is_some()`, mirroring how `subtree` is only added when
    `filter.place_id.is_some()` — the two CTEs are independent and both optional). When
    `want_storage == true`, push clause `a.place_id IN (SELECT id FROM storage_ids)` (substituting the
    query's own place-bearing alias — `a`/`d`/`c` — for each function); when `want_storage == false`,
    push `(a.place_id IS NULL OR a.place_id NOT IN (SELECT id FROM storage_ids))` (a `NULL` place is
    "не в складском месте" — not ambiguous, per D-11's "место — необязательное поле" precedent, D-07).
    Do NOT touch `status_id`/`filter.status_id` clauses anywhere in this file — D-11.5 requires the
    two filters stay independent, both in SQL and (Plan 18's job) in the UI.
  </action>
  <verify>
    <automated>grep -c "location_id\|location_name" crates/trackly-app/src/dto/reports.rs && grep -c "place_id\|place_path\|is_storage" crates/trackly-app/src/dto/reports.rs && grep -c "is_storage" crates/trackly-app/src/services/report_service.rs && grep -c "storage_ids(id) AS" crates/trackly-app/src/services/report_service.rs && cargo build -p trackly-app</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "location_id\|location_name" crates/trackly-app/src/dto/reports.rs` returns 0
    - `grep -c "place_id\|place_path\|is_storage" crates/trackly-app/src/dto/reports.rs` returns at
      least 3
    - `grep -c "is_storage" crates/trackly-app/src/services/report_service.rs` returns at least 5
      (one read per report query function touched by Tasks 1/2)
    - `grep -c "storage_ids(id) AS" crates/trackly-app/src/services/report_service.rs` returns at
      least 5 (the ancestor-walk CTE added per function, distinct from Task 1's descendant-walk
      `subtree` CTE)
    - `cargo build -p trackly-app` succeeds — this is the plan's final crate-boundary gate, now
      covering `dto/reports.rs` as well as every file Tasks 1-3 already own
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Report/request filter `place_id` params from any caller | Read-only filters, no write surface — SQL injection risk is the only relevant concern here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-10-01 | Tampering | Report/request filter SQL construction | mitigate | Every changed clause continues using `rusqlite::params![...]` parameterization, matching the pre-existing pattern in these files — no new string-concatenated SQL introduced by this rename |
| T-39-10-02 | Information Disclosure | `printer_options` exposing more than `{id, name, place}` | accept | Unchanged scope — this endpoint already deliberately excludes SNMP/community/IP/serial fields (BOLA/BOPLA closure, T-11-02-I, pre-existing); this plan only renames the location field, does not widen the payload |
</threat_model>

<verification>
`grep`-based acceptance criteria above per task. Task 3's `cargo build -p trackly-app` gate covers
every file this plan owns in isolation (this plan does not depend on Plan 06/07/09, and Plan 07 has
since moved to wave 4 — this plan's own wave-3 build gate is meaningful standalone). Full
`cargo test -p trackly-app` (the whole test suite, not just this plan's own build) is deferred to
the Wave 3 completion checkpoint alongside Plan 06/09, since other wave-3 plans' files are not this
plan's concern.
</verification>

<success_criteria>
Zero remaining `LEFT JOIN locations` in `report_service.rs`, `request_service.rs`,
`requests_sqlite.rs`, `printers_sqlite.rs`; all read via `place_full_paths`; printer-options sort
contract (unresolved-place sorts last) is preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-10-SUMMARY.md` when done
</output>
