---
phase: 39-place-tree
plan: 09
type: execute
wave: 3
depends_on: ["39-04", "39-03"]
files_modified:
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
autonomous: true
requirements: [PLC-04]

must_haves:
  truths:
    - "Все пять операций перехода картриджа (Install/ReturnToStock/ToRefill/FromRefill/WriteOff) принимают place_id вместо текстового location; upsert_location_in_tx (авто-создание по имени, аналог resolve_location_id_in_tx для картриджей) удалён полностью (D-13)"
    - "При операции Install место картриджа по умолчанию наследуется от места принтера (D-13); при этом картридж хранит СВОЁ поле place_id, не вычисляемое из принтера при последующих чтениях (D-12)"
    - "Список складских мест (is_storage с учётом предков, D-11.4) доступен сервису картриджей для предложения места при ReturnToStock"
  artifacts:
    - path: "crates/trackly-app/src/services/cartridge_service.rs"
      provides: "5 transition ops on place_id; Install defaults from printer's device.place_id"
      contains: "place_id"
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "cartridges CRUD joined to place_full_paths; upsert_location_in_tx removed"
      contains: "place_full_paths"
---

<objective>
Migrate the Cartridges entity's five transition operations (Install/ReturnToStock/ToRefill/
FromRefill/WriteOff) off freeform `location: String` onto `place_id: Option<i64>`, removing
`upsert_location_in_tx` (the cartridge-side twin of `resolve_location_id_in_tx`) and wiring
Install's printer-place default.

Purpose: cartridges get the same "caller passes a validated place_id, no implicit create" contract
Plan 06 established for devices — D-12/D-13's core mechanics.
Output: `cartridge_service.rs` and `cartridges_sqlite.rs` migrated onto `place_id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-RESEARCH.md
@.planning/phases/39-place-tree/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: cartridges_sqlite.rs — remove upsert_location_in_tx, place_id CRUD, place_full_paths join</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/cartridges_sqlite.rs (grep-confirmed lines: 55/76 `SELECT`/`from_row`
    with `c.location`; 168-197 `upsert_location_in_tx` doc + call inside the create path — "Performs
    location round-trip: if location is non-empty, inserts INSERT OR IGNORE INTO locations"; 412-413
    doc referencing "Location round-trip (INSERT OR IGNORE INTO locations)" on the update path; 463-659
    the five transition-op match arms extracting `location`/`new_location` from each
    `CartridgeTransitionOp` variant and writing them via `UPDATE cartridges SET ... location=?3 ...`
    and a second `Self::upsert_location_in_tx` call at line 659).
  </read_first>
  <action>
    Delete `upsert_location_in_tx` entirely — this is the cartridge-side equivalent of
    `resolve_location_id_in_tx` (RESEARCH Common Pitfall 4 applies identically here even though
    RESEARCH's inventory focused on the device-side helper). Change the cartridge `SELECT`/`from_row`
    to join `place_full_paths pfp ON pfp.place_id = c.place_id` and read `place_id`/`full_path`
    instead of the raw `location` text column. In the five transition-op match arms (~463-659),
    change every extracted `location`/`new_location` binding to `place_id`/`new_place_id: Option<i64>`,
    and change the `UPDATE cartridges SET status_id=?1, state_id=?2, location=?3, ...` statement to
    `place_id=?3`. Remove the trailing `Self::upsert_location_in_tx(tx, new_location, now_utc)?` call
    at line 659 — `place_id` is now written directly in the same `UPDATE`, no separate round-trip.
    Preserve the `previous_cartridge_location`-driven "restore on undo" logic (~lines 543-634) but
    rename every binding/JSON key in it to `place_id`/`previous_cartridge_place_id` per Plan 03's
    domain rename.
  </action>
  <verify>
    <automated>grep -c "upsert_location_in_tx\|c\.location\b" crates/trackly-infra/src/repos/cartridges_sqlite.rs && grep -c "place_id\|place_full_paths" crates/trackly-infra/src/repos/cartridges_sqlite.rs</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "upsert_location_in_tx\|c\.location\b" crates/trackly-infra/src/repos/cartridges_sqlite.rs`
      returns 0
    - `grep -c "place_id\|place_full_paths" crates/trackly-infra/src/repos/cartridges_sqlite.rs`
      returns at least 8 (SELECT join + 5 op arms + previous-place restore logic)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: cartridge_service.rs — Install default-from-printer, ReturnToStock storage suggestion contract</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/cartridge_service.rs (locate the public methods wrapping each of
    the five `CartridgeTransitionOp` variants — grep `CartridgeTransitionOp::` in this file for exact
    line numbers), crates/trackly-core/src/ports/places.rs (`list_storage_place_ids`, from Plan 02) —
    the D-11.4 ancestor-inheriting storage query this task exposes to the caller, not the query logic
    itself (already implemented in Plan 04).
  </read_first>
  <action>
    Update every public method wrapping a `CartridgeTransitionOp` variant to accept `place_id:
    Option&lt;i64&gt;` instead of `location: String`, validating (via the FK constraint, same pattern
    as Plan 06/07) rather than auto-creating. For `Install`: when the caller omits `place_id`, default
    it to the target printer's `device.place_id` (read via the existing printer/device lookup already
    present in this method — D-13's literal default). Do not implement any UI-facing "suggest a
    storage place" heuristic here beyond exposing the data: add a thin
    `pub async fn storage_place_ids(&self) -> Result&lt;Vec&lt;i64&gt;, AppError&gt;` wrapper around
    `PlaceRepository::list_storage_place_ids` (read-only, `ReadPlaces`-gated via the injected
    `PlaceService`/`PlaceRepository` dependency — add it as a constructor field on `CartridgeService`
    mirroring how other cross-entity dependencies are already injected in this codebase) so the
    frontend (Plan 16) can pre-select a storage place for `ReturnToStock` client-side; do not
    hardcode "pick the first storage place" business logic into the backend — D-11.3 explicitly frames
    this as a form default the user can override, which is a UI concern.
  </action>
  <verify>
    <automated>grep -c "location: String\|location: &str" crates/trackly-app/src/services/cartridge_service.rs && grep -n "device.place_id\|printer.*place_id" crates/trackly-app/src/services/cartridge_service.rs && grep -c "storage_place_ids" crates/trackly-app/src/services/cartridge_service.rs</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "location: String\|location: &str" crates/trackly-app/src/services/cartridge_service.rs`
      returns 0 in the five transition-op wrapper signatures
    - `grep -n "device.place_id\|printer.*place_id" crates/trackly-app/src/services/cartridge_service.rs`
      confirms the Install default-from-printer wiring exists
    - `grep -c "storage_place_ids" crates/trackly-app/src/services/cartridge_service.rs` returns at
      least 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cartridge transition-op `place_id` from any caller | Same trust model as device/act migrations — FK-validated, no implicit create |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09-01 | Elevation of Privilege | Removed `upsert_location_in_tx` auto-create path | mitigate | Same rationale as T-39-06-01 — cartridge operators can no longer implicitly create place-tree nodes by typing an arbitrary location string |
| T-39-09-02 | Information Disclosure | `storage_place_ids()` exposing the full storage-place set to any authenticated caller | accept | Gated behind the existing `CartridgeService` caller identity checks (whatever action already gates cartridge transition ops); storage-place ids are not sensitive data — they identify tree nodes, not PII |
</threat_model>

<verification>
`grep`-based acceptance criteria above. Full `cargo build -p trackly-app` deferred to the Wave 3
completion checkpoint (cross-plan compile dependency across 06/07/09/10, expected — see Plan 06's
verification note).
</verification>

<success_criteria>
All five cartridge transition operations reference `place_id`; `upsert_location_in_tx` is gone;
Install defaults from the printer's place; a storage-place-ids read path exists for the frontend's
D-11.3 suggestion UX.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-09-SUMMARY.md` when done
</output>
