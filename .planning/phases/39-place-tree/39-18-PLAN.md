---
phase: 39-place-tree
plan: 18
type: execute
wave: 7
depends_on: ["39-12", "39-13", "39-10"]
files_modified:
  - ui/src/features/reports/ReportsPage.svelte
  - ui/src/features/reports/ReportFilters.svelte
autonomous: true
requirements: [PLC-03, PLC-04]

must_haves:
  truths:
    - "Фильтр отчётов по месту использует PlacePicker вместо текстового автокомплита; выбор захватывает поддерево целиком (D-28), под полем — подсказка «Включая вложенные места» (UI-SPEC §12)"
    - "Все девять колонок отчётов с ключом location_name переименованы в place_path; значение отображается сокращённым путём (два последних сегмента) с полным путём в title (D-26)"
  artifacts:
    - path: "ui/src/features/reports/ReportsPage.svelte"
      provides: "place_path columns + PlacePicker-based place filter"
      contains: "place_path"
---

<objective>
Wire `PlacePicker` into the reports filter and rename every `location_name` report column to
`place_path`, including the D-26 short-path-with-tooltip display convention.

Purpose: closes the reports side of D-17/D-28 — parallel to Plans 15/16/17.
Output: `ReportsPage.svelte`, `ReportFilters.svelte` migrated onto `PlacePicker`/`place_path`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportsPage.svelte — 9 location_name columns → place_path, D-26 short-path display</name>
  <files>ui/src/features/reports/ReportsPage.svelte</files>
  <read_first>
    ui/src/features/reports/ReportsPage.svelte (grep-confirmed lines: 28/36/49 type-level
    `location_name?`/`location_id?` fields, 131/141/150/154/159/166/172/177/183 the nine column
    definitions `{ key: 'location_name', label: '...' }` with labels varying between "Локация" and
    "Расположение" across different report tables, 452/454 the `locations_autocomplete` fetch for
    building filter options, 517/523 `locationName`/`locations` props passed to `ReportFilters`),
    39-UI-SPEC.md §12 (D-26: table cells show the last two path segments, full path in `title`;
    unified term "Место" for every label).
  </read_first>
  <action>
    Rename every `location_name`/`location_id` type field and column `key` to `place_path`/
    `place_id`. Unify every column `label` from the current mix of "Локация"/"Расположение" to
    "Место" (single term, per UI-SPEC §12 — this is a deliberate simplification consumers of this
    file must accept: the phase's copywriting contract mandates exactly one term across the entire
    UI, superseding whatever per-report label variation existed before). For each table's cell
    renderer of this column, implement the D-26 short-path convention: split `place_path` on `" / "`,
    render the last two segments (or the whole string if it has ≤ 2 segments), and set the cell's
    `title` attribute to the full `place_path`. Delete the `locations_autocomplete` fetch (lines
    452/454) entirely — `ReportFilters`'s place filter now uses `PlacePicker` directly (Task 2), no
    prefetched name list needed.
  </action>
  <verify>
    <automated>grep -c "location_name\|location_id\|locations_autocomplete" ui/src/features/reports/ReportsPage.svelte && grep -c "place_path" ui/src/features/reports/ReportsPage.svelte && grep -c "Локация\|Расположение" ui/src/features/reports/ReportsPage.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "location_name\|location_id\|locations_autocomplete" ui/src/features/reports/ReportsPage.svelte`
      returns 0
    - `grep -c "place_path" ui/src/features/reports/ReportsPage.svelte` returns at least 9 (one per
      renamed column)
    - `grep -c "Локация\|Расположение" ui/src/features/reports/ReportsPage.svelte` returns 0 (unified
      to "Место")
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ReportFilters.svelte — PlacePicker-based place filter, D-28 subtree hint</name>
  <files>ui/src/features/reports/ReportFilters.svelte</files>
  <read_first>
    ui/src/features/reports/ReportFilters.svelte (grep-confirmed: line 8/22/28
    `location_name?`/`locationName?: string | null`/`locations?: string[]` props, line 45/51
    destructured `_locationName`/`_locations` — currently unused per the leading underscore,
    suggesting these props are accepted but not yet rendered; confirm by reading the full component
    body before assuming there is no existing render site to replace), 39-UI-SPEC.md §12 ("Включая
    вложенные места" hint text, `--tr-text-label`/`--tr-text-secondary`).
  </read_first>
  <action>
    Replace the `locationName?: string | null` / `locations?: string[]` props with `placeId?: number
    | null` and remove the `locations` list prop entirely (no longer needed — `PlacePicker` fetches
    its own data). Render `&lt;PlacePicker value={placeId} onChange={(id) => onFilterChange({ placeId:
    id })} /&gt;` (or whichever change-callback convention this component already uses for its other
    filter fields — match that exactly) with a hint line below it: "Включая вложенные места" in
    `--tr-text-label`/`--tr-text-secondary` (D-28 — confirms the filter captures the whole subtree,
    matching Plan 10's report-query semantics where a `place_id` filter should already resolve
    against descendants, not just an exact match — if Plan 10's SQL only did an exact `= place_id`
    comparison rather than a subtree `IN (...)` comparison, flag this in the SUMMARY as a gap for
    Plan 10 to close, since D-28 requires subtree-inclusive filtering and this task cannot silently
    change backend SQL).
  </action>
  <verify>
    <automated>grep -c "locationName\|locations?:" ui/src/features/reports/ReportFilters.svelte && grep -c "PlacePicker\|placeId" ui/src/features/reports/ReportFilters.svelte && grep -n "Включая вложенные места" ui/src/features/reports/ReportFilters.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "locationName\|locations?:" ui/src/features/reports/ReportFilters.svelte` returns 0
    - `grep -c "PlacePicker\|placeId" ui/src/features/reports/ReportFilters.svelte` returns at least
      2
    - `grep -n "Включая вложенные места" ui/src/features/reports/ReportFilters.svelte` confirms the
      D-28 hint text is present
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Report filter `place_id` param | Read-only filter parameter, same SQL-injection posture as the underlying Plan 10 queries (parameterized) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-18-01 | Information Disclosure | Report place filter exposing the full tree structure to any report viewer | accept | Reports are already gated by the existing report-access role check (unchanged by this plan); `PlacePicker` itself only exposes places, not report contents |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors. Manual verification that filtering by a
building-level place includes devices in its floors/rooms deferred to Plan 21's end-to-end
checkpoint (this depends on Plan 10's subtree-inclusive SQL, flagged above as a cross-plan
dependency to verify).
</verification>

<success_criteria>
Report tables display `place_path` with the D-26 short-path/tooltip convention; the place filter
uses `PlacePicker` and communicates D-28's subtree-inclusive semantics to the user.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-18-SUMMARY.md` when done
</output>
