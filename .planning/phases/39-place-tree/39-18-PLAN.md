---
phase: 39-place-tree
plan: 18
type: execute
wave: 7
depends_on: ["39-12", "39-13", "39-10"]
files_modified:
  - ui/src/features/reports/ReportsPage.svelte
  - ui/src/features/reports/ReportFilters.svelte
  - ui/src/features/reports/ReportTable.svelte
  - ui/src/features/requests/RequestDetail.svelte
  - ui/src/features/printers/PrinterDetail.svelte
  - ui/src/lib/components/PrinterSelect.svelte
  - ui/src/lib/components/GroupedPrinterSelect.svelte
autonomous: true
requirements: [PLC-03, PLC-04]

must_haves:
  truths:
    - "Фильтр отчётов по месту использует PlacePicker вместо текстового автокомплита; выбор захватывает поддерево целиком (D-28), под полем — подсказка «Включая вложенные места» (UI-SPEC §12)"
    - "Все девять колонок отчётов с ключом location_name переименованы в place_path; значение отображается сокращённым путём (два последних сегмента) с полным путём в title (D-26)"
    - "Отдельный, отдельно подписанный быстрый фильтр «Складское место» (Все / На складе / В эксплуатации) не смешан с фильтром статуса предмета — два независимых измерения (D-11.2, D-11.5)"
    - "ReportTable.svelte (отдельный от ReportsPage.svelte файл — универсальная таблица с разделителями групп) читает place_path вместо location_name в своём локальном интерфейсе ReportRow и в вычислении separatorKey — иначе snapshot-отчёты (тип, где разделитель строится по месту) молча схлопывают все группы в одну, так как поле необязательное и TypeScript не ловит рассинхронизацию"
    - "RequestDetail.svelte/PrinterDetail.svelte/PrinterSelect.svelte/GroupedPrinterSelect.svelte читают devicePlace/printerPlace/place (переименовано с deviceLocation/printerLocation/location в ui/src/bindings-phase6.ts Планом 10 Task 6) и DeviceDto.full_path (переименовано с location Планом 06) — все четыре файла являются потребителями этих переименований, ни один не был заявлен ни одним планом до этого раунда ревизии"
  artifacts:
    - path: "ui/src/features/reports/ReportsPage.svelte"
      provides: "place_path columns + PlacePicker-based place filter"
      contains: "place_path"
    - path: "ui/src/features/reports/ReportFilters.svelte"
      provides: "PlacePicker place filter + separate D-11.2 is_storage quick filter"
      contains: "is_storage"
---

<objective>
Wire `PlacePicker` into the reports filter and rename every `location_name` report column to
`place_path`, including the D-26 short-path-with-tooltip display convention.

Purpose: closes the reports side of D-17/D-28 — parallel to Plans 15/16/17.
Output: `ReportsPage.svelte`, `ReportFilters.svelte` migrated onto `PlacePicker`/`place_path`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReportsPage.svelte — 9 location_name columns → place_path, D-26 short-path display</name>
  <files>ui/src/features/reports/ReportsPage.svelte</files>
  <read_first>
    ui/src/features/reports/ReportsPage.svelte (grep-confirmed lines: 28/36/49 type-level
    `location_name?`/`location_id?` fields, 131/141/150/154/159/166/172/177/183 the nine column
    definitions `{ key: 'location_name', label: '...' }` with labels varying between "Локация" and
    "Расположение" across different report tables, 452/454 the `locations_autocomplete` fetch for
    building filter options, 517/523 `locationName`/`locations` props passed to `ReportFilters`),
    39-UI-SPEC.md §12 (D-26: table cells show the last two path segments, full path in `title`;
    unified term "Место" for every label).
  </read_first>
  <action>
    Rename every `location_name`/`location_id` type field and column `key` to `place_path`/
    `place_id`. Unify every column `label` from the current mix of "Локация"/"Расположение" to
    "Место" (single term, per UI-SPEC §12 — this is a deliberate simplification consumers of this
    file must accept: the phase's copywriting contract mandates exactly one term across the entire
    UI, superseding whatever per-report label variation existed before). For each table's cell
    renderer of this column, implement the D-26 short-path convention: split `place_path` on `" / "`,
    render the last two segments (or the whole string if it has ≤ 2 segments), and set the cell's
    `title` attribute to the full `place_path`. Delete the `locations_autocomplete` fetch (lines
    452/454) entirely — `ReportFilters`'s place filter now uses `PlacePicker` directly (Task 2), no
    prefetched name list needed. Add `is_storage?: boolean | null`
    to this file's local `ReportFilter` interface (~line 27, alongside `status_id`/`type_id`/etc. —
    D-11.2's quick filter, a geographic dimension distinct from `status_id`'s item-status meaning per
    D-11.5, do not conflate the two fields) and pass `isStorage={filter.is_storage ?? null}` as a new
    prop to `&lt;ReportFilters&gt;` alongside the existing `locationName`/`statusId`/etc. props
    (~line 517) — the existing `onFilterChange={(f) => { filter = { ...filter, ...f }; }}` merge
    pattern already forwards whatever keys Task 2's new control emits, no change needed there.
  </action>
  <verify>
    <automated>grep -c "location_name\|location_id\|locations_autocomplete" ui/src/features/reports/ReportsPage.svelte && grep -c "place_path" ui/src/features/reports/ReportsPage.svelte && grep -c "Локация\|Расположение" ui/src/features/reports/ReportsPage.svelte && grep -c "is_storage\|isStorage" ui/src/features/reports/ReportsPage.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "location_name\|location_id\|locations_autocomplete" ui/src/features/reports/ReportsPage.svelte`
      returns 0
    - `grep -c "place_path" ui/src/features/reports/ReportsPage.svelte` returns at least 9 (one per
      renamed column)
    - `grep -c "Локация\|Расположение" ui/src/features/reports/ReportsPage.svelte` returns 0 (unified
      to "Место")
    - `grep -c "is_storage\|isStorage" ui/src/features/reports/ReportsPage.svelte` returns at least 2
      (D-11.2 filter interface field + prop passthrough)
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ReportFilters.svelte — PlacePicker-based place filter, D-28 subtree hint</name>
  <files>ui/src/features/reports/ReportFilters.svelte</files>
  <read_first>
    ui/src/features/reports/ReportFilters.svelte (grep-confirmed: line 8/22/28
    `location_name?`/`locationName?: string | null`/`locations?: string[]` props, line 45/51
    destructured `_locationName`/`_locations` — currently unused per the leading underscore,
    suggesting these props are accepted but not yet rendered; confirm by reading the full component
    body before assuming there is no existing render site to replace), 39-UI-SPEC.md §12 ("Включая
    вложенные места" hint text, `--tr-text-label`/`--tr-text-secondary`). Confirmed by full read: this
    component's local `interface ReportFilter` (~line 7-14: `location_name`, `status_id`, `type_id`,
    `model_id`, `color`, `search` — snake_case, no `location_id`/place-shaped field at all today) is
    the type of `onFilterChange`'s `Partial&lt;ReportFilter&gt;` parameter — GAP-R4 (a prior phase)
    stripped every rendered filter control from this file, keeping only the Export/Print buttons, so
    there is no existing "change-callback convention" to match for a new control; this task
    reactivates filtering for the FIRST time since GAP-R4, so both the place filter AND the new
    is_storage filter this task adds must extend this local interface themselves, using its existing
    snake_case convention (`place_id?: number | null`, `is_storage?: boolean | null`) — NOT `placeId`
    (camelCase would not type-check against `Partial&lt;ReportFilter&gt;` and would not match any
    field this interface declares either way). D-11.5 (39-CONTEXT.md): the `is_storage` control must
    be visually and logically distinct from `status_id` — do not merge them into one dropdown or one
    label.
  </read_first>
  <action>
    Add `place_id?: number | null` and `is_storage?: boolean | null` to this file's local
    `interface ReportFilter` (~line 7-14, matching its existing snake_case field convention). Replace
    the `locationName?: string | null` / `locations?: string[]` props with `placeId?: number | null`
    and remove the `locations` list prop entirely (no longer needed — `PlacePicker` fetches its own
    data); add a new `isStorage?: boolean | null` prop alongside it. Render `&lt;PlacePicker
    value={placeId} onChange={(id) => onFilterChange({ place_id: id })} /&gt;` with a hint line below
    it: "Включая вложенные места" in `--tr-text-label`/`--tr-text-secondary` (D-28 — confirms the
    filter captures the whole subtree, matching Plan 10's report-query semantics where a `place_id`
    filter should already resolve against descendants, not just an exact match — if Plan 10's SQL only
    did an exact `= place_id` comparison rather than a subtree `IN (...)` comparison, flag this in the
    SUMMARY as a gap for Plan 10 to close, since D-28 requires subtree-inclusive filtering and this
    task cannot silently change backend SQL). Render a SEPARATE, distinctly-labeled quick filter for
    D-11.2/D-11.4 next to (not merged with) the place filter: a three-option control — "Все" (value
    `null`), "На складе" (value `true`), "В эксплуатации" (value `false`) — labeled "Складское место"
    (NOT "Статус" and NOT reusing any status-filter label, per D-11.5's explicit requirement that the
    two dimensions stay independently nameable), calling `onFilterChange({ is_storage: value })` on
    change. Use whichever existing control this codebase's Dropdown/Select component provides for a
    small fixed-option list (grep `Dropdown.svelte`'s usage elsewhere in `ui/src/features/reports/` or
    a sibling feature for the established 3-option pattern before inventing new markup).
  </action>
  <verify>
    <automated>grep -c "locationName\|locations?:" ui/src/features/reports/ReportFilters.svelte && grep -c "PlacePicker\|placeId" ui/src/features/reports/ReportFilters.svelte && grep -n "Включая вложенные места" ui/src/features/reports/ReportFilters.svelte && grep -c "is_storage\|isStorage" ui/src/features/reports/ReportFilters.svelte && grep -n "Складское место" ui/src/features/reports/ReportFilters.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "locationName\|locations?:" ui/src/features/reports/ReportFilters.svelte` returns 0
    - `grep -c "PlacePicker\|placeId" ui/src/features/reports/ReportFilters.svelte` returns at least
      2
    - `grep -n "Включая вложенные места" ui/src/features/reports/ReportFilters.svelte` confirms the
      D-28 hint text is present
    - `grep -c "is_storage\|isStorage" ui/src/features/reports/ReportFilters.svelte` returns at
      least 3 (interface field, prop, onFilterChange call)
    - `grep -n "Складское место" ui/src/features/reports/ReportFilters.svelte` confirms the D-11.2
      quick-filter label is present and distinct from any status-filter label
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RequestDetail.svelte + PrinterDetail.svelte + PrinterSelect.svelte + GroupedPrinterSelect.svelte — Plan 10 DTO rename consumers</name>
  <files>ui/src/features/requests/RequestDetail.svelte, ui/src/features/printers/PrinterDetail.svelte, ui/src/lib/components/PrinterSelect.svelte, ui/src/lib/components/GroupedPrinterSelect.svelte</files>
  <read_first>
    ui/src/features/requests/RequestDetail.svelte (~line 730 `prefillLocation={request.printerLocation
    ?? undefined}` — `RequestDto.printerLocation` renamed to `printerPlace` in `bindings-phase6.ts`
    by Plan 10 Task 6), ui/src/features/printers/PrinterDetail.svelte (~line 357 `deviceData?.location`
    — `deviceData` is a `DeviceDto`, whose `.location` field was renamed to `.full_path` by Plan 06
    Task 3), ui/src/lib/components/PrinterSelect.svelte (~line 46 `p.deviceLocation` —
    `PrinterDto.deviceLocation` renamed to `devicePlace` by Plan 10 Task 6), ui/src/lib/components/GroupedPrinterSelect.svelte
    (~line 50 `opt.location` — `RequestPrinterOptionDto.location` renamed to `place` by Plan 10 Task
    6). All four files are consumers of DTO renames declared elsewhere in this phase (Plan 06/Plan
    10) and were not declared in any plan's `files_modified` before this revision round.
  </read_first>
  <action>
    In `RequestDetail.svelte`: rename `request.printerLocation` to `request.printerPlace` (the
    `prefillLocation` PROP NAME passed to `OperationModal` stays as-is — that is `OperationModal`'s
    own prop name, defined and consumed entirely within Plan 16's `OperationModal.svelte`; only the
    VALUE expression reading off `request` changes here). In `PrinterDetail.svelte`: rename
    `deviceData?.location` to `deviceData?.full_path`. In `PrinterSelect.svelte`: rename
    `p.deviceLocation` (both occurrences in `printerLabel()`) to `p.devicePlace`. In
    `GroupedPrinterSelect.svelte`: rename `opt.location` to `opt.place` in the `groups` derivation's
    label lookup.
  </action>
  <verify>
    <automated>grep -c "printerLocation" ui/src/features/requests/RequestDetail.svelte && grep -c "deviceData?\.location" ui/src/features/printers/PrinterDetail.svelte && grep -c "deviceLocation" ui/src/lib/components/PrinterSelect.svelte && grep -c "opt\.location" ui/src/lib/components/GroupedPrinterSelect.svelte && grep -c "printerPlace\|full_path\|devicePlace\|opt\.place" ui/src/features/requests/RequestDetail.svelte ui/src/features/printers/PrinterDetail.svelte ui/src/lib/components/PrinterSelect.svelte ui/src/lib/components/GroupedPrinterSelect.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "printerLocation" ui/src/features/requests/RequestDetail.svelte` returns 0
    - `grep -c "deviceData?\.location" ui/src/features/printers/PrinterDetail.svelte` returns 0
    - `grep -c "deviceLocation" ui/src/lib/components/PrinterSelect.svelte` returns 0
    - `grep -c "opt\.location" ui/src/lib/components/GroupedPrinterSelect.svelte` returns 0
    - `pnpm --dir ui run svelte-check` reports 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: ReportTable.svelte — location_name → place_path in local ReportRow interface + separatorKey grouping</name>
  <files>ui/src/features/reports/ReportTable.svelte</files>
  <read_first>
    ui/src/features/reports/ReportTable.svelte (~line 3 comment "Snapshot: separators by
    location_name", ~line 21 local `ReportRow.location_name?: string | null` — declared OPTIONAL, so
    TypeScript does NOT flag the mismatch once the backend column is renamed by Plan 10 Task 4 —
    every row silently stops carrying the key; ~line 82 comment "insert separator when month_key
    (temporal) or location_name (snapshot) changes"; ~line 88
    `const separatorKey = isSnapshot ? (row.location_name ?? '') : (row.month_key ?? '')` — with
    `row.location_name` always `undefined` post-rename, `separatorKey` collapses to `''` for every
    snapshot row, destroying snapshot-report grouping silently at runtime, not compile time).
  </read_first>
  <action>
    Rename `ReportRow.location_name?: string | null` to `place_path?: string | null`. Update the two
    comments (~line 3, ~line 82) to say "place_path" instead of "location_name". Update the
    `separatorKey` computation (~line 88) to `isSnapshot ? (row.place_path ?? '') : (row.month_key ??
    '')`.
  </action>
  <verify>
    <automated>grep -c "location_name" ui/src/features/reports/ReportTable.svelte && grep -c "place_path" ui/src/features/reports/ReportTable.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "location_name" ui/src/features/reports/ReportTable.svelte` returns 0
    - `grep -c "place_path" ui/src/features/reports/ReportTable.svelte` returns at least 3 (interface
      field + both comments + separatorKey read — at least 3 of these 4 sites)
    - `pnpm --dir ui run svelte-check` reports 0 errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Report filter `place_id` param | Read-only filter parameter, same SQL-injection posture as the underlying Plan 10 queries (parameterized) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-18-01 | Information Disclosure | Report place filter exposing the full tree structure to any report viewer | accept | Reports are already gated by the existing report-access role check (unchanged by this plan); `PlacePicker` itself only exposes places, not report contents |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors. Manual verification that filtering by a
building-level place includes devices in its floors/rooms deferred to Plan 21's end-to-end
checkpoint (this depends on Plan 10's subtree-inclusive SQL, flagged above as a cross-plan
dependency to verify).
</verification>

<success_criteria>
Report tables display `place_path` with the D-26 short-path/tooltip convention; the place filter
uses `PlacePicker` and communicates D-28's subtree-inclusive semantics to the user;
`ReportTable.svelte`'s snapshot-report grouping works correctly off the renamed column; the printer/
request detail and select surfaces (`RequestDetail`, `PrinterDetail`, `PrinterSelect`,
`GroupedPrinterSelect`) read the renamed DTO fields from Plans 06/10.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-18-SUMMARY.md` when done
</output>
