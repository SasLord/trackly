---
phase: 39-place-tree
plan: 16
type: execute
wave: 7
depends_on: ["39-12", "39-13", "39-09"]
files_modified:
  - ui/src/features/cartridges/CartridgeFormBody.svelte
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: true
requirements: [PLC-03, PLC-04]

must_haves:
  truths:
    - "Форма картриджа и все пять операций перехода (Install/ReturnToStock/ToRefill/FromRefill/WriteOff) выбирают место через PlacePicker, привязанный к place_id (D-13)"
    - "При операции Install место предзаполняется местом принтера через PlacePicker (не текстом); пользователь может изменить выбор"
    - "При выборе складского места в форме операции появляется Checkbox «Перевести устройство в статус «На складе»», включённый по умолчанию, без принудительной смены при снятии галочки (D-11.3, D-10)"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "5 transition ops bound to PlacePicker/place_id with Install prefill and storage-status suggestion checkbox"
      contains: "PlacePicker"
---

<objective>
Wire `PlacePicker` into the cartridge-family surfaces: the cartridge create/edit form
(`CartridgeFormBody`, D-12's own `place_id`) and the five-operation transition modal
(`OperationModal`, D-13), including the D-11.3 storage-status suggestion checkbox.

Purpose: closes the cartridge side of D-17 — parallel to Plans 15/17/18.
Output: `CartridgeFormBody.svelte`, `OperationModal.svelte` migrated onto `PlacePicker`/`place_id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartridgeFormBody.svelte — own place_id (D-12)</name>
  <files>ui/src/features/cartridges/CartridgeFormBody.svelte</files>
  <read_first>
    ui/src/features/cartridges/CartridgeFormBody.svelte (grep-confirmed: line 13
    `import LocationAutocomplete`, line 64 `let location = $state(target?.location ?? '')`, lines
    145-161 the update-payload construction passing `location.trim() || null`, lines 315-320 the
    field markup `&lt;label ... for="cart-location"&gt;Расположение&lt;/label&gt; &lt;LocationAutocomplete
    value={location} ... id="cart-location" onChange={(v) => (location = v)} /&gt;`).
  </read_first>
  <action>
    Replace `let location = $state(target?.location ?? '')` with `let placeId = $state&lt;number |
    null&gt;(target?.placeId ?? null)`. Replace the field markup: label text "Место" (unified term,
    for="cart-place"), `&lt;PlacePicker value={placeId} id="cart-place" onChange={(id) => (placeId =
    id)} /&gt;` replacing the `LocationAutocomplete` block. Update the update-payload construction to
    send `place_id: placeId` instead of `location: location.trim() || null`.
  </action>
  <verify>
    <automated>grep -c "LocationAutocomplete\|location" ui/src/features/cartridges/CartridgeFormBody.svelte && grep -c "PlacePicker\|placeId" ui/src/features/cartridges/CartridgeFormBody.svelte && pnpm --dir ui build</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "LocationAutocomplete\|location" ui/src/features/cartridges/CartridgeFormBody.svelte`
      returns 0
    - `grep -c "PlacePicker\|placeId" ui/src/features/cartridges/CartridgeFormBody.svelte` returns
      at least 3
    - `pnpm --dir ui build` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: OperationModal.svelte — 5 transition ops, Install prefill, D-11.3 storage checkbox</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <read_first>
    ui/src/features/cartridges/OperationModal.svelte (grep-confirmed: line 13 `import
    LocationAutocomplete`, line 39 doc "Pre-fill «Расположение» from the request's printer location
    (D-05)", lines 81-92 `location`/`locationAutofilled`/`locationError` state, lines 109-111 comment
    on `previous_cartridge_location` reuse, lines 237-268 the printer-selection-driven autofill logic
    [`location = printer.deviceLocation; locationAutofilled = true` when switching printers or on
    initial prefill], lines 501-566 the five op-payload constructions each setting `location:
    location.trim()`, line 383 `&lt;LocationAutocomplete&gt;` field markup), 39-CONTEXT.md D-11.3
    (storage-status suggestion checkbox, default checked, no forced status change) and D-11
    (is_storage does not determine occupant status), the `storage_place_ids()` read added to
    `CartridgeService` by Plan 09 (exposed via a `places_*`-equivalent read the frontend can call —
    confirm exact endpoint name against Plan 09's SUMMARY once available, or call
    `apiCall('cartridge_storage_place_ids', {})` if Plan 09 exposed it as a cartridge-scoped
    command rather than a places_* one — check Plan 09's actual wiring before assuming the name).
  </read_first>
  <action>
    Replace `location`/`locationAutofilled`/`locationError` state with `placeId: $state&lt;number |
    null&gt;` / `placeAutofilled: $state&lt;boolean&gt;` / `placeError: $state&lt;string&gt;`. Replace the
    printer-driven autofill logic: when a printer is selected/switched (Install operation), set
    `placeId = printer.devicePlaceId` (the printer's resolved `place_id`, not a text string — Plan
    06's device/printer domain migration exposes this field; confirm the exact DTO field name via
    `crates/trackly-app/src/dto/printer.rs` before wiring) and `placeAutofilled = true`; manual
    `PlacePicker` selection clears `placeAutofilled`. Replace the field markup at line 383 with
    `&lt;PlacePicker value={placeId} onChange={(id) => { placeId = id; placeAutofilled = false; }}
    /&gt;`. Update all five op-payload constructions (~501-566) to send `place_id: placeId` instead of
    `location: location.trim()`; rename `previous_cartridge_location`/`previousCartridgeLocation`
    references to `previousCartridgePlaceId` (matching Plan 03's domain rename and Plan 09's SQL
    rename). Implement D-11.3: after `placeId` resolves (via prefill or manual selection), check
    whether it is a storage place — fetch the storage-place-ids set once per modal open (from Plan
    09's exposed read) and test membership including ancestor inheritance already resolved
    server-side (the set Plan 09 exposes already reflects D-11.4's ancestor logic, no client-side
    ancestor walk needed) — if `placeId` (or a value in its ancestor chain, but again this is already
    resolved server-side into the flat id set) is in that set, show `&lt;Checkbox checked={true}&gt;
    Перевести устройство в статус «На складе»&lt;/Checkbox&gt;` (default checked, per D-11.3);
    unchecking it means the submit payload does NOT include a status override (D-10: no forced
    change) — checking it (default) includes an explicit status-change field in the ReturnToStock
    payload.
  </action>
  <verify>
    <automated>grep -c "LocationAutocomplete\|location\b" ui/src/features/cartridges/OperationModal.svelte && grep -c "PlacePicker\|placeId" ui/src/features/cartridges/OperationModal.svelte && grep -n "Перевести устройство в статус" ui/src/features/cartridges/OperationModal.svelte && pnpm --dir ui build</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "LocationAutocomplete\|location\b" ui/src/features/cartridges/OperationModal.svelte`
      returns 0 (excluding any remaining literal Russian UI copy that happens to contain the
      substring — verify manually if the grep count is non-zero for that reason alone)
    - `grep -c "PlacePicker\|placeId" ui/src/features/cartridges/OperationModal.svelte` returns at
      least 5 (one per transition op)
    - `grep -n "Перевести устройство в статус" ui/src/features/cartridges/OperationModal.svelte`
      confirms the D-11.3 checkbox copy is present
    - `pnpm --dir ui build` succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cartridge operation `place_id` payload | Same trust model as every other place-bearing write — server validates via FK + `authorize()`, client only needs to send a value the user selected through `PlacePicker` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-16-01 | Tampering | Client omitting `place_id` on a transition op the server expects | accept | `place_id` is `Option&lt;i64&gt;` throughout (Plan 03/09) — `None` is a valid, meaningful state (D-07: place is optional), not an error to guard against client-side |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors. Manual verification of the full Install →
autofill → D-11.3 checkbox flow deferred to Plan 21's end-to-end checkpoint.
</verification>

<success_criteria>
All five cartridge transition operations and the cartridge form use `PlacePicker`/`place_id`; D-11.3's
storage-status suggestion checkbox appears exactly when the selected place is a storage place.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-16-SUMMARY.md` when done
</output>
