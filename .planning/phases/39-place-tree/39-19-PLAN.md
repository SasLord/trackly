---
phase: 39-place-tree
plan: 19
type: execute
wave: 7
depends_on: ["39-12", "39-13"]
files_modified:
  - ui/src/features/places/PlaceFormModal.svelte
  - ui/src/features/places/PlaceMoveModal.svelte
autonomous: true
requirements: [PLC-01, PLC-02]

must_haves:
  truths:
    - "Поле «Уровень» показывается только для типа «Этаж» и принимает 0 и отрицательные значения без клиентской валидации, запрещающей их (PLC-02)"
    - "Диалог перемещения показывает последствия («переедет N вложенных мест и M устройств») до подтверждения, с согласованием числительных и пропуском нулевых частей (D-21)"
    - "Ошибка цикла при перемещении показывается инлайн тем же текстом, что и на бэкенде — «Нельзя переместить место внутрь самого себя или своего вложенного места.» (D-21)"
    - "Тип узла (D-02) — Dropdown variant=select с ровно шестью значениями; любой выбор допустим независимо от родителя (D-01), родитель лишь подсказывает типичный дочерний тип"
    - "Форма создания места позволяет создать вложенный узел-склад («Кабинет 214 / Шкаф-склад») с собственным is_storage=true — паттерн D-09 доступен через обычное «Создать вложенное место», без специального UI"
  artifacts:
    - path: "ui/src/features/places/PlaceFormModal.svelte"
      provides: "create/rename modal per UI-SPEC §11.1-11.2"
      min_lines: 80
    - path: "ui/src/features/places/PlaceMoveModal.svelte"
      provides: "move modal with PlacePicker + consequences callout + cycle error per UI-SPEC §11.3"
      min_lines: 60
---

<objective>
Build the two remaining place-mutation modals — create/rename (`PlaceFormModal`) and move
(`PlaceMoveModal`) — per UI-SPEC §11.1-§11.3. Built as standalone components in this wave so
Plan 14 (Wave 8, the tree UI) can wire its `ActionMenu` to them without a same-wave file-existence
race.

Purpose: closes the mutation-UI gap between the completed backend API (Plan 12) and the tree UI
that will invoke these modals (Plan 14).
Output: `PlaceFormModal.svelte`, `PlaceMoveModal.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<interfaces>
<!-- Both modals are invoked by Plan 14's PlaceTreeNode ActionMenu and by PlacesPage's primary
"Создать место" button. Export contract every later consumer relies on: -->

```
PlaceFormModal.svelte props:
  mode: 'create' | 'rename'
  place: PlaceDto | null       // null for create-at-root, populated for rename or create-nested
  defaultParentId: number | null
  onClose: () => void
  onSaved: (place: PlaceDto) => void

PlaceMoveModal.svelte props:
  place: PlaceDto              // the node being moved
  onClose: () => void
  onMoved: (place: PlaceDto) => void
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlaceFormModal.svelte — create/rename</name>
  <files>ui/src/features/places/PlaceFormModal.svelte</files>
  <read_first>
    39-UI-SPEC.md §11.1 (field table: Название/Тип/Родительское место/Уровень/Складское место/
    Порядок) and §11.2 (uniqueness-error copy) and §14.3 (empty-name, non-integer-level error copy),
    any existing `Modal size="md"` form in this codebase (grep for `size="md"` usage in
    `ui/src/features/acts/` or `ui/src/features/devices/` for the create-modal structural analog —
    field layout, submit/cancel button placement, inline-error rendering pattern),
    ui/src/lib/components/Dropdown.svelte (the `variant="select" flat` mode used for the Тип field).
  </read_first>
  <behavior>
    - Test/manual: title reads "Новое место" in create mode, "Переименовать место" in rename mode.
    - Test/manual: the "Уровень" field is hidden unless "Тип" is set to "Этаж"; when visible, typing
      `-1` or `0` is accepted without a client-side range error.
    - Test/manual: selecting a "Родительское место" via the embedded `PlacePicker` pre-suggests a
      typical child type in the "Тип" dropdown when creating a nested place (D-01: suggestion only,
      any type remains selectable) — e.g. parent type "здание" suggests "этаж", but the user can
      still pick "помещение" or any other value.
    - Test/manual: submitting a duplicate sibling name surfaces the inline error under "Название"
      with UI-SPEC §11.2's exact copy, field goes `.invalid`.
  </behavior>
  <action>
    Create `ui/src/features/places/PlaceFormModal.svelte` per the props contract in this plan's
    `<interfaces>` block. Fields per UI-SPEC §11.1: `Input` "Название" (required, autofocus),
    `Dropdown variant="select" flat` "Тип" with the six D-02 labels (Территория/Зона/Здание/Этаж/
    Помещение/Уличный объект) mapped to the backend's English `kind` tokens
    (territory/zone/building/floor/room/outdoor), `PlacePicker` "Родительское место" (empty = root,
    D-03), `Input type="number"` "Уровень" (conditionally rendered only when kind === 'floor', hint
    text "Подвал — отрицательное значение; 0 допустим"), `Checkbox` "Складское место" (hint text per
    §11.1), `Input type="number"` "Порядок" (optional, hint "Пусто — автоматический порядок"). On
    submit, call `apiCall('places_create', ...)` or `apiCall('places_rename', ...)` depending on
    `mode`, mapping server validation errors (duplicate name → D-04 inline error per §11.2; empty
    name / non-integer level → §14.3 copy) to the corresponding field's inline error state. Call
    `onSaved(place)` on success and `pushToast({ kind: 'success', message: mode === 'create' ? 'Место
    создано' : 'Место переименовано' })` per §14.4.
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <done>Create and rename both work through one component; level field visibility/validation and
  D-04 error copy match UI-SPEC exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlaceMoveModal.svelte — consequences preview + cycle error</name>
  <files>ui/src/features/places/PlaceMoveModal.svelte</files>
  <read_first>
    39-UI-SPEC.md §11.3 (full copy contract: warning-callout consequences text with singular/plural
    agreement and zero-part omission rules, cycle-error inline copy, button labels), the
    `places_subtree_stats` response shape from Plan 12 (fields: `direct_children`, `nested_places`,
    `device_count`, `cartridge_count`) — this is the data source for the consequences count.
  </read_first>
  <behavior>
    - Test/manual: opening the modal with a node that has 3 nested places and 47 devices shows
      "Вместе с местом переедет 3 вложенных места и 47 устройств." — matching UI-SPEC §11.3's exact
      singular/plural agreement rules (test with 1/1, and with one part zero — e.g. 0 nested places
      and 47 devices → "Вместе с местом переедет 47 устройств." with the nested-places clause fully
      omitted, not rendered as "0 вложенных мест").
    - Test/manual: opening the modal with a completely empty node (zero everything) shows NO callout
      at all.
    - Test/manual: selecting a target that is the node's own descendant (via `PlacePicker` inside
      this modal) surfaces the cycle-error inline copy from §14.3, and the "Переместить" button does
      not submit.
  </behavior>
  <action>
    Create `ui/src/features/places/PlaceMoveModal.svelte` per the props contract. On open, fetch
    `apiCall('places_subtree_stats', { rootId: place.id })` and build the consequences sentence with
    correct Russian pluralization (implement a small local pluralization helper — Russian has three
    plural forms; UI-SPEC's examples only show the "1 X and 1 Y" / "N X and M Y" / omit-zero cases
    explicitly, so cover at minimum: zero (omit clause), one (singular form: "1 вложенное место",
    "1 устройство"), many (genitive plural form shown in the examples: "3 вложенных места", "47
    устройств") — do not attempt full Russian numeral-agreement grammar beyond what UI-SPEC's
    examples specify). Render the callout with the exact CSS from §11.3
    (`--tr-warning-soft`/`--tr-warning`/`--tr-warning-text`, `border-left: 3px solid
    var(--tr-warning)`). Embed a `PlacePicker` labeled "Новое родительское место". On submit, call
    `apiCall('places_move', { id: place.id, newParentId: selectedPlaceId, version: place.version })`;
    if the server returns the cycle-rejection error (matching the exact message text Plan 05 defined),
    render it inline in `--tr-danger-text` per §14.3 and do not close the modal. On success, call
    `onMoved(place)` and `pushToast({ kind: 'success', message: 'Место перемещено' })`.
  </action>
  <verify>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <done>Consequences callout matches UI-SPEC §11.3's singular/plural/zero-omission rules; cycle error
  surfaces inline without closing the modal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Modal submit payloads (`places_create`/`places_rename`/`places_move`) | Client-constructed but validated server-side by Plan 05/12's `authorize()` + FK constraints — these modals only need to render server errors correctly, not duplicate server-side validation logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-19-01 | Elevation of Privilege | These modals rendering for a Manager | accept (mitigated upstream) | Modals are only invoked from Admin-gated UI entry points (Plan 14's ActionMenu, hidden for Manager per D-20); even if reached, the server rejects the underlying `places_create`/`places_rename`/`places_move` call for a non-Admin caller (Plan 05/12) |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors. Manual verification of both modals happens
as part of Plan 14's end-to-end checkpoint (Wave 8), once the ActionMenu wiring exists to open them
from the tree.
</verification>

<success_criteria>
`PlaceFormModal` and `PlaceMoveModal` fully implement UI-SPEC §11.1-§11.3, ready for Plan 14 to
invoke from the tree's `ActionMenu`.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-19-SUMMARY.md` when done
</output>
