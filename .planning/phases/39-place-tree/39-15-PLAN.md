---
phase: 39-place-tree
plan: 15
type: execute
wave: 7
depends_on: ["39-12", "39-13", "39-06"]
files_modified:
  - ui/src/features/devices/DeviceAutocompleteField.svelte
  - ui/src/features/devices/DeviceFormBody.svelte
  - ui/src/features/devices/DeviceImportCsvModal.svelte
  - ui/src/features/printers/PrinterCreateModal.svelte
autonomous: true
requirements: [PLC-03, PLC-04]

must_haves:
  truths:
    - "Форма устройства и форма создания принтера выбирают место через PlacePicker, привязанный к place_id, а не к текстовому полю location (PLC-03)"
    - "CSV-импорт устройств разрешает колонку размещения по полному пути через дерево мест; нераспознанное значение попадает в отчёт об ошибках строкой «Строка N: место «...» не найдено в дереве.» (D-путь из UI-SPEC §12)"
    - "DeviceAutocompleteField больше не содержит специального случая field==='location' — этот путь автодополнения полностью удалён вместе с параллельным запросом locations_autocomplete"
  artifacts:
    - path: "ui/src/features/devices/DeviceFormBody.svelte"
      provides: "place_id field bound to PlacePicker"
      contains: "PlacePicker"
  key_links:
    - from: "ui/src/features/devices/DeviceFormBody.svelte"
      to: "ui/src/lib/components/PlacePicker.svelte"
      via: "import + bind place_id"
      pattern: "PlacePicker"
---

<objective>
Wire `PlacePicker` into the device-family surfaces: the device create/edit form, the
`DeviceAutocompleteField` special-case removal, the printer-creation modal, and CSV import's
place-column resolution.

Purpose: closes the device side of D-17 ("PlacePicker — единственный контрол") — one of five
parallel consumer-wiring plans (this one, 16, 17, 18) that all depend only on Plans 12+13.
Output: four files migrated off `LocationAutocomplete`/`location` onto `PlacePicker`/`place_id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeviceAutocompleteField.svelte + DeviceFormBody.svelte + PrinterCreateModal.svelte</name>
  <files>ui/src/features/devices/DeviceAutocompleteField.svelte, ui/src/features/devices/DeviceFormBody.svelte, ui/src/features/printers/PrinterCreateModal.svelte</files>
  <read_first>
    ui/src/features/devices/DeviceAutocompleteField.svelte (grep-confirmed lines: 32 `FieldName` type
    incl. `'location'`, 67/80/97/100/164-166/235-237/396 the `field === 'location'` special-case
    branches doing a parallel `apiCall&lt;string[]&gt;('locations_autocomplete', ...)` fetch — remove all
    of them), ui/src/features/devices/DeviceFormBody.svelte (locate wherever it currently renders a
    `LocationAutocomplete`/`DeviceAutocompleteField field="location"` for the device's place field),
    ui/src/features/printers/PrinterCreateModal.svelte (line 8 import, line 128
    `&lt;LocationAutocomplete&gt;` usage — printer creation constructs a `DeviceNew`-shaped payload per
    Plan 06's `tauri_cmds/printers.rs` change, now expecting `place_id`).
  </read_first>
  <action>
    In `DeviceAutocompleteField.svelte`: remove `'location'` from the `FieldName` union type; delete
    every `field === 'location'` branch (the `allLocationSuggestions` state, the parallel
    `locations_autocomplete` fetch at both call sites, the conditional render block at line ~396).
    This component now only handles `name | model | specs | kit | state`. In `DeviceFormBody.svelte`,
    replace whatever currently renders the device's place field (either a direct
    `LocationAutocomplete` or a `DeviceAutocompleteField field="location"` usage) with `&lt;PlacePicker
    value={placeId} onChange={(id) => (placeId = id)} /&gt;` bound to the device's `place_id` field,
    label "Место" (unified term per UI-SPEC §12 — remove any "Расположение"/"Размещение" label text
    in this file). In `PrinterCreateModal.svelte`, replace the `LocationAutocomplete` import and usage
    at line 128 with `PlacePicker` bound to `place_id`, matching the payload shape Plan 06's
    `tauri_cmds/printers.rs` now expects.
  </action>
  <verify>
    <automated>grep -c "location" ui/src/features/devices/DeviceAutocompleteField.svelte && grep -c "LocationAutocomplete" ui/src/features/devices/DeviceFormBody.svelte ui/src/features/printers/PrinterCreateModal.svelte && grep -c "PlacePicker" ui/src/features/devices/DeviceFormBody.svelte ui/src/features/printers/PrinterCreateModal.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "location" ui/src/features/devices/DeviceAutocompleteField.svelte` returns 0
    - `grep -c "LocationAutocomplete" ui/src/features/devices/DeviceFormBody.svelte ui/src/features/printers/PrinterCreateModal.svelte`
      returns 0
    - `grep -c "PlacePicker" ui/src/features/devices/DeviceFormBody.svelte ui/src/features/printers/PrinterCreateModal.svelte`
      returns at least 2
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: DeviceImportCsvModal.svelte — CSV place column resolved by full path</name>
  <files>ui/src/features/devices/DeviceImportCsvModal.svelte</files>
  <read_first>
    ui/src/features/devices/DeviceImportCsvModal.svelte (grep-confirmed lines: 45 `{ value:
    'location', label: 'Расположение' }` column-mapping option, 85 `return 'location';` — a
    heuristic column-name guesser), 39-UI-SPEC.md §12 (CSV-import error copy: "Строка 12: место
    «Здание А / 2 этаж / 214» не найдено в дереве.").
  </read_first>
  <action>
    Rename the column-mapping option's `value`/`label` from `'location'`/`'Расположение'` to
    `'place'`/`'Место'` (unified term). Update the column-name guesser (line 85) to recognize
    "место"/"place" header text in addition to whatever it already matches for the old label.
    For each imported row's place value (a full-path string like "Здание А / 2 этаж / 214"),
    resolve it against the tree via `apiCall('places_search', { query: fullPathValue })` and match
    the row whose `full_path` equals the CSV value EXACTLY (case-insensitive, matching
    `PlaceService::search`'s Rust-side `.to_lowercase()` comparison — do not silently accept a
    partial/fuzzy match for CSV import, exact full-path match only, since ambiguity here would
    silently misplace a device). If no exact match is found, add the row to the import's existing
    error-report mechanism with the message "Строка {n}: место «{value}» не найдено в дереве."
    (UI-SPEC §12's exact copy) instead of failing the whole import — mirror whatever partial-success/
    per-row-error reporting pattern this file already uses for other unresolved fields.
  </action>
  <verify>
    <automated>grep -c "'location'\|Расположение" ui/src/features/devices/DeviceImportCsvModal.svelte && grep -n "не найдено в дереве" ui/src/features/devices/DeviceImportCsvModal.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "'location'\|Расположение" ui/src/features/devices/DeviceImportCsvModal.svelte`
      returns 0
    - `grep -n "не найдено в дереве" ui/src/features/devices/DeviceImportCsvModal.svelte` confirms
      the exact UI-SPEC §12 error copy is present
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CSV-imported place text | User-supplied file content — resolved against the existing, already-validated tree via exact-match search; never auto-creates a place row (D-18's Admin-only creation boundary holds even for bulk import) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-15-01 | Tampering | CSV place column silently creating arbitrary place-tree nodes from untrusted file content | mitigate | Import resolution only matches against existing places via `places_search`; unmatched values are reported as row-level errors, never auto-created — consistent with D-18 across every entry point, not just the interactive form |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors for all four files. Manual CSV-import
smoke test deferred to Plan 21's end-to-end checkpoint.
</verification>

<success_criteria>
Every device-family surface (form, autocomplete, printer creation, CSV import) selects/resolves
place via the tree instead of freeform text.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-15-SUMMARY.md` when done
</output>
