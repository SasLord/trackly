---
phase: 39-place-tree
plan: 11
type: execute
wave: 5
depends_on: ["39-07"]
files_modified:
  - crates/trackly-app/src/dto/act.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/templates/act_handover.minijinja
  - crates/trackly-app/templates/act_handover.html
  - crates/trackly-app/src/services/template_service.rs
  - ui/src/features/settings/TemplateEditor.svelte
  - crates/trackly-app/tests/acts_place_snapshot.rs
autonomous: true
requirements: [PLC-04]

must_haves:
  truths:
    - "Массовый возврат и возврат по позициям принимают place_id/place_id_override от вызывающей стороны вместо bulk_location_name/location_name_override — оставшиеся 6 из 8 call sites resolve_location_id_in_tx в этом файле переведены (все 8 из 8 суммарно с Планом 07)"
    - "dto/act.rs — ActReturnDto/ActReturnItemDto/ActUpdateReturnDto — несёт bulk_place_id/place_id_override вместо bulk_location_id/bulk_location_name/location_id_override/location_name_override; это последние три структуры dto/act.rs, оставшиеся после Плана 07 (который владеет ActDto/ActItemDto/ActCreateDto/ActUpdateDto)"
    - "Шаблон акта печатает act.place_path (снимок на момент выписки), а не act.location_name; существующий незатронутый шаблон в БД (посевной) корректно рендерится после миграции благодаря авто-апгрейду неизменённых дефолтов (ловушка DB-backed templates upgrade trap)"
    - "Переименование места после выписки акта НЕ меняет уже напечатанный документ — печать использует place_path_snapshot, а не текущий place_full_paths (D-16)"
    - "Печатная форма акта показывает ПОЛНЫЙ путь места (act.place_path), а не сокращённый — документ обязан быть однозначным без доступа к базе (D-27)"
    - "Production-шаблон crates/trackly-app/templates/act_handover.html (не .minijinja — активный HTML-путь печати с Фазы 16) больше не документирует act.location_name в шапке-контракте; template_service.rs::demo_context_for_kind() поставляет place_path вместо location_name в демо-данных предпросмотра редактора шаблонов, иначе предпросмотр показывает несоответствующий контракту ключ"
    - "TemplateEditor.svelte больше не показывает пользователю токен act.location_name в списке доступных переменных — заменён на act.place_path, синхронно с переименованным контрактом шаблона"
    - "crates/trackly-app/templates/_legacy_defaults/v20…v25/act_handover.html НЕ изменяются этим или любым другим планом фазы — исторические снимки для определения «дефолт не тронут руками» при авто-апгрейде; переименование токена в них ломает это сравнение (решение пользователя 2026-08-23)"
  artifacts:
    - path: "crates/trackly-app/src/dto/act.rs"
      provides: "ActReturnDto/ActReturnItemDto/ActUpdateReturnDto carry bulk_place_id/place_id_override"
      contains: "bulk_place_id"
    - path: "crates/trackly-app/templates/act_handover.minijinja"
      provides: "act.place_path contract replacing act.location_name"
      contains: "place_path"
    - path: "crates/trackly-app/tests/acts_place_snapshot.rs"
      provides: "D-16 regression: rename after handover does not alter the printed snapshot; unmodified seeded template re-renders correctly post-migration"
      min_lines: 40
  key_links:
    - from: "crates/trackly-app/templates/act_handover.minijinja"
      to: "crates/trackly-app/src/services/act_service.rs"
      via: "act.place_path renders from acts.place_path_snapshot, written at handover time"
      pattern: "place_path"
    - from: "crates/trackly-app/src/dto/act.rs"
      to: "crates/trackly-app/src/services/act_service.rs"
      via: "ActReturnDto.bulk_place_id / ActReturnItemDto.place_id_override consumed directly by the bulk-return write path, no name-based resolution"
      pattern: "bulk_place_id"
---

<objective>
Finish `dto/act.rs` and `act_service.rs`'s location→place_id migration by converting the bulk-return
DTO structs (`ActReturnDto`/`ActReturnItemDto`/`ActUpdateReturnDto` — the three structs Plan 07 does
NOT touch) and the corresponding bulk-return/per-item-override call sites (6 of the 8
`resolve_location_id_in_tx` occurrences remaining after Plan 07), and rename the act print template's
contract from `act.location_name`/`return.location_default` to `act.place_path` across BOTH the
frozen `act_handover.minijinja` and the ACTIVE `act_handover.html` (Phase 16 HTML pivot), plus the
template editor's preview demo data (`template_service.rs`) and the Admin-facing token list
(`TemplateEditor.svelte`) — guarding against the project's documented "DB-backed templates upgrade
trap" (bundled template changes do not reach existing DBs unless the seed's
auto-upgrade-untouched-defaults path also recognizes the new contract), while explicitly excluding
`templates/_legacy_defaults/v20…v25/act_handover.html` from any rename (user decision 2026-08-23 —
these are historical snapshots the auto-upgrade comparison depends on staying byte-for-byte
unchanged).

Purpose: completes D-16 (act stores + prints a frozen path snapshot) and closes the last
`dto/act.rs`/`act_service.rs` gap before Plan 12 can build transports against a fully place-based act
API.
Output: `dto/act.rs` fully migrated (all six act-family structs, split across Plans 07/11);
`act_service.rs` fully migrated; `act_handover.minijinja` contract renamed; `acts_place_snapshot.rs`
proving both the D-16 freeze behavior and the template-upgrade safety net.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-RESEARCH.md
@.planning/phases/39-place-tree/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dto/act.rs — ActReturnDto/ActReturnItemDto/ActUpdateReturnDto onto bulk_place_id/place_id_override</name>
  <files>crates/trackly-app/src/dto/act.rs</files>
  <read_first>
    crates/trackly-app/src/dto/act.rs (grep-confirmed: `ActReturnDto.bulk_location_id:
    Option&lt;i64&gt;`/`.bulk_location_name: Option&lt;String&gt;` at ~lines 148-152;
    `ActReturnItemDto.location_id_override: Option&lt;i64&gt;`/`.location_name_override:
    Option&lt;String&gt;` at ~lines 201-204; `ActUpdateReturnDto.location_id`/`.location_name`/
    `.bulk_location_id`/`.bulk_location_name` at ~lines 342-354 — this struct mirrors
    `ActReturnDto`'s bulk fields PLUS its own single `location_id`/`location_name` pair for the
    non-bulk header fields it also carries; test fixtures at ~lines 588-589/597-598/615-621/636-637
    using `bulk_location_id`/`bulk_location_name`/`location_id_override`/`location_name_override`
    literals and their snake_case JSON-key assertions). Plan 07 already migrated `ActDto`/
    `ActItemDto`/`ActCreateDto`/`ActUpdateDto` in this same file — do not re-touch those structs,
    only the three named above.
  </read_first>
  <action>
    Rename `ActReturnDto.bulk_location_id: Option&lt;i64&gt;` to `bulk_place_id: Option&lt;i64&gt;`
    and DELETE `ActReturnDto.bulk_location_name: Option&lt;String&gt;` entirely (D-18 — no name-based
    resolution anywhere in this file after this task). Rename
    `ActReturnItemDto.location_id_override: Option&lt;i64&gt;` to `place_id_override:
    Option&lt;i64&gt;` and DELETE `.location_name_override: Option&lt;String&gt;` entirely. In
    `ActUpdateReturnDto`: rename its own `location_id`/`bulk_location_id` fields to
    `place_id`/`bulk_place_id`, DELETE `location_name`/`bulk_location_name` entirely. Update every
    doc comment referencing "UX-friendly: имя расположения" / "приоритет над ...Id" to remove the
    now-nonexistent priority-resolution language (there is no more name-vs-id priority — only
    `place_id` exists). Update every test fixture (~588-589/597-598/615-621/636-637) to the renamed
    field names, dropping the deleted name-field literals and their corresponding
    `s.contains("bulk_location_name")`/`"bulk_location_name": null` JSON-key assertions (replace with
    `bulk_place_id`-equivalent assertions using the same idiom).
  </action>
  <verify>
    <automated>grep -c "bulk_location\|location_id_override\|location_name_override\|ActUpdateReturnDto.*location_id\|location_name" crates/trackly-app/src/dto/act.rs && grep -c "bulk_place_id\|place_id_override" crates/trackly-app/src/dto/act.rs</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "bulk_location\|location_id_override\|location_name_override" crates/trackly-app/src/dto/act.rs`
      returns 0
    - `grep -c "bulk_place_id\|place_id_override" crates/trackly-app/src/dto/act.rs` returns at
      least 6 (one per renamed field across the three structs, plus test fixtures)
    - `ActUpdateReturnDto` no longer has a `location_id`/`location_name` pair (renamed to
      `place_id`, with no name counterpart)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: act_service.rs — bulk return + per-item override onto place_id</name>
  <files>crates/trackly-app/src/services/act_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs (grep-confirmed remaining call sites post-Plan-07:
    lines ~1079-1284 bulk return — `resolved_bulk_location_id` from `payload.bulk_location_name` or
    `payload.bulk_location_id`, per-item `location_id_override`/`location_name_override` resolution
    at lines ~1274-1284; lines ~1616-1657 a second bulk-return code path with the identical shape;
    lines ~1773-2073 the "current state comparison" logic comparing `(status_id, location_id, state)`
    against submitted expectations, plus `location_id`-keyed audit-log JSON at lines ~2036-2073; lines
    ~2661 `"location_name": act.location` in what appears to be a different context — read this
    specific line directly before touching it, it may be an unrelated field on a different struct).
    Note: this task depends on Task 1's `dto/act.rs` rename in this same plan having landed first —
    `payload.bulk_place_id`/`item.place_id_override` are the Task-1-renamed field names this task's
    code reads.
  </read_first>
  <action>
    For both bulk-return code paths (~1079-1284 and ~1616-1657): remove the
    `resolve_location_id_in_tx` calls; `resolved_bulk_place_id` now comes directly from
    `payload.bulk_place_id` (caller-supplied via `PlacePicker`, D-18). For per-item overrides: replace
    `location_id_override`/`location_name_override` resolution with `item.place_id_override` passed
    straight through (no name-based resolution remains anywhere in this file after this task). Update
    the "current state comparison" logic (~1773-2073) to compare `place_id` instead of `location_id`
    in its optimistic-concurrency check (the field being compared changes name; the comparison
    semantics — reject if the device's live state diverged from what the caller expected — are
    unchanged). Update every remaining `"location_id":`/`"bulk_location_id":` audit-log JSON key to
    `"place_id":`/`"bulk_place_id":`. Investigate line ~2661's `"location_name": act.location`
    directly (read it first per `<read_first>`) — if it is part of the same act-return JSON
    construction, rename to `"place_path": act.place_path_snapshot`; if it belongs to an unrelated
    struct (e.g. a device-level field), leave it for the plan that owns that struct and note the
    exclusion in the SUMMARY.
  </action>
  <verify>
    <automated>grep -c "resolve_location_id_in_tx\|location_id\|location_name\|bulk_location" crates/trackly-app/src/services/act_service.rs && grep -c "place_id_override\|bulk_place_id\|place_path_snapshot" crates/trackly-app/src/services/act_service.rs</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "resolve_location_id_in_tx\|location_id\|location_name\|bulk_location" crates/trackly-app/src/services/act_service.rs`
      returns 0 (all 8 original call sites now converted, combining this task with Plan 07's 2)
    - `grep -c "place_id_override\|bulk_place_id\|place_path_snapshot" crates/trackly-app/src/services/act_service.rs`
      returns at least 6
    - `cargo build -p trackly-app` succeeds for every file this plan and Plan 07 together own
      (`dto/act.rs`, `act_service.rs`, `acts_sqlite.rs`) — the wave-boundary compile gate for the
      full act migration; this is meaningful now because both this plan's `depends_on: ["39-07"]`
      and Plan 07's `depends_on: ["39-06"]` mean every upstream rename (devices, act domain/DTO) has
      already landed by the time this task runs
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: act_handover.minijinja + act_handover.html + template_service.rs demo data — rename contract to place_path (Section D legacy defaults excluded)</name>
  <files>crates/trackly-app/templates/act_handover.minijinja, crates/trackly-app/templates/act_handover.html, crates/trackly-app/src/services/template_service.rs</files>
  <read_first>
    crates/trackly-app/templates/act_handover.minijinja (grep-confirmed contract header comment at
    lines 10/14: `act.location_name, act.items[], act.parent` and `return.condition_default,
    return.location_default` — this is the frozen krilla-era path per project memory
    `pdf_pivot_to_html_print`, kept but not actively used for rendering), crates/trackly-app/templates/act_handover.html
    (the ACTIVE production print template since the Phase 16 HTML pivot — line 49's doc-comment
    contract header lists `act.location_name, act.items[], act.items_grouped[], act.parent`; the
    template BODY does not currently interpolate `act.location_name` anywhere visible, only the
    doc-comment lists it as an available context key — still must be corrected, a stale contract
    comment misleads the next editor of this file), crates/trackly-app/src/services/template_service.rs
    `demo_context_for_kind()` (~line 513 — `"location_name": "Офис 101"` inside the `act_handover`
    demo-preview JSON context fed to `validate_preview("act_handover", body)`; this is sample data
    for the in-app template editor's live preview, not real organization data, per the project's
    privacy rule), 39-RESEARCH.md Common Pitfall 5 (the DB-backed-templates-upgrade-trap analysis and
    the recommendation this task follows: rename the contract outright since data migration is not
    required in this pre-production repo, and rely on the existing seed
    auto-upgrade-untouched-defaults path — do not add a deprecated-alias shim, that was the
    alternative RESEARCH considered and rejected for this specific case).
  </read_first>
  <action>
    In `act_handover.minijinja`: rename every `act.location_name` reference in the template body to
    `act.place_path`, and `return.location_default` to `return.place_default`. Update the contract
    header comment (lines 10/14) to list the new variable names. In `act_handover.html` (the ACTIVE
    production template — this file drives real printed acts, unlike the frozen `.minijinja` sibling):
    rename the line-49 doc-comment's `act.location_name` to `act.place_path` (the template body itself
    has no other `location_name` reference to change — confirm via grep before editing that this is
    the only occurrence in the file). In `template_service.rs::demo_context_for_kind()`, rename the
    `"location_name": "Офис 101"` key to `"place_path": "Офис 101"` in the `act_handover`/fallback
    branch's demo JSON, keeping the placeholder value unchanged (invented data, not real). Do not add
    a `default("—", true)`-filtered backward-compatible alias for the old name in either template —
    per RESEARCH Common Pitfall 5's explicit recommendation for this pre-production repo, the clean
    rename is correct; Task 4's regression test is what proves the seed's existing
    auto-upgrade-untouched-defaults mechanism (added by the prior `260704-uw3` fix, referenced in
    project memory) correctly picks up this renamed default on an existing DB.

    SCOPE FENCE (user decision 2026-08-23, do not violate): `crates/trackly-app/templates/_legacy_defaults/v20…v25/act_handover.html`
    are historical snapshots used by the seed's auto-upgrade-untouched-defaults comparison to detect
    "this default was never hand-edited." They are explicitly OUT OF SCOPE for this task and this
    entire phase — do NOT rename `act.location_name` inside them. Renaming the token there would break
    the untouched-default comparison for existing installs. Leave all six files under
    `_legacy_defaults/` byte-for-byte unchanged.
  </action>
  <verify>
    <automated>grep -c "location_name\|location_default" crates/trackly-app/templates/act_handover.minijinja crates/trackly-app/templates/act_handover.html crates/trackly-app/src/services/template_service.rs && grep -c "place_path\|place_default" crates/trackly-app/templates/act_handover.minijinja crates/trackly-app/templates/act_handover.html crates/trackly-app/src/services/template_service.rs && grep -rc "location_name" crates/trackly-app/templates/_legacy_defaults/ | awk -F: '{s+=$2} END{print s}'</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "location_name\|location_default" crates/trackly-app/templates/act_handover.minijinja crates/trackly-app/templates/act_handover.html crates/trackly-app/src/services/template_service.rs`
      returns 0 across all three files
    - `grep -c "place_path\|place_default" crates/trackly-app/templates/act_handover.minijinja crates/trackly-app/templates/act_handover.html crates/trackly-app/src/services/template_service.rs`
      returns at least 4 total
    - `grep -rc "location_name" crates/trackly-app/templates/_legacy_defaults/ | awk -F: '{s+=$2} END{print s}'`
      returns a NON-ZERO count equal to the pre-existing count (legacy defaults untouched — this
      acceptance criterion is inverted from every other criterion in this phase on purpose: a zero
      here would mean the scope fence was violated)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: TemplateEditor.svelte — user-facing token list rename to act.place_path</name>
  <files>ui/src/features/settings/TemplateEditor.svelte</files>
  <read_first>
    ui/src/features/settings/TemplateEditor.svelte (~line 99 — `{ code: 'act.location_name', desc:
    'расположение' }` inside the `act_handover` kind's available-variables list shown to the Admin
    editing the print template; this list is the user-facing reference for exactly the same contract
    Task 3 renamed on the Rust side — `act.place_path`).
  </read_first>
  <action>
    Rename the token entry `{ code: 'act.location_name', desc: 'расположение' }` to `{ code:
    'act.place_path', desc: 'расположение' }` (description text unchanged — only the `code` token
    changes, matching Task 3's renamed template contract). Do not touch any other entry in this
    variables list.
  </action>
  <verify>
    <automated>grep -c "act.location_name" ui/src/features/settings/TemplateEditor.svelte && grep -c "act.place_path" ui/src/features/settings/TemplateEditor.svelte</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "act.location_name" ui/src/features/settings/TemplateEditor.svelte` returns 0
    - `grep -c "act.place_path" ui/src/features/settings/TemplateEditor.svelte` returns at least 1
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: acts_place_snapshot.rs — D-16 freeze test + template-upgrade regression</name>
  <files>crates/trackly-app/tests/acts_place_snapshot.rs</files>
  <read_first>
    crates/trackly-app/tests/acts_update_return.rs or acts_update.rs (existing acts test file — read
    one in full to establish the fixture/setup idiom this new file should follow: DB setup, act
    creation via `ActService`, template rendering invocation if one already exists in these files).
  </read_first>
  <behavior>
    - Test: create an act with `place_id` pointing at "Здание А / 2 этаж / 214"; confirm
      `act.place_path_snapshot` equals "Здание А / 2 этаж / 214" immediately after creation.
    - Test (D-16's literal claim): rename the "214" node to "215" via `PlaceService::rename` AFTER
      the act was created; re-fetch the act — `place_path_snapshot` still reads "Здание А / 2 этаж /
      214" (unchanged), while a live lookup via `place_full_paths` for that `place_id` now returns
      "Здание А / 2 этаж / 215" — the two must diverge, proving the snapshot is frozen and navigation
      is live, simultaneously.
    - Test (Common Pitfall 5 regression): render `act_handover.minijinja` against an act fixture
      using ONLY the seeded default template (not a customized one) on a DB that has gone through the
      full V001-V038 migration chain; confirm the rendered output contains the place path text (no
      empty/missing-variable silently swallowed by minijinja's `default()` filter) — this is the
      exact regression class the `260704-uw3` prior fix targeted, now applied to this contract
      rename.
  </behavior>
  <action>
    Create `crates/trackly-app/tests/acts_place_snapshot.rs` covering all three Behavior-block cases,
    using only invented place/act data (no real organization data, per the project's hard privacy
    rule).
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test acts_place_snapshot -- --skip login_remember_persistent_cookie</automated>
  </verify>
  <done>All three cases pass; D-16's "print shows frozen snapshot, navigation uses live place_id" is
  proven end-to-end, and the seeded template renders correctly on a migrated DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Act return payload `bulk_place_id`/`place_id_override` | Same FK-validated, no-implicit-create trust model as Plan 07's create/update paths |
| Rendered act template output | `place_path` is server-computed at write time (Plan 07), never client-supplied text — the template only formats already-trusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-11-01 | Tampering | Bulk-return `place_id`/override forged to reference a place outside the caller's intent | mitigate | Same FK + service-layer validation as every other write path in this phase; no new surface introduced |
| T-39-11-02 | Repudiation | A printed act silently changing after the fact when its place is renamed | mitigate | `place_path_snapshot` is immutable once written (only re-captured on an explicit subsequent `place_id` change to that SAME act, never by an unrelated place rename) — proven by Task 4's D-16 divergence test |
</threat_model>

<verification>
`cargo build -p trackly-app` for the combined act migration (Plans 07+11 together own every act-family
file). `cargo test -p trackly-app --test acts_place_snapshot -- --skip login_remember_persistent_cookie`
green. Combined with Plan 07, `grep -c "resolve_location_id_in_tx" act_service.rs` returns 0, and
`grep -c "location_id\|location_name\|bulk_location" dto/act.rs` returns 0 across all six act-family
structs.
</verification>

<success_criteria>
`dto/act.rs` and `act_service.rs` fully migrated off `locations`; the print template contract is
`act.place_path` in both the active HTML template and the frozen minijinja sibling, in the template
editor's preview demo data, and in the Admin-facing token list; the six `_legacy_defaults` snapshot
files remain untouched; D-16's freeze behavior and the seeded-template-upgrade safety are both proven
by automated tests.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-11-SUMMARY.md` when done
</output>
