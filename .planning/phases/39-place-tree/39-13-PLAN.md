---
phase: 39-place-tree
plan: 13
type: execute
wave: 6
depends_on: ["39-12"]
files_modified:
  - ui/src/lib/components/PlacePicker.svelte
  - ui/src/features/showcase/sections/PlacePickerSection.svelte
  - ui/src/pages/ComponentShowcasePage.svelte
autonomous: false
requirements: [PLC-03]

must_haves:
  truths:
    - "PlacePicker — единственный контрол выбора места во всём приложении; открывается сразу по фокусу и переключается в плоский список совпадений по полному пути при наборе текста (D-17)"
    - "Создание нового места из панели PlacePicker видно только Admin — Manager видит вместо строки создания пустое состояние (D-18)"
    - "Архивные узлы не показываются в панели, кроме случая, когда архивный узел является текущим выбранным значением (D-15)"
    - "Выбрать можно узел любого уровня, не только лист — клик по имени выбирает, клик по chevron только раскрывает (D-06)"
  artifacts:
    - path: "ui/src/lib/components/PlacePicker.svelte"
      provides: "field + two-mode (tree/search) dropdown panel per UI-SPEC §10"
      min_lines: 150
  key_links:
    - from: "ui/src/lib/components/PlacePicker.svelte"
      to: "crates/trackly-app/src/tauri_cmds/places.rs"
      via: "apiCall('places_list_children'/'places_search', ...)"
      pattern: "places_list_children|places_search"
---

<objective>
Build `PlacePicker.svelte` — the single reusable place-selection control specified in
39-UI-SPEC.md §10, replacing `LocationAutocomplete.svelte` everywhere. This plan builds the
component and its showcase entry only; wiring it into existing forms is Plans 15-19 (parallel,
each touching different consumer files); deleting `LocationAutocomplete.svelte` is Plan 21 (after
every consumer has migrated).

Purpose: interface-first delivery of the one new UI primitive every other Phase 39 frontend plan
depends on.
Output: `PlacePicker.svelte`, `PlacePickerSection.svelte` showcase entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlacePicker.svelte — field + tree-mode panel</name>
  <files>ui/src/lib/components/PlacePicker.svelte</files>
  <read_first>
    ui/src/lib/components/LocationAutocomplete.svelte (full file, 249 lines — the mechanics to
    inherit verbatim: `onDestroy` timer cleanup [WR-05], `use:portal` + `use:dropdownAnchor`,
    open-on-focus behavior, 200ms debounce, the `.dropdown--location` global-class-namespacing
    discipline [WR-03] — this file is DELETED in Plan 21, do not edit it, only read its patterns),
    ui/src/lib/components/Dropdown.svelte (targeted grep for `variant`/`flat` props only — UI-SPEC
    §6.2 explicitly rules out building PlacePicker as a Dropdown wrapper; inherit visual/keyboard
    language only, not structure), 39-UI-SPEC.md §10.1-§10.2, §10.5 (field visual contract, tree-mode
    panel contract, keyboard/ARIA contract — treat as locked, do not redesign).
  </read_first>
  <behavior>
    - Test/manual: field shows placeholder "Выберите место" when empty, full path when a value is
      selected; clear button "×" (28px ghost, `aria-label="Очистить место"`) appears only when
      non-empty.
    - Test/manual: focusing the field opens the panel immediately in tree mode, showing root nodes
      (or the branch expanded to the current value, if one is set).
    - Test/manual: `role="combobox"` + `aria-expanded` + `aria-controls` on the field; panel has
      `role="tree"`; `aria-activedescendant` tracks the active row.
  </behavior>
  <action>
    Create `ui/src/lib/components/PlacePicker.svelte` with a `Props` interface: `value:
    number | null` (selected `place_id`), `onChange: (placeId: number | null) => void`, `id?:
    string`, `disabled?: boolean`, `invalid?: boolean`. Field markup/CSS mirrors
    `LocationAutocomplete`'s field exactly per UI-SPEC §10.1 (36px height, `--tr-surface-raised`
    background, `--tr-border-strong` border, focus/invalid/disabled states identical). On focus,
    fetch root-level children via `apiCall('places_list_children', { parentId: null })` (or the
    exact camelCase param name Plan 12's payload struct defines — confirm against
    `crates/trackly-app/src/http/places.rs`'s `#[serde(rename_all = "camelCase")]` payload) and open
    the panel in tree mode (`use:portal` + `use:dropdownAnchor`, namespaced class `.dropdown--place`
    per UI-SPEC §10.2/WR-03 — do NOT reuse the `.dropdown--location` global class name). Rows are 32px,
    same indent formula as the tree (`padding-left: calc(var(--tr-space-xs) + var(--depth) *
    var(--tr-space-md))`), same chevron convention (empty 16px slot for leaves). Clicking a row's name
    selects it (calls `onChange(placeId)`, closes panel); clicking the chevron expands/collapses
    without selecting — fetch that node's children lazily via `places_list_children`. Archived nodes
    are excluded from `places_list_all`/`places_list_children` results by default (server-side, per
    Plan 08); if the CURRENT `value` resolves to an archived place, fetch and display it anyway with
    the "Архив" badge (`Badge variant="default" appearance="soft" size="sm"`) and
    `--tr-text-tertiary` color, per UI-SPEC §10.2's explicit archived-value exception. Implement the
    keyboard map from UI-SPEC §10.5 (↑/↓ visible rows, →/← expand/collapse in tree mode, Enter select,
    Tab select-and-close, Escape per §10.3's two-stage rule — implement the tree-mode Escape now,
    search-mode Escape in Task 2).
  </action>
  <verify>
    <automated>pnpm --dir ui exec svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -c "PlacePicker.svelte.*error" | grep -qx 0</automated>
  </verify>
  <done>Field and tree-mode panel match UI-SPEC §10.1/§10.2/§10.5 exactly; archived-value exception
  works; any-level selection works (D-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlacePicker.svelte — search mode + D-18 create-row + showcase</name>
  <files>ui/src/lib/components/PlacePicker.svelte, ui/src/features/showcase/sections/PlacePickerSection.svelte, ui/src/pages/ComponentShowcasePage.svelte</files>
  <read_first>
    39-UI-SPEC.md §10.3 (search-mode rendering: debounce 200ms, flat list max 50 rows, matched
    fragment styling — ancestors `--tr-text-secondary`, last segment `--tr-text-body-strong`, matched
    substring `--tr-accent-text` no background fill) and §10.4 (D-18 create-row: "[+] Создать «214» в
    «Здание А / 2 этаж»", visible only to Admin, text color `--tr-accent-text`), §14.2 (Manager's
    no-create empty-state copy: "Ничего не найдено. Уточните запрос или обратитесь к
    администратору..."), ui/src/features/showcase/sections/DropdownSection.svelte or
    TableSection.svelte (existing showcase-section structural analog — any one is fine to establish
    the file's boilerplate shape), ui/src/pages/ComponentShowcasePage.svelte (registration list of
    existing sections, to append the new one to).
  </read_first>
  <behavior>
    - Test/manual: typing in the field debounces 200ms then calls `apiCall('places_search', { query
      })`, switches the panel to a flat list of up to 50 matches, each row showing the full path with
      the matched substring highlighted.
    - Test/manual: when zero matches AND the current user role is Admin, the panel's last row reads
      "Создать «{query}» в «{selected-or-root-path}»" and is clickable — invoking it calls
      `apiCall('places_create', ...)` with `parentId` set to whatever node was highlighted in the
      panel before the query was typed (or `null` for root) and `name` set to the typed query, then
      selects the newly created place.
    - Test/manual: when zero matches AND the current user role is Manager, no create-row appears —
      the empty-state copy from UI-SPEC §14.2 renders instead.
    - Test/manual: first Escape press in search mode clears the query and returns to tree mode;
      second Escape press closes the panel entirely (two-stage Escape, §10.3).
  </behavior>
  <action>
    Implement search mode: on non-empty input, debounce 200ms (clear any pending timer in
    `onDestroy`, per WR-05 — this is the exact bug class that regression was filed for), call
    `apiCall('places_search', { query: value })`, render results as a flat list (no indent/chevron),
    each row's text built from the `full_path` string split on `" / "` — all segments except the last
    in `--tr-text-secondary`, last segment in `--tr-text-body-strong`, with the substring matching the
    (lowercased) query wrapped in a `--tr-accent-text` span with no background. Determine the current
    user's role from whatever existing auth store this project uses (grep
    `ui/src/lib/stores/auth.svelte` for the established pattern, same one `sidebar-config.ts` reads)
    to decide whether to render the D-18 create-row or the Manager empty-state. Implement the
    two-stage Escape. Create `PlacePickerSection.svelte` in the showcase following the existing
    section-file structural pattern, seeded with a small invented demo tree ("Здание А" → "2 этаж" →
    "214" — fictional data only, per the project's hard privacy rule) served from local component
    state (not a real API call, matching how other showcase sections demo without live data, if that
    is the established showcase convention — confirm via the read_first analog file). Register the
    new section in `ComponentShowcasePage.svelte`'s section list.
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>Search mode, D-18 create-row (Admin-only), Manager empty-state, and two-stage Escape all
  match UI-SPEC §10.3/§10.4/§14.2; showcase entry exists and demos both modes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: PlacePicker</name>
  <action>No code changes in this task — see &lt;how-to-verify&gt; for the manual verification steps to perform.</action>
  <what-built>PlacePicker.svelte (field + tree-mode panel + search-mode panel + D-18 create-row) and
  its component-showcase demo entry.</what-built>
  <how-to-verify>
    1. Run `cargo tauri dev`; open the component showcase (`/showcase`, Admin role); scroll to the
       PlacePicker section.
    2. Tab into the field — confirm the panel opens immediately in tree mode showing invented demo
       root nodes.
    3. Arrow-key navigate (↑/↓), expand a branch with →, select a leaf with Enter — confirm the field
       shows the full path ("Здание А / 2 этаж / 214" or the demo's equivalent invented path).
    4. Type a partial Cyrillic query (e.g. lowercase "здание") — confirm the panel switches to search
       mode and highlights the matched fragment.
    5. As Admin, type a query with no match — confirm the "Создать «...» в «...»" row appears.
    6. Run `pnpm --dir ui build`, then open the same showcase page from a LAN browser tab pointed at
       the running server's address — repeat steps 2-5 there (WKWebView/WebView2 vs browser parity
       per project convention).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PlacePicker's D-18 create-row visibility | Client-side role check is UX-only (per D-20's documented convention) — the real enforcement is Plan 12's server-side `authorize(&Action::MutatePlaces)`, which rejects a Manager's `places_create` call even if this component's role-check were somehow bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-13-01 | Elevation of Privilege | Manager forging a `places_create` call by manipulating client state to show the hidden create-row | accept (mitigated upstream) | This component's role-check is cosmetic; Plan 12's server-side `MutatePlaces` gate is the actual boundary — documented explicitly in UI-SPEC §2 ("Ролевой гейт как граница безопасности — серверный") |
</threat_model>

<verification>
`pnpm --dir ui exec svelte-check` 0 errors. `pnpm --dir ui build` succeeds. Manual verification per
each task's `<human-check>` in both the Tauri dev webview and (after `pnpm --dir ui build`) a LAN
browser tab pointed at the running server.
</verification>

<success_criteria>
`PlacePicker.svelte` fully implements UI-SPEC §10; showcase entry demonstrates both tree and search
modes with invented data only.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-13-SUMMARY.md` when done
</output>
