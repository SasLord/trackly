---
phase: 39-place-tree
plan: 05
type: execute
wave: 3
depends_on: ["39-04"]
files_modified:
  - crates/trackly-app/src/services/place_service.rs
  - crates/trackly-app/tests/places_service_crud.rs
  - crates/trackly-app/tests/places_move_cycle.rs
  - crates/trackly-app/tests/places_delete_blocked.rs
autonomous: true
requirements: [PLC-01]

must_haves:
  truths:
    - "Каждая мутация места (create/rename/move/archive/unarchive/delete) проходит authorize(&Action::MutatePlaces) — Manager, вызвавший мутацию напрямую, получает Forbidden независимо от того, что видит в UI (D-20)"
    - "Каждая мутация проходит через self.writer.execute (единственный писатель); ни одна SQLite-запись не выполняется в обход очереди writer'а"
    - "Удаление непустого узла возвращает точный текст с числами по образцу «в месте 12 устройств и 2 вложенных места», а не общий отказ (D-14)"
    - "Архивация (D-15) не удаляет узел и не убирает его из дерева/карточек/истории — только помечает archived_at_utc, скрывающий узел из PlacePicker (реализуется чтением, не этим планом, но контракт данных для этого — здесь)"
  artifacts:
    - path: "crates/trackly-app/src/services/place_service.rs"
      provides: "PlaceService mutation methods: create/rename/move_node/archive/unarchive/delete_hard, each authorize()-gated and audit-logged"
      contains: "authorize(caller, &Action::MutatePlaces)"
  key_links:
    - from: "crates/trackly-app/src/services/place_service.rs"
      to: "crates/trackly-core/src/auth.rs"
      via: "authorize(caller, &Action::MutatePlaces)"
      pattern: "Action::MutatePlaces"
    - from: "crates/trackly-app/src/services/place_service.rs"
      to: "crates/trackly-infra/src/repos/places_sqlite.rs"
      via: "self.writer.execute(move |conn| { let tx = conn.transaction()...; repo.move_node(&tx, ...) })"
      pattern: "writer.execute"
---

<objective>
Implement the mutation half of `PlaceService` — `create`/`rename`/`move_node`/`archive`/
`unarchive`/`delete_hard` — each authorize()-gated (Admin-only per D-20), audit-logged, and routed
through the single-writer task. Split from the read half (Plan 08, same file, sequential wave) to
keep both plans within context budget.

Purpose: gives Wave 4/5 (place transports, entity migrations needing place validation) a working
mutation API.
Output: mutation methods on `crates/trackly-app/src/services/place_service.rs`, proven by
`places_move_cycle.rs` (cycle rejection surfaces correctly through the service) and
`places_delete_blocked.rs` (D-14 exact-count message).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-RESEARCH.md
@.planning/phases/39-place-tree/39-PATTERNS.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlaceService — create/rename/archive/unarchive</name>
  <files>crates/trackly-app/src/services/place_service.rs, crates/trackly-app/tests/places_service_crud.rs</files>
  <read_first>
    crates/trackly-app/tests/devices_crud.rs (lines 1-70 — timeout/multi-thread test-file idiom to replicate in the new places_service_crud.rs),
    crates/trackly-app/src/services/device_service.rs (lines 150-213 — single-writer `create()` shape
    with `writer.execute(move |conn| { tx = conn.transaction(); ...; audit_log INSERT; tx.commit() })`
    and the `spawn_blocking` read-path shape for `get()`), crates/trackly-core/src/ports/places.rs
    (from Plan 02), crates/trackly-infra/src/repos/places_sqlite.rs (from Plan 04, the concrete impl
    this service wires to).
  </read_first>
  <behavior>
    - Test: `create` with a valid `PlaceNew` and Admin caller succeeds, returns a `PlaceDto`-shaped
      result with the new id, and writes an `audit_log` row with `entity_type = 'place'`,
      `action = 'create'`.
    - Test: `create` called with a Manager caller returns `AppError::Forbidden` before any DB write
      occurs.
    - Test: `rename` with a duplicate sibling name (D-04) surfaces the repo's uniqueness error as a
      validation-shaped `AppError`, not a raw SQLite constraint message.
    - Test: `archive` sets `archived_at_utc`; a subsequent `get` on that node still returns it (not
      soft-deleted, still visible to direct lookup) — `unarchive` clears it back to `None`.
  </behavior>
  <action>
    Create `crates/trackly-app/src/services/place_service.rs` with `pub struct PlaceService { writer:
    ..., readers: ..., repo: SqlitePlaceRepository, clock: Arc&lt;dyn Clock + Send + Sync&gt; }`
    (mirror `DeviceService`'s field shape exactly). Implement `pub async fn create(&self, caller:
    &Identity, new: PlaceNew) -> Result&lt;PlaceDto, AppError&gt;`: `authorize(caller,
    &Action::MutatePlaces)?` first, then validate `new.name` non-empty (mirrors existing
    `AppError::Validation` convention), then `self.writer.execute(move |conn| { let tx =
    conn.transaction()?; let id = repo.create(&mut tx, &new, now)?; ...audit_log INSERT
    entity_type='place'...; tx.commit()?; Ok(id) }).await`, then `self.get(id).await` to return the
    hydrated DTO. Implement `rename(&self, caller, id, name, version) -> Result&lt;PlaceDto,
    AppError&gt;` and `archive`/`unarchive(&self, caller, id, version) -> Result&lt;(), AppError&gt;`
    identically shaped (authorize → writer.execute → audit_log → commit). Every method takes the
    caller's `expected_version` for optimistic-lock CAS, matching the `ActPatch`/`DevicePatch`
    convention already in this codebase. Create `crates/trackly-app/tests/places_service_crud.rs`
    covering every Behavior-block case above, following `devices_crud.rs`'s timeout/multi-thread
    idiom.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test places_service_crud -- --skip login_remember_persistent_cookie</automated>
  </verify>
  <done>All Behavior-block cases pass; every mutation method authorize()-gates on `MutatePlaces`
  before touching the writer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlaceService — move_node (cycle-safe) + delete_hard (conflict-shaped)</name>
  <files>crates/trackly-app/src/services/place_service.rs, crates/trackly-app/tests/places_move_cycle.rs, crates/trackly-app/tests/places_delete_blocked.rs</files>
  <read_first>
    39-UI-SPEC.md §11.3 (move-modal consequences copy: "Вместе с местом переедет 3 вложенных места и
    47 устройств.", singular agreement rules, zero-parts omitted) and §11.5/§14.3 (delete-blocked
    copy: "Место нельзя удалить: в нём 12 устройств и 2 вложенных места. Перенесите содержимое или
    архивируйте место." — and the cycle-error copy "Нельзя переместить место внутрь самого себя или
    своего вложенного места.").
  </read_first>
  <behavior>
    - Test (`places_move_cycle.rs`): moving a node into its own descendant returns an
      `AppError::Validation` whose message matches UI-SPEC §14.3's cycle-error copy exactly; moving a
      node to an unrelated valid parent succeeds.
    - Test (`places_delete_blocked.rs`): deleting a place containing 2 devices and 1 nested place
      returns an error whose message contains the exact counts ("2" and "1" — or whatever the test
      fixture's real counts are) formatted per UI-SPEC §11.5's template; deleting an empty leaf place
      succeeds.
  </behavior>
  <action>
    Implement `move_node(&self, caller, id, new_parent_id, version) -> Result&lt;PlaceDto, AppError&gt;`:
    `authorize(caller, &Action::MutatePlaces)?`, then `self.writer.execute(...)` calling
    `repo.move_node` (which internally runs the Plan 04 cycle check) — propagate the repo's
    cycle-rejection `AppError::Validation` unchanged (its message is already the UI-SPEC-locked copy,
    written in Plan 04 — do not re-wrap or reword it here). Implement `delete_hard(&self, caller, id,
    version) -> Result&lt;(), AppError&gt;`: `authorize(...)?`, then call `repo.subtree_stats` first
    (read path, via `spawn_blocking`), and if non-empty, build the exact D-14 message ("Место нельзя
    удалить: в нём {device_count} устройств и {nested_places} вложенных места. Перенесите содержимое
    или архивируйте место." — match UI-SPEC §11.5's literal Russian template and its singular/plural
    agreement rule already specified for the move-consequences copy in §11.3, applied identically
    here) as an `AppError::Conflict`; otherwise call `self.writer.execute(...)` with `repo.delete_hard`.
    Add both test files following `devices_crud.rs`'s timeout/multi-thread idiom.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test places_move_cycle --test places_delete_blocked -- --skip login_remember_persistent_cookie</automated>
  </verify>
  <done>Cycle rejection and delete-blocked-with-exact-counts are both proven by dedicated integration
  tests; error copy matches UI-SPEC §11.3/§11.5/§14.3 verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Any caller (Tauri command / axum handler, wired in Plan 12) → `PlaceService` mutation methods | Every mutation crosses `authorize()` here before reaching the writer — this is the actual enforcement point Plan 12's thin transport adapters rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | Elevation of Privilege | `PlaceService::create/rename/move_node/archive/unarchive/delete_hard` | mitigate | Every method calls `authorize(caller, &Action::MutatePlaces)` as its first statement, before any DB write — Manager/Employee callers rejected regardless of transport (this is the service-layer half of the dual-transport gate; Plan 12 wires both Tauri and axum callers to it) |
| T-39-05-02 | Tampering / DoS (data integrity) | `delete_hard` on a non-empty subtree | mitigate | `subtree_stats` checked before any `DELETE`; no cascade, no auto-reparenting — matches D-14 literally |
| T-39-05-03 | Repudiation | Place mutations with no audit trail | mitigate | Every mutation writes an `audit_log` row (`entity_type = 'place'`), matching the existing convention for every other mutable entity in this codebase |
</threat_model>

<verification>
`cargo test -p trackly-app --test places_move_cycle --test places_delete_blocked -- --skip
login_remember_persistent_cookie` green, with `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1` set.
</verification>

<success_criteria>
`PlaceService`'s mutation surface is complete, Admin-gated, audit-logged, and single-writer; cycle
rejection and delete-blocked exact counts are proven by automated tests matching UI-SPEC copy.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-05-SUMMARY.md` when done
</output>
