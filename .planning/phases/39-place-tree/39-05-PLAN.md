---
phase: 39-place-tree
plan: 05
type: execute
wave: 3
depends_on: ["39-04"]
files_modified:
  - crates/trackly-app/src/dto/place.rs
  - crates/trackly-app/src/services/place_service.rs
  - crates/trackly-app/src/context.rs
  - crates/trackly-app/tests/places_service_crud.rs
  - crates/trackly-app/tests/places_move_cycle.rs
  - crates/trackly-app/tests/places_delete_blocked.rs
autonomous: true
requirements: [PLC-01]

must_haves:
  truths:
    - "Каждая мутация места (create/rename/move/archive/unarchive/delete) проходит authorize(&Action::MutatePlaces) — Manager, вызвавший мутацию напрямую, получает Forbidden независимо от того, что видит в UI (D-20)"
    - "Каждая мутация проходит через self.writer.execute (единственный писатель); ни одна SQLite-запись не выполняется в обход очереди writer'а"
    - "Удаление непустого узла возвращает точный текст с числами по образцу «в месте 12 устройств и 2 вложенных места», а не общий отказ (D-14)"
    - "Архивация (D-15) не удаляет узел и не убирает его из дерева/карточек/истории — только помечает archived_at_utc, скрывающий узел из PlacePicker (реализуется чтением, не этим планом, но контракт данных для этого — здесь)"
    - "PlaceDto/PlaceTreeNodeDto/PlacePathDto существуют в dto/place.rs с serde+specta деривами (аналог DeviceDto) — это единственный источник форм ответа для всех транспортных слоёв фазы, определён до, а не после первого потребителя"
    - "AppCtx содержит поле pub places: Arc<PlaceService>, построенное в AppCtx::build() тем же паттерном, что и остальные сервисы (writer.clone()/readers.clone()/clock.clone()) — tauri_cmds/places.rs и http/places.rs (план 12) могут вызывать ctx.places.<метод> без дополнительной проводки"
  artifacts:
    - path: "crates/trackly-app/src/dto/place.rs"
      provides: "PlaceDto, PlaceTreeNodeDto, PlacePathDto — serde+specta transport shapes (DeviceDto analog)"
      contains: "pub struct PlaceDto"
    - path: "crates/trackly-app/src/services/place_service.rs"
      provides: "PlaceService mutation methods: create/rename/move_node/archive/unarchive/delete_hard, each authorize()-gated and audit-logged"
      contains: "authorize(caller, &Action::MutatePlaces)"
  key_links:
    - from: "crates/trackly-app/src/services/place_service.rs"
      to: "crates/trackly-core/src/auth.rs"
      via: "authorize(caller, &Action::MutatePlaces)"
      pattern: "Action::MutatePlaces"
    - from: "crates/trackly-app/src/services/place_service.rs"
      to: "crates/trackly-infra/src/repos/places_sqlite.rs"
      via: "self.writer.execute(move |conn| { let tx = conn.transaction()...; repo.move_node(&tx, ...) })"
      pattern: "writer.execute"
    - from: "crates/trackly-app/src/context.rs"
      to: "crates/trackly-app/src/services/place_service.rs"
      via: "pub places: Arc<PlaceService> constructed in AppCtx::build() via PlaceService::new(writer.clone(), readers.clone(), clock.clone())"
      pattern: "PlaceService::new"
---

<objective>
Define the `dto/place.rs` transport contracts (`PlaceDto`/`PlaceTreeNodeDto`/`PlacePathDto`) that
every downstream Phase 39 plan returns/consumes, implement the mutation half of `PlaceService` —
`create`/`rename`/`move_node`/`archive`/`unarchive`/`delete_hard` — each authorize()-gated
(Admin-only per D-20), audit-logged, and routed through the single-writer task, and wire the
finished service into `AppCtx` so Plan 12's transport adapters have `ctx.places.<method>` to call.
Split from the read half (Plan 08, same file, sequential wave) to keep both plans within context
budget.

Purpose: gives Wave 4/5 (place transports, entity migrations needing place validation) a working,
DTO-shaped, AppCtx-reachable mutation API — this plan is the composition-root wiring point for the
whole phase, not just the mutation logic.
Output: `crates/trackly-app/src/dto/place.rs`; mutation methods on
`crates/trackly-app/src/services/place_service.rs`, proven by `places_move_cycle.rs` (cycle
rejection surfaces correctly through the service) and `places_delete_blocked.rs` (D-14 exact-count
message); `pub places: Arc<PlaceService>` on `AppCtx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-RESEARCH.md
@.planning/phases/39-place-tree/39-PATTERNS.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dto/place.rs contracts + PlaceService — create/rename/archive/unarchive</name>
  <files>crates/trackly-app/src/dto/place.rs, crates/trackly-app/src/services/place_service.rs, crates/trackly-app/tests/places_service_crud.rs</files>
  <read_first>
    crates/trackly-app/src/dto/device.rs (full file — `DeviceDto`/`impl From&lt;DeviceRow&gt; for
    DeviceDto` convention to mirror exactly: `#[derive(Debug, Clone, PartialEq, Eq, Serialize,
    Deserialize, Type)]`, `#[specta(type = i32)]` on every `i64` id/version/timestamp field, snake_case
    field names — NO `rename_all = "camelCase"`), crates/trackly-app/tests/devices_crud.rs (lines
    1-70 — timeout/multi-thread test-file idiom to replicate in the new places_service_crud.rs),
    crates/trackly-app/src/services/device_service.rs (lines 150-213 — single-writer `create()` shape
    with `writer.execute(move |conn| { tx = conn.transaction(); ...; audit_log INSERT; tx.commit() })`
    and the `spawn_blocking` read-path shape for `get()`), crates/trackly-core/src/ports/places.rs
    (from Plan 02 — note `create` returns only the new `id: i64`, unlike `rename`/`move_node` which
    already return the full `PlaceRow`), crates/trackly-infra/src/repos/places_sqlite.rs (from Plan
    04, the concrete impl this service wires to).
  </read_first>
  <behavior>
    - Test: `create` with a valid `PlaceNew` and Admin caller succeeds, returns a `PlaceDto`-shaped
      result with the new id, and writes an `audit_log` row with `entity_type = 'place'`,
      `action = 'create'`.
    - Test: `create` called with a Manager caller returns `AppError::Forbidden` before any DB write
      occurs.
    - Test: `rename` with a duplicate sibling name (D-04) surfaces the repo's uniqueness error as a
      validation-shaped `AppError`, not a raw SQLite constraint message.
    - Test: `archive` sets `archived_at_utc`; a subsequent `get` on that node still returns it (not
      soft-deleted, still visible to direct lookup) — `unarchive` clears it back to `None`.
  </behavior>
  <action>
    Create `crates/trackly-app/src/dto/place.rs` FIRST, before any service code — this is the
    interface-first contract every later Phase 39 plan (08/12/13/14/19/20) returns/consumes. Define
    `pub struct PlaceDto` with fields mirroring `PlaceRow` (id, parent_id, kind, name, level,
    is_storage, sort_order, archived_at_utc, notes, full_path, created_at_utc, updated_at_utc,
    version — `#[specta(type = i32)]` on every `i64`), where `kind: String` is produced via
    `PlaceKind::as_str().to_string()` (per Plan 02: `PlaceKind` itself has no serde/specta derive by
    design — the DTO layer owns the string conversion) and `impl From&lt;PlaceRow&gt; for PlaceDto`
    doing that conversion. Define `pub struct PlaceTreeNodeDto` — every `PlaceDto` field plus
    `#[specta(type = i32)] pub content_count: i64` (D-25's per-node counter, "sum including nested
    places", populated by Plan 08's subtree-stats-backed list methods — this task only defines the
    shape). Define `pub struct PlacePathDto { #[specta(type = i32)] pub place_id: i64, pub full_path:
    String, pub kind: String }` (the `places_search` result row shape Plan 08 Task 2 will populate).
    All three derive `Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Type`, snake_case field
    names (no `rename_all`), matching `DeviceDto`'s convention exactly. Then create
    `crates/trackly-app/src/services/place_service.rs` with `pub struct PlaceService { writer:
    ..., readers: ..., repo: SqlitePlaceRepository, clock: Arc&lt;dyn Clock + Send + Sync&gt; }`
    (mirror `DeviceService`'s field shape exactly) and `pub fn new(writer: Arc&lt;WriterHandle&gt;,
    readers: Arc&lt;ReaderPool&gt;, clock: Arc&lt;dyn Clock + Send + Sync&gt;) -> Self` constructing
    `repo: SqlitePlaceRepository` internally (mirrors `CartridgeService::new`'s
    `cart_repo: Arc::new(SqliteCartridgeRepository)` pattern — repo is a zero-sized unit struct, never
    passed in from the caller). Implement `pub async fn create(&self, caller: &Identity, new:
    PlaceNew) -> Result&lt;PlaceDto, AppError&gt;`: `authorize(caller, &Action::MutatePlaces)?` first,
    then validate `new.name` non-empty (mirrors existing `AppError::Validation` convention), then
    `self.writer.execute(move |conn| { let tx = conn.transaction()?; let id = repo.create(&mut tx,
    &new, now)?; ...audit_log INSERT entity_type='place'...; let row = repo.get(&tx, id)?; tx.commit()?;
    Ok(row) }).await` and convert the returned `PlaceRow` to `PlaceDto` — do NOT call a `self.get(id)`
    read-path method here: `get()` is Plan 08's method (same file, later wave) and does not exist yet
    when this task's own `cargo test` runs; fetching the row inside the same write transaction (via
    `repo.get(&tx, id)`, already defined by Plan 02/04) avoids the forward reference entirely and is
    strictly cheaper (no second connection acquisition). Implement `rename(&self, caller, id, name,
    version) -> Result&lt;PlaceDto, AppError&gt;` by converting `repo.rename(...)`'s already-returned
    `PlaceRow` directly (no extra read needed — the port's `rename` signature already returns the
    updated row) and `archive`/`unarchive(&self, caller, id, version) -> Result&lt;(), AppError&gt;`
    identically shaped otherwise (authorize → writer.execute → audit_log → commit). Every method
    takes the caller's `expected_version` for optimistic-lock CAS, matching the `ActPatch`/
    `DevicePatch` convention already in this codebase. Create `crates/trackly-app/tests/places_service_crud.rs`
    covering every Behavior-block case above, following `devices_crud.rs`'s timeout/multi-thread
    idiom.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test places_service_crud -- --skip login_remember_persistent_cookie</automated>
  </verify>
  <done>All Behavior-block cases pass; every mutation method authorize()-gates on `MutatePlaces`
  before touching the writer; `create` does not reference any Plan-08-only method.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlaceService — move_node (cycle-safe) + delete_hard (conflict-shaped)</name>
  <files>crates/trackly-app/src/services/place_service.rs, crates/trackly-app/tests/places_move_cycle.rs, crates/trackly-app/tests/places_delete_blocked.rs</files>
  <read_first>
    39-UI-SPEC.md §11.3 (move-modal consequences copy: "Вместе с местом переедет 3 вложенных места и
    47 устройств.", singular agreement rules, zero-parts omitted) and §11.5/§14.3 (delete-blocked
    copy: "Место нельзя удалить: в нём 12 устройств и 2 вложенных места. Перенесите содержимое или
    архивируйте место." — and the cycle-error copy "Нельзя переместить место внутрь самого себя или
    своего вложенного места.").
  </read_first>
  <behavior>
    - Test (`places_move_cycle.rs`): moving a node into its own descendant returns an
      `AppError::Validation` whose message matches UI-SPEC §14.3's cycle-error copy exactly; moving a
      node to an unrelated valid parent succeeds.
    - Test (`places_delete_blocked.rs`): deleting a place containing 2 devices and 1 nested place
      returns an error whose message contains the exact counts ("2" and "1" — or whatever the test
      fixture's real counts are) formatted per UI-SPEC §11.5's template; deleting an empty leaf place
      succeeds.
  </behavior>
  <action>
    Implement `move_node(&self, caller, id, new_parent_id, version) -> Result&lt;PlaceDto, AppError&gt;`:
    `authorize(caller, &Action::MutatePlaces)?`, then `self.writer.execute(...)` calling
    `repo.move_node` (which internally runs the Plan 04 cycle check) — propagate the repo's
    cycle-rejection `AppError::Validation` unchanged (its message is already the UI-SPEC-locked copy,
    written in Plan 04 — do not re-wrap or reword it here). Implement `delete_hard(&self, caller, id,
    version) -> Result&lt;(), AppError&gt;`: `authorize(...)?`, then call `repo.subtree_stats` first
    (read path, via `spawn_blocking`), and if non-empty, build the exact D-14 message ("Место нельзя
    удалить: в нём {device_count} устройств и {nested_places} вложенных места. Перенесите содержимое
    или архивируйте место." — match UI-SPEC §11.5's literal Russian template and its singular/plural
    agreement rule already specified for the move-consequences copy in §11.3, applied identically
    here) as an `AppError::Conflict`; otherwise call `self.writer.execute(...)` with `repo.delete_hard`.
    Add both test files following `devices_crud.rs`'s timeout/multi-thread idiom.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test places_move_cycle --test places_delete_blocked -- --skip login_remember_persistent_cookie</automated>
  </verify>
  <done>Cycle rejection and delete-blocked-with-exact-counts are both proven by dedicated integration
  tests; error copy matches UI-SPEC §11.3/§11.5/§14.3 verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Wire PlaceService into AppCtx</name>
  <files>crates/trackly-app/src/context.rs</files>
  <read_first>
    crates/trackly-app/src/context.rs (full file — locate the `pub struct AppCtx` field list, the
    `pub cartridges: Arc&lt;CartridgeService&gt;` field/doc-comment as the structural analog, and the
    `let cartridges = Arc::new(CartridgeService::new(writer.clone(), readers.clone(),
    clock.clone()));` construction call site — copy this exact three-argument shape; `PlaceService::new`
    takes the same three arguments per Task 1).
  </read_first>
  <action>
    Add `pub places: Arc&lt;PlaceService&gt;` to `AppCtx`'s field list, with a doc comment following
    the existing `/// &lt;Service&gt; — &lt;one-line purpose&gt;. Added in Phase 39 Plan 05.` convention
    used by every other service field. Import `PlaceService` into `context.rs`'s existing
    `use crate::services::{ ... }` block (alongside `CartridgeService`/`DeviceService`/etc.). In
    `AppCtx::build()`, add `let places = Arc::new(PlaceService::new(writer.clone(), readers.clone(),
    clock.clone()));` at the same point in the construction sequence where `cartridges` is built
    (after `writer`/`readers`/`clock` are available, no ordering dependency on any other service —
    `PlaceService` has zero cross-entity dependencies unlike `DeviceService`'s `printer_repo`). Add
    `places,` to the final `AppCtx { ... }` struct-literal assembly at the end of `build()`.
  </action>
  <verify>
    <automated>grep -n "pub places: Arc<PlaceService>" crates/trackly-app/src/context.rs && grep -n "PlaceService::new" crates/trackly-app/src/context.rs && cargo build -p trackly-app</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -n "pub places: Arc<PlaceService>" crates/trackly-app/src/context.rs` confirms the field
    - `grep -n "PlaceService::new" crates/trackly-app/src/context.rs` confirms construction
    - `cargo build -p trackly-app` succeeds for this module's own scope (full-crate build may still
      fail on sibling Wave-3+ plans not yet landed — cross-plan compile dependency, same convention
      already documented in Plan 06's verification notes)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Any caller (Tauri command / axum handler, wired in Plan 12) → `PlaceService` mutation methods | Every mutation crosses `authorize()` here before reaching the writer — this is the actual enforcement point Plan 12's thin transport adapters rely on |
| `AppCtx.places` composition-root field | No new trust boundary — same in-process wiring pattern as every other `AppCtx` service field; the boundary is still `authorize()` inside `PlaceService`, not the field's mere existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | Elevation of Privilege | `PlaceService::create/rename/move_node/archive/unarchive/delete_hard` | mitigate | Every method calls `authorize(caller, &Action::MutatePlaces)` as its first statement, before any DB write — Manager/Employee callers rejected regardless of transport (this is the service-layer half of the dual-transport gate; Plan 12 wires both Tauri and axum callers to it) |
| T-39-05-02 | Tampering / DoS (data integrity) | `delete_hard` on a non-empty subtree | mitigate | `subtree_stats` checked before any `DELETE`; no cascade, no auto-reparenting — matches D-14 literally |
| T-39-05-03 | Repudiation | Place mutations with no audit trail | mitigate | Every mutation writes an `audit_log` row (`entity_type = 'place'`), matching the existing convention for every other mutable entity in this codebase |
</threat_model>

<verification>
`cargo test -p trackly-app --test places_move_cycle --test places_delete_blocked -- --skip
login_remember_persistent_cookie` green, with `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1` set.
`cargo build -p trackly-app` confirms `AppCtx.places` compiles (Task 3).
</verification>

<success_criteria>
`PlaceService`'s mutation surface is complete, Admin-gated, audit-logged, and single-writer; cycle
rejection and delete-blocked exact counts are proven by automated tests matching UI-SPEC copy;
`dto/place.rs` exists as the phase's shared transport contract; `AppCtx.places` is reachable by
Plan 12's transport adapters.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-05-SUMMARY.md` when done
</output>
