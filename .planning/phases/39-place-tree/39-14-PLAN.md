---
phase: 39-place-tree
plan: 14
type: execute
wave: 8
depends_on: ["39-13", "39-19", "39-12"]
files_modified:
  - ui/src/features/places/PlacesPage.svelte
  - ui/src/features/places/PlacesMasterDetail.svelte
  - ui/src/features/places/PlaceTree.svelte
  - ui/src/features/places/PlaceTreeNode.svelte
  - ui/src/routes.ts
  - ui/src/features/layout/sidebar-config.ts
autonomous: true
requirements: [PLC-01, PLC-02, PLC-06]

must_haves:
  truths:
    - "Раздел «Места» доступен по маршруту /places, пункт сайдбара стоит сразу после «Карта», видим Admin и Manager, не виден Employee (D-19, D-20)"
    - "Раздел «Места» — отдельный пункт сайдбара, а не вкладка в Настройках; макет — две панели (дерево слева, содержимое справа), один экран закрывает и построение дерева, и просмотр содержимого (D-19, D-22)"
    - "Дерево — role=tree/treeitem/group с roving tabindex; клавиатурная карта (↑↓→←/Home/End/Enter/F2/Escape) реализована полностью, не только мышь (D-21, Фаза 30 паритет)"
    - "Порядок братьев — sort_order, затем level (этажи, включая 0 и отрицательные), затем натуральное сравнение имени; узел без sort_order/level не ломает сортировку остальных (D-05, PLC-02)"
    - "Счётчик узла показывает сумму с учётом вложенных мест и не рендерится при нуле (D-25)"
    - "Перетаскивание доступно только Admin, работает только «внутрь узла» и всегда открывает диалог подтверждения (никогда не перемещает молча) — тот же PlaceMoveModal, что и пункт меню «Переместить в…» (D-21)"
  artifacts:
    - path: "ui/src/features/places/PlaceTree.svelte"
      provides: "role=tree with roving tabindex, keyboard map per UI-SPEC §8.5"
      min_lines: 100
    - path: "ui/src/features/places/PlaceTreeNode.svelte"
      provides: "32px row, chevron/badges/counter/ActionMenu per UI-SPEC §8.2-8.4"
      min_lines: 80
  key_links:
    - from: "ui/src/features/places/PlaceTreeNode.svelte"
      to: "ui/src/features/places/PlaceFormModal.svelte"
      via: "ActionMenu 'Переименовать'/'Создать вложенное место' opens PlaceFormModal"
      pattern: "PlaceFormModal"
    - from: "ui/src/features/places/PlaceTreeNode.svelte"
      to: "ui/src/features/places/PlaceMoveModal.svelte"
      via: "ActionMenu 'Переместить в…' AND drag-drop both open PlaceMoveModal"
      pattern: "PlaceMoveModal"
---

<objective>
Build the "Места" section shell: routing, sidebar entry, the master-detail layout, and the left-
panel tree (`PlaceTree`/`PlaceTreeNode`) with full keyboard/ARIA parity and drag-n-drop, wired to
the modals Plan 19 already built. The right-panel content view is Plan 20 (Wave 9, depends on this
plan for the master-detail shell to embed into).

Purpose: this is the primary user-facing surface of PLC-01/PLC-06 — the tree that lets an Admin
build the place hierarchy and lets Admin/Manager navigate it.
Output: `/places` route, sidebar entry, `PlacesMasterDetail.svelte`, `PlaceTree.svelte`,
`PlaceTreeNode.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<interfaces>
<!-- ui/src/features/places/PlaceFormModal.svelte and PlaceMoveModal.svelte (Plan 19) — exact props
contract this plan's ActionMenu wiring must match: -->
```
PlaceFormModal.svelte: { mode: 'create'|'rename', place: PlaceDto|null, defaultParentId: number|null, onClose, onSaved }
PlaceMoveModal.svelte: { place: PlaceDto, onClose, onMoved }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: routes.ts + sidebar-config.ts + PlacesPage.svelte + PlacesMasterDetail.svelte</name>
  <files>ui/src/routes.ts, ui/src/features/layout/sidebar-config.ts, ui/src/features/places/PlacesPage.svelte, ui/src/features/places/PlacesMasterDetail.svelte</files>
  <read_first>
    ui/src/routes.ts (full file, 32 lines — insertion point for `/places`), ui/src/features/layout/sidebar-config.ts
    (full file, 46 lines — insertion point immediately after the `/map` entry, before the first
    divider, per UI-SPEC §7; the `PINNED:` comment count needs updating from "11 items + 4 dividers =
    15" to "12 items + 4 dividers = 16"), ui/src/features/requests/RequestsMasterDetail.svelte (full
    file — UI-SPEC §6.2 mandates `PlacesMasterDetail` be a literal copy: `Snippet`-based `master`/
    `detail` props, grid `35% / 65%`, `gap: var(--tr-space-md)`, panel styling
    `--tr-surface-raised`/`--tr-border`/`--tr-radius-md`/`--tr-elev-1`, `min-width: 320px`/`480px`),
    39-UI-SPEC.md §7 (route/sidebar/PageHeader/deep-link contract).
  </read_first>
  <action>
    In `routes.ts`, add `import PlacesPage from './features/places/PlacesPage.svelte';` and `'/places':
    PlacesPage,` immediately after the `'/map': MapPage,` entry. In `sidebar-config.ts`, insert `{
    kind: 'item', route: '/places', label: 'Места', roles: ['admin', 'manager'] }` immediately after
    the `/map` entry (before the first `{ kind: 'divider' }`), and update the `PINNED:` comment to "12
    items + 4 dividers = 16 entries." Create `PlacesMasterDetail.svelte` as a literal copy of
    `RequestsMasterDetail.svelte`'s structure (same `Snippet` props, same grid/panel CSS — UI-SPEC §3
    explicitly forbids recalculating these values). Create `PlacesPage.svelte`: `PageHeader
    title="Места"` with an `actions` slot containing `Button variant="primary"` "Создать место"
    (rendered only when the current user's role is Admin — reuse whichever role-check pattern
    `sidebar-config.ts`'s `getVisibleItems` or an equivalent auth store exposes), body = `PlacesMasterDetail`
    with `master` = `PlaceTree` (Task 2) and `detail` = a placeholder until Plan 20 lands ("Место не
    выбрано" empty state per UI-SPEC §14.2, `Table`-style empty-state component if one already
    exists in this codebase, otherwise plain markup matching the copy). Wire the "Создать место"
    button to open `PlaceFormModal` with `mode="create"`, `place={null}`, `defaultParentId={null}`.
    Implement the deep-link hash contract (`#/places?id=…`, UI-SPEC §7) — reading the hash on mount to
    pre-select a node, and updating it whenever the tree selection changes (needed by Plan 20's
    "Показать содержимое" navigation from the delete-blocked error).
  </action>
  <verify>
    <automated>grep -n "'/places'" ui/src/routes.ts && grep -n "route: '/places'" ui/src/features/layout/sidebar-config.ts && grep -c "roles: \['admin', 'manager'\]" ui/src/features/layout/sidebar-config.ts && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -n "'/places'" ui/src/routes.ts` confirms the route is registered
    - `grep -n "route: '/places'" ui/src/features/layout/sidebar-config.ts` confirms the sidebar
      entry, positioned immediately after `/map` in the file
    - `grep -c "roles: \['admin', 'manager'\]" ui/src/features/layout/sidebar-config.ts` includes the
      new `/places` entry among its matches
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlaceTree.svelte + PlaceTreeNode.svelte — role=tree, roving tabindex, sort, counters</name>
  <files>ui/src/features/places/PlaceTree.svelte, ui/src/features/places/PlaceTreeNode.svelte</files>
  <read_first>
    39-UI-SPEC.md §8.1-§8.3, §8.5 (toolbar, row anatomy, action menu, keyboard/ARIA — treat as
    locked), ui/src/lib/components/ActionMenu.svelte (grep for the `label` prop signature — UI-SPEC
    §8.2 requires a per-row `label={`Действия: ${node.name}`}`, NOT the component's default
    `aria-label="Действия"`), ui/src/lib/components/Badge.svelte (variant/appearance/size prop
    signatures for the "Склад"/"Архив" badges).
  </read_first>
  <behavior>
    - Test/manual: root nodes render with `--depth: 0`; a child under a child renders with
      `--depth: 2`, indent formula `calc(var(--tr-space-xs) + var(--depth) * var(--tr-space-md))`.
    - Test/manual: siblings with `sort_order` set win over `level`/name ordering; siblings with only
      `level` (floors, including `-1` and `0`) sort numerically, not alphabetically; siblings with
      neither sort by natural name comparison ("2 этаж" before "10 этаж").
    - Test/manual: a node with `content_count: 0` renders no counter span at all (not "0"); a node
      with `content_count: 47` renders "47" in `.tr-mono`.
    - Test/manual: arrow keys move focus among VISIBLE nodes only (collapsed subtrees are skipped);
      Home/End jump to first/last visible node; → expands or moves to first child; ← collapses or
      moves to parent; F2 opens `PlaceFormModal` in rename mode for the focused node (Admin only).
  </behavior>
  <action>
    Create `PlaceTree.svelte`: `role="tree"` `aria-label="Дерево мест"` container, fetches the full
    tree via `apiCall('places_list_all', { includeArchived: showArchived })` (bound to the toolbar's
    "Показывать архивные" `Checkbox`, default off), applies the natural/level/sort_order comparator
    (port the exact algorithm from `crates/trackly-core/src/domain/places.rs::sibling_cmp` to
    TypeScript — do not call the backend for sorting, sort client-side once the flat list is fetched)
    at each level to build the nested render structure, implements roving tabindex (exactly one
    `tabindex="0"` element at a time, the rest `tabindex="-1"`), and the full keyboard map from
    UI-SPEC §8.5. When the toolbar search `Input` is non-empty, switch to the flat search-results
    mode (§8.1 — reuse `apiCall('places_search', ...)`, same logic as `PlacePicker`'s search mode,
    factor out shared matching/highlight logic into a small shared utility module if that avoids
    duplicating the Rust-mirrored comparator/highlight code — do not literally duplicate the full
    `PlacePicker` component). Create `PlaceTreeNode.svelte`: 32px row per UI-SPEC §8.2's exact
    layout (chevron 16×16 `aria-hidden="true"` with empty-slot-for-leaves, name in `--tr-text-body`/
    `-body-strong` when selected/`-tertiary` when archived, "Склад"/"Архив" badges, right-aligned
    `.tr-mono` counter omitted at zero, `ActionMenu variant="ghost-sm"` visible on hover/focus-within
    and permanently for the selected row, with the mandatory per-row `label`). Wire `ActionMenu`'s
    items per UI-SPEC §8.3 order (Переименовать · Создать вложенное место · Переместить в… ·
    Архивировать/Вернуть из архива · Удалить) to open `PlaceFormModal`/`PlaceMoveModal` (Plan 19) or
    call the corresponding `places_archive`/`places_unarchive`/`places_delete` API directly (delete
    opens a lightweight confirm — either reuse `PlaceFormModal` with a `mode="delete"` variant if
    Plan 19's component supports extension, or add a minimal inline confirm here; UI-SPEC §11.5's
    copy is the source of truth either way). Implement HTML5 native drag-n-drop (Admin only): dragging
    a row over another shows the valid/invalid-target styling from UI-SPEC §8.4, dropping ALWAYS
    opens `PlaceMoveModal` pre-filled with the drop target as the new parent (never a silent move —
    UI-SPEC §8.4/§17.4's explicit rule), and a "В корень дерева" drop zone appears at list-end during
    a drag (D-03).
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>Tree renders with correct depth/sort/counter behavior; full keyboard map works; drag-n-drop
  and the "Переместить в…" menu item both route through the same `PlaceMoveModal` confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Drag-n-drop / ActionMenu mutation triggers | All routed through the same `PlaceMoveModal`/`PlaceFormModal` components (Plan 19), which call the server-validated `places_*` mutation endpoints (Plan 05/12) — no client-side-only tree reordering is ever persisted without a server round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-14-01 | Elevation of Privilege | `ActionMenu`/drag-n-drop rendering for a Manager | accept (mitigated upstream) | `ActionMenu` is only rendered when the current role is Admin (client-side UX hiding, per D-20's documented convention); the actual boundary is the server-side `MutatePlaces` gate (Plan 05/12), which rejects the underlying call regardless |
| T-39-14-02 | Denial of Service | Fetching the entire tree client-side on every load (`places_list_all`) | accept | Confirmed real scale (~300 place rows, RESEARCH.md) makes a single full-tree fetch trivial; no pagination needed at this data size |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors. Full interactive verification (keyboard,
drag-drop, role gating for Manager vs Admin, Tauri + LAN browser parity) happens in Plan 20's
end-of-wave checkpoint once the right panel exists to complete the screen.
</verification>

<success_criteria>
`/places` is reachable, sidebar-gated correctly for Admin/Manager (hidden for Employee); the tree
implements D-05 sorting, D-25 counters, D-21 drag-n-drop-always-confirms, and full UI-SPEC §8.5
keyboard/ARIA parity.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-14-SUMMARY.md` when done
</output>
