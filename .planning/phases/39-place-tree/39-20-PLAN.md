---
phase: 39-place-tree
plan: 20
type: execute
wave: 9
depends_on: ["39-14"]
files_modified:
  - ui/src/features/places/PlaceContents.svelte
  - ui/src/features/places/PlacesPage.svelte
autonomous: false
requirements: [PLC-06]

must_haves:
  truths:
    - "Правая панель показывает одним списком всё, что размещено в выбранном узле и во вложенных местах, с колонкой «Тип» — устройство/принтер/картридж (D-23, PLC-06)"
    - "Содержимое — ОДНА таблица с колонкой «Тип» и вкладками-фильтрами, а не отдельные таблицы под каждую сущность; структура готова к пятой вкладке «АРМ» в Фазе 41 без переделки экрана (D-23)"
    - "Тумблер «Только здесь» по умолчанию выключен (показываются вложенные); при включении колонка «Место» скрывается, а список сужается до самого узла (D-24)"
    - "Ошибка удаления непустого узла (D-14) предлагает кнопку «Показать содержимое», которая выделяет узел в дереве, включает показ вложенных и закрывает модалку — переход происходит без перезагрузки страницы"
  artifacts:
    - path: "ui/src/features/places/PlaceContents.svelte"
      provides: "breadcrumbs header + Tabs + Table content screen per UI-SPEC §9"
      min_lines: 100
---

<objective>
Build the right-panel content screen (`PlaceContents.svelte`) — breadcrumbs, type-filtered tabs,
sticky-header table with the D-26 short-path column, and the D-24 "Только здесь" toggle. Wire it
into `PlacesPage`'s detail slot, completing the "Места" section end-to-end. Close with a manual
checkpoint verifying the full screen's keyboard/ARIA/drag-drop parity in both Tauri and a LAN
browser, per the project's "synthetic harness ≠ real verification" rule.

Purpose: this is the last piece of PLC-06 and the last UI plan before final cleanup (Plan 21).
Output: `PlaceContents.svelte`, wired into `PlacesPage.svelte`'s detail slot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlaceContents.svelte — breadcrumbs, tabs, table, only-here toggle</name>
  <files>ui/src/features/places/PlaceContents.svelte</files>
  <read_first>
    39-UI-SPEC.md §9.1-§9.3 (header/breadcrumbs, tabs+toggle panel, table column contract — D-26's
    short-path-with-title column, hidden when "Только здесь" is on), §14.2 (empty-state copy for
    "content pusto with nested shown" vs "content pusto with only-here on"), ui/src/lib/components/Tabs.svelte
    and Table.svelte (prop signatures — `variant="underline"`, `fillHeight`, `framed`).
  </read_first>
  <behavior>
    - Test/manual: header row 1 renders clickable breadcrumb segments (each selects that ancestor in
      the tree); row 2 renders the selected node's name + badges + type label.
    - Test/manual: Tabs render "Все · Устройства · Принтеры · Картриджи" with per-tab counts;
      switching tabs filters the table client-side (data already fetched once per node/toggle state)
      or refetches with a type filter — either is acceptable, but the displayed counts must match
      the actual filtered row count.
    - Test/manual: toggling "Только здесь" on removes the "Место" column and refetches with
      `nested=false`; toggling off restores both.
    - Test/manual: an empty node with nested content shown displays "В этом месте пусто / Здесь и во
      вложенных местах ничего не размещено."; the same empty node with "Только здесь" on displays
      "Здесь ничего нет / Содержимое вложенных мест скрыто..." (exact UI-SPEC §14.2 copy pair).
  </behavior>
  <action>
    Create `PlaceContents.svelte` with props `{ place: PlaceDto, onSelectAncestor: (id: number) =>
    void }`. Render the §9.1 header (breadcrumbs from `place`'s ancestor chain — either included in
    the `PlaceDto`/`places_get` response or derived by splitting `full_path`, confirm which via Plan
    12's DTO shape before assuming; each segment's click calls `onSelectAncestor`). Render the §9.2
    control row (`Tabs variant="underline"` + `Checkbox` "Только здесь", default unchecked). Fetch
    `apiCall('places_contents', { rootId: place.id, nested: !onlyHere })` whenever `place.id` or
    `onlyHere` changes. Render the §9.3 `Table` with `fillHeight framed={false}`, sticky `thead`,
    columns Тип/Название/Инв.№ или Серийный №/Место (hidden when `onlyHere`)/Статус, each row
    clickable → navigates to that entity's own detail page/route (use whatever existing
    entity-detail navigation helper `DeviceListRow`/`CartridgeListRow` already use). Implement both
    empty-state copy pairs from UI-SPEC §14.2 exactly.
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>Breadcrumbs, tabs with correct counts, only-here toggle, and both empty-state copy variants
  all match UI-SPEC §9/§14.2 exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PlaceContents into PlacesPage detail slot + D-14 "Показать содержимое" navigation</name>
  <files>ui/src/features/places/PlacesPage.svelte</files>
  <read_first>
    ui/src/features/places/PlacesPage.svelte (as left by Plan 14 — locate the placeholder detail slot
    and the deep-link hash handling), 39-UI-SPEC.md §11.5 ("Показать содержимое" button behavior:
    selects the node in the tree, turns on nested-content display, closes the delete-blocked modal —
    all three in one click, no page reload).
  </read_first>
  <action>
    Replace `PlacesPage`'s detail-slot placeholder with `PlaceContents` bound to the currently
    selected tree node (or the "Место не выбрано" empty state per UI-SPEC §14.2 when nothing is
    selected). Wire `onSelectAncestor` to update the same selection state the tree uses, keeping the
    deep-link hash (`#/places?id=…`) in sync in both directions (tree click → hash update; hash
    present on load → tree pre-selection, already partially built by Plan 14). Wire the delete-
    blocked error modal's "Показать содержимое" button (built in Plan 14's `PlaceTreeNode` delete
    flow, or added here if Plan 14 left it as a stub) to: select the failed-delete node, set
    `onlyHere = false` (ensures nested content — including whatever blocked the delete — is visible),
    and close the modal, all without a page reload.
  </action>
  <verify>
    <automated>grep -n "PlaceContents" ui/src/features/places/PlacesPage.svelte && grep -n "Показать содержимое" ui/src/features/places/PlacesPage.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -n "PlaceContents" ui/src/features/places/PlacesPage.svelte` confirms it replaces the
      Plan 14 placeholder
    - `grep -n "Показать содержимое" ui/src/features/places/PlacesPage.svelte` (or `PlaceTreeNode.svelte`
      if the button lives there) confirms the D-14 navigation action exists and is wired, not a dead
      button
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: The complete "Места" section: routing, sidebar, tree (left panel), content scree</name>
  <action>No code changes in this task — see &lt;how-to-verify&gt; for the manual verification steps to perform.</action>
  <what-built>The complete "Места" section: routing, sidebar, tree (left panel), content screen
  (right panel), all four mutation modals, drag-n-drop, keyboard/ARIA contract, role gating for
  Admin/Manager/Employee.</what-built>
  <how-to-verify>
    1. Run `cargo tauri dev` as Admin. Navigate to «Места». Build a small invented tree (e.g.
       «Здание А» → «2 этаж» [level=2] → «214»; also add a floor with level=0 and one with a
       negative level to confirm PLC-02's sort-by-level-not-name behavior).
    2. Select a node — confirm the right panel shows breadcrumbs, tabs with counts, and (if you
       assigned a device/cartridge to that place via its form) the item in the "Все" tab.
    3. Toggle "Только здесь" — confirm the "Место" column disappears and nested items drop out.
    4. Try to delete a non-empty node — confirm the exact-count error and "Показать содержимое"
       button work (selects the node, shows nested content, closes the modal).
    5. Drag one node onto another — confirm the consequences dialog appears before anything moves;
       cancel it — confirm nothing changed.
    6. Full keyboard pass: Tab into the tree, arrow-navigate, expand/collapse with →/←, F2 to rename,
       Escape to clear search.
    7. Log in as Manager — confirm «Места» is visible but no create button / no ActionMenu / no
       drag-n-drop; confirm Manager CAN still select nodes and see content.
    8. Log in as Employee (or check the sidebar config) — confirm «Места» does not appear at all.
    9. Run `pnpm --dir ui build`, open the same section from a LAN browser tab pointed at the running
       server, repeat steps 2-4 there (WKWebView/WebView2 vs browser parity).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PlaceContents row navigation → entity detail pages | Reuses each entity's existing (already-authorized) detail route — no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-20-01 | Information Disclosure | Content screen exposing devices/cartridges a Manager should not see | accept | `places_contents` is gated by `ReadPlaces` (Admin|Manager, Plan 12) — same visibility Manager already has for devices/cartridges lists elsewhere in the app; no new disclosure surface |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors. Manual checkpoint above is the primary
verification for this plan (UI-SPEC keyboard/ARIA/drag-drop behavior is not automatable per this
project's documented harness limitation).
</verification>

<success_criteria>
The full "Места" section works end-to-end for Admin and Manager, is invisible to Employee, and
passes the manual keyboard/ARIA/drag-drop checkpoint in both Tauri and LAN-browser environments.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-20-SUMMARY.md` when done
</output>
