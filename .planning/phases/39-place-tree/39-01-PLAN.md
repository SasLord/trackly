---
phase: 39-place-tree
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V037__places.sql
  - migrations/V038__places_migrate_devices_acts_cartridges.sql
  - crates/trackly-infra/tests/migration_idempotency.rs
autonomous: true
requirements: [PLC-01, PLC-02, PLC-04]

must_haves:
  truths:
    - "Место любого из шести типов (D-02) может существовать без родителя (D-03) и без указанного этажа/уровня"
    - "Два места с одинаковым именем у одного родителя невозможны, но одинаковые имена под разными родителями — валидны (D-04)"
    - "Этаж хранит level как INTEGER без ограничения на неотрицательность — 0 и отрицательные значения проходят миграцию и вставку (PLC-02)"
    - "is_storage — булев столбец на обычном узле places (обычно тип «помещение»), а не седьмой тип узла и не отдельная таблица (D-08); вложенный склад внутри кабинета оформляется отдельным дочерним узлом с is_storage=true, а не флагом на самом кабинете — схема не мешает этому паттерну (D-09)"
    - "После миграции таблица locations и колонки devices.location_id / cartridges.location / acts.location_id физически отсутствуют в схеме (PLC-04); данные размещения НЕ переносятся — обнуляются, как подтверждено дважды пользователем в CONTEXT.md (Deferred Ideas) — миграция схемная, не ETL"
    - "place_full_paths — VIEW, не денормализованная колонка: переименование узла места отражается в результате SELECT из view без каких-либо доп. шагов (PLC-05 фундамент, RESEARCH Pitfall 1)"
  artifacts:
    - path: "migrations/V037__places.sql"
      provides: "places table (adjacency list) + place_full_paths recursive-CTE view + UNIQUE(parent,name) index"
      contains: "CREATE TABLE places"
    - path: "migrations/V038__places_migrate_devices_acts_cartridges.sql"
      provides: "place_id/bulk_place_id/place_path_snapshot columns on devices/acts/act_items/cartridges, DROP TABLE locations, cartridges_fts trigger redefinition without location"
      contains: "DROP TABLE locations"
  key_links:
    - from: "migrations/V038__places_migrate_devices_acts_cartridges.sql"
      to: "migrations/V037__places.sql"
      via: "FK devices.place_id REFERENCES places(id)"
      pattern: "REFERENCES places\\(id\\)"
---

<objective>
Create the `places` adjacency-list schema (V037) and migrate every existing location-bearing
table onto `place_id` while dropping `locations` entirely (V038). This is the foundation every
other Phase 39 plan builds on — no data migration is performed (locked decision, confirmed twice
in CONTEXT.md and REQUIREMENTS.md "Out of Scope": app is pre-production, placement values are
zeroed out; migration is schema-only DROP+CREATE, not ETL).

Purpose: give every downstream plan (domain types, repo, service, all entity migrations, UI) a
stable, already-migrated schema to build against.
Output: `migrations/V037__places.sql`, `migrations/V038__places_migrate_devices_acts_cartridges.sql`,
extended `crates/trackly-infra/tests/migration_idempotency.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-RESEARCH.md
@.planning/phases/39-place-tree/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write V037__places.sql (places table + place_full_paths view)</name>
  <files>migrations/V037__places.sql</files>
  <read_first>
    migrations/V002__core_entities.sql (locations table — standard4 column convention to mirror),
    migrations/V004__acts.sql (ON DELETE RESTRICT precedent on location_id/parent_act_id),
    migrations/V012__indexes_and_fts.sql and migrations/V013__devices_fts_triggers.sql (partial-index
    WHERE deleted_at_utc IS NULL convention), 39-RESEARCH.md Architecture Patterns > Pattern 1 (full
    SQL already drafted there — use verbatim, do not re-derive).
  </read_first>
  <action>
    Create `migrations/V037__places.sql` exactly per 39-RESEARCH.md Pattern 1: `places` table with
    columns `id INTEGER PRIMARY KEY AUTOINCREMENT`, `parent_id INTEGER NULL REFERENCES places(id) ON
    DELETE RESTRICT`, `kind TEXT NOT NULL CHECK (kind IN ('territory','zone','building','floor','room','outdoor'))`
    (D-02's six closed values — English enum tags, Russian labels live in the domain layer per
    the `PlaceKind`/`AutocompleteField` `from_str`/`as_str` convention from PATTERNS.md), `name TEXT
    NOT NULL`, `level INTEGER NULL` (no CHECK constraint — must accept 0 and negative, PLC-02),
    `is_storage INTEGER NOT NULL DEFAULT 0`, `sort_order INTEGER NULL`, `archived_at_utc INTEGER
    NULL`, `notes TEXT NULL`, plus the `standard4` columns (`created_at_utc INTEGER NOT NULL`,
    `updated_at_utc INTEGER NOT NULL`, `deleted_at_utc INTEGER NULL`, `version INTEGER NOT NULL
    DEFAULT 1`). Add `CREATE UNIQUE INDEX idx_places_parent_name_unique ON places(COALESCE(parent_id,
    0), name) WHERE deleted_at_utc IS NULL` (D-04) and `CREATE INDEX idx_places_parent ON
    places(parent_id) WHERE deleted_at_utc IS NULL`. Add the `place_full_paths` VIEW using `WITH
    RECURSIVE path_cte` exactly as drafted in RESEARCH.md Pattern 1 (root-to-leaf, ` / `-joined,
    `SELECT id AS place_id, path AS full_path FROM path_cte WHERE parent_id IS NULL`). End with
    `PRAGMA user_version = 37;`. Do not add any place text to FTS5 tables in this migration (see
    Common Pitfall 1 — that comes nowhere in this phase).
  </action>
  <verify>
    <automated>cargo test -p trackly-infra --test migration_idempotency</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `migrations/V037__places.sql` exists and contains `CREATE TABLE places`, `CREATE VIEW
      place_full_paths`, and `PRAGMA user_version = 37;`
    - The `kind` CHECK constraint lists exactly six values: territory, zone, building, floor, room, outdoor
    - `level` column has no CHECK/NOT NULL constraint restricting sign (grep confirms no `level.*CHECK`)
    - `cargo test -p trackly-infra --test migration_idempotency` passes after this task (refinery
      auto-discovers the new file by filename convention; no manual registration step exists in this
      codebase — confirm by reading how V036 was picked up, no separate registry file)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write V038 (place_id migration, drop locations, redefine cartridges_fts triggers)</name>
  <files>migrations/V038__places_migrate_devices_acts_cartridges.sql</files>
  <read_first>
    migrations/V005__cartridges.sql (cartridges.location freeform TEXT + comment "locations table is
    for devices" — the exact historical decision being reversed here),
    migrations/V016__cartridges_kind_color_settings.sql (cartridges_fts_ai/ad/au trigger definitions,
    lines ~52-77, including the `location` FTS5 column — must be redefined WITHOUT it),
    migrations/V013__devices_fts_triggers.sql (idx_devices_autocomplete_name_location — to be dropped),
    39-RESEARCH.md Architecture Patterns > Pattern 1 (V038 SQL already drafted, use as the base).
  </read_first>
  <action>
    Create `migrations/V038__places_migrate_devices_acts_cartridges.sql`. Add columns: `ALTER TABLE
    devices ADD COLUMN place_id INTEGER NULL REFERENCES places(id) ON DELETE RESTRICT`; `ALTER TABLE
    cartridges ADD COLUMN place_id INTEGER NULL REFERENCES places(id) ON DELETE RESTRICT`; `ALTER
    TABLE acts ADD COLUMN place_id INTEGER NULL REFERENCES places(id) ON DELETE RESTRICT`; `ALTER
    TABLE acts ADD COLUMN bulk_place_id INTEGER NULL REFERENCES places(id) ON DELETE RESTRICT`;
    `ALTER TABLE acts ADD COLUMN place_path_snapshot TEXT NULL` (D-16, act-level granularity per the
    2026-08-22 clarification — NOT on `act_items`); `ALTER TABLE act_items ADD COLUMN
    place_id_override INTEGER NULL REFERENCES places(id) ON DELETE RESTRICT`. Drop old columns:
    `ALTER TABLE devices DROP COLUMN location_id`; `ALTER TABLE cartridges DROP COLUMN location`;
    `ALTER TABLE acts DROP COLUMN location_id`. Drop obsolete indexes: `DROP INDEX
    idx_devices_location`; `DROP INDEX idx_devices_autocomplete_name_location`. Create replacements:
    `CREATE INDEX idx_devices_place ON devices(place_id) WHERE deleted_at_utc IS NULL AND place_id IS
    NOT NULL`; `CREATE INDEX idx_cartridges_place ON cartridges(place_id) WHERE deleted_at_utc IS NULL
    AND place_id IS NOT NULL`. `DROP TABLE locations;` (PLC-04). Redefine the three `cartridges_fts_*`
    triggers (`DROP TRIGGER cartridges_fts_ai/ad/au` then `CREATE TRIGGER` with the exact V016 body
    minus the `location` column from both the column list and the VALUES list), then `INSERT INTO
    cartridges_fts(cartridges_fts) VALUES('rebuild');` to purge stale `location` tokens from the FTS5
    index. Add no new place-related column to `devices_fts`/`cartridges_fts` (place search resolves
    live via `place_full_paths`, per RESEARCH Common Pitfall 1 — this is the single biggest trap in
    this phase, do not add it "for completeness"). End with `PRAGMA user_version = 38;`.
  </action>
  <verify>
    <automated>grep -c "cartridges_fts.*location\|location.*cartridges_fts" migrations/V038*.sql && sqlite3 :memory: < migrations/V037__places.sql migrations/V038*.sql</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `migrations/V038__places_migrate_devices_acts_cartridges.sql` exists, contains `DROP TABLE
      locations;` and `PRAGMA user_version = 38;`
    - `grep -c "cartridges_fts.*location\|location.*cartridges_fts" migrations/V038*.sql` returns 0
      after the rebuild statement (no lingering `location` column reference in the redefined triggers)
    - No `place_path` or similar column is added to `devices_fts`/`cartridges_fts` CREATE VIRTUAL
      TABLE statements anywhere in this migration (grep confirms `ALTER.*fts.*place\|CREATE.*fts.*place`
      returns 0 matches in this file)
    - `sqlite3 :memory: < migrations/V037__places.sql migrations/V038*.sql` (or the project's existing
      migration test harness) applies cleanly on top of V001-V036 with zero errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend migration_idempotency.rs to assert schema-only migration correctness</name>
  <files>crates/trackly-infra/tests/migration_idempotency.rs</files>
  <read_first>
    crates/trackly-infra/tests/migration_idempotency.rs (existing 61-line file, full read — understand
    the `migrations::max_known_version()` dynamic-count pattern before adding assertions),
    crates/trackly-infra/src/migrations.rs or equivalent runner module (locate via grep for
    `max_known_version` to find the exact refinery invocation this test drives).
  </read_first>
  <behavior>
    - Test: after running all migrations on a fresh in-memory/tempfile DB, `SELECT name FROM
      sqlite_master WHERE type='table' AND name='locations'` returns zero rows (table gone, PLC-04).
    - Test: `PRAGMA table_info(devices)` contains no row with `name = 'location_id'`, and contains a
      row with `name = 'place_id'`.
    - Test: `PRAGMA table_info(cartridges)` contains no row with `name = 'location'`, and contains a
      row with `name = 'place_id'`.
    - Test: `PRAGMA table_info(acts)` contains rows for `place_id`, `bulk_place_id`,
      `place_path_snapshot`, and no row for `location_id`.
    - Test: `PRAGMA table_info(act_items)` contains a row for `place_id_override`.
    - Test: `SELECT sql FROM sqlite_master WHERE type='view' AND name='place_full_paths'` returns
      exactly one row (view exists) — this is the schema-only "existing DB upgrades without crashing"
      guarantee (VALIDATION.md manual-verification concern); this automated test is the safety net
      that a pre-Phase-39 fixture DB (all V001-V036 applied) upgrades cleanly to V038 without error,
      independent of whether any location data existed.
  </behavior>
  <action>
    Add a new `#[test]` function (or extend the existing idempotency test with additional assertion
    blocks, matching the file's existing style) that opens a connection, runs the full migration
    chain (reusing whatever helper the existing test already calls — do not hand-roll a second
    migration runner), and asserts every point in the Behavior list above via `PRAGMA table_info`
    and `sqlite_master` queries. Do not hardcode a migration-count assertion for V037/V038 — the file
    already avoids that per the `max_known_version()` dynamic pattern; only add schema-presence
    assertions.
  </action>
  <verify>
    <automated>cargo test -p trackly-infra --test migration_idempotency</automated>
  </verify>
  <done>All new assertions pass against the full V001-V038 migration chain; `locations` table and
  `location_id`/`location` columns are confirmed absent; `place_id`/`bulk_place_id`/
  `place_path_snapshot`/`place_id_override`/`place_full_paths` are confirmed present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Migration file → SQLite schema | Static SQL, no runtime user input — low risk surface, but a malformed FK/index could silently weaken PLC-04's guarantee |
| Existing portable DB → V037/V038 upgrade | An operator's real (pre-Phase-39) DB file runs this migration on next app launch — must not corrupt or fail to open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Tampering | `place_full_paths` view / recursive CTE | accept | View is read-only, driven entirely by trusted `places` rows written through the (Admin-gated, later plan) service layer — no user input reaches this migration directly |
| T-39-01-02 | Information Disclosure | `DROP TABLE locations` data loss | accept | Explicitly the locked decision (confirmed twice, REQUIREMENTS.md Out of Scope) — not a defect; documented in must_haves.truths |
| T-39-01-03 | Denial of Service | Migration failure on an existing large/locked portable DB | mitigate | Task 3's schema-presence assertions run against the full V001-V038 chain on every CI run, catching a broken migration before it reaches a real operator's DB |
</threat_model>

<verification>
`cargo test -p trackly-infra --test migration_idempotency` green. Manual: copy a scratch pre-Phase-39
fixture DB (fixture must contain no real org data — invented rows only, e.g. "Здание А"/"Иванов
И.И." per project privacy rule) and confirm the built app opens it without error after this plan
lands (full end-to-end confirmation happens in Plan 21's manual checkpoint, once the rest of the
stack compiles against the new schema).
</verification>

<success_criteria>
V037 and V038 exist, apply cleanly on top of V001-V036, `locations` table and all `location_id`/
`location` columns are gone, `place_id`/`place_path_snapshot`/`place_id_override` columns and the
`place_full_paths` view exist, and `migration_idempotency.rs` proves this automatically.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-01-SUMMARY.md` when done
</output>
