---
phase: 39-place-tree
plan: 17
type: execute
wave: 7
depends_on: ["39-12", "39-13", "39-11", "39-09"]
files_modified:
  - ui/src/features/acts/ActFormBody.svelte
  - ui/src/features/acts/ReturnModal.svelte
  - ui/src/features/acts/ReturnItemsTable.svelte
  - ui/src/features/acts/returnPayload.ts
  - ui/src/features/acts/ActDetail.svelte
  - ui/src/features/acts/ActFormItemsTable.svelte
autonomous: true
requirements: [PLC-03, PLC-04]

must_haves:
  truths:
    - "Форма акта, форма массового возврата и таблица возврата по позициям выбирают место через PlacePicker вместо текстовых полей location_name/bulk_location_name/location_name_override"
    - "Печатная форма акта продолжает получать снимок пути (place_path_snapshot), сформированный на бэкенде в момент выписки — фронтенд не формирует и не отправляет текст пути, только place_id"
    - "Форма массового возврата (ReturnModal) поднимает складские места наверх списка через quick-pick-строку над PlacePicker и подставляет одно из них по умолчанию, используя list_storage_place_ids (D-11.1)"
    - "ActDetail.svelte показывает act.full_path (переименовано с act.location Планом 07) в карточке акта; ActFormItemsTable.svelte строит DeviceFilter.place_id (переименовано с location_id Планом 06) при подборе устройств «на складе» для позиций акта"
  artifacts:
    - path: "ui/src/features/acts/ActFormBody.svelte"
      provides: "place_id field bound to PlacePicker replacing location_name"
      contains: "PlacePicker"
    - path: "ui/src/features/acts/ReturnItemsTable.svelte"
      provides: "per-row place_id_override via PlacePicker"
      contains: "PlacePicker"
    - path: "ui/src/features/acts/ReturnModal.svelte"
      provides: "D-11.1 storage-places quick-pick row + default preselection above PlacePicker"
      contains: "cartridge_storage_place_ids"
---

<objective>
Wire `PlacePicker` into the act-family surfaces: the act create/edit form (own `place_id`), the
bulk-return modal (`bulk_place_id`), and the per-row return-items table (`place_id_override`).

Purpose: closes the act side of D-17 — parallel to Plans 15/16/18.
Output: `ActFormBody.svelte`, `ReturnModal.svelte`, `ReturnItemsTable.svelte` migrated onto
`PlacePicker`/`place_id`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-place-tree/39-CONTEXT.md
@.planning/phases/39-place-tree/39-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActFormBody.svelte — own place_id</name>
  <files>ui/src/features/acts/ActFormBody.svelte</files>
  <read_first>
    ui/src/features/acts/ActFormBody.svelte (grep-confirmed: line 10 `import LocationAutocomplete`,
    line 85 `let location = $state(...)`, lines 176-177/207-210 payload construction sending
    `location_id: null, location_name: location.trim()...` — a stale TODO-commented pattern per line
    207's own comment "location_id wiring TODO: Phase 2 currently stores location as text", now fully
    resolved by this phase, lines 324-329 the field markup).
  </read_first>
  <action>
    Replace `let location = $state(...)` with `let placeId = $state&lt;number | null&gt;(isEditPrefill ?
    (initialAct!.placeId ?? null) : null)`. Replace both payload-construction blocks (~176-177,
    ~207-210) with `place_id: placeId` (drop the `location_id`/`location_name` pair and the stale
    "TODO: Phase 2" comment entirely — the TODO is resolved by this phase, remove it rather than
    updating it). Replace the field markup at ~324-329 (label "Расположение" →
    label="Место"; `LocationAutocomplete` → `PlacePicker value={placeId} id="act-place"
    onChange={(id) => (placeId = id)}`).
  </action>
  <verify>
    <automated>grep -c "LocationAutocomplete\|location_id\|location_name\|location =" ui/src/features/acts/ActFormBody.svelte && grep -c "PlacePicker\|placeId" ui/src/features/acts/ActFormBody.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "LocationAutocomplete\|location_id\|location_name\|location =" ui/src/features/acts/ActFormBody.svelte`
      returns 0
    - `grep -c "PlacePicker\|placeId" ui/src/features/acts/ActFormBody.svelte` returns at least 3
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ReturnModal.svelte — bulk_place_id</name>
  <files>ui/src/features/acts/ReturnModal.svelte</files>
  <read_first>
    ui/src/features/acts/ReturnModal.svelte (grep-confirmed: line 32 `import LocationAutocomplete`,
    lines 109/119/153 `locationOverrideName` row-state initialization referencing `it.device_location`
    — note this device field is now `devicePlace`/`device_place` per Plan 06's domain rename, confirm
    exact DTO field name before wiring, lines 184-233 the `applyToAll`/`bulk_location_name` validation
    and payload-construction logic, line 383 the field markup). For D-11.1 (склад поднят наверх / выбран по умолчанию):
    `list_storage_place_ids` (Plan 04) is currently exposed to the frontend ONLY as the
    cartridge-scoped `cartridge_storage_place_ids` Tauri command / `POST
    /api/v1/cartridge_storage_place_ids` (Plan 09) — it returns the exact same global storage-place-id
    set D-11 names for all three of its effects, it is simply reached through a cartridge-prefixed
    command name; confirmed via `crates/trackly-core/src/auth.rs`'s permission matrix that this
    command's gate (`Action::ReadData`, Admin|Manager) and `ReturnModal`'s own gate
    (`Action::MutateActs`, Admin|Manager) are the SAME role tier, so reusing it here introduces no
    access-control gap. `places_get` (Plan 12, `Action::ReadPlaces`, also Admin|Manager) returns a
    `PlaceDto` with `full_path: String` (snake_case, no `rename_all` — per Plan 05) for resolving each
    storage id's display text. `PlacePicker`'s `Props` interface (Plan 13) is fixed at `value`/
    `onChange`/`id?`/`disabled?`/`invalid?` — it has no built-in "priority" or "pinned" list, so
    "поднято наверх списка" is implemented in THIS file as a quick-pick row rendered above the
    `PlacePicker` field, not by reordering the tree/search results inside `PlacePicker` itself (that
    would change behavior for every other `PlacePicker` consumer in the app, out of this plan's
    scope).
  </read_first>
  <action>
    Replace every `bulk_location_name`/`bulk_location_id` reference with `bulk_place_id: number |
    null` throughout this file's state and payload construction (~184-276). Replace the row-state
    initialization at lines 109/119/153 to read `it.devicePlace` (or whatever Plan 06 actually named
    the resolved-path field — confirm via `dto/device.rs` before finalizing) — but note per-row state
    in THIS modal only needs a place_id/text display, not the id, for the "applyToAll" bulk case;
    per-row overrides are `ReturnItemsTable.svelte`'s scope (Task 3, this file only handles the bulk
    "apply to all" path). Replace the field markup at line 383 with `PlacePicker value={bulkPlaceId}
    onChange={(id) => (bulkPlaceId = id)}` labeled "Место" (unified term). Update the
    "hard-require condition, location may be empty" comment (~line 186-189) to reference `place`
    instead of `location` if its logic is preserved unchanged (bulk place remains optional, matching
    the pre-existing UX rule — only rename the terminology, do not change the requiredness rule). Implement D-11.1: on modal
    open, fetch `storageIds = await apiCall&lt;number[]&gt;('cartridge_storage_place_ids', {})`
    (confirm the exact zero-argument call shape against Plan 09's registered command before wiring);
    for each id (typically 4-5 in this org's real tree, per CONTEXT.md's confirmed scale — no
    pagination needed), resolve display text via `apiCall('places_get', { id })`'s `full_path`. Render
    these as a row of quick-select chips/buttons ABOVE the `PlacePicker` field (label "Складские
    места" or equivalent, styled as compact `Badge`/chip buttons per this codebase's existing
    chip-button convention — match whatever pattern `ActFormBody.svelte`/other modals in this feature
    already use for a small clickable-tag row), each calling the same `onChange={(id) => (bulkPlaceId
    = id)}` handler `PlacePicker` uses — clicking a chip sets `bulkPlaceId` exactly like selecting
    that node in `PlacePicker` would. Preselect a default: if `bulkPlaceId` is still unset when
    `storageIds` resolves (i.e. no printer/device-derived default already populated it), set
    `bulkPlaceId = storageIds[0] ?? null` — the user can still override via the chips or `PlacePicker`
    itself (this is a suggested default, not a forced value, matching D-11.3's "предложение, не
    принуждение" framing extended to this sibling effect). If `storageIds` is empty (no storage places
    exist yet in this org's tree), render nothing extra — `PlacePicker` alone remains fully functional.
  </action>
  <verify>
    <automated>grep -c "LocationAutocomplete\|bulk_location\|locationOverrideName" ui/src/features/acts/ReturnModal.svelte && grep -c "PlacePicker\|bulk_place_id\|bulkPlaceId" ui/src/features/acts/ReturnModal.svelte && grep -c "cartridge_storage_place_ids\|storageIds" ui/src/features/acts/ReturnModal.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "LocationAutocomplete\|bulk_location\|locationOverrideName" ui/src/features/acts/ReturnModal.svelte`
      returns 0
    - `grep -c "PlacePicker\|bulk_place_id\|bulkPlaceId" ui/src/features/acts/ReturnModal.svelte`
      returns at least 3
    - `grep -c "cartridge_storage_place_ids\|storageIds" ui/src/features/acts/ReturnModal.svelte`
      returns at least 2 (D-11.1's storage-places-first quick-pick + default preselection)
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: ReturnItemsTable.svelte — per-row place_id_override</name>
  <files>ui/src/features/acts/ReturnItemsTable.svelte</files>
  <read_first>
    ui/src/features/acts/ReturnItemsTable.svelte (grep-confirmed: line 14 `import
    LocationAutocomplete`, line 30 `locationOverrideName: string` row-type field, line 56 the
    `next` array-update helper, line 66 `&lt;div class="th col-location"&gt;Расположение&lt;/div&gt;`, line
    73 `locOverridden` computed flag, lines 112-122 the per-row `LocationAutocomplete` markup, line
    178 `.col-location` CSS class), ui/src/features/acts/returnPayload.ts (full file, 114 lines —
    CONFIRMED this task must edit it: `buildReturnItems()`'s composite-key grouping function reads
    `r.locationOverrideName.trim()` as part of its group key at line 80, and both the `applyToAll`
    and per-row-override branches construct `location_id_override: null` /
    `location_name_override: ...` on the returned `ActReturnItemDto` objects at lines 72-73 and
    110-111 — every one of these must become `place_id_override`).
  </read_first>
  <action>
    Rename the row-type field `locationOverrideName: string` to `placeIdOverride: number | null`.
    Update the `next` array-update helper and the `locOverridden` computed flag to check `placeIdOverride
    !== null` instead of a non-empty string. Replace the column header at line 66 ("Расположение" →
    "Место", `.col-location` → `.col-place`, update the CSS class throughout the `&lt;style&gt;` block).
    Replace the per-row markup (~112-122) with `PlacePicker value={row.placeIdOverride}
    onChange={(id) => updateRow(idx, { placeIdOverride: id })}`. In `returnPayload.ts`, update
    `buildReturnItems()`'s composite-key function (line ~79-80) to key on `r.placeIdOverride` (e.g.
    `String(r.placeIdOverride ?? '')`) instead of `r.locationOverrideName.trim()`; update the
    `applyToAll` branch's constructed items (~lines 66-74) to emit `place_id_override: null` only
    (drop `location_id_override`/`location_name_override` entirely — the backend DTO carries a
    single `place_id_override: Option&lt;i64&gt;` field, no separate name field, per Plan 07/11's
    domain rename); update the per-row-override branch (~lines 90-112) to carry the group's
    `placeIdOverride` value through and emit `place_id_override: g.pid` (renaming the local group
    struct's `loc: string` field to `pid: number | null` accordingly) instead of
    `location_id_override: null, location_name_override: g.loc...`. Update the file's leading
    JSDoc `@example` blocks to use `placeIdOverride`/`place_id_override` instead of
    `locationOverrideName`/`location_name_override` so the documentation matches the new shape. Note
    in the SUMMARY that this file's `ActReturnItemDto` type (imported from `../../bindings`) only
    reflects `place_id_override` once the backend DTO (`crates/trackly-app/src/dto/act.rs`) is
    renamed — if that backend rename has not landed yet when this task runs, `pnpm --dir ui build`
    will fail on the bindings mismatch; this is the same cross-plan compile-dependency pattern
    already documented elsewhere in this phase (e.g. Plan 06's verification notes), not a defect in
    this task.
  </action>
  <verify>
    <automated>grep -c "LocationAutocomplete\|locationOverrideName\|col-location" ui/src/features/acts/ReturnItemsTable.svelte && grep -c "PlacePicker\|placeIdOverride" ui/src/features/acts/ReturnItemsTable.svelte && grep -c "location_id_override\|location_name_override\|locationOverrideName" ui/src/features/acts/returnPayload.ts && grep -c "place_id_override" ui/src/features/acts/returnPayload.ts && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "LocationAutocomplete\|locationOverrideName\|col-location" ui/src/features/acts/ReturnItemsTable.svelte`
      returns 0
    - `grep -c "PlacePicker\|placeIdOverride" ui/src/features/acts/ReturnItemsTable.svelte` returns
      at least 3
    - `grep -c "location_id_override\|location_name_override\|locationOverrideName" ui/src/features/acts/returnPayload.ts`
      returns 0
    - `grep -c "place_id_override" ui/src/features/acts/returnPayload.ts` returns at least 2 (both
      the `applyToAll` and per-row-override branches)
    - `pnpm --dir ui run svelte-check` reports 0 errors (full `pnpm --dir ui build` runs at the
      wave-completion checkpoint / Plan 21's final gate, not per task — W4 feedback-latency fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: ActDetail.svelte + ActFormItemsTable.svelte — location field rename</name>
  <files>ui/src/features/acts/ActDetail.svelte, ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    ui/src/features/acts/ActDetail.svelte (~line 109 `act.location` inside a `DetailField
    label="Расположение"` — `ActDto.location` was renamed to `full_path` by Plan 07 Task 1, the
    live-resolved display path distinct from `place_path_snapshot`; the act detail card shows the
    LIVE path here, not the frozen print snapshot — do not swap in `place_path_snapshot`, that would
    change this screen's semantics), ui/src/features/acts/ActFormItemsTable.svelte (~line 150
    `location_id: null` inside a `DeviceFilter`-shaped payload passed to `devices.listGrouped(...)`
    when searching for "на складе" devices to add as act items — `DeviceFilter.location_id` was
    renamed to `place_id` by Plan 06 Task 3).
  </read_first>
  <action>
    In `ActDetail.svelte`: rename `act.location` to `act.full_path` in the `DetailField
    label="Расположение"` value binding (label text unchanged — this is the existing act-detail
    "Расположение" field, not a new PlacePicker surface; that per-act place field belongs to Task
    1's `ActFormBody.svelte`, this is read-only display). In `ActFormItemsTable.svelte`: rename
    `location_id: null` to `place_id: null` in the `devices.listGrouped(...)` filter payload.
  </action>
  <verify>
    <automated>grep -c "act\.location" ui/src/features/acts/ActDetail.svelte && grep -c "location_id" ui/src/features/acts/ActFormItemsTable.svelte && grep -c "full_path" ui/src/features/acts/ActDetail.svelte && grep -c "place_id" ui/src/features/acts/ActFormItemsTable.svelte && pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>All acceptance_criteria listed above are satisfied.</done>
  <acceptance_criteria>
    - `grep -c "act\.location\b" ui/src/features/acts/ActDetail.svelte` returns 0
    - `grep -c "location_id" ui/src/features/acts/ActFormItemsTable.svelte` returns 0
    - `grep -c "full_path" ui/src/features/acts/ActDetail.svelte` returns at least 1
    - `grep -c "place_id" ui/src/features/acts/ActFormItemsTable.svelte` returns at least 1
    - `pnpm --dir ui run svelte-check` reports 0 errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Act/return `place_id`/`bulk_place_id`/`place_id_override` payloads | Same server-validated trust model as every other place-bearing write in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-17-01 | Tampering | Client attempting to send `place_path`/snapshot text directly instead of `place_id` | mitigate | These forms only ever collect and send `place_id` (an integer FK reference); the server (Plan 07/11) is the sole place that computes `place_path_snapshot` — no client code path constructs snapshot text |
</threat_model>

<verification>
`pnpm --dir ui build` succeeds. `svelte-check` 0 errors. Manual verification of the full act
create → return → printed-snapshot flow deferred to Plan 21's end-to-end checkpoint.
</verification>

<success_criteria>
Act creation, bulk return, and per-row return override all select place via `PlacePicker`/
`place_id`; no client code constructs snapshot path text.
</success_criteria>

<output>
Create `.planning/phases/39-place-tree/39-17-SUMMARY.md` when done
</output>
