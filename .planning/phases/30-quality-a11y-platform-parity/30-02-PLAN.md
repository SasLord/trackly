---
phase: 30-quality-a11y-platform-parity
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - ui/src/lib/components/Dropdown.svelte
  - ui/src/features/cartridges/ModelListRow.svelte
  - ui/src/lib/components/TableRow.svelte
autonomous: true
requirements: [QA-02]

must_haves:
  truths:
    - "Клавиатурный фокус на строке поиска Dropdown (.tr-dropdown-search-input, портал-панель select-варианта) показывает видимое кольцо — не голый outline:none без замены (D-02)."
    - "Клавиатурный фокус на кебаб-кнопке ModelListRow (.kebab-btn) не обрезается ancestor overflow:hidden (.models-list, ModelsList.svelte:71) — используется inset-кольцо, как у соседних ActListRow/CartridgeListRow/PrinterListRow/RequestListRow (D-02)."
    - "Клавиатурный фокус на шевроне сворачивания группы (TableRow.svelte .tr-row-chevron) показывает СВОЁ видимое inset-кольцо вместо необрезаемого outward-кольца из глобального baseline — актуально для каждой сгруппированной таблицы приложения (минимум Устройства) (D-02)."
    - "node ui/scripts/check-focus-outline.mjs теперь завершается кодом 0 (Dropdown.svelte больше не нарушение) — durable регресс-защита из 30-01 подтверждает фикс (D-02/D-04)."
    - "Tabs.svelte НЕ тронут — его focus-visible уже корректен (см. 30-01 objective и 30-PATTERNS.md correction) (D-02)."
  artifacts:
    - path: "ui/src/lib/components/Dropdown.svelte"
      provides: "видимое кольцо фокуса на .tr-dropdown-search-input"
    - path: "ui/src/features/cartridges/ModelListRow.svelte"
      provides: "inset-кольцо на .kebab-btn, не обрезаемое .models-list overflow:hidden"
    - path: "ui/src/lib/components/TableRow.svelte"
      provides: "собственное inset-кольцо на .tr-row-chevron"
  key_links:
    - from: "ui/src/lib/components/Dropdown.svelte .tr-dropdown-search-input:focus-visible"
      to: "ui/scripts/check-focus-outline.mjs"
      via: "box-shadow declaration inside the enclosing rule scope"
      pattern: "box-shadow.*var\\(--tr-focus-ring\\)"
---

<objective>
Точечно починить три пре-триаженных дефекта кольца фокуса из `30-PATTERNS.md` (не аудит заново —
конкретные файлы/строки уже найдены): голый `outline:none` без замены в `Dropdown.svelte`, и два
случая, где outward box-shadow-кольцо режется родительским `overflow:hidden` (`ModelListRow.svelte`,
`TableRow.svelte`) — мигрируя оба на уже существующий в кодовой базе inset-ring идиом.

Purpose: QA-02 (ROADMAP Success Criterion #2 — видимое кольцо фокуса на каждом интерактивном
элементе). Глобальный `*:focus-visible` baseline (`global.scss:40`) остаётся safety-net'ом и НЕ
трогается — фаза чинит только места, где он гасится или обрезается (D-02, `30-CONTEXT.md`).

Output: три файла с точечными CSS-правками (0 изменений разметки/логики); `check-focus-outline.mjs`
из плана 30-01 переходит с 1 нарушения на 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-quality-a11y-platform-parity/30-CONTEXT.md
@.planning/phases/30-quality-a11y-platform-parity/30-PATTERNS.md
</context>

<interfaces>
<!-- Canonical inset-ring idiom, already established repo-wide (ActListRow.svelte:106-109,
     CartridgeListRow.svelte:148-151, PrinterListRow.svelte:133-136, RequestListRow.svelte:165-168,
     Sidebar.svelte:152-155/237-240) — copy this shape, do not invent a new one: -->
```
&:focus-visible {
  outline: none;
  box-shadow: inset 0 0 0 2px var(--tr-accent);
}
```
<!-- Canonical non-clipped ring idiom (Button.svelte:87-89 and 20+ other primitives) — for the
     Dropdown search-input fix, which is NOT inside an overflow:hidden ancestor: -->
```
&:focus-visible {
  box-shadow: 0 0 0 2px var(--tr-focus-ring);
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dropdown.svelte — видимое кольцо на .tr-dropdown-search-input</name>
  <files>ui/src/lib/components/Dropdown.svelte</files>
  <read_first>
    ui/src/lib/components/Dropdown.svelte строки 793-935 (портал-панель `.tr-dropdown-panel`,
    `overflow: auto`, `max-height: 280px`; дефект — `.tr-dropdown-search-input` строки 922-935,
    `outline: none;` на строке 927, БЕЗ box-shadow; референс-идиом в ЭТОМ ЖЕ файле —
    `.tr-dropdown-field:focus-visible` строки 719-723, `box-shadow: 0 0 0 3px var(--tr-focus-ring)`);
    ui/src/features/layout/Sidebar.svelte строки 237-240 (`.logout-btn:focus-visible { box-shadow: 0
    0 0 2px var(--tr-focus-ring); }` — тот же 2px-масштаб для маленького инлайн-элемента, который
    этот фикс копирует, а не 3px основного поля).
  </read_first>
  <action>
    В `Dropdown.svelte`, внутри правила `:global(.tr-dropdown-panel .tr-dropdown-search-input) {
    ... }` (строки 922-935), добавить новый вложенный блок `&:focus-visible { box-shadow: 0 0 0 2px
    var(--tr-focus-ring); }` СРАЗУ ПОСЛЕ существующего `&::placeholder { color:
    var(--tr-text-tertiary); }` блока (перед закрывающей `}` правила). 2px (не 3px) — потому что это
    маленький инлайн-элемент внутри `.tr-dropdown-search-box` (высота 30px), тот же масштаб, что
    `Sidebar.svelte:237-240` `.logout-btn`. НЕ inset-вариант — `.tr-dropdown-panel` использует
    `overflow: auto` (скролл, не `hidden`), а сам `.tr-dropdown-search` контейнер `position: sticky;
    top: 0` (строки 896-906), т.е. не обрезается по вертикали при фокусе; outward-кольцо здесь не в
    зоне клипа. Больше ничего в файле не менять — `.tr-dropdown-option` (drill-in panel items) НЕ
    трогать в этом плане (UAT-кандидат, не слепой фикс — см. план 30-03 финальный чекпоинт).
  </action>
  <acceptance_criteria>
    - `grep -n "tr-dropdown-search-input" ui/src/lib/components/Dropdown.svelte` показывает правило,
      содержащее НОВЫЙ `&:focus-visible { box-shadow: 0 0 0 2px var(--tr-focus-ring); }` блок
    - `node ui/scripts/check-focus-outline.mjs` завершается кодом 0 (0 нарушений — Dropdown.svelte
      больше не единственное нарушение из 30-01 Task 3)
    - `grep -c "tr-dropdown-option:focus-visible\|tr-dropdown-option) {" ui/src/lib/components/Dropdown.svelte` — счётчик НЕ увеличился относительно состояния до этой задачи (подтверждает: `.tr-dropdown-option` не тронут, UAT-кандидат остаётся нетронутым)
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-focus-outline.mjs; echo "exit=$?"</automated>
  </verify>
  <done>.tr-dropdown-search-input показывает видимое кольцо на клавиатурном фокусе; check-focus-outline.mjs теперь зелёный (0 нарушений).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ModelListRow.svelte — inset-кольцо на .kebab-btn (клип .models-list overflow:hidden)</name>
  <files>ui/src/features/cartridges/ModelListRow.svelte</files>
  <read_first>
    ui/src/features/cartridges/ModelListRow.svelte строки 160-183 (текущий non-inset
    `.kebab-btn:focus-visible { outline: none; box-shadow: 0 0 0 3px var(--tr-focus-ring); }` строки
    179-182); ui/src/features/cartridges/ModelsList.svelte строки 62-72 (фактический клип-контейнер
    — `.models-list { ...; overflow: hidden; }` строка 71, НЕ `Table.svelte`'s `.tr-table-framed.
    framed` — `ModelsList.svelte:48` явно передаёт `framed={false}`, так что framed-класс не
    применяется; клип идёт от внешней обёртки `.models-list`, не от Table); ui/src/features/acts/
    ActListRow.svelte строки 102-110 (эталонный inset-идиом для копирования — `.cell-number
    &:focus-visible { outline: none; box-shadow: inset 0 0 0 2px var(--tr-accent); }`).
  </read_first>
  <action>
    В `ModelListRow.svelte`, в правиле `.kebab-btn { ... &:focus-visible { ... } }` (строки 160-183),
    заменить содержимое существующего `&:focus-visible` блока (строки 179-182) с `outline: none;
    box-shadow: 0 0 0 3px var(--tr-focus-ring);` на `outline: none; box-shadow: inset 0 0 0 2px
    var(--tr-accent);` — тот же inset-идиом, что уже у `ActListRow.svelte`/`CartridgeListRow.svelte`/
    `PrinterListRow.svelte`/`RequestListRow.svelte` для ячеек внутри клип-контекста. Причина: кебаб
    расположен у правого края строки внутри `.models-list { overflow: hidden; }` — outward 3px-кольцо
    обрезается этой обёрткой. Никакой другой CSS/разметку/логику (`toggleMenu`/`handleClickOutside`/
    `ctx-menu`) не трогать.
  </action>
  <acceptance_criteria>
    - `grep -A2 "\.kebab-btn {" ui/src/features/cartridges/ModelListRow.svelte` показывает блок,
      где `&:focus-visible` содержит `box-shadow: inset 0 0 0 2px var(--tr-accent);`
    - `grep -c "0 0 0 3px var(--tr-focus-ring)" ui/src/features/cartridges/ModelListRow.svelte`
      возвращает 0 (старое non-inset правило удалено, не задвоено)
    - `pnpm --dir ui svelte-check` завершается кодом 0 (чисто CSS-правка, не должна ломать типы)
  </acceptance_criteria>
  <verify>
    <automated>grep -A2 "\.kebab-btn {" ui/src/features/cartridges/ModelListRow.svelte | grep -c "inset 0 0 0 2px var(--tr-accent)"</automated>
  </verify>
  <done>.kebab-btn использует inset-кольцо, не обрезаемое .models-list overflow:hidden; визуально идентично соседним *ListRow-компонентам.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: TableRow.svelte — новое inset-кольцо на .tr-row-chevron (широкий blast-radius)</name>
  <files>ui/src/lib/components/TableRow.svelte</files>
  <read_first>
    ui/src/lib/components/TableRow.svelte строки 84-158 (весь `<style>`-блок — `.tr-row-chevron`
    строки 137-157 сейчас БЕЗ собственного `&:focus-visible`; референс-техника в этом же файле —
    `.tr-row.selected :global(> td:first-child) { box-shadow: inset 3px 0 0 var(--tr-accent); }`
    строки 124-126, тот же inset-приём, другое назначение); ui/src/lib/components/Table.svelte
    строки 91-97 (`.tr-table-framed.framed { ...; overflow: hidden; }` — DEFAULT `framed = true`
    per `Table.svelte:43`, значит БОЛЬШИНСТВО потребителей `TableRow`/группового режима клипуются
    этим правилом по умолчанию); ui/src/features/acts/ActListRow.svelte строки 106-109 (эталонный
    inset-идиом для копирования).
  </read_first>
  <action>
    В `TableRow.svelte`, в правиле `.tr-row-chevron { ... }` (строки 137-157), добавить новый
    вложенный блок `&:focus-visible { outline: none; box-shadow: inset 0 0 0 2px var(--tr-accent); }`
    СРАЗУ ПОСЛЕ существующего `&.expanded { transform: rotate(90deg); }` блока (перед закрывающей `}`
    правила `.tr-row-chevron`). Это самостоятельная кнопка сворачивания групповой строки (`<button
    class="tr-row-chevron">`, строки 57-73) без собственного правила фокуса — сейчас полагается
    исключительно на глобальный non-inset baseline (`global.scss:40-43`), который обрезается
    `Table.svelte`'s `overflow: hidden` при `framed=true` (дефолт). `TableRow` — общий примитив для
    ВСЕХ сгруппированных таблиц приложения (минимум Устройства — см. собственный doc-комментарий
    файла про DeviceGroupRow precedent), поэтому это самая широкая по охвату правка плана. Больше
    ничего в файле не менять.
  </action>
  <acceptance_criteria>
    - `grep -A5 "\.tr-row-chevron {" ui/src/lib/components/TableRow.svelte` показывает новый
      `&:focus-visible { outline: none; box-shadow: inset 0 0 0 2px var(--tr-accent); }` блок
    - `grep -c "tr-row-chevron" ui/src/lib/components/TableRow.svelte` не уменьшился (структура
      остальных правил не задета)
    - `pnpm --dir ui svelte-check` завершается кодом 0
  </acceptance_criteria>
  <verify>
    <automated>grep -A6 "\.tr-row-chevron {" ui/src/lib/components/TableRow.svelte | grep -c "focus-visible"</automated>
  </verify>
  <done>.tr-row-chevron показывает собственное inset-кольцо фокуса, не обрезаемое framed-таблицей; применяется ко всем существующим потребителям TableRow group-режима без правок в них самих.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет новых | Три чисто CSS-правки (добавление `&:focus-visible` блоков и замена одного box-shadow значения) — ноль изменений разметки, скриптов, обработчиков событий, ноль новых интерактивных путей. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Tampering | Dropdown.svelte / ModelListRow.svelte / TableRow.svelte (`<script>`-логика) | accept | Acceptance criteria каждой задачи ограничивают diff конкретным CSS-правилом (grep на точный selector); `toggleMenu`/`handleClickOutside`/`handleChevronClick`/`onToggleGroup` остаются нетронутыми — правки только внутри `<style lang="scss">`. |
| T-30-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Этот план не устанавливает пакетов. |
</threat_model>

<verification>
- `node ui/scripts/check-focus-outline.mjs` завершается кодом 0 (0 нарушений — регрессия из 30-01 закрыта)
- `pnpm --dir ui svelte-check` завершается кодом 0
- `pnpm --dir ui lint` — все гейты, включая оба скрипта из 30-01, зелёные
- `pnpm --dir ui build` успешен
</verification>

<success_criteria>
- Dropdown search-input, ModelListRow kebab-button, TableRow chevron все показывают видимое, не обрезаемое кольцо фокуса при клавиатурной навигации
- check-focus-outline.mjs переходит с 1 нарушения (конец 30-01) на 0 (конец 30-02)
- Tabs.svelte не тронут
</success_criteria>

<output>
Create `.planning/phases/30-quality-a11y-platform-parity/30-02-SUMMARY.md` when done
</output>
