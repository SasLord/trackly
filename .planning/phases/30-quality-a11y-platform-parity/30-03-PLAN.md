---
phase: 30-quality-a11y-platform-parity
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - .planning/phases/30-quality-a11y-platform-parity/30-WINDOWS-PARITY.md
autonomous: false
requirements: [QA-02, QA-03]

must_haves:
  truths:
    - "Финальный both-theme UAT-чекпоинт — блокирующий (type=checkpoint:human-verify gate=blocking), НЕ авто-одобряется — прецедент Фазы 24 LEARNINGS: авто-одобрение маскировало непроверенное, дало ложный [VERIFIED] (D-04)."
    - "pnpm --dir ui lint (вкл. check-tokens.mjs, check-contrast.mjs, check-focus-outline.mjs), pnpm --dir ui svelte-check, pnpm --dir ui build — все зелёные перед тем, как чекпоинт открывается человеку."
    - "Windows/WebView2-паритет задокументирован как best-effort чек-лист-артефакт (30-WINDOWS-PARITY.md) для отдельной машины — НЕ блокирует закрытие фазы на dev-Маке (D-03)."
    - "Паритет desktop (Tauri WKWebView) ↔ LAN-браузер (Chrome) подтверждён человеком на дашборде/устройствах/актах/логине в ОБЕИХ темах — репрезентативная выборка из ROADMAP Success Criterion #3."
    - "Клавиатурный focus-ring walk включает Dropdown .tr-dropdown-option (drill-in panel items) как явный UAT-кандидат из 30-PATTERNS.md — не слепой код-фикс, решение принимается по итогам этой самой проверки (D-02)."
    - "AA-контраст спот-чек глазами покрывает то, что check-contrast.mjs сознательно не проверяет: rgba/soft-бейджи (альфа-композитинг вне периметра скрипта, см. 30-01) (D-01)."
  artifacts:
    - path: ".planning/phases/30-quality-a11y-platform-parity/30-WINDOWS-PARITY.md"
      provides: "документированный best-effort чек-лист паритета для Windows/WebView2 (экраны × темы × категории)"
---

<objective>
Закрыть фазу: создать документированный best-effort чек-лист паритета для Windows/WebView2
(D-03 — фактический прогон вне dev-Мака), прогнать финальные автоматические гейты по всей фазе
(вкл. оба новых скрипт-гейта из 30-01), и провести ОБЯЗАТЕЛЬНЫЙ блокирующий человеческий UAT
QA-02/QA-03 на реальном экране Мака: контраст-спот-чек, focus-ring walk по типам интерактива, и
паритет Tauri WKWebView ↔ LAN Chrome на дашборде/устройствах/актах/логине в обеих темах.

Purpose: ROADMAP Success Criteria #1-3 (AA-контраст, видимое кольцо на каждом интерактивном
элементе, визуальный паритет desktop↔LAN) требуют доказательства ГЛАЗАМИ там, где машина не видит
(клипы/наложения — D-02; рендер двух реальных вьюпортов — D-03). Прецедент Фаз 26-29 (D-17/D-18,
`24-LEARNINGS.md`): чекпоинт ОБЯЗАН быть блокирующим, не авто-одобряемым — авто-одобрение уже дважды
маскировало непроверенную работу в Фазе 24.

Output: `30-WINDOWS-PARITY.md` (best-effort чек-лист-артефакт); зелёные `pnpm lint`/`svelte-check`/
`build`; подписанный человеком UAT — либо `[VERIFIED]`, либо документированный список гэпов,
маршрутизируемых в gap-closure планирование.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-quality-a11y-platform-parity/30-CONTEXT.md
@.planning/phases/30-quality-a11y-platform-parity/30-PATTERNS.md
@.planning/phases/26-windows-with-mockup/26-CONTEXT.md
@.planning/phases/29-login-and-employee-shell/29-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 30-WINDOWS-PARITY.md — best-effort чек-лист Windows/WebView2 (D-03)</name>
  <files>.planning/phases/30-quality-a11y-platform-parity/30-WINDOWS-PARITY.md</files>
  <read_first>
    .planning/phases/30-quality-a11y-platform-parity/30-CONTEXT.md раздел D-03 (полный текст решения
    — «Мак: WKWebView ↔ LAN Chrome сейчас; Windows/WebView2 — best-effort», определение
    «идентично», допустимые платформенные различия); ui/src/routes.ts строки 17-27 (список
    маршрутов — `/` Dashboard, `/devices`, `/acts`, `/login`, плюс полный список остальных 8 окон
    приложения для секции «дополнительно, не обязательно» чек-листа).
  </read_first>
  <action>
    Создать `.planning/phases/30-quality-a11y-platform-parity/30-WINDOWS-PARITY.md` — статический
    чек-лист-артефакт (не исполняемый скрипт, не блокирует фазу) со следующей структурой: заголовок
    и одна вводная фраза, что этот файл — best-effort D-03 для отдельной Windows-машины (не
    dev-Мак), заполняется вручную ПОСЛЕ прогона, не блокирует закрытие v1.2 на Маке. Таблица со
    строками = 4 обязательных экрана (Дашборд `/`, Устройства `/devices`, Акты `/acts`, Логин
    `/login`) × 2 темы (светлая/тёмная) = 8 строк, столбцы: «Экран», «Тема», «Раскладка совпадает с
    Мак-baseline (да/нет/расхождение)», «Токены/цвета совпадают (да/нет/расхождение)», «Нативные
    контролы (скроллбар/шрифт — ожидаемо иные, не баг)», «Заметки». Ниже — короткая секция
    «Дополнительно (необязательно)» с оставшимися 8 окнами приложения (Принтеры, Картриджи, Заявки,
    Отчёты, Настройки, Пользователи, EmployeeLayout, FirstRunWizard/Pending/Blocked) в виде простого
    списка без обязательных полей. В конце — секция «Как запускать» с двумя пунктами: на
    Windows-машине собрать `pnpm --dir ui build`, затем сравнить `cargo tauri dev` (WebView2) против
    LAN-режима (тот же `ui/dist`, открытый в Edge/Chrome на Windows) по колонкам таблицы выше;
    результат — не блокер закрытия фазы (D-03), фиксируется здесь для трекинга долга.
  </action>
  <acceptance_criteria>
    - Файл существует по указанному пути
    - Содержит таблицу минимум с 8 строками (4 экрана × 2 темы)
    - Содержит явную фразу о best-effort/не-блокирующем статусе (ссылка на D-03)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/30-quality-a11y-platform-parity/30-WINDOWS-PARITY.md && grep -c "best-effort\|D-03" .planning/phases/30-quality-a11y-platform-parity/30-WINDOWS-PARITY.md</automated>
  </verify>
  <done>30-WINDOWS-PARITY.md существует как задокументированный best-effort чек-лист-артефакт для отдельной Windows-машины, явно не блокирующий закрытие фазы.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Финальные автоматические гейты по всей фазе</name>
  <files>none (verification-only task, no files modified)</files>
  <read_first>
    Ничего дополнительно — этот таск прогоняет уже существующие скрипты проекта против файлов,
    затронутых 30-01/30-02. Гоча проекта (память): LAN-браузер/серверный режим отдаёт `ui/dist`;
    `cargo tauri dev` хотрелоадит ТОЛЬКО desktop-webview — `pnpm --dir ui build` ОБЯЗАТЕЛЕН перед
    Task 3's браузерной частью проверки.
  </read_first>
  <action>
    Прогнать по порядку: `pnpm --dir ui lint` (eslint + prettier + `check-tokens.mjs` +
    `check-contrast.mjs` + `check-focus-outline.mjs` — все пять гейтов из существующего пайплайна
    плюс два новых из 30-01), `pnpm --dir ui svelte-check`, `pnpm --dir ui build` (запускает
    `prebuild` → `cargo test -p trackly-app --test export_bindings`, регенерирует `bindings.ts` —
    ожидается no-op diff, эта фаза не трогает backend/API). Подтвердить все четыре гейта кодом 0.
    Это последний автоматический барьер перед человеческим UAT в Task 3.
  </action>
  <acceptance_criteria>
    - `pnpm --dir ui lint` завершается кодом 0
    - `pnpm --dir ui svelte-check` завершается кодом 0
    - `pnpm --dir ui build` завершается кодом 0, `ui/dist/` создан/обновлён
    - `git diff --stat` не показывает неожиданных изменений в `bindings.ts` (подтверждает: фаза только фронтенд)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check && pnpm --dir ui build; echo "exit=$?"</automated>
  </verify>
  <done>Все 4 автоматических гейта зелёные; ui/dist пересобран и готов для LAN-браузерной части Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Both-theme UAT — контраст, focus-ring walk, паритет WKWebView↔Chrome</name>
  <read_first>
    .planning/phases/30-quality-a11y-platform-parity/30-CONTEXT.md разделы D-01/D-02/D-03/D-04
    (определения «идентично», охват аудита кольца, критерий AA/исключений); 30-PATTERNS.md раздел
    про `.tr-dropdown-option` (UAT-кандидат, не починенный кодом в 30-02 — решение принимается
    здесь); ui/src/App.svelte строки 15-49 (desktop auto-admin ветка при `desktop_lock_enabled=false`
    — нужно знать, как реально открыть LoginPage на desktop для паритет-сравнения).
  </read_first>
  <action>Провести блокирующую человеческую проверку QA-02/QA-03 по how-to-verify ниже в обеих темах; не авто-одобрять (gate=blocking).</action>
  <what-built>
    Durable-гейты `check-contrast.mjs`/`check-focus-outline.mjs` зелёные (30-01); 3 точечных
    focus-ring фикса применены (30-02: Dropdown search-input, ModelListRow kebab, TableRow chevron);
    `--tr-text-tertiary`/`--tr-warning`/`--tr-success` центрально исправлены под AA. Ничего в
    поведении/API/бэкенде не менялось.
  </what-built>
  <how-to-verify>
    1. Если не сделано в Task 2: `pnpm --dir ui build`. Запустить оба режима: `cargo tauri dev`
       (desktop, WKWebView) и серверный/LAN-режим, указывающий на свежесобранный `ui/dist`, открытый
       в обычном Chrome на том же Маке (`http://<lan-ip>:<port>` или `http://127.0.0.1:<port>`).
    2. **Контраст-спот-чек (QA-02, SC #1).** В обеих темах глазами проверить: (a) текст обычной
       плотности (списки Устройств/Актов, поля форм) читаем без напряжения; (b) soft-бейджи статусов
       (success/warning/danger/info — например, статус заявки, состояние картриджа) — эти пары
       НАМЕРЕННО не проверяются `check-contrast.mjs` (rgba-альфа-композитинг вне периметра скрипта,
       30-01 objective) — если какая-то плохо читается, это НАХОДКА этого чекпоинта, не пропущенный
       баг скрипта; (c) solid-бейджи success/warning (`Badge.svelte` appearance="solid") теперь
       используют исправленные `--tr-success`/`--tr-warning` (светлая тема) — подтвердить белый текст
       читаем на обоих тонах.
    3. **Focus-ring walk (QA-02, SC #2).** Клавишей Tab пройти по каждому типу интерактива минимум
       на Устройствах и Актах в обеих темах: кнопки (Button), поля (Input/Select/Textarea/Checkbox),
       ссылки, строки таблицы (в т.ч. групповые строки Устройств — проверить именно шеврон
       сворачивания, починенный в 30-02), вкладки (Tabs — подтвердить, что кольцо и так было
       корректным, без правок), Dropdown (открыть, Tab к строке поиска — починена в 30-02 — и К
       ПУНКТАМ СПИСКА `.tr-dropdown-option`, включая прокрутку панели до самого верха/низа при
       фокусе — это НЕ починенный код, а явный UAT-кандидат из `30-PATTERNS.md`: если кольцо на
       крайних пунктах реально обрезается скроллом панели — зафиксировать как находку для
       gap-closure, НЕ чинить прямо сейчас). На каждом типе — кольцо должно быть видимым и не
       обрезанным.
    4. **Паритет WKWebView ↔ LAN Chrome (QA-03, SC #3).** В обеих темах сравнить side-by-side:
       Дашборд (`#/`), Устройства (`#/devices`), Акты (`#/acts`), Логин (`#/login` — на desktop он
       появляется только если `desktop_lock_enabled=true` в Настройках, иначе Tauri
       автоматически логинит как admin; в LAN-режиме `/login` всегда достижим напрямую, т.к. там нет
       auto-admin ветки, см. `App.svelte` строки 15-49). Критерий «идентично» по D-01
       `30-CONTEXT.md`: одинаковая раскладка, токены, отсутствие расхождения нативных контролов;
       ДОПУСТИМО отличие рендера шрифта и вида скроллбара (платформенное, не баг).
    5. Windows/WebView2: НЕ проверяется здесь (нет машины на Маке) — `30-WINDOWS-PARITY.md` (Task 1)
       остаётся best-effort артефактом для отдельного прогона, не блокирует одобрение этого чекпоинта
       (D-03).
  </how-to-verify>
  <acceptance_criteria>
    - Пункт 2 (контраст-спот-чек) пройден в обеих темах без замечаний, либо замечания явно
      зафиксированы как находка для gap-closure
    - Пункт 3 (focus-ring walk) пройден по каждому перечисленному типу интерактива в обеих темах на
      Устройствах и Актах, включая явное решение по `.tr-dropdown-option` (починить в gap-closure /
      подтвердить как ОК)
    - Пункт 4 (паритет WKWebView↔Chrome) подтверждён на всех 4 экранах (`#/`, `#/devices`, `#/acts`,
      `#/login`) в обеих темах по критерию «идентично» из D-01
    - Человек явно ответил на `resume-signal` — "approved" или конкретный список расхождений
      (не тишина / не авто-одобрение)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass in both themes, or describe the specific screen + theme + issue if not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет новых | Один новый статический MD-документ + два verification-таска без изменения кода приложения. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | — | 30-WINDOWS-PARITY.md (статическая документация) | accept | Не исполняемый код, не влияет на runtime/сборку; не открывает новую поверхность атаки. |
| T-30-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Этот план не устанавливает пакетов. |
</threat_model>

<verification>
- `pnpm --dir ui lint` / `svelte-check` / `build` — все зелёные (Task 2)
- `30-WINDOWS-PARITY.md` создан (Task 1)
- Человеческий UAT (Task 3) подтверждён явным "approved" по всем пунктам в обеих темах — БЛОКИРУЮЩИЙ, не авто-одобряемый
</verification>

<success_criteria>
- Все 3 ROADMAP Success Criteria фазы 30 подтверждены: AA-контраст (автоматически + глазами на soft-вариантах), видимое кольцо фокуса на каждом типе интерактива (включая пре-триаженные фиксы + живой UAT-кандидат Dropdown option), визуальный паритет desktop↔LAN на дашборде/устройствах/актах/логине в обеих темах
- Windows/WebView2 задокументирован как best-effort, не блокирует
- Любые находки чекпоинта маршрутизируются в gap-closure, фаза не закрывается молча с невыполненными пунктами
</success_criteria>

<output>
Create `.planning/phases/30-quality-a11y-platform-parity/30-03-SUMMARY.md` when done
</output>
