---
status: complete
phase: 30-quality-a11y-platform-parity
source: [30-01-SUMMARY.md, 30-02-SUMMARY.md, 30-04-SUMMARY.md, 30-05-SUMMARY.md, 30-06-SUMMARY.md, 30-REVIEW-FIX.md]
started: 2026-07-25T00:00:00Z
updated: 2026-07-25T09:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Дашборд — кольцо фокуса на переключателе периода не обрезано (Gap 1)
expected: На Дашборде (#/) в карточке «Динамика расхода картриджей» переключатель «3/6/12 мес.» — Tab на сегмент показывает кольцо фокуса целиком, не обрезано границей карточки. Обе темы.
result: issue
reported: "Кольцо фокуса больше не обрезано, но обводка прилипает к тексту — надо добавить свободного места у текста по краям (внутренний отступ у сегмента)."
severity: cosmetic

### 2. Дашборд — изолированный скролл (Gap 2)
expected: На Дашборде при большом объёме контента скроллится ТОЛЬКО область раздела; сайдбар и хедер зафиксированы и не уезжают. Скролл-полосы на всём окне нет. Обе темы.
result: issue
reported: "По-прежнему два скролла: появляется скролл на весь layout приложения (включая сайдбар), которого вообще не должно быть. Скролл должен быть только у раздела Дашборд."
severity: major

### 3. Комбобокс с поиском — фокус в поле поиска достижим клавиатурой (Gap 3)
expected: Открой любой Dropdown с поиском (напр. Картриджи → фильтр «Модель»). При открытии панели фокус сразу переходит в поле «Поиск» — можно печатать без клика мышью.
result: issue
reported: "Плохо: (1) поиск сразу активен, но синяя обводка только портит картинку, а Tab уводит фокус непонятно куда и скрывает комбобокс; (2) ввод в поиск вообще не фильтрует список. Нужно: оставить авто-фокус на поиске при раскрытии, но БЕЗ синей обводки; введённые значения должны фильтровать список; стрелками вверх/вниз выбирать пункт и Enter — подтверждать (стрелки уже работают при активном инпуте). Итого: убрать синий фокус у поиска + настроить фильтрацию списка по вводу."
severity: major

### 4. Комбобокс с поиском — ArrowLeft двигает каретку, а не выходит из группы (WR-01)
expected: В поле поиска дропдауна введи текст, ArrowLeft в середине строки двигает каретку внутри текста (не выходит). ArrowLeft в позиции 0 (и на кнопке-триггере) — выходит из drill-in группы.
result: pass

### 5. Комбобокс — возврат фокуса на триггер при закрытии (WR-02)
expected: После выбора значения / Escape / клика вне панели фокус возвращается на кнопку-триггер дропдауна (не «пропадает» на body). При закрытии через Tab фокус уходит вперёд по порядку, не отскакивает назад.
result: pass

### 6. Drill-in дропдаун — выход из группы клавиатурой (Gap 5)
expected: «Новый акт» → автокомплит устройства → войди в группу (drill-in, «← Назад …»). Клавишей ArrowLeft (и Escape) можно вернуться из группы на верхний уровень; кнопка «Назад» достижима. Нет keyboard-trap.
result: pass

### 7. Таблицы — кольцо фокуса охватывает всю строку и рендерится (Gap 4 + CR-01)
expected: В списках Актов/Картриджей/Принтеров/Заявок кольцо фокуса охватывает ВСЮ строку (не только первую ячейку) и реально видно на WKWebView. Согласованно между таблицами. Выбранная+сфокусированная строка сохраняет и акцент выбора, и кольцо. Устройства — кольцо на шевроне/строке видимо.
result: issue
reported: "Нет, по-прежнему выделяется только первая ячейка (не вся строка). Больше того: раньше вся первая ячейка выделялась целиком, а теперь левая граница выделения не влезает и обрезается. (скрин: Принтеры, вторая строка «Р...»)"
severity: major

### 8. Контраст-спот-чек — бейджи читаемы в обеих темах (SC #1)
expected: Глазами: обычный текст (списки, поля) читаем без напряжения; soft-бейджи статусов (success/warning/danger/info — статус заявки, состояние картриджа) читаемы; solid-бейджи success/warning — белый текст читаем на обоих тонах. В обеих темах.
result: pass
note: "User: soft-бейджи (напр. «Нет данных») слегка сливаются с фоном, но «в принципе пока ок». Проверено в светлой и тёмной темах. Принято как приемлемо, не маршрутизируется в gap-closure; кандидат на будущий тюнинг soft-бейджей."

### 9. Focus-ring walk — видимое кольцо на каждом типе интерактива (SC #2)
expected: Tab по Устройствам и Актам в обеих темах: кнопки, поля (Input/Select/Textarea/Checkbox), ссылки, строки таблицы (вкл. шеврон групп Устройств), вкладки (Tabs), Dropdown (поле поиска + пункты .tr-dropdown-option, вкл. прокрутку до краёв) — на каждом типе кольцо видимо и не обрезано.
result: issue
reported: "У всех остальных элементов (кнопки/поля/чекбоксы/ссылки/вкладки) фокус правильный. Проблемы только в таблицах: (1) таблица Устройств отличается от остальных — на групповых строках фокус-кольцо теперь и на шевроне (как и было), и добавилось на всю строку; надо оставить ТОЛЬКО на шевроне. (2) Фокус-кольца на шевроне имеют квадратные углы — надо скруглить. ПОПРАВКА пользователя: кольцо кнопки «три точки» в Устройствах уже со скруглёнными углами (изначально показалось квадратным — плохо разглядел); проверить кебаб в остальных таблицах, основной кейс скругления — шеврон."
severity: minor

### 10. Паритет WKWebView ↔ LAN Chrome (SC #3)
expected: Side-by-side desktop (cargo tauri dev, WKWebView) и LAN-браузер (Chrome на ui/dist) — идентичные раскладка/токены/цвета на Дашборде (#/), Устройствах (#/devices), Актах (#/acts), Логине (#/login) в обеих темах. Допустимо: иной рендер шрифта и вид скроллбара (платформенное).
result: pass

## Summary

total: 10
passed: 5
issues: 5
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Кольцо фокуса на сегменте переключателя периода имеет запас от текста (не прилипает)"
  status: failed
  reason: "User reported: кольцо больше не обрезано, но обводка прилипает к тексту — нет свободного места у текста по краям (нужен внутренний отступ у .toggle-btn)"
  severity: cosmetic
  test: 1
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "На Дашборде скроллится только контент раздела; сайдбар/хедер зафиксированы, скролла на весь layout нет"
  status: failed
  reason: "User reported: по-прежнему два скролла — появляется скролл на весь layout приложения (включая сайдбар), которого быть не должно. 30-06 фикс (min-height:0 на .dashboard-grid) не закрыл гэп."
  severity: major
  test: 2
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Dropdown с поиском: авто-фокус на поле без видимой синей обводки; ввод фильтрует список; стрелки+Enter выбирают пункт"
  status: failed
  reason: "User reported: авто-фокус есть, но (1) синяя focus-обводка у поиска портит вид и Tab уводит фокус в никуда, скрывая панель; (2) ввод в поиск НЕ фильтрует список. Требуется: сохранить авто-фокус, убрать синий фокус-ринг у .tr-dropdown-search-input, реализовать фильтрацию списка по вводу, стрелки↑↓ + Enter для выбора (стрелки уже работают)."
  severity: major
  test: 3
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Кольцо фокуса строки таблицы охватывает всю строку, не обрезается, согласованно между таблицами"
  status: failed
  reason: "User reported: по-прежнему выделяется только первая ячейка (не вся строка); мало того — раньше первая ячейка выделялась целиком, а теперь левая граница кольца не влезает и обрезается. Проявляется в Принтерах (master-detail список). 30-05 (.tr-row:has(:focus-visible)) + CR-01 (<td> cell rules) не дали row-wide кольца в этом списке."
  severity: major
  test: 7
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Модель фокуса таблиц согласована: Устройства-группы — только шеврон (без row-ring); кольца шеврона/кебаба со скруглёнными углами"
  status: failed
  reason: "User reported: остальные типы интерактива ОК. В таблицах: (1) на групповых строках Устройств появилось лишнее row-level кольцо в дополнение к шеврону — оставить только шеврон (Устройства-таблица отличается моделью фокуса от остальных; побочка 30-05 .tr-row:has(:focus-visible) на не-фокусируемых строках Устройств); (2) фокус-кольцо шеврона имеет квадратные углы — нужно скругление под дизайн. Поправка пользователя: кебаб «три точки» в Устройствах уже скруглён; проверить кебаб в остальных таблицах."
  severity: minor
  test: 9
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
