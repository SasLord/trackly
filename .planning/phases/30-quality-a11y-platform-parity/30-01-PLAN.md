---
phase: 30-quality-a11y-platform-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/scripts/check-contrast.mjs
  - ui/scripts/check-focus-outline.mjs
  - ui/package.json
  - ui/src/styles/_tokens.scss
autonomous: true
requirements: [QA-02]

must_haves:
  truths:
    - "node ui/scripts/check-contrast.mjs завершается кодом 0 и печатает `[check-contrast] PASS — 0 нарушений` после починки токенов в Task 2 — durable регресс-защита AA-контраста в обеих темах (D-01)."
    - "check-contrast.mjs подключён в ui/package.json → scripts.lint (&&-цепочка после check-tokens.mjs) и поэтому исполняется при каждом pnpm lint / CI прогоне (D-01/D-04)."
    - "check-focus-outline.mjs корректно отличает реальный дефект (Dropdown.svelte .tr-dropdown-search-input — голый outline:none без box-shadow где-либо в охватывающем правиле) от ложного срабатывания (Tabs.svelte .tab — outline:none в родительском правиле, box-shadow в его же вложенном &:focus-visible) и от 37 уже спаренных same-block сайтов (D-04)."
    - "--tr-text-tertiary (обе темы), --tr-warning (светлая), --tr-success (светлая) отредактированы централизованно в _tokens.scss так, что все 43 канонические пары текст↔фон проходят порог AA (4.5:1 обычный текст; 3:1 документированное исключение для --tr-text-tertiary — роль «плейсхолдеры/мета/счётчики» по 23-UI-SPEC.md:81, не основной текст) в обеих темах (D-01)."
  artifacts:
    - path: "ui/scripts/check-contrast.mjs"
      provides: "durable WCAG-контраст гейт по закрытой канонической таблице пар токенов (43 пары × 2 темы), zero-dependency"
    - path: "ui/scripts/check-focus-outline.mjs"
      provides: "durable-линт голого outline:none без парного box-shadow в охватывающем правиле, с inline-исключением"
    - path: "ui/src/styles/_tokens.scss"
      provides: "исправленные центральные значения --tr-text-tertiary/--tr-warning/--tr-success, проходящие AA"
  key_links:
    - from: "ui/package.json scripts.lint"
      to: "ui/scripts/check-contrast.mjs"
      via: "&&-цепочка (тот же паттерн, что check-tokens.mjs)"
      pattern: "check-contrast\\.mjs"
    - from: "ui/package.json scripts.lint"
      to: "ui/scripts/check-focus-outline.mjs"
      via: "&&-цепочка"
      pattern: "check-focus-outline\\.mjs"
---

<objective>
Создать два постоянных zero-dependency скрипт-гейта (`check-contrast.mjs` для WCAG AA-контраста,
`check-focus-outline.mjs` для голого `outline: none` без замены) по образцу `check-tokens.mjs`,
подключить оба в `pnpm lint`, и починить обнаруженные скриптом реальные AA-провалы центральными
правками `_tokens.scss` (обе темы, где применимо).

Purpose: QA-02 (ROADMAP Success Criterion #1 — AA-контраст в обеих темах) требует durable
регресс-защиты, а не разового ручного чек-листа (D-01/D-04, `30-CONTEXT.md`). В проекте нет
npm-зависимости для WCAG-математики (`30-PATTERNS.md` → «No Analog Found») — формула считается
с нуля, как и во всех прежних скрипт-гейтах проекта.

Output: `check-contrast.mjs` (зелёный после Task 2), `check-focus-outline.mjs` (по конструкции
детектирует единственный известный реальный дефект — `Dropdown.svelte` — который чинится в
Wave 2, поэтому `pnpm lint` станет полностью зелёным только после плана 30-02, это ожидаемо),
`_tokens.scss` с четырьмя точечными правками токенов.

**Важно для читателя/верификатора:** CONTEXT.md утверждает, что у `Tabs.svelte` нет своего
`focus-visible` — это устаревшее утверждение, `30-PATTERNS.md` эмпирически проверил (2026-07-24):
у `Tabs.svelte` оба варианта (`.tabs-underline .tab`, `.tabs-segmented .tab`) уже содержат корректный
`&:focus-visible { box-shadow: 0 0 0 3px var(--tr-focus-ring); }`. Ни в этом, ни в следующих планах
фазы `Tabs.svelte` не правится.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-quality-a11y-platform-parity/30-CONTEXT.md
@.planning/phases/30-quality-a11y-platform-parity/30-PATTERNS.md
@.planning/phases/23-design-tokens-foundations/23-UI-SPEC.md
</context>

<interfaces>
<!-- ui/scripts/check-tokens.mjs — the exact style/skeleton precedent both new scripts must match.
     Read in full (279 lines) before writing. Key reusable pieces (verbatim signatures): -->

`function readFileSafe(filePath)` — try/catch `fs.readFileSync(filePath, 'utf8')`, returns `null` on
failure. Reuse verbatim in both new scripts.

`function lineNumberAt(content, index)` — counts `\n` up to `index`, 1-based. Reuse verbatim in
`check-focus-outline.mjs` (needed for violation line numbers); `check-contrast.mjs` does not need it
(violations are theme+token-pair, not line-based).

`function collectSourceFiles(srcDir)` — recursive `fs.readdirSync(srcDir, { recursive: true })`
filtered to `.svelte`/`.scss`, tolerant of a missing directory. Reuse verbatim in
`check-focus-outline.mjs` (it must scan every `.svelte` file's `<style>` block, same universe as
`check-tokens.mjs` Rule 2/4). `check-contrast.mjs` does NOT need this — it only ever reads the single
file `ui/src/styles/_tokens.scss`.

`const STYLE_BLOCK_RE = /<style[^>]*>([\s\S]*?)<\/style>/g;` — the exact regex `check-tokens.mjs`
Rules 2/4 use to extract `<style lang="scss">...</style>` block content plus its start offset
(`styleMatch.index + styleMatch[0].indexOf(block)`) for accurate line numbers. Reuse verbatim in
`check-focus-outline.mjs`.

Exit-code / reporting contract (`main()`, lines 222-276) — accumulate a `violations[]` array per
rule, `console.error` one line per violation prefixed `[check-tokens] ...`, sum a `totalViolations`
counter across all enabled rules, then: `totalViolations > 0` → `console.error('[check-tokens] FAIL —
N нарушений')` + `process.exit(1)`; else `console.error('[check-tokens] PASS — 0 нарушений')` +
`process.exit(0)`. Both new scripts must follow this exact contract, swapping the `[check-tokens]`
prefix for `[check-contrast]` / `[check-focus-outline]` respectively.

`ui/package.json:16` (current, verbatim): `"lint": "eslint . --ext .ts,.svelte && prettier --check .
&& node scripts/check-tokens.mjs"` — the only integration point; append new scripts here with the
same `&&`-chaining, never in `build`/`prebuild`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: check-contrast.mjs — WCAG AA-контраст гейт по канонической таблице пар токенов</name>
  <files>ui/scripts/check-contrast.mjs, ui/package.json</files>
  <read_first>
    ui/scripts/check-tokens.mjs (весь файл, 279 строк — стиль/скелет/exit-code контракт);
    ui/src/styles/_tokens.scss (весь файл, 267 строк — светлый блок `:root,\n[data-theme='light']
    {...}` строки 12-88, тёмный блок `[data-theme='dark'] {...}` строки 90-170, оба блока —
    плоские списки `--tr-x: value;` без вложенных фигурных скобок); ui/package.json:16 (текущая
    строка `scripts.lint`); .planning/phases/23-design-tokens-foundations/23-UI-SPEC.md:52
    (`"контраст/AA-валидация — Фаза 30 (QA-02), не эта фаза"` — подтверждает, что hex-значения
    скопированы из Claude Design без AA-проверки, это ожидаемо) и :81 (`--tr-text-tertiary` роль
    «Placeholders, meta, counters»).
  </read_first>
  <action>
    Создать `ui/scripts/check-contrast.mjs` по скелету `check-tokens.mjs` (шапка-докстринг с
    `[check-contrast]` префиксом и ссылкой на Phase 30/D-01; import только `node:fs`, `node:path`,
    `node:url`; `__dirname`/`UI_ROOT` тем же способом). Скрипт читает ЕДИНСТВЕННЫЙ файл —
    `path.join(UI_ROOT, 'src', 'styles', '_tokens.scss')` — через `readFileSafe` (скопировать
    функцию verbatim).

    Разбить содержимое файла на два блока по теме: найти индекс подстроки `[data-theme='light']`,
    от него — индекс первой `{` после неё (открытие блока), затем найти её парную `}` подсчётом
    глубины скобок (инкремент на `{`, декремент на `}`, остановиться когда глубина возвращается к 0)
    — это диапазон текста светлого блока. Аналогично найти `[data-theme='dark']` → диапазон текста
    тёмного блока. (Оба блока в реальности плоские без вложенных `{}`, но подсчёт глубины — защитный
    паттерн на случай будущих вложенных блоков, тот же дух, что closed-world Rule 3 в check-tokens.)

    Внутри каждого диапазона построить `Map<name, value>` регулярным выражением
    `/(--tr-[a-z0-9-]+)\s*:\s*([^;]+);/g` (name = группа 1 без пробелов, value = группа 2,
    `.trim()`).

    Реализовать WCAG 2.x относительную яркость и коэффициент контраста с нуля (в проекте нет
    npm-зависимости для этого — `30-PATTERNS.md` → «No Analog Found»): распарсить 6-значный hex в
    RGB 0-255; на каждый канал c применить sRGB→linear: `c/255`, затем если результат ≤ 0.03928 —
    делить на 12.92, иначе — `((x+0.055)/1.055) ** 2.4`; относительная яркость L =
    `0.2126*R + 0.7152*G + 0.0722*B` (линеаризованные каналы); коэффициент контраста двух цветов =
    `(max(L1,L2) + 0.05) / (min(L1,L2) + 0.05)`. Значения принимаются ТОЛЬКО в формате
    `/^#[0-9a-fA-F]{6}$/` — если токен из канонической пары резолвится в rgba()/другой формат в
    любой теме, это отдельное нарушение («non-hex value not supported by contrast checker») —
    защита от будущего дрейфа (кто-то превратит opaque-токен в rgba и тихо выведет пару из-под
    проверки).

    Захардкодить каноническую таблицу пар «foreground token / background token / порог» ПРЯМО в
    скрипте (массив объектов `{ fg, bg, threshold }`), без CLI-параметров, ровно этот список из 43
    пар (все имена без `--` для краткости — в коде использовать полное `--tr-...`):

    Порог 4.5 (обычный текст): text-primary × [bg, surface, surface-raised, surface-sunken,
    row-hover, group] (6 пар); text-secondary × те же 6 фонов (6 пар); accent-text × [bg, surface,
    surface-raised, surface-sunken] (4 пары); success-text, warning-text, danger-text, info-text —
    каждый × те же 4 фона (16 пар); on-accent × [accent, danger, success, warning, info] (5 пар).
    Итого 37 пар при пороге 4.5.

    Порог 3.0 (документированное исключение «крупный текст / мета», D-01 — text-tertiary
    используется только для плейсхолдеров/иконок/мета/счётчиков per 23-UI-SPEC.md:81, не для
    основного текста): text-tertiary × [bg, surface, surface-raised, surface-sunken, row-hover,
    group] (6 пар). Итого 6 пар при пороге 3.0.

    Всего 43 пары × 2 темы = 86 проверок за прогон. `--tr-row-selected`/`--tr-accent-soft`/
    `--tr-*-soft`/`--tr-focus-ring`/`--tr-danger-ring`/`--tr-overlay` (все rgba-токены) сознательно
    НЕ входят в каноническую таблицу — альфа-композитинг поверх фактического фона вне периметра
    этого скрипта (см. `30-PATTERNS.md` → «No Analog Found»); остаточный риск по ним закрывается
    финальным ручным UAT в плане 30-03 (D-01 явно допускает это: «недостающее добирается глазами на
    UAT»). `--tr-text-disabled` тоже не входит — WCAG 1.4.3 явно исключает неактивные UI-элементы.

    Для каждой пары × каждой темы: найти fg/bg в соответствующей Map, посчитать коэффициент,
    сравнить с порогом; если ниже — накопить нарушение с полями тема/пара/факт/порог. Вывод и
    exit-code — по контракту `check-tokens.mjs` `main()`: каждая строка нарушения через
    `console.error` с префиксом `[check-contrast]`, формат `[check-contrast] <THEME> <fg> on <bg>:
    ratio X.XX < threshold Y.Y`; в конце `[check-contrast] FAIL — N нарушений` + `process.exit(1)`,
    либо `[check-contrast] PASS — 0 нарушений` + `process.exit(0)`.

    Подключить в `ui/package.json:16` scripts.lint: добавить `&& node scripts/check-contrast.mjs` в
    конец существующей `&&`-цепочки (после `check-tokens.mjs`), не трогая `prebuild`/`build`.
  </action>
  <acceptance_criteria>
    - `grep -c "check-contrast.mjs" ui/package.json` возвращает 1
    - `node ui/scripts/check-contrast.mjs` в ТЕКУЩЕМ (ещё не починенном Task 2) состоянии
      `_tokens.scss` завершается кодом 1, и его stderr содержит хотя бы одну строку с
      `--tr-text-tertiary` и хотя бы одну строку с `--tr-warning` — подтверждает, что скрипт и
      WCAG-математика корректно обнаруживают известные заранее AA-провалы (см. Task 2)
    - `head -20 ui/scripts/check-contrast.mjs` содержит только `import fs`/`import path`/
      `import { fileURLToPath }` из `node:` — ноль npm-зависимостей
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-contrast.mjs; echo "exit=$?"</automated>
  </verify>
  <done>check-contrast.mjs существует, реализует WCAG-математику с нуля, содержит закрытую каноническую таблицу из 43 пар × 2 темы, подключён в pnpm lint, и на текущих (ещё не исправленных) значениях _tokens.scss корректно репортит известные провалы.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Починка 4 AA-провалов центральными токенами (обе темы, где применимо)</name>
  <files>ui/src/styles/_tokens.scss</files>
  <read_first>
    ui/src/styles/_tokens.scss (светлый блок строки 12-88, тёмный блок строки 90-170 — точные
    строки объявлений: `--tr-text-tertiary` светлая :33 / тёмная :110; `--tr-warning` светлая :46;
    `--tr-success` светлая :42); ui/scripts/check-contrast.mjs (только что созданный в Task 1 —
    канонический список пар и пороги); .planning/phases/23-design-tokens-foundations/23-UI-SPEC.md:81
    (`--tr-text-tertiary` роль «Placeholders, meta, counters» — обоснование 3:1-порога, не 4.5:1);
    ui/src/lib/components/Badge.svelte строки 155-169 (appearance="solid" tone="warning"/"success" —
    `background: var(--tr-warning)`/`var(--tr-success)`, `color: var(--tr-on-accent)` — подтверждает
    реальный контекст использования, из-за которого пороги 4.5:1 применимы).
  </read_first>
  <action>
    Четыре точечных правки hex-значений в `_tokens.scss` (значения посчитаны и провалидированы
    против всех 43 канонических пар заранее, при планировании — см. must_haves; правки минимальны,
    не «заметный сдвиг палитры» per D-01, поэтому применяются напрямую, без чекпоинта):

    1. `--tr-text-tertiary` в СВЕТЛОМ блоке (строка ~33): `#8891a4` → `#7a849a` (нужно ≥3:1 против
       всех 6 канонических фонов; худший случай — `--tr-surface-sunken`).
    2. `--tr-text-tertiary` в ТЁМНОМ блоке (строка ~110): `#6b7486` → `#6d7688` (та же логика; худший
       случай — `--tr-row-hover`).
    3. `--tr-warning` в СВЕТЛОМ блоке (строка ~46): `#b9720c` → `#a8680b` (нужно ≥4.5:1 против
       `--tr-on-accent`/`#ffffff` — используется как solid-фон с белым текстом в
       `Badge.svelte:160-161` appearance="solid" tone="warning"; текущее значение даёт только
       3.84:1). НЕ трогать `--tr-warning` в тёмном блоке (там сочетание тёмного `--tr-on-accent`/
       `--tr-warning` уже 8.49:1 — проходит).
    4. `--tr-success` в СВЕТЛОМ блоке (строка ~42): `#12894e` → `#12884d` (нужно ≥4.5:1 против
       `--tr-on-accent`/`#ffffff` — `Badge.svelte` appearance="solid" tone="success"; текущее
       значение даёт 4.4548:1, формально ниже 4.5). НЕ трогать тёмный блок (уже 7.89:1).

    Не трогать никакие другие токены (`--tr-warning-soft`/`--tr-warning-text`/`--tr-success-soft`/
    `--tr-success-text` — независимо заданные литералы, уже проходят AA, править их не требуется и
    не нужно синхронизировать с solid-вариантом, они не производные).
  </action>
  <acceptance_criteria>
    - `grep -n "tr-text-tertiary: #7a849a" ui/src/styles/_tokens.scss` находит ровно 1 совпадение
    - `grep -n "tr-text-tertiary: #6d7688" ui/src/styles/_tokens.scss` находит ровно 1 совпадение
    - `grep -n "tr-warning: #a8680b" ui/src/styles/_tokens.scss` находит ровно 1 совпадение (только светлый блок)
    - `grep -n "tr-success: #12884d" ui/src/styles/_tokens.scss` находит ровно 1 совпадение (только светлый блок)
    - `node ui/scripts/check-contrast.mjs` завершается кодом 0, последняя строка stderr —
      `[check-contrast] PASS — 0 нарушений`
    - `node ui/scripts/check-tokens.mjs` (уже существующий гейт Фазы 23) по-прежнему завершается
      кодом 0 — правки не создали hex-литерал вне `_tokens.scss` (единственный файл, где hex
      разрешён) и не сломали closed-world/старые-имена правила
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-contrast.mjs && node ui/scripts/check-tokens.mjs; echo "exit=$?"</automated>
  </verify>
  <done>_tokens.scss содержит все 4 точечные правки; check-contrast.mjs репортит 0 нарушений на обеих темах; check-tokens.mjs остаётся зелёным.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: check-focus-outline.mjs — линт голого outline:none без парного box-shadow</name>
  <files>ui/scripts/check-focus-outline.mjs, ui/package.json</files>
  <read_first>
    ui/scripts/check-tokens.mjs (весь файл — collectSourceFiles/readFileSafe/STYLE_BLOCK_RE/
    lineNumberAt/exit-code контракт, все переиспользуются verbatim); ui/src/lib/components/
    Dropdown.svelte строки 894-935 (реальный дефект: `.tr-dropdown-search-input` строка 922-935,
    `outline: none;` на строке 927, БЕЗ box-shadow нигде в этом правиле); ui/src/lib/components/
    Tabs.svelte строки 63-104 и 139-164 (ложноположительный кандидат: `.tab { ...; outline: none;
    ...; &:focus-visible { ...; box-shadow: 0 0 0 3px var(--tr-focus-ring); } }` — outline:none и
    box-shadow в РАЗНЫХ вложенных под-правилах, но внутри ОДНОГО охватывающего `.tab { }` блока);
    ui/src/features/acts/ActListRow.svelte строки 102-110 (same-block паттерн: `&:focus-visible {
    outline: none; box-shadow: inset 0 0 0 2px var(--tr-accent); }` — оба в одном правиле);
    ui/package.json (уже содержит `&& node scripts/check-contrast.mjs` после Task 1 — новая строка
    добавляется в конец той же цепочки).
  </read_first>
  <action>
    Создать `ui/scripts/check-focus-outline.mjs` по скелету `check-tokens.mjs` (докстринг-шапка с
    `[check-focus-outline]` префиксом, ссылка на Phase 30/D-04; те же три `node:` импорта).
    Переиспользовать verbatim: `readFileSafe`, `collectSourceFiles(path.join(UI_ROOT, 'src'))`
    (собрать `.svelte`-файлы — только они содержат `&:focus-visible`-паттерны, которые проверяет
    этот скрипт), `lineNumberAt`, `STYLE_BLOCK_RE`.

    Для каждого `.svelte`-файла извлечь содержимое `<style>`-блока (через `STYLE_BLOCK_RE`,
    получить смещение блока в исходном файле для точных номеров строк). Внутри блока найти каждое
    вхождение `outline:\s*none\s*;` через regex `/outline:\s*none\s*;/g`.

    Для каждого найденного вхождения outline:none определить его НЕПОСРЕДСТВЕННО ОХВАТЫВАЮЩЕЕ
    правило по подсчёту глубины фигурных скобок: пройти по тексту блока от начала до позиции
    вхождения, отслеживая глубину (инкремент на каждой `{`, декремент на каждой `}`); последняя
    `{` перед вхождением, после которой глубина не опускалась ниже уровня непосредственно после неё
    — начало охватывающего правила. От этой `{` пройти ВПЕРЁД, снова считая глубину, чтобы найти
    парную `}` — это конец охватывающего правила. Полный текст между этой парой скобок (включая
    любые вложенные под-правила вроде `&:focus-visible { ... }`) — это «scope» для проверки.

    Если внутри scope (где угодно — на той же строке, до или после outline:none, включая вложенные
    под-правила) встречается `box-shadow` — вхождение БЕЗОПАСНО (пропустить). Это ЕДИНОЕ правило
    корректно покрывает оба случая из read_first одновременно: same-block паттерн ActListRow (scope
    = `&:focus-visible {}` само правило, box-shadow внутри него) И cross-nested-block паттерн Tabs
    (scope = весь `.tab { }` включая вложенный `&:focus-visible { box-shadow: ...; }` — box-shadow
    внутри полного scope, хотя и не в той же под-строке, что outline:none). Дополнительная
    cross-file логика поиска по имени селектора НЕ нужна — глубина скобок уже достаточна.

    Если box-shadow НЕ найден в scope — нарушение. ИСКЛЮЧЕНИЕ (whitelist-механизм, D-04): если
    строка с `outline: none;` (или строка непосредственно перед ней) содержит inline-комментарий
    `// check-focus-outline: ignore` — пропустить без нарушения (защита от ложных срабатываний,
    которых пока не найдено, но D-04 требует механизм на будущее).

    Вывод/exit-code — тот же контракт: `[check-focus-outline] <relPath>:<line> — bare outline: none
    without a paired box-shadow in the enclosing rule`; `[check-focus-outline] FAIL — N нарушений` /
    `process.exit(1)`, либо `[check-focus-outline] PASS — 0 нарушений` / `process.exit(0)`.

    Подключить в `ui/package.json` scripts.lint: добавить `&& node scripts/check-focus-outline.mjs`
    в конец `&&`-цепочки (после `check-contrast.mjs` из Task 1).

    ОЖИДАЕМЫЙ результат прогона прямо сейчас (до плана 30-02): РОВНО одно нарушение —
    `Dropdown.svelte:927` (сам файл `Dropdown.svelte` в этом плане не редактируется, так что
    сдвига нумерации строк не будет). Это ОЖИДАЕТСЯ и НЕ является багом скрипта — это реальный
    дефект, который чинит план 30-02. `pnpm lint` не станет полностью зелёным до завершения 30-02 —
    задокументировать это в SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c "check-focus-outline.mjs" ui/package.json` возвращает 1
    - `node ui/scripts/check-focus-outline.mjs` завершается кодом 1 (ожидаемо — Dropdown.svelte ещё
      не починен)
    - вывод `node ui/scripts/check-focus-outline.mjs` содержит строку с `Dropdown.svelte`
    - вывод `node ui/scripts/check-focus-outline.mjs` НЕ содержит строку с `Tabs.svelte` (ложное
      срабатывание было бы регрессией — Tabs.svelte корректен, чинить его не нужно)
    - `head -20 ui/scripts/check-focus-outline.mjs` содержит только `node:`-импорты
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-focus-outline.mjs; echo "exit=$?"</automated>
  </verify>
  <done>check-focus-outline.mjs существует, подключён в pnpm lint, по конструкции безопасно отличает same-block и cross-nested-block парные паттерны от голого outline:none, и на текущем дереве репортит ровно один известный реальный дефект (Dropdown.svelte), не давая ложных срабатываний на Tabs.svelte или 37 прочих спаренных сайтах.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет новых | Фаза не трогает auth/данные/сеть/бэкенд — только CSS-токены, focus-стили и build-time lint-скрипты. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | ui/scripts/check-contrast.mjs, check-focus-outline.mjs (новый код, исполняется в CI/pnpm lint) | mitigate | Оба скрипта zero-dependency — только `node:fs`/`node:path`/`node:url`; ноль новых npm-пакетов, ноль новой supply-chain поверхности. Acceptance criteria обеих задач явно проверяют отсутствие сторонних импортов. |
| T-30-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Этот план не устанавливает ни одного нового пакета — package.json diff должен показывать 0 новых записей в `dependencies`/`devDependencies`. |
</threat_model>

<verification>
- `node ui/scripts/check-contrast.mjs` завершается кодом 0 (после Task 2)
- `node ui/scripts/check-tokens.mjs` завершается кодом 0 (регресс не внесён)
- `node ui/scripts/check-focus-outline.mjs` завершается кодом 1 с ровно одним ожидаемым нарушением (Dropdown.svelte — чинится в 30-02)
- `ui/package.json` scripts.lint содержит обе новые записи
- `pnpm --dir ui svelte-check` и `pnpm --dir ui build` не регрессируют (эта фаза не трогает `.ts`/логику компонентов, только `.scss`/новые `.mjs`-скрипты)
</verification>

<success_criteria>
- check-contrast.mjs — durable AA-контраст гейт, закрытая таблица 43 пары × 2 темы, 0 нарушений после починки токенов
- check-focus-outline.mjs — durable голый-outline:none линт с whitelist-механизмом, корректно отличает Tabs.svelte (безопасно) от Dropdown.svelte (реальный дефект)
- --tr-text-tertiary/--tr-warning/--tr-success починены центральными правками токенов, обе темы валидны
- Оба скрипта подключены в pnpm lint / CI
</success_criteria>

<output>
Create `.planning/phases/30-quality-a11y-platform-parity/30-01-SUMMARY.md` when done
</output>
