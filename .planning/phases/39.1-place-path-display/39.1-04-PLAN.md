---
phase: 39.1
plan: 04
type: execute
wave: 2
depends_on: ["39.1-01"]
files_modified:
  - crates/trackly-core/src/domain/cartridges.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/src/dto/cartridge.rs
autonomous: true
requirements: [PLC-08]
must_haves:
  truths:
    - "D-17: список картриджей получает сокращённый путь (place_path_short) через LEFT JOIN place_effective_variant на SELECT_CARTRIDGES"
  artifacts:
    - path: "crates/trackly-core/src/domain/cartridges.rs"
      provides: "CartridgeRow.place_path_short: Option<String>"
      contains: "place_path_short"
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "SELECT_CARTRIDGES присоединяет place_effective_variant, list() вычисляет place_path_short"
  key_links:
    - from: "cartridges_sqlite.rs::list"
      to: "place_effective_variant"
      via: "LEFT JOIN place_effective_variant pev ON pev.place_id = c.place_id"
      pattern: "place_effective_variant"
---

<objective>
Подключить `place_path_short` к списку картриджей (`SqliteCartridgeRepository::list`) — единственный
read-путь, присоединяющий `place_full_paths` в `cartridges_sqlite.rs`.

Purpose: PLC-08 — картриджи должны показывать эффективный сокращённый вариант так же, как устройства
(план 03), симметрично.
Output: `CartridgeRow`/`CartridgeDto.place_path_short: Option<String>`, `list()` присоединяет
`place_effective_variant` и вычисляет короткий путь.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из плана 01: `trackly_core::domain::places::{PathDisplayVariant, shorten_place_path}` — см. план 03
`<interfaces>` для полной сигнатуры и образца чтения org-сепараторов
(`cartridges_sqlite.rs:930-950`, метод `low_stock` — этот ЖЕ файл уже содержит канонический
образец, находится в нём самом).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: place_path_short в списке картриджей</name>
  <files>crates/trackly-core/src/domain/cartridges.rs, crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-app/src/dto/cartridge.rs</files>
  <read_first>
    crates/trackly-core/src/domain/cartridges.rs (строки 16-45 — `CartridgeRow`, поле `full_path`
    на строке ~39, новое поле после него);
    crates/trackly-infra/src/repos/cartridges_sqlite.rs (строки 45-90 — `SELECT_CARTRIDGES`
    константа + `map_row`; строки 930-950 — `low_stock`'s scalar-настройка read с фолбэком,
    буквальный образец для чтения `place_path_sep_ends`/`place_path_sep_last_two`; строки
    1268-1356 — `fn list`, единственный вызывающий `SELECT_CARTRIDGES`);
    crates/trackly-app/src/dto/cartridge.rs (строки 22-70 — `CartridgeDto` + `impl
    From<CartridgeRow>`).
  </read_first>
  <behavior>
    - Картридж с местом, чей `place_effective_variant` = 'last_two', `full_path` = "Здание А /
      1 этаж / 1-05" → `place_path_short` = "1 этаж / 1-05" (используя org sep_last_two, дефолт
      ' / ').
    - Картридж без `place_id` → `place_path_short: None`.
  </behavior>
  <action>
    В `crates/trackly-core/src/domain/cartridges.rs`: добавить `pub place_path_short:
    Option<String>` в `CartridgeRow` сразу после `pub full_path: Option<String>` (строка ~39).
    В `crates/trackly-infra/src/repos/cartridges_sqlite.rs`: добавить приватную функцию для
    однократного чтения `(sep_ends, sep_last_two)` из `app_settings`, мирроринг `low_stock`'ового
    паттерна в ЭТОМ ЖЕ файле (строки 930-950 — буквально та же форма
    `conn.query_row(...).ok().and_then(...).unwrap_or(default)`, дефолты `" // "`/`" / "`).
    Расширить `SELECT_CARTRIDGES` дополнительным `LEFT JOIN place_effective_variant pev ON
    pev.place_id = c.place_id` и колонкой `pev.effective_variant` (индекс 18, после `c.version`
    на индексе 17). В `fn list` (единственный вызывающий `SELECT_CARTRIDGES`): после
    `query_map`/сборки `Vec<CartridgeRow>`, постобработкой вычислить `place_path_short` для каждой
    строки — распарсить `effective_variant` через `PathDisplayVariant::from_str`, вызвать
    `shorten_place_path(full_path, variant, &sep_ends, &sep_last_two)`, `None` при отсутствующем
    `place_id`/`effective_variant`. Мирроринг подхода из плана 03 (постобработка после
    query_map, не переписывание `map_row`).
    В `crates/trackly-app/src/dto/cartridge.rs`: добавить `pub place_path_short: Option<String>`
    в `CartridgeDto` сразу после `full_path` (строка ~43) и в `impl From<CartridgeRow>` (строка
    ~69) — `place_path_short: r.place_path_short,`.
  </action>
  <acceptance_criteria>
    - `CartridgeRow`/`CartridgeDto` оба несут `place_path_short: Option<String>`.
    - `list()` возвращает корректный `place_path_short` по формуле `shorten_place_path`.
    - Картридж без `place_id` → `place_path_short: None`.
    - `cargo test -p trackly-app --test export_bindings` регенерирует `bindings.ts` с новым полем
      на `CartridgeDto`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra cartridges_sqlite -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app --test export_bindings -- --test-threads=1</automated>
  </verify>
  <done>`place_path_short` доступен на `list()`, DTO обновлён, биндинги перегенерированы.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| cartridges_sqlite::list → app_settings | Чтение сепараторов только на чтение |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-09 | Information Disclosure | Ничего чувствительного — путь места уже виден в `full_path` сегодня | accept | `place_path_short` — производная от уже экспонированного `full_path`, не расширяет поверхность данных |
</threat_model>

<verification>
- `cargo test -p trackly-infra cartridges_sqlite -- --test-threads=1`
- `cargo test -p trackly-app --test export_bindings -- --test-threads=1`
</verification>

<success_criteria>
- Список картриджей несёт `place_path_short`, вычисленный по единой формуле, симметрично устройствам.
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-04-SUMMARY.md` when done
</output>
