---
phase: 39.1
plan: 10
type: execute
wave: 4
depends_on: ["39.1-03", "39.1-04", "39.1-05", "39.1-07", "39.1-08"]
files_modified:
  - ui/src/features/reports/ReportTable.svelte
  - ui/src/features/devices/DeviceListRow.svelte
  - ui/src/features/cartridges/CartridgeListRow.svelte
  - ui/src/features/places/PlaceContents.svelte
  - ui/src/lib/utils/placePath.ts
  - ui/src/App.svelte
  - ui/src/lib/stores/auth.svelte.ts
  - ui/src/bindings.ts
  - crates/trackly-infra/src/config.rs
  - crates/trackly-infra/tests/config_test.rs
  - trackly.config.toml.example
  - crates/trackly-app/src/dto/auth.rs
  - crates/trackly-app/src/http/auth.rs
  - crates/trackly-app/src/tauri_cmds/auth.rs
autonomous: true
requirements: [PLC-07, PLC-08]
must_haves:
  truths:
    - "D-21: полный путь остаётся в title во всех экранных местах — без изменений; только видимый текст ячейки переключается на place_path_short, атрибут title продолжает читать full_path/place_path"
    - "D-22: ключ [organization] place_path_display в trackly.config.toml удалён целиком, включая enum PlacePathDisplay/deserialize_place_path_display в config.rs, тесты 7-11 в config_test.rs, документацию в trackly.config.toml.example — без разового импорта в БД и без warn-хука на старте"
  artifacts:
    - path: "ui/src/lib/utils/placePath.ts"
      provides: "старые PlacePathDisplay/normalizePlacePathDisplay/shortenPlacePath удалены; остаётся только previewShortenPath/monoReadout из плана 08"
    - path: "crates/trackly-infra/src/config.rs"
      provides: "OrganizationConfig.place_path_display поле удалено, PlacePathDisplay enum удалён"
    - path: "crates/trackly-app/src/dto/auth.rs"
      provides: "AuthStatusDto.place_path_display поле удалено"
  key_links:
    - from: "ReportTable.svelte / DeviceListRow.svelte / CartridgeListRow.svelte / PlaceContents.svelte"
      to: "row.place_path_short"
      via: "прямое чтение готового поля вместо вызова shortenPlacePath(fullPath, authStore.placePathDisplay)"
      pattern: "place_path_short"
---

<objective>
Завершающая волна: переключить 4 фронтовых компонента-потребителя (`ReportTable`, `DeviceListRow`,
`CartridgeListRow`, `PlaceContents`) с локального вычисления `shortenPlacePath(...,
authStore.placePathDisplay)` на прямое чтение готового `place_path_short` поля (уже доступного с
Wave 2/3), затем удалить весь устаревший конфиг-путь (D-22) целиком по обеим сторонам границы —
backend (`config.rs`, `AuthStatusDto.place_path_display`) и frontend (`authStore.placePathDisplay`,
старые экспорты `placePath.ts`).

Purpose: до этого плана 4 компонента всё ещё вызывают старую (дефолтную-на-конфиге) формулу — их
переключение и удаление мёртвого кода НАМЕРЕННО отложены в последнюю волну, чтобы не ломать
компиляцию/`svelte-check` промежуточных волн (удаление `AuthStatusDto.place_path_display` раньше
времени сломало бы `App.svelte`/`auth.svelte.ts`, которые его читают).
Output: 4 компонента читают `place_path_short`; `trackly.config.toml` больше не содержит
`place_path_display` ни в коде, ни в документации; `PlacePathDisplay`/
`normalizePlacePathDisplay`/старый `shortenPlacePath` удалены из `placePath.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из планов 03/04/05/07 (уже смержены): `DeviceDto.place_path_short`, `CartridgeDto.
place_path_short`, `ReportRow.place_path_short`, `PlaceContentDto.place_path_short` — все
`Option<String>`/`string | null` на wire, уже в `ui/src/bindings.ts` после соответствующих
`export_bindings` шагов. `PlaceContentDto.place_path_short` — единственное НЕ-опциональное поле
среди четырёх (мирроринг `full_path: string` на том же DTO, из плана 07 Task 2).

Текущий вызов (удаляется, `ui/src/features/devices/DeviceListRow.svelte:70-71`):
```svelte
<td class="cell" title={device.full_path ?? ''}
  >{device.full_path ? shortenPlacePath(device.full_path, authStore.placePathDisplay) : '—'}</td
>
```
Новый вид: `title` НЕ меняется (`device.full_path ?? ''` остаётся — D-21), содержимое ячейки
становится `{device.place_path_short ?? '—'}`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 4 компонента переключаются на place_path_short (title остаётся полным)</name>
  <files>ui/src/features/reports/ReportTable.svelte, ui/src/features/devices/DeviceListRow.svelte, ui/src/features/cartridges/CartridgeListRow.svelte, ui/src/features/places/PlaceContents.svelte, ui/src/bindings.ts</files>
  <read_first>
    ui/src/features/devices/DeviceListRow.svelte (строка 70-71 — точка правки, `title` атрибут
    НЕ трогать);
    ui/src/features/cartridges/CartridgeListRow.svelte (строки 102-106 — тот же паттерн);
    ui/src/features/places/PlaceContents.svelte (строки 307-310 — тот же паттерн, `title=
    {row.full_path}` НЕ Option, не менять);
    ui/src/features/reports/ReportTable.svelte (строки 102-143 — `formatPlaceCell`/
    `formatCellTitle`/`formatCellDisplay`; `formatCellTitle` ОСТАЁТСЯ без изменений — уже читает
    `row.place_path` через `transformPath: (p) => p`, D-21 не требует правки; только
    `formatCellDisplay`'s вызов `formatPlaceCell(row, col, (p) => shortenPlacePath(p,
    authStore.placePathDisplay))` меняется).
  </read_first>
  <action>
    В `DeviceListRow.svelte`: строка 71 — заменить `{device.full_path ? shortenPlacePath
    (device.full_path, authStore.placePathDisplay) : '—'}` на `{device.place_path_short ?? '—'}`.
    Удалить импорт `shortenPlacePath`/использование `authStore.placePathDisplay` в ЭТОМ файле,
    если больше нигде в файле не используются (проверь остальные строки файла перед удалением
    импорта).
    В `CartridgeListRow.svelte`: аналогично, строки 103-105 — заменить на
    `{cartridge.place_path_short ?? '—'}`.
    В `PlaceContents.svelte`: строка 309 — заменить `{shortenPlacePath(row.full_path,
    authStore.placePathDisplay)}` на `{row.place_path_short}` (поле не-опциональное — прямое
    чтение без `??`).
    В `ReportTable.svelte`: изменить вызов `formatCellDisplay`'s `place_path` ветку (строка ~143)
    с `formatPlaceCell(row, col, (p) => shortenPlacePath(p, authStore.placePathDisplay))` на
    `formatPlaceCell(row, col, () => row.place_path_short ?? '')` — третий аргумент
    (`transformPath`) теперь игнорирует свой сырой-путь аргумент и возвращает уже готовое
    backend-поле; `formatPlaceCell`'s внутренняя логика (композитный префикс `printer_name` +
    `, ` + путь) остаётся БЕЗ изменений — сокращается только путевая часть, префикс не трогается
    (симметрично `combine_printer_and_place` из плана 05). `formatCellTitle` (полный путь, D-21)
    НЕ менять. Удалить импорт `shortenPlacePath` и комментарий-ссылку на quick 260827-ui3 (строки
    101-105), заменить на комментарий, объясняющий, что путь приходит уже сокращённым с бэкенда
    (Phase 39.1).
    Регенерировать `ui/src/bindings.ts` (если ещё не актуален после предыдущих волн) — запусти
    `cargo test -p trackly-app --test export_bindings`.
  </action>
  <acceptance_criteria>
    - Ни один из 4 файлов не содержит вызовов `shortenPlacePath(...)` больше.
    - Ни один из 4 файлов не читает `authStore.placePathDisplay`.
    - `title`-атрибуты во всех 4 местах НЕ изменены — по-прежнему читают полный путь
      (`full_path`/`place_path`), не `place_path_short` (D-21).
    - `ReportTable.svelte`'s композитная ячейка (например «Принтер, Место») сокращает только
      путевую часть — визуально: имя принтера идентично в короткой и полной версии.
    - `svelte-check`/`eslint`/`pnpm build` зелёные.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check --output human</automated>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <done>Все 4 компонента читают `place_path_short` вместо локального вычисления; `title` остаётся
  полным везде (D-21).</done>
</task>

<task type="auto">
  <name>Task 2: Удаление устаревшего конфиг-ключа и AuthStatusDto.place_path_display (D-22)</name>
  <files>crates/trackly-infra/src/config.rs, crates/trackly-infra/tests/config_test.rs, trackly.config.toml.example, crates/trackly-app/src/dto/auth.rs, crates/trackly-app/src/http/auth.rs, crates/trackly-app/src/tauri_cmds/auth.rs, ui/src/App.svelte, ui/src/lib/stores/auth.svelte.ts, ui/src/lib/utils/placePath.ts</files>
  <read_first>
    crates/trackly-infra/src/config.rs (строки 95-215 — `OrganizationConfig.place_path_display`
    поле, `PlacePathDisplay` enum, `deserialize_place_path_display` функция — ВСЁ это удаляется);
    crates/trackly-infra/tests/config_test.rs (строки 162-265 — тесты `test_7`..`test_11`,
    последние в файле, удаление безопасно, не сдвигает нумерацию последующих тестов — их нет);
    trackly.config.toml.example (строки 148-164 — `place_path_display` документация, удалить блок
    целиком);
    crates/trackly-app/src/dto/auth.rs (строки 105-118, 301, 306 — `AuthStatusDto.
    place_path_display` поле + тест, проверяющий его наличие в JSON — тест тоже удаляется/
    правится);
    crates/trackly-app/src/http/auth.rs (строки 216-219 — чтение `ctx.config.organization.
    place_path_display` при сборке `AuthStatusDto`);
    crates/trackly-app/src/tauri_cmds/auth.rs (строки 42-45 — то же чтение на Tauri-транспорте);
    ui/src/App.svelte (строка 15 — импорт `normalizePlacePathDisplay`, строка 27 —
    `authStore.placePathDisplay = normalizePlacePathDisplay(status.place_path_display)`);
    ui/src/lib/stores/auth.svelte.ts (строка 3 — импорт типа `PlacePathDisplay`, строка 20 —
    поле `placePathDisplay` в `authStore`);
    ui/src/lib/utils/placePath.ts (весь файл — `PlacePathDisplay` тип, `normalizePlacePathDisplay`,
    старый `shortenPlacePath` — все три удаляются; НОВЫЙ `previewShortenPath`/`monoReadout` из
    плана 08 ОСТАЮТСЯ, не трогать).
  </read_first>
  <action>
    ПОРЯДОК ОБЯЗАТЕЛЕН (компиляционная зависимость, снизу вверх по цепочке потребления):
    (1) Frontend-потребители (уже переключены Task 1) больше не читают
    `authStore.placePathDisplay` — подтверди это (Task 1 уже выполнен в этом же плане, тот же
    execution).
    (2) В `ui/src/App.svelte`: удалить импорт `normalizePlacePathDisplay` (строка 15) и строку
    `authStore.placePathDisplay = normalizePlacePathDisplay(status.place_path_display);` (строка
    27) — `bootstrapNeeded`/остальная логика `loadAuthStatus` не трогается.
    (3) В `ui/src/lib/stores/auth.svelte.ts`: удалить импорт `PlacePathDisplay` (строка 3) и поле
    `placePathDisplay: 'ends' as PlacePathDisplay,` (строка 20) из `authStore`.
    (4) В `ui/src/lib/utils/placePath.ts`: удалить `export type PlacePathDisplay`,
    `normalizePlacePathDisplay`, старый `shortenPlacePath` — файл-докстринг наверху тоже обновить
    (он ссылается на `AuthStatusDto.place_path_display`/`authStore.placePathDisplay`, оба больше
    не существуют). НЕ трогать `previewShortenPath`/`monoReadout`, добавленные планом 08.
    (5) В `crates/trackly-app/src/dto/auth.rs`: удалить поле `pub place_path_display: String`
    (строка 118) из `AuthStatusDto`, удалить `place_path_display: "ends".to_string(),` (строка
    301) из тестового конструктора, удалить/скорректировать `assert!(json.contains
    ("place_path_display"));` (строка 306) — либо удалить строку целиком, либо заменить на assert
    ОТСУТСТВИЯ поля (`assert!(!json.contains("place_path_display"))`), выбери явный assert
    отсутствия — фиксирует контракт удаления, а не просто убирает проверку молча.
    (6) В `crates/trackly-app/src/http/auth.rs` и `crates/trackly-app/src/tauri_cmds/auth.rs`:
    удалить чтение `ctx.config.organization.place_path_display` из обоих мест сборки
    `AuthStatusDto`.
    (7) В `crates/trackly-infra/src/config.rs`: удалить поле `place_path_display` из
    `OrganizationConfig` (строки 117-129) и его инициализацию в `impl Default` (строка 136),
    удалить весь `enum PlacePathDisplay` (строки 149-186) и `fn deserialize_place_path_display`
    (строки 187-215).
    (8) В `crates/trackly-infra/tests/config_test.rs`: удалить `test_7`..`test_11` целиком
    (строки 162-265) вместе с их doc-комментарием-преамбулой про «quick 260827-ui3»/
    «Orchestrator amendment».
    (9) В `trackly.config.toml.example`: удалить блок документации `place_path_display` (строки
    148-164).
    После удаления — перегенерировать `ui/src/bindings.ts` (`cargo test -p trackly-app --test
    export_bindings`), убедиться, что тип `AuthStatusDto` в биндингах больше НЕ содержит
    `place_path_display`.
  </action>
  <acceptance_criteria>
    - `grep -rn "place_path_display\|PlacePathDisplay" crates/ ui/src trackly.config.toml.example`
      возвращает ТОЛЬКО совпадения в `.planning/`/git-истории (если запускать по рабочему
      дереву исходников — ноль совпадений в перечисленных путях).
    - `cargo build --workspace` компилируется без ошибок (подтверждает, что все 6 backend-файлов
      синхронно очищены — компиляция провалилась бы при частичном удалении).
    - `pnpm --dir ui build` компилируется без ошибок (подтверждает синхронную очистку 3
      frontend-файлов).
    - `crates/trackly-infra/tests/config_test.rs` — ровно тесты 1-6 остаются (7-11 удалены),
      все зелёные.
    - `trackly.config.toml.example` не содержит секции `place_path_display`.
  </acceptance_criteria>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -40</automated>
    <automated>cargo test -p trackly-infra config_test -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app dto::auth -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app --test export_bindings -- --test-threads=1</automated>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <done>`place_path_display`/`PlacePathDisplay` полностью удалены из кода и документации (backend
  + frontend), workspace компилируется, `config_test.rs` содержит только тесты 1-6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Удаление конфиг-ключа | Чисто структурное изменение, не затрагивает границы доверия — источник истины уже переехал в app_settings (планы 01/02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-20 | Information Disclosure (регресс D-21) | Компоненты Task 1 случайно переключают `title`-атрибут на короткий путь вместе с видимым текстом | mitigate | Явный acceptance-критерий "title НЕ изменён" на каждый из 4 файлов; grep-проверка перед коммитом |
| T-39.1-21 | Availability (частичное удаление ломает компиляцию) | Удаление `AuthStatusDto.place_path_display` раньше очистки его потребителей | mitigate | Строгий порядок в Task 2 (frontend-потребители Task 1 → App.svelte/auth store → placePath.ts → backend DTO → config.rs → тесты → документация), `cargo build --workspace`/`pnpm build` как финальный гейт |
</threat_model>

<verification>
- `pnpm --dir ui exec svelte-check --output human`
- `pnpm --dir ui build`
- `cargo build --workspace`
- `cargo test -p trackly-infra config_test -- --test-threads=1`
- `cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1` (полный
  пакет, финальный гейт волны — memory `cargo_no_concurrent_test`)
- `grep -rn "place_path_display\|PlacePathDisplay" crates/ ui/src trackly.config.toml.example`
  → ноль совпадений вне `.planning/`.
</verification>

<success_criteria>
- Все экранные списки/отчёты/содержимое места показывают сокращённый путь через готовое
  backend-поле, title остаётся полным везде.
- `trackly.config.toml`'s `place_path_display` ключ и весь связанный код (backend + frontend)
  удалены без остатка.
- Полный тестовый пакет `trackly-app`/`trackly-infra` зелёный, `pnpm build` зелёный.
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-10-SUMMARY.md` when done
</output>
