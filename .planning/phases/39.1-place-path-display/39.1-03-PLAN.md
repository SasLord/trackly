---
phase: 39.1
plan: 03
type: execute
wave: 2
depends_on: ["39.1-01"]
files_modified:
  - crates/trackly-core/src/domain/devices.rs
  - crates/trackly-infra/src/repos/devices_sqlite.rs
  - crates/trackly-app/src/dto/device.rs
  - crates/trackly-app/src/services/device_service.rs
autonomous: true
requirements: [PLC-08]
must_haves:
  truths:
    - "D-17: экранные списки устройств получают сокращённый путь (place_path_short) через LEFT JOIN place_effective_variant на существующих read-путях list/search_fts/list_grouped"
    - "D-19: autocomplete устройств и restore_from_snapshot_in_tx НЕ получают place_path_short — граница quick 260827-ui3 не двигается, эти пути остаются на full_path"
    - "D-24: DeviceService::export_csv остаётся на ПОЛНОМ пути (device.full_path) — код не меняется, добавлен только тест-замок, фиксирующий отсутствие сокращения"
  artifacts:
    - path: "crates/trackly-core/src/domain/devices.rs"
      provides: "DeviceRow.place_path_short: Option<String>"
      contains: "place_path_short"
    - path: "crates/trackly-infra/src/repos/devices_sqlite.rs"
      provides: "SELECT_DEVICES/search_fts/list_grouped присоединяют place_effective_variant и вычисляют place_path_short"
  key_links:
    - from: "devices_sqlite.rs::list/search_fts/list_grouped"
      to: "place_effective_variant"
      via: "LEFT JOIN place_effective_variant pev ON pev.place_id = d.place_id"
      pattern: "place_effective_variant"
    - from: "devices_sqlite.rs row-mapping"
      to: "trackly_core::domain::places::shorten_place_path"
      via: "вызов формулы на каждой строке с уже прочитанными full_path/effective_variant/sep_ends/sep_last_two"
      pattern: "shorten_place_path"
---

<objective>
Подключить сокращённый путь места (`place_path_short`) к экранным read-путям устройств: список
(`list`), поиск (`search_fts`), группировка (`list_grouped`) — без изменения `autocomplete` и
`restore_from_snapshot_in_tx` (D-19), и без изменения `export_csv` (D-24).

Purpose: PLC-08 требует, чтобы эффективный вариант (с учётом переопределения места и наследования)
был виден в списках устройств, которые сегодня показывают только `full_path`.
Output: `DeviceRow`/`DeviceDto.place_path_short: Option<String>`, три read-пути в
`devices_sqlite.rs` присоединяют `place_effective_variant` и вызывают `shorten_place_path`,
регрессионный тест на `export_csv` (D-24).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из плана 01 (уже смержен):
`trackly_core::domain::places::{PathDisplayVariant, shorten_place_path}` — сигнатура
`shorten_place_path(full_path: &str, variant: PathDisplayVariant, sep_ends: &str, sep_last_two:
&str) -> String`. `PathDisplayVariant::from_str(&str) -> Result<Self, AppError>` — токен из вью
`place_effective_variant.effective_variant` ВСЕГДА валиден (вью гарантирует один из 3 токенов),
но парсинг всё равно проходит через `from_str` для типобезопасности (не unwrap "as is").

Канонический образец чтения scalar-настроек с фолбэком внутри repo-файла
(`crates/trackly-infra/src/repos/cartridges_sqlite.rs:930-950`, метод `low_stock`):
```rust
let threshold: i64 = conn
    .query_row(
        "SELECT value FROM app_settings WHERE key = 'low_stock_threshold'",
        [],
        |r| r.get::<_, String>(0),
    )
    .ok()
    .and_then(|s| s.trim().parse::<i64>().ok())
    .filter(|&t| t > 0)
    .unwrap_or(2);
```
Тот же shape — `conn.query_row(...).ok().and_then(...).unwrap_or(default)` — используй для чтения
`place_path_sep_ends`/`place_path_sep_last_two` ОДИН РАЗ перед выполнением основного запроса (не
на каждую строку), с фолбэками `" // "`/`" / "` на случай отсутствующей строки (defensive,
миграция V039 их уже сеет).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: place_path_short в list/search_fts/list_grouped устройств</name>
  <files>crates/trackly-core/src/domain/devices.rs, crates/trackly-infra/src/repos/devices_sqlite.rs, crates/trackly-app/src/dto/device.rs</files>
  <read_first>
    crates/trackly-core/src/domain/devices.rs (строки 87-105 — `DeviceRow`, поле `full_path` —
    новое поле добавляется сразу после него);
    crates/trackly-infra/src/repos/devices_sqlite.rs (строки 20-62 — `SELECT_DEVICES` константа +
    `from_row`; строки 558-608 — `fn list`; строки 719-881 — `fn search_fts`, JOIN на строке ~858;
    строки 960-1217 — `fn list_grouped`, три отдельных SQL-литерала со своими JOIN на строках
    ~1027/1061/1097; строки 881-960 — `fn autocomplete`, БЕЗ JOIN на `place_full_paths` —
    подтверди, что там и остаётся, ничего не добавляй);
    crates/trackly-app/src/dto/device.rs (строки 33-83 — `DeviceDto` + `impl From<DeviceRow>`).
  </read_first>
  <behavior>
    - `list()` с местом, чей `place_effective_variant` = 'ends' и `full_path` = "Здание А / 1
      этаж / 1-05" → возвращаемый `DeviceRow.place_path_short` = `Some("Здание А // 1-05")`
      (используя сеп из app_settings, дефолт ' // ').
    - Устройство без `place_id` (`place_id IS NULL`) → `place_path_short` = `None` (мирроринг
      текущего поведения `full_path: None`).
    - `search_fts()` с текстовым запросом, находящее устройство с местом → возвращаемая строка
      несёт `place_path_short`, идентичный тому, что вернул бы `list()` для того же устройства
      (одна и та же формула на одном и том же `place_id`).
    - `autocomplete()` — сигнатура/возвращаемый тип НЕ меняется, `place_path_short` там не
      появляется (D-19, не читать явно из этого метода).
  </behavior>
  <action>
    В `crates/trackly-core/src/domain/devices.rs`: добавить поле `pub place_path_short:
    Option<String>` в `DeviceRow` сразу после `pub full_path: Option<String>` (строка ~99).
    В `crates/trackly-infra/src/repos/devices_sqlite.rs`: добавить приватную функцию (рядом с
    `SELECT_DEVICES`) для однократного чтения `(sep_ends, sep_last_two)` из `app_settings`,
    мирроринг `low_stock`'ового паттерна из `<interfaces>`. Расширить `SELECT_DEVICES` константу
    дополнительным `LEFT JOIN place_effective_variant pev ON pev.place_id = d.place_id` и
    дополнительной колонкой `pev.effective_variant` в SELECT-списке (индекс 16, после
    `place_path` на индексе 15). В `from_row`: прочитать `effective_variant: Option<String>` из
    новой колонки; если `Some`, распарсить через `PathDisplayVariant::from_str` и вычислить
    `place_path_short = full_path.as_deref().map(|p| shorten_place_path(p, variant, &sep_ends,
    &sep_last_two))`; если `None` (устройство без места) — `place_path_short = None`. Так как
    `from_row` — свободная функция без доступа к заранее прочитанным `sep_ends`/`sep_last_two`,
    либо (а) преобразовать `from_row` в замыкание, захватывающее `sep_ends`/`sep_last_two` по
    ссылке, передаваемое в `query_map` вместо функции-указателя, либо (б) вычислять
    `place_path_short` ПОСЛЕ `query_map` — постобработкой `Vec<DeviceRow>` перед возвратом из
    `list`/`search_fts`. Выбери вариант (б) — он не требует переписывать сигнатуру `from_row`,
    используемую в нескольких местах, и держит формулу в одном постобработочном месте на каждый
    вызывающий метод.
    Применить симметрично к `search_fts` (SQL-литерал на строке ~858 — добавить тот же
    `LEFT JOIN place_effective_variant` + колонку) и к КАЖДОЙ из трёх SQL-веток `list_grouped`
    (строки ~1027/1061/1097 — три отдельных `pfp.full_path AS place_path` литерала, каждый
    получает соседнюю `pev.effective_variant AS place_variant` колонку и свой JOIN).
    В `crates/trackly-app/src/dto/device.rs`: добавить `pub place_path_short: Option<String>` в
    `DeviceDto` сразу после `full_path` (строка ~53) и в `impl From<DeviceRow> for DeviceDto`
    (строка ~76) — `place_path_short: row.place_path_short,`.
  </action>
  <acceptance_criteria>
    - `DeviceRow`/`DeviceDto` оба несут `place_path_short: Option<String>`.
    - `list()`, `search_fts()`, все три ветки `list_grouped()` возвращают корректный
      `place_path_short`, вычисленный по общей формуле `shorten_place_path` из плана 01.
    - `autocomplete()` НЕ содержит `place_path_short`/JOIN на `place_effective_variant` — сигнатура
      неизменна.
    - Устройство без `place_id` → `place_path_short: None`.
    - `cargo test -p trackly-app --test export_bindings` регенерирует `bindings.ts` с новым полем
      `place_path_short` на `DeviceDto` без ошибок.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra devices_sqlite -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app --test export_bindings -- --test-threads=1</automated>
  </verify>
  <done>`place_path_short` доступен в `list`/`search_fts`/`list_grouped`, отсутствует в
  `autocomplete`, DTO обновлён и биндинги перегенерированы.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-24 тест-замок — export_csv остаётся на полном пути</name>
  <files>crates/trackly-app/src/services/device_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/device_service.rs (строки 830-915 — `export_csv`, колонка
    «Место» строится из `device.full_path`, НЕ из `place_path_short` — код НЕ меняется в этом
    плане, только добавляется тест).
  </read_first>
  <behavior>
    - `export_csv` для устройства с местом, чей `place_effective_variant` = 'ends' (короткий путь
      отличался бы от полного) → CSV-колонка «Место» содержит ПОЛНЫЙ путь (`full_path`), НЕ
      сокращённый — доказывает D-24 (третий CSV-экспорт, вне охвата D-17/D-18, но по духу D-18
      остаётся полным).
  </behavior>
  <action>
    Добавить `#[cfg(test)]` тест (или расширить существующий тестовый модуль, если он уже есть в
    этом файле — проверь наличие `mod tests` в `device_service.rs` перед добавлением нового) с
    именем `export_csv_place_column_stays_full_path_not_shortened`: создать место с
    многосегментным путём (минимум 3 сегмента, например «Здание А / 1 этаж / 1-05» — только
    вымышленные названия), установить `path_variant_override` = 'ends' на этом месте (или на
    умолчании организации), создать устройство с этим `place_id`, вызвать `export_csv`,
    распарсить CSV-вывод и assert, что колонка «Место» содержит строку, РАВНУЮ полному пути
    ("Здание А / 1 этаж / 1-05"), а НЕ сокращённой форме ("Здание А // 1-05") — явное
    доказательство отсутствия регрессии, а не просто "тест проходит потому что код не менялся".
    Никакого изменения самой функции `export_csv`.
  </action>
  <acceptance_criteria>
    - Новый тест существует, явно сравнивает CSV-ячейку с полным (не сокращённым) путём.
    - `device_service.rs::export_csv` не изменена (только тестовый модуль).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app device_service::export_csv_place_column_stays_full_path -- --test-threads=1</automated>
  </verify>
  <done>Регрессионный тест на D-24 существует и зелёный, код `export_csv` не тронут.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| devices_sqlite read-paths → app_settings | Чтение сепараторов только на чтение, без мутации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-07 | Information Disclosure | `place_path_short` в `autocomplete` (случайное появление) | mitigate | Явный acceptance-критерий Task 1 запрещает JOIN в `autocomplete` — D-19 граница подтверждена тестом, отсутствующим полем |
| T-39.1-08 | Information Disclosure | `export_csv` случайно сокращает путь при будущем рефакторинге | mitigate | Регрессионный тест Task 2 фиксирует полный путь как контракт — любое изменение `export_csv`, ломающее это, роняет тест |
</threat_model>

<verification>
- `cargo test -p trackly-infra devices_sqlite -- --test-threads=1`
- `cargo test -p trackly-app device_service:: -- --test-threads=1`
- `cargo test -p trackly-app --test export_bindings -- --test-threads=1`
</verification>

<success_criteria>
- Списки/поиск/группировка устройств несут `place_path_short`, вычисленный по единой формуле.
- Автокомплит и CSV-экспорт устройств остаются на полном пути — оба зафиксированы тестами.
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-03-SUMMARY.md` when done
</output>
