---
phase: 39.1-place-path-display
plan: 02
subsystem: backend
tags: [tauri-command, axum-http, app-settings, specta, rbac]

# Dependency graph
requires:
  - phase: 39.1-01
    provides: "PathDisplayVariant::from_str/as_str; V039-seeded app_settings defaults (place_path_variant/sep_ends/sep_last_two)"
provides:
  - "OrgPathDisplayDto { variant, sep_ends, sep_last_two } in crate::dto::reports"
  - "build_settings_get/set_place_path_defaults + #[tauri::command] wrappers settings_get/set_place_path_defaults"
  - "HTTP mirror POST /api/v1/settings_get_place_path_defaults and /api/v1/settings_set_place_path_defaults"
  - "ui/src/bindings.ts entries OrgPathDisplayDto / settingsGetPlacePathDefaults / settingsSetPlacePathDefaults (generated, gitignored) for Plan 08"
affects: [39.1-08, 39.1-10]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Three-key app_settings group written inside ONE ctx.writer.execute closure (single writer transaction) instead of three separate awaited writes — keeps the org path-display defaults atomic"
    - "Server-side validation before any DB touch: PathDisplayVariant::from_str for the token, explicit .is_empty() (never .trim().is_empty()) for both separators"

key-files:
  created: []
  modified:
    - crates/trackly-app/src/dto/reports.rs
    - crates/trackly-app/src/tauri_cmds/settings_org.rs
    - crates/trackly-app/src/http/settings_org.rs
    - crates/trackly-app/src/specta_export.rs
    - crates/trackly-app/tests/role_endpoint_matrix.rs

key-decisions:
  - "HTTP payload wraps the whole DTO in one `patch` field (SetPlacePathDefaultsPayload { patch: OrgPathDisplayDto }), mirroring SaveOrgFieldsPayload rather than three flat fields — one round-trip for the UI subsection in Plan 08"
  - "PathDisplayVariant::from_str's AppError::Validation field name is remapped from `path_variant` to `variant` at the command boundary so the API error field matches the DTO field the UI binds to"
  - "GET keeps a defensive Rust-side fallback to 'ends'/' // '/' / ' even though V039 seeds the rows — a manually deleted app_settings row must not make the settings screen fail to load"

patterns-established:
  - "Any future org-level setting group follows this shape: one DTO, one `patch`-wrapped payload, one writer closure, validation before write"

requirements-completed: [PLC-07]

# Metrics
duration: ~45min (2 API-terminated executor attempts + inline completion)
completed: 2026-08-28
---

# Phase 39.1 Plan 02: Умолчание сокращения пути в app_settings Summary

**Умолчание отображения пути организации переехало из `trackly.config.toml` в `app_settings`: пара команд `settings_get/set_place_path_defaults` на обоих транспортах, три отдельных ключа, серверная валидация варианта и непустоты разделителей, ролевая матрица (Employee → 403).**

## Performance

- **Duration:** ~45 мин (два прерванных API-ошибкой запуска исполнителя, доведено inline)
- **Completed:** 2026-08-28
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- `OrgPathDisplayDto { variant, sep_ends, sep_last_two }` добавлен в `crates/trackly-app/src/dto/reports.rs` рядом с `OrgPatch`/`OrgSettingsDto` (snake_case, как соседние DTO этого файла).
- `build_settings_get_place_path_defaults` читает три ключа `place_path_variant` / `place_path_sep_ends` / `place_path_sep_last_two` из `app_settings` и возвращает DTO; при отсутствующей строке — защитный фолбэк на `"ends"` / `" // "` / `" / "` (D-09: значения не проходят через `.trim()` ни на одном пути).
- `build_settings_set_place_path_defaults` начинается с `authorize(caller_identity, &Action::ManageSettings)` (T-39.1-04), затем валидирует `variant` через `PathDisplayVariant::from_str` и обе строки-разделителя через `.is_empty()` (D-10), и только потом делает три `INSERT ... ON CONFLICT(key) DO UPDATE` внутри **одного** `ctx.writer.execute` closure — параметризованным bind'ом (T-39.1-05).
- Тонкие `#[tauri::command] #[specta::specta]` обёртки рядом с `settings_get/set_low_stock_basis`, с тем же `resolve_tauri_identity` на SET.
- HTTP-зеркало в `crates/trackly-app/src/http/settings_org.rs`: `SetPlacePathDefaultsPayload { patch }`, `handler_get/set_place_path_defaults` и два роута, зарегистрированных рядом с парой `low_stock_basis`.
- Обе команды зарегистрированы в `specta_export.rs`; `cargo test -p trackly-app --test export_bindings` перегенерировал `ui/src/bindings.ts` с типом `OrgPathDisplayDto` и функциями `settingsGetPlacePathDefaults`/`settingsSetPlacePathDefaults` — готово к плану 08.
- `role_endpoint_matrix.rs` Case 49: Employee-сессия → `POST /api/v1/settings_set_place_path_defaults` → 403 Forbidden.

## Verification

| Проверка | Результат |
|----------|-----------|
| `cargo test -p trackly-app --lib place_path -- --test-threads=1` | ok — 4 юнит-теста (дефолты на свежей БД, отказ на `variant: "bogus"`, отказ на пустом `sep_ends`, round-trip разделителя из одних пробелов без обрезки) |
| `cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1` | ok — 1 passed (включая Case 49) |
| `cargo test -p trackly-app --test export_bindings -- --test-threads=1` | ok — биндинги перегенерированы |
| `cargo clippy -p trackly-app --all-targets` | без ошибок |
| `cargo fmt --all -- --check` | чисто |

## Deviations

- **Плана не касается, но починено попутно:** `cargo fmt --all -- --check` был красным из-за файла плана 01 (`crates/trackly-infra/tests/place_effective_variant.rs`). Формат исправлен отдельным коммитом `style(39.1-01)`, чтобы фаза не набирала волны на красном CI-гейте.
- **Исполнение:** два запуска subagent'а были оборваны ошибками API (`response stopped arriving`, затем `Request timed out`) — Task 1 успел закоммититься в первом, Task 2 остался незакоммиченным в рабочем дереве. План доведён inline оркестратором: изменения Task 2 проверены против плана, прогнаны все три verify-команды и закоммичены атомарно. Отклонений от содержания плана нет.

## Self-Check: PASSED

- [x] `OrgPathDisplayDto` экспортирован с тремя `String`-полями
- [x] GET на свежей БД возвращает `ends` / `" // "` / `" / "`
- [x] SET с `variant: "bogus"` → `AppError::Validation { field: "variant" }`
- [x] SET с `sep_ends: ""` → `AppError::Validation { field: "sep_ends" }` до записи в БД
- [x] Round-trip разделителя без `.trim()`
- [x] Оба роута в `router()`, обе команды в `specta_export.rs`
- [x] Case 49 (Employee → 403) зелёный
