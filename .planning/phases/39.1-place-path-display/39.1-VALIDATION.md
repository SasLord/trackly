---
phase: 39.1
slug: place-path-display
status: draft
nyquist_compliant: true
wave_0_complete: false  # Wave 0 (Plan 01, Wave 1) not yet executed — planning-time only
created: 2026-08-28
---

# Phase 39.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `39.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust unit + integration, workspace-standard) + `svelte-check` / `eslint` / `pnpm --dir ui build` (фронт, статические гейты) |
| **Config file** | none — конвенция workspace (`Cargo.toml` + `#[cfg(test)]` модули, `crates/*/tests/*.rs`) |
| **Quick run command** | `cargo test -p trackly-core shorten_place_path -- --test-threads=1` |
| **Full suite command** | `cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1` |
| **Estimated runtime** | quick ~15s · full ~4–6 min |

**Environment requirements (обязательно, иначе `trackly-app` тесты падают):**
- `TRACKLY_AD_MOCK=1`, `TRACKLY_SNMP_MOCK=1`
- реальный собранный `ui/dist` (`pnpm --dir ui build`) — сервер-мод тесты читают его с диска

**Конкурентность:** ровно ОДИН `cargo test` за раз. Два процесса конкурируют за
lock в `target/` и выглядят как многоминутное зависание.

**Известный зависающий тест:** `login_remember_persistent_cookie` — pre-existing,
всегда прогонять с `--skip`, включая `-p trackly-app`.

---

## Sampling Rate

- **After every task commit:** узкий `cargo test -p <crate> <module>::` на затронутый модуль
- **After every plan wave:** `cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1` (полный пакет, один процесс)
- **Before `/gsd-verify-work`:** полный набор зелёный + `svelte-check` + `eslint` + `pnpm --dir ui build`
- **Max feedback latency:** ~15s (quick) / ~360s (full wave gate)

**Печатные формы:** статические гейты и text-extraction тесты НЕ видят перекрытий и
переполнений в PDF. Изменение шаблона акта (D-20) проверяется живым рендером —
throwaway `#[ignore]`-тест + визуальный осмотр PNG, по прецеденту `act_pdf_word_fidelity`.

---

## Per-Task Verification Map

Карта заполнена планировщиком после разбиения фазы на 10 планов (10 волна-групп: Wave 1-4).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| T-01-01 | 01 | 1 | PLC-07/08 | T-39.1-01/02 | Миграция V039 применяется на свежей БД | integration | `cargo test -p trackly-infra --lib db::migrations -- --test-threads=1` | ❌ new | ⬜ pending |
| T-01-02 | 01 | 1 | PLC-08 | T-39.1-01/02 | place_effective_variant: наследование/top-level/перемещение (D-02/03/04/06/23) | integration | `cargo test -p trackly-infra --test place_effective_variant -- --test-threads=1` | ❌ new | ⬜ pending |
| T-01-03 | 01 | 1 | PLC-07/08 | T-39.1-03 | shorten_place_path формула (D-13..16) | unit | `cargo test -p trackly-core domain::places -- --test-threads=1` | ❌ new | ⬜ pending |
| T-02-01 | 02 | 2 | PLC-07 | T-39.1-06 | settings_get/set_place_path_defaults, D-09/D-10 валидация | unit+integration | `cargo test -p trackly-app settings_org::place_path -- --test-threads=1` | ❌ new | ⬜ pending |
| T-02-02 | 02 | 2 | PLC-07 | T-39.1-04 | HTTP-роуты + specta + Case 49 (Employee → 403) | integration | `cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1` | ⚠️ existing+case | ⬜ pending |
| T-03-01 | 03 | 2 | PLC-08 | T-39.1-07 | place_path_short в list/search_fts/list_grouped, НЕ в autocomplete (D-19) | integration | `cargo test -p trackly-infra devices_sqlite -- --test-threads=1` | ⚠️ existing | ⬜ pending |
| T-03-02 | 03 | 2 | PLC-08 | T-39.1-08 | export_csv остаётся полным (D-24) | unit | `cargo test -p trackly-app device_service::export_csv_place_column_stays_full_path -- --test-threads=1` | ❌ new | ⬜ pending |
| T-04-01 | 04 | 2 | PLC-08 | — | place_path_short в list() картриджей | integration | `cargo test -p trackly-infra cartridges_sqlite -- --test-threads=1` | ⚠️ existing | ⬜ pending |
| T-05-01 | 05 | 3 | PLC-08 | — | place_path_short на ReportRow, 5 доменов | integration | `cargo test -p trackly-app report_service:: -- --test-threads=1` | ⚠️ existing | ⬜ pending |
| T-05-02 | 05 | 3 | PLC-08 | T-39.1-13/14 | row_field(shorten) CSV/PDF асимметрия (D-17/18) | unit+integration | `cargo test -p trackly-app report_service::csv_pdf_asymmetry -- --test-threads=1` | ❌ new | ⬜ pending |
| T-06-01 | 06 | 2 | PLC-08 | T-39.1-12 | Пре-снапшот v27 в KNOWN_LEGACY_DEFAULTS | unit | `cargo test -p trackly-app pdf::html_templates -- --test-threads=1` | ⚠️ existing | ⬜ pending |
| T-06-02 | 06 | 2 | PLC-08 | T-39.1-10/11 | place_path_short в render_pdf по текущему варианту (D-20) | integration+manual | `cargo test -p trackly-app act_service::render_pdf -- --ignored --test-threads=1` | ❌ new | ⬜ pending |
| T-07-01 | 07 | 3 | PLC-08 | T-39.1-15/16/17 | places_set_path_variant CAS-мутация, MutatePlaces (D-06/12) | unit+integration | `cargo test -p trackly-app place_service::set_path_variant -- --test-threads=1` | ❌ new | ⬜ pending |
| T-07-02 | 07 | 3 | PLC-08 | T-39.1-15 | PlaceContents place_path_short + Case 50 (Employee → 403) | integration | `cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1` | ⚠️ existing+case | ⬜ pending |
| T-08-01 | 08 | 3 | PLC-07 | — | Input.svelte mono + previewShortenPath формула | static | `pnpm --dir ui exec svelte-check --output human` | ⚠️ existing | ⬜ pending |
| T-08-02 | 08 | 3 | PLC-07 | T-39.1-18 | OrgSettings.svelte подраздел, живой предпросмотр (D-11) | static+manual | `pnpm --dir ui build` | ⚠️ existing | ⬜ pending |
| T-09-01 | 09 | 4 | PLC-08 | T-39.1-19 | PlaceFormModal дропдаун + вторая RPC (D-05/12) | static+manual | `pnpm --dir ui build` | ⚠️ existing | ⬜ pending |
| T-10-01 | 10 | 4 | PLC-07/08 | T-39.1-20 | 4 компонента читают place_path_short, title остаётся полным (D-21) | static | `pnpm --dir ui build` | ⚠️ existing | ⬜ pending |
| T-10-02 | 10 | 4 | PLC-07/08 | T-39.1-21 | Удаление place_path_display конфига (D-22), full workspace build | integration | `cargo build --workspace` | ⚠️ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Требование → тест (контракт, из RESEARCH.md)

| Req / Decision | Проверяемое поведение | Test Type | Automated Command | File |
|----------------|----------------------|-----------|-------------------|------|
| PLC-07 | Умолчание организации (вариант + 2 разделителя) сохраняется/читается через `app_settings` | unit + integration | `cargo test -p trackly-app settings_org::` | ❌ Wave 0 |
| PLC-07 | Employee → `settings_set_place_path_*` → 403 (`ManageSettings`) | integration | `cargo test -p trackly-app --test role_endpoint_matrix` | ⚠️ файл есть, нужен новый Case (образец — Case 44) |
| PLC-08 | Переопределение места через `places_set_path_variant`, гейт `Action::MutatePlaces` (НЕ `ManageSettings`, по D-12) | integration | `cargo test -p trackly-app --test role_endpoint_matrix` | ⚠️ новый Case по образцу `MutatePlaces` |
| PLC-08 / D-03 / D-04 | Наследование по цепочке предков; переопределение не сбрасывается при перемещении; «Как у родителя» подхватывает вариант нового родителя | integration (SQL-вью) | `cargo test -p trackly-infra place_effective_variant` | ❌ Wave 0 |
| PLC-08 / D-06 | Место верхнего уровня (`parent_id IS NULL`) наследует умолчание организации напрямую | integration | тот же файл | ❌ Wave 0 |
| D-13…D-16 | Таблица «вход → выход»: 3 варианта × длины пути 1/2/3/4+ | unit | `cargo test -p trackly-core shorten_place_path` | ❌ Wave 0 |
| D-09 / D-10 | Разделитель `", "` переживает round-trip без тримминга пробелов; пустой разделитель отклоняется `AppError::Validation`; разделитель из одних пробелов принимается | unit + integration | `cargo test -p trackly-app settings_org::path_display_separator` | ❌ Wave 0 |
| D-17 / D-18 | Асимметрия CSV vs PDF на одном `ReportResponse`: PDF сокращён, CSV полный | integration | `cargo test -p trackly-app report_service::csv_pdf_asymmetry` | ❌ Wave 0 |
| D-17 | `combine_printer_and_place`: сокращается ТОЛЬКО путевая часть, имя принтера не тронуто | unit | `cargo test -p trackly-app report_service::combine_printer_and_place` | ⚠️ файл есть (тесты ~2311–2354), нужен новый кейс |
| D-20 | Печатная форма акта берёт вариант по ТЕКУЩЕМУ `acts.place_id`, применяя его к замороженному `place_path_snapshot`; `place_id IS NULL` или удалённое место → умолчание организации, без паники | integration + живой рендер | `cargo test -p trackly-app act_service::render_pdf -- --ignored` | ❌ Wave 0 |
| D-22 | Загрузка `trackly.config.toml` без `[organization].place_path_display` не ломается; тесты 7–11 в `config_test.rs` удалены, остальные зелёные | unit | `cargo test -p trackly-infra config_test` | ✅ файл есть (задача — удаление) |

---

## Wave 0 Requirements

- [ ] `crates/trackly-core/src/domain/places.rs` (или соседний новый модуль) — юнит-тесты
      `shorten_place_path` по полной таблице «вход → выход» (RESEARCH.md Pattern 3).
      **Первыми в фазе** — формула фундамент для всего остального.
- [ ] `crates/trackly-infra/tests/` — тесты вью `place_effective_variant`: цепочка предков,
      `parent_id IS NULL` → умолчание организации, живые сценарии перемещения (D-03/D-04).
- [ ] `crates/trackly-app/tests/role_endpoint_matrix.rs` — 2 новых кейса:
      Employee → `settings_set_place_path_*` → 403; Employee → `places_set_path_variant` → 403.
- [ ] `report_service.rs` тест-модуль — асимметрия CSV/PDF (`place_path`, `printer_place`),
      по образцу существующих `combine_printer_and_place_*` / `row_field_printer_place_*`.
- [ ] `act_service.rs` — интеграционный кейс на `place_id IS NULL` / исчезнувшее место.

Инфраструктура тестов существует — Wave 0 добавляет только новые файлы/кейсы,
установка фреймворка не требуется.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Живой предпросмотр в «Настройки → Организация»: сетка 3×2 пересчитывается при вводе разделителя, оба поля видны одновременно | PLC-07 / D-11 | Рантайм-реактивность Svelte-рун; `svelte-check`/`eslint`/`build` её не видят | Запустить `cargo tauri dev`, открыть Настройки → Организация, менять разделители, убедиться, что все 6 ячеек обновляются без переключения радио |
| Дропдаун «Вариант сокращения» в форме Места — кастомный `Dropdown`, не нативный `<select>` | PLC-08 / D-12 | Визуальная проверка компонента | Открыть создание/редактирование Места, сверить с дропдауном «Тип» в том же модале |
| Печатная форма акта: сокращённый путь не ломает вёрстку, не перекрывается и не переполняет строку | D-20 | Text-extraction тесты не видят overlap/overflow | Отрендерить реальный PDF акта с длинным путём, открыть PNG (`qlmanage`), осмотреть |
| LAN-браузер видит те же значения, что и десктоп | PLC-07 / SC#6 | Два вебвью, разные транспорты | `pnpm --dir ui build`, открыть сервер-мод в браузере, сверить Настройки и списки |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (18 задач в 10 планах,
      каждая с `<verify><automated>`; frontend-задачи используют static gates
      `svelte-check`/`eslint`/`pnpm build` + Manual-Only Verifications таблица выше)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (Plan 01 = Wave 1 = формула + миграция + вью-тесты,
      выполняется первым)
- [x] No watch-mode flags
- [x] Feedback latency < 360s (quick ~15-30s на задачу, full-wave gate ~4-6 мин)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-generated, 2026-08-28 (10 планов, 18 задач, Wave 1-4)
