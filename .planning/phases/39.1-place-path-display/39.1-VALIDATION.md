---
phase: 39.1
slug: place-path-display
status: audited
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-28
audited: 2026-09-01
---

# Phase 39.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `39.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust unit + integration, workspace-standard) + `pnpm lint` (фронт: eslint, prettier и 7 гейтов `ui/scripts/check-*.mjs` — из них `check-placepath-parity.mjs` реально ИСПОЛНЯЕТ формулу, остальные структурные) + `svelte-check` |
| **Config file** | none — конвенция workspace (`Cargo.toml` + `#[cfg(test)]` модули, `crates/*/tests/*.rs`) |
| **Quick run command** | `cargo test -p trackly-core shorten_place_path -- --test-threads=1` |
| **Full suite command** | `cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1` |
| **Estimated runtime** | quick ~15s · full ~4–6 min |

**Environment requirements (обязательно, иначе `trackly-app` тесты падают):**
- `TRACKLY_AD_MOCK=1`, `TRACKLY_SNMP_MOCK=1`
- реальный собранный `ui/dist` (`pnpm --dir ui build`) — сервер-мод тесты читают его с диска

**Конкурентность:** ровно ОДИН `cargo test` за раз. Два процесса конкурируют за
lock в `target/` и выглядят как многоминутное зависание.

**Известный зависающий тест:** `login_remember_persistent_cookie` — pre-existing,
всегда прогонять с `--skip`, включая `-p trackly-app`.

---

## Sampling Rate

- **After every task commit:** узкий `cargo test -p <crate> <module>::` на затронутый модуль
- **After every plan wave:** `cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1` (полный пакет, один процесс)
- **Before `/gsd-verify-work`:** полный набор зелёный + `svelte-check` + `pnpm lint` (включает оба гейта пути места) + `pnpm --dir ui build`
- **Max feedback latency:** ~15s (quick) / ~360s (full wave gate)

**Печатные формы:** статические гейты и text-extraction тесты НЕ видят перекрытий и
переполнений в PDF. Изменение шаблона акта (D-20) проверяется живым рендером —
throwaway `#[ignore]`-тест + визуальный осмотр PNG, по прецеденту `act_pdf_word_fidelity`.

---

## Per-Task Verification Map

Карта заполнена планировщиком после разбиения фазы на 10 планов (10 волна-групп: Wave 1-4).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| T-01-01 | 01 | 1 | PLC-07/08 | T-39.1-01/02 | Миграция V039 применяется на свежей БД | integration | `cargo test -p trackly-infra --lib db::migrations -- --test-threads=1` | ✅ есть | ✅ green |
| T-01-02 | 01 | 1 | PLC-08 | T-39.1-01/02 | place_effective_variant: наследование/top-level/перемещение (D-02/03/04/06/23) | integration | `cargo test -p trackly-infra --test place_effective_variant -- --test-threads=1` | ✅ есть (6) | ✅ green |
| T-01-03 | 01 | 1 | PLC-07/08 | T-39.1-03 | shorten_place_path формула (D-13..16) | unit | `cargo test -p trackly-core domain::places -- --test-threads=1` | ✅ есть (27) | ✅ green |
| T-02-01 | 02 | 2 | PLC-07 | T-39.1-06 | settings_get/set_place_path_defaults, D-09/D-10 валидация | unit+integration | `cargo test -p trackly-app settings_org::place_path -- --test-threads=1` | ✅ есть (4) | ✅ green |
| T-02-02 | 02 | 2 | PLC-07 | T-39.1-04 | HTTP-роуты + specta + Case 49 (Employee → 403) | integration | `cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1` | ✅ Case 49 :1525 | ✅ green |
| T-03-01 | 03 | 2 | PLC-08 | T-39.1-07 | place_path_short в list/search_fts/list_grouped, НЕ в autocomplete (D-19) | integration | `cargo test -p trackly-infra --test devices_place_path_short -- --test-threads=1` | ✅ есть (5) | ✅ green |
| T-03-02 | 03 | 2 | PLC-08 | T-39.1-08 | export_csv остаётся полным (D-24) | unit | `cargo test -p trackly-app --test devices_csv_export -- --test-threads=1` | ✅ есть (11) | ✅ green |
| T-04-01 | 04 | 2 | PLC-08 | — | place_path_short в list() картриджей | integration | `cargo test -p trackly-infra --test cartridges_place_path_short -- --test-threads=1` | ✅ есть (3) | ✅ green |
| T-05-01 | 05 | 3 | PLC-08 | — | place_path_short на ReportRow, 5 доменов | integration | `cargo test -p trackly-app --test report_place_path_short -- --test-threads=1` | ✅ есть (6) | ✅ green |
| T-05-02 | 05 | 3 | PLC-08 | T-39.1-13/14 | row_field(shorten) CSV/PDF асимметрия (D-17/18) | unit+integration | `cargo test -p trackly-app report_service::csv_pdf_asymmetry -- --test-threads=1` | ✅ есть | ✅ green |
| T-06-01 | 06 | 2 | PLC-08 | T-39.1-12 | Пре-снапшот v27 в KNOWN_LEGACY_DEFAULTS | unit | `cargo test -p trackly-app pdf::html_templates -- --test-threads=1` | ✅ есть (16) | ✅ green |
| T-06-02 | 06 | 2 | PLC-08 | T-39.1-10/11 | place_path_short в render_pdf по текущему варианту (D-20) | integration+manual | `cargo test -p trackly-app --test acts_place_path_short -- --test-threads=1` | ✅ есть (3, **не** `#[ignore]`) | ✅ green |
| T-07-01 | 07 | 3 | PLC-08 | T-39.1-15/16/17 | places_set_path_variant CAS-мутация, MutatePlaces (D-06/12) | unit+integration | `cargo test -p trackly-app place_service::set_path_variant -- --test-threads=1` | ✅ есть (5) | ✅ green |
| T-07-02 | 07 | 3 | PLC-08 | T-39.1-15 | PlaceContents place_path_short + Case 50 (Employee → 403) | integration | `cargo test -p trackly-app --test role_endpoint_matrix --test places_contents -- --test-threads=1` | ✅ Case 50 :1546 + 5 | ✅ green |
| T-08-01 | 08 | 3 | PLC-07 | — | previewShortenPath побайтово равен Rust shorten_place_path (WR-03) | **исполняющий** (golden-фикстура) | `cd ui && node scripts/check-placepath-parity.mjs` + `cargo test -p trackly-core shorten_place_path_matches_golden_fixture` | ✅ **добавлен аудитом** | ✅ green |
| T-08-02 | 08 | 3 | PLC-07 | T-39.1-18 | OrgSettings.svelte подраздел, живой предпросмотр (D-11) | static+manual | `cd ui && pnpm svelte-check` (0 errors) + Manual-Only #1 | ⚠️ рантайм-рун — только вручную | ✅ green (UAT #1 passed) |
| T-09-01 | 09 | 4 | PLC-08 | T-39.1-19 | PlaceFormModal дропдаун + вторая RPC (D-05/12) | static+manual | `cd ui && pnpm svelte-check` + Manual-Only #2 | ⚠️ сеть/CAS — только вручную | ✅ green (UAT #2 passed) |
| T-10-01 | 10 | 4 | PLC-07/08 | T-39.1-20 | 5 компонентов читают place_path_short, title остаётся полным (D-21, WR-04) | **структурный гейт** | `cd ui && node scripts/check-place-path-short.mjs` | ✅ **добавлен аудитом** | ✅ green |
| T-10-02 | 10 | 4 | PLC-07/08 | T-39.1-21 | Удаление place_path_display конфига (D-22), full workspace build | integration | `cargo test -p trackly-infra --test config_test -- --test-threads=1` | ✅ есть (6) | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Требование → тест (контракт, из RESEARCH.md)

| Req / Decision | Проверяемое поведение | Test Type | Automated Command | File |
|----------------|----------------------|-----------|-------------------|------|
| PLC-07 | Умолчание организации (вариант + 2 разделителя) сохраняется/читается через `app_settings` | unit + integration | `cargo test -p trackly-app settings_org::place_path` | ✅ `tauri_cmds/settings_org.rs` (4 теста) |
| PLC-07 | Employee → `settings_set_place_path_*` → 403 (`ManageSettings`) | integration | `cargo test -p trackly-app --test role_endpoint_matrix` | ✅ Case 49 (`role_endpoint_matrix.rs:1525`) |
| PLC-08 | Переопределение места через `places_set_path_variant`, гейт `Action::MutatePlaces` (НЕ `ManageSettings`, по D-12) | integration | `cargo test -p trackly-app --test role_endpoint_matrix` | ✅ Case 50 (`role_endpoint_matrix.rs:1546`) + `place_service::set_path_variant` (5) |
| PLC-08 / D-03 / D-04 | Наследование по цепочке предков; переопределение не сбрасывается при перемещении; «Как у родителя» подхватывает вариант нового родителя | integration (SQL-вью) | `cargo test -p trackly-infra --test place_effective_variant` | ✅ `place_effective_variant.rs` (6) |
| PLC-08 / D-06 | Место верхнего уровня (`parent_id IS NULL`) наследует умолчание организации напрямую | integration | тот же файл | ✅ там же |
| D-13…D-16 | Таблица «вход → выход»: 3 варианта × длины пути 1/2/3/4+ | unit | `cargo test -p trackly-core domain::places` | ✅ `places.rs` (27, включая golden-фикстуру) |
| D-09 / D-10 | Разделитель `", "` переживает round-trip без тримминга пробелов; пустой разделитель отклоняется `AppError::Validation`; разделитель из одних пробелов принимается | unit + integration | `cargo test -p trackly-app settings_org::place_path` | ✅ 4 теста (`set_with_empty_sep_ends_is_rejected`, `set_with_whitespace_only_sep_round_trips_untrimmed`, …) |
| D-17 / D-18 | Асимметрия CSV vs PDF на одном `ReportResponse`: PDF сокращён, CSV полный | integration | `cargo test -p trackly-app report_service::csv_pdf_asymmetry` | ✅ `report_service.rs:2522` |
| D-17 | `combine_printer_and_place`: сокращается ТОЛЬКО путевая часть, имя принтера не тронуто | unit | `cargo test -p trackly-app report_service::row_field_printer_place_shorten_true_shortens_only_place_not_printer_name` | ✅ кейс добавлен |
| D-20 | Печатная форма акта берёт вариант по ТЕКУЩЕМУ `acts.place_id`, применяя его к замороженному `place_path_snapshot`; `place_id IS NULL` или удалённое место → умолчание организации, без паники | integration + живой рендер | `cargo test -p trackly-app --test acts_place_path_short` | ✅ `acts_place_path_short.rs` (3, обычные — НЕ `#[ignore]`, как планировалось) |
| D-22 | Загрузка `trackly.config.toml` без `[organization].place_path_display` не ломается; тесты 7–11 в `config_test.rs` удалены, остальные зелёные | unit | `cargo test -p trackly-infra --test config_test` | ✅ 6 тестов зелёные |
| D-11 / WR-03 | JS-зеркало `previewShortenPath` побайтово равно Rust `shorten_place_path` на общей golden-фикстуре (23 кейса, включая 2-сегментный путь — тот, на котором зеркало разошлось) | **исполняющий, обе стороны** | `node ui/scripts/check-placepath-parity.mjs` + `cargo test -p trackly-core shorten_place_path_matches_golden_fixture` | ✅ **добавлено аудитом 2026-09-01** — фикстура `scripts/fixtures/place-path/shorten-cases.json` |
| D-21 / WR-04 | Ячейка «Место» рендерит `place_path_short`, а полный путь остаётся в `title` — во всех 5 компонентах (списки устройств плоский/группированный, картриджи, содержимое места, таблица отчётов) | **структурный гейт** | `node ui/scripts/check-place-path-short.mjs` | ✅ **добавлено аудитом 2026-09-01** |

---

## Wave 0 Requirements

Все выполнены (фаза исполнена, прогон подтверждён аудитом 2026-09-01):

- [x] `crates/trackly-core/src/domain/places.rs` — юнит-тесты `shorten_place_path`
      по полной таблице «вход → выход». **27 тестов зелёные** (26 плановых + 1
      golden-фикстура, добавленная аудитом).
- [x] `crates/trackly-infra/tests/place_effective_variant.rs` — цепочка предков,
      `parent_id IS NULL` → умолчание организации, сценарии перемещения (D-03/D-04).
      **6 тестов зелёные.**
- [x] `crates/trackly-app/tests/role_endpoint_matrix.rs` — Case 49 (`:1525`,
      Employee → `settings_set_place_path_defaults` → 403) и Case 50 (`:1546`,
      Employee → `places_set_path_variant` → 403). **Оба зелёные.**
- [x] `report_service.rs` — асимметрия CSV/PDF
      (`csv_pdf_asymmetry_same_report_response_differs_on_place_path`, `:2522`)
      + `row_field_printer_place_shorten_true_shortens_only_place_not_printer_name`.
- [x] `acts_place_path_short.rs` — `place_id IS NULL` / исчезнувшее место / текущий
      вариант вместо создания. **3 теста, обычные (не `#[ignore]`)** — сильнее, чем
      планировалось: живой рендер остался ручной проверкой вёрстки, но сам контракт
      данных теперь держится автоматом.

Инфраструктура тестов существовала до фазы — Wave 0 добавил только новые файлы и кейсы.

## Manual-Only Verifications

> Прогон выполнен пользователем 2026-08-31, все 4 пункта passed
> (`39.1-HUMAN-UAT.md`, сборка `v1.4.0-phase39.3` из `a06eff15`).
>
> **Аудит 2026-09-01 снял два пункта из «только руками» в автоматические:**
> формула предпросмотра (была «сверять глазами» → `check-placepath-parity.mjs`)
> и чтение `place_path_short` компонентами (была визуальная сверка →
> `check-place-path-short.mjs`). Оставшиеся ниже пункты ручные по существу:
> рантайм-реактивность рун, конкурентные сценарии и вёрстка печатной формы.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Живой предпросмотр в «Настройки → Организация»: сетка 3×2 пересчитывается при вводе разделителя, оба поля видны одновременно | PLC-07 / D-11 | Рантайм-реактивность Svelte-рун; `svelte-check`/`eslint`/`build` её не видят | Запустить `cargo tauri dev`, открыть Настройки → Организация, менять разделители, убедиться, что все 6 ячеек обновляются без переключения радио |
| Дропдаун «Вариант сокращения» в форме Места — кастомный `Dropdown`, не нативный `<select>` | PLC-08 / D-12 | Визуальная проверка компонента | Открыть создание/редактирование Места, сверить с дропдауном «Тип» в том же модале |
| Печатная форма акта: сокращённый путь не ломает вёрстку, не перекрывается и не переполняет строку | D-20 | Text-extraction тесты не видят overlap/overflow | Отрендерить реальный PDF акта с длинным путём, открыть PNG (`qlmanage`), осмотреть |
| LAN-браузер видит те же значения, что и десктоп | PLC-07 / SC#6 | Два вебвью, разные транспорты | `pnpm --dir ui build`, открыть сервер-мод в браузере, сверить Настройки и списки |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (18 задач в 10 планах,
      каждая с `<verify><automated>`; frontend-задачи используют static gates
      `svelte-check`/`eslint`/`pnpm build` + Manual-Only Verifications таблица выше)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (Plan 01 = Wave 1 = формула + миграция + вью-тесты,
      выполняется первым)
- [x] No watch-mode flags
- [x] Feedback latency < 360s (quick ~15-30s на задачу, full-wave gate ~4-6 мин)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-generated, 2026-08-28 (10 планов, 18 задач, Wave 1-4);
**audited 2026-09-01** — вся карта прогнана, статусы проставлены по фактическому прогону,
2 дыры закрыты новыми гейтами.

---

## Validation Audit 2026-09-01

| Metric | Count |
|--------|-------|
| Строк карты проверено | 19 |
| Подтверждено зелёными прогоном | 17 |
| Дыр найдено | 2 |
| Дыр закрыто | 2 |
| Эскалировано в Manual-Only | 0 |

### Что прогонялось

| Команда | Результат |
|---------|-----------|
| `cargo test -p trackly-core domain::places -- --test-threads=1` | ✅ 27 passed |
| `cargo test -p trackly-infra -- --test-threads=1` | ✅ весь пакет зелёный (130 lib + 12 integration-бинарей) |
| `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=1` | ✅ дошло до Doc-tests без падений |
| `cargo clippy -p trackly-core --all-targets -- -D warnings` | ✅ чисто |
| `cargo fmt -p trackly-core -- --check` | ✅ чисто |
| `cd ui && pnpm svelte-check` | ✅ 0 errors (60 pre-existing warnings) |
| `cd ui && pnpm lint` | ✅ eslint + prettier + **7** гейтов `check-*.mjs` |
| `node scripts/check-privacy.mjs --hashes scripts/privacy-tokens.sha256` | ✅ 0 нарушений |

### Найденные дыры

Обе — строки, где карта заявляла покрытие, а названная команда физически не могла
увидеть отказ. Обе уже регрессировали ВНУТРИ этой фазы и чинились руками, не оставив
после себя ни одного теста.

**GAP-1 → строка T-08-01.** Заявлено: `svelte-check`. Реально: `previewShortenPath`
(`ui/src/lib/utils/placePath.ts`) — рукописное JS-зеркало Rust-формулы
`shorten_place_path`. Тайпчекер видит типы, а не формулу; собственный комментарий в
файле признавал «нет JS-тестов — проверять глазами при правке». Зеркало разошлось с
Rust на 2-сегментном пути под вариантом `last` (**WR-03**), уехало в сборку, найдено
ревьюером, починено вручную в `10707242`.

**GAP-2 → строка T-10-01.** Заявлено: `pnpm --dir ui build`. Успешная сборка не
доказывает, КАКОЕ поле читает компонент. Сгруппированный список устройств молча
игнорировал `place_path_short` и расходился с плоским видом (**WR-04**) — поймал
человек-ревьюер, починено вручную в `8fa995e5`.

### Чем закрыты

| Гейт | Тип | Файлы | Самотест «падает на регрессе» |
|------|-----|-------|-------------------------------|
| `ui/scripts/check-placepath-parity.mjs` + `shorten_place_path_matches_golden_fixture` | исполняющий, обе стороны пришпилены к одной фикстуре | `scripts/fixtures/place-path/shorten-cases.json` (23 кейса), `crates/trackly-core/src/domain/places.rs` | ✅ на дофиксной форме WR-03 падает 2 кейсами из 23 (`two-segments-last-*`) |
| `ui/scripts/check-place-path-short.mjs` | структурный, по образцу `check-print-isolation.mjs` | 5 компонентов: `DeviceListRow`, `DeviceGroupRow`, `CartridgeListRow`, `PlaceContents`, `ReportTable` | ✅ ловит 3 разные формы регресса: INV-1 (дофиксный `DeviceGroupRow` из WR-04), INV-2 (`title` потерял полный путь), INV-4 (`formatCellTitle` начал сокращать) |

Оба вписаны в `lint` в `ui/package.json`, который CI прогоняет шагом «pnpm lint»
(`.github/workflows/ci-full.yml:126`) — иначе это были бы не гейты, а скрипты.

### Границы гарантии (честно)

- `check-place-path-short.mjs` — **структурный**: он доказывает, что компонент читает
  нужное поле, и НЕ доказывает, что ячейка отрисовалась правильно. Вёрстка, обрезание
  и hover остаются в Manual-Only, как и было. Его задача — падать, когда выстраданный
  фикс молча убрали при рефакторинге.
- `check-placepath-parity.mjs` требует пакет `typescript` (уже прямой devDependency
  `ui/package.json`) — снять аннотации типов перед исполнением. Node 20-совместимо,
  без `--experimental-strip-types` (тот появился только в 22.18, а CI пинит node 20).
- Пункт «живой предпросмотр реактивен» (D-11) закрыть автоматом по-прежнему нечем:
  рантайм-реактивность рун не видна компиляционным гейтам. Паритет ФОРМУЛЫ теперь
  автоматический, реактивность СЕТКИ — нет.
- Строка T-06-02 в карте исправлена: план предполагал `#[ignore]`-тест, исполнение
  сделало 3 обычных теста в `acts_place_path_short.rs`. Это сильнее плана, но живой
  рендер PDF всё равно остаётся ручным (перекрытия и переполнение text-extraction
  не видит — прецедент `act_pdf_word_fidelity`).
