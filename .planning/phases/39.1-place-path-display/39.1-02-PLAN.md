---
phase: 39.1
plan: 02
type: execute
wave: 2
depends_on: ["39.1-01"]
files_modified:
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/src/tauri_cmds/settings_org.rs
  - crates/trackly-app/src/http/settings_org.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: true
requirements: [PLC-07]
must_haves:
  truths:
    - "D-07: умолчание пути организации читается/пишется через новую пару Tauri-команд + HTTP-эндпоинтов settings_get/set_place_path_defaults, живущих рядом с существующими settings_org.rs low_stock-командами"
    - "D-08: вариант и оба разделителя ('ends'/'last_two') хранятся как ТРИ отдельных ключа в app_settings, а не одно общее поле"
    - "D-09: значения по умолчанию (' // ' для 'ends', ' / ' для 'last_two') уже семplиированы миграцией V039 (план 01) — GET без предварительного SET возвращает именно их"
    - "D-10: пустой разделитель отклоняется на СЕРВЕРЕ через AppError::Validation при SET; неизвестное значение variant отклоняется тем же путём — молчаливой деградации нет (в отличие от старого TOML-парсера)"
    - "PLC-07: Employee -> POST /api/v1/settings_set_place_path_defaults -> 403 (Action::ManageSettings), покрыто новым кейсом role_endpoint_matrix"
  artifacts:
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "OrgPathDisplayDto { variant, sep_ends, sep_last_two }"
      contains: "pub struct OrgPathDisplayDto"
    - path: "crates/trackly-app/src/tauri_cmds/settings_org.rs"
      provides: "build_settings_get/set_place_path_defaults + #[tauri::command] обёртки"
      contains: "settings_set_place_path_defaults"
    - path: "crates/trackly-app/src/http/settings_org.rs"
      provides: "handler_get/set_place_path_defaults + роуты /api/v1/settings_get/set_place_path_defaults"
  key_links:
    - from: "ui (Plan 08)"
      to: "settings_set_place_path_defaults"
      via: "apiCall('settings_set_place_path_defaults', {patch})"
      pattern: "settings_set_place_path_defaults"
    - from: "settings_set_place_path_defaults"
      to: "app_settings"
      via: "3 × INSERT ... ON CONFLICT DO UPDATE, параметризованный bind"
      pattern: "ON CONFLICT\\(key\\) DO UPDATE"
---

<objective>
Перенести умолчание сокращения пути организации из `trackly.config.toml` в `app_settings`:
новая пара Tauri-команд + HTTP-эндпоинтов `settings_get/set_place_path_defaults`, копирующая
канонический паттерн `settings_get/set_low_stock_basis` (тот же файл) на три новых ключа
(`place_path_variant`, `place_path_sep_ends`, `place_path_sep_last_two`).

Purpose: PLC-07 требует, чтобы администратор задавал умолчание в «Настройки → Организация», а не
в файле конфигурации — это backend-половина того подраздела (UI — план 08).
Output: рабочие эндпоинты на обоих транспортах, покрытые ролевой матрицей и юнит-тестом на
D-09/D-10 (значимость пробелов, отказ на пустом разделителе).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
<!-- Из плана 01 (уже смержен на момент выполнения этого плана): -->
`crates/trackly-core::domain::places::PathDisplayVariant::from_str(&str) -> Result<Self, AppError>`
— используй для валидации `variant` при SET (мирроринг `LowStockBasis::parse().ok_or_else()`,
только через `from_str`, который сам возвращает `AppError::Validation` — не нужен отдельный
`ok_or_else`).

Канонический образец (буквально копируется, `crates/trackly-app/src/tauri_cmds/settings_org.rs:
183-296`):
```rust
pub async fn build_settings_get_low_stock_basis(ctx: &AppCtx) -> Result<String, AppError> {
    let readers = ctx.readers.clone();
    tokio::task::spawn_blocking(move || -> Result<String, AppError> {
        let conn = readers.acquire();
        let result: rusqlite::Result<Option<String>> = conn.query_row(
            "SELECT value FROM app_settings WHERE key = 'low_stock_basis'",
            [], |r| r.get(0),
        );
        let basis = match result {
            Ok(Some(s)) => LowStockBasis::parse(s.trim()).unwrap_or(LowStockBasis::DEFAULT),
            Ok(None) => LowStockBasis::DEFAULT,
            Err(rusqlite::Error::QueryReturnedNoRows) => LowStockBasis::DEFAULT,
            Err(e) => return Err(map_rusqlite(e)),
        };
        Ok(basis.as_str().to_string())
    }).await.map_err(|e| AppError::Internal { source_chain: format!("spawn_blocking: {e}") })?
}

pub async fn build_settings_set_low_stock_basis(
    ctx: &AppCtx, caller_identity: &Identity, basis: String,
) -> Result<(), AppError> {
    authorize(caller_identity, &Action::ManageSettings)?;
    let parsed = LowStockBasis::parse(&basis).ok_or_else(|| AppError::Validation {
        field: "basis".to_string(),
        message: format!("Недопустимое значение базы подсчёта: {basis}"),
    })?;
    let now = ctx.clock.unix_seconds();
    ctx.writer.execute(move |conn| {
        conn.execute(
            "INSERT INTO app_settings (key, value, created_at_utc, updated_at_utc) \
             VALUES ('low_stock_basis', ?1, ?2, ?2) \
             ON CONFLICT(key) DO UPDATE SET value = ?1, updated_at_utc = ?2",
            rusqlite::params![parsed.as_str(), now],
        ).map(|_| ()).map_err(map_rusqlite)
    }).await
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DTO + build_* хелперы — settings_get/set_place_path_defaults</name>
  <files>crates/trackly-app/src/dto/reports.rs, crates/trackly-app/src/tauri_cmds/settings_org.rs</files>
  <read_first>
    crates/trackly-app/src/tauri_cmds/settings_org.rs (полностью — раздел "Low stock basis",
    строки 183-296, буквальный образец скопирован в `<interfaces>` выше);
    crates/trackly-app/src/dto/reports.rs (раздел `OrgPatch`/`OrgSettingsDto`, строки ~197-249 —
    сосед, куда добавляется `OrgPathDisplayDto`);
    crates/trackly-core/src/domain/places.rs (`PathDisplayVariant::from_str`/`as_str`, добавлены
    планом 01 — точные имена токенов).
  </read_first>
  <behavior>
    - GET без предшествующего SET (свежая БД после миграции V039) возвращает
      `OrgPathDisplayDto { variant: "ends", sep_ends: " // ", sep_last_two: " / " }`.
    - SET с `variant: "bogus"` → `Err(AppError::Validation)`, поле `field` = `"variant"`.
    - SET с `sep_ends: ""` (пустая строка) → `Err(AppError::Validation)`, поле `field` =
      `"sep_ends"`, сообщение содержит «не может быть пустым».
    - SET с `sep_ends: "   "` (строка из одних пробелов) → успех, значение сохраняется БЕЗ
      обрезки пробелов (round-trip через GET возвращает те же 3 пробела).
    - SET с `sep_last_two: ", "` (запятая+пробел) → успех, GET возвращает точную строку `", "`
      без изменений (D-09 round-trip).
    - После успешного SET троих значений разом — GET читает уже обновлённые значения (не кэш).
  </behavior>
  <action>
    В `crates/trackly-app/src/dto/reports.rs`, рядом с `OrgPatch`/`OrgSettingsDto` (после строки
    ~249), добавить `#[derive(Debug, Clone, Serialize, Deserialize, Type)] pub struct
    OrgPathDisplayDto { pub variant: String, pub sep_ends: String, pub sep_last_two: String }`
    (snake_case, без `rename_all camelCase`, мирроринг остальных DTO в этом файле).
    В `crates/trackly-app/src/tauri_cmds/settings_org.rs`, в новом разделе после блока "Low stock
    basis" (после строки ~296): добавить `pub async fn build_settings_get_place_path_defaults(ctx:
    &AppCtx) -> Result<OrgPathDisplayDto, AppError>` — читает 3 ключа `place_path_variant`/
    `place_path_sep_ends`/`place_path_sep_last_two` через `conn.query_row` (три вызова или один
    `SELECT key, value FROM app_settings WHERE key IN (...)` + сборка структуры), фолбэк на
    `"ends"`/`" // "`/`" / "` при отсутствующем ключе (defensive — миграция V039 их уже сеет, но
    GET не должен падать, если кто-то вручную удалил строку). НЕ использовать `.trim()` нигде на
    значениях разделителей (D-09).
    Добавить `pub async fn build_settings_set_place_path_defaults(ctx: &AppCtx, caller_identity:
    &Identity, patch: OrgPathDisplayDto) -> Result<(), AppError>`: `authorize(caller_identity,
    &Action::ManageSettings)?`, затем `PathDisplayVariant::from_str(&patch.variant)?` (пробрасывает
    `AppError::Validation` с полем `"variant"` при неизвестном токене — из плана 01), затем
    `if patch.sep_ends.is_empty() { return Err(AppError::Validation { field: "sep_ends".into(),
    message: "Разделитель «Крайние» не может быть пустым — введите хотя бы один символ.".into()
    }) }` и симметрично для `sep_last_two` с полем `"sep_last_two"` и текстом «Разделитель «Два
    последних» не может быть пустым…». Проверка ИМЕННО `.is_empty()`, НЕ `.trim().is_empty()`
    (пробельная строка обязана пройти валидацию, D-10). После валидации — 3 × `INSERT INTO
    app_settings ... ON CONFLICT(key) DO UPDATE SET value = ?, updated_at_utc = ?` внутри ОДНОГО
    `ctx.writer.execute(move |conn| { ... })` closure (три `conn.execute` подряд в одной
    транзакции writer'а, не три отдельных `.await`).
    Добавить `#[tauri::command] #[specta::specta] pub async fn settings_get_place_path_defaults`
    и `settings_set_place_path_defaults` тонкие обёртки рядом с существующими
    `settings_get/set_low_stock_basis` wrapper'ами (строки ~535-550), тот же паттерн
    `resolve_tauri_identity` для SET.
  </action>
  <acceptance_criteria>
    - `OrgPathDisplayDto` экспортирован из `crate::dto::reports` с тремя `String`-полями.
    - `build_settings_get_place_path_defaults` на свежей БД возвращает variant="ends",
      sep_ends=" // ", sep_last_two=" / ".
    - `build_settings_set_place_path_defaults` с variant="bogus" возвращает
      `AppError::Validation { field: "variant", .. }`.
    - `build_settings_set_place_path_defaults` с sep_ends="" возвращает
      `AppError::Validation { field: "sep_ends", .. }` без обращения к БД (валидация до записи).
    - Round-trip теста: SET sep_ends=", " → GET возвращает sep_ends=", " байт-в-байт.
    - Ни один путь SET не использует `.trim()` на `sep_ends`/`sep_last_two`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app settings_org::place_path -- --test-threads=1</automated>
  </verify>
  <done>`build_settings_get/set_place_path_defaults` + Tauri-обёртки существуют, юнит/интеграционные
  тесты на D-09/D-10 зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: HTTP-зеркало + specta-регистрация + ролевая матрица</name>
  <files>crates/trackly-app/src/http/settings_org.rs, crates/trackly-app/src/specta_export.rs, crates/trackly-app/tests/role_endpoint_matrix.rs</files>
  <read_first>
    crates/trackly-app/src/http/settings_org.rs (полностью — payload-структуры строки 63-72,
    GET/SET handler'ы 153-165/258-270, router 379-437, буквальный образец `SetLowStockBasisPayload`
    + `handler_get/set_low_stock_basis`);
    crates/trackly-app/src/specta_export.rs (строки 160-193 — где регистрируются
    `settings_org`-команды и `places`-команды, вставить новые записи в правильную группу);
    crates/trackly-app/tests/role_endpoint_matrix.rs (Case 44, строки 1505-1522 — буквальный
    образец: Employee cookie → POST → assert 403).
  </read_first>
  <action>
    В `crates/trackly-app/src/http/settings_org.rs`: добавить `#[derive(serde::Deserialize)]
    #[serde(rename_all = "camelCase")] pub struct SetPlacePathDefaultsPayload { pub patch:
    OrgPathDisplayDto }` рядом с `SetLowStockBasisPayload` (D-09 payload wraps whole DTO in одно
    поле `patch`, мирроринг `SaveOrgFieldsPayload { patch: OrgPatch }`, НЕ плоские 3 поля —
    консистентно с "объединённый эндпоинт" рекомендацией RESEARCH.md). Добавить
    `handler_get_place_path_defaults`/`handler_set_place_path_defaults`, мирроринг
    `handler_get/set_low_stock_basis` буквально (session_identity + authorize ManageSettings на
    SET). Импортировать `build_settings_get/set_place_path_defaults`,
    `OrgPathDisplayDto` в блок `use` наверху файла. Зарегистрировать 2 новых роута в `router()`:
    `.route("/api/v1/settings_get_place_path_defaults", post(handler_get_place_path_defaults))` и
    `.route("/api/v1/settings_set_place_path_defaults", post(handler_set_place_path_defaults))`,
    вставить рядом с существующими `low_stock_basis`-роутами (после строки ~419).
    В `crates/trackly-app/src/specta_export.rs`: добавить
    `crate::tauri_cmds::settings_org::settings_get_place_path_defaults,` и
    `crate::tauri_cmds::settings_org::settings_set_place_path_defaults,` в массив команд сразу
    после `settings_set_low_stock_basis` (строка ~172).
    В `crates/trackly-app/tests/role_endpoint_matrix.rs`: добавить Case 49 (следующий свободный
    номер после Case 48) — Employee session (HTTP) → `POST /api/v1/settings_set_place_path_defaults`
    с телом `{"patch": {"variant": "ends", "sep_ends": " // ", "sep_last_two": " / "}}` → assert
    `StatusCode::FORBIDDEN`, мирроринг Case 44's структуры и комментария-заголовка блока (`// Case
    49 (Phase 39.1 Plan 02): Employee session → POST /api/v1/settings_set_place_path_defaults →
    403 Forbidden. Same authorize(&Action::ManageSettings) gate as
    settings_set_low_stock_basis/settings_set_low_stock_threshold.`).
  </action>
  <acceptance_criteria>
    - `router()` в `http/settings_org.rs` содержит оба новых роута.
    - `specta_export.rs` регистрирует обе новые Tauri-команды — `cargo test -p trackly-app --test
      export_bindings` регенерирует `ui/src/bindings.ts` с новым типом `OrgPathDisplayDto` и
      функциями `settingsGetPlacePathDefaults`/`settingsSetPlacePathDefaults` без ошибок.
    - Case 49 в `role_endpoint_matrix.rs`: Employee → `settings_set_place_path_defaults` → 403.
    - Полный пакет `trackly-app` компилируется и проходит (см. verify).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app --test export_bindings -- --test-threads=1</automated>
  </verify>
  <done>HTTP-эндпоинты зарегистрированы, specta-биндинги перегенерированы, Case 49 (Employee → 403)
  зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tauri invoke / axum HTTP → settings_set_place_path_defaults | Мутирующая точка входа, требует Action::ManageSettings; принимает произвольную строку в 3 полях |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-04 | Elevation of Privilege | `settings_set_place_path_defaults` (оба транспорта) | mitigate | `authorize(caller, &Action::ManageSettings)` первой строкой в `build_settings_set_place_path_defaults`, покрыто Case 49 (Employee → 403) на HTTP-транспорте |
| T-39.1-05 | Tampering | Значение `sep_ends`/`sep_last_two` записывается в SQL | mitigate | Параметризованный `rusqlite::params![...]` bind во ВСЕХ трёх INSERT, никакой конкатенации в SQL-текст |
| T-39.1-06 | Improper Input Handling | Неизвестный `variant`, пустой разделитель | mitigate | `PathDisplayVariant::from_str` + явная `.is_empty()` проверка возвращают `AppError::Validation` до записи в БД — сервер источник истины, не полагаемся на фронт |
</threat_model>

<verification>
- `cargo test -p trackly-app settings_org::place_path -- --test-threads=1`
- `cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1`
- `cargo test -p trackly-app --test export_bindings -- --test-threads=1`
</verification>

<success_criteria>
- `settings_get/set_place_path_defaults` работают на обоих транспортах, покрыты ролевой матрицей.
- D-09 (значимые пробелы) и D-10 (пустой разделитель отклоняется) проверены round-trip тестами.
- `ui/src/bindings.ts` содержит новый тип и функции для плана 08 (OrgSettings UI).
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-02-SUMMARY.md` when done
</output>
