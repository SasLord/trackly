---
phase: 39.1
plan: 07
type: execute
wave: 3
depends_on: ["39.1-01", "39.1-02"]
files_modified:
  - crates/trackly-core/src/domain/places.rs
  - crates/trackly-infra/src/repos/places_sqlite.rs
  - crates/trackly-app/src/dto/place.rs
  - crates/trackly-app/src/services/place_service.rs
  - crates/trackly-app/src/tauri_cmds/places.rs
  - crates/trackly-app/src/http/places.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: true
requirements: [PLC-08]
must_haves:
  truths:
    - "D-06: дропдаун/переопределение варианта доступно у ВСЕХ мест, включая места верхнего уровня (parent_id IS NULL) — у них 'Как у родителя' означает умолчание организации напрямую"
    - "D-12: права на изменение варианта места — Action::MutatePlaces (та же семантика прав, что редактирование Названия места), НЕ Action::ManageSettings; новый выделенный вызов places_set_path_variant, а не places_rename с третьим полем"
    - "PLC-08: Employee -> places_set_path_variant -> 403 (MutatePlaces), покрыто новым кейсом role_endpoint_matrix"
    - "Содержимое места (PlaceContents) получает place_path_short для перечисленных устройств/принтеров/картриджей, симметрично спискам устройств/картриджей (D-17)"
  artifacts:
    - path: "crates/trackly-app/src/tauri_cmds/places.rs"
      provides: "build_places_set_path_variant + places_set_path_variant Tauri-команда"
      contains: "places_set_path_variant"
    - path: "crates/trackly-infra/src/repos/places_sqlite.rs"
      provides: "SqlitePlaceRepository::set_path_variant (CAS UPDATE на path_variant_override) + JOIN place_effective_variant в list_subtree_contents_impl"
      contains: "path_variant_override"
    - path: "crates/trackly-app/src/dto/place.rs"
      provides: "PlaceDto.path_variant_override + PlaceContentDto.place_path_short"
      contains: "path_variant_override"
  key_links:
    - from: "ui (Plan 09)"
      to: "places_set_path_variant"
      via: "apiCall('places_set_path_variant', {id, path_variant_override, version})"
      pattern: "places_set_path_variant"
    - from: "places_sqlite.rs::list_subtree_contents_impl"
      to: "place_effective_variant"
      via: "LEFT JOIN place_effective_variant pev ON pev.place_id = d.place_id/c.place_id (3 UNION-ветки)"
      pattern: "place_effective_variant"
---

<objective>
Backend-половина переопределения варианта на месте (PLC-08): новый выделенный
`places_set_path_variant` (CAS-мутация одного поля `places.path_variant_override`, гейт
`Action::MutatePlaces` — D-12), плюс `PlaceDto.path_variant_override` (для формы, чтобы показать
текущее значение) и `PlaceContentDto.place_path_short` (содержимое места — экранный список,
D-17).

Purpose: `places_rename` мутирует ТОЛЬКО `name` — расширять его третьим полем противоречило бы
заголовочному комментарию `PlaceFormModal.svelte` и смешивало бы семантику; RESEARCH.md
рекомендует отдельный вызов, вызываемый ВТОРЫМ запросом из формы (план 09).
Output: рабочий `places_set_path_variant` на обоих транспортах, покрытый ролевой матрицей;
`PlaceContents` (план 10 использует) несёт короткий путь.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из плана 01: `trackly_core::domain::places::{PathDisplayVariant, shorten_place_path}`.

Канонический образец CAS-мутации одного поля (`crates/trackly-infra/src/repos/places_sqlite.rs:
350-369`, `fn rename`):
```rust
fn rename(&self, conn: &mut Self::Conn, id: i64, name: &str, version: i64, now_utc: i64)
    -> Result<PlaceRow, AppError>
{
    let affected = conn.execute(
        "UPDATE places SET name = ?1, updated_at_utc = ?2, version = version + 1 \
         WHERE id = ?3 AND version = ?4 AND deleted_at_utc IS NULL",
        rusqlite::params![name, now_utc, id, version],
    ).map_err(map_rusqlite)?;
    if affected == 0 { return Err(resolve_cas_failure(conn, id, version)); }
    get_impl(conn, id)
}
```

Канонический образец `build_places_rename` (`crates/trackly-app/src/tauri_cmds/places.rs:43-51`):
```rust
pub async fn build_places_rename(
    ctx: &AppCtx, caller: &Identity, id: i64, name: String, version: i64,
) -> Result<PlaceDto, AppError> {
    authorize(caller, &Action::MutatePlaces)?;
    ctx.places.rename(caller, id, name, version).await
}
```

`PlaceService::rename` (`crates/trackly-app/src/services/place_service.rs:199-254`) — полный
образец сервисного метода: `authorize` первой строкой, `self.writer.execute` closure с
before/after audit-записью через `audit_repo.insert`. `set_path_variant` мирроринг этой же формы
(с полем `path_variant_override` вместо `name` — audit `action: "set_path_variant"`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: places_set_path_variant — repo + service + оба транспорта</name>
  <files>crates/trackly-core/src/domain/places.rs, crates/trackly-infra/src/repos/places_sqlite.rs, crates/trackly-app/src/dto/place.rs, crates/trackly-app/src/services/place_service.rs, crates/trackly-app/src/tauri_cmds/places.rs, crates/trackly-app/src/http/places.rs</files>
  <read_first>
    crates/trackly-core/src/domain/places.rs (`PlaceRow` — строки 65-95 из плана 01 контекста,
    поле `full_path` — новое поле `path_variant_override: Option<PathDisplayVariant>` добавляется
    сразу после него);
    crates/trackly-infra/src/repos/places_sqlite.rs (строки 30-40 — `SELECT_PLACES` константа,
    новая колонка добавляется в SELECT-список и `from_row`; строки 350-369 — `fn rename`,
    буквальный образец CAS-UPDATE в `<interfaces>`);
    crates/trackly-app/src/dto/place.rs (`PlaceDto` строки 23-63 — новое поле
    `path_variant_override: Option<String>` добавляется рядом с `full_path`);
    crates/trackly-app/src/services/place_service.rs (строки 199-254 — `fn rename`, полный
    образец с audit-записью, воспроизвести структуру для `set_path_variant`);
    crates/trackly-app/src/tauri_cmds/places.rs (шапка файла — принцип "build_* helper + тонкий
    tauri-command wrapper", строки 43-51 — `build_places_rename` буквальный образец);
    crates/trackly-app/src/http/places.rs (найти `handler_rename` — мирроринг для нового
    `handler_set_path_variant`).
  </read_first>
  <behavior>
    - `places_set_path_variant(id, path_variant_override: Some("last_two"), version)` на
      существующем месте с правильной `version` → успех, `PlaceDto.path_variant_override` =
      `Some("last_two")`, `version` инкрементирован.
    - `places_set_path_variant(id, path_variant_override: None, version)` → успех, override
      сброшен на «Как у родителя» (`NULL` в БД).
    - `places_set_path_variant(id, path_variant_override: Some("bogus"), version)` →
      `AppError::Validation` (неизвестный токен), БД не изменена.
    - `places_set_path_variant` с устаревшей `version` → та же CAS-ошибка, что и `places_rename`
      на несовпадающей версии (`resolve_cas_failure`).
    - Место ВЕРХНЕГО уровня (`parent_id IS NULL`) → `places_set_path_variant` работает так же, как
      на вложенном месте — дропдаун/API не делают исключения для top-level (D-06).
  </behavior>
  <action>
    В `crates/trackly-core/src/domain/places.rs`: добавить `pub path_variant_override:
    Option<PathDisplayVariant>` в `PlaceRow`.
    В `crates/trackly-infra/src/repos/places_sqlite.rs`: расширить `SELECT_PLACES` колонкой
    `p.path_variant_override` (последняя позиция перед `p.created_at_utc`), обновить `from_row`
    — распарсить через `PathDisplayVariant::from_str` при `Some`, `None` при NULL. Добавить метод
    `set_path_variant(&self, conn: &mut Self::Conn, id: i64, path_variant_override:
    Option<&str>, version: i64, now_utc: i64) -> Result<PlaceRow, AppError>` в
    `PlaceRepository` (trait в `crates/trackly-core/src/ports/places.rs` — добавить сигнатуру
    метода туда же, затем реализацию здесь) — CAS-UPDATE на `path_variant_override`, мирроринг
    `fn rename` буквально (тот же `WHERE id = ?3 AND version = ?4 AND deleted_at_utc IS NULL`,
    та же `resolve_cas_failure` при `affected == 0`). Валидация токена (`from_str`) выполняется на
    уровне DTO/сервиса ДО вызова репозитория (repo принимает уже валидную `Option<&str>`), либо
    внутри `set_path_variant` — выбери валидацию на уровне сервиса (симметрично `Self::
    validate_name` в `rename`), чтобы репозиторий оставался тонким SQL-слоем.
    В `crates/trackly-app/src/dto/place.rs`: добавить `pub path_variant_override:
    Option<String>` в `PlaceDto` (сразу после `full_path`) и в `impl From<PlaceRow>` —
    `path_variant_override: row.path_variant_override.map(|v| v.as_str().to_string()),`.
    В `crates/trackly-app/src/services/place_service.rs`: добавить `pub async fn
    set_path_variant(&self, caller: &Identity, id: i64, path_variant_override: Option<String>,
    version: i64) -> Result<PlaceDto, AppError>` — мирроринг `rename` (строки 199-254):
    `authorize(caller, &Action::MutatePlaces)?`, валидация токена через `PathDisplayVariant::
    from_str` при `Some(v)` (пропустить при `None`), `self.writer.execute` closure с
    before/after audit (`action: "set_path_variant"`, тот же `AuditEntry`-паттерн, что `rename`).
    В `crates/trackly-app/src/tauri_cmds/places.rs`: добавить `build_places_set_path_variant`
    (мирроринг `build_places_rename` буквально — `authorize` + делегирование сервису) и
    `#[tauri::command] #[specta::specta] pub async fn places_set_path_variant` тонкую обёртку.
    В `crates/trackly-app/src/http/places.rs`: добавить payload-структуру
    `SetPathVariantPayload { id: i64, path_variant_override: Option<String>, version: i64 }` и
    `handler_set_path_variant`, мирроринг `handler_rename`, зарегистрировать роут
    `/api/v1/places_set_path_variant` в `router()`.
  </action>
  <acceptance_criteria>
    - `PlaceRepository::set_path_variant` — CAS-UPDATE, идентичная структура ошибок с `rename`.
    - `build_places_set_path_variant` гейтует `Action::MutatePlaces` (НЕ `ManageSettings`, D-12)
      первой строкой.
    - `PlaceDto.path_variant_override` присутствует и round-trip'ится через
      `places_set_path_variant` → `places_get`.
    - Место верхнего уровня (`parent_id: None`) успешно принимает `places_set_path_variant` без
      специальной обработки.
    - HTTP-роут `/api/v1/places_set_path_variant` зарегистрирован.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app place_service::set_path_variant -- --test-threads=1</automated>
    <automated>cargo test -p trackly-infra places_sqlite -- --test-threads=1</automated>
  </verify>
  <done>`places_set_path_variant` работает на обоих транспортах, CAS-семантика идентична
  `places_rename`, гейт — MutatePlaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: place_path_short в содержимом места (PlaceContents) + specta + ролевая матрица</name>
  <files>crates/trackly-core/src/domain/places.rs, crates/trackly-infra/src/repos/places_sqlite.rs, crates/trackly-app/src/dto/place.rs, crates/trackly-app/src/specta_export.rs, crates/trackly-app/tests/role_endpoint_matrix.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/places_sqlite.rs (строки 172-234 —
    `list_subtree_contents_impl`, три UNION-ветки device/printer/cartridge, каждая со своим
    `JOIN place_full_paths pfp`; строки 930-950 паттерн для сепараторов уже цитирован в плане 04
    `<interfaces>` — воспроизвести здесь тот же shape);
    crates/trackly-core/src/domain/places.rs (`PlaceContentRow` строки 150-157 — поле `full_path:
    String` НЕ `Option`, новое поле `place_path_short: String` той же нулабельности);
    crates/trackly-app/src/dto/place.rs (`PlaceContentDto` строки 189-210 — `full_path: String`,
    новое поле рядом);
    crates/trackly-app/src/specta_export.rs (строки 182-193 — блок регистрации `places`-команд,
    добавить `places_set_path_variant` туда же);
    crates/trackly-app/tests/role_endpoint_matrix.rs (Case 45, строки 1525+ — образец структуры
    блока для нового Case на `places_set_path_variant`).
  </read_first>
  <behavior>
    - `list_subtree_contents(root_id, nested=true)` с устройством, местом которого чей
      `place_effective_variant` = 'last', `full_path` = "Здание А / 1 этаж / 1-05" → возвращаемая
      строка несёт `place_path_short` = "1-05".
    - Employee session → `places_set_path_variant` → 403 (Case 50).
  </behavior>
  <action>
    В `crates/trackly-core/src/domain/places.rs`: добавить `pub place_path_short: String` в
    `PlaceContentRow` сразу после `pub full_path: String` (не `Option` — мирроринг существующей
    нулабельности поля, `shorten_place_path` на непустой `full_path` детерминированно вернёт
    непустую строку).
    В `crates/trackly-infra/src/repos/places_sqlite.rs::list_subtree_contents_impl`: добавить
    чтение `(sep_ends, sep_last_two)` из `app_settings` ОДИН раз в начале функции (мирроринг
    паттерна из плана 04 `<interfaces>`). В КАЖДОЙ из трёх UNION-веток (device/printer/cartridge)
    добавить `LEFT JOIN place_effective_variant pev ON pev.place_id = d.place_id` (или `c.place_id`
    для картриджной ветки) и колонку `pev.effective_variant`. В row-mapping closure —
    вычислить `place_path_short` через `PathDisplayVariant::from_str` + `shorten_place_path`.
    В `crates/trackly-app/src/dto/place.rs`: добавить `pub place_path_short: String` в
    `PlaceContentDto` сразу после `full_path` (строка ~195) и в конструктор (строка ~206).
    В `crates/trackly-app/src/specta_export.rs`: добавить
    `crate::tauri_cmds::places::places_set_path_variant,` в блок регистрации `places`-команд
    (после `places_rename` на строке ~183).
    В `crates/trackly-app/tests/role_endpoint_matrix.rs`: добавить Case 50 — Employee session
    (HTTP) → `POST /api/v1/places_set_path_variant` с телом `{"id": 1, "path_variant_override":
    "ends", "version": 1}` → assert `StatusCode::FORBIDDEN`, комментарий-заголовок в стиле Case 45
    (`// Case 50 (Phase 39.1 Plan 07): Employee session → POST /api/v1/places_set_path_variant →
    403 Forbidden. authorize(&Action::MutatePlaces) — D-12: та же семантика прав, что
    places_rename, НЕ Action::ManageSettings.`).
  </action>
  <acceptance_criteria>
    - `PlaceContentDto`/`PlaceContentRow` несут `place_path_short: String`, вычисленный по единой
      формуле для всех трёх UNION-веток (device/printer/cartridge).
    - `specta_export.rs` регистрирует `places_set_path_variant`; `export_bindings` тест
      перегенерирует `bindings.ts` без ошибок с новыми полями/функцией.
    - Case 50: Employee → `places_set_path_variant` → 403.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra places_sqlite::list_subtree_contents -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app --test export_bindings -- --test-threads=1</automated>
  </verify>
  <done>PlaceContents несёт короткий путь, specta-биндинги перегенерированы, Case 50 (Employee →
  403 на places_set_path_variant) зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tauri invoke / axum HTTP → places_set_path_variant | Мутирующая точка входа, требует Action::MutatePlaces (D-12 — та же семантика, что редактирование места, НЕ отдельная настройка) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-15 | Elevation of Privilege | `places_set_path_variant` (оба транспорта) | mitigate | `authorize(caller, &Action::MutatePlaces)` первой строкой в `build_places_set_path_variant` И в `PlaceService::set_path_variant` (defense-in-depth, мирроринг остальных `build_places_*`), покрыто Case 50 |
| T-39.1-16 | Tampering | Неизвестный токен `path_variant_override` при SET | mitigate | `PathDisplayVariant::from_str` отклоняет с `AppError::Validation` до записи в БД |
| T-39.1-17 | Race Condition | Одновременная мутация `name` (rename) и `path_variant_override` (set_path_variant) на одном месте | accept | Оба используют `version`-based CAS независимо — конкурентный вызов одного из двух получит `resolve_cas_failure` и повторит с актуальной версией, как любая другая CAS-мутация в этом файле |
</threat_model>

<verification>
- `cargo test -p trackly-app place_service::set_path_variant -- --test-threads=1`
- `cargo test -p trackly-infra places_sqlite -- --test-threads=1`
- `cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1`
- `cargo test -p trackly-app --test export_bindings -- --test-threads=1`
</verification>

<success_criteria>
- `places_set_path_variant` работает на обоих транспортах, CAS-семантика идентична `places_rename`.
- Места верхнего уровня используют тот же путь мутации, без исключений (D-06).
- Содержимое места несёт короткий путь (D-17), симметрично спискам устройств/картриджей.
- Ролевая матрица покрывает новый эндпоинт (Employee → 403).
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-07-SUMMARY.md` when done
</output>
