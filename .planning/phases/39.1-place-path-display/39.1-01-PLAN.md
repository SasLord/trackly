---
phase: 39.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V039__place_path_display.sql
  - crates/trackly-infra/tests/place_effective_variant.rs
  - crates/trackly-core/src/domain/places.rs
autonomous: true
requirements: [PLC-07, PLC-08]
must_haves:
  truths:
    - "D-01: сокращение пути вычисляется на бэкенде (новая чистая функция shorten_place_path в trackly-core), а не на фронте — фронт получит готовую строку в последующих планах"
    - "D-02/D-23: эффективный вариант пересчитывается на КАЖДОМ чтении через SQL-вью place_effective_variant (без кэша); свежая БД после миграции даёт организации 'ends' и НОЛЬ переопределений мест"
    - "D-03: место со значением 'Как у родителя' (path_variant_override IS NULL) динамически подхватывает вариант нового родителя при перемещении — проверено живым UPDATE parent_id в тесте"
    - "D-04: переопределение места (path_variant_override NOT NULL) не сбрасывается при перемещении, включая перенос на верхний уровень (parent_id = NULL)"
    - "D-13/D-14: разделитель варианта соединяет показанные сегменты и появляется ТОЛЬКО когда что-то реально выброшено — путь из 2 сегментов при варианте 'ends' отдаётся как есть через обычный ' / ', без sep_ends"
    - "D-15: вариант 'last' на пути из одного сегмента возвращает его же, без маркера сокращения"
    - "D-16: 'last_two' на пути из 3+ сегментов даёт последние два сегмента через sep_last_two; 'ends' на том же пути — первый+последний через sep_ends"
  artifacts:
    - path: "migrations/V039__place_path_display.sql"
      provides: "places.path_variant_override колонка, 3 ключа app_settings (умолчания организации), вью place_effective_variant"
    - path: "crates/trackly-core/src/domain/places.rs"
      provides: "PathDisplayVariant enum + shorten_place_path()"
      contains: "pub fn shorten_place_path"
    - path: "crates/trackly-infra/tests/place_effective_variant.rs"
      provides: "интеграционные тесты наследования варианта по цепочке предков"
  key_links:
    - from: "migrations/V039__place_path_display.sql"
      to: "place_effective_variant view"
      via: "WITH RECURSIVE восходящий обход parent_id + COALESCE на app_settings"
      pattern: "CREATE VIEW place_effective_variant"
    - from: "crates/trackly-core/src/domain/places.rs"
      to: "shorten_place_path"
      via: "чистая функция, вызываемая read-путями из плана 02-07"
      pattern: "pub fn shorten_place_path"
---

<objective>
Заложить фундамент фазы 39.1: схему хранения (миграция V039) и формулу сокращения пути
(`shorten_place_path` в `trackly-core`). Это Wave 0 из VALIDATION.md — все последующие планы
(настройки организации, переопределение места, чтение в устройствах/картриджах/отчётах/актах)
зависят от этой миграции и этой формулы.

Purpose: без живого SQL-вью и чистой Rust-формулы ни один read-путь не может произвести
`place_path_short` — это единственная точка входа для PLC-07/PLC-08.
Output: `migrations/V039__place_path_display.sql` (колонка + 3 ключа app_settings + вью
`place_effective_variant`), `shorten_place_path()`/`PathDisplayVariant` в
`crates/trackly-core/src/domain/places.rs`, интеграционные тесты вью в
`crates/trackly-infra/tests/place_effective_variant.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция V039 — колонка переопределения, умолчания организации, вью наследования</name>
  <files>migrations/V039__place_path_display.sql</files>
  <read_first>
    migrations/V037__places.sql (вью place_full_paths — образец для рекурсивного CTE и для
    комментария «пересчёт на каждом чтении, никогда не устаревает», D-02);
    migrations/V038__places_migrate_devices_acts_cartridges.sql (образец ALTER TABLE ... ADD
    COLUMN на places-соседей, PRAGMA user_version в конце файла);
    migrations/V016__cartridges_kind_color_settings.sql (создание таблицы app_settings —
    подтверди, что таблица уже существует, эта миграция её НЕ создаёт заново, только INSERT
    новых ключей).
  </read_first>
  <action>
    Создать `migrations/V039__place_path_display.sql` со следующим содержимым (порядок обязателен):
    (1) `ALTER TABLE places ADD COLUMN path_variant_override TEXT NULL` — значения
    `'ends'`/`'last_two'`/`'last'`/`NULL` (`NULL` = «Как у родителя», D-06). Добавь
    `CHECK (path_variant_override IN ('ends','last_two','last') OR path_variant_override IS NULL)`
    в самой `ALTER TABLE` строке невозможно в SQLite — вместо этого задокументируй допустимые
    значения комментарием и полагайся на валидацию на уровне сервиса (Task 3 этого плана +
    план 07), как это уже сделано для `places.kind` (валидация в Rust через `PlaceKind::from_str`,
    не CHECK-constraint на каждый токен).
    (2) Три `INSERT INTO app_settings (key, value, created_at_utc, updated_at_utc) VALUES (...)`
    (мирроринг `V016`'s `low_stock_threshold` seed, `unixepoch()` для обеих временных колонок):
    ключ `place_path_variant` = `'ends'` (D-23 — организация после миграции = «Крайние»), ключ
    `place_path_sep_ends` = `' // '` (D-09, ДВА пробела по краям, НЕ обрезать), ключ
    `place_path_sep_last_two` = `' / '` (D-09). Литералы разделителей в SQL пиши в одинарных
    кавычках буквально с пробелами внутри — SQLite не тримит строковые литералы.
    (3) `CREATE VIEW place_effective_variant AS` — рекурсивный CTE, ВОСХОДЯЩИЙ по `parent_id`
    (в отличие от `place_full_paths`, который идёт от корня к листу; здесь наоборот — от каждого
    узла к предкам), с ранней остановкой на первом непустом `path_variant_override` и `COALESCE`
    на `(SELECT value FROM app_settings WHERE key = 'place_path_variant')` в терминальном SELECT
    для узлов, у которых ни у одного предка (включая себя) нет переопределения. RESEARCH.md
    § Pattern 2 содержит полный черновик SQL с обоснованием предиката
    `WHERE variant IS NOT NULL OR parent_id IS NULL` — используй его как основу, не изобретай
    другую форму рекурсии. Вью фильтрует `deleted_at_utc IS NULL` на каждом шаге, мирроринг
    `place_full_paths`. Столбцы вью: `place_id` (INTEGER), `effective_variant` (TEXT, никогда
    NULL — либо переопределение места, либо умолчание организации).
    (4) `PRAGMA user_version = 39;` последней строкой (D-Migrations-02 конвенция, подтверждена
    в `crates/trackly-infra/src/db/migrations.rs` doc-комментарием).
  </action>
  <acceptance_criteria>
    - Файл начинается с `-- V039:` комментария, объясняющего назначение (мирроринг V037/V038).
    - `ALTER TABLE places ADD COLUMN path_variant_override TEXT NULL` присутствует один раз.
    - Ровно 3 `INSERT INTO app_settings` со значениями `place_path_variant`='ends',
      `place_path_sep_ends`=' // ' (с пробелами), `place_path_sep_last_two`=' / ' (с пробелами).
    - `CREATE VIEW place_effective_variant AS` присутствует, содержит `WITH RECURSIVE` и хотя бы
      одну ссылку на `app_settings` внутри `COALESCE`/подзапроса.
    - Файл заканчивается `PRAGMA user_version = 39;`.
    - Свежая тестовая БД (через `trackly_infra::test_support::test_db()`) применяет миграцию без
      ошибок — покрыто автотестом Task 1 verify ниже.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra --lib db::migrations -- --test-threads=1</automated>
  </verify>
  <done>Миграция V039 существует, применяется на свежей БД, `PRAGMA user_version` = 39 после
  прогона (проверяется существующим тестом `db::migrations`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Интеграционные тесты place_effective_variant — наследование, top-level, перемещение</name>
  <files>crates/trackly-infra/tests/place_effective_variant.rs</files>
  <read_first>
    crates/trackly-infra/tests/places_crud.rs (полный файл — образец: `tokio::time::timeout(30s)`,
    `test_db()`, `SqlitePlaceRepository`, вспомогательная `new_place()`, стиль имён тестов);
    migrations/V039__place_path_display.sql (только что созданный в Task 1 — точные имена
    колонки/вью/ключей app_settings).
  </read_first>
  <behavior>
    - Место без родителя и без `path_variant_override` (`parent_id IS NULL`, override NULL) →
      `place_effective_variant.effective_variant` = умолчание организации ('ends' после свежей
      миграции, D-06/D-23).
    - Место с явным `path_variant_override` = 'last_two' → вью возвращает 'last_two' независимо
      от родителей/умолчания организации.
    - Цепочка из 3 уровней (root → child → grandchild), override только на `root` = 'last' →
      `child` и `grandchild` (оба NULL override) наследуют 'last' от root через цепочку предков
      (D-03).
    - Динамическое наследование (D-03): место A (NULL override) — child места B
      (override='ends'). `UPDATE places SET parent_id = NULL WHERE id = A` (перенос A на верхний
      уровень, где override не наследуется от B) → `effective_variant` для A немедленно меняется
      на умолчание организации, БЕЗ повторного запуска какой-либо миграции/пересчёта — просто
      следующий SELECT из вью.
    - Override не сбрасывается при перемещении (D-04): место с override='last' перемещается
      (`UPDATE places SET parent_id = <новый>`) → `effective_variant` остаётся 'last'.
    - Умолчание организации меняется (`UPDATE app_settings SET value = 'last' WHERE key =
      'place_path_variant'`) → все места без переопределения в цепочке предков немедленно видят
      новое умолчание при следующем чтении вью (без явного тестового шага "инвалидации").
  </behavior>
  <action>
    Создать новый интеграционный тест-файл, мирроринг заголовка/стиля `places_crud.rs`. Каждый
    тест — отдельная `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` функция,
    обёрнутая в `tokio::time::timeout(Duration::from_secs(30), async { ... })`. Создание/перемещение
    мест — через `PlaceRepository::create`/`PlaceRepository::move_node` (существующий trait,
    `trackly_core::ports::places::PlaceRepository`, реализован `SqlitePlaceRepository`).
    `path_variant_override` и значение `app_settings.place_path_variant` — устанавливай напрямую
    через `conn.execute("UPDATE places SET path_variant_override = ?1 WHERE id = ?2", ...)` и
    `conn.execute("UPDATE app_settings SET value = ?1 WHERE key = 'place_path_variant'", ...)`
    (в Plan 01 ещё нет сервисного метода для установки переопределения — он появится в Plan 07;
    здесь допустим прямой SQL, так как тест проверяет ТОЛЬКО поведение вью, не API мутации).
    Чтение эффективного варианта — `conn.query_row("SELECT effective_variant FROM
    place_effective_variant WHERE place_id = ?1", [id], |r| r.get::<_, String>(0))`.
    Только вымышленные названия мест («Здание А», «2 этаж», «214» — как в `places_crud.rs`).
  </action>
  <acceptance_criteria>
    - Минимум 6 тестовых функций, покрывающих каждый из пунктов `<behavior>` выше отдельным тестом.
    - Все тесты обёрнуты в `tokio::time::timeout(Duration::from_secs(30), ...)`.
    - Ни один тест не использует захардкоженный реальный организационный контент — только
      вымышленные названия мест.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-infra --test place_effective_variant -- --test-threads=1</automated>
  </verify>
  <done>Файл `place_effective_variant.rs` существует, все тесты зелёные, покрывают D-02/D-03/D-04/
  D-06/D-23.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: shorten_place_path — формула сокращения (trackly-core)</name>
  <files>crates/trackly-core/src/domain/places.rs</files>
  <read_first>
    crates/trackly-core/src/domain/places.rs (полный файл — образцы `PlaceKind::from_str`/
    `as_str` для закрытого enum-парсера с русским сообщением об ошибке, `natural_name_cmp` +
    `#[cfg(test)] mod tests` в конце файла для стиля юнит-тестов);
    ui/src/lib/utils/placePath.ts (текущая TS-реализация `shortenPlacePath` — формула переносится
    в Rust дословно с уточнениями D-13..D-16 из RESEARCH.md § Pattern 3; НЕ копировать ветку
    `variant === 'full' => return fullPath` — семантически противоположна новому варианту `Last`).
  </read_first>
  <behavior>
    Таблица «вход → выход» (RESEARCH.md § Pattern 3, дословно, `' / '` — разделитель полного пути
    из `place_full_paths`):
    - 1 сегмент, любой вариант → сегмент как есть, без разделителя ("Склад" → "Склад").
    - 2 сегмента, Ends → полный путь через обычный ' / ' (НЕ sep_ends — нечего выбрасывать, D-14).
    - 2 сегмента, LastTwo → полный путь через обычный ' / ' (симметрично D-14).
    - 2 сегмента, Last → только последний сегмент, без разделителя.
    - 3+ сегментов, Ends → первый сегмент + `sep_ends` + последний сегмент.
    - 3+ сегментов, LastTwo → предпоследний + `sep_last_two` + последний сегмент.
    - 3+ сегментов, Last → только последний сегмент.
    - Пустая строка на входе → пустая строка на выходе (аналог TS `if (!fullPath) return fullPath`).
  </behavior>
  <action>
    Добавить в конец файла (после существующего `natural_name_cmp`, перед `#[cfg(test)] mod
    tests`, либо расширить существующий `mod tests`):
    `pub enum PathDisplayVariant { Ends, LastTwo, Last }` с `#[derive(Debug, Clone, Copy,
    PartialEq, Eq)]`, методами `from_str(s: &str) -> Result<Self, AppError>` (токены `"ends"`,
    `"last_two"`, `"last"` — сохранить старые токены `ends`/`last_two`, заменить только `full` →
    `last`, RESEARCH.md Open Question 3) и `as_str(&self) -> &'static str`, мирроринг
    `PlaceKind::from_str`/`as_str` буквально (тот же файл, строки ~31-70) — тот же формат
    Russian-language `AppError::Validation` с перечислением всех допустимых значений на
    нераспознанном токене.
    Добавить `pub fn shorten_place_path(full_path: &str, variant: PathDisplayVariant, sep_ends:
    &str, sep_last_two: &str) -> String`, разбивающую `full_path` по `" / "` (`split(' / ')`,
    как в TS-версии) и реализующую таблицу из `<behavior>` выше. Функция не обращается к БД, не
    обходит дерево — работает исключительно со строкой и уже разрешённым вариантом (Pitfall из
    RESEARCH.md: «вызывать shorten_place_path внутри цикла по предкам» — запрещено, обход дерева
    живёт только в SQL-вью из Task 1/2).
  </action>
  <acceptance_criteria>
    - `PathDisplayVariant::from_str("full")` возвращает `Err(AppError::Validation)` (токен `full`
      более не существует — заменён на `last`).
    - `PathDisplayVariant::from_str("last")` возвращает `Ok(PathDisplayVariant::Last)`.
    - Юнит-тесты покрывают все 8 строк таблицы `<behavior>` минимум по одному разу каждая
      (минимум 8 `#[test]`-функций или табличный тест с ≥8 кейсами).
    - `shorten_place_path("Здание А / 1 этаж / 1-05", PathDisplayVariant::Ends, " // ", " / ")`
      возвращает буквально `"Здание А // 1-05"`.
    - `shorten_place_path("Здание А / 1 этаж", PathDisplayVariant::Ends, " // ", " / ")`
      возвращает буквально `"Здание А / 1 этаж"` (НЕ `" // "` — D-14).
    - Пробелы в `sep_ends`/`sep_last_two` не обрезаются функцией (проверить явным тестом со
      значением `", "` как `sep_ends`, дающим перечисление через запятую на 3+ сегментах).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-core domain::places -- --test-threads=1</automated>
  </verify>
  <done>`PathDisplayVariant` и `shorten_place_path` существуют, экспортированы из
  `trackly_core::domain::places`, юнит-тесты по полной таблице D-13..D-16 зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| SQL-вью → app_settings | `place_effective_variant` читает `app_settings.place_path_variant` через скалярный подзапрос — значение туда пишут только Wave 2 планы (Plan 02), здесь только чтение |
| Rust-формула ← вызывающие планы | `shorten_place_path` — чистая функция без I/O, принимает уже провалидированные строки от вызывающих планов (02-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-01 | Tampering | `place_effective_variant` view SQL | accept | Вью — read-only, параметров пользователя не принимает; вся мутация идёт через отдельные INSERT/UPDATE в Plan 02/07 с параметризованным bind |
| T-39.1-02 | Denial of Service | Рекурсивный CTE на восходящем обходе | accept | Дерево мест ограничено практикой (~300 узлов, тот же порядок, что `place_full_paths`); восходящий обход по одному `parent_id` за шаг не может зациклиться благодаря существующему cycle-check в `PlaceService::move_node` (Phase 39) |
| T-39.1-03 | Information Disclosure | `shorten_place_path` — чистая функция | accept | Не читает БД, не логирует, детерминированная строковая операция — нет поверхности утечки |
</threat_model>

<verification>
- `cargo test -p trackly-infra --lib db::migrations -- --test-threads=1` — миграция применяется.
- `cargo test -p trackly-infra --test place_effective_variant -- --test-threads=1` — наследование корректно.
- `cargo test -p trackly-core domain::places -- --test-threads=1` — формула сокращения корректна.
- Ровно один `cargo test` процесс за раз (см. memory `cargo_no_concurrent_test`).
</verification>

<success_criteria>
- Миграция V039 существует и применяется без ошибок на свежей БД.
- `place_effective_variant` корректно разрешает наследование по цепочке предков, top-level
  fallback на умолчание организации, динамическое наследование при перемещении и сохранение
  переопределения при перемещении.
- `shorten_place_path`/`PathDisplayVariant` в `trackly-core` покрывают полную таблицу D-13..D-16
  юнит-тестами и готовы к использованию Wave 2/3 планами.
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-01-SUMMARY.md` when done
</output>
