---
phase: 39.1
plan: 09
type: execute
wave: 4
depends_on: ["39.1-07"]
files_modified:
  - ui/src/features/places/PlaceFormModal.svelte
autonomous: true
requirements: [PLC-08]
must_haves:
  truths:
    - "D-05: НИКАКОГО field-hint/подсказки/названия унаследованного варианта рядом с пунктом 'Как у родителя' — ни в create, ни в rename режиме, ни для мест верхнего уровня"
    - "Дропдаун 'Вариант сокращения' видим и редактируем в ОБОИХ режимах (create и rename), в отличие от Типа/Родителя/Уровня (create-time-only) — потому что поле мутируемо после создания места (D-12)"
  artifacts:
    - path: "ui/src/features/places/PlaceFormModal.svelte"
      provides: "Dropdown «Вариант сокращения» + вторая RPC places_set_path_variant в handleSubmit"
      contains: "PATH_VARIANT_OPTIONS"
  key_links:
    - from: "PlaceFormModal.svelte handleSubmit"
      to: "places_set_path_variant"
      via: "второй apiCall после places_create/places_rename, используя id+version только что сохранённого места"
      pattern: "places_set_path_variant"
---

<objective>
UI-половина PLC-08: дропдаун «Вариант сокращения» в `PlaceFormModal.svelte`, видимый в ОБОИХ
режимах (create/rename), сохраняющийся ВТОРЫМ RPC-вызовом `places_set_path_variant` (backend из
плана 07) сразу после основного `places_create`/`places_rename` в одном `handleSubmit`.

Purpose: PLC-08 требует, чтобы любое место могло переопределить вариант сокращения из формы
редактирования места — без отдельной кнопки сохранения (D-12: те же права и то же действие
пользователя, что редактирование названия).
Output: рабочий дропдаун по UI-SPEC.md «Поверхность 2», обновлённый заголовочный комментарий файла
(устаревшее утверждение «places_rename мутирует ТОЛЬКО name» — план 07 это изменило по факту, хоть
и вторым вызовом, не расширением самого `places_rename`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-UI-SPEC.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из плана 07 (уже смержен): Tauri-команда `places_set_path_variant({ id: number,
path_variant_override: string | null, version: number }) -> PlaceDto`, гейт `Action::MutatePlaces`
(та же семантика, что у `places_rename`). `PlaceDto.path_variant_override: string | null` —
текущее значение переопределения (уже доступно на пропе `place: PlaceDto | null`, переданном
модалу при открытии в режиме `rename`).

UI-SPEC.md «Поверхность 2» (строки 391-476) — ПОЛНЫЙ DOM/copy контракт, `PATH_VARIANT_OPTIONS`
массив (строки 441-446) буквально:
```ts
const PATH_VARIANT_OPTIONS = [
  { value: null, label: 'Как у родителя' },
  { value: 'ends', label: 'Крайние' },
  { value: 'last_two', label: 'Два последних' },
  { value: 'last', label: 'Последнее' },
];
```

Канонический образец дропдауна «Тип» (`PlaceFormModal.svelte`, строки 227-251) — тот же
`Dropdown` контракт (`variant="select" flat={true} groups getGroupId getGroupName
getGroupCount getGroupExpandable isGroupSelected onExpandGroup getMemberId getMemberName onSearch
onPickGroup onPickMember`), новый дропдаун копирует эту же форму с `searchable={false}`
(UI-SPEC.md строка 421).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Dropdown «Вариант сокращения» + вторая RPC в handleSubmit</name>
  <files>ui/src/features/places/PlaceFormModal.svelte</files>
  <read_first>
    ui/src/features/places/PlaceFormModal.svelte (полный файл — заголовочный комментарий строки
    1-16 (утверждение о `places_rename`, которое эта задача делает устаревшим — обнови его);
    `Props`/деструктуризация ~строки 28-35; блок Dropdown «Тип» строки 227-251 (буквальный образец
    контракта); `handleSubmit` строки 170-199 (точка добавления второго RPC-вызова); DOM порядок
    полей — «Название» первым, затем `{#if !isRename}` условный блок для create-only полей);
    .planning/phases/39.1-place-path-display/39.1-UI-SPEC.md § «Поверхность 2» целиком (строки
    391-476) — точный порядок полей (Название → Вариант сокращения → [create-only: Тип →
    Родитель → Уровень → Складское место → Порядок]), запрет D-05, `searchable={false}`
    обоснование, права доступа (тот же гейт, что «Название»).
  </read_first>
  <behavior>
    - Режим `create`: дропдаун «Вариант сокращения» видим, дефолтное значение «Как у родителя»
      (`null`). После успешного `places_create` → второй вызов `places_set_path_variant(newId,
      selectedVariant, newVersion)` — ЕСЛИ `selectedVariant !== null`, ИЛИ безусловно (значение
      всё равно `null` по умолчанию, вызов идемпотентен) — выбери безусловный вызов для простоты
      кода (второй RPC с `null` эквивалентен отсутствию переопределения, не создаёт лишнего
      состояния).
    - Режим `rename`: дропдаун предзаполнен текущим значением `place.path_variant_override` (или
      «Как у родителя», если `null`). После успешного `places_rename` → второй вызов
      `places_set_path_variant(place.id, selectedVariant, renamedPlace.version)` — используя
      `version`, ВОЗВРАЩЁННУЮ `places_rename` (не исходную `place.version`, она уже устарела после
      первого CAS-инкремента).
    - Ни при каких условиях рядом с пунктом «Как у родителя» не появляется подсказка/пример/
      название унаследованного варианта — ни в `placeholder`, ни в отдельном `field-hint` элементе
      (D-05).
    - Ошибка второго RPC (`places_set_path_variant`) обрабатывается тем же `mapServerError`/
      `serverErr` путём, что ошибка первого RPC — пользователь видит единый `.server-error` блок,
      не два раздельных тоста.
  </behavior>
  <action>
    Обновить заголовочный комментарий файла (строки 6-10): заменить утверждение «`places_rename(id,
    name, version)` мутирует ТОЛЬКО `name` — нет `places_update`» на описание, что «Вариант
    сокращения» теперь сохраняется ВТОРЫМ RPC-вызовом (`places_set_path_variant`) в том же
    `handleSubmit`, а не расширением `places_rename` — форма по-прежнему шлёт ОДИН пользовательский
    submit, но два сетевых запроса.
    Добавить константу `PATH_VARIANT_OPTIONS` (буквально как в `<interfaces>`) и `$state`
    `pathVariant: string | null` — инициализируется из `place?.path_variant_override ?? null` при
    монтировании (мирроринг того, как `name`/`kind` уже инициализируются из `place` в режиме
    rename).
    Добавить `pickPathVariant` handler (мирроринг существующего `pickKind`) — устанавливает
    `pathVariant` из выбранного пункта `PATH_VARIANT_OPTIONS`.
    Вставить блок `.form-field` с `<label for="pf-path-variant">Вариант сокращения</label>` и
    `<Dropdown id="pf-path-variant" variant="select" flat={true} value={pathVariantLabel}
    placeholder="Как у родителя" searchable={false} groups={PATH_VARIANT_OPTIONS} ...>` СРАЗУ
    ПОСЛЕ блока «Название», ВНЕ условия `{#if !isRename}` (видим в обоих режимах — единственное
    новое поле с таким свойством, буквально по UI-SPEC.md строки 403-410).
    В `handleSubmit`: после успешного `saved = await apiCall<PlaceDto>('places_rename'|
    'places_create', {...})` и ДО `pushToast`/`onSaved(saved)` — добавить `saved = await
    apiCall<PlaceDto>('places_set_path_variant', { id: saved.id, path_variant_override:
    pathVariant, version: saved.version })` (переиспользуя обновлённый `saved` с новой `version`
    из первого вызова). Обернуть в тот же `try/catch` блок, что уже оборачивает первый вызов —
    ошибка второго RPC попадает в тот же `mapServerError(e)` путь, не отдельный catch.
  </action>
  <acceptance_criteria>
    - Дропдаун «Вариант сокращения» рендерится ВНЕ `{#if !isRename}` — видим в обоих режимах.
    - `PATH_VARIANT_OPTIONS` содержит ровно 4 пункта, первый — `{ value: null, label: 'Как у
      родителя' }`.
    - Нигде в новом коде нет условной подсказки/суффикса к пункту «Как у родителя» (grep-проверка:
      ни один `field-hint`/tooltip-related класс не соседствует с новым Dropdown).
    - `handleSubmit` вызывает `places_set_path_variant` ВТОРЫМ запросом, используя `version` из
      результата ПЕРВОГО запроса (не исходный `place.version`).
    - Заголовочный комментарий файла обновлён, не содержит устаревшего утверждения про
      "places_rename мутирует ТОЛЬКО name".
    - `svelte-check`/`eslint`/`pnpm build` зелёные.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check --output human</automated>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <done>Дропдаун «Вариант сокращения» работает в обоих режимах, сохраняется вторым RPC-вызовом,
  D-05 запрет соблюдён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PlaceFormModal.svelte → places_set_path_variant | Мутирующий вызов, гейт на сервере (план 07), фронт не является источником истины |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-19 | Race Condition | Второй RPC (`places_set_path_variant`) использует устаревшую `version`, если первый RPC (`places_create`/`places_rename`) вернул её, а параллельный запрос успел изменить место между двумя вызовами | accept | Тот же CAS-паттерн, что и любая другая двух-шаговая мутация в проекте — второй RPC получит `AppError::Validation`/CAS-конфликт при устаревшей версии, пользователь увидит `.server-error` и повторит сохранение (стандартное поведение формы, не специфичное для этой фичи) |
</threat_model>

<verification>
- `pnpm --dir ui exec svelte-check --output human`
- `pnpm --dir ui exec eslint src`
- `pnpm --dir ui build`
- Живая проверка (per VALIDATION.md Manual-Only): открыть создание/редактирование Места, сверить
  дропдаун «Вариант сокращения» с дропдауном «Тип» в том же модале (визуальная консистентность).
</verification>

<success_criteria>
- Дропдаун «Вариант сокращения» доступен в обоих режимах формы Места, сохраняется вместе с
  остальными полями одной кнопкой (двумя RPC), без утечки унаследованного значения в UI (D-05).
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-09-SUMMARY.md` when done
</output>
