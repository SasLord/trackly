---
phase: 39.1
plan: 06
type: execute
wave: 2
depends_on: ["39.1-01"]
files_modified:
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/templates/act_handover.html
  - crates/trackly-app/templates/_legacy_defaults/v27/act_handover.html
  - crates/trackly-app/src/pdf/html_templates.rs
autonomous: true
requirements: [PLC-08]
must_haves:
  truths:
    - "D-20: печатная форма акта использует ТЕКУЩИЙ эффективный вариант по acts.place_id (не замороженный на момент создания акта), применённый к замороженному place_path_snapshot; place_id пуст или место исчезло -> фолбэк на умолчание организации без паники"
    - "Pitfall 2 закрыт: правится ТОЛЬКО активный файловый HTML-пайплайн (templates/act_handover.html), НЕ document_templates в БД (тот заморожен вместе с krilla)"
    - "Pre-change снапшот templates/_legacy_defaults/v27/act_handover.html зафиксирован и зарегистрирован в KNOWN_LEGACY_DEFAULTS ДО правки живого шаблона — иначе существующие portable-инсталляции молча не получат апгрейд"
    - "Pitfall 3 закрыт: логика сокращения пути живёт в Rust (act_service.rs), НЕ в minijinja-условии внутри .html-шаблона"
  artifacts:
    - path: "crates/trackly-app/templates/_legacy_defaults/v27/act_handover.html"
      provides: "снапшот тела шаблона ДО правки place_path -> place_path_short"
    - path: "crates/trackly-app/src/pdf/html_templates.rs"
      provides: "запись v27 в KNOWN_LEGACY_DEFAULTS"
      contains: "_legacy_defaults/v27/act_handover.html"
  key_links:
    - from: "act_service.rs::render_pdf"
      to: "shorten_place_path"
      via: "вычисление place_path_short из place_path_snapshot + текущего place_effective_variant по acts.place_id, вставка в контекст рендера"
      pattern: "place_path_short"
    - from: "templates/act_handover.html"
      to: "act.place_path_short"
      via: "поле-строка 'Расположение:' читает {{ act.place_path_short }} вместо {{ act.place_path }}"
      pattern: "act.place_path_short"
---

<objective>
Сократить путь места в печатной форме акта приёма-передачи: `render_pdf` вычисляет
`place_path_short` из замороженного `place_path_snapshot`, применяя ТЕКУЩИЙ эффективный вариант
по `acts.place_id` (D-20). Обязательный пре-снапшот `_legacy_defaults/v27/` ДО правки шаблона.

Purpose: D-17/D-20 требуют, чтобы печатная форма акта показывала сокращённый путь, но снимок пути
(D-16 фазы 39) остаётся замороженным — сокращается по актуальному варианту, не по варианту на
момент создания акта.
Output: `render_pdf` вставляет `place_path_short` в контекст рендера, `act_handover.html` читает
новое поле, снапшот v27 зафиксирован и зарегистрирован для апгрейда существующих инсталляций.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из плана 01: `trackly_core::domain::places::{PathDisplayVariant, shorten_place_path}`.

`act_service.rs` уже импортирует `trackly_infra::repos::{...}` (строка 29) и держит
`self.readers: Arc<ReaderPool>` (используется на строках 2140+ для read-only запросов вне
writer-транзакции) — тот же паттерн `let conn = readers.acquire();` используется здесь для
однократного чтения `place_effective_variant.effective_variant` по `acts.place_id` + org
sep_ends/sep_last_two из `app_settings` (мирроринг `cartridges_sqlite.rs:930-950`'s
`conn.query_row(...).ok().and_then(...).unwrap_or(default)` shape — план 04 `<interfaces>` содержит
полный образец).

`ActDto.place_id: Option<i64>` (`crates/trackly-app/src/dto/act.rs:65`) — уже доступен на `act`
внутри `render_pdf` через `self.get(act_id).await?` (строка 2558).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Пре-снапшот шаблона v27 + регистрация в KNOWN_LEGACY_DEFAULTS</name>
  <files>crates/trackly-app/templates/_legacy_defaults/v27/act_handover.html, crates/trackly-app/src/pdf/html_templates.rs</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (ТЕКУЩЕЕ, ДО правки этим планом — копируется
    байт-в-байт в v27);
    crates/trackly-app/src/pdf/html_templates.rs (строки 70-89 — массив `KNOWN_LEGACY_DEFAULTS`,
    запись `act_handover.html` со списком `include_str!` на v20..v26 — новая запись v27
    добавляется ПОСЛЕДНЕЙ в этот же список, тот же комментарий-стиль, что у записи v26);
    ls `crates/trackly-app/templates/_legacy_defaults/` — подтверди, что v26 действительно
    последняя (следующий свободный номер v27).
  </read_first>
  <action>
    Скопировать ТЕКУЩЕЕ (до какой-либо правки этим планом) содержимое
    `crates/trackly-app/templates/act_handover.html` байт-в-байт в новый файл
    `crates/trackly-app/templates/_legacy_defaults/v27/act_handover.html`. Это ОБЯЗАН быть первый
    под-шаг Task 1 — Task 2 правит живой файл, и если снапшот сделан после правки, он бесполезен
    (Pitfall 2). Затем добавить `include_str!("../../templates/_legacy_defaults/v27/
    act_handover.html"),` последней строкой в список `act_handover.html` внутри
    `KNOWN_LEGACY_DEFAULTS` (после текущей записи v26), с комментарием в стиле существующего
    комментария к v26: поясни, что снапшот сделан НЕПОСРЕДСТВЕННО перед заменой поля
    `{{ act.place_path }}` на `{{ act.place_path_short }}` (Phase 39.1 Plan 06) — без этой записи
    инсталляции, материализовавшие body до этого плана, будут ошибочно классифицированы как
    "user-customized" и никогда не получат сокращённый путь.
  </action>
  <acceptance_criteria>
    - `_legacy_defaults/v27/act_handover.html` существует и байт-в-байт идентичен состоянию
      `act_handover.html` ДО правки Task 2 (проверяется git diff/визуально до коммита).
    - `KNOWN_LEGACY_DEFAULTS` содержит запись `_legacy_defaults/v27/act_handover.html` последней
      в списке для `"act_handover.html"`.
    - `every_default_template_has_a_known_legacy_defaults_entry` (существующий тест в
      `html_templates.rs`) остаётся зелёным.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app pdf::html_templates -- --test-threads=1</automated>
  </verify>
  <done>Снапшот v27 создан ДО правки живого шаблона, зарегистрирован в KNOWN_LEGACY_DEFAULTS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: place_path_short в контексте render_pdf + правка шаблона</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/templates/act_handover.html</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs (строки 2556-2600 — начало `render_pdf`, где
    доступен `act: ActDto` с полем `place_id`; строки 2680-2700 — существующий блок контекста
    `"act": { ..., "place_path": act.place_path_snapshot, ... }`, точка вставки нового поля);
    crates/trackly-app/templates/act_handover.html (строка 257 — `<div class="field-row">
    Расположение: {% if act.place_path %}{{ act.place_path }}{% else %}<span
    class="value-blank"></span>{% endif %}</div>`).
  </read_first>
  <behavior>
    - Акт с `place_id` = валидное место с `path_variant_override` = 'ends', `place_path_snapshot`
      = "Здание А / 1 этаж / 1-05" (замороженная строка) → печатная форма показывает
      "Здание А // 1-05" (текущий вариант 'ends' применён к замороженной строке).
    - Акт с `place_id` = NULL (места нет) → печатная форма показывает `place_path_snapshot`,
      сокращённый по УМОЛЧАНИЮ ОРГАНИЗАЦИИ (D-20 фолбэк), рендер НЕ падает.
    - Акт с `place_path_snapshot` = NULL (нет ни места, ни снимка) → шаблонное условие
      `{% if act.place_path_short %}` уходит в `<span class="value-blank">`, как сегодня для
      `place_path`.
  </behavior>
  <action>
    В `act_service.rs::render_pdf`, перед сборкой `ctx` (до строки ~2666), добавить блок,
    вычисляющий `place_path_short: Option<String>`: если `act.place_path_snapshot` пусто —
    сразу `None`; иначе через `self.readers.acquire()` прочитать эффективный вариант ОДНИМ
    запросом — `act.place_id.and_then(|pid| conn.query_row("SELECT effective_variant FROM
    place_effective_variant WHERE place_id = ?1", [pid], |r| r.get::<_, String>(0)).ok())`, и ЕСЛИ
    результат `None` (нет `place_id` ИЛИ `place_id` не нашёлся в вью — оба случая, Pitfall 4)
    — читать умолчание организации через тот же `conn` (`SELECT value FROM app_settings WHERE key
    = 'place_path_variant'`, фолбэк `"ends"` при отсутствии). Прочитать `sep_ends`/`sep_last_two`
    из `app_settings` тем же `conn` (мирроринг паттерна из плана 04 `<interfaces>`). Распарсить
    вариант через `PathDisplayVariant::from_str` (фолбэк на `PathDisplayVariant::Ends` при
    неожиданной ошибке парсинга — рендер акта НИКОГДА не должен паниковать/падать из-за этого
    поля, это некритичный визуальный элемент). Вызвать `shorten_place_path(snapshot, variant,
    &sep_ends, &sep_last_two)`. Использовать `Option`-цепочку без `.expect()`/`?` на всём пути
    (Pitfall 4 — акт без места или с исчезнувшим местом не должен ронять рендер).
    Добавить `"place_path_short": place_path_short,` рядом с существующим `"place_path":
    act.place_path_snapshot,` в блоке `"act": { ... }` контекста (строка ~2694) — ОБА поля
    остаются в контексте, `place_path` (замороженный, немодифицированный) НЕ удаляется (Pitfall 3
    — никакой логики форматирования в шаблоне, только переключение имени читаемого поля).
    В `templates/act_handover.html` (строка 257): заменить `{{ act.place_path }}` на
    `{{ act.place_path_short }}` и условие `{% if act.place_path %}` на `{% if
    act.place_path_short %}` — структура `field-row`/`value-blank` не меняется, меняется только
    имя читаемого поля.
  </action>
  <acceptance_criteria>
    - `render_pdf` вычисляет `place_path_short` без `.expect()`/`.unwrap()`/`?` на пути,
      зависящем от наличия `place_id` — акт с `place_id = NULL` рендерится без паники.
    - Контекст рендера содержит и `place_path` (неизменный снимок), и `place_path_short`
      (сокращённый по текущему варианту).
    - `act_handover.html:257` читает `act.place_path_short`, НЕ содержит условной логики
      сокращения (`{% if %}` только на пустоту, не на длину/сегменты).
    - Живой рендер (throwaway `#[ignore]`-тест) подтверждает: акт с местом из 3+ сегментов и
      вариантом 'ends' печатает сокращённую форму на PNG-осмотре.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app act_service::render_pdf -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app act_service::render_pdf -- --ignored --test-threads=1</automated>
  </verify>
  <done>`place_path_short` вычисляется по текущему варианту и вставляется в печатную форму акта;
  акт без места рендерится без паники, используя умолчание организации.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| act_service.rs::render_pdf → place_effective_variant / app_settings | Только чтение, place_id уже провалидирован при создании акта |
| minijinja-шаблон | Рендерит уже готовые Rust-строки, не выполняет форматирующую логику сам (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-10 | Denial of Service | `render_pdf` паникует на акте без места/с исчезнувшим местом | mitigate | Option-цепочка без `.expect()`/`?`, фолбэк на умолчание организации на любой ветке отсутствия данных (Pitfall 4) |
| T-39.1-11 | Tampering | Логика сокращения в шаблоне вместо Rust | mitigate | Вся арифметика выполняется в `act_service.rs` (Rust), шаблон только читает готовую строку — Pitfall 3 |
| T-39.1-12 | Missed Upgrade | Правка bundled-дефолта не доходит до существующих portable-инсталляций | mitigate | Обязательный пре-снапшот v27 в Task 1, ДО правки Task 2, зарегистрирован в `KNOWN_LEGACY_DEFAULTS` |
</threat_model>

<verification>
- `cargo test -p trackly-app pdf::html_templates -- --test-threads=1`
- `cargo test -p trackly-app act_service::render_pdf -- --test-threads=1`
- `cargo test -p trackly-app act_service::render_pdf -- --ignored --test-threads=1` (живой рендер +
  визуальный PNG-осмотр через `qlmanage`, по прецеденту `act_pdf_word_fidelity`)
</verification>

<success_criteria>
- Печатная форма акта показывает путь, сокращённый по ТЕКУЩЕМУ эффективному варианту.
- Акт без места или с исчезнувшим местом печатается без паники, используя умолчание организации.
- Существующие portable-инсталляции с уже материализованным на диске шаблоном получат апгрейд.
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-06-SUMMARY.md` when done
</output>
