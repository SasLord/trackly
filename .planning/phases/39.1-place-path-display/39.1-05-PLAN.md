---
phase: 39.1
plan: 05
type: execute
wave: 3
depends_on: ["39.1-01", "39.1-02"]
files_modified:
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/src/services/report_service.rs
autonomous: true
requirements: [PLC-08]
must_haves:
  truths:
    - "D-17: экранные отчёты и PDF-экспорт отчётов (reports_export_pdf) получают сокращённый путь (place_path_short) — row_field(row, col, tz, shorten=true) для PDF"
    - "D-18: CSV-экспорт отчётов отдаёт ПОЛНЫЙ путь — row_field(row, col, tz, shorten=false) для export_csv, намеренная асимметрия закреплена тестом на одном и том же ReportResponse"
    - "combine_printer_and_place сокращает ТОЛЬКО путевую часть композитной ячейки 'Принтер / Место' отчёта 'Заявки' — имя принтера не трогается ни при shorten=true, ни при shorten=false"
  artifacts:
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "ReportRow.place_path_short: Option<String>"
      contains: "place_path_short"
    - path: "crates/trackly-app/src/services/report_service.rs"
      provides: "row_field(row, col, tz, shorten: bool) — общая функция с явным переключателем CSV/PDF"
      contains: "fn row_field"
  key_links:
    - from: "export_csv"
      to: "row_field(row, col, tz, false)"
      via: "явный аргумент shorten=false — CSV всегда полный путь"
      pattern: "row_field(row, col, tz, false)"
    - from: "export_pdf"
      to: "row_field(row, col, tz, true)"
      via: "явный аргумент shorten=true — PDF/печать сокращённый путь"
      pattern: "row_field(row, col, tz, true)"
---

<objective>
Подключить `place_path_short` ко всем 5 доменам отчётов (`query_acts_inner`,
`query_device_snapshot`, `query_cartridge_audit`, `query_cartridge_snapshot`,
`query_requests_inner`) и закрепить намеренную асимметрию CSV (полный путь, D-18) vs PDF
(сокращённый путь, D-17) явным параметром `shorten: bool` в `row_field`.

Purpose: `row_field` сегодня используется ОДИНАКОВО из `export_csv` и `export_pdf` — без явного
переключателя оба вывода синхронно либо сократятся, либо останутся полными, что нарушает
D-17/D-18 в любую сторону (Pitfall 5 из RESEARCH.md).
Output: `ReportRow.place_path_short`, 5 SQL-запросов присоединяют `place_effective_variant`,
`row_field` получает `shorten: bool`, регрессионный тест на CSV/PDF-асимметрию.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-RESEARCH.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из плана 01: `trackly_core::domain::places::{PathDisplayVariant, shorten_place_path}` — см. план 03
`<interfaces>` для сигнатуры и образца чтения org-сепараторов.

Из плана 02 (уже смержен на момент этого плана): `OrgPathDisplayDto` в `crate::dto::reports` —
НЕ нужен напрямую этому плану (он читает `app_settings` сепараторы напрямую тем же способом, что и
планы 03/04), но файл `dto/reports.rs` уже содержит эту структуру — не удалять и не конфликтовать
с ней при добавлении `place_path_short` на `ReportRow`.

Текущая сигнатура (мирроринг, `report_service.rs:1016`):
```rust
fn row_field(row: &ReportRow, col: &str, tz: UtcOffset) -> String {
    match col {
        "place_path" => row.place_path.as_deref().unwrap_or("").to_string(),
        "printer_place" => {
            combine_printer_and_place(row.device_name.clone(), row.place_path.clone())
                .unwrap_or_default()
        }
        // ...
    }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: place_path_short на ReportRow — 5 доменов отчётов</name>
  <files>crates/trackly-app/src/dto/reports.rs, crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/dto/reports.rs (строки 69-90 — `ReportRow`, поле `place_path` на
    строке ~87, новое поле сразу после);
    crates/trackly-app/src/services/report_service.rs (функции `query_acts_inner` ~1070,
    `query_device_snapshot` ~1207, `query_cartridge_audit` ~1317, `query_cartridge_snapshot`
    ~1458, `query_requests_inner` ~1583 — КАЖДАЯ строит собственный SQL-литерал с `pfp.full_path
    AS place_path` и `LEFT JOIN place_full_paths pfp ON pfp.place_id = <fk>` — 5 независимых мест
    правки; строки 1140-1170 — образец полного SQL-литерала `query_acts_inner` для копирования
    структуры JOIN).
  </read_first>
  <behavior>
    - Каждый из 5 доменов (`query_acts_inner`, `query_device_snapshot`, `query_cartridge_audit`,
      `query_cartridge_snapshot`, `query_requests_inner`) с местом, чей `place_effective_variant`
      = 'ends' → возвращаемая строка несёт `place_path_short`, равный "первый // последний"
      сегменты того же `place_path`.
    - Строка без места (`place_id IS NULL` в underlying таблице) → `place_path_short: None`.
  </behavior>
  <action>
    В `crates/trackly-app/src/dto/reports.rs`: добавить `pub place_path_short: Option<String>` в
    `ReportRow` сразу после `pub place_path: Option<String>` (строка ~87).
    В `crates/trackly-app/src/services/report_service.rs`: добавить приватную функцию однократного
    чтения `(sep_ends, sep_last_two)` из `app_settings` (мирроринг паттерна из плана 04
    `<interfaces>`), вызываемую в начале каждой из 5 `query_*`-функций (или один раз в общей
    точке входа, если все 5 вызываются из одного общего места — проверь по коду; если нет общей
    точки, дублировать вызов в каждой из 5 функций приемлемо, это дешёвый однократный SELECT).
    В КАЖДОЙ из 5 SQL-литералов: добавить `LEFT JOIN place_effective_variant pev ON pev.place_id =
    <тот же fk-столбец, что использует существующий LEFT JOIN place_full_paths>` и колонку
    `pev.effective_variant AS place_variant` в SELECT-список. В соответствующей Rust-функции
    построения `ReportRow` из строки результата — вычислить `place_path_short` через
    `PathDisplayVariant::from_str` + `shorten_place_path`, симметрично планам 03/04 (`None` при
    отсутствующем месте/варианте).
  </action>
  <acceptance_criteria>
    - `ReportRow.place_path_short: Option<String>` присутствует.
    - Все 5 доменных запросов (`query_acts_inner`, `query_device_snapshot`,
      `query_cartridge_audit`, `query_cartridge_snapshot`, `query_requests_inner`) присоединяют
      `place_effective_variant` и заполняют `place_path_short`.
    - `cargo test -p trackly-app --test export_bindings` перегенерирует `bindings.ts` с новым
      полем на `ReportRow`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app report_service:: -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app --test export_bindings -- --test-threads=1</automated>
  </verify>
  <done>Все 5 доменов отчётов несут `place_path_short`, вычисленный по единой формуле.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: row_field(shorten: bool) — CSV/PDF асимметрия закреплена тестом</name>
  <files>crates/trackly-app/src/services/report_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/report_service.rs (строка 1016-1038 — полный текст `row_field`;
    строка 852 — вызов из `export_csv`; строка 970 — вызов из `export_pdf`; строки 284-293 —
    `combine_printer_and_place`, БЕЗ изменений сигнатуры; строки 2311-2354 — существующие тесты
    `combine_printer_and_place_*`/`row_field_printer_place_*`, образец для новых кейсов).
  </read_first>
  <behavior>
    - `row_field(row, "place_path", tz, false)` возвращает `row.place_path` (полный).
    - `row_field(row, "place_path", tz, true)` возвращает `row.place_path_short` (сокращённый,
      если отличается от полного — на пути из 3+ сегментов).
    - `row_field(row, "printer_place", tz, false)` → `combine_printer_and_place(device_name,
      row.place_path)` — имя принтера + ПОЛНЫЙ путь.
    - `row_field(row, "printer_place", tz, true)` → `combine_printer_and_place(device_name,
      row.place_path_short)` — то же имя принтера + СОКРАЩЁННЫЙ путь. Имя принтера идентично в
      обоих случаях (не сокращается никогда).
    - Один и тот же `ReportResponse` (одна и та же строка), экспортированный в CSV
      (`export_csv`) и в PDF (`export_pdf`) — значение колонки `place_path`/`printer_place`
      РАЗЛИЧАЕТСЯ между двумя выводами, если исходный путь имел 3+ сегмента (CSV длиннее или
      равен PDF, никогда наоборот).
  </behavior>
  <action>
    Изменить сигнатуру `row_field` на `fn row_field(row: &ReportRow, col: &str, tz: UtcOffset,
    shorten: bool) -> String`. В ветке `"place_path"`: `if shorten {
    row.place_path_short.as_deref() } else { row.place_path.as_deref() }.unwrap_or("").to_string()`.
    В ветке `"printer_place"`: передать вторым аргументом в `combine_printer_and_place` либо
    `row.place_path_short.clone()` (если `shorten`), либо `row.place_path.clone()` (иначе) —
    `printer_name`/первый аргумент не меняется независимо от `shorten`. Обновить ОБА вызывающих
    места: `export_csv` (строка ~852) → `row_field(row, col, tz, false)` (D-18, всегда полный);
    `export_pdf` (строка ~970) → `row_field(row, col, tz, true)` (D-17, всегда сокращённый).
    Добавить тесты рядом с существующими `combine_printer_and_place_*`/`row_field_printer_place_*`
    (после строки ~2354): `row_field_place_path_shorten_false_returns_full`,
    `row_field_place_path_shorten_true_returns_short`,
    `row_field_printer_place_shorten_true_shortens_only_place_not_printer_name` (assert имя
    принтера идентично между shorten=true/false вызовами на одной и той же `ReportRow`), и
    интеграционный тест `csv_pdf_asymmetry_same_report_response_differs_on_place_path`,
    экспортирующий ОДИН `ReportResponse` (с местом из 3+ сегментов) в оба формата и сравнивающий
    значения колонки — CSV должен содержать более длинную (или равную) строку, чем PDF, для того
    же логического ряда.
  </action>
  <acceptance_criteria>
    - `row_field` принимает `shorten: bool` четвёртым параметром, оба call site обновлены с
      правильными литеральными значениями (`false` в `export_csv`, `true` в `export_pdf`).
    - Новые unit-тесты подтверждают: `place_path`/`printer_place` расходятся между
      `shorten=true`/`false`, имя принтера — никогда.
    - Интеграционный тест на асимметрию CSV/PDF (один и тот же `ReportResponse`) зелёный.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app report_service::row_field -- --test-threads=1</automated>
    <automated>cargo test -p trackly-app report_service::csv_pdf_asymmetry -- --test-threads=1</automated>
  </verify>
  <done>`row_field` имеет явный `shorten` параметр, CSV/PDF асимметрия зафиксирована тестами,
  `combine_printer_and_place` никогда не сокращает имя принтера.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| export_csv → третьи лица | CSV-файл покидает приложение без hover-подсказок — полный путь обязателен (D-18) |
| export_pdf → печать/экран | PDF/печать остаются внутри контролируемого UI-контекста — сокращение допустимо (D-17) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-13 | Information Disclosure (потеря информации) | `row_field` синхронно сокращает CSV вместе с PDF при будущем рефакторинге | mitigate | Явный булев параметр `shorten` в сигнатуре + интеграционный тест `csv_pdf_asymmetry_*`, ловящий регрессию на уровне одного `ReportResponse` |
| T-39.1-14 | Tampering (искажение данных) | `combine_printer_and_place` случайно сокращает имя принтера | mitigate | Сигнатура `combine_printer_and_place` НЕ меняется — `shorten` применяется ДО вызова, к аргументу `printer_place`, не к `printer_name`; тест явно сравнивает имя принтера между обоими режимами |
</threat_model>

<verification>
- `cargo test -p trackly-app report_service:: -- --test-threads=1`
- `cargo test -p trackly-app --test export_bindings -- --test-threads=1`
</verification>

<success_criteria>
- Все 5 доменов отчётов несут `place_path_short`.
- CSV-экспорт отчётов остаётся на полном пути, PDF/печать — на сокращённом, асимметрия
  зафиксирована тестом на одном и том же `ReportResponse`.
- `combine_printer_and_place` никогда не сокращает имя принтера.
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-05-SUMMARY.md` when done
</output>
