---
phase: 39.1
plan: 08
type: execute
wave: 3
depends_on: ["39.1-02"]
files_modified:
  - ui/src/lib/components/Input.svelte
  - ui/src/lib/utils/placePath.ts
  - ui/src/features/settings/OrgSettings.svelte
autonomous: true
requirements: [PLC-07]
must_haves:
  truths:
    - "D-11: предпросмотр показывает ВСЕ ТРИ варианта разом на ДВУХ образцах (сетка 3x2) — «Здание А / 1 этаж / 1-05» и «Территория А / Объект Х / помещение 3», оба поля разделителей видны и применяются одновременно без переключения радио-кнопок"
  artifacts:
    - path: "ui/src/features/settings/OrgSettings.svelte"
      provides: "новый подраздел «Формат отображения пути места» под .logo-section: радио-группа, 2 поля разделителей, таблица предпросмотра 3x2, кнопка «Сохранить формат пути»"
      contains: "path-section"
    - path: "ui/src/lib/utils/placePath.ts"
      provides: "новая параметризованная функция предпросмотра (variant + sepEnds + sepLastTwo), НЕ трогает старые shortenPlacePath/normalizePlacePathDisplay/PlacePathDisplay (удаляются планом 10)"
  key_links:
    - from: "OrgSettings.svelte"
      to: "settings_get/set_place_path_defaults"
      via: "apiCall('settings_get_place_path_defaults', {}) / apiCall('settings_set_place_path_defaults', {patch})"
      pattern: "settings_set_place_path_defaults"
    - from: "OrgSettings.svelte предпросмотр"
      to: "новая JS-формула"
      via: "$derived пересчёт 6 ячеек на каждый keystroke в полях разделителей"
      pattern: "path-preview"
---

<objective>
UI-половина PLC-07: новый подраздел «Формат отображения пути места» в «Настройки → Организация»
(D-07) — радио-группа варианта (D-06 label copy), два поля разделителей со значимыми пробелами
(D-09/D-10), живой предпросмотр 3×2 (D-11), кнопка «Сохранить формат пути», вызывающая backend
из плана 02.

Purpose: PLC-07 требует UI, где администратор задаёт умолчание, не трогая `trackly.config.toml`.
Output: рабочий подраздел OrgSettings.svelte по UI-SPEC.md «Поверхность 1», новая
параметризованная JS-формула предпросмотра.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39.1-place-path-display/39.1-CONTEXT.md
@.planning/phases/39.1-place-path-display/39.1-UI-SPEC.md
@.planning/phases/39.1-place-path-display/39.1-PATTERNS.md
</context>

<interfaces>
Из плана 02 (уже смержен): Tauri-команды `settings_get_place_path_defaults() ->
OrgPathDisplayDto` и `settings_set_place_path_defaults({ patch: OrgPathDisplayDto }) -> void`,
где `OrgPathDisplayDto = { variant: string; sep_ends: string; sep_last_two: string }` (уже
сгенерировано в `ui/src/bindings.ts` планом 02's `export_bindings` шага).

UI-SPEC.md «Поверхность 1» (строки 124-390) — ПОЛНЫЙ DOM-контракт, copy, CSS-классы уже
зафиксированы checker'ом; следуй ему буквально, не изобретай альтернативную структуру.

Канонический образец радио-группы + сохранения (`ui/src/features/settings/
ThresholdSettings.svelte`, полный файл, ~60-100):
```svelte
<div class="radio-group" onchange={saveBasis}>
  <div class="radio-label">
    <Radio bind:group={basis} value="printer_model">
      <span class="radio-text"><span class="radio-title">По модели принтера</span></span>
    </Radio>
  </div>
</div>
```
УТОЧНЕНИЕ ОТ UI-SPEC: в этом плане save идёт через ОТДЕЛЬНУЮ кнопку «Сохранить формат пути»
(`Button onclick={savePathDefaults}`), НЕ через `onchange` на радио-обёртке — `Radio.svelte`
требует `bind:group`, но само сохранение асинхронно вызывается кнопкой, не событием радио.

Канонический образец загрузки/сохранения org-полей (`ui/src/features/settings/OrgSettings.svelte`,
`loadOrg()`/`saveOrg()`, строки 55-130) — тот же try/catch + `pushToast` паттерн.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Input mono-проп + новая JS-формула предпросмотра</name>
  <files>ui/src/lib/components/Input.svelte, ui/src/lib/utils/placePath.ts</files>
  <read_first>
    ui/src/lib/components/Input.svelte (полный файл — `Props` интерфейс строки 5-17, `<input>`
    строки 37-54, `.input` SCSS-блок строки 75-112);
    ui/src/lib/utils/placePath.ts (полный файл — СУЩЕСТВУЮЩИЕ `PlacePathDisplay`/
    `normalizePlacePathDisplay`/`shortenPlacePath` НЕ трогать в этом плане, они удаляются планом
    10 после того, как все их вызывающие стороны переключатся);
    .planning/phases/39.1-place-path-display/39.1-UI-SPEC.md § «Проблема 1 — значимые пробелы»
    (строки 226-276) — полный контракт `mono` пропа + `monoReadout` + формула таблицы 350-357.
  </read_first>
  <behavior>
    Новая функция предпросмотра — таблица «вход → выход» из UI-SPEC.md (буквально):
    - `previewShortenPath("Здание А / 1 этаж / 1-05", "ends", " // ", " / ")` →
      `"Здание А // 1-05"`.
    - `previewShortenPath("Здание А / 1 этаж / 1-05", "last_two", " // ", " / ")` →
      `"1 этаж / 1-05"`.
    - `previewShortenPath("Здание А / 1 этаж / 1-05", "last", " // ", " / ")` → `"1-05"`.
    - `previewShortenPath("Территория А / Объект Х / помещение 3", "ends", " // ", " / ")` →
      `"Территория А // помещение 3"`.
    - `monoReadout(" // ")` → `"·//·"` (каждый пробел U+0020 заменён на «·» U+00B7, только в
      readout-строке, не в самом значении поля).
  </behavior>
  <action>
    В `ui/src/lib/components/Input.svelte`: добавить `mono?: boolean;` в интерфейс `Props`
    (после `iconLeft`), деструктурировать с дефолтом `mono = false`, добавить `class:mono` на
    `<input>` рядом с существующим `class:invalid` (строка 43), добавить SCSS-правило `.input.mono
    { font-family: var(--tr-font-mono); }` внутри блока `.input` (после строки ~85, рядом с
    `&.invalid`). Не менять поведение при `mono` не переданном — дефолт `false` сохраняет текущий
    вид всех ~30 существующих вызовов `Input` в проекте.
    В `ui/src/lib/utils/placePath.ts`: добавить (НЕ заменяя существующий код) новую экспортируемую
    функцию `previewShortenPath(fullPath: string, variant: 'ends' | 'last_two' | 'last', sepEnds:
    string, sepLastTwo: string): string`, портирующую формулу D-13..D-16 (та же таблица «вход →
    выход», что и Rust `shorten_place_path` из плана 01 — segments = fullPath.split(' / '); ≤1
    сегмент → сам сегмент; 2 сегмента → полный путь через обычный ' / ' независимо от варианта
    (D-14); 3+ сегментов: 'ends' → первый+последний через sepEnds, 'last_two' → предпоследний+
    последний через sepLastTwo, 'last' → только последний). Добавить `monoReadout(v: string):
    string { return v.replace(/ /g, '·'); }` — точная реализация из UI-SPEC.md.
  </action>
  <acceptance_criteria>
    - `Input.svelte` принимает `mono` проп, дефолт `false`, не влияет на существующие вызовы без
      этого пропа.
    - `previewShortenPath` даёт РОВНО значения из таблицы UI-SPEC.md «Формула для JS-предпросмотра»
      (6 значений на двух образцах × 3 варианта).
    - `monoReadout` заменяет ТОЛЬКО пробелы (U+0020) на «·», не трогает остальные символы.
    - Существующие `PlacePathDisplay`/`normalizePlacePathDisplay`/`shortenPlacePath` в этом же
      файле НЕ изменены (проверяется тем, что 4 их текущих вызывающих компонента продолжают
      компилироваться без изменений — план 10 их тронет).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check --output human</automated>
  </verify>
  <done>`Input.svelte` несёт `mono` проп, `placePath.ts` несёт новую параметризованную формулу
  предпросмотра, старый код не тронут.</done>
</task>

<task type="auto">
  <name>Task 2: Подраздел «Формат отображения пути места» в OrgSettings.svelte</name>
  <files>ui/src/features/settings/OrgSettings.svelte</files>
  <read_first>
    ui/src/features/settings/OrgSettings.svelte (полный файл — `<script>` блок для добавления
    состояния/функций после существующего `saveOrg` (~строка 130); DOM — `.logo-section` строки
    325-360, новый `.path-section` вставляется СРАЗУ ПОСЛЕ него, внутри того же `<section
    class="settings-section">` (закрывается на строке 362); `<style lang="scss">` блок после
    строки 362 — новые классы добавляются туда же);
    .planning/phases/39.1-place-path-display/39.1-UI-SPEC.md § «Поверхность 1» целиком (строки
    124-390) — ПОЛНЫЙ буквальный DOM/copy/CSS контракт, включая
    `path-variant-group`/`path-sep-fields`/`path-preview`/`save-row` разметку и SCSS-блок
    `.path-section`/`.path-lead`/`.path-variant-group`/`.path-radio-row`/`.path-preview-wrap`/
    `.path-preview` (строки 139-213 разметка, 226-343 обоснование, 359-387 SCSS).
  </read_first>
  <behavior>
    - При открытии страницы Настроек → Организация подраздел «Формат отображения пути места»
      загружает текущие значения через `settings_get_place_path_defaults` и сразу показывает
      корректный предпросмотр (не пустые ячейки).
    - Изменение любого из двух полей разделителей ($state) немедленно (реактивно, без клика)
      обновляет все 6 ячеек таблицы предпросмотра — оба поля разделителей видны и применяются
      ОДНОВРЕМЕННО, независимо от того, какая радио-кнопка выбрана (D-11 — все три варианта
      считаются разом на каждый keystroke).
    - Пустой `sepEnds` (`""`) → `Input` получает `invalid={true}`, `.field-error` показывает текст
      из Copywriting Contract («Разделитель не может быть пустым…»), клик «Сохранить формат пути»
      всё ещё возможен (сервер — источник истины, ошибка всплывает через `pushToast` при 400).
    - Успешное сохранение → `pushToast('success', 'Формат пути сохранён')`.
  </behavior>
  <action>
    В `<script>` блоке `OrgSettings.svelte`: добавить `$state`-переменные `pathVariant`
    (`'ends'|'last_two'|'last'`, дефолт `'ends'`), `sepEnds`/`sepLastTwo` (строки, дефолт `' // '`/
    `' / '` до загрузки), `savingPath` (boolean). Добавить `$derived`-выражения для 6 ячеек
    предпросмотра, вызывающие `previewShortenPath` из Task 1 на двух образцах-константах
    `"Здание А / 1 этаж / 1-05"` и `"Территория А / Объект Х / помещение 3"` (буквально эти
    строки, дословно из D-11/UI-SPEC.md). Добавить `loadPathDefaults()` (вызывается из
    существующего `onMount`, добавляется к вызову `loadOrg()`) — `apiCall<OrgPathDisplayDto>
    ('settings_get_place_path_defaults', {})`, заполняет три состояния, try/catch +
    `pushToast('error', ...)` мирроринг `loadOrg()`. Добавить `savePathDefaults()` — `apiCall<void>
    ('settings_set_place_path_defaults', { patch: { variant: pathVariant, sep_ends: sepEnds,
    sep_last_two: sepLastTwo } })`, `savingPath` флаг вокруг вызова, try/catch +
    `pushToast('success'|'error', ...)`.
    В DOM: вставить блок `.path-section` СРАЗУ ПОСЛЕ `.logo-section` (внутри `<section
    class="settings-section">`), буквально по разметке UI-SPEC.md строки 142-213 —
    заголовок `.subsection-title` «Формат отображения пути места», `.path-lead` пояснение,
    `role="radiogroup" aria-label="Вариант сокращения по умолчанию"` с тремя `<Radio
    bind:group={pathVariant} value="ends|last_two|last">`, `.form-grid.path-sep-fields` с двумя
    `<Input mono invalid={...} oninput={...}>` полями (лейблы «Разделитель «Крайние»»/«Разделитель
    «Два последних»»), `.field-hint.tr-mono` readout через `monoReadout()` под каждым полем,
    `<table class="path-preview">` 3×2 с `<caption class="sr-only">`, `<div class="save-row">` с
    `<Button variant="primary" loading={savingPath} onclick={savePathDefaults}>Сохранить формат
    пути</Button>`.
    В `<style lang="scss">`: добавить классы `.path-section`/`.path-lead`/`.path-variant-group`/
    `.path-radio-row`/`.path-preview-wrap`/`.path-preview` буквально по UI-SPEC.md SCSS (строки
    293-321, 359-387).
  </action>
  <acceptance_criteria>
    - Подраздел расположен СРАЗУ ПОСЛЕ `.logo-section`, внутри того же `<section
      class="settings-section">`.
    - Радио-группа использует `bind:group={pathVariant}` на все три `<Radio>` (не отдельные
      `checked` пропы — Radio.svelte требует `bind:group`, memory
      `native_select_vs_custom_dropdown`/проектный прецедент `ThresholdSettings.svelte`).
    - Оба поля разделителей ВСЕГДА видимы и активны одновременно, независимо от выбранного
      radio (НЕ вложены под конкретный radio-пункт).
    - Таблица предпросмотра показывает РОВНО значения из UI-SPEC.md таблицы (6 ячеек) при
      дефолтных разделителях.
    - `svelte-check`/`eslint`/`pnpm build` зелёные.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check --output human</automated>
    <automated>pnpm --dir ui build</automated>
  </verify>
  <done>Подраздел «Формат отображения пути места» существует, загружает/сохраняет умолчания
  организации, живой предпросмотр 3×2 реактивен.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| OrgSettings.svelte → settings_set_place_path_defaults | Фронт не является источником истины по валидации — сервер (план 02) отклоняет пустой разделитель/неизвестный вариант независимо от клиентской проверки |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-18 | Tampering (обход клиентской валидации) | Прямой вызов `settings_set_place_path_defaults` мимо UI | accept | Backend (план 02) — единственный источник истины валидации; UI-проверка — только UX-подсказка, не гейт |
</threat_model>

<verification>
- `pnpm --dir ui exec svelte-check --output human`
- `pnpm --dir ui exec eslint src`
- `pnpm --dir ui build`
- Живая проверка (per VALIDATION.md Manual-Only): `cargo tauri dev`, открыть Настройки →
  Организация, менять оба поля разделителей, убедиться, что все 6 ячеек обновляются без
  переключения радио-кнопок.
</verification>

<success_criteria>
- Администратор задаёт умолчание организации и оба разделителя в UI, видит live-предпросмотр 3×2,
  сохранение вызывает backend из плана 02.
</success_criteria>

<output>
Create `.planning/phases/39.1-place-path-display/39.1-08-SUMMARY.md` when done
</output>
