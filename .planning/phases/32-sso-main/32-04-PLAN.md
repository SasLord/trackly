---
phase: 32-sso-main
plan: 04
type: execute
wave: 4
depends_on: ["32-03"]
files_modified:
  - crates/trackly-app/src/dto/act.rs
  - crates/trackly-app/src/http/sso.rs
  - crates/trackly-app/src/pdf/html_templates.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/services/auth.rs
  - crates/trackly-app/tests/acts_archived_at.rs
  - crates/trackly-app/tests/acts_date_source.rs
  - crates/trackly-app/tests/acts_update.rs
  - crates/trackly-app/tests/acts_update_return.rs
  - crates/trackly-app/tests/html_act_render.rs
  - crates/trackly-app/tests/report_returns_sub_number.rs
  - crates/trackly-infra/src/ad/keytab.rs
  - crates/trackly-infra/src/ad/sso.rs
  - crates/trackly-infra/src/repos/audit_log_sqlite.rs
autonomous: true
requirements: [SSO-02]

must_haves:
  truths:
    - "cargo fmt --all -- --check passes workspace-wide before this branch can merge to main (D-11/D-12, RESEARCH.md Pitfall 1 — verified failing at planning time, must be fixed as its own commit)."
    - "cargo clippy --workspace --all-targets -- -D warnings is green on the full workspace (D-12)."
    - "cargo test --workspace --no-fail-fast -- --test-threads=1 is green (matches ci-fast/ci-full's own invocation) before opening the merge PR."
    - "A PR from spike/ad-sso-kerberos to main is opened, triggering a real ci-full run across all 3 OSes (ubuntu/macos/windows) plus the ProcMon Windows check — this branch has only ever run ci-fast until now (Open Question #3)."
  artifacts:
    - path: "(fmt-only diff, no new files)"
      provides: "Workspace-wide rustfmt compliance, zero semantic changes (rustfmt is AST-preserving)"
      contains: "N/A — verified via cargo fmt --all -- --check exit code"
  key_links:
    - from: "spike/ad-sso-kerberos branch"
      to: "main branch"
      via: "gh pr create --base main --head spike/ad-sso-kerberos"
      pattern: "ci-full.yml pull_request trigger"
---

<objective>
Fix the pre-existing, verified `cargo fmt --all -- --check` drift (14 files, none in
Phase 32's own functional territory) that would otherwise fail `main`'s CI the
instant this branch merges, run the full workspace verification gate (fmt + clippy +
test), then open a PR from `spike/ad-sso-kerberos` to `main` to get a real, free
`ci-full` dry-run (3-OS matrix + ProcMon Windows check) BEFORE the actual merge —
this branch has only ever run `ci-fast` (ubuntu-only) so far.

Purpose: De-risk the merge itself. D-12's premise (a Windows-gated `gssapi` Cargo
feature) does NOT match the actual code (verified in RESEARCH.md: SPNEGO uses the
pure-Rust `sspi` crate, unconditionally compiled, no feature flag) — so the real
merge-readiness gate is "fmt/clippy/test green + a real ci-full run," not "add
feature-gating."
Output: A `chore` commit fixing fmt drift, a full green verification run, and an open
PR with a real `ci-full` run link — the human-gated merge itself happens in Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-sso-main/32-CONTEXT.md
@.planning/phases/32-sso-main/32-RESEARCH.md
@.planning/phases/32-sso-main/32-VALIDATION.md
</context>

<interfaces>
Confirmed (re-run at planning time, 2026-08-03) exact file list with fmt drift —
matches RESEARCH.md's Pitfall 1 finding, none touched by Plans 01-03 of this phase
(except `auth.rs`, which those plans DO touch with new, correctly-formatted code —
running `cargo fmt --all` after Plans 01-03 land formats both the pre-existing drift
in that file AND, harmlessly/idempotently, whatever Plans 01-03 wrote):

```
crates/trackly-app/src/dto/act.rs
crates/trackly-app/src/http/sso.rs
crates/trackly-app/src/pdf/html_templates.rs
crates/trackly-app/src/services/act_service.rs
crates/trackly-app/src/services/auth.rs
crates/trackly-app/tests/acts_archived_at.rs
crates/trackly-app/tests/acts_date_source.rs
crates/trackly-app/tests/acts_update.rs
crates/trackly-app/tests/acts_update_return.rs
crates/trackly-app/tests/html_act_render.rs
crates/trackly-app/tests/report_returns_sub_number.rs
crates/trackly-infra/src/ad/keytab.rs
crates/trackly-infra/src/ad/sso.rs
crates/trackly-infra/src/repos/audit_log_sqlite.rs
```

CI trigger scopes (`.github/workflows/ci-fast.yml` / `ci-full.yml`, confirmed in
RESEARCH.md): `ci-fast` runs on every push to any branch (ubuntu-only); `ci-full`
runs ONLY on `pull_request` or `push: branches: [main]` — this branch has never had
`ci-full` run against it. `.github/workflows/release.yml` triggers ONLY on
`push: tags: v*.*.*` (three-segment) or `workflow_dispatch`. No merged PRs exist yet
in this repo (`gh pr list --state merged` returns empty) — there is no established
squash-vs-merge-commit convention; Plan 05 defaults to a standard merge commit unless
the user requests otherwise at that checkpoint.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix workspace fmt drift and run the full verification gate</name>
  <files>crates/trackly-app/src/dto/act.rs, crates/trackly-app/src/http/sso.rs, crates/trackly-app/src/pdf/html_templates.rs, crates/trackly-app/src/services/act_service.rs, crates/trackly-app/src/services/auth.rs, crates/trackly-app/tests/acts_archived_at.rs, crates/trackly-app/tests/acts_date_source.rs, crates/trackly-app/tests/acts_update.rs, crates/trackly-app/tests/acts_update_return.rs, crates/trackly-app/tests/html_act_render.rs, crates/trackly-app/tests/report_returns_sub_number.rs, crates/trackly-infra/src/ad/keytab.rs, crates/trackly-infra/src/ad/sso.rs, crates/trackly-infra/src/repos/audit_log_sqlite.rs</files>
  <read_first>
    Run `cargo fmt --all -- --check` first to reconfirm the CURRENT drift list
    (Plans 01-03 will have added new formatted code to `auth.rs`/`config.rs`/
    `context.rs` by this point — those should already be clean since executors run
    `cargo fmt` habitually, but re-verify). Read RESEARCH.md's "Pitfall 1" section
    for the full rationale (ci-fast/ci-full both gate on this check; it already
    fails on this branch's own most recent ci-fast run per RESEARCH.md's cited run
    ID). Do NOT hand-edit any of these files — `rustfmt` output only, no manual
    formatting changes.
  </read_first>
  <action>
    Run `cargo fmt --all` (no `--check`) once at the workspace root. Inspect
    `git diff --stat` to confirm ONLY the files listed above (plus possibly
    `auth.rs`/`config.rs`/`context.rs` if Plans 01-03 left any minor formatting
    inconsistency) changed — no other files. Spot-check `git diff` on 2-3 of the
    files to confirm changes are purely whitespace/line-wrapping (rustfmt is
    AST-preserving by design; this is a sanity check, not a deep review). Run
    `cargo clippy --workspace --all-targets -- -D warnings` and confirm zero
    warnings. Run `cargo test --workspace --no-fail-fast -- --test-threads=1` and
    confirm all tests pass (per repo memory: never run a second `cargo test`
    concurrently — this must be the only build/test command running; budget
    several minutes for a full workspace compile). Commit the fmt fix as its own
    `chore` commit, separate from any Plan 01-03 code commits:
    `chore(fmt): fix workspace formatting drift before v1.3 merge`.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -20 && cargo test --workspace --no-fail-fast -- --test-threads=1 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0 (no diff).
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace --no-fail-fast -- --test-threads=1` exits 0 (all
      tests, including Plan 01-03's new tests and the full pre-existing suite,
      pass).
    - `git log -1 --format=%s` shows the `chore(fmt): ...` commit message.
    - `git diff HEAD~1 --stat` (the fmt commit itself) touches ONLY files already
      listed in this plan's `files_modified` plus, optionally, files Plans 01-03
      touched — no unrelated files.
  </acceptance_criteria>
  <done>
    The workspace is fully `cargo fmt --all -- --check`-clean, `clippy -D warnings`-
    clean, and `cargo test --workspace`-green, committed as a standalone `chore`
    commit ready to merge.
  </done>
</task>

<task type="auto">
  <name>Task 2: Push branch and open a PR to main for a free ci-full dry-run</name>
  <files>(no source files — git/gh operations only)</files>
  <read_first>
    Read `.github/workflows/ci-full.yml` lines 1-25 (trigger scope: `pull_request` +
    `push: branches: [main]`, 3-OS matrix + ProcMon) to confirm what opening this PR
    will actually run.
  </read_first>
  <action>
    Push the branch: `git push origin spike/ad-sso-kerberos`. Open a PR via
    `gh pr create --base main --head spike/ad-sso-kerberos --title "AD-SSO Kerberos
    parity + auto-admin by login list (v1.3)" --body "..."` summarizing: Phase 31
    (SSO-01/SSO-03, service-bind ФИО + group-role mapping) + Phase 32 (SSO-02,
    admin_logins auto-admin) + the fmt-drift fix from Task 1. Do NOT merge the PR in
    this task — only open it. After creation, poll for the triggered `ci-full` run
    via `gh pr checks &lt;pr-number&gt; --watch` or `gh run list --branch
    spike/ad-sso-kerberos --workflow=ci-full.yml` until it completes (this may take
    several minutes across 3 OSes — this is expected, not a hang). Report the PR URL
    and the final `ci-full` run status/URL as this task's output for Plan 05's
    checkpoint to reference.
  </action>
  <verify>
    <automated>gh pr view --json url,state 2>&1 && gh run list --branch spike/ad-sso-kerberos --workflow=ci-full.yml --limit 1 --json status,conclusion,url 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view --json state` reports `"OPEN"` (not merged, not closed — merge is
      Plan 05's job).
    - `gh run list --branch spike/ad-sso-kerberos --workflow=ci-full.yml --limit 1`
      shows a run triggered by this PR (not an older/unrelated run).
    - The reported `ci-full` run `conclusion` is captured verbatim (success or
      failure) for Plan 05's human checkpoint to act on — if it failed, this task
      is NOT done (fix the failure and re-push before considering Task 2 complete).
  </acceptance_criteria>
  <done>
    A PR from `spike/ad-sso-kerberos` to `main` is open, `ci-full` has run against
    it (3-OS matrix + ProcMon), and the PR URL + run status are recorded for the
    human merge/tag checkpoint in Plan 05.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `spike/ad-sso-kerberos` -> GitHub Actions | Opening a PR triggers `ci-full.yml` to run against this branch's code on GitHub-hosted runners — standard GitHub Actions trust model, no new boundary introduced by this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-11 | Tampering | merging unverified code into `main` | mitigate | Opening the PR BEFORE merging gets a real, free `ci-full` run (3-OS matrix + ProcMon Windows portable-mode check) as a pre-merge gate — this is exactly the mitigation RESEARCH.md's Open Question #3 recommends, cheaper than discovering a Windows-only failure after `main` is already red. |
| T-32-12 | Tampering (supply chain, low) | `cargo fmt --all` output | accept | `rustfmt` is a trusted, already-pinned toolchain component (rustfmt 1.8.0-stable, confirmed identical output under both the pinned 1.92.0 and CI's 1.88 toolchains in RESEARCH.md) — no new trust introduced, this is formatting an already-reviewed codebase, not accepting new external code. |

</threat_model>

<verification>
- `cargo fmt --all -- --check` exits 0.
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
- `cargo test --workspace --no-fail-fast -- --test-threads=1` exits 0.
- PR from `spike/ad-sso-kerberos` to `main` is open with a completed `ci-full` run.
</verification>

<success_criteria>
- The pre-existing fmt drift is fixed and committed as its own `chore` commit.
- The full workspace (fmt + clippy + test) is green.
- A real `ci-full` dry-run has executed against this branch via an open PR, with its
  result recorded for Plan 05's human-gated merge decision.
</success_criteria>

<output>
Create `.planning/phases/32-sso-main/32-04-SUMMARY.md` when done
</output>
