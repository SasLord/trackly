---
phase: 32-sso-main
plan: 05
type: execute
wave: 5
depends_on: ["32-04"]
files_modified: []
autonomous: false
requirements: [SSO-02]

must_haves:
  truths:
    - "spike/ad-sso-kerberos merges into main ONLY after a human explicitly confirms the ci-full PR check (opened in Plan 04) is green — this is an operator/human gate, not something an executor decides autonomously (planning_guidance explicit instruction)."
    - "A three-segment tag v1.3.0 is pushed AFTER the merge to main, triggering release.yml's build/draft-release pipeline (D-11 — release.yml ignores two-segment tags like v1.3, confirmed in RESEARCH.md)."
    - "SSO functionality exits the 0.0.x spike version lineage into the normal v1.3.0 release tag (ROADMAP Phase 32 operational note)."
  artifacts:
    - path: "(no new files — git/gh operations on existing branches/tags)"
      provides: "main branch updated with Phase 31 + Phase 32 SSO work; v1.3.0 tag pushed"
      contains: "N/A"
  key_links:
    - from: "spike/ad-sso-kerberos"
      to: "main"
      via: "gh pr merge (after human approval)"
      pattern: "merge"
    - from: "v1.3.0 tag push"
      to: ".github/workflows/release.yml"
      via: "push: tags: v*.*.*"
      pattern: "v1.3.0"
---

<objective>
Human-gated final step: confirm the `ci-full` run from Plan 04's PR is green, then
merge `spike/ad-sso-kerberos` into `main` and push the three-segment release tag
`v1.3.0` — completing the ROADMAP Phase 32 operational note ("ветка
spike/ad-sso-kerberos мержится в main, версия выходит из спайковой линейки 0.0.x в
обычный релизный тег").

Purpose: This is a milestone-defining, hard-to-undo action (merging a spike branch
into `main` and cutting a public release tag) — per this phase's explicit planning
guidance, an executor must NOT perform the merge/tag push autonomously, even though
it is technically automatable via `gh`/`git`. A human confirms readiness first.
Output: `main` contains Phase 31 + Phase 32; `v1.3.0` tag pushed; `release.yml`
building a draft release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-sso-main/32-CONTEXT.md
@.planning/phases/32-sso-main/32-RESEARCH.md
</context>

<interfaces>
`.github/workflows/release.yml` (confirmed in RESEARCH.md, lines 9-11): triggers on
`push: tags: v*.*.*` (three-segment ONLY — `v1.3` alone does NOT trigger a build) or
`workflow_dispatch`. Version is patched transiently at BUILD time (`perl -0pi`,
lines 142-173 of that file) — `main`'s own `Cargo.toml` stays at `version = "0.1.0"`
permanently; do NOT hand-edit `Cargo.toml` version fields as part of this plan.

No merged PRs exist yet in this repo — default to `gh pr merge --merge` (a standard
merge commit, preserving full branch history) unless the human reviewer requests
squash/rebase instead at the Task 1 checkpoint.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm ci-full is green before merge</name>
  <what-built>
    Plan 04 fixed the pre-existing workspace fmt drift, verified
    fmt/clippy/test are all green locally, and opened a PR from
    `spike/ad-sso-kerberos` to `main` — which triggered a real `ci-full` run (3-OS
    matrix: ubuntu/macos/windows, plus the ProcMon Windows portable-mode check).
    This branch carries the full v1.3 SSO milestone: Phase 31 (SSO-01/SSO-03 —
    service-account AD bind for real ФИО + AD-group role mapping) and Phase 32
    (SSO-02 — admin_logins auto-admin by trusted login list).
  </what-built>
  <how-to-verify>
    1. Open the PR URL reported by Plan 04's Task 2 (or run `gh pr view --json url`
       on this branch).
    2. Confirm the `ci-full` check on the PR is GREEN across all 3 OS jobs
       (ubuntu-latest, macos-latest, windows-latest) AND the ProcMon portable-mode
       step.
    3. Optionally skim the PR diff for anything unexpected (this PR should contain
       ONLY Phase 31 + Phase 32 work plus the Plan 04 fmt-fix chore commit — no
       unrelated changes).
    4. Confirm you are comfortable with this branch's SSO functionality shipping in
       the next release as `v1.3.0` (real domain/AD verification on Windows is a
       separate manual step per 32-VALIDATION.md's "Manual-Only Verifications" —
       this checkpoint is specifically about CI-green + code-review confidence, not
       live-AD verification).
  </how-to-verify>
  <resume-signal>
    Type "approved" to proceed with merging `spike/ad-sso-kerberos` into `main` and
    pushing tag `v1.3.0` — or describe what needs to change first (e.g. "ci-full
    failed on windows, investigate before merging").
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Merge to main and push the v1.3.0 release tag</name>
  <files>(no source files — git/gh operations only)</files>
  <read_first>
    This task only runs after Task 1's "approved" resume signal. Re-read
    `.github/workflows/release.yml` lines 1-20 to reconfirm the exact tag pattern
    (`v*.*.*`, three segments, e.g. `v1.3.0` — NOT `v1.3`) before creating the tag.
  </read_first>
  <action>
    Merge the PR: `gh pr merge --merge` (standard merge commit — no squash/rebase
    unless the human reviewer requested otherwise at the Task 1 checkpoint).
    Update the local `main` branch: `git checkout main && git pull origin main`.
    Create an annotated tag: `git tag -a v1.3.0 -m "v1.3.0 — AD-SSO паритет
    (Kerberos/SPNEGO service-bind ФИО, AD-group роли, авто-админ по списку
    логинов) + релиз spike-ветки в main"`. Push the tag:
    `git push origin v1.3.0`. Confirm `release.yml` was triggered:
    `gh run list --workflow=release.yml --limit 1 --json status,conclusion,url`.
    Report the release workflow run URL and, once it completes, the resulting draft
    GitHub Release URL.
  </action>
  <verify>
    <automated>git log main -1 --format=%H && git tag -l v1.3.0 && gh run list --workflow=release.yml --limit 1 --json status,conclusion,url 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l v1.3.0` shows the tag exists locally and
      `git ls-remote --tags origin v1.3.0` confirms it was pushed.
    - `gh run list --workflow=release.yml --limit 1` shows a run triggered by the
      `v1.3.0` tag push (not an older/unrelated run).
    - `git log main -1` shows either the merge commit from Task 2's `gh pr merge` or
      the fast-forwarded HEAD of `spike/ad-sso-kerberos` — `main` now contains Phase
      31 + Phase 32 work.
    - The `spike/ad-sso-kerberos` branch's PR is in `MERGED` state
      (`gh pr view --json state`).
  </acceptance_criteria>
  <done>
    `main` contains the full v1.3 SSO milestone (Phase 31 + Phase 32), the
    `v1.3.0` tag is pushed, and `release.yml` has started building the draft
    GitHub Release.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human approval -> `main` merge | The merge into `main` and the release tag push are gated behind an explicit human "approved" resume signal (Task 1) — not autonomous, per this phase's planning guidance. This is the only plan in Phase 32 with `autonomous: false`. |
| `v1.3.0` tag -> `release.yml` -> public GitHub Release | Pushing this tag triggers a build pipeline that produces a public, downloadable draft release — the last point before this phase's work (including the SSO-02 privilege-escalation-by-design feature) becomes distributable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-13 | Elevation of Privilege / Tampering | merge-to-main + tag-push sequence | mitigate | Explicit human checkpoint (Task 1) gates the merge — an executor cannot autonomously push privilege-escalation-capable code (SSO-02's admin_logins feature) or a public release tag without a human confirming `ci-full` is green and the diff is as expected. This is the primary mitigation for the entire phase's security-significant nature (D-07). |
| T-32-14 | Repudiation | who approved the merge | accept | The human's "approved" resume signal is recorded in the plan execution's conversation/session log (standard GSD execution-log behavior) — no additional tooling needed for this low-frequency, high-visibility operational action. |

</threat_model>

<verification>
- Task 1's human checkpoint is answered with an explicit "approved" (or equivalent)
  before Task 2 runs.
- `main` contains the merged PR; `v1.3.0` tag is pushed; `release.yml` run is
  triggered and its status recorded.
</verification>

<success_criteria>
- `spike/ad-sso-kerberos` is merged into `main` only after human-confirmed
  `ci-full` green status.
- `v1.3.0` (three-segment) tag is pushed, triggering `release.yml`.
- Phase 32 (SSO-02) and the v1.3 milestone's operational merge/release note are both
  complete.
</success_criteria>

<output>
Create `.planning/phases/32-sso-main/32-05-SUMMARY.md` when done
</output>
