---
phase: 32-sso-main
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-infra/src/config.rs
  - trackly.config.toml.example
autonomous: true
requirements: [SSO-02]

must_haves:
  truths:
    - "Admin can list trusted domain logins in trackly.config.toml under [ad] admin_logins; empty/absent list = feature fully off, nobody gets auto-admin (D-01, D-03)."
    - "admin_logins parses as a flat TOML string array (no [[ad.admin_logins]] sub-table needed) — simpler than role_mapping's array-of-tables (D-01)."
    - "Config (not DB/UI) is the deliberate storage choice: it solves the chicken-and-egg first-admin problem without requiring an existing admin, mirrors ADMIN_AD_LOGINS's deployment-time semantics, survives portable-mode copy, and matches how Phase 31 already stores role_mapping (D-02)."
    - "The config file's comment documents the security-significant nature of this field (D-07: it overrides manual admin blocking) and the 'requires restart, no live reload' behavior (Pitfall 3)."
  artifacts:
    - path: crates/trackly-infra/src/config.rs
      provides: "AdConfig.admin_logins: Vec<String> field (#[serde(default)]) + Default impl entry + Debug impl entry + parsing tests"
      contains: "pub admin_logins: Vec<String>"
    - path: trackly.config.toml.example
      provides: "Documented admin_logins block next to role_mapping, with security warning + restart-required note"
      contains: "admin_logins"
  key_links:
    - from: "trackly.config.toml [ad] admin_logins = [...]"
      to: "AppConfig.ad.admin_logins"
      via: "toml::from_str deserialization (serde derive on AdConfig)"
      pattern: "admin_logins"
---

<objective>
Add the `admin_logins: Vec<String>` field to `AdConfig` — the deployment-time TOML
config field that lists trusted domain logins which get forced Administrator role on
AD-bind (SSO-02). This plan covers ONLY the config/parsing layer: the field, its
`Default`/`Debug` wiring, parsing tests, and documentation in the example config file.
The runtime provisioning logic that consumes this field lives in Plan 02 (Wave 2).

Purpose: Give the "авто-админ по списку логинов" feature a config-time source of
truth, mirroring the exact pattern Phase 31 already established for `role_mapping` —
zero new parsing mechanism, zero new dependency.
Output: `AdConfig.admin_logins` compiles, deserializes, defaults to empty, and is
documented for the deployment operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-sso-main/32-CONTEXT.md
@.planning/phases/32-sso-main/32-RESEARCH.md
@.planning/phases/32-sso-main/32-PATTERNS.md
</context>

<interfaces>
Current shape of AdConfig (crates/trackly-infra/src/config.rs:161-288), extracted so
the executor does not need to re-derive it before editing.

Struct (:161-234) — `role_mapping` is the LAST field today, at :229-233:
- `enabled: bool`, `use_mock: bool`, `host: String`, `port: u16`, `domain: String`,
  `base_dn: String`, `name_attr: String`, `no_tls_verify: bool`
- `#[serde(default)] sso_enabled: bool`, `#[serde(default)] spn: String`,
  `#[serde(default)] keytab_path: String`
- `#[serde(default)] bind_dn: String`, `#[serde(default)] bind_password: String`
- `#[serde(default = "default_display_name_cache_ttl_secs")] display_name_cache_ttl_secs: u64`
- `#[serde(default = "default_group_cache_ttl_secs")] group_cache_ttl_secs: u64`
- `#[serde(default)] role_mapping: Vec<RoleMappingEntry>` <- new field goes right after this

Manual `impl std::fmt::Debug for AdConfig` (:236-265) — `.field("role_mapping",
&self.role_mapping)` is the last `.field()` call before `.finish()` at :262.

`impl Default for AdConfig` (:267-288) — `role_mapping: Vec::new(),` is the last field
at :285.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin_logins field to AdConfig with parsing tests</name>
  <files>crates/trackly-infra/src/config.rs</files>
  <read_first>
    Read crates/trackly-infra/src/config.rs lines 161-419 in full (struct, manual Debug
    impl, Default impl, and the existing #[cfg(test)] mod tests block including
    role_mapping_array_of_tables_deserializes at line 397). This is the exact analog to
    mirror — see 32-PATTERNS.md "Pattern 1: Config field mirrors role_mapping exactly"
    for the precise diff shape.
  </read_first>
  <behavior>
    - Test 1 (admin_logins_flat_array_deserializes_and_defaults_empty): empty TOML
      (toml::from_str("")) parses AppConfig successfully with
      cfg.ad.admin_logins == Vec::&lt;String&gt;::new().
    - Test 2 (same test fn, second assertion): a [ad] section containing
      admin_logins = ["us100", "us777"] alongside the other required [ad] fields
      (enabled/use_mock/host/port/domain/base_dn/name_attr/no_tls_verify) parses into
      cfg.ad.admin_logins == vec!["us100".to_string(), "us777".to_string()].
    - Test 3 (extend the existing "old config without new TTL fields/role_mapping"
      backward-compat test near line 340-360, if present): confirm a config predating
      this field still parses with admin_logins defaulting to empty (forward/backward
      compat, matches role_mapping's own precedent).
  </behavior>
  <action>
    Add a new field `admin_logins: Vec&lt;String&gt;` with `#[serde(default)]` to
    `AdConfig`, placed immediately after the existing `role_mapping` field (struct
    order only — TOML key order does not matter). Doc comment states: TOML key
    `admin_logins`, list of sAMAccountName-style domain logins (case-insensitive
    matching happens in `trackly-app::services::auth::is_admin_login`, Phase 32,
    SSO-02), empty/absent = feature off (D-03), no secrets (safe to Debug-print
    unredacted, unlike `bind_password`). Add `admin_logins: Vec::new(),` to the
    `Default for AdConfig` impl in the same relative position. Add a
    `.field("admin_logins", &self.admin_logins)` call to the manual
    `impl std::fmt::Debug for AdConfig`, right after the `role_mapping` field line —
    do NOT redact (no secrets, unlike `bind_password`'s `&"***"`). Add the new test
    function inside the existing `#[cfg(test)] mod tests` block, following the exact
    TOML-string-building style of `role_mapping_array_of_tables_deserializes`
    (lines 397-419) but using the flat array syntax `admin_logins = ["us100",
    "us777"]` (no `[[ad.admin_logins]]` table-array — this is a `Vec&lt;String&gt;`,
    not `Vec&lt;RoleMappingEntry&gt;`).
  </action>
  <verify>
    <automated>cargo test -p trackly-infra config:: 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-infra config::` passes, including the new
      admin_logins_flat_array_deserializes_and_defaults_empty test.
    - `grep -n "pub admin_logins: Vec<String>" crates/trackly-infra/src/config.rs`
      returns exactly one match inside AdConfig.
    - `grep -c "admin_logins" crates/trackly-infra/src/config.rs` is >= 4 (field decl,
      Default entry, Debug field entry, at least one test reference).
    - `cargo clippy -p trackly-infra -- -D warnings` is clean (no new warnings
      introduced by this field).
  </acceptance_criteria>
  <done>
    AdConfig.admin_logins: Vec&lt;String&gt; exists, defaults to empty, round-trips
    through TOML parsing, is printed unredacted by the manual Debug impl, and has a
    passing parsing test proving both the empty-default case and the populated case.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document admin_logins in trackly.config.toml.example</name>
  <files>trackly.config.toml.example</files>
  <read_first>
    Read trackly.config.toml.example lines 41-79 in full: the [ad] section's
    role_mapping block (lines 69-79) is the attribution/tone analog; the bind_dn/
    bind_password block (lines 52-60) is the "security-significant, explicit warning"
    tone analog. See 32-PATTERNS.md's "trackly.config.toml.example (config/docs)"
    section for the exact comment block to add.
  </read_first>
  <action>
    Add a new commented-out block immediately after the existing [[ad.role_mapping]]
    example entries (after line 79), in the same [ad] section, documenting
    admin_logins. Content must convey, in Russian (matches the file's existing
    language): (1) Phase 32 / SSO-02 attribution; (2) matching is by sAMAccountName,
    case-insensitive, UPN (user@domain) / NetBIOS (DOMAIN\user) forms all normalize to
    the same bare login (give a concrete example: us100, US100@example.local,
    EXAMPLE\us100 all match a config entry us100); (3) explicit security warning — a
    login in this list gets role admin AND immediate activation on EVERY AD/SSO login,
    bypassing ad_auto_accept, pending-registration, AND manual block/soft-delete
    (D-07) — whoever can edit this file can create an administrator, treat write
    access to this file with the same care as access to replace the .exe; (4)
    empty/absent = feature fully off (default); (5) requires a process restart to take
    effect, no live reload (Pitfall 3, matches every other [ad] TOML-only setting).
    End with a commented-out example line: `# admin_logins = ["us100", "us777"]`.
  </action>
  <verify>
    <automated>grep -n "admin_logins" trackly.config.toml.example</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "admin_logins" trackly.config.toml.example` >= 1.
    - The new block appears in the `[ad]` section, after the `role_mapping` example
      entries, before any unrelated section header.
    - The block mentions both "перезапуск" (or "restart") and the override-priority
      warning (bypasses ad_auto_accept / pending / manual block).
  </acceptance_criteria>
  <done>
    trackly.config.toml.example documents admin_logins with the security warning and
    restart-required note, immediately after the role_mapping block.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem -> AppConfig | `trackly.config.toml` (gitignored, deployment-time) is read by `AppConfig::load_or_default`; whoever has filesystem write access to this file controls its contents — this phase does not add a new boundary, it adds a new field to an existing one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Elevation of Privilege | `trackly.config.toml` -> `AdConfig.admin_logins` | accept | This is an ACCEPTED, intentional trust boundary (D-01/D-02): whoever can edit the config file already has broader filesystem control than the app can defend against — same trust level as replacing the .exe. Documented explicitly in this plan's Task 2 (config example comment), matching the existing `bind_password` warning tone. Not defended against in-app; this plan does NOT add UI or runtime editing of this field (D-01/D-03). |
| T-32-02 | Information Disclosure | `AdConfig` Debug impl | accept | `admin_logins` contains only login names (no secrets, unlike `bind_password`) — safe to print unredacted in logs/diagnostics, same classification as `role_mapping`'s `group_dn` strings. |

</threat_model>

<verification>
- `cargo test -p trackly-infra config::` green (includes the new admin_logins test).
- `cargo clippy -p trackly-infra -- -D warnings` clean.
- `grep -c "admin_logins" crates/trackly-infra/src/config.rs` >= 4.
- `grep -c "admin_logins" trackly.config.toml.example` >= 1.
</verification>

<success_criteria>
- `AdConfig.admin_logins: Vec<String>` field exists, `#[serde(default)]`, defaults to
  an empty vector, is Debug-printed unredacted, and is covered by a passing parsing
  test for both the empty and populated cases.
- `trackly.config.toml.example` documents the field with the security warning and
  restart-required note, ready for Plan 02 to consume via
  `config.ad.admin_logins.clone()`.
</success_criteria>

<output>
Create `.planning/phases/32-sso-main/32-01-SUMMARY.md` when done
</output>
