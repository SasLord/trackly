---
phase: 32-sso-main
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - crates/trackly-app/tests/ad_admin_logins.rs
autonomous: true
requirements: [SSO-02]

must_haves:
  truths:
    - "Unknown login in admin_logins becomes an active Administrator on first login, with NO ad_register request created (SSO-02 Success Criteria #2, D-04)."
    - "A pending user's login added to admin_logins is activated as admin AND their open ad_register request is auto-completed in the SAME transaction, with an audit_log row capturing prior_state='pending' (Pitfall 2, ASVS V9)."
    - "A blocked/soft-deleted user's login in admin_logins is revived as active admin, overriding the manual block (D-07)."
    - "An existing active non-admin user's login in admin_logins is escalated to admin on next login (D-06)."
    - "A login already active admin is idempotent on repeat logins — version is NOT bumped."
    - "A login NOT in admin_logins is completely unaffected — Phase 31 behavior (SSO-01/SSO-03) is fully unchanged (SSO-02 Success Criteria #3, D-08)."
    - "admin_logins forces admin even when AdDirectory::resolve returns Unreachable — the check does not depend on directory reachability (D-10)."
  artifacts:
    - path: crates/trackly-app/tests/ad_admin_logins.rs
      provides: "Full state-matrix integration coverage for SSO-02 (unknown/pending/blocked/active-escalation/idempotent/not-in-list/directory-unreachable)"
      contains: "admin_logins_unknown_user_becomes_active_admin_no_pending_request"
  key_links:
    - from: "ad_admin_logins.rs"
      to: "AuthService::sso_login"
      via: "#[tokio::test] async fn ... calling svc.sso_login(login, display_name)"
      pattern: "sso_login"
    - from: "ad_admin_logins.rs"
      to: "AuthService::login (LDAPS path)"
      via: "one regression test proving try_ad_login also gets forced-admin treatment (shared on_ad_bind_success injection)"
      pattern: "svc.login(LoginRequest"
---

<objective>
Write the full integration test file proving every branch of Plan 02's forced-admin
state machine behaves correctly: the 5-state matrix (unknown/pending/blocked/
active-escalation/idempotent-no-op), the not-in-list regression (Phase 31 unchanged),
and the directory-unreachable case (D-10 independence from `AdDirectory`). This is the
automated behavioral verification that Plan 02 deferred.

Purpose: SSO-02 is a security-significant privilege-escalation path (D-07) — every
state transition it can produce must be proven, not assumed. This file IS the
evidence the phase's threat-model mitigations (audit_log, dangling-request closure,
directory-independence) actually hold.
Output: `crates/trackly-app/tests/ad_admin_logins.rs`, a new integration test file
covering all Wave-0 gaps identified in 32-VALIDATION.md's Per-Task Verification Map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-sso-main/32-CONTEXT.md
@.planning/phases/32-sso-main/32-RESEARCH.md
@.planning/phases/32-sso-main/32-PATTERNS.md
@.planning/phases/32-sso-main/32-VALIDATION.md
</context>

<interfaces>
Existing helper/fixture patterns to copy directly (do NOT invent new fixtures or a
new normalization algorithm — reuse these exactly):

From `crates/trackly-app/tests/ad_directory_sso.rs` (lines 1-64) — independent
`AuthService` construction helper taking explicit `ad_client`/`directory`:

```rust
fn make_auth_service_with_directory(
    ad_client: Arc<dyn trackly_core::ports::ad::AdClient + Send + Sync>,
    directory: Arc<dyn AdDirectory + Send + Sync>,
) -> (AuthService, tempfile::TempDir) {
    let (writer, readers, dir) = test_writer_and_readers();
    let clock: Arc<dyn Clock + Send + Sync> = Arc::new(SystemClock);
    let (ws_tx, _) = tokio::sync::broadcast::channel(128);
    let svc = AuthService::new(writer, readers, clock, ad_client, Arc::new(ws_tx), directory);
    (svc, dir)
}
```
This plan's helper adds one line: `.with_admin_logins(admin_logins)` on the
constructed `svc` before returning it.

From `crates/trackly-infra/src/ad/directory_mock.rs` (lines 34-67) — reuse these
fixtures as-is, do not invent new identities:
- `MockAdDirectory::default_fixtures()` — `us100` -> `Иванов Иван Иванович`,
  `role: Some(Role::Manager)`; `us200` -> `Петрова Анна Сергеевна`, `role: None`.
- `MockAdDirectory::unreachable()` — every `resolve()` call returns
  `Err(DirectoryError::Unreachable)`.

From `crates/trackly-app/tests/ad_register.rs` (lines 75-104) — direct-seed helper for
a blocked/soft-deleted user row (bypasses `create_user`, which requires a password):

```rust
async fn seed_blocked_ad_user(svc: &AuthService, login: &str, full_name: &str, deleted: bool) -> i64 {
    // INSERT INTO users (login, full_name, password_hash, role, ad_user, is_active,
    //   deleted_at_utc, created_at_utc, updated_at_utc, version)
    //   VALUES (?1, ?2, NULL, 'employee', 1, 0, ?3, ?4, ?4, 1)
    // params: login, full_name, (if deleted { Some(now) } else { None }), now
}
```
Use `deleted=false` for a plain "blocked" (`is_active=0`, `deleted_at_utc=NULL`, NO
open request) row, `deleted=true` for a soft-deleted row.

From `crates/trackly-app/tests/ad_auth.rs` (lines 54-70) — direct-seed helper for an
ACTIVE user row with an explicit role (use `role="employee"` for the
active-non-admin-escalation test, `role="admin"` for the idempotent-no-op test):

```rust
async fn seed_ad_user(svc: &AuthService, login: &str, full_name: &str, role: &str) {
    // INSERT INTO users (login, full_name, password_hash, role, ad_user, is_active,
    //   created_at_utc, updated_at_utc, version)
    //   VALUES (?1, ?2, NULL, ?3, 1, 1, ?4, ?4, 1)
}
```

From `crates/trackly-app/tests/ad_register.rs` (lines 183-199) — the exact call shape
that naturally creates a "pending" user (auto-accept OFF by default, unknown login):

```rust
let request_id = match svc.login(LoginRequest {
    login: "us100".to_string(), password: "Passw0rd!".to_string(), remember: false,
}).await {
    Err(AppError::RegistrationPending { request_id }) => request_id,
    other => panic!("expected RegistrationPending, got {other:?}"),
};
```
This is the RECOMMENDED way to build the "pending" fixture for this plan's test 2
(reuses real business logic instead of hand-rolling the `requests` row) — requires
`MockAdClient::default_fixtures()` as the `ad_client` (has `us100`/password combo) and
`svc.set_ad_enabled(true, &admin_caller())` first, matching `ad_register.rs`'s own
setup.

`AuthService.readers` field is `pub` — read `version`/`role`/`is_active` directly via
`tokio::task::spawn_blocking` + `readers.acquire()` for post-condition assertions
(see `ad_register.rs` lines ~210-225 for the exact read-back style).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test file scaffold + unknown/pending/blocked/active-escalation branches</name>
  <files>crates/trackly-app/tests/ad_admin_logins.rs</files>
  <read_first>
    Read crates/trackly-app/tests/ad_directory_sso.rs in full (header doc-comment
    style, helper functions, test naming/assertion conventions). Read
    crates/trackly-app/tests/ad_register.rs lines 1-104 and 178-290 (seed helpers +
    the pending-state test pattern). Read crates/trackly-app/tests/ad_auth.rs lines
    40-70 (seed_ad_user helper + mock_default/mock_unreachable). Read
    crates/trackly-infra/src/ad/directory_mock.rs lines 1-80 (fixture identities).
    See 32-PATTERNS.md's "crates/trackly-app/tests/ad_admin_logins.rs (new, test/
    integration)" section for the exact helper/header conventions to copy.
  </read_first>
  <behavior>
    - `admin_logins_unknown_user_becomes_active_admin_no_pending_request`: fresh DB,
      `admin_logins=["us100"]`, `svc.sso_login("us100", "us100").await` succeeds
      (not `RegistrationPending`), returned `UserDto.role == "admin"`,
      `UserDto.is_active == true`; assert NO row exists in `requests` for this user
      (`request_type='ad_register'`) — proves the bypass path creates no info
      request.
    - `admin_logins_pending_user_activated_and_request_completed`: seed a pending
      user via the real `svc.login(...)` auto-accept-OFF flow (using
      `MockAdClient::default_fixtures()` + `set_ad_enabled(true)`), confirm
      `RegistrationPending { request_id }` is returned; THEN construct a SECOND
      `AuthService` sharing the same writer/readers with
      `admin_logins=["us100"]` and call `sso_login("us100", ...)`; assert the
      returned `UserDto.role == "admin"` and `is_active == true`; read back the
      original `request_id`'s `status` column — assert it is now `'completed'`;
      read back `audit_log` for this user — assert a row with
      `action='ad_auto_admin'` exists.
    - `admin_logins_blocked_user_revived_as_admin`: `seed_blocked_ad_user(&svc,
      "us100", "...", deleted=false)`, THEN construct/reuse `AuthService` with
      `admin_logins=["us100"]`, call `sso_login`; assert `role=="admin"`,
      `is_active==true`.
    - `admin_logins_soft_deleted_user_revived_as_admin`: same as above but
      `deleted=true` — assert `role=="admin"`, `is_active==true`, and (read back)
      `deleted_at_utc IS NULL`.
    - `admin_logins_active_non_admin_escalated_to_admin`: `seed_ad_user(&svc,
      "us100", "...", "employee")`, THEN `admin_logins=["us100"]`, call
      `sso_login`; assert `role=="admin"`.
  </behavior>
  <action>
    Create `crates/trackly-app/tests/ad_admin_logins.rs` with a header doc-comment
    (mirroring `ad_directory_sso.rs`'s style: Phase 32/SSO-02 attribution, "no real
    org data" privacy note, reuse of existing placeholder identities `us100`/`us200`
    only — no new invented names). Add a helper
    `make_auth_service_with_admin_logins(admin_logins: Vec&lt;String&gt;, directory:
    Arc&lt;dyn AdDirectory + Send + Sync&gt;) -> (AuthService, tempfile::TempDir)`
    that builds the service exactly like `ad_directory_sso.rs`'s
    `make_auth_service_with_directory` but chains
    `.with_admin_logins(admin_logins)` before returning. Add a second helper
    variant/overload (or a `writer`/`readers`-sharing constructor, mirroring
    `make_request_service_sharing` in `ad_register.rs`) that builds a SECOND
    `AuthService` on the SAME writer/readers pair as an already-constructed one — needed
    for the pending-user test, which must first create the pending state via one
    `AuthService` (default, empty `admin_logins`) and then re-login via a second
    `AuthService` instance configured with `admin_logins=["us100"]` sharing the same
    DB. Write the 5 test functions described in `<behavior>` above, using
    `MockAdDirectory::default_fixtures()` unless a test specifically needs
    `MockAdDirectory::unreachable()` (not needed in this task — that's Task 2). Use
    `seed_blocked_ad_user`/`seed_ad_user`-equivalent local helpers (independent
    copies in this file, per the established "small independent fixtures"
    convention — do not import from `ad_register.rs`/`ad_auth.rs`).
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test ad_admin_logins 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test ad_admin_logins` passes with all 5 tests from
      this task green.
    - `admin_logins_unknown_user_becomes_active_admin_no_pending_request` explicitly
      asserts zero rows in `requests` for the promoted user.
    - `admin_logins_pending_user_activated_and_request_completed` explicitly asserts
      both the request's `status='completed'` AND an `audit_log` row with
      `action='ad_auto_admin'` exists.
    - `admin_logins_soft_deleted_user_revived_as_admin` explicitly asserts
      `deleted_at_utc IS NULL` after revival.
  </acceptance_criteria>
  <done>
    4 of the 5 pre-existing user states (unknown, pending, blocked, soft-deleted,
    active-non-admin — 5 tests covering these) are proven correct by passing
    integration tests, including the mandatory audit_log and dangling-request-closure
    assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotency, not-in-list regression, directory-unreachable, and case-matching tests</name>
  <files>crates/trackly-app/tests/ad_admin_logins.rs</files>
  <read_first>
    Read the file you just created in Task 1 in full (helpers + 5 tests already
    written) before adding to it. Read
    crates/trackly-app/tests/ad_directory_sso.rs's fail-closed test (the one using
    `MockAdDirectory::unreachable()` / `DirectoryError::Unreachable`) for the
    unreachable-fixture assertion style to mirror for this task's D-10 test.
  </read_first>
  <behavior>
    - `admin_logins_already_admin_is_idempotent_noop`: `seed_ad_user(&svc, "us100",
      "...", "admin")`, read back `version` (expect `1`), call `sso_login` TWICE with
      `admin_logins=["us100"]`, read back `version` again after both calls — assert
      it is STILL `1` (no bump on either login).
    - `admin_logins_not_in_list_phase31_behavior_unchanged`: `admin_logins=["us100"]`
      (does NOT contain `us200`), call `sso_login("us200", "us200")` against a fresh
      DB with `MockAdDirectory::default_fixtures()` — assert the result matches
      pre-Phase-32 behavior unchanged (either `RegistrationPending` under default
      auto-accept-off, or an active `employee`-role user under auto-accept-on,
      whichever this codebase's existing `ad_directory_sso.rs`/`ad_register.rs`
      tests already assert for `us200` — match that exact expectation, do not invent
      a new one).
    - `admin_logins_forces_admin_when_directory_unreachable`: `admin_logins=["us100"]`
      + `MockAdDirectory::unreachable()` (NOT `default_fixtures()`), call
      `sso_login("us100", "us100")` — assert it STILL succeeds with `role=="admin"`,
      `is_active==true`, proving the admin_logins check runs independently of
      `directory.resolve`'s outcome (D-10).
    - `admin_logins_forces_admin_on_ldaps_password_bind_path_too`: build the service
      with `MockAdClient::default_fixtures()` (has `us100`) and
      `admin_logins=["us100"]`, call `svc.login(LoginRequest{login:"us100",
      password:"Passw0rd!", remember:false})` (the LDAPS `try_ad_login` path, NOT
      `sso_login`) — assert `role=="admin"`, proving the shared
      `on_ad_bind_success` injection point covers BOTH entry points, not just SSO
      (locked design decision from Plan 02).
  </behavior>
  <action>
    Add the 4 test functions described in `<behavior>` to
    `crates/trackly-app/tests/ad_admin_logins.rs`, reusing the Task 1 helpers. For
    the not-in-list regression test, first run `cargo test -p trackly-app --test
    ad_directory_sso` and/or `--test ad_register` to confirm the CURRENT expected
    behavior for `us200`/unknown users under default settings, then assert the exact
    same outcome in this new test (do not guess). After adding all tests, run the
    full workspace test suite and lint gates to confirm nothing in Plan 02's change
    regressed any EXISTING Phase 31 test file.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test ad_admin_logins 2>&1 | tail -60 && cargo test -p trackly-app --test ad_directory_sso --test ad_register --test ad_auth 2>&1 | tail -60</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test ad_admin_logins` passes with all 9 tests in
      the file green (5 from Task 1 + 4 from this task).
    - `cargo test -p trackly-app --test ad_directory_sso --test ad_register --test
      ad_auth` — ALL pre-existing Phase 31/Phase 9 tests still pass unmodified
      (regression proof for D-08: logins outside `admin_logins` are unaffected).
    - `cargo clippy -p trackly-app --tests -- -D warnings` is clean on the new test
      file.
    - `admin_logins_forces_admin_when_directory_unreachable` explicitly uses
      `MockAdDirectory::unreachable()`, not `default_fixtures()`.
  </acceptance_criteria>
  <done>
    The full SSO-02 state matrix (9 tests total) is green, Phase 31's existing test
    suite is proven unaffected, and D-10 (directory-independence) plus dual-entry-point
    coverage (SSO + LDAPS) are both explicitly proven.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test doubles -> real state machine | `MockAdDirectory`/`MockAdClient` simulate AD without a live domain controller (standing dev-environment constraint — no AD reachable from macOS); the state machine under test is the REAL `AuthService` code from Plan 02, not a mock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | Repudiation (verification gap) | forced-admin audit trail | mitigate | This plan is itself the mitigation: `admin_logins_pending_user_activated_and_request_completed` explicitly asserts the `audit_log` row exists — an untested audit path is equivalent to no audit path for a security-significant feature (D-07). |
| T-32-09 | Elevation of Privilege (regression risk) | Phase 31 provisioning paths for non-listed logins | mitigate | `admin_logins_not_in_list_phase31_behavior_unchanged` plus re-running the FULL existing `ad_directory_sso`/`ad_register`/`ad_auth` suites proves Plan 02's new top-of-function branch does not silently widen access for logins outside the list (D-08 is a security property, not just a functional one — an accidental widening here would be an unintended elevation of privilege for every AD user, not just listed ones). |
| T-32-10 | Denial of Service (availability regression risk) | admin_logins vs directory reachability | mitigate | `admin_logins_forces_admin_when_directory_unreachable` proves the feature keeps working (fails OPEN for admin_logins specifically, by design, D-10) even when the OPTIONAL directory-enrichment feature is down — without this test, a future refactor could accidentally couple `is_admin_login` to `directory.resolve` and silently break the "first administrator" bootstrap path exactly when an org needs it most (DC down). |

</threat_model>

<verification>
- `cargo test -p trackly-app --test ad_admin_logins` — all 9 tests green.
- `cargo test -p trackly-app --test ad_directory_sso --test ad_register --test
  ad_auth` — zero regressions in existing Phase 31/Phase 9 suites.
- `cargo clippy -p trackly-app --tests -- -D warnings` clean.
</verification>

<success_criteria>
- Every row in 32-VALIDATION.md's Per-Task Verification Map for SSO-02 (unknown/
  pending/blocked/active-escalation/idempotent/not-in-list/unreachable) has a
  corresponding passing automated test.
- Both `sso_login` and `try_ad_login` entry points are proven to receive identical
  forced-admin treatment.
- Zero regressions in Phase 31's existing test suite.
</success_criteria>

<output>
Create `.planning/phases/32-sso-main/32-03-SUMMARY.md` when done
</output>
