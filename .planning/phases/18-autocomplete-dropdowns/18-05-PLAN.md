---
phase: 18-autocomplete-dropdowns
plan: 05
type: execute
wave: 3
depends_on: ["18-04"]
files_modified:
  - ui/src/features/acts/ActFormItemsTable.svelte
autonomous: false
requirements: [AUTO-04, AUTO-05]

must_haves:
  truths:
    - "Клик по раскрываемой группе (есть серийные/инвентарные экземпляры ИЛИ смешанный condition) заменяет список её экземплярами с кнопкой «← Назад»"
    - "Серийные/инвентарные экземпляры внутри раскрытой группы отображаются отдельными строками (qty=1 жёстко); несерийные/безынвентарные — подгруппированы по состоянию с ×count и вводом количества"
    - "Группа, состоящая только из несерийных/безынвентарных устройств с одним condition, НЕ раскрывается — клик сразу выбирает (текущая clone-семантика, D-08)"
    - "Если после фильтрации остаётся ровно одна группа — она не отображается как строка группы: сразу показывается плоский список её экземпляров (без кнопки «← Назад»)"
    - "Изменение текста фильтра сбрасывает view-mode строки обратно к списку групп"
  artifacts:
    - path: ui/src/features/acts/ActFormItemsTable.svelte
      provides: "Drill-in навигация (D-06/D-07) + single-group auto-flatten (D-09) поверх Plan 18-04 group-list"
  key_links:
    - from: "ui/src/features/acts/ActFormItemsTable.svelte"
      to: "devices.listByIds (существующий, без backend-изменений)"
      via: "devices.listByIds(g.ids) при клике на раскрываемую группу"
      pattern: "listByIds\\("
---

<objective>
Завершить пикер устройства в форме акта: drill-in по клику на раскрываемую группу
(AUTO-04, D-06/D-07) — используя уже готовый `devices.listByIds(ids)` (без backend-
изменений) для получения серийных/инвентарных/state-атрибутов каждого экземпляра — и
автоматическое схлопывание единственной оставшейся группы в плоский список (AUTO-05,
D-09). Финальный чекпоинт проверяет всю цепочку AUTO-01..05 целиком, включая
позиционирование дропдауна из Plan 18-02/18-03/18-04.

Purpose: Plan 18-04 доставил рабочий список групп (portal, focus-open, фильтр,
name+model+count рендер), но клик по группе с серийными/инвентарными устройствами до сих
пор ведёт себя как clone-выбор — не показывает конкретные экземпляры. Это последний шаг,
делающий AUTO-04/AUTO-05 полностью функциональными.

Output: `ActFormItemsTable.svelte` — полный пикер устройства; ручное подтверждение всей
фичи (AUTO-01..05) в конце.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-autocomplete-dropdowns/18-CONTEXT.md
@.planning/phases/18-autocomplete-dropdowns/18-UI-SPEC.md
@.planning/phases/18-autocomplete-dropdowns/18-PATTERNS.md

<interfaces>
`devices.listByIds(ids: number[]): Promise<DeviceDto[]>` (`ui/src/lib/api/devices.ts:53`,
Tauri command `devices_list_by_ids` / HTTP — уже гейтован RBAC, без изменений в этой
фазе). Возвращает полные `DeviceDto` (id, name, inventory_no, serial_no, model, state,
...) для каждого id в группе — источник per-instance деталей для drill-in, БЕЗ
backend-изменений (per 18-PATTERNS.md "Important existing capability").

`DeviceGroup { repr: DeviceDto; count: number; ids: number[]; condition_distinct_count: number }`
(`ui/src/bindings.ts:1549`) — результат Plan 18-01's `list_grouped` (group_by_condition=true):
верхний уровень группировки name+model, `condition_distinct_count` сигнализирует
неоднородность condition внутри группы (использовать как один из признаков
"раскрываемости", НЕ как SQL group key).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drill-in навигация — раскрытие группы, per-instance рендер (D-06/D-07)</name>
  <files>ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    ui/src/features/acts/ActFormItemsTable.svelte (текущее состояние после Plan 18-04 —
    `rowInputEls`/`rowDropdownEls`/`activeIndexByRow`/`fetchGroups`/`pickGroup`/
    `getSelectedIds` уже на месте); .planning/phases/18-autocomplete-dropdowns/18-UI-SPEC.md
    раздел "AUTO-04 — Группировка name+model с drill-in" → "Уровень 2 — drill-in" (строки
    197-222, точный ASCII-макет и D-07 таблица "Тип экземпляра / Отображение / Выбор") и
    "D-08 — нераскрываемая группа" (строки 195-196); .planning/phases/18-autocomplete-dropdowns/18-CONTEXT.md
    D-06/D-07/D-08 (дословная формулировка модели группы от пользователя, раздел
    &lt;specifics&gt;).
  </read_first>
  <action>
    Добавить state: `let viewModeByRow = $state<Record<number, 'groups' | 'members'>>({});`
    (default 'groups' при отсутствии ключа), `let drillGroupByRow = $state<Record<number, DeviceGroup | null>>({});`,
    `let membersByRow = $state<Record<number, import('../../bindings').DeviceDto[]>>({});`.
    Определить `function isExpandable(g: DeviceGroup): boolean { return g.condition_distinct_count > 1 || !!g.repr.serial_no || !!g.repr.inventory_no; }`
    (D-08: группа НЕ раскрывается, только если `condition_distinct_count <= 1` И у
    представителя нет ни serial_no, ни inventory_no — иначе раскрывается). Изменить
    обработчик клика по строке группы: если `!isExpandable(g)` — вызвать существующий
    `pickGroup(idx, g)` без изменений (D-08, текущая clone-семантика); если `isExpandable(g)` —
    вызвать `async function drillInto(idx, g)`: `const ids = g.ids.filter((id) => !getSelectedIds(idx).has(id));`
    (сохраняет DEF-2A и для drill-in), `membersByRow[idx] = await devices.listByIds(ids);`,
    `drillGroupByRow[idx] = g; viewModeByRow[idx] = 'members';` (дропдаун остаётся открытым,
    `openByRow[idx]` не меняется). Добавить `function backToGroups(idx) { viewModeByRow[idx] = 'groups'; drillGroupByRow[idx] = null; membersByRow[idx] = []; }`.
    В `handleQueryInput` (после удаления early-return в Plan 18-04) — в начале функции
    добавить сброс `viewModeByRow[idx] = 'groups'; drillGroupByRow[idx] = null;` (per
    UI-SPEC: изменение текста фильтра всегда возвращает к списку групп). В разметке
    `{#if openByRow[idx] && ...}` — при `viewModeByRow[idx] === 'members'` рендерить: (1)
    заголовок drill-in `<li class="drill-header"><button type="button" onclick={() => backToGroups(idx)}>← Назад</button><span>{drillGroupByRow[idx]?.repr.name} · {drillGroupByRow[idx]?.repr.model}</span></li>`
    (D-06); (2) партиционировать `membersByRow[idx]` на serial/inventory-экземпляры
    (`d.serial_no || d.inventory_no`) — рендерить каждый отдельной строкой: `SN {d.serial_no}`
    и/или `инв. {d.inventory_no}` (что заполнено) + `{d.state ?? '—'}`, клик вызывает
    выбор с `quantity: 1` (жёстко, has_serial=true семантика) и `group_ids: [d.id]`; и
    несерийные-безынвентарные — сгруппировать client-side по `d.state` (`Map<string | null, DeviceDto[]>`),
    рендерить одну строку на group: `«Без номера · {state ?? '—'}»` + `×{count}` + числовой
    инпут количества (переиспользовать `handleQtyInput`-подобную логику, cap = размеру
    ЭТОЙ под-группы, НЕ `g.count` всей группы), клик/ввод количества вызывает выбор с
    `group_ids` = id'ы именно этой под-группы. Разделитель между мета-полями строки — ` · `
    (per UI-SPEC Copywriting Contract). Сохранить `mousedown`+`preventDefault()` на всех
    кликабельных строках (существующая конвенция, предотвращает blur до onclick).
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>Клик по раскрываемой группе показывает per-instance члены (серийные/инвентарные отдельно qty=1, несерийные/безынвентарные подгруппированы по state с ×count) с кнопкой «← Назад»; D-08 нераскрываемая группа продолжает работать как прямой clone-выбор.</done>
  <acceptance_criteria>
    - Source: `isExpandable()` возвращает false только когда `condition_distinct_count<=1`
      И нет serial_no/inventory_no у representative.
    - Behavior: клик по группе с `g.repr.serial_no != null` вызывает `devices.listByIds`,
      НЕ `pickGroup` напрямую.
    - Source: серийные/инвентарные member-строки жёстко устанавливают `quantity: 1`.
    - Source: несерийные/безынвентарные member-строки группируются по `state` client-side
      (не по backend-запросу — `devices.listByIds` вызывается один раз на drill-in).
    - Build: `pnpm --dir ui run svelte-check` без новых ошибок.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Single-group auto-flatten (AUTO-05/D-09) + сброс view-mode на новый ввод</name>
  <files>ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    ui/src/features/acts/ActFormItemsTable.svelte (после Task 1 этого плана);
    .planning/phases/18-autocomplete-dropdowns/18-UI-SPEC.md раздел "AUTO-05 — Единственная
    группа → плоский список (D-09)" (строки 225-238, ASCII-макет без заголовка/кнопки
    «Назад»).
  </read_first>
  <action>
    В `fetchGroups(idx, query)` (Plan 18-04) — после присвоения
    `suggestionsByRow[idx] = filtered;` добавить проверку: если `filtered.length === 1`
    (ровно одна группа после DEF-2A-фильтрации) — НЕ показывать её как строку группы;
    вместо этого немедленно вызвать ту же логику, что `drillInto`/`pickGroup` использует
    по признаку `isExpandable(filtered[0])`: если раскрываема — выполнить эквивалент
    `drillInto(idx, filtered[0])` (устанавливает `membersByRow`/`viewModeByRow='members'`),
    но с флагом подавления заголовка (см. ниже); если НЕ раскрываема (D-08 случай с одной
    группой) — оставить как есть, рендерить её единственной строкой БЕЗ группового
    заголовка/count-badge, сразу как «плоский список из одного элемента» (fallback:
    можно переиспользовать member-рендер и для этого случая, оборачивая единственную
    группу в фиктивный `membersByRow` через `devices.listByIds(filtered[0].ids)`, чтобы
    поведение D-09 было идентичным и для раскрываемых, и для нераскрываемых групп —
    предпочтительный вариант, упрощает код: единственная группа ВСЕГДА разворачивается
    в member-список, drill-in header просто скрывается). Добавить
    `showDrillHeaderByRow: Record<number, boolean>` (default true), устанавливать `false`
    при single-group auto-flatten и `true` при обычном клике на раскрываемую группу.
    Модифицировать разметку заголовка из Task 1: рендерить `<li class="drill-header">`
    только если `showDrillHeaderByRow[idx] !== false`. Убедиться, что `backToGroups(idx)`
    также сбрасывает `showDrillHeaderByRow[idx] = true` для следующего обычного открытия.
    Подтвердить (пере-проверка Task 1's действия), что смена текста фильтра
    (`handleQueryInput`) сбрасывает `viewModeByRow`/`drillGroupByRow`/`membersByRow`/
    `showDrillHeaderByRow` к начальному состоянию — иначе после auto-flatten
    редактирование текста оставит "залипший" member-список.
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check && pnpm --dir ui run build</automated>
  </verify>
  <done>Единственная оставшаяся после фильтрации группа автоматически показывается как плоский список экземпляров без заголовка drill-in; смена текста фильтра сбрасывает view-mode строки к списку групп.</done>
  <acceptance_criteria>
    - Behavior: фильтрация, возвращающая ровно 1 группу, рендерит member-строки БЕЗ
      заголовка "← Назад" / названия группы.
    - Behavior: фильтрация, возвращающая 2+ групп, рендерит обычные строки групп (не
      member-список).
    - Source: `showDrillHeaderByRow` управляет условным рендером заголовка drill-in.
    - Regression: изменение текста в поле после auto-flatten или обычного drill-in
      возвращает view к списку групп (не остаётся на member-экране со старыми данными).
    - Build: `pnpm --dir ui run svelte-check` и `pnpm --dir ui run build` без ошибок.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Финальная сквозная проверка AUTO-01..05</name>
  <what-built>
    Полный пикер устройства в форме акта (Акты → Позиции): portal-дропдаун (AUTO-01,
    Plan 18-02/18-03/18-04), открытие по фокусу (AUTO-02), рабочая фильтрация по
    name/инв.№/SN (AUTO-03, Plan 18-01+18-04), группировка name+model с drill-in по
    экземплярам (AUTO-04, Plan 18-04+18-05 Task 1), схлопывание единственной группы
    (AUTO-05, Plan 18-05 Task 2). Также портированы на portal: LocationAutocomplete,
    PersonAutocomplete, DeviceAutocompleteField (Plan 18-02/18-03); 4 native-select
    компонента задокументированы как AUTO-01-совместимые без изменений.
  </what-built>
  <how-to-verify>
    1. Собрать frontend для LAN-режима (обязательно после фронтенд-изменений):
       `pnpm --dir ui build`.
    2. Открыть приложение (desktop или LAN-браузер), перейти Акты → Создать акт (или
       открыть существующий черновик) → секция «Позиции».
    3. Кликнуть в поле «Устройство со складе» БЕЗ ввода текста — дропдаун должен открыться
       сразу, показывая до 20 групп, отсортированных по убыванию количества на складе
       (AUTO-02/D-04).
    4. Открыть модальное окно/диалог, где рендерится дропдаун (форма акта уже в модалке
       по конвенции проекта) — убедиться, что список НЕ обрезается краем модалки и НЕ
       добавляет внутренний скролл модалке (только у самого дропдауна) (AUTO-01).
       Прокрутить содержимое модалки/окно — дропдаун должен СЛЕДОВАТЬ за полем
       (репозиция), а не закрываться (D-02).
    5. Ввести часть наименования устройства — список должен сузиться в реальном времени;
       затем очистить и ввести инвентарный № или серийный № существующего устройства —
       список должен показать соответствующее устройство/группу (AUTO-03).
     6. Найти группу с несколькими экземплярами, у которых есть серийный № или
        инвентарный № (или разное состояние) — кликнуть по ней: список должен замениться
        экземплярами с кнопкой «← Назад», серийные/инвентарные — отдельными строками
        (выбор = qty 1), несерийные/безынвентарные — строками по состоянию с ×count и
        вводом количества (AUTO-04/D-06/D-07). Нажать «← Назад» — возврат к списку групп.
     7. Найти/отфильтровать так, чтобы осталась РОВНО одна группа — убедиться, что
        группа НЕ отображается как строка группы: сразу показан плоский список
        экземпляров, БЕЗ заголовка/кнопки «← Назад» (AUTO-05/D-09).
     8. Регрессия: выбрать несерийное устройство с остатком > 1, ввести количество больше
        остатка — значение должно быть ограничено остатком (cap); ввести > 1000 — должно
        ограничиться 1000 (MAX_CLONE_QTY); выбрать устройство в одной строке позиций,
        затем убедиться, что оно/его группа больше не предлагается в дропдауне другой
        строки позиций (DEF-2A дедуп).
     9. Быстрая проверка остальных мигрированных автокомплитов (LocationAutocomplete в
        форме акта/устройства, PersonAutocomplete в полях «Кто сдал»/«Кто принял») —
        открыть их внутри модалки, убедиться, что список не обрезается.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет новых доверительных границ. `devices.listByIds` — существующий, уже RBAC-гейтованный
эндпоинт (используется без изменений сигнатуры/авторизации).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Information Disclosure (BOLA — доступ к чужим device ids через drill-in) | `devices.listByIds(g.ids)` вызов из drill-in | accept | `g.ids` получены ИЗ ТОГО ЖЕ авторизованного `list_grouped`-ответа (тот же caller, тот же RBAC-контекст) — drill-in не позволяет запросить произвольные id, только те, что уже вернул авторизованный список групп |
| T-18-12 | Tampering (регрессия clone-qty при drill-in выборе) | Member-строки (серийные qty=1, несерийные cap по под-группе) | mitigate | Acceptance criteria Task 1 явно фиксируют qty=1 для серийных и cap-по-под-группе для несерийных; финальный чекпоинт (шаг 8) вручную проверяет MAX_CLONE_QTY/stock cap/DEF-2A |
| T-18-13 | Information Disclosure / XSS | Рендер `d.serial_no`/`d.inventory_no`/`d.state`/`d.name` в member-строках | accept | Svelte-интерполяция с автоэкранированием, `{@html}` не используется |
</threat_model>

<verification>
- `pnpm --dir ui run svelte-check` и `pnpm --dir ui run build` зелёные после Task 1/2.
- Финальный `checkpoint:human-verify` подтверждает AUTO-01..05 end-to-end и отсутствие
  регрессии clone-qty/DEF-2A.
</verification>

<success_criteria>
1. Drill-in по клику на раскрываемую группу показывает реальные экземпляры (серийные —
   отдельно qty=1, несерийные — по состоянию с ×count) с кнопкой «← Назад».
2. Нераскрываемая группа (D-08) продолжает работать как прямой clone-выбор.
3. Единственная оставшаяся после фильтрации группа автоматически показывается как
   плоский список без заголовка (AUTO-05).
4. Смена текста фильтра сбрасывает view-mode к списку групп.
5. Пользователь подтверждает чекпоинт: вся цепочка AUTO-01..05 работает end-to-end, без
   регрессии clone-qty/DEF-2A.
</success_criteria>

<output>
Create `.planning/phases/18-autocomplete-dropdowns/18-05-SUMMARY.md` when done
</output>
