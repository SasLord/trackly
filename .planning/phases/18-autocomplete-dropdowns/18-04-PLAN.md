---
phase: 18-autocomplete-dropdowns
plan: 04
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - ui/src/features/acts/ActFormItemsTable.svelte
autonomous: true
requirements: [AUTO-01, AUTO-02, AUTO-03, AUTO-04]

must_haves:
  truths:
    - "Пикер устройства в форме акта раскрывает список сразу при получении фокуса (без ввода текста)"
    - "Ввод текста фильтрует список по наименованию (реалтайм); backend уже фильтрует по инв.№/SN (Plan 18-01) — фронтенд просто передаёт введённый текст"
    - "Дропдаун пикера устройства рендерится в document.body (portal), не обрезается модалкой акта"
    - "Строка группы показывает name + model + ×count badge (D-05/UI-SPEC), а не только name"
    - "Clone-qty семантика (MAX_CLONE_QTY=1000, cap по stock_available, has_serial→qty=1) и DEF-2A дедуп продолжают работать без регрессии"
  artifacts:
    - path: ui/src/features/acts/ActFormItemsTable.svelte
      provides: "Portal+anchor дропдаун на raw <input> per-row; focus-open; фильтр без early-return; D-05 group-row рендер"
  key_links:
    - from: "ui/src/features/acts/ActFormItemsTable.svelte"
      to: "ui/src/lib/utils/dropdownAnchor.ts"
      via: "use:dropdownAnchor={{ anchorEl: rowInputEls[idx] }}"
      pattern: "use:dropdownAnchor"
    - from: "ui/src/features/acts/ActFormItemsTable.svelte"
      to: "devices.listGrouped (Plan 18-01 контракт)"
      via: "group_by_condition: true, name_prefix: v.trim()"
      pattern: "listGrouped\\("
---

<objective>
Первая часть переработки пикера устройства в `ActFormItemsTable.svelte`: portal+anchor
дропдаун на уровне строки таблицы (AUTO-01), открытие по фокусу без ввода (AUTO-02),
рабочая фильтрация текстом (AUTO-03, теперь backend реально фильтрует — Plan 18-01), и
рендер группы верхнего уровня name+model+count по D-05 (AUTO-04, частично — только
уровень списка групп, БЕЗ раскрытия/drill-in). Clone-qty семантика (V015,
`MAX_CLONE_QTY=1000`, cap по `stock_available`, DEF-2A дедуп) сохраняется без изменений.

Purpose: сейчас дропдаун обрезается модалкой (`position: absolute` внутри grid-ячейки),
фильтрация не работает (`v.trim().length < 1` ранний return блокирует пустой ввод, и
backend игнорировал `name_prefix` до Plan 18-01), а строка группы не показывает model.
Drill-in (раскрытие группы по клику, AUTO-04 D-06/D-07) и схлопывание единственной
группы (AUTO-05 D-09) — предмет Plan 18-05, который продолжает работу над этим же
файлом.

Output: `ActFormItemsTable.svelte` с portal-дропдауном, focus-open, работающей
фильтрацией и обновлённым рендером строки группы — полностью автономная, проверяемая
часть; drill-in ещё не подключён (клик по группе продолжает работать как сегодня —
прямой выбор,clone-семантика).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-autocomplete-dropdowns/18-CONTEXT.md
@.planning/phases/18-autocomplete-dropdowns/18-UI-SPEC.md
@.planning/phases/18-autocomplete-dropdowns/18-PATTERNS.md

<interfaces>
`devices.listGrouped(filter: DeviceFilter, pagination: Pagination): Promise<DeviceGroup[]>`
(`ui/src/lib/api/devices.ts:48`) — контракт БЕЗ изменений полей; семантика true-ветки
изменена Plan 18-01 (группировка по name+model, сортировка count DESC, name_prefix теперь
реально фильтрует по name/inv#/SN через FTS5).

`DeviceGroup` (`ui/src/bindings.ts:1549`): `{ repr: DeviceDto; count: number; ids: number[]; condition_distinct_count: number }`.
`DeviceDto` (`ui/src/bindings.ts:1512`): содержит `id, name, inventory_no, serial_no,
model, state, ...` — все нужные поля для рендера строки группы уже присутствуют в `repr`.

`dropdownAnchor(node, { anchorEl, gap?, maxHeight? })` — Svelte use-action из Plan 18-02
(`ui/src/lib/utils/dropdownAnchor.ts`).

`portal(node, target?)` — существующий move-only action (`ui/src/lib/utils/portal.ts`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal+anchor дропдаун на raw &lt;input&gt; per-row + рендер группы name+model+count (AUTO-01, D-05)</name>
  <files>ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    ui/src/features/acts/ActFormItemsTable.svelte (полностью — уже прочитан в ходе
    планирования: `<Input>` компонент на строке 207-213 не поддерживает `bind:this` —
    Props-интерфейс `Input.svelte` не имеет ref-forwarding; `.dropdown` CSS строки
    317-332 с `top: 40px` (хардкод высоты строки, НЕ `calc(100% + 2px)` — ячейка не
    `position: relative` от инпута); `.opt`/`.opt-name`/`.opt-model`/`.opt-sn`/`.opt-inv`/
    `.opt-count`/`.opt-state` CSS строки 333-368 — текущий рендер строки опции);
    ui/src/lib/components/Input.svelte (полностью — подтверждает отсутствие ref-forwarding,
    обосновывает замену на raw `<input>` для этой конкретной колонки);
    ui/src/lib/utils/dropdownAnchor.ts, ui/src/lib/utils/portal.ts (контракты из Plan 18-02);
    .planning/phases/18-autocomplete-dropdowns/18-UI-SPEC.md раздел "AUTO-04 — Группировка
    name+model с drill-in" → "Уровень 1 — список групп" (строки 176-196) — точный визуальный
    контракт строки группы (порядок: name / model / ×count badge / chevron).
  </read_first>
  <action>
    Заменить `<Input type="text" value={row.query} ... oninput={(v) => handleQueryInput(idx, v)} />`
    (строки 207-213) на raw `<input type="text" bind:this={rowInputEls[idx]} class="device-input" class:invalid={!!errFor(idx,'device_id')} value={row.query} placeholder="Устройство со складе" autocomplete="off" aria-autocomplete="list" oninput={...} />`
    — добавить `let rowInputEls = $state<Record<number, HTMLInputElement | null>>({});`
    рядом с существующими `suggestionsByRow`/`loadingByRow`/`openByRow` (строки 64-67).
    В `<style lang="scss">` добавить `.device-input` класс, копирующий СТРОГО те же
    CSS-свойства, что `.qty-input` в этом же файле (строки 395-417: display/width/height/
    padding/background/color/border/border-radius/font-family/font-size/line-height/
    focus-visible/invalid states) — визуальная эквивалентность `Input.svelte` сохраняется
    (UI-SPEC: "дизайн-токены не меняются"). Добавить `let rowDropdownEls = $state<Record<number, HTMLUListElement | null>>({});`.
    На `<ul class="dropdown" role="listbox">` (строка 218) добавить `use:portal`,
    `use:dropdownAnchor={{ anchorEl: rowInputEls[idx] }}`, `bind:this={rowDropdownEls[idx]}`.
    В `<style>` заменить `.dropdown` блок (строки 317-332: `position: absolute; top: 40px; left: 0; right: 0; ...`)
    на `:global(.dropdown) { position: fixed; z-index: 1000; ... }` (сохранить max-height:
    240px/overflow/border/border-radius/background, заменить
    `box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08)` на `var(--shadow-elev-2)`). Обновить
    разметку внутри `{#each suggestionsByRow[idx]... as g}` (строки 219-236): заменить
    текущий условный блок (`serial_no` ИЛИ `inventory_no` ИЛИ `×count`, строки 224-230) —
    ВСЕГДА показывать `{g.repr.name}` (класс `.opt-name`, `--font-weight-medium` 500) И
    `{g.repr.model}` (класс `.opt-model`, только если non-null, `--font-size-label` 13px
    `--color-text-secondary`) на одной строке, ЗАТЕМ `×{g.count}` badge справа
    (`margin-left: auto`, `--color-accent`, `--font-weight-medium`) — per UI-SPEC ASCII-блок
    "Уровень 1". НЕ добавлять chevron/раскрытие в этой задаче (drill-in — Plan 18-05);
    клик по строке группы продолжает вызывать существующий `pickGroup(idx, g)` без
    изменений (сохраняет clone-qty семантику как есть). Строки `serial_no`/`inventory_no`/
    `state` из старого рендера (строки 224-233) — переносятся КАК ЕСТЬ, но теперь это
    просто дополнительная мета-информация под именем/моделью, а не замена им (репрезентация
    "count=1, серийное/инвентарное устройство" остаётся видна через существующие
    `opt-sn`/`opt-inv`/`opt-state` классы).
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check</automated>
  </verify>
  <done>Дропдаун пикера устройства использует use:portal + use:dropdownAnchor на raw &lt;input&gt; per-row; строка группы рендерит name+model+×count по D-05.</done>
  <acceptance_criteria>
    - Source: `rowInputEls`/`rowDropdownEls` объявлены как `$state<Record<number, ...>>`.
    - Source: `<input>` для устройства использует `bind:this={rowInputEls[idx]}`, НЕ
      компонент `<Input>`.
    - Source: `.dropdown` CSS использует `position: fixed` внутри `:global(...)`, не
      `top: 40px`.
    - Source: строка опции всегда рендерит `g.repr.name` И условно `g.repr.model` (не
      взаимоисключающе с serial/inv/count, как раньше).
    - Build: `pnpm --dir ui run svelte-check` без новых ошибок.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Focus-open (AUTO-02) + фильтрация без early-return (AUTO-03) + click-outside для портированного дропдауна</name>
  <files>ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    ui/src/features/acts/ActFormItemsTable.svelte (после Task 1 — тот же файл, продолжение
    работы); ui/src/lib/components/LocationAutocomplete.svelte строки 59-63 (`handleFocus`
    канонический паттерн) и 80-85 (ArrowDown-при-закрытом) — реплицировать 1-в-1 логику,
    адаптированную под per-row state; .planning/phases/18-autocomplete-dropdowns/18-CONTEXT.md
    раздел D-03/D-04 (focus-open + top-20 по остатку); 18-PATTERNS.md раздел
    "Focus-open dropdown (DEF-1/D-03)".
  </read_first>
  <action>
    Удалить ранний return `if (v.trim().length < 1) { suggestionsByRow[idx] = []; openByRow[idx] = false; return; }`
    (строки 92-96 в `handleQueryInput`) — пустой ввод теперь валиден: debounced fetch
    всегда выполняется (backend Plan 18-01 возвращает top-20-по-остатку при пустом
    `name_prefix`). Извлечь общую fetch-логику `handleQueryInput`'а (строки 97-127,
    `devices.listGrouped(...)` + DEF-2A фильтр + присвоение `suggestionsByRow`/`openByRow`)
    в приватную функцию `async function fetchGroups(idx: number, query: string)`,
    переиспользуемую и debounced-веткой, и focus-веткой. Добавить
    `function handleFocus(idx: number) { if (debounceTimers[idx]) clearTimeout(debounceTimers[idx]); fetchGroups(idx, items[idx].query); }`
    (delay 0, без debounce — как `LocationAutocomplete.handleFocus`) и повесить на
    `<input onfocus={() => handleFocus(idx)}>`. Добавить `activeIndexByRow: Record<number, number>`
    state (default -1) и `function handleRowKeydown(idx: number, e: KeyboardEvent)`,
    реплицирующую паттерн `LocationAutocomplete.handleKeydown` (строки 73-105): `Escape`
    закрывает (`openByRow[idx] = false`); `ArrowDown` при закрытом дропдауне вызывает
    `handleFocus(idx)`; при открытом — `ArrowDown`/`ArrowUp` двигают `activeIndexByRow[idx]`
    по кругу над `suggestionsByRow[idx]`; `Enter`/`Tab` при `activeIndexByRow[idx] >= 0`
    вызывают `pickGroup(idx, suggestionsByRow[idx][activeIndexByRow[idx]])`. Повесить
    `onkeydown={(e) => handleRowKeydown(idx, e)}` на `<input>`. Обновить/добавить
    document-level click-outside эффект: для каждой строки с `openByRow[idx]===true`,
    закрыть если клик вне И `rowInputEls[idx]`, И `rowDropdownEls[idx]` (по аналогии с
    Plan 18-02/18-03, но теперь по массиву строк, а не одному ref) — использовать
    существующий паттерн `$effect(() => { document.addEventListener('mousedown', ...); return () => ...; })`.
  </action>
  <verify>
    <automated>pnpm --dir ui run svelte-check && pnpm --dir ui run build</automated>
  </verify>
  <done>Фокус на поле открывает список без ввода (top-20 по остатку); ввод текста фильтрует реалтайм через новый backend-контракт; click-outside работает для портированного per-row дропдауна; clone-qty/DEF-2A не регрессируют.</done>
  <acceptance_criteria>
    - Behavior: `grep -c "v.trim().length < 1" ui/src/features/acts/ActFormItemsTable.svelte`
      возвращает 0 внутри `handleQueryInput` (ранний return удалён).
    - Source: `handleFocus(idx)` вызывает fetch с delay 0 (без debounce) через ту же
      `fetchGroups`, что использует debounced ввод.
    - Source: click-outside эффект проверяет и `rowInputEls[idx]`, и `rowDropdownEls[idx]`
      для каждой открытой строки.
    - Build: `pnpm --dir ui run svelte-check` и `pnpm --dir ui run build` без ошибок.
    - Regression: `MAX_CLONE_QTY`, `qtyMax()`, `handleQtyInput()`, `getSelectedIds()` (DEF-2A)
      остаются текстуально неизменными — `git diff` по этим функциям пуст.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Нет новых доверительных границ. Текстовый фильтр, вводимый в `<input>`, попадает в
`devices.listGrouped` → `devices_list_grouped` (Tauri/HTTP) → `list_grouped` — тот же
эндпоинт и та же санитизация (Plan 18-01, `build_fts_query`), фронтенд не добавляет
собственную SQL/строковую конкатенацию.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Tampering (injection через фильтр) | `ActFormItemsTable.svelte` → `devices.listGrouped` | accept | Санитизация выполняется на backend (Plan 18-01, T-18-01) — фронтенд передаёт `name_prefix` как есть, никакой SQL-логики на клиенте нет |
| T-18-09 | Information Disclosure / XSS | Рендер `g.repr.name`/`g.repr.model`/`g.repr.serial_no`/`g.repr.inventory_no`/`g.repr.state` | accept | Все поля рендерятся через Svelte-интерполяцию (`{...}`), автоэкранирование; `{@html}` не используется |
| T-18-10 | Tampering (регрессия clone-qty/дедуп) | `pickGroup`/`handleQtyInput`/`getSelectedIds` | mitigate | Acceptance criteria Task 2 требует нулевого диффа по этим функциям — регрессия V015/DEF-2A ловится на review до мержа |
</threat_model>

<verification>
- `pnpm --dir ui run svelte-check` и `pnpm --dir ui run build` зелёные.
- `git diff` по `pickGroup`/`handleQtyInput`/`getSelectedIds`/`qtyMax` пуст (clone-qty
  семантика не тронута).
- Ручная проверка (неблокирующая, часть финального чекпоинта Plan 18-05): фокус на поле
  устройства в форме акта раскрывает список без ввода текста; ввод текста фильтрует
  реалтайм.
</verification>

<success_criteria>
1. Дропдаун пикера устройства — portal + fixed-позиционирование, не обрезается модалкой
   акта.
2. Фокус на поле открывает список (top-20 по остатку, backend Plan 18-01); ввод текста
   фильтрует по name/inv#/SN в реальном времени.
3. Строка группы показывает name + model + ×count; clone-qty/DEF-2A не регрессируют.
</success_criteria>

<output>
Create `.planning/phases/18-autocomplete-dropdowns/18-04-SUMMARY.md` when done
</output>
