---
phase: 19-acts-date-edit
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/tests/acts_update.rs
autonomous: true
requirements: [ACT-02]

must_haves:
  truths:
    - "Редактирование только шапки акта не меняет состояние/расположение устройств (D-05)"
    - "Добавленная позиция переводит устройство «на_складе» → «в_работе» с локацией акта, как при create (D-06)"
    - "Убранная позиция возвращает устройство к состоянию непосредственно перед последним изменением этим актом, а не к самому первому состоянию (D-06, Pitfall 2 — most-recent snapshot)"
    - "Попытка убрать/заменить устройство, уже возвращённое по return-акту, отклоняется на сервере с Conflict/409 ДО любой мутации — не только через задизейбленную кнопку в UI (D-08)"
    - "Попытка отредактировать return-акт отклоняется на сервере (D-07)"
    - "Устаревший expected_version при сохранении возвращает OptimisticLockMismatch (→ 409)"
    - "Смена номера акта (number_override) повторно проверяет уникальность, как при create (A3)"
  artifacts:
    - path: "crates/trackly-app/src/services/act_service.rs"
      provides: "ActService::update + Self::validate_update + populate_outstanding_device_ids_in_tx"
    - path: "crates/trackly-app/tests/acts_update.rs"
      provides: "9 integration tests covering every ACT-02 backend behavior listed above"
  key_links:
    - from: "ActService::update"
      to: "SqliteActRepository::update_act_header_in_tx"
      via: "CAS UPDATE WHERE version=?expected_version"
      pattern: "update_act_header_in_tx"
    - from: "ActService::update"
      to: "SqliteAuditLogRepository::select_latest_device_mutation"
      via: "restore-on-remove (D-06)"
      pattern: "select_latest_device_mutation"
    - from: "ActService::update"
      to: "populate_outstanding_device_ids_in_tx"
      via: "D-08 guard, runs before any device mutation"
      pattern: "populate_outstanding_device_ids_in_tx"
---

<objective>
ACT-02 core: implement `ActService::update` — the method that does not exist anywhere in the stack today (confirmed by RESEARCH.md's grep: zero `fn update`/`fn patch` on `ActService`/`ActRepository`/`SqliteActRepository`). This is the actual fix for the user-reported "Редактировать не работает" symptom: the button was always wired correctly in `ActDetail.svelte`, but nothing on the backend could handle the call.

Every hard part here (optimistic-concurrency CAS, delta reconciliation for added/removed devices, "is this device still editable" guards) already has a canonical, tested sibling implementation in this exact file (`create`, `do_return`, `delete_soft`) — this plan is "extract + adapt," not "design from zero," per RESEARCH.md's own conclusion.

Purpose: closes the backend half of ACT-02 with full D-05/D-06/D-07/D-08 enforcement, all server-side (not just UI-disabled), matching the security_context requirement.
Output: `ActService::update` (+ `validate_update` + `populate_outstanding_device_ids_in_tx`), plus `crates/trackly-app/tests/acts_update.rs` with 9 integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-acts-date-edit/19-CONTEXT.md
@.planning/phases/19-acts-date-edit/19-RESEARCH.md
@.planning/phases/19-acts-date-edit/19-PATTERNS.md

<interfaces>
<!-- From Plan 19-02 — already exist, ready to call: -->
```rust
// crates/trackly-app/src/dto/act.rs
pub struct ActUpdateDto {
    pub id: i64,
    pub expected_version: i64,
    pub number_override: Option<i64>,
    pub giver_name: String,
    pub receiver_name: String,
    pub location_id: Option<i64>,
    pub location_name: Option<String>,
    pub notes: Option<String>,
    pub deadline_utc: Option<i64>,
    pub handover_date_utc: Option<i64>,
    pub items: Vec<ActUpdateItemDto>,  // FULL replacement set, not a delta
}
pub struct ActUpdateItemDto {
    pub device_id: i64,
    pub complectation_at_time: Option<String>,
}

// crates/trackly-core/src/domain/acts.rs
pub struct ActPatch {
    pub giver_name: Option<String>,
    pub receiver_name: Option<String>,
    pub location_id: Option<Option<i64>>,
    pub notes: Option<Option<String>>,
    pub deadline_utc: Option<Option<i64>>,
    pub handover_date_utc: Option<i64>,
    pub number: Option<i64>,
    pub expected_version: i64,
}

// crates/trackly-infra/src/repos/acts_sqlite.rs (SqliteActRepository)
pub fn update_act_header_in_tx(&self, tx: &Transaction<'_>, id: i64, patch: &ActPatch, now_utc: i64) -> Result<(), AppError>;

// crates/trackly-infra/src/repos/audit_log_sqlite.rs (SqliteAuditLogRepository)
pub fn select_latest_device_mutation(&self, tx: &Transaction<'_>, act_id: i64, device_id: i64) -> Result<Option<String>, AppError>;
```

<!-- create()'s per-device add-loop body to copy verbatim for `added` device_ids (act_service.rs:429-471) -->
```rust
for &dev_id in &effective_device_ids {
    let before = devices_repo.get_in_tx(&tx, dev_id)?;
    let before_json = device_snapshot_json(&before).map_err(|e| AppError::Internal {
        source_chain: format!("before_json: {e}"),
    })?;
    let after = devices_repo.update_status_and_location_in_tx(
        &tx, dev_id, in_work_status_id, resolved_location_id, now,
    )?;
    let after_json = device_snapshot_json(&after).map_err(|e| AppError::Internal {
        source_chain: format!("after_json: {e}"),
    })?;
    let payload_json = serde_json::json!({ "act_id": act_id, "kind": "handover" }).to_string();
    audit_repo.insert(&tx, AuditEntry {
        entity_type: "device", entity_id: dev_id, action: "update", user_id: user_id_opt,
        before_json: Some(before_json), after_json: Some(after_json),
        payload_json: Some(payload_json), created_at_utc: now,
    })?;
}
```
Status guard that must run BEFORE this loop for each added device (act_service.rs:348-358):
```rust
let d = devices_repo.get_in_tx(&tx, dev_id)?;
if d.status_id != on_warehouse_status_id {
    return Err(AppError::Conflict {
        reason: format!("Устройство id={} больше не на складе — обновите список и повторите.", dev_id),
    });
}
```

<!-- do_return's act_type guard, the exact shape to copy for D-07 (act_service.rs:603-608) -->
```rust
if parent.act_type != ActType::Handover {
    return Err(AppError::Validation {
        field: "act_id".into(),
        message: "Возврат можно оформить только по handover-акту".into(),
    });
}
```

<!-- populate_outstanding_device_ids — the D-08 predicate to twin as a `_in_tx` helper (act_service.rs:1846-1873) -->
```rust
fn populate_outstanding_device_ids(
    conn: &rusqlite::Connection, act_id: i64, items: &mut [ActItemDto],
) -> Result<(), AppError> {
    let mut stmt = conn.prepare(
        "SELECT device_id FROM act_items WHERE act_id = ?1 \
         EXCEPT \
         SELECT rai.device_id FROM act_items rai \
           JOIN acts ra ON ra.id = rai.act_id \
          WHERE ra.parent_act_id = ?1 AND ra.deleted_at_utc IS NULL",
    ).map_err(map_rusqlite)?;
    let outstanding: std::collections::HashSet<i64> = stmt
        .query_map(params![act_id], |r| r.get::<_, i64>(0)).map_err(map_rusqlite)?
        .collect::<rusqlite::Result<_>>().map_err(map_rusqlite)?;
    // ... mutates items in place
}
```

<!-- number-uniqueness check to re-run for A3 (act_service.rs:247-264, create) -->
```rust
let exists: bool = tx.query_row(
    "SELECT EXISTS(SELECT 1 FROM acts WHERE number=?1 LIMIT 1)",
    params![custom], |r| r.get(0),
).map_err(map_rusqlite)?;
if exists {
    return Err(AppError::Conflict { reason: format!("Акт №{custom} уже существует") });
}
```

<!-- validate_create's dedup pattern to mirror in validate_update (act_service.rs:145-183) -->
```rust
let mut seen_device_ids: std::collections::HashSet<i64> = std::collections::HashSet::new();
for (idx, it) in p.items.iter().enumerate() {
    if !seen_device_ids.insert(it.device_id) {
        return Err(AppError::Validation {
            field: format!("items[{idx}].device_ids"),
            message: format!("Устройство id={} включено в акт более одного раза", it.device_id),
        });
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActService::update core — header edit, add position, CAS, D-07 guard</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/tests/acts_update.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs (create, lines 200-509, full function — the add-loop body at 429-471 and status guard at 344-359 are copied verbatim; validate_create, lines 115-194, for validate_update's style; delete_soft, lines 1203-1327, for the writer.execute + CAS + guard shape; do_return's act_type guard, lines 603-608)
    crates/trackly-app/tests/acts_undo.rs (test_writer_and_readers(), ActService::new(writer, readers, clock), seed_devices_with_state/seed_location helper conventions — model the new test file's scaffolding on this file)
  </read_first>
  <behavior>
    - header_only_edit_does_not_touch_devices: submit an ActUpdateDto with the SAME device_id set as the act currently has, only `giver_name` changed → after update, every device's status_id/location_id is byte-for-byte unchanged from before the call; act.giver_name reflects the new value; act.version incremented by 1.
    - add_position_transitions_device: submit an ActUpdateDto whose `items` includes one MORE device_id (currently на_складе) than the act currently has → that device's status transitions to в_работе with the act's location_id; a new act_items row exists for it; an audit_log row (entity_type="device", action="update", payload_json contains "act_id" and "kind":"handover") exists for it.
    - version_mismatch_returns_conflict: submit `expected_version` one less than the act's actual current version → `ActService::update` returns `Err(AppError::OptimisticLockMismatch { entity: "act", .. })`, and no act/device row was mutated (verify by re-fetching and comparing to pre-call state).
    - reject_update_on_return_act: submit an ActUpdateDto targeting a return-type act's id → `ActService::update` returns `Err(AppError::Validation { field: "id", .. })` (or equivalent act_type-guard error) before any mutation runs.
  </behavior>
  <action>
    Add `fn validate_update(p: &ActUpdateDto) -> Result<(), AppError>` to `ActService`'s impl block, mirroring `validate_create`'s style: non-empty `giver_name`/`receiver_name` (same "Поле «Сдал»/«Принял» обязательно" messages), non-empty `items` (same "Добавьте хотя бы одну позицию" message — `items` is the FULL replacement set per the interface contract, so an edit that would leave zero positions is invalid, matching `create`'s rule), max 100 items, dedup `device_id` within `items` via the same `HashSet` pattern as `validate_create` (lines 145-183, adapted since `ActUpdateItemDto` has no `quantity`/`device_ids` sub-list — one `device_id` per item, dedup is a flat check), and if `number_override.is_some()`, the same `n < 1` bound check as `validate_create` (lines 185-192).

    Add `pub async fn update(&self, payload: ActUpdateDto) -> Result<ActDto, AppError>` immediately after `create` ends (~line 509), before `validate_return` (~line 515): call `Self::validate_update(&payload)?` first; clone `acts_repo`/`audit_repo`/`devices_repo` `Arc`s (mirrors `create`'s setup at 203-206); `let user_id_opt: Option<i64> = None;`. Inside `self.writer.execute(move |conn| { let tx = conn.transaction()...; ... })`:
    1. `let act = acts_repo.fetch_full_in_tx(&tx, payload.id)?;` — `NotFound` if `act.deleted_at_utc.is_some()` (mirrors `delete_soft` 1216-1218).
    2. D-07 guard: if `act.act_type != ActType::Handover`, return `AppError::Validation { field: "id".into(), message: "Редактировать можно только акты выдачи (handover)".into() }` — copy `do_return`'s 603-608 shape, adapted message. This runs server-side regardless of what the UI sends (the UI's "Редактировать" button is only disabled client-side for return-acts — this is the authoritative check).
    3. Redundant defense-in-depth version check BEFORE calling the repo helper: if `act.version != payload.expected_version`, return `AppError::OptimisticLockMismatch { entity: "act", id: payload.id, expected: payload.expected_version, actual: act.version }` — mirrors `delete_soft`'s 1219-1226 double-check convention (the repo helper's own `WHERE version=?` clause is the structural guarantee; this early check gives a cleaner error path before any device work starts).
    4. Resolve `in_work_status_id`/`on_warehouse_status_id` exactly as `create` does (lines 214-244).
    5. Compute `d_old: HashSet<i64>` via `SELECT device_id FROM act_items WHERE act_id = ?1` and `d_new: HashSet<i64>` from `payload.items.iter().map(|i| i.device_id)`. `let added: Vec<i64> = d_new.difference(&d_old).copied().collect();`
    6. For each `added` device_id: run the status guard (348-358 shape) then copy `create`'s add-loop body (429-471) verbatim (before/after device snapshot, `update_status_and_location_in_tx(&tx, dev_id, in_work_status_id, resolved_location_id, now)`, audit `action:"update"` with `payload_json:{"act_id":payload.id,"kind":"handover"}`), immediately followed by `INSERT INTO act_items (act_id, device_id, quantity, condition_at_time, complectation_at_time, parent_act_item_id) VALUES (?,?,1,?,?,NULL)` using the source device's `state` for `condition_at_time` and `(the matching ActUpdateItemDto.complectation_at_time, falling back to the source device's kit if None)` for `complectation_at_time` (mirrors `create`'s 401-424, simplified to canonical single-device-per-item — no clone-on-handover support for edit-added positions, per RESEARCH.md Pattern 3).
    7. For `unchanged` device_ids (`d_old ∩ d_new`) whose matching `ActUpdateItemDto.complectation_at_time` is `Some(v)`: `UPDATE act_items SET complectation_at_time = ?v WHERE act_id = ?payload.id AND device_id = ?dev_id` (D-04 комплектация edit on retained rows — see Plan 19-02's `ActUpdateItemDto` doc comment for the scope decision; `None` on an unchanged row leaves the column untouched, no UPDATE issued).
    8. LEAVE a `// TODO(Plan 19-03 Task 2): removed-device restore (D-06) + D-08 outstanding guard + number uniqueness re-check (A3) land here` comment marker at the point where `removed` device handling belongs (Task 2 of this same plan fills it in next — do not ship this plan's task boundary as two commits with a broken intermediate state if avoidable, but the marker documents intent clearly for the second task).
    9. Build an `ActPatch` from `payload`'s header fields (`giver_name`, `receiver_name`, `location_id` — resolve via `location_name` exactly like `create`'s 271-278 if provided, `notes`, `deadline_utc`, `handover_date_utc: payload.handover_date_utc`, `number: payload.number_override`, `expected_version: payload.expected_version`) and call `acts_repo.update_act_header_in_tx(&tx, payload.id, &patch, now)?` — this call's own CAS `WHERE version=?` is the authoritative lock (step 3 above is early-exit convenience only).
    10. Write a final `audit_log` row: `action: "update", entity_type: "act", entity_id: payload.id`, with `before_json`/`after_json` snapshots of the changed header fields (`giver_name`, `receiver_name`, `location_id`, `notes`, `deadline_utc`, `handover_date_utc`, `number`, `version`), mirroring `create`'s tail act_after_json shape (474-501) but with a real `before_json` this time (a diff, not a create).
    11. `tx.commit().map_err(map_rusqlite)?; Ok(payload.id)`.
    Outside the closure: `self.get(payload.id).await` (mirrors `do_return`'s tail, line 922-ish) for the fresh `ActDto`.

    Create `crates/trackly-app/tests/acts_update.rs` with the four `<behavior>` tests above, following `acts_undo.rs`'s scaffolding conventions exactly (`test_writer_and_readers()`, `ActService::new(writer, readers, clock)`, explicit device/location seed helpers via direct `writer.execute` SQL inserts where a service call isn't the simplest path).
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test acts_update` passes all 4 tests in this task.
    - `cargo clippy -p trackly-app -- -D warnings` is clean.
    - `grep -n "TODO(Plan 19-03 Task 2)" crates/trackly-app/src/services/act_service.rs` matches exactly once (marker present, to be removed by Task 2).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test acts_update</automated>
  </verify>
  <done>ActService::update exists; handles header-only edits (device-inert, D-05), adds new positions (device на_складе→в_работе, D-06 add-half), enforces CAS + D-07 server-side; removed-device restore/D-08/A3 land in Task 2 of this same plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Removed-device delta reconciliation (D-06) + D-08 outstanding guard + number uniqueness (A3)</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/tests/acts_update.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs (undo_device_mutations_for_act, lines 1707-1744 — restore-loop shape to adapt for a single device; populate_outstanding_device_ids, lines 1846-1873 — D-08 predicate to twin as `_in_tx`; create's number-uniqueness check, lines 247-264, and override-audit shape, lines 302-323)
    crates/trackly-infra/src/repos/audit_log_sqlite.rs (select_latest_device_mutation, added in Plan 19-02 Task 3 — already ORDER BY DESC LIMIT 1, no `.rev()` needed since it returns a single row)
  </read_first>
  <behavior>
    - remove_position_restores_prior_state: submit an ActUpdateDto whose `items` OMITS a device_id the act currently has (and that device is still outstanding, i.e. not yet consumed by a return) → after update, that device's status/location match its state immediately before this act's handover (the audit `before_json` snapshot); a new audit_log row exists with `action: "custom:update_remove"`, `payload_json` containing `"act_id"` (still discoverable by `select_device_mutations_for_act`'s bulk query for a later full-act delete); the `act_items` row for that device is gone.
    - double_edit_restores_most_recent_snapshot: perform THREE sequential edits on the same act — edit #1 adds device X, edit #2 removes device X, edit #3 re-adds device X then a FOURTH edit removes it again — assert the device's final restored state matches its state immediately before edit #4 (i.e. its state as of edit #3's add), NOT its original pre-handover state from before edit #1 (Pitfall 2 regression — proves the "most recent," not "first," audit row is used).
    - reject_removal_of_returned_device: create a handover act with 2 devices, issue a return for ONE of them (via `ActService::do_return`), then submit an ActUpdateDto that removes BOTH devices from `items` → `ActService::update` returns `Err(AppError::Conflict { .. })` for the already-returned device, and NEITHER device's state changed (verify no partial mutation — re-fetch both devices, confirm unchanged).
    - header_edit_free_even_with_existing_return: same fixture as above (act with a completed return on one device) — submit an ActUpdateDto whose `items` is UNCHANGED (same device_id set as currently on the act) but with a different `giver_name` → update succeeds (the D-08 guard must not fire for retained-unchanged device sets, only for actual removals).
    - number_change_rejects_duplicate: create two handover acts with distinct numbers; submit an ActUpdateDto for act A with `number_override` set to act B's number → `ActService::update` returns `Err(AppError::Conflict { .. })` with a message matching create's "Акт №{n} уже существует" pattern.
  </behavior>
  <action>
    In `ActService::update`'s writer closure (built in Task 1 of this plan), replace the `// TODO(Plan 19-03 Task 2)` marker with:
    1. Add a private free function `fn populate_outstanding_device_ids_in_tx(tx: &rusqlite::Transaction<'_>, act_id: i64) -> Result<std::collections::HashSet<i64>, AppError>` near the existing `populate_outstanding_device_ids` (act_service.rs, bottom-of-file free-function section) — same `EXCEPT` SQL as lines 1851-1858, but taking `&Transaction` instead of `&Connection` and returning the `HashSet<i64>` directly (no `items` mutation). Call it inside the writer closure, BEFORE any device mutation for `removed` devices.
    2. Compute `removed: Vec<i64> = d_old.difference(&d_new).copied().collect();`. For each id in `removed`: if it is NOT in the outstanding set returned by `populate_outstanding_device_ids_in_tx` (meaning it has already been consumed by a completed/active return), `return Err(AppError::Conflict { reason: format!("Устройство id={} уже возвращено по акту возврата — редактирование позиции невозможно", removed_id) })` IMMEDIATELY — this must be the FIRST thing checked among all `removed` devices, before any `tx.execute` mutation runs for ANY of them (validate-then-commit: if even one `removed` device fails the guard, the WHOLE update aborts with zero partial writes — matches RESEARCH.md Pattern 4's "reject the ENTIRE update" requirement).
    3. For each `removed` id that passes the guard: `audit_repo.select_latest_device_mutation(&tx, payload.id, removed_id)?` → `Some(before_json)` → parse to `serde_json::Value`, `devices_repo.restore_from_snapshot_in_tx(&tx, removed_id, &snapshot, now)?`, snapshot the restored row for `after_json`, `audit_repo.insert(&tx, AuditEntry { entity_type: "device", entity_id: removed_id, action: "custom:update_remove", user_id: user_id_opt, before_json: Some(before_json), after_json: Some(after_json), payload_json: Some(serde_json::json!({"act_id": payload.id}).to_string()), created_at_utc: now })?` (action name distinct from `"custom:undo"` per RESEARCH.md, so audit history stays legible, but `payload_json` still carries `"act_id"` so `select_device_mutations_for_act`'s bulk query — used by a LATER full-act `delete_soft` — still finds and unwinds it). `None` (should not happen for a genuinely-outstanding device — treat as `AppError::Internal { source_chain: format!("update: no audit trail for outstanding device {removed_id} on act {}", payload.id) }`). Then `tx.execute("DELETE FROM act_items WHERE act_id = ?1 AND device_id = ?2", params![payload.id, removed_id])?`.
    4. Number uniqueness (A3): if `payload.number_override.is_some_and(|n| n != act.number)`, re-run the exact uniqueness check from `create` (247-260: `SELECT EXISTS(SELECT 1 FROM acts WHERE number=?1 LIMIT 1)`) — on conflict, `AppError::Conflict { reason: format!("Акт №{n} уже существует") }` (abort before any mutation — check this alongside the D-08 removed-device guard, both are pre-mutation validations that should run before step 6/add-loop and step 3/remove-loop from Task 1/this task); on success, after `update_act_header_in_tx` commits the new number, write a `custom:act_number_override`-shaped audit row mirroring `create`'s 302-323 (`payload_json: {"requested": n, "previous": act.number}`).
    Extend `crates/trackly-app/tests/acts_update.rs` with the five `<behavior>` tests above.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test acts_update` passes all 9 tests (4 from Task 1 + 5 from this task).
    - `cargo clippy -p trackly-app -- -D warnings` is clean.
    - `grep -c "TODO(Plan 19-03" crates/trackly-app/src/services/act_service.rs` returns 0 (marker fully resolved).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test acts_update</automated>
  </verify>
  <done>ActService::update fully implements D-06 (restore removed devices to MOST RECENT prior state, not original), D-08 (server-side reject of edits touching return-bound devices, whole-transaction abort on violation), and A3 (number-change uniqueness re-check + audit); all 9 acts_update.rs tests green; no TODO markers remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ActService::update`'s caller (any future transport) → device/act mutation | This is where D-07/D-08 become load-bearing: the ONLY server-side enforcement point for "which acts are editable" and "which positions may be removed." No transport exists yet (Plan 19-04 wires Tauri/HTTP) — this plan is unreachable from outside the process, but is the authoritative gate once those transports exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | Client-supplied `expected_version` | mitigate | CAS `UPDATE ... WHERE version=?` (via `update_act_header_in_tx`) is the structural guarantee; the early in-closure check (step 3, Task 1) is defense-in-depth, not a substitute — the actual write is never a read-then-write in application code. Covered by `version_mismatch_returns_conflict`. |
| T-19-06 | Tampering / Elevation of Privilege | Client submits an `items` diff removing a device already consumed by a completed return, attempting to bypass the UI's disabled-row state | mitigate | `populate_outstanding_device_ids_in_tx` guard runs INSIDE the writer transaction, before any device mutation, and rejects the WHOLE update on violation (no partial writes) — server-side, independent of what any client (raw HTTP, forged Tauri invoke) sends. Covered by `reject_removal_of_returned_device` + `header_edit_free_even_with_existing_return` (proves the guard doesn't over-fire). |
| T-19-07 | Tampering | Client submits an update against a `return`-type act id, bypassing the UI's disabled "Редактировать" button | mitigate | `act.act_type != ActType::Handover` guard runs first, before any other work in the closure — mirrors `do_return`'s own parent-type guard. Covered by `reject_update_on_return_act`. |
| T-19-08 | Tampering (data integrity) | Client submits a `number_override` colliding with an existing act number | mitigate | Re-runs `create`'s exact uniqueness check (including soft-deleted acts per D-Soft-vs-Hard-Acts-01, inherited from the reused SQL) before committing the rename; audited via `custom:act_number_override`. Covered by `number_change_rejects_duplicate`. |

D-05 (header-only edits are device-inert) and D-06 (delta reconciliation for add/remove) are functional-correctness requirements, not security boundaries per se, but are exercised by `header_only_edit_does_not_touch_devices`, `add_position_transitions_device`, `remove_position_restores_prior_state`, and `double_edit_restores_most_recent_snapshot`.
</threat_model>

<verification>
1. `cargo test -p trackly-app --test acts_update` (all 9 tests)
2. `cargo clippy -p trackly-app -- -D warnings`
3. `cargo test --workspace` (single invocation — no concurrent `cargo test`, per project convention; confirms no regression in create/do_return/delete_soft's shared helpers)
</verification>

<success_criteria>
- ActService::update exists and is fully implemented: header edit, add position, remove position (with most-recent-snapshot restore), CAS, D-07 guard, D-08 guard, number uniqueness re-check.
- All 9 acts_update.rs integration tests pass.
- No transport exposes this method yet (Plan 19-04) — but the method itself is complete and correct in isolation.
</success_criteria>

<output>
Create `.planning/phases/19-acts-date-edit/19-03-SUMMARY.md` when done
</output>
