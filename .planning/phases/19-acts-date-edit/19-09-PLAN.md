---
phase: 19-acts-date-edit
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/acts/ActFormItemsTable.svelte
autonomous: true
requirements: [ACT-02]
gap_closure: true

must_haves:
  truths:
    - "In edit mode, retained positions show the device name as read-only text (not a blank warehouse picker)"
    - "The edit dialog has no «Комплектация» input field"
    - "Newly-added rows during an edit session still use the warehouse picker"
    - "Retained positions round-trip their existing complectation_at_time value unchanged in the update payload"
  artifacts:
    - path: "ui/src/features/acts/ActFormItemsTable.svelte"
      provides: "Read-only device name for retained edit rows; комплектация UI removed; FormItemRow.complectation_at_time field retained"
      contains: "device-readonly"
    - path: "ui/src/features/acts/ActFormBody.svelte"
      provides: "Edit payload mapping keeps complectation_at_time (unchanged this round — regression guard only)"
      contains: "complectation_at_time"
  key_links:
    - from: "ActFormItemsTable device column"
      to: "read-only device_label cell"
      via: "mode === 'edit' && row.complectation_at_time !== undefined"
      pattern: "complectation_at_time !== undefined"
---

<objective>
Close Gap 1 (UAT round 2, D-09 + D-10): make the act edit dialog visually identical to the create dialog.

- **D-10:** In edit mode, retained (prefilled) positions currently render the warehouse-picker `<input value={row.query}>`, but `itemsFromInitialAct` sets `query: ''`, so the field shows a blank "Устройство со склада" placeholder. Fix: render the device name (`row.device_label`) as READ-ONLY text for retained rows. Newly-added rows keep the normal picker.
- **D-09:** Remove the «Комплектация» UI block (input + label + SCSS + `handleComplectationInput`). KEEP `complectation_at_time` in the `FormItemRow` interface and KEEP `ActFormBody`'s edit payload mapping, so retained positions round-trip their existing комплектация value unchanged. Do NOT touch the Rust WR-03 audit backend.

Purpose: the live UAT showed retained rows appear empty and an unwanted комплектация field. This makes edit match create.
Output: edited `ActFormItemsTable.svelte`; `ActFormBody.svelte` verified unchanged (regression guard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-acts-date-edit/19-CONTEXT.md
@.planning/phases/19-acts-date-edit/19-PATTERNS.md

Project conventions: Svelte 5 runes (`$state`/`$derived`/`$props`), SCSS design tokens (`var(--color-*)`, `var(--space-*)`, `var(--font-size-*)`), Russian-only UI copy. After frontend changes, LAN-browser mode requires `pnpm --dir ui build` (memory: dev-browser-testing-needs-ui-build). No Rust changes in this plan.

<interfaces>
<!-- FormItemRow shape (ActFormItemsTable.svelte lines 19-44). KEEP the interface field. -->
FormItemRow {
  device_id: number | null;
  quantity: number;
  device_label: string;   // human-readable name (name + inv_no) — source for the read-only cell
  query: string;          // '' for retained rows (itemsFromInitialAct) — cause of the D-10 blank bug
  picked: boolean;
  has_serial?: boolean;
  stock_available?: number;
  group_ids?: number[];
  complectation_at_time?: string | null;  // set ONLY by itemsFromInitialAct on RETAINED rows; undefined on fresh rows — this is the retained-vs-new marker
}

<!-- Retained-marker semantics: itemsFromInitialAct (ActFormBody line 70-80) sets complectation_at_time
     on every prefilled row (may be null, but always DEFINED). makeEmpty()/pickGroup()/pickDevice()
     never set it, so a fresh row keeps it `undefined`. `!== undefined` reliably distinguishes retained
     positions from rows added during the edit session. -->

<!-- Read-only cell styling analog: ActItemsTable.svelte .device-name (font-weight:500) — a filled,
     non-editable text cell. Mirror its look for .device-readonly. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-only device name for retained edit rows (D-10)</name>
  <files>ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    - ui/src/features/acts/ActFormItemsTable.svelte — device column `.col-device` markup (lines 530-683); `FormItemRow` interface (19-44); `mode` prop (59-66).
    - ui/src/features/acts/ActItemsTable.svelte — `.device-name` read-only cell styling analog (lines 28-33, 91-93) for the filled non-editable look.
    - ui/src/features/acts/ActFormBody.svelte — `itemsFromInitialAct` (70-80) confirming retained rows get `complectation_at_time` set and `query: ''`.
  </read_first>
  <behavior>
    - Retained edit row (mode 'edit', complectation_at_time defined): device column renders `row.device_label` as read-only text, NOT the picker input; no dropdown/loading spinner mounts for that row.
    - Fresh edit row (complectation_at_time undefined): device column renders the existing picker input + dropdown unchanged.
    - Create mode (mode 'create'): device column renders the existing picker input unchanged (no read-only branch reached, since no row has complectation_at_time set).
    - The `device_id` field error paragraph (`errFor(idx, 'device_id')`) still renders for both branches.
  </behavior>
  <action>
    In the `.col-device` block (around lines 530-683), wrap the existing picker markup (the `<input class="device-input">`, the `{#if loadingByRow[idx]}` spinner, and the `{#if openByRow[idx]}` dropdown `<ul>`) in an `{:else}` branch of a new conditional whose `{#if}` branch is the read-only cell.

    Condition for the read-only branch: `mode === 'edit' && row.complectation_at_time !== undefined` (the retained-position marker documented on the `FormItemRow.complectation_at_time` field). In that branch render a single element `<span class="device-readonly">{row.device_label}</span>` — plain text, no input, no dropdown, no spinner.

    Keep the trailing `{#if errFor(idx, 'device_id')}` error `<p>` OUTSIDE the if/else so it applies to both branches (retained rows won't normally error, but keep parity).

    Add a `.device-readonly` SCSS rule inside the `<style>` block styled like a filled non-editable cell: same `height: 36px`, `padding: 0 var(--space-md)`, flex vertical-centering, `color: var(--color-text-primary)`, `font-family: var(--font-family-base)`, `font-size: var(--font-size-body)`, `line-height: var(--line-height-body)` — visually matching `.device-input` minus the border/background/focus (a static label). Mirror the `.qty-fixed` rule (lines ~1040-1048) for the flex/align approach.

    Do NOT change `itemsFromInitialAct` in ActFormBody (query stays `''`; the read-only cell is what closes the blank-field bug).
  </action>
  <acceptance_criteria>
    - `grep -c 'device-readonly' ui/src/features/acts/ActFormItemsTable.svelte` >= 2 (markup span + SCSS rule).
    - `grep -c "complectation_at_time !== undefined" ui/src/features/acts/ActFormItemsTable.svelte` >= 1 (retained-marker guard now gates the device cell).
    - `grep -c 'row.device_label' ui/src/features/acts/ActFormItemsTable.svelte` >= 1 (read-only cell renders the label).
    - `grep -c 'complectation_at_time' ui/src/features/acts/ActFormItemsTable.svelte` >= 1 (FormItemRow interface field still present).
    - `pnpm --dir ui exec svelte-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check</automated>
  </verify>
  <done>Retained edit rows show the device name as read-only text; fresh rows and create mode keep the picker; svelte-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove комплектация UI, keep round-trip (D-09)</name>
  <files>ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    - ui/src/features/acts/ActFormItemsTable.svelte — `handleComplectationInput` (70-75); комплектация markup block (712-729); `.col-complectation` SCSS (785-818); `FormItemRow.complectation_at_time` doc comment (39-43).
    - ui/src/features/acts/ActFormBody.svelte — edit payload mapping `complectation_at_time: it.complectation_at_time ?? null` (153-158) and `itemsFromInitialAct` (70-80): confirm these are NOT touched — комплектация must still round-trip.
  </read_first>
  <action>
    In ActFormItemsTable.svelte remove the комплектация UI entirely (D-09 = "убрать только UI"):
    - Delete the `handleComplectationInput` function (lines ~70-75).
    - Delete the комплектация markup block: the `{#if mode === 'edit' && row.picked && row.device_id !== null && row.complectation_at_time !== undefined}` … `<div class="col-complectation">` … input … closing `{/if}` (lines ~712-729), including its explanatory comment.
    - Delete the `.col-complectation { ... }` SCSS rule and its comment (lines ~785-818).

    KEEP `complectation_at_time?: string | null` in the `FormItemRow` interface (lines 39-43). Update its doc comment to note the field is now UI-hidden but retained for two reasons: (a) it round-trips the existing комплектация value unchanged through ActFormBody's edit payload, and (b) its presence marks a retained position for the read-only device cell (Task 1).

    Do NOT touch ActFormBody.svelte — its `itemsFromInitialAct` and the `complectation_at_time: it.complectation_at_time ?? null` payload mapping stay exactly as-is so retained positions round-trip. Do NOT touch any Rust / WR-03 audit code.
  </action>
  <acceptance_criteria>
    - `grep -c 'col-complectation' ui/src/features/acts/ActFormItemsTable.svelte` == 0 (UI block + SCSS removed).
    - `grep -c 'handleComplectationInput' ui/src/features/acts/ActFormItemsTable.svelte` == 0 (handler removed).
    - `grep -c 'complectation_at_time' ui/src/features/acts/ActFormItemsTable.svelte` >= 1 (interface field retained).
    - `grep -c 'complectation_at_time' ui/src/features/acts/ActFormBody.svelte` >= 2 (itemsFromInitialAct prefill + edit payload mapping both intact — round-trip preserved).
    - `pnpm --dir ui exec svelte-check` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check</automated>
  </verify>
  <done>No комплектация field renders in the edit dialog; FormItemRow.complectation_at_time and ActFormBody's payload round-trip are intact; svelte-check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → acts_update (Tauri/HTTP) | `complectation_at_time` from retained positions crosses to the server via the existing update payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09-01 | Tampering | client-supplied `complectation_at_time` on retained positions | accept | Pre-existing behavior unchanged this round: комплектация now round-trips read-only through the UI; server-side WR-03 audit path (Plan 19-07) already records the value. No new client-writable field is introduced — this plan REMOVES the editable input. |
| T-19-09-SC | Tampering | npm installs | accept | No package installs in this plan (pure `.svelte` edits). |
</threat_model>

<verification>
- `pnpm --dir ui exec svelte-check` exits 0 (0 errors).
- All grep assertions in both tasks pass.
- Manual (LAN): `pnpm --dir ui build`, open an existing handover act's edit dialog — retained rows show device names as text, no комплектация field; adding a new row shows the picker.
</verification>

<success_criteria>
Edit dialog is visually identical to create dialog: picker for new rows, read-only device name for retained rows, static "1" qty (from 19-08), no комплектация field. Retained positions still round-trip their existing комплектация value through the update payload.
</success_criteria>

<output>
Create `.planning/phases/19-acts-date-edit/19-09-SUMMARY.md` when done.
</output>
