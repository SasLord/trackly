---
phase: 19-acts-date-edit
plan: 05
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - ui/src/features/acts/ActFormBody.svelte
  - ui/src/features/acts/ActFormItemsTable.svelte
  - ui/src/features/acts/ActFormModal.svelte
  - ui/src/features/acts/ActDetail.svelte
  - ui/src/features/acts/ActsPage.svelte
autonomous: true
requirements: [ACT-02]

must_haves:
  truths:
    - "Кнопка «Редактировать» на карточке handover-акта активна (не задизейблена) и открывает форму, предзаполненную текущими данными акта"
    - "Кнопка «Редактировать» на карточке return-акта задизейблена с поясняющим тултипом (D-07, зеркалит поведение кнопки «Возврат»)"
    - "Изменение шапки акта (даты, Сдал/Принял, №, Сроком до) сохраняется и отражается в детальном просмотре после сохранения"
    - "Добавление/удаление позиции в форме редактирования сохраняется и отражается в детальном просмотре"
    - "Комплектация (per-item, ретейнед позиции) редактируема в форме; технические характеристики (devices.notes) НЕ редактируемы через эту форму (вне скоупа фазы, см. Open Question 1)"
    - "Форма редактирования предзаполняется из свежего acts.get(id), а не из строки list()/search() (Pitfall 5 — предотвращает пустой outstanding_device_ids)"
    - "Устаревшая версия акта при сохранении показывает понятный тост, а не силентную ошибку (OptimisticLockMismatch → 409)"
  artifacts:
    - path: "ui/src/features/acts/ActFormBody.svelte"
      provides: "mode='create'|'edit' + initialAct prop, prefill, submit branch to acts.update"
    - path: "ui/src/features/acts/ActFormItemsTable.svelte"
      provides: "FormItemRow.complectation_at_time + edit-mode per-row комплектация input"
    - path: "ui/src/features/acts/ActFormModal.svelte"
      provides: "mode/initialAct props, edit-mode title/button label"
    - path: "ui/src/features/acts/ActDetail.svelte"
      provides: "D-07 button gating (disabled for return-acts, enabled incl. archived handover-acts)"
    - path: "ui/src/features/acts/ActsPage.svelte"
      provides: "editModalOpen/editTargetAct/handleEdit orchestration, second edit-mode ActFormModal instance"
  key_links:
    - from: "ui/src/features/acts/ActDetail.svelte"
      to: "ui/src/features/acts/ActsPage.svelte::handleEdit"
      via: "onclick={() => onEdit?.(act)}"
      pattern: "onEdit\\?\\.\\(act\\)"
    - from: "ui/src/features/acts/ActsPage.svelte"
      to: "ui/src/lib/api/acts.ts::update"
      via: "ActFormBody's handleSubmit (mode='edit') calls acts.update(payload)"
      pattern: "acts\\.update"
    - from: "ui/src/features/acts/ActFormBody.svelte"
      to: "ActDto.version"
      via: "expected_version: initialAct.version in the ActUpdateDto payload"
      pattern: "expected_version"
---

<objective>
ACT-02 UI: wire the "Редактировать" button (already fully plumbed in `ActDetail.svelte` — `onEdit?.(act)`, `disabled={!onEdit}` — but never supplied by its caller, per RESEARCH.md's diagnosis) through to a working edit-mode form, reusing `ActFormBody`/`ActFormModal` (create-mode UI, per CONTEXT.md's discretion note recommending reuse "для консистентности"). Add D-07 gating (disable Редактировать for return-acts) and D-04's schema-consistent комплектация editing on retained position rows.

This is the LAST plan in the phase — after it, a user can actually click "Редактировать," see the current act's data, change it, and save.

Purpose: closes the frontend half of ACT-02, delivering the actual user-facing fix for "кнопка Редактировать не работает."
Output: edit-mode-aware `ActFormBody`/`ActFormItemsTable`/`ActFormModal`, D-07-gated `ActDetail`, and `ActsPage` orchestration wiring `onEdit` → edit modal → save → refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-acts-date-edit/19-CONTEXT.md
@.planning/phases/19-acts-date-edit/19-RESEARCH.md
@.planning/phases/19-acts-date-edit/19-PATTERNS.md

<interfaces>
<!-- From Plan 19-04 — already exists, ready to call: -->
```typescript
// ui/src/lib/api/acts.ts
update: (payload: ActUpdateDto) => apiCall<ActDto>('acts_update', { payload }),
```

<!-- Current ActFormBody.svelte state/props (full file — this task extends it in place, does not clone it) -->
```typescript
interface Props {
  onSaved: (_act: ActDto) => void;
  onLoading: (_l: boolean) => void;
  onCanSubmitChange: (_c: boolean) => void;
  onRegisterSubmit: (_fn: () => void) => void;
}
let numberOverride = $state<number | null>(null);
let giverName = $state('');
let receiverName = $state('');
let location = $state('');
let deadlineISO = $state('');
let handoverDateISO = $state(todayISO());
let notes = $state('');
let items = $state<FormItemRow[]>([{ device_id: null, quantity: 1, device_label: '', query: '', picked: false }]);
function isoToUnix(iso: string): number | null { /* YYYY-MM-DD -> unix seconds, UTC midnight */ }
```

<!-- Current FormItemRow (ui/src/features/acts/ActFormItemsTable.svelte:19-39) -->
```typescript
export interface FormItemRow {
  device_id: number | null;
  quantity: number;
  device_label: string;
  query: string;
  picked: boolean;
  has_serial?: boolean;
  stock_available?: number;
  group_ids?: number[];
}
```

<!-- Current ActDetail.svelte's Редактировать button (line 70) — already wired, caller just never passes onEdit -->
```svelte
<Button variant="secondary" size="sm" onclick={() => onEdit?.(act)} disabled={!onEdit}>
  Редактировать
</Button>
```
Analog for D-07 gating — the "Возврат" button's disabled-with-tooltip pattern (lines 73-81):
```svelte
{#if onReturn && act.act_type === 'handover' && !act.archived}
  <Button variant="secondary" size="sm" onclick={() => onReturn(act)}>Возврат</Button>
{:else}
  <span title={act.archived ? 'Акт уже в Архиве' : 'Возврат доступен только для handover-актов'}>
    <Button variant="secondary" size="sm" disabled>Возврат</Button>
  </span>
{/if}
```
Note: D-07 explicitly allows editing ARCHIVED handover acts (unlike Возврат) — the edit-button gate must NOT include an `!act.archived` condition.

<!-- Current ActsPage.svelte orchestration analogs to mirror (returnModalOpen/returnTargetAct/handleReturn) -->
```typescript
let returnModalOpen = $state(false);
let returnTargetAct = $state<ActDto | null>(null);
function handleReturn(act: ActDto) {
  returnTargetAct = act;
  returnModalOpen = true;
}
// <ActDetail ... onReturn={handleReturn} ... />
// <ReturnModal open={returnModalOpen} act={returnTargetAct} onClose={...} onSuccess={handleReturnSuccess} />
```
The `selectedAct` `$effect` (lines 89-112) already fetches via `acts.get(id)` — this is why `handleEdit(act)` can safely reuse the `act` argument directly (it always IS `selectedAct`, since `onEdit` is only ever invoked from `ActDetail` where `act === selectedAct`) instead of re-fetching (Pitfall 5 — `outstanding_device_ids` is only populated by `acts.get(id)`, never by `list()`/`search()` rows).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActFormBody + ActFormItemsTable — edit-mode prefill, комплектация editing, submit branch</name>
  <files>ui/src/features/acts/ActFormBody.svelte, ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    ui/src/features/acts/ActFormBody.svelte (full file — Props/state at 18-56, isoToUnix at 73-77, handleSubmit at 82-148 including the error-handling branch at 122-144)
    ui/src/features/acts/ActFormItemsTable.svelte (FormItemRow interface at 19-39, Props at 54-60, the table-row rendering section for where to add the комплектация input)
    ui/src/bindings.ts (ActUpdateDto/ActUpdateItemDto/ActDto/ActItemDto shapes, confirmed present after Plan 19-04)
  </read_first>
  <action>
    Extend `ActFormBody.svelte`'s `Props` interface with `mode: 'create' | 'edit'` and `initialAct: ActDto | null`. On mount (or via a top-level `$effect`/init block guarded by `mode === 'edit' && initialAct`), initialize state from `initialAct` instead of the create-mode defaults: `giverName = initialAct.giver_name`, `receiverName = initialAct.receiver_name`, `location = initialAct.location ?? ''`, `deadlineISO` derived from `initialAct.deadline_utc` (write the inverse of `isoToUnix` — unix seconds → `YYYY-MM-DD` string, UTC), `handoverDateISO` derived the same way from `initialAct.handover_date_utc`, `notes = initialAct.notes ?? ''`, `numberOverride = initialAct.number_raw`, and `items` built from `initialAct.items`: each existing `ActItemDto` becomes a `FormItemRow` with `device_id: it.device_id, quantity: 1, device_label: it.device_name, query: '', picked: true, group_ids: [], complectation_at_time: it.complectation_at_time` — inject these directly as `FormItemRow` state, bypassing the live `fetchGroups`/search path entirely (existing positions are `в_работе`, not `на_складе`, so a live re-search would never find them — this is the "wrinkle" RESEARCH.md flags; new rows the user adds during this edit session still go through the normal on-warehouse picker unchanged).

    Extend `FormItemRow` in `ActFormItemsTable.svelte` (lines 19-39) with an optional `complectation_at_time?: string | null` field. Add a new optional `mode?: 'create' | 'edit'` prop to `ActFormItemsTable`'s `Props` (default behaves as `'create'` if omitted, for backward compat with the create-mode caller which doesn't need to pass it). When `mode === 'edit'` AND a row is `picked === true` with a non-null `device_id` that came from the initial prefill (i.e., a RETAINED position, not one added fresh during this edit session — this distinction only matters for whether to show the комплектация input at all; track it by checking `row.complectation_at_time !== undefined`, since only prefilled rows ever have that field populated by `ActFormBody`), render an additional text input beneath that row bound to `row.complectation_at_time`, labeled "Комплектация". This is the ONLY act-header-adjacent field from D-04 with a schema-consistent per-item home (`act_items.complectation_at_time`) — per Plan 19-02's `ActUpdateItemDto` doc comment and RESEARCH.md Pitfall 4/Open Question 1: "технические характеристики" (`item.specs`, backed by live `devices.notes`) has NO input anywhere in this task — it stays read-only, out of scope for this phase (editing it would mutate device data as an act-edit side effect, a materially different security surface never analyzed for D-05/D-06). Do not add any specs/notes-editing UI.

    In `handleSubmit` (82-148), branch on `mode`: the `'create'` branch is UNCHANGED (existing `ActCreateDto`/`acts.create(payload)` path). The `'edit'` branch builds an `ActUpdateDto`: `{ id: initialAct!.id, expected_version: initialAct!.version, number_override: numberOverride, giver_name: giverName.trim(), receiver_name: receiverName.trim(), location_id: null, location_name: location.trim() || null, notes: notes.trim() || null, deadline_utc: isoToUnix(deadlineISO), handover_date_utc: isoToUnix(handoverDateISO), items: items.filter(it => it.device_id !== null).map(it => ({ device_id: it.device_id as number, complectation_at_time: it.complectation_at_time ?? null })) }` and calls `acts.update(payload)` instead of `acts.create(payload)`; success toast becomes `` `Акт №${saved.number} обновлён` ``. Reuse the existing `Validation`/`Conflict` error-handling branch (122-144) verbatim for both modes. Add a new branch before the generic fallback: `else if (err.code === 'OptimisticLockMismatch') { pushToast('error', 'Акт был изменён другим пользователем — обновите страницу и попробуйте снова.'); }`.
  </action>
  <acceptance_criteria>
    - `pnpm --dir ui exec svelte-check` reports no new type errors.
    - `grep -n "mode === 'edit'" ui/src/features/acts/ActFormBody.svelte` matches at least the state-init branch and the submit branch.
    - `grep -n "complectation_at_time" ui/src/features/acts/ActFormItemsTable.svelte` matches the extended `FormItemRow` interface and the new conditional input.
    - `grep -n "specs\|devices.notes\|технические характеристики" ui/src/features/acts/ActFormBody.svelte ui/src/features/acts/ActFormItemsTable.svelte` returns no new editable-input matches (confirms specs/notes stayed out of scope).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check</automated>
  </verify>
  <done>ActFormBody supports both create and edit modes from one component; edit-mode prefill sources from initialAct (an acts.get(id) result, per the caller contract established in Task 3); retained positions expose an editable "Комплектация" field; devices.notes/specs remains untouched.</done>
</task>

<task type="auto">
  <name>Task 2: ActFormModal edit-mode props + ActDetail D-07 button gating</name>
  <files>ui/src/features/acts/ActFormModal.svelte, ui/src/features/acts/ActDetail.svelte</files>
  <read_first>
    ui/src/features/acts/ActFormModal.svelte (full file — Props at 10-16, Modal title/footer at 34-54)
    ui/src/features/acts/ActDetail.svelte (full file — Props at 10-18, the Редактировать button at line 70, the Возврат button's disabled-with-tooltip analog at 73-81)
  </read_first>
  <action>
    Extend `ActFormModal.svelte`'s `Props` with `mode: 'create' | 'edit'` and `initialAct: ActDto | null`; pass both through to `<ActFormBody>`. Change `<Modal title="Новый акт" ...>` to a mode-aware title: `` <Modal title={mode === 'edit' ? `Редактировать акт №${initialAct?.number}` : 'Новый акт'} ...> ``. Change the footer button's loading/idle labels from the hardcoded `{#if formLoading}Создание…{:else}Создать акт{/if}` to a mode-aware version: loading → `Сохранение…` for edit / `Создание…` for create; idle → `Сохранить` for edit / `Создать акт` for create.

    In `ActDetail.svelte`, change the Редактировать button's `disabled={!onEdit}` (line 70) to `disabled={!onEdit || act.act_type !== 'handover'}`, and wrap it in the same disabled-with-tooltip structural pattern already used for "Возврат" (73-81): when `act.act_type !== 'handover'`, show a `<span title="Редактировать можно только акты выдачи (handover)">` wrapping a disabled `<Button>`; otherwise render the normal enabled button with its existing `onclick={() => onEdit?.(act)}`. Per D-07, do NOT add an `!act.archived` condition — archived handover acts remain editable (this differs from the Возврат button, which does disable on `act.archived`).
  </action>
  <acceptance_criteria>
    - `pnpm --dir ui exec svelte-check` reports no new type errors.
    - `grep -n "act.act_type !== 'handover'" ui/src/features/acts/ActDetail.svelte` matches the edit-button gate.
    - `grep -n "act.archived" ui/src/features/acts/ActDetail.svelte` — confirm this identifier does NOT appear inside the edit-button's condition (only inside the pre-existing Возврат button's condition).
    - `grep -n "mode === 'edit'" ui/src/features/acts/ActFormModal.svelte` matches both the title and button-label branches.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check</automated>
  </verify>
  <done>ActFormModal renders correctly in both create and edit mode (title + footer button label); ActDetail's Редактировать button is disabled with an explanatory tooltip for return-acts, remains enabled (including for archived acts) for handover-acts.</done>
</task>

<task type="auto">
  <name>Task 3: ActsPage orchestration — wire onEdit, edit-modal state, save/refresh</name>
  <files>ui/src/features/acts/ActsPage.svelte</files>
  <read_first>
    ui/src/features/acts/ActsPage.svelte (full file — returnModalOpen/returnTargetAct/handleReturn triple at 34-35, 134-137; createModalOpen/openCreate/handleSaved triple at 33, 119-127; the `<ActDetail>` element at 222-231; the create-mode `<ActFormModal>` instance at 236-240; the `selectedAct` `$effect` at 89-112 that fetches via `acts.get(id)`)
  </read_first>
  <action>
    Add `let editModalOpen = $state(false); let editTargetAct = $state<ActDto | null>(null);` mirroring the `returnModalOpen`/`returnTargetAct` pair (lines 34-35). Add `function handleEdit(act: ActDto) { editTargetAct = act; editModalOpen = true; }` mirroring `handleReturn` (134-137) — reuse the `act` argument directly (do NOT call `acts.get(act.id)` again here): `onEdit` is only ever invoked from `ActDetail` where `act === selectedAct`, and `selectedAct` is already guaranteed fresh via the existing `acts.get(id)` `$effect` (89-112), which satisfies Pitfall 5 (never prefill an edit form from a stale `list()`/`search()` row — only `acts.get(id)` populates `outstanding_device_ids`) without a redundant fetch.

    Pass `onEdit={handleEdit}` into the existing `<ActDetail>` element (222-231), alongside the existing `onCreate`/`onDelete`/`onReturn`/`onPrint` props. Add a SECOND `<ActFormModal>` instance (separate from the existing create-mode one at 236-240 — two modal instances is simpler than threading shared open/mode state through one, and matches how `ReturnModal` and the create-mode `ActFormModal` already coexist as separate top-level modal instances in this file): `<ActFormModal mode="edit" initialAct={editTargetAct} open={editModalOpen} onClose={() => { editModalOpen = false; editTargetAct = null; }} onSaved={handleEditSaved} />`. Add `function handleEditSaved(act: ActDto) { editModalOpen = false; editTargetAct = null; selectedActId = act.id; refresh(); refreshCounts(); }` mirroring `handleSaved` (122-127).
  </action>
  <acceptance_criteria>
    - `pnpm --dir ui exec svelte-check` reports no new type errors.
    - `pnpm --dir ui build` succeeds.
    - `grep -n "onEdit={handleEdit}" ui/src/features/acts/ActsPage.svelte` matches.
    - `grep -n 'mode="edit"' ui/src/features/acts/ActsPage.svelte` matches a SECOND `<ActFormModal>` instance, distinct from the existing create-mode instance.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui exec svelte-check && pnpm --dir ui build</automated>
    <human-check>
      Manual verification (workflow.human_verify_mode=end-of-phase — no live checkpoint pause, verify at phase end):
      1. Open Acts page (desktop Tauri build, then again via LAN browser after `pnpm --dir ui build`). Select an existing handover act. Confirm "Редактировать" is enabled; click it — form opens pre-filled with current header fields (№, даты, Сдал/Принял, Расположение, Заметки) and current positions.
      2. Change a header field (e.g. Сдал) and save — success toast appears; detail view reflects the new value immediately.
      3. Add a position from stock, save — the device transitions from "на складе" to "в работе" (check Devices page); the new position appears in the act's item list.
      4. Remove an existing position, save — the device returns to its prior status/location (check Devices page); the position disappears from the act's item list.
      5. Edit the same act's Комплектация field on a retained position, save — the value persists and is visible on re-opening the edit form.
      6. Open the same act in two browser tabs, save from tab 1, then attempt to save from tab 2 (stale version) — confirm the "изменён другим пользователем" toast appears (409/OptimisticLockMismatch), not a silent failure or generic error.
      7. Select a return-act — confirm "Редактировать" is disabled with a tooltip explaining why; select an ARCHIVED handover act — confirm "Редактировать" is still enabled (unlike "Возврат", which is disabled for archived acts).
    </human-check>
  </verify>
  <done>Editing an existing handover act end-to-end (open pre-filled form → edit header/positions/комплектация → save → detail view + Devices page reflect the change) works in both Tauri desktop and LAN-browser (server) modes; return-acts show a disabled Редактировать button with a tooltip; stale-version saves surface a clear toast instead of failing silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI (this plan) → `acts.update` (Plan 19-04's client, calling the Plan 19-03/19-04 server-side-enforced endpoint) | The UI's D-07 gating (this plan, Task 2) is a UX convenience layer ON TOP OF the authoritative server-side guard already built in Plan 19-03 — it is NOT itself a security boundary. A user (or a modified client) bypassing the disabled button still hits the server-side `act.act_type != ActType::Handover` rejection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Tampering | Client-side D-07 button gating (`ActDetail.svelte`) could be mistaken for the authoritative check | accept (defense-in-depth only, not primary control) | The primary control is server-side (`ActService::update`'s act_type guard, Plan 19-03, verified by `reject_update_on_return_act`). This plan's UI gating is UX-only — disabling it or bypassing it via devtools cannot achieve anything the server doesn't independently reject. No new risk introduced by relying on it as a convenience layer. |
| T-19-13 | Information Disclosure | `OptimisticLockMismatch` toast message reveals that "another user" made a concurrent change | accept | Standard, expected UX for optimistic-concurrency systems (same pattern already used elsewhere in this app for other CAS-protected mutations); no sensitive data disclosed (no other user's identity, no diff content) — matches the existing tone of other conflict toasts in this codebase. |

No new server-reachable trust boundary is introduced by this plan — it is a pure UI wiring plan against endpoints already fully authorized and validated in Plans 19-03/19-04.
</threat_model>

<verification>
1. `pnpm --dir ui exec svelte-check` (after each task)
2. `pnpm --dir ui build` (after Task 3, confirms LAN/browser bundle is current)
3. Manual verification per Task 3's `<human-check>` block (end-of-phase, per workflow.human_verify_mode=end-of-phase)
</verification>

<success_criteria>
- "Редактировать" opens a working, pre-filled edit form for handover acts; disabled with a tooltip for return-acts.
- Header edits, position add/remove, and комплектация edits all save correctly and are reflected in the detail view and Devices page.
- Stale-version saves surface a clear "изменён другим пользователем" toast, not a silent failure.
- Works identically in Tauri desktop and LAN-browser (server) mode.
- This closes ACT-02 and the phase's ROADMAP.md Success Criteria 2 and 3.
</success_criteria>

<output>
Create `.planning/phases/19-acts-date-edit/19-05-SUMMARY.md` when done
</output>
