---
phase: 14-act-data-structure
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/trackly-app/src/dto/act.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/context.rs
autonomous: true
requirements: [PDFA-04, PDFA-06]

must_haves:
  truths:
    - "Контекст рендера акта несёт живые specs устройства (item.specs), не захардкоженный Null"
    - "Контекст рендера акта читает org-реквизиты из org_settings (OrgDbService::get_for_pdf), а не из org.json"
    - "org.* в контексте несёт phone/fax/email/okpo/ogrn"
    - "Существующий акт, созданный до фазы, генерирует PDF без ошибок; отсутствующие specs/реквизиты деградируют в пусто, не в ошибку"
  artifacts:
    - path: "crates/trackly-app/src/dto/act.rs"
      provides: "ActItemDto с полем specs: Option<String>"
      contains: "pub specs"
    - path: "crates/trackly-app/src/services/act_service.rs"
      provides: "items_json specs из device.notes; org-контекст из OrgSettingsDto с реквизитами"
      contains: "it.specs"
    - path: "crates/trackly-app/src/context.rs"
      provides: "org_db прокинут в ActService pdf pipeline (D-05)"
      contains: "org_db"
  key_links:
    - from: "act_service.rs load_items_for_act"
      to: "devices.notes column"
      via: "SELECT d.notes mapped to ActItemDto.specs"
      pattern: "d\\.notes"
    - from: "act_service.rs render_pdf org context"
      to: "OrgDbService::get_for_pdf"
      via: "get_for_pdf reads org_settings"
      pattern: "get_for_pdf"
---

<objective>
Прокинуть недостающие данные в контекст рендера акта: (1) расхардкодить specs (Технические
характеристики) — брать живое значение devices.notes через ActItemDto.specs (D-01); (2) переключить
источник org-реквизитов act-рендера с org.json на org_settings через OrgDbService::get_for_pdf,
включив новые phone/fax/email/okpo/ogrn в org-контекст (D-02/D-05); (3) проверить обратную
совместимость — старый акт генерирует PDF без ошибок.

Purpose: PDFA-04 — Технические характеристики попадают в контекст PDF. PDFA-03 (через D-05) —
шапка отражает реквизиты из org_settings. PDFA-06 — контекст act_handover несёт всё, что нужно
переработанному в Phase 15 шаблону. Это единственный реальный data-gap фазы (specs: Null) плюс
унификация источника org-данных.
Output: ActItemDto.specs, обновлённый items_json + org-контекст в render_pdf, org_db в pipeline,
backward-compat тест.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-act-data-structure/14-CONTEXT.md
@.planning/phases/14-act-data-structure/14-PATTERNS.md
@.planning/phases/14-act-data-structure/14-01-SUMMARY.md

<interfaces>
Из plan 01: OrgSettingsDto несёт phone/fax/email/okpo/ogrn; OrgDbService::get_for_pdf() возвращает
  (OrgSettingsDto, Option<Vec<u8>>, Option<String>).

crates/trackly-app/src/dto/act.rs L92-109 — ActItemDto (id, device_id, quantity, device_name,
  inventory_no, serial_no, model, condition_at_time, complectation_at_time, outstanding_device_ids).
  #[derive(..., Type)] — добавление specs авто-обновит bindings.
crates/trackly-app/src/services/act_service.rs:
  - render_pdf() L1322-1405 — org читается через pipeline.organization.read() (org.json!), items_json
    L1346-1361 c "specs": serde_json::Value::Null, org-context L1363-1370.
  - load_items_for_act() L1681-1720 — SELECT ai.id,device_id,quantity,condition_at_time,
    complectation_at_time, d.name,d.inventory_number,d.serial_number,d.model (индексы 0-8).
  - PdfPipelineRefs L1512-1514 { templates, organization }; with_pdf_pipeline L79-89; поля
    templates/organization L52-53.
  - devices_sqlite.rs L9-11: DTO specs ↔ DB notes, kit ↔ complectation, state ↔ condition.
crates/trackly-app/src/context.rs L211 org_db создаётся; L251-257 ActService::new(...).with_pdf_pipeline(
  templates, organization, pdf). report_service.rs L522 — прецедент HeaderBlock из OrgSettingsDto (DB).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: specs↔notes — расхардкодить Технические характеристики (D-01)</name>
  <files>crates/trackly-app/src/dto/act.rs, crates/trackly-app/src/services/act_service.rs</files>
  <read_first>
    - crates/trackly-app/src/dto/act.rs L92-109 (ActItemDto — файл, который меняем)
    - crates/trackly-app/src/services/act_service.rs L1346-1361 (items_json с Null), L1681-1720 (load_items_for_act SELECT) (файл, который меняем; см. 14-PATTERNS.md §act_service Edit 1 L287-334)
    - crates/trackly-infra/src/repos/devices_sqlite.rs L9-11 (маппинг specs↔notes — источник истины)
  </read_first>
  <behavior>
    - Акт с устройством, у которого devices.notes = "Intel i5, 8GB" → item.specs = "Intel i5, 8GB" в контексте (не Null)
    - Акт с устройством, у которого devices.notes IS NULL → item.specs = None → шаблон деградирует в «—»/пусто (не ошибка)
    - kit (complectation_at_time) и condition (condition_at_time) остаются point-in-time снимками, НЕ меняются
  </behavior>
  <action>
    В dto/act.rs добавить в ActItemDto поле pub specs: Option<String> (сразу после model). Derive Type
    обновит bindings при regen. Это ЖИВОЕ значение устройства, НЕ снимок — колонку specs_at_time на
    act_items НЕ создавать (D-01).

    В act_service.rs load_items_for_act (L1685-1706): добавить d.notes последней колонкой SELECT
    (индекс 9), и specs: r.get(9)? в конструирование ActItemDto. НЕ трогать condition_at_time/
    complectation_at_time — они остаются снимками.

    В render_pdf items_json (L1355): заменить "specs": serde_json::Value::Null на "specs": it.specs.

    Backward-compat: d.notes nullable → Option<String> → сериализуется в JSON null → шаблон (Phase 15)
    деградирует в «—» через существующий default-фильтр. НЕ падать при NULL.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo build -p trackly-app 2>&1 | tail -5; grep -n "serde_json::Value::Null" crates/trackly-app/src/services/act_service.rs | grep -i spec</automated>
  </verify>
  <acceptance_criteria>
    - grep "pub specs" crates/trackly-app/src/dto/act.rs находит specs: Option<String> в ActItemDto
    - act_service.rs load_items_for_act SELECT содержит "d.notes"; конструирование ActItemDto содержит specs: r.get(...)
    - в render_pdf items_json больше НЕ содержит "specs": serde_json::Value::Null (заменён на it.specs) — grep выше не находит совпадений для specs-строки
    - condition_at_time / complectation_at_time SELECT и маппинг не изменены (остаются снимками)
    - cargo build -p trackly-app без ошибок
  </acceptance_criteria>
  <done>ActItemDto.specs добавлен; load_items_for_act тащит d.notes; items_json отдаёт живые specs вместо Null; kit/condition-снимки нетронуты.</done>
</task>

<task type="auto">
  <name>Task 2: Переключить источник org-реквизитов act-рендера на org_settings (D-05) + org-контекст</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/src/context.rs</files>
  <read_first>
    - crates/trackly-app/src/services/act_service.rs L52-89 (поля pipeline + with_pdf_pipeline), L1322-1370 (render_pdf org read + org-context), L1512-1514 (PdfPipelineRefs) (файл, который меняем; см. 14-PATTERNS.md §act_service Edit 2 L336-367)
    - crates/trackly-app/src/services/report_service.rs L522-532 (прецедент HeaderBlock из OrgSettingsDto — D-05 Option A)
    - crates/trackly-app/src/services/org_db_service.rs L351-383 (get_for_pdf — сигнатура/возврат)
    - crates/trackly-app/src/context.rs L211, L251-257 (создание org_db + вызов with_pdf_pipeline — файл, который меняем)
  </read_first>
  <action>
    D-05 Option A — единый источник org-данных = org_settings (то, что пишет Settings UI):

    В act_service.rs добавить в PdfPipeline поле org_db: Option<Arc<OrgDbService>> (рядом с
    organization L53), расширить with_pdf_pipeline (L79-89) параметром org_db и присвоением, и
    PdfPipelineRefs (L1512-1514) полем org_db. НЕ удалять organization целиком — она ещё нужна для
    safe_logo_canonical/лого-пути и render_acceptance_pdf; но для act_handover ORG-РЕКВИЗИТЫ читать из
    org_db.get_for_pdf().

    ВАЖНО (иначе build ломается): pdf_pipeline() (L1498-1509) конструирует PdfPipelineRefs из
    кортежа `match (&self.templates, &self.organization, &self.pdf)`. Добавив поле org_db в
    PdfPipelineRefs, обязательно расширить и этот match-кортеж (добавить `&self.org_db`), и заполнение
    PdfPipelineRefs, и `_ => Err(...)`-ветку. Сделать org_db Option-aware, чтобы helper-фикстуры
    (ActService::new без with_pdf_pipeline) не падали. Проверяется `cargo build -p trackly-app`.

    В render_pdf (L1322-1370): получить (org_dto, _logo_bytes, _logo_mime) = pipeline.org_db.get_for_pdf().
    Заменить org-context блок (L1363-1370) на значения из org_dto: name=org_dto.org_name, inn=org_dto.inn,
    kpp=org_dto.kpp, address=org_dto.address, ПЛЮС phone/fax/email/okpo/ogrn из org_dto. Сохранить
    logo_path через существующий safe_logo (pipeline.organization) — лого-путь не меняем. Пустые
    реквизиты (DEFAULT '') прокидываются как есть → шаблон Phase 15 деградирует в пусто/«—», НЕ ошибка.

    render_acceptance_pdf (org-блок ~L1468): по D-03 семантика фазы = act_handover; acceptance можно
    ОСТАВИТЬ на organization.read() (org.json) без реквизитов ИЛИ, если тривиально, тоже переключить —
    подтвердить в SUMMARY, что acceptance не сломан (генерирует PDF). Приоритет — не сломать backward-compat.

    В context.rs: прокинуть org_db.clone() в вызов with_pdf_pipeline (L252-256) новым аргументом.
    org_db уже создан (L211) ДО acts (L251), порядок корректен.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo build -p trackly-app 2>&1 | tail -8; grep -c "get_for_pdf" crates/trackly-app/src/services/act_service.rs</automated>
  </verify>
  <acceptance_criteria>
    - act_service.rs render_pdf вызывает org_db get_for_pdf (grep get_for_pdf в act_service.rs больше или равно 1)
    - org-context в render_pdf содержит ключи phone/fax/email/okpo/ogrn из OrgSettingsDto
    - with_pdf_pipeline и PdfPipelineRefs имеют поле org_db
    - context.rs передаёт org_db в with_pdf_pipeline
    - cargo build -p trackly-app без ошибок
    - render_acceptance_pdf по-прежнему компилируется и не удалён
  </acceptance_criteria>
  <done>Act-рендер читает org-реквизиты из org_settings через get_for_pdf; org-контекст несёт 5 реквизитов; org_db прокинут через pipeline; acceptance не сломан.</done>
</task>

<task type="auto">
  <name>Task 3: Backward-compat — старый акт генерирует PDF, регресс-тесты и bindings</name>
  <files>crates/trackly-app/src/services/act_service.rs</files>
  <read_first>
    - crates/trackly-app/src/services/act_service.rs L1322-1405 (render_pdf — цель теста)
    - существующие тесты act render pipeline (grep -rln "render_pdf" crates/trackly-app/tests/)
    - 14-PATTERNS.md §Migration test coverage + §Specta bindings regen L394-405
  </read_first>
  <action>
    Добавить/расширить интеграционный тест (в существующем act render test-файле, найти через
    grep render_pdf в crates/trackly-app/tests/): создать акт с устройством, у которого notes = NULL
    и пустые org_settings-реквизиты (DEFAULT ''), затем вызвать render_pdf и утвердить Ok(bytes) с
    непустым PDF — доказывает success criterion #4 (обратная совместимость: отсутствующие specs/
    реквизиты → пусто, не ошибка рендера). Дополнительно — вариант с заполненными notes/реквизитами,
    утвердить, что рендер успешен.

    Регенерировать bindings (ActItemDto.specs изменил контракт): cargo test export_bindings.
    Прогнать полный набор существующих PDF-пайплайн тестов, чтобы подтвердить отсутствие регресса
    (мультиустройство, single-item — данные уже отдавались через act.items[], поведение сохранено).
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --test export_bindings 2>&1 | tail -3; TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app render 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Новый/расширенный тест: render_pdf для акта с NULL notes и пустыми реквизитами возвращает Ok с непустым PDF (успех, не ошибка)
    - export_bindings зелёный; ui/src/bindings.ts содержит поле specs в ActItemDto (grep specs bindings.ts)
    - Все существующие render/PDF-тесты trackly-app проходят (нет регресса)
    - cargo clippy -p trackly-app без warnings по изменённым файлам
  </acceptance_criteria>
  <done>Backward-compat доказан тестом (старый акт → PDF без ошибок); bindings синхронизированы; регресс-набор зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| org_settings → act render context | Реквизиты организации из DB попадают в JSON контекста MiniJinja |
| devices.notes → act item context | Живое значение specs устройства попадает в контекст позиции акта |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Denial of Service | render_pdf с NULL notes / пустыми реквизитами | mitigate | Option<String> + serde null → шаблонный default; тест Task 3 доказывает Ok, не panic/error (success criterion #4) |
| T-14-03-02 | Information Disclosure | смена источника org на org_settings может показать иные реквизиты в старых актах | accept | Ожидаемая унификация per D-05; org_settings — canonical источник, редактируемый в Настройках |
| T-14-03-03 | Injection | specs/реквизиты в MiniJinja контексте | mitigate | Значения идут как JSON-строки в serde_json контекст, не как код шаблона; MiniJinja экранирует; DocSpec — структурированный IR, не raw |
| T-14-03-SC | Tampering | npm/pip/cargo installs | mitigate | Нет новых зависимостей — только расширение существующих struct/SQL/pipeline. Package-legitimacy gate не применяется |
</threat_model>

<verification>
- cargo build + clippy без ошибок/warnings
- export_bindings зелёный, bindings несут specs
- render-тесты зелёные, включая новый backward-compat кейс (NULL notes + пустые реквизиты → Ok)
</verification>

<success_criteria>
- items[].specs — живое devices.notes, не Null (success criterion #3, PDFA-04)
- org-контекст несёт расширенные реквизиты из org_settings (success criterion #3, PDFA-03 через D-05)
- Старый акт генерирует PDF без ошибок (success criterion #4)
- Контекст act_handover несёт всё для переработанного шаблона Phase 15 (PDFA-06)
</success_criteria>

<output>
Create `.planning/phases/14-act-data-structure/14-03-SUMMARY.md` when done
</output>
