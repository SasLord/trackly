---
phase: 14-act-data-structure
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/trackly-app/src/http/settings_org.rs
  - crates/trackly-app/src/tauri_cmds/settings_org.rs
  - ui/src/bindings.ts
  - ui/src/features/settings/OrgSettings.svelte
autonomous: true
requirements: [PDFA-03]

must_haves:
  truths:
    - "Админ может ввести телефон/факс/e-mail/ОКПО/ОГРН в Настройках организации"
    - "Введённые реквизиты сохраняются через settings_save_org_fields и подгружаются обратно через settings_get_org"
    - "bindings.ts отражает новые поля OrgPatch/OrgSettingsDto"
    - "Реквизиты работают в обоих транспортах (Tauri desktop + LAN browser)"
  artifacts:
    - path: "ui/src/features/settings/OrgSettings.svelte"
      provides: "5 input-полей реквизитов + загрузка/сохранение"
      contains: "phone"
    - path: "ui/src/bindings.ts"
      provides: "OrgPatch/OrgSettingsDto с 5 новыми полями"
      contains: "phone"
  key_links:
    - from: "OrgSettings.svelte saveOrg"
      to: "settings_save_org_fields command"
      via: "apiCall patch"
      pattern: "settings_save_org_fields"
    - from: "OrgSettings.svelte loadOrg"
      to: "settings_get_org command"
      via: "apiCall dto"
      pattern: "settings_get_org"
---

<objective>
Прокинуть 5 новых реквизитов организации через транспортный слой и UI Настроек:
подтвердить/дописать HTTP-route и Tauri-command, регенерировать bindings, добавить input-поля
в OrgSettings.svelte (загрузка + сохранение). После плана админ может ввести и сохранить
телефон/факс/e-mail/ОКПО/ОГРН в обоих режимах (desktop + браузер).

Purpose: PDFA-03 — реквизиты должны редактироваться пополе в Настройках и храниться в org_settings.
Плана 01 расширил schema/DTO/service; этот план завершает сквозной путь до пользователя.
Output: рабочая форма Настроек организации с 5 новыми полями, обновлённые bindings, ребилд ui/dist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-act-data-structure/14-CONTEXT.md
@.planning/phases/14-act-data-structure/14-PATTERNS.md
@.planning/phases/14-act-data-structure/14-01-SUMMARY.md

<interfaces>
Из plan 01 (dto/reports.rs): OrgPatch и OrgSettingsDto теперь несут phone/fax/email/okpo/ogrn: String.

crates/trackly-app/src/http/settings_org.rs L176-189 — handler_save_org_fields пробрасывает OrgPatch
  прозрачно (Json payload → build_settings_save_org_fields). Route /api/v1/settings_save_org_fields (L338).
crates/trackly-app/src/tauri_cmds/settings_org.rs L303-311 — settings_save_org_fields(patch: OrgPatch)
  пробрасывает OrgPatch прозрачно. build_settings_save_org_fields L28-34.
ui/src/features/settings/OrgSettings.svelte L8-47 (loadOrg + $state), L75-82 (saveOrg payload),
  L211-225 (input-поле ИНН — образец разметки), L306-311 (.form-grid 2-column).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Транспорт-проверка + регенерация bindings</name>
  <files>crates/trackly-app/src/http/settings_org.rs, crates/trackly-app/src/tauri_cmds/settings_org.rs, ui/src/bindings.ts</files>
  <read_first>
    - crates/trackly-app/src/http/settings_org.rs L176-189 (handler_save_org_fields — файл, который проверяем; см. 14-PATTERNS.md §settings_org route L165-185)
    - crates/trackly-app/src/tauri_cmds/settings_org.rs L28-34, L303-311 (build + command — файл, который проверяем; см. 14-PATTERNS.md §settings_org command L189-209)
    - crates/trackly-app/tests/export_bindings.rs (тест-генератор bindings.ts)
  </read_first>
  <action>
    Прочитать handler_save_org_fields (http) и settings_save_org_fields (tauri). Per 14-PATTERNS.md
    оба, скорее всего, пробрасывают OrgPatch прозрачно (Json/specta), и НИКАКИХ изменений кода не нужно —
    новые поля DTO текут автоматически. Убедиться в этом; менять эти файлы ТОЛЬКО если обнаружится,
    что payload-обёртка перечисляет поля явно (напр. SaveOrgFieldsPayload с ручным маппингом). Если
    явного перечисления нет — оставить файлы без изменений (files_modified в frontmatter покрывают
    оба сценария). authorize с Action ManageSettings на обоих транспортах остаётся.

    Регенерировать bindings: cargo test -p trackly-app --test export_bindings. Убедиться, что
    ui/src/bindings.ts подхватил phone/fax/email/okpo/ogrn в OrgPatch и OrgSettingsDto.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --test export_bindings 2>&1 | tail -5; grep -c "phone" ui/src/bindings.ts</automated>
  </verify>
  <acceptance_criteria>
    - cargo test export_bindings проходит (нет drift-ошибки)
    - grep -c "phone" ui/src/bindings.ts больше или равно 2 (OrgPatch + OrgSettingsDto)
    - grep OrgSettingsDto ui/src/bindings.ts показывает поля fax/email/okpo/ogrn
    - Если http/tauri файлы не менялись — это допустимо (прозрачный проброс OrgPatch)
  </acceptance_criteria>
  <done>bindings.ts несёт 5 новых реквизитов в обоих DTO; транспорт-слой подтверждён/дописан; export_bindings зелёный.</done>
</task>

<task type="auto">
  <name>Task 2: UI Настроек — input-поля реквизитов (загрузка + сохранение)</name>
  <files>ui/src/features/settings/OrgSettings.svelte</files>
  <read_first>
    - ui/src/features/settings/OrgSettings.svelte L8-47, L75-82, L211-225, L306-311 (файл, который меняем; см. 14-PATTERNS.md §OrgSettings.svelte L213-247)
    - ui/src/bindings.ts (после Task 1 — сверить имена полей OrgSettingsDto/OrgPatch)
  </read_first>
  <action>
    В OrgSettings.svelte добавить пять $state('') переменных: phone, fax, email, okpo, ogrn.
    В loadOrg() заполнить их из dto (dto.phone и т.д.). В saveOrg() расширить объект patch пятью
    полями (phone, fax, email, okpo, ogrn) в вызове apiCall settings_save_org_fields. Обновить
    локальный TS-интерфейс OrgSettingsDto в файле (L219) новыми полями, чтобы svelte-check не ругался
    (или импортировать из bindings, если так принято в файле).

    Добавить 5 input-полей по образцу поля ИНН (L211-225): каждое — обычный .form-field с
    .form-label и .form-input (type text; email можно type email), русские лейблы «Телефон», «Факс»,
    «E-mail», «ОКПО», «ОГРН», bind:value на соответствующую переменную. Разместить в .form-grid
    (2-column, короткие поля half-width). Порядок полей — на усмотрение исполнителя. НЕ трогать
    логику логотипа/version/optimistic-lock.

    Ребилд ui/dist для LAN-browser тестирования: pnpm --dir ui build.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && pnpm --dir ui svelte-check 2>&1 | tail -5; pnpm --dir ui build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "bind:value={phone}" ui/src/features/settings/OrgSettings.svelte равно 1 (и аналогично для fax/email/okpo/ogrn)
    - OrgSettings.svelte содержит лейблы «Телефон», «Факс», «E-mail», «ОКПО», «ОГРН»
    - saveOrg patch включает phone/fax/email/okpo/ogrn; loadOrg присваивает их из dto
    - pnpm svelte-check: 0 ошибок
    - pnpm --dir ui build завершается успешно (ui/dist пересобран)
  </acceptance_criteria>
  <done>Форма Настроек организации имеет 5 новых input-полей с загрузкой и сохранением; svelte-check чист; ui/dist пересобран.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN browser / Tauri webview → HTTP/Tauri command | Пользователь-админ отправляет patch с новыми реквизитами |
| Transport handler → org_db_service | Проброс OrgPatch к single-writer UPDATE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Elevation of Privilege | settings_save_org_fields (HTTP+Tauri) | mitigate | authorize(caller, ManageSettings) на обоих транспортах остаётся (http L184, tauri L309) — не ослаблять при добавлении полей |
| T-14-02-02 | Tampering | OrgPatch с клиента | mitigate | Строки идут в params! single-writer UPDATE; никаких новых привилегированных операций не вводится |
| T-14-02-03 | Cross-Site Scripting | реквизиты, отрендеренные в UI/PDF | accept | Svelte экранирует по умолчанию; PDF-рендер (Phase 15) через DocSpec, не raw HTML |
| T-14-02-SC | Tampering | npm/pip/cargo installs | mitigate | Нет новых зависимостей — только existing apiCall + svelte-компонент. Package-legitimacy gate не применяется |
</threat_model>

<verification>
- export_bindings зелёный, bindings.ts несёт новые поля
- svelte-check 0 ошибок, ui/dist пересобран
- Разметка формы содержит 5 новых полей с русскими лейблами
</verification>

<success_criteria>
- Реквизиты редактируются в Настройках и сохраняются в org_settings (success criterion #2)
- Оба транспорта (desktop + браузер) работают через единый apiCall
- bindings синхронизированы с Rust DTO
</success_criteria>

<output>
Create `.planning/phases/14-act-data-structure/14-02-SUMMARY.md` when done
</output>
