---
phase: 14-act-data-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V033__org_settings_requisites.sql
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/src/services/org_db_service.rs
  - crates/trackly-app/src/pdf/docspec.rs
autonomous: true
requirements: [PDFA-03, PDFA-06]

must_haves:
  truths:
    - "org_settings хранит 5 новых реквизитов (phone/fax/email/okpo/ogrn), пустых по умолчанию"
    - "OrgDbService читает и пишет 5 новых реквизитов через get/save_fields/get_for_pdf"
    - "HeaderBlock несёт поля phone/fax/email/okpo/ogrn и десериализуется из старых шаблонов без них"
    - "Существующие акты и старые строки org_settings не дают NULL/ошибки после миграции"
  artifacts:
    - path: "migrations/V033__org_settings_requisites.sql"
      provides: "ADD COLUMN phone/fax/email/okpo/ogrn TEXT NOT NULL DEFAULT ''; PRAGMA user_version=33"
      contains: "ALTER TABLE org_settings ADD COLUMN phone"
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "OrgPatch + OrgSettingsDto расширены 5 полями"
      contains: "pub phone: String"
    - path: "crates/trackly-app/src/services/org_db_service.rs"
      provides: "3 SQL-сайта (get/save_fields/get_for_pdf) читают/пишут новые колонки"
      contains: "phone"
    - path: "crates/trackly-app/src/pdf/docspec.rs"
      provides: "HeaderBlock расширен реквизитами с serde(default)"
      contains: "org_phone"
  key_links:
    - from: "org_db_service.rs get_for_pdf"
      to: "org_settings phone/fax/email/okpo/ogrn columns"
      via: "SELECT with appended columns"
      pattern: "SELECT org_name.*phone"
    - from: "docspec.rs HeaderBlock"
      to: "MiniJinja template JSON"
      via: "serde deserialize with default"
      pattern: "serde\\(default\\)"
---

<objective>
Заложить схему и типы для расширенных реквизитов организации (телефон/факс/e-mail/ОКПО/ОГРН):
миграция V033, расширение DTO (OrgPatch/OrgSettingsDto), трёх SQL-сайтов OrgDbService и
структуры HeaderBlock. Это фундамент — plan 02 подключит UI/транспорт, plan 03 прокинет
реквизиты в контекст акта.

Purpose: PDFA-03 требует, чтобы шапка PDF отражала расширенные реквизиты из org_settings,
а не только name/inn/kpp/address. PDFA-06 требует, чтобы дефолт-шаблон оставался редактируем —
HeaderBlock с serde(default) сохраняет обратную совместимость со старыми шаблонами.
Output: миграция V033, расширенные OrgPatch/OrgSettingsDto, 3 обновлённых SQL-запроса,
расширенный HeaderBlock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-act-data-structure/14-CONTEXT.md
@.planning/phases/14-act-data-structure/14-PATTERNS.md

<interfaces>
OrgSettingsDto get_for_pdf returns a tuple; report_service.rs L522 is the HeaderBlock precedent.

crates/trackly-app/src/dto/reports.rs L177-208 — current OrgPatch (4 fields) + OrgSettingsDto (4 + has_logo).
crates/trackly-app/src/services/org_db_service.rs:
  - get() L51-77 — SELECT org_name,inn,kpp,address,(logo IS NOT NULL) mapped to OrgSettingsDto
  - save_fields() L86-104 — UPDATE ... SET org_name=?2,inn=?3,kpp=?4,address=?5,updated_at_utc,version+1 WHERE id=1
  - get_for_pdf() L351-383 — SELECT org_name,inn,kpp,address,(logo IS NOT NULL),logo_blob,logo_mime mapped to (OrgSettingsDto, Option, Option)
crates/trackly-app/src/pdf/docspec.rs L34-53 — HeaderBlock with org_name/org_inn/org_kpp/org_address, logo_path, serde(default) logo_bytes, serde(default) logo_mime, act_label, date_label.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Миграция V033 + расширение OrgDbService (3 SQL-сайта)</name>
  <files>migrations/V033__org_settings_requisites.sql, crates/trackly-app/src/dto/reports.rs, crates/trackly-app/src/services/org_db_service.rs</files>
  <read_first>
    - migrations/V026__org_settings.sql (аналог ADD-COLUMN-safe паттерна; см. 14-PATTERNS.md §V033 L29-73)
    - migrations/V032__cartridge_model_compatibility_printer_name.sql (последняя миграция — подтвердить, что V033 свободна)
    - crates/trackly-app/src/dto/reports.rs L177-208 (OrgPatch/OrgSettingsDto — файл, который меняем)
    - crates/trackly-app/src/services/org_db_service.rs L51-104, L351-383 (3 SQL-сайта — файл, который меняем; см. 14-PATTERNS.md §org_db_service L109-162)
  </read_first>
  <action>
    Создать migrations/V033__org_settings_requisites.sql: пять операторов
    ALTER TABLE org_settings ADD COLUMN для колонок phone, fax, email, okpo, ogrn — каждая
    типа TEXT NOT NULL DEFAULT пустая строка (именно пустая строка, НЕ плейсхолдер-строки как name/inn в V026 —
    отсутствующие реквизиты деградируют в пусто, per D-02 и 14-CONTEXT specifics). Завершить
    файл строкой PRAGMA user_version = 33. Порядок/именование колонок — Claude's Discretion (D-02).

    В dto/reports.rs добавить в OrgPatch и в OrgSettingsDto пять полей типа String:
    phone, fax, email, okpo, ogrn (snake_case; String, не Option — колонки NOT NULL DEFAULT пусто).
    Оба struct уже derive specta Type — новые поля авто-попадут в bindings при regen (plan 02).

    В org_db_service.rs расширить ВСЕ ТРИ SQL-сайта, добавляя новые колонки В КОНЕЦ SELECT/UPDATE,
    чтобы не сдвигать существующие ordinal-индексы r.get(N):
    - get() (L51-77): добавить phone,fax,email,okpo,ogrn в SELECT, заполнить их в OrgSettingsDto с
      корректными индексами r.get(...).
    - save_fields() (L86-104): добавить phone, fax, email, okpo, ogrn в UPDATE SET и
      соответствующие patch-значения в params! (сохранить version=version+1, WHERE id=1,
      guard authorize с Action ManageSettings).
    - get_for_pdf() (L351-383): добавить те же 5 колонок в SELECT, заполнить их в OrgSettingsDto внутри
      query_row-замыкания. Это критический сайт — plan 03/Phase 15 читают шапку отсюда.
    Все записи идут через self.writer.execute (single-writer), чтения через spawn_blocking + readers —
    не открывать сырые соединения.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo build -p trackly-app 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - migrations/V033__org_settings_requisites.sql содержит строку начинающуюся с "ALTER TABLE org_settings ADD COLUMN phone TEXT NOT NULL DEFAULT" и аналогичные для fax/email/okpo/ogrn
    - migrations/V033__org_settings_requisites.sql содержит "PRAGMA user_version = 33"
    - grep -c "pub phone: String" crates/trackly-app/src/dto/reports.rs возвращает 2 (OrgPatch + OrgSettingsDto)
    - grep -c phone crates/trackly-app/src/services/org_db_service.rs больше или равно 3 (get/save_fields/get_for_pdf)
    - cargo build -p trackly-app завершается без ошибок
  </acceptance_criteria>
  <done>Миграция V033 создана; OrgPatch/OrgSettingsDto и 3 SQL-сайта OrgDbService несут 5 новых реквизитов; крейт компилируется.</done>
</task>

<task type="auto">
  <name>Task 2: Расширить HeaderBlock реквизитами + проверить миграцию</name>
  <files>crates/trackly-app/src/pdf/docspec.rs</files>
  <read_first>
    - crates/trackly-app/src/pdf/docspec.rs L34-53 (HeaderBlock — файл, который меняем; см. 14-PATTERNS.md §docspec L250-278)
    - crates/trackly-app/src/services/report_service.rs L522-532 (прецедент прямого построения HeaderBlock — потребует заполнить новые поля хотя бы пустыми при конструировании)
    - crates/trackly-app/tests/downgrade_protection.rs (тест, который прогонит V033)
  </read_first>
  <action>
    В docspec.rs добавить в HeaderBlock пять полей типа String: org_phone, org_fax, org_email,
    org_okpo, org_ogrn. Каждое пометить serde(default), чтобы старые шаблоны, не выдающие эти
    ключи в JSON, десериализовались без ошибки (per 14-PATTERNS.md — RESEARCH Pitfall 7;
    backward-compat для success criterion #4). HeaderBlock уже derive Default. На прямых сайтах
    построения (report_service.rs L522, template_service.rs L318 если есть) добавить инициализацию
    новых полей пустой строкой, только чтобы код компилировался — реальное заполнение реквизитами
    в act-контексте делает plan 03. НЕ трогать логику рендера/вёрстки шапки — это Phase 15.

    Проверить миграцию: прогнать downgrade_protection + idempotency тесты, чтобы V033 применялась
    корректно и sequential user_version был непрерывным.
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test downgrade_protection 2>&1 | tail -8; cargo test -p trackly-infra --test migration_idempotency 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "org_phone" crates/trackly-app/src/pdf/docspec.rs больше или равно 1
    - в docspec.rs каждое из org_phone/org_fax/org_email/org_okpo/org_ogrn имеет атрибут serde(default)
    - cargo test downgrade_protection проходит (V033 применена, user_version непрерывен)
    - cargo test migration_idempotency (trackly-infra) проходит
  </acceptance_criteria>
  <done>HeaderBlock несёт 5 реквизитов с serde(default); миграционные тесты зелёные; крейт компилируется.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI/HTTP → org_db_service | Пользователь-админ вводит реквизиты; строки пишутся в org_settings и позже рендерятся в PDF |
| org_settings → PDF render context | Значения реквизитов попадают в JSON контекста MiniJinja → DocSpec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | save_fields UPDATE org_settings | mitigate | authorize(caller, ManageSettings) guard остаётся на mutation (org_db_service.rs L84); single-writer через self.writer.execute |
| T-14-02 | Injection | phone/fax/email/okpo/ogrn как параметры SQL | mitigate | Только params!/bind, никаких строковых конкатенаций в SQL — паттерн V026/существующих сайтов |
| T-14-03 | Information Disclosure | новые реквизиты в PDF | accept | Реквизиты организации — не PII/секрет; предназначены для печати в акте |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | Плана без новых зависимостей: миграция + расширение существующих struct/SQL. Нет install-задач → package-legitimacy gate не применяется |
</threat_model>

<verification>
- cargo build -p trackly-app без ошибок
- downgrade_protection + migration_idempotency зелёные (V033 применена)
- grep-проверки полей в reports.rs / org_db_service.rs / docspec.rs
</verification>

<success_criteria>
- V033 создаёт 5 колонок TEXT NOT NULL DEFAULT пусто, user_version=33
- OrgPatch/OrgSettingsDto/HeaderBlock несут реквизиты
- 3 SQL-сайта OrgDbService читают/пишут реквизиты
- Миграционные тесты подтверждают backward-compat (success criterion #4/#5)
</success_criteria>

<output>
Create `.planning/phases/14-act-data-structure/14-01-SUMMARY.md` when done
</output>
