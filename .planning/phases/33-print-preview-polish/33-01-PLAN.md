---
phase: 33-print-preview-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/package.json
  - ui/src/lib/pdfPreview/bootstrapScript.js
  - ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts
  - ui/src/lib/pdfPreview/pagedPreviewBridge.ts
  - ui/src/lib/utils/pluralize.ts
autonomous: true
requirements: [PRV-01, PRV-02]

must_haves:
  truths:
    - "pagedjs 0.4.3 (exact-pinned, per D-04) is a direct dependency in ui/package.json, and pnpm-lock.yaml reflects it"
    - "ui/src/lib/pdfPreview/bootstrapScript.js contains the static, non-interpolated Paged.js bootstrap protocol script (no per-call templating) — this is the frozen contract Plan 33-02's CSP hash and Plan 33-04's print paths both depend on byte-for-byte"
    - "PAGED_PREVIEW_INLINE_SCRIPT in pagedPreviewBootstrap.ts is built as exactly `pagedjsLibraryText + ';\\n' + bootstrapText` — this formula must be replicated identically by Plan 33-02's hash-drift script"
    - "buildSrcdoc() wraps PAGED_PREVIEW_INLINE_SCRIPT in a single inline <script> element using the '<' + 'script>' concatenation idiom (matching PdfPreviewModal.svelte's existing autoPrint idiom), never a template literal containing a literal </script> substring"
    - "buildSrcdoc() injects theme-scoped literal chrome CSS (backdrop #e4e8f0 light / #0a0d12 dark on body, literal --tr-elev-2-equivalent box-shadow on .pagedjs_page) as a separate <style> tag, kept out of the CSP-hashed <script> text (D-08, D-09)"
    - "pagedPreviewBridge.ts validates postMessage senders via event.source identity against the iframe's own contentWindow, never via event.origin string comparison (C-01)"
    - "ui/src/lib/utils/pluralize.ts exports a RU pluralization helper implementing the standard 1 / 2-4 / 5+ (and 11-14 exception) agreement rule, usable for the page counter"
  artifacts:
    - path: ui/src/lib/pdfPreview/bootstrapScript.js
      provides: "Static Paged.js bootstrap protocol script text (plain browser JS, no ES module syntax)"
    - path: ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts
      provides: "srcdoc-construction contract"
      exports: ["buildSrcdoc", "PAGED_PREVIEW_INLINE_SCRIPT", "THEME_CHROME"]
    - path: ui/src/lib/pdfPreview/pagedPreviewBridge.ts
      provides: "postMessage bridge with opaque-origin-safe sender validation"
      exports: ["attachBridge"]
    - path: ui/src/lib/utils/pluralize.ts
      provides: "RU pluralization helper"
      exports: ["pluralizeRu"]
  key_links:
    - from: "ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts"
      to: "ui/src/lib/pdfPreview/bootstrapScript.js"
      via: "Vite raw-text import"
      pattern: "bootstrapScript\\.js\\?raw"
    - from: "ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts"
      to: "pagedjs/dist/paged.min.js"
      via: "Vite raw-text import"
      pattern: "pagedjs/dist/paged\\.min\\.js\\?raw"
---

<objective>
Establish the frozen, interface-first contract that every other Plan 33 plan builds on: add the
`pagedjs` dependency, and create three new `ui/src/lib/**` modules — the srcdoc-construction
builder (`pagedPreviewBootstrap.ts` + its static `bootstrapScript.js` payload), the
opaque-origin-safe `postMessage` bridge (`pagedPreviewBridge.ts`), and a RU pluralization helper
(`pluralize.ts`). None of these are wired into `PdfPreviewModal.svelte` yet (that's Plan 33-03) —
this plan only creates and freezes the contract, because Plan 33-02's CSP hash-source depends on
`bootstrapScript.js`'s exact byte content never changing without a deliberate, tracked update.

Purpose: give downstream plans (33-02 CSP hash, 33-03 on-screen preview, 33-04 print paths) stable,
already-defined exports instead of requiring them to invent the contract mid-implementation.
Output: `ui/package.json` with `pagedjs` added; four new TS/JS files under `ui/src/lib/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-print-preview-polish/33-CONTEXT.md
@.planning/phases/33-print-preview-polish/33-RESEARCH.md
@.planning/phases/33-print-preview-polish/33-UI-SPEC.md

<interfaces>
<!-- No prior-plan interfaces to consume — this plan IS the interface-defining plan. -->
<!-- Downstream plans (33-02, 33-03, 33-04) will consume exactly what this plan exports: -->
<!--   buildSrcdoc(actHtml: string, theme: 'light' | 'dark'): string -->
<!--   PAGED_PREVIEW_INLINE_SCRIPT: string  (= pagedjsLibraryText + ';\n' + bootstrapText) -->
<!--   THEME_CHROME: { light: {backdrop, shadow}, dark: {backdrop, shadow} }  (exported, not module-private) -->
<!--   attachBridge(iframeEl: HTMLIFrameElement, onMsg: (data: unknown) => void): () => void -->
<!--   pluralizeRu(n: number, forms: [string, string, string]): string -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pinned pagedjs dependency</name>
  <files>ui/package.json, ui/pnpm-lock.yaml</files>
  <read_first>
    ui/package.json (current dependencies block)
    .planning/phases/33-print-preview-polish/33-RESEARCH.md — "Standard Stack" / "Installation" section
    .planning/phases/33-print-preview-polish/33-PATTERNS.md — "ui/package.json (config, dependency manifest)" section
  </read_first>
  <action>
    Add `"pagedjs": "0.4.3"` to the `dependencies` block in `ui/package.json`, alphabetically
    between `"@tauri-apps/plugin-shell"` and `"pdfjs-dist"` (matching the existing sort order).
    Use an EXACT pin (`"0.4.3"`, no `^`) rather than RESEARCH.md's suggested `^0.4.3` — this phase
    deliberately trades routine-update convenience for CSP hash stability: Plan 33-02 hardcodes a
    SHA-256 hash of the installed `paged.min.js` bytes into `crates/trackly-app/src/http/mod.rs`,
    and a caret range would let a compatible `pagedjs` patch release silently change those bytes
    on a future `pnpm install`, breaking LAN-mode pagination via CSP block with no visible error
    (RESEARCH.md Pitfall 1). Run `pnpm --dir ui install` to update `pnpm-lock.yaml`. Then run
    `pnpm --dir ui build` once (no `?raw` imports exist yet in this task — this only proves Vite
    resolves the new package and the existing build is not broken by its presence).
  </action>
  <acceptance_criteria>
    - `ui/package.json` `dependencies` contains the literal line `"pagedjs": "0.4.3"`.
    - `ui/pnpm-lock.yaml` contains a `pagedjs@0.4.3` (or `pagedjs@0.4.3:` in the v9 lockfile format) entry.
    - `pnpm --dir ui build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c '"pagedjs": "0.4.3"' ui/package.json && pnpm --dir ui build</automated>
  </verify>
  <done>pagedjs 0.4.3 is an exact-pinned direct dependency; the existing build is unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Create the srcdoc-construction contract (bootstrapScript.js + pagedPreviewBootstrap.ts)</name>
  <files>ui/src/lib/pdfPreview/bootstrapScript.js, ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts</files>
  <read_first>
    ui/src/features/acts/PdfPreviewModal.svelte — lines 181-199 (`printViaSystemBrowser`'s
    `'<' + 'script>'` / `'<' + '/script>'` concatenation idiom — MUST be reused verbatim, never a
    template literal containing a literal `</script>` substring) and lines 320-338 (current buggy
    `.pdf-page-frame`/`.pdf-iframe` CSS — the bug this phase's D-08/D-09 fix targets, for context
    only; do not edit this file in this task)
    .planning/phases/33-print-preview-polish/33-RESEARCH.md — "Pattern 1: Building the inline-script
    srcdoc", "Anti-Patterns to Avoid" (do NOT re-declare `@page` in the injected chrome CSS), Primary
    Sources (`Previewer.preview()`/`chunker.on('renderedPage', ...)` API confirmed by direct
    inspection of `dist/paged.js`)
    .planning/phases/33-print-preview-polish/33-UI-SPEC.md — "Color" section (backdrop/sheet hex
    values) and "Dark-Theme Rendering (D-08 mechanics)" section (why literal hex, not CSS vars,
    must be inlined into the opaque-origin document)
    .planning/phases/33-print-preview-polish/33-PATTERNS.md — "Design tokens for D-08/D-09" section
    (exact `--tr-elev-2` box-shadow literals for both themes)
  </read_first>
  <action>
    Create `ui/src/lib/pdfPreview/bootstrapScript.js` as a PLAIN standalone browser script (no
    `import`/`export` statements — it will be concatenated raw into an HTML `<script>` element, so
    it must be valid non-module JS on its own). It must, in an IIFE: (1) instantiate
    `new window.Paged.Previewer()`; (2) listen on `previewer.chunker.on('renderedPage', ...)`,
    incrementing a 1-indexed page counter and calling
    `parent.postMessage({ type: 'trackly-pagedjs-progress', pages: <n> }, '*')` on each event;
    (3) listen on `window.addEventListener('message', ...)` for incoming messages whose
    `e.data.type === 'trackly-theme-update'`, setting `document.body.style.background =
    e.data.backdrop` (do NOT validate `e.source` here — this direction is parent-to-iframe, and the
    iframe has no way to identify its own parent more precisely than "whoever posted to it"; the
    payload is a non-secret CSS color string, per RESEARCH.md Pattern 2); (4) call
    `previewer.preview()` (parameterless — auto-extracts the document's own `<style>`/`@page` via
    Paged.js's internal `removeStyles()`), and on resolution read
    `document.querySelector('.pagedjs_pages')?.scrollHeight` (fallback to
    `document.body.scrollHeight` if that selector is absent) and
    `parent.postMessage({ type: 'trackly-pagedjs-done', total: flow.total, height: <height> }, '*')`;
    on rejection, `parent.postMessage({ type: 'trackly-pagedjs-error', message: String(err) }, '*')`.
    Use exactly these four message-type string literals — `trackly-pagedjs-progress`,
    `trackly-pagedjs-done`, `trackly-pagedjs-error`, `trackly-theme-update` — downstream plans
    match on them by exact string.

    Create `ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts` that: imports the Paged.js UMD bundle
    text via `import pagedjsLibraryText from 'pagedjs/dist/paged.min.js?raw';` and this new
    bootstrap script via `import bootstrapText from './bootstrapScript.js?raw';`; exports
    `export const PAGED_PREVIEW_INLINE_SCRIPT = pagedjsLibraryText + ';\n' + bootstrapText;` —
    this EXACT concatenation formula (library text, literal `;\n`, bootstrap text, in that order)
    is load-bearing: Plan 33-02's CSP hash-drift script recomputes a hash using this identical
    formula, so do not reorder or add any character between the two. Exports
    `export const THEME_CHROME = { light: { backdrop: '#e4e8f0', shadow: '0 2px 6px rgba(16, 22,
    34, 0.09), 0 1px 2px rgba(16, 22, 34, 0.06)' }, dark: { backdrop: '#0a0d12', shadow: '0 3px
    10px rgba(0, 0, 0, 0.55), 0 1px 2px rgba(0, 0, 0, 0.5)' } } as const;` (exported, not
    module-private — Plan 33-03 needs these same literal values for the live `trackly-theme-update`
    postMessage so it never has to duplicate them). Exports
    `export function buildSrcdoc(actHtml: string, theme: 'light' | 'dark'): string` that: builds a
    `<style>` block setting `body { margin: 0; background: ${THEME_CHROME[theme].backdrop}; }`,
    `.pagedjs_pages { display: flex; flex-direction: column; align-items: center; gap: 24px;
    padding: 16px 0; }`, and `.pagedjs_page { box-shadow: ${THEME_CHROME[theme].shadow}; }` (do
    NOT add any `@page {...}` rule here — Paged.js's own base stylesheet already neutralizes the
    source `@page`, per RESEARCH.md's Anti-Patterns section; a competing `@page` rule here would
    reintroduce the double-margin bug); builds the script tag as
    `'<' + 'script>' + PAGED_PREVIEW_INLINE_SCRIPT + '<' + '/script>'` (same concatenation idiom as
    `printViaSystemBrowser`'s existing `autoPrint`, read above); and inserts both immediately before
    `</body>` if present (`actHtml.replace(/<\/body>/i, ...)`), else appends both to the end of the
    string — mirroring the existing `printViaSystemBrowser` fallback logic exactly.
  </action>
  <acceptance_criteria>
    - `bootstrapScript.js` contains zero `import`/`export` keywords (grep confirms).
    - `bootstrapScript.js` contains the literal substrings `trackly-pagedjs-progress`,
      `trackly-pagedjs-done`, `trackly-pagedjs-error`, `trackly-theme-update`.
    - `pagedPreviewBootstrap.ts` exports `buildSrcdoc`, `PAGED_PREVIEW_INLINE_SCRIPT`, and
      `THEME_CHROME` (grep for `export`).
    - The source contains the literal formula `pagedjsLibraryText + ';\n' + bootstrapText` (or
      the exact byte-equivalent — no extra characters between the two texts).
    - `buildSrcdoc`'s script-tag construction never contains a literal `</script>` substring in any
      single string/template literal (grep the file for `</script>` — it must not appear; only the
      split `'<' + '/script>'` form is allowed).
    - `buildSrcdoc`'s injected `<style>` block contains no `@page` rule (grep confirms).
    - `pnpm --dir ui check` (svelte-check + eslint + prettier + token/contrast scripts) passes with
      these two new files included.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui check</automated>
  </verify>
  <done>
    bootstrapScript.js and pagedPreviewBootstrap.ts exist, export the documented contract, and
    type-check/lint cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create the postMessage bridge and RU pluralization helper</name>
  <files>ui/src/lib/pdfPreview/pagedPreviewBridge.ts, ui/src/lib/utils/pluralize.ts</files>
  <read_first>
    .planning/phases/33-print-preview-polish/33-RESEARCH.md — "Pattern 2: postMessage bridge
    validation (C-01)" and "Pitfall 2: Assuming event.origin can validate the preview iframe's
    messages"
    ui/src/features/acts/PdfPreviewModal.svelte — lines 250-254 (`afterprint` listener + cleanup
    closure idiom — mirror this "attach + return teardown" shape)
    ui/src/lib/utils/date.ts — module shape and Russian-JSDoc comment convention for `ui/src/lib/utils/`
    .planning/phases/33-print-preview-polish/33-UI-SPEC.md — "Copywriting Contract" (page-counter
    format `"{N} страниц{а/ы/…}"`) and "Open Items for Planner/Executor" item 3 (check no existing
    pluralization helper before writing one — confirmed absent by PATTERNS.md's grep, do not
    re-grep, proceed to create it)
  </read_first>
  <action>
    Create `ui/src/lib/pdfPreview/pagedPreviewBridge.ts` exporting
    `export function attachBridge(iframeEl: HTMLIFrameElement, onMsg: (data: unknown) => void):
    () => void`. Internally: define a named `handler(e: MessageEvent)` that returns early unless
    `e.source === iframeEl.contentWindow` (object-identity check — NEVER `e.origin`, since an
    opaque-origin sender's `e.origin` is always the literal string `"null"` and carries no
    distinguishing information, per RESEARCH.md Pitfall 2), then calls `onMsg(e.data)`;
    `window.addEventListener('message', handler)`; return a cleanup closure that calls
    `window.removeEventListener('message', handler)` — same "attach + return teardown" shape as
    the existing `afterprint` cleanup pattern in `PdfPreviewModal.svelte`.

    Create `ui/src/lib/utils/pluralize.ts` (flat file, matching `date.ts`/`dropdownAnchor.ts`'s
    existing one-function-per-file convention in `ui/src/lib/utils/`) exporting
    `export function pluralizeRu(n: number, forms: [string, string, string]): string` implementing
    the standard Russian plural-agreement rule: let `mod10 = n % 10`, `mod100 = n % 100`; if
    `mod10 === 1 && mod100 !== 11` return `forms[0]`; else if `mod10 >= 2 && mod10 <= 4 && (mod100 <
    10 || mod100 >= 20)` return `forms[1]`; else return `forms[2]`. Add a top-of-file Russian JSDoc
    comment stating the purpose and the intended call shape for the page counter, e.g.
    `pluralizeRu(3, ['страница', 'страницы', 'страниц'])`.
  </action>
  <acceptance_criteria>
    - `pagedPreviewBridge.ts` exports `attachBridge`; its handler body contains
      `e.source === iframeEl.contentWindow` and does NOT contain any comparison against `e.origin`.
    - `attachBridge` returns a function (the cleanup closure) — grep confirms a `return () =>` or
      equivalent named-function return.
    - `pluralize.ts` exports `pluralizeRu` with the documented `mod10`/`mod100` branch structure.
    - By source inspection, the following hold for the implemented algorithm:
      `pluralizeRu(1, [...])` selects `forms[0]`; `pluralizeRu(2,[...])`, `pluralizeRu(3,[...])`,
      `pluralizeRu(4,[...])`, `pluralizeRu(22,[...])` select `forms[1]`; `pluralizeRu(5,[...])`,
      `pluralizeRu(11,[...])`, `pluralizeRu(12,[...])`, `pluralizeRu(20,[...])`,
      `pluralizeRu(21,[...])` — wait, 21 must select `forms[0]` (mod10=1, mod100=21≠11) — verify
      the 11-14 exception and the mod100 20-boundary are both handled per the rule above.
    - `pnpm --dir ui check` passes with both new files included.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui check</automated>
  </verify>
  <done>
    pagedPreviewBridge.ts and pluralize.ts exist, export the documented functions, and
    type-check/lint cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → `ui/node_modules` | New third-party dependency (`pagedjs`) enters the build via `pnpm install`. |
| `bootstrapScript.js` (static asset) → CSP hash-source (Plan 33-02) | This plan's frozen file content is the input to a security control implemented two plans later; a change here without updating that control silently reopens a gap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01-01 | Tampering | `pagedjs` npm dependency ingestion | accept | RESEARCH.md's Package Legitimacy Audit: npm-registry-confirmed `0.4.3`/MIT, `slopcheck install pagedjs --ecosystem npm` → `[OK]`, disposition Approved (not `[ASSUMED]`/`[SUS]`) — no blocking human-verify checkpoint required per the package-legitimacy gate. Version pinned exact `0.4.3` (not `^0.4.3`) specifically so Plan 33-02's CSP hash-source cannot silently drift on a routine `pnpm install`. |
| T-33-01-02 | Tampering | `ui/src/lib/pdfPreview/bootstrapScript.js` (static inline-script source of truth) | mitigate | File is committed as plain, hand-authored static text with zero per-call interpolation (all per-render variance — the backdrop color — lives only in the `<style>` chrome block built by `buildSrcdoc`, which stays outside the CSP-hashed `<script>` boundary). This is the structural precondition for Plan 33-02's hash-drift guard to be meaningful; templating user/request data into this file would defeat both the hash mechanism and the CSP protection it enables. |
| T-33-01-03 | Information Disclosure | `pagedPreviewBridge.ts` message-bridge contract (defined here, wired in Plan 33-03) | accept | Message payloads by design are non-secret: page counts, a pixel height, and a CSS color hex string — no user/session data crosses this channel, documented here so Plan 33-03's wiring does not introduce anything beyond this shape. |
</threat_model>

<verification>
- `pnpm --dir ui build` succeeds after Task 1 (dependency resolves cleanly).
- `pnpm --dir ui check` (svelte-check + eslint + prettier + token/contrast gates) passes after
  Tasks 2 and 3.
- No runtime/visual verification applies to this plan — it creates but does not wire any new UI;
  visual/print fidelity checks belong to Plans 33-03/33-04 per VALIDATION.md.
</verification>

<success_criteria>
- `pagedjs` is an exact-pinned dependency and the build is unaffected.
- `buildSrcdoc`, `PAGED_PREVIEW_INLINE_SCRIPT`, `THEME_CHROME`, `attachBridge`, and `pluralizeRu`
  are all exported with the exact signatures documented above, ready for Plans 33-02/33-03/33-04
  to import without further design decisions.
</success_criteria>

<output>
Create `.planning/phases/33-print-preview-polish/33-01-SUMMARY.md` when done
</output>
</content>
