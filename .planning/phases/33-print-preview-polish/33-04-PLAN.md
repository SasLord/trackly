---
phase: 33-print-preview-polish
plan: 04
type: execute
wave: 3
depends_on: ["33-03"]
files_modified:
  - ui/src/features/acts/PdfPreviewModal.svelte
autonomous: true
requirements: [PRV-03]

must_haves:
  truths:
    - "Desktop print (printViaSystemBrowser) embeds the same PAGED_PREVIEW_INLINE_SCRIPT used for on-screen preview into the temp .html file and waits for Paged.js pagination to finish before calling window.print(), instead of firing on the load event — D-06, C-02, C-03"
    - "LAN print (printViaTopLevel) dynamically imports pagedjs and re-runs pagination against #act-print-root before calling window.print(), instead of directly injecting the unpaginated body markup — D-06"
    - "Both print branches print the already-paginated Paged.js markup, not the original unpaginated HTML flow, so screen preview and paper share one pagination engine — D-06"
  artifacts:
    - path: ui/src/features/acts/PdfPreviewModal.svelte
      provides: "Reworked print branches (printViaSystemBrowser, printViaTopLevel) — modified, not new"
  key_links:
    - from: "ui/src/features/acts/PdfPreviewModal.svelte printViaTopLevel"
      to: "pagedjs package"
      via: "dynamic import('pagedjs') — self-hosted ESM, not inline, no CSP hash needed"
      pattern: "import\\('pagedjs'\\)"
    - from: "ui/src/features/acts/PdfPreviewModal.svelte printViaSystemBrowser"
      to: "ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts"
      via: "PAGED_PREVIEW_INLINE_SCRIPT reuse (same frozen text, no CSP applies to file:// docs)"
      pattern: "PAGED_PREVIEW_INLINE_SCRIPT"
---

<objective>
Close the WYSIWYG gap (PRV-03, D-06): rework both print branches of `PdfPreviewModal.svelte` so
they print the SAME Paged.js-paginated output the user sees in the on-screen preview, instead of
relying on the browser's own native print pagination (which the pre-Phase-33 code implicitly
trusted). Desktop print reuses the frozen inline script from Plan 33-01 in a self-contained temp
file (C-02); LAN print dynamically imports the `pagedjs` package (no CSP change needed for this
path, per Plan 33-02's scoped hash-source decision).

Purpose: make "what you see in the modal" and "what comes out on paper" the same pagination
engine's output, closing PRV-03.
Output: `printViaSystemBrowser` and `printViaTopLevel` rewritten; existing `@media print`
visibility-scoping, `afterprint` cleanup, and `isTauri` branch structure preserved as-is.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-print-preview-polish/33-CONTEXT.md
@.planning/phases/33-print-preview-polish/33-RESEARCH.md

<interfaces>
<!-- From Plan 33-01 (already implemented on disk): -->
<!-- ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts: -->
<!--   export const PAGED_PREVIEW_INLINE_SCRIPT: string  (library + ';\n' + bootstrap, static text) -->
<!-- From Plan 33-03 (already implemented on disk — read the live file, not a summary): -->
<!--   ui/src/features/acts/PdfPreviewModal.svelte now has: isTauri imported from -->
<!--   '$lib/stores/transport.svelte'; handlePrint() branching to printViaSystemBrowser/ -->
<!--   printViaTopLevel exactly as before (Task 1/2/3 of Plan 33-03 did not touch these two -->
<!--   functions' bodies — this plan is the first to modify them). -->

<!-- pagedjs UMD API (RESEARCH.md, confirmed by direct source inspection of dist/paged.js): -->
<!--   window.Paged.Previewer — new Previewer() -->
<!--   Previewer.preview(content?, stylesheets?, renderTo?): Promise<Flow>  (Flow.total = page count) -->
<!--   previewer.chunker.on('renderedPage', cb) -->
<!-- ESM package export (for printViaTopLevel's dynamic import): -->
<!--   import('pagedjs') -> { Previewer } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop print via Paged.js (printViaSystemBrowser)</name>
  <files>ui/src/features/acts/PdfPreviewModal.svelte</files>
  <read_first>
    ui/src/features/acts/PdfPreviewModal.svelte — `printViaSystemBrowser` function (current lines
    181-199, as it stands after Plan 33-03's edits — re-read the live file; Plan 33-03 did not
    touch this function)
    ui/src/lib/pdfPreview/pagedPreviewBootstrap.ts (Plan 33-01 output — `PAGED_PREVIEW_INLINE_SCRIPT`)
    .planning/phases/33-print-preview-polish/33-CONTEXT.md — D-06, C-02, C-03
    .planning/phases/33-print-preview-polish/33-RESEARCH.md — Architecture Diagram, desktop print
    branch section ("bootstrap calls previewer.preview().then(() => window.print()) [C-03: wait
    for pagination, not load]")
  </read_first>
  <action>
    Add `import { PAGED_PREVIEW_INLINE_SCRIPT } from '$lib/pdfPreview/pagedPreviewBootstrap';` at
    the top of the `<script>` block. In `printViaSystemBrowser(html)`: remove the existing
    `autoPrint` variable and its `load`-event-triggered `window.print()` (C-03 — printing must
    wait for pagination to finish, not the document `load` event). Build TWO inline `<script>`
    tags using the SAME `'<' + 'script>'` / `'<' + '/script>'` concatenation idiom already used in
    this function (do not switch to a template literal containing a literal `</script>`
    anywhere): the first is `'<' + 'script>' + PAGED_PREVIEW_INLINE_SCRIPT + '<' + '/script>'`
    (reuses the exact frozen text from Plan 33-01 — bundles Paged.js + the bootstrap protocol into
    the temp file, satisfying C-02's self-contained/no-CDN requirement); the second is a small,
    print-trigger-only script:
    `'<' + 'script>window.addEventListener("message",function(e){if(e.source!==window)return;if(e.data&&e.data.type==="trackly-pagedjs-done"){setTimeout(function(){window.print()},100)}})<' + '/script>'`.
    This works without any modification to `bootstrapScript.js`: in a top-level `file://` document
    (not an iframe), `parent === window`, so `bootstrapScript.js`'s existing
    `parent.postMessage({type:'trackly-pagedjs-done', ...}, '*')` call dispatches the message to
    `window` itself, and this second listener (registered on the same `window`) receives it — a
    document can `postMessage` to itself. Concatenate both script tags after the existing HTML
    (before `</body>` if present, else appended — same fallback logic as the current code) and
    write the result to the temp file exactly as before (same `writeTextFile`/`openPath`/
    `tempDir`/`join` calls, unchanged). No CSP applies to this path — confirmed in RESEARCH.md:
    `tauri.conf.json` sets `"security": {"csp": null}` for the Tauri app itself, and Tauri does not
    control the *external* browser this temp file is opened in.
  </action>
  <acceptance_criteria>
    - `printViaSystemBrowser` no longer contains `addEventListener("load"`.
    - `printViaSystemBrowser` imports and reuses `PAGED_PREVIEW_INLINE_SCRIPT` rather than
      re-declaring the Paged.js bundle or bootstrap text inline.
    - Both appended `<script>` tags are built via the `'<' + 'script>'`/`'<' + '/script>'`
      concatenation idiom — grep the function body for a literal `</script>` substring inside any
      single string/template literal; it must not appear.
    - The print-trigger listener checks `e.source !== window` before acting (mirrors the parent's
      own bridge validation idiom from Plan 33-01, applied here in the self-messaging context) and
      matches on `e.data.type === "trackly-pagedjs-done"`.
    - `pnpm --dir ui check` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui check</automated>
  </verify>
  <done>
    Desktop print embeds the same Paged.js pagination used on-screen and waits for it to finish
    before invoking the system print dialog.
  </done>
</task>

<task type="auto">
  <name>Task 2: LAN print via Paged.js (printViaTopLevel)</name>
  <files>ui/src/features/acts/PdfPreviewModal.svelte</files>
  <read_first>
    ui/src/features/acts/PdfPreviewModal.svelte — `printViaTopLevel` function (current lines
    209-258, as it stands after Task 1 of this plan — re-read the live file)
    .planning/phases/33-print-preview-polish/33-RESEARCH.md — Architecture Diagram (LAN print
    branch: "import('pagedjs') [dynamic ESM, code-split, 'self'-hosted — NOT inline, so LAN CSP
    script-src is fine]"), "Open Questions" item 2 (`Polisher.add()` stylesheet-argument-shape
    uncertainty — read this before writing the call, it directly affects this task), Primary
    Sources (`Previewer.preview(content, stylesheets, renderTo)` signature, confirmed by direct
    inspection of `dist/paged.js`)
  </read_first>
  <action>
    Keep the existing `parsed = new DOMParser().parseFromString(html, 'text/html')`,
    `bodyHtml`/`styleHtml` extraction, `printRoot`/`printStyle` host-element creation, the
    `@media print { body > :not(#act-print-root) { display: none !important; } ... }`
    visibility-scoping `<style>` content, and the `afterprint` cleanup listener EXACTLY as they
    are — all still correct per PATTERNS.md and unrelated to this change. Change only how
    `printRoot`'s content is populated and when `window.print()` fires: replace the direct
    `printRoot.innerHTML = bodyHtml;` assignment with `const { Previewer } = await
    import('pagedjs');` (dynamic ESM import — code-split by Vite automatically since it's a
    dynamic `import()`; this is `'self'`-hosted by the app's own bundle, so it needs no CSP
    hash-source, unlike the opaque-origin preview iframe's inline script) followed by
    `const previewer = new Previewer(); await previewer.preview(bodyHtml, [styleHtml],
    printRoot);` — try this exact argument shape first (raw HTML content string, an array
    containing the raw `<style>...</style>` outerHTML string already produced by the existing
    `styleHtml` extraction, and the target DOM node), matching the confirmed
    `Previewer.preview(content, stylesheets, renderTo)` signature. RESEARCH.md flags this
    stylesheet-argument shape as unverified (Open Question 2) — if `Polisher.add()` does not
    correctly parse `styleHtml`'s `<style>`/`</style>` wrapper tags (visible symptom: pagination
    completes but the `@page` margins/fonts are not applied to `#act-print-root`'s printed output),
    strip the wrapper tags first and pass only the inner CSS text (`styleHtml.replace(/<\/?style[^>]*>/gi,
    '')`, mirroring the existing wrapper-stripping pattern already used elsewhere in this same
    function for `printStyle.textContent`) as the array element instead. Move the existing
    `window.focus(); window.print();` calls to execute only AFTER `await
    previewer.preview(...)` resolves (i.e., make the function's body `async` from this point down
    if it is not already, and place these two calls immediately after the `await` — not
    immediately after synchronous DOM injection as before), so printing waits for pagination
    exactly as C-03 requires for the desktop branch, applied here for the same underlying reason
    (D-06: printing must reflect the paginated result, not fire before it exists).
  </action>
  <acceptance_criteria>
    - `printViaTopLevel` contains `import('pagedjs')` as a dynamic import (not a static top-level
      `import` statement for this module).
    - The direct `printRoot.innerHTML = bodyHtml;` assignment is gone; `printRoot`'s paginated
      content comes from `previewer.preview(...)` targeting `printRoot` as `renderTo`.
    - `window.print()` executes only after `await previewer.preview(...)` resolves — grep confirms
      `window.print()` appears after the `preview(` call in source order within this function, not
      immediately after the `printRoot.innerHTML`/`printStyle.textContent` assignments.
    - The existing `@media print` visibility-scoping `<style>` content, the `afterprint` cleanup
      listener, and the `printRoot`/`printStyle` element creation/reuse logic are byte-identical to
      before this task (diff shows changes confined to the content-population + print-timing lines).
    - `pnpm --dir ui check` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui check</automated>
  </verify>
  <done>
    LAN print re-runs Paged.js against #act-print-root and waits for it before printing, using a
    self-hosted dynamic import that needs no CSP change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Desktop temp file (`tauri-plugin-fs` temp dir) ↔ OS default browser (`tauri-plugin-shell` open) | Pre-existing (Phase 16), now embeds a larger static script payload. |
| App top-level document ↔ dynamically-imported `pagedjs` module | New: `import('pagedjs')` runs in the app's own trusted top-level context, not sandboxed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04-01 | Tampering | Desktop temp file (`trackly-print-{timestamp}.html`) now also embeds the full `PAGED_PREVIEW_INLINE_SCRIPT` text | accept | Same pre-existing trust model as Phase 16: a `file://` document outside any CSP, single-user desktop app, short-lived temp file (system temp dir, timestamp-named, not reused), containing only server-rendered act/report HTML plus this phase's static pagination script — no secrets, no user session data. Embedding a larger static script changes the file's size, not its risk profile. |
| T-33-04-02 | Elevation of Privilege (scope minimization) | LAN print path (`printViaTopLevel`) loads Paged.js via `import('pagedjs')` (dynamic ESM, code-split, served from the app's own `'self'` origin) rather than an inline `<script>` | mitigate | No CSP `script-src` change is needed for this path — it is already covered by the existing `script-src 'self'` directive. This keeps Plan 33-02's CSP change surface minimal and confined to the one context (the opaque-origin `srcdoc` preview) that structurally requires an inline script; this plan deliberately does not introduce a second inline-script CSP dependency. |
| T-33-04-03 | Tampering (regression risk, accepted) | Re-running Paged.js independently in two contexts (preview iframe vs. `#act-print-root`) could in principle diverge in page-break points (RESEARCH.md Pitfall 4 / Assumption A2) | accept | Both invocations read the same `@page` (absolute mm units, D-01/D-13) from the same source HTML, so divergence is expected to be low-probability, but this is NOT proven by any automated test in this codebase (no frontend rendering harness exists — VALIDATION.md). This is explicitly called out as a required manual-UAT item rather than silently assumed correct; building a headless-browser determinism harness to close this gap is an infrastructure decision beyond this phase's scope (see `<planner_authority_limits>` — not something this plan can resolve by adding more code). |
</threat_model>

<verification>
- `pnpm --dir ui check` passes after every task.
- Manual (VALIDATION.md, required not optional — no automated proof is possible without a
  frontend/browser test harness):
  - Desktop: print a multi-device act and a long report from the Tauri app; confirm the system
    browser's print dialog opens only after pagination completes (visible page count matches the
    on-screen preview) and the printed/Saved-as-PDF output's page breaks match the preview.
  - LAN: same check from a real LAN browser against a `pnpm --dir ui build` + server-mode axum
    instance; additionally confirm no `Content-Security-Policy` console errors, since this task's
    dynamic `import('pagedjs')` must not trigger any (it needs no new CSP source).
</verification>

<success_criteria>
- Both print branches route through the same Paged.js pagination the on-screen preview uses,
  closing PRV-03's WYSIWYG requirement — subject to the manual UAT confirmation above, since no
  automated frontend harness exists in this project to prove pixel/page-break parity.
</success_criteria>

<output>
Create `.planning/phases/33-print-preview-polish/33-04-SUMMARY.md` when done
</output>
</content>
