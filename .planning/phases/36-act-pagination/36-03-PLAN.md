---
phase: 36-act-pagination
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - crates/trackly-app/tests/html_act_render.rs
  - crates/trackly-app/tests/pdf_render_act.rs
  - crates/trackly-app/tests/acts_e2e_smoke.rs
autonomous: true
requirements: [DOC-10, DOC-11]

must_haves:
  truths:
    - "extract_first_ul переименован/переписан в extract_first_ol, ищет открывающий тег ol с атрибутами (например ol class=\"device-summary\") — прежний литерал <ul> больше нигде в N>1-ветке не рендерится (D-07)."
    - "html_handover_single_device_renders_singular_intro_not_plural_summary дополнен ассерцией отсутствия appendix-table/класса appendix при N=1 — DOC-10 (SC#1) подтверждён негативной проверкой, не только позитивной."
    - "html_handover_multi_device_renders_plural_summary_listing_every_name переписан: сохраняет проверку 'один li на устройство', ЗАМЕНЯЕТ ассерцию 'каждый .device-block повторяет лейбл 3 раза' (D-02a Фазы 35) на ассерцию ПОЛНОГО отсутствия .device-block при N>1 — D-08."
    - "Новый тест подтверждает: appendix-таблица содержит один tbody.device-group на устройство, счётчик совпадает с act.items.len() — DOC-11."
    - "Новый тест подтверждает: столбец Кол-во печатает прочерк при quantity == 1 и числовое значение при quantity > 1 — D-03."
    - "Новый тест подтверждает: номер в ol совпадает с содержимым первого td (№) соответствующей строки appendix-таблицы — D-07."
    - "Новый структурный тест (образец html_page_parity.rs — include_str!, только чтение) подтверждает наличие break-before: page в правиле .appendix и break-inside: avoid в правиле .appendix-table tbody.device-group — регресс-гейт для D-15/D-16, дешёвая замена геометрической проверки."
    - "pdf_render_act.rs::render_handover_multi_device_fields_attributable_to_own_device переписан: сплит по литералу div class=\"device-block\" заменён на сплит по tbody class=\"device-group, сохранена та же структура 'своё поле присутствует / чужое отсутствует' для каждого устройства — D-08 не ломает намерение CR-01 (атрибуция), просто переносит её на новую разметку."
    - "pdf_render_act.rs::render_handover_multi_device_wraps_long_fields остаётся зелёным без изменения логики ассерций (нет многоточия, маркер СЕРЕДИНА-МАРКЕР-ЗНАЧЕНИЯ присутствует) — длинное поле теперь рендерится в device-subrow appendix-таблицы, но факт отсутствия усечения не зависит от того, где именно оно рендерится."
    - "acts_e2e_smoke.rs::handover_pdf_render_within_e2e (2 seeded devices, N=2 > 1) подтверждён зелёным против НОВОЙ разметки — комментарий над функцией обновлён, что сценарий с этой фазы проходит через appendix-ветку, а не через старый .device-block-поток."
    - "Полный TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1 зелёный после этого плана — тестовый дрейф закрыт полностью, не только в перечисленных выше функциях."
  artifacts:
    - path: "crates/trackly-app/tests/html_act_render.rs"
      provides: "extract_first_ol, обновлённые N=1/N>1-тесты, новые appendix-структурные тесты (row-count, quantity, ol<->№ соответствие, break-before/break-inside CSS-гейт)"
    - path: "crates/trackly-app/tests/pdf_render_act.rs"
      provides: "Атрибуция полей переписана под tbody.device-group; wraps_long_fields подтверждён без изменения логики"
    - path: "crates/trackly-app/tests/acts_e2e_smoke.rs"
      provides: "Актуальный комментарий, подтверждающий прохождение N>1-сценария через новую appendix-ветку"
  key_links:
    - from: "crates/trackly-app/tests/html_act_render.rs"
      to: "crates/trackly-app/templates/act_handover.html"
      via: "render_pdf возвращает HTML, тесты проверяют присутствие/отсутствие строк по markup из Плана 02"
      pattern: "html\\.contains"
    - from: "crates/trackly-app/tests/pdf_render_act.rs"
      to: "crates/trackly-app/templates/act_handover.html"
      via: "html.split(\"tbody class=\\\"device-group\") заменяет прежний сплит по device-block — та же per-device атрибуция на новой разметке"
      pattern: "device-group"
---

<objective>
Закрыть плановый тестовый дрейф (C-05/Pitfall 6 из RESEARCH.md), возникший от правок Плана 02:
переписать три существующих файла под новую N=1/N>1-разметку и добавить структурные тесты для
appendix-таблицы (row-count, quantity-логика, ol<->№ соответствие, CSS-гейт break-before/
break-inside), которых раньше не существовало.

Purpose: без этой правки `cargo test -p trackly-app` красный после Плана 02 — это ожидаемое,
задокументированное поведение (тесты писались вокруг одноуровневого потока, который D-07/D-08
намеренно заменяют), но плановое, а не забытое (та же ловушка уже кусала Фазы 15/34/35).
Output: три обновлённых тестовых файла + новые appendix-тесты; полный сьют зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-act-pagination/36-CONTEXT.md
@.planning/phases/36-act-pagination/36-RESEARCH.md
@.planning/phases/36-act-pagination/36-PATTERNS.md
@.planning/phases/36-act-pagination/36-VALIDATION.md
</context>

<interfaces>
Образец для нового структурного CSS-гейта — crates/trackly-app/tests/html_page_parity.rs (49
строк, читается целиком в Task 1's read_first): const ... = include_str!("../templates/act_handover.html")
на верхнем уровне файла + функция-хелпер, извлекающая регексом нужный фрагмент, + #[test] с
assert!/assert_eq! и информативным сообщением при провале. Тот же приём копируется, меняются
только извлекаемый селектор (.appendix и .appendix-table tbody.device-group) и условие (наличие
break-before: page / break-inside: avoid внутри тела правила, а не byte-identity между файлами).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: html_act_render.rs — extract_first_ol, N=1/N>1-тесты, appendix-структурные тесты</name>
  <files>crates/trackly-app/tests/html_act_render.rs</files>
  <read_first>
    crates/trackly-app/tests/html_act_render.rs строки 189-302 (extract_first_ul, html_handover_single_device_renders_singular_intro_not_plural_summary, html_handover_multi_device_renders_plural_summary_listing_every_name)
    crates/trackly-app/templates/act_handover.html (после Плана 02 — актуальная разметка ol.device-summary и appendix-table)
    crates/trackly-app/tests/html_page_parity.rs (целиком — образец include_str! + regex-извлечение + assert для нового структурного гейта)
  </read_first>
  <action>
    Переименовать extract_first_ul (строки 189-201) в extract_first_ol; заменить поиск литерала
    "<ul>" на поиск открывающего тега с атрибутом — искать подстроку "<ol class=" (начало тега),
    затем от найденной позиции искать первый ">" как конец открывающего тега, затем "</ol>" как
    закрывающий маркер; сохранить панику с информативным сообщением при отсутствии любого из
    маркеров, как в оригинале. В html_handover_single_device_renders_singular_intro_not_plural_summary
    (строки 210-241) — заменить ассерцию "!html.contains(\"<ul>\")" на "!html.contains(\"<ol class=\")"
    и добавить новую ассерцию "!html.contains(\"appendix-table\")" с сообщением, что N=1 не должен
    рендерить appendix-раздел вообще (DOC-10 SC#1). В
    html_handover_multi_device_renders_plural_summary_listing_every_name (строки 250-302) —
    заменить extract_first_ul на extract_first_ol и "<li>{name}</li>"-цикл адаптировать без
    изменения намерения (3 устройства, один li на каждое); УДАЛИТЬ (не переписать) финальный блок
    ассерций "each of the 3 .device-block sections must keep its own singular label" (строки
    ~278-301, включая "было получено устройство:".count() == 3-проверку) — при N>1 .device-block
    больше не рендерится вовсе (D-08), эта ассерция теперь всегда ложна по построению. Вместо неё
    добавить: (a) assert!(!html.contains("<div class=\"device-block\">")) — .device-block
    полностью отсутствует при N>1; (b) новый тест
    html_handover_appendix_table_has_one_row_group_per_device — 3 устройства, подсчитать
    html.matches("<tbody class=\"device-group").count() == 3; (c) новый тест
    html_handover_appendix_ol_numbering_matches_table_number_column — для каждого i в 0..3
    извлечь li через extract_first_ol и подтвердить, что i+1-й <li> соответствует порядку
    act.items, а appendix-таблица содержит <td>{i+1}</td> как первую ячейку i-й
    tbody.device-group-группы (сверка через loop.index, соответствие индекс-в-списке == номер
    в таблице); (d) новый тест html_handover_appendix_quantity_column_dash_at_one_value_at_more
    — создать акт с 2 устройствами, одно с quantity=1 (через штатный ActItemNewDto с quantity: 1),
    убедиться что для этого устройства колонка Кол-во содержит "—", и с фикстурой quantity>1
    (или явным вторым тестовым сценарием) — что печатается числовое значение. Новый структурный
    тест html_handover_appendix_css_declares_forced_break_and_keep_together (по образцу
    html_page_parity.rs, include_str! act_handover.html, regex-извлечение <style>-блока и тела
    правил .appendix и .appendix-table tbody.device-group) подтверждает вхождение "break-before:
    page" в первом и "break-inside: avoid" во втором.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_act_render -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Все тесты в html_act_render.rs зелёные, включая переименованную extract_first_ol и 4 новых теста, перечисленных в action
    - html_handover_single_device_renders_singular_intro_not_plural_summary содержит ассерцию отсутствия "appendix-table" при N=1
    - Ни одна из оставшихся функций файла не содержит литерал "<ul>" применительно к N>1-сводке
    - grep -c "device-block" в тестовом файле для multi-device теста показывает только негативную проверку (assert!(!html.contains(...)))
  </acceptance_criteria>
  <done>extract_first_ol заменяет extract_first_ul; N=1/N>1-тесты отражают новую разметку; 4 новых appendix-структурных теста существуют и зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: pdf_render_act.rs — атрибуция полей на tbody.device-group, подтверждение wraps_long_fields</name>
  <files>crates/trackly-app/tests/pdf_render_act.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_render_act.rs строки 278-460 (render_handover_multi_device_wraps_long_fields, render_handover_multi_device_fields_attributable_to_own_device)
    crates/trackly-app/templates/act_handover.html (после Плана 02 — актуальная разметка tbody.device-group и device-subrow)
    .planning/phases/36-act-pagination/36-PATTERNS.md (секция tests/pdf_render_act.rs)
  </read_first>
  <action>
    В render_handover_multi_device_fields_attributable_to_own_device (строки 354-460, использует 3
    устройства: device 0 получает inventory_no, device 1 — kit, device 2 — ничего) — заменить
    "html.split(\"<div class=\\\"device-block\\\">\")" (строка 400) на
    "html.split(\"<tbody class=\\\"device-group\")" (частичное совпадение префикса класса — тег
    выглядит как <tbody class="device-group row-even"> или row-odd, split по префиксу работает
    без учёта суффикса). Обновить assert_eq!(parts.len(), ...) — при сплите по tbody-маркеру
    первый элемент parts[0] это преамбула ДО таблицы (включая ol-перечень и хвост первого листа
    целиком), затем по одному элементу на каждый tbody — итого parts.len() == 4 (1 преамбула + 3
    группы), тот же счётчик, что и раньше, но по новому маркеру. Сохранить остальную структуру
    цикла (для каждого i из 0..3 — parts[i+1] содержит своё имя/своё поле, НЕ содержит чужие имена/
    поля) без изменения намерения — только источник parts меняется. render_handover_multi_device_wraps_long_fields
    (строки 278-341, 5 устройств, длинное значение kit на 2 из них) — перечитать после Плана 02 и
    подтвердить, что ассерции "!html.contains('…')" и "html.contains(\"СЕРЕДИНА-МАРКЕР-ЗНАЧЕНИЯ\")"
    не завязаны на .device-block-селектор (они проверяют html целиком через .contains, не через
    split) — обновить только doc-комментарий функции, отметив, что длинное значение теперь
    рендерится в device-subrow appendix-таблицы (colspan="7"), а не в .device-block; логику
    ассерций не менять, если тест уже проходит без правок.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test pdf_render_act -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Все тесты в pdf_render_act.rs зелёные
    - render_handover_multi_device_fields_attributable_to_own_device использует "tbody class=\"device-group" как маркер сплита, не "device-block"
    - render_handover_multi_device_wraps_long_fields зелёный, doc-комментарий обновлён (упоминает device-subrow вместо device-block)
    - render_handover_act_contains_d09_intro_phrase (D-01 Фазы 35, не в зоне этой фазы) байт-в-байт не изменён
  </acceptance_criteria>
  <done>pdf_render_act.rs зелёный целиком; атрибуция полей переписана под tbody.device-group; длинные поля подтверждены без усечения в новой позиции.</done>
</task>

<task type="auto">
  <name>Task 3: acts_e2e_smoke.rs комментарий + полный сьют workspace-таргета trackly-app</name>
  <files>crates/trackly-app/tests/acts_e2e_smoke.rs</files>
  <read_first>
    crates/trackly-app/tests/acts_e2e_smoke.rs строки 255-295 (handover_pdf_render_within_e2e — seed_devices(&p.writer, 2), ассерции html.len() > 1000 / "<html" / "Петров")
    .planning/phases/36-act-pagination/36-VALIDATION.md (§Sampling Rate, §Wave 0 Requirements)
  </read_first>
  <action>
    В handover_pdf_render_within_e2e (строки 259-295, текущий HEAD ДО этого плана) — обновить
    комментарий над функцией (строки 10-11 doc-comment в шапке файла): отметить, что с этой фазы
    2-device act (seed_devices(&p.writer, 2)) рендерится через N>1-appendix-ветку (D-01..D-16), а
    не через старый двойной .device-block-поток; ассерции html.len() > 1000 /
    html.to_lowercase().contains("<html") / html.contains("Петров") НЕ менять — они инвариантны к
    ветвлению (receiver_name всегда в интро, appendix-таблица только увеличивает html.len()).
    Запустить полный TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --
    --test-threads=1 (требует предварительного pnpm --dir ui build, если ui/dist ещё placeholder)
    — НЕ cargo test --workspace (виснет на pre-existing login_remember_persistent_cookie), НЕ
    параллельно с другим cargo test. Если full-suite прогон обнаруживает остаточный дрейф вне
    Task 1/Task 2 (например html_header_parity.rs или другой ранее не enumerated тест) — исправить
    точечно в этом же task'е, отметив находку в acceptance_criteria/SUMMARY, а не молча пропустить.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Комментарий над handover_pdf_render_within_e2e обновлён, три существующие ассерции (len/contains "<html"/contains "Петров") не изменены
    - Полный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1` зелёный (0 failed)
    - `pnpm --dir ui lint` зелёный (html_page_parity не через lint, а через cargo test — уже покрыт Планом 02/Task 1 этого плана; здесь фиксируется, что print-isolation/check-tokens/check-contrast/check-focus-outline не пострадали от новых CSS-классов appendix-table)
  </acceptance_criteria>
  <done>acts_e2e_smoke.rs комментарий актуален; весь cargo test -p trackly-app зелёный; pnpm --dir ui lint зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Тестовый код → публичный git-репозиторий | Все имена/значения в новых/изменённых ассерциях — уже существующие в кодовой базе вымышленные фикстуры ("Ноутбук-{i}", "Иванов И.И." и т.п.) — новых литералов с потенциально реальными данными не вводится |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Repudiation | Удалённая ассерция "each of the 3 .device-block sections must keep its own singular label" (Task 1) | mitigate | Заменяется явной новой ассерцией (полное отсутствие .device-block при N>1 + row-count/attribution-тесты на новой разметке) — покрытие атрибуции не уменьшается, а переносится на appendix-таблицу |
| T-36-07 | Tampering | html_handover_appendix_css_declares_forced_break_and_keep_together (новый файл-раздел, только чтение) | accept | Тест только читает act_handover.html через include_str! (compile-time), не модифицирует ничего — риска нет по построению, аналогично html_page_parity.rs |
</threat_model>

<verification>
Финальная проверка плана: `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1` — весь сьют зелёный (требует `pnpm --dir ui build` заранее). Не запускать `cargo test --workspace` и не запускать два `cargo test` параллельно.
</verification>

<success_criteria>
- Три перечисленных тестовых файла отражают markup из Плана 02, не старое одноуровневое поведение.
- Новые appendix-структурные тесты (row-count, quantity, ol<->№ соответствие, break-before/break-inside CSS-гейт) существуют и зелёные.
- `cargo test -p trackly-app -- --test-threads=1` зелёный целиком.
</success_criteria>

<output>
Create `.planning/phases/36-act-pagination/36-03-SUMMARY.md` when done
</output>
