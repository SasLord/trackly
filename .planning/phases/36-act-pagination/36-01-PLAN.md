---
phase: 36-act-pagination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/templates/_legacy_defaults/v24/act_handover.html
  - crates/trackly-app/src/pdf/html_templates.rs
autonomous: true
requirements: [DOC-10, DOC-11]

must_haves:
  truths:
    - "Срез _legacy_defaults/v24/act_handover.html снят С ТЕКУЩЕГО HEAD (пост-Фаза-35 тело, ДО правок пагинации Плана 02) — Pitfall 7/C-01, ловушка снятия среза ПОСЛЕ правки уже кусала Фазы 34/35 и quick 260704-uw3."
    - "v24-срез зарегистрирован в KNOWN_LEGACY_DEFAULTS как пятый элемент (индекс 4) слайса act_handover.html, следом за v20/v21/v22/v23; act_acceptance.html в этой фазе не меняется, значит v24-слайса для него нет (C-01)."
    - "Новый тест upgrade_replaces_v24_legacy_default_with_current_bundled_body мирроит upgrade_replaces_v23_...-тест 1:1, включая assert_ne! precondition-гвард (v24_body != current) — без него тест мог бы пройти тривиально, ничего не доказывая."
  artifacts:
    - path: "crates/trackly-app/templates/_legacy_defaults/v24/act_handover.html"
      provides: "Байт-в-байт снимок act_handover.html на начало Фазы 36 (пост-Фаза-35 тело)"
    - path: "crates/trackly-app/src/pdf/html_templates.rs"
      provides: "KNOWN_LEGACY_DEFAULTS расширен пятым элементом (v24) для act_handover.html + регрессионный тест upgrade_replaces_v24_..."
      contains: "upgrade_replaces_v24_legacy_default_with_current_bundled_body"
  key_links:
    - from: "crates/trackly-app/src/pdf/html_templates.rs"
      to: "crates/trackly-app/templates/_legacy_defaults/v24/act_handover.html"
      via: "include_str! (compile-time) встраивает v24-срез как bodies.get(4) в слайсе KNOWN_LEGACY_DEFAULTS для \"act_handover.html\""
      pattern: "_legacy_defaults/v24/act_handover\\.html"
---

<objective>
Снять срез `_legacy_defaults/v24/act_handover.html` с текущего HEAD (состояние ПОСЛЕ Фазы 35,
ДО правок пагинации этой фазы), зарегистрировать его в `KNOWN_LEGACY_DEFAULTS` и добавить
регрессионный тест, доказывающий, что срез реально драйвит апгрейд уже установленных копий.

Purpose: без этого шага — выполненного ПЕРВЫМ, до любой правки `act_handover.html` — уже
установленные копии Trackly не получат новую пагинацию после обновления и увидят её только на
свежих установках (Pitfall 7, C-01). Это первая по сложности ловушка, которая уже трижды кусала
проект (Фазы 34/35, quick `260704-uw3`) — секвенирование этого плана в Wave 1 (до Плана 02)
структурно исключает повтор.
Output: пятый элемент слайса `act_handover.html` в `KNOWN_LEGACY_DEFAULTS`, новый тест
`upgrade_replaces_v24_legacy_default_with_current_bundled_body` зелёный.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-act-pagination/36-CONTEXT.md
@.planning/phases/36-act-pagination/36-RESEARCH.md
@.planning/phases/36-act-pagination/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Снять срез v24 и зарегистрировать в KNOWN_LEGACY_DEFAULTS</name>
  <files>crates/trackly-app/templates/_legacy_defaults/v24/act_handover.html, crates/trackly-app/src/pdf/html_templates.rs</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (текущий HEAD целиком, 195 строк — это ровно то, что копируется в v24, БЕЗ каких-либо изменений)
    crates/trackly-app/templates/_legacy_defaults/v23/act_handover.html (сосед — подтверждает, что срез это чистая копия, без трансформации)
    crates/trackly-app/src/pdf/html_templates.rs строки 75-107 (KNOWN_LEGACY_DEFAULTS — текущий слайс act_handover.html с 4 элементами v20..v23)
  </read_first>
  <action>
    Создать каталог `crates/trackly-app/templates/_legacy_defaults/v24/` и записать в
    `act_handover.html` внутри него байт-в-байт копию текущего
    `crates/trackly-app/templates/act_handover.html` (195 строк текущего HEAD, ДО любой правки
    пагинации — этот план НЕ трогает живой шаблон). Затем в `html_templates.rs` внутри
    `KNOWN_LEGACY_DEFAULTS` (строки 75-84, слайс `"act_handover.html"`) добавить пятым элементом
    `include_str!("../../templates/_legacy_defaults/v24/act_handover.html")` следом за
    `.../v23/act_handover.html`. Слайс `"act_acceptance.html"` (строки 86-93) НЕ трогать — файл
    в этой фазе не меняется, v24-среза для него нет (C-01, подтверждено CONTEXT.md/RESEARCH.md).
  </action>
  <verify>
    <automated>diff crates/trackly-app/templates/act_handover.html crates/trackly-app/templates/_legacy_defaults/v24/act_handover.html</automated>
  </verify>
  <acceptance_criteria>
    - `diff` между живым `act_handover.html` и новым `_legacy_defaults/v24/act_handover.html` не выводит ничего (файлы байт-в-байт идентичны на момент создания)
    - `grep -c "_legacy_defaults/v24/act_handover.html" crates/trackly-app/src/pdf/html_templates.rs` выводит 1
    - слайс `"act_acceptance.html"` в `KNOWN_LEGACY_DEFAULTS` остаётся из 4 элементов (v20..v23), без нового v24
    - `cargo check -p trackly-app` компилируется без ошибок (include_str! путь валиден)
  </acceptance_criteria>
  <done>v24-срез создан как точная копия текущего HEAD и зарегистрирован пятым элементом слайса act_handover.html в KNOWN_LEGACY_DEFAULTS; act_acceptance.html не тронут.</done>
</task>

<task type="auto">
  <name>Task 2: Регрессионный тест upgrade_replaces_v24_...</name>
  <files>crates/trackly-app/src/pdf/html_templates.rs</files>
  <read_first>
    crates/trackly-app/src/pdf/html_templates.rs строки 536-585 (upgrade_replaces_v23_legacy_default_with_current_bundled_body — точный образец для копирования, включая assert_ne! precondition-гвард)
    crates/trackly-app/src/pdf/html_templates.rs строки 478-527 (upgrade_replaces_v22_..., второй референс той же схемы — подтверждает индекс-конвенцию)
  </read_first>
  <action>
    Добавить в `#[cfg(test)] mod tests` (после `upgrade_replaces_v23_legacy_default_with_current_bundled_body`,
    строка ~585) новую функцию `upgrade_replaces_v24_legacy_default_with_current_bundled_body`,
    мирроящую структуру v23-теста 1:1 с единственной заменой: везде `v23`→`v24` и
    `bodies.get(3)`→`bodies.get(4)`, `bodies.len() > 3`→`bodies.len() > 4`. Обязательно сохранить
    `assert_ne!(v24_body, current, ...)` precondition-гвард ПЕРЕД записью файла в tempdir — без
    него тест не доказывает реальный апгрейд (Pitfall 7). Doc-комментарий над функцией — по образцу
    v23-теста, с явной ссылкой на Фазу 36/D-13-D-16 вместо Фазы 35.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib pdf::html_templates -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --lib pdf::html_templates::tests::upgrade_replaces_v24_legacy_default_with_current_bundled_body` проходит зелёным
    - Функция содержит `assert_ne!(v24_body, current, ...)` ДО `std::fs::write` (precondition-гвард на месте)
    - Весь модуль `pdf::html_templates` (`cargo test -p trackly-app --lib pdf::html_templates`) зелёный, включая уже существующие v20/v21/v22/v23-тесты и `every_default_template_has_a_known_legacy_defaults_entry`
  </acceptance_criteria>
  <done>Новый регрессионный тест доказывает, что v24-срез реально драйвит апгрейд установленной копии на текущий bundled body; весь модуль html_templates зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_legacy_defaults/v24/act_handover.html` (compile-time `include_str!`) → бинарник | Статичный файл из репозитория, встраивается на этапе компиляции — не runtime-ввод, не пользовательские данные |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | `KNOWN_LEGACY_DEFAULTS` регистр (структурное определение "untouched" через byte-match) | accept | Механизм не меняется этим планом — только добавляется один элемент по уже проверенному (4 предыдущих версии) паттерну; fail-closed поведение (пользовательская правка никогда не перезаписывается) остаётся неизменным |
</threat_model>

<verification>
`TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib pdf::html_templates -- --test-threads=1` — весь модуль зелёный, включая новый v24-тест.
</verification>

<success_criteria>
- `_legacy_defaults/v24/act_handover.html` — точная копия HEAD на начало фазы, снята ДО правки живого шаблона.
- `KNOWN_LEGACY_DEFAULTS` содержит пятый элемент (v24) для `act_handover.html`; `act_acceptance.html` не тронут.
- Новый регрессионный тест `upgrade_replaces_v24_...` зелёный, с precondition-гвардом.
</success_criteria>

<output>
Create `.planning/phases/36-act-pagination/36-01-SUMMARY.md` when done
</output>
