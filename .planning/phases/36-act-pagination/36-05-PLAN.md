---
phase: 36-act-pagination
plan: 05
type: execute
wave: 4
depends_on: ["36-03", "36-04"]
files_modified: []
autonomous: false
requirements: [DOC-10, DOC-11]

must_haves:
  truths:
    - "Приватность проверена ДО закрытия фазы: diff всех файлов, изменённых Планами 01-04, не содержит реальных ФИО сотрудников или реквизитов организации — только уже утверждённые вымышленные значения — C-08."
    - "pnpm --dir ui build выполнен, чтобы LAN-браузерный транспорт раздавал актуальный ui/dist, а не устаревший placeholder — Pitfall 4/дерево C-04."
    - "Полный TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1 зелёный как финальный phase gate."
    - "pnpm --dir ui lint зелёный целиком, включая html_page_parity (через cargo test), check-print-isolation.mjs и check-pagedjs-csp-hash.mjs."
    - "Акт на одно устройство целиком умещается на одном листе с полным описанием — подтверждено живым рендером на обоих транспортах, не тестом извлечения текста — DOC-10 (SC#1)."
    - "Акт на несколько устройств выводит на первом листе только перечень имён и отсылку к «Приложению №1», без полного описания на этом листе — подтверждено живым рендером — DOC-11 (SC#2)."
    - "Зебра appendix-таблицы и print-color-adjust: exact переживают РЕАЛЬНУЮ печать/сохранение в PDF (не только предпросмотр в iframe) на обоих транспортах — D-04/D-05."
    - "Акт на одно устройство, не влезший в лист по объёму текста, спокойно перетекает на второй без усечения — подтверждено живым рендером с длинной фикстурой — D-14."
    - "Печать многостраничного акта на обоих транспортах не ломает изоляцию печатного DOM — durable-гейт ui/scripts/check-print-isolation.mjs остаётся зелёным — Критерий #4 фазы."
    - "Windows/WebView2-прогон зафиксирован как отложенный pre-close UAT-пункт для пользователя — dev-машина только macOS."
  artifacts: []
  key_links: []
---

<objective>
Закрыть фазу: privacy-грep перед финализацией (C-08), финальный автоматический phase gate (полный
сьют + lint), и обязательная ручная визуальная проверка вёрстки пагинации на обоих транспортах —
единственный способ подтвердить DOC-10/DOC-11 (text-extraction тесты не видят геометрию, разрывы
страниц или реальную печать фона).

Purpose: Критерии #1-#4 фазы («рендер настоящего PDF на обоих транспортах», «durable-гейт изоляции
печати остаётся зелёным») требуют человеческого подтверждения, не автоматизируются полностью —
Планы 01-04 закрывают код и автоматические тесты, этот план закрывает финальную верификацию.
Output: privacy-грep выполнен и чист; полный сьют и lint зелёные; фаза подтверждена визуально на
десктопе и в LAN-браузере по всем четырём критериям ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-act-pagination/36-CONTEXT.md
@.planning/phases/36-act-pagination/36-RESEARCH.md
@.planning/phases/36-act-pagination/36-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Privacy-грep + финальный phase gate (полный сьют + lint)</name>
  <files>Нет изменяемых файлов — операционные действия (grep/build/test) над файлами, изменёнными в Планах 01-04</files>
  <read_first>
    CLAUDE.md (раздел Constraints — жёсткое условие приватности)
    scripts/check-privacy-requisites.sh (существующий гейт org-реквизитов — не покрывает ФИО, только inn/kpp/okpo/ogrn/phone/fax)
    .planning/phases/36-act-pagination/36-CONTEXT.md (раздел C-08)
  </read_first>
  <action>
    Выполнить `git status`/`git diff` по всем файлам, изменённым в Планах 01-04 этой фазы
    (_legacy_defaults/v24/act_handover.html, html_templates.rs, act_handover.html,
    html_act_render.rs, pdf_render_act.rs, acts_e2e_smoke.rs, bootstrapScript.js,
    PdfPreviewModal.svelte, http/mod.rs), и вручную просмотреть diff на предмет реальных ФИО или
    реквизитов организации — допустимы только уже утверждённые вымышленные значения, используемые
    в этой кодовой базе (например «Иванов И.И.», «Петров П.П.», «Ноутбук-{i}»-фикстуры). Запустить
    `./scripts/check-privacy-requisites.sh` и убедиться, что он зелёный — он не покрывает ФИО, но
    подтверждает отсутствие новых org-реквизитных литералов. Затем выполнить `pnpm --dir ui build`
    (обновляет `ui/dist` для LAN-транспорта — без этого `security_headers`-тест и живой
    LAN-предпросмотр видят устаревшую сборку), финальный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1
    cargo test -p trackly-app -- --test-threads=1` (не запускать `cargo test --workspace` — виснет
    на auth_remember_cookie; не запускать параллельно с другим cargo test) и `pnpm --dir ui lint`
    целиком (включает check-pagedjs-csp-hash.mjs и check-print-isolation.mjs — оба должны быть
    зелёными после Плана 04).
  </action>
  <verify>
    <automated>./scripts/check-privacy-requisites.sh && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/check-privacy-requisites.sh` завершается с кодом 0
    - Ручной просмотр diff не находит строк, похожих на реальные ФИО/реквизиты, вне утверждённого списка вымышленных значений
    - `pnpm --dir ui build` завершается без ошибок, `ui/dist` содержит свежую сборку
    - Полный `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1` зелёный (0 failed)
    - `pnpm --dir ui lint` зелёный целиком, включая `check-pagedjs-csp-hash.mjs` и `check-print-isolation.mjs`
  </acceptance_criteria>
  <done>Privacy-грep чист, полный автоматический сьют и lint зелёные — фаза готова к ручной UAT-проверке.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Ручная визуальная UAT-проверка пагинации на обоих транспортах</name>
  <files>Нет изменяемых файлов — визуальная верификация Критериев #1-#4 фазы</files>
  <read_first>
    .planning/phases/36-act-pagination/36-VALIDATION.md (раздел Manual-Only Verifications — полный перечень пунктов)
  </read_first>
  <action>Провести блокирующую человеческую проверку DOC-10/DOC-11/Критерия #4 фазы на десктопе и в LAN-браузере по инструкции ниже; не авто-одобрять (gate=blocking) — геометрия страниц и реальная печать фона не видны text-extraction тестам.</action>
  <what-built>
    Ветвление act_handover.html по количеству устройств (Планы 01-03): N=1 — один лист с полным
    описанием (без изменений); N>1 — перечень + отсылка на первом листе, appendix-таблица с
    зеброй/прочерками/quantity-логикой и повторяющимся thead (План 04) со второго листа. Доставка
    в уже установленные копии через legacy-defaults v24.
  </what-built>
  <how-to-verify>
    Десктоп: запустить `cargo tauri dev`, создать акт приёма-передачи на ОДНО устройство с полным
    набором полей (инв.№, серийный №, модель, комплектация, тех. характеристики, состояние),
    открыть предпросмотр — убедиться, что весь акт умещается на одном листе вместе с полным
    описанием устройства (DOC-10 SC#1). Создать акт на 2-3 устройства — убедиться, что первый лист
    содержит ТОЛЬКО нумерованный перечень имён и строку-отсылку к «Приложению №1», БЕЗ полного
    описания какого-либо устройства (DOC-11 SC#2); со второго листа — таблица-опись со всеми
    полями (DOC-11 SC#3). Создать акт на ОДНО устройство с очень длинными значениями полей
    (например длинная строка в «Тех. характеристики»), убедиться, что акт спокойно перетекает на
    второй лист без усечения текста и без превращения в форму приложения (D-14). Выполнить реальную
    печать/«Сохранить как PDF» (не только предпросмотр в модальном окне) для многоустройственного
    акта — убедиться, что зебра appendix-таблицы видна в напечатанном/сохранённом результате при
    настройке диалога печати по умолчанию (D-04/D-05). Убедиться, что шапка организации
    (_header.html) НЕ повторяется на appendix-листах (D-11).

    LAN-браузер: запустить сервер-режим (после `pnpm --dir ui build` из Task 1), открыть
    предпросмотр тех же трёх актов (N=1, N=2-3, N=1-с-длинным-полем) в браузере из локальной сети —
    повторить все проверки выше. Выполнить реальную печать многостраничного акта — убедиться, что
    печатный DOM не протекает посторонним содержимым приложения (Критерий #4 фазы,
    check-print-isolation.mjs уже подтверждён структурно в Task 1, здесь — визуальное
    подтверждение).

    Отметить в резюме, что реальный прогон на Windows/WebView2 остаётся отложенным pre-close
    UAT-пунктом для пользователя (dev-машина только macOS).

    🔒 При описании результата НЕ указывать реальные ФИО/реквизиты — обезличивать.
  </how-to-verify>
  <resume-signal>Опишите результат: "approved" (все четыре критерия фазы подтверждены на обоих транспортах) или опишите конкретные расхождения — обезличенно, без реальных ФИО/реквизитов.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Diff фазы → публичный git-репозиторий | Последняя точка контроля перед закрытием фазы — единственный durable способ не допустить утечку реальных данных, т.к. закоммиченное остаётся в истории git даже после удаления из HEAD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-SC | Information Disclosure | Diff всех файлов фазы (Планы 01-04) перед закрытием | mitigate | Ручной privacy-грep + `check-privacy-requisites.sh` + явная сверка со списком утверждённых вымышленных значений (Task 1) — blocking-проверка перед закрытием фазы |
| T-36-08 | Information Disclosure | Ручной UAT-отчёт (Task 2 resume-signal) | mitigate | Инструкция явно требует обезличенного описания результата — реальные ФИО/реквизиты не должны попасть в текст ответа пользователя или в SUMMARY |
</threat_model>

<verification>
`TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app -- --test-threads=1` зелёный + `pnpm --dir ui lint` зелёный (Task 1) + человеческое подтверждение всех четырёх критериев фазы на обоих транспортах (Task 2) — это финальный gate фазы 36 перед `/gsd-verify-work`.
</verification>

<success_criteria>
- Privacy-грep чист, ни одного реального ФИО/реквизита не найдено в diff фазы.
- Полный автоматический сьют и `pnpm --dir ui lint` зелёные.
- Пользователь подтвердил все четыре критерия успеха фазы визуально на десктопе и в LAN-браузере.
- Windows/WebView2-прогон зафиксирован как отложенный пункт.
</success_criteria>

<output>
Create `.planning/phases/36-act-pagination/36-05-SUMMARY.md` when done
</output>
