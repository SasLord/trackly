---
phase: 36-act-pagination
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - crates/trackly-app/templates/act_handover.html
autonomous: true
requirements: [DOC-10, DOC-11]

must_haves:
  truths:
    - "N=1 остаётся байт-в-байт прежним потоком: .device-block-цикл с безусловным лейблом «было получено устройство: {{ item.name }}» рендерится ТОЛЬКО при act.items | length == 1 — D-08/DOC-10."
    - "При N>1 вместо .device-block-цикла на первом листе выводится нумерованный <ol class=\"device-summary\"> (один <li> на устройство, порядок = act.items) и следом field-row «Полное описание устройств — в Приложении №1.» — D-07."
    - "При N>1 .device-block не рендерится вообще (ни одного вхождения) — решение D-02a Фазы 35 замещено таблицей приложения, атрибуция описания устройству обеспечивается строкой таблицы, а не подзаголовком — D-08."
    - "Appendix-таблица содержит 7 колонок в порядке № / Наименование / Кол-во / Инв.№ / Серийный № / Модель / Состояние, портретная ориентация, @page не тронут — D-01/C-06."
    - "Каждое устройство — один <tbody class=\"device-group ...\"> с основной строкой и опциональной под-строкой colspan=\"7\" (Комплектация/Тех. характеристики), выводимой только если item.kit или item.specs непусты — D-01."
    - "Пустые значения инв.№/серийный №/модель/состояние заменяются прочерком «—» через фильтр | default(\"—\", true), тем же приёмом что в act_acceptance.html — D-02."
    - "Столбец «Кол-во» присутствует всегда; значение печатается только при item.quantity > 1, иначе «—» — D-03."
    - "Зебра (чередование фона по устройству через loop.cycle) без явных границ ячеек; print-color-adjust: exact и -webkit-print-color-adjust: exact обязательны на зебра-ячейках, иначе браузер/WebView2 выбросят фон при печати; кегль таблицы меньше основного текста (~10pt против 12pt) — D-04."
    - "Тонкая светло-серая волосяная линия под шапкой таблицы и между устройствами — страховка на случай, если фон-зебра не напечатается — D-05."
    - "Блок .signatures остаётся ровно там же (после хвоста первого листа, ДО appendix) и не дублируется в приложении — appendix это чистая таблица без подписей — D-06."
    - "Хвост первого листа («Сроком до», опциональный act.parent) не меняется и остаётся в текущем порядке; нигде не добавлена строка-итог «Всего устройств: N» — D-09."
    - "Отметка приложения — правый верхний угол, две строки (~10pt): «Приложение №1» / «к акту приема-передачи №{{ act.number }}{{ act.suffix }} от {{ act.date_human }}»; по центру листа — заголовок описи (например «Опись передаваемых устройств») — D-10."
    - "{% include _header.html %} остаётся ровно один раз, в самом начале body — appendix-блок его НЕ включает повторно — D-11."
    - "Нет @page-колонтитула с counter(page)/counter(pages) и нет статичного текста «Лист N» — D-12."
    - "Порог ветвления строго act.items | length > 1 (два устройства уже дают приложение) — используется идентично и для <ol>, и для appendix-блока — D-13."
    - "Никакой JS/Jinja-логики измерения высоты или принудительного обрезания длинных полей не добавлено — переполнение N=1 отдаётся Paged.js естественно, без усечения многоточием — D-14."
    - ".appendix-table tbody.device-group получает break-inside: avoid (не голый tr) — единственный код-путь, который Paged.js честно проверяет для TBODY/THEAD (RESEARCH.md Pattern 4, layout.js:582-589); повторение thead НЕ реализуется в этом плане, только группировка — D-15 (частично, thead-repeat — План 04)."
    - ".appendix { break-before: page } — разрыв перед приложением ВСЕГДА принудительный, безусловно, не «по месту на листе» — D-16."
    - "Doc-комментарий в шапке файла (было: строки 2-41) переписан: описывает обе ветки (N=1/N>1) и упоминает, что act.items[].quantity теперь читается шаблоном, а не просто присутствует в контексте — C-02."
  artifacts:
    - path: "crates/trackly-app/templates/act_handover.html"
      provides: "Ветвление N=1/N>1: неизменный одноустройственный поток + appendix-таблица с зеброй, прочерками, quantity-колонкой, tbody-группировкой и принудительным разрывом"
      contains: "appendix-table"
  key_links:
    - from: "crates/trackly-app/templates/act_handover.html"
      to: "crates/trackly-app/src/services/act_service.rs"
      via: "act.items[].quantity/inventory_no/serial_no/model/specs/kit/condition уже собраны act_service::render_pdf (act_service.rs:2612-2627) — бэкенд не меняется, шаблон просто начинает читать quantity"
      pattern: "item\\.quantity"
---

<objective>
Переработать act_handover.html: сохранить N=1 неизменным, при N>1 заменить повторяющиеся
.device-block на первом листе нумерованным перечнем + отсылкой к «Приложению №1», и добавить новый
appendix-раздел — таблицу полного описания устройств с принудительным разрывом страницы, зеброй,
прочерками для пустых значений и группировкой строк устройства через tbody (D-01..D-16).

Purpose: единственный содержательный шаблонный шаг фазы — закрывает DOC-10 (N=1 без изменений) и
DOC-11 (N>1 перечень + приложение) по решениям, уже согласованным пользователем в CONTEXT.md; все
конкретные значения (зебра, кегль, формулировки) — в рамках Claude's Discretion.
Output: act_handover.html с ветвлением N=1/N>1, appendix-таблицей и обновлённым doc-комментарием;
@page-блок и производственный рендер act_service::render_pdf не меняются.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-act-pagination/36-CONTEXT.md
@.planning/phases/36-act-pagination/36-RESEARCH.md
@.planning/phases/36-act-pagination/36-PATTERNS.md
</context>

<interfaces>
Целевая структура appendix-раздела (используется идентично Планом 03 для написания тестов и Планом
04 для Paged.js-хендлера — класс-имена ниже финальны, не менять):

Обёртка div.appendix (получает break-before: page) содержит по порядку: div.appendix-mark (две
строки текста через br: «Приложение №1» и «к акту приема-передачи №{{ act.number }}{{ act.suffix |
default('') }} от {{ act.date_human | default(act.date) }}»), div.appendix-title (по центру,
например «Опись передаваемых устройств»), затем table.appendix-table.

Внутри таблицы: один thead со строкой из 7 th (№ / Наименование / Кол-во / Инв.№ / Серийный № /
Модель / Состояние), затем цикл {% for item in act.items %} — на каждой итерации ОДИН
tbody.device-group с дополнительным классом {{ loop.cycle('row-even', 'row-odd') }}, внутри
которого основная tr из 7 td (значения по порядку колонок, loop.index для №,
{% if item.quantity > 1 %}{{ item.quantity }}{% else %}—{% endif %} для Кол-во,
{{ item.X | default("—", true) }} для инв.№/серийный №/модель/состояние) и опциональная
tr.device-subrow с одним td colspan="7", выводимая только если item.kit or item.specs истинно
(текст: «Комплектация: {{ item.kit }}» и/или «Тех. характеристики: {{ item.specs }}», разделены
пробелом, если оба присутствуют).

CSS-класс row-even/row-odd навешивается на весь tbody (не на отдельные tr) — так зебра ключуется по
устройству, а не по строке таблицы (обе строки одного устройства — основная и под-строка —
получают один и тот же фон). break-inside: avoid идёт на .appendix-table tbody.device-group (не на
tr) — это единственный путь, который overflow-finder Paged.js честно проверяет для TBODY/THEAD
(RESEARCH.md Pattern 4).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Doc-комментарий + ветвление первого листа (N=1 неизменно / N&gt;1 — ol вместо .device-block)</name>
  <files>crates/trackly-app/templates/act_handover.html</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (текущий HEAD целиком — doc-комментарий строки 1-41, старый блок строки 133-164)
    .planning/phases/36-act-pagination/36-RESEARCH.md (Pattern 1/Pattern 2 — точная цитата целевой Jinja-разметки)
    .planning/phases/36-act-pagination/36-PATTERNS.md (секция act_handover.html — doc-комментарий и N=1-ветка)
  </read_first>
  <action>
    Переписать doc-комментарий (текущий HEAD, строки 2-41): после фразы про "per-item field_rows
    ... no Устройство №N" добавить абзац, описывающий новое ветвление — при act.items | length == 1
    поток не меняется; при > 1 первый лист выводит нумерованный ol.device-summary + отсылку к
    «Приложению №1», а полное описание устройств переезжает во второй+ лист таблицей
    appendix-table с принудительным разрывом (D-01..D-16, Фаза 36). В списке ключей контекста явно
    отметить, что act.items[].quantity теперь ИСПОЛЬЗУЕТСЯ шаблоном (D-03), а не просто
    присутствует. Затем заменить блок строк 133-164 (текущий HEAD ДО этой правки: сначала
    безусловный if act.items | length > 1 с ul/li, затем БЕЗУСЛОВНЫЙ for item in act.items
    div.device-block…) на единую if act.items | length > 1 … else … endif конструкцию: в ветке > 1
    — ol.device-summary с одним li.{{ item.name }} на устройство (та же петля, что была у ul),
    затем div.field-row с текстом «Полное описание устройств — в Приложении №1.»; в ветке else —
    существующий for item in act.items div.device-block... цикл ПЕРЕНЕСЁН как есть, без изменения
    текста внутри (лейбл «было получено устройство: {{ item.name }}» и все опциональные if item.X
    строки — байт-в-байт). Класс .device-summary добавить в style (margin для списка, например
    margin: 4pt 0 4pt 20pt). @page-блок (строки 47-50) и всё остальное содержимое файла
    (title/subtitle/intro/tail/signatures) не трогать — это Task 2.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_act_render html_handover_single_device_renders_singular_intro_not_plural_summary -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - html_handover_single_device_renders_singular_intro_not_plural_summary остаётся зелёным (N=1 не сломан)
    - grep -c "device-summary" файла выводит не менее 2 (класс в style + разметка ol)
    - grep -c "act.items | length" файла выводит не менее 3 (условие для ol/else-ветки; appendix-условие добавится в Task 2)
    - cargo check -p trackly-app компилируется без ошибок
    - Известный плановый дрейф (не регрессия): html_handover_multi_device_renders_plural_summary_listing_every_name (проверяет старый ul) КРАСНЕЕТ после этого task — переписывается в Плане 03, это ожидаемо
  </acceptance_criteria>
  <done>N=1 не изменён; N>1 первый лист выводит нумерованный ol + отсылку к приложению; .device-block больше не рендерится при N>1.</done>
</task>

<task type="auto">
  <name>Task 2: Appendix-таблица (thead/tbody-группировка/зебра/прочерки/quantity) + CSS</name>
  <files>crates/trackly-app/templates/act_handover.html</files>
  <read_first>
    crates/trackly-app/templates/act_handover.html (после Task 1 — читать актуальное состояние файла, вставка идёт после закрывающего div.signatures, перед /body)
    crates/trackly-app/templates/act_acceptance.html строки 124-127 (идиома | default("—", true) — копируется дословно)
    .planning/phases/36-act-pagination/36-RESEARCH.md (Pattern 2/Pattern 3/Pattern 4 и Pitfall 3 — точная цитата appendix-разметки и обоснование tbody-группировки)
  </read_first>
  <action>
    После закрывающего тега div.signatures (текущий конец файла — после Task 1 позиция не
    меняется) и перед /body добавить новый блок: {% if act.items | length > 1 %} div.appendix
    (структура и классы — см. блок interfaces этого плана: appendix-mark с двумя строками через
    br, appendix-title по центру, table.appendix-table с thead из 7 th и циклом for item in
    act.items, где каждая итерация — tbody.device-group с классом
    {{ loop.cycle('row-even', 'row-odd') }}, основная tr из 7 td, опциональная tr.device-subrow
    colspan="7" при item.kit or item.specs) {% endif %}. Значения td: № — {{ loop.index }};
    Наименование — {{ item.name }}; Кол-во — {% if item.quantity > 1 %}{{ item.quantity }}
    {% else %}—{% endif %} (D-03); Инв.№/Серийный №/Модель/Состояние — item.inventory_no /
    item.serial_no / item.model / item.condition, каждое через фильтр | default("—", true) (D-02,
    идиома из act_acceptance.html). Добавить CSS-правила: .appendix { break-before: page; }
    (D-16); .appendix-mark { text-align: right; font-size: 10pt; margin-bottom: 4pt; } (D-10);
    .appendix-title { text-align: center; font-size: 13pt; font-weight: bold; margin: 4pt 0 10pt
    0; }; .appendix-table { width: 100%; border-collapse: collapse; font-size: 10pt; } (D-04,
    кегль меньше основного 12pt); .appendix-table th, .appendix-table td { padding: 3pt 4pt;
    text-align: left; vertical-align: top; }; .appendix-table thead tr { border-bottom: 0.5pt
    solid #999; } (D-05, волосяная линия под шапкой); .appendix-table tbody.device-group {
    break-inside: avoid; border-top: 0.5pt solid #ccc; } (D-15 частично — группировка,
    волосяная линия между устройствами D-05); .appendix-table tbody.row-even td { background:
    #f2f2f2; print-color-adjust: exact; -webkit-print-color-adjust: exact; } (D-04, зебра только
    на чётных tbody — нечётные остаются белыми, print-color-adjust на обеих префиксных формах
    обязателен, иначе браузер/WebView2 подавят фон при печати). НЕ трогать @page-блок (строки
    47-50) — гейт html_page_parity.rs требует его байт-в-байт идентичным во всех трёх шаблонах
    (C-06).
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_page_parity -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - html_page_parity::all_three_templates_share_identical_page_block остаётся зелёным (@page не тронут)
    - grep -c "break-before: page" файла выводит не менее 1 (правило .appendix)
    - grep -c "break-inside: avoid" файла выводит не менее 2 (.device-block уже было, .appendix-table tbody.device-group — новое)
    - grep -c "print-color-adjust: exact" файла выводит не менее 1 и grep -c "-webkit-print-color-adjust: exact" выводит не менее 1
    - grep -c "default(\"—\", true)" файла выводит не менее 4 (inventory_no/serial_no/model/condition)
    - grep -c "appendix-table" файла выводит не менее 2 (класс в CSS + в разметке table)
    - cargo check -p trackly-app компилируется без ошибок
  </acceptance_criteria>
  <done>Appendix-таблица существует с 7-колоночной шапкой, tbody-группировкой на устройство, зеброй с print-color-adjust, прочерками для пустых значений, quantity-колонкой и принудительным разрывом перед собой; @page не тронут.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| act.items[].name/inventory_no/serial_no/model/specs/kit/condition/quantity (значения из БД, введены через форму создания акта) → новые ячейки appendix-таблицы | Новые МЕСТА интерполяции уже существующего проверенного sink'а (T-16-01) — те же поля, что уже безопасно интерполируются в .device-block на N=1, autoescape не меняется |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering (stored XSS) | Все {{ item.X }}-интерполяции внутри appendix-table | mitigate | Обычная {{ var }}-интерполяция под AutoEscape::Html (build_safe_html_env); НЕ добавлять \| safe ни на одном из новых сайтов — проверяется отсутствием "\| safe" рядом с item.* в acceptance_criteria обеих задач |
| T-36-05 | Information Disclosure | Doc-комментарий и appendix-mark текст | accept | Правки ограничены структурными/CSS изменениями и уже существующими контекстными ключами (act.number/suffix/date_human) — новых литералов с потенциально реальными данными не вводится |
</threat_model>

<verification>
После Task 2: TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_page_parity --test pdf_render_act render_handover_act_produces_cyrillic_pdf render_handover_act_contains_d09_intro_phrase -- --test-threads=1 — @page-паритет и базовый N=1-рендер остаются зелёными. Полный сьют (включая appendix-специфичные ассерции) намеренно откладывается на План 03/05 — новые структурные тесты появляются там.
</verification>

<success_criteria>
- N=1 не изменён байт-в-байт по содержанию .device-block-ветки.
- N>1 первый лист — только ol.device-summary + отсылка, без .device-block.
- appendix-table существует с 7 колонками, tbody-группировкой, зеброй, прочерками, quantity-логикой и принудительным break-before.
- @page-блок не тронут — html_page_parity.rs зелёный.
</success_criteria>

<output>
Create `.planning/phases/36-act-pagination/36-02-SUMMARY.md` when done
</output>
