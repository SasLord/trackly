---
phase: 40.1-audit-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/places/PlaceContents.svelte
  - ui/src/features/cartridges/CartridgesPage.svelte
  - ui/src/features/devices/DevicesPage.svelte
  - ui/src/features/devices/DeviceFormModal.svelte
  - ui/src/features/devices/DeviceFormBody.svelte
  - ui/scripts/check-place-tree-invalidation.mjs
  - crates/trackly-app/src/services/device_service.rs
autonomous: false

requirements: [PLC-06]

must_haves:
  truths:
    - "Правка места предмета через PlaceEntityViewModal на экране «Места» инвалидирует счётчики дерева, не только массовый перенос (D-14)."
    - "Правка места картриджа из списка «Картриджи» тоже инвалидирует счётчики дерева, передавая и старое, и новое место (D-14, D-15)."
    - "Правка места устройства из списка «Устройства» тоже инвалидирует счётчики дерева, передавая и старое, и новое место — D-14 называет ОБА списка («Устройства»/«Картриджи») дословно, ни один не может быть молча пропущен (D-14, D-15)."
    - "Потребитель PlaceTree.svelte и существующие инварианты INV-1..INV-6 (эффект untrack, монотонный seq) не изменены и остаются зелёными (D-16)."
    - "Новый структурный инвариант INV-7 гейта check-place-tree-invalidation.mjs проверяет все три новых write-site (PlaceContents/PlaceEntityViewModal, CartridgesPage, DevicesPage) отдельно друг от друга — проверка PlaceContents.svelte локализована именно на блок <PlaceEntityViewModal ...>, а не на файл целиком, потому что в этом файле после этого плана два разных вызова notifyPlaceContentChanged сосуществуют и файл-уровневая проверка не отличила бы удаление одного от присутствия другого (D-17)."
    - "Каскад «картридж едет за принтером» задокументирован как явное ограничение в комментарии рядом с серверным кодом каскада — не решается новым API в этой фазе и не игнорируется молча (D-18)."
    - "Находка закрыта структурным гейтом (INV-7) и живой проверкой, а не только комментарием в коде (D-24)."
  artifacts:
    - path: "ui/src/features/places/PlaceContents.svelte"
      provides: "notifyPlaceContentChanged вызывается также из PlaceEntityViewModal's onChanged consumer"
    - path: "ui/src/features/cartridges/CartridgesPage.svelte"
      provides: "notifyPlaceContentChanged([oldPlaceId, newPlaceId]) в handleFormSuccess"
    - path: "ui/src/features/devices/DevicesPage.svelte"
      provides: "notifyPlaceContentChanged([oldPlaceId, newPlaceId]) в onSaved (D-14 второй список, симметрично CartridgesPage)"
    - path: "ui/src/features/devices/DeviceFormModal.svelte"
      provides: "onSaved прокидывает финальный place_id наружу вместе с typeId (без чего DevicesPage не может узнать новое место)"
    - path: "ui/src/features/devices/DeviceFormBody.svelte"
      provides: "onSaved передаёт уже известный локальный placeId вызывающей стороне"
    - path: "ui/scripts/check-place-tree-invalidation.mjs"
      provides: "INV-7 — структурная проверка производителей события, раздельно для трёх write-site"
    - path: "crates/trackly-app/src/services/device_service.rs"
      provides: "комментарий, документирующий D-18 ограничение серверного каскада"
  key_links:
    - from: "ui/src/features/places/PlaceContents.svelte"
      to: "ui/src/lib/stores/placeContentEvents.svelte.ts"
      via: "notifyPlaceContentChanged([place.id])"
      pattern: "notifyPlaceContentChanged\\("
    - from: "ui/src/features/cartridges/CartridgesPage.svelte"
      to: "ui/src/lib/stores/placeContentEvents.svelte.ts"
      via: "notifyPlaceContentChanged([oldPlaceId, newPlaceId])"
      pattern: "notifyPlaceContentChanged\\("
    - from: "ui/src/features/devices/DevicesPage.svelte"
      to: "ui/src/lib/stores/placeContentEvents.svelte.ts"
      via: "notifyPlaceContentChanged([oldPlaceId, newPlaceId])"
      pattern: "notifyPlaceContentChanged\\("
    - from: "ui/src/features/devices/DeviceFormBody.svelte"
      to: "ui/src/features/devices/DeviceFormModal.svelte"
      via: "onSaved(placeId) -> onSaved({ typeId, placeId })"
      pattern: "onSaved\\(placeId\\)"
    - from: "ui/src/lib/stores/placeContentEvents.svelte.ts"
      to: "ui/src/features/places/PlaceTree.svelte"
      via: "placeContentEventsStore -> ancestorsAndSelf -> statsCache инвалидация"
      pattern: "ancestorsAndSelf"
---

<objective>
Закрыть WARNING-1 из аудита вехи v1.4: `notifyPlaceContentChanged`
(`ui/src/lib/stores/placeContentEvents.svelte.ts`) сегодня вызывается ровно из одного
продюсера — массового переноса в `PlaceContents.svelte:280`. Счётчики содержимого узлов
дерева «Места» остаются устаревшими, если предмет переместили через
`PlaceEntityViewModal` (правка со страницы «Места») или через списки «Устройства»/
«Картриджи» — до размонтирования страницы. Это наследник WR-01 из аудита 2026-08-27,
закрытый частично.

Purpose: закрыть ТРИ клиентских write-site (D-14 дословно называет оба списка —
«Устройства» и «Картриджи» — плюс правку через `PlaceEntityViewModal`) сверх уже
существующего, добавить структурный инвариант, который не даст будущей фазе снова завести
write-site без уведомления и который различает все три write-site по отдельности (не только
«вызов где-то в файле» — для `PlaceContents.svelte` в файле после этого плана будет ДВА
разных вызова, и гейт обязан ловить пропажу именно нового), и явно задокументировать
единственный write-site, который остаётся закрытым по структурным причинам (серверный
каскад «картридж едет за принтером», D-18).

Output: три новых вызова `notifyPlaceContentChanged` (`PlaceContents.svelte`,
`CartridgesPage.svelte`, `DevicesPage.svelte`), минимальный плумбинг `place_id` через
`DeviceFormBody.svelte`/`DeviceFormModal.svelte` (нужен `DevicesPage.svelte`, чтобы узнать
новое место так же напрямую, как `CartridgesPage.svelte` уже узнаёт его из `CartridgeDto`),
новый инвариант INV-7 гейта `check-place-tree-invalidation.mjs` (раздельный для всех трёх
write-site), комментарий-ограничение в `device_service.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-audit-gap-closure/40.1-CONTEXT.md
@.planning/phases/40.1-audit-gap-closure/40.1-PATTERNS.md
</context>

<interfaces>
<!-- Store и единственный существующий продюсер — уже существующий контракт, не меняется. -->

`ui/src/lib/stores/placeContentEvents.svelte.ts` (не меняется в этой фазе):
```typescript
export const placeContentEventsStore = $state<{ seq: number; placeIds: number[] }>({
  seq: 0,
  placeIds: [],
});

export function notifyPlaceContentChanged(placeIds: number[]): void {
  if (placeIds.length === 0) return;
  placeContentEventsStore.seq += 1;
  placeContentEventsStore.placeIds = placeIds;
}
```

Единственный существующий вызов (`PlaceContents.svelte:280`, паттерн для повторения):
```typescript
await apiCall<number>('places_move_subtree_contents', { rootId: place.id, targetPlaceId: moveTargetId, note: null });
notifyPlaceContentChanged([place.id, moveTargetId]);
```

Текущий потребитель `PlaceEntityViewModal` в `PlaceContents.svelte` (~строки 399-405) —
`onChanged` не несёт старое/новое место, только сигнал "сохранено":
```svelte
{#if viewRow}
  <PlaceEntityViewModal row={viewRow} onClose={() => (viewRow = null)} onChanged={() => (reloadToken += 1)} />
{/if}
```

`CartridgesPage.svelte` — `formModalTarget: CartridgeDto | null` (старое место через
`formModalTarget?.place_id`) устанавливается перед открытием формы (`handleMenuAction`,
`op === 'edit'`); `handleFormSuccess(cart: CartridgeDto)` получает сохранённую сущность с
новым `cart.place_id` напрямую (~строки 279-295) — самый чистый write-site, данные уже под
рукой без дополнительных запросов.

`CartridgeDto.place_id: number | null` (`ui/src/bindings.ts`).

`DevicesPage.svelte` — текущее состояние (`editTarget: DeviceDto | null`, `openEdit(d)` захватывает
СТАРЫЙ `DeviceDto` перед открытием формы; `onSaved()` сегодня НЕ получает от `DeviceFormModal`
никаких данных о месте, строки ~162-171):
```typescript
let editTarget = $state<DeviceDto | null>(null);

function openEdit(d: DeviceDto) {
  editTarget = d;
  modalOpen = true;
}

function onSaved() {
  modalOpen = false;
  refresh();
  refreshCounts();
}
```
```svelte
<DeviceFormModal open={modalOpen} target={editTarget} onClose={...} {onSaved} />
```

`DeviceFormModal.svelte` — сегодняшняя сигнатура и единственное текущее использование
`result` (`~строки 36-43, 123-126, 136`), уже форвардит `typeId` наружу тем же паттерном,
который нужно расширить `placeId`-полем:
```typescript
interface Props {
  open: boolean;
  target: DeviceDto | null;
  onClose: () => void;
  onSaved: (result?: { typeId: number }) => void;
}
// ...
function handleBodySaved() {
  onSaved({ typeId });
}
```
```svelte
<DeviceFormBody {target} {stateHints} {typeId} onSaved={handleBodySaved} ... />
```

`DeviceFormBody.svelte` — форма УЖЕ знает финальный `place_id` в локальном `$state` (проп
`placeId`, строка ~94, читается из `target?.place_id ?? null` и правится через `PlacePicker`);
и `patch`/`newDevice` payload'ы (строки ~217,238) уже используют именно это значение. Никакого
дополнительного API-запроса не требуется — нужно лишь ПЕРЕДАТЬ уже известное значение наружу
через `onSaved`, сегодня объявленный как `() => void` (строка 62) и вызываемый без аргументов
(строка 258, `onSaved();`, сразу после `devices.update(...)`/`devices.bulkCreate(...)`).

`DeviceDto.place_id: number | null` (`ui/src/bindings.ts`, как и у `CartridgeDto`).

Существующие вызовы `DeviceFormModal`'s `onSaved` в других файлах (`PlaceEntityViewModal.svelte:257`,
`PrinterDetail.svelte:456-462` -> форвардит `result` в `onDeviceSaved?: (result?: { typeId: number }) => void`
`PrintersPage.svelte:205`) НЕ разрушаются добавлением поля `placeId` в объект `result` — TypeScript
допускает передачу объекта с ДОПОЛНИТЕЛЬНЫМ полем туда, где ожидается более узкий тип (не
литерал, а переменная), и вызывающие функции с меньшим числом параметров тоже остаются
совместимыми. Эти три файла НЕ входят в `files_modified` и трогать их не нужно.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Продюсеры 1 и 2 из 3 — PlaceContents (over-invalidation корня) и CartridgesPage (точный старое/новое)</name>
  <files>ui/src/features/places/PlaceContents.svelte, ui/src/features/cartridges/CartridgesPage.svelte</files>
  <read_first>
    ui/src/features/places/PlaceContents.svelte (целиком — импорт notifyPlaceContentChanged строка 30, handleMoveConfirm строки 268-289, PlaceEntityViewModal-блок строки 399-405)
    ui/src/features/places/PlaceEntityViewModal.svelte (Props/onChanged строки 46-55, handleEditSaved строки 176-180 — НЕ меняется в этом плане, только читается для понимания, что onChanged не несёт данных места)
    ui/src/features/cartridges/CartridgesPage.svelte (импорты строки 1-25, formModalTarget/handleMenuAction строки 200-245, handleFormSuccess строки 279-295)
    ui/src/lib/stores/placeContentEvents.svelte.ts (целиком — контракт notifyPlaceContentChanged)
    .planning/phases/40.1-audit-gap-closure/40.1-PATTERNS.md (раздел «WARNING-1», write-site 1 и write-site 2)
  </read_first>
  <action>
    В `PlaceContents.svelte`: в блоке `{#if viewRow}<PlaceEntityViewModal ... onChanged={() =>
    (reloadToken += 1)} />{/if}` заменить однострочную стрелочную функцию `onChanged` на
    блок-тело (нужно для Task 3's структурной проверки INV-7 — гейт локализует write-site
    именно внутри разметки `<PlaceEntityViewModal ...>`, поэтому форма должна остаться внутри
    этого тега, а не переехать в отдельную именованную функцию `<script>`):
    `onChanged={() => { notifyPlaceContentChanged([place.id]); reloadToken += 1; }}` (место —
    текущий просматриваемый корень, проп `place: PlaceDto` компонента). `PlaceEntityViewModal`
    НЕ меняется — сигнатура `onChanged?: () => void` остаётся прежней, инвалидация полностью
    на стороне потребителя (`PlaceContents.svelte`), симметрично существующему паттерну
    `handleMoveConfirm` (который остаётся ЕДИНСТВЕННЫМ местом файла, вызывающим
    `notifyPlaceContentChanged([place.id, moveTargetId])` с ДВУМЯ аргументами — эта форма
    Task 3's гейту нужна как отличительный признак, не путать два вызова между собой).
    Добавить комментарий той же формы, что у остальных док-комментариев файла, ПОЧЕМУ выбрана
    инвалидация именно текущего корня, а не точного старого/нового места правленной записи:
    `PlaceContentDto`/`onSaved`/`onSuccess` не несут place_id ни до, ни после сохранения без
    расширения сигнатур `DeviceFormModal`/`CartridgeFormModal`/`PlaceEntityViewModal` — вне
    объёма именно этого write-site (D-14 discretion; ниже, в Task 2, ради `DevicesPage.svelte`
    сигнатура `DeviceFormModal`/`DeviceFormBody` всё же расширяется, но `PlaceEntityViewModal`
    и здесь остаётся нетронутым — два разных решения для двух разных потребителей, оба
    осознанные); лишняя инвалидация корня и его предков (`ancestorsAndSelf` на стороне
    `PlaceTree.svelte`) безопасна, но неточна для записей из вложенных мест — известное
    ограничение, не молчаливое упущение.

    В `CartridgesPage.svelte`: добавить импорт `import { notifyPlaceContentChanged } from
    '$lib/stores/placeContentEvents.svelte';`. В `handleFormSuccess(cart: CartridgeDto)` (после
    существующей логики выбора/обновления `selectedCartridge`) добавить вызов
    `notifyPlaceContentChanged` со старым местом из `formModalTarget?.place_id` (захвачен ДО
    открытия формы) и новым местом из `cart.place_id` (сохранённая сущность) — собрать массив,
    отфильтровать `null`, убрать дубликаты (например через `Array.from(new Set(...))`), не
    вызывать вовсе, если результат пуст (стор сам это делает, но пустой массив — не ошибка).
    Добавить комментарий той же формы, поясняющий D-15 (передаются оба места, а не одно) и
    ссылающийся на то, что это самый точный из двух write-site этого плана.
  </action>
  <verify>
    <automated>cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "notifyPlaceContentChanged" ui/src/features/places/PlaceContents.svelte` находит
      ДВА вызова: существующий (`handleMoveConfirm`) и новый (в блоке `PlaceEntityViewModal`).
    - `grep -n "notifyPlaceContentChanged" ui/src/features/cartridges/CartridgesPage.svelte`
      находит один новый вызов внутри `handleFormSuccess`, использующий и `formModalTarget?.place_id`,
      и `cart.place_id`.
    - `cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json` завершается без новых ошибок
      в обоих файлах.
  </acceptance_criteria>
  <done>Оба новых write-site вызывают notifyPlaceContentChanged; PlaceEntityViewModal.svelte не изменён (инвалидация на стороне потребителя); CartridgesPage передаёт и старое, и новое место.</done>
</task>

<task type="auto">
  <name>Task 2: Продюсер 3/3 — DevicesPage (D-14 второй список), плумбинг place_id через DeviceFormBody/DeviceFormModal</name>
  <files>ui/src/features/devices/DeviceFormBody.svelte, ui/src/features/devices/DeviceFormModal.svelte, ui/src/features/devices/DevicesPage.svelte</files>
  <read_first>
    ui/src/features/devices/DeviceFormBody.svelte (Props/onSaved строки 60-63; placeId $state строка 94; handleSubmit строки 199-266, оба вызова `onSaved()` — edit-ветка ~строка 232, create-ветка ~строка 258)
    ui/src/features/devices/DeviceFormModal.svelte (Props/onSaved строки 30-46; handleBodySaved строки 123-127; DeviceFormBody-разметка строки 133-141)
    ui/src/features/devices/DevicesPage.svelte (editTarget $state строка 27; openEdit строки 162-165; onSaved строки 167-171; DeviceFormModal-разметка строки 302-307)
    ui/src/features/cartridges/CartridgesPage.svelte (handleFormSuccess, изменённый в Task 1 — образец формы для повторения: старое место из захваченного ДО открытия формы объекта, новое — из результата сохранения)
    .planning/phases/40.1-audit-gap-closure/40.1-PATTERNS.md (раздел «Write-site 2 — списки Устройства/Картриджи (D-14)», абзац про DevicesPage.svelte:162-171 и асимметрию с CartridgesPage.svelte)
  </read_first>
  <action>
    D-14 называет ОБА списка дословно («Устройства»/«Картриджи») — CartridgesPage уже закрыт
    в Task 1 напрямую (полный `CartridgeDto` приходит в `onSuccess`), но `DeviceFormModal`'s
    `onSaved` сегодня форвардит только `{ typeId }`, без места. Минимальный путь к точному
    старому+новому месту (D-15) — не читать место откуда-то заново, а ПЕРЕДАТЬ уже известное
    значение: `DeviceFormBody` уже держит финальный `placeId` в собственном `$state` (это
    значение, которое только что ушло в `devices.update`/`devices.bulkCreate`).

    В `DeviceFormBody.svelte`: изменить тип пропа `onSaved: () => void;` на
    `onSaved: (placeId: number | null) => void;` (доккомент — почему: место, только что
    сохранённое формой, уже известно локально, лишнего запроса не нужно). В обеих ветках
    `handleSubmit` (edit и create) заменить `onSaved();` на `onSaved(placeId);` — `placeId` уже
    в замыкании, никаких новых переменных не требуется.

    В `DeviceFormModal.svelte`: изменить `onSaved: (result?: { typeId: number }) => void;` на
    `onSaved: (result?: { typeId: number; placeId: number | null }) => void;` (доккомент —
    обновить существующий, объясняющий смысл `result`, добавить упоминание `placeId`).
    `handleBodySaved` — изменить сигнатуру на `function handleBodySaved(placeId: number | null)`
    и тело на `onSaved({ typeId, placeId });`; проброс в разметку `<DeviceFormBody ...
    onSaved={handleBodySaved} ... />` не меняется (сигнатура функции меняется, не способ
    передачи). Это расширение обратно совместимо: `PlaceEntityViewModal.svelte`,
    `PrinterDetail.svelte`, `PrintersPage.svelte` (три существующих потребителя
    `DeviceFormModal`, файлы НЕ входят в `files_modified` этого плана) читают `result.typeId`
    и игнорируют остальное — TypeScript допускает передачу объекта с лишним полем туда, где
    ожидается более узкий тип (см. `<interfaces>`), и обработчики с меньшим числом параметров
    остаются валидными без изменений.

    В `DevicesPage.svelte`: добавить импорт `import { notifyPlaceContentChanged } from
    '$lib/stores/placeContentEvents.svelte';`. Изменить `function onSaved()` на
    `function onSaved(result?: { typeId: number; placeId: number | null })`. Внутри, ДО того
    как `editTarget` может быть переиспользован (он ещё хранит старое значение на момент
    вызова — `openEdit`/`openCreate` очищают/переустанавливают его только при следующем
    открытии формы, не в `onSaved`), собрать массив из `editTarget?.place_id ?? null` (старое
    место — `null` при создании нового устройства, это корректно, симметрично `CartridgesPage`'s
    `formModalTarget?.place_id`) и `result?.placeId ?? null` (новое место), отфильтровать
    `null`, убрать дубликаты (`Array.from(new Set(...))`), вызвать `notifyPlaceContentChanged`
    с результатом, если он не пуст. Добавить комментарий той же формы, что и в
    `CartridgesPage.svelte` (Task 1), поясняющий D-14/D-15 и то, что старое место читается из
    `editTarget`, а не из аргумента (симметрично, но не идентично — у картриджей старое место
    читается из отдельной переменной `formModalTarget`, у устройств она уже называется
    `editTarget` и используется по той же роли).
  </action>
  <verify>
    <automated>cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "onSaved(placeId)" ui/src/features/devices/DeviceFormBody.svelte` находит ДВА
      вызова (edit-ветка и create-ветка `handleSubmit`).
    - `grep -n "placeId: number | null" ui/src/features/devices/DeviceFormModal.svelte`
      находит новый тип `onSaved` результата.
    - `grep -n "notifyPlaceContentChanged" ui/src/features/devices/DevicesPage.svelte` находит
      один новый вызов внутри `onSaved`, использующий и `editTarget?.place_id`, и
      `result?.placeId`.
    - `cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json` завершается без новых ошибок
      ни в изменённых файлах, ни в трёх непрямых потребителях `DeviceFormModal`
      (`PlaceEntityViewModal.svelte`, `PrinterDetail.svelte`, `PrintersPage.svelte`) — расширение
      типа `result` обратно совместимо и не должно их затронуть.
  </acceptance_criteria>
  <done>DevicesPage.svelte стал третьим продюсером notifyPlaceContentChanged, передающим точное старое и новое место (D-14/D-15); DeviceFormBody/DeviceFormModal форвардят уже известный place_id без дополнительных запросов; три непрямых потребителя DeviceFormModal не сломаны.</done>
</task>

<task type="auto">
  <name>Task 3: Инвариант INV-7 гейта (раздельно для трёх write-site) + документирование D-18 (каскад принтер-картридж)</name>
  <files>ui/scripts/check-place-tree-invalidation.mjs, crates/trackly-app/src/services/device_service.rs</files>
  <read_first>
    ui/scripts/check-place-tree-invalidation.mjs (целиком — заголовочный список INV-1..INV-6 строки 14-40, balancedParens строки 99-110, effectBlocks строки 112-122, indicesOf/stripComments строки 68-97,138-148, checkStore строки 360-390, main() строки 394-439)
    ui/src/features/places/PlaceContents.svelte (после Task 1 — блок `<PlaceEntityViewModal ... onChanged={() => { notifyPlaceContentChanged([place.id]); reloadToken += 1; }} />` и отдельно `handleMoveConfirm`'s `notifyPlaceContentChanged([place.id, moveTargetId]);` — ДВА разных вызова в одном файле)
    crates/trackly-app/src/services/device_service.rs (строки 320-360 — cascade_place_for_printer_in_tx, контекст вызова)
    .planning/phases/40.1-audit-gap-closure/40.1-PATTERNS.md (подраздел «check-place-tree-invalidation.mjs — новый инвариант о производителях» и «Write-site 3 (D-18) — каскад картридж едет за принтером»)
  </read_first>
  <action>
    В `check-place-tree-invalidation.mjs`: добавить пункт `INV-7` в заголовочный doc-комментарий
    (та же форма — номер, откуда взят «40.1, аудит 2026-09-17», формулировка «почему», отсылка к
    WR-01/аудиту 2026-08-27 как истории полузакрытого дефекта; явно упомянуть, что проверка
    ТРИ write-site раздельно, а не «хотя бы один вызов где-то в репозитории», потому что
    `PlaceContents.svelte` после этого плана содержит ДВА разных вызова и файл-уровневая
    проверка не отличила бы удаление нового от присутствия старого —
    `mutation_test_anchor_must_be_unique`). Добавить константы путей для новых продюсеров
    (`const CONTENTS = 'src/features/places/PlaceContents.svelte';`, `const CARTRIDGES_PAGE =
    'src/features/cartridges/CartridgesPage.svelte';`, `const DEVICES_PAGE =
    'src/features/devices/DevicesPage.svelte';`) и константу искомой подстроки
    (`const NOTIFY = 'notifyPlaceContentChanged(';`).

    Добавить хелпер `tagBlock(src, tagName)` — по образцу `balancedParens`, но балансирует
    ФИГУРНЫЕ скобки, а не круглые (нужно, т.к. атрибуты Svelte-тега вида
    `onChanged={() => { ... }}` сами содержат вложенные `{}`): найти `<${tagName}`, затем
    сканировать вперёд, считая `{`/`}` (глубина), и вернуть срез до первого `>` на глубине 0
    (что покрывает и self-closing `/>`, т.к. символ перед `>` не влияет на подсчёт). Вернуть
    `null`, если тег не найден.

    Добавить функцию `checkProducers(contentsSrc, cartridgesSrc, devicesSrc, violations)` с
    ДВУМЯ разными стратегиями проверки в одной функции — обе бьют в `INV-7`, но описаны разными
    `message`/`hint`:
    1. Для `contentsSrc` (`PlaceContents.svelte`) — через `stripComments` получить `code`,
       вызвать `tagBlock(code, 'PlaceEntityViewModal')`; если `null` — violation «тег не
       найден» (компонент удалён/переименован при рефакторинге, обнови гейт осознанно); если
       найден, но НЕ содержит `NOTIFY` — violation `INV-7` с сообщением, что write-site 1
       (правка предмета через `PlaceEntityViewModal`) больше не инвалидирует дерево, и hint,
       ссылающийся на WR-01/на то, что `handleMoveConfirm`'s вызов остаётся нетронутым, но это
       НЕ считается — предполётная логика для этого write-site пропала.
    2. Для `cartridgesSrc`/`devicesSrc` (`CartridgesPage.svelte`, `DevicesPage.svelte`) —
       файл-уровневая проверка через `stripComments` + `indicesOf(code, NOTIFY).length === 0`
       (то же, что в исходном черновике этой задачи). Это НЕ недосмотр и не асимметрия по
       забывчивости: в отличие от `PlaceContents.svelte`, в каждом из этих двух файлов ровно
       ОДИН вызов `notifyPlaceContentChanged` (введён в Task 1/Task 2 этого плана, до них — ноль
       вхождений), поэтому файл-уровневая проверка однозначно указывает на конкретный
       write-site и не путает его ни с чем другим — локализация до конкретного блока тут
       не нужна. Зафиксировать это явно комментарием над функцией (следующему читателю гейта не
       должно казаться ошибкой, что три проверки в одной функции выглядят по-разному).

    В `main()` прочитать все три файла через уже существующий хелпер `read(rel)` (добавить
    `devicesSrc = read(DEVICES_PAGE)`) и вызвать `checkProducers(contentsSrc, cartridgesSrc,
    devicesSrc, violations)` рядом с `checkTree(...)`/`checkStore(...)`. Гейт УЖЕ
    зарегистрирован в `pnpm lint` (`ui/package.json:16`, последний в списке) — `package.json` НЕ
    трогать.

    В `device_service.rs`: рядом с вызовом `cartridge_repo.cascade_place_for_printer_in_tx(...)`
    (~строки 333-357) добавить комментарий, документирующий D-18: каскад места картриджей за
    принтером выполняется целиком на сервере в той же транзакции, что и сохранение принтера;
    клиент (`DeviceFormModal`/`DeviceDto`) не получает список id затронутых картриджей в ответе,
    поэтому счётчики дерева «Места» для этих картриджей могут оставаться неточными до полной
    перезагрузки страницы — известное задокументированное ограничение (WARNING-1, аудит
    2026-09-17), а не молчаливое упущение; решается вне объёма этой фазы новым API, если
    когда-нибудь понадобится точность без перезагрузки. Комментарий не меняет поведение кода.
  </action>
  <verify>
    <automated>node ui/scripts/check-place-tree-invalidation.mjs; cargo build -p trackly-app</automated>
  </verify>
  <acceptance_criteria>
    - `node ui/scripts/check-place-tree-invalidation.mjs` завершается кодом 0 (`PASS`) на
      текущем репозитории (все три продюсера из Task 1/Task 2 уже на месте).
    - Мутационный самотест №1 (уникальный якорь, локализованная проверка): скопировать `ui/` во
      временную директорию (`cp -r ui /tmp/mut1`), внутри копии в
      `src/features/places/PlaceContents.svelte` удалить ТОЛЬКО вызов
      `notifyPlaceContentChanged([place.id]);` из блока `<PlaceEntityViewModal ...
      onChanged={...} />`, НЕ трогая `handleMoveConfirm`'s
      `notifyPlaceContentChanged([place.id, moveTargetId]);` (эти два вызова синтаксически
      различимы — разное число аргументов и разное окружение — заменить именно первый на пустую
      строку), прогнать `node ui/scripts/check-place-tree-invalidation.mjs --src=/tmp/mut1`: код
      выхода 1, вывод содержит `INV-7` и `PlaceContents.svelte`. Это доказывает, что оставшийся
      нетронутым второй вызов в том же файле НЕ маскирует пропажу первого (ровно тот дефект
      дизайна, который был найден ревизией этого плана).
    - Мутационный самотест №2 (файл-уровневые продюсеры): скопировать `ui/` в `/tmp/mut2`,
      удалить единственный вызов `notifyPlaceContentChanged` из
      `src/features/cartridges/CartridgesPage.svelte` (не трогая `DevicesPage.svelte`),
      прогнать гейт с `--src=/tmp/mut2`: код выхода 1, вывод содержит `INV-7` и
      `CartridgesPage.svelte`. Повторить симметрично для `DevicesPage.svelte` (`/tmp/mut3`,
      удалить только её вызов) — код выхода 1, вывод содержит `DevicesPage.svelte`.
    - Позитивный контроль: на НЕизменённой копии (без мутации) гейт с `--src=<copy>` завершается
      кодом 0 — мутационные самотесты действительно проверяют регрессию, а не сломанный путь
      копирования.
    - `grep -n "D-18\|cascade_place_for_printer_in_tx" crates/trackly-app/src/services/device_service.rs`
      показывает комментарий рядом с вызовом каскада.
    - `cargo build -p trackly-app` завершается кодом 0 (комментарий не ломает компиляцию).
  </acceptance_criteria>
  <done>INV-7 добавлен и проходит на текущем коде; три независимых мутационных самотеста доказывают, что гейт ловит регрессию на каждом из трёх write-site по отдельности, включая случай двух сосуществующих вызовов в одном файле; каскад принтер-картридж задокументирован как явное ограничение.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Живая проверка инвалидации счётчиков дерева в запущенном приложении</name>
  <files>N/A — задача только проверяет поведение, не меняет файлы</files>
  <read_first>
    ui/src/features/places/PlaceContents.svelte (изменения Task 1)
    ui/src/features/cartridges/CartridgesPage.svelte (изменения Task 1)
    ui/src/features/devices/DevicesPage.svelte (изменения Task 2)
  </read_first>
  <what-built>
    Три новых продюсера инвалидации счётчиков дерева «Места» (правка предмета из
    `PlaceEntityViewModal`, правка места картриджа из списка «Картриджи», правка места
    устройства из списка «Устройства» — D-14 называет оба списка дословно) + структурный гейт
    INV-7, защищающий все три от будущей регрессии.
  </what-built>
  <how-to-verify>
    D-25: svelte-check/eslint/pnpm build не ловят рантайм-ошибки рун Svelte 5 (этот класс
    дефекта уже вешал страницу целиком — UAT3-03a, коммит 5042e674) — нужен живой прогон.
    1. Предварительно: `cd ui && pnpm run lint` должен быть зелёным (включает новый INV-7).
    2. Запустить `cargo tauri dev`, открыть раздел «Места».
    3. Открыть место с вложенными местами и содержимым (например здание с этажами), запомнить
       отображаемые счётчики на узлах дерева.
    4. Открыть содержимое места, кликнуть по устройству/картриджу -> «Просмотр» -> «Редактировать»
       -> сменить его место на другое (в пределах того же поддерева или другое) -> сохранить.
    5. Без перезагрузки страницы убедиться, что счётчики затронутых узлов дерева обновились
       (не только таблица содержимого справа).
    6. Открыть раздел «Картриджи», отредактировать картридж, сменив его место -> сохранить.
    7. Вернуться на «Места» (без перезагрузки, если приложение это позволяет через навигацию) —
       убедиться, что счётчик узла, где теперь находится картридж, и узла, откуда он уехал,
       обновились.
    8. Открыть раздел «Устройства», отредактировать устройство (не принтер — простое
       устройство), сменив его место -> сохранить. Вернуться на «Места» (без перезагрузки) —
       убедиться, что счётчик узла-приёмника и узла-источника обновились так же, как в шаге 7.
    9. Создать НОВОЕ устройство из раздела «Устройства», сразу указав ему место -> сохранить.
       Вернуться на «Места» — убедиться, что счётчик узла, куда оно попало, увеличился без
       перезагрузки (старое место в этом случае отсутствует — `editTarget` пуст при создании,
       ожидаемо инвалидируется только новое).
    10. Убедиться, что страница НЕ подвисает и не выдаёт ошибку `effect_update_depth_exceeded` в
        консоли разработчика в течение всей проверки (шаги 2-9).
  </how-to-verify>
  <action>Пройти шаги <how-to-verify> в запущенном приложении и сообщить результат — "approved" либо конкретное расхождение с ожидаемым поведением.</action>
  <verify>
    <automated>MISSING — рантайм-поведение Svelte 5 рун (включая класс дефекта effect_update_depth_exceeded) не ловится ни одним статическим гейтом проекта (D-25); единственная проверка — ручной прогон cargo tauri dev по шагам how-to-verify.</automated>
  </verify>
  <acceptance_criteria>
    - Счётчики дерева «Места» обновляются без перезагрузки после правки места предмета со всех
      трёх новых экранов (PlaceEntityViewModal, «Картриджи», «Устройства», включая создание
      нового устройства сразу с местом).
    - Страница не подвисает, в консоли разработчика нет effect_update_depth_exceeded.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>Пользователь подтвердил "approved" в запущенном приложении, либо явно описал расхождение для следующей итерации.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Клиентский стор (`placeContentEventsStore`) | Только same-tab/same-session сигнал, не пересекает границу доверия — D-контекст (a) явно фиксирует «это НЕ WebSocket» |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-05 | Denial of Service (клиентский, не сетевой) | `$effect` инвалидации в `PlaceTree.svelte` | accept | Класс дефекта (бесконечный `$effect`-цикл, `effect_update_depth_exceeded`) уже случался и был закрыт (5042e674); этот план НЕ меняет `PlaceTree.svelte`/стор, только добавляет вызовы уже существующей функции — INV-1..INV-6 (Task 3) остаются проверяемым инвариантом, что регрессия не повторится. |
| T-40.1-06 | Information Disclosure | Комментарий в `device_service.rs` про D-18 | accept | Чисто документационное изменение (комментарий), не меняет поведение и не раскрывает ничего клиенту — фиксируется внутри кодовой базы для будущих разработчиков. |
</threat_model>

<verification>
1. `cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json` — без новых ошибок (включая три непрямых потребителя DeviceFormModal, не входящих в files_modified).
2. `node ui/scripts/check-place-tree-invalidation.mjs` — PASS, включая новый INV-7.
3. `cargo build -p trackly-app` — компилируется с новым комментарием.
4. Три независимых мутационных самотеста INV-7 (Task 3 acceptance_criteria) — гейт реально ловит регрессию на каждом из трёх write-site по отдельности, включая случай двух сосуществующих вызовов в одном файле (PlaceContents.svelte).
5. Живой прогон `cargo tauri dev` (checkpoint, Task 4) — счётчики дерева обновляются со всех трёх новых экранов, включая создание нового устройства.
</verification>

<success_criteria>
- Счётчики содержимого дерева мест инвалидируются после правки места предмета с экрана «Места» и из ОБОИХ списков — «Картриджи» и «Устройства» (D-14) — не только после массового переноса.
- Существующие инварианты INV-1..INV-6 не нарушены.
- Новый инвариант INV-7 защищает все три новых write-site от будущей регрессии, включая случай, где два вызова сосуществуют в одном файле (PlaceContents.svelte).
- Каскад «картридж едет за принтером» явно задокументирован как известное ограничение.
</success_criteria>

<output>
Create `.planning/phases/40.1-audit-gap-closure/40.1-03-SUMMARY.md` when done
</output>
