---
phase: 40.1
slug: audit-gap-closure
status: validated
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-18
---

# Phase 40.1 — Validation Strategy

> Реконструировано ретроактивно (/gsd-validate-phase, State B) из PLAN/SUMMARY/VERIFICATION фазы.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` (integration `crates/trackly-app/tests/*.rs` + `--lib` unit) · node-структурные гейты `ui/scripts/check-*.mjs` в цепочке `pnpm lint` |
| **Config file** | `Cargo.toml` (workspace), `ui/package.json` (`lint`) |
| **Quick run command** | `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test places_delete_blocked --test report_movements --test report_csv_export --test acts_changed_place_ids -- --test-threads=1` |
| **Full suite command** | quick + `cargo test -p trackly-app --lib -- place_service tauri_cmds::reports report_service` + `pnpm --dir ui run lint` |
| **Estimated runtime** | ~5 s тесты (после сборки), ~30 s lint |

На dev-маке без принятой лицензии Xcode — префикс `DEVELOPER_DIR=/Library/Developer/CommandLineTools`. Не запускать два `cargo test` одновременно.

---

## Sampling Rate

- **After every task commit:** quick run command
- **After every plan wave:** full suite
- **Before `/gsd-verify-work`:** full suite green
- **Max feedback latency:** ~60 s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 40.1-01-01 | 01 | 1 | PLC-01 | T-40.1-02 | параметризованный SQL подсчёта | build + integration | `cargo test -p trackly-app --test places_delete_blocked` | ✅ | ✅ green |
| 40.1-01-02 | 01 | 1 | PLC-01 | T-40.1-01 | русское сообщение, без текста SQLite | unit | `cargo test -p trackly-app --lib place_service` | ✅ | ✅ green |
| 40.1-01-03 | 01 | 1 | PLC-01 | T-40.1-01 | `!reason.contains("FOREIGN KEY")` | integration | `cargo test -p trackly-app --test places_delete_blocked` (`delete_blocked_by_movement_{from,to}_place_id_*`) | ✅ | ✅ green |
| 40.1-02-01 | 02 | 1 | HST-04 | — | N/A | structural gate | `node ui/scripts/check-movements-type-filter.mjs` (INV-1…INV-4) | ✅ (добавлен аудитом) | ✅ green |
| 40.1-02-01b | 02 | 1 | HST-04 | — | N/A | integration | `cargo test -p trackly-app --test report_movements` (`report_movements_type_filter_narrows_rows`, `report_movements_place_and_type_filters_combine_with_and`) | ✅ (добавлен аудитом) | ✅ green |
| 40.1-02-02 | 02 | 1 | HST-04 | — | N/A | integration + unit + structural | `cargo test -p trackly-app --test report_movements` (omit_type_column, filter_summary, deleted place WR-02) · `--lib tauri_cmds::reports report_service` · `check-movements-type-filter.mjs` INV-5 (паритет JS↔Rust колонок) | ✅ | ✅ green |
| 40.1-03-01..03 | 03 | 1 | PLC-06 | — | N/A | structural gate | `node ui/scripts/check-place-tree-invalidation.mjs` (INV-1…INV-7, реестр write-site) | ✅ | ✅ green |
| 40.1-03 (WR-06) | 03 | 1 | PLC-06 | — | N/A | integration | `cargo test -p trackly-app --test acts_changed_place_ids` (5 тестов) | ✅ | ✅ green |
| 40.1-03-04 | 03 | 1 | PLC-06 | — | N/A | manual | — (см. Manual-Only) | — | ⚠️ manual |
| 40.1-04-01 | 04 | 1 | PLC-04 | — | CSV formula-injection guard, BOM | integration | `cargo test -p trackly-app --test report_csv_export` | ✅ | ✅ green |
| 40.1-04-02 | 04 | 1 | PLC-04 | — | N/A | unit | `cargo test -p trackly-app --lib tauri_cmds::reports` (`column_labels_for_is_index_aligned_with_columns_for`) | ✅ | ✅ green |
| 40.1-04-03 | 04 | 1 | PLC-04, HST-04 | — | N/A | integration | `cargo test -p trackly-app --test report_movements` (`report_movements_export_csv_has_russian_headers`) | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky/manual*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Счётчики дерева «Места» обновляются после создания/правки/возврата/удаления акта | PLC-06 | INV-7 структурный; рантайм рун Svelte 5 (класс `effect_update_depth_exceeded`) статикой не ловится | `cargo tauri dev`: акт на устройства из разных мест, дерево открыто; сохранить, отредактировать, вернуть (с per-item override), удалить. Счётчики старого и нового места и их предков обновляются без перехода |
| Счётчики дерева после правки места из PrinterDetail («Данные устройства») и из «Просмотра» в DeviceContextMenu | PLC-06 | новые write-site WR-07/WR-08, живьём не проверялись | сменить место в обоих UI-входах, счётчики обновляются сразу |
| Dropdown «Тип устройства» реально сужает таблицу в WKWebView | HST-04 | гейт структурный | уже approved на живой UAT плана 02; повторять при правке ReportFilters |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (кроме живых чекпоинтов 02-02/03-04 — они manual по дизайну, D-25)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-09-18

---

## Validation Audit 2026-09-18

| Metric | Count |
|--------|-------|
| Gaps found | 3 |
| Resolved | 3 |
| Escalated | 0 |

- G1 (HST-04, MISSING): не было теста, что `type_id` сужает строки. Существующие тесты смотрели только заголовки и оставались зелёными при удалённом фильтре (проверено мутацией). Закрыто 2 интеграционными тестами.
- G2 (HST-04 UI, MISSING): закрыто гейтом `check-movements-type-filter.mjs` (INV-1…INV-4) в цепочке `pnpm lint`.
- G3 (HST-04 паритет JS↔Rust, PARTIAL): закрыто INV-5 того же гейта. Мутационный самотест: 17/17 мутантов красные.
- Тесты: коммит `0c1dc794`.
