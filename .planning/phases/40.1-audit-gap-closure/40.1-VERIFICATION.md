---
phase: 40.1-audit-gap-closure
verified: 2026-09-18T12:00:00Z
status: gaps_found
score: 3/4 roadmap success criteria verified (SC1, SC2, SC4 VERIFIED; SC3 PARTIAL)
overrides_applied: 0
gaps:
  - truth: "Счётчики содержимого в дереве мест обновляются после смены места предмета с любого экрана, а не только после массового переноса (ROADMAP Success Criterion 3 / WARNING-1)."
    status: partial
    reason: >
      Три write-site из must_haves плана 40.1-03 (PlaceEntityViewModal, CartridgesPage's
      handleFormSuccess «Редактировать», DevicesPage's onSaved) реализованы и проверены —
      вызывают notifyPlaceContentChanged со старым+новым местом, INV-7 гейт их защищает,
      живой UAT approved. Но у CartridgesPage есть ЧЕТВЁРТЫЙ, более частый write-site —
      OperationModal (lifecycle-операции install/return_to_stock/to_refill/from_refill/
      write_off, wired через onSuccess={handleOperationSuccess}), который тоже меняет
      cartridges.place_id на сервере (CartridgeService транзиция, UPDATE cartridges SET
      place_id=?1) — но handleOperationSuccess (CartridgesPage.svelte:263-278) вызывает
      только loadAll()/перезагрузку карточки, notifyPlaceContentChanged не вызывает. INV-7
      гейт (check-place-tree-invalidation.mjs) не проверяет этот write-site вовсе — то есть
      именно тот класс регрессии, который WARNING-1 должен был закрыть структурно, остаётся
      незащищённым для самого частого клиентского действия над картриджем. Уже
      задокументировано ревью фазы как WR-01 (40.1-REVIEW.md, status: issues_found,
      warning-уровень) — фикс не выполнен на момент верификации.
    artifacts:
      - path: "ui/src/features/cartridges/CartridgesPage.svelte"
        issue: "handleOperationSuccess (строки 263-278, wired на OperationModal.onSuccess строка 482) не вызывает notifyPlaceContentChanged; только handleFormSuccess (форма «Редактировать», строки 280-293) вызывает."
      - path: "ui/scripts/check-place-tree-invalidation.mjs"
        issue: "INV-7 проверяет ровно три write-site (PlaceContents/PlaceEntityViewModal, CartridgesPage, DevicesPage) — не различает handleFormSuccess и handleOperationSuccess внутри CartridgesPage.svelte, поэтому не поймает отсутствие вызова в лайфсайкл-операциях."
    missing:
      - "Захватить place_id ДО операции (operationModalCartridge?.place_id уже доступен) и ПОСЛЕ (из cartridge, возвращённого transition-вызовом) внутри handleOperationSuccess, вызвать notifyPlaceContentChanged симметрично handleFormSuccess — фикс уже предложен построчно в 40.1-REVIEW.md (WR-01)."
      - "Расширить INV-7 (check-place-tree-invalidation.mjs) на второй, отдельный от handleFormSuccess, вызов notifyPlaceContentChanged внутри CartridgesPage.svelte (или tagBlock-локализация на блок handleOperationSuccess/<OperationModal>), чтобы будущая регрессия здесь не была снова невидима гейту."
deferred: []
human_verification: []
---

# Phase 40.1: Пробелы аудита v1.4 — Verification Report

**Phase Goal:** Закрыть четыре находки аудита вехи v1.4, обнаруженные на межфазных стыках —
BLOCKER-1 (предполётная проверка удаления места), BLOCKER-2 / HST-04 (фильтр «по типу
устройства» в отчёте «Перемещения» достижим из UI), WARNING-1 (продюсеры
`notifyPlaceContentChanged` для счётчиков дерева мест), WARNING-4 (русские подписи заголовков
`ReportService::export_csv`).

**Verified:** 2026-09-18T12:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Метод

Каждая из четырёх находок проверена независимо от текста SUMMARY.md: код прочитан на HEAD,
целевые regression-тесты запущены заново в этой сессии (не переиспользованы результаты
исполнителя), структурный гейт `check-place-tree-invalidation.mjs` прогнан заново. Для двух
находок (BLOCKER-2, WARNING-1), закрытых через `checkpoint:human-verify`, живой UAT в
`cargo tauri dev` уже подтверждён пользователем (см. `already_established` в задании этой
верификации и `40.1-02-SUMMARY.md`/`40.1-03-SUMMARY.md` Task Commits) — не переспрашивался
повторно, доверие оправдано только потому, что подтверждение исходит от прямого сообщения
пользователя, а не от текста SUMMARY.

Дополнительно прочитан `40.1-REVIEW.md` (code review этой фазы, `status: issues_found`,
2 warning-находки) — обе перепроверены построчно в текущем коде, не приняты на веру.

## Goal Achievement

### Roadmap Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Попытка удалить место, участвовавшее в перемещениях, отклоняется русскоязычным сообщением с точными счётчиками; предполётная проверка считает `place_movements`, зафиксировано тестом | ✓ VERIFIED | `SubtreeStats.referencing_movement_count` (places.rs:156), SQL-подзапрос + маппинг (places_sqlite.rs:191,200), обе предполётные суммы (places_sqlite.rs:575, place_service.rs:487), пятая `ru_plural`-ветка (place_service.rs:935-940). Тесты `delete_blocked_by_movement_from_place_id_even_when_otherwise_empty` и `..._to_place_id_...` запущены заново в этой сессии — `2 passed` (полный файл `places_delete_blocked.rs` — `8 passed; 0 failed`). |
| SC2 | Пользователь может построить отчёт «Перемещения» с фильтром одновременно по месту и по типу устройства — HST-04 выполняется дословно | ✓ VERIFIED | `ReportFilters.svelte` — третий `Dropdown` (`variant="select"`, `flat={true}`, не native `<select>`) в ветке `movements`, читает `typeId`/`deviceTypes`, вызывает `onFilterChange?.({ type_id })` (строки 119,155-178). Бэкенд `report_service.rs:1449-1455` не тронут. Живой UAT (`cargo tauri dev`) approved пользователем — заявлено в задании верификации как уже установленный факт. `report_movements`/`report_csv_export` тестовые файлы (включая тесты `omits_type_column_when_type_filter_set`) перезапущены заново — `15 passed`/`2 passed`, 0 failed. |
| SC3 | Счётчики содержимого в дереве мест обновляются после смены места предмета с любого экрана, а не только после массового переноса | ⚠️ PARTIAL | Три из must_haves плана 03 write-site реализованы, покрыты INV-7 гейтом, живой UAT approved (`ui/scripts/check-place-tree-invalidation.mjs` — `PASS — 0 нарушений`, перезапущен заново). НО: четвёртый, более частый write-site (`CartridgesPage.svelte`'s `handleOperationSuccess`, lifecycle-операции install/return_to_stock/to_refill/from_refill/write_off) меняет `cartridges.place_id` на сервере, но не вызывает `notifyPlaceContentChanged` — счётчики дерева остаются устаревшими после этих операций до навигации прочь и обратно. INV-7 гейт этот write-site не проверяет. См. gap ниже и `40.1-REVIEW.md` WR-01 (уже задокументировано ревью фазы, не исправлено). |
| SC4 | Заголовки CSV-экспорта отчётов совпадают с подписями на экране и в PDF | ✓ VERIFIED | `export_csv` пишет `wtr.write_record(column_labels)` (report_service.rs:930), `build_reports_export_csv` передаёт `column_labels_for(&report_type)` (tauri_cmds/reports.rs:379), `http/reports.rs` не тронут (общая точка `build_reports_export_csv`, строка 270). Тест `report_movements_export_csv_has_russian_headers` перезапущен заново — `ok`, весь файл `15 passed; 0 failed`. |

**Score:** 3/4 roadmap success criteria fully verified, 1 partial (SC3).

### PLAN-level Must-Haves (детализация по находкам)

#### BLOCKER-1 (Plan 40.1-01, req PLC-01)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Место с записью в `place_movements` (from ИЛИ to) не удаляется | ✓ VERIFIED | Оба направления FK покрыты тестами, перезапущены заново. |
| 2 | Русское сообщение вместо сырого текста SQLite | ✓ VERIFIED | `reason.starts_with("Место нельзя удалить:")`, `!reason.contains("FOREIGN KEY")` — оба ассерта проходят. |
| 3 | Обе предполётные суммы синхронны | ✓ VERIFIED | `place_service.rs:487` и `places_sqlite.rs:575` — независимые суммы, обе включают новое поле. |
| 4 | Тест воспроизводит кейс аудита | ✓ VERIFIED | `delete_blocked_by_movement_from_place_id_even_when_otherwise_empty`/`..._to_place_id_...`, оба `ok`. |
| 5 | Только вымышленные данные в фикстурах | ✓ VERIFIED | `grep -n "Иванов\|Петров\|Склад"` — не проверялся заново вручную, но diff плана 01 не вводит новых ФИО/реквизитов (подтверждено чтением файла целиком). |
| 6 | Закрыто тестом, не только компиляцией | ✓ VERIFIED | Regression-тесты существуют и проходят (см. выше), не только `cargo build`. |

#### BLOCKER-2 (Plan 40.1-02, req HST-04)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Третий контрол «Тип устройства» в ветке movements | ✓ VERIFIED | `ReportFilters.svelte:150-178`. |
| 2 | Кастомный Dropdown, не native `<select>` | ✓ VERIFIED | `Dropdown` компонент, `variant="select"` (это тип поведения дропдауна, не HTML-тег), `flat={true}`. |
| 3 | Источник — уже переданный `deviceTypes`, бэкенд не менялся отдельно | ✓ VERIFIED | `report_service.rs:1449-1455` не в списке файлов плана 02 (не изменён этим планом напрямую под BLOCKER-2; изменения `report_service.rs` в этом плане относятся к отдельному user-requested дополнению `filter_summary`, не к самому фильтру). |
| 4 | Пустой выбор = «Все типы» → `type_id: null` | ✓ VERIFIED | `onPickGroup={(o) => onFilterChange?.({ type_id: o.id === '' ? null : Number(o.id) })}`. |
| 5 | Живая проверка в `cargo tauri dev`, не синтетический харнесс | ✓ VERIFIED (по already_established) | Подтверждено пользователем; в этой сессии дополнительно перезапущены regression-тесты, зависящие от того же кода. |

#### WARNING-1 (Plan 40.1-03, req PLC-06)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Правка предмета через `PlaceEntityViewModal` инвалидирует счётчики | ✓ VERIFIED | `PlaceContents.svelte:415`. |
| 2 | Правка места картриджа из списка «Картриджи» (форма редактирования) | ✓ VERIFIED | `CartridgesPage.svelte:293` (`handleFormSuccess`). |
| 3 | Правка места устройства из списка «Устройства» | ✓ VERIFIED | `DevicesPage.svelte:181`. |
| 4 | INV-1..6 не нарушены, гейт зелёный | ✓ VERIFIED | `node ui/scripts/check-place-tree-invalidation.mjs` → `PASS — 0 нарушений`. |
| 5 | INV-7 проверяет три write-site раздельно | ✓ VERIFIED (для трёх заявленных) / ⚠️ НЕ покрывает четвёртый | См. gap выше. |
| 6 | Каскад «картридж едет за принтером» задокументирован | ✓ VERIFIED | `crates/trackly-app/src/services/device_service.rs` — комментарий у `cascade_place_for_printer_in_tx` (не перепроверен построчно в этой сессии, подтверждён grep в плане/summary). |
| 7 | Закрыто гейтом и живой проверкой | ⚠️ ЧАСТИЧНО | Гейт и живая проверка покрывают три заявленных write-site, но не четвёртый (`handleOperationSuccess`), который де-факто существует в том же файле и меняет то же поле сущности. |

#### WARNING-4 (Plan 40.1-04, req PLC-04, HST-04)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `export_csv` пишет заголовок из `column_labels_for`, не сырые ключи | ✓ VERIFIED | `report_service.rs:930`. |
| 2 | `row_field` продолжает читать по `columns` | ✓ VERIFIED | Цикл по `rows.rows` не менялся (только сигнатура функции расширена). |
| 3 | Индексная согласованность защищена существующим тестом, не задублирована | ✓ VERIFIED | `column_labels_for_is_index_aligned_with_columns_for` doc-комментарий расширен, не новый тест. |
| 4 | Фикс в `build_reports_export_csv`, `http/reports.rs` не менялся отдельно | ✓ VERIFIED | `http/reports.rs` вызывает тот же `build_reports_export_csv` (строка 270), не редактировался этим планом. |
| 5 | `report_movements.rs` тест переписан на русские подписи | ✓ VERIFIED | `report_movements_export_csv_has_russian_headers`, `ok`. |
| 6 | BOM и `;`-разделитель не изменены | ✓ VERIFIED | `csv_export_has_utf8_bom_and_semicolon` — `ok`. |
| 7 | Данные в тестах — вымышленные | ✓ VERIFIED | Не введено новых реальных данных (только подписи `column_labels_for`, взятые из уже существующего кода). |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/trackly-core/src/domain/places.rs` | `SubtreeStats.referencing_movement_count` | ✓ VERIFIED | Поле присутствует, doc-комментарий ссылается на `place_movements`/V040. |
| `crates/trackly-infra/src/repos/places_sqlite.rs` | SQL-подсчёт + дублирующая сумма в `delete_hard` | ✓ VERIFIED | Оба места найдены. |
| `crates/trackly-app/src/services/place_service.rs` | Предполётная сумма + `build_delete_blocked_message` ветка | ✓ VERIFIED | Оба места найдены. |
| `crates/trackly-app/tests/places_delete_blocked.rs` | Регрессионные тесты на оба направления FK | ✓ VERIFIED | `8 passed; 0 failed` (перезапущено). |
| `ui/src/features/reports/ReportFilters.svelte` | Контрол «Тип устройства» | ✓ VERIFIED | Найден, wired, `pnpm exec svelte-check`/`pnpm run lint` заявлены зелёными в already_established (не перезапускались повторно). |
| `ui/src/features/places/PlaceContents.svelte` | Второй вызов `notifyPlaceContentChanged` | ✓ VERIFIED | Найден на строке 415. |
| `ui/src/features/cartridges/CartridgesPage.svelte` | `notifyPlaceContentChanged` в `handleFormSuccess` | ✓ VERIFIED (частично — см. WR-01) | Найден в `handleFormSuccess`, отсутствует в `handleOperationSuccess`. |
| `ui/src/features/devices/DevicesPage.svelte` | `notifyPlaceContentChanged` в `onSaved` | ✓ VERIFIED | Найден на строке 181. |
| `ui/scripts/check-place-tree-invalidation.mjs` | INV-7 инвариант | ✓ VERIFIED (для заявленного скоупа) | `PASS — 0 нарушений`, не расширен на четвёртый write-site. |
| `crates/trackly-app/src/services/report_service.rs` | `export_csv(rows, columns, column_labels)` | ✓ VERIFIED | Сигнатура и `write_record(column_labels)` найдены. |
| `crates/trackly-app/src/tauri_cmds/reports.rs` | `build_reports_export_csv` передаёт `column_labels_for` | ✓ VERIFIED | Найдено на строке 379. |
| `crates/trackly-app/tests/report_movements.rs` | Тест на русские заголовки | ✓ VERIFIED | `report_movements_export_csv_has_russian_headers` — `ok`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `places_sqlite.rs::subtree_stats_impl` | `places.rs::SubtreeStats` | SQL `AS referencing_movement_count` → `row.get(5)?` | ✓ WIRED | Подтверждено. |
| `place_service.rs::delete_hard` | `build_delete_blocked_message` | `stats.referencing_movement_count > 0` → `ru_plural` | ✓ WIRED | Подтверждено, тестом. |
| `ReportFilters.svelte` | `ReportsPage.svelte` | `onFilterChange?.({ type_id })` | ✓ WIRED | Подтверждено. |
| `ReportsPage.svelte` | `report_service.rs` | `filter.type_id` в `query_movements_inner` | ✓ WIRED | Не тронуто этим планом, уже было готово (Phase 40). |
| `CartridgesPage.svelte::handleFormSuccess` | `placeContentEvents.svelte.ts` | `notifyPlaceContentChanged([old,new])` | ✓ WIRED | Подтверждено. |
| `CartridgesPage.svelte::handleOperationSuccess` | `placeContentEvents.svelte.ts` | — | ✗ NOT WIRED | Не вызывает `notifyPlaceContentChanged` — см. gap. |
| `DevicesPage.svelte::onSaved` | `placeContentEvents.svelte.ts` | `notifyPlaceContentChanged([old,new])` | ✓ WIRED | Подтверждено, включая плумбинг через `DeviceFormBody`/`DeviceFormModal`. |
| `tauri_cmds/reports.rs::build_reports_export_csv` | `report_service.rs::export_csv` | `ctx.reports.export_csv(&rows, &cols, &labels)` | ✓ WIRED | Подтверждено. |
| `http/reports.rs::handler_export_csv` | `build_reports_export_csv` | общая точка | ✓ WIRED | Подтверждено, HTTP-транспорт получает фикс бесплатно. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| PLC-01 | 40.1-01 | Дерево мест без потери привязок при удалении | ✓ SATISFIED | BLOCKER-1 закрыт (SC1 VERIFIED). |
| HST-04 | 40.1-02, 40.1-04 | Отчёт о перемещениях с фильтром по месту и типу устройства | ✓ SATISFIED | BLOCKER-2 закрыт (SC2 VERIFIED), CSV-заголовки корректны (SC4 VERIFIED). |
| PLC-06 (попутно) | 40.1-03 | Открыть место и увидеть всё содержимое, включая вложенные | ⚠️ PARTIAL | Базовое требование (просмотр содержимого) не нарушено — это про инвалидацию счётчиков дерева, отдельную от самого просмотра; WARNING-1 закрыт частично (см. SC3 gap). PLC-06 сам по себе (возможность увидеть содержимое) не пострадал, только *живость* счётчиков при определённых операциях. |
| PLC-04 (попутно) | 40.1-04 | Заголовки/списки используют место из дерева, не свободный текст | ✓ SATISFIED | Не по существу этого требования — 40.1-04 фиксирует лишь CSV-заголовки; требование как таковое (устранение свободнотекстового поля) закрыто ещё Phase 39, этот план лишь косвенно упомянут в frontmatter. |

**Orphaned requirements:** нет — все ID из frontmatter четырёх планов (PLC-01, HST-04, PLC-06, PLC-04) присутствуют в REQUIREMENTS.md.

**Документационная заметка (не блокер):** `REQUIREMENTS.md`'s traceability-таблица (строки 147-158) и хвостовая сноска всё ещё привязывают PLC-01/PLC-04/PLC-06/HST-04 к Phase 39/40 и содержат устаревшую оговорку про BLOCKER-2 («HST-04 закрыт с оговоркой… см. BLOCKER-2»), хотя Phase 40.1 эту оговорку уже сняла. Housekeeping, не влияет на код.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | Debt markers (TBD/FIXME/XXX) | — | Не найдено ни в одном из изменённых файлов фазы. |
| `ui/src/features/cartridges/CartridgesPage.svelte` | 263-278 | Отсутствующий вызов `notifyPlaceContentChanged` в `handleOperationSuccess` | ⚠️ Warning | См. gap SC3 / WR-01 — счётчики дерева устаревают после lifecycle-операций картриджа. |
| `crates/trackly-app/src/tauri_cmds/reports.rs` | 483,487 | `build_movements_filter_summary` пробрасывает `AppError::NotFound` через `?` для `from_place_id`/`to_place_id` вместо graceful-деградации (в отличие от соседнего `device_type_name`, который явно деградирует) | ℹ️ Info/Warning (не блокер этой фазы) | Если место, на которое указывает активный фильтр отчёта, удалено между выбором фильтра и печатью, PDF-экспорт целиком падает `NotFound` вместо того, чтобы просто опустить сегмент сводки. Уже задокументировано ревью фазы (WR-02, `40.1-REVIEW.md`), не исправлено. Не входит в исходные четыре находки аудита (это дефект в user-requested дополнении Плана 02, `filter_summary`), поэтому не блокирует статус фазы, но фиксируется здесь как известный незакрытый риск. |
| `crates/trackly-app/src/tauri_cmds/reports.rs` | 483-493 | `filter_summary` разделяет части `"; "` без экранирования буквального `;` в имени места/типа | ℹ️ Info | Косметика, не влияет на данные/безопасность (см. `40.1-REVIEW.md` IN-02). |
| `crates/trackly-app/src/pdf/html_templates.rs` | 490-495 | Устаревший doc-комментарий про `report.html`'s `bodies.get(2)` | ℹ️ Info | Логика теста корректна, комментарий вводит в заблуждение (см. `40.1-REVIEW.md` IN-01). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Место с историей перемещений не удаляется (оба направления FK) | `cargo test -p trackly-app --test places_delete_blocked -- --test-threads=1` | `8 passed; 0 failed` | ✓ PASS |
| CSV-заголовки отчёта «Перемещения» — русские подписи | `cargo test -p trackly-app --test report_movements --test report_csv_export -- --test-threads=1` | `15 passed` + `2 passed`, `0 failed` | ✓ PASS |
| Структурный гейт инвалидации дерева (INV-1..7) | `node ui/scripts/check-place-tree-invalidation.mjs` | `PASS — 0 нарушений` | ✓ PASS (для заявленного скоупа — НЕ покрывает handleOperationSuccess) |
| Живой прогон `cargo tauri dev` (BLOCKER-2, WARNING-1) | Ручная проверка пользователем | Approved (уже установлено на момент верификации) | ✓ PASS (по already_established, не переспрошено повторно) |

### Human Verification Required

Нет новых пунктов. Оба checkpoint:human-verify (Task 2 плана 02, Task 4 плана 03) уже пройдены и
approved пользователем до начала этой верификации (см. `already_established` в задании).

## Gaps Summary

Три из четырёх находок аудита закрыты полностью и подтверждены независимо (код прочитан на HEAD,
regression-тесты перезапущены заново в этой сессии, а не приняты по тексту SUMMARY): BLOCKER-1,
BLOCKER-2, WARNING-4.

**WARNING-1 закрыт частично.** Три write-site, явно перечисленные в must_haves плана 40.1-03
(`PlaceEntityViewModal`, форма редактирования картриджа, форма редактирования устройства),
реализованы, покрыты структурным гейтом INV-7 и подтверждены живой проверкой. Но
`CartridgesPage.svelte` имеет ЧЕТВЁРТЫЙ, более часто используемый write-site —
`OperationModal` (lifecycle-операции: установка в принтер, возврат на склад, отправка/приём
из заправки, списание) — который тоже меняет `cartridges.place_id` на сервере, но не вызывает
`notifyPlaceContentChanged`. Это ровно тот класс регрессии, который WARNING-1/INV-7 должны были
закрыть структурно, и он остаётся невидимым для гейта, введённого именно с этой целью. Находка
уже зафиксирована `40.1-REVIEW.md` (WR-01, warning-уровень, с готовым построчным фиксом), но
исправление не выполнено на момент этой верификации.

Дополнительно, не блокируя статус фазы (это дефект в user-requested дополнении Плана 02, а не в
одной из исходных четырёх находок аудита), отмечен WR-02: `build_movements_filter_summary` может
целиком обрушить PDF-экспорт отчёта, если место, на которое указывает активный фильтр, было
удалено между выбором фильтра и печатью — вместо graceful-деградации, применённой по соседству
для `device_type_name`. Рекомендуется закрыть вместе с WR-01 в отдельном коротком плане перед
переходом к Фазе 41, либо явно принять как известный риск через override.

**Рекомендация:** не считать Фазу 40.1 полностью закрытой, пока WR-01 (и по желанию WR-02) не
исправлены отдельным небольшим планом (`/gsd-plan-phase 40.1 --gaps`), либо пока пользователь не
подпишет override, явно принимающий этот остаточный риск (четвёртый write-site как «известное
ограничение», аналогично уже принятому D-18 про каскад принтер-картридж).

---
_Verified: 2026-09-18T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
