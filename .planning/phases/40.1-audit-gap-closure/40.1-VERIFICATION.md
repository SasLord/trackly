---
phase: 40.1-audit-gap-closure
verified: 2026-09-17T21:13:57Z
status: gaps_found
score: 3.5/4 roadmap success criteria verified (SC1, SC2, SC4 VERIFIED; SC3 PARTIAL — new uncovered write-site found)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "3/4 roadmap success criteria verified (SC1, SC2, SC4 VERIFIED; SC3 PARTIAL)"
  gaps_closed:
    - "WR-01: CartridgesPage.svelte's handleOperationSuccess (lifecycle-операции install/return_to_stock/to_refill/from_refill/write_off) теперь вызывает notifyPlaceContentChanged со старым+новым местом; INV-7 гейт переработан на функцию-уровневую (functionBody) локализацию, различает handleFormSuccess и handleOperationSuccess раздельно, с уникальными мутационными самотестами на каждую (commit 61ba1c5b)."
    - "WR-02: build_movements_filter_summary теперь резолвит from_place_id/to_place_id через новый хелпер place_full_path_or_none (AppError::NotFound → Ok(None)) вместо необёрнутого ?, симметрично уже-graceful device_type_name; новый regression-тест report_movements_export_pdf_omits_deleted_place_from_filter_summary_instead_of_failing (commit 8d7a22a7)."
  gaps_remaining:
    - "НОВАЯ находка этого раунда (не была видна раунду 1): пятый клиентский write-site, меняющий cartridges.place_id (RequestDetail.svelte's handleInstallSuccess, установка картриджа через утверждение заявки), не вызывает notifyPlaceContentChanged и не покрыт гейтом INV-7 — см. gap ниже."
  regressions: []
gaps:
  - truth: "Счётчики содержимого в дереве мест обновляются после смены места предмета с ЛЮБОГО экрана (ROADMAP Success Criterion 3 / WARNING-1) — структурно закрыто гейтом, не только точечным фиксом."
    status: partial
    reason: >
      Раунд 2 закрыл ровно то, что было найдено раундом 1 (WR-01: CartridgesPage.svelte's
      handleOperationSuccess) — фикс корректен, INV-7 расширен и функция-уровнево различает
      handleFormSuccess/handleOperationSuccess внутри одного файла. Однако при независимой
      проверке "все ли клиентские write-site покрыты" найден ПЯТЫЙ write-site, не упомянутый
      ни в одном плане/SUMMARY/REVIEW этой фазы: OperationModal переиспользуется ДВУМЯ
      клиентскими экранами — CartridgesPage.svelte (теперь исправлен) и
      RequestDetail.svelte (установка картриджа при утверждении заявки на замену, REQ-05,
      op="install", преlFillPrinterId — тот же серверный CartridgeService transition,
      меняющий cartridges.place_id на место принтера). RequestDetail.svelte's
      handleInstallSuccess был ФАКТИЧЕСКИ ЗАТРОНУТ тем же коммитом WR-01 (61ba1c5b) — сигнатура
      колбэка расширена с (cartridgeId: number) до (cartridge: CartridgeDto), потому что
      OperationModal's onSuccess контракт был расширен для нужд handleOperationSuccess — но вызов
      notifyPlaceContentChanged в этот обработчик не добавлен. INV-7 (check-place-tree-invalidation.mjs)
      вообще не читает и не проверяет RequestDetail.svelte — файл отсутствует в списке read()
      в main(). Это ровно тот класс регрессии, который WARNING-1/INV-7 должны закрывать
      структурно (SC3 дословно требует "с любого экрана"), и он остаётся невидимым для гейта,
      введённого специально с этой целью — даже после того, как этот же файл был открыт и
      частично изменён в рамках закрытия WR-01.
    artifacts:
      - path: "ui/src/features/requests/RequestDetail.svelte"
        issue: "handleInstallSuccess (строки 398-425, wired на OperationModal.onSuccess строка 734, <OperationModal op=\"install\" .../> строки 723-735) не вызывает notifyPlaceContentChanged после успешной установки картриджа в принтер через утверждение заявки — при этом сервер (CartridgeService install transition) реально меняет cartridges.place_id на место принтера."
      - path: "ui/scripts/check-place-tree-invalidation.mjs"
        issue: "main() читает только TREE/STORE/CONTENTS/CARTRIDGES_PAGE/DEVICES_PAGE — RequestDetail.svelte не читается и не проверяется вовсе; checkProducers() не знает о пятом write-site."
    missing:
      - "В RequestDetail.svelte's handleInstallSuccess захватить место ДО операции (запрос принтера/картриджа недоступен напрямую, но `cartridge.place_id` из ответа transition — место ПОСЛЕ; местом ДО, скорее всего, было null/склад — потребуется решить, откуда брать старое место, т.к. cartridge=null передаётся в OperationModal и предыдущее место картриджа на этом экране не подгружено заранее) и вызвать notifyPlaceContentChanged симметрично CartridgesPage.svelte's handleOperationSuccess."
      - "Расширить INV-7 (check-place-tree-invalidation.mjs) пятым write-site: прочитать RequestDetail.svelte и проверить тело handleInstallSuccess на вызов notifyPlaceContentChanged, по аналогии с functionBody-локализацией, уже применённой к CartridgesPage.svelte."
deferred: []
human_verification: []
---

# Phase 40.1: Пробелы аудита v1.4 — Verification Report (Re-verification, Round 2)

**Phase Goal:** Закрыть четыре находки аудита вехи v1.4, обнаруженные на межфазных стыках —
BLOCKER-1 (предполётная проверка удаления места), BLOCKER-2 / HST-04 (фильтр «по типу
устройства» в отчёте «Перемещения» достижим из UI), WARNING-1 (продюсеры
`notifyPlaceContentChanged` для счётчиков дерева мест), WARNING-4 (русские подписи заголовков
`ReportService::export_csv`).

**Verified:** 2026-09-17T21:13:57Z (HEAD `8feb6e5e`)
**Status:** gaps_found
**Re-verification:** Yes — после закрытия WR-01/WR-02, найденных раундом 1

## Метод

Раунд 1 (`status: gaps_found`, коммит `6ec7ca14`) нашёл единственный незакрытый пробел —
WR-01 (четвёртый write-site `CartridgesPage.svelte`'s `handleOperationSuccess` не вызывал
`notifyPlaceContentChanged`) — и одну не-блокирующую находку, зафиксированную ревью,
WR-02 (`build_movements_filter_summary` мог полностью обрушить PDF-экспорт при удалённом
месте фильтра). Три остальные находки аудита (BLOCKER-1, BLOCKER-2, WARNING-4) были
VERIFIED раундом 1 независимо (код прочитан на HEAD, тесты перезапущены в сессии) — их
файлы НЕ изменялись после раунда 1 (`git diff --stat 6ec7ca14..HEAD` подтверждает: только
`reports.rs`, `report_movements.rs`, `check-place-tree-invalidation.mjs`,
`CartridgesPage.svelte`, `OperationModal.svelte`, `RequestDetail.svelte` и два SUMMARY
изменились), поэтому для них выполнена только быстрая grep-подтверждающая проверка на
текущем HEAD, без повторного полного прогона тестов.

Для WR-01/WR-02 выполнена ПОЛНАЯ трёхуровневая проверка заново (не по тексту SUMMARY):
код прочитан на HEAD, `node ui/scripts/check-place-tree-invalidation.mjs` перезапущен,
целевой тестовый файл (`report_movements.rs`, 16 тестов) перезапущен заново в этой сессии,
`pnpm exec svelte-check` перезапущен (0 ошибок). Дополнительно, по прямому требованию
задания, проведён независимый обратный аудит: "все ли клиентские write-site, меняющие место
устройства/картриджа, покрыты и вызовом notifyPlaceContentChanged, И гейтом INV-7?" — найден
grep'ом всех потребителей `OperationModal` (`grep -rn "OperationModal" ui/src`), а не
переиспользованием списка write-site из плана/SUMMARY. Это вскрыло пятый write-site
(`RequestDetail.svelte`), не упомянутый ни в одном артефакте фазы.

## Goal Achievement

### Roadmap Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Попытка удалить место, участвовавшее в перемещениях, отклоняется русскоязычным сообщением с точными счётчиками | ✓ VERIFIED (не изменялось с раунда 1) | `referencing_movement_count` присутствует в `places.rs:156`, `places_sqlite.rs:191,200,575`, `place_service.rs:487,935-940` — grep-подтверждено на HEAD, файлы не менялись с раунда 1. |
| SC2 | Пользователь может построить отчёт «Перемещения» с фильтром одновременно по месту и по типу устройства | ✓ VERIFIED (не изменялось с раунда 1) | `ReportFilters.svelte` не менялся с раунда 1 (не в diff `6ec7ca14..HEAD`). |
| SC3 | Счётчики содержимого в дереве мест обновляются после смены места предмета с любого экрана, а не только после массового переноса | ⚠️ PARTIAL (было 3/4 write-site, теперь 4/5 write-site — прогресс, но не полное закрытие) | WR-01 закрыт корректно и подтверждён заново (см. ниже). Но независимый аудит всех потребителей `OperationModal` вскрыл ПЯТЫЙ write-site — `RequestDetail.svelte`'s `handleInstallSuccess` (установка картриджа через утверждение заявки) — который тоже меняет `cartridges.place_id` на сервере, но не вызывает `notifyPlaceContentChanged`, и не покрыт INV-7. См. gap. |
| SC4 | Заголовки CSV-экспорта отчётов совпадают с подписями на экране и в PDF | ✓ VERIFIED (не изменялось с раунда 1) | `report_service.rs:930` (`write_record(column_labels)`) — grep-подтверждено, файл не менялся с раунда 1. |

**Score:** 3/4 полностью VERIFIED без изменений, SC3 остаётся PARTIAL (прогресс есть — WR-01 закрыт корректно, — но новый write-site найден при более широком, чем в раунде 1, аудите потребителей `OperationModal`).

### WR-01 Closure — Full Re-check

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | `handleOperationSuccess` (CartridgesPage.svelte) вызывает `notifyPlaceContentChanged` со старым+новым местом | ✓ VERIFIED | `CartridgesPage.svelte:263-282` — `oldPlaceId` из `operationModalCartridge?.place_id` (захвачен до открытия модалки), `newPlaceId` из `updated.place_id` (ответ transition), dedup + null-фильтр, `notifyPlaceContentChanged(changedPlaceIds)`. |
| 2 | `OperationModal.onSuccess` контракт расширен до полного `CartridgeDto` | ✓ VERIFIED | `OperationModal.svelte:65` — `onSuccess: (_cartridge: CartridgeDto) => void \| Promise<void>`; `:758` — `await onSuccess(transitioned)`. |
| 3 | INV-7 различает `handleFormSuccess`/`handleOperationSuccess` раздельно (не файл-уровнево) | ✓ VERIFIED | `check-place-tree-invalidation.mjs:491-511` — цикл `for (const fnName of ['handleFormSuccess', 'handleOperationSuccess'])`, локализация через `functionBody(cartridgesCode, fnName)`. |
| 4 | Гейт зелёный на текущем коде | ✓ VERIFIED | `node ui/scripts/check-place-tree-invalidation.mjs` → `PASS — 0 нарушений` (перезапущено в этой сессии). |
| 5 | Единственный другой потребитель того же колбэка (`RequestDetail.svelte`) обновлён под новую сигнатуру | ✓ VERIFIED (сигнатура), ✗ НЕ ДОБАВЛЕН вызов инвалидации | `git diff 6ec7ca14..HEAD -- ui/src/features/requests/RequestDetail.svelte` — только тип параметра `cartridgeId: number` → `cartridge: CartridgeDto` и `linkedCartridgeId: cartridgeId` → `cartridge.id`. `notifyPlaceContentChanged` НЕ добавлен — это и есть новый gap (см. ниже). |
| 6 | Живая проверка `cargo tauri dev` (install/return_to_stock/write_off со сменой места) | ✓ PASS (по already_established в SUMMARY) | Approved пользователем per `40.1-03-SUMMARY.md` Gap closure section — не переспрашивалось повторно в этой сессии. |

**Вывод по WR-01:** фикс корректен для того, что было найдено раундом 1. Но закрытие
неполное относительно формулировки самого WARNING-1/SC3 ("с любого экрана") — см. новый
gap ниже, найденный независимым обратным аудитом потребителей `OperationModal`, а не
повторной проверкой уже задокументированного.

### WR-02 Closure — Full Re-check

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | `place_full_path_or_none` хелпер добавлен, деградирует на `AppError::NotFound` | ✓ VERIFIED | `reports.rs:512-522` — `match ... Ok(path) => Ok(Some(path)), Err(AppError::NotFound {..}) => Ok(None), Err(e) => Err(e)`. |
| 2 | `build_movements_filter_summary` использует хелпер вместо голого `?` для обоих направлений | ✓ VERIFIED | `reports.rs:479-491` — оба `from_id`/`to_id` резолвятся через `place_full_path_or_none(...).await?`, симметрично `device_type_name`. |
| 3 | Новый regression-тест воспроизводит сценарий аудита (удалённое место в фильтре не роняет PDF) | ✓ VERIFIED | `report_movements_export_pdf_omits_deleted_place_from_filter_summary_instead_of_failing` — перезапущен в этой сессии, `ok`. |
| 4 | Весь файл `report_movements.rs` зелёный, не только новый тест | ✓ VERIFIED | `cargo test -p trackly-app --test report_movements -- --test-threads=1` → `16 passed; 0 failed` (перезапущено в этой сессии, включая новый тест — было 15 в раунде 1, теперь 16). |
| 5 | Живая проверка `cargo tauri dev` (удаление места, активного в фильтре, затем печать) | ✓ PASS (по already_established в SUMMARY) | Approved пользователем per `40.1-02-SUMMARY.md` Gap closure section. |

**Вывод по WR-02:** закрыт полностью, без замечаний.

### Новая находка: пятый write-site не покрыт (RequestDetail.svelte)

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | `RequestDetail.svelte` использует `OperationModal` с `op="install"` | ✓ подтверждено | `RequestDetail.svelte:723-735` — `<OperationModal open={operationModalOpen} op="install" cartridge={null} preFillPrinterId={...} onSuccess={handleInstallSuccess} />`. |
| 2 | Серверная транзакция `install` реально меняет `cartridges.place_id` | ✓ подтверждено | `cartridge_service.rs:430-459` — D-13, install без явного `place_id` резолвит место из `devices.place_id` целевого принтера и перезаписывает `payload.place_id`; UPDATE cartridges SET place_id=... выполняется тем же путём, что и для CartridgesPage's install. |
| 3 | `handleInstallSuccess` вызывает `notifyPlaceContentChanged` | ✗ НЕ ВЫЗЫВАЕТ | `RequestDetail.svelte:398-425` — функция читает `cartridge.id` для `linkedCartridgeId`, но нигде не импортирует/не вызывает `notifyPlaceContentChanged` (grep по файлу — 0 вхождений). |
| 4 | INV-7 проверяет `RequestDetail.svelte` | ✗ НЕ ПРОВЕРЯЕТ | `check-place-tree-invalidation.mjs`'s `main()` читает только `TREE/STORE/CONTENTS/CARTRIDGES_PAGE/DEVICES_PAGE` — файл `RequestDetail.svelte` отсутствует в списке чтения гейта целиком. |

**Является ли это блокером цели фазы?** Да, для формулировки самого SC3/WARNING-1
("с любого экрана", дословно из ROADMAP) — это тот же класс регрессии, который фаза
закрывала, найденный на пятом, не входившем в исходный аудит write-site. Он не был
пропущен по невнимательности старого раунда — он не входил в must_haves ни одного из
четырёх планов фазы (`grep -rn "RequestDetail" .planning/phases/40.1-audit-gap-closure/*-PLAN.md` — 0 совпадений), то есть это НОВАЯ находка этого раунда верификации, а не регрессия
уже проверенного. Тем не менее технически он падает под тот же Success Criterion и ту же
формулировку WARNING-1, поэтому классифицируется как BLOCKER для статуса `passed` этой
фазы — не как отложенный follow-up, потому что: (а) он в том же файле-классе (потребитель
`OperationModal`, тот же паттерн install), (б) сам файл `RequestDetail.svelte` был открыт и
частично изменён в рамках закрытия WR-01 тем же коммитом (`61ba1c5b`), то есть исполнитель
имел файл перед глазами и не заметил отсутствие вызова, (в) фикс тривиален по объёму
(одна функция + расширение гейта), не требует новой архитектуры.

### Required Artifacts (обновлено)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `ui/src/features/cartridges/CartridgesPage.svelte` | `notifyPlaceContentChanged` и в `handleFormSuccess`, и в `handleOperationSuccess` | ✓ VERIFIED | Оба найдены, раздельно проверены гейтом. |
| `ui/scripts/check-place-tree-invalidation.mjs` | INV-7 различает write-site функция-уровнево там, где нужно | ✓ VERIFIED (для 4 покрытых write-site) / ⚠️ не читает `RequestDetail.svelte` | См. gap. |
| `crates/trackly-app/src/tauri_cmds/reports.rs` | `place_full_path_or_none` + использование в `build_movements_filter_summary` | ✓ VERIFIED | `reports.rs:479-491,512-522`. |
| `crates/trackly-app/tests/report_movements.rs` | Новый тест на graceful-деградацию удалённого места фильтра | ✓ VERIFIED | 16/16 passed, включая новый тест. |
| `ui/src/features/requests/RequestDetail.svelte` | Отсутствует в must_haves фазы, но релевантен по факту (см. gap) | ✗ НЕ ПОКРЫТ | Пятый write-site без инвалидации и без гейта. |

### Key Link Verification (дельта к раунду 1)

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `CartridgesPage.svelte::handleOperationSuccess` | `placeContentEvents.svelte.ts` | `notifyPlaceContentChanged(changedPlaceIds)` | ✓ WIRED (было NOT WIRED в раунде 1) | Подтверждено. |
| `RequestDetail.svelte::handleInstallSuccess` | `placeContentEvents.svelte.ts` | — | ✗ NOT WIRED (новая находка) | Не вызывает; не был в скоупе раунда 1. |
| `reports.rs::build_movements_filter_summary` | `place_full_path_or_none` | `.await?` на `Result<Option<String>, AppError>` | ✓ WIRED (было hard-fail в раунде 1) | Подтверждено. |

### Requirements Coverage

Без изменений относительно раунда 1 — PLC-01, HST-04 SATISFIED; PLC-06 остаётся PARTIAL
(теперь по новой, более узкой причине — не WR-01, который закрыт, а вновь найденный пятый
write-site); PLC-04 SATISFIED (не по существу, косвенно упомянут). Orphaned requirements: нет.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | Debt markers (TBD/FIXME/XXX) в изменённых файлах раунда 2 | — | Не найдено (`grep -n "TBD\|FIXME\|XXX"` по всем 6 изменённым файлам — 0 совпадений). |
| `ui/src/features/requests/RequestDetail.svelte` | 398-425 | Отсутствующий вызов `notifyPlaceContentChanged` в `handleInstallSuccess` | ⚠️ Warning → классифицирован как BLOCKER для статуса фазы (см. gap) | Счётчики дерева «Места» остаются устаревшими после установки картриджа через утверждение заявки, пока пользователь не переключит раздел и не вернётся в «Места». |
| `ui/scripts/check-place-tree-invalidation.mjs` | main() | Гейт не читает `RequestDetail.svelte` | ⚠️ Warning | Будущая регрессия (или текущее отсутствие фикса) на этом write-site невидима гейту, специально введённому для предотвращения именно такого класса регрессий. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| INV-7 гейт (4 покрытых write-site) | `node ui/scripts/check-place-tree-invalidation.mjs` | `PASS — 0 нарушений` | ✓ PASS (для заявленного скоупа — не покрывает RequestDetail.svelte) |
| WR-02 regression + весь файл отчёта «Перемещения» | `cargo test -p trackly-app --test report_movements -- --test-threads=1` | `16 passed; 0 failed` | ✓ PASS |
| Типы/раннтайм фронтенда без ошибок | `pnpm exec svelte-check` | `0 errors, 60 warnings` (warnings — pre-existing, несвязанные файлы: `PeriodSelector.svelte`, `AccessDenied.svelte`) | ✓ PASS |
| Изменённые файлы раунда 2 без debt-маркеров | `grep -n "TBD\|FIXME\|XXX"` по 6 изменённым файлам | 0 совпадений | ✓ PASS |

### Human Verification Required

Нет новых пунктов, требующих ЖИВОГО прогона в этой верификации — WR-01/WR-02's checkpoint'ы
уже approved пользователем (см. Gap closure секции в `40.1-02-SUMMARY.md`/`40.1-03-SUMMARY.md`).
Новая находка (пятый write-site) ещё НЕ исправлена — живая проверка потребуется после фикса,
не сейчас.

## Gaps Summary

**Раунд 1 → Раунд 2: прогресс подтверждён.** WR-01 (CartridgesPage.svelte's
`handleOperationSuccess`) и WR-02 (`build_movements_filter_summary` hard-fail на удалённом
месте фильтра) оба закрыты корректно, регрессионными тестами и структурным гейтом (там, где
гейт применим), с одобренной живой проверкой пользователем. BLOCKER-1, BLOCKER-2, WARNING-4
остаются VERIFIED без изменений (файлы не трогались с раунда 1).

**SC3/WARNING-1 остаётся частично закрытым — но по НОВОЙ причине.** Независимый обратный
аудит "какие ещё клиентские экраны меняют место устройства/картриджа" (не ограниченный
списком write-site из планов фазы) нашёл пятый write-site: `RequestDetail.svelte`'s
`handleInstallSuccess` (установка картриджа при утверждении заявки на замену, REQ-05).
Он использует тот же компонент `OperationModal` с `op="install"`, вызывающий ту же
серверную транзакцию, что меняет `cartridges.place_id` на место принтера — но клиентский
обработчик успеха не инвалидирует счётчики дерева, и структурный гейт INV-7 не проверяет
этот файл вовсе. Примечательно, что этот файл был частично изменён тем же коммитом,
закрывшим WR-01 (сигнатура колбэка расширена под новый контракт `OperationModal.onSuccess`),
но вызов инвалидации туда добавлен не был.

**Рекомендация:** не считать Фазу 40.1 полностью закрытой (WARNING-1 не закрыт структурно
в исходной формулировке "с любого экрана"), пока новый пятый write-site
(`RequestDetail.svelte`) не получит и вызов `notifyPlaceContentChanged`, и покрытие INV-7
гейтом — небольшой, хорошо очерченный фикс, аналогичный по объёму только что выполненному
WR-01. Альтернатива: пользователь может явно принять его как известный остаточный риск через
`overrides:` в этом файле (аналогично уже принятому D-18 про каскад принтер-картридж), если
сочтёт частоту установки картриджа через заявки достаточно низкой, чтобы отложить.

---
_Verified: 2026-09-17T21:13:57Z_
_Verifier: Claude (gsd-verifier)_
