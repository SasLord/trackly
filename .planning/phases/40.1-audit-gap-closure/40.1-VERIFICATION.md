---
phase: 40.1-audit-gap-closure
verified: 2026-09-18T05:40:00Z
status: gaps_found
score: 3.5/4 roadmap success criteria verified (SC1, SC2, SC4 VERIFIED; SC3 PARTIAL — новый непокрытый класс write-site найден)
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "3.5/4 roadmap success criteria verified (SC1, SC2, SC4 VERIFIED; SC3 PARTIAL — новый write-site найден)"
  gaps_closed:
    - "WR-05: RequestDetail.svelte's handleInstallSuccess (установка картриджа при утверждении заявки, REQ-05) теперь вызывает notifyPlaceContentChanged со старым (снимок previousPlaceId из OperationModal ДО transition) и новым местом; OperationModal.onSuccess расширен вторым опциональным параметром previousPlaceId — CartridgesPage.handleOperationSuccess его игнорирует, обратная совместимость подтверждена (структурная типизация TS, gate + сборка зелёные). INV-7 расширен пятым write-site, локализованным на тело handleInstallSuccess; уникальность анкера подтверждена мутационным самотестом, независимо перезапущенным в этой сессии (RED ровно на этом write-site, остальные 4 целы)."
  gaps_remaining:
    - "НОВАЯ находка этого раунда: клиентский write-site класса «Акты приёма-передачи» (ActsPage.svelte's handleSaved/handleEditSaved/handleReturnSuccess) меняет devices.place_id на сервере тем же классом операции, что и уже закрытые 5 write-site, но не вызывает notifyPlaceContentChanged и не покрыт INV-7 вовсе — см. gap ниже."
  regressions: []
gaps:
  - truth: "Счётчики содержимого в дереве мест обновляются после смены места предмета с ЛЮБОГО экрана (ROADMAP Success Criterion 3 / WARNING-1) — структурно закрыто гейтом на ВСЕ клиентские write-site, а не только на пять, найденные раундами 1-2."
    status: partial
    reason: >
      Раунд 3 закрыл ровно то, что нашёл раунд 2 (WR-05: RequestDetail.svelte's
      handleInstallSuccess) — фикс корректен, подтверждён независимым мутационным
      самотестом (RED на удалении вызова ровно на write-site 5, 0 маскировки других
      четырёх), OperationModal's расширенная сигнатура onSuccess обратно совместима
      с единственным другим потребителем (CartridgesPage.svelte — структурная
      типизация TS позволяет функции с меньшим числом параметров реализовывать тип
      колбэка с доп. опциональным аргументом; подтверждено — CartridgesPage не
      менялся, ошибок типов нет, `pnpm exec svelte-check` 0 ошибок).

      Задание этого раунда прямо потребовало независимого поиска "device flows too,
      not only OperationModal consumers" — по этому направлению найден ШЕСТОЙ,
      структурно иной класс write-site, ни разу не упомянутый ни в одном плане/
      SUMMARY/REVIEW фазы и не покрытый ни INV-7, ни ручным вызовом:
      акты приёма-передачи (`ActFormBody.svelte`/`ReturnModal.svelte`, оркестрируются
      `ActsPage.svelte`). Создание акта (`handleSaved`), редактирование акта
      (`handleEditSaved`) и оформление возврата (`handleReturnSuccess`) — все три
      меняют `devices.place_id` через `CartridgeService`-аналогичный серверный путь
      (`act_service.rs`: `update_status_and_place_in_tx` при создании,
      симметричный путь при редактировании, `update_full_in_tx` при возврате;
      каждый путь также пишет запись в `place_movements` через
      `record_movement_if_applicable`). Ни один из трёх клиентских обработчиков
      успеха в `ActsPage.svelte` не вызывает `notifyPlaceContentChanged`; ни один
      файл feature `acts/` не читается гейтом `check-place-tree-invalidation.mjs`
      (`main()` читает только TREE/STORE/CONTENTS/CARTRIDGES_PAGE/DEVICES_PAGE/
      REQUEST_DETAIL).

      Это тот же класс дефекта, который WARNING-1/SC3 существуют, чтобы закрыть
      структурно ("с любого экрана"), и по значимости — с учётом, что «акты
      приёма-передачи» прямо названы Core Value проекта в CLAUDE.md — как минимум
      не менее важен, чем уже закрытые install-через-заявку (WR-05) или
      lifecycle-операции картриджей (WR-01).
    artifacts:
      - path: "ui/src/features/acts/ActsPage.svelte"
        issue: >
          Три обработчика успеха — handleSaved (строки 176-181, создание акта
          «передача», вызывается из <ActFormModal onSaved={handleSaved} .../>),
          handleEditSaved (строки 222-233, редактирование акта, place_id
          редактируем через PlacePicker в ActFormBody.svelte без disabled),
          handleReturnSuccess (строки 241-266, оформление возврата через
          ReturnModal) — ни один не вызывает notifyPlaceContentChanged, хотя
          сервер в каждом из трёх путей реально меняет devices.place_id.
      - path: "crates/trackly-app/src/services/act_service.rs"
        issue: >
          Подтверждено, что все три клиентских пути реально мутируют место
          устройства на сервере: создание акта — devices_repo.update_status_and_place_in_tx
          (строки ~468-476, resolved_place_id = payload.place_id, D-18); редактирование
          акта — симметричный путь (~761); возврат — devices_repo.update_full_in_tx
          (~1500-1506, effective_location). Каждый путь также пишет
          place_movements_repo.record_movement_if_applicable — то есть это не
          побочный эффект, а основная семантика акта.
      - path: "ui/scripts/check-place-tree-invalidation.mjs"
        issue: >
          main() (строка ~605-620) читает только TREE/STORE/CONTENTS/
          CARTRIDGES_PAGE/DEVICES_PAGE/REQUEST_DETAIL — ни один файл каталога
          ui/src/features/acts/ не читается и не проверяется вовсе;
          checkProducers() не знает о write-site класса «Акты».
    missing:
      - "В ActsPage.svelte's handleSaved/handleEditSaved/handleReturnSuccess вызвать notifyPlaceContentChanged с местом(ами), изменившимися по акту — старое место каждого затронутого устройства (до операции) + новое (act.place_id для передачи/редактирования, per-item effective_location для возврата, который может отличаться для каждой строки). Для handleSaved/handleEditSaved старое место неизвестно клиенту напрямую (акт создаётся сразу для набора устройств, не для одного) — потребуется решить, откуда его взять: либо ActFormBody передаёт список device_id вместе с их предыдущими местами (были в форме до сабмита), либо сервер возвращает changed_place_ids в ответе acts.create/acts.update/returns."
      - "Расширить INV-7 (check-place-tree-invalidation.mjs) шестым классом write-site: прочитать ActsPage.svelte (и/или ActFormBody.svelte/ReturnModal.svelte) и проверить вызов notifyPlaceContentChanged в каждом из трёх обработчиков успеха, аналогично localized-подходу, уже применённому к CartridgesPage.svelte/RequestDetail.svelte (functionBody-локализация — в ActsPage.svelte несколько других lifecycle-обработчиков заявки/акта, файл-уровневая проверка недостаточна)."
deferred: []
human_verification: []
---

# Phase 40.1: Пробелы аудита v1.4 — Verification Report (Re-verification, Round 3)

**Phase Goal:** Закрыть четыре находки аудита вехи v1.4 — BLOCKER-1 (предполётная проверка
удаления места), BLOCKER-2 / HST-04 (фильтр «по типу устройства» в отчёте «Перемещения»
достижим из UI), WARNING-1 (продюсеры `notifyPlaceContentChanged` для счётчиков дерева мест),
WARNING-4 (русские подписи заголовков `ReportService::export_csv`).

**Verified:** 2026-09-18T05:40:00Z (HEAD `37965324`, до docs-коммита `f782132e`)
**Status:** gaps_found
**Re-verification:** Yes — раунд 3, после закрытия WR-05 (пятый write-site, найден раундом 2)

## История раундов (компактно)

| Раунд | Статус | Что нашли | Что закрыли |
|---|---|---|---|
| 1 | gaps_found | WR-01 (CartridgesPage's handleOperationSuccess не звал notifyPlaceContentChanged), WR-02 (build_movements_filter_summary падал на удалённом месте фильтра) | — |
| 2 | gaps_found | WR-05 (RequestDetail's handleInstallSuccess, пятый write-site — обнаружен независимым обратным аудитом потребителей OperationModal) | WR-01 (61ba1c5b), WR-02 (8d7a22a7) — оба закрыты корректно, подтверждены заново |
| 3 (этот) | gaps_found | Шестой класс write-site — «Акты приёма-передачи» (ActsPage.svelte: create/edit/return), не покрытый ни разу ни в одном плане фазы | WR-05 (37965324) — закрыт корректно, подтверждён независимым мутационным самотестом |

BLOCKER-1, BLOCKER-2, WARNING-4 остаются VERIFIED без изменений с раунда 1 (файлы не
трогались — `git diff --stat 6ec7ca14..HEAD` пуст для всех их артефактов).

## Метод этого раунда

1. Прочитан полный диф `37965324` (WR-05 фикс) построчно — не по тексту SUMMARY.
2. Гейт `node ui/scripts/check-place-tree-invalidation.mjs` перезапущен на HEAD — `PASS`.
3. **Независимый мутационный самотест**, выполненный заново в этой сессии (не переиспользован
   отчёт исполнителя): временно удалён вызов `notifyPlaceContentChanged` из тела
   `handleInstallSuccess`, гейт перезапущен → `FAIL — 1 нарушений`, указывает ровно на
   `RequestDetail.svelte`/`handleInstallSuccess`. Файл восстановлен, `git diff` — пусто, гейт
   снова `PASS`.
4. Проверена обратная совместимость `OperationModal.onSuccess`'s нового опционального
   параметра `previousPlaceId` с единственным другим потребителем — `CartridgesPage.svelte`'s
   `handleOperationSuccess(updated: CartridgeDto)` (один параметр) — TS структурная типизация
   допускает это, `pnpm exec svelte-check` подтверждает 0 ошибок.
5. **Независимый обратный аудит "device flows too, not only OperationModal consumers"** (по
   прямому требованию задания): найдены все клиентские файлы, содержащие `place_id`
   (`grep -rln place_id --include=*.svelte`), проверен каждый нетривиальный кандидат на предмет
   реальной серверной мутации `devices.place_id`/`cartridges.place_id` и наличия/отсутствия
   `notifyPlaceContentChanged`. Найден новый непокрытый класс — «Акты» (см. Gaps).
6. `pnpm run lint` (все 14 гейтов, включая INV-7) — `PASS`. `pnpm exec svelte-check` — `0 errors,
   60 warnings` (все warning — предсуществующие, не в изменённых файлах). `git diff --stat
   6ec7ca14..HEAD` для файлов BLOCKER-1/BLOCKER-2/WARNING-4 — пусто (не тронуты, повторный
   грепп подтверждает содержимое такое же, как в раунде 2).

## Goal Achievement

### Roadmap Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Попытка удалить место, участвовавшее в перемещениях, отклоняется русскоязычным сообщением с точными счётчиками | ✓ VERIFIED (не изменялось с раунда 2) | `referencing_movement_count` в `places.rs`/`places_sqlite.rs`/`place_service.rs` — файлы не менялись, `git diff --stat` пуст. |
| SC2 | Пользователь может построить отчёт «Перемещения» с фильтром одновременно по месту и по типу устройства | ✓ VERIFIED (не изменялось с раунда 2) | `ReportFilters.svelte` не менялся. |
| SC3 | Счётчики содержимого в дереве мест обновляются после смены места предмета с любого экрана | ⚠️ PARTIAL (было 4/5 write-site, теперь 5/5 покрытых write-site из ранее известных + 1 новый непокрытый КЛАСС найден) | WR-05 закрыт корректно и подтверждён заново мутационным самотестом. Независимый обратный аудит нашёл шестой, structurally иной класс write-site — «Акты приёма-передачи» (ActsPage.svelte) — меняющий devices.place_id на сервере, но не вызывающий notifyPlaceContentChanged и не покрытый INV-7. См. gap. |
| SC4 | Заголовки CSV-экспорта отчётов совпадают с подписями на экране и в PDF | ✓ VERIFIED (не изменялось с раунда 2) | `report_service.rs:930` (`write_record(column_labels)`) — grep-подтверждено, файл не менялся. |

**Score:** 3/4 полностью VERIFIED без изменений; SC3 остаётся PARTIAL — прогресс есть (write-site
5 закрыт корректно), но независимый аудит вскрыл новый непокрытый класс write-site (акты),
не входивший ни в один план фазы.

### WR-05 Closure — Full Re-check

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | `handleInstallSuccess` (RequestDetail.svelte) вызывает `notifyPlaceContentChanged` со старым (из `previousPlaceId`)+новым местом | ✓ VERIFIED | `RequestDetail.svelte:398-425` — `oldPlaceId = previousPlaceId ?? null`, `newPlaceId = cartridge.place_id ?? null`, дедуп + null-фильтр, `notifyPlaceContentChanged(changedPlaceIds)`. |
| 2 | `OperationModal.onSuccess` контракт расширен вторым опциональным `previousPlaceId` | ✓ VERIFIED | `OperationModal.svelte:65` — `onSuccess: (_cartridge, _previousPlaceId?: number \| null) => ...`; `:745` — снимок `effectiveCartridge!.place_id` ДО `transition()`; `:775` — `await onSuccess(transitioned, previousPlaceId)`. |
| 3 | `CartridgesPage.svelte` (единственный другой потребитель) не сломан новым параметром | ✓ VERIFIED | `CartridgesPage.svelte:263` — `handleOperationSuccess(updated: CartridgeDto)`, один параметр, файл не менялся с раунда 2, структурная типизация TS допускает присвоение; `svelte-check` 0 ошибок. |
| 4 | INV-7 расширен пятым write-site, локализованным на тело функции | ✓ VERIFIED | `check-place-tree-invalidation.mjs:552-581` — `functionBody(requestDetailCode, 'handleInstallSuccess')`, отдельная ветка `checkProducers`. |
| 5 | Гейт зелёный на HEAD | ✓ VERIFIED | `node ui/scripts/check-place-tree-invalidation.mjs` → `PASS — 0 нарушений`. |
| 6 | **Независимый мутационный самотест** (не из отчёта исполнителя) | ✓ VERIFIED | Вызов вручную удалён из тела `handleInstallSuccess` в этой сессии → гейт даёт `FAIL — 1 нарушений`, указывает ровно на `RequestDetail.svelte`/`handleInstallSuccess`; после восстановления файла (`git diff` пуст) гейт снова `PASS`. Подтверждает отсутствие маскировки — самотест исполнителя не просто переиспользован, а перепроверен независимо. |
| 7 | Живая проверка `cargo tauri dev` для WR-05 | ⚠️ НЕ ВЫПОЛНЕНА в этом раунде | По признанию исполнителя (commit message + задание раунда) — не переигрывалась; аргумент "структурно идентично WR-01, уже одобренному живьём" принят как обоснованный (тот же серверный transition, тот же клиентский паттерн `notifyPlaceContentChanged(dedup([old,new]))`), но формально остаётся неподтверждённым вживую этим write-site отдельно. Не блокирует статус сам по себе (риск низкий, паттерн идентичен уже проверенному), но стоит зафиксировать как известный остаточный пробел живого покрытия. |

**Вывод по WR-05:** закрыт корректно, независимо перепроверен мутационным самотестом.
Единственная оговорка — живой `cargo tauri dev`-прогон для этого конкретного write-site не
переигрывался (принято решение полагаться на структурную идентичность с уже одобренным WR-01).

### Новая находка раунда 3: класс write-site «Акты приёма-передачи» не покрыт

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | Создание акта передачи меняет `devices.place_id` на сервере | ✓ подтверждено | `act_service.rs:468-476` — `devices_repo.update_status_and_place_in_tx(&tx, dev_id, in_work_status_id, resolved_place_id, now)`, `resolved_place_id = payload.place_id` (из `PlacePicker`, D-18). |
| 2 | Редактирование акта меняет `devices.place_id` (place редактируем через `PlacePicker`, не `disabled`) | ✓ подтверждено | `ActFormBody.svelte:321` — `<PlacePicker value={placeId} .../>` без `disabled`, активен и в `mode='edit'`; `act_service.rs:~725-800` — симметричный update-путь. |
| 3 | Возврат по акту меняет `devices.place_id` | ✓ подтверждено | `act_service.rs:1500-1506` — `devices_repo.update_full_in_tx(&tx, device_id, on_warehouse_status_id, effective_location, ...)`. |
| 4 | `ActsPage.svelte`'s `handleSaved`/`handleEditSaved`/`handleReturnSuccess` вызывают `notifyPlaceContentChanged` | ✗ НЕ ВЫЗЫВАЮТ | `ActsPage.svelte:176-181,222-233,241-266` — все три делают `refresh()`+`refreshCounts()` (обновляют список актов и его собственные счётчики), но 0 вхождений `notifyPlaceContentChanged` во всём каталоге `features/acts/` (`grep -rn`). |
| 5 | INV-7 проверяет какой-либо файл `features/acts/` | ✗ НЕ ПРОВЕРЯЕТ | `check-place-tree-invalidation.mjs`'s `main()` — список чтения: TREE/STORE/CONTENTS/CARTRIDGES_PAGE/DEVICES_PAGE/REQUEST_DETAIL. Ни одного файла из `acts/` нет. |
| 6 | Акты — единственный write-site, меняющий место КАРТРИДЖЕЙ (а не только устройств) через этот механизм? | Проверено — НЕТ | `grep -n cartridge act_service.rs` — акты работают только с `device_id`/`devices`, у картриджей своя ветка через `CartridgeService`/`OperationModal` (уже покрыта write-site 2/3/5). Находка ограничена устройствами. |

**Является ли это блокером цели фазы?** Да. Формулировка WARNING-1/SC3 ("с любого экрана")
и прямая инструкция этого раунда верификации ("consider device flows too, not only
OperationModal consumers") указывают ровно на этот класс дефекта. Акты приёма-передачи —
Core Value проекта по `CLAUDE.md` ("Учёт устройств и картриджей с актами приёма-передачи...
должен работать надёжно"), задействуются чаще, чем install-через-заявку (WR-05, уже принятый
как блокер раундом 2). Раунд 2 установил прецедент внутри этой же фазы: новый непокрытый
write-site того же класса дефекта — блокер для статуса `passed`, не отложенный follow-up
(даже несмотря на то, что фикс структурно тривиален). Оснований отходить от этого прецедента
именно сейчас, когда находка технически весомее уже закрытых, — нет.

### Required Artifacts (обновлено)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `ui/src/features/requests/RequestDetail.svelte` | `notifyPlaceContentChanged` в `handleInstallSuccess` | ✓ VERIFIED | Подтверждено, включая мутационный самотест. |
| `ui/src/features/cartridges/OperationModal.svelte` | `onSuccess` контракт с `previousPlaceId`, обратная совместимость | ✓ VERIFIED | Подтверждено. |
| `ui/scripts/check-place-tree-invalidation.mjs` | INV-7 покрывает ВСЕ клиентские write-site смены `place_id` | ✓ VERIFIED (для 5 известных write-site) / ✗ НЕ ПОКРЫВАЕТ акты | См. gap. |
| `ui/src/features/acts/ActsPage.svelte` | Отсутствует в must_haves фазы, но релевантен по факту (см. gap) | ✗ НЕ ПОКРЫТ | Три write-site без инвалидации и без гейта. |

### Key Link Verification (дельта к раунду 2)

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `RequestDetail.svelte::handleInstallSuccess` | `placeContentEvents.svelte.ts` | `notifyPlaceContentChanged(changedPlaceIds)` | ✓ WIRED (было NOT WIRED в раунде 2) | Подтверждено + мутационный самотест. |
| `ActsPage.svelte::handleSaved/handleEditSaved/handleReturnSuccess` | `placeContentEvents.svelte.ts` | — | ✗ NOT WIRED (новая находка) | Не вызывают; не были в скоупе ни одного плана фазы. |

### Requirements Coverage

PLC-01, HST-04 — SATISFIED (не менялось с раунда 2). PLC-06 ("открыть любое место и увидеть
всё, что размещено в нём") остаётся PARTIAL — теперь по новой причине: класс write-site
«Акты» способен оставить счётчик места устаревшим при взгляде через дерево мест. PLC-04
SATISFIED (косвенно). Orphaned requirements: нет.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | Debt markers (TBD/FIXME/XXX) в файлах, изменённых раундом 3 (`OperationModal.svelte`, `RequestDetail.svelte`, `check-place-tree-invalidation.mjs`) | — | Не найдено. |
| `ui/src/features/acts/ActsPage.svelte` | 176-266 | Отсутствующий вызов `notifyPlaceContentChanged` в трёх обработчиках успеха, меняющих `devices.place_id` | 🛑 Blocker для статуса фазы (см. gap) | Счётчики дерева «Места» остаются устаревшими после операций с актами приёма-передачи, пока пользователь не переключит раздел и не вернётся в «Места» (в рамках одной клиентской сессии дерево остаётся смонтированным дольше одной навигации — установленный прецедент этой же фазы, см. store's doc comment: "same-tab/same-session staleness"). |
| `ui/scripts/check-place-tree-invalidation.mjs` | main() | Гейт не читает ни один файл `features/acts/` | ⚠️ Warning | Регрессия/пробел на этом классе write-site невидима гейту, специально введённому для этой цели. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| INV-7 гейт (5 известных write-site) | `node ui/scripts/check-place-tree-invalidation.mjs` | `PASS — 0 нарушений` | ✓ PASS (не покрывает акты) |
| Мутационный самотест write-site 5 (независимо перезапущен) | удаление вызова → гейт → восстановление → гейт | `FAIL — 1 нарушений` → `PASS — 0 нарушений`, `git diff` пуст | ✓ PASS |
| Весь `pnpm run lint` (14 гейтов) | `pnpm run lint` | все PASS, включая eslint/prettier/INV-7 | ✓ PASS |
| Типы/раннтайм фронтенда | `pnpm exec svelte-check` | `0 errors, 60 warnings` (предсуществующие, несвязанные файлы) | ✓ PASS |
| Файлы BLOCKER-1/BLOCKER-2/WARNING-4 не менялись с раунда 2 | `git diff --stat 6ec7ca14..HEAD -- <файлы>` | пусто | ✓ PASS |

### Human Verification Required

Нет новых пунктов для ЖИВОГО прогона в рамках уже закрытого WR-05 (риск принят как
структурно идентичный уже одобренному WR-01). Новая находка (акты) ещё НЕ исправлена — живая
проверка потребуется после фикса, не сейчас.

## Gaps Summary

**Раунд 2 → Раунд 3: прогресс подтверждён.** WR-05 (`RequestDetail.svelte`'s
`handleInstallSuccess`) закрыт корректно, независимо перепроверен мутационным самотестом
(не переиспользован отчёт исполнителя). `OperationModal.onSuccess`'s расширенный контракт не
сломал единственного другого потребителя. BLOCKER-1, BLOCKER-2, WARNING-4 остаются VERIFIED
без изменений.

**SC3/WARNING-1 остаётся частично закрытым — по НОВОЙ причине, найденной независимым
обратным аудитом.** По прямому требованию задания ("consider device flows too, not only
OperationModal consumers") проверены все клиентские места, где `place_id` устройства может
измениться. Найден шестой класс write-site — Акты приёма-передачи (создание, редактирование,
возврат) — который реально меняет `devices.place_id` на сервере (тот же класс операции, что
уже закрытые пять), но клиентские обработчики успеха (`ActsPage.svelte`) не вызывают
`notifyPlaceContentChanged`, и структурный гейт INV-7 не проверяет ни один файл `features/acts/`
вовсе. Учитывая, что акты — Core Value проекта, и что раунд 2 уже установил прецедент
трактовать такие находки как блокер (не follow-up) в рамках этой же фазы, эта находка
классифицируется так же.

**Рекомендация:** не считать Фазу 40.1 закрытой, пока класс write-site «Акты» не получит и
вызовы `notifyPlaceContentChanged` (во всех трёх точках — создание/редактирование/возврат), и
покрытие INV-7 гейтом. Объём фикса сопоставим с уже выполненным WR-05 (нужно решить, откуда
взять «старое место» для актов, у которых несколько устройств в одной операции — это
единственное реальное усложнение относительно уже закрытых write-site с одним предметом).
Альтернатива: пользователь может явно принять текущее состояние как известный остаточный
риск через `overrides:` в этом файле, если сочтёт, что акты создаются достаточно редко в
рамках одной сессии с открытым деревом мест, чтобы отложить фикс на отдельную фазу — в этом
случае статус следующей ре-верификации должен быть `passed (override)`, а не молчаливое
закрытие фазы без явной фиксации решения.

---
_Verified: 2026-09-18T05:40:00Z_
_Verifier: Claude (gsd-verifier)_
