---
phase: 40.1-audit-gap-closure
verified: 2026-09-18T07:19:48Z
status: human_needed
score: 4/4 roadmap success criteria structurally verified (SC1, SC2, SC3, SC4); live cargo tauri dev confirmation pending for 6 new write-sites
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "3.5/4 roadmap success criteria verified (SC1, SC2, SC4 VERIFIED; SC3 PARTIAL — новый непокрытый класс write-site найден)"
  gaps_closed:
    - "WR-06 (акты приёма-передачи): ActsPage.svelte's handleSaved/handleEditSaved/handleReturnSuccess/handleDelete теперь вызывают notifyPlaceContentChanged; серверная сторона (act_service.rs) считает changed_place_ids внутри транзакции для create/update/do_return/update_return (агрегация по всем затронутым устройствам, не только по одному полю place_id акта) и возвращает их клиенту через ActDto/build_acts_delete на обоих транспортах (tauri_cmds/acts.rs, http/acts.rs)."
    - "WR-07 (найден не раундами верификации, а построением реестра INV-7): PrinterDetail.svelte's <DeviceFormModal onSaved={...}> для «Данные устройства» теперь инвалидирует (было полностью непокрыто — тот же серверный путь, что и DevicesPage.svelte, но второй UI-вход никогда не был подключён)."
    - "WR-08 (тоже найден построением реестра): DeviceContextMenu.svelte's handleViewChanged для <PlaceEntityViewModal onChanged={...}> из кебаб-меню списка «Устройства» — обработчик буквально отбрасывал аргумент changedPlaceIds; теперь вызывает notifyPlaceContentChanged."
    - "INV-7 (check-place-tree-invalidation.mjs) переписан из хардкод-списка файлов/функций в РЕЕСТР place-mutating поверхностей (MUTATING_COMPONENTS/FORWARDING_COMPONENTS/DIRECT_CALL_MARKERS) + скан ВСЕГО ui/src/features на каждое использование зарегистрированного компонента — структурный класс дефекта (\"новый экран не в списке гейта\"), который повторялся 3 раунда подряд, закрыт на уровне механизма, а не очередной точечной правкой."
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
human_verification:
  - test: "Создать акт приёма-передачи (handover) на несколько устройств из разных текущих мест; держать открытым раздел «Места» с деревом на затронутых узлах в другой вкладке/окне того же приложения; сохранить акт."
    expected: "Счётчики содержимого узлов дерева мест (и старого, и нового места, и их предков) обновляются без ручного обновления/перехода."
    why_human: "Гейт INV-7 структурный — доказывает, что notifyPlaceContentChanged вызывается с корректно вычисленными аргументами по коду, но не запускает Svelte-рантайм; аналогичный класс дефекта (effect_update_depth_exceeded, INV-1/INV-2) уже ловился только живым прогоном, не статическими гейтами."
  - test: "Отредактировать акт (добавить/убрать устройство), оформить возврат по акту (в т.ч. с per-item override места), и мягко удалить акт (undo-каскад) — каждый раз проверяя счётчики дерева мест."
    expected: "Счётчики обновляются корректно после каждой из трёх операций, включая случай, когда разные устройства в одном акте перемещаются в РАЗНЫЕ места (return с per-row override)."
    why_human: "Тот же класс — структурно доказано наличие вызова и корректность source-агрегации на сервере (5 тестов), но конечное поведение дерева в реальном Svelte-рантайме не проверялось живым прогоном."
  - test: "Отредактировать «Данные устройства» со страницы принтера (PrinterDetail) — сменить место; отдельно — открыть «Просмотр» устройства из кебаб-меню списка «Устройства» (DeviceContextMenu) и сменить место там же."
    expected: "В обоих случаях счётчики дерева мест обновляются немедленно, без перехода на другой экран."
    why_human: "Оба write-site — новые продюсеры, добавленные этим проходом (WR-07/WR-08), ранее НИ РАЗУ не покрытые живой проверкой ни в одном из 4 планов фазы."
---

# Phase 40.1: Пробелы аудита v1.4 — Verification Report (Round 4, FINAL)

**Phase Goal:** Закрыть четыре находки аудита вехи v1.4 — BLOCKER-1 (предполётная проверка
удаления места), BLOCKER-2 / HST-04 (фильтр «по типу устройства» в отчёте «Перемещения»
достижим из UI), WARNING-1 (продюсеры `notifyPlaceContentChanged` для счётчиков дерева мест),
WARNING-4 (русские подписи заголовков `ReportService::export_csv`).

**Requirements:** PLC-01, HST-04 (попутно PLC-06, PLC-04)

**Verified:** 2026-09-18T07:19:48Z (HEAD `a5ab4272`, включает exhaustive sweep поверх коммитов
`080bdcc4`/`fea52cc6`/`fbf52618`/`8e9a9d79`, которые лежат В ИСТОРИИ до `a5ab4272`, хотя сам
`a5ab4272` — это докс-коммит round-3 отчёта, зафиксированный последним в сессии; фактическое
содержимое рабочего дерева включает весь sweep)
**Status:** human_needed
**Re-verification:** Yes — раунд 4 (финальный), после exhaustive sweep, закрывшего находку
раунда 3 (класс write-site «Акты») + 2 дополнительных write-site, найденных построением
реестра гейта, а не раундами верификации

## История раундов (компактно)

| Раунд | Статус | Что нашли | Что закрыли |
|---|---|---|---|
| 1 | gaps_found | WR-01 (CartridgesPage's handleOperationSuccess не звал notifyPlaceContentChanged), WR-02 (build_movements_filter_summary падал на удалённом месте фильтра) | — |
| 2 | gaps_found | WR-05 (RequestDetail's handleInstallSuccess, пятый write-site — обнаружен независимым обратным аудитом потребителей OperationModal) | WR-01, WR-02 — оба закрыты корректно, подтверждены заново |
| 3 | gaps_found | Шестой класс write-site — «Акты приёма-передачи» (ActsPage.svelte: create/edit/return), не покрытый ни разу ни в одном плане фазы | WR-05 закрыт корректно, подтверждён независимым мутационным самотестом |
| 4 (этот, финальный) | **human_needed** | Экзистенцию находки раунда 3 закрыл ОДИН исчерпывающий проход (не точечный фикс): WR-06 (акты, 4 обработчика) + WR-07 (PrinterDetail, найден построением реестра гейта) + WR-08 (DeviceContextMenu, найден тем же путём); INV-7 переписан на registry-driven completeness gate | WR-06/07/08 закрыты и независимо подтверждены; BLOCKER-1/BLOCKER-2/WARNING-4 подтверждены неизменными с раунда 2 |

BLOCKER-1, BLOCKER-2, WARNING-4 остаются VERIFIED без изменений с раунда 2 — `git diff --stat
f782132e..HEAD` для их артефактов (`place_service.rs`, `places.rs`/`places_sqlite*.rs`,
`ReportFilters.svelte`, `report_service.rs`, `tauri_cmds/reports.rs`, `http/reports.rs`) —
пусто, подтверждено заново в этом раунде.

## Метод этого раунда

1. Прочитан код (не SUMMARY) для каждого из четырёх заявленных коммитов: `080bdcc4` (сервер),
   `fea52cc6` (UI wiring), `fbf52618` (гейт), `8e9a9d79` (докс-append) — построчно через `git
   show`/`Read`, не по тексту коммит-сообщений.
2. Server-side: подтверждено, что `ActDto.changed_place_ids: Vec<i64>` реально заполняется
   внутри транзакции для `create`/`update`/`do_return`/`update_return` через
   `dedupe_place_ids(touched_place_ids)`, и что `delete_soft` возвращает `Vec<i64>` вместо `()`
   — прочитаны сигнатуры и тела на местах (`act_service.rs:487,567,1146,1615,2351,2675`), не
   только doc-комментарии.
3. UI: прочитаны все 4 обработчика `ActsPage.svelte` (`handleSaved`/`handleEditSaved`/
   `handleReturnSuccess`/`handleDelete`) целиком — подтверждено, что каждый читает
   `changed_place_ids` (три первых) или возвращаемое значение `acts.delete()` (четвёртый) и
   зовёт `notifyPlaceContentChanged` с непустой проверкой. То же для `PrinterDetail.svelte`'s
   `DeviceFormModal.onSaved` (старое место из `deviceData`, новое — из `result?.placeId`,
   дедуп через `Set`) и `DeviceContextMenu.svelte`'s `handleViewChanged` (ранее отбрасывал
   аргумент — подтверждено построчно, что теперь передаёт его в `notifyPlaceContentChanged`).
4. Транспортная сквозная проверка `acts.delete`: `ui/src/lib/api/acts.ts` → `acts_delete`
   (Tauri, `Result<Vec<i32>, AppError>`) и `handler_delete` (HTTP, `Result<Json<Vec<i64>>, ...>`)
   — оба читают возврат `build_acts_delete`, не отбрасывают его.
5. `cargo test -p trackly-app --test acts_changed_place_ids` — перезапущен независимо (не
   переиспользован отчёт исполнителя) → `5 passed; 0 failed`.
6. Гейт `node ui/scripts/check-place-tree-invalidation.mjs` — перезапущен на HEAD → `PASS`.
7. **Независимый мутационный самотест #1** (спот-проверка, не все 12 — по прямому указанию
   задания этого раунда): вручную удалён вызов `notifyPlaceContentChanged` из тела
   `PrinterDetail.svelte`'s `DeviceFormModal.onSaved` → гейт → `FAIL — 1 нарушений`, указывает
   ровно на этот write-site → файл восстановлен (`git diff` пуст) → гейт снова `PASS`.
8. **Независимый мутационный самотест #2** (проверка claim «новый экран ловится
   автоматически», не переиспользован отчёт исполнителя): создан СИНТЕТИЧЕСКИЙ новый файл
   `ui/src/features/__gate_spot_check__/SyntheticConsumer.svelte`, рендерящий `<DeviceFormModal
   onSaved={onSaved}>` без вызова инвалидации → гейт → `FAIL — 1 нарушений`, указывает ровно на
   новый файл → файл удалён → гейт снова `PASS`. Подтверждает, что INV-7 действительно
   реестро-driven (сканирует ВЕСЬ `ui/src/features`), а не всё ещё хардкод-список под новым
   названием.
9. Прочитан весь `checkCompleteness`/реестр (`MUTATING_COMPONENTS`/`FORWARDING_COMPONENTS`/
   `DIRECT_CALL_MARKERS`) и доккомментарий над ним целиком — подтверждены заявленные
   blind spot'ы (cross-file резолюция колбэков, один хоп форвардинга, структурная а не
   семантическая проверка аргументов) как реально задокументированные в самом гейте, а не
   только в SUMMARY.
10. Регрессия ранее закрытых write-site (1–6): `grep notifyPlaceContentChanged` в
    `CartridgesPage.svelte`, `DevicesPage.svelte`, `PlaceContents.svelte`,
    `RequestDetail.svelte`, `OperationModal.svelte` — все на месте, без изменений в этом
    раунде.
11. Полный прогон гейтов: `cargo test -p trackly-app -- --skip login_remember_persistent_cookie
    --test-threads=4` (0 failed), `cargo clippy --workspace --tests -- -D warnings` (чисто),
    `cd ui && pnpm run lint` (все 14 гейтов PASS, включая INV-7).
12. BLOCKER-1/BLOCKER-2/WARNING-4: `git diff --stat f782132e..HEAD` для их файлов — пусто,
    подтверждено, что раунд 4 их не затронул.

## Goal Achievement

### Roadmap Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Попытка удалить место, участвовавшее в перемещениях, отклоняется русскоязычным сообщением с точными счётчиками | ✓ VERIFIED (не изменялось с раунда 2) | `referencing_movement_count` в `places.rs`/`places_sqlite.rs`/`place_service.rs` — файлы не менялись, `git diff --stat f782132e..HEAD` пуст. |
| SC2 | Пользователь может построить отчёт «Перемещения» с фильтром одновременно по месту и по типу устройства | ✓ VERIFIED (не изменялось с раунда 2) | `ReportFilters.svelte` не менялся. |
| SC3 | Счётчики содержимого в дереве мест обновляются после смены места предмета с любого экрана | ✓ СТРУКТУРНО ЗАКРЫТО, живая проверка pending | Все 12 известных серверных write-site покрыты продюсерами `notifyPlaceContentChanged`; INV-7 переписан на registry-driven completeness gate — независимо подтверждено (два спот-мутационных самотеста, включая синтетический новый файл), что гейт ловит и точечное удаление, и совершенно новый непокрытый экран автоматически, без ручного добавления в список. Живой `cargo tauri dev`-прогон для 3 новых write-site (акты, PrinterDetail, DeviceContextMenu) не выполнялся в рамках этой сессии — см. Human Verification. |
| SC4 | Заголовки CSV-экспорта отчётов совпадают с подписями на экране и в PDF | ✓ VERIFIED (не изменялось с раунда 2) | `report_service.rs::export_csv` пишет `wtr.write_record(column_labels)`; `build_reports_export_csv` передаёт `column_labels_for(&report_type)`; файлы не менялись с раунда 2. |

**Score:** 4/4 структурно VERIFIED. SC3 — единственный критерий с исторически найденными живыми
регрессиями того же класса дефекта (INV-1/INV-2, `effect_update_depth_exceeded`), поэтому
статус фазы — `human_needed`, а не `passed`, пока пользователь не подтвердит живым прогоном 3
новых write-site.

### WR-06/07/08 Closure — детальная проверка

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| 1 | `ActDto.changed_place_ids` заполняется сервером для create/update/return | ✓ VERIFIED | `act_service.rs:567,1146,1615,2351` — `changed_place_ids: dedupe_place_ids(touched_place_ids)`. |
| 2 | `delete_soft` возвращает затронутые id, не `()` | ✓ VERIFIED | `act_service.rs:2675` — `pub async fn delete_soft(&self, id: i64, version: i64) -> Result<Vec<i64>, AppError>`. |
| 3 | Оба транспорта (Tauri/HTTP) пробрасывают `changed_place_ids`/возврат `delete_soft` без потерь | ✓ VERIFIED | `tauri_cmds/acts.rs:230-232` (`acts_delete` возвращает `Vec<i32>`), `http/acts.rs:183-192` (`handler_delete` возвращает `Json<Vec<i64>>`). |
| 4 | `ActsPage.svelte`'s 4 обработчика вызывают `notifyPlaceContentChanged` | ✓ VERIFIED | `handleSaved`/`handleEditSaved`/`handleReturnSuccess` читают `act.changed_place_ids`/`returnDto.changed_place_ids`; `handleDelete` читает возврат `acts.delete()` напрямую — все 4 с проверкой `.length > 0`. |
| 5 | `PrinterDetail.svelte`'s `DeviceFormModal.onSaved` инвалидирует | ✓ VERIFIED | Старое место — снимок `deviceData?.place_id`, новое — `result?.placeId`, дедуп через `Set`, `notifyPlaceContentChanged(changedPlaceIds)`. |
| 6 | `DeviceContextMenu.svelte`'s `handleViewChanged` больше не отбрасывает аргумент | ✓ VERIFIED | `function handleViewChanged(changedPlaceIds: number[]) { viewRow = null; if (changedPlaceIds.length > 0) notifyPlaceContentChanged(changedPlaceIds); onDelete(); }`. |
| 7 | INV-7 переписан registry-driven, а не расширен ещё одним хардкод-именем | ✓ VERIFIED | `checkCompleteness` сканирует `walkSvelteFiles(ui/src/features)` целиком против реестра `MUTATING_COMPONENTS`/`FORWARDING_COMPONENTS`/`DIRECT_CALL_MARKERS`, а не читает фиксированный список файлов. |
| 8 | Независимый мутационный самотест — точечное удаление | ✓ VERIFIED | Спот-проверка на `PrinterDetail.svelte` (не переиспользован отчёт исполнителя) — `FAIL → 1 нарушений`, локализовано ровно на этот write-site, восстановлено, `git diff` пуст, гейт снова `PASS`. |
| 9 | Независимый мутационный самотест — новый непокрытый экран ловится автоматически | ✓ VERIFIED | Синтетический файл `__gate_spot_check__/SyntheticConsumer.svelte` (никогда не упомянут в гейте) → `FAIL`, удалён → `PASS`. Подтверждает реестровую, а не списочную природу гейта. |
| 10 | 5 новых серверных тестов (`acts_changed_place_ids.rs`) зелёные | ✓ VERIFIED | `cargo test -p trackly-app --test acts_changed_place_ids` — `5 passed; 0 failed`, перезапущено независимо. |
| 11 | Живой `cargo tauri dev`-прогон для WR-06/07/08 | ⚠️ НЕ ВЫПОЛНЕН в этой сессии | Пользователь ведёт параллельный живой прогон — на момент этого отчёта не подтверждён; см. Human Verification. Не понижает структурный вывод, но блокирует `passed`. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/trackly-app/src/dto/act.rs` | `changed_place_ids: Vec<i64>` поле | ✓ VERIFIED | Строка 113, инициализируется `Vec::new()` в базовом конструкторе (строка 487), переопределяется мутациями. |
| `crates/trackly-app/src/services/act_service.rs` | `dedupe_place_ids` + 4 write-site заполняют поле | ✓ VERIFIED | `create`/`update`/`do_return`/`update_return`/`delete_soft` — все подтверждены. |
| `crates/trackly-app/tests/acts_changed_place_ids.rs` | 5 тестов на все серверные пути | ✓ VERIFIED | `5 passed; 0 failed`, перезапущено. |
| `ui/src/features/acts/ActsPage.svelte` | 4 обработчика вызывают `notifyPlaceContentChanged` | ✓ VERIFIED | Прочитано построчно, см. таблицу выше. |
| `ui/src/features/printers/PrinterDetail.svelte` | `DeviceFormModal.onSaved` инвалидирует | ✓ VERIFIED | Прочитано построчно + мутационный самотест. |
| `ui/src/features/devices/DeviceContextMenu.svelte` | `handleViewChanged` инвалидирует | ✓ VERIFIED | Прочитано построчно. |
| `ui/scripts/check-place-tree-invalidation.mjs` | Registry-driven completeness gate | ✓ VERIFIED | 873 строки, реестр + скан всего `features/`, 2 независимых мутационных самотеста подтверждают. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `act_service.rs` (create/update/return/delete) | `ActDto`/return-значение `delete_soft` | `changed_place_ids`/`Vec<i64>` | ✓ WIRED | Подтверждено на уровне кода. |
| `tauri_cmds/acts.rs`, `http/acts.rs` | UI (`ui/src/lib/api/acts.ts`) | Оба транспорта возвращают одинаковую форму данных | ✓ WIRED | `acts_delete` (Tauri) / `handler_delete` (HTTP) — оба пробрасывают возврат `build_acts_delete` без потерь. |
| `ActsPage.svelte` (4 обработчика) | `placeContentEvents.svelte.ts` | `notifyPlaceContentChanged(...)` | ✓ WIRED | Все 4 подтверждены построчно. |
| `PrinterDetail.svelte::DeviceFormModal.onSaved` | `placeContentEvents.svelte.ts` | `notifyPlaceContentChanged(changedPlaceIds)` | ✓ WIRED | Подтверждено + мутационный самотест. |
| `DeviceContextMenu.svelte::handleViewChanged` | `placeContentEvents.svelte.ts` | `notifyPlaceContentChanged(changedPlaceIds)` | ✓ WIRED | Подтверждено — ранее аргумент отбрасывался, теперь используется. |
| Новый неизвестный write-site (гипотетический) | INV-7 гейт | Реестр + скан `ui/src/features` | ✓ WIRED (по конструкции) | Подтверждено синтетическим файлом — гейт падает на новом непокрытом использовании ЗАРЕГИСТРИРОВАННОГО компонента автоматически. Не покрывает совершенно новую серверную мутирующую поверхность вне реестра (задокументированный blind spot #1 гейта) — это follow-up, не блокер этой фазы. |

### Requirements Coverage

PLC-01, HST-04 — SATISFIED (не менялось с раунда 2). PLC-06 («открыть любое место и увидеть всё,
что размещено в нём») — теперь SATISFIED структурно: все известные write-site, способные
оставить счётчик устаревшим, закрыты, включая найденный аудитом класс «Акты» и два
дополнительных, найденных построением реестра гейта. PLC-04 — SATISFIED (косвенно, не менялось).
Orphaned requirements: нет.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | Debt markers (TBD/FIXME/XXX) в файлах, изменённых этим проходом (`act.rs`, `act_service.rs`, `tauri_cmds/acts.rs`, `http/acts.rs`, `acts_changed_place_ids.rs`, `ActsPage.svelte`, `acts.ts`, `PrinterDetail.svelte`, `DeviceContextMenu.svelte`, `check-place-tree-invalidation.mjs`) | — | Не найдено. |
| `ui/scripts/check-place-tree-invalidation.mjs` | доккомментарий над `checkCompleteness` | 4 явно задокументированных blind spot'a (cross-file резолюция, один хоп форвардинга, структурная не семантическая проверка, реестр требует ручного расширения для НОВОЙ серверной мутирующей поверхности) | ℹ️ Info — follow-up, не блокер | Зафиксировано самим гейтом как осознанное ограничение подхода, не скрытый дефект; корректно отражает то, что доказывает структурная проверка. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| INV-7 гейт (12 известных write-site) | `node ui/scripts/check-place-tree-invalidation.mjs` | `PASS — 0 нарушений` | ✓ PASS |
| Мутационный самотест #1 (точечное удаление, независимо перезапущен) | удаление вызова в `PrinterDetail.svelte` → гейт → восстановление → гейт | `FAIL — 1 нарушений` → `PASS`, `git diff` пуст | ✓ PASS |
| Мутационный самотест #2 (синтетический новый непокрытый экран) | новый файл, рендерящий `<DeviceFormModal>` без notify → гейт → удаление файла → гейт | `FAIL — 1 нарушений` → `PASS` | ✓ PASS |
| 5 новых серверных тестов (`acts_changed_place_ids`) | `cargo test -p trackly-app --test acts_changed_place_ids` | `5 passed; 0 failed` | ✓ PASS |
| Весь пакет тестов (кроме известного зависания) | `cargo test -p trackly-app -- --skip login_remember_persistent_cookie --test-threads=4` | `0 failed` во всём выводе | ✓ PASS |
| Clippy (весь workspace) | `cargo clippy --workspace --tests -- -D warnings` | чисто | ✓ PASS |
| `pnpm run lint` (все 14 гейтов) | `cd ui && pnpm run lint` | все PASS, включая eslint/prettier/INV-7 | ✓ PASS |
| Файлы BLOCKER-1/BLOCKER-2/WARNING-4 не менялись с раунда 2 | `git diff --stat f782132e..HEAD -- <файлы>` | пусто | ✓ PASS |

### Human Verification Required

См. YAML frontmatter (`human_verification`) — три пункта живого `cargo tauri dev`-прогона:
(1) создание акта на несколько устройств, (2) редактирование/возврат/удаление акта, (3)
редактирование места устройства через `PrinterDetail` и через `DeviceContextMenu`'s
«Просмотр». Все три покрывают ТОЛЬКО новые продюсеры этого прохода (WR-06/07/08) — WR-01/WR-02
уже одобрены живым UAT ранее (раунд 1→2), WR-05 принят по структурной аналогии с WR-01 (раунд
3, зафиксировано как известный остаточный пробел живого покрытия, не переоткрывается здесь).

## Gaps Summary

**Раунд 3 → Раунд 4: находка полностью закрыта одним исчерпывающим проходом, а не очередной
точечной правкой.** Класс write-site «Акты», найденный раундом 3, закрыт на всех четырёх точках
(create/edit/return/delete), с корректной серверной агрегацией `changed_place_ids` (обоснованной
необходимостью: один акт может двигать много устройств из РАЗНЫХ прежних мест — единственное
поле `place_id` акта этого не выражает). Построение реестра для нового гейта дополнительно
вскрыло 2 ЕЩЁ ОДНИХ непокрытых write-site (PrinterDetail, DeviceContextMenu), которые не были
названы ни в одном из трёх предыдущих раундов верификации — оба закрыты тем же проходом.
Корневая причина трёхкратного повторения находки (гейт проверял фиксированный список файлов, а
не «что вообще может менять место») устранена на уровне МЕХАНИЗМА: INV-7 теперь реестро-driven
completeness gate, независимо подтверждено двумя мутационными самотестами (включая
синтетический новый файл), что он ловит и точечную регрессию, и полностью новый непокрытый
экран автоматически.

**Почему `human_needed`, а не `passed`.** Все структурные проверки (существование, содержательность,
проводка, полнота реестра, серверные тесты, clippy, весь пакет тестов, `pnpm run lint`) —
зелёные. Но именно этот класс дефекта (реактивность счётчиков дерева мест) уже дважды показывал
разрыв между «структурно верно» и «работает в реальном Svelte-рантайме» — INV-1/INV-2
(`effect_update_depth_exceeded`) были пойманы ТОЛЬКО живым прогоном, ни одним статическим
гейтом. 3 новых продюсера этого прохода (акты, PrinterDetail, DeviceContextMenu) ни разу не
проходили живую проверку. Пользователь ведёт параллельный живой прогон — по инструкции этого
раунда, при полностью зелёных структурных гейтах и незавершённой живой проверке корректный
статус — `human_needed`, не `gaps_found` (гипотетических непокрытых write-site не найдено — see
Key Link Verification) и не `passed` (живое подтверждение — необходимое условие для этого
конкретного класса дефекта, установленное самим проектом).

**Рекомендация:** после того как пользователь подтвердит 3 пункта в `human_verification`
(YAML), фаза может быть закрыта без дополнительной ре-верификации — структурная часть работы
завершена и независимо перепроверена в этом раунде. Если живой прогон вскроет дефект (по
аналогии с UAT3-03a), это НОВЫЙ раунд, а не доработка этого отчёта.

---
_Verified: 2026-09-18T07:19:48Z_
_Verifier: Claude (gsd-verifier)_
