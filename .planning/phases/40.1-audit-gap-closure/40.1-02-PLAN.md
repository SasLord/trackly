---
phase: 40.1-audit-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/reports/ReportFilters.svelte
autonomous: false

requirements: [HST-04]

must_haves:
  truths:
    - "В отчёте «Перемещения» отрисован третий контрол — фильтр по типу устройства — рядом с существующими «Откуда»/«Куда» (D-08)."
    - "Контрол — кастомный Dropdown (variant=\"select\", flat), а не нативный <select> и не обёртка Select (D-09)."
    - "Источник списка типов — уже переданный проп deviceTypes, новых бэкенд-команд не заведено; бэкенд-фильтр report_service.rs:1449-1455 не менялся (D-10)."
    - "Пустой выбор = «Все типы»: filter.type_id уходит null, фильтр не применяется (D-11)."
    - "Остальные пять принятых-и-выброшенных пропов (statusId, modelId, color, search, deviceTypes остальных доменов) не тронуты — решение пользователя (D-12)."
    - "Фильтр реально сужает выдачу в запущенном приложении (cargo tauri dev), а не только компилируется — проверено вручную, не синтетическим харнессом (D-13, D-25)."
    - "Находка закрыта живой проверкой в запущенном приложении, а не только достижимостью на уровне сервиса — именно так BLOCKER-2 был ошибочно засчитан верификацией Фазы 40 (D-24)."
  artifacts:
    - path: "ui/src/features/reports/ReportFilters.svelte"
      provides: "Контрол «Тип устройства» в ветке reportDomain === 'movements'"
  key_links:
    - from: "ui/src/features/reports/ReportFilters.svelte"
      to: "ui/src/features/reports/ReportsPage.svelte"
      via: "onFilterChange?.({ type_id })"
      pattern: "type_id"
    - from: "ui/src/features/reports/ReportsPage.svelte"
      to: "crates/trackly-app/src/services/report_service.rs"
      via: "filter.type_id применяется в query_movements_inner"
      pattern: "filter.type_id"
---

<objective>
Закрыть BLOCKER-2 из аудита вехи v1.4 (HST-04 выполнен наполовину): фильтр «по типу
устройства» для отчёта «Перемещения» уже реализован на бэкенде
(`report_service.rs:1449-1455` применяет `filter.type_id`), проп `typeId` уже передаётся
из `ReportsPage.svelte`, но `ReportFilters.svelte` принимает его как `typeId: _typeId`
(«принято и не используется») и никогда не отрисовывает контрол в ветке `movements`.

Purpose: сделать HST-04 достижимым из интерфейса дословно, а не наполовину — пользователь
должен суметь построить отчёт «Перемещения» одновременно с фильтром по месту И по типу
устройства.

Output: третий контрол в ветке `movements` `ReportFilters.svelte`, живая проверка сужения
выдачи в запущенном приложении.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40.1-audit-gap-closure/40.1-CONTEXT.md
@.planning/phases/40.1-audit-gap-closure/40.1-PATTERNS.md
</context>

<interfaces>
<!-- Текущее состояние ReportFilters.svelte — деструктуризация пропов и ветка movements. -->

Props (уже объявлены, только меняется, читаются ли они):
```typescript
typeId?: number | null;
deviceTypes?: Array<{ id: number; name: string }>;
```

Деструктуризация (сегодня «принято и не используется»):
```typescript
const {
  ...
  typeId: _typeId,
  ...
  deviceTypes: _deviceTypes,
  ...
}: Props = $props();
```

Источник справочника типов уже прокинут родителем (`ReportsPage.svelte:284-287,583,587`):
```typescript
let filterDeviceTypes = $state<Array<{ id: number; name: string }>>([
  { id: 1, name: 'Устройство' },
  { id: 2, name: 'Расходник' },
]);
```
```svelte
<ReportFilters
  typeId={filter.type_id ?? null}
  deviceTypes={filterDeviceTypes}
  onFilterChange={(f) => { filter = { ...filter, ...f }; }}
  ...
/>
```

Готовый аналог кастомного Dropdown «тип» — `ui/src/features/cartridges/CartridgeFilters.svelte:67-126`:
```typescript
const TYPE_OPTIONS: FilterOption[] = [
  { id: '', label: 'Все' },
  { id: '1', label: 'Картридж' },
  { id: '2', label: 'Фотобарабан' },
];
const kindValue = $derived(kindId !== null ? String(kindId) : '');
const kindLabel = $derived(TYPE_OPTIONS.find((o) => o.id === kindValue)?.label ?? 'Все');
```
```svelte
<Dropdown
  variant="select"
  flat={true}
  searchable={false}
  value={kindLabel}
  placeholder="Все"
  searchPlaceholder="Поиск"
  loading={false}
  groups={TYPE_OPTIONS}
  getGroupId={(o) => o.id}
  getGroupName={(o) => o.label}
  getGroupCount={() => 0}
  isGroupExpandable={() => false}
  isGroupSelected={(o) => o.id === kindValue}
  onExpandGroup={noExpand}
  getMemberId={(o) => o.id}
  getMemberName={(o) => o.label}
  onSearch={() => {}}
  onPickGroup={(o) => onKindChange(o.id === '' ? null : Number(o.id))}
  onPickMember={() => {}}
/>
```

Бэкенд уже готов, НЕ трогать (`report_service.rs:1449-1455`):
```rust
if let Some(type_id) = filter.type_id {
    ...
    clauses.push(format!("(pm.entity_type = 'device' AND d.type_id = ?{idx})"));
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Контрол «Тип устройства» в ветке movements</name>
  <files>ui/src/features/reports/ReportFilters.svelte</files>
  <read_first>
    ui/src/features/reports/ReportFilters.svelte (целиком — деструктуризация пропов, ветка movements строки ~114-141, STORAGE_OPTIONS-дропдаун строки ~40-44,156-181 как внутрифайловый аналог)
    ui/src/features/cartridges/CartridgeFilters.svelte (строки 1-130 — TYPE_OPTIONS/kindValue/kindLabel/Dropdown, тот же класс фильтра «тип»)
    ui/src/features/reports/ReportsPage.svelte (строки ~280-290 filterDeviceTypes, ~575-595 проброс пропов ReportFilters)
    .planning/phases/40.1-audit-gap-closure/40.1-PATTERNS.md (раздел «BLOCKER-2», подраздел «ReportFilters.svelte — ветка movements»)
  </read_first>
  <action>
    В деструктуризации `Props` заменить `typeId: _typeId` на `typeId` (проп читается) и
    `deviceTypes: _deviceTypes` на `deviceTypes` — сигнатуру `Props` не менять, оба поля уже
    объявлены. Значения по умолчанию не нужны — родитель уже передаёт `typeId={filter.type_id ??
    null}` и `deviceTypes={filterDeviceTypes}` безусловно.

    Добавить производный список опций `TYPE_OPTIONS` (по образцу `CartridgeFilters.svelte`):
    `$derived` от `deviceTypes`, маппинг `{ id: String(t.id), label: t.name }`, с элементом
    `{ id: '', label: 'Все типы' }` первым (D-11). Добавить `typeValue = $derived(typeId !==
    null && typeId !== undefined ? String(typeId) : '')` и `typeLabel = $derived(TYPE_OPTIONS
    .find((o) => o.id === typeValue)?.label ?? 'Все типы')`.

    В ветке `{#if reportDomain === 'movements'}` добавить третий блок `<div
    class="place-filter-group">` (переиспользовать существующие классы `.filter-label`/
    `.filter-dropdown`, объявленные в `<style>` этого файла) СРАЗУ после блока «Куда», с меткой
    `<span class="filter-name">Тип устройства</span>` и `Dropdown` той же формы пропов, что у
    `CartridgeFilters.svelte`'s TYPE_OPTIONS-дропдауна: `variant="select"`, `flat={true}`,
    `searchable={false}`, `value={typeLabel}`, `placeholder="Все типы"`, `groups={TYPE_OPTIONS}`,
    `getGroupId={(o) => o.id}`, `getGroupName={(o) => o.label}`, `getGroupCount={() => 0}`,
    `isGroupExpandable={() => false}`, `isGroupSelected={(o) => o.id === typeValue}`,
    `onExpandGroup={noExpand}` (уже объявленная в файле заглушка), `getMemberId={(o) => o.id}`,
    `getMemberName={(o) => o.label}`, `onSearch={() => {}}`, `onPickGroup={(o) =>
    onFilterChange?.({ type_id: o.id === '' ? null : Number(o.id) })}`, `onPickMember={() => {}}`.

    Не трогать: ветку `{:else}` (не-movements домены), пять остальных принятых-и-выброшенных
    пропов (`statusId`, `modelId`, `color`, `search`, а также `deviceTypes` в контексте других
    доменов) — D-12, зафиксировано пользователем. Не менять `ReportsPage.svelte` — пропы уже
    передаются корректно.
  </action>
  <verify>
    <automated>cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "typeId: _typeId\|deviceTypes: _deviceTypes" ui/src/features/reports/ReportFilters.svelte`
      не находит совпадений (оба проп переименованы, подчёркивание убрано).
    - `grep -n "onFilterChange?.({ type_id:" ui/src/features/reports/ReportFilters.svelte`
      находит ровно один вызов внутри ветки `movements`.
    - `grep -c "Тип устройства" ui/src/features/reports/ReportFilters.svelte` >= 1.
    - `cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json` завершается без новых ошибок
      типов в `ReportFilters.svelte`.
    - `cd ui && pnpm run lint` (включает `check-report-type-parity.mjs`, `check-tokens.mjs` и
      весь остальной набор) завершается кодом 0 — фильтр по типу не меняет `reportType`/
      `reportDomain`, риска коллизии ключа `'all'` нет, но прогон обязателен (D-13).
  </acceptance_criteria>
  <done>Третий Dropdown отрисован в ветке movements, читает typeId/deviceTypes, вызывает onFilterChange с type_id; статические гейты и проверка типов зелёные.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Живая проверка фильтра «Тип устройства» в запущенном приложении</name>
  <files>N/A — задача только проверяет поведение, не меняет файлы</files>
  <read_first>
    ui/src/features/reports/ReportFilters.svelte (изменения Task 1)
  </read_first>
  <what-built>
    Контрол «Тип устройства» в отчёте «Перемещения» (`ui/src/features/reports/ReportFilters.svelte`)
    — третий Dropdown рядом с «Откуда»/«Куда», вызывающий бэкенд-фильтр
    `report_service.rs`'s `filter.type_id`, который уже существовал, но был недостижим из UI.
  </what-built>
  <how-to-verify>
    D-13/D-25: одной компиляции недостаточно — svelte-check/eslint/pnpm build не ловят рантайм-ошибки
    рун Svelte 5, нужен живой прогон.
    1. Запустить `cargo tauri dev` из корня проекта, дождаться загрузки десктопного окна.
    2. Открыть раздел «Отчёты» -> вкладку «Перемещения».
    3. Задать период (любой достаточно широкий, чтобы были строки).
    4. Убедиться, что рядом с «Откуда»/«Куда» появился третий контрол «Тип устройства» с
       выпадающим списком (кастомный Dropdown — открывается кликом, список прокручиваемый, НЕ
       нативное OS-меню браузера/ОС).
    5. Выбрать конкретный тип (например «Устройство» или «Расходник») — убедиться, что таблица
       отчёта сузилась (число строк уменьшилось или осталось равным, если все записи одного
       типа, но фильтр применился видимо — сравнить со значением "Все типы").
    6. Сбросить фильтр обратно на «Все типы» — убедиться, что полный список вернулся.
    7. Проверить, что фильтр по месту («Откуда»/«Куда») продолжает работать одновременно с
       фильтром по типу (задать оба — выдача сужается по обоим условиям).
  </how-to-verify>
  <action>Пройти шаги <how-to-verify> в запущенном приложении и сообщить результат — "approved" либо конкретное расхождение с ожидаемым поведением.</action>
  <verify>
    <automated>MISSING — рантайм-поведение Svelte 5 рун не ловится ни одним статическим гейтом проекта (D-25); единственная проверка — ручной прогон cargo tauri dev по шагам how-to-verify.</automated>
  </verify>
  <acceptance_criteria>
    - Контрол «Тип устройства» виден и кликабелен в запущенном приложении.
    - Выбор конкретного типа видимо сужает таблицу отчёта «Перемещения».
    - Одновременный выбор места и типа сужает выдачу по обоим условиям.
    - В консоли разработчика нет ошибок вида effect_update_depth_exceeded или иных исключений.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>Пользователь подтвердил "approved" в запущенном приложении, либо явно описал расхождение для следующей итерации.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser/desktop UI -> report_service.rs | `type_id` — пользовательский ввод, попадает в SQL как параметризованное значение (не строится строкой) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40.1-03 | Tampering | `ReportFilters.svelte` -> `onFilterChange({ type_id })` | accept | `type_id` — числовой id из уже загруженного справочника типов устройств (`deviceTypes`), не свободный ввод; бэкенд `report_service.rs:1449-1455` уже параметризует значение через `owned_params.push(Box::new(type_id))` (не строковую конкатенацию) — этот план не меняет бэкенд и не расширяет поверхность атаки. |
| T-40.1-04 | Elevation of Privilege | Доступность фильтра/отчёта по ролям | accept | Экспорт и чтение отчёта «Перемещения» уже гейтуются `Action::ReadPlaces` (Фаза 40, план 40-14) — этот план не меняет авторизацию, только добавляет визуальный контрол существующего параметра. |
</threat_model>

<verification>
1. `cd ui && pnpm exec svelte-check --tsconfig ./tsconfig.json` — без новых ошибок.
2. `cd ui && pnpm run lint` — весь набор гейтов (включая `check-report-type-parity.mjs`) зелёный.
3. Живой прогон `cargo tauri dev` (checkpoint) — фильтр реально сужает выдачу отчёта.
</verification>

<success_criteria>
- Пользователь видит и может использовать фильтр «Тип устройства» в отчёте «Перемещения».
- Фильтр по месту и по типу устройства работают одновременно (HST-04 дословно).
- Остальные домены отчётов не затронуты.
</success_criteria>

<output>
Create `.planning/phases/40.1-audit-gap-closure/40.1-02-SUMMARY.md` when done
</output>
