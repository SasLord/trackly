---
phase: 29-login-and-employee-shell
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - ui/src/features/auth/PendingScreen.svelte
  - ui/src/features/auth/BlockedScreen.svelte
autonomous: true
requirements: [WIN-10]

must_haves:
  truths:
    - "PendingScreen.svelte renders its back-to-login control as a Button (variant=link), wrapped in AuthShell (D-02), with zero bespoke .btn-link/.login-container/.login-card classes"
    - "BlockedScreen.svelte renders all 4 conditional branches' CTAs as Button (primary/link variants), wrapped in AuthShell stack=true (D-02), with zero bespoke .btn-submit/.btn-link/.login-container/.login-card classes"
    - "BlockedScreen's 4-branch conditional logic (submitted / pending / rejection_reason / default) and handleRestoreRequest's request_ad_restore call are byte-identical to before this plan"
  artifacts:
    - path: "ui/src/features/auth/PendingScreen.svelte"
      provides: "Pending status screen on AuthShell + Button"
    - path: "ui/src/features/auth/BlockedScreen.svelte"
      provides: "Blocked status screen (4 branches) on AuthShell + Button"
  key_links:
    - from: "ui/src/features/auth/PendingScreen.svelte"
      to: "ui/src/lib/components/AuthShell.svelte"
      via: "import + <AuthShell> wrapper"
      pattern: "import AuthShell from '\\$lib/components/AuthShell\\.svelte'"
    - from: "ui/src/features/auth/BlockedScreen.svelte"
      to: "ui/src/lib/components/AuthShell.svelte"
      via: "<AuthShell stack> wrapper (preserves flex-column+gap card layout)"
      pattern: "stack"
---

<objective>
Migrate `PendingScreen.svelte` and `BlockedScreen.svelte` — the two status-only auth screens (no
form fields) — off their hand-rolled `.login-container`/`.login-card`/`.btn-link`/`.btn-submit`
markup onto `AuthShell` and `Button`, built in Wave 1 (29-01). These are the simplest of the 4
auth screens: no `Input`/`Checkbox`/`FormField` involved, only shell chrome + button variants.

Purpose: WIN-10 / D-02 (shared shell) from `29-CONTEXT.md`. `BlockedScreen` specifically exercises
`AuthShell`'s `stack` prop (built in 29-01 to preserve this screen's existing flex-column+gap card
layout across its 4 conditional branches).
Output: Both screens compiled against `AuthShell` + `Button`, zero bespoke chrome/button CSS
remaining in either file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-login-and-employee-shell/29-CONTEXT.md
@.planning/phases/29-login-and-employee-shell/29-PATTERNS.md
@.planning/phases/29-login-and-employee-shell/29-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts created by 29-01 — use directly, do not re-derive. -->

AuthShell.svelte (29-01 Task 2):
```typescript
interface Props {
  maxWidth?: number;  // default 360 — both screens use the default, no need to pass it
  stack?: boolean;    // default false — BlockedScreen passes stack={true} (or bare `stack`)
  children?: Snippet;
}
```
`stack=true` renders `.auth-card`'s modifier: `display:flex; flex-direction:column; gap:
var(--tr-space-md); text-align:center;` — this is BlockedScreen's exact current card style
(`BlockedScreen.svelte:130-133`), now centralized. PendingScreen does NOT need `stack` — its
current card has no flex-column/gap, just `text-align:center` with default block-stacked children
(title has its own bottom margin, paragraph has its own bottom margin) — verify visually that
default (non-stack) AuthShell reproduces this; if the default block layout does not naturally add
the `text-align:center` PendingScreen needs, add a local `.pending-card { text-align:center; }`
wrapper div inside AuthShell's children as a minimal scoped fallback (Claude's Discretion — do not
change AuthShell's default behavior for this one screen if it already has the right visual base).

Button.svelte (unchanged, existing primitive):
```typescript
interface Props {
  variant?: 'primary' | 'secondary' | 'destructive' | 'ghost' | 'link';
  size?: 'sm' | 'md';
  loading?: boolean;
  disabled?: boolean;
  type?: 'button' | 'submit';
  onclick?: () => void;
  children?: Snippet;
}
```
`variant="link"` already matches `.btn-link`'s current visual contract (transparent bg, `var(--tr-
accent)` text, underline on hover, focus-ring). `variant="primary"` + `loading` already matches
`.btn-submit`'s contract and folds the manual `{#if submitting}Отправка…{:else}...{/if}` text-swap
into the `loading` prop, same pattern as Plan 29-02.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate PendingScreen.svelte to AuthShell + Button</name>
  <files>ui/src/features/auth/PendingScreen.svelte</files>
  <read_first>
    ui/src/features/auth/PendingScreen.svelte (full file, 77 lines — already captured above in full);
    this plan's `<interfaces>` block for the AuthShell/Button contracts.
  </read_first>
  <action>
    This is the simplest screen — no props/state changes at all, purely a markup/style swap. Import
    `AuthShell` and `Button` from `$lib/components/`. Replace lines 12-23 (`.login-container`/
    `.login-card` wrapper) with `<AuthShell>` wrapping the existing `<h1 class="login-title">Заявка
    отправлена</h1>` and `<p class="screen-body">...</p>` (both kept verbatim, same copy, same local
    classes) plus the CTA. Replace `<button class="btn-link" type="button" onclick={onBackToLogin}>
    Войти под другим пользователем</button>` with `<Button variant="link" onclick={onBackToLogin}>
    Войти под другим пользователем</Button>` — straight swap, `onBackToLogin` prop forwarded exactly
    as before, no logic change (this component has zero internal state — it is a pure props-in,
    events-out component; `interface Props { onBackToLogin: () => void; }` is unchanged). Delete the
    now-unused `.login-container`, `.login-card`, `.btn-link` CSS rules (now owned by AuthShell /
    Button) from the `<style>` block. Keep `.login-title` and `.screen-body` locally (unchanged copy
    placement).
  </action>
  <acceptance_criteria>
    - grep -c "class=\"btn-link\"" ui/src/features/auth/PendingScreen.svelte returns 0
    - grep -c "login-container\|login-card" ui/src/features/auth/PendingScreen.svelte returns 0
    - grep -c "import AuthShell" ui/src/features/auth/PendingScreen.svelte returns 1 and grep -c "import Button" returns 1
    - grep -c "variant=\"link\"" ui/src/features/auth/PendingScreen.svelte returns 1
    - `interface Props { onBackToLogin: () => void; }` unchanged (grep -c "onBackToLogin: () => void" returns 1)
    - `pnpm --dir ui build` succeeds; `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <done>PendingScreen.svelte renders through AuthShell + Button with zero bespoke chrome/button classes; same copy, same single CTA, same Props contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate BlockedScreen.svelte to AuthShell (stack) + Button (4 branches)</name>
  <files>ui/src/features/auth/BlockedScreen.svelte</files>
  <read_first>
    ui/src/features/auth/BlockedScreen.svelte (full file, 197 lines — already captured above in full,
    including the 4 conditional branches at lines 63-109 and `handleRestoreRequest` at lines 42-58);
    this plan's `<interfaces>` block; Task 1 above (same substitution pattern for the simpler
    PendingScreen — read it first since BlockedScreen repeats the same swap 4x with variation).
  </read_first>
  <action>
    Leave lines 1-58 (script: `Props`, `submitted`/`submitting`/`serverError` state,
    `handleRestoreRequest`'s `request_ad_restore` call) completely untouched. Import `AuthShell` and
    `Button` from `$lib/components/`. Wrap the whole component in `<AuthShell stack>` (bare `stack`
    shorthand for `stack={true}`) — this reproduces the exact current `.login-card`'s `display:flex;
    flex-direction:column; gap:var(--tr-space-md); text-align:center;` modifier that all 4 branches
    rely on for spacing. Inside, keep the `{#if submitted}...{:else if blockedDetails.pending}...
    {:else if blockedDetails.rejection_reason}...{:else}...{/if}` structure and all 4 branches' `<h1
    class="login-title">`/`<p class="screen-body">` copy exactly as-is (zero copy/condition changes).
    In each branch, replace `<button class="btn-link" type="button" onclick={onBackToLogin}>Войти под
    другим пользователем</button>` (appears in all 4 branches) with `<Button variant="link"
    onclick={onBackToLogin}>Войти под другим пользователем</Button>`. In the `rejection_reason` and
    default (`else`) branches, replace `<button class="btn-submit" type="button" disabled={submitting}
    onclick={handleRestoreRequest}>{#if submitting}Отправка…{:else}Запросить снова{/if}</button>` (and
    the default branch's `Запросить восстановление доступа` variant) with `<Button variant="primary"
    loading={submitting} onclick={handleRestoreRequest}>Запросить снова</Button>` (respectively
    `Запросить восстановление доступа</Button>`) — `loading` folds the manual text-swap, pass the
    label unconditionally as children. Keep `{#if serverError}<div class="server-error">
    {serverError}</div>{/if}` exactly as-is in both branches that have it (no primitive, same pattern
    as Plan 29-02). Delete the now-unused `.login-container`, `.login-card`, `.btn-submit`, `.btn-link`
    CSS rules from the `<style>` block. Keep `.login-title`, `.screen-body`, `.server-error` locally.
  </action>
  <acceptance_criteria>
    - grep -c "class=\"btn-link\"\|class=\"btn-submit\"" ui/src/features/auth/BlockedScreen.svelte returns 0
    - grep -c "login-container\|login-card" ui/src/features/auth/BlockedScreen.svelte returns 0
    - grep -c "import AuthShell" ui/src/features/auth/BlockedScreen.svelte returns 1 and grep -c "import Button" returns 1
    - grep -c "<AuthShell stack" ui/src/features/auth/BlockedScreen.svelte returns 1
    - grep -c "variant=\"link\"" ui/src/features/auth/BlockedScreen.svelte returns 4 (one per branch)
    - grep -c "variant=\"primary\"" ui/src/features/auth/BlockedScreen.svelte returns 2 (rejection_reason + default branches)
    - `handleRestoreRequest`, `submitted`/`submitting`/`serverError` state, and the 4-branch `{#if
      submitted}...{:else if blockedDetails.pending}...{:else if blockedDetails.rejection_reason}...
      {:else}` structure are present and byte-identical to before (diff shows no edits to lines 1-58 or to any condition expression)
    - `pnpm --dir ui build` succeeds; `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <done>BlockedScreen.svelte renders all 4 branches through AuthShell(stack) + Button with zero bespoke chrome/button classes; restoration-flow logic and copy are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `request_ad_restore` Tauri command / HTTP endpoint | Untrusted restore-request input crosses here — UNCHANGED by this plan (markup/style layer only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Information Disclosure | BlockedScreen.svelte restoration-flow branching (D-REG-03) | mitigate | The 4-branch conditional (`submitted`/`pending`/`rejection_reason`/default) and `handleRestoreRequest` are not touched — only surrounding markup/style changes; acceptance criteria assert byte-identity of lines 1-58 and all condition expressions. |
| T-29-07 | Repudiation | BlockedScreen.svelte restore-CTA (`request_ad_restore`) | accept | Button's `onclick` prop forwards to the exact same `handleRestoreRequest` function reference — no new indirection, no change to when/how the request fires. |
| T-29-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No new package-manager installs in this plan. |
</threat_model>

<verification>
- `pnpm --dir ui lint` exits 0
- `pnpm --dir ui svelte-check` exits 0
- `pnpm --dir ui build` succeeds
- Manual diff review confirming zero changes to either screen's conditional logic / state / API calls
</verification>

<success_criteria>
- PendingScreen.svelte and BlockedScreen.svelte render 100% of their chrome/CTAs through AuthShell + Button
- Zero bespoke `.login-container`/`.login-card`/`.btn-link`/`.btn-submit` classes remain in either file
- BlockedScreen's 4-branch restoration-flow logic is unchanged
</success_criteria>

<output>
Create `.planning/phases/29-login-and-employee-shell/29-03-SUMMARY.md` when done
</output>
