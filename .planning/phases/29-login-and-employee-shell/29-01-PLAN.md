---
phase: 29-login-and-employee-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/Input.svelte
  - ui/src/lib/components/AuthShell.svelte
  - ui/src/lib/components/FormField.svelte
autonomous: true
requirements: [WIN-10]

must_haves:
  truths:
    - "Input.svelte's type union accepts 'password' additively (D-01) — all 19 existing consumers keep compiling and rendering unchanged"
    - "A single reusable AuthShell component exists (D-02) that owns the center-card chrome (background/border/radius/shadow/max-width) currently duplicated 4x across the auth screens"
    - "A single reusable FormField component exists (D-01/D-02) that renders label + child control + error/hint, and computes the aria-describedby id so field wiring is correct by construction for downstream consumers"
  artifacts:
    - path: "ui/src/lib/components/Input.svelte"
      provides: "type union extended to include 'password'"
      contains: "'password'"
    - path: "ui/src/lib/components/AuthShell.svelte"
      provides: "auth center-card shell primitive (Props: maxWidth, stack, children)"
      min_lines: 20
    - path: "ui/src/lib/components/FormField.svelte"
      provides: "label/error/hint field wrapper with snippet-param aria wiring"
      min_lines: 20
  key_links:
    - from: "ui/src/lib/components/FormField.svelte"
      to: "Snippet<[{ describedBy, invalid }]> children param"
      via: "{@render children({ describedBy, invalid })}"
      pattern: "children\\(\\{ describedBy"
---

<objective>
Build the two shared artifacts that Wave 2 (the 4 auth screens) depend on: extend the `Input`
primitive to support `type="password"`, and extract a reusable `AuthShell` (center-card chrome)
+ `FormField` (label/control/error/hint) pair from the byte-near-identical markup duplicated
today across `LoginPage.svelte`, `FirstRunWizard.svelte`, `PendingScreen.svelte`, and
`BlockedScreen.svelte`.

Purpose: D-01 (Input extension) and D-02 (shared auth-shell) from `29-CONTEXT.md`. This is the
"interface-first" wave — Wave 2 plans (29-02, 29-03) implement against the exact contracts
defined here, so no downstream executor needs to invent the shape of these components.
Output: `Input.svelte` (extended), `AuthShell.svelte` (new), `FormField.svelte` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-login-and-employee-shell/29-CONTEXT.md
@.planning/phases/29-login-and-employee-shell/29-PATTERNS.md
@.planning/phases/27-core-workflow-windows/27-CONTEXT.md
</context>

<interfaces>
<!-- Exact current state of the files this plan touches/extracts from — no exploration needed. -->

From ui/src/lib/components/Input.svelte (current, unmodified):
```typescript
interface Props {
  type?: 'text' | 'number' | 'search';
  value: string;
  placeholder?: string;
  disabled?: boolean;
  invalid?: boolean;
  id?: string;
  'aria-describedby'?: string;
  oninput?: (_value: string) => void;
  iconLeft?: Snippet;
}

let {
  type = 'text',
  value = $bindable(''),
  ...
}: Props = $props();
```
The `<input {type} ... />` element already spreads `type` straight through — no other line in this
file changes. `value = $bindable('')` uses `let`, not `const` (Phase 24 lesson — preserve exactly).

From ui/src/lib/components/PageHeader.svelte and DetailPanel.svelte (extraction precedent — Snippet
props, `const` destructuring, no `$bindable`, scoped SCSS, single responsibility):
```typescript
interface Props {
  title?: string;
  actions?: Snippet;
}
const { title, actions }: Props = $props();
```

Chrome block duplicated identically (modulo max-width 360 vs 400, and BlockedScreen's flex-column)
across all 4 auth screens today (`LoginPage.svelte:164-188`, `FirstRunWizard.svelte:179-203`,
`PendingScreen.svelte:26-51`, `BlockedScreen.svelte:114-134`):
```scss
.login-container { display:flex; align-items:center; justify-content:center; min-height:100vh; background:var(--tr-bg); }
.login-card { background:var(--tr-surface); border:1px solid var(--tr-border); border-radius:var(--tr-radius-lg);
  padding:var(--tr-space-2xl) var(--tr-space-4xl, 2rem); width:100%; max-width:360px; box-shadow:var(--tr-elev-2); }
```
`BlockedScreen.svelte`'s `.login-card` additionally has `text-align:center; display:flex; flex-direction:column;
gap:var(--tr-space-md);` (lines 130-133) — its 4 conditional branches (title/paragraph/error/buttons) rely on
this flex+gap for spacing, unlike the other 3 screens where spacing comes from the title's own margin or the
form's own `gap`. `--tr-space-4xl` IS defined (`_tokens.scss:185`, `48px`) — the `, 2rem` fallback is dead and
must be dropped when this chrome moves into `AuthShell`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Input.svelte type union to include 'password'</name>
  <files>ui/src/lib/components/Input.svelte</files>
  <read_first>
    ui/src/lib/components/Input.svelte (full file, already quoted above — read to confirm no drift
    before editing); 29-PATTERNS.md section "ui/src/lib/components/Input.svelte (primitive — D-01
    extension point)" for the full consumer list (19 files) that must not regress.
  </read_first>
  <action>
    In the `Props` interface, change `type?: 'text' | 'number' | 'search';` to
    `type?: 'text' | 'number' | 'search' | 'password';`. Do not add `'email'` — no in-scope screen in
    this phase uses `type="email"` (per D-01 discretion note, only add union members with an actual
    consumer). Make no other edit to this file: the `{type}` spread on the native `<input>` element
    already forwards the value untouched, and `value = $bindable('')` must stay declared with `let`
    (not `const`) — this is a two-way-binding contract per the Phase 24 lesson, not a style choice.
  </action>
  <acceptance_criteria>
    - Input.svelte's `Props` interface `type` union literally contains `'password'` (grep -c "'password'" ui/src/lib/components/Input.svelte returns >= 1)
    - No other line in Input.svelte changed (diff shows a single-line edit to the union)
    - `value = $bindable(''` still present with `let`, not `const` (grep -c "let {" ui/src/lib/components/Input.svelte returns 1 covering the destructure)
    - `pnpm --dir ui build` succeeds (additive union change cannot break any of the 19 existing `type="text"`/omitted-type consumers)
  </acceptance_criteria>
  <done>Input.svelte accepts `type="password"`; zero other consumers touched; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create AuthShell.svelte — extracted center-card chrome</name>
  <files>ui/src/lib/components/AuthShell.svelte</files>
  <read_first>
    ui/src/lib/components/PageHeader.svelte and ui/src/lib/components/DetailPanel.svelte (extraction
    precedent — Snippet props, `const` destructure, scoped SCSS); the 4 duplicated chrome blocks quoted
    in `<interfaces>` above (`LoginPage.svelte:164-188`, `FirstRunWizard.svelte:179-203`,
    `PendingScreen.svelte:26-51`, `BlockedScreen.svelte:114-134`); `ui/src/styles/_tokens.scss` lines
    50-55/127-132/185/190 (confirms `--tr-space-4xl` and `--tr-danger-text` exist — needed for Task 3).
  </read_first>
  <action>
    Create `AuthShell.svelte` with Props `{ maxWidth?: number; stack?: boolean; children?: Snippet }`
    (defaults: `maxWidth = 360`, `stack = false`), destructured with `const` (no `$bindable` prop here).
    Render `&lt;div class="auth-shell"&gt;&lt;div class="auth-card" class:stack style:max-width="{maxWidth}px"&gt;
    {@render children?.()}&lt;/div&gt;&lt;/div&gt;`. Do NOT render a title element inside AuthShell — each
    screen keeps its own `&lt;h1&gt;`/paragraph markup as children, since title-to-content spacing differs
    per screen (LoginPage relies on the title's own bottom margin; BlockedScreen relies on the card's
    flex+gap) and this phase must not redesign layout (CONTEXT.md "Вне границ").
    SCSS: `.auth-shell` = `display:flex; align-items:center; justify-content:center; min-height:100vh;
    background:var(--tr-bg);` (ported verbatim from `.login-container`). `.auth-card` = `background:
    var(--tr-surface); border:1px solid var(--tr-border); border-radius:var(--tr-radius-lg); padding:
    var(--tr-space-2xl) var(--tr-space-4xl); width:100%; box-shadow:var(--tr-elev-2);` — drop the
    `, 2rem` fallback (confirmed dead token per Task 2's read_first). Add `&.stack { display:flex;
    flex-direction:column; gap:var(--tr-space-md); text-align:center; }` for the `stack=true` case
    (this is BlockedScreen's exact current card modifier, moved into the shared component instead of
    duplicated). `max-width` is set via the `style:` directive per-instance (`maxWidth` prop), not
    hardcoded in SCSS, so callers can pass 360 (default, matches Login/Pending/Blocked) or 400
    (FirstRunWizard, preserving its existing visually-wider card exactly as today).
  </action>
  <acceptance_criteria>
    - ui/src/lib/components/AuthShell.svelte exists, exports a component with Props `maxWidth`, `stack`, `children`
    - grep -c "style:max-width" ui/src/lib/components/AuthShell.svelte returns 1
    - grep -c "var(--tr-space-4xl, 2rem)" ui/src/lib/components/AuthShell.svelte returns 0 (dead fallback dropped)
    - grep -c "class:stack" ui/src/lib/components/AuthShell.svelte returns 1
    - No `title` prop / no `&lt;h1&gt;` rendered inside AuthShell.svelte (grep -c "&lt;h1" returns 0)
    - `pnpm --dir ui build` succeeds (new isolated component, no consumers yet — this task only creates it)
  </acceptance_criteria>
  <done>AuthShell.svelte exists as a standalone, uninstantiated component ready for Wave 2 to consume; matches the exact visual output of the 4 screens' current chrome (verified visually once wired in Wave 2).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create FormField.svelte — label/control/error/hint wrapper with aria wiring</name>
  <files>ui/src/lib/components/FormField.svelte</files>
  <read_first>
    ui/src/lib/components/DetailField.svelte (label/value pair precedent — closest existing analog,
    though read-only); `ui/src/features/devices/DeviceFormBody.svelte` lines 196-211 (the app-wide
    `.field`/`.label`/`.field-error` convention this component formalizes for auth); the exact
    `.form-field`/`.form-label`/`.field-error`/`.format-hint` CSS block in `LoginPage.svelte:196-241`
    (source of the styling to port); `.planning/reference/design-system-v2/Fields.dc.html` line 115
    (`errHint = { fontSize:'12px', color:'var(--tr-danger-text)' }` — the canonical error-hint color,
    distinct from the app-wide `--tr-danger` convention).
  </read_first>
  <action>
    Create `FormField.svelte` with Props `{ label: string; id: string; error?: string | null; hint?:
    string; children: Snippet<[{ describedBy: string | undefined; invalid: boolean }]> }`, destructured
    with `const`. Compute `const describedBy = $derived(error ? \`${id}-error\` : hint ? \`${id}-hint\`
    : undefined);`. Render: `&lt;div class="form-field"&gt;&lt;label class="form-label" for={id}&gt;
    {label}&lt;/label&gt;{@render children({ describedBy, invalid: !!error })}{#if error}&lt;span
    class="field-error" id="{id}-error"&gt;{error}&lt;/span&gt;{:else if hint}&lt;span class=
    "format-hint" id="{id}-hint"&gt;{hint}&lt;/span&gt;{/if}&lt;/div&gt;`. This snippet-with-parameters
    shape is the mechanism that lets each screen's call site receive a ready-made `describedBy`/`invalid`
    pair and forward it straight to `&lt;Input&gt;` — no screen re-derives this logic itself (closes the
    D-01 "Действие для планировщика" ask: error/hint were only visually adjacent before, never wired via
    `aria-describedby`). SCSS: port `.form-field` (`display:flex; flex-direction:column; gap:
    var(--tr-space-2xs);`), `.form-label` (`font-size:var(--tr-font-size-label); font-weight:
    var(--tr-font-weight-medium); color:var(--tr-text-secondary);`), and `.format-hint` (`font-size:
    var(--tr-font-size-label); color:var(--tr-text-tertiary); line-height:var(--tr-line-height-label);`)
    verbatim from `LoginPage.svelte`. For `.field-error`, use `color:var(--tr-danger-text)` (NOT
    `var(--tr-danger)`) — this is a deliberate deviation from the pre-existing `LoginPage.svelte`
    convention, per Claude's Discretion in `29-PATTERNS.md`: `Fields.dc.html` (the design-system source
    of truth for this phase, since no `.dc` mockup exists for auth screens specifically) uses
    `--tr-danger-text` for its error hint, and this component is net-new so there is no existing
    consumer to regress.
  </action>
  <acceptance_criteria>
    - ui/src/lib/components/FormField.svelte exists, Props include `label`, `id`, `error`, `hint`, `children`
    - grep -c "children(\\s*{\\s*describedBy" ui/src/lib/components/FormField.svelte (or equivalent single-line form) confirms the snippet is invoked with a parameter object — i.e. grep -c "children({ describedBy" returns 1
    - grep -c "\-\-tr-danger-text" ui/src/lib/components/FormField.svelte returns 1 (error color uses the Fields.dc-sourced token, not `--tr-danger`)
    - grep -c "id=\"{id}-error\"" ui/src/lib/components/FormField.svelte returns 1 and grep -c "id=\"{id}-hint\"" returns 1
    - `pnpm --dir ui build` succeeds
  </acceptance_criteria>
  <done>FormField.svelte exists as a standalone component with the exact snippet-parameter contract Wave 2 will consume; error/hint ids are always generated from the field's own `id`, ready to be passed to `Input`'s `aria-describedby`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan only creates/extends UI primitives (no new data flow, no new trust boundary crosses here). Input.svelte's `type='password'` extension does not change how the value is captured, stored, or transmitted — it is a purely additive DOM attribute passthrough. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | Input.svelte type union | accept | Additive-only change (new union member, zero logic change); `pnpm --dir ui build` proves the 19 existing consumers are unaffected — no code path exists that could be tampered with by this edit. |
| T-29-02 | Information Disclosure | Input.svelte (password masking) | mitigate | `type="password"` relies on the browser's native masking via the `type` attribute passthrough already in place (line `{type}` on the `<input>`) — no new `value` logging, no `console.log`, no custom rendering of the raw value is introduced by this plan. |
| T-29-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No new package-manager installs in this plan — Input/AuthShell/FormField are hand-written Svelte components using only already-vetted project dependencies (Svelte 5 runes). Package Legitimacy Gate not triggered. |
</threat_model>

<verification>
- `pnpm --dir ui lint` exits 0 (eslint + prettier + check-tokens.mjs)
- `pnpm --dir ui svelte-check` exits 0
- `pnpm --dir ui build` succeeds
- Manual grep verification per each task's acceptance_criteria above
</verification>

<success_criteria>
- Input.svelte's type union includes 'password' with zero regressions to its 19 consumers
- AuthShell.svelte and FormField.svelte exist as standalone, buildable components with the exact contracts Wave 2 plans (29-02, 29-03) will consume
- No auth screen file modified yet (that is Wave 2's job) — this plan is purely additive infrastructure
</success_criteria>

<output>
Create `.planning/phases/29-login-and-employee-shell/29-01-SUMMARY.md` when done
</output>
