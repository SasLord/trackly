---
phase: 29-login-and-employee-shell
plan: 04
type: execute
wave: 3
depends_on: ["29-02", "29-03"]
files_modified:
  - ui/src/features/layout/EmployeeLayout.svelte
autonomous: false
requirements: [WIN-11]

must_haves:
  truths:
    - "EmployeeLayout's header height and content min-height resolve from --header-height with no hardcoded 56px fallback anywhere in the file"
    - "EmployeeLayout remains header-only — no sidebar/nav-toggle is added (D-03: the ROADMAP.md SC #2 mention of 'сайдбар' is descriptive boilerplate carried over from roadmap phrasing, NOT a requirement; this mismatch is recorded here so the verifier does not flag a missing sidebar as an SC #2 failure)"
    - "features/requests/RequestsPage.svelte (migrated in Phase 28, WIN-06) renders correctly inside EmployeeLayout in both light and dark themes — no re-migration performed, only visual parity confirmed (D-04)"
    - "All 4 auth screens (LoginPage/FirstRunWizard/PendingScreen/BlockedScreen) and EmployeeLayout pass pnpm lint / svelte-check / build / check-tokens and a human visual UAT in both themes"
  artifacts:
    - path: "ui/src/features/layout/EmployeeLayout.svelte"
      provides: "header-only employee shell, consistency-passed onto --header-height token, no fallback"
  key_links:
    - from: "ui/src/features/layout/EmployeeLayout.svelte"
      to: "ui/src/styles/_tokens.scss --header-height"
      via: "var(--header-height) with no fallback (matches PageHeader.svelte convention)"
      pattern: "var\\(--header-height\\)(?!, 56px)"
---

<objective>
Finish WIN-11 with a header-chrome consistency pass on `EmployeeLayout.svelte` (D-03: header-only
stays header-only — no sidebar), then close the phase with the mandatory automated gates and a
human visual UAT across all 5 touched surfaces (4 auth screens + EmployeeLayout) in both themes,
including a visual-parity check that the already-migrated `RequestsPage` (Phase 28) still renders
correctly inside the employee-shell (D-04 — not a re-migration).

Purpose: WIN-11 from `29-CONTEXT.md`; final-gate precedent from Phase 26 (D-17/D-18), Phase 27-09,
Phase 28-10 (build gate + both-theme visual UAT checkpoint before closing the phase).
Output: `EmployeeLayout.svelte` on token-clean header chrome; green `lint`/`svelte-check`/`build`/
`check-tokens`; signed-off human visual UAT covering the whole phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-login-and-employee-shell/29-CONTEXT.md
@.planning/phases/29-login-and-employee-shell/29-PATTERNS.md
@.planning/phases/10-employee-role-restriction/10-CONTEXT.md
</context>

<interfaces>
<!-- Current EmployeeLayout.svelte state — already read in full, relevant excerpt below. -->

Current header/content CSS (`EmployeeLayout.svelte:127-136,165-169`):
```scss
.employee-header {
  height: var(--header-height, 56px);   // redundant fallback — --header-height IS defined (56px, _tokens.scss:190)
  ...
}
.employee-content {
  ...
  min-height: calc(100vh - 56px);       // hardcoded 56px instead of the token
  overflow: auto;
}
```
`PageHeader.svelte`'s equivalent convention (no fallback, confirmed at `_tokens.scss:190`):
```scss
.page-header {
  height: var(--header-height);
  ...
}
```
`.skip-link` (`EmployeeLayout.svelte:172-188`) is already byte-for-byte identical to `Layout.svelte`'s
own `.skip-link` — no change needed. `Button`/`ThemeSwitcher` are already primitives in this file
(lines 9-10, 108-111) — nothing to swap there.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: EmployeeLayout.svelte header-chrome consistency pass</name>
  <files>ui/src/features/layout/EmployeeLayout.svelte</files>
  <read_first>
    ui/src/features/layout/EmployeeLayout.svelte (full file, already captured above in full); `ui/src/
    lib/components/PageHeader.svelte` (the token-clean convention to match); `ui/src/styles/
    _tokens.scss` line 190 (confirms `--header-height: 56px` is defined, making both fallbacks dead).
  </read_first>
  <action>
    Two single-line edits only, no markup change, no logic change. In `.employee-header`'s `height`
    declaration, change `var(--header-height, 56px)` to `var(--header-height)` — matches `PageHeader.
    svelte`'s convention exactly, and the token is confirmed to exist so the fallback is dead code. In
    `.employee-content`'s `min-height` declaration, change `calc(100vh - 56px)` to `calc(100vh -
    var(--header-height))` — replaces the hardcoded magic number with the same token the header itself
    uses, so the two values cannot drift independently in future token changes. Do not touch anything
    else in this file: `onMount`/`connectWs`/`onWsEvent`/`handleEmployeeWsEvent`/`logout` (lines
    22-95), the `<Button variant="ghost" size="sm" onclick={logout}>` (already a primitive), `<a
    href="#main" class="skip-link">` (already token-clean), or `<ThemeSwitcher />`.
  </action>
  <acceptance_criteria>
    - grep -c "var(--header-height, 56px)" ui/src/features/layout/EmployeeLayout.svelte returns 0
    - grep -c "calc(100vh - 56px)" ui/src/features/layout/EmployeeLayout.svelte returns 0
    - grep -c "var(--header-height)" ui/src/features/layout/EmployeeLayout.svelte returns 2 (header height + content min-height)
    - `onMount`, `connectWs`, `onWsEvent`, `handleEmployeeWsEvent`, `logout` are present and byte-identical to before (diff shows only the 2 CSS-value edits, zero script/markup changes)
    - `pnpm --dir ui build` succeeds; `pnpm --dir ui svelte-check` exits 0; `node ui/scripts/check-tokens.mjs` (or `pnpm --dir ui lint`, which includes it) exits 0
  </acceptance_criteria>
  <done>EmployeeLayout.svelte's header/content sizing is driven entirely by the --header-height token with zero hardcoded fallbacks; WS/logout/skip-link logic untouched; still header-only (no sidebar added, per D-03).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final automated gates across the whole phase</name>
  <files>none (verification-only task, no files modified)</files>
  <read_first>
    None — this task runs the project's existing gate scripts against the files touched by 29-01
    through 29-04. Project memory gotcha: LAN-browser/server-mode serves `ui/dist`, and `cargo tauri
    dev` only HMRs the desktop webview, so `pnpm --dir ui build` MUST be run before Task 3's browser
    verification step can see any of this phase's changes.
  </read_first>
  <action>
    Run, in order: `pnpm --dir ui lint` (eslint + prettier + check-tokens.mjs), `pnpm --dir ui
    svelte-check`, `pnpm --dir ui build` (this also runs `prebuild` → `cargo test -p trackly-app
    --test export_bindings`, regenerating `bindings.ts` — expected to be a no-op diff since this phase
    makes no backend/API changes). Confirm all four gates exit 0. This step exists to catch any
    cross-file regression before the human checkpoint (e.g. a token typo in `AuthShell.svelte` that
    `check-tokens.mjs`'s closed-world gate would reject, or a `svelte-check` type error from the
    `FormField` snippet-param contract).
  </action>
  <acceptance_criteria>
    - `pnpm --dir ui lint` exits 0
    - `pnpm --dir ui svelte-check` exits 0
    - `pnpm --dir ui build` exits 0 and produces `ui/dist/`
    - `git diff --stat` shows no unexpected changes to `bindings.ts` (confirms no accidental backend drift — this phase is frontend-only)
  </acceptance_criteria>
  <done>All 4 automated gates green; ui/dist rebuilt and ready for LAN-browser verification in Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Both-theme visual UAT — 4 auth screens + EmployeeLayout + RequestsPage parity</name>
  <action>Conduct a blocking human visual review of all 5 touched surfaces (LoginPage, FirstRunWizard, PendingScreen, BlockedScreen, EmployeeLayout) plus RequestsPage-in-employee-shell parity, in both light and dark themes, per how-to-verify below; do not auto-advance (gate=blocking).</action>
  <what-built>
    Auth screens (`LoginPage`, `FirstRunWizard`, `PendingScreen`, `BlockedScreen`) fully on
    `Input`/`Button`/`Checkbox` primitives + shared `AuthShell`/`FormField`; `EmployeeLayout`'s header
    chrome consistency-passed onto the `--header-height` token. No behavior changed anywhere in this
    phase — auth-routing, error codes, anti-enumeration, reserved-SSO stub, WS logic, and
    `RequestsPage`'s own logic are all untouched.
  </what-built>
  <how-to-verify>
    1. Run `pnpm --dir ui build` if not already done in Task 2 (LAN-browser/server-mode serves
       `ui/dist` — `cargo tauri dev` alone will NOT reflect these changes in a browser tab).
    2. Start the app in both delivery modes if possible: `cargo tauri dev` (desktop webview) and the
       server/LAN mode pointed at the freshly built `ui/dist`.
    3. **LoginPage** (`#/login` or first screen shown pre-auth): confirm the login/password fields,
       "Запомнить меня" checkbox, "Войти" submit button (with loading spinner while submitting), and
       the greyed-out "Вход по учётной записи Windows (скоро)" button (confirm it does NOT respond to
       clicks and is skipped when Tab-navigating from the checkbox to the submit button) all look
       visually consistent with the rest of the redesigned app (Devices/Acts/etc. from Phases 24-28).
       Check in BOTH light and dark theme (use the theme switcher if reachable, or toggle OS theme).
    4. **FirstRunWizard**: on a fresh DB (or by temporarily renaming the DB file) confirm the 4-field
       admin-creation form renders identically in style to LoginPage's fields, wider card (400px)
       intact, both themes.
    5. **PendingScreen**: trigger by attempting login with a pending-registration AD account (or
       inspect visually via the component in isolation if no such account is available) — confirm the
       "Войти под другим пользователем" link renders as the app's link-style Button, both themes.
    6. **BlockedScreen**: trigger via a blocked/soft-deleted AD account across its 3-4 states (no
       request / pending / rejected-with-reason) if reachable, or inspect the card's flex-column+gap
       layout is visually unchanged from before, both themes.
    7. **EmployeeLayout**: log in as an `employee`-role user; confirm the header (brand, user
       name/role, theme switcher, "Выйти" button) renders correctly at the same height as before, both
       themes. Confirm there is still NO sidebar (this is intentional per D-03 — do not report this as
       a bug).
    8. **RequestsPage parity inside employee-shell (D-04)**: as the same employee user, confirm the
       Заявки page (list, own-request filter, form) renders with no visual regressions inside
       EmployeeLayout, in both themes — this page was already migrated in Phase 28 and should look
       identical to how it looks inside the main admin `Layout.svelte` shell.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 surfaces look correct in both themes, or describe the specific screen + theme + issue if not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | EmployeeLayout's WS event handling (`connectWs`/`onWsEvent`), the `is_visible_to` server-side filter that gates which requests an employee can see, and `logout`'s session-clearing behavior are all UNCHANGED by this plan — the only edit is 2 CSS token values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering | EmployeeLayout.svelte WS/logout logic | accept | Task 1's edit is scoped to exactly 2 CSS property values (`height`/`min-height`); `onMount`/`connectWs`/`onWsEvent`/`handleEmployeeWsEvent`/`logout` are asserted byte-identical in acceptance criteria. |
| T-29-09 | Elevation of Privilege | EmployeeLayout scope (D-03 header-only) | mitigate | This plan explicitly does NOT add a sidebar or any new navigable route/section — the security boundary for the employee role remains backend 403 (Phase 10), not client-side UI hiding; adding client-only nav affordances without a corresponding backend check would be the actual EoP risk, and this plan introduces none. |
| T-29-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No new package-manager installs in this plan. |
</threat_model>

<verification>
- `pnpm --dir ui lint` exits 0
- `pnpm --dir ui svelte-check` exits 0
- `pnpm --dir ui build` succeeds, `ui/dist` regenerated
- Human visual UAT (Task 3) approved in both themes across all 5 surfaces
</verification>

<success_criteria>
- EmployeeLayout.svelte's header sizing is token-clean (no hardcoded fallback), header-only preserved (D-03)
- All 4 auth screens + EmployeeLayout pass every automated gate
- Human sign-off confirms visual consistency with the rest of the redesigned app, in both themes, including RequestsPage-in-employee-shell parity (D-04)
- The SC #2 "сайдбар" wording in ROADMAP.md is explicitly recorded here as descriptive boilerplate, not a requirement — verifier must not flag a missing sidebar as a failure
</success_criteria>

<output>
Create `.planning/phases/29-login-and-employee-shell/29-04-SUMMARY.md` when done
</output>
