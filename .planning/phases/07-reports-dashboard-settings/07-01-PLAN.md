---
phase: 07-reports-dashboard-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V026__org_settings.sql
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/src/dto/mod.rs
  - crates/trackly-app/tests/report_acts.rs
  - crates/trackly-app/tests/report_cartridges.rs
  - crates/trackly-app/tests/report_period_bounds.rs
  - crates/trackly-app/tests/report_csv_export.rs
  - crates/trackly-app/tests/dashboard_widgets.rs
  - crates/trackly-app/tests/org_settings.rs
  - crates/trackly-app/tests/backup_service.rs
  - crates/trackly-app/tests/supervisor.rs
  - crates/trackly-app/tests/template_edit.rs
autonomous: true
requirements:
  - SET-01
  - SET-02
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06
  - RPT-07
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - SET-03
  - SET-04
  - SET-05
  - SET-06
  - SET-07
  - SET-09

must_haves:
  truths:
    - "V026 миграция org_settings создаёт таблицу с CHECK (id = 1) и seed-строкой"
    - "Все DTO (ReportFilter, ReportRow, DashboardWidgetDto, ConsumptionPoint, OrgPatch, BackupConfigPatch) компилируются с serde + specta"
    - "9 тест-скаффолдов существуют (Red), описывают контракт поведения и падают с TODO"
  artifacts:
    - path: "migrations/V026__org_settings.sql"
      provides: "org_settings table + logo_blob BLOB + seed row; PRAGMA user_version = 26"
      contains: "CREATE TABLE org_settings"
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "ReportFilter, ReportRow, DashboardWidgetDto, ConsumptionPoint, OrgPatch structs"
      exports: ["ReportFilter", "ReportRow", "DashboardWidgetDto", "ConsumptionPoint"]
    - path: "crates/trackly-app/tests/report_acts.rs"
      provides: "RED test scaffold for RPT-01/04/05"
  key_links:
    - from: "migrations/V026__org_settings.sql"
      to: "trackly-infra migrations embed"
      via: "embed_migrations! macro — refinery picks up V026 by filename order"
      pattern: "PRAGMA user_version = 26"
    - from: "crates/trackly-app/src/dto/reports.rs"
      to: "crates/trackly-app/src/dto/mod.rs"
      via: "pub mod reports; pub use reports::*"
---

<objective>
Wave 1 foundation: V026 org_settings migration, all Phase 7 DTOs (reports, dashboard, org, backup), and 9 RED integration test scaffolds. This plan creates the data contract that all subsequent plans build against — executors for plans 02–07 will read these DTOs as their interface definition rather than inventing their own.

Purpose: Establish typed contracts (DTO structs + migration schema) before any service or UI code. Integration test scaffolds define WHAT must work; they fail today and pass after the implementing plan executes.

Output: migrations/V026 + dto/reports.rs + 9 test stub files in crates/trackly-app/tests/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reports-dashboard-settings/07-CONTEXT.md
@.planning/phases/07-reports-dashboard-settings/07-RESEARCH.md
@.planning/phases/07-reports-dashboard-settings/07-PATTERNS.md
</context>

<interfaces>
<!-- Key patterns executor MUST replicate. Verified in live codebase. -->

From crates/trackly-app/src/dto/device.rs (DTO pattern):
```rust
#[derive(Debug, Clone, Serialize, Deserialize, specta::Type)]
#[serde(rename_all = "camelCase")]
pub struct DeviceDto { ... }
```

From migrations/V016__cartridges_kind_color_settings.sql (migration pattern):
```sql
CREATE TABLE app_settings (
  key TEXT PRIMARY KEY, value TEXT NOT NULL,
  created_at_utc INTEGER NOT NULL, updated_at_utc INTEGER NOT NULL
);
PRAGMA user_version = 16;
```

From crates/trackly-app/tests/cartridges_low_stock.rs (integration test pattern):
```rust
use trackly_infra::test_support::test_writer_and_readers;
#[tokio::test(flavor = "multi_thread", worker_threads = 4)]
async fn test_name() {
    tokio::time::timeout(Duration::from_secs(30), async { ... }).await.expect("budget")
}
```

From crates/trackly-core/src/domain/cartridges.rs:176 (VERIFIED — audit_log action string):
  CartridgeTransitionOp::Install { .. } => "custom:install"
  CartridgeTransitionOp::ToRefill { .. } => "custom:to_refill"
  CartridgeTransitionOp::FromRefill { .. } => "custom:from_refill"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V026 migration + org_settings table</name>
  <files>migrations/V026__org_settings.sql</files>
  <read_first>
    - migrations/V025__cartridge_printer_link.sql (previous migration — confirm user_version = 25 and table pattern)
    - migrations/V016__cartridges_kind_color_settings.sql (seed pattern to follow)
    - .planning/phases/07-reports-dashboard-settings/07-RESEARCH.md §Pattern 5
  </read_first>
  <behavior>
    - Migration creates table org_settings with columns: id INTEGER PRIMARY KEY CHECK (id = 1), org_name TEXT NOT NULL DEFAULT 'Ваша организация', inn TEXT NOT NULL DEFAULT '0000000000', kpp TEXT NOT NULL DEFAULT '000000000', address TEXT NOT NULL DEFAULT 'Адрес не указан', logo_blob BLOB NULL, logo_mime TEXT NULL, created_at_utc INTEGER NOT NULL, updated_at_utc INTEGER NOT NULL, version INTEGER NOT NULL DEFAULT 1
    - Migration inserts seed row with id=1, placeholder values, created_at_utc=unixepoch(), updated_at_utc=unixepoch()
    - Migration sets PRAGMA user_version = 26
    - test_db() fixture (from trackly-infra) runs all migrations including V026 and schema_version becomes 26
  </behavior>
  <action>
    Create migrations/V026__org_settings.sql with the org_settings table definition per RESEARCH Pattern 5. Single-row table enforced by CHECK (id = 1). Seed row uses unixepoch() for timestamps. Set PRAGMA user_version = 26.

    After creating the migration, verify that the trackly-infra test fixture picks it up: the migration filename must follow the V{NNN}__{snake_case}.sql convention that refinery 0.9 embed_migrations! uses. Do not renumber or rename existing migrations.

    Acceptance: `cargo test -p trackly-infra -- --test-thread=1 2>&1 | grep -E "schema_version|migration"` shows V026 applied; the existing `downgrade_protection.rs` test still passes (it checks schema_version increments sequentially).
  </action>
  <verify>
    <automated>cargo test -p trackly-infra --test downgrade_protection -- --nocapture 2>&1 | tail -5</automated>
  </verify>
  <done>
    V026__org_settings.sql exists. Running `cargo test -p trackly-infra --test downgrade_protection` passes. The migration file contains "CREATE TABLE org_settings" and "PRAGMA user_version = 26".
  </done>
</task>

<task type="auto">
  <name>Task 2: Phase 7 DTOs + test scaffolds (9 RED files)</name>
  <files>
    crates/trackly-app/src/dto/reports.rs,
    crates/trackly-app/src/dto/mod.rs,
    crates/trackly-app/tests/report_acts.rs,
    crates/trackly-app/tests/report_cartridges.rs,
    crates/trackly-app/tests/report_period_bounds.rs,
    crates/trackly-app/tests/report_csv_export.rs,
    crates/trackly-app/tests/dashboard_widgets.rs,
    crates/trackly-app/tests/org_settings.rs,
    crates/trackly-app/tests/backup_service.rs,
    crates/trackly-app/tests/supervisor.rs,
    crates/trackly-app/tests/template_edit.rs
  </files>
  <read_first>
    - crates/trackly-app/src/dto/mod.rs (existing — to understand pub mod list before extending)
    - crates/trackly-app/src/dto/device.rs (DTO pattern: serde + specta::Type + camelCase)
    - crates/trackly-app/tests/cartridges_low_stock.rs (integration test pattern to clone)
    - crates/trackly-app/tests/cartridges_history.rs (see how audit_log action strings are asserted — "custom:install" not "install")
    - crates/trackly-core/src/domain/printers.rs (PrinterCounts and PrinterAlertRow structs for DASH-05 DTO)
    - crates/trackly-core/src/domain/requests.rs (RequestCounts struct for DASH-04 DTO)
    - crates/trackly-infra/src/repos/cartridges_sqlite.rs lines 678-724 (CartridgeCounts + LowStockItem for DASH-02 DTO)
  </read_first>
  <action>
    Create crates/trackly-app/src/dto/reports.rs with these public structs (all derive Debug, Clone, Serialize, Deserialize, specta::Type with serde rename_all = "camelCase"):

    - ReportFilter: date_from_utc: Option<i64>, date_to_utc: Option<i64>, location_id: Option<i64>, status_id: Option<i64>, type_id: Option<i64>, act_type: Option<String>, model_id: Option<i64>, color: Option<String>, search: Option<String>
      -- type_id: device-type filter for device domain reports (per D-04: Устройства → тип)
      -- color: cartridge color filter for cartridge domain reports (per D-04: Картриджи → цвет)
      -- status_id: cartridge status filter (per D-04: Картриджи → статус) + device status (Устройства → статус)
    - ReportRow: id: i64, month_key: Option<String>, number: Option<String>, sub_number: Option<String>, giver_name: Option<String>, receiver_name: Option<String>, handover_date_utc: Option<i64>, location_name: Option<String>, act_type: Option<String>, device_name: Option<String>, quantity: Option<i64>, code: Option<String>, model_label: Option<String>, status_name: Option<String>
    - ReportResponse: rows: Vec<ReportRow>, total: i64
    - ConsumptionPoint: month_key: String, model_label: String, installs: i64
    - StatusCount: status_name: String, count: i64
    - DashboardWidgetDto: devices_total: i64, devices_by_status: Vec<StatusCount>, cartridge_by_status: Vec<StatusCount>, low_stock_count: i64, low_stock_models: Vec<String>, request_counts_open: i64, request_counts_in_progress: i64, request_counts_completed: i64, printer_online: i64, printer_offline: i64, printer_problematic: i64
    - OrgPatch: org_name: String, inn: String, kpp: String, address: String
    - OrgLogoDto: logo_bytes: Option<Vec<u8>>, logo_mime: Option<String> (logo_bytes is serde skip when None; for Tauri the bytes are transmitted as Vec<u8>)
    - OrgSettingsDto: org_name: String, inn: String, kpp: String, address: String, has_logo: bool
    - BackupConfigPatch: backup_folder: Option<String>, schedule: Option<String>, retention: Option<i64>
    - PeriodDto: mode: String (values: "month" | "year" | "range"), year: Option<i32>, month: Option<u8>, date_from: Option<String> (ISO date "2026-06-01"), date_to: Option<String>

    Add "pub mod reports;" to crates/trackly-app/src/dto/mod.rs and "pub use reports::*;" (or specific re-exports matching the existing pattern in that file).

    Create the 9 test stub files. Each is a minimal compilable Rust file with a TODO comment and a compile-only test that asserts the needed service/function exists via a type assertion or calls todo!(). Pattern to follow: cartridges_low_stock.rs. Each file must compile (cargo check passes) even without the service implementations — use dead_code allows or #[allow(unused)] on the test function body where needed. The test body calls `todo!("implement in plan 02/03")` which causes the test to fail at runtime (RED state) but compile. Required stubs:

    1. report_acts.rs — #[tokio::test] async fn report_acts_filtered_by_period() { todo!("impl plan 03") }
    2. report_cartridges.rs — consumption events grouped by month, action = 'custom:install'
    3. report_period_bounds.rs — UTC period math for Europe/Moscow UTC+3
    4. report_csv_export.rs — UTF-8 BOM + semicolon delimiter
    5. dashboard_widgets.rs — device/cartridge/request/printer widget counts
    6. org_settings.rs — org save/load round-trip + logo BLOB
    7. backup_service.rs — manual backup + integrity_check + UNC rejection
    8. supervisor.rs — catch-up semantics (overdue task fires on startup)
    9. template_edit.rs — template update + validation + reset_to_default

    Important: each test stub MUST include a comment at the top: "// audit_log.action for cartridge install = 'custom:install' (verified in CartridgeTransitionOp::audit_action)"
    This annotation ensures that plan-03 executor uses the correct action string in the consumption SQL query.
  </action>
  <verify>
    <automated>cargo check -p trackly-app 2>&1 | grep -E "error|warning" | head -20</automated>
  </verify>
  <done>
    crates/trackly-app/src/dto/reports.rs exists with all 10 structs. All 9 test stub files exist under crates/trackly-app/tests/. `cargo check -p trackly-app` produces zero errors. Each stub file compiles but the test functions call todo!() (RED).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL | Internal — written by executor, not user input; no injection risk in migration files |
| DTO structs | Internal type definitions — no trust boundary at this plan level; all user input validation deferred to implementing plans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-a | Tampering | migrations/V026 PRAGMA user_version | mitigate | user_version must equal 26 (sequential); downgrade_protection test catches drift |
| T-07-01-b | Tampering | org_settings CHECK (id = 1) | mitigate | DB-level constraint prevents multi-row insertion; single-row invariant maintained at schema level |
| T-07-01-SC | Tampering | npm/pip/cargo installs | accept | Zero new package installs in this plan; slopcheck not required |
</threat_model>

<verification>
- `cargo check -p trackly-app` exits 0 (no errors in DTO or test stubs)
- `cargo test -p trackly-infra --test downgrade_protection -- --nocapture` passes (migration V026 applied cleanly)
- `grep -c "CREATE TABLE org_settings" migrations/V026__org_settings.sql` returns 1
- `grep -c "PRAGMA user_version = 26" migrations/V026__org_settings.sql` returns 1
- `grep -c "logo_blob" migrations/V026__org_settings.sql` returns 1
- `ls crates/trackly-app/tests/report_acts.rs crates/trackly-app/tests/org_settings.rs crates/trackly-app/tests/backup_service.rs crates/trackly-app/tests/supervisor.rs` all exist
- All 9 stub test files contain the comment "// audit_log.action for cartridge install = 'custom:install'"
</verification>

<success_criteria>
- V026__org_settings.sql creates org_settings table with BLOB logo column, single-row CHECK, seed row
- crates/trackly-app/src/dto/reports.rs exports ReportFilter, ReportRow, ReportResponse, ConsumptionPoint, DashboardWidgetDto, OrgPatch, OrgSettingsDto, OrgLogoDto, BackupConfigPatch, PeriodDto
- 9 RED integration test stubs exist and compile but fail at runtime (todo!())
- downgrade_protection test passes confirming V026 fits schema version sequence
- cargo check -p trackly-app is clean
</success_criteria>

<output>
Create .planning/phases/07-reports-dashboard-settings/07-01-SUMMARY.md when done
</output>
