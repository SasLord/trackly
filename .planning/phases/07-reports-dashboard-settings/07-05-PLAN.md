---
phase: 07-reports-dashboard-settings
plan: 05
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - ui/src/pages/Dashboard.svelte
  - ui/src/features/dashboard/DashboardPage.svelte
  - ui/src/features/dashboard/StatWidget.svelte
  - ui/src/features/dashboard/ChartWidget.svelte
  - ui/src/features/dashboard/PeriodToggle.svelte
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "Dashboard is the home page (route '/') — DashboardPage replaces placeholder"
    - "5 widgets load in parallel on mount; each has independent loading/error/populated states"
    - "StatWidget «Устройства» shows total count + breakdown by status (DASH-01)"
    - "StatWidget «Картриджи» shows counts by status + low-stock warning in --color-warning (DASH-02)"
    - "ChartWidget «Динамика расхода» shows SVG polyline for 3/6/12 month window; no external chart library (DASH-03)"
    - "StatWidget «Заявки» shows open/in_progress/completed counts (DASH-04)"
    - "StatWidget «Принтеры» shows online/offline counts; проблемные count uses Badge variant='destructive' (DASH-05)"
    - "Dashboard has period selector (compact button group) for period-sensitive widgets (D-12)"
    - "SVG chart has role='img' aria-label and a visually-hidden data table (accessibility)"
    - "No new npm dependencies"
  artifacts:
    - path: "ui/src/features/dashboard/DashboardPage.svelte"
      provides: "5-widget fixed responsive grid; parallel widget loading"
      contains: "dashboard_get_all_widgets"
    - path: "ui/src/features/dashboard/ChartWidget.svelte"
      provides: "SVG polyline chart; 3/6/12 month toggle; hand-drawn, zero npm deps"
      contains: "dashboard_get_consumption_chart"
    - path: "ui/src/features/dashboard/StatWidget.svelte"
      provides: "Generic stat card (number + label + breakdown)"
      contains: "stat-widget"
  key_links:
    - from: "ui/src/features/dashboard/DashboardPage.svelte"
      to: "dashboard_get_all_widgets Tauri command (plan 07)"
      via: "apiCall<DashboardWidgetDto>('dashboard_get_all_widgets', { period })"
      pattern: "dashboard_get_all_widgets"
    - from: "ui/src/features/dashboard/ChartWidget.svelte"
      to: "dashboard_get_consumption_chart Tauri command (plan 07)"
      via: "apiCall<ConsumptionPoint[]>('dashboard_get_consumption_chart', { windowMonths })"
      pattern: "dashboard_get_consumption_chart"
---

<objective>
Dashboard UI: DashboardPage replaces the placeholder component at route '/'. Implements 5 read-only widgets in a fixed 2-column responsive grid. ChartWidget uses hand-drawn SVG polyline — zero new npm dependencies. Each widget loads independently so a slow query doesn't block the entire page.

Purpose: Complete the dashboard vertical slice — admin can see at a glance device/cartridge/request/printer status and consumption dynamics.

Output: DashboardPage + StatWidget + ChartWidget + PeriodToggle + updated Dashboard.svelte stub
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-reports-dashboard-settings/07-UI-SPEC.md
@.planning/phases/07-reports-dashboard-settings/07-PATTERNS.md
@.planning/phases/07-reports-dashboard-settings/07-03-SUMMARY.md
</context>

<interfaces>
<!-- Existing patterns executor MUST replicate -->

From ui/src/pages/Dashboard.svelte (current placeholder — read to understand current structure before replacing):
  Likely: import Placeholder from '...'; <Placeholder title="Дашборд" />

From ui/src/features/cartridges/LowStockBanner.svelte (warning alert card pattern):
  background: color-mix(in srgb, var(--color-warning) 10%, transparent)
  border: 1px solid var(--color-warning)

From ui/src/features/acts/ActsList.svelte (loading/error/empty state pattern):
  {#if loading} <Spinner> {:else if error} <error msg> {:else if items.length === 0} <empty state> {:else} content {/if}

From ui/src/lib/components/Badge.svelte (badge variants):
  <Badge variant="destructive"> for проблемные printer count
  <Badge variant="warning"> for low-stock

From PATTERNS.md §ChartWidget SVG polyline (hand-drawn, no deps):
  function toPoints(series: number[], maxVal: number): string { ... }
  <svg role="img" aria-label="..." viewBox="0 0 400 200" preserveAspectRatio="none">
    <polyline points={toPoints(...)} fill="none" stroke="var(--color-accent)" stroke-width="2" />
  </svg>
  <table class="sr-only" aria-label="Данные графика"> ... </table>

Month names array (Russian, hardcoded):
  const MONTHS = ['Январь','Февраль','Март','Апрель','Май','Июнь','Июль','Август','Сентябрь','Октябрь','Ноябрь','Декабрь']
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: StatWidget + PeriodToggle + ChartWidget components</name>
  <files>
    ui/src/features/dashboard/StatWidget.svelte,
    ui/src/features/dashboard/PeriodToggle.svelte,
    ui/src/features/dashboard/ChartWidget.svelte
  </files>
  <read_first>
    - ui/src/features/cartridges/LowStockBanner.svelte (warning card pattern to replicate in stat widget low-stock alert)
    - ui/src/features/cartridges/CartridgeFilters.svelte lines 119-155 (status-bar button group — basis for PeriodToggle)
    - ui/src/lib/components/Spinner.svelte (import path and usage)
    - ui/src/lib/components/Badge.svelte (props: variant, size — check existing interface)
    - .planning/phases/07-reports-dashboard-settings/07-UI-SPEC.md §Dashboard Layout §DASH-02 and §DASH-05
    - .planning/phases/07-reports-dashboard-settings/07-PATTERNS.md §StatWidget and §ChartWidget patterns
  </read_first>
  <action>
    Create ui/src/features/dashboard/StatWidget.svelte:

    Props ($props): id: string, title: string, mainNumber: number | null, mainLabel: string, breakdown: Array<{label: string, count: number}>, loading: boolean, error: string | null, warningItems: Array<string> (optional, for low-stock alert in DASH-02).

    Markup: <section class="stat-widget" aria-labelledby="widget-title-{id}">
      <h2 class="widget-title" id="widget-title-{id}">{title}</h2>
      {#if loading} <div class="widget-loading"><Spinner size="sm" /></div>
      {:else if error} <div class="widget-error">Ошибка загрузки</div>
      {:else}
        <p class="stat-number">{mainNumber ?? '—'}</p>
        <p class="stat-label">{mainLabel}</p>
        {#if breakdown.length > 0}
          <ul class="breakdown-list">
            {#each breakdown as row}<li>{row.label}: {row.count}</li>{/each}
          </ul>
        {/if}
        {#if warningItems && warningItems.length > 0}
          <div class="widget-warning">
            <span>Низкий остаток:</span>
            <ul>{#each warningItems as m}<li>{m}</li>{/each}</ul>
          </div>
        {/if}
      {/if}
    </section>

    SCSS: .stat-widget { background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: var(--space-lg); min-height: 120px; }
    .stat-number { font-size: var(--font-size-display); font-weight: var(--font-weight-semibold); margin: 0; }
    .stat-label { font-size: var(--font-size-label); color: var(--color-text-secondary); margin: var(--space-xs) 0 0; }
    .breakdown-list { list-style: none; padding: 0; margin: var(--space-sm) 0 0; font-size: var(--font-size-label); }
    .widget-loading, .widget-error { display: flex; align-items: center; justify-content: center; height: 80px; }
    .widget-error { color: var(--color-text-muted); font-size: var(--font-size-label); }
    .widget-warning { margin-top: var(--space-sm); padding: var(--space-xs) var(--space-sm); background: color-mix(in srgb, var(--color-warning) 10%, transparent); border: 1px solid var(--color-warning); border-radius: var(--radius-sm); font-size: var(--font-size-label); }
    .widget-warning ul { list-style: disc; margin: var(--space-xs) 0 0 var(--space-md); padding: 0; }

    Create ui/src/features/dashboard/PeriodToggle.svelte:

    Props: windowMonths: 3 | 6 | 12, onWindowChange: (months: 3 | 6 | 12) => void.
    Renders button group: {#each ([3, 6, 12] as const) as m}
      <button class="toggle-btn" class:active={windowMonths === m} onclick={() => onWindowChange(m)}>{m} мес.</button>
    {/each}
    SCSS: .toggle-btn same pattern as CartridgeFilters.svelte status-bar tabs (border-bottom: 2px solid transparent; .active: border-bottom-color: var(--color-accent); color: var(--color-accent)).

    Create ui/src/features/dashboard/ChartWidget.svelte:

    Props: data: Array<{month_key: string, model_label: string, installs: number}>, windowMonths: 3 | 6 | 12, loading: boolean, error: string | null, onWindowChange: (months: 3 | 6 | 12) => void.

    Derived: group data by model_label into series map; compute maxVal = max installs across all series; compute unique months (sorted); compute per-model series of counts aligned to months (fill 0 for missing months).

    Helper function toPoints(series: number[], maxVal: number): string — maps series to SVG coordinates in viewBox 0 0 400 200 (x: 10 to 390 spread over series.length-1 points; y: 10 to 190 inverted by count/maxVal). Guard: if series.length < 2, return empty string.

    Assign colors: first model gets --color-accent; additional models get hardcoded CSS custom-property strings (--color-success, --color-warning) up to 3 models. Beyond 3 models, collapse extras (acceptable in v1).

    Markup: <section class="chart-widget" aria-labelledby="chart-title">
      <div class="chart-header">
        <h2 class="widget-title" id="chart-title">Динамика расхода картриджей</h2>
        <PeriodToggle {windowMonths} {onWindowChange} />
      </div>
      {#if loading} <div class="chart-state"><Spinner /></div>
      {:else if error} <div class="chart-state chart-error">Ошибка загрузки</div>
      {:else if data.length === 0} <div class="chart-state chart-empty">Нет данных о расходе за выбранный период</div>
      {:else}
        <svg role="img" aria-label="График динамики расхода картриджей за {windowMonths} месяцев"
             viewBox="0 0 400 200" preserveAspectRatio="xMidYMid meet" class="chart-svg">
          {#each modelKeys as model, mi}
            <polyline points={toPoints(seriesData[model], maxVal)}
              fill="none" stroke={COLORS[mi % COLORS.length]} stroke-width="2" />
          {/each}
          <!-- X-axis month labels -->
          {#each uniqueMonths as month, i}
            <text x={i/(uniqueMonths.length-1||1)*380+10} y="198" font-size="9" text-anchor="middle" fill="var(--color-text-muted)">
              {month.slice(5)}
            </text>
          {/each}
        </svg>
        <!-- Visually-hidden data table for accessibility -->
        <table class="sr-only" aria-label="Данные графика расхода картриджей">
          <thead><tr><th>Месяц</th>{#each modelKeys as m}<th>{m}</th>{/each}</tr></thead>
          <tbody>
            {#each uniqueMonths as month}
              <tr><td>{month}</td>{#each modelKeys as m}<td>{seriesData[m]?.[uniqueMonths.indexOf(month)] ?? 0}</td>{/each}</tr>
            {/each}
          </tbody>
        </table>
      {/if}
    </section>

    SCSS: .chart-widget { background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: var(--space-lg); min-height: 220px; }
    .chart-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: var(--space-md); }
    .chart-svg { width: 100%; height: 180px; }
    .chart-state { display: flex; align-items: center; justify-content: center; height: 150px; color: var(--color-text-muted); font-size: var(--font-size-label); }
    .sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; }
  </action>
  <verify>
    <automated>pnpm svelte-check --tsconfig ui/tsconfig.json 2>&1 | grep -E "Error|error" | head -20</automated>
  </verify>
  <done>
    StatWidget.svelte, PeriodToggle.svelte, ChartWidget.svelte exist in ui/src/features/dashboard/. pnpm svelte-check exits 0. ChartWidget contains no import of any chart library npm package (grep -r "chart.js\|recharts\|d3\|apexcharts" ui/src/features/dashboard/ returns 0 matches). SVG has role="img" aria-label attribute. sr-only table exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: DashboardPage assembly + Dashboard.svelte stub update</name>
  <files>
    ui/src/features/dashboard/DashboardPage.svelte,
    ui/src/pages/Dashboard.svelte
  </files>
  <read_first>
    - ui/src/pages/Dashboard.svelte (current file — read to understand placeholder before replacing)
    - ui/src/features/requests/RequestsPage.svelte (layout pattern for page wrapper + grid)
    - ui/src/features/devices/DevicesPage.svelte (onMount parallel load + loading state pattern)
    - .planning/phases/07-reports-dashboard-settings/07-UI-SPEC.md §Dashboard Layout (2-column responsive grid, breakpoint 1280px)
  </read_first>
  <action>
    Create ui/src/features/dashboard/DashboardPage.svelte:

    State: let widgetData = $state<DashboardWidgetDto | null>(null); let chartData = $state<ConsumptionPoint[]>([]); let widgetError = $state<string|null>(null); let chartError = $state<string|null>(null); let widgetsLoading = $state(true); let chartLoading = $state(true); let windowMonths = $state<3|6|12>(3); let periodYear = $state(new Date().getFullYear()); let periodMonth = $state(new Date().getMonth() + 1).

    On mount ($effect runs once): load widgets and chart in parallel (two independent apiCall invocations, not awaited together — let them proceed independently to support per-widget error states):
    - apiCall<DashboardWidgetDto>('dashboard_get_all_widgets', { period: { mode: 'month', year: periodYear, month: periodMonth } }).then(d => { widgetData = d; widgetsLoading = false; }).catch(e => { widgetError = String(e); widgetsLoading = false; });
    - loadChart() called immediately too.

    loadChart(): chartLoading = true; apiCall<ConsumptionPoint[]>('dashboard_get_consumption_chart', { windowMonths }).then(d => { chartData = d; chartLoading = false; }).catch(e => { chartError = String(e); chartLoading = false; });

    $effect(() => { windowMonths; loadChart(); }); — reloads chart when toggle changes.

    Page markup:
    <div class="dashboard-page">
      <header class="page-header">
        <h1 class="page-title">Дашборд</h1>
        <!-- period selector for period-sensitive widgets (D-12) -->
        <div class="period-selector" role="group" aria-label="Период">
          <select bind:value={periodMonth} onchange={reloadWidgets}>
            {#each MONTHS as name, i}<option value={i+1}>{name}</option>{/each}
          </select>
          <select bind:value={periodYear} onchange={reloadWidgets}>
            {#each YEARS as y}<option value={y}>{y}</option>{/each}
          </select>
        </div>
      </header>
      <div class="dashboard-grid">
        <!-- Column 1: Devices, Cartridges, Chart -->
        <div class="dashboard-col dashboard-col--main">
          <StatWidget id="devices" title="Устройства"
            mainNumber={widgetData?.devices_total ?? null}
            mainLabel="устройств в базе"
            breakdown={widgetData?.devices_by_status.map(s => ({label: s.status_name, count: s.count})) ?? []}
            loading={widgetsLoading} error={widgetData === null && !widgetsLoading ? widgetError : null} />

          <StatWidget id="cartridges" title="Картриджи"
            mainNumber={widgetData ? widgetData.cartridge_by_status.reduce((a,s)=>a+s.count,0) : null}
            mainLabel="картриджей"
            breakdown={widgetData?.cartridge_by_status.map(s => ({label: s.status_name, count: s.count})) ?? []}
            warningItems={widgetData?.low_stock_models ?? []}
            loading={widgetsLoading} error={widgetData === null && !widgetsLoading ? widgetError : null} />

          <ChartWidget data={chartData} {windowMonths} loading={chartLoading} error={chartError}
            onWindowChange={(m) => windowMonths = m} />
        </div>

        <!-- Column 2: Requests, Printers -->
        <div class="dashboard-col dashboard-col--side">
          <StatWidget id="requests" title="Заявки"
            mainNumber={widgetData?.request_counts_open ?? null}
            mainLabel="активных"
            breakdown={widgetData ? [
              { label: 'Новые', count: widgetData.request_counts_open },
              { label: 'В работе', count: widgetData.request_counts_in_progress },
              { label: 'Выполнены', count: widgetData.request_counts_completed },
            ] : []}
            loading={widgetsLoading} error={null} />

          <StatWidget id="printers" title="Принтеры"
            mainNumber={widgetData?.printer_online ?? null}
            mainLabel="онлайн"
            breakdown={widgetData ? [
              { label: 'Онлайн', count: widgetData.printer_online },
              { label: 'Офлайн', count: widgetData.printer_offline },
              { label: 'Проблемные', count: widgetData.printer_problematic },
            ] : []}
            loading={widgetsLoading} error={null} />
        </div>
      </div>
    </div>

    SCSS:
    .dashboard-page { display: flex; flex-direction: column; height: 100%; }
    .page-header { padding: var(--space-lg) var(--space-xl); border-bottom: 1px solid var(--color-border); flex-shrink: 0; display: flex; align-items: center; gap: var(--space-md); }
    .page-title { margin: 0; font-size: var(--font-size-heading); font-weight: var(--font-weight-semibold); flex: 1; }
    .period-selector { display: flex; gap: var(--space-sm); }
    .dashboard-grid { flex: 1; overflow: auto; display: grid; grid-template-columns: 3fr 2fr; gap: var(--space-xl); padding: var(--space-xl); }
    .dashboard-col { display: flex; flex-direction: column; gap: var(--space-lg); }
    @media (max-width: 1280px) { .dashboard-grid { grid-template-columns: 1fr; } }

    Update ui/src/pages/Dashboard.svelte: replace placeholder content with <script lang="ts"> import DashboardPage from '../features/dashboard/DashboardPage.svelte'; </script> <DashboardPage />.
  </action>
  <verify>
    <automated>pnpm svelte-check --tsconfig ui/tsconfig.json 2>&1 | grep -E "Error|error" | head -20</automated>
  </verify>
  <done>
    DashboardPage.svelte exists. Dashboard.svelte imports and renders DashboardPage. pnpm svelte-check exits 0. DashboardPage imports 'dashboard_get_all_widgets' and 'dashboard_get_consumption_chart'. 2-column grid uses CSS grid-template-columns: 3fr 2fr with 1280px media query breakpoint. No chart library imported (grep confirms).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard widget data | Server-side aggregated counts; read-only in UI; no user mutation from this page |
| Chart data | Server-returned ConsumptionPoint[]; rendered as SVG points — no innerHTML injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | ChartWidget SVG injection | mitigate | All SVG content is computed from numeric data (toPoints generates coordinate strings from numbers); no unescaped string interpolation into SVG; Svelte templates auto-escape text nodes |
| T-07-05-02 | DoS | Dashboard parallel API calls | accept | 2 parallel calls on mount (widgets + chart); widgets loading independently avoids cascade failure; widget error state shown per widget |
| T-07-05-03 | Spoofing | Widget data manipulation | accept | Widgets are read-only display; no mutations from this page; auth is enforced on the backend handlers (plan 07) |
| T-07-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new npm packages; hand-drawn SVG avoids chart.js/d3 dependency |
</threat_model>

<verification>
- `pnpm svelte-check --tsconfig ui/tsconfig.json` exits 0
- `ls ui/src/features/dashboard/` shows DashboardPage.svelte, StatWidget.svelte, ChartWidget.svelte, PeriodToggle.svelte
- `grep -c "role=\"img\"" ui/src/features/dashboard/ChartWidget.svelte` returns 1 (accessibility)
- `grep -c "sr-only" ui/src/features/dashboard/ChartWidget.svelte` returns >= 1 (hidden data table)
- `grep -c "chart.js\|recharts\|d3\|apexcharts" ui/src/features/dashboard/ChartWidget.svelte` returns 0 (no external chart library)
- `grep -c "dashboard_get_all_widgets" ui/src/features/dashboard/DashboardPage.svelte` returns >= 1
- `grep -c "dashboard_get_consumption_chart" ui/src/features/dashboard/DashboardPage.svelte` returns >= 1
- `grep -c "DashboardPage" ui/src/pages/Dashboard.svelte` returns >= 1 (stub updated)
- `grep -c "\$state" ui/src/features/dashboard/DashboardPage.svelte` returns >= 5 (Svelte 5 runes)
</verification>

<success_criteria>
- Dashboard route '/' shows all 5 widgets in 2-column responsive grid
- Each widget has independent loading/error/populated states
- ChartWidget draws SVG polyline from returned data; toggles 3/6/12 month window
- Zero new npm dependencies added
- Accessibility: SVG has role/aria-label; hidden data table backup; section aria-labelledby
- Svelte 5 runes throughout; all copy Russian
</success_criteria>

<output>
Create .planning/phases/07-reports-dashboard-settings/07-05-SUMMARY.md when done
</output>
