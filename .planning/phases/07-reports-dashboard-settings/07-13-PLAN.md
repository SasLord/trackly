---
phase: 07-reports-dashboard-settings
plan: 13
type: execute
wave: 2
depends_on:
  - "07-12"
files_modified:
  - ui/src/features/settings/OrgSettings.svelte
  - ui/src/features/settings/StorageSettings.svelte
  - ui/src/features/settings/BackupSettings.svelte
  - crates/trackly-app/capabilities/main.json
autonomous: true
requirements:
  - SET-02
  - SET-04
  - SET-05
gap_closure: true
gaps_addressed:
  - G2-1
  - G2-2
  - G2-3

must_haves:
  truths:
    - "G2-1: uploadLogo in OrgSettings detects desktop via '__TAURI_INTERNALS__' in window (not !!(window).__TAURI__), picks the file via plugin-dialog open(), reads it via plugin-fs readFile (dynamic import from @tauri-apps/plugin-fs), and capabilities/main.json contains fs:allow-read-file so the read succeeds; OrgSettings.svelte does NOT contain window.__TAURI__"
    - "G2-2: openFolder in StorageSettings calls apiCall<void>('settings_open_db_folder', {}) — no path argument — because the backend derives the DB folder from AppCtx internally"
    - "G2-3: pickFolder in BackupSettings calls apiCall<void>('settings_save_backup_config', { patch: { backup_folder: selected } }); saveConfig calls apiCall<void>('settings_save_backup_config', { patch: { schedule, retention } }) — snake_case backup_folder matches BackupConfigPatch field in bindings.ts"
    - "G2-3: all three arg-wrapping sites are consistent: no flat args remain"
  artifacts:
    - path: "ui/src/features/settings/OrgSettings.svelte"
      provides: "Correct Tauri2 detection + plugin-fs readFile in uploadLogo"
      contains: "__TAURI_INTERNALS__"
    - path: "crates/trackly-app/capabilities/main.json"
      provides: "fs:allow-read-file permission granted for plugin-fs readFile in desktop mode"
      contains: "fs:allow-read-file"
    - path: "ui/src/features/settings/StorageSettings.svelte"
      provides: "openFolder calls settings_open_db_folder with no args"
      contains: "settings_open_db_folder"
    - path: "ui/src/features/settings/BackupSettings.svelte"
      provides: "Both apiCall sites wrap args in { patch: { ... } } with snake_case fields"
      contains: "patch:"
  key_links:
    - from: "OrgSettings.svelte uploadLogo"
      to: "plugin-fs readFile + fs:allow-read-file capability"
      via: "dynamic import('@tauri-apps/plugin-fs').readFile(filePath)"
      pattern: "readFile"
    - from: "BackupSettings.svelte pickFolder"
      to: "settings_save_backup_config BackupConfigPatch"
      via: "{ patch: { backup_folder } }"
      pattern: "patch.*backup_folder"
---

<objective>
Fix three frontend argument/detection bugs in Settings components (G2-1, G2-2, G2-3).

Purpose: All three bugs share the same class of root cause — either a wrong Tauri API detection predicate (G2-1) or wrong arg-shape passed to Rust commands (G2-2 command name, G2-3 flat vs nested args). The fixes are isolated to three Svelte files plus one capability JSON file, with no additional backend changes required (backend prerequisite `settings_open_db_folder` was added in plan 07-12).

Output: Three corrected Svelte settings components + updated capabilities/main.json with fs:allow-read-file. No new commands — only existing command calls fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-reports-dashboard-settings/07-CONTEXT.md
@.planning/phases/07-reports-dashboard-settings/07-12-SUMMARY.md
</context>

<interfaces>
<!-- BackupConfigPatch (from crates/trackly-app/src/dto/reports.rs:220-229 + confirmed in ui/src/bindings.ts:1115-1127) -->
<!-- No #[serde(rename_all)] on BackupConfigPatch — specta emits snake_case field names exactly as declared. -->
BackupConfigPatch {
  backup_folder: string | null,   // snake_case — confirmed in bindings.ts line 1119
  schedule: string | null,
  retention: number | null,       // i64 in Rust, number in TS via #[specta(type = Option<i32>)]
}

// Tauri command signatures for these fixes:
// settings_save_backup_config(patch: BackupConfigPatch) → Result<(), AppError>
//   → call as: apiCall<void>('settings_save_backup_config', { patch: { backup_folder, schedule, retention } })
//
// settings_open_db_folder() → Result<(), AppError>  [new, from plan 07-12]
//   → Rust signature: settings_open_db_folder(app: tauri::AppHandle, state: tauri::State<'_, AppCtx>)
//   → No user-supplied path — backend derives folder from AppCtx.paths.db_path() internally.
//   → call as: apiCall<void>('settings_open_db_folder', {})
//
// settings_save_org_logo(logo_bytes: number[], logo_mime: string) → Result<(), AppError>
//   → These are individual flat params (not wrapped) — no patch wrapper on this command.

// Correct Tauri 2 desktop detection predicate:
// const isTauri = typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window;
// (window.__TAURI__ is undefined in Tauri 2 — old check always returns false)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix OrgSettings.svelte uploadLogo detection + file reading + grant fs:allow-read-file capability (G2-1)</name>
  <files>ui/src/features/settings/OrgSettings.svelte, crates/trackly-app/capabilities/main.json</files>

  <read_first>
    Current broken code in OrgSettings.svelte, function `uploadLogo` (lines 103-138):
    - Line 105: `const isTauri = typeof window !== 'undefined' && !!(window as unknown as Record<string, unknown>).__TAURI__;`
      → In Tauri 2, window.__TAURI__ is undefined. Detection always returns false. Logo picker never runs.
    - Lines 116-117: reads file with `const { readFile } = await import('@tauri-apps/plugin-fs'); const bytes = await readFile(filePath);`
      → No fs:allow-read-file capability in capabilities/main.json → permission denied at runtime.

    capabilities/main.json current state (confirmed by read):
    - Has: "core:default", "dialog:default", "fs:default", scoped "fs:allow-write-file" (PDF paths only).
    - Missing: "fs:allow-read-file" — the plugin-fs readFile call fails without it.

    Note: read_file_bytes command in fs_helpers.rs has a CSV extension check (line 151) — it CANNOT be used
    for image files. The correct fix is plugin-fs readFile with the capability grant.

    Analog for the correct detection predicate (any other Tauri file in the codebase):
    Search for '__TAURI_INTERNALS__' — that is the Tauri 2 documented property.
  </read_first>

  <action>
    Step 1 — Add fs:allow-read-file to crates/trackly-app/capabilities/main.json.
    Add the string "fs:allow-read-file" to the permissions array (no scope restriction needed —
    the readFile call is gated behind the '__TAURI_INTERNALS__' in window check, so only the
    desktop user can trigger it, and the file path came from a native OS dialog the user invoked).

    The permissions array after the edit should contain:
      "core:default",
      "dialog:default",
      "fs:default",
      "fs:allow-read-file",
      { "identifier": "fs:allow-write-file", "allow": [...] }

    Step 2 — Fix the detection predicate in uploadLogo (OrgSettings.svelte line 105).
    Change the broken line:
      `const isTauri = typeof window !== 'undefined' && !!(window as unknown as Record<string, unknown>).__TAURI__;`
    to:
      `const isTauri = typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window;`

    Step 3 — The file-reading on lines 116-117 uses plugin-fs readFile. With fs:allow-read-file now
    in the capability (Step 1), this call will succeed at runtime. Leave the readFile import and
    call as-is — it is already the correct pattern for image files. Ensure the result is converted
    to `Array.from(bytes)` (number[]) before passing to settings_save_org_logo if it is a Uint8Array.

    Step 4 — Confirm the import for @tauri-apps/plugin-fs remains (dynamic import inside function is fine).
    Do NOT switch to apiCall('read_file_bytes', ...) — that command rejects non-.csv extensions.

    Final state of uploadLogo:
    1. Uses `'__TAURI_INTERNALS__' in window` for detection (no window.__TAURI__ reference anywhere).
    2. Opens the file picker with plugin-dialog.
    3. Reads the picked file with plugin-fs readFile (now has capability).
    4. Converts bytes and calls apiCall<void>('settings_save_org_logo', { logo_bytes: Array.from(bytes), logo_mime: mime }).
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; pnpm --filter ui svelte-check 2>&amp;1 | tail -10 &amp;&amp; grep -c "__TAURI_INTERNALS__" ui/src/features/settings/OrgSettings.svelte &amp;&amp; grep -c "fs:allow-read-file" crates/trackly-app/capabilities/main.json</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter ui svelte-check` exits 0 (no TypeScript errors in OrgSettings.svelte).
    - `grep -c "__TAURI_INTERNALS__" ui/src/features/settings/OrgSettings.svelte` returns 1.
    - `grep -c "__TAURI__" ui/src/features/settings/OrgSettings.svelte` returns 0 (old check fully removed; OrgSettings.svelte must NOT contain the string `window.__TAURI__`).
    - `grep -c "fs:allow-read-file" crates/trackly-app/capabilities/main.json` returns 1.
    - `grep -c "read_file_bytes" ui/src/features/settings/OrgSettings.svelte` returns 0 (we do NOT use the csv-only command for images).
    - The uploadLogo Tauri path reads the image via plugin-fs readFile (not via read_file_bytes or any other backend command).
  </acceptance_criteria>

  <done>OrgSettings.svelte uploadLogo detects Tauri 2 correctly with '__TAURI_INTERNALS__' in window. Image reading uses plugin-fs readFile with the fs:allow-read-file capability grant in main.json. pnpm svelte-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix StorageSettings openFolder command name (G2-2 frontend) + fix BackupSettings arg wrapping (G2-3)</name>
  <files>ui/src/features/settings/StorageSettings.svelte, ui/src/features/settings/BackupSettings.svelte</files>

  <read_first>
    StorageSettings.svelte, function `openFolder` (line 29-38):
    - Current: `await apiCall<void>('fs_open_folder', { path: dbPath });`
    - Bug 1: command name `fs_open_folder` does not exist. New command from plan 07-12 is `settings_open_db_folder`.
    - Bug 2: the `path` argument must be removed. The backend command signature is:
        `settings_open_db_folder(app: tauri::AppHandle, state: tauri::State<'_, AppCtx>) → Result<(), AppError>`
      It derives the DB folder from AppCtx.paths.db_path() internally — no user-supplied path.
    - Fix: `await apiCall<void>('settings_open_db_folder', {})` — command renamed, args empty.

    BackupSettings.svelte, function `pickFolder` (lines 63-89):
    - Line 78: `await apiCall<void>('settings_save_backup_config', { backup_folder: selected });`
    - Bug: flat arg instead of nested patch wrapper. The Rust command is:
        `settings_save_backup_config(state, patch: BackupConfigPatch) → Result<(), AppError>`
      The outer param name is `patch`; BackupConfigPatch has field `backup_folder` (snake_case —
      confirmed in bindings.ts line 1119: `backup_folder: string | null`; no rename_all annotation).
    - Fix: `await apiCall<void>('settings_save_backup_config', { patch: { backup_folder: selected } });`

    BackupSettings.svelte, function `saveConfig` (lines 91-105):
    - Line 94: `await apiCall<void>('settings_save_backup_config', { schedule, retention });`
    - Bug: flat args without patch wrapper.
    - Fix: `await apiCall<void>('settings_save_backup_config', { patch: { schedule, retention } });`
      (backup_folder absent → BackupConfigPatch.backup_folder is Option<String> → None in Rust)

    Analog for correct nesting pattern:
    OrgSettings.svelte calls settings_save_org_fields as:
      `await apiCall<void>('settings_save_org_fields', { patch: { org_name: orgName, inn, kpp, address } })`
    Same wrapping pattern — outer `patch` key, inner struct fields.

    Audit of ALL settings apiCall sites (for G2-3 completeness):
    - OrgSettings.svelte: settings_get_org (no args), settings_save_org_fields (has patch: {...} ✓),
      settings_get_org_logo (no args), settings_save_org_logo (logo_bytes, logo_mime — flat individual
      params, correct for this command), settings_remove_org_logo (no args). No bugs.
    - StorageSettings.svelte: settings_get_db_path (no args), fs_open_folder (BROKEN → fix),
      settings_move_db (new_path: newPath — flat, correct for `settings_move_db(state, new_path: String)`),
      app_restart (no args). One bug.
    - BackupSettings.svelte: settings_get_backup_config (no args), settings_save_backup_config in
      pickFolder (BROKEN), settings_save_backup_config in saveConfig (BROKEN), backup_run_manual (no args).
      Two bugs.
    - ThresholdSettings.svelte: settings_get_low_stock_threshold (no args), settings_set_low_stock_threshold
      (threshold: N — flat, correct for `(state, threshold: i32)`). No bugs.
    - TemplateEditor.svelte: templates_list_for_editor (no args), templates_validate_preview (kind, body —
      flat individual params, correct), templates_update_body (kind, body — flat, correct),
      templates_reset_to_default (kind — flat, correct). No bugs.
  </read_first>

  <action>
    In ui/src/features/settings/StorageSettings.svelte:

    1. In function `openFolder` (around line 29-38): change the apiCall from:
         `await apiCall<void>('fs_open_folder', { path: dbPath });`
       to:
         `await apiCall<void>('settings_open_db_folder', {});`
       (No path argument — the backend derives the path from AppCtx internally. The `dbPath` state
       variable is not passed. The args object is empty `{}`.)

    In ui/src/features/settings/BackupSettings.svelte:

    2. In function `pickFolder` (around line 78): change the apiCall from:
         `await apiCall<void>('settings_save_backup_config', { backup_folder: selected });`
       to:
         `await apiCall<void>('settings_save_backup_config', { patch: { backup_folder: selected } });`
       (snake_case `backup_folder` — confirmed from bindings.ts; patch is the outer Rust param name)

    3. In function `saveConfig` (around line 94): change the apiCall from:
         `await apiCall<void>('settings_save_backup_config', { schedule, retention });`
       to:
         `await apiCall<void>('settings_save_backup_config', { patch: { schedule, retention } });`
       (Leave backup_folder absent — Option<String> fields absent from the object become None in Rust.)

    These are the only three broken apiCall sites identified by the audit in read_first. Make all
    three changes then run svelte-check.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; pnpm --filter ui svelte-check 2>&amp;1 | tail -10 &amp;&amp; grep -c "settings_open_db_folder" ui/src/features/settings/StorageSettings.svelte &amp;&amp; grep -c "fs_open_folder" ui/src/features/settings/StorageSettings.svelte &amp;&amp; grep -c "patch:" ui/src/features/settings/BackupSettings.svelte</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter ui svelte-check` exits 0.
    - `grep -c "fs_open_folder" ui/src/features/settings/StorageSettings.svelte` returns 0 (old name gone).
    - `grep -c "settings_open_db_folder" ui/src/features/settings/StorageSettings.svelte` returns 1.
    - `grep -c "path: dbPath" ui/src/features/settings/StorageSettings.svelte` returns 0 (path arg removed from openFolder).
    - `grep -c "backup_folder: selected" ui/src/features/settings/BackupSettings.svelte` returns 0 (bare flat arg gone; only the nested form remains).
    - `grep -c "patch: { backup_folder: selected" ui/src/features/settings/BackupSettings.svelte` returns 1 (snake_case backup_folder inside patch wrapper in pickFolder).
    - `grep -c "patch:" ui/src/features/settings/BackupSettings.svelte` returns 2 (one in pickFolder, one in saveConfig).
    - `grep -c "patch: { schedule" ui/src/features/settings/BackupSettings.svelte` returns 1.
    - No other settings apiCall sites (ThresholdSettings, TemplateEditor, OrgSettings) were modified (audit confirmed those are correct).
  </acceptance_criteria>

  <done>StorageSettings calls settings_open_db_folder with no args. BackupSettings wraps both save calls in { patch: { ... } } with snake_case field names matching BackupConfigPatch in bindings.ts. pnpm svelte-check passes. Runtime: "Открыть папку" and backup config save no longer error with "Не удалось связаться с приложением".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-fs readFile | File path comes from native dialog picker — user explicitly chose the file; OS dialog is the trust boundary. The `fs:allow-read-file` capability without scope restriction is acceptable because only desktop users can trigger this code path (gated behind `'__TAURI_INTERNALS__' in window`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13-01 | Information Disclosure | plugin-fs readFile image path | accept | File path provided by OS native picker; user controls which file to read; size is validated at backend (512 KB cap in org_db_service); no path traversal possible via dialog |
| T-07-13-02 | Spoofing | Tauri detection predicate fix | accept | `'__TAURI_INTERNALS__' in window` is the documented Tauri 2 reliable predicate; no security impact — it gates UX behavior, not auth |
</threat_model>

<verification>
After all tasks complete:
1. `pnpm --filter ui svelte-check` — 0 errors.
2. `grep "__TAURI__" ui/src/features/settings/OrgSettings.svelte` — no match (old check gone).
3. `grep "__TAURI_INTERNALS__" ui/src/features/settings/OrgSettings.svelte` — 1 match.
4. `grep -c "fs:allow-read-file" crates/trackly-app/capabilities/main.json` — 1 match.
5. `grep "fs_open_folder" ui/src/features/settings/StorageSettings.svelte` — no match.
6. `grep "settings_open_db_folder" ui/src/features/settings/StorageSettings.svelte` — 1 match.
7. `grep -c "patch: { backup_folder: selected" ui/src/features/settings/BackupSettings.svelte` — 1 match (snake_case).
8. `grep -c "patch:" ui/src/features/settings/BackupSettings.svelte` — 2 matches.
</verification>

<success_criteria>
- G2-1: OrgSettings.svelte uses '__TAURI_INTERNALS__' in window (not window.__TAURI__); logo file reading uses plugin-fs readFile with fs:allow-read-file capability in main.json; no more permission denied on logo upload in desktop mode.
- G2-2 (frontend): StorageSettings.svelte calls settings_open_db_folder (from plan 07-12) with no path arg.
- G2-3: Both settings_save_backup_config calls in BackupSettings.svelte wrap args in { patch: { ... } } with snake_case field names (backup_folder, not backupFolder) matching BackupConfigPatch as emitted by specta.
</success_criteria>

<output>
Create `.planning/phases/07-reports-dashboard-settings/07-13-SUMMARY.md` when done.
</output>
