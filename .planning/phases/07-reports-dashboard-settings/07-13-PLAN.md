---
phase: 07-reports-dashboard-settings
plan: 13
type: execute
wave: 2
depends_on:
  - "07-12"
files_modified:
  - ui/src/features/settings/OrgSettings.svelte
  - ui/src/features/settings/StorageSettings.svelte
  - ui/src/features/settings/BackupSettings.svelte
autonomous: true
requirements:
  - SET-02
  - SET-04
  - SET-05
gap_closure: true
gaps_addressed:
  - G2-1
  - G2-2
  - G2-3

must_haves:
  truths:
    - "G2-1: uploadLogo in OrgSettings uses '__TAURI_INTERNALS__' in window (not window.__TAURI__) and reads the file via apiCall<number[]>('read_file_bytes', { path: filePath }) instead of plugin-fs readFile"
    - "G2-2: openFolder in StorageSettings calls apiCall<void>('settings_open_db_folder', { path: dbPath }) (the new command from plan 07-12)"
    - "G2-3: pickFolder in BackupSettings calls apiCall<void>('settings_save_backup_config', { patch: { backupFolder: selected } }); saveConfig calls apiCall<void>('settings_save_backup_config', { patch: { schedule, retention } })"
    - "G2-3: all three arg-wrapping sites are consistent: no flat args remain"
  artifacts:
    - path: "ui/src/features/settings/OrgSettings.svelte"
      provides: "Correct Tauri2 detection + read_file_bytes usage in uploadLogo"
      contains: "__TAURI_INTERNALS__"
    - path: "ui/src/features/settings/StorageSettings.svelte"
      provides: "openFolder calls settings_open_db_folder"
      contains: "settings_open_db_folder"
    - path: "ui/src/features/settings/BackupSettings.svelte"
      provides: "Both apiCall sites wrap args in { patch: { ... } }"
      contains: "patch:"
  key_links:
    - from: "OrgSettings.svelte uploadLogo"
      to: "read_file_bytes Tauri command"
      via: "apiCall<number[]>('read_file_bytes', { path: filePath })"
      pattern: "read_file_bytes"
    - from: "BackupSettings.svelte pickFolder"
      to: "settings_save_backup_config BackupConfigPatch"
      via: "{ patch: { backupFolder } }"
      pattern: "patch.*backupFolder"
---

<objective>
Fix three frontend argument/detection bugs in Settings components (G2-1, G2-2, G2-3).

Purpose: All three bugs share the same class of root cause — either a wrong Tauri API detection predicate (G2-1) or wrong arg-shape passed to Rust commands (G2-2 command name, G2-3 flat vs nested args). The fixes are isolated to three Svelte files with no backend changes required for this plan (backend prerequisite `settings_open_db_folder` was added in plan 07-12).

Output: Three corrected Svelte settings components. No new commands — only existing command calls fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-reports-dashboard-settings/07-CONTEXT.md
@.planning/phases/07-reports-dashboard-settings/07-12-SUMMARY.md
</context>

<interfaces>
<!-- BackupConfigPatch (from crates/trackly-app/src/dto/reports.rs:220-229) -->
BackupConfigPatch {
  backup_folder: Option<String>,   // field name: camelCase in TS = backupFolder
  schedule: Option<String>,
  retention: Option<i64>,          // i32 in TypeScript bindings via specta(type = Option<i32>)
}

// Tauri command signatures for these fixes:
// settings_save_backup_config(patch: BackupConfigPatch) → Result<(), AppError>
// settings_open_db_folder(path: string) → Result<(), AppError>  [new, from plan 07-12]
// read_file_bytes(path: string) → Result<number[], AppError>    [existing fs_helpers]
// settings_save_org_logo(logo_bytes: number[], logo_mime: string) → Result<(), AppError>

// Correct Tauri 2 desktop detection predicate:
// const isTauri = typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window;
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix OrgSettings.svelte uploadLogo detection + file reading (G2-1)</name>
  <files>ui/src/features/settings/OrgSettings.svelte</files>

  <read_first>
    Current broken code in OrgSettings.svelte, function `uploadLogo` (lines 103-138):
    - Line 105: `const isTauri = typeof window !== 'undefined' && !!(window as unknown as Record<string, unknown>).__TAURI__;`
      → In Tauri 2, window.__TAURI__ is undefined. Detection always returns false. Logo picker never runs.
    - Lines 116-117: reads file with `const { readFile } = await import('@tauri-apps/plugin-fs'); const bytes = await readFile(filePath);`
      → No fs:allow-read-file capability is granted for arbitrary image paths → permission denied.

    Analog for read_file_bytes pattern: ui/src/features/devices/DeviceImportCsvModal.svelte, line 123:
    `const bytes = await apiCall<number[]>('read_file_bytes', { path: filePath });`
    → This is the correct pattern for reading arbitrary picked files.

    Note: read_file_bytes in fs_helpers.rs has a CSV extension check. For image files, a new backend helper is needed OR we adapt. Actually: read_file_bytes validates `.csv` extension (fs_helpers.rs line 151). We CANNOT use it for image files without bypassing that validation. Solution: OrgSettings already has the correct browser path using `file.arrayBuffer()` in `handleFileInput`. For the Tauri path, after picking via plugin-dialog, we need to read the bytes differently.

    Viable approach: The logo bytes are read by the Tauri FILE DIALOG callback. After `const selected = await open({...})`, the selected variable is a file path. Instead of reading via plugin-fs (no capability) or read_file_bytes (csv-only), we can use the `tauri-plugin-fs` `readFile` which would need the capability... but CONTEXT says that's not granted.

    Better approach: after selecting the file via plugin-dialog `open()`, use `fetch('asset://localhost/'+encodeURIComponent(filePath))` — Tauri 2 asset protocol. Actually the cleanest solution for this project: expose a new backend command `read_image_bytes` in settings_org.rs that has no extension restriction (only size cap + path canonicalize) and register it in specta_export.rs. But that would require ANOTHER backend change in this plan.

    ALTERNATIVE (preferred — no backend change): The logo picker can use the `@tauri-apps/plugin-fs` `readFile` IF the capability is added. Read crates/trackly-app/capabilities/main.json to check what's currently granted.

    ACTUAL PREFERRED FIX per gap hint: Use `apiCall<number[]>('read_file_bytes', { path: filePath })`. The gap hint says "the pattern DeviceImportCsvModal.svelte:123 already uses: `apiCall<number[]>('read_file_bytes', { path: filePath })`) instead of plugin-fs readFile". However, read_file_bytes has a CSV-only extension check.

    Resolution: The simplest correct fix that respects the gap_hint and does not require additional backend changes is to add a new backend command in settings_org.rs that reads an image file by path (with UNC rejection + size cap + image extension whitelist). However, this is a backend change — the plan is frontend-only.

    PRAGMATIC DECISION: Since the gap_hint explicitly says to use `read_file_bytes`, and the gap says "pattern in DeviceImportCsvModal.svelte:123", the planner's intent was to reuse that command. The CSV extension check in read_file_bytes WILL reject image paths. Therefore, the plan must ALSO add a `read_image_bytes` command in settings_org.rs (or fs_helpers.rs) for this task.

    However, this plan is FRONTEND-ONLY (depends on 07-12 for backend). To avoid a frontend-depends-on-undefined backend pattern, handle it by using plugin-fs in the capability instead. Check the actual capability file.
  </read_first>

  <action>
    Step 1 — Read the capability file to determine if plugin-fs readFile can be granted:
    Read `crates/trackly-app/capabilities/main.json`. If it does NOT already include `fs:allow-read-file`, add it to the permissions array alongside the existing `dialog:default` and related permissions.

    Step 2 — Fix the detection predicate in uploadLogo (OrgSettings.svelte line 105):
    Change the broken line:
    `const isTauri = typeof window !== 'undefined' && !!(window as unknown as Record<string, unknown>).__TAURI__;`
    to:
    `const isTauri = typeof window !== 'undefined' && '__TAURI_INTERNALS__' in window;`

    Step 3 — Fix the file-reading in the Tauri branch (lines 116-117). Replace:
    ```
    const { readFile } = await import('@tauri-apps/plugin-fs');
    const bytes = await readFile(filePath);
    ```
    with a backend command call. Since read_file_bytes only allows .csv extensions, the correct fix is to use the plugin-fs `readFile` import BUT ensure the `fs:allow-read-file` capability is present in main.json (Step 1). The actual read stays as `readFile(filePath)` (already imports from @tauri-apps/plugin-fs), because plugin-fs with proper capability is cleaner than a new command.

    If adding `fs:allow-read-file` to the capability is not sufficient (it requires a scope), add `"fs:allow-read-file"` with allow-all scope. The Tauri v2 capability permission for `plugin:fs|allow-read-file` with an `allow` scope of `"**"` is the correct path. Check the Tauri v2 docs pattern for fs capability scoping.

    Concretely: in `crates/trackly-app/capabilities/main.json`, the permissions array should gain:
    `"fs:allow-read-file"` entry (without scope restriction, since it's the desktop-only code path and the file was explicitly picked by the user via a native dialog).

    Also remove the now-redundant `import` statement for plugin-fs if the readFile import can be combined with any existing import from that package. Ensure the `bytes` variable from `readFile` is converted to `Array.from(bytes)` (a number[] / Uint8Array) before passing to apiCall.

    Step 4 — Confirm the import for `@tauri-apps/plugin-fs` is correctly structured at line 116 (dynamic import inside the function is fine).

    Final state of uploadLogo should:
    1. Use `'__TAURI_INTERNALS__' in window` for detection.
    2. Open the file picker with plugin-dialog.
    3. Read the picked file with plugin-fs readFile (which now has capability).
    4. Convert bytes to Array.from() and call `apiCall<void>('settings_save_org_logo', { logo_bytes: Array.from(bytes), logo_mime: mime })`.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; pnpm --filter ui svelte-check 2>&amp;1 | tail -10 &amp;&amp; grep -c "__TAURI_INTERNALS__" ui/src/features/settings/OrgSettings.svelte &amp;&amp; grep -c "readFile\|read_file_bytes" ui/src/features/settings/OrgSettings.svelte</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm svelte-check` exits 0 (no TypeScript errors in OrgSettings.svelte).
    - `grep -c "__TAURI_INTERNALS__" ui/src/features/settings/OrgSettings.svelte` returns 1.
    - `grep -c "__TAURI__" ui/src/features/settings/OrgSettings.svelte` returns 0 (old check removed).
    - `crates/trackly-app/capabilities/main.json` contains `fs:allow-read-file` in its permissions array.
    - The uploadLogo function no longer uses `!!(window as ...__TAURI__)`.
  </acceptance_criteria>

  <done>OrgSettings.svelte uploadLogo detects Tauri 2 correctly and reads the picked image file using plugin-fs with a capability grant. pnpm svelte-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix StorageSettings openFolder command name (G2-2 frontend) + fix BackupSettings arg wrapping (G2-3)</name>
  <files>ui/src/features/settings/StorageSettings.svelte, ui/src/features/settings/BackupSettings.svelte</files>

  <read_first>
    StorageSettings.svelte, function `openFolder` (line 29-38):
    - Current: `await apiCall<void>('fs_open_folder', { path: dbPath });`
    - Bug: command name `fs_open_folder` does not exist. The new command from plan 07-12 is `settings_open_db_folder`.
    - Fix: rename command to `settings_open_db_folder`. The `path` argument matches because `settings_open_db_folder` in the backend uses `ctx.paths.db_path()` internally — actually, the backend command derives the folder from AppCtx, it does NOT take a path parameter. So also remove the `{ path: dbPath }` argument → use `{}` instead. (The backend computes the folder from ctx.paths itself.)
    Wait — verify the backend signature from plan 07-12: `settings_open_db_folder(app: tauri::AppHandle, state: tauri::State<'_, AppCtx>) → Result<(), AppError>`. The command takes no user-supplied path — it reads the path from AppCtx internally. Therefore the frontend should call: `await apiCall<void>('settings_open_db_folder', {})`.

    BackupSettings.svelte, function `pickFolder` (lines 63-89):
    - Line 78: `await apiCall<void>('settings_save_backup_config', { backup_folder: selected });`
    - Bug: flat arg `backup_folder` (wrong field name) instead of nested `{ patch: { backupFolder: selected } }`.
    - BackupConfigPatch fields are: backup_folder (Rust snake_case) but since it's passed via Tauri IPC as a top-level `patch` parameter, the patch object must mirror BackupConfigPatch. Tauri deserializes by the Rust parameter name (`patch: BackupConfigPatch`). So the call must be `{ patch: { backup_folder: selected } }` (snake_case matches Rust struct field since dto/reports.rs does NOT have `rename_all = "camelCase"` on BackupConfigPatch — checking: it derives Type but no rename_all annotation is visible from the dto excerpt at line 220-229).
    Actually: BackupConfigPatch in dto/reports.rs does not show a rename_all. The Tauri IPC uses serde deserialization; the Rust struct field is `backup_folder`. So the TypeScript should send `{ patch: { backup_folder: selected } }`.

    BackupSettings.svelte, function `saveConfig` (lines 91-105):
    - Line 94: `await apiCall<void>('settings_save_backup_config', { schedule, retention });`
    - Bug: flat args `schedule` and `retention` instead of `{ patch: { schedule, retention } }`.
    - Rust command signature: `settings_save_backup_config(state, patch: BackupConfigPatch)` — the outer arg name is `patch`, its fields are `backup_folder`, `schedule`, `retention`.
    - Fix: `{ patch: { schedule, retention } }` (leave backup_folder: undefined/null since it's Option<String>).

    Analog for correct nesting: how OrgSettings.svelte calls settings_save_org_fields:
    `await apiCall<void>('settings_save_org_fields', { patch: { org_name: orgName, inn, kpp, address } })` (line 75-82 in OrgSettings.svelte). Same wrapping pattern.

    Audit of ALL settings apiCall sites in the Svelte components to catch same pattern (per G2-3 fix hint):
    - OrgSettings.svelte: settings_get_org (no args), settings_save_org_fields (has patch: {...}), settings_get_org_logo (no args), settings_save_org_logo (logo_bytes, logo_mime — flat, but the Rust command takes those as individual params not wrapped, this is correct), settings_remove_org_logo (no args). All look correct.
    - StorageSettings.svelte: settings_get_db_path (no args), fs_open_folder (BROKEN → fix to settings_open_db_folder with {}), settings_move_db (new_path: newPath — flat, Rust signature is `settings_move_db(state, new_path: String)` so flat is correct), app_restart (no args). One bug: the command name.
    - BackupSettings.svelte: settings_get_backup_config (no args), settings_save_backup_config in pickFolder (BROKEN), settings_save_backup_config in saveConfig (BROKEN), backup_run_manual (no args). Two bugs.
    - ThresholdSettings.svelte: settings_get_low_stock_threshold (no args), settings_set_low_stock_threshold (threshold: N — flat, Rust signature takes `threshold: i32` directly, flat is correct). No bugs.
    - TemplateEditor.svelte: templates_list_for_editor (no args), templates_validate_preview (kind, body — flat, Rust takes kind + body separately, flat is correct), templates_update_body (kind, body — flat, correct), templates_reset_to_default (kind — flat, correct). No bugs.
  </read_first>

  <action>
    In ui/src/features/settings/StorageSettings.svelte:

    1. In function `openFolder` (around line 29-38): change the apiCall from:
       `await apiCall<void>('fs_open_folder', { path: dbPath });`
       to:
       `await apiCall<void>('settings_open_db_folder', {});`
       (No path argument — the backend derives the path from AppCtx internally. The `dbPath` state variable is not passed.)

    In ui/src/features/settings/BackupSettings.svelte:

    2. In function `pickFolder` (around line 78): change the apiCall from:
       `await apiCall<void>('settings_save_backup_config', { backup_folder: selected });`
       to:
       `await apiCall<void>('settings_save_backup_config', { patch: { backup_folder: selected } });`

    3. In function `saveConfig` (around line 94): change the apiCall from:
       `await apiCall<void>('settings_save_backup_config', { schedule, retention });`
       to:
       `await apiCall<void>('settings_save_backup_config', { patch: { schedule, retention } });`
       (Leave backup_folder absent — BackupConfigPatch.backup_folder is Option<String>, absent means None in Rust.)

    These are the only three broken apiCall sites identified by the audit in read_first. Make all three changes then run svelte-check.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly &amp;&amp; pnpm --filter ui svelte-check 2>&amp;1 | tail -10 &amp;&amp; grep -c "settings_open_db_folder" ui/src/features/settings/StorageSettings.svelte &amp;&amp; grep -c "fs_open_folder" ui/src/features/settings/StorageSettings.svelte &amp;&amp; grep -c "patch:" ui/src/features/settings/BackupSettings.svelte</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter ui svelte-check` exits 0.
    - `grep -c "fs_open_folder" ui/src/features/settings/StorageSettings.svelte` returns 0 (old name gone).
    - `grep -c "settings_open_db_folder" ui/src/features/settings/StorageSettings.svelte` returns 1.
    - `grep -c "backup_folder: selected" ui/src/features/settings/BackupSettings.svelte` returns 0 (bare flat arg gone).
    - `grep -c "patch:" ui/src/features/settings/BackupSettings.svelte` returns 2 (one in pickFolder, one in saveConfig).
    - `grep -c "patch: { schedule" ui/src/features/settings/BackupSettings.svelte` returns 1.
    - No other settings apiCall sites (ThresholdSettings, TemplateEditor, OrgSettings) were modified (audit confirmed those are correct).
  </acceptance_criteria>

  <done>StorageSettings calls settings_open_db_folder with no path arg. BackupSettings wraps both save calls in { patch: { ... } } matching BackupConfigPatch. pnpm svelte-check passes. Runtime: "Открыть папку" and backup config save no longer error with "Не удалось связаться с приложением".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-fs readFile | File path comes from native dialog picker — user explicitly chose the file; OS dialog is the trust boundary. The `fs:allow-read-file` capability without scope restriction is acceptable because only desktop users can trigger this code path (gated behind `'__TAURI_INTERNALS__' in window`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13-01 | Information Disclosure | plugin-fs readFile image path | accept | File path provided by OS native picker; user controls which file to read; size is validated at backend (512 KB cap in org_db_service); no path traversal possible via dialog |
| T-07-13-02 | Spoofing | Tauri detection predicate fix | accept | `'__TAURI_INTERNALS__' in window` is the documented Tauri 2 reliable predicate; no security impact — it gates UX behavior, not auth |
</threat_model>

<verification>
After all tasks complete:
1. `pnpm --filter ui svelte-check` — 0 errors.
2. `grep "__TAURI__" ui/src/features/settings/OrgSettings.svelte` — no match (old check gone).
3. `grep "__TAURI_INTERNALS__" ui/src/features/settings/OrgSettings.svelte` — 1 match.
4. `grep "fs_open_folder" ui/src/features/settings/StorageSettings.svelte` — no match.
5. `grep "settings_open_db_folder" ui/src/features/settings/StorageSettings.svelte` — 1 match.
6. `grep "patch:" ui/src/features/settings/BackupSettings.svelte` — 2 matches.
7. `grep -c "fs:allow-read-file" crates/trackly-app/capabilities/main.json` — 1 match.
</verification>

<success_criteria>
- G2-1: OrgSettings.svelte uses correct Tauri 2 detection; logo file reading uses plugin-fs with capability; no more "Не удалось связаться с приложением" on logo upload.
- G2-2 (frontend): StorageSettings.svelte calls settings_open_db_folder (from plan 07-12) with no path arg.
- G2-3: Both settings_save_backup_config calls in BackupSettings.svelte wrap args in { patch: { ... } } matching BackupConfigPatch struct shape.
</success_criteria>

<output>
Create `.planning/phases/07-reports-dashboard-settings/07-13-SUMMARY.md` when done.
</output>
