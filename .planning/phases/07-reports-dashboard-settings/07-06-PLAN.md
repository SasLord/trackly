---
phase: 07-reports-dashboard-settings
plan: 06
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - ui/src/features/reports/ReportsPage.svelte
  - ui/src/features/reports/ReportSubNav.svelte
  - ui/src/features/reports/PeriodSelector.svelte
  - ui/src/features/reports/ReportTable.svelte
  - ui/src/features/reports/ReportFilters.svelte
  - ui/src/pages/ReportsPage.svelte
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04
  - RPT-05
  - RPT-06
  - RPT-07
  - RPT-08

must_haves:
  truths:
    - "ReportsPage has two-level navigation: domain sub-nav (Устройства / Картриджи) + switch-bar for report type (D-01)"
    - "Period selector shows Месяц/Год/Диапазон buttons; default = current month (D-03)"
    - "Snapshot reports (Что в работе, Что на складе) hide/disable period selector with helper text (D-02)"
    - "Report table has month-separator rows «Сентябрь 2026» for temporal reports, grouped by location/status for snapshots"
    - "Contextual filters: Устройства → локация/тип/статус; Картриджи → модель/статус/цвет (D-04 full spec)"
    - "Search input (RPT-05) filters table client-side or re-fetches with search param"
    - "Export CSV button calls report command and downloads/saves file (D-07, D-08)"
    - "Export PDF button calls report command and downloads/saves file or opens in system viewer (D-06)"
    - "Print button: Tauri → opens PDF in system viewer; browser → window.print() (D-06)"
    - "All copy Russian; all components Svelte 5 runes only"
  artifacts:
    - path: "ui/src/features/reports/ReportsPage.svelte"
      provides: "Orchestrator: domain sub-nav + period + filters + search + table + export"
      contains: "reports_list_device_acts"
    - path: "ui/src/features/reports/ReportTable.svelte"
      provides: "Table with month-separator rows; loading/error/empty states"
      contains: "month-separator"
    - path: "ui/src/features/reports/PeriodSelector.svelte"
      provides: "Месяц/Год/Диапазон button group + sub-controls"
      contains: "PeriodDto"
    - path: "ui/src/pages/ReportsPage.svelte"
      provides: "Route page that renders features/reports/ReportsPage.svelte"
  key_links:
    - from: "ui/src/features/reports/ReportsPage.svelte"
      to: "reports_list_device_acts Tauri command (plan 07)"
      via: "apiCall<ReportResponse>('reports_list_device_acts', { filter, period })"
      pattern: "reports_list_device_acts"
    - from: "ui/src/features/reports/ReportsPage.svelte"
      to: "reports_export_csv Tauri command (plan 07)"
      via: "apiCall<number[]>('reports_export_csv', { reportType, filter, period })"
      pattern: "reports_export_csv"
---

<objective>
Reports UI: ReportsPage implements the two-level navigation, period selector, contextual filters, search, and universal table with month-separator rows. Export (CSV/PDF) and print connect to backend commands registered in plan 07. Temporal vs snapshot report types handled visually (period selector shown/hidden, separator style).

Purpose: Complete the reports vertical slice — admin can browse all 8 report types with period filtering, export/print.

Output: 5 new Svelte components + updated ReportsPage.svelte stub at pages/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-reports-dashboard-settings/07-UI-SPEC.md
@.planning/phases/07-reports-dashboard-settings/07-PATTERNS.md
@.planning/phases/07-reports-dashboard-settings/07-03-SUMMARY.md
</context>

<interfaces>
<!-- Verified UI patterns — executor MUST replicate these exactly -->

From ui/src/features/acts/ActsSearchAndTabs.svelte (switch-bar tab pattern):
  .tab { display: inline-flex; align-items: center; gap: var(--space-xs); padding: var(--space-xs) var(--space-md);
          border: 1px solid var(--color-border); border-radius: var(--radius-sm); height: 32px; }
  .tab.active { background: color-mix(in srgb, var(--color-accent) 10%, transparent); border-color: var(--color-accent); }

From ui/src/features/cartridges/CartridgeFilters.svelte (status-bar button + filter select pattern):
  .filter-select { height: 28px; padding: 0 var(--space-sm); background: var(--color-bg); border: 1px solid var(--color-border); }

From ui/src/lib/components/DatePicker.svelte (date input for range mode):
  Import path to check; may be ui/src/lib/components/DatePicker.svelte

Month separator SCSS (from PATTERNS.md):
  .month-separator td { padding: var(--space-xs) var(--space-md); height: var(--row-height-dense);
    background: var(--color-surface-sunken); font-size: var(--font-size-body);
    font-weight: var(--font-weight-semibold); border-top: 1px solid var(--color-border-strong); }

Russian month names array for display (month_key "2026-09" → "Сентябрь 2026"):
  const MONTH_NAMES = ['Январь','Февраль','Март','Апрель','Май','Июнь','Июль','Август','Сентябрь','Октябрь','Ноябрь','Декабрь'];
  function formatMonthKey(key: string): string {
    const [year, month] = key.split('-');
    return MONTH_NAMES[parseInt(month) - 1] + ' ' + year;
  }

Report types configuration (drives sub-nav and behavior):
  DEVICE_REPORTS = [
    { key: 'acts', label: 'Акты', temporal: true, cmd: 'reports_list_device_acts' },
    { key: 'returns', label: 'Возвраты', temporal: true, cmd: 'reports_list_device_returns' },
    { key: 'in_use', label: 'В работе', temporal: false, cmd: 'reports_list_device_in_use' },
    { key: 'in_stock', label: 'На складе', temporal: false, cmd: 'reports_list_device_in_stock' },
  ]
  CARTRIDGE_REPORTS = [
    { key: 'consumption', label: 'Расход', temporal: true, cmd: 'reports_list_cartridge_consumption' },
    { key: 'refills', label: 'История заправок', temporal: true, cmd: 'reports_list_cartridge_refills' },
    { key: 'in_use', label: 'В работе', temporal: false, cmd: 'reports_list_cartridge_in_use' },
    { key: 'in_stock', label: 'На складе', temporal: false, cmd: 'reports_list_cartridge_in_stock' },
  ]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PeriodSelector + ReportFilters + ReportSubNav + ReportTable</name>
  <files>
    ui/src/features/reports/PeriodSelector.svelte,
    ui/src/features/reports/ReportFilters.svelte,
    ui/src/features/reports/ReportSubNav.svelte,
    ui/src/features/reports/ReportTable.svelte
  </files>
  <read_first>
    - ui/src/features/cartridges/CartridgeFilters.svelte (full file — filter select + status-bar button pattern to replicate)
    - ui/src/features/acts/ActsSearchAndTabs.svelte (full file — tab pattern for sub-nav)
    - ui/src/features/acts/ActsList.svelte lines 34-70 (loading/empty/error state pattern for ReportTable)
    - ui/src/lib/components/DatePicker.svelte (verify import path and props)
    - .planning/phases/07-reports-dashboard-settings/07-UI-SPEC.md §Reports Layout §Period Selector §Report table
  </read_first>
  <action>
    Create ui/src/features/reports/ReportSubNav.svelte:

    Props: activeDomain: 'devices'|'cartridges', activeReport: string, rowCount: number,
           onDomainChange: (d: 'devices'|'cartridges') => void, onReportChange: (r: string) => void.

    Computed: let activeReports = $derived(activeDomain === 'devices' ? DEVICE_REPORTS : CARTRIDGE_REPORTS).

    Markup: domain tabs row (Устройства / Картриджи) using .tab pattern from ActsSearchAndTabs.svelte; report switch-bar tabs below (drives second-level report selection). Switch-bar tabs show a Badge variant="default" with rowCount on the active tab only. Switching domain calls onDomainChange and automatically sets activeReport to the first key of the new domain.

    SCSS: exact .tab and .domain-nav / .report-nav patterns from ActsSearchAndTabs.svelte. Domain nav bottom border separates it from report switch-bar.

    Create ui/src/features/reports/PeriodSelector.svelte:

    Props: period: PeriodDto (imported from dto/reports.ts TypeScript types), isSnapshot: boolean,
           onPeriodChange: (p: PeriodDto) => void.

    State: let mode = $state<'month'|'year'|'range'>(period.mode ?? 'month'); let selectedMonth = $state(period.month ?? new Date().getMonth() + 1); let selectedYear = $state(period.year ?? new Date().getFullYear()); let dateFrom = $state(period.date_from ?? ''); let dateTo = $state(period.date_to ?? '').

    If isSnapshot=true: render period controls as disabled with helper text "Отчёт отражает текущее состояние" (--color-text-muted) and aria-disabled="true" on controls.

    Mode button group: «Месяц» | «Год» | «Диапазон» (3 buttons, .period-btn with .active).
    When mode='month': two <select> (month names + years). MONTHS array hardcoded Russian. Years range: current_year-3 to current_year. onChange calls onPeriodChange({ mode: 'month', month: selectedMonth, year: selectedYear }).
    When mode='year': single year <select>. onChange → onPeriodChange({ mode: 'year', year: selectedYear }).
    When mode='range': two DatePicker fields (начало / конец). Validate start <= end before calling onPeriodChange; show inline error "Начало не может быть позже окончания" if invalid.

    Create ui/src/features/reports/ReportFilters.svelte:

    Props: reportDomain: 'devices'|'cartridges', reportType: string, locationId: number|null, statusId: number|null, typeId: number|null, modelId: number|null, color: string|null, search: string, locations: Array<{id:number,name:string}>, statuses: Array<{id:number,name:string}>, deviceTypes: Array<{id:number,name:string}>, cartridgeModels: Array<{id:number,label:string}>, cartridgeStatuses: Array<{id:number,name:string}>, cartridgeColors: Array<string>, onFilterChange: (f: Partial<ReportFilter>) => void, onExportCsv: () => void, onExportPdf: () => void, onPrint: () => void, csvExporting: boolean, pdfExporting: boolean.

    Device domain filters (per D-04: Устройства → локация/тип/статус): location <select> (Все + list from locations prop) + device-type <select> (Все + deviceTypes prop, bound to typeId) + status <select> (Все + statuses prop, bound to statusId). Cartridge domain filters (per D-04: Картриджи → модель/статус/цвет): model <select> (Все + cartridgeModels prop, bound to modelId) + status <select> (Все + cartridgeStatuses prop, bound to statusId) + color <select> (Все + cartridgeColors prop mapped as {value: c, label: c}, bound to color). All filter changes call onFilterChange with the updated field (type_id, status_id, model_id, color, etc. — field names match ReportFilter Rust DTO). Both domains show search Input (placeholder "Поиск в отчёте…").

    Export buttons right-aligned in same row: Button variant="secondary" size="sm" loading={csvExporting} onclick={onExportCsv} "Экспорт CSV", Button variant="secondary" size="sm" loading={pdfExporting} onclick={onExportPdf} "Экспорт PDF", Button variant="ghost" size="sm" onclick={onPrint} with print SVG icon "Печать". The export callbacks (onExportCsv, onExportPdf, onPrint) are passed in from the parent ReportsPage.svelte orchestrator — ReportFilters does not call apiCall directly for export.

    SCSS: .filters-row { display: flex; align-items: center; gap: var(--space-sm); flex-wrap: wrap; padding: var(--space-sm) 0; }; export buttons pushed to right via margin-left: auto on print button or flex justify-content: space-between split.

    Create ui/src/features/reports/ReportTable.svelte:

    Props: rows: ReportRow[], columns: Array<{key: string, label: string}>, loading: boolean, error: string|null, reportType: string, isSnapshot: boolean.

    Helper formatMonthKey(key: string): string as described in interfaces above.

    Computing grouped rows: iterate rows; when month_key changes from previous row (temporal reports), insert a separator object {type: 'separator', label: formatMonthKey(row.month_key)}. For snapshot reports, insert separator on location_name change instead (grouped by location). Result: Array<ReportRow | {type:'separator', label:string}>.

    Markup: standard <table> with sticky <thead>. {#each grouped as item} — if item.type==='separator': <tr class="month-separator" aria-hidden="true"><td colspan={columns.length}>{item.label}</td></tr> — else: <tr>{#each columns as col}<td title={String(item[col.key] ?? '—')}>{item[col.key] ?? '—'}</td>{/each}</tr>. Loading/empty/error states follow ActsList.svelte pattern.

    Empty state copy: "Нет данных за выбранный период" heading + "Измените диапазон дат или выберите другой тип отчёта." body (matches UI-SPEC).

    SCSS: table layout matches existing ActsList.svelte; --row-height 40px rows; month-separator rows at --row-height-dense 32px; sticky thead.
  </action>
  <verify>
    <automated>pnpm svelte-check --tsconfig ui/tsconfig.json 2>&1 | grep -E "Error|error" | head -20</automated>
  </verify>
  <done>
    All 4 components exist in ui/src/features/reports/. pnpm svelte-check exits 0. ReportTable contains "month-separator" class. PeriodSelector contains "Месяц" and "Год" and "Диапазон" strings. ReportSubNav contains DEVICE_REPORTS and CARTRIDGE_REPORTS config. All components use Svelte 5 $state/$derived/$props.
  </done>
</task>

<task type="auto">
  <name>Task 2: ReportsPage orchestrator + ReportsPage.svelte route update</name>
  <files>
    ui/src/features/reports/ReportsPage.svelte,
    ui/src/pages/ReportsPage.svelte
  </files>
  <read_first>
    - ui/src/pages/ReportsPage.svelte (current file — understand placeholder before replacing)
    - ui/src/features/acts/ActsPage.svelte (full page orchestration pattern — master-detail with search/filter/list)
    - ui/src/features/cartridges/CartridgesPage.svelte (alternative page orchestration pattern)
    - ui/src/features/cartridges/CartridgesPage.svelte (onMount data-loading pattern: how it calls cartridge_models_list and wires results to filter props)
    - crates/trackly-app/src/tauri_cmds/devices.rs lines 209-214 (locations_autocomplete signature — returns Vec<String>)
    - crates/trackly-app/src/tauri_cmds/cartridges.rs lines 290-294 (cartridge_models_list signature — returns Vec<CartridgeModelDto>)
    - crates/trackly-app/src/dto/cartridge.rs lines 78-100 (CartridgeModelDto shape: id, brand, model, color: Option<String>)
    - .planning/phases/07-reports-dashboard-settings/07-UI-SPEC.md §Export/Print interaction contracts §Report Navigation
  </read_first>
  <action>
    Create ui/src/features/reports/ReportsPage.svelte:

    COMMAND INTERFACES (verified in codebase — use exact names):
    - locations_autocomplete({ prefix: '' }) → Promise<string[]>  [devices.rs:209]
    - cartridge_models_list({}) → Promise<CartridgeModelDto[]>  [cartridges.rs:290]
    - No device-types command exists: device types are a 2-item static enum (type_id 1 = Устройство, type_id 2 = Расходник) hard-coded in device_service::resolve_type_id.
    - No cartridge-statuses command exists: cartridge statuses are a 4-item static enum (1=На складе, 2=В работе, 3=На заправке, 4=Списано) matching known seed IDs.
    - Cartridge colors: derive from cartridge_models_list result — extract distinct non-null .color values.

    FILTER DROPDOWN STATE (add these state vars, separate from filter state):
    let filterLocations = $state<string[]>([]);
    let filterDeviceTypes = $state<Array<{id:number, name:string}>>([
      { id: 1, name: 'Устройство' },
      { id: 2, name: 'Расходник' },
    ]);
    let filterCartridgeModels = $state<Array<{id:number, label:string}>>([]);
    let filterCartridgeStatuses = $state<Array<{id:number, name:string}>>([
      { id: 1, name: 'На складе' },
      { id: 2, name: 'В работе' },
      { id: 3, name: 'На заправке' },
      { id: 4, name: 'Списано' },
    ]);
    let filterCartridgeColors = $state<string[]>([]);

    Static filter lists (filterDeviceTypes, filterCartridgeStatuses) are declared inline as shown above — no command call needed.

    DYNAMIC FILTER LOADING (onMount — fires once on component mount):
    import { onMount } from 'svelte';
    onMount(async () => {
      // Load locations — returns string[] (location names, not IDs).
      // ReportFilter location select binds to string names; service layer resolves name → id.
      const locs = await apiCall<string[]>('locations_autocomplete', { prefix: '' });
      filterLocations = locs;

      // Load cartridge models — used for both model filter and color derivation.
      const models = await apiCall<CartridgeModelDto[]>('cartridge_models_list', {});
      filterCartridgeModels = models.map(m => ({ id: m.id, label: m.brand + ' ' + m.model }));
      filterCartridgeColors = [...new Set(
        models.map(m => m.color).filter((c): c is string => c != null)
      )].sort();
    });

    Note on location filter: because locations_autocomplete returns name strings (not IDs), the
    ReportFilter component emits onFilterChange({ location_name: selectedName }) for the location
    select (not location_id). The report service (plan 03) must handle location_name in ReportFilter
    or resolve it via "SELECT id FROM locations WHERE name = ?1". The ReportFilter prop locationId
    changes to locationName: string|null for this component.

    REPORT STATE (unchanged from original):
    let activeDomain = $state<'devices'|'cartridges'>('devices');
    let activeReport = $state('acts');
    let period = $state<PeriodDto>({ mode: 'month', year: new Date().getFullYear(), month: new Date().getMonth()+1 });
    let filter = $state<Partial<ReportFilter>>({});
    let rows = $state<ReportResponse | null>(null);
    let loading = $state(false);
    let error = $state<string|null>(null);
    let csvExporting = $state(false);
    let pdfExporting = $state(false).

    Helper isSnapshot(): boolean = ['in_use','in_stock'].includes(activeReport).

    Helper currentCmd(): string — looks up in DEVICE_REPORTS/CARTRIDGE_REPORTS by activeReport to get cmd string.

    Columns by report type (constant map): acts → [{key:'number',label:'Номер'},{key:'giver_name',label:'Сдал'},{key:'receiver_name',label:'Принял'},{key:'handover_date_utc',label:'Дата'},{key:'location_name',label:'Локация'},{key:'device_name',label:'Устройства'}]; returns → similar with sub_number; in_use/in_stock → [{key:'device_name',label:'Наименование'},{key:'location_name',label:'Расположение'},{key:'status_name',label:'Статус'}]; consumption → [{key:'month_key',label:'Месяц'},{key:'model_label',label:'Модель'},{key:'code',label:'Код картриджа'},{key:'location_name',label:'Локация'}]; etc.

    loadReport(): loading = true; error = null; const cmd = currentCmd(); const filterPayload = { ...filter, search: filter.search || null }; apiCall<ReportResponse>(cmd, { filter: filterPayload, period: isSnapshot() ? undefined : period }).then(r => { rows = r; }).catch(e => { error = String(e); }).finally(() => { loading = false; });

    $effect(() => { activeDomain; activeReport; period; filter; loadReport(); }) — auto-reload on any relevant state change.

    exportCsv(): csvExporting = true; apiCall<number[]>('reports_export_csv', { reportType: currentCmd(), filter, period }).then(bytes => { const blob = new Blob([new Uint8Array(bytes)], {type:'text/csv;charset=utf-8'}); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'отчёт.csv'; a.click(); URL.revokeObjectURL(url); }).catch(e => pushToast('error','Ошибка при экспорте CSV. Попробуйте ещё раз.')).finally(() => csvExporting = false).

    exportPdf(): pdfExporting = true; In Tauri context: apiCall<number[]>('reports_export_pdf', { reportType: currentCmd(), filter, period }) → save bytes to file via tauri-plugin-fs writeBinaryFile with save dialog → open in system viewer via tauri-plugin-shell open. In browser context: download as blob. pushToast on error. finally: pdfExporting = false.

    printReport(): In Tauri context: same PDF generation, open in system viewer (D-06). In browser context: window.print() or download PDF (let user choose via browser print dialog).

    Page layout:
    <div class="reports-page">
      <header class="page-header"><h1 class="page-title">Отчёты</h1></header>
      <div class="reports-content">
        <ReportSubNav {activeDomain} {activeReport} rowCount={rows?.total ?? 0}
          onDomainChange={(d) => { activeDomain = d; activeReport = d === 'devices' ? 'acts' : 'consumption'; }}
          onReportChange={(r) => activeReport = r} />
        <ReportFilters
          reportDomain={activeDomain} reportType={activeReport}
          locationName={filter.location_name ?? null}
          statusId={filter.status_id ?? null}
          typeId={filter.type_id ?? null}
          modelId={filter.model_id ?? null}
          color={filter.color ?? null}
          search={filter.search ?? ''}
          locations={filterLocations}
          deviceTypes={filterDeviceTypes}
          cartridgeModels={filterCartridgeModels}
          cartridgeStatuses={filterCartridgeStatuses}
          cartridgeColors={filterCartridgeColors}
          onFilterChange={(f) => filter = { ...filter, ...f }}
          onExportCsv={exportCsv} onExportPdf={exportPdf} onPrint={printReport}
          {csvExporting} {pdfExporting} />
        {#if !isSnapshot()}
          <PeriodSelector {period} isSnapshot={false} onPeriodChange={(p) => period = p} />
        {:else}
          <PeriodSelector period={period} isSnapshot={true} onPeriodChange={() => {}} />
        {/if}
        <ReportTable rows={rows?.rows ?? []} columns={currentColumns()} {loading} {error}
          reportType={activeReport} isSnapshot={isSnapshot()} />
      </div>
    </div>

    SCSS: .reports-page { display: flex; flex-direction: column; height: 100%; }
    .page-header { padding: var(--space-lg) var(--space-xl); border-bottom: 1px solid var(--color-border); flex-shrink: 0; }
    .reports-content { flex: 1; overflow: auto; padding: 0 var(--space-xl); display: flex; flex-direction: column; gap: var(--space-sm); }

    Update ui/src/pages/ReportsPage.svelte: replace placeholder with import + render of features/reports/ReportsPage.svelte.
  </action>
  <verify>
    <automated>pnpm svelte-check --tsconfig ui/tsconfig.json 2>&1 | grep -E "Error|error" | head -20</automated>
  </verify>
  <done>
    ReportsPage.svelte in features/reports/ exists with all imports. ui/src/pages/ReportsPage.svelte renders the features component. pnpm svelte-check exits 0.
    grep "reports_list_device_acts" ui/src/features/reports/ReportsPage.svelte returns a match.
    grep "reports_export_csv" returns a match.
    grep "locations_autocomplete" ui/src/features/reports/ReportsPage.svelte returns a match (onMount data loading).
    grep "cartridge_models_list" ui/src/features/reports/ReportsPage.svelte returns a match.
    grep "filterLocations\|filterCartridgeModels\|filterCartridgeColors" ui/src/features/reports/ReportsPage.svelte returns >= 3 matches.
    All five ReportFilters dropdown props (locations, deviceTypes, cartridgeModels, cartridgeStatuses, cartridgeColors) receive the populated state vars — not empty array literals.
    isSnapshot() function exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReportFilter from UI | User-provided filter values passed to backend commands; backend must parameterize all values |
| CSV download bytes | Bytes from backend; passed through Blob API; formula injection prevention done on backend |
| PDF bytes | Bytes from backend krilla renderer; no HTML injection possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Injection | ReportFilter values → SQL | mitigate | Filter values sent to backend; backend uses params_from_iter (plan 03); UI does not construct SQL |
| T-07-06-02 | Injection | CSV formula injection | mitigate | Backend csv_safe helper (device_service pattern) prefixes formula characters; frontend only receives bytes |
| T-07-06-03 | Tampering | Date range validation (start > end) | mitigate | PeriodSelector shows inline error "Начало не может быть позже окончания" and blocks onPeriodChange call when invalid |
| T-07-06-04 | DoS | Unlimited report export | accept | Reports are admin-only tool; row limit enforced in backend service (LIMIT 1000 per query) |
| T-07-06-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
- `pnpm svelte-check --tsconfig ui/tsconfig.json` exits 0
- `ls ui/src/features/reports/` shows ReportsPage.svelte, ReportSubNav.svelte, PeriodSelector.svelte, ReportTable.svelte, ReportFilters.svelte
- `grep -c "month-separator" ui/src/features/reports/ReportTable.svelte` returns >= 1
- `grep -c "Месяц.*Год.*Диапазон\|Год.*Диапазон\|mode.*month\|mode.*year\|mode.*range" ui/src/features/reports/PeriodSelector.svelte` returns >= 1 (period modes)
- `grep -c "isSnapshot\|Отчёт отражает" ui/src/features/reports/PeriodSelector.svelte` returns >= 1 (snapshot handling)
- `grep -c "reports_list_device_acts\|currentCmd" ui/src/features/reports/ReportsPage.svelte` returns >= 1
- `grep -c "reports_export_csv" ui/src/features/reports/ReportsPage.svelte` returns >= 1
- `grep -c "\$state\|\$derived" ui/src/features/reports/ReportsPage.svelte` returns >= 5
- All user-facing strings are in Russian (no English CTAs or labels)
</verification>

<success_criteria>
- Reports page has 2-level nav; switching domain/report type triggers data reload
- Period selector: Месяц/Год/Диапазон modes; snapshot reports disable it with helper text
- Report table: month-separator rows for temporal, location-separator rows for snapshots
- CSV and PDF export buttons trigger backend commands and download/save the resulting file
- Print triggers system viewer (Tauri) or window.print() (browser)
- pnpm svelte-check exits 0
</success_criteria>

<output>
Create .planning/phases/07-reports-dashboard-settings/07-06-SUMMARY.md when done
</output>
