---
phase: 07-reports-dashboard-settings
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/src/tauri_cmds/settings_org.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/src/services/template_service.rs
autonomous: true
requirements:
  - SET-04
  - SET-07
gap_closure: true
gaps_addressed:
  - G2-2
  - G2-4

must_haves:
  truths:
    - "G2-2: a Tauri command named `settings_open_db_folder` exists, is registered in specta_export.rs, and appears in regenerated ui/src/bindings.ts"
    - "G2-2: the command opens the containing directory of ctx.paths.db_path() via tauri_plugin_shell, path-guarded with canonicalize + directory existence check (no arbitrary path open)"
    - "G2-4: validate_preview renders BOTH act_handover AND act_acceptance default templates without 'undefined value' error — unit test covers both"
  artifacts:
    - path: "crates/trackly-app/src/tauri_cmds/settings_org.rs"
      provides: "settings_open_db_folder Tauri command"
      contains: "settings_open_db_folder"
    - path: "crates/trackly-app/src/specta_export.rs"
      provides: "settings_open_db_folder registered in builder"
      contains: "settings_open_db_folder"
    - path: "crates/trackly-app/src/services/template_service.rs"
      provides: "validate_preview with expanded demo_ctx covering device/document/act/org contexts"
      contains: "device"
  key_links:
    - from: "crates/trackly-app/src/specta_export.rs"
      to: "ui/src/bindings.ts"
      via: "cargo test --test export_bindings"
      pattern: "settings_open_db_folder"
    - from: "template_service.rs validate_preview"
      to: "act_acceptance.minijinja device.{}/document.{} vars"
      via: "demo_ctx serde_json"
      pattern: "device"
---

<objective>
Add the missing `settings_open_db_folder` backend command (G2-2) and fix the `validate_preview` demo context gap (G2-4).

Purpose: Both gaps share the same root: missing backend completeness. G2-2 is a command called in the UI but never created in Rust. G2-4 is a demo_ctx that covers act_handover but not act_acceptance, causing MiniJinja undefined-value error when the user previews the acceptance template.

Output: New Tauri command registered in specta_export.rs + expanded unit test for validate_preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reports-dashboard-settings/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add settings_open_db_folder command (G2-2 backend)</name>
  <files>crates/trackly-app/src/tauri_cmds/settings_org.rs, crates/trackly-app/src/specta_export.rs</files>

  <read_first>
    Source-of-truth analog for secure shell wrapper: crates/trackly-app/src/tauri_cmds/acts.rs, function `acts_open_pdf_in_system` (lines 218-268). It canonicalizes the path, guards it must reside in a specific directory, then calls app.shell().open(). Reuse this pattern with different guards for a folder path.

    For the DB path to open: crates/trackly-app/src/tauri_cmds/settings_org.rs function `build_settings_get_db_path` returns ctx.paths.db_path() — the folder to open is that path's parent().

    specta_export.rs registration example: every #[tauri::command] #[specta::specta] fn must appear in the collect_commands![] block. Current file ends at line 152. New entry goes after `settings_get_db_path` (line 139).

    AppCtx in settings_org.rs already imports tauri_plugin_shell via `use tauri_plugin_shell::ShellExt;` — NOT present in settings_org.rs (only in acts.rs). Must add the import.

    ShellExt::open is deprecated per acts.rs comment (TODO Phase 4); use the same #[allow(deprecated)] pattern to suppress the warning.
  </read_first>

  <action>
    In crates/trackly-app/src/tauri_cmds/settings_org.rs:

    1. Add `use tauri_plugin_shell::ShellExt;` to the existing use block at the top of the file (after the existing `use std::path::Path;` line).

    2. Add a `build_settings_open_db_folder` build helper below the existing `build_settings_get_db_path` function. The helper takes `ctx: &AppCtx` and `app: tauri::AppHandle`. It must:
       - Derive the DB directory: `ctx.paths.db_path().parent()`, returning Validation error if no parent (should not happen in practice, but defensive).
       - Call `std::fs::canonicalize(dir_path)` — returns Validation error if the directory does not exist.
       - Reject UNC paths on the canonical result: starts_with("\\\\") or starts_with("//") → Validation error with "UNC-пути не поддерживаются".
       - Call `app.shell().open(canonical_str, None)` with `#[allow(deprecated)]` to suppress the deprecation (same pattern as acts_open_pdf_in_system). Map the shell error to AppError::Internal.

    3. Add the Tauri command wrapper function directly below the build helper. Signature:
       `pub async fn settings_open_db_folder(app: tauri::AppHandle, state: tauri::State<'_, AppCtx>) -> Result<(), AppError>`
       Decorated with `#[tauri::command]` then `#[specta::specta]` (tauri-first, specta-second order required). No auth guard needed — opening a folder in file manager is a read-only OS action.

    In crates/trackly-app/src/specta_export.rs:
    4. Add one line to the collect_commands![] block immediately after the `settings_get_db_path` entry (currently line 139):
       `crate::tauri_cmds::settings_org::settings_open_db_folder,`

    After writing both files: run `cargo build -p trackly-app` to confirm zero compilation errors.
    Then run `cargo test --test export_bindings` to regenerate ui/src/bindings.ts.
    The acceptance criterion for this task requires the string "settings_open_db_folder" to appear in ui/src/bindings.ts after regeneration.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo build -p trackly-app 2>&amp;1 | tail -5 &amp;&amp; cargo test --test export_bindings 2>&amp;1 | tail -10 &amp;&amp; grep -c "settings_open_db_folder" ui/src/bindings.ts</automated>
  </verify>

  <acceptance_criteria>
    - `cargo build -p trackly-app` exits 0 with no errors.
    - `cargo test --test export_bindings` exits 0.
    - `grep -c "settings_open_db_folder" ui/src/bindings.ts` returns 1 or more.
    - `grep -c "settings_open_db_folder" crates/trackly-app/src/specta_export.rs` returns 1.
    - The function in settings_org.rs canonicalizes the directory path before opening (no raw user-supplied path passed directly to shell::open).
  </acceptance_criteria>

  <done>Command `settings_open_db_folder` exists in Rust, is registered in specta_export.rs, and the name appears in the regenerated bindings.ts. Cargo build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand validate_preview demo_ctx to cover act_acceptance template (G2-4)</name>
  <files>crates/trackly-app/src/services/template_service.rs</files>

  <read_first>
    The existing validate_preview demo_ctx (lines 252-285 in template_service.rs) covers: org.{name,inn,kpp,address,logo_path}, act.{number,suffix,date,date_human,giver_name,receiver_name,location_name,deadline,deadline_human,parent,items[]}, return.{condition_default,location_default}.

    The act_acceptance template (crates/trackly-app/templates/act_acceptance.minijinja) uses:
    - org.{name,inn,kpp,address,logo_path} — already in demo_ctx, OK
    - document.{giver_name,receiver_name,date_human} — MISSING from demo_ctx
    - device.{name,inventory_no,serial_no,model,condition} — MISSING from demo_ctx

    The existing unit test `validate_preview_returns_pdf_bytes` (lines 339-368 in template_service.rs) tests only act_handover. It passes because that template uses only org/act/return keys. act_acceptance fails because demo_ctx has no device or document.

    MiniJinja with UndefinedBehavior::Strict (confirmed in build_safe_env used by validate_preview) raises "undefined value" on the first missing variable access. So device.name and document.date_human both trigger errors.

    The fix has two parts: (a) add device and document keys to demo_ctx in validate_preview; (b) extend the existing unit test to also verify act_acceptance. No template changes required — the templates are correct; only the demo context is incomplete.
  </read_first>

  <behavior>
    - validate_preview with act_handover default body returns Ok(bytes) where bytes.len() > 0 and bytes[0..4] == b"%PDF" — already tested, must remain passing.
    - validate_preview with act_acceptance default body returns Ok(bytes) where bytes.len() > 0 — NEW failing case to add as a test. Currently fails with AppError from MiniJinja undefined-value for device.* or document.*.
    - validate_preview with a body referencing {{ device.name }} returns Ok(bytes) (not an error).
    - validate_preview with a body referencing {{ document.date_human }} returns Ok(bytes) (not an error).
  </behavior>

  <action>
    In crates/trackly-app/src/services/template_service.rs, in the `validate_preview` method:

    1. Add to the demo_ctx serde_json::json! block, alongside existing org/act/return keys:
       - A `device` object containing: name, inventory_no, serial_no, model, condition (all reasonable string placeholders).
       - A `document` object containing: giver_name, receiver_name, date_human (all string placeholders).

    Example shape to add to the existing json! (add after the "return": {...} key):
    - `"device"` key with fields: name ("HP LaserJet Pro M404n"), inventory_no ("ИНВ-001"), serial_no ("SN-001"), model ("LaserJet Pro M404n"), condition ("Рабочее").
    - `"document"` key with fields: giver_name ("Иванов И.И."), receiver_name ("Петров П.П."), date_human ("17 июня 2026").

    2. In the #[cfg(test)] block, add a second test `validate_preview_act_acceptance_returns_pdf_bytes` immediately after the existing `validate_preview_returns_pdf_bytes` test. It must:
       - Create the same build_test_db() + TemplateService.
       - Look up `DEFAULT_TEMPLATES` for kind == "act_acceptance" (panic if not found).
       - Call `svc.validate_preview(body).await`.
       - Assert Ok(bytes) where bytes.len() > 0, and bytes start with b"%PDF".

    TDD gate: write the new test FIRST, run it to confirm it fails with the missing-variable error, then apply the demo_ctx fix, then confirm both tests pass.
  </action>

  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --lib -- validate_preview 2>&amp;1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - Both `validate_preview_returns_pdf_bytes` (act_handover) and `validate_preview_act_acceptance_returns_pdf_bytes` (act_acceptance) pass.
    - `cargo test -p trackly-app --lib -- validate_preview` exits 0 with "2 passed; 0 failed".
    - The demo_ctx in validate_preview contains both "device" and "document" top-level keys.
    - No changes to the .minijinja template files — the fix is in the demo context only.
  </acceptance_criteria>

  <done>Both template kinds render successfully in validate_preview. Test suite shows 2 passed. Runtime: clicking "Проверить (превью PDF)" on either template in TemplateEditor now produces a PDF blob URL without an error toast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri command → OS shell::open | The path passed to shell::open is derived from ctx.paths.db_path(), not from user input. However, it is canonicalized before use as defense-in-depth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12-01 | Tampering | settings_open_db_folder shell::open | mitigate | Canonicalize the directory path; reject UNC paths (starts_with("\\\\") or "//") before calling shell::open; path is derived from internal ctx.paths, not from raw user string |
| T-07-12-02 | Spoofing | template preview demo_ctx | accept | Demo context contains only placeholder data that never reaches the DB or network; all rendering is in-process |
</threat_model>

<verification>
After all tasks complete:
1. `cargo build --workspace` — 0 errors.
2. `cargo test -p trackly-app --lib -- validate_preview` — 2 passed.
3. `cargo test --test export_bindings` — exits 0.
4. `grep "settings_open_db_folder" ui/src/bindings.ts` — match found.
5. `grep "settings_open_db_folder" crates/trackly-app/src/specta_export.rs` — match found.
</verification>

<success_criteria>
- G2-2 backend: settings_open_db_folder command compiles, is registered in specta_export.rs, and appears in regenerated bindings.ts. Path is canonicalized before shell::open (no arbitrary-path traversal).
- G2-4: validate_preview with the default act_acceptance template body returns Ok(PDF bytes > 0). Unit test `validate_preview_act_acceptance_returns_pdf_bytes` passes alongside the existing act_handover test.
</success_criteria>

<output>
Create `.planning/phases/07-reports-dashboard-settings/07-12-SUMMARY.md` when done.
</output>
