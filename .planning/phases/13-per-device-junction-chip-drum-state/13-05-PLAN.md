---
phase: 13-per-device-junction-chip-drum-state
plan: 05
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - crates/trackly-app/tests/cartridges_crud.rs
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-app/src/tauri_cmds/cartridges.rs
  - crates/trackly-app/src/http/cartridges.rs
autonomous: true
requirements: [SPEC-13-R2]
must_haves:
  truths:
    - "printer_compatib_* интеграционные тесты в cartridges_crud.rs больше не вызывают SqlitePrinterRepository::set_compatible_models_in_tx/get_compatible_model_ids/get_compatible_device_ids (удалённые в Plan 13-02)"
    - "Тест на pass-through (D-05/D-14): модель без заданной совместимости проходит фильтр для любого принтера — поведение сохранено в V005-терминах"
    - "Тест на сужение: модель с printer_name, совпадающим с devices.name связанного принтера (регистронезависимо, с TRIM), проходит фильтр; модель с несовпадающим printer_name — не проходит"
    - "suggest_compat_printer возвращает варианты из devices.name (роستер принтеров, type_id=2), а не из ранее сохранённых cartridge_model_compatibility.printer_name"
  artifacts:
    - path: "crates/trackly-app/tests/cartridges_crud.rs"
      provides: "printer_compatib_* тесты переписаны на V005 printer_name семантику"
  key_links:
    - from: "crates/trackly-app/src/services/cartridge_service.rs::suggest_compat_printer"
      to: "devices.name WHERE type_id = 2"
      via: "SELECT DISTINCT name FROM devices WHERE type_id = 2 AND name LIKE ?1 || '%'"
      pattern: "type_id = 2"
---

<objective>
Переписать три интеграционных теста printer_compatib_* в cartridges_crud.rs (Plan 12-05) с V029-junction семантики на V005 printer_name семантику (сохранив проверяемые поведения: сужение по совпадению, pass-through при пустой совместимости — D-05/D-14), и переработать suggest_compat_printer так, чтобы автокомплит подсказывал реальные имена принтеров из таблицы devices (D-06), а не ранее введённые free-text значения из cartridge_model_compatibility.

Purpose: тесты Plan 12-05 эксплуатируют удалённые в Plan 13-02 репозиторные методы — без переписывания они не скомпилируются. suggest_compat_printer семантически не подходит для D-06 (нужен роستер принтеров, не история уже сохранённых строк) — нужно реализовать новый источник данных для автокомплита, который Plan 13-06 (фронтенд) будет вызывать.
Output: cartridges_crud.rs компилируется и проходит с новыми V005-based тестами; suggest_compat_printer подсказывает devices.name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-SPEC.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-CONTEXT.md

<interfaces>
<!-- crates/trackly-app/tests/cartridges_crud.rs — existing helpers to REUSE as-is -->
fn make_cartridge_service() -> (CartridgeService, tempfile::TempDir);
async fn seed_model(svc: &CartridgeService) -> i64;  // brand=Pantum, model=TL-5120X, kind_id=1, compatibility: vec![]
fn make_cartridge_service_with_handles() -> (CartridgeService, Arc<WriterHandle>, Arc<ReaderPool>, TempDir);
async fn seed_printer_device(writer: &Arc<WriterHandle>, name: &str) -> i64;  // INSERT INTO devices type_id=2

<!-- Existing tests to REPLACE (full bodies already read — exact current logic):
  - printer_compatib_list_narrows_to_linked_model (lines ~270-350): seeds 2 models, links device to model A
    via SqlitePrinterRepository::set_compatible_models_in_tx, filters list() by compatible_with_printer_device_id,
    asserts only model A's cartridge returned.
  - printer_compatib_unconfigured_device_does_not_narrow (lines ~355-432): seeds 2 models, NO links inserted,
    asserts filtered == unfiltered (both 2 items) — D-14 pass-through.
  - printer_compatib_round_trip_both_directions (lines ~437-499): round-trips
    SqlitePrinterRepository::set_compatible_models_in_tx + get_compatible_model_ids + get_compatible_device_ids
    (BOTH METHODS DELETED in Plan 13-02 — this test must be deleted/replaced entirely, not merely adapted). -->

<!-- crates/trackly-app/src/dto/cartridge.rs (Plan 13-02 output) -->
pub struct CartridgeModelCreateDto { pub brand: String, pub model: String, pub kind_id: i64, pub color: Option<String>, pub notes: Option<String>, pub compatibility: Vec<String> }

<!-- crates/trackly-infra/src/repos/cartridges_sqlite.rs (Plan 13-01 output) -->
pub fn upsert_compatibility_in_tx(&self, tx: &Transaction<'_>, model_id: i64, names: &[String]) -> Result<(), AppError>;
pub fn get_compatibility(&self, conn: &Connection, model_id: i64) -> Result<Vec<String>, AppError>;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Переписать printer_compatib_* тесты на V005 printer_name семантику</name>
  <files>crates/trackly-app/tests/cartridges_crud.rs</files>
  <read_first>
    crates/trackly-app/tests/cartridges_crud.rs
    crates/trackly-infra/src/repos/cartridges_sqlite.rs
  </read_first>
  <behavior>
    - printer_compatib_list_narrows_to_linked_model (переписан): seed_printer_device(writer, "Pantum Office-2000") создаёт принтер с конкретным name; model_create с compatibility: vec!["Pantum Office-2000".into()] для модели A, compatibility: vec![] для модели B (либо vec!["Другой Принтер".into()] — несовпадающее имя, для явного негативного случая); list() с compatible_with_printer_device_id=Some(device_id) возвращает только картридж модели A.
    - printer_compatib_unconfigured_device_does_not_narrow (переписан минимально): обе модели создаются с compatibility: vec![] (пустая совместимость — D-05 pass-through) — filtered.items.len() == unfiltered.items.len() == 2, без обращения к удалённым методам (тест уже не использовал set_compatible_models_in_tx напрямую — только семантика filter, так что фактическая правка минимальна, нужно лишь убедиться, что использует актуальный seed_model/model_create, без ссылок на printer_cartridge_models).
    - printer_compatib_case_insensitive_match (новый тест, заменяет round_trip_both_directions): seed_printer_device(writer, "HP LaserJet M404") создаёт принтер; model_create с compatibility: vec!["hp   laserjet m404".into()] (другой регистр + лишние пробелы) для модели A; list() с compatible_with_printer_device_id=Some(device_id) всё равно возвращает картридж модели A — подтверждает регистронезависимое + TRIM сравнение (D-03).
  </behavior>
  <action>
    Удалить printer_compatib_round_trip_both_directions целиком (тестирует удалённые SqlitePrinterRepository::get_compatible_model_ids/get_compatible_device_ids — методы отсутствуют после Plan 13-02, тест не подлежит адаптации, только удалению и замене новым).

    Переписать printer_compatib_list_narrows_to_linked_model: убрать блок writer.execute(...) с SqlitePrinterRepository::set_compatible_models_in_tx; вместо него передать compatibility: vec!["{exact device name}".into()] прямо в CartridgeModelCreateDto при создании модели A (seed_model() хелпер создаёт модель с compatibility: vec![] — для этого теста создать модель A напрямую через svc.model_create(...) с непустым compatibility, как уже делает model B в соседних тестах). Оставить seed_printer_device для получения device_id и его конкретного name. Остальная assert-логика (resp.items.len() == 1, resp.items[0].model_id == model_a) не меняется.

    Переписать printer_compatib_unconfigured_device_does_not_narrow: убедиться, что обе модели создаются с compatibility: vec![] (уже так — seed_model() и второй model_create оба передают compatibility: vec![]), убрать любые остаточные упоминания printer_cartridge_models из комментариев модуля (doc-комментарий файла, строки 1-7, и doc-комментарий теста, строки ~352-354) — заменить на актуальное описание (V005 printer_name pass-through, D-05).

    Добавить printer_compatib_case_insensitive_match: новый тест по описанному в <behavior> сценарию, использующий make_cartridge_service_with_handles + seed_printer_device + model_create с разным регистром/пробелами в compatibility.

    Обновить doc-комментарий модуля (строки 1-7) и imports (убрать use trackly_infra::repos::printers_sqlite::SqlitePrinterRepository и use trackly_core::ports::printers::PrinterRepository, если они становятся неиспользуемыми после удаления round_trip теста — проверить, использует ли что-то ещё в файле эти импорты, прежде чем удалять).
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test cartridges_crud printer_compatib</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p trackly-app --test cartridges_crud` проходит полностью (все тесты файла, не только printer_compatib_*).
    `grep -n "set_compatible_models_in_tx\|get_compatible_model_ids\|get_compatible_device_ids\|printer_cartridge_models" crates/trackly-app/tests/cartridges_crud.rs` возвращает пусто.
    `grep -c "fn printer_compatib_" crates/trackly-app/tests/cartridges_crud.rs` равен 3 (list_narrows, unconfigured_does_not_narrow, case_insensitive_match).
  </acceptance_criteria>
  <done>cartridges_crud.rs компилируется и проходит; printer_compatib_* тесты подтверждают V005 printer_name сужение, pass-through (D-05) и регистронезависимое+TRIM сравнение (D-03) без единой ссылки на удалённую V029-инфраструктуру.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: suggest_compat_printer — автокомплит из devices.name (D-06)</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs, crates/trackly-app/src/tauri_cmds/cartridges.rs, crates/trackly-app/src/http/cartridges.rs</files>
  <read_first>
    crates/trackly-app/src/services/cartridge_service.rs
    crates/trackly-app/src/tauri_cmds/cartridges.rs
    crates/trackly-app/src/http/cartridges.rs
  </read_first>
  <behavior>
    - suggest_compat_printer("Pant", ...) с существующим в devices принтером "Pantum Office-2000" (type_id=2) → возвращает ["Pantum Office-2000", ...] среди результатов.
    - suggest_compat_printer с prefix, не совпадающим ни с одним devices.name → возвращает пустой Vec (фронтенд показывает «Нет совпадений — будет сохранено как есть», свободный ввод всё равно сохраняется при submit — это уже фронтендовая, не серверная логика).
    - Принтеры с type_id != 2 (например, обычные устройства) НЕ попадают в подсказки.
  </behavior>
  <action>
    В cartridge_service.rs изменить сигнатуру suggest_compat_printer: убрать параметр field: String (старая семантика "printer_brand"|"printer_model" больше не применима к единой колонке printer_name) — новая сигнатура pub async fn suggest_compat_printer(&self, prefix: String) -> Result<Vec<String>, AppError>. Реализация: self.readers.acquire() + spawn_blocking + SQL "SELECT DISTINCT name FROM devices WHERE type_id = 2 AND deleted_at_utc IS NULL AND name LIKE ?1 || '%' ORDER BY name LIMIT 20" (следуя простому паттерну suggest_brand, не сложному generic-builder из devices_sqlite.rs — этот случай не нуждается в множественных условиях). Параметризовать prefix через bind-параметр (никакой конкатенации в текст SQL).

    В tauri_cmds/cartridges.rs обновить build_cartridges_suggest_compat_printer(ctx, caller, prefix: String) -> Result<Vec<String>, AppError> — убрать параметр field, обновить вызов ctx.cartridges.suggest_compat_printer(prefix).await. Обновить #[tauri::command] pub async fn cartridges_suggest_compat_printer(state, prefix: String) -> ... — убрать параметр field из сигнатуры команды.

    В http/cartridges.rs обновить SuggestCompatPayload { pub prefix: String } (убрать pub field: String), обновить handler_suggest_compat_printer вызов build_cartridges_suggest_compat_printer(&ctx, &identity, p.prefix) (без p.field).

    Обновить specta_export.rs реестр не требуется (имя команды cartridges_suggest_compat_printer не меняется, только её сигнатура параметров) — но регенерация bindings.ts обязательна (см. acceptance_criteria), так как меняется shape входного payload.
  </action>
  <verify>
    <automated>cargo test -p trackly-app suggest_compat_printer</automated>
  </verify>
  <acceptance_criteria>
    `cargo build -p trackly-app` проходит.
    `grep -n "field: String\|field:.*printer_brand\|field:.*printer_model" crates/trackly-app/src/services/cartridge_service.rs crates/trackly-app/src/tauri_cmds/cartridges.rs crates/trackly-app/src/http/cartridges.rs` возвращает пусто (параметр field полностью удалён из всех трёх слоёв).
    `grep -n "type_id = 2" crates/trackly-app/src/services/cartridge_service.rs` находит новый SQL-источник подсказок.
    `cd crates/trackly-app && cargo test export_bindings 2>&1 | tail -5` завершается успешно; бинд cartridges_suggest_compat_printer в ui/src/bindings.ts больше не принимает поле field.
  </acceptance_criteria>
  <done>suggest_compat_printer подсказывает реальные имена принтеров (devices.name, type_id=2) без устаревшего параметра field; bindings.ts регенерирован под новую сигнатуру.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| axum HTTP -> suggest_compat_printer | free-text prefix от LAN-браузера попадает в LIKE-запрос против devices.name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-10 | Tampering | suggest_compat_printer SQL (prefix bind) | mitigate | prefix передаётся через rusqlite params!, не конкатенируется в текст запроса — LIKE ?1 || '%' использует bind-параметр для самого значения, оператор || применяется к литералу '%', не к пользовательскому вводу |
| T-13-11 | Information Disclosure | suggest_compat_printer раскрывает devices.name всем ролям с ReadData | accept | Имена принтеров не являются секретом — те же данные уже доступны через printers_list/printers_get для тех же ролей |
</threat_model>

<verification>
cargo test -p trackly-app --test cartridges_crud
cargo build -p trackly-app
cargo clippy -p trackly-app -- -D warnings
</verification>

<success_criteria>
cartridges_crud.rs компилируется и проходит со всеми тестами переписанными на V005 семантику; suggest_compat_printer работает на роستере devices.name вместо истории cartridge_model_compatibility.
</success_criteria>

<output>
Create `.planning/phases/13-per-device-junction-chip-drum-state/13-05-SUMMARY.md` when done
</output>
