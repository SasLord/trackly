---
phase: 13-per-device-junction-chip-drum-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V032__cartridge_model_compatibility_printer_name.sql
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-core/src/domain/cartridges.rs
autonomous: true
requirements: [SPEC-13-R1, SPEC-13-R2]
must_haves:
  truths:
    - "Существующие строки cartridge_model_compatibility (V005) сохраняются после миграции — TRIM(printer_brand || ' ' || printer_model) переносится в новую колонку printer_name, ни одна строка не теряется"
    - "Таблица printer_cartridge_models (V029) больше не существует в схеме после миграции"
    - "Список картриджей, отфильтрованный по printer_device_id, сопоставляет по cartridge_model_compatibility.printer_name через devices.name (регистронезависимо, с обрезкой пробелов), а не через V029-таблицу"
    - "Если для модели картриджа нет строк совместимости — модель проходит ФИЛЬТР ВЫБОРА КАРТРИДЖА для ЛЮБОГО принтера (pass-through, D-05, ограничено только list()-фильтром)"
    - "compatible_model_aggregates НЕ применяет pass-through: модель без совпадающей строки cartridge_model_compatibility отсутствует в агрегате карточки принтера (R4 — данные агрегата совпадают строго с V005-связями)"
  artifacts:
    - path: "migrations/V032__cartridge_model_compatibility_printer_name.sql"
      provides: "Схема: printer_name вместо printer_brand+printer_model; DROP printer_cartridge_models"
      contains: "PRAGMA user_version = 32"
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "upsert_compatibility_in_tx/get_compatibility на Vec<String>; list() фильтрует по devices.name"
  key_links:
    - from: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      to: "cartridge_model_compatibility.printer_name"
      via: "parameterized SQL (rusqlite params!)"
      pattern: "printer_name"
---

<objective>
Создать миграцию V032 (схема: схлопнуть printer_brand+printer_model в единую free-text колонку printer_name в V005 cartridge_model_compatibility, перенести существующие данные, DROP TABLE printer_cartridge_models из V029) и переписать репозиторий картриджей (cartridges_sqlite.rs) на работу с новой схемой: совместимость как Vec<String>, фильтрация списка картриджей по принтеру через сопоставление printer_name с devices.name (регистронезависимо, с обрезкой пробелов), с pass-through при пустом наборе совместимости (D-05).

Purpose: это фундаментальная схемная и репозиторная основа для всего Phase 13 — все остальные планы (printer-репозиторий, сервисный слой, фронтенд) зависят от существования новой колонки printer_name и переписанных функций совместимости.
Output: применённая миграция V032, cartridges_sqlite.rs с новой сигнатурой совместимости и переписанным list()-фильтром.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-SPEC.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-CONTEXT.md

<interfaces>
<!-- Existing V005 schema (migrations/V005__cartridges.sql) — TO BE ALTERED -->
CREATE TABLE cartridge_model_compatibility (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  cartridge_model_id  INTEGER NOT NULL REFERENCES cartridge_models(id) ON DELETE CASCADE,
  printer_brand       TEXT    NOT NULL,
  printer_model       TEXT    NOT NULL
);

<!-- Existing V029 schema (migrations/V029__printer_cartridge_models.sql) — TO BE DROPPED -->
CREATE TABLE printer_cartridge_models (
    id                 INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id          INTEGER NOT NULL REFERENCES devices(id) ON DELETE CASCADE,
    cartridge_model_id INTEGER NOT NULL REFERENCES cartridge_models(id) ON DELETE CASCADE,
    created_at_utc     INTEGER NOT NULL
);
CREATE UNIQUE INDEX idx_printer_cartridge_models_unique ON printer_cartridge_models(device_id, cartridge_model_id);
CREATE INDEX idx_printer_cartridge_models_model ON printer_cartridge_models(cartridge_model_id);

<!-- House style for column-drop rebuild migrations (V030/V031 precedent) -->
PRAGMA foreign_keys = OFF;
CREATE TABLE {table}_new ( ... );
INSERT INTO {table}_new (...) SELECT ... FROM {table};
DROP TABLE {table};
ALTER TABLE {table}_new RENAME TO {table};
PRAGMA foreign_keys = ON;
PRAGMA user_version = N;

<!-- crates/trackly-infra/src/db/migrations.rs — test infra is dynamic, no test file changes needed -->
pub fn max_known_version() -> u32  // computed at runtime from embedded migrations
pub fn run(conn: &mut Connection) -> Result<MigrationReport, AppError>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V032 миграция — printer_name + DROP printer_cartridge_models</name>
  <files>migrations/V032__cartridge_model_compatibility_printer_name.sql</files>
  <read_first>
    migrations/V005__cartridges.sql
    migrations/V029__printer_cartridge_models.sql
    migrations/V030__printers_drop_connectivity_check.sql
    migrations/V031__requests_status_add_cancelled.sql
    crates/trackly-infra/src/db/migrations.rs
  </read_first>
  <action>
    Создать migrations/V032__cartridge_model_compatibility_printer_name.sql по образцу rebuild-паттерна V030/V031 (PRAGMA foreign_keys = OFF, create-new-table + copy + drop + rename, PRAGMA foreign_keys = ON, PRAGMA user_version = 32 в конце файла).

    Шаг А — пересборка cartridge_model_compatibility: создать cartridge_model_compatibility_new(id INTEGER PRIMARY KEY AUTOINCREMENT, cartridge_model_id INTEGER NOT NULL REFERENCES cartridge_models(id) ON DELETE CASCADE, printer_name TEXT NOT NULL). Перенести данные: INSERT INTO cartridge_model_compatibility_new (id, cartridge_model_id, printer_name) SELECT id, cartridge_model_id, TRIM(printer_brand || ' ' || printer_model) FROM cartridge_model_compatibility — ни одна существующая строка не должна потеряться (D-02). DROP TABLE cartridge_model_compatibility; ALTER TABLE cartridge_model_compatibility_new RENAME TO cartridge_model_compatibility.

    Шаг Б — добавить индекс для будущих lookup'ов по cartridge_model_id (если такого нет в текущей схеме — его и не было, не добавлять лишнего: оставить как было, без новых индексов, если SPEC/CONTEXT явно не требует).

    Шаг В — DROP TABLE printer_cartridge_models (V029) вместе с её индексами (idx_printer_cartridge_models_unique, idx_printer_cartridge_models_model падают автоматически вместе с таблицей в SQLite — отдельных DROP INDEX не требуется). printer_cartridge_models ссылается на devices(id), НЕ на printers(id) — её DROP не пересекается с шагом А.

    Объединить оба шага в одном файле V032 (один PRAGMA foreign_keys OFF/ON блок на весь файл, refinery выполняет файл в одной транзакции). Завершить файл строкой PRAGMA user_version = 32;. Добавить заголовочный комментарий в стиле V030/V031, перечисляющий: что мигрирует (R1, R2 из 13-SPEC.md), что переносится (printer_name = TRIM(brand||' '||model)), что дропается (printer_cartridge_models), и что миграция должна быть безопасна на БД, где уже применены и V005, и V029 (идемпотентность всей цепочки проверяется существующим migration_idempotency.rs/migrations.rs::tests без изменений — они динамически читают max_known_version()).
  </action>
  <verify>
    <automated>cargo test -p trackly-infra --lib db::migrations</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p trackly-infra --lib db::migrations` проходит (run_applies_all_known_migrations_on_fresh_db, max_known_version_returns_current, run_is_idempotent_on_same_connection — все динамические, видят V032 автоматически).
    `cargo test -p trackly-infra --test migration_idempotency` проходит (migrations_are_idempotent_and_wal_persists_across_reopens) — подтверждает, что применение всех миграций (включая V032) на свежей БД и повторный прогон работают идемпотентно.
    `grep -c "PRAGMA user_version = 32" migrations/V032__cartridge_model_compatibility_printer_name.sql` равен 1.
    `grep -c "printer_brand\|printer_model" migrations/V032__cartridge_model_compatibility_printer_name.sql` равен 0 в финальной схеме новой таблицы (старые имена колонок встречаются только в SELECT-источнике переноса данных, не в CREATE TABLE _new).
  </acceptance_criteria>
  <done>V032 применяется на чистой БД и на БД с уже примененными V005+V029; существующие совместимости конвертированы без потери строк; printer_cartridge_models отсутствует в схеме.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: cartridges_sqlite.rs — совместимость на Vec&lt;String&gt;</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/cartridges_sqlite.rs
    crates/trackly-core/src/domain/cartridges.rs
  </read_first>
  <action>
    Переписать upsert_compatibility_in_tx (строки ~283-304) и get_compatibility (строки ~307-330) в cartridges_sqlite.rs под новую схему с единой колонкой printer_name.

    upsert_compatibility_in_tx(tx, model_id, names: &[String]): сигнатура меняется с pairs: &[(String, String)] на names: &[String]. DELETE FROM cartridge_model_compatibility WHERE cartridge_model_id = ?1 остаётся; цикл INSERT меняется на "INSERT INTO cartridge_model_compatibility (cartridge_model_id, printer_name) VALUES (?1, ?2)" с params![model_id, name] на каждое имя из names (D-04: значения сохраняются как введены — TRIM применяется при СРАВНЕНИИ в фильтре, не обязательно при записи, если CONTEXT.md явно не требует TRIM при записи — действовать по D-02/D-03/D-04, не добавлять собственную нормализацию записи сверх того, что зафиксировано в 13-CONTEXT.md).

    get_compatibility(conn, model_id) -> Result<Vec<String>, AppError>: SELECT printer_name FROM cartridge_model_compatibility WHERE cartridge_model_id = ?1 ORDER BY printer_name ASC; собрать в Vec<String> (убрать tuple-маппинг (String,String)).

    Переписать оба фильтра в impl CartridgeRepository for SqliteCartridgeRepository::list() (COUNT-запрос ~строки 1085-1110 и основной SELECT ~строки 1112-1129), убрав ссылки на printer_cartridge_models. ВАЖНО — ГРАНИЦА ИЗМЕНЕНИЙ: в обоих SQL-блоках сосуществуют условие `?5` (installable_only — фильтр installable-набора по kind/state_id, строки ~1093-1096/~1119-1122) и условие `?6` (compatible_with_printer_device_id). Условие `?5` НЕ ТРОГАТЬ — оставить байт-в-байт как есть (D-12: installable-фильтр `(?5 = 0 OR (c.status_id = 1 AND ((m.kind_id = 1 AND c.state_id IN (1, 2)) OR (m.kind_id = 2 AND c.state_id IN (4, 5)))))` должен сохраниться без единого изменения символа — переписывается ТОЛЬКО соседнее условие `?6`). Новое условие для compatible_with_printer_device_id (?6, остаётся Option<i64>, тип параметра в CartridgeFilter не меняется — param остаётся device_id, меняется только внутренний SQL): "(?6 IS NULL OR NOT EXISTS (SELECT 1 FROM cartridge_model_compatibility cmc WHERE cmc.cartridge_model_id = c.model_id) OR EXISTS (SELECT 1 FROM cartridge_model_compatibility cmc JOIN devices d ON d.id = ?6 WHERE cmc.cartridge_model_id = c.model_id AND LOWER(TRIM(cmc.printer_name)) = LOWER(TRIM(d.name))))" — пройти по D-03 (регистронезависимое + TRIM сравнение) и D-05 (пустой набор строк совместимости для модели = pass-through, NOT EXISTS ветка — это D-05 pass-through ограничено ИМЕННО этим фильтром выбора картриджа, не распространяется на агрегаты, см. ниже). Применить идентичное условие `?6` в обоих запросах (COUNT и SELECT), как было с printer_cartridge_models ранее — сохранить параметризацию (никакой конкатенации free-text в SQL, ?6 остаётся bind-параметром).

    Добавить новый инherent-метод compatible_model_aggregates(&self, conn: &Connection, printer_device_id: i64) -> Result<Vec<CompatibleModelAggregate>, AppError> на impl SqliteCartridgeRepository (рядом с get_compatibility) — нужен Task для R4 (Plan 13-03), но контракт типа объявляется здесь, так как этот файл — источник истины для cartridge-репозитория. Добавить тип CompatibleModelAggregate { model_id: i64, brand: String, model: String, in_stock: i64, at_refill: i64, in_use: i64 } в crates/trackly-core/src/domain/cartridges.rs (рядом с LowStockItem) и реализовать SQL: для printer_device_id найти devices.name через JOIN devices d ON d.id = ?1, затем включить в агрегат ТОЛЬКО те cartridge_model_id, у которых есть ХОТЯ БЫ ОДНА строка в cartridge_model_compatibility с printer_name матчащим LOWER(TRIM())=LOWER(TRIM(d.name)) — НЕ применять здесь D-05 pass-through (pass-through из D-05 ограничен исключительно фильтром выбора картриджа выше (`?6` в list()) и НЕ распространяется на агрегат карточки принтера — R4/D-07 требуют, чтобы агрегат отражал ТОЛЬКО реальные V005-связи для этого принтера, иначе пустой агрегат («Нет совместимых моделей картриджей.») стал бы недостижим вопреки UI-SPEC). Для каждой найденной модели сгруппировать живые (deleted_at_utc IS NULL) cartridges по model_id и посчитать COUNT по status_id IN (1,2,3) раздельно (1=На складе, 2=В работе, 3=На заправке; 4=Списано исключить — D-07). Модели без единой совпадающей строки совместимости в агрегат НЕ попадают. Сортировка результата — по brand ASC, model ASC (предсказуемый порядок для UI).
  </action>
  <verify>
    <automated>cargo build -p trackly-infra</automated>
  </verify>
  <acceptance_criteria>
    `cargo build -p trackly-infra` проходит без ошибок типов.
    `grep -n "printer_cartridge_models" crates/trackly-infra/src/repos/cartridges_sqlite.rs` возвращает пусто (0 строк).
    `grep -n "printer_brand\|printer_model" crates/trackly-infra/src/repos/cartridges_sqlite.rs` возвращает пусто.
    `grep -n "fn compatible_model_aggregates" crates/trackly-infra/src/repos/cartridges_sqlite.rs` находит новый метод.
    `grep -n "m.kind_id = 1 AND c.state_id IN (1, 2)" crates/trackly-infra/src/repos/cartridges_sqlite.rs` находит ровно 2 совпадения (COUNT-запрос + основной SELECT) — подтверждает, что условие `?5` installable_only сохранено byte-for-byte в обоих местах (D-12 guard).
    compatible_model_aggregates НЕ содержит pass-through ветки (никакого `OR NOT EXISTS` / `model without compatibility rows included` в SQL агрегата) — модель без единой совпадающей строки совместимости отсутствует в результате, что делает достижимым пустой агрегат («Нет совместимых моделей картриджей.», R4/UI-SPEC).
    Существующий юнит-тест `params_are_parameterized_not_concatenated` (строка ~1568) продолжает проходить без модификации логики проверки (подтверждает, что новый SQL для printer_name остаётся параметризованным).
  </acceptance_criteria>
  <done>cartridges_sqlite.rs компилируется; upsert_compatibility_in_tx/get_compatibility работают с Vec&lt;String&gt;; list() фильтрует по printer_name через devices.name с pass-through; новый агрегатный метод доступен для Plan 13-03.</done>
</task>

<task type="auto">
  <name>Task 3: domain/cartridges.rs — типы и doc-комментарии под V005-only схему</name>
  <files>crates/trackly-core/src/domain/cartridges.rs</files>
  <read_first>
    crates/trackly-core/src/domain/cartridges.rs
  </read_first>
  <action>
    Добавить структуру CompatibleModelAggregate { pub model_id: i64, pub brand: String, pub model: String, pub in_stock: i64, pub at_refill: i64, pub in_use: i64 } рядом с LowStockItem (контракт, который Task 2 уже использует в cartridges_sqlite.rs — этот task фиксирует тип на core-уровне). Обновить doc-комментарий поля compatible_with_printer_device_id в CartridgeFilter (строки ~225-228): убрать упоминание "через printer_cartridge_models", заменить на описание V005-схемы — "ограничивает выборку моделями, чья cartridge_model_compatibility.printer_name совпадает (регистронезависимо, с TRIM) с devices.name связанного принтера; пустой набор строк совместимости для модели не сужает выборку (D-05)". Не менять тип поля (остаётся Option<i64>, семантика параметра как device_id не меняется).
  </action>
  <verify>
    <automated>cargo build -p trackly-core</automated>
  </verify>
  <acceptance_criteria>
    `cargo build -p trackly-core` проходит без ошибок.
    `grep -n "printer_cartridge_models" crates/trackly-core/src/domain/cartridges.rs` возвращает пусто.
    `grep -n "struct CompatibleModelAggregate" crates/trackly-core/src/domain/cartridges.rs` находит новый тип.
  </acceptance_criteria>
  <done>CompatibleModelAggregate определён в core; doc-комментарии CartridgeFilter отражают V005-only семантику без упоминания V029.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| axum HTTP -> cartridges_sqlite.rs | free-text printer_name приходит от LAN-браузера через JSON-payload, далее напрямую в SQL-фильтр и в новую таблицу cartridge_model_compatibility |
| migration runner -> существующая БД | V032 выполняет DROP TABLE на продуктивных данных пользователя — необратимая операция |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-01 | Tampering | cartridges_sqlite.rs list()/compatible_model_aggregates SQL | mitigate | Весь printer_name участвует в SQL только через rusqlite `params![...]` bind-параметры — никакой строковой конкатенации free-text в SQL-тексте (проверяется существующим тестом `params_are_parameterized_not_concatenated`) |
| T-13-02 | Repudiation/Data-loss | V032 migration DROP TABLE printer_cartridge_models | accept | Намеренное необратимое удаление, одобрено в 13-SPEC.md (R1); миграция идемпотентна и протестирована на уже мигрированных БД через max_known_version()-based тесты — повторный запуск безопасен, повторного DROP не происходит на уже примененной версии |
| T-13-03 | Tampering | V032 миграция на БД с частично прикладными версиями | mitigate | rebuild-паттерн (create _new, copy, drop, rename) внутри одной refinery-транзакции; PRAGMA foreign_keys=OFF/ON ограничены областью одного файла миграции, не overlap с пользовательским трафиком (refinery run_grouped(false)) |
</threat_model>

<verification>
cargo test -p trackly-infra --lib db::migrations
cargo test -p trackly-infra --test migration_idempotency
cargo build -p trackly-infra -p trackly-core
cargo clippy -p trackly-infra -p trackly-core -- -D warnings
</verification>

<success_criteria>
V032 применяется чисто на свежей и на уже-мигрированной (V005+V029) БД, не теряя существующих строк совместимости. cartridges_sqlite.rs больше не ссылается на printer_cartridge_models/printer_brand/printer_model. Новый агрегатный метод доступен для последующих планов.
</success_criteria>

<output>
Create `.planning/phases/13-per-device-junction-chip-drum-state/13-01-SUMMARY.md` when done
</output>
