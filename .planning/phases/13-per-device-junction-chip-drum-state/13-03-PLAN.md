---
phase: 13-per-device-junction-chip-drum-state
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - crates/trackly-app/src/tauri_cmds/cartridges.rs
  - crates/trackly-app/src/http/cartridges.rs
  - crates/trackly-app/src/tauri_cmds/printers.rs
  - crates/trackly-app/src/http/printers.rs
  - crates/trackly-app/src/dto/printer.rs
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: true
requirements: [SPEC-13-R1, SPEC-13-R4]
must_haves:
  truths:
    - "Tauri-команды cartridge_models_get_compatible_devices, cartridge_models_set_compatible_devices, printers_get_compatible_models, printers_set_compatible_models больше не существуют ни в одном транспорте (Tauri invoke и axum HTTP)"
    - "Новая команда чтения агрегатов совместимости для принтера (R4) доступна через ОБА транспорта, защищена тем же authorize(&Action::ReadData), что printers_get/cartridges_get"
    - "role_endpoint_matrix.rs не содержит тест-кейсов на удалённые команды; новая read-команда покрыта аналогичным 403-тестом для роли Employee"
  artifacts:
    - path: "crates/trackly-app/src/dto/printer.rs"
      provides: "PrinterCompatibleAggregatesDto { device_id, models: Vec<CompatibleModelAggregateDto> }"
    - path: "crates/trackly-app/src/tauri_cmds/printers.rs"
      provides: "build_printers_get_compatible_aggregates + printers_get_compatible_aggregates (#[tauri::command])"
    - path: "crates/trackly-app/src/http/printers.rs"
      provides: "POST /api/v1/printers_get_compatible_aggregates"
  key_links:
    - from: "ui/src/bindings.ts"
      to: "crates/trackly-app/src/specta_export.rs"
      via: "cargo test export_bindings regenerates bindings.ts from collect_commands!"
      pattern: "printers_get_compatible_aggregates"
---

<objective>
Удалить весь Tauri/HTTP command surface для V029 per-device junction compatibility (4 команды: cartridge_models_get_compatible_devices, cartridge_models_set_compatible_devices, printers_get_compatible_models, printers_set_compatible_models) из обоих транспортов, из specta_export.rs реестра и из role_endpoint_matrix.rs; добавить НОВУЮ read-only команду printers_get_compatible_aggregates (R4 — агрегаты совместимых моделей картриджей по статусам На складе/На заправке/В работе для конкретного принтера), реализованную через ОБА транспорта с тем же authorize(&Action::ReadData) гейтом, что у printers_get, и зарегистрированную в specta_export.rs + покрытую RBAC-тестом.

Purpose: R1 требует удаления Rust-кода чтения/записи V029 не только из repo (Plan 13-01/13-02), но и из командной поверхности (Tauri+HTTP+specta+RBAC-тесты) — этот план закрывает оставшуюся часть R1 и весь R4 (бэкенд-половина: фронтенд-блок агрегатов идёт в Plan 13-06).
Output: bindings.ts регенерируется с новой командой printers_get_compatible_aggregates и без 4 удалённых команд; role_endpoint_matrix.rs актуализирован.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-SPEC.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-CONTEXT.md

<interfaces>
<!-- crates/trackly-core/src/domain/cartridges.rs (Plan 13-01, Task 3) -->
pub struct CompatibleModelAggregate {
    pub model_id: i64, pub brand: String, pub model: String,
    pub in_stock: i64, pub at_refill: i64, pub in_use: i64,
}

<!-- crates/trackly-infra/src/repos/cartridges_sqlite.rs (Plan 13-01, Task 2) -->
impl SqliteCartridgeRepository {
    pub fn compatible_model_aggregates(&self, conn: &Connection, printer_device_id: i64) -> Result<Vec<CompatibleModelAggregate>, AppError>;
}

<!-- Existing dual-transport pattern to mirror exactly (printers_get_by_device_id, the most
     recent precedent — Phase 12 Plan 21) -->
// tauri_cmds/printers.rs:
pub async fn build_printers_get_by_device_id(ctx: &AppCtx, caller: &Identity, device_id: i64) -> Result<PrinterDto, AppError> {
    authorize(caller, &Action::ReadData)?;
    ctx.printers.get_by_device_id(device_id).await
}
#[tauri::command] #[specta::specta]
pub async fn printers_get_by_device_id(state: tauri::State<'_, AppCtx>, device_id: i32) -> Result<PrinterDto, AppError> {
    let caller = resolve_tauri_identity(state.inner()).await?;
    build_printers_get_by_device_id(state.inner(), &caller, device_id as i64).await
}
// http/printers.rs:
#[derive(serde::Deserialize)] #[serde(rename_all = "camelCase")]
pub struct GetByDeviceIdPayload { pub device_id: i32 }
pub async fn handler_get_by_device_id(State(ctx): State<AppCtx>, session: Session, Json(p): Json<GetByDeviceIdPayload>) -> Result<Json<PrinterDto>, AppErrorResponse> { ... }
// router(): .route("/api/v1/printers_get_by_device_id", post(handler_get_by_device_id))

<!-- role_endpoint_matrix.rs exact test-case pattern (Case 33 being deleted, Case 40 precedent kept) -->
{
    let status = post_with_cookie(new_app!(), "/api/v1/printers_get_compatible_models", json!({ "deviceId": 1 }), Some(&employee_cookie)).await;
    assert_eq!(status, StatusCode::FORBIDDEN, "Case 33: ... expected 403, got {status}");
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Удалить 4 V029 команды из tauri_cmds + http + specta_export (оба домена)</name>
  <files>crates/trackly-app/src/tauri_cmds/cartridges.rs, crates/trackly-app/src/http/cartridges.rs, crates/trackly-app/src/tauri_cmds/printers.rs, crates/trackly-app/src/http/printers.rs, crates/trackly-app/src/specta_export.rs</files>
  <read_first>
    crates/trackly-app/src/tauri_cmds/cartridges.rs
    crates/trackly-app/src/http/cartridges.rs
    crates/trackly-app/src/tauri_cmds/printers.rs
    crates/trackly-app/src/http/printers.rs
    crates/trackly-app/src/specta_export.rs
  </read_first>
  <action>
    В tauri_cmds/cartridges.rs удалить: build_cartridge_models_get_compatible_devices (~178-189), build_cartridge_models_set_compatible_devices (~192-206), их #[tauri::command] обёртки cartridge_models_get_compatible_devices (~446-453) и cartridge_models_set_compatible_devices (~456-465). Удалить неиспользуемый импорт CartridgeModelCompatibleDevicesDto, если остался.

    В http/cartridges.rs удалить handler_models_get_compatible_devices (~363-376), handler_models_set_compatible_devices (~378-396), их payload-структуры GetCompatibleDevicesPayload (~101-104), SetCompatibleDevicesPayload (~106-110), маршруты в router() (".route(\"/api/v1/cartridge_models_get_compatible_devices\", ...)" и set-вариант, ~491-498), и обновить импорт из tauri_cmds::cartridges (убрать build_cartridge_models_get_compatible_devices/build_cartridge_models_set_compatible_devices из use-списка).

    В tauri_cmds/printers.rs удалить: build_printers_get_compatible_models (~216-227), build_printers_set_compatible_models (~230-244), #[tauri::command] обёртки printers_get_compatible_models (~336-342), printers_set_compatible_models (~346-359).

    В http/printers.rs удалить handler_get_compatible_models (~217-230), handler_set_compatible_models (~232-250), payload-структуры GetCompatibleModelsPayload (~83-85), SetCompatibleModelsPayload (~89-92), маршруты в router() (~272-279), обновить use-импорт из tauri_cmds::printers и убрать PrinterCompatibleModelsDto из use crate::dto::printer::{...}.

    В specta_export.rs убрать 4 строки реестра: crate::tauri_cmds::cartridges::cartridge_models_get_compatible_devices, crate::tauri_cmds::cartridges::cartridge_models_set_compatible_devices, crate::tauri_cmds::printers::printers_get_compatible_models, crate::tauri_cmds::printers::printers_set_compatible_models (и их предшествующие doc-комментарии "Phase 12 gap closure (GAP-12-02 ...)").
  </action>
  <verify>
    <automated>cargo build -p trackly-app</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn "get_compatible_devices\|set_compatible_devices\|get_compatible_models\|set_compatible_models" crates/trackly-app/src/tauri_cmds/ crates/trackly-app/src/http/ crates/trackly-app/src/specta_export.rs` возвращает пусто.
    `cargo build -p trackly-app` проходит без ошибок (после Task 2 этого плана добавляет замену для тех мест UI/тестов, что ссылались на удалённые команды).
  </acceptance_criteria>
  <done>4 команды (model-side get/set, printer-side get/set) удалены из Tauri, HTTP и specta_export.rs — ни одной живой ссылки не остаётся.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Новая read-команда printers_get_compatible_aggregates (R4, оба транспорта)</name>
  <files>crates/trackly-app/src/dto/printer.rs, crates/trackly-app/src/services/cartridge_service.rs, crates/trackly-app/src/tauri_cmds/printers.rs, crates/trackly-app/src/http/printers.rs, crates/trackly-app/src/specta_export.rs, crates/trackly-app/tests/role_endpoint_matrix.rs</files>
  <read_first>
    crates/trackly-app/src/dto/printer.rs
    crates/trackly-app/src/services/cartridge_service.rs
    crates/trackly-app/src/tauri_cmds/printers.rs
    crates/trackly-app/src/http/printers.rs
    crates/trackly-app/src/specta_export.rs
    crates/trackly-app/tests/role_endpoint_matrix.rs
  </read_first>
  <behavior>
    - Employee (роль без ReadData) вызывает printers_get_compatible_aggregates через HTTP → 403 Forbidden, тот же гейт что printers_get/printers_get_by_device_id (проверяется новым кейсом в role_endpoint_matrix.rs).
    - Admin/Manager вызывает printers_get_compatible_aggregates(deviceId) для принтера без совместимых моделей → возвращает models: [] (пустой массив, не ошибка).
    - Для принтера с совместимыми моделями → models содержит CompatibleModelAggregateDto с полями modelId/brand/model/inStock/atRefill/inUse, статус «Списано» не включён в подсчёт (D-07).
  </behavior>
  <action>
    В dto/printer.rs добавить (camelCase, #[serde(rename_all = "camelCase")] — следуя существующей конвенции файла, ОТЛИЧНОЙ от cartridge.rs): pub struct CompatibleModelAggregateDto { pub model_id: i64, pub brand: String, pub model: String, pub in_stock: i64, pub at_refill: i64, pub in_use: i64 } и pub struct PrinterCompatibleAggregatesDto { pub device_id: i64, pub models: Vec<CompatibleModelAggregateDto> }.

    В cartridge_service.rs (домен принадлежит cartridge-репозиторию, т.к. compatible_model_aggregates живёт в cartridges_sqlite.rs из Plan 13-01) добавить async fn compatible_aggregates_for_printer(&self, printer_device_id: i64) -> Result<Vec<CompatibleModelAggregate>, AppError>, реализованную через self.readers.acquire() + spawn_blocking + self.cart_repo.compatible_model_aggregates(&conn, printer_device_id), по образцу model_list/model_get.

    В tauri_cmds/printers.rs добавить build_printers_get_compatible_aggregates(ctx: &AppCtx, caller: &Identity, device_id: i64) -> Result<PrinterCompatibleAggregatesDto, AppError>: authorize(caller, &Action::ReadData)? (та же гейтинг-точка, что printers_get/printers_get_by_device_id — управленческая read-функция, не требует MutatePrinters), затем ctx.cartridges.compatible_aggregates_for_printer(device_id).await?, маппинг CompatibleModelAggregate -> CompatibleModelAggregateDto, обернуть в PrinterCompatibleAggregatesDto { device_id, models }. Добавить #[tauri::command] #[specta::specta] pub async fn printers_get_compatible_aggregates(state: tauri::State<'_, AppCtx>, device_id: i32) -> Result<PrinterCompatibleAggregatesDto, AppError> по образцу printers_get_by_device_id.

    В http/printers.rs добавить #[derive(serde::Deserialize)] #[serde(rename_all = "camelCase")] pub struct GetCompatibleAggregatesPayload { pub device_id: i32 }, handler_get_compatible_aggregates по образцу handler_get_by_device_id, маршрут .route("/api/v1/printers_get_compatible_aggregates", post(handler_get_compatible_aggregates)) в router(), обновить use-импорт build_printers_get_compatible_aggregates из tauri_cmds::printers и PrinterCompatibleAggregatesDto из dto::printer.

    В specta_export.rs добавить crate::tauri_cmds::printers::printers_get_compatible_aggregates в реестр (рядом с другими printers_* командами), с doc-комментарием "Phase 13 (R4) — read-only агрегаты совместимых моделей картриджей по принтеру, заменяет удалённый per-device junction (V029)".

    В role_endpoint_matrix.rs удалить Cases 33-35 (printers_get_compatible_models, printers_set_compatible_models, cartridge_models_set_compatible_devices — три кейса на удалённые команды; если в файле присутствует и кейс на cartridge_models_get_compatible_devices, удалить и его). Добавить новый кейс (после последнего существующего, с комментарием "// Case N (Plan 13-03): Employee → printers_get_compatible_aggregates → 403 Forbidden.") по точному образцу Case 40: post_with_cookie(new_app!(), "/api/v1/printers_get_compatible_aggregates", json!({ "deviceId": 1 }), Some(&employee_cookie)).await, assert_eq!(status, StatusCode::FORBIDDEN, "Case N: ... expected 403, got {status}").
  </behavior>
  <verify>
    <automated>cargo test -p trackly-app --test role_endpoint_matrix</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p trackly-app --test role_endpoint_matrix` проходит, включая новый Case для printers_get_compatible_aggregates.
    `grep -rn "printers_get_compatible_models\|printers_set_compatible_models\|cartridge_models_get_compatible_devices\|cartridge_models_set_compatible_devices" crates/trackly-app/tests/role_endpoint_matrix.rs` возвращает пусто.
    `cd crates/trackly-app && cargo test export_bindings 2>&1 | tail -5` завершается успешно и регенерирует ui/src/bindings.ts с новым типом PrinterCompatibleAggregatesDto и командой printers_get_compatible_aggregates; `grep -c "printers_get_compatible_aggregates" ui/src/bindings.ts` >= 1.
    `grep -c "printers_get_compatible_models\|printers_set_compatible_models" ui/src/bindings.ts` равен 0.
  </acceptance_criteria>
  <done>printers_get_compatible_aggregates существует в обоих транспортах, защищена ReadData, зарегистрирована в specta_export.rs, bindings.ts регенерирован, RBAC-тест на 403 для Employee добавлен, старые 4 команды полностью отсутствуют из тестовой матрицы.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| axum HTTP -> printers_get_compatible_aggregates | LAN-браузер любого аутентифицированного пользователя может вызвать read-команду; авторизация должна блокировать роль Employee до похода в БД |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-06 | Elevation of Privilege | printers_get_compatible_aggregates (новая команда) | mitigate | authorize(caller, &Action::ReadData)? вызывается ПЕРВОЙ строкой build_-функции, до любого обращения к БД — идентично паттерну printers_get/printers_get_by_device_id; покрыто блокирующим RBAC-тестом (role_endpoint_matrix.rs) |
| T-13-07 | Tampering | удаление 4 команд без orphan-ссылок | mitigate | `cargo build` как acceptance-gate гарантирует отсутствие compile-time orphan-ссылок на удалённые build_*/handler_* функции в обоих транспортах |
</threat_model>

<verification>
cargo build -p trackly-app
cargo test -p trackly-app --test role_endpoint_matrix
cargo clippy -p trackly-app -- -D warnings
cd crates/trackly-app && cargo test export_bindings
</verification>

<success_criteria>
4 V029-командные поверхности полностью удалены из Tauri+HTTP+specta+RBAC-тестов; новая printers_get_compatible_aggregates работает на обоих транспортах с идентичной авторизацией и покрыта тестом.
</success_criteria>

<output>
Create `.planning/phases/13-per-device-junction-chip-drum-state/13-03-SUMMARY.md` when done
</output>
