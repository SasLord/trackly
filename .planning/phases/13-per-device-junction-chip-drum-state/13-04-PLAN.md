---
phase: 13-per-device-junction-chip-drum-state
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-infra/src/repos/printers_sqlite.rs
autonomous: true
requirements: [SPEC-13-R7, SPEC-13-R8]
must_haves:
  truths:
    - "Авто-возврат предыдущего картриджа при установке нового (D-10/D-11) выбирает состояние 5 «Изношенный» по умолчанию для фотобарабанов (kind_id=2), а не захардкоженное 3 «На заправке»"
    - "Авто-возврат для обычных картриджей (kind_id=1) продолжает выбирать состояние 3 «На заправке» по умолчанию — поведение не регрессирует"
    - "Серверный список принтеров (printers_sqlite.rs::list()) отдаёт принтеры без обрезки .min(200) — D-13 требует uncapped read, парк маленький, пагинация не вводится"
  artifacts:
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "transition_in_tx с kind-aware default state для авто-возврата"
    - path: "crates/trackly-infra/src/repos/printers_sqlite.rs"
      provides: "list() без скрытого обрезания ниже фронтового запроса (D-13: uncapped)"
  key_links:
    - from: "crates/trackly-infra/src/repos/cartridges_sqlite.rs::transition_in_tx"
      to: "prev_current.model_kind_id"
      via: "branch on Option<i64> == Some(2)"
      pattern: "model_kind_id == Some\\(2\\)"
---

<objective>
Закрыть два свёрнутых chip-задачи из 13-SPEC.md: (R7) серверный баг авто-возврата предыдущего картриджа при установке нового — текущий код жёстко берёт state_id=3 («На заправке») независимо от типа картриджа, что для фотобарабана (kind_id=2) даёт несуществующее/неверное состояние; нужно сделать выбор kind-aware (фотобарабан → 5 «Изношенный», D-10). (R8) серверный лимит списка принтеров (.min(200)) меньше фронтендового запроса OperationModal (limit:500) — по D-13 решено сделать чтение списка принтеров uncapped (парк маленький, полная пагинация не вводится), а не просто поднять потолок до другого числа.

Purpose: оба бага — расхождения между фронтендом и устаревшим серверным кодом, который их не поддерживает. R8: фронтенд (OperationModal) уже запрашивает 500 принтеров, но сервер капает на 200 — этот план закрывает только серверную половину. R7: фронтенд OperationModal на момент этого плана ВСЁ ЕЩЁ хардкодит состояния 1/2/3 для авто-возврата предыдущего картриджа (несмотря на то, что `stateOptions`/`DRUM_STATES` уже существуют в файле и используются в ДРУГОМ блоке формы — `return_to_stock`/`from_refill`) — фронтенд-фикс для авто-возврата делается отдельно в Plan 13-08 (после Plan 13-06, той же волны 3, чтобы не делить редактирование OperationModal.svelte между волнами); этот план (13-04) закрывает ТОЛЬКО серверную часть R7 (kind-aware default state в transition_in_tx).
Output: transition_in_tx выбирает kind-aware default state при авто-возврате; printers_sqlite.rs::list() отдаёт полный список принтеров без капа .min(200) (D-13 uncapped read).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-SPEC.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-CONTEXT.md

<interfaces>
<!-- crates/trackly-infra/src/repos/cartridges_sqlite.rs::transition_in_tx — current buggy block -->
if let CartridgeTransitionOp::Install {
    printer_device_id: Some(pid), previous_cartridge_state_id, previous_cartridge_location,
    given_by_name: install_given_by_name, given_to_name: install_given_to_name, ..
} = op
{
    let resolved_state_id = previous_cartridge_state_id.unwrap_or(3); // BUG: hardcoded 3, ignores kind
    let resolved_location = previous_cartridge_location.as_deref().unwrap_or("");
    let previous: Option<(i64, i64)> = tx.query_row(/* find prev cartridge IN_USE on same printer */)...;
    if let Some((prev_id, prev_version)) = previous {
        let prev_current = self.fetch_in_tx(tx, prev_id)?;  // prev_current.model_kind_id available HERE
        let prev_affected = tx.execute("UPDATE cartridges SET status_id=1, state_id=?1, ...", params![resolved_state_id, ...])...;
        // ...
    }
}
// CartridgeRow.model_kind_id: Option<i64> — 1=Картридж, 2=Фотобарабан
// cartridge_states: kind 1 → 1=Полный/2=Частичный/3=Пустой; kind 2 → 4=Новый/5=Изношенный/6=Отработанный

<!-- crates/trackly-infra/src/repos/printers_sqlite.rs::list() — current cap -->
fn list(&self, conn: &Self::Conn, filter: &PrinterFilter, page: &Pagination) -> Result<(Vec<PrinterRow>, u64), AppError> {
    let limit = page.limit.min(200) as i64;  // R8/D-13 target — remove this cap entirely, uncapped read
    ...
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R7 — kind-aware default state для авто-возврата предыдущего картриджа</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/cartridges_sqlite.rs
    crates/trackly-core/src/domain/cartridges.rs
  </read_first>
  <behavior>
    - Тест 1 (новый регрессионный, REGRESSION для R7): устанавливают картридж kind_id=2 (фотобарабан) на принтер, где уже "В работе" другой фотобарабан без явного previous_cartridge_state_id → авто-возвращённый предыдущий фотобарабан получает state_id=5 («Изношенный», D-10), НЕ 3.
    - Тест 2 (существующий, не должен сломаться): установка обычного картриджа (kind_id=1) на принтер с другим картриджем "В работе" без явного previous_cartridge_state_id → авто-возвращённый картридж получает state_id=3 («На заправке») — поведение до фикса сохраняется для kind_id=1.
    - Тест 3 (существующий, не должен сломаться): явный previous_cartridge_state_id в payload — используется как есть, без kind-aware default (override остаётся приоритетным).
  </behavior>
  <action>
    В transition_in_tx переместить вычисление resolved_state_id ПОСЛЕ строки let prev_current = self.fetch_in_tx(tx, prev_id)?; (внутри if let Some((prev_id, prev_version)) = previous блока) — сейчас resolved_state_id вычисляется ДО того, как известен kind предыдущего картриджа. Заменить previous_cartridge_state_id.unwrap_or(3) на previous_cartridge_state_id.unwrap_or_else(|| if prev_current.model_kind_id == Some(2) { 5 } else { 3 }) — фотобарабан по умолчанию «Изношенный» (5, D-10/D-11), обычный картридж по умолчанию «На заправке» (3, текущее поведение без регрессии). resolved_location не зависит от kind — остаётся вычисленным как раньше (previous_cartridge_location.as_deref().unwrap_or("")), можно оставить на исходном месте до query_row или переместить вместе — не принципиально, главное чтобы resolved_state_id использовался только после прочтения prev_current.model_kind_id. Добавить doc-комментарий у этого блока: "R7 (13-SPEC.md): kind-aware default — фотобарабан (kind_id=2) возвращается в state 5 «Изношенный», обычный картридж (kind_id=1) — в state 3 «На заправке» (D-10/D-11/D-12), если previous_cartridge_state_id не передан явно".

    Добавить юнит-тест (рядом с существующими transition_* тестами в #[cfg(test)] mod tests, ~после строки 1512) auto_return_uses_kind_aware_default_state: seed две модели — одну kind_id=1 (Pantum TL-5120X), одну kind_id=2 (фотобарабан — проверить точный способ задания kind_id в seed_model или добавить вариант seed-хелпера с kind_id параметром, если текущий seed_model жёстко на kind_id=1; если необходимо — расширить seed_model сигнатуру с опциональным kind_id или добавить seed_drum_model хелпер локально в тестовом модуле), создать 2 картриджа (один на каждой модели), установить первый "В работе" на принтер X, затем install второго (того же или другого kind — тест должен покрыть именно kind_id=2 случай) на тот же принтер X без previous_cartridge_state_id → assert первый автоматически переведён в state_id=5. Добавить второй тест (или расширить существующий) для kind_id=1 случая, подтверждающий state_id=3 не регрессировал.
  </action>
  <verify>
    <automated>cargo test -p trackly-infra cartridges_sqlite -- auto_return</automated>
  </verify>
  <acceptance_criteria>
    `cargo test -p trackly-infra --lib repos::cartridges_sqlite` проходит, включая новый(е) regression-тест(ы) на kind-aware авто-возврат.
    `grep -n "unwrap_or(3)" crates/trackly-infra/src/repos/cartridges_sqlite.rs` возвращает пусто (захардкоженный 3 заменён на kind-aware branch).
    `grep -n "model_kind_id == Some(2)" crates/trackly-infra/src/repos/cartridges_sqlite.rs` находит новую ветку в transition_in_tx.
    Существующие тесты transition_install_changes_status и transition_wrong_status_returns_validation_error продолжают проходить без модификации их assertions.
  </acceptance_criteria>
  <done>Авто-возврат фотобарабана даёт state_id=5, авто-возврат обычного картриджа даёт state_id=3 (без явного override); явный previous_cartridge_state_id продолжает работать как override; новый regression-тест защищает оба случая.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R8 — uncapped read списка принтеров (D-13)</name>
  <files>crates/trackly-infra/src/repos/printers_sqlite.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/printers_sqlite.rs
    ui/src/features/cartridges/OperationModal.svelte
  </read_first>
  <behavior>
    - Тест (новый регрессионный): seed 250+ принтеров (выше старого капа 200) в БД через writer; list() с filter по умолчанию и page.limit = 500 (либо большим числом) возвращает ВСЕ 250+ принтеров, включая принтер с максимальным id (ранее отсекаемый ORDER BY p.id DESC при capped-запросе) — total count в ответе тоже равен полному числу seed-принтеров.
  </behavior>
  <action>
    В printers_sqlite.rs::list() убрать let limit = page.limit.min(200) as i64; — заменить на let limit = page.limit as i64; (никакого .min(...) — D-13 требует uncapped read, парк маленький, полная пагинация не вводится в этой фазе). Если page.limit имеет тип u64/usize с потенциальным переполнением при приведении к i64 — оставить простое as i64 приведение (тот же паттерн, что и раньше, просто без .min(200)); не вводить новый защитный потолок, так как D-13 явно говорит «отдаёт ВСЕ принтеры без обрезки». Обновить/удалить doc-комментарий рядом со старым .min(200) (если есть) — заменить на: "R8/D-13: uncapped read — фронтенд (OperationModal) запрашивает до 500 принтеров для селектора установки; сервер больше не капит ниже фронтового запроса; полная пагинация — будущая фаза при росте парка выше разумного объёма".

    Добавить юнит-тест list_returns_all_printers_above_old_cap (рядом с существующими printers_sqlite тестами): seed 250 принтеров через writer (используя существующий printer-seed хелпер модуля, если есть, иначе минимальный INSERT INTO devices (type_id=2, name=...) + INSERT INTO printers (device_id=...) в цикле), вызвать list() с page.limit=500, assert resp.0.len() == 250 (или фактическое seed-число) и что среди возвращённых строк присутствует принтер с максимальным seeded id (подтверждает отсутствие отсечения по ORDER BY p.id DESC LIMIT 200).
  </action>
  <verify>
    <automated>cargo test -p trackly-infra printers_sqlite -- list_returns_all_printers</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "page.limit.min(200)\|page.limit.min(" crates/trackly-infra/src/repos/printers_sqlite.rs` возвращает пусто (никакого капа .min(...) не осталось).
    `cargo build -p trackly-infra` проходит.
    `cargo test -p trackly-infra --lib repos::printers_sqlite` проходит, включая новый regression-тест на >200 принтеров.
  </acceptance_criteria>
  <done>printers_sqlite.rs::list() отдаёт полный список принтеров без скрытого обрезания ниже фронтового запроса — D-13 uncapped read закрывает разрыв 500-vs-200 без введения полной пагинации.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| axum HTTP -> printers_sqlite.rs::list() | удаление капа .min(200) увеличивает максимальный объём данных, отдаваемый за один аутентифицированный запрос — не открывает новую авторизационную поверхность (тот же authorize-гейт, что и раньше) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-08 | Denial of Service | printers_sqlite.rs::list() без капа | accept | LAN-only сервер с ограниченным числом устройств в реальной организации (парк ~40, рост до сотен реалистичен, но не unbounded — нет внешнего untrusted-источника, способного раздувать таблицу printers); запрос уже защищён сессионной аутентификацией (D-13 — явное решение пользователя принять uncapped read вместо пагинации на этом этапе) |
| T-13-09 | Tampering | transition_in_tx kind-aware branch | mitigate | branch читает только model_kind_id (Option<i64>, из БД, не из пользовательского free-text) — нет новой инъекционной поверхности, только дополнительная ветка сравнения |
</threat_model>

<verification>
cargo test -p trackly-infra --lib repos::cartridges_sqlite
cargo test -p trackly-infra --lib repos::printers_sqlite
cargo build -p trackly-infra
cargo clippy -p trackly-infra -- -D warnings
</verification>

<success_criteria>
Авто-возврат картриджа при установке нового выбирает kind-aware default state (5 для фотобарабана, 3 для обычного картриджа) только когда previous_cartridge_state_id не передан явно; серверный список принтеров отдаёт полный список без капа .min(200) (D-13 uncapped read), закрывая разрыв с фронтендовым запросом limit:500 в OperationModal.
</success_criteria>

<output>
Create `.planning/phases/13-per-device-junction-chip-drum-state/13-04-SUMMARY.md` when done
</output>
