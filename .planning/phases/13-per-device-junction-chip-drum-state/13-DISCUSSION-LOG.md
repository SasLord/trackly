# Phase 13: Редизайн совместимости Принтеры↔Картриджи + свёрнутые chip-задачи - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-25
**Phase:** 13-per-device-junction-chip-drum-state
**Areas discussed:** Идентификация принтера для матчинга, Хранение V005, Drum-дефолт авто-возврата, Агрегаты карточки принтера, Диалог редактирования устройства, Пустая совместимость, Нормализация матчинга, Кап лимита принтеров (chip-2)

---

## Идентификация принтера для матчинга

| Option | Description | Selected |
|--------|-------------|----------|
| devices.model (одно поле) | Наименование = модель; DISTINCT по devices.model | |
| devices.name (одно поле) | Наименование = name устройства (NOT NULL) | ✓ |
| brand + model (пара) | Сохранить оба столбца V005; матч по паре | |

**User's choice:** devices.name (одно поле)
**Notes:** Привело к follow-up по хранению в двухстолбцовой V005.

---

## Хранение наименования в V005

| Option | Description | Selected |
|--------|-------------|----------|
| printer_model = name, brand пустой | Legacy-столбец не используется; матч printer_model = name | |
| Матч по brand+' '+model | Конкатенация против devices.name | |
| Миграция схемы V005 | Один столбец printer_name вместо brand+model | ✓ |

**User's choice:** Миграция схемы V005
**Notes:** Старые строки сворачиваются в printer_name; чище, больше кода в миграции, но согласуется с моделью «одно поле».

---

## Drum-дефолт авто-возврата (chip-1)

| Option | Description | Selected |
|--------|-------------|----------|
| Изношенный (5) | Безопасный нейтральный дефолт | ✓ |
| Отработанный (6) | Прямой аналог «Пустой»=3; риск преждевременного списания | |
| Новый (4) | Консервативно, но нелогично для использованного барабана | |

**User's choice:** Изношенный (5)
**Notes:** Claude отметил: дефолт 5 попадает в installable-набор drum (фильтр 4,5) — намеренно, не менять.

---

## Агрегаты карточки принтера

| Option | Description | Selected |
|--------|-------------|----------|
| В работе + На складе | Два ключевых статуса (как в примере SPEC) | |
| Все статусы | Включая Списано и др. | |
| В работе + На складе (только устанавливаемые) | На складе только installable | |

**User's choice:** Free-text — «В следующем порядке и эти статусы: На складе + На заправке + В работе»
**Notes:** Итог: статусы 1→3→2 в этом порядке, без Списано (4). Группировка по совместимой модели.

---

## Диалог «Редактирование устройства»

| Option | Description | Selected |
|--------|-------------|----------|
| Переиспользовать DeviceFormModal | Тот же компонент, что в разделе «Устройства» | ✓ |
| Отдельный упрощённый диалог | Новый диалог только с полями принтера | |

**User's choice:** Переиспользовать DeviceFormModal
**Notes:** Открытие с device_id принтера; единый UX/валидация.

---

## Пустая совместимость модели

| Option | Description | Selected |
|--------|-------------|----------|
| Pass-through (показать все) | Модель без записей доступна любому принтеру (семантика D-14) | ✓ |
| Исключить (ничего) | Без записей модель не предлагается ни для одного принтера | |

**User's choice:** Pass-through (показать все)
**Notes:** Сохраняет текущую семантику D-14, не ломает старые данные.

---

## Нормализация матчинга имён

| Option | Description | Selected |
|--------|-------------|----------|
| TRIM + без регистра | NOCASE/LOWER + trim с обеих сторон | ✓ |
| Строгое равенство | Точное байт-в-байт совпадение | |

**User's choice:** TRIM + без регистра
**Notes:** Устойчиво к различиям пробелов/регистра.

---

## Кап лимита принтеров (chip-2)

| Option | Description | Selected |
|--------|-------------|----------|
| Uncapped для этого read | Команда списка принтеров отдаёт все без капа | ✓ |
| Поднять кап до 1000 | Конечный предел ≥ фронтовых 500 | |

**User's choice:** Uncapped для этого read
**Notes:** Пользователь сначала не понимал суть chip-2; Claude пояснил: фронт шлёт limit:500, бэкенд капит min(200) без пагинации → при >200 принтерах принтер с малым id теряется в селекторе установки. Пользователь подтвердил uncapped.

---

## Claude's Discretion

- Форма миграции V032 (create-new + copy + drop для смены столбцов в SQLite), при сохранении строк.
- Новая/параметризованная read-команда для uncapped-списка принтеров (с учётом двойного транспорта + specta + role_endpoint_matrix).
- Конкретная вёрстка блоков на карточке принтера.

## Deferred Ideas

- Нормализованный каталог моделей принтеров (printer_models с FK) — отклонён в пользу free-text V005.
- Автодедупликация/нормализация похожих текстовых наименований принтеров в V005.
- Полная пагинация списка принтеров в OperationModal.
