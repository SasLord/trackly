---
phase: 13-per-device-junction-chip-drum-state
plan: 08
type: execute
wave: 3
depends_on: ["13-03", "13-06"]
files_modified:
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: true
requirements: [SPEC-13-R1, SPEC-13-R2]
must_haves:
  truths:
    - "OperationModal больше не вызывает удалённые printers.getCompatibleModels/cartridges.modelsGetCompatibleDevices (V029 команды, удалены в Plan 13-03)"
    - "Предупреждение «Совместимость не задана» (compatibilityUnconfigured) продолжает корректно показываться при отсутствии V005-совместимости для принтера из preFillPrinterId контекста (D-05 pass-through сохранён в новой реализации)"
    - "Подсветка совместимых принтеров в PrinterSelect (compatibleDeviceIds) продолжает работать для cartridge-центричного install-флоу, теперь на основе V005 printer_name матчинга вместо удалённого per-device lookup"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "D-21 reverse-compatibility lookup переписан на V005 (printer_name match), без обращения к удалённым командам"
  key_links:
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "cartridges.modelsGet(cartridge.model_id)"
      via: "fetch compatibility: string[] for client-side device_id derivation"
      pattern: "modelsGet\\(cartridge\\.model_id\\)"
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "printers.getCompatibleAggregates(preFillPrinterId)"
      via: "replace compatibilityUnconfigured check"
      pattern: "getCompatibleAggregates\\(preFillPrinterId\\)"
---

<objective>
Заменить два места в OperationModal.svelte, вызывающие удалённые в Plan 13-03 V029-команды (printers.getCompatibleModels, cartridges.modelsGetCompatibleDevices), на эквивалентную логику над V005 (D-21 reverse-lookup переход с per-device junction на printer_name матчинг).

Purpose: без этого плана OperationModal.svelte не компилируется (TS ошибки на отсутствующие методы api.ts) после Plan 13-03/13-06 — два эффекта (`compatibilityUnconfigured` warning, `compatibleDeviceIds` подсветка в PrinterSelect) должны сохранить пользовательское поведение, но на новом источнике данных.
Output: OperationModal.svelte компилируется и оба эффекта (warning + highlighting) работают на V005 printer_name семантике.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-SPEC.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-CONTEXT.md

<interfaces>
<!-- ui/src/features/printers/api.ts AFTER Plan 13-06 Task 1 -->
export const printers = {
  list: (filter: PrinterFilter, pagination: Pagination) => apiCall<{ items: PrinterDto[]; total: number }>('printers_list', { filter, pagination }),
  getCompatibleAggregates: (deviceId: number) => apiCall<PrinterCompatibleAggregatesDto>('printers_get_compatible_aggregates', { deviceId }),
};
<!-- PrinterCompatibleAggregatesDto = { models: CompatibleModelAggregateDto[] }; CompatibleModelAggregateDto = { modelId, brand, model, inStock, atRefill, inUse } -->

<!-- ui/src/features/cartridges/api.ts (unmodified by this plan) -->
export const cartridges = {
  modelsGet: (id: number) => apiCall<CartridgeModelDto>('cartridge_models_get', { id }),
};
<!-- CartridgeModelDto.compatibility: string[] (printer names, AFTER Plan 13-02) -->

<!-- Current OperationModal.svelte effects (full bodies read, exact current state) -->
<!-- Effect A (~line 295-311): compatibilityUnconfigured warning -->
$effect(() => {
  if (!(open && op === 'install' && cartridge === null && preFillPrinterId !== undefined)) {
    compatibilityUnconfigured = false; return;
  }
  printers.getCompatibleModels(preFillPrinterId).then((res) => {
    compatibilityUnconfigured = res.modelIds.length === 0;
  }).catch(() => { compatibilityUnconfigured = false; });
});
<!-- Effect B (~line 320-341): compatibleDeviceIds highlighting for PrinterSelect -->
$effect(() => {
  if (!(open && op === 'install' && cartridge !== null && preFillPrinterId === undefined)) {
    printerOptions = []; compatibleDeviceIds = new Set(); return;
  }
  Promise.all([
    printers.list({ status: null, search: null }, { offset: 0, limit: 500 }),
    cartridges.modelsGetCompatibleDevices(cartridge.model_id),
  ]).then(([printersRes, compatRes]) => {
    printerOptions = printersRes.items;
    compatibleDeviceIds = new Set(compatRes.device_ids);
  }).catch(() => { printerOptions = []; compatibleDeviceIds = new Set(); });
});
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Effect A — compatibilityUnconfigured warning на V005 агрегатах</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <read_first>
    ui/src/features/cartridges/OperationModal.svelte
  </read_first>
  <behavior>
    - preFillPrinterId указывает на принтер с >=1 совместимой моделью (V005 printer_name match) → compatibilityUnconfigured становится false (предупреждение не показывается).
    - preFillPrinterId указывает на принтер без единой совместимой модели (D-05 — модели либо не настроены вовсе, либо ни одна не указывает имя этого принтера) → compatibilityUnconfigured становится true (предупреждение показывается) — ТОЛЬКО если ХОТЯ БЫ ОДНА модель в системе имеет непустой compatibility (иначе все модели pass-through совместимы — нет смысла предупреждать о «неконфигурированности» там, где никто вообще не настраивал совместимость; этот нюанс лучше явно отметить в коде, но финальное поведенческое решение оставлено на усмотрение исполнителя при сохранении общего духа «warn when this printer has zero matches» — getCompatibleAggregates.models.length === 0 как прямой эквивалент старого res.modelIds.length === 0 достаточен и не требует доп. эвристики).
    - Ошибка сети → compatibilityUnconfigured = false (fail-safe, поведение не меняется).
  </behavior>
  <action>
    Заменить вызов printers.getCompatibleModels(preFillPrinterId) на printers.getCompatibleAggregates(preFillPrinterId); заменить res.modelIds.length === 0 на res.models.length === 0 (прямая замена — PrinterCompatibleAggregatesDto.models — это именно те модели, что совместимы с данным принтером по V005, эквивалент старого modelIds по семантике «список совместимых моделей для этого устройства»). Обновить doc-комментарий блока (текущие строки ~282-292, ссылающиеся на printer_cartridge_models/12-05's compatible_with_printer_device_id filter) — заменить на актуальное описание: "D-21/R4 (Phase 13): источник — V005 cartridge_model_compatibility через printer_name матчинг (Plan 13-03's printers_get_compatible_aggregates), не per-device junction".
  </action>
  <verify>
    <automated>cd ui && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "OperationModal"</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "getCompatibleModels\b" ui/src/features/cartridges/OperationModal.svelte` возвращает пусто.
    `grep -n "getCompatibleAggregates(preFillPrinterId)" ui/src/features/cartridges/OperationModal.svelte` находит новый вызов.
  </acceptance_criteria>
  <done>compatibilityUnconfigured warning работает на printers_get_compatible_aggregates (V005), без вызова удалённой printers_get_compatible_models команды.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Effect B — compatibleDeviceIds подсветка на V005 printer_name матчинге</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <read_first>
    ui/src/features/cartridges/OperationModal.svelte
  </read_first>
  <behavior>
    - cartridge.model_id указывает на модель с compatibility: ["Pantum Office-2000"] (непустой список); printerOptions содержит принтер с deviceName === "Pantum Office-2000" (точное совпадение после trim+lowercase) → compatibleDeviceIds содержит его deviceId.
    - Модель с compatibility: [] (пустой список, D-05 pass-through) → ВСЕ принтеры из printerOptions считаются совместимыми (compatibleDeviceIds = Set всех deviceId из printerOptions) — сохраняет старое pass-through поведение, которое раньше обеспечивалось backend-фильтром, теперь вычисляется client-side.
    - Сетевая ошибка (modelsGet провалился) → printerOptions = [], compatibleDeviceIds = new Set() (fail-safe, поведение не меняется).
  </behavior>
  <action>
    Заменить cartridges.modelsGetCompatibleDevices(cartridge.model_id) на cartridges.modelsGet(cartridge.model_id) в Promise.all. В .then(([printersRes, modelRes]) => {...}) заменить старую логику (compatRes.device_ids) на client-side derivation: printerOptions = printersRes.items; затем если modelRes.compatibility.length === 0 — compatibleDeviceIds = new Set(printerOptions.map(p => p.deviceId)) (D-05 pass-through — пустая совместимость означает «совместим с любым»); иначе построить normalizedNames = new Set(modelRes.compatibility.map(n => n.trim().toLowerCase())) и compatibleDeviceIds = new Set(printerOptions.filter(p => normalizedNames.has((p.deviceName ?? '').trim().toLowerCase())).map(p => p.deviceId)) (D-03 — case-insensitive + trim матчинг, идентичный серверной логике Plan 13-01).

    Обновить doc-комментарий блока (текущие строки ~313-319, ссылающиеся на printer_cartridge_models/12-20's reverse compatibility lookup) — заменить на: "D-21/R3 (Phase 13): client-side derivation поверх V005 — cartridge.model_id даёт compatibility: string[] (имена принтеров), сопоставляется с printerOptions[].deviceName (case-insensitive+trim, зеркалит серверный матчинг из Plan 13-01) для построения compatibleDeviceIds, используемого PrinterSelect для подсветки".
  </action>
  <verify>
    <automated>cd ui && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "OperationModal"</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "modelsGetCompatibleDevices" ui/src/features/cartridges/OperationModal.svelte` возвращает пусто.
    `grep -n "cartridges.modelsGet(cartridge.model_id)" ui/src/features/cartridges/OperationModal.svelte` находит новый вызов.
    `grep -n "trim().toLowerCase()" ui/src/features/cartridges/OperationModal.svelte` подтверждает case-insensitive+trim матчинг присутствует.
    `cd ui && pnpm exec tsc --noEmit -p tsconfig.json` не выдаёт ошибок, связанных с OperationModal.svelte.
  </acceptance_criteria>
  <done>compatibleDeviceIds вычисляется client-side через V005 compatibility список модели + printerOptions deviceName матчинг (D-03 case-insensitive+trim, D-05 pass-through), PrinterSelect подсветка работает без удалённой modelsGetCompatibleDevices команды.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Браузер (OperationModal client-side matching) | Сопоставление printer_name происходит в браузере над уже полученными (авторизованными) данными — не новая граница доверия, просто перенос вычисления с сервера на клиент для UI-подсветки (не security-релевантно: сервер всё равно явно валидирует совместимость отдельно при installable_only фильтре списка картриджей) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-16 | Tampering | Client-side compatibleDeviceIds — пользователь теоретически может подделать подсветку через devtools | accept | Подсветка — чисто UX-хинт (какие принтеры выделены в PrinterSelect), не блокирует выбор и не обходит серверную валидацию; реальное ограничение совместимости при install происходит через installable_only + cartridges_list фильтр на сервере (не затронут этим планом) |
</threat_model>

<verification>
cd ui && pnpm exec tsc --noEmit -p tsconfig.json
cd ui && pnpm exec svelte-check
</verification>

<success_criteria>
OperationModal.svelte компилируется без обращения к удалённым V029-командам; compatibilityUnconfigured warning и compatibleDeviceIds подсветка работают на V005 printer_name матчинге с идентичным пользовательским поведением (D-03 case-insensitive+trim, D-05 pass-through).
</success_criteria>

<output>
Create `.planning/phases/13-per-device-junction-chip-drum-state/13-08-SUMMARY.md` when done
</output>
