---
phase: 13-per-device-junction-chip-drum-state
plan: 06
type: execute
wave: 3
depends_on: ["13-02", "13-05"]
files_modified:
  - ui/src/features/cartridges/CompatibilityEditor.svelte
  - ui/src/features/cartridges/CompatibleDevicesEditor.svelte
  - ui/src/features/cartridges/ModelFormModal.svelte
  - ui/src/features/cartridges/api.ts
  - ui/src/features/printers/api.ts
autonomous: true
requirements: [SPEC-13-R3]
must_haves:
  truths:
    - "Форма модели картриджа (ModelFormModal) показывает РОВНО ОДИН блок «Совместимые принтеры» — свободный список текстовых строк (имя принтера), без раздельных полей Бренд/Модель"
    - "Удаление CompatibleDevicesEditor.svelte не оставляет мёртвых импортов/ссылок где-либо в ui/src"
    - "Автокомплит в CompatibilityEditor подсказывает значения из devices.name (через suggestCompatPrinter без параметра field — Plan 13-05) "
    - "api.ts (cartridges, printers) не содержит вызовов удалённых команд cartridge_models_get/set_compatible_devices, printers_get/set_compatible_models"
  artifacts:
    - path: "ui/src/features/cartridges/CompatibilityEditor.svelte"
      provides: "Однополевой (printer name) редактор списка совместимых принтеров с автокомплитом"
    - path: "ui/src/features/cartridges/ModelFormModal.svelte"
      provides: "Единый блок совместимости (R3), без CompatibleDevicesEditor"
  key_links:
    - from: "ui/src/features/cartridges/ModelFormModal.svelte"
      to: "CartridgeModelCreateDto.compatibility: string[]"
      via: "submit payload"
      pattern: "compatibility:\\s*compatibility"
    - from: "ui/src/features/cartridges/CompatibilityEditor.svelte"
      to: "cartridges.suggestCompatPrinter"
      via: "autocomplete fetch"
      pattern: "suggestCompatPrinter\\(prefix\\)"
---

<objective>
Свернуть редактирование совместимости в ModelFormModal до ОДНОГО блока «Совместимые принтеры» (R3): CompatibilityEditor.svelte переходит с пар (Бренд+Модель) на список одиночных строк free-text «имя принтера» (V005, D-04), с автокомплитом из devices.name. Удалить CompatibleDevicesEditor.svelte (по-устройственный чеклист, V029-артефакт) и сопутствующие api.ts обёртки удалённых команд.

Purpose: до этого плана форма модели показывала ДВА блока совместимости (старый brand/model редактор + чеклист устройств) — после редизайна должен остаться ровно один, отражающий новую модель данных «совместимость = список имён принтеров» (D-04/D-05).
Output: ModelFormModal с единственным блоком совместимости; api.ts без мёртвых вызовов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-SPEC.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-CONTEXT.md

<interfaces>
<!-- ui/src/bindings.ts (regenerated by Plan 13-02 + 13-05 — types/commands the executor MUST use, no exploration needed) -->
export type CartridgeModelDto = { id: number; brand: string; model: string; kindId: number; color: string | null; notes: string | null; compatibility: string[]; version: number; /* ... */ };
export type CartridgeModelCreateDto = { brand: string; model: string; kindId: number; color: string | null; notes: string | null; compatibility: string[] };
export type CartridgeModelPatchDto = { id: number; version: number; /* ...; */ compatibility: string[] };

<!-- ui/src/features/cartridges/api.ts — current shape BEFORE this plan (apiCall wrapper pattern) -->
export const cartridges = {
  modelsCreate: (payload: CartridgeModelCreateDto) => apiCall<CartridgeModelDto>('cartridge_models_create', { payload }),
  modelsUpdate: (payload: CartridgeModelPatchDto) => apiCall<CartridgeModelDto>('cartridge_models_update', { payload }),
  modelsGetCompatibleDevices: (modelId: number) => apiCall<{ device_ids: number[] }>('cartridge_models_get_compatible_devices', { modelId }),  // DELETE — command removed in Plan 13-03
  modelsSetCompatibleDevices: (modelId: number, deviceIds: number[]) => apiCall<null>('cartridge_models_set_compatible_devices', { modelId, deviceIds }),  // DELETE — command removed in Plan 13-03
  suggestCompatPrinter: (field: 'printer_brand' | 'printer_model', prefix: string) => apiCall<string[]>('cartridges_suggest_compat_printer', { field, prefix }),  // SIGNATURE CHANGED in Plan 13-05 — field param removed
};

<!-- ui/src/features/printers/api.ts — current shape BEFORE this plan -->
export const printers = {
  getCompatibleModels: (deviceId: number) => apiCall<{ modelIds: number[] }>('printers_get_compatible_models', { deviceId }),  // DELETE — command removed in Plan 13-03
  setCompatibleModels: (payload: PrinterCompatibleModelsDto) => apiCall<null>('printers_set_compatible_models', { payload }),  // DELETE — command removed in Plan 13-03
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — удалить обёртки удалённых команд, обновить suggestCompatPrinter</name>
  <files>ui/src/features/cartridges/api.ts, ui/src/features/printers/api.ts</files>
  <read_first>
    ui/src/features/cartridges/api.ts
    ui/src/features/printers/api.ts
    ui/src/bindings.ts
  </read_first>
  <action>
    В cartridges/api.ts: удалить modelsGetCompatibleDevices и modelsSetCompatibleDevices целиком (команды cartridge_models_get_compatible_devices/set_compatible_devices больше не существуют после Plan 13-03). Изменить suggestCompatPrinter: убрать параметр field, новая сигнатура suggestCompatPrinter: (prefix: string) => apiCall<string[]>('cartridges_suggest_compat_printer', { prefix }) (соответствует новой серверной сигнатуре из Plan 13-05 Task 2).

    В printers/api.ts: удалить getCompatibleModels и setCompatibleModels целиком (команды printers_get_compatible_models/set_compatible_models больше не существуют после Plan 13-03). Добавить новую обёртку getCompatibleAggregates: (deviceId: number) => apiCall<PrinterCompatibleAggregatesDto>('printers_get_compatible_aggregates', { deviceId }) — сигнатура команды и имя DTO взять из bindings.ts (сгенерирован Plan 13-03 Task 2), импортировать PrinterCompatibleAggregatesDto в типовой импорт сверху файла.

    Проверить импорт PrinterCompatibleModelsDto в printers/api.ts (если использовался только для удаляемого setCompatibleModels) — убрать неиспользуемый импорт.
  </action>
  <verify>
    <automated>cd ui && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "api.ts" | head -20</automated>
  </verify>
  <done>cartridges/api.ts и printers/api.ts не содержат вызовов удалённых команд; suggestCompatPrinter принимает только prefix; printers.getCompatibleAggregates добавлена.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompatibilityEditor — переход на одно поле (имя принтера) + удаление CompatibleDevicesEditor</name>
  <files>ui/src/features/cartridges/CompatibilityEditor.svelte, ui/src/features/cartridges/CompatibleDevicesEditor.svelte, ui/src/features/cartridges/ModelFormModal.svelte</files>
  <read_first>
    ui/src/features/cartridges/CompatibilityEditor.svelte
    ui/src/features/cartridges/CompatibleDevicesEditor.svelte
    ui/src/features/cartridges/ModelFormModal.svelte
  </read_first>
  <behavior>
    - CompatibilityEditor рендерит список строк (одно текстовое поле «Имя принтера» на строку, без отдельных Бренд/Модель колонок), кнопку «+ Добавить принтер», кнопку удаления на каждой строке — как и раньше, но с одним полем вместо двух.
    - Фокус на поле строки открывает dropdown с автокомплитом через единственную suggestFn(prefix) проп (заменяет suggestBrandFn+suggestModelFn).
    - При вводе текста, не совпадающего ни с одной подсказкой, значение всё равно сохраняется как free-text при submit формы (свободный ввод не блокируется отсутствием совпадений — D-04).
    - При сохранении формы compatibility передаётся как string[] (просто массив непустых trim-нутых строк, без дублей) — не массив пар.
  </behavior>
  <action>
    Переписать CompatibilityEditor.svelte: заменить CompatRow interface { printer_brand, printer_model } на простой rows: string[]; Props: { compatibility: string[]; onChange: (_names: string[]) => void; suggestFn: (_prefix: string) => Promise<string[]> } (убрать suggestBrandFn/suggestModelFn, оставить единственную suggestFn). Убрать getKey(index, field) различение по 'brand'|'model' — заменить на единый key = String(index) на строку. Убрать updateBrand/updateModel — заменить на updateName(index, value). Убрать вторую autocomplete-wrapper секцию (модель принтера) из markup — оставить один input на строку с label «Имя принтера». Убрать compat-header с двумя field-label (Бренд принтера / Модель принтера) — заменить на одну метку «Имя принтера» или убрать заголовок колонки вовсе (один столбец не требует header-строки, см. UI-SPEC §CompatibilityEditor если есть точные требования к разметке — иначе на усмотрение, сохраняя текущий visual style карточек .compat-row/.compat-field/.remove-btn).

    Удалить файл CompatibleDevicesEditor.svelte (V029-артефакт, по-устройственный чеклист — заменён единым блоком совместимости через CompatibilityEditor).

    В ModelFormModal.svelte: удалить import CompatibleDevicesEditor; удалить блок markup `{#if isEdit}<div class="field field-full compat-section"><h3>Совместимые принтеры (по справочнику устройств)</h3><CompatibleDevicesEditor cartridgeModelId={target.id} /></div>{/if}` (строки ~455-460) целиком — это был ВТОРОЙ блок совместимости, удаляется по R3. Заменить interface CompatRow { printer_brand, printer_model } на простой compatibility: string[] = $state(target?.compatibility ?? []) (CartridgeModelDto.compatibility теперь string[] после Plan 13-02 — никакого .map(([pb,pm]) => ...) преобразования не требуется, это прямое присваивание). Обновить $effect сброса формы (строки ~70-78): compatibility = target?.compatibility ?? [] (без .map). Обновить вызов CompatibilityEditor: передать {compatibility} onChange={(names) => (compatibility = names)} suggestFn={(prefix) => cartridges.suggestCompatPrinter(prefix)} (убрать suggestBrandFn/suggestModelFn, и поле field в вызове suggestCompatPrinter — соответствует новой сигнатуре из Task 1).

    Обновить filteredCompatibility (строка ~261) и submit payload (строки ~277, ~286): убрать .filter по printer_brand/printer_model непустоте, заменить на compatibility.map(s => s.trim()).filter(s => s.length > 0) с дедупликацией (Array.from(new Set(...))) — передавать как compatibility: filteredCompatibility прямо в payload (CartridgeModelCreateDto/CartridgeModelPatchDto теперь принимают compatibility: string[], не [string, string][]).

    Обновить заголовочный doc-комментарий ModelFormModal.svelte (строка 4, "size=wide из-за CompatibilityEditor") — оставить как есть, ширина модалки не меняется логически, но добавить заметку про Phase 13 редизайн (единый блок compatibility: string[]).
  </action>
  <verify>
    <automated>cd ui && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "CompatibilityEditor|ModelFormModal|CompatibleDevicesEditor"</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn "CompatibleDevicesEditor" ui/src` возвращает пусто (файл удалён, импорты убраны).
    `grep -n "printer_brand\|printer_model" ui/src/features/cartridges/CompatibilityEditor.svelte ui/src/features/cartridges/ModelFormModal.svelte` возвращает пусто.
    `pnpm exec tsc --noEmit -p ui/tsconfig.json` не сообщает ошибок в затронутых файлах.
  </acceptance_criteria>
  <done>ModelFormModal содержит ровно один блок «Совместимые принтеры» (CompatibilityEditor, одно поле на строку); CompatibleDevicesEditor.svelte удалён; типы согласованы со string[].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Браузер (ModelFormModal) -> Tauri/HTTP cartridge_models_create/update | Список имён принтеров (free-text) передаётся как есть, без серверной валидации против реестра devices (D-04 допускает произвольный текст) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-12 | Tampering | compatibility: string[] payload | accept | Свободный текст разрешён по дизайну (D-04) — сопоставление при фильтрации идёт по TRIM+LOWER против devices.name на сервере (Plan 13-01), некорректные строки просто не сужают список, не создают инъекцию (параметризованный SQL) |
| T-13-13 | Denial of Service | compatibility массив без верхнего предела элементов | accept | UI-driven форма, не публичный API в большом масштабе; разумный предел (например, сотни строк) не критичен для LAN-приложения с ~20 пользователями |
</threat_model>

<verification>
cd ui && pnpm exec tsc --noEmit -p tsconfig.json
cd ui && pnpm exec svelte-check
</verification>

<success_criteria>
ModelFormModal показывает единственный блок совместимости (free-text имена принтеров со автокомплитом); CompatibleDevicesEditor удалён; api.ts очищен от вызовов удалённых команд.
</success_criteria>

<output>
Create `.planning/phases/13-per-device-junction-chip-drum-state/13-06-SUMMARY.md` when done
</output>
