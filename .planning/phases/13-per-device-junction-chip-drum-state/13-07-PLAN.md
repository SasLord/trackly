---
phase: 13-per-device-junction-chip-drum-state
plan: 07
type: execute
wave: 3
depends_on: ["13-03", "13-06"]
files_modified:
  - ui/src/features/printers/PrinterDetail.svelte
  - ui/src/features/printers/CompatibleModelsEditor.svelte
  - ui/src/features/printers/api.ts
autonomous: true
requirements: [SPEC-13-R4, SPEC-13-R5, SPEC-13-R6]
must_haves:
  truths:
    - "Карточка принтера показывает read-only блок совместимых моделей картриджей с агрегатами по статусам в порядке На складе → На заправке → В работе (D-07), без «Списано», без элементов редактирования"
    - "CompatibleModelsEditor.svelte (V029 per-device редактор) удалён, никаких dead imports"
    - "Карточка принтера показывает блок данных устройства: Инвентарный №, Серийный №, Расположение, Состояние (D-08) с кнопкой/иконкой «Редактировать», открывающей DeviceFormModal (D-09)"
    - "Установленный картридж на карточке принтера отображается как код (C-XXXXXX) + наименование модели (brand model), без внутреннего числового id (R6)"
  artifacts:
    - path: "ui/src/features/printers/PrinterDetail.svelte"
      provides: "Read-only агрегаты совместимости + блок устройства с диалогом редактирования + код+наименование установленного картриджа"
  key_links:
    - from: "ui/src/features/printers/PrinterDetail.svelte"
      to: "printers.getCompatibleAggregates(printer.deviceId)"
      via: "$effect fetch on printer change"
      pattern: "getCompatibleAggregates\\(printer\\.deviceId\\)"
    - from: "ui/src/features/printers/PrinterDetail.svelte"
      to: "DeviceFormModal"
      via: "device edit dialog (D-09)"
      pattern: "DeviceFormModal"
    - from: "ui/src/features/printers/PrinterDetail.svelte"
      to: "cartridges.get(printer.currentCartridgeId)"
      via: "fetch installed cartridge code+model"
      pattern: "cartridges\\.get\\(printer\\.currentCartridgeId\\)"
---

<objective>
Переработать карточку принтера (PrinterDetail.svelte) под три требования: (R4) блок совместимости становится read-only агрегатами по статусам (На складе → На заправке → В работе, без Списано, D-07), удаляя редактируемый CompatibleModelsEditor.svelte (V029-артефакт); (R5) добавить блок данных устройства (Инвентарный №, Серийный №, Расположение, Состояние) с кнопкой «Редактировать», открывающей переиспользуемый DeviceFormModal (D-08/D-09); (R6) установленный картридж показывается по коду (C-XXXXXX) + наименованию модели вместо внутреннего id.

Purpose: завершает фронтенд-часть редизайна совместимости (после удаления per-device V029 в Plan 13-01..13-03) и закрывает оставшиеся 3 SPEC-требования карточки принтера.
Output: PrinterDetail.svelte с тремя переработанными блоками; CompatibleModelsEditor.svelte удалён.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-SPEC.md
@.planning/phases/13-per-device-junction-chip-drum-state/13-CONTEXT.md

<interfaces>
<!-- ui/src/bindings.ts — types this plan consumes, no further exploration needed -->
export type DeviceDto = { id: number; version: number; type_id: number; name: string; inventory_no: string | null; serial_no: string | null; model: string | null; specs: string | null; kit: string | null; state: string | null; location_id: number | null; location: string | null; status_id: number; created_at_utc: number; updated_at_utc: number };
export type CartridgeDto = { id: number; version: number; code: string; model_id: number; model_brand: string | null; model_name: string | null; model_kind_id: number | null; status_id: number; status_name: string | null; state_id: number | null; state_name: string | null; location: string | null; holder_name: string | null; notes: string | null; created_at_utc: number; updated_at_utc: number; deleted_at_utc: number | null };
export type PrinterDto = { id: number; deviceId: number; ipAddress: string | null; /* ...; */ currentCartridgeId: number | null; deviceName: string | null; /* ... */ };
<!-- Plan 13-03 output (bindings regenerated) -->
export type CompatibleModelAggregateDto = { modelId: number; brand: string; model: string; inStock: number; atRefill: number; inUse: number };
export type PrinterCompatibleAggregatesDto = { models: CompatibleModelAggregateDto[] };

<!-- ui/src/features/devices/DeviceFormModal.svelte — Props contract (READ from source, exact) -->
interface Props { open: boolean; target: DeviceDto | null; onClose: () => void; onSaved: () => void; }
<!-- Opens with target=DeviceDto (full object, not just id) — modalTitle becomes "Редактирование устройства" when target !== null. -->

<!-- ui/src/lib/api/devices.ts -->
export const devices = { get: (id: number) => apiCall<DeviceDto>('devices_get', { id }), /* update, etc. */ };

<!-- ui/src/features/cartridges/api.ts (existing, unmodified by this plan) -->
export const cartridges = { get: (id: number) => apiCall<CartridgeDto>('cartridges_get', { id }), /* ... */ };

<!-- ui/src/features/printers/api.ts AFTER Plan 13-06 Task 1 (already applied) -->
export const printers = { getCompatibleAggregates: (deviceId: number) => apiCall<PrinterCompatibleAggregatesDto>('printers_get_compatible_aggregates', { deviceId }), /* getCompatibleModels/setCompatibleModels REMOVED */ };
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R4 — read-only агрегаты совместимости, удалить CompatibleModelsEditor</name>
  <files>ui/src/features/printers/PrinterDetail.svelte, ui/src/features/printers/CompatibleModelsEditor.svelte</files>
  <read_first>
    ui/src/features/printers/PrinterDetail.svelte
    ui/src/features/printers/CompatibleModelsEditor.svelte
  </read_first>
  <behavior>
    - Принтер с >=1 совместимой моделью: блок «Совместимые модели картриджей» рендерит список строк вида «{brand} {model}: На складе {inStock}, На заправке {atRefill}, В работе {inUse}» — порядок полей строго На складе → На заправке → В работе (D-07), без поля «Списано».
    - Принтер без совместимых моделей (V005 не настроен ни для одной модели под именем этого принтера): блок показывает «Совместимость не настроена» (или аналогичный muted текст), не пустой список без объяснения.
    - Блок не содержит ни одной кнопки/чекбокса для добавления или удаления связи — чисто read-only вывод.
  </behavior>
  <action>
    Удалить файл CompatibleModelsEditor.svelte целиком (V029 per-device редактор — заменяется read-only выводом).

    В PrinterDetail.svelte: удалить import CompatibleModelsEditor from './CompatibleModelsEditor.svelte'. Добавить $state<CompatibleModelAggregateDto[]>([]) compatAggregates и $state(false) compatLoading. Добавить $effect, реагирующий на изменение printer (аналогично существующему readings $effect): при printer !== null вызвать printers.getCompatibleAggregates(printer.deviceId), записать res.models в compatAggregates, обернуть в try/catch с fail-safe (catch → compatAggregates = []), finally → compatLoading = false; при printer === null — сбросить compatAggregates = [] без вызова.

    Заменить секцию `<!-- Секция: совместимые модели картриджей -->` (текущие строки ~195-199): убрать `<CompatibleModelsEditor deviceId={printer.deviceId} />`, заменить на условную разметку: если compatLoading — Spinner; иначе если compatAggregates.length === 0 — `<p class="muted">Совместимость не настроена</p>`; иначе `{#each compatAggregates as agg (agg.modelId)}<p class="compat-agg-row">{agg.brand} {agg.model}: На складе {agg.inStock}, На заправке {agg.atRefill}, В работе {agg.inUse}</p>{/each}`. Добавить CSS класс .compat-agg-row в `<style lang="scss">` по аналогии с существующим .counter-row (margin:0, font-size: var(--font-size-body), color: var(--color-text-primary)).

    Импортировать тип CompatibleModelAggregateDto из '../../bindings' в начале файла.
  </action>
  <verify>
    <automated>cd ui && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "PrinterDetail\|CompatibleModelsEditor"</automated>
  </verify>
  <acceptance_criteria>
    `grep -rn "CompatibleModelsEditor" ui/src` возвращает пусто.
    `grep -n "На складе.*На заправке.*В работе\|inStock.*atRefill.*inUse" ui/src/features/printers/PrinterDetail.svelte` подтверждает правильный порядок полей в строке вывода.
    `grep -n "Списано\|status_id.*4\|writtenOff" ui/src/features/printers/PrinterDetail.svelte` возвращает пусто (поле Списано не выводится).
  </acceptance_criteria>
  <done>Блок совместимости на карточке принтера — read-only агрегаты в порядке На складе→На заправке→В работе, без Списано; CompatibleModelsEditor.svelte удалён.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: R5 — блок данных устройства + диалог редактирования; R6 — установленный картридж по коду+наименованию</name>
  <files>ui/src/features/printers/PrinterDetail.svelte</files>
  <read_first>
    ui/src/features/devices/DeviceFormModal.svelte
    ui/src/lib/api/devices.ts
  </read_first>
  <behavior>
    - При открытой карточке принтера с deviceId=N: загружается DeviceDto через devices.get(N); блок «Устройство» показывает 4 строки: Инвентарный № (inventory_no ?? '—'), Серийный № (serial_no ?? '—'), Расположение (location ?? '—'), Состояние (state ?? '—').
    - Клик по кнопке/иконке «Редактировать» открывает DeviceFormModal с target = загруженный DeviceDto; onSaved повторно загружает devices.get(N) и обновляет блок без полного reload страницы.
    - Принтер с currentCartridgeId !== null: блок «Установленный картридж» показывает «{code} — {model_brand} {model_name}» (например «C-000123 — Cactus CS-TL-5120»), без числового id.
    - Принтер с currentCartridgeId === null: «Картридж не закреплён» (текущее поведение не меняется).
  </behavior>
  <action>
    Импортировать DeviceFormModal from '../devices/DeviceFormModal.svelte' и devices from '../devices/api' (или '$lib/api/devices' — использовать тот же путь импорта, что уже сложился в кодовой базе для devices API, проверить через grep перед добавлением). Импортировать тип DeviceDto из '../../bindings'.

    Добавить $state<DeviceDto | null>(null) deviceData, $state(false) deviceLoading, $state(false) deviceEditOpen. Добавить $effect реагирующий на printer (как существующий readings $effect): при printer !== null — devices.get(printer.deviceId).then(d => deviceData = d).catch(() => deviceData = null).finally(() => deviceLoading = false); при printer === null — deviceData = null.

    Добавить новую секцию `<!-- Секция: данные устройства (R5, D-08/D-09) -->` ПЕРЕД секцией метаданных (или после блока совместимости — на усмотрение, сохраняя визуальный порядок detail-section): заголовок «Устройство» + кнопка «Редактировать» (Button variant="secondary" size="sm", onclick={() => (deviceEditOpen = true)}) в одной строке (flex, justify-content: space-between — по аналогии с title-row); 4 строки meta-row-подобной разметки (Инвентарный №, Серийный №, Расположение, Состояние) — использовать существующий .meta-row/.meta-label/.meta-value CSS, либо новый аналогичный класс .device-row, если семантически уместнее (на усмотрение, сохраняя текущий visual style карточек).

    Добавить `<DeviceFormModal open={deviceEditOpen} target={deviceData} onClose={() => (deviceEditOpen = false)} onSaved={() => { deviceEditOpen = false; if (printer) devices.get(printer.deviceId).then(d => (deviceData = d)); }} />` после закрывающего `</div>` блока detail-body (вне scrollable области, как и положено модалкам — на верхнем уровне printer-detail div).

    Изменить секцию `<!-- Секция: установленный картридж -->` (текущие строки ~185-193): добавить $state<CartridgeDto | null>(null) installedCartridge; добавить $effect реагирующий на printer.currentCartridgeId: при printer !== null && printer.currentCartridgeId !== null — cartridges.get(printer.currentCartridgeId).then(c => installedCartridge = c).catch(() => installedCartridge = null); иначе installedCartridge = null. Импортировать cartridges from '../cartridges/api' и тип CartridgeDto из '../../bindings'. В markup заменить `<p class="cartridge-row">Картридж #{printer.currentCartridgeId}</p>` на условный рендер: если installedCartridge !== null — `<p class="cartridge-row">{installedCartridge.code} — {installedCartridge.model_brand} {installedCartridge.model_name}</p>`; иначе (загрузка ещё идёт, но currentCartridgeId уже установлен) можно показать тот же текст с заглушкой '…' или оставить текущий null-check — приоритет: не показывать числовой id ни в одном промежуточном состоянии.
  </action>
  <verify>
    <automated>cd ui && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "PrinterDetail"</automated>
  </verify>
  <acceptance_criteria>
    `grep -n "Картридж #{printer.currentCartridgeId}\|Картридж #\${" ui/src/features/printers/PrinterDetail.svelte` возвращает пусто (числовой id больше не выводится напрямую).
    `grep -n "installedCartridge.code\|installedCartridge.model_brand" ui/src/features/printers/PrinterDetail.svelte` подтверждает новый формат.
    `grep -n "DeviceFormModal" ui/src/features/printers/PrinterDetail.svelte` подтверждает интеграцию диалога редактирования.
    `grep -n "Инвентарный\|Серийный\|Расположение\|Состояние" ui/src/features/printers/PrinterDetail.svelte | wc -l` >= 4 (все четыре поля присутствуют).
  </acceptance_criteria>
  <done>Карточка принтера показывает блок устройства (4 поля) с кнопкой Редактировать, открывающей DeviceFormModal; установленный картридж отображается по коду+наименованию модели без числового id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Браузер (PrinterDetail) -> devices_get/cartridges_get/printers_get_compatible_aggregates | Три read-команды вызываются на основе deviceId/currentCartridgeId, полученных из уже авторизованного printers_get ответа — нет нового способа угадать чужой id (тот же RBAC-гейт ReadData/ReadPrinters на всех трёх) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-14 | Information Disclosure | DeviceFormModal открывается с полным DeviceDto принтера | accept | DeviceFormModal уже используется для редактирования любых устройств в системе тем же набором ролей (MutateDevices) — нет повышения привилегий, тот же диалог что и в разделе «Устройства» |
| T-13-15 | Tampering | onSaved повторно вызывает devices.get без проверки версии конфликта | accept | DeviceFormModal сам обрабатывает version-conflict при сохранении (существующая логика); повторный get после onSaved — чистое чтение, не запись |
</threat_model>

<verification>
cd ui && pnpm exec tsc --noEmit -p tsconfig.json
cd ui && pnpm exec svelte-check
</verification>

<success_criteria>
Карточка принтера показывает read-only агрегаты совместимости (D-07 порядок), блок данных устройства с диалогом редактирования (D-08/D-09), установленный картридж по коду+наименованию (R6); CompatibleModelsEditor.svelte удалён.
</success_criteria>

<output>
Create `.planning/phases/13-per-device-junction-chip-drum-state/13-07-SUMMARY.md` when done
</output>
