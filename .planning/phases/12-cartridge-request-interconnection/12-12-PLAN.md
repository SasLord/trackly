---
phase: 12-cartridge-request-interconnection
plan: 12
type: execute
gap_closure: true
wave: 2
depends_on: ["12-11"]
files_modified:
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: true
requirements: [GAP-12-05]

must_haves:
  truths:
    - "При установке картриджа из заявки информация о принтере (наименование + IP) отображается первой строкой формы, не порядковым #id"
    - "Подсказка к полям «Кто выдал»/«Кому выдал» в блоке «Предыдущий картридж» поясняет перевёрнутую семантику (кто устанавливает новый — тот принимает старый назад; кому установили новый — тот и отдаёт старый на возврат), без добавления новых полей"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "printerContextHint показывает device_name + ip_address вместо #id, рендерится первым полем формы"
      contains: "printer.deviceName"
  key_links:
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "ui/src/lib/api/printers.ts (printers.get)"
      via: "существующий $effect (строки 142-166) уже вызывает printers.get(preFillPrinterId) — переиспользуется для подсказки, без второго вызова API"
      pattern: "printers\\.get\\(preFillPrinterId\\)"
---

<objective>
Доработать диалог установки картриджа (GAP-12-05, A2): (1) переместить информацию о принтере в начало формы и показывать наименование+IP вместо порядкового `#id`; (2) добавить поясняющую подсказку к полям «Кто выдал»/«Кому выдал» для блока «Предыдущий картридж» о перевёрнутой семантике (без новых полей — переиспользуются существующие).

ВАЖНО — что уже сделано в плане 12-09 и НЕ требует повторной работы: выбор «Состояние заряда» (Полный/Частичный/Пустой — все три опции уже есть в `CARTRIDGE_STATES`) и поле «Расположение» для предыдущего картриджа уже существуют и редактируемы. Этот план НЕ трогает per-device junction совместимости принтер↔картридж (вынесено в Фазу 13).

Purpose: оператор сразу видит, в какой физический принтер устанавливает картридж (не abstract id), и понимает, что поля Кто/Кому относятся к НОВОМУ картриджу, а не к возвращаемому старому.
Output: переупорядоченная и дополненная подсказками форма OperationModal для request-centric потока установки.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-cartridge-request-interconnection/12-HUMAN-UAT.md
@.planning/phases/12-cartridge-request-interconnection/12-09-SUMMARY.md

<read_first>
ui/src/features/cartridges/OperationModal.svelte строки 100-265 (printerContextHint строка 131-135 использует #id; previous-cartridge lookup $effect строки 142-166 уже вызывает printers.get(preFillPrinterId) и cartridges.get; previousCartridge state)
ui/src/features/cartridges/OperationModal.svelte строки 404-466 (текущий порядок рендера: cartridge-select → printerContextHint → previous-cartridge block; Select для charge state УЖЕ содержит все 3 опции 1/2/3 — Полный/Частичный/Пустой)
crates/trackly-app/src/dto/printer.rs строки 16-50 (PrinterDto — уже содержит device_name, ip_address; никаких backend-изменений не требуется)
ui/src/lib/api/printers.ts (printers.get — возвращает PrinterDto с camelCase deviceName/ipAddress)
</read_first>

<interfaces>
`PrinterDto` (camelCase на проводе через specta, crates/trackly-app/src/dto/printer.rs:16-50) уже содержит:
```
deviceName: string | null
ipAddress: string | null
```
Переиспользуется напрямую — никаких новых полей DTO не требуется.

Текущий previous-cartridge lookup `$effect` (OperationModal.svelte:142-166) уже вызывает `printers.get(preFillPrinterId)` и сохраняет результат в локальную переменную внутри `.then()` callback, но НЕ сохраняет сам объект `printer` в `$state` — только извлекает `printer.currentCartridgeId` для второго вызова. Чтобы показать `deviceName`/`ipAddress` в `printerContextHint`, нужно завести новый `$state<PrinterDto | null>` (например `printerContext`) и сохранять туда результат `printers.get(preFillPrinterId)` внутри того же `$effect` (не создавать второй вызов API).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Задача 1: Подсказка о принтере — имя+IP вместо #id, первой строкой формы</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <behavior>
    - Когда `preFillPrinterId` задан и `printers.get()` вернул принтер с `deviceName="HP LaserJet"`, `ipAddress="192.168.1.50"` — `printerContextHint` равен `"Устанавливается в принтер: HP LaserJet (192.168.1.50)"`.
    - Когда `ipAddress` равен `null` (USB-only принтер) — `printerContextHint` равен `"Устанавливается в принтер: HP LaserJet"` (без скобок с пустым IP).
    - Когда `deviceName` равен `null` — fallback на `"Устанавливается в принтер #{id}"` (текущее поведение, на случай отсутствия joined-имени).
    - В template `printerContextHint` рендерится ПЕРВЫМ элементом формы (до cartridge-select), а не после него.
  </behavior>
  <action>
В `OperationModal.svelte` завести новый `$state<PrinterDto | null>(null)` с именем `printerContext`. В существующем `$effect` (строки 142-166), который уже вызывает `printers.get(preFillPrinterId)`, сохранить результат в `printerContext` (через ту же цепочку `.then()`, без второго вызова API) — переименовать локальную переменную или добавить присвоение `printerContext = printer` сразу после получения. Заменить производное `printerContextHint` (строки 131-135) на вычисление из `printerContext`: если `printerContext !== null`, формировать строку `Устанавливается в принтер: {deviceName}{ipAddress ? ` (${ipAddress})` : ''}` с fallback на `deviceName ?? `#${preFillPrinterId}`` если `deviceName` отсутствует. В template (строки 414-437) переместить блок `{#if printerContextHint}<p class="field-hint">{printerContextHint}</p>{/if}` так, чтобы он рендерился ПЕРВЫМ внутри `{#if op === 'install' || op === 'to_refill'}`, перед блоком выбора картриджа (`{#if op === 'install' && cartridge === null}`).
  </action>
  <acceptance_criteria>
`pnpm --dir ui exec svelte-check` — 0 ошибок. Юнит-тест (если в проекте есть тесты для .svelte логики через vitest) или ручная проверка: открыть «Установить картридж» из заявки на принтер с IP — первая строка формы показывает «Устанавливается в принтер: {имя} ({IP})»; для USB-only принтера — без скобок; cartridge-select рендерится ПОСЛЕ этой строки.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Задача 2: Подсказка о перевёрнутой семантике Кто/Кому для предыдущего картриджа</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <action>
В блоке `previous-cartridge-block` (строки 443-465) добавить под существующей строкой `<p class="field-hint">Сейчас в принтере: ...</p>` новую строку подсказки: `<p class="field-hint">Поля «Кто выдал» / «Кому выдал» ниже относятся к НОВОМУ картриджу. При возврате этого картриджа на склад роли переворачиваются: кто устанавливает новый — принимает этот обратно; кому устанавливают новый — тот и сдаёт этот на возврат.</p>`. Не добавлять новые input-поля — это чисто информационная подсказка, поясняющая уже существующие поля «Кто выдал»/«Кому выдал» формы установки (строки 473-501), которые применяются И к новому картриджу, И (по обратной логике) к возвращаемому. Никаких изменений в `buildPayload()` — backend уже не пишет отдельных Кто/Кому полей для авто-возврата (подтверждено в 12-06: auto-return cascade в cartridges_sqlite.rs пишет только status_id/holder_name=NULL/current_printer_device_id=NULL, без actor-полей).
  </action>
  <acceptance_criteria>
`pnpm --dir ui exec svelte-check` — 0 ошибок. Текстовая подсказка присутствует в DOM при открытой previous-cartridge-block секции (видна когда `previousCartridge !== null`). Никаких новых полей формы, никаких изменений в `CartridgeTransitionPayload`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Пользовательский ввод формы → cartridges_transition payload | Не меняется этим планом — только текстовые подсказки и порядок рендера |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-12-01 | Information Disclosure | Отображение IP-адреса принтера в форме | accept | IP принтера уже доступен Admin/Manager через карточку принтера (ReadPrinters action); это просто более удобное отображение того же значения в другом месте UI, не новая точка раскрытия |
</threat_model>

<verification>
1. `pnpm --dir ui exec svelte-check` — 0 ошибок.
2. `pnpm --dir ui build` — собирается без ошибок.
3. Ручная проверка через `cargo tauri dev`: открыть заявку «Замена картриджа» в работе → «Установить картридж» → первая строка формы — имя+IP принтера; если у целевого принтера уже стоит картридж «В работе» — блок «Предыдущий картридж» содержит подсказку о перевёрнутых ролях.
</verification>

<success_criteria>
- Информация о принтере (имя+IP) отображается первой строкой формы установки, без `#id`.
- Подсказка о перевёрнутой семантике Кто/Кому видна в блоке «Предыдущий картридж».
- Не добавлено новых полей формы, не изменён CartridgeTransitionPayload.
- Не затронута модель совместимости принтер↔картридж (junction остаётся как в 12-08, без изменений).
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-12-SUMMARY.md` when done
</output>
