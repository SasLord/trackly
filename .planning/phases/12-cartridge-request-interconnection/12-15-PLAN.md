---
phase: 12-cartridge-request-interconnection
plan: 15
type: execute
gap_closure: true
wave: 2
depends_on: ["12-14"]
files_modified:
  - ui/src/features/requests/api.ts
  - ui/src/features/requests/RequestDetail.svelte
autonomous: true
requirements: [GAP-12-07]

must_haves:
  truths:
    - "Специалист/Администратор видит кнопку «Отклонить» и для заявок в статусе «В работе», не только «Новая»"
    - "Специалист/Администратор видит кнопку «Удалить» для заявки в ЛЮБОМ статусе, с подтверждением перед удалением"
    - "Сотрудник видит кнопку «Отменить заявку» только для своих заявок в статусе «Новая», с подтверждением перед отменой"
  artifacts:
    - path: "ui/src/features/requests/api.ts"
      provides: "requests.delete() и requests.cancel() — обёртки над новыми эндпоинтами requests_delete/requests_cancel"
      contains: "cancel:"
    - path: "ui/src/features/requests/RequestDetail.svelte"
      provides: "Кнопка Удалить (isSpecialist, любой статус) и кнопка Отменить заявку (Employee-автор, только open), оба с confirmation-модалкой"
      contains: "handleCancelConfirm"
  key_links:
    - from: "ui/src/features/requests/RequestDetail.svelte"
      to: "ui/src/features/requests/api.ts"
      via: "handleDeleteConfirm/handleCancelConfirm вызывают requests.delete()/requests.cancel()"
      pattern: "requests\\.(delete|cancel)\\("
---

<objective>
Добавить UI для управления жизненным циклом заявки (GAP-12-07, A4 — frontend часть): кнопку «Удалить» для Admin/Manager (любой статус, с подтверждением) и кнопку «Отменить заявку» для Employee-автора (только статус «Новая», с подтверждением). Backend-эндпоинты `requests_delete`/`requests_cancel` уже реализованы планом 12-14.

Purpose: дать визуальный доступ к новым backend-операциям без обращения к API напрямую.
Output: новые методы в `requests` API wrapper, новые кнопки + confirmation-модалки в `RequestDetail.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-cartridge-request-interconnection/12-HUMAN-UAT.md
@.planning/phases/12-cartridge-request-interconnection/12-14-PLAN.md

<read_first>
ui/src/features/requests/api.ts (полный файл — requests object с list/get/create/transition/etc, образец apiCall<T>('command_name', {args}) паттерна)
ui/src/features/requests/RequestDetail.svelte строки 1-60 (Props, isSpecialist/isAdmin derived — isSpecialist = admin||manager, "Специалист" = role manager)
ui/src/features/requests/RequestDetail.svelte строки 208-240 (handleRejectConfirm — образец confirm-handler: submitting flag, try/catch с pushToast, onTransition() после успеха)
ui/src/features/requests/RequestDetail.svelte строки 419-521 (текущая структура actions-блоков по статусам: isAdRegister/isAdmin, isSpecialist с open/in_progress/terminal ветками, employee terminal-only ветка — ТЕРМИНАЛЬНЫЕ статусы сейчас не показывают НИКАКИХ действий ни для isSpecialist, ни для employee)
ui/src/features/requests/RequestDetail.svelte строки 548-567 (Modal confirm-«Отклонить» — точный образец confirmation-modal структуры: Modal+footer snippet+Button variant="destructive")
</read_first>

<interfaces>
`RequestDto` (ui/src/bindings-phase6.ts, после применения плана 12-14) содержит `id: number, version: number, status: string, requestedByUserId: number, deletedAtUtc: number | null`.

Новые backend-эндпоинты (из плана 12-14):
```
requests_delete(id: number, version: number) -> Result<(), AppError>
requests_cancel(id: number, version: number) -> Result<RequestDto, AppError>
```

`identity: CurrentUser | null` prop уже передаётся в `RequestDetail` (строка 23), содержит `id: number, role: UserRole`. Ownership-проверка на фронте — `identity?.id === request.requestedByUserId` (UI-уровень, только для показа/скрытия кнопки; реальная проверка владения — на сервере в `cancel()`, уже покрыта планом 12-14's BOLA-guard).
</interfaces>

<tasks>

<task type="auto">
  <name>Задача 1: API wrapper — requests.delete() и requests.cancel()</name>
  <files>ui/src/features/requests/api.ts</files>
  <action>
В `ui/src/features/requests/api.ts`, в объекте `requests` (после существующего `transition`), добавить два новых метода: `delete: (id: number, version: number) => apiCall<void>('requests_delete', { id, version })` и `cancel: (id: number, version: number) => apiCall<RequestDto>('requests_cancel', { id, version })`. Импортировать `RequestDto` если ещё не импортирован (уже есть в текущих импортах файла).
  </action>
  <acceptance_criteria>
`pnpm --dir ui exec svelte-check` — 0 ошибок. `ui/src/features/requests/api.ts` экспортирует `requests.delete`/`requests.cancel` с правильными типами параметров, соответствующими `requests_delete`/`requests_cancel` сигнатурам из перегенерированных bindings.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Задача 2: UI — кнопка «Удалить» (Specialist/Admin, любой статус) с подтверждением</name>
  <files>ui/src/features/requests/RequestDetail.svelte</files>
  <action>
Добавить новый `$state`: `deleteModalOpen = $state(false)`, `deleteSubmitting = $state(false)`. Добавить функцию `handleDeleteConfirm()` по образцу `handleRejectConfirm()` (строки 208-240): вызывает `requests.delete(request.id, request.version)`, при успехе — `pushToast('success', 'Заявка удалена')`, `deleteModalOpen = false`, `onTransition()` (закроет/обновит список — родительский компонент должен снять выбор удалённой заявки; если `onTransition()` не снимает выбор автоматически, это поведение родителя `RequestsPage.svelte` — не менять этот файл, не входит в scope GAP-12-07's frontend части UAT-текста). При ошибке — `pushToast('error', msg)` по тому же паттерну catch, что в `handleRejectConfirm`.

Добавить кнопку «Удалить» (`variant="destructive"`, открывает `deleteModalOpen = true`) ВНЕ существующих status-ветвящихся `{#if}` блоков — рендерить её для `isSpecialist` (admin||manager) НЕЗАВИСИМО от `request.status` (включая "completed"/"rejected"/"open"/"in_progress"/"cancelled"), как отдельный безусловный `{#if isSpecialist}` блок-секцию после существующей `{:else if isSpecialist}` секции (строки 444-512) — не внутри status-ветвлений, а как дополнительная отдельная секция, видимая всегда для этой роли. Не показывать для `isAdRegister` заявок (если по UAT/архитектуре ad_register заявки не предполагают delete — проверить через grep, не делает ли что-то особое уже существующая ad_register секция; если нет явного запрета, кнопка показывается и для ad_register, так как UAT-текст говорит «заявки» без исключений).

Добавить confirmation-Modal по образцу строк 548-567: title="Удалить заявку?", текст «Заявка будет удалена без возможности восстановления через интерфейс. Действие необратимо.», footer с Button variant="secondary" (Отмена) + Button variant="destructive" loading={deleteSubmitting} (Удалить) → onclick={handleDeleteConfirm}.
  </action>
  <acceptance_criteria>
`pnpm --dir ui exec svelte-check` — 0 ошибок. Ручная проверка через `cargo tauri dev`: открыть заявку в статусе «Выполнена» как Admin/Manager — кнопка «Удалить» видна и работает (после подтверждения заявка удаляется, тост успеха показан).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Задача 3: UI — кнопка «Отменить заявку» (Employee-автор, только статус open) с подтверждением</name>
  <files>ui/src/features/requests/RequestDetail.svelte</files>
  <action>
Добавить новый `$state`: `cancelModalOpen = $state(false)`, `cancelSubmitting = $state(false)`. Добавить производное значение `isOwnRequest = $derived(identity !== null && request !== null && identity.id === request.requestedByUserId)`. Добавить функцию `handleCancelConfirm()` по образцу `handleRejectConfirm()`: вызывает `requests.cancel(request.id, request.version)`, при успехе — `pushToast('success', 'Заявка отменена')`, `cancelModalOpen = false`, `onTransition()`. При ошибке — `pushToast('error', msg)` тем же паттерном.

В существующей employee-ветке (строка ~513, `{:else if (request.status === 'completed' || request.status === 'rejected') && request.resolutionNotes}` — это единственная ветка, рендерящаяся для НЕ-isAdRegister-НЕ-isSpecialist, то есть для employee) добавить НОВУЮ параллельную ветку: `{:else if !isSpecialist && !isAdRegister && isOwnRequest && request.status === 'open'}` — рендерит кнопку «Отменить заявку» (`variant="destructive"`, открывает `cancelModalOpen = true`). Эта ветка должна идти ПЕРЕД существующей terminal-status employee-веткой в if/else if цепочке (строка 513), так как `'open'` и `'completed'/'rejected'` — взаимоисключающие статусы, порядок не критичен для корректности, но добавить логически рядом с другими employee-specific ветками для читаемости.

Добавить confirmation-Modal по образцу строк 548-567: title="Отменить заявку?", текст «Заявка будет отменена. Чтобы продолжить работу с этим запросом, потребуется создать новую заявку.», footer с Button variant="secondary" (Отмена) + Button variant="destructive" loading={cancelSubmitting} (Отменить заявку) → onclick={handleCancelConfirm}.
  </action>
  <acceptance_criteria>
`pnpm --dir ui exec svelte-check` — 0 ошибок. Ручная проверка через браузер (LAN, employee login): своя заявка в статусе «Новая» показывает кнопку «Отменить заявку», работает с подтверждением; своя заявка в статусе «В работе» — кнопки нет; чужая заявка — employee не видит её вовсе (существующий list-фильтр уже ограничивает видимость только своими заявками, GAP-12-07 не меняет это поведение).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UI-уровень показ/скрытие кнопок | Не security-boundary — реальная авторизация и BOLA-guard выполняются на сервере (план 12-14); скрытие кнопки в UI — только UX-удобство |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-15-01 | Spoofing | Employee подделывает DOM/JS для показа кнопки «Отменить» на чужой заявке | accept | UI-проверка isOwnRequest — чисто косметическая; реальная защита — BOLA-guard в RequestService::cancel() (план 12-14, Case 39), сервер отклонит запрос независимо от того, что показано в UI |
</threat_model>

<verification>
1. `pnpm --dir ui exec svelte-check` — 0 ошибок.
2. `pnpm --dir ui build` — собирается без ошибок.
3. Ручная проверка (Admin/Manager): кнопка «Удалить» видна и работает для заявки в любом статусе.
4. Ручная проверка (Employee, браузер LAN): кнопка «Отменить заявку» видна только для своей open-заявки; обновление `pnpm --dir ui build` выполнено перед проверкой через браузер (dev_browser_testing_needs_ui_build).
</verification>

<success_criteria>
- Admin/Manager может удалить заявку в любом статусе через UI с подтверждением.
- Employee может отменить свою open-заявку через UI с подтверждением.
- Кнопка отмены не показывается для чужих заявок или для заявок в in_progress/completed/rejected.
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-15-SUMMARY.md` when done
</output>
