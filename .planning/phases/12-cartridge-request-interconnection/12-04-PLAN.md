---
phase: 12-cartridge-request-interconnection
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/tests/acts_suggest.rs
autonomous: true
gap_closure: true
requirements: [D-09, D-10]

must_haves:
  truths:
    - "Поле «Кому выдал»/«Кто выдал» в картриджных операциях (OperationModal) подсказывает имена, ранее введённые и в актах, и в картриджных операциях (holder_name), без изменений на фронтенде"
    - "Поле «Кто сдал»/«Кто принял» в актных модалках продолжает работать без регрессий (старые подсказки из acts не теряются)"
  artifacts:
    - path: "crates/trackly-app/src/services/act_service.rs"
      provides: "suggest_person() читает UNION acts.{giver_name|receiver_name} + cartridges.holder_name"
      contains: "UNION ALL"
  key_links:
    - from: "crates/trackly-app/src/services/act_service.rs"
      to: "cartridges.holder_name"
      via: "SQL UNION ALL внутри suggest_person()"
      pattern: "UNION ALL.*cartridges"
---

<objective>
Сделать автокомплит имён (`acts.suggestPerson`, единственный backend-источник подсказок для `PersonAutocomplete.svelte` во всех формах — актных и картриджных) общим: расширить SQL в `suggest_person()` так, чтобы он агрегировал имена из `acts.giver_name`/`acts.receiver_name` И `cartridges.holder_name`, по аналогии с уже работающим общим автокомплитом локаций (`upsert_location_in_tx` → общая таблица `locations`).

Purpose: закрывает GAP-12-01 — сейчас имена, введённые в картриджных операциях (`OperationModal` install/to_refill → `given_to_name` → персистится в `cartridges.holder_name`), не попадают в подсказки `PersonAutocomplete`, потому что `suggest_person()` читает только таблицу `acts`.
Output: один изменённый Rust-файл (`act_service.rs`) + новый regression-тест, без изменений в DTO, фронтенде, миграциях.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md
@.planning/phases/12-cartridge-request-interconnection/12-HUMAN-UAT.md

<interfaces>
<!-- Текущая реализация suggest_person() — crates/trackly-app/src/services/act_service.rs:1058-1113. -->
<!-- Executor должен МОДИФИЦИРОВАТЬ именно это тело функции, не создавать новую функцию. -->

```rust
/// G-5: autocomplete для полей «Кто сдал» / «Кто принял».
/// Источник — DISTINCT `acts.{giver_name|receiver_name}` отсортированные
/// по frequency DESC (alpha ASC tiebreak). LIKE-prefix match с
/// `escape_like` защитой от SQL injection через `%` / `_` / `\`.
/// Soft-deleted акты не учитываются.
///
/// Phase 5 (future): UNION ALL с AD displayName — расширение в SQL без
/// изменения сигнатуры / UI contract.
pub async fn suggest_person(
    &self,
    field: SuggestPersonField,
    prefix: &str,
    limit: usize,
) -> Result<Vec<String>, AppError> {
    if prefix.chars().count() > 100 {
        return Err(AppError::Validation {
            field: "prefix".into(),
            message: "prefix слишком длинный (макс. 100 символов)".into(),
        });
    }
    let bounded_limit = limit.clamp(1, 20) as i64;
    let pattern = format!("{}%", escape_like(prefix));
    // whitelisted column через enum (НЕ string interpolation от user).
    let column = match field {
        SuggestPersonField::Giver => "giver_name",
        SuggestPersonField::Receiver => "receiver_name",
    };
    let sql = format!(
        "SELECT {col} AS name, COUNT(*) AS freq \
           FROM acts \
          WHERE {col} LIKE ?1 ESCAPE '\\' \
            AND deleted_at_utc IS NULL \
          GROUP BY {col} \
          ORDER BY freq DESC, {col} ASC \
          LIMIT ?2",
        col = column
    );
    // ... spawn_blocking + query_map unchanged below
}
```

`SuggestPersonField` enum (crates/trackly-app/src/dto/suggest.rs, unchanged by this plan):
```rust
pub enum SuggestPersonField {
    Giver,
    Receiver,
}
```
The comment at the top of `dto/suggest.rs` already anticipates this exact extension: "Phase 5 (future): источник UNION с AD displayName — расширение `enum` не требуется, сервис добавит вторую ветку в SQL `UNION ALL`." This plan adds the `cartridges.holder_name` branch now; AD displayName stays a future UNION arm — do not implement AD in this plan.

`cartridges` table schema (migrations/V005__cartridges.sql) — relevant column:
```sql
holder_name     TEXT    NULL,    -- denormalised current holder
deleted_at_utc  INTEGER NULL,
```
`holder_name` is set by `cartridges_sqlite.rs::transition_in_tx()` on `Install`/`ToRefill` ops (`given_to_name`) and cleared (`NULL`) on `ReturnToStock`/`FromRefill`. Soft-deleted cartridges use `deleted_at_utc IS NOT NULL` — exclude them the same way `acts` excludes soft-deleted rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: suggest_person — UNION ALL acts + cartridges.holder_name</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/tests/acts_suggest.rs</files>
  <read_first>
    - crates/trackly-app/src/services/act_service.rs (lines 1058-1113, full current `suggest_person` body — see &lt;interfaces&gt; above, do not re-read if already in context)
    - migrations/V005__cartridges.sql (cartridges table — holder_name, deleted_at_utc columns)
    - crates/trackly-app/tests/acts_suggest.rs (existing test file — find an existing `suggest_person` test, if any, to follow its helper/assertion style; if none exists, follow the seed-and-assert style used by other tests in this file, e.g. direct `conn.execute("INSERT INTO acts ...")` seeding)
  </read_first>
  <behavior>
    - Test 1: seed one act with `giver_name = 'Иванов И.И.'` (soft-delete = NULL) and one cartridge with `holder_name = 'Иванов И.И.'` (not soft-deleted) → `suggest_person(Giver, "Иван", 20)` returns exactly one occurrence of `"Иванов И.И."` in the result (deduplicated by name, frequency-aggregated across both sources, not two separate identical rows).
    - Test 2: seed a cartridge with `holder_name = 'Петров П.П.'` and NO matching act row at all → `suggest_person(Receiver, "Петр", 20)` still returns `"Петров П.П."` (proves the cartridges source is reachable independently of acts having any rows).
    - Test 3 (regression guard): seed a cartridge with `deleted_at_utc` set (soft-deleted) and `holder_name = 'Скрытый С.С.'`, no matching act → `suggest_person(Giver, "Скрыт", 20)` returns an empty list (soft-deleted cartridges must not leak into suggestions, mirroring the existing acts `deleted_at_utc IS NULL` guard).
  </behavior>
  <action>
    Modify the SQL inside `suggest_person()` (crates/trackly-app/src/services/act_service.rs) to UNION the existing `acts` query with a second arm over `cartridges.holder_name`, combining frequency counts so a name appearing in both sources is deduplicated and its frequencies summed (use a CTE or subquery wrapping both `SELECT col, COUNT(*)` arms with `UNION ALL`, then an outer `GROUP BY name, SUM(freq)` — do not let the same name appear twice in the result). The `cartridges` arm filters on `holder_name LIKE ?1 ESCAPE '\\' AND deleted_at_utc IS NULL` — reuse the same `pattern` parameter (no new bind parameter needed, both arms share `?1`/`?2` if structured as a single combined query, or pass `pattern` twice if using two separate prepared params — match whichever rusqlite `params![]` shape keeps the existing `bounded_limit` as the LAST param applied to the outer/final `LIMIT`). `SuggestPersonField::Giver` and `SuggestPersonField::Receiver` both map to `cartridges.holder_name` (cartridges has no giver/receiver distinction — `holder_name` is the only person-name column on that table, so both enum variants read from it identically; only the `acts` arm differentiates `giver_name` vs `receiver_name` by field). Update the function's doc comment to state the new source (acts + cartridges.holder_name) and keep the existing "Phase 5 (future): AD displayName UNION" note intact, since this plan adds the second UNION arm without exhausting the slot reserved for AD. Do not touch `escape_like()`, `dto/suggest.rs`, or any frontend file — `PersonAutocomplete.svelte` and `acts.suggestPerson()` already call this same backend function generically with no changes needed.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test acts_suggest -- --test-threads=1` passes all 3 new tests (RED before the SQL change, GREEN after)
    - `grep -c "UNION ALL" crates/trackly-app/src/services/act_service.rs` returns at least 1
    - `grep -n "cartridges" crates/trackly-app/src/services/act_service.rs | grep -v "^[0-9]*: *//" | grep -c "holder_name"` returns at least 1 (the new SQL references `cartridges.holder_name`, not just a comment)
    - `cargo clippy -p trackly-app --lib -- -D warnings` reports zero NEW warnings in `act_service.rs` (pre-existing unrelated warnings in other files are out of scope per project convention)
  </acceptance_criteria>
  <done>suggest_person() returns deduplicated, frequency-merged names from both acts.{giver_name|receiver_name} and cartridges.holder_name; soft-deleted cartridges are excluded; existing acts-only suggestions are unaffected (no regression); 3 new tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UI autocomplete input → suggest_person prefix | Untrusted free-text prefix (max 100 chars, already validated) flows into a LIKE pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Tampering | suggest_person SQL (new cartridges arm) | mitigate | Reuses the existing `escape_like()` + bound `params![]` pattern already proven safe for the acts arm — no string interpolation of the prefix; column choice (`holder_name`) is fixed by Rust match, not client-supplied |
| T-12-04-02 | Information Disclosure | cartridges.holder_name exposed via suggestions | accept | holder_name is already fully visible to any caller with ReadData (cartridge list/detail) — suggest_person requires no additional auth beyond what acts.suggestPerson already grants (Action::ReadData equivalent via existing Tauri/HTTP command gate, unchanged by this plan) |
</threat_model>

<verification>
- `cargo test -p trackly-app --test acts_suggest` — all tests pass including the 3 new ones
- `cargo build -p trackly-app` — compiles with no new errors
- Manual: open any cartridge OperationModal install/to_refill form, type a name previously entered only via a cartridge operation (not an act) into «Кому выдал» — suggestion appears
</verification>

<success_criteria>
- suggest_person() aggregates names from both acts and cartridges.holder_name, deduplicated and frequency-merged
- Soft-deleted cartridges excluded from suggestions
- No frontend changes required; no DTO contract change; no migration
- Zero regression to existing acts-only suggestion behavior
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-04-SUMMARY.md` when done
</output>
