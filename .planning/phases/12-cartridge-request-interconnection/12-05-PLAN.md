---
phase: 12-cartridge-request-interconnection
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/V029__printer_cartridge_models.sql
  - crates/trackly-core/src/domain/printers.rs
  - crates/trackly-core/src/domain/cartridges.rs
  - crates/trackly-core/src/ports/printers.rs
  - crates/trackly-app/src/dto/printer.rs
  - crates/trackly-app/src/dto/cartridge.rs
  - crates/trackly-infra/src/repos/printers_sqlite.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/src/services/cartridge_service.rs
  - crates/trackly-app/src/tauri_cmds/cartridges.rs
  - crates/trackly-app/src/http/cartridges.rs
  - crates/trackly-app/src/specta_export.rs
  - crates/trackly-app/tests/cartridges_crud.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
autonomous: true
gap_closure: true
requirements: [D-11, D-12, D-13, D-14, D-15, D-15a]

must_haves:
  truths:
    - "На карточке принтера можно указать, какие модели картриджей (kind_id=1) с ним совместимы — список редактируется"
    - "На карточке модели картриджа можно указать, с какими принтерами (устройствами type_id=2) она совместима — тот же набор связей, редактируемый с другой стороны"
    - "Список картриджей для установки в заявке/принтере фильтруется по: kind_id=1, статус «На складе», заряд Полный(1)/Частичный(2), и модель ∈ совместимые модели этого принтера"
    - "Если у принтера не задана совместимость — список НЕ фильтруется по модели (показываются все kind_id=1 на складе), фронтенд получает признак, что совместимость не задана"
  artifacts:
    - path: "migrations/V029__printer_cartridge_models.sql"
      provides: "Таблица printer_cartridge_models(device_id, cartridge_model_id) — junction FK↔FK, в отличие от free-text cartridge_model_compatibility"
      contains: "CREATE TABLE printer_cartridge_models"
    - path: "crates/trackly-infra/src/repos/printers_sqlite.rs"
      provides: "get_compatible_models_in_tx / get_compatible_models / set_compatible_models_in_tx для printer_cartridge_models"
      contains: "printer_cartridge_models"
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "list() принимает новый фильтр compatible_with_printer_device_id, JOIN на printer_cartridge_models"
      contains: "printer_cartridge_models"
  key_links:
    - from: "crates/trackly-app/src/dto/cartridge.rs"
      to: "crates/trackly-core/src/domain/cartridges.rs"
      via: "CartridgeFilter.compatible_with_printer_device_id -> into_domain()"
      pattern: "compatible_with_printer_device_id"
    - from: "crates/trackly-app/src/tauri_cmds/cartridges.rs"
      to: "crates/trackly-app/src/http/cartridges.rs"
      via: "build_* helper shared by both transports (S-1)"
      pattern: "build_printer_compatible_models"
---

<objective>
Закрыть GAP-12-02 (backend половина): ввести новую junction-таблицу `printer_cartridge_models(device_id, cartridge_model_id)`, связывающую принтер (устройство) с совместимыми моделями картриджей по ID (а не по free-text бренду/модели, как существующая `cartridge_model_compatibility`). Дать возможность редактировать связь с ОБЕИХ сторон (карточка принтера И карточка модели картриджа — одна таблица, две точки входа, D-12). Расширить фильтр картриджей (`CartridgeFilter`) новым параметром «совместимо с принтером», который ограничивает выборку kind_id=1 на складе моделями из этой связки (D-13), либо возвращает признак «совместимость не задана» если связей нет (D-14) — без блокировки выбора.

Purpose: устраняет корневую причину GAP-12-02 — заявка «Замена картриджа» приходит с пустым `cartridge_model_id`, и сейчас единственный способ сузить список картриджей для установки — это free-text таблица `cartridge_model_compatibility`, которая никогда не заполняется и не привязана к конкретному устройству-принтеру.
Output: новая миграция V029, новые domain/DTO поля, новые repo-методы на `SqlitePrinterRepository` (для CRUD связки) и расширение `SqliteCartridgeRepository::list()` (для фильтрации), новые dual-transport команды (`printers_get_compatible_models`, `printers_set_compatible_models`), расширенный `CartridgeFilter` с новым полем и его эффект (совместимость задана/не задана) в `CartridgeListResponse`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md
@.planning/phases/12-cartridge-request-interconnection/12-HUMAN-UAT.md

<interfaces>
<!-- Существующая free-text compatibility таблица (V005) — НЕ удалять, НЕ менять её схему.
     Новая junction-таблица СУЩЕСТВУЕТ ПАРАЛЛЕЛЬНО для device-id-based матчинга. -->

migrations/V005__cartridges.sql (existing, read-only reference):
```sql
CREATE TABLE cartridge_model_compatibility (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    cartridge_model_id INTEGER NOT NULL REFERENCES cartridge_models(id) ON DELETE CASCADE,
    printer_brand     TEXT NOT NULL,
    printer_model     TEXT NOT NULL
);
```

migrations/V025__cartridge_printer_link.sql (existing, read-only reference — pattern for the new migration's header comment style):
```sql
-- D-PRN07-01: link a cartridge to the printer device it is currently
-- installed in. Setter: PrinterService.install_cartridge/remove_cartridge.
-- Getter: PrinterRepository.current_cartridge_for_printer(printer_device_id).
ALTER TABLE cartridges ADD COLUMN current_printer_device_id INTEGER NULL REFERENCES devices(id);
```

`device_types` seed (migrations/V001__init_pragmas_and_lookups.sql, read-only reference):
```sql
INSERT INTO device_types (id, name) VALUES (1, 'Устройство'), (2, 'Принтер');
```
Device-type-by-name subquery idiom (avoid hardcoding `type_id = 2`, follow WR-04 precedent from
crates/trackly-app/src/services/request_service.rs printer_options()):
```sql
WHERE d.type_id = (SELECT id FROM device_types WHERE name = 'Принтер')
```

`PrinterRepository` trait (crates/trackly-core/src/ports/printers.rs, full file already in context —
add new trait methods here, do NOT add `*_in_tx` write helpers to the trait itself, they live as
inherent methods on `SqlitePrinterRepository` per the file's own header comment).

`CartridgeRepository::list()` current WHERE clause (crates/trackly-infra/src/repos/cartridges_sqlite.rs,
lines 945-1017, full function already read this session):
```sql
(?1 = 1 OR c.deleted_at_utc IS NULL)
  AND (?2 IS NULL OR c.status_id = ?2)
  AND (?3 IS NULL OR m.kind_id = ?3)
  AND (?4 IS NULL OR c.model_id = ?4)
  AND (?5 = 0 OR (c.status_id = 1 AND (
        (m.kind_id = 1 AND c.state_id IN (1, 2)) OR
        (m.kind_id = 2 AND c.state_id IN (4, 5))
      )))
```
Bound params order: `include_deleted, status_id, kind_id, model_id, installable_only, [limit, offset]`.
This same WHERE clause string is duplicated in BOTH the COUNT query and the SELECT query in `list()` —
any new predicate must be added to both.

`CartridgeFilter` domain struct (crates/trackly-core/src/domain/cartridges.rs, lines 196-214, already
read this session):
```rust
pub struct CartridgeFilter {
    pub status_id: Option<i64>,
    pub kind_id: Option<i64>,
    pub model_id: Option<i64>,
    pub search: Option<String>,
    pub include_deleted: bool,
    pub installable_only: bool,
}
```

`CartridgeFilter` DTO (crates/trackly-app/src/dto/cartridge.rs, lines 326-355, already read this session) —
plain snake_case wire contract (NO `#[serde(rename_all)]` on this struct, unlike PrinterDto):
```rust
pub struct CartridgeFilter {
    pub status_id: Option<i64>,
    pub kind_id: Option<i64>,
    pub model_id: Option<i64>,
    pub search: Option<String>,
    #[serde(default)]
    pub include_deleted: bool,
    #[serde(default)]
    pub installable_only: bool,
}
impl CartridgeFilter {
    pub fn into_domain(self) -> trackly_core::domain::cartridges::CartridgeFilter { /* 1:1 field copy */ }
}
```

`PrinterDto` (crates/trackly-app/src/dto/printer.rs, lines 1-70, already read this session) — uses
`#[serde(rename_all = "camelCase")]`:
```rust
pub struct PrinterDto {
    pub id: i64,
    pub device_id: i64,
    // ... existing fields unchanged ...
    pub current_cartridge_id: Option<i64>,
    pub version: i64,
}
```
Do NOT add a compatible-models array field onto `PrinterDto` itself — fetch compatibility via a
dedicated command (`printers_get_compatible_models`) instead, to avoid widening this already-large
DTO's `From<PrinterRow>` impl for a feature only needed on the printer detail page and the model
form modal.

`SqliteCartridgeRepository` / `SqlitePrinterRepository` are unit structs (`pub struct X;`) — methods
are effectively static, callable as `SqlitePrinterRepository.method(...)` or via
`SqlitePrinterRepository::method(...)` if no `&self` is taken. Follow existing precedent exactly
(`set_current_cartridge_in_tx`, `upsert_compatibility_in_tx`).

Dual-transport wiring (S-1 convention, crates/trackly-app/src/tauri_cmds/cartridges.rs header comment):
every command has ONE `build_*` async fn taking `(state: &AppCtx, caller: &Identity, ...)params` that
calls `authorize(caller, &Action::X)?` then delegates to the service. A thin `#[tauri::command]
#[specta::specta]` wrapper in tauri_cmds/cartridges.rs AND a thin axum handler in http/cartridges.rs
both call the SAME `build_*` fn. New commands MUST be appended to the `collect_commands![...]` list in
crates/trackly-app/src/specta_export.rs (single registration point — main.rs's invoke_handler consumes
this list generically, no edit needed there) AND added as a `.route(...)` in
crates/trackly-app/src/http/cartridges.rs's `router()` fn.

RBAC actions already defined (crates/trackly-core/src/auth.rs lines 88-113) — reuse, do NOT add a new
variant: `Action::MutatePrinters` (Admin|Manager) gates the printer-side compatibility editor;
`Action::MutateCartridges` (Admin|Manager) gates the cartridge-model-side compatibility editor;
`Action::ReadData` (Admin|Manager) gates plain reads of the compatibility list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration V029 + domain/DTO contracts for printer_cartridge_models</name>
  <files>migrations/V029__printer_cartridge_models.sql, crates/trackly-core/src/domain/printers.rs, crates/trackly-core/src/domain/cartridges.rs, crates/trackly-core/src/ports/printers.rs, crates/trackly-app/src/dto/printer.rs, crates/trackly-app/src/dto/cartridge.rs</files>
  <read_first>
    - migrations/V025__cartridge_printer_link.sql (header-comment style precedent)
    - migrations/V005__cartridges.sql (existing free-text compatibility table — do not touch)
    - crates/trackly-core/src/ports/printers.rs (full file, already in context — `PrinterRepository` trait, add new method signatures here)
    - crates/trackly-core/src/domain/cartridges.rs lines 196-214 (`CartridgeFilter` struct — add new field)
    - crates/trackly-app/src/dto/cartridge.rs lines 326-355 (`CartridgeFilter` DTO + `into_domain()` — mirror the new field)
  </read_first>
  <action>
    Create `migrations/V029__printer_cartridge_models.sql` with a header comment documenting the purpose (D-11/D-12, Phase 12 gap closure — device-id-based printer↔cartridge-model compatibility, distinct from the free-text `cartridge_model_compatibility`). Define `CREATE TABLE printer_cartridge_models (id INTEGER PRIMARY KEY AUTOINCREMENT, device_id INTEGER NOT NULL REFERENCES devices(id) ON DELETE CASCADE, cartridge_model_id INTEGER NOT NULL REFERENCES cartridge_models(id) ON DELETE CASCADE, created_at_utc INTEGER NOT NULL)` with a `CREATE UNIQUE INDEX idx_printer_cartridge_models_unique ON printer_cartridge_models(device_id, cartridge_model_id)` to prevent duplicate links, and a plain (non-unique) index on `cartridge_model_id` alone for the reverse-lookup query (model-side editor needs `WHERE cartridge_model_id = ?`). Do not add a `version`/`deleted_at_utc`/soft-delete columns — this is a pure link table, mutated via DELETE+re-INSERT inside a transaction (mirrors `upsert_compatibility_in_tx`'s pattern for the existing free-text table), not soft-deleted rows.

    In `crates/trackly-core/src/domain/cartridges.rs`, add a new field `pub compatible_with_printer_device_id: Option<i64>` to `CartridgeFilter` (after `installable_only`, doc comment referencing D-13: "Когда задан — ограничивает выборку моделями, связанными с этим устройством-принтером через `printer_cartridge_models`; пустой набор связей не сужает выборку (D-14)").

    In `crates/trackly-core/src/ports/printers.rs`, add two new trait methods to `PrinterRepository`: `fn get_compatible_model_ids(&self, conn: &Self::Conn, device_id: i64) -> Result<Vec<i64>, AppError>` (doc: "Returns cartridge_model_id list linked via printer_cartridge_models; empty Vec means 'not configured' — D-14, caller must not treat empty as a hard filter") and `fn get_compatible_device_ids(&self, conn: &Self::Conn, cartridge_model_id: i64) -> Result<Vec<i64>, AppError>` (doc: "Reverse lookup for the cartridge-model-side editor — devices linked to this model").

    In `crates/trackly-app/src/dto/printer.rs`, add a new small DTO `PrinterCompatibleModelsDto { #[specta(type=i32)] pub device_id: i64, pub model_ids: Vec<i64> }` with `#[serde(rename_all = "camelCase")]` to match this file's existing convention (PrinterDto uses camelCase), placed near the end of the file — this is the read/write payload shape for the new commands, NOT embedded into `PrinterDto` itself (per the `<interfaces>` note above).

    In `crates/trackly-app/src/dto/cartridge.rs`, add `pub compatible_with_printer_device_id: Option<i64>` to the `CartridgeFilter` DTO struct (snake_case, no rename — matches this struct's existing convention) directly after `installable_only`, and mirror it into `into_domain()`'s field-copy block.
  </action>
  <acceptance_criteria>
    - `cargo build -p trackly-infra` succeeds after migration is embedded (refinery picks up `migrations/*.sql` at compile time — confirm via `grep -c "V029" $(find /Users/madsas/Projects/trackly -path '*/target/*' -prune -o -name 'embedded.rs' -print 2>/dev/null)` is not required; instead confirm via `cargo test -p trackly-infra --lib migrations -- --test-threads=1` or any existing migration-smoke test passing)
    - `grep -c "CREATE TABLE printer_cartridge_models" migrations/V029__printer_cartridge_models.sql` returns 1
    - `grep -c "UNIQUE INDEX idx_printer_cartridge_models_unique" migrations/V029__printer_cartridge_models.sql` returns 1
    - `grep -c "compatible_with_printer_device_id" crates/trackly-core/src/domain/cartridges.rs` returns at least 1
    - `grep -c "compatible_with_printer_device_id" crates/trackly-app/src/dto/cartridge.rs` returns at least 2 (struct field + into_domain copy)
    - `cargo build --workspace` succeeds (new trait methods have no impl yet — expected to fail at this point if trait methods are added without impl; if so, this task's "done" state is reached only once Task 2 supplies the impl — treat Task 1+2 as a single RED→GREEN pair, run `cargo build -p trackly-core -p trackly-app` (excluding trackly-infra) to confirm domain/DTO layers alone compile)
  </acceptance_criteria>
  <done>V029 migration file exists with the new junction table + unique index; CartridgeFilter (domain + DTO) carries the new optional printer-device filter field; PrinterRepository trait declares the two new compatibility-lookup methods (no impl yet — supplied in Task 2); PrinterCompatibleModelsDto exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repo impls — compatibility CRUD + cartridge list filter join</name>
  <files>crates/trackly-infra/src/repos/printers_sqlite.rs, crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-app/tests/cartridges_crud.rs</files>
  <read_first>
    - crates/trackly-infra/src/repos/printers_sqlite.rs lines 160-220 (`set_current_cartridge_in_tx` — exact unit-struct static-fn calling convention to mirror)
    - crates/trackly-infra/src/repos/cartridges_sqlite.rs lines 283-330 (`upsert_compatibility_in_tx` / `get_compatibility` — DELETE+re-INSERT pattern to mirror for the new junction table's mutation methods)
    - crates/trackly-infra/src/repos/cartridges_sqlite.rs lines 945-1017 (full `list()` impl — both COUNT and SELECT queries share the WHERE clause string; the new predicate must be added to both, and to the bound-params list, in the same relative position as `installable_only` i.e. after it)
  </read_first>
  <behavior>
    - Test 1: insert a device (type_id=2, "Принтер тест") and two cartridge_models (kind_id=1), link device to model A only via the new junction table. Call `cartridges.list()` with `compatible_with_printer_device_id = Some(device_id)`, `kind_id = Some(1)`, `installable_only = true` → only cartridges of model A appear in results; cartridges of model B (kind_id=1, in stock) are excluded.
    - Test 2: same device, but with ZERO rows in `printer_cartridge_models` for it (no links configured at all) → `cartridges.list()` with `compatible_with_printer_device_id = Some(device_id)` returns ALL kind_id=1 in-stock cartridges unfiltered by model (D-14 "not configured = no narrowing" behavior) — same result set as if `compatible_with_printer_device_id` were `None`.
    - Test 3: `SqlitePrinterRepository::get_compatible_model_ids` round-trips a 2-model link set (insert via the new mutation method, read back via the getter, assert both IDs present in any order); `get_compatible_device_ids` reverse-lookup returns the linked device for a given model_id.
  </behavior>
  <action>
    In `crates/trackly-infra/src/repos/printers_sqlite.rs`, implement the two new `PrinterRepository` trait methods (`get_compatible_model_ids`, `get_compatible_device_ids`) with plain `SELECT cartridge_model_id FROM printer_cartridge_models WHERE device_id = ?1` / `SELECT device_id FROM printer_cartridge_models WHERE cartridge_model_id = ?1` queries. Add two NEW inherent (non-trait) static methods on `SqlitePrinterRepository` for mutation, following the `upsert_compatibility_in_tx` DELETE+re-INSERT pattern exactly: `pub fn set_compatible_models_in_tx(tx: &Transaction<'_>, device_id: i64, model_ids: &[i64], now_utc: i64) -> Result<(), AppError>` (DELETE all rows for `device_id`, then INSERT one row per id in `model_ids`) and `pub fn set_compatible_devices_in_tx(tx: &Transaction<'_>, cartridge_model_id: i64, device_ids: &[i64], now_utc: i64) -> Result<(), AppError>` (DELETE all rows for `cartridge_model_id`, then INSERT one row per id in `device_ids`) — both are two equally-valid write paths into the SAME table from opposite sides (D-12); do not let them diverge in row shape.

    In `crates/trackly-infra/src/repos/cartridges_sqlite.rs`'s `list()` function, add a 6th filter predicate to BOTH the COUNT query and the SELECT query's WHERE clause: `AND (?6 IS NULL OR NOT EXISTS (SELECT 1 FROM printer_cartridge_models pcm WHERE pcm.device_id = ?6) OR c.model_id IN (SELECT cartridge_model_id FROM printer_cartridge_models WHERE device_id = ?6))` — this single predicate encodes both D-13 (filter when links exist) and D-14 (no-op when zero links exist for that device) without a separate code branch. Shift the existing `?6`/`?7` LIMIT/OFFSET placeholders in the SELECT query to `?7`/`?8` to make room. Bind `filter.compatible_with_printer_device_id` as the new `?6` param in both queries, in the same position in the `params![]` macro call as the WHERE clause placement (immediately after `installable_only`).
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test cartridges_crud printer_compatib -- --test-threads=1` passes all 3 new tests (RED before repo impl, GREEN after)
    - `cargo build --workspace` succeeds with zero trait-method-missing errors
    - `grep -c "printer_cartridge_models" crates/trackly-infra/src/repos/cartridges_sqlite.rs` returns at least 2 (COUNT query + SELECT query both reference the join/subquery)
    - `grep -c "fn set_compatible_models_in_tx\|fn set_compatible_devices_in_tx" crates/trackly-infra/src/repos/printers_sqlite.rs` returns 2
  </acceptance_criteria>
  <done>SqlitePrinterRepository exposes working compatibility-link CRUD (both directions); SqliteCartridgeRepository::list() narrows by printer-device compatibility when links exist and passes through unfiltered when they don't; 3 new tests pass; existing cartridges_crud.rs tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service methods + dual-transport commands + RBAC tests</name>
  <files>crates/trackly-app/src/services/cartridge_service.rs, crates/trackly-app/src/tauri_cmds/cartridges.rs, crates/trackly-app/src/http/cartridges.rs, crates/trackly-app/src/specta_export.rs, crates/trackly-app/tests/role_endpoint_matrix.rs</files>
  <read_first>
    - crates/trackly-app/src/services/cartridge_service.rs lines 486-603 (model_list/model_get/model_create — service method shape: spawn_blocking + readers.acquire() for reads, writer.execute(move |conn| {...tx...}) for writes)
    - crates/trackly-app/src/tauri_cmds/cartridges.rs lines 126-180, 330-376 (build_* helper + thin #[tauri::command] wrapper pairing for cartridge_models_* commands — mirror exactly for the new printer-compatibility commands)
    - crates/trackly-app/src/http/cartridges.rs lines 412-460 (router() — .route() registration pattern) and handler_models_list/handler_models_get (handler_* thin wrapper pairing)
    - crates/trackly-app/src/specta_export.rs (full file, already in context — collect_commands! list, single registration point)
    - crates/trackly-app/tests/role_endpoint_matrix.rs (find the highest existing Case N — 12-02-SUMMARY.md notes the file's actual max was Case 32 after round 1; add new cases starting at the next sequential number, do NOT assume a stale number)
  </read_first>
  <behavior>
    - Test 1 (role_endpoint_matrix): `printers_get_compatible_models` is callable by Manager and Admin (Action::ReadData, same gate as printers_get/cartridges_get) — Employee call returns Forbidden/403-equivalent AppError (this is an Admin|Manager management feature, not an employee-facing read; confirmed by cartridges_list/printers_list both gating on Action::ReadData today, which is Admin|Manager only per auth.rs).
    - Test 2 (role_endpoint_matrix): `printers_set_compatible_models` requires `Action::MutatePrinters` (Admin|Manager) — Employee call returns Forbidden/403-equivalent AppError.
    - Test 3 (role_endpoint_matrix): `cartridge_models_set_compatible_devices` requires `Action::MutateCartridges` (Admin|Manager) — Employee call returns Forbidden/403-equivalent AppError.
  </behavior>
  <action>
    In `crates/trackly-app/src/services/cartridge_service.rs` (or alongside the existing printer-facing methods if a `PrinterService` is more appropriate — check which service currently owns printer detail reads; if `PrinterService` exists and owns `PrinterDto` reads/writes, place the printer-side methods there instead of cartridge_service.rs and adjust `files_modified` accordingly in the SUMMARY), add `get_compatible_models(&self, device_id: i64) -> Result<Vec<i64>, AppError>` and `set_compatible_models(&self, device_id: i64, model_ids: Vec<i64>) -> Result<Vec<i64>, AppError>` wrapping the new repo methods (spawn_blocking + readers.acquire() for the getter; writer.execute + transaction + `SqlitePrinterRepository::set_compatible_models_in_tx` + audit_repo insert with `entity_type: "printer_compatibility"`, `action: "set_compatible_models"` + commit, for the setter). Add the model-side mirror `get_compatible_devices(&self, model_id: i64) -> Result<Vec<i64>, AppError>` and `set_compatible_devices(&self, model_id: i64, device_ids: Vec<i64>) -> Result<Vec<i64>, AppError>` on `CartridgeService`.

    In `crates/trackly-app/src/tauri_cmds/cartridges.rs` (and a printers.rs equivalent if the printer-side methods land on PrinterService), add `build_printers_get_compatible_models`/`build_printers_set_compatible_models` and `build_cartridge_models_get_compatible_devices`/`build_cartridge_models_set_compatible_devices` helper fns following the exact `build_cartridge_models_list`/`build_cartridge_models_create` pairing pattern: getter helpers call `authorize(caller, &Action::ReadData)?` (the SAME gate `cartridges_get`/`printers_get` already use today — confirmed Admin|Manager only, NOT all roles; this is a management-feature read, not an employee-facing one, so do not relax it to CreateRequest/ReadRequests); setter helpers call `authorize(caller, &Action::MutatePrinters)?` (printer-side) or `authorize(caller, &Action::MutateCartridges)?` (model-side) respectively. Add the four thin `#[tauri::command] #[specta::specta]` wrappers (`printers_get_compatible_models`, `printers_set_compatible_models`, `cartridge_models_get_compatible_devices`, `cartridge_models_set_compatible_devices`) calling their `build_*` helper.

    In `crates/trackly-app/src/http/cartridges.rs` (and printers.rs http file if applicable), add four thin axum `handler_*` fns mirroring `handler_models_list`/`handler_models_create`'s shape, and register all four as `.route("/api/v1/{command_name}", post(handler_x))` entries in `router()`.

    In `crates/trackly-app/src/specta_export.rs`, append all four new command paths to the `collect_commands![...]` list (no other file needs editing for Tauri registration — `main.rs`'s `invoke_handler(builder.invoke_handler())` consumes this list generically).

    Add the 3 new RBAC test cases to `crates/trackly-app/tests/role_endpoint_matrix.rs`, numbered sequentially starting from the file's current highest case number (read the file to find it — do not assume).
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test role_endpoint_matrix -- --test-threads=1` passes including the 3 new cases
    - `cargo build --workspace` succeeds
    - `grep -c "printers_get_compatible_models\|printers_set_compatible_models\|cartridge_models_get_compatible_devices\|cartridge_models_set_compatible_devices" crates/trackly-app/src/specta_export.rs` returns 4
    - `grep -c "printers_get_compatible_models\|printers_set_compatible_models\|cartridge_models_get_compatible_devices\|cartridge_models_set_compatible_devices" crates/trackly-app/src/http/cartridges.rs` returns at least 4 (route registrations, handler names may differ from command names per project's handler_* convention — confirm route path strings match command names exactly since the frontend calls by command name)
    - `cd /Users/madsas/Projects/trackly/src-tauri 2>/dev/null || true; cargo test -p trackly-app --test export_bindings 2>/dev/null; echo "bindings regen check ran (failure here is non-blocking if test does not exist — verify manually if absent)"`
  </acceptance_criteria>
  <done>Four new dual-transport commands exist (printer-side get/set, model-side get/set compatible links), each gated by the correct Action; RBAC matrix covers read (Admin|Manager, Action::ReadData) and write (Admin|Manager, Action::MutatePrinters/MutateCartridges) for both sides; bindings.ts will regenerate the new command signatures on next `cargo test` run of the specta export integration test (gitignored, regenerated automatically — no manual edit needed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Tauri invoke / HTTP POST → compatibility commands | Caller-supplied `device_id`/`model_ids`/`cartridge_model_id`/`device_ids` arrays cross into SQL |
| RBAC boundary | Read vs write split between all-roles and Admin\|Manager-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | set_compatible_models_in_tx / set_compatible_devices_in_tx | mitigate | All IDs bound via `params![]`, never string-interpolated; FK constraints (`ON DELETE CASCADE`) on `printer_cartridge_models` reject orphan IDs at the DB layer (insert fails with FK violation mapped to AppError, not a silent no-op) |
| T-12-05-02 | Elevation of Privilege | printers_set_compatible_models / cartridge_models_set_compatible_devices | mitigate | Gated by `Action::MutatePrinters` / `Action::MutateCartridges` (Admin\|Manager only) — Employee calls rejected at the `build_*` helper before reaching the service layer (BFLA prevention, ASVS V4) |
| T-12-05-03 | Denial of Service | cartridges_list with compatible_with_printer_device_id subquery | accept | Subquery is a simple indexed lookup (`idx_printer_cartridge_models_unique` covers device_id prefix) against a link table bounded by device/model counts (low cardinality for a single-org LAN deployment) — no pagination-bypass risk |
| T-12-05-SC | Tampering | No new package-manager installs in this plan | accept | Plan touches only Rust/SQL/internal code — no npm/pip/cargo new dependency added, Package Legitimacy Gate not applicable |
</threat_model>

<verification>
- `cargo test -p trackly-app --test cartridges_crud --test role_endpoint_matrix -- --test-threads=1` — all pass
- `cargo build --workspace` — compiles
- `cargo clippy --workspace -- -D warnings` — zero new warnings
- Manual smoke (after 12-07 frontend lands): open a printer card, link a cartridge model, open «Установить картридж» from a request targeting that printer — only the linked model's in-stock cartridges appear
</verification>

<success_criteria>
- New junction table `printer_cartridge_models` exists, editable from both printer and cartridge-model sides via dual-transport commands
- CartridgeFilter gains `compatible_with_printer_device_id`; list() narrows when links exist, passes through unfiltered when absent (D-14)
- RBAC: reads and writes both restricted to Admin|Manager (Action::ReadData for reads, Action::MutatePrinters/MutateCartridges for writes) — this is a management feature, not employee-facing
- No regression to the existing free-text `cartridge_model_compatibility` table or its editor
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-05-SUMMARY.md` when done
</output>
