---
phase: 12-cartridge-request-interconnection
plan: 09
type: execute
wave: 3
depends_on: ["12-06", "12-08"]
files_modified:
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: true
gap_closure: true
requirements: [D-16]

must_haves:
  truths:
    - "При установке нового картриджа в принтер, у которого уже есть картридж «В работе», форма установки показывает блок «Предыдущий картридж»: код+модель (read-only), редактируемое «Состояние заряда» (по умолчанию Пустой), редактируемое «Расположение» (по умолчанию пусто)"
    - "Если у принтера нет текущего картриджа «В работе», блок «Предыдущий картридж» не отображается вообще"
    - "Значения из блока «Предыдущий картридж» передаются на backend (12-06) как часть единого вызова transition(), без отдельного запроса"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "Блок предыдущего картриджа в install-форме, условно показываемый при preFillPrinterId с currentCartridgeId"
      contains: "previousCartridge"
  key_links:
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "printers_get (PrinterDto.currentCartridgeId)"
      via: "printers.get(preFillPrinterId) lookup внутри $effect"
      pattern: "currentCartridgeId"
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "cartridges_transition (CartridgeTransitionPayload::Install.printer_device_id)"
      via: "buildPayload() передаёт preFillPrinterId как printer_device_id"
      pattern: "printer_device_id"
---

<objective>
Закрыть GAP-12-03 (фронтенд-часть, D-16): добавить в форму установки картриджа (`OperationModal.svelte`, request-centric вход) блок «Предыдущий картридж», который показывается, когда принтер заявки уже имеет картридж «В работе» — отображает его код+модель (read-only), и даёт отредактировать «Состояние заряда» (дефолт Пустой) и «Расположение» (дефолт пусто) перед сохранением. Значения уходят в тот же `cartridges_transition` вызов через новое поле `printer_device_id` (добавлено backend-планом 12-06).

Purpose: 12-06 реализовал backend-логику авто-возврата предыдущего картриджа на склад внутри транзакции установки нового, но backend всегда использует ЖЁСТКИЕ дефолты (state_id=3 Пустой, location='') — без возможности пользователя скорректировать их в форме. Этот план добавляет UI-видимость и редактируемость ЭТИХ дефолтов перед отправкой, замыкая D-16's полное требование (показать какой стоял, дать поля для состояния/расположения).
Output: один изменённый файл (`OperationModal.svelte`) — новый блок полей, новый lookup-эффект, изменённый `buildPayload()` для install-ветки.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md
@.planning/phases/12-cartridge-request-interconnection/12-HUMAN-UAT.md

<interfaces>
<!-- This plan lands AFTER 12-06 (backend: printer_device_id field + auto-return
     logic) and AFTER 12-08 (frontend: compatibility filter/warning in the SAME
     file's install branch) — sequential by file ownership (both touch
     OperationModal.svelte's install rendering block and buildPayload()). -->

`CartridgeTransitionPayload::Install` (crates/trackly-app/src/dto/cartridge.rs, extended by 12-06 Task 1):
```rust
Install {
    #[specta(type = i32)] cartridge_id: i64,
    #[specta(type = i32)] version: i64,
    #[specta(type = i32)] date_utc: i64,
    given_by_name: String,
    given_to_name: String,
    location: String,
    #[specta(type = Option<i32>)] printer_device_id: Option<i64>,  // NEW, 12-06
}
```
Serde default for `printer_device_id` is NOT `#[serde(default)]`-annotated by 12-06 (it's a required-but-nullable field, matching this enum's existing convention of no `#[serde(default)]` anywhere) — the frontend `buildPayload()` MUST always include the key (value `null` when no printer context), never omit it, or JSON deserialization on the Rust side will fail with a missing-field error.

`PrinterDto.currentCartridgeId: number | null` (ui/src/bindings-phase6.ts, UNCHANGED — already exists per V025/D-PRN07-01, populated by `printer_service.rs::get()`'s enrichment step). This is the read needed to detect "does the printer already have a cartridge В работе" — call `printers.get(preFillPrinterId)` and read `.currentCartridgeId`. If non-null, that id is the previous cartridge to show in the new block; fetch its full record via `cartridges.get(id)` (existing method, ui/src/features/cartridges/api.ts) to get `code`/`model_brand`/`model_name` for display.

`CartridgeDto` fields needed for display (crates/trackly-app/src/dto/cartridge.rs, UNCHANGED, snake_case per this DTO's convention — confirm exact frontend field naming by checking how `CartridgeSelect.svelte` already renders `{code} — {brand} {name}`, lines ~unknown but pattern confirmed: `model_brand`, `model_name`, `code` are the relevant fields, NOT camelCase since this DTO's existing convention is snake_case unlike PrinterDto/PrinterCompatibleModelsDto):
```
code: string
model_brand: string | null
model_name: string | null
```

`LocationAutocomplete.svelte` (full file, already read, 215 lines) — props `{ value: string; placeholder?: string; id?: string; invalid?: boolean; disabled?: boolean; onChange: (_v: string) => void }`. Reuse EXACTLY this component for the new "Расположение" field in the previous-cartridge block (same component already used for the main install form's own location field at lines 404-412 — two separate instances of the same component, one per cartridge, both on screen at once when the block is visible).

State charge options (existing pattern — grep `state_id` Select usage elsewhere in this codebase, e.g. ReturnToStock branch's "Состояние заряда" field if present in OperationModal.svelte's `op === 'return_to_stock'` branch; reuse the SAME option list/labels: 1=Полный, 2=Частичный, 3=Пустой for kind_id=1 cartridges — default to 3/Пустой per D-16's explicit requirement. NOTE: D-16's previous cartridge is, per the gap description, always a `kind_id=1` cartridge in THIS plan's scope since photo-drum replacement is explicitly out of scope (per CONTEXT.md) — do not add drum-specific state options (4/5/6) to this new block's Select.

Existing install-branch template structure (OperationModal.svelte, current state after 12-08 lands — cartridge picker block around line 343-363, `printerContextHint` around 364-366, then Дата/Кто выдал/Кому выдал/Расположение fields 367-418). The new previous-cartridge block goes IMMEDIATELY AFTER `printerContextHint` and BEFORE the "Дата" field — visually: cartridge picker (if applicable) → printer hint → [NEW: previous-cartridge block, conditional] → Дата → Кто выдал → Кому выдал → Расположение (for the NEW cartridge, unchanged).

`buildPayload()` (OperationModal.svelte, lines 207-260 prior to this plan, exact line numbers may have shifted slightly after 12-08's edits — re-locate by searching for the function name, do not assume line numbers are unchanged) — the `install` branch currently builds a `CartridgeTransitionPayload::Install`-shaped object without `printer_device_id`. Add `printer_device_id: preFillPrinterId ?? null` to that object literal. Additionally, when the new previous-cartridge block is visible (i.e., a previous cartridge was detected), this plan's payload does NOT need a second separate API call for the auto-return — 12-06's backend handles the auto-return server-side INSIDE the same `transition()` call. However, the user-edited "Состояние заряда"/"Расположение" values for the PREVIOUS cartridge are NOT currently passable to the backend's hardcoded auto-return defaults (state_id=3, location='') — this is the GAP this plan must close on the backend-payload-shape level too: re-check whether 12-06's `transition_in_tx` auto-return logic accepts overrides, or whether this plan must ALSO widen `CartridgeTransitionPayload::Install`/`CartridgeTransitionOp::Install` with two more optional fields (`previous_cartridge_state_id: Option<i64>`, `previous_cartridge_location: Option<String>`) so the user's edits in this new UI block actually reach the backend instead of being silently discarded in favor of 12-06's hardcoded defaults. THIS IS A REQUIRED BACKEND TOUCH-UP for this plan — see Task 1's action for the exact small Rust addition needed (kept inside this frontend-labeled plan because it is a thin, tightly-coupled payload-shape extension of 12-06's existing Install variant, not a new architectural concern; if `files_modified` below includes the two Rust files, that is intentional).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread previous-cartridge overrides through the Install payload (backend touch-up)</name>
  <files>crates/trackly-core/src/domain/cartridges.rs, crates/trackly-app/src/dto/cartridge.rs, crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-app/tests/cartridges_lifecycle.rs</files>
  <read_first>
    - crates/trackly-infra/src/repos/cartridges_sqlite.rs (the auto-return block added by 12-06 Task 2 — re-read only the new lines added by that task, locate via `grep -n "current_printer_device_id" crates/trackly-infra/src/repos/cartridges_sqlite.rs` to find the exact line range, since 12-06 already landed before this plan executes)
    - crates/trackly-core/src/domain/cartridges.rs (CartridgeTransitionOp::Install, post-12-06 shape, already carries printer_device_id per 12-06 Task 1)
  </read_first>
  <behavior>
    - Test 1: `Install { printer_device_id: Some(pid), previous_cartridge_state_id: Some(1), previous_cartridge_location: Some("Кабинет 5".into()), .. }` against a printer that has a cartridge "В работе" → after `transition()`, the previous cartridge's `state_id == 1` (Полный, user-overridden, NOT the old hardcoded default 3) and `location == "Кабинет 5"` (user-overridden, NOT empty string default).
    - Test 2 (defaults preserved when fields are None): `Install { printer_device_id: Some(pid), previous_cartridge_state_id: None, previous_cartridge_location: None, .. }` → previous cartridge falls back to `state_id = 3` (Пустой) and `location = ""`, exactly matching 12-06's original hardcoded behavior — proves backward compatibility with 12-06's own tests (re-run 12-06's existing auto-return tests to confirm they still pass unmodified after this widening, since they construct `Install { .. }` without these two new fields, which is valid in Rust only if the fields are added with defaults at the CONSTRUCTION site too — verify every existing `Install { .. }` test-construction site still compiles, since adding non-Optional-wrapped-with-default fields to a struct variant breaks `{ field1, field2 }`-style literal construction without `..Default::default()`; since these new fields ARE `Option<T>`, existing call sites using `{ .. }` rest-pattern in MATCH arms are unaffected, but call sites that CONSTRUCT an `Install { ... }` literal directly — e.g. in tests — DO need the two new fields added, even if `None`).
  </behavior>
  <action>
    Add two new optional fields to `CartridgeTransitionOp::Install` in crates/trackly-core/src/domain/cartridges.rs: `previous_cartridge_state_id: Option<i64>` (doc: "Override для D-16: заряд предыдущего картриджа при авто-возврате; None = дефолт 3 (Пустой)") and `previous_cartridge_location: Option<String>` (doc: "Override для D-16: расположение предыдущего картриджа при авто-возврате; None = дефолт пустая строка"). Mirror both fields into `CartridgeTransitionPayload::Install` in crates/trackly-app/src/dto/cartridge.rs (camelCase is NOT this struct's convention — keep snake_case, matching every other field in this same struct) with `#[specta(type = Option<i32>)]` on the state_id field only (location is already a plain `Option<String>`, no specta override needed, matching the existing `notes: Option<String>` pattern elsewhere in this same enum). Update the `Install` arm of `impl From<CartridgeTransitionPayload> for CartridgeTransitionOp` to forward both new fields. In crates/trackly-infra/src/repos/cartridges_sqlite.rs's auto-return block (added by 12-06), change the hardcoded `state_id = 3` / `location = ''` literals in the previous-cartridge UPDATE statement to use `op`'s `previous_cartridge_state_id.unwrap_or(3)` / `previous_cartridge_location.as_deref().unwrap_or("")` instead (extract these two values from the `CartridgeTransitionOp::Install { previous_cartridge_state_id, previous_cartridge_location, .. }` match arm's destructure at the point where `pid` is already being matched out for the printer-link logic — same `if let CartridgeTransitionOp::Install { printer_device_id: Some(pid), previous_cartridge_state_id, previous_cartridge_location, .. } = op` guard). Update the existing audit `payload_json` for the auto-return (built via `op_payload_json(&CartridgeTransitionOp::ReturnToStock { state_id, location, notes: None })` per 12-06) to use these resolved (overridden-or-default) values rather than the literal `3`/`String::new()` 12-06 originally hardcoded. Update every existing test in crates/trackly-app/tests/cartridges_lifecycle.rs that directly constructs an `Install { .. }` struct literal (both in 12-06's new tests and any pre-existing ones, e.g. `install_changes_status_to_in_use`) to include `previous_cartridge_state_id: None, previous_cartridge_location: None` at each construction site — search via `grep -n "CartridgeTransitionPayload::Install {" crates/trackly-app/tests/cartridges_lifecycle.rs` to find every site needing the two new keys added.
  </action>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0
    - `cargo test -p trackly-app --test cartridges_lifecycle -- --test-threads=1` passes all tests including 12-06's pre-existing ones (no regression) and the 2 new override tests
    - `grep -c "previous_cartridge_state_id\|previous_cartridge_location" crates/trackly-core/src/domain/cartridges.rs` returns at least 2
    - `grep -c "previous_cartridge_state_id\|previous_cartridge_location" crates/trackly-infra/src/repos/cartridges_sqlite.rs` returns at least 2 (actual usage in the UPDATE/payload_json, not just the struct destructure)
    - `cargo clippy --workspace --lib -- -D warnings` reports zero NEW warnings
  </acceptance_criteria>
  <done>Install payload accepts optional previous_cartridge_state_id/previous_cartridge_location overrides; auto-return logic uses them when present, falls back to 12-06's original defaults (3/empty string) when absent; all existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Previous-cartridge UI block in OperationModal.svelte</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <read_first>
    - ui/src/features/cartridges/OperationModal.svelte (re-read the file's CURRENT state after 12-08's edits before making further changes — 12-08 already modified the list-loading effect and warning block in this same file; do not assume the original line numbers from earlier context still apply)
    - ui/src/lib/components/LocationAutocomplete.svelte (full file, already in context above — prop contract for reuse)
  </read_first>
  <behavior>
    - Test 1: `preFillPrinterId` set to a printer whose `currentCartridgeId` (via `printers.get`) is non-null → the previous-cartridge block renders, showing that cartridge's `code` + `model_brand` + `model_name` as read-only text, a "Состояние заряда" select defaulted to Пустой(3), and a "Расположение" LocationAutocomplete defaulted to empty string.
    - Test 2: `preFillPrinterId` set to a printer whose `currentCartridgeId` is null (no cartridge currently installed) → the previous-cartridge block does not render at all.
    - Test 3: `preFillPrinterId` is `undefined` (no printer context, e.g. old D-08 cartridge-centric entry where `cartridge !== null` anyway, OR a future caller that omits it) → the previous-cartridge block does not render (no lookup attempted, avoiding a wasted API call when there is no printer context at all).
    - Test 4: editing the "Состояние заряда" select to Полный(1) and the "Расположение" field to "Кабинет 5", then submitting → `buildPayload()`'s install branch includes `previous_cartridge_state_id: 1` and `previous_cartridge_location: "Кабинет 5"` (not the defaults), alongside `printer_device_id: preFillPrinterId`.
  </behavior>
  <action>
    Add a new `$state` for `previousCartridge: CartridgeDto | null` and `previousCartridgeStateId: number` (default `3`) and `previousCartridgeLocation: string` (default `''`). Add a new `$effect` that runs when `open && op === 'install' && cartridge === null && preFillPrinterId !== undefined`: call `printers.get(preFillPrinterId)` (import `printers` from `'../printers/api'`), and if `.currentCartridgeId !== null`, call `cartridges.get(currentCartridgeId)` to populate `previousCartridge`; if `.currentCartridgeId === null`, set `previousCartridge = null`. Reset `previousCartridgeStateId`/`previousCartridgeLocation` to their defaults whenever the modal's existing reset `$effect` (lines ~93-111 prior to 12-08, re-locate current line numbers) fires, alongside the other field resets already happening there. In the template, immediately after `{#if printerContextHint}...{/if}` and before the "Дата" field, add a new `{#if previousCartridge}` block rendering a `<div class="field field-full previous-cartridge-block">` (new SCSS class, add minimal styling consistent with `.field`/`.field-hint` conventions already in this file's `<style lang="scss">` block) containing: a read-only line `<p class="field-hint">Сейчас в принтере: {previousCartridge.code} ({previousCartridge.model_brand} {previousCartridge.model_name})</p>`, a "Состояние заряда" `<select bind:value={previousCartridgeStateId}>` with options Полный(1)/Частичный(2)/Пустой(3) (use a plain `<select>`+`<option>` if no reusable Select-with-options component fits cleanly, matching this file's existing inline-markup style rather than introducing a new shared component), and a "Расположение" `<LocationAutocomplete value={previousCartridgeLocation} onChange={(v) => (previousCartridgeLocation = v)} id="op-prev-location" placeholder="Расположение" />`. Update `buildPayload()`'s `install` branch to add `printer_device_id: preFillPrinterId ?? null` and, when `previousCartridge !== null`, `previous_cartridge_state_id: previousCartridgeStateId, previous_cartridge_location: previousCartridgeLocation` (when `previousCartridge === null`, pass both as `null` — the backend's `unwrap_or` defaults handle that case identically to omitting them, but the JSON key must still be present per the no-`#[serde(default)]` constraint noted in interfaces above).
  </action>
  <acceptance_criteria>
    - `grep -c "previousCartridge" ui/src/features/cartridges/OperationModal.svelte | grep -v '^0$'` confirms new state/logic present
    - `grep -c "previous_cartridge_state_id\|previous_cartridge_location" ui/src/features/cartridges/OperationModal.svelte` returns at least 2 (in buildPayload's object literal)
    - `grep -c "printer_device_id" ui/src/features/cartridges/OperationModal.svelte` returns at least 1
    - `pnpm --dir ui exec svelte-check` reports zero new errors
    - `pnpm --dir ui build` succeeds
  </acceptance_criteria>
  <done>OperationModal shows a previous-cartridge block (read-only code/model, editable charge state defaulted to Пустой, editable location defaulted to empty) exactly when the target printer currently has a cartridge "В работе"; the block's values flow into the same transition() call via the new payload fields; no block shown when the printer has no current cartridge or no printer context exists at all.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Frontend previous-cartridge overrides → cartridges_transition | Client-supplied state_id (bounded select, 1/2/3 only) and free-text location, both flow into the same authenticated transition() call already gated by Action::MutateCartridges-equivalent (existing cartridges_transition RBAC, unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-09-01 | Tampering | previous_cartridge_state_id client override | mitigate | UI constrains the select to the 3 valid charge-state ids (1/2/3); backend should still validate the value is in {1,2,3} before using it in the UPDATE (existing `state_id` column has no CHECK constraint per schema — if absent, note as accepted risk equivalent to existing ReturnToStock's own state_id field, which already has the same unvalidated-range characteristic today, no NEW exposure introduced by this plan) |
| T-12-09-02 | Tampering | previous_cartridge_location free text | accept | Same trust level as the existing `location` field on every other transition op — already free text, already subject to the same upsert_location_in_tx round-trip; no new injection surface (bound param, not interpolated) |
</threat_model>

<verification>
- `cargo test -p trackly-app --test cartridges_lifecycle` — all tests pass
- `pnpm --dir ui build` succeeds
- `pnpm --dir ui exec svelte-check` reports zero new errors
- Manual: open «Установить картридж» from a request whose printer has a cartridge В работе — previous-cartridge block appears, edit state/location, submit, confirm previous cartridge shows the edited values (not the hardcoded defaults) and the new cartridge is installed
- Manual: open «Установить картридж» from a request whose printer has NO current cartridge — no previous-cartridge block appears, install proceeds normally
</verification>

<success_criteria>
- Previous-cartridge block appears exactly when the target printer has a cartridge "В работе"
- Charge state and location are editable, defaulting to Пустой/empty per D-16
- Edited values reach the backend via the same single transition() call, no second request
- No block and no extra API calls when there is no printer context or no current cartridge
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-09-SUMMARY.md` when done
</output>
