---
phase: 12-cartridge-request-interconnection
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/printers/PrinterListRow.svelte
autonomous: true
gap_closure: true
requirements: [PRN-07]
must_haves:
  truths:
    - "В строке списка принтеров видно Расположение устройства (device_location), а не только IP"
    - "Расположение показывается слева, IP — справа в одной нижней строке"
    - "Если у принтера нет IP, справа показывается «USB» (есть usb_host_device_id) или «—» (нет ни IP, ни USB)"
    - "Если у принтера нет device_location, слева показывается «—», строка не падает на null"
  artifacts:
    - path: "ui/src/features/printers/PrinterListRow.svelte"
      provides: "Рендер device_location слева + IP справа в .bottom-строке"
      contains: "printer.deviceLocation"
  key_links:
    - from: "ui/src/features/printers/PrinterListRow.svelte"
      to: "PrinterDto.deviceLocation (bindings-phase6.ts)"
      via: "$derived locationText из printer.deviceLocation"
      pattern: "printer\\.deviceLocation"
---

<objective>
Закрыть GAP-12-09 (B1): в списке принтеров (`PrinterListRow.svelte`) под именем
показывается только IP — поля Расположения устройства нет вовсе. Бэкенд УЖЕ отдаёт
`device_location` в list-DTO: `SELECT_PRINTERS` джойнит `locations`
(printers_sqlite.rs:30, idx 9), `PrinterRow.device_location` заполнен, сервис `list()`
маппит через `PrinterDto::from(row)` (printer_service.rs:154), TS-биндинг уже несёт
`deviceLocation: string | null` (bindings-phase6.ts:20). Значит это ЧИСТО фронтенд-правка
одного компонента: показать Расположение слева и IP справа в нижней строке.

Purpose: оператор должен в списке видеть, где физически стоит принтер, не открывая
карточку.
Output: обновлённый `PrinterListRow.svelte` с двухколоночной нижней строкой
(слева Расположение, справа IP/USB/—).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-cartridge-request-interconnection/12-HUMAN-UAT.md

<interfaces>
<!-- Уже сгенерированный биндинг — НЕ менять, использовать как есть -->
From ui/src/bindings-phase6.ts (PrinterDto):
  id: number;
  deviceName: string | null;
  deviceLocation: string | null;   // <- это поле уже есть, использовать его
  ipAddress: string | null;
  usbHostDeviceId: number | null;
  status: string | null;
  hasAlert: boolean;
  tonerLevels: ... | null;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Показать Расположение слева + IP/USB справа в строке списка принтеров</name>
  <files>ui/src/features/printers/PrinterListRow.svelte</files>
  <read_first>
    - ui/src/features/printers/PrinterListRow.svelte — текущий компонент целиком.
      Ключевые места: `locationLabel` $derived (строки 41-43, сейчас показывает IP),
      `displayName` (45), разметка `.bottom` (строки 93-98), SCSS `.location` (167-169)
      и `.toner-hint` (171-174).
    - ui/src/bindings-phase6.ts — убедиться в точном имени поля `deviceLocation`
      (camelCase) и его типе `string | null` (строка 20).
  </read_first>
  <action>
    В `PrinterListRow.svelte`:
    1. Ввести два отдельных `$derived`:
       - `locationText = printer.deviceLocation ?? '—'` (Расположение, левая часть).
       - `ipText` — правая часть: `printer.ipAddress` если задан, иначе
         `printer.usbHostDeviceId` непуст → `'USB'`, иначе `'—'`.
       Переименовать/удалить старый `locationLabel` (он сейчас отдаёт IP) — его роль
       забирает `ipText`. НЕ оставлять мёртвый `locationLabel`.
    2. В разметке `.bottom` заменить единственный `<span class="location">{locationLabel}</span>`
       на двухколоночную раскладку в ОДНУ строку: слева `<span class="row-location">{locationText}</span>`,
       справа `<span class="row-ip">{ipText}</span>`. Тонер-хинт (`tonerSummary`) сохранить —
       размещать после Расположения, перед/около IP по месту (не ломать существующий
       условный рендер `{#if tonerSummary}`). IP-блок должен прижиматься вправо
       (`margin-left: auto` на правой части ИЛИ flex `justify-content: space-between`
       для контейнера `.bottom`).
    3. В SCSS: `.bottom` уже `display:flex; gap; align-items:center` — добавить
       выравнивание так, чтобы IP был справа (например `.row-ip { margin-left: auto; }`).
       `font-variant-numeric: tabular-nums` оставить на IP-части (это числовой IP),
       не на Расположении.
    НЕ менять props-контракт, не трогать backend, не регенерировать биндинги.
  </action>
  <verify>
    <automated>pnpm --dir ui exec svelte-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir ui exec svelte-check` → 0 errors.
    - `grep -c "printer.deviceLocation" ui/src/features/printers/PrinterListRow.svelte` ≥ 1.
    - Источник: в файле НЕТ строки, где `locationLabel` всё ещё равен IP
      (`grep -n "locationLabel" ui/src/features/printers/PrinterListRow.svelte` → пусто,
      либо переименовано в ip-ориентированное имя).
    - Поведение: при `deviceLocation=null` слева рендерится «—»; при `ipAddress=null` и
      `usbHostDeviceId!=null` справа «USB»; при обоих null справа «—» (видно из логики
      `$derived`, без runtime-падения на null).
  </acceptance_criteria>
  <done>
    В нижней строке элемента списка принтеров слева видно Расположение, справа — IP
    (или USB / —); svelte-check зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend list-DTO → webview render | `device_location` уже пересекает границу как часть существующего `PrinterDto`; новых данных по проводу не добавляется |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16-01 | Information Disclosure | PrinterListRow рендер device_location | accept | `device_location` — это название кабинета/локации, уже отдаётся в detail-DTO и list-DTO; не секрет (community/IP-внутренние данные не добавляются). Read-доступ к принтерам уже закрыт от Employee в Phase 10 (authorize(ReadData)); этот план не трогает эндпоинты. |
| T-12-16-02 | Tampering | npm/pip/cargo installs | mitigate | Плана installs не содержит — изменяется один существующий .svelte файл, новых зависимостей нет. |
</threat_model>

<verification>
- `pnpm --dir ui exec svelte-check` → 0 errors.
- `pnpm --dir ui build` собирается без ошибок (для server-mode/LAN-браузера — см. MEMORY:
  dev browser testing needs ui build).
- Визуально (после `pnpm --dir ui build` + запуск): в списке принтеров видно
  Расположение слева, IP справа.
</verification>

<success_criteria>
В каждой строке списка принтеров отображается device_location (или «—»), IP прижат
вправо (или «USB»/«—»), svelte-check и build зелёные, backend не менялся.
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-16-SUMMARY.md` when done
</output>
