---
phase: 12-cartridge-request-interconnection
plan: 08
type: execute
wave: 2
depends_on: ["12-05"]
files_modified:
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: true
gap_closure: true
requirements: [D-13, D-14]

must_haves:
  truths:
    - "В модалке установки картриджа (request-centric вход) список картриджей фильтруется по совместимости с принтером заявки, если совместимость настроена (D-13)"
    - "Если для принтера совместимость НЕ настроена (нет связей в printer_cartridge_models), список показывает ВСЕ картриджи kind_id=1 на складе без блокировки формы, с предупреждением «Совместимость не задана — проверьте вручную» (D-14)"
    - "Список установки больше никогда не включает фотобарабаны (kind_id=2) — старый WR-02 placeholder текст полностью заменён"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "Список установки фильтруется по compatible_with_printer_device_id + kind_id=1; новое предупреждение о ненастроенной совместимости"
      contains: "compatible_with_printer_device_id"
  key_links:
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "cartridges_list (CartridgeFilter.compatible_with_printer_device_id)"
      via: "cartridges.list({...}) call inside the list-loading $effect"
      pattern: "compatible_with_printer_device_id"
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "printers_get_compatible_models"
      via: "printers.getCompatibleModels(preFillPrinterId) — проверка, настроена ли совместимость"
      pattern: "getCompatibleModels"
---

<objective>
Закрыть GAP-12-02 (фронтенд-часть, D-13/D-14): заменить текущий WR-02 placeholder-фильтр в `OperationModal.svelte` (фильтрация только по `model_id`, предупреждение «Модель не указана») на фильтрацию по реальной связке принтер↔модель из `printer_cartridge_models` (12-05's `compatible_with_printer_device_id`), плюс жёсткое исключение фотобарабанов (`kind_id=1`) из списка установки.

Purpose: сейчас список картриджей для установки в модалке (request-centric вход) либо фильтруется по `cartridgeModelId` (которое почти всегда `undefined`, поскольку заявки приходят без явной модели — WR-02), либо не фильтруется вообще — пользователь видит общий список «На складе» без учёта того, какие картриджи реально подходят к принтеру заявки. 12-05 уже добавил backend-фильтр; этот план подключает его к UI и меняет текст предупреждения на формулировку из D-14.
Output: один изменённый файл (`OperationModal.svelte`) — новая логика загрузки списка картриджей (двух-этапная: проверка наличия связей + фильтрованный список) и новый текст предупреждения.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md
@.planning/phases/12-cartridge-request-interconnection/12-HUMAN-UAT.md

<interfaces>
<!-- OperationModal.svelte full file already read (519 lines). Exact current
     state of the two blocks this plan replaces: -->

Current placeholder warning (lines 122-131, TO BE REPLACED):
```javascript
const noModelScopeWarning = $derived(
  op === 'install' && cartridge === null && cartridgeModelId === undefined
    ? 'Модель не указана — проверьте совместимость вручную'
    : null,
);
```

Current list-loading effect (lines 138-167, TO BE REPLACED):
```javascript
$effect(() => {
  if (!(open && op === 'install' && cartridge === null)) return;
  cartridgeListLoading = true;
  cartridges
    .list(
      {
        status_id: 1,
        installable_only: true,
        model_id: cartridgeModelId ?? null,
        kind_id: null,
        search: null,
        include_deleted: false,
      },
      { offset: 0, limit: 200 },
    )
    .then((res) => { cartridgeOptions = res.items; })
    .catch(...)
    .finally(() => { cartridgeListLoading = false; });
});
```

Props (lines 21-42, UNCHANGED by this plan — `preFillPrinterId?: number` already exists and is exactly the device_id needed for the new filter):
```typescript
interface Props {
  open: boolean;
  op: 'install' | 'return_to_stock' | 'to_refill' | 'from_refill' | 'write_off';
  cartridge: CartridgeDto | null;
  preFillPrinterId?: number;
  cartridgeModelId?: number;
  prefillLocation?: string;
  prefillGivenToName?: string;
  onClose: () => void;
  onSuccess: (_cartridgeId: number) => void | Promise<void>;
}
```

`CartridgeFilter` (crates/trackly-app/src/dto/cartridge.rs, extended by 12-05 — snake_case, NO camelCase rename, confirmed by existing frontend call site using `status_id`/`installable_only`/`model_id`/`kind_id`/`search`/`include_deleted` snake_case keys):
```rust
pub struct CartridgeFilter {
    pub status_id: Option<i64>,
    pub kind_id: Option<i64>,
    pub model_id: Option<i64>,
    pub search: Option<String>,
    pub include_deleted: bool,
    pub installable_only: bool,
    pub compatible_with_printer_device_id: Option<i64>,  // NEW, added by 12-05
}
```
The SQL predicate behind `compatible_with_printer_device_id` (cartridges_sqlite.rs, 12-05) already encodes BOTH D-13 (filter when links exist) and D-14 (no-op when zero links) in one WHERE clause — meaning passing `compatible_with_printer_device_id: preFillPrinterId ?? null` into `cartridges.list()` is SUFFICIENT for the actual filtering behavior. The only reason this plan ALSO needs a separate `printers.getCompatibleModels()` call is to DECIDE whether to show the D-14 warning text (the list-filtering SQL already self-adjusts, but the UI still needs to know "were there zero links" to decide whether to render the warning, since an empty result list and "filter didn't narrow because unconfigured" are different UI states).

`printers.getCompatibleModels(deviceId)` (added in 12-07, SAME WAVE — file ownership note: 12-07 modifies `ui/src/features/printers/api.ts`, this plan does NOT touch that file, only CONSUMES the method it adds; if 12-07 has not yet executed when this plan runs, `printers.getCompatibleModels` will not exist yet — this plan's executor MUST check `ui/src/features/printers/api.ts` for the method's existence before importing it, and if absent, add a minimal one-line wrapper inline as a defensive measure: `apiCall<{ deviceId: number; modelIds: number[] }>('printers_get_compatible_models', { deviceId })` called directly via `import { apiCall } from '$lib/api/client'` instead of importing from the printers api module, to avoid a hard cross-plan ordering dependency within the same wave).

`noModelScopeWarning`'s NEW required text (D-14, exact wording, do not paraphrase): "Совместимость не задана — проверьте вручную".

`CartridgeSelect.svelte` (full file, already read, 117 lines) — unchanged by this plan; it already renders an empty-state "Нет подходящих картриджей на складе" when `options.length === 0`, which is DIFFERENT from and complementary to the new D-14 warning (empty list vs. unconfigured-but-nonempty-list are distinct UI states, both can coexist — empty list still shows when zero kind_id=1 in-stock cartridges exist regardless of compatibility config).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace placeholder filter/warning with compatibility-aware filter</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <read_first>
    - ui/src/features/cartridges/OperationModal.svelte (lines 1-330, already in context above — props, state, the two blocks being replaced, and the `effectiveCartridge`/`isDrum` derived values)
    - ui/src/features/printers/api.ts (current state — check whether `getCompatibleModels` exists yet per the interfaces note above; if 12-07 already landed, import from there; otherwise use the defensive inline `apiCall` fallback)
  </read_first>
  <behavior>
    - Test 1 (D-13, configured compatibility): `preFillPrinterId` set to a device with 2 linked kind_id=1 models → `cartridgeOptions` after load contains only cartridges whose `model_id` is one of those 2 models (verify by mocking/observing the `cartridges.list()` call args include `compatible_with_printer_device_id: preFillPrinterId` and `kind_id: 1`); `noModelScopeWarning` is `null` (no warning shown, compatibility IS configured).
    - Test 2 (D-14, unconfigured compatibility): `preFillPrinterId` set to a device with ZERO rows in `printer_cartridge_models` → `cartridgeOptions` contains ALL kind_id=1 in-stock cartridges (unfiltered by model, per the backend's D-14 self-adjusting predicate); `noModelScopeWarning` renders exactly "Совместимость не задана — проверьте вручную".
    - Test 3 (no printer context at all, D-08 old cartridge-centric entry — `cartridge !== null`): the list-loading effect does not run at all (existing `cartridge === null` guard unchanged), no warning shown (the warning's condition must also gate on `cartridge === null`, matching the list-loading guard, to avoid showing a spurious warning on the old entry point where there is no cartridge picker rendered in the first place).
    - Test 4 (photo-drum exclusion, regression-proof): even when `preFillPrinterId` is undefined (no printer context, e.g. old request flow before D-13 callers existed) and no `cartridgeModelId` either, the list call still always passes `kind_id: 1` — verifying the OLD `kind_id: null` behavior is fully gone, not just conditionally overridden.
  </behavior>
  <action>
    Replace the `noModelScopeWarning` derived value (lines 122-131) with a new `$state<boolean>` flag (e.g. `compatibilityUnconfigured`) set by a new `$effect` that runs whenever `open && op === 'install' && cartridge === null && preFillPrinterId !== undefined`: call `printers.getCompatibleModels(preFillPrinterId)` (or the defensive inline `apiCall` per the interfaces note), and set `compatibilityUnconfigured = (result.modelIds.length === 0)`. When `preFillPrinterId === undefined` (no printer context at all — distinct from "configured with zero links"), do NOT show the new warning either (mirrors the old behavior's intent of only warning when there's ambiguity to surface, but the NEW wording in this plan focuses specifically on "совместимость не задана" which only makes sense when a printer context exists at all; if no printer context exists, there's no compatibility relationship to be missing — leave a `// TODO` comment is unnecessary, just gate the new effect itself on `preFillPrinterId !== undefined` and default `compatibilityUnconfigured` to `false` otherwise). Render the warning via `{#if compatibilityUnconfigured}<span class="field-warning">Совместимость не задана — проверьте вручную</span>{/if}` in place of the old `{#if noModelScopeWarning}` block (same template location, lines 359-361). Replace the list-loading `$effect`'s `cartridges.list(...)` call args (lines 138-167): change `kind_id: null` to `kind_id: 1` (always exclude photo-drums from the install picker, per D-13's explicit requirement — this list is cartridge-only, drums are replaced via a separate flow per the CONTEXT.md's "Out of scope: separate photo-drum replacement flow" note) and change `model_id: cartridgeModelId ?? null` to ALSO pass `compatible_with_printer_device_id: preFillPrinterId ?? null` as a new key in the same filter object (keep `model_id: cartridgeModelId ?? null` as-is — both filters can coexist since the backend predicate for `compatible_with_printer_device_id` only narrows by model, same axis as `model_id`; if a caller somehow supplies both a specific `cartridgeModelId` AND a `preFillPrinterId`, the narrower of the two effectively wins via SQL `AND` semantics — no special-casing needed in the frontend).
  </action>
  <acceptance_criteria>
    - `grep -c "compatible_with_printer_device_id" ui/src/features/cartridges/OperationModal.svelte` returns at least 1
    - `grep -c "kind_id: 1" ui/src/features/cartridges/OperationModal.svelte` returns at least 1
    - `grep -c "kind_id: null" ui/src/features/cartridges/OperationModal.svelte` returns 0 (old unfiltered-kind behavior fully removed from the install list-loading call)
    - `grep -c "Совместимость не задана — проверьте вручную" ui/src/features/cartridges/OperationModal.svelte` returns at least 1
    - `grep -c "Модель не указана — проверьте совместимость вручную" ui/src/features/cartridges/OperationModal.svelte` returns 0 (old warning text fully removed, not left dead/unreachable)
    - `pnpm --dir ui exec svelte-check` reports zero new errors
    - `pnpm --dir ui build` succeeds
  </acceptance_criteria>
  <done>Install picker filters by printer compatibility (when configured) and always excludes photo-drums; D-14 warning shows exactly when a printer context exists but has zero configured links; old WR-02 placeholder warning text and unfiltered kind_id are fully removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Frontend filter object → cartridges_list | Client-supplied filter fields, all numeric ids or null, no free text beyond existing `search` (unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-08-01 | Information Disclosure | printers.getCompatibleModels called from install picker | accept | Same Action::ReadData gate as printers_get/cartridges_get (12-05) — caller already has Admin/Manager-equivalent install privileges (Action::CreateRequest path leads here, but the compatibility-check call itself requires ReadData; if an Employee-driven flow ever calls this without ReadData rights, the 403 surfaces as a caught error and falls back to `compatibilityUnconfigured = false`, a fail-safe default that shows no list change is needed since this is a UX hint only, not a security boundary) |
</threat_model>

<verification>
- `pnpm --dir ui build` succeeds
- `pnpm --dir ui exec svelte-check` reports zero new errors
- Manual: open «Установить картридж» from a request whose printer has configured compatibility — list shows only matching models, no warning
- Manual: open «Установить картридж» from a request whose printer has NO configured compatibility — list shows all kind_id=1 in-stock cartridges, warning visible
- Manual: confirm no photo-drum ever appears in the install picker regardless of compatibility config
</verification>

<success_criteria>
- Install picker list narrows by printer_cartridge_models compatibility when configured
- Install picker list falls back to unfiltered kind_id=1 stock with a clear warning when compatibility is unconfigured
- Photo-drums never appear in the install picker
- Old WR-02 placeholder text and logic fully removed
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-08-SUMMARY.md` when done
</output>
