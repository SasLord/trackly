# Phase 12: Взаимосвязь картриджной заявки - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-22
**Phase:** 12-cartridge-request-interconnection
**Areas discussed:** Выбор картриджа, Кому отдал, Авто-локация, Связь+входы

---

## Выбор картриджа

| Option | Description | Selected |
|--------|-------------|----------|
| Совместимые + на складе + устанавливаемый заряд | Фильтр по cartridgeModelId из заявки, только «На складе» с зарядом Полный(1)/Частичный(2) | ✓ |
| Все на складе с устанавливаемым зарядом | Без фильтра совместимости — любой картридж «На складе» Полный/Частичный | |
| Совместимые по умолчанию + переключатель «показать все» | По умолчанию совместимые, галочка снять фильтр | |

**User's choice:** Совместимые + на складе + устанавливаемый заряд (рекомендованный)
**Notes:** Максимум взаимосвязи. `RequestDto.cartridgeModelId` уже хранит нужную модель.

---

## Кому отдал (given_to_name)

| Option | Description | Selected |
|--------|-------------|----------|
| Предзаполнить заявителем, редактируемо | Подставить requesterName, специалист может поправить | ✓ |
| Предзаполнить и заблокировать | Всегда = заявитель, без редактирования | |

**User's choice:** Предзаполнить заявителем, редактируемо (рекомендованный)
**Notes:** Заявку мог создать один, а картридж забрать другой.

---

## Авто-локация

| Option | Description | Selected |
|--------|-------------|----------|
| Предзаполнить, редактируемо | Подставить location принтера, можно поправить через LocationAutocomplete | ✓ |
| Предзаполнить, только если пусто | Подставлять только когда у принтера задана локация | |
| Без авто-локации | Оставить ручной ввод (как сейчас) | |

**User's choice:** Предзаполнить, редактируемо (рекомендованный)
**Notes:** Если у принтера локация пустая — поле остаётся пустым.

---

## Связь установленного картриджа + судьба старого входа

| Option | Description | Selected |
|--------|-------------|----------|
| Записать completedCartridgeId + оставить оба входа | Запись связи в заявку + история; старый cartridge-centric вход без изменений | ✓ |
| Записать связь, старый вход убрать | Установка только из заявки | |
| Связь не писать | completedCartridgeId=null | |

**User's choice:** Записать completedCartridgeId + оставить оба входа (рекомендованный)
**Notes:** Картридж иногда ставят вне заявок — старый вход остаётся как fallback.

---

## Claude's Discretion

- Fallback при `cartridgeModelId=null` — показать все картриджи на складе с устанавливаемым зарядом.
- Пустое состояние, если совместимых картриджей нет — понятное сообщение, без блокировки.
- Конкретная форма селектора картриджа (список/поиск) и точка его рендера в OperationModal.
- requesterName как логин/AD — покрыто редактируемым полем.

## Deferred Ideas

- Массовая установка нескольких картриджей по одной заявке — отдельная фаза.
- Создание/заправка картриджа прямо из заявки — вне scope.
- Изменение lifecycle/статусов самих заявок — вне scope.
