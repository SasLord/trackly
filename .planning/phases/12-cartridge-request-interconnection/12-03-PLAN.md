---
phase: 12-cartridge-request-interconnection
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - ui/src/lib/components/CartridgeSelect.svelte
  - ui/src/features/cartridges/OperationModal.svelte
  - ui/src/features/requests/RequestDetail.svelte
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-08]

must_haves:
  truths:
    - "Специалист открывает «Установить картридж» из заявки cartridge_replace и видит список совместимых картриджей на складе (статус На складе, заряд Полный/Частичный, модель = модель заявки) (D-01/D-02/D-03)"
    - "Поля «Кому отдал» и «Расположение» предзаполнены из заявки (заявитель, расположение принтера), но остаются редактируемыми (D-04/D-05)"
    - "После установки заявка переходит в Выполнена с completedCartridgeId установленного картриджа, история показывает код+модель (D-06)"
    - "Старый вход «картридж → Установить в принтер» (без заявки) продолжает работать без изменений (D-08)"
  artifacts:
    - path: "ui/src/lib/components/CartridgeSelect.svelte"
      provides: "Флэт-список картриджей для выбора в OperationModal (без optgroup)"
      min_lines: 60
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "Селектор картриджа + auto-prefill props (cartridgeModelId, prefillLocation, prefillGivenToName) + onSuccess(cartridgeId)"
      contains: "CartridgeSelect"
    - path: "ui/src/features/requests/RequestDetail.svelte"
      provides: "handleInstallSuccess(cartridgeId) передаёт linkedCartridgeId в complete"
      contains: "linkedCartridgeId: cartridgeId"
  key_links:
    - from: "ui/src/features/requests/RequestDetail.svelte"
      to: "ui/src/features/cartridges/OperationModal.svelte"
      via: "props cartridgeModelId/prefillLocation/prefillGivenToName + onSuccess callback"
      pattern: "prefillLocation"
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "ui/src/features/cartridges/api.ts"
      via: "cartridges.list({ status_id: 1, installable_only: true, model_id })"
      pattern: "installable_only"
    - from: "ui/src/features/requests/RequestDetail.svelte"
      to: "ui/src/features/requests/api.ts"
      via: "requests.transition({op:'complete', linkedCartridgeId: cartridgeId})"
      pattern: "linkedCartridgeId: cartridgeId"
---

<objective>
Замкнуть фронтенд: дать `OperationModal` способ показать список совместимых
картриджей на складе и выбрать один (D-01/D-02/D-03), предзаполнить «Кому отдал»
и «Расположение» данными заявки, оставив поля редактируемыми (D-04/D-05), и
прокинуть id выбранного картриджа обратно в `RequestDetail`, чтобы `complete`
записал реальный `linkedCartridgeId` вместо `null` (D-06). Старый
cartridge-centric вход (меню картриджа) не меняется (D-08).

Purpose: Это последний шаг цепочки — backend (Wave 1/2) уже умеет фильтровать
картриджи и обогащать историю, но без UI пользователь не может этим
воспользоваться. После этого плана фича end-to-end работает.
Output: новый `CartridgeSelect.svelte`, расширенный `OperationModal.svelte` с
внутренним state выбранного картриджа, `RequestDetail.svelte` с полным
проброс данных заявки и передачей `cartridgeId` в `complete`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md

<!-- Wave 1/2 дали: CartridgeFilter.installable_only (snake_case wire), RequestDto.printerLocation
     (camelCase wire — RequestDto несёт rename_all=camelCase), RequestService::transition обогащает
     историю автоматически на бэкенде при непустом linkedCartridgeId — фронту достаточно передать id. -->

<interfaces>
<!-- GroupedPrinterSelect.svelte — ПОЛНЫЙ эталон для CartridgeSelect.svelte (149 строк), упростить без optgroup -->
```svelte
<script lang="ts">
  import type { RequestPrinterOptionDto } from '../../bindings-phase6';
  interface Props {
    options: RequestPrinterOptionDto[];
    value: string;
    disabled?: boolean;
    invalid?: boolean;
    id?: string;
    onchange?: (_value: string) => void;
  }
  const { options, value = $bindable(''), disabled = false, invalid = false, id, onchange }: Props = $props();
</script>
<div class="select-wrapper">
  <select {id} {disabled} class="select" class:invalid {value} onchange={(e) => {
    const v = (e.currentTarget as HTMLSelectElement).value;
    onchange?.(v);
  }}>
    <option value="">Выберите принтер</option>
    {#if options.length === 0}
      <option value="" disabled>Принтеры не найдены</option>
    {:else}
      {#each groups as [label, printers] (label)}
        <optgroup label={label}>
          {#each printers as p (p.id)}<option value={String(p.id)}>{p.name || `Принтер #${p.id}`}</option>{/each}
        </optgroup>
      {/each}
    {/if}
  </select>
  <svg class="caret" width="12" height="12" viewBox="0 0 12 12" fill="none" aria-hidden="true">
    <path d="M2 4l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" />
  </svg>
</div>
<!-- SCSS (lines 88-149): .select-wrapper/.select/.caret с design tokens --color-bg/--color-border/--radius-sm/--color-accent/--color-accent-focus/--color-destructive/--color-surface-sunken/--color-text-secondary/--color-text-primary/--color-text-muted — копировать 1:1, убрав только optgroup-специфичный блок стилей. -->
```

<!-- CartridgeDto форма — нужные поля для рендера опции (код, модель, заряд) -->
<!-- crates/trackly-app/src/dto/cartridge.rs CartridgeDto (snake_case, нет rename_all): -->
```typescript
// из ui/src/bindings.ts (регенерирован Wave 2) — ориентировочная форма:
interface CartridgeDto {
  id: number;
  code: string;            // "C-000042"
  model_id: number;
  model_brand: string | null;
  model_name: string | null;
  model_kind_id: number | null;
  status_id: number;
  status_name: string | null;
  state_id: number | null;
  state_name: string | null;   // "Полный" / "Частичный" / ...
  location: string | null;
  holder_name: string | null;
  notes: string | null;
  version: number;
  // ...
}
```

<!-- cartridges.list() клиент — ui/src/features/cartridges/api.ts, lines 22-24 (сигнатура НЕ меняется) -->
```typescript
export const cartridges = {
  list: (filter: CartridgeFilter, pagination: Pagination) =>
    apiCall<CartridgeListResponse>('cartridges_list', { filter, pagination }),
  // ...
  transition: (payload: CartridgeTransitionPayload) =>
    apiCall<CartridgeDto>('cartridges_transition', { payload }),
};
```

<!-- OperationModal.svelte — ТЕКУЩИЙ полный props/state контракт (lines 18-44) -->
```typescript
type Op = 'install' | 'return_to_stock' | 'to_refill' | 'from_refill' | 'write_off';
interface Props {
  open: boolean;
  op: Op;
  cartridge: CartridgeDto | null;
  preFillPrinterId?: number;
  onClose: () => void;
  onSuccess: () => void;
}
const { open, op, cartridge, preFillPrinterId, onClose, onSuccess }: Props = $props();

let dateIso = $state('');
let givenByName = $state('');
let givenToName = $state('');
let location = $state('');
let stateId = $state(3);
let notes = $state('');
let submitting = $state(false);
```

<!-- Текущий reset-эффект (lines 57-74) -->
```typescript
$effect(() => {
  void op;
  if (open) {
    const now = new Date();
    const y = now.getFullYear();
    const m = String(now.getMonth() + 1).padStart(2, '0');
    const d = String(now.getDate()).padStart(2, '0');
    dateIso = `${y}-${m}-${d}`;
    givenByName = '';
    givenToName = '';
    location = '';
    notes = '';
    stateId = defaultStateId;
    locationError = '';
    givenByError = '';
    givenToError = '';
  }
});
```

<!-- Текущий install-branch JSX (lines 246-301) — printerContextHint блок, дата/кто выдал/кому выдал/расположение -->
<!-- Текущий handleSubmit (lines 208-227) -->
```typescript
async function handleSubmit() {
  if (!cartridge || submitting) return;
  if (!validate()) return;
  submitting = true;
  try {
    await cartridges.transition(buildPayload());
    onSuccess();
    onClose();
    pushToast('success', `Операция выполнена успешно.`);
  } catch (e: unknown) { /* ... */ } finally { submitting = false; }
}
```

<!-- RequestDetail.svelte — ТЕКУЩИЙ handleInstallSuccess + JSX проброс (lines 299-323, 581-591) -->
```typescript
async function handleInstallSuccess() {
  if (!request) return;
  operationModalOpen = false;
  try {
    await requests.transition({
      op: 'complete', requestId: request.id, version: request.version,
      notes: null, linkedCartridgeId: null,
    });
    pushToast('success', 'Заявка выполнена');
    onTransition();
  } catch (e: unknown) { /* ... */ onTransition(); }
}
```
```svelte
{#if request !== null}
  <OperationModal
    open={operationModalOpen}
    op="install"
    cartridge={null}
    preFillPrinterId={request.printerDeviceId ?? undefined}
    onClose={() => (operationModalOpen = false)}
    onSuccess={handleInstallSuccess}
  />
{/if}
```

<!-- RequestDto поля, нужные для проброса (camelCase wire, regenerated Wave 1/2) -->
```typescript
interface RequestDto {
  id: number;
  printerDeviceId: number | null;
  cartridgeModelId: number | null;
  completedCartridgeId: number | null;
  requesterName: string | null;
  printerName: string | null;
  printerLocation: string | null;   // новое поле из Wave 1
  version: number;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartridgeSelect.svelte — новый компонент выбора картриджа</name>
  <files>ui/src/lib/components/CartridgeSelect.svelte</files>
  <read_first>
    ui/src/lib/components/GroupedPrinterSelect.svelte (весь файл, 149 строк) — прямой эталон для копирования структуры и SCSS
  </read_first>
  <action>
    Создать `ui/src/lib/components/CartridgeSelect.svelte` по образцу `GroupedPrinterSelect.svelte`, но без группировки по `<optgroup>` (DISC-03 — для картриджей нет естественной локации-группы). Props-контракт: `options: CartridgeDto[]`, `value: string` (`$bindable('')`), `disabled?: boolean`, `invalid?: boolean`, `id?: string`, `onchange?: (_value: string) => void` (типы импортировать из `'../../bindings'` — `CartridgeDto` уже там после регенерации Wave 2). Рендер: `<option value="">Выберите картридж</option>`, при `options.length === 0` — `<option value="" disabled>Нет подходящих картриджей на складе</option>` (D-02/DISC-02 — текст empty state), иначе `{#each options as o (o.id)}` с `<option value={String(o.id)}>{o.code} — {[o.model_brand, o.model_name].filter(Boolean).join(' ')} ({o.state_name ?? '—'})</option>` (человекочитаемая строка код+модель+заряд, NULL-safe через `??`/`filter(Boolean)` — модель/заряд могут быть `null` по established JOIN-паттерну). Caret-иконка — скопировать SVG-блок 1:1 (lines 76-85 источника). SCSS — скопировать `.select-wrapper`/`.select`/`.caret` блоки 1:1 (lines 88-149 источника), убрав только правило `optgroup { ... }` внутри `.select` (картриджи рендерятся флэт-списком, optgroup-стиль не нужен).
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check</automated>
  </verify>
  <acceptance_criteria>
    `pnpm --dir ui svelte-check` не выдаёт ошибок типов для нового файла;
    компонент экспортирует те же 6 props (`options`/`value`/`disabled`/`invalid`/`id`/`onchange`), что и `GroupedPrinterSelect`, минус группировка;
    файл не импортирует `RequestPrinterOptionDto` (использует `CartridgeDto`).
  </acceptance_criteria>
  <done>CartridgeSelect.svelte существует, типизирован на CartridgeDto[], рендерит флэт-список "код — модель (заряд)" с пустым состоянием "Нет подходящих картриджей на складе".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OperationModal.svelte — селектор + auto-prefill props (D-01..D-05)</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <read_first>
    ui/src/features/cartridges/OperationModal.svelte (весь файл, 396 строк — уже в контексте этого плана выше, но executor должен перечитать целиком перед правкой, т.к. правки затрагивают props/state/reset-effect/install-branch/handleSubmit одновременно)
  </read_first>
  <behavior>
    - Поведение 1: при `op === 'install'` и `cartridge` prop равен `null` (request-centric вход, D-08 различение) — модалка показывает `CartridgeSelect` со списком, загруженным через `cartridges.list({ status_id: 1, installable_only: true, model_id: cartridgeModelId ?? null, kind_id: null, search: null, include_deleted: false }, { offset: 0, limit: 200 })` (D-01/D-02; DISC-01 — `model_id: null` когда `cartridgeModelId` не передан → не фильтрует по модели).
    - Поведение 2: при `op === 'install'` и `cartridge` prop НЕ `null` (старый cartridge-centric вход, D-08) — `CartridgeSelect` НЕ рендерится, форма работает ровно как сейчас (regression guard).
    - Поведение 3: при выборе картриджа в `CartridgeSelect` — внутренний `$state<CartridgeDto | null>` обновляется, `givenToName`/`location` остаются как есть (не сбрасываются повторно), `canSubmit` становится `true` при наличии выбранного картриджа.
    - Поведение 4: reset-эффект при открытии модалки предзаполняет `givenToName = prefillGivenToName ?? ''` и `location = prefillLocation ?? ''` вместо безусловного `''` (D-04/D-05) — для cartridge-centric входа (`prefillGivenToName`/`prefillLocation` оба `undefined`) поведение не меняется (fallback на `''`, как сейчас).
    - Поведение 5: успешный submit вызывает `onSuccess(installedCartridge.id)` вместо `onSuccess()` (новая сигнатура).
  </behavior>
  <action>
    Расширить `interface Props`: добавить `cartridgeModelId?: number` (фильтр списка по модели заявки, D-02), `prefillLocation?: string` (D-05), `prefillGivenToName?: string` (D-04), и сменить сигнатуру `onSuccess: () => void` на `onSuccess: (_cartridgeId: number) => void`. Сохранить `preFillPrinterId?: number` как есть (используется для `printerContextHint`, не меняется).

    Добавить новое внутреннее состояние: `let selectedCartridge = $state<CartridgeDto | null>(null);` и `let cartridgeOptions = $state<CartridgeDto[]>([]);` и `let cartridgeListLoading = $state(false);`. Добавить `$derived` `const effectiveCartridge = $derived(cartridge ?? selectedCartridge);` — ВСЯ существующая логика (`isDrum`, `defaultStateId`, `buildPayload()`, `validate()`, `canSubmit`), которая сейчас читает `cartridge!`/`cartridge?.`, должна читать `effectiveCartridge` вместо прямого prop `cartridge` (это единственный способ поддержать оба входа D-08 без дублирования веток). `buildPayload()`: заменить `cartridge!.id`/`cartridge!.version` на `effectiveCartridge!.id`/`effectiveCartridge!.version`. `handleSubmit()`: проверку `if (!cartridge || submitting) return;` заменить на `if (!effectiveCartridge || submitting) return;`; в случае успеха вызвать `onSuccess(effectiveCartridge.id);` вместо `onSuccess();`. `canSubmit`: `$derived(!submitting && !!effectiveCartridge)`.

    Добавить новый `$effect`, который запускает загрузку списка картриджей при `open && op === 'install' && cartridge === null` (request-centric вход без заранее заданного картриджа): вызвать `cartridges.list(...)` (см. Behavior 1), записать результат в `cartridgeOptions`, обернуть в `try/catch` с `cartridgeListLoading` флагом аналогично другим async-эффектам проекта (паттерн `RequestDetail.svelte` `$effect` загрузки истории, lines 108-126 — `.then().catch().finally()`); при ошибке — `pushToast('error', ...)` и `cartridgeOptions = []`.

    Изменить reset-эффект (lines 57-74): заменить `givenToName = '';` на `givenToName = prefillGivenToName ?? '';` и `location = '';` на `location = prefillLocation ?? '';`; добавить `selectedCartridge = null;` в сброс (чтобы повторное открытие модалки не подсовывало картридж из прошлой сессии).

    В install-branch JSX (lines 246-301): ПЕРЕД блоком `{#if printerContextHint}` вставить новый блок, видимый только когда `cartridge === null` (request-centric, D-08):
    `{#if op === 'install' && cartridge === null}` → `<div class="field"><label class="label" for="op-cartridge">Картридж</label><CartridgeSelect options={cartridgeOptions} value={selectedCartridge ? String(selectedCartridge.id) : ''} id="op-cartridge" onchange={(v) => { selectedCartridge = cartridgeOptions.find((c) => String(c.id) === v) ?? null; }} /></div>{/if}` — импортировать `CartridgeSelect` из `'$lib/components/CartridgeSelect.svelte'` и `CartridgeFilter`/`Pagination` типы и `cartridges` из существующего `'./api'` (уже импортирован).

    НЕ менять `validate()` — она уже корректно работает через `effectiveCartridge` опосредованно (валидирует только текстовые поля, не сам факт выбора картриджа — выбор картриджа гейтится через `canSubmit`/`effectiveCartridge`).
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check && pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    `pnpm --dir ui svelte-check` без ошибок типов;
    `pnpm --dir ui build` успешен;
    `grep -n "effectiveCartridge\|CartridgeSelect\|prefillGivenToName\|prefillLocation\|cartridgeModelId" ui/src/features/cartridges/OperationModal.svelte` находит все новые идентификаторы;
    старый cartridge-centric путь (cartridge prop передан напрямую) визуально не показывает `CartridgeSelect` (human-verify в Task 3 это подтвердит интерактивно).
  </acceptance_criteria>
  <done>OperationModal.svelte поддерживает оба входа установки (D-08): cartridge-centric (как раньше) и request-centric (новый CartridgeSelect + auto-prefill givenToName/location, остаются редактируемыми); onSuccess передаёт id установленного картриджа.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RequestDetail.svelte — проброс данных заявки + linkedCartridgeId (D-06) + human-verify</name>
  <files>ui/src/features/requests/RequestDetail.svelte</files>
  <read_first>
    ui/src/features/requests/RequestDetail.svelte (весь файл, 760 строк — уже в контексте этого плана, перечитать handleInstallSuccess и OperationModal JSX перед правкой)
  </read_first>
  <behavior>
    - Поведение: `handleInstallSuccess` принимает `cartridgeId: number`, передаёт его как `linkedCartridgeId: cartridgeId` в `requests.transition({op: 'complete', ...})` вместо `null`.
    - Поведение: `OperationModal` получает новые пропы из `request`: `cartridgeModelId={request.cartridgeModelId ?? undefined}`, `prefillLocation={request.printerLocation ?? undefined}`, `prefillGivenToName={request.requesterName ?? undefined}`.
  </behavior>
  <action>
    Изменить сигнатуру `async function handleInstallSuccess()` (lines 301-323) на `async function handleInstallSuccess(cartridgeId: number)`, заменить `linkedCartridgeId: null` на `linkedCartridgeId: cartridgeId` внутри вызова `requests.transition({op: 'complete', requestId: request.id, version: request.version, notes: null, linkedCartridgeId: cartridgeId})`. Остальная структура try/catch/pushToast/onTransition не меняется.

    В JSX-блоке `<OperationModal ... />` (lines 582-591) добавить три новых пропа: `cartridgeModelId={request.cartridgeModelId ?? undefined}`, `prefillLocation={request.printerLocation ?? undefined}`, `prefillGivenToName={request.requesterName ?? undefined}` — рядом с существующим `preFillPrinterId={request.printerDeviceId ?? undefined}`. `onSuccess={handleInstallSuccess}` остаётся как есть (Svelte автоматически передаёт аргумент callback'а — сигнатура `(cartridgeId: number) => void` совпадает с новым `onSuccess` контрактом `OperationModal`).
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check && pnpm --dir ui build</automated>
  </verify>
  <acceptance_criteria>
    `pnpm --dir ui svelte-check` без ошибок типов (особенно совпадение сигнатуры `onSuccess`/`handleInstallSuccess`);
    `pnpm --dir ui build` успешен;
    `grep -n "linkedCartridgeId: cartridgeId\|prefillLocation\|prefillGivenToName\|cartridgeModelId=" ui/src/features/requests/RequestDetail.svelte` находит все правки.
  </acceptance_criteria>
  <done>RequestDetail передаёт реальный linkedCartridgeId в complete после успешной установки; OperationModal получает модель заявки, расположение принтера и имя заявителя для предзаполнения.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Человеческая верификация — установка картриджа из заявки end-to-end</name>
  <what-built>
    Полная цепочка «Заявка → выбор картриджа → установка → завершение заявки → история»:
    backend (Wave 1/2) фильтрует картриджи по статусу/заряду/модели и обогащает историю
    заявки кодом+моделью установленного картриджа; frontend (Wave 3, Tasks 1-3) добавляет
    селектор картриджа в OperationModal с авто-подстановкой «Кому отдал»/«Расположение»
    (редактируемых) и прокидывает id установленного картриджа в completedCartridgeId.
    Старый cartridge-centric вход (меню картриджа → «Установить в принтер») не менялся.
  </what-built>
  <how-to-verify>
    1. Запустить `pnpm --dir ui build` (если ещё не выполнено в Task 1-3) и `cargo tauri dev` (или текущий dev-процесс проекта).
    2. Сотрудник (Employee) создаёт заявку «Замена картриджа» на принтер с известным расположением.
    3. Специалист (Manager/Admin) принимает заявку в работу (status → in_progress).
    4. На карточке заявки нажать «Установить картридж» — должна открыться модалка с выпадающим списком картриджей, содержащим ТОЛЬКО картриджи: статус «На складе», заряд «Полный» или «Частичный», модель = модель из заявки. Если подходящих картриджей нет — список показывает «Нет подходящих картриджей на складе» (DISC-02), форма не блокируется (можно закрыть и отклонить заявку или использовать старый вход).
    5. Проверить, что поле «Кому выдал» предзаполнено именем заявителя (сотрудника из шага 2), и поле «Расположение» предзаполнено расположением принтера — оба поля при этом РЕДАКТИРУЕМЫ (можно изменить вручную).
    6. Выбрать картридж из списка, заполнить «Кто выдал», нажать «Установить».
    7. Убедиться: заявка перешла в статус «Выполнена»; в разделе «История» карточки заявки появилась запись, содержащая код картриджа (например «C-000042») и модель (бренд + название).
    8. Регрессия D-08: открыть карточку картриджа со статусом «На складе» напрямую (не через заявку) → «Установить в принтер» — старая форма должна работать ровно как раньше, БЕЗ нового селектора картриджа (он там не нужен — картридж уже выбран контекстом).
    9. (Опционально, LAN-браузер) Повторить шаги 4-7 через `pnpm --dir ui build` + браузер на `https://<host>:8443` — тот же сценарий должен работать identично (axum сервирует тот же SPA-бандл).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Specialist browser/webview → cartridges_list (installable_only) | Клиент запрашивает список картриджей для установки — RBAC (`Action::ReadData`, Admin\|Manager only) уже проверен в Wave 1/2 backend, фронт лишь формирует фильтр; Employee физически не видит кнопку «Установить картридж» (UI-гейт `isSpecialist`, существующий), но даже при прямом вызове API получил бы 403 (T-12-01 закрыт в Wave 2). |
| Specialist → requests.transition(complete, linkedCartridgeId) | Клиент передаёт id картриджа, выбранного из СВОЕГО же только что полученного списка (`cartridgeOptions`) — нет произвольного ввода id, только выбор из предложенных серверов вариантов; даже если бы клиент подменил id на произвольное число, backend (`cartridge_repo.get`) вернёт `NotFound` при несуществующем id, а при существующем — операция всё равно gated `TransitionRequests` (Admin\|Manager only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-01 | Elevation of Privilege | UI-кнопка «Установить картридж» в RequestDetail | accept | Уже гейтится `isSpecialist` (существующий UI-чек, не новый в этой фазе); backend RBAC — единственная реальная граница доверия (закрыта в Wave 2), UI-гейт — только UX-улучшение, не security boundary. |
| T-12-04 | Tampering | `selectedCartridge`/`effectiveCartridge` client-side state в OperationModal | accept | Любое клиентское изменение `cartridge_id`/`version` в payload перед отправкой перехватывается существующей optimistic-lock проверкой на backend (`OptimisticLockMismatch`) — клиент не может обойти version-check, независимо от того, как он получил id (через новый селектор или вручную через devtools). |
</threat_model>

<verification>
- `pnpm --dir ui svelte-check` — без ошибок типов по всем трём изменённым/новым файлам.
- `pnpm --dir ui lint` — чисто (ESLint + eslint-plugin-svelte).
- `pnpm --dir ui build` — успешная сборка SPA.
- Human-verify checkpoint (Task 4) — подтверждает happy path + DISC-02 empty state + D-08 regression интерактивно.
</verification>

<success_criteria>
Специалист может выбрать физический картридж прямо из карточки заявки «Замена картриджа», поля «Кому отдал»/«Расположение» предзаполнены, но редактируемы; после установки заявка завершается с корректным completedCartridgeId, история показывает код+модель; старый cartridge-centric вход не регрессировал.
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-03-SUMMARY.md` when done
</output>
