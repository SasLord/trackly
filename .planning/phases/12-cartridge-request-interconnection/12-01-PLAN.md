---
phase: 12-cartridge-request-interconnection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-core/src/domain/cartridges.rs
  - crates/trackly-core/src/domain/requests.rs
  - crates/trackly-app/src/dto/cartridge.rs
  - crates/trackly-app/src/dto/request.rs
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-infra/src/repos/requests_sqlite.rs
  - crates/trackly-app/tests/cartridges_lifecycle.rs
autonomous: true
requirements: [D-01, D-02, D-05]

must_haves:
  truths:
    - "Список картриджей можно отфильтровать так, что в результате остаются только записи со статусом «На складе» и зарядом Полный(1)/Частичный(2) (D-01/D-02)"
    - "RequestDto несёт расположение принтера заявки (printer_location), полученное JOIN'ом, а не отдельным запросом (D-05)"
  artifacts:
    - path: "crates/trackly-core/src/domain/cartridges.rs"
      provides: "CartridgeFilter.installable_only: bool"
      contains: "installable_only"
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "list() WHERE c.state_id IN (1, 2) под флагом installable_only"
      contains: "state_id IN (1, 2)"
    - path: "crates/trackly-infra/src/repos/requests_sqlite.rs"
      provides: "SELECT_REQUESTS JOIN locations + printer_location column idx 19"
      contains: "printer_location"
    - path: "crates/trackly-app/tests/cartridges_lifecycle.rs"
      provides: "RED→GREEN тест на installable_only фильтр"
      contains: "installable_only"
  key_links:
    - from: "crates/trackly-app/src/dto/cartridge.rs"
      to: "crates/trackly-core/src/domain/cartridges.rs"
      via: "CartridgeFilter::into_domain()"
      pattern: "installable_only"
    - from: "crates/trackly-infra/src/repos/requests_sqlite.rs"
      to: "crates/trackly-core/src/domain/requests.rs"
      via: "map_row_request row.get(19)"
      pattern: "printer_location"
---

<objective>
Расширить доменный и DTO-слой картриджей флагом «устанавливаемый заряд» (D-01) и
добавить расположение принтера в `RequestDto` через готовый JOIN на `locations`
(D-05). Это backend-фундамент: без этих двух полей ни селектор картриджей в
`OperationModal`, ни авто-подстановка «Расположение» физически не могут работать —
данных просто нет в ответах API.

Purpose: Дать Wave 2 (сервисный слой) и Wave 3 (фронтенд) готовые,
протестированные поля для чтения — без необходимости трогать SQL-слой повторно.
Output: `CartridgeFilter.installable_only`, `RequestDto.printer_location`,
обновлённый `cartridges_lifecycle.rs` с RED→GREEN тестом на фильтр.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md
@.planning/phases/12-cartridge-request-interconnection/12-RESEARCH.md

<interfaces>
<!-- Текущая форма CartridgeFilter (domain) — crates/trackly-core/src/domain/cartridges.rs, lines 196-209 -->
```rust
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct CartridgeFilter {
    pub status_id: Option<i64>,
    pub kind_id: Option<i64>,
    pub model_id: Option<i64>,
    pub search: Option<String>,
    pub include_deleted: bool,
}
```

<!-- Текущая форма CartridgeFilter (DTO) — crates/trackly-app/src/dto/cartridge.rs, lines 324-349 -->
```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Type, Default)]
pub struct CartridgeFilter {
    #[specta(type = Option<i32>)]
    pub status_id: Option<i64>,
    #[specta(type = Option<i32>)]
    pub kind_id: Option<i64>,
    #[specta(type = Option<i32>)]
    pub model_id: Option<i64>,
    pub search: Option<String>,
    #[serde(default)]
    pub include_deleted: bool,
}

impl CartridgeFilter {
    pub fn into_domain(self) -> trackly_core::domain::cartridges::CartridgeFilter {
        trackly_core::domain::cartridges::CartridgeFilter {
            status_id: self.status_id,
            kind_id: self.kind_id,
            model_id: self.model_id,
            search: self.search,
            include_deleted: self.include_deleted,
        }
    }
}
```

<!-- list() в crates/trackly-infra/src/repos/cartridges_sqlite.rs, lines 945-1005 — оба запроса (COUNT + SELECT) несут идентичный WHERE; LIMIT/OFFSET сейчас ?5/?6 в SELECT. -->
```rust
let total: i64 = conn.query_row(
    "SELECT COUNT(*) FROM cartridges c \
     LEFT JOIN cartridge_models m ON m.id = c.model_id \
     WHERE (?1 = 1 OR c.deleted_at_utc IS NULL) \
       AND (?2 IS NULL OR c.status_id = ?2) \
       AND (?3 IS NULL OR m.kind_id = ?3) \
       AND (?4 IS NULL OR c.model_id = ?4)",
    params![include_deleted as i64, filter.status_id, filter.kind_id, filter.model_id],
    |r| r.get(0),
).map_err(map_rusqlite)?;

let mut stmt = conn.prepare(&format!(
    "{SELECT_CARTRIDGES} \
     WHERE (?1 = 1 OR c.deleted_at_utc IS NULL) \
       AND (?2 IS NULL OR c.status_id = ?2) \
       AND (?3 IS NULL OR m.kind_id = ?3) \
       AND (?4 IS NULL OR c.model_id = ?4) \
     ORDER BY c.created_at_utc DESC, c.id DESC \
     LIMIT ?5 OFFSET ?6"
)).map_err(map_rusqlite)?;
```

<!-- RequestRow в crates/trackly-core/src/domain/requests.rs, lines 13-46 (полный список текущих полей) -->
```rust
pub struct RequestRow {
    pub id: i64,
    pub request_type: String,
    pub status: String,
    pub requested_by_user_id: i64,
    pub assigned_to_user_id: Option<i64>,
    pub printer_device_id: Option<i64>,
    pub cartridge_model_id: Option<i64>,
    pub category_id: Option<i64>,
    pub completed_cartridge_id: Option<i64>,
    pub description: Option<String>,
    pub resolution_notes: Option<String>,
    pub requester_name: Option<String>,
    pub printer_name: Option<String>,
    pub created_at_utc: i64,
    pub updated_at_utc: i64,
    pub deleted_at_utc: Option<i64>,
    pub version: i64,
    pub ad_subtype: Option<String>,
    pub category_name: Option<String>,
}
```

<!-- SELECT_REQUESTS + map_row_request — crates/trackly-infra/src/repos/requests_sqlite.rs, lines 18-72 -->
```rust
const SELECT_REQUESTS: &str = "
    SELECT r.id, r.request_type, r.status,
           r.requested_by_user_id, r.assigned_to_user_id,
           r.printer_device_id, r.cartridge_model_id,
           r.category_id, r.completed_cartridge_id,
           r.description, r.resolution_notes,
           u.full_name AS requester_name,
           d.name AS printer_name,
           r.created_at_utc, r.updated_at_utc, r.deleted_at_utc, r.version,
           r.ad_subtype,
           rc.name AS category_name
      FROM requests r
      LEFT JOIN users u ON u.id = r.requested_by_user_id
      LEFT JOIN devices d ON d.id = r.printer_device_id
      LEFT JOIN request_categories rc ON rc.id = r.category_id
";
// category_name появился LAST (idx 18) — комментарий в коде явно запрещает
// вставку в середину списка, это сдвинуло бы все последующие row.get(n).

fn map_row_request(row: &rusqlite::Row<'_>) -> rusqlite::Result<RequestRow> {
    Ok(RequestRow {
        id: row.get(0)?,
        request_type: row.get(1)?,
        status: row.get(2)?,
        requested_by_user_id: row.get(3)?,
        assigned_to_user_id: row.get(4)?,
        printer_device_id: row.get(5)?,
        cartridge_model_id: row.get(6)?,
        category_id: row.get(7)?,
        completed_cartridge_id: row.get(8)?,
        description: row.get(9)?,
        resolution_notes: row.get(10)?,
        requester_name: row.get(11)?,
        printer_name: row.get(12)?,
        created_at_utc: row.get(13)?,
        updated_at_utc: row.get(14)?,
        deleted_at_utc: row.get(15)?,
        version: row.get(16)?,
        ad_subtype: row.get(17)?,
        category_name: row.get(18)?,
    })
}
```

<!-- Эталонный аналог LEFT JOIN locations — RequestService::printer_options(), crates/trackly-app/src/services/request_service.rs, lines 241-250 -->
```rust
"SELECT d.id, d.name, l.name AS location \
 FROM devices d \
 LEFT JOIN locations l ON d.location_id = l.id \
 WHERE d.type_id = (SELECT id FROM device_types WHERE name = 'Принтер') \
   AND d.deleted_at_utc IS NULL \
 ORDER BY l.name IS NULL, l.name, d.name"
```

<!-- RequestDto — crates/trackly-app/src/dto/request.rs, lines 17-77 (ключевые строки) -->
```rust
#[derive(Debug, Clone, Serialize, Deserialize, Type)]
#[serde(rename_all = "camelCase")]
pub struct RequestDto {
    // ...
    #[specta(type = Option<i32>)]
    pub printer_device_id: Option<i64>,
    // ...
    #[specta(type = Option<i32>)]
    pub completed_cartridge_id: Option<i64>,
    pub description: Option<String>,
    pub resolution_notes: Option<String>,
    pub requester_name: Option<String>,
    pub printer_name: Option<String>,
    // ...
}

impl From<RequestRow> for RequestDto {
    fn from(r: RequestRow) -> Self {
        Self {
            // ...
            requester_name: r.requester_name,
            printer_name: r.printer_name,
            // ...
        }
    }
}
```

<!-- Готовый тестовый каркас — crates/trackly-app/tests/cartridges_lifecycle.rs, lines 1-86 -->
```rust
fn make_cartridge_service() -> (CartridgeService, tempfile::TempDir) {
    let (writer, readers, dir) = test_writer_and_readers();
    let clock = Arc::new(SystemClock);
    let svc = CartridgeService::new(writer, readers, clock);
    (svc, dir)
}

async fn seed_model(svc: &CartridgeService) -> i64 {
    svc.model_create(CartridgeModelCreateDto {
        brand: "HP".into(), model: "CE285A".into(), kind_id: 1,
        color: Some("Чёрный".into()), notes: None, compatibility: vec![],
    }).await.expect("seed model").id
}

async fn create_stock_cartridge(svc: &CartridgeService, model_id: i64) -> trackly_app::dto::cartridge::CartridgeDto {
    svc.create(CartridgeCreateDto {
        model_id, code_override: None, state_id: Some(1), // Полный
        location: Some("Склад".into()), notes: None,
    }).await.expect("create cartridge")
}

#[tokio::test(flavor = "multi_thread", worker_threads = 4)]
async fn install_changes_status_to_in_use() {
    tokio::time::timeout(Duration::from_secs(30), async {
        // ... .await.expect("install_changes_status_to_in_use budget")
    }).await.expect("...")
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CartridgeFilter.installable_only — domain + DTO + SQL + RED→GREEN тест</name>
  <files>crates/trackly-core/src/domain/cartridges.rs, crates/trackly-app/src/dto/cartridge.rs, crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-app/tests/cartridges_lifecycle.rs</files>
  <read_first>
    crates/trackly-core/src/domain/cartridges.rs lines 196-209 (текущий CartridgeFilter);
    crates/trackly-app/src/dto/cartridge.rs lines 324-349 (DTO-зеркало + into_domain());
    crates/trackly-infra/src/repos/cartridges_sqlite.rs lines 945-1005 (list() COUNT+SELECT, идиома `(?N IS NULL OR col = ?N)`);
    crates/trackly-app/tests/cartridges_lifecycle.rs lines 1-86 (helpers make_cartridge_service/seed_model/create_stock_cartridge + timeout-wrapper конвенция)
  </read_first>
  <behavior>
    - Test 1 (installable_filters_to_full_and_partial_charge): создать модель + 3 картриджа на складе с state_id 1 (Полный), 2 (Частичный), 3 (Пустой) — `svc.list(CartridgeFilter { status_id: Some(1), installable_only: true, ..Default::default() }, Pagination::default())` должен вернуть ровно 2 записи (state_id ∈ {1,2}), не 3.
    - Test 2 (installable_only_false_returns_all): тот же сетап, `installable_only: false` (default) → возвращает все 3.
    - Test 3 (installable_only_respects_model_filter): два картриджа разных моделей, оба state_id=1 на складе; `installable_only: true, model_id: Some(target)` → только картридж нужной модели (DISC-01: model_id: None не должен фильтровать вовсе — отдельной assert-веткой в том же тесте или соседним тестом).
    - Test 4 (installable_only_empty_result_is_ok_not_error): нет ни одного картриджа с state_id∈{1,2} на складе → `Ok((vec![], 0))`, не `Err` (DISC-02 на уровне сервиса).
  </behavior>
  <action>
    В `crates/trackly-core/src/domain/cartridges.rs` добавить в `CartridgeFilter` (после `include_deleted`, сохраняя стиль doc-comment на отдельной строке) поле `pub installable_only: bool` с комментарием в духе "Только статус «На складе» (1) и заряд Полный(1)/Частичный(2) — для селектора установки из заявки (D-01, Phase 12)". `#[derive(Default)]` уже на struct, `false` — безопасный дефолт. Выполнить `grep -rn "CartridgeFilter {" crates/` и убедиться, что все site-конструкции либо используют `..Default::default()`, либо явно добавить `installable_only: false` в каждую.

    В `crates/trackly-app/src/dto/cartridge.rs` добавить зеркальное поле `#[serde(default)] pub installable_only: bool` в DTO `CartridgeFilter` (та же позиция, тот же стиль, что и `include_deleted` на line 334) и прокинуть его в `into_domain()` (`installable_only: self.installable_only,`). DTO snake_case (нет rename_all на этом struct) — `installable_only` уйдёт на wire как есть.

    В `crates/trackly-infra/src/repos/cartridges_sqlite.rs::list()` добавить пятый параметр `?5 = filter.installable_only as i64` в ОБА запроса (COUNT и SELECT) с условием `AND (?5 = 0 OR c.state_id IN (1, 2))` — именно `IN (1, 2)`, не `state_id = 1 AND state_id = 2` (это невозможное условие на одной строке). В SELECT-запросе сдвинуть `LIMIT`/`OFFSET` плейсхолдеры с `?5`/`?6` на `?6`/`?7` и добавить соответствующие позиционные значения `limit`/`offset` в конец `params![...]`.

    В `crates/trackly-app/tests/cartridges_lifecycle.rs` добавить 4 новые `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` функции по образцу `install_changes_status_to_in_use` — каждая обёрнута в `tokio::time::timeout(Duration::from_secs(30), async { ... }).await.expect("...")`. Использовать существующие `make_cartridge_service()`/`seed_model()`/`create_stock_cartridge()` — при необходимости передавать произвольный `state_id` через прямой вызов `svc.create(CartridgeCreateDto { model_id, code_override: None, state_id: Some(N), location: Some("Склад".into()), notes: None })` вместо `create_stock_cartridge` (которая хардкодит state_id=1), либо завести локальный helper `create_stock_cartridge_with_state(svc, model_id, state_id)` рядом с существующими helper-функциями.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test cartridges_lifecycle installable -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    `cargo build -p trackly-core -p trackly-app -p trackly-infra` проходит без ошибок;
    4 новых теста запускаются и зелёные;
    `grep -n "installable_only" crates/trackly-core/src/domain/cartridges.rs crates/trackly-app/src/dto/cartridge.rs crates/trackly-infra/src/repos/cartridges_sqlite.rs` находит поле во всех трёх местах;
    существующий `install_changes_status_to_in_use` и другие тесты файла остаются зелёными (regression).
  </acceptance_criteria>
  <done>CartridgeFilter (domain + DTO) несёт installable_only: bool с дефолтом false; SQL list() фильтрует по state_id IN (1,2) только когда флаг true; 4 новых теста зелёные; ни один существующий тест не сломан.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RequestDto.printer_location — JOIN на locations + RequestRow + map_row_request</name>
  <files>crates/trackly-core/src/domain/requests.rs, crates/trackly-app/src/dto/request.rs, crates/trackly-infra/src/repos/requests_sqlite.rs</files>
  <read_first>
    crates/trackly-core/src/domain/requests.rs lines 13-46 (RequestRow, полный список полей);
    crates/trackly-infra/src/repos/requests_sqlite.rs lines 18-72 (SELECT_REQUESTS + map_row_request, комментарий "category_name appended LAST (idx 18)");
    crates/trackly-app/src/dto/request.rs lines 17-77 (RequestDto + From&lt;RequestRow&gt;), lines 99-108 (RequestPrinterOptionDto.location — эталонный Option&lt;String&gt; от LEFT JOIN);
    crates/trackly-app/src/services/request_service.rs lines 241-250 (printer_options() SQL — эталонный LEFT JOIN locations)
  </read_first>
  <behavior>
    - Test (request_dto_carries_printer_location): создать устройство-принтер с `location_id`, привязанное к `locations` через сидинг (или существующий test-фикстура с `LocationAutocomplete`/printer seed helper, если есть — иначе прямой `conn.execute("INSERT INTO locations ...")` + `conn.execute("INSERT INTO devices ...")`), создать заявку `cartridge_replace` с этим `printer_device_id`, вызвать `svc.get(id, &caller)`, проверить `dto.printer_location == Some("...")`.
    - Test (request_dto_printer_location_none_when_no_location_or_no_printer): заявка `free_form` без printer_device_id → `printer_location == None`; заявка cartridge_replace с принтером без location_id → также `None` (NULL-safe LEFT JOIN, не panic).
  </behavior>
  <action>
    В `crates/trackly-core/src/domain/requests.rs` добавить в `RequestRow` поле `pub printer_location: Option<String>,` сразу после `pub printer_name: Option<String>,` с doc-комментарием в духе "Joined `locations.name` через `devices.location_id` принтера заявки (D-05, Phase 12); `None` если принтер не выбран или у него нет расположения."

    В `crates/trackly-infra/src/repos/requests_sqlite.rs` изменить `SELECT_REQUESTS`: добавить `LEFT JOIN locations dl ON dl.id = d.location_id` сразу после строки `LEFT JOIN devices d ON d.id = r.printer_device_id`, и добавить `dl.name AS printer_location` В КОНЕЦ списка SELECT-колонок — строго ПОСЛЕ `rc.name AS category_name` (становится idx 19), не вставлять в середину (существующий комментарий в коде явно предупреждает об опасности сдвига индексов). В `map_row_request` добавить `printer_location: row.get(19)?,` последней строкой перед закрывающей скобкой `Ok(RequestRow { ... })`. Эта же `SELECT_REQUESTS` используется и в `fetch_in_tx` (внутри транзакции) — убедиться, что там column count совпадает (это тот же константный SQL, переиспользуется без дублирования).

    В `crates/trackly-app/src/dto/request.rs` добавить `pub printer_location: Option<String>,` в `RequestDto` сразу после `pub printer_name: Option<String>,` (struct несёт `#[serde(rename_all = "camelCase")]` на уровне контейнера — поле автоматически уйдёт на wire как `printerLocation`, без доп. атрибутов). В `impl From<RequestRow> for RequestDto` добавить `printer_location: r.printer_location,` сразу после `printer_name: r.printer_name,`.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test phase06_stubs printer_location -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    `cargo build -p trackly-core -p trackly-app -p trackly-infra` проходит;
    `grep -n "printer_location" crates/trackly-core/src/domain/requests.rs crates/trackly-app/src/dto/request.rs crates/trackly-infra/src/repos/requests_sqlite.rs` находит поле во всех трёх местах;
    новые 2 теста зелёные;
    существующие тесты `requests_sqlite`/`request_service`/`phase06_stubs` не сломаны (особенно `test_request_lifecycle`, который не трогает printer_device_id, но проходит через тот же SELECT_REQUESTS).
  </acceptance_criteria>
  <done>RequestRow и RequestDto несут printer_location: Option&lt;String&gt;, заполняемое JOIN'ом на locations через devices.location_id; NULL-safe для заявок без принтера или принтера без расположения; оба новых теста зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Frontend → cartridges_list/requests_get | Клиент передаёт `CartridgeFilter` (включая новый `installable_only`) и читает `RequestDto` (включая новый `printer_location`) — оба уже проходят через существующие RBAC-гейты на уровне Wave 2/tauri_cmds/http слоя, эта плата (Wave 1) не меняет границу авторизации, только форму данных. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-01 | Information Disclosure | `CartridgeFilter.installable_only` / `RequestDto.printer_location` | accept | Оба поля не раскрывают новых данных за пределами уже читаемых через `cartridges_list`/`requests_get` (которые уже требуют `Action::ReadData`/owner-check) — это перекомпоновка существующих читаемых столбцов, RBAC не меняется в этом плане. |
| T-12-02 | Tampering | SQL `state_id IN (1, 2)` — захардкоженные значения, не из пользовательского ввода | accept | Значения 1/2 — доменные константы (D-01), не параметризуются как `Vec` именно чтобы исключить SQL injection через произвольный список — `installable_only` это `bool`, не принимает произвольные значения state_id от клиента. |
</threat_model>

<verification>
- `cargo build --workspace` — компилируется без ошибок (3 затронутых crate).
- `cargo test -p trackly-app --test cartridges_lifecycle` — все тесты зелёные (включая 4 новых).
- `cargo test -p trackly-app --test phase06_stubs` — все тесты зелёные (включая 2 новых на printer_location), `test_request_lifecycle` без регрессии.
- `cargo clippy --workspace -- -D warnings` — чисто.
</verification>

<success_criteria>
`CartridgeFilter` (domain + DTO) несёт `installable_only: bool`, проверенный SQL-фильтром `state_id IN (1, 2)`, покрытый 4 тестами.
`RequestDto`/`RequestRow` несут `printer_location: Option<String>` через `LEFT JOIN locations`, покрытый 2 тестами.
Никаких новых миграций, никаких новых RBAC Action — только расширение существующих полей по уже установленным паттернам.
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-01-SUMMARY.md` when done
</output>
