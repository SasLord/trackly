---
phase: 12-cartridge-request-interconnection
plan: 20
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/PrinterSelect.svelte
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: true
gap_closure: true
requirements: [D-20, D-21, D-22]
must_haves:
  truths:
    - "В cartridge-centric установке (меню картриджа → «Установить в принтер») оператор может ОПЦИОНАЛЬНО выбрать принтер прямо в OperationModal — без выбора форма ведёт себя как раньше (D-20, нет регрессии D-08)"
    - "Список принтеров в селекторе сначала показывает принтеры, совместимые с моделью этого картриджа (printer_cartridge_models), затем остальные принтеры — без блокировки, если совместимость не задана (D-21, реверс D-13/D-14)"
    - "Если выбранный принтер уже содержит картридж «В работе», показывается блок «Предыдущий картридж» с редактируемыми «Расположение» и «Уровень заряда» (по умолчанию Пустой), и эти значения уходят в transition() вместе с новой установкой (D-22)"
    - "Если принтер НЕ выбран — printer_device_id уходит как null, блок «Предыдущий картридж» не показывается, поведение идентично существующему D-08 потоку"
    - "Request-centric вход (cartridge===null, preFillPrinterId передан из RequestDetail) не изменился — новый селектор не показывается, когда preFillPrinterId уже задан извне"
  artifacts:
    - path: "ui/src/lib/components/PrinterSelect.svelte"
      provides: "Плоский <select>/<optgroup> компонент: группы «Совместимые принтеры»/«Остальные принтеры», по образцу GroupedPrinterSelect.svelte"
      contains: "optgroup"
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "selectedPrinterId $state + effectivePrinterId, подключённые к существующему лукапу printerContext/previousCartridge и к buildPayload()"
      contains: "selectedPrinterId"
  key_links:
    - from: "OperationModal selectedPrinterId (UI)"
      to: "printerContext/previousCartridge $effect (строки ~175-201) и buildPayload() printer_device_id"
      via: "effectivePrinterId = preFillPrinterId ?? selectedPrinterId ?? undefined"
      pattern: "effectivePrinterId"
    - from: "PrinterSelect (cartridge-centric ветка)"
      to: "cartridges.modelsGetCompatibleDevices(effectiveCartridge.model_id) + printers.list()"
      via: "$effect, грузит оба списка при открытии модалки для cartridge!=null && op==='install' && preFillPrinterId===undefined"
      pattern: "modelsGetCompatibleDevices"
---

<objective>
Закрыть оставшиеся 2 FAILED truth из `12-VERIFICATION-ROUND3.md` (GAP-12-11 и
GAP-12-12 п.1/3): cartridge-centric вход установки картриджа («Установить в
принтер» из меню карточки картриджа, `CartridgesPage.svelte` →
`OperationModal` с `cartridge !== null`) структурно не может задать
`preFillPrinterId` — в форме нет UI для выбора принтера. Backend
(`current_printer_device_id`, авто-возврат, инверт. актор) уже реализован и
протестирован (планы 12-06/12-19) — закрыть гэп можно ЧИСТО на фронтенде,
добавив опциональный выбор принтера внутри `OperationModal.svelte`.

По решению пользователя (Round 4, `12-CONTEXT.md` `<gap_closure_round4>`)
это СОЗНАТЕЛЬНО отменяет узкую часть исходного D-08 («старый вход не
меняется») — добавляется новая опциональная возможность, без удаления
старого поведения (если принтер не выбран — 100% legacy-поведение D-08).

D-20: выбор принтера — опционален. D-21: список принтеров приоритизирует
совместимые с моделью картриджа (реверс существующего forward-фильтра
`compatible_with_printer_device_id` из плана 12-05/12-08), без блокировки
при отсутствии связей совместимости. D-22: при выборе принтера с картриджем
«В работе» — показать существующий блок «Предыдущий картридж» (Расположение
+ Уровень заряда, по умолчанию Пустой), переиспользуя уже реализованную в
`OperationModal` разметку и backend-поля `previous_cartridge_state_id` /
`previous_cartridge_location`.

DISC-08 разрешён: НЕ нужен новый backend read-эндпоинт. Реверс-лукап
«совместимые принтеры по модели картриджа» уже существует —
`cartridge_models_get_compatible_devices(modelId) -> {model_id, device_ids}`
(план 12-05/12-07), уже гейтится `Action::ReadData` (Admin|Manager — те же
роли, что видят раздел «Картриджи»). Полный список принтеров — существующий
`printers_list` (тоже `Action::ReadData`). Оба эндпоинта уже покрыты
`role_endpoint_matrix.rs` (Cases 33-35 и существующие printers-кейсы) —
новых RBAC-тестов не требуется.

Purpose: оператор, устанавливающий картридж из карточки картриджа (не из
заявки), получает те же возможности привязки к принтеру/авто-возврата, что
и request-centric поток — без обязательности (D-20) и без молчаливости
авто-возврата (D-22).
Output: новый компонент `PrinterSelect.svelte` + правка `OperationModal.svelte`
(локальный `selectedPrinterId` state, эффект загрузки списка принтеров,
рендер селектора, проводка в существующий лукап и `buildPayload()`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md
@.planning/phases/12-cartridge-request-interconnection/12-VERIFICATION-ROUND3.md
@.planning/phases/12-cartridge-request-interconnection/12-PATTERNS.md

<interfaces>
<!-- Существующие контракты — НЕ менять сигнатуры. Используются напрямую. -->

From ui/src/features/printers/api.ts:
  printers.list(filter: PrinterFilter, pagination: Pagination): Promise<PrinterListResponse>;
    // PrinterFilter = { status: string | null; search: string | null } — оба можно
    // передать null/null для полного списка (как делает D-21's fallback).
  printers.get(id: number): Promise<PrinterDto>;
    // PrinterDto: { id, deviceId, ipAddress, deviceName, deviceLocation,
    //   currentCartridgeId, version, ... } — уже используется в существующем
    // лукапе printerContext (OperationModal.svelte:181-184).

From ui/src/features/cartridges/api.ts:
  cartridges.modelsGetCompatibleDevices(modelId: number): Promise<CartridgeModelCompatibleDevicesDto>;
    // CartridgeModelCompatibleDevicesDto = { model_id: number; device_ids: number[] }
    // Действует как D-21's реверс-лукап «принтеры, совместимые с этой моделью
    // картриджа» — device_ids это PrinterDto.deviceId (НЕ PrinterDto.id).
  cartridges.get(id: number): Promise<CartridgeDto>;
    // Уже используется в существующем лукапе previousCartridge.

From ui/src/bindings.ts / bindings-phase6.ts (точные поля, не выдумывать):
  PrinterDto: { id: number; deviceId: number; ipAddress: string | null;
    deviceName: string | null; deviceLocation: string | null;
    currentCartridgeId: number | null; version: number; ... }
  CartridgeDto: { id: number; model_id: number; model_brand: string | null;
    model_name: string | null; code: string; ... }
  CartridgeTransitionPayload (Install variant, уже существует):
    { op: 'install'; cartridge_id; version; date_utc; given_by_name;
      given_to_name; location; printer_device_id: number | null;
      previous_cartridge_state_id: number | null;
      previous_cartridge_location: string | null }

Существующий паттерн-образец для нового компонента (НЕ менять, только
скопировать форму): ui/src/lib/components/GroupedPrinterSelect.svelte —
плоский `<select>` + `<optgroup>` по группам, проп `options: T[]`, `value`,
`onchange`. PrinterSelect повторяет тот же скелет, но группирует по
совместимости (через переданный набор `compatibleDeviceIds: Set<number>`),
а не по location, и принимает `PrinterDto[]` вместо `RequestPrinterOptionDto[]`.

OperationModal.svelte текущая структура (для точной вставки):
  - `printerContext` $state (строка 145), `printerContextHint` $derived (152-160)
    — уже гейтится `op === 'install' && preFillPrinterId !== undefined`.
  - Лукап-эффект printerContext/previousCartridge (строки 175-201) — гейт
    `open && op === 'install' && preFillPrinterId !== undefined`.
  - `buildPayload()` install-ветка (строки 314-327) — `printer_device_id:
    preFillPrinterId ?? null`.
  - reset-эффект (116-138) — обнуляет previousCartridge/printerContext при
    открытии модалки.
  - previous-cartridge блок рендера (485-523) — гейт `{#if previousCartridge}`,
    НЕ зависит от входа — менять не нужно.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать PrinterSelect.svelte — селектор принтера с приоритизацией совместимых (D-21)</name>
  <files>ui/src/lib/components/PrinterSelect.svelte</files>
  <read_first>
    - ui/src/lib/components/GroupedPrinterSelect.svelte — весь файл (149 строк).
      Скелет для копирования: `<div class="select-wrapper"><select>...<optgroup>` +
      caret SVG + идентичный SCSS-блок (.select-wrapper/.select/.caret/optgroup/option).
    - ui/src/bindings-phase6.ts (строки 10-35) — точные поля `PrinterDto`
      (id, deviceId, ipAddress, deviceName, deviceLocation, currentCartridgeId).
  </read_first>
  <action>
    Создать `ui/src/lib/components/PrinterSelect.svelte` — новый компонент по
    образцу `GroupedPrinterSelect.svelte` (идентичная разметка `<select>` +
    `<optgroup>` + caret SVG + идентичный `<style lang="scss">` блок —
    скопировать .select-wrapper/.select/.caret/optgroup/option стили без
    изменений для визуальной консистентности). Отличия от образца:
    1. Props: `options: PrinterDto[]`, `compatibleDeviceIds: Set<number>`,
       `value: string` (id принтера как строка, как в GroupedPrinterSelect),
       `disabled?: boolean`, `invalid?: boolean`, `id?: string`,
       `onchange?: (_value: string) => void`. Импортировать `PrinterDto` из
       `'../../bindings-phase6'`.
    2. Группировка через `$derived.by`: если `compatibleDeviceIds.size === 0`
       — ОДНА группа без optgroup-заголовка (просто плоский список опций,
       как при отсутствии связей совместимости — D-21 fallback, не блокирует).
       Если `compatibleDeviceIds.size > 0` — ДВЕ группы: `optgroup label="Совместимые принтеры"`
       (где `compatibleDeviceIds.has(p.deviceId)`) и `optgroup label="Остальные принтеры"`
       (остальные); группу «Совместимые» рендерить первой. Группа «Остальные»
       не скрывается и не блокируется — только идёт ниже (D-21: «показать ВСЕ
       принтеры без блокировки»).
    3. Текст опции: `${p.deviceName ?? `Принтер #${p.deviceId}`}${p.deviceLocation ? ` — ${p.deviceLocation}` : ''}`
       (имя+расположение, для узнаваемости устройства — без IP, т.к. оператор
       выбирает по месту установки, а IP уже показан позже в printerContextHint
       после выбора). `value={String(p.deviceId)}` — ВАЖНО: используем
       `deviceId` (не `p.id`), т.к. `preFillPrinterId`/`printer_device_id` во
       всём остальном коде модалки — это device id принтера, не id записи
       printers-таблицы (см. `printers.get(preFillPrinterId)` в OperationModal).
    4. Первая опция-плейсхолдер `<option value="">Без привязки к принтеру</option>`
       (D-20: пустое значение — валидный выбор «не привязывать», а не «не
       выбрано»/ошибка валидации).
    5. Пустой `options.length === 0` → `<option value="" disabled>Принтеры не найдены</option>`
       после плейсхолдера (как в GroupedPrinterSelect).
    НЕ переиспользовать `GroupedPrinterSelect.svelte` напрямую — он типизирован
    на `RequestPrinterOptionDto[]` и группирует по location, а не по
    совместимости; переиспользование потребовало бы изменения его сигнатуры
    и могло бы задеть `RequestFormModal.svelte` (другой потребитель).
  </action>
  <verify>
    <automated>pnpm --dir ui exec svelte-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir ui exec svelte-check` → 0 новых ошибок в `PrinterSelect.svelte`.
    - Файл экспортирует компонент с props `options`, `compatibleDeviceIds`,
      `value`, `onchange` (минимум) — `grep -n "interface Props" -A 8 ui/src/lib/components/PrinterSelect.svelte`
      показывает все 6 полей.
    - `grep -c "optgroup" ui/src/lib/components/PrinterSelect.svelte` ≥ 2
      (условный рендер двух optgroup при наличии совместимых).
    - `grep -c "Без привязки к принтеру" ui/src/lib/components/PrinterSelect.svelte`
      == 1 (плейсхолдер для D-20).
  </acceptance_criteria>
  <done>
    `PrinterSelect.svelte` существует, типобезопасен, рендерит плоский список
    при отсутствии совместимости и две группы (Совместимые/Остальные) при её
    наличии; пустой выбор — валидное «без принтера».
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Подключить PrinterSelect в OperationModal — опциональный выбор принтера в cartridge-centric установке (D-20/D-21/D-22)</name>
  <files>ui/src/features/cartridges/OperationModal.svelte</files>
  <read_first>
    - ui/src/features/cartridges/OperationModal.svelte — весь файл (687 строк),
      уже прочитан в этой сессии. Ключевые точки вставки:
      * Props (22-53) — без изменений сигнатуры.
      * `printerContext`/`printerContextHint` (145-160) — без изменений логики,
        НО `preFillPrinterId` в их выражениях ЗАМЕНЯЕТСЯ на новую переменную
        `effectivePrinterId` (см. ниже).
      * Лукап-эффект (175-201), гейт строка 176 — `preFillPrinterId` →
        `effectivePrinterId`.
      * `compatibilityUnconfigured`-эффект (216-232) и `cartridgeOptions`-эффект
        (239-269) — гейтятся `cartridge === null`, НЕ трогать (D-08 регрессия,
        request-centric только).
      * `buildPayload()` install-ветка (314-327) — `printer_device_id:
        preFillPrinterId ?? null` → `printer_device_id: effectivePrinterId ?? null`.
      * reset-эффект (116-138) — добавить обнуление новых state-переменных при
        открытии модалки.
      * Рендер (447-632) — секция `{#if op === 'install' || op === 'to_refill'}`
        (457-575) — место для нового селектора, ПЕРЕД `printerContextHint`
        (см. action п.5 про порядок).
  </read_first>
  <action>
    В `OperationModal.svelte`:

    1. Импортировать новый компонент: `import PrinterSelect from
       '$lib/components/PrinterSelect.svelte';` и тип `PrinterDto` уже
       импортирован (строка 18) — добавить импорт `PrinterFilter` не нужен
       (передаём `{ status: null, search: null }` инлайн).

    2. Добавить новые `$state`-переменные рядом с `printerContext` (после
       строки 145): `selectedPrinterId = $state<number | undefined>(undefined)`
       (D-20: оператор выбрал принтер в cartridge-centric потоке, undefined =
       «не выбран»); `printerOptions = $state<PrinterDto[]>([])` (полный список
       для селектора); `compatibleDeviceIds = $state<Set<number>>(new Set())`
       (D-21: device_ids из реверс-лукапа).

    3. Добавить производную `effectivePrinterId = $derived(preFillPrinterId ?? selectedPrinterId)`
       — единая точка истины: request-centric поток продолжает использовать
       проп `preFillPrinterId` (приоритет), cartridge-centric поток использует
       новый `selectedPrinterId`. Использовать `effectivePrinterId` ВМЕСТО
       `preFillPrinterId` во ВСЕХ местах, которые сейчас читают `preFillPrinterId`
       для логики лукапа/payload (строки 152-160 printerContextHint, 175-201
       лукап-эффект, 314-327 buildPayload). НЕ менять `compatibilityUnconfigured`-
       эффект (216) и `cartridgeOptions`-эффект (239) — они проверяют
       `preFillPrinterId` именно как «это request-centric с контекстом» и
       должны остаться на исходном пропе (cartridge-centric никогда не задаёт
       этот проп, только локальный `selectedPrinterId`).

    4. Добавить новый `$effect`, загружающий `printerOptions` и
       `compatibleDeviceIds` ТОЛЬКО для нового cartridge-centric селектора:
       гейт `open && op === 'install' && cartridge !== null && preFillPrinterId === undefined`
       (D-20/DISC-07: показываем селектор именно в этом случае — карточка
       картриджа уже задана пропом, но контекст принтера НЕ пришёл извне).
       Внутри: `Promise.all([printers.list({ status: null, search: null }, { offset: 0, limit: 500 }), cartridges.modelsGetCompatibleDevices(cartridge.model_id)])`
       — `.then(([printersRes, compatRes]) => { printerOptions = printersRes.items; compatibleDeviceIds = new Set(compatRes.device_ids); })`
       — `.catch(() => { printerOptions = []; compatibleDeviceIds = new Set(); })`
       (fail-safe: ошибка лукапа совместимости — лишь сужение приоритета, не
       блокировка; D-21 требует показывать ВСЕ принтеры в любом случае, пустой
       `printerOptions` при ошибке — единственный случай, когда список пуст,
       и тогда `PrinterSelect` покажет «Принтеры не найдены», что приемлемо
       как fail-safe деградация, а не блокировка установки без принтера).
       Early-return (когда гейт не выполнен) должен обнулять оба state —
       `printerOptions = []; compatibleDeviceIds = new Set();`.

    5. В шаблоне, внутри `{#if op === 'install' || op === 'to_refill'}` (строка
       457), ПЕРЕД существующим `{#if printerContextHint}` (458), добавить:
       `{#if op === 'install' && cartridge !== null && preFillPrinterId === undefined}`
       — новый блок `<div class="field">` с `<label for="op-printer">Принтер
       (опционально)</label>` и `<PrinterSelect options={printerOptions}
       compatibleDeviceIds={compatibleDeviceIds} value={selectedPrinterId !== undefined ? String(selectedPrinterId) : ''}
       id="op-printer" onchange={(v) => { selectedPrinterId = v ? parseInt(v, 10) : undefined; }} />`.
       Это рендерится ТОЛЬКО для cartridge-centric установки без внешнего
       контекста — request-centric (`cartridge === null`) и предзаполненный
       (`preFillPrinterId !== undefined`, из заявки) входы его не показывают
       (без регрессии в request-centric UI).

    6. В reset-эффекте (строка 116-138) добавить `selectedPrinterId = undefined;`
       рядом с существующим `previousCartridge = null;`/`printerContext = null;`
       (строки 130-133) — выбор принтера должен сбрасываться при каждом
       открытии модалки, как и остальной printer-контекст.

    7. Previous-cartridge блок (строки 485-523) уже гейтится `{#if previousCartridge}`
       и НЕ зависит от входа — после правок п.2-4 он автоматически появится,
       когда `effectivePrinterId` (через выбор оператора) указывает на принтер
       с картриджем «В работе» (D-22 удовлетворяется без изменения разметки
       блока — переиспользование, как и было решено).

    НЕ менять `validate()`, `handleSubmit()`, `CARTRIDGE_STATES`/`DRUM_STATES`,
    previous-cartridge `<Select>` options (известный WR-02, вне scope Round 4).
    НЕ трогать backend, не регенерировать биндинги.
  </action>
  <verify>
    <automated>pnpm --dir ui exec svelte-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --dir ui exec svelte-check` → 0 ошибок.
    - `pnpm --dir ui build` собирается без ошибок.
    - Source-assertion: `grep -n "effectivePrinterId" ui/src/features/cartridges/OperationModal.svelte`
      показывает определение через `$derived` и минимум 3 использования
      (printerContextHint, лукап-эффект, buildPayload).
    - Source-assertion: `grep -c "cartridge === null" ui/src/features/cartridges/OperationModal.svelte`
      не уменьшилось относительно текущего значения (4) — новый код не
      затронул существующие request-centric гейты `compatibilityUnconfigured`/
      `cartridgeOptions`.
    - Source-assertion: `grep -n "PrinterSelect" ui/src/features/cartridges/OperationModal.svelte`
      показывает импорт и одно использование в шаблоне, гейтящееся
      `cartridge !== null && preFillPrinterId === undefined`.
    - Поведенческая проверка (после build, ручная или через существующий e2e,
      если есть): открыть «Установить в принтер» с карточки картриджа без
      предыдущего принтер-контекста — виден новый селектор «Принтер
      (опционально)»; выбрать принтер с картриджем «В работе» — появляется
      блок «Предыдущий картридж» с редактируемыми полями; не выбирать принтер
      — форма ведёт себя как раньше (без блока, `printer_device_id=null` в
      payload).
  </acceptance_criteria>
  <done>
    Cartridge-centric установка позволяет опционально выбрать принтер;
    список принтеров приоритизирует совместимые с моделью картриджа; выбор
    принтера с активным картриджем показывает редактируемый блок «Предыдущий
    картридж»; request-centric поток и поток без выбора принтера не
    регрессировали; svelte-check и build зелёные.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| webview → printers.list / cartridges.modelsGetCompatibleDevices | Read-вызовы; уже гейтятся `authorize(caller, &Action::ReadData)` на бэкенде (Admin\|Manager). Раздел «Картриджи» в принципе недоступен Employee (Phase 10 закрыл доступ) — модалка физически не открывается для роли без доступа к этим эндпоинтам. |
| webview → cartridges_transition (printer_device_id) | Существующая граница, не меняется этим планом — `printer_device_id` уже опционален в backend DTO и уже проходит через `authorize(&Action::MutateCartridges)` (план 12-06/12-19). Этот план добавляет только клиентский ИСТОЧНИК значения (выбор оператора вместо хардкода `null`/`preFillPrinterId`-пропа). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-20-01 | Information Disclosure | `printers.list({status:null,search:null}, {limit:500})` — полный список принтеров загружается в cartridge-centric модалке | accept | Тот же эндпоинт (`Action::ReadData`) и те же данные, что уже доступны на странице «Принтеры» этой же роли (Admin\|Manager). Никаких новых полей/данных не покидает сервер — `PrinterDto` уже используется в `PrintersPage.svelte`. Employee не имеет доступа к разделу «Картриджи», модалка ему недостижима. |
| T-12-20-02 | Tampering | Клиент произвольно задаёт `printer_device_id` в payload `cartridges_transition` | accept (существующий риск, не новый) | Backend уже не доверяет клиенту слепо: `transition_in_tx` проверяет существование устройства через FK (`cartridges.current_printer_device_id REFERENCES devices`) — несуществующий id отклоняется на уровне БД constraint. Это та же поверхность, что уже существовала в request-centric потоке (план 12-06) — план 20 не расширяет её, только добавляет второй клиентский путь к тому же полю. |
| T-12-20-03 | Tampering | npm-зависимости | mitigate | Новых npm-зависимостей не добавляется — один новый `.svelte`-файл (без внешних импортов кроме существующих типов) и правка одного существующего файла. |
| T-12-20-04 | Denial of Service | Лишний `printers.list`/`modelsGetCompatibleDevices` запрос при каждом открытии cartridge-centric установки | mitigate | Эффект гейтится строго на `op === 'install' && cartridge !== null && preFillPrinterId === undefined` — НЕ выполняется для return_to_stock/to_refill/from_refill/write_off и НЕ выполняется, когда модалка открыта из заявки (`preFillPrinterId` уже задан). `limit: 500` ограничивает размер ответа при большом парке принтеров. |
| T-12-20-05 | Elevation of Privilege | Оператор через cartridge-centric поток получает возможность того же действия (привязка к принтеру + авто-возврат), что уже доступна через request-centric поток той же роли | accept | Обе ветки UI гейтятся одинаковой ролью (Admin\|Manager — доступ к разделу «Картриджи»); это не новая возможность для роли, а унификация существующей возможности между двумя точками входа, как явно решено пользователем (D-20/D-21/D-22, Round 4). |
</threat_model>

<verification>
- `pnpm --dir ui exec svelte-check` → 0 ошибок.
- `pnpm --dir ui build` собирается без ошибок (`✓ built`).
- `TRACKLY_AD_MOCK=1 cargo test` (по одному вызову, без параллельного второго —
  пользовательское ограничение) — backend не тронут этим планом, ожидается
  без изменений относительно текущего зелёного состояния (план чисто
  фронтенд, нет новых Rust-тестов).
- Ручная/UAT проверка после build: cartridge-centric установка с выбором
  совместимого принтера → блок «Предыдущий картридж» (если есть активный
  картридж) → после подтверждения `current_printer_device_id` записан,
  предыдущий картридж возвращён на склад с указанными оператором
  значениями. Без выбора принтера — поведение идентично D-08 (legacy).
</verification>

<success_criteria>
Оба FAILED truth из `12-VERIFICATION-ROUND3.md` (GAP-12-11, GAP-12-12 п.1/3)
закрыты: cartridge-centric установка показывает опциональный выбор
принтера, приоритизирующий совместимые модели (D-21), и при выборе принтера
с активным картриджем — редактируемый блок «Предыдущий картридж» (D-22), без
обязательности (D-20) и без регрессии request-centric потока или
старого «без принтера» cartridge-centric потока. svelte-check/build зелёные.
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-20-SUMMARY.md` when done
</output>
