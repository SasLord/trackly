---
phase: 12-cartridge-request-interconnection
plan: 07
type: execute
wave: 2
depends_on: ["12-05"]
files_modified:
  - ui/src/bindings-phase6.ts
  - ui/src/features/printers/CompatibleModelsEditor.svelte
  - ui/src/features/printers/PrinterDetail.svelte
  - ui/src/features/printers/api.ts
  - ui/src/features/cartridges/CompatibleDevicesEditor.svelte
  - ui/src/features/cartridges/ModelFormModal.svelte
autonomous: true
gap_closure: true
requirements: [D-12]

must_haves:
  truths:
    - "На карточке принтера (PrinterDetail) можно увидеть и отредактировать список совместимых моделей картриджей (kind_id=1) для этого принтера"
    - "В форме редактирования модели картриджа (ModelFormModal) можно увидеть и отредактировать список принтеров, совместимых с этой моделью"
    - "Обе формы пишут/читают одну и ту же связку printer_cartridge_models через backend-команды из 12-05 — изменение с одной стороны видно с другой"
    - "Старый free-text редактор совместимости (CompatibilityEditor, V005 cartridge_model_compatibility) продолжает работать без изменений — не путается с новым редактором"
  artifacts:
    - path: "ui/src/features/printers/CompatibleModelsEditor.svelte"
      provides: "Чеклист моделей картриджей (kind_id=1) с чекбоксами, save через printers_set_compatible_models"
      min_lines: 40
    - path: "ui/src/features/cartridges/CompatibleDevicesEditor.svelte"
      provides: "Чеклист принтеров (devices type_id=2) с чекбоксами, save через cartridge_models_set_compatible_devices"
      min_lines: 40
  key_links:
    - from: "ui/src/features/printers/PrinterDetail.svelte"
      to: "printers_get_compatible_models / printers_set_compatible_models"
      via: "ui/src/features/printers/api.ts wrapper calls"
      pattern: "getCompatibleModels|setCompatibleModels"
    - from: "ui/src/features/cartridges/ModelFormModal.svelte"
      to: "cartridge_models_get_compatible_devices / cartridge_models_set_compatible_devices"
      via: "ui/src/features/cartridges/api.ts wrapper calls"
      pattern: "getCompatibleDevices|setCompatibleDevices"
---

<objective>
Закрыть GAP-12-02 (фронтенд-часть, D-12): добавить на карточку принтера (PrinterDetail.svelte) редактор совместимых моделей картриджей, и на форму модели картриджа (ModelFormModal.svelte) — редактор совместимых принтеров. Обе стороны управляют одной и той же junction-таблицей `printer_cartridge_models` через dual-transport команды, созданные в 12-05.

Purpose: до этого плана связка принтер↔модель картриджа существовала только в backend (12-05) — не было способа её заполнить через UI, поэтому фильтр `compatible_with_printer_device_id` в `cartridges_list` всегда работал в "не настроено" режиме (D-14 fallback), и заявка «Замена картриджа» никогда показывала реально отфильтрованный список (GAP-12-02 root cause).
Output: два новых компонента-редактора (`CompatibleModelsEditor.svelte`, `CompatibleDevicesEditor.svelte`), вписанные в существующие карточки; новые wrapper-методы в `printers/api.ts` и `cartridges/api.ts`; ручное добавление 4 новых команд в `bindings-phase6.ts` (поскольку этот файл — НЕ автогенерируемый, см. interfaces ниже).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md

<interfaces>
<!-- Backend contract established in 12-05-PLAN.md (already merged/DONE) — this plan
     consumes it, does not redefine it. -->

Commands (dual-transport, registered in specta_export.rs + http/cartridges.rs by 12-05):
```
printers_get_compatible_models(deviceId: number) -> PrinterCompatibleModelsDto
printers_set_compatible_models(payload: PrinterCompatibleModelsDto) -> PrinterCompatibleModelsDto
cartridge_models_get_compatible_devices(cartridgeModelId: number) -> number[]
cartridge_models_set_compatible_devices(cartridgeModelId: number, deviceIds: number[]) -> number[]
```
RBAC (12-05): read = Action::ReadData (Admin|Manager); printer-side write = Action::MutatePrinters (Admin|Manager); model-side write = Action::MutateCartridges (Admin|Manager). Employee role gets 403 on all four — these are management-only commands, the new editors must NOT be rendered/usable for Employee (PrinterDetail.svelte and ModelFormModal.svelte are already Admin/Manager-only screens per existing route guards — verify this assumption holds, do not add a redundant role check inside the new components if the parent screen already gates access).

`PrinterCompatibleModelsDto` shape (crates/trackly-app/src/dto/printer.rs, added by 12-05):
```rust
#[serde(rename_all = "camelCase")]
pub struct PrinterCompatibleModelsDto {
    #[specta(type = i32)] pub device_id: i64,
    pub model_ids: Vec<i64>,
}
```
TypeScript shape (camelCase, per S-2): `{ deviceId: number, modelIds: number[] }`.

`cartridge_models_get_compatible_devices` / `cartridge_models_set_compatible_devices` return/accept a PLAIN `number[]` (no wrapper DTO) per 12-05's task description — confirm this exact shape when wiring (do not invent a wrapper object for the model-side pair; the printer-side pair uses a DTO, the model-side pair uses bare arrays, by 12-05's design, asymmetric on purpose since the model-side setter's only needed input/output is the device id list keyed by the already-known cartridge_model_id route param).

<!-- bindings-phase6.ts is HAND-MAINTAINED, not gitignored, not auto-regenerated —
     confirmed by its own header comment: "Phase 6 types — manually maintained
     alongside Rust DTOs... supplement the generated bindings.ts (gitignored,
     regenerated by cargo test)." The 4 new printer-side types/commands from 12-05
     belong here since PrinterDto already lives here. The model-side pair
     (cartridge_models_get_compatible_devices/set) calls through cartridges/api.ts
     which imports from `../../bindings` (the AUTO-GENERATED file) — that file
     regenerates automatically on the next `cargo test` run that exercises the
     specta export integration test; do not hand-edit ui/src/bindings.ts. -->

`PrinterDto` (ui/src/bindings-phase6.ts, current shape, unchanged by this plan):
```typescript
export type PrinterDto = {
  id: number;
  deviceId: number;
  // ...
  currentCartridgeId: number | null;
  version: number;
};
```
`printer.deviceId` is the FK value to bind into `printers_get_compatible_models({ deviceId: printer.deviceId })` — NOT `printer.id` (the printers table's own PK differs from the devices table's PK; `printer_cartridge_models.device_id` references `devices.id`, which is `PrinterDto.deviceId`).

`ui/src/features/printers/api.ts` current shape (full file, already read — add two new wrapper methods following its existing flat-object style, no class, just object literal with arrow functions calling `apiCall`):
```typescript
export const printers = {
  list: (filter, pagination) => apiCall<PrinterListResponse>('printers_list', { filter, pagination }),
  get: (id) => apiCall<PrinterDto>('printers_get', { id }),
  // ... existing methods unchanged
};
```

`ui/src/features/cartridges/api.ts` current shape (full file, already read) — `cartridges` object literal; model-side methods live under the "Models CRUD" comment block (`modelsList`, `modelsGet`, `modelsCreate`, `modelsUpdate`, `modelsDelete`) — add the two new methods adjacent to those, importing the two new function names from `'../../bindings'` (auto-generated; these types will exist after 12-05's backend lands and `cargo test` regenerates bindings.ts — if bindings.ts has not yet regenerated when this plan executes, run `cargo test -p trackly-app --test specta_export` or equivalent once to force regeneration before writing the TS import).

<!-- PrinterDetail.svelte insertion point — full file already read (450 lines).
     Section "Установленный картридж" (lines 184-192) is the existing analog
     section to place the new compatibility editor AFTER (separate <section
     class="detail-section">), inside the `{:else}` branch (printer !== null
     guaranteed there), within `.detail-body`. -->

Existing section pattern to copy (PrinterDetail.svelte lines 184-192):
```svelte
<section class="detail-section">
  <h3 class="section-heading">Установленный картридж</h3>
  {#if printer.currentCartridgeId !== null}
    <p class="cartridge-row">Картридж #{printer.currentCartridgeId}</p>
  {:else}
    <p class="muted">Картридж не закреплён</p>
  {/if}
</section>
```

<!-- ModelFormModal.svelte insertion point — full file already read (581 lines).
     CompatibilityEditor (the OLD V005 free-text editor) sits at lines 442-451,
     inside `.form-grid`, gated by nothing (always rendered). The NEW D-12 editor
     must be a SEPARATE section, gated by `isEdit` (only when target !== null,
     since it needs a real model id), with a DISTINCT heading so it is never
     confused with "Совместимые принтеры" (old editor's heading). -->

Existing old-editor block to insert AFTER (ModelFormModal.svelte lines 442-451):
```svelte
<div class="field field-full compat-section">
  <h3 class="compat-heading">Совместимые принтеры</h3>
  <CompatibilityEditor
    {compatibility}
    onChange={(pairs) => (compatibility = pairs)}
    suggestBrandFn={(prefix) => cartridges.suggestCompatPrinter('printer_brand', prefix)}
    suggestModelFn={(prefix) => cartridges.suggestCompatPrinter('printer_model', prefix)}
  />
</div>
```
`isEdit = $derived(target !== null)` (line 38) — gate the new section the same way DeviceFormBody-style components gate edit-only sections; `target: CartridgeModelDto | null` is the prop (line 31), `target.id` is the model id once `isEdit` is true.

<!-- Checkbox convention: NO dedicated Checkbox.svelte component exists in
     ui/src/lib/components/ — codebase convention is raw `<input type="checkbox">`
     (confirmed in DeviceFilters.svelte, ReturnItemsTable.svelte, ActiveDirectorySettings.svelte,
     UserFormModal.svelte). The new editors follow this same raw-input convention,
     do not invent a new Checkbox component for this plan. -->

`GroupedPrinterSelect.svelte` (full file, already read) — NOT directly reused (it's a single-select `<select>`), but its grouping-by-location pattern (`$derived.by` building a `Map<string, T[]>`) is the reference style for `CompatibleModelsEditor.svelte`'s printer-side checklist IF grouping by location is desired for the device picklist on the model-side editor (optional polish — flat list with brand/model labels is also acceptable per Claude's discretion, since D-12 does not mandate grouping).

`printers.list(filter: PrinterFilter, pagination)` returns `PrinterListResponse` (paginated) — for the model-side editor's device checklist, call with a high `limit` (e.g. 500) and `filter: { status: null, search: null }` to get the full printer roster in one call (org-scale device counts are small, per CLAUDE.md's "20 concurrent users" LAN scale — no need for paginated UI here).

`cartridges.modelsList()` returns `CartridgeModelDto[]` (full file, already read in cartridges/api.ts) — for the printer-side editor's model checklist, call this and CLIENT-SIDE filter to `kind_id === 1` (exclude photo-drums, kind_id=2 — D-12 scope is cartridge models only per the printer-side compatibility relationship's intended use, matching D-13's kind_id=1 exclusion in the install picker).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API wrappers + bindings-phase6.ts additions</name>
  <files>ui/src/bindings-phase6.ts, ui/src/features/printers/api.ts, ui/src/features/cartridges/api.ts</files>
  <read_first>
    - ui/src/bindings-phase6.ts (lines 1-60 — PrinterDto + header comment, already in context above)
    - ui/src/features/printers/api.ts (full file, 38 lines, already in context above)
    - ui/src/features/cartridges/api.ts (full file, 71 lines, already in context above)
  </read_first>
  <action>
    In ui/src/bindings-phase6.ts, add a new exported type `PrinterCompatibleModelsDto = { deviceId: number; modelIds: number[] }` near `PrinterDto`, with a doc comment referencing "D-12, Phase 12 Plan 05/07 — printer_cartridge_models junction." In ui/src/features/printers/api.ts, add two methods to the `printers` object: `getCompatibleModels: (deviceId: number) => apiCall<PrinterCompatibleModelsDto>('printers_get_compatible_models', { deviceId })` and `setCompatibleModels: (payload: PrinterCompatibleModelsDto) => apiCall<PrinterCompatibleModelsDto>('printers_set_compatible_models', { payload })`, importing `PrinterCompatibleModelsDto` from `'../../bindings-phase6'`. In ui/src/features/cartridges/api.ts, add two methods adjacent to the existing `modelsList`/`modelsGet`/etc block: `modelsGetCompatibleDevices: (cartridgeModelId: number) => apiCall<number[]>('cartridge_models_get_compatible_devices', { cartridgeModelId })` and `modelsSetCompatibleDevices: (cartridgeModelId: number, deviceIds: number[]) => apiCall<number[]>('cartridge_models_set_compatible_devices', { cartridgeModelId, deviceIds })`. If `ui/src/bindings.ts` does not yet contain the regenerated `cartridge_models_get_compatible_devices`/`cartridge_models_set_compatible_devices` command names (check via grep before assuming), run `cargo test -p trackly-app --test specta_export 2>&1 | tail -20` (or the equivalent integration test that triggers bindings export — search for the test name if `specta_export` is not exact) once to force regeneration before finalizing this task's TypeScript imports.
  </action>
  <acceptance_criteria>
    - `grep -c "PrinterCompatibleModelsDto" ui/src/bindings-phase6.ts` returns at least 1
    - `grep -c "getCompatibleModels\|setCompatibleModels" ui/src/features/printers/api.ts` returns 2
    - `grep -c "modelsGetCompatibleDevices\|modelsSetCompatibleDevices" ui/src/features/cartridges/api.ts` returns 2
    - `pnpm --dir ui exec tsc --noEmit` reports zero new type errors attributable to these two files
  </acceptance_criteria>
  <done>Both api.ts wrapper files expose typed methods for all 4 backend commands from 12-05; bindings-phase6.ts carries the new DTO type; TypeScript compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompatibleModelsEditor.svelte + wire into PrinterDetail.svelte</name>
  <files>ui/src/features/printers/CompatibleModelsEditor.svelte, ui/src/features/printers/PrinterDetail.svelte</files>
  <read_first>
    - ui/src/features/printers/PrinterDetail.svelte (full file, 450 lines, already in context above — section "Установленный картридж" lines 184-192 is the insertion anchor)
    - ui/src/features/cartridges/api.ts (modelsList method, for fetching the kind_id=1 model roster)
    - ui/src/features/devices/DeviceFilters.svelte (grep result confirms raw `<input type="checkbox">` convention — read lines around its checkbox usage if exact markup/SCSS class names are needed)
  </read_first>
  <behavior>
    - Test 1 (component-level, manual verification since no test harness exists for .svelte files in this codebase — confirm via `pnpm --dir ui build` + browser smoke, not an automated unit test): opening PrinterDetail for a printer with zero compatibility rows shows an empty/unchecked checklist of all kind_id=1 cartridge models; checking 2 models and clicking save calls `printers.setCompatibleModels({ deviceId, modelIds: [id1, id2] })` and the component reflects the saved state after the call resolves (no stale unchecked state).
    - Test 2: re-opening the same printer (or switching away and back via PrintersList selection) reloads the previously saved 2 checked models from `printers.getCompatibleModels(deviceId)` — persistence round-trip confirmed.
    - Test 3: a photo-drum model (kind_id=2) never appears in the checklist, even if it exists in `cartridges.modelsList()`'s results (client-side filter, D-12/D-13 alignment — printer-cartridge compatibility is scoped to kind_id=1 only).
  </behavior>
  <action>
    Create ui/src/features/printers/CompatibleModelsEditor.svelte with props `{ deviceId: number }` (Svelte 5 runes, `$props()`). On mount (`$effect`), call `cartridges.modelsList()` (import from `'../cartridges/api'`) and filter client-side to `kind_id === 1`, building the full checklist roster; separately call `printers.getCompatibleModels(deviceId)` to get the currently-linked `modelIds`, used to pre-check the matching checkboxes. Render a `<section class="detail-section">` (reuse PrinterDetail's existing `.detail-section`/`.section-heading` SCSS classes for visual consistency — pass them as local class names in this new component's own `<style lang="scss">` block, duplicating the token-based styles rather than importing PrinterDetail's scoped styles, per Svelte's style-scoping rules) with heading "Совместимые модели картриджей", a list of `<label><input type="checkbox" bind:checked={...} /> {brand} {model}</label>` rows (one per kind_id=1 model), and a "Сохранить" `Button` (import from `$lib/components/Button.svelte`) that calls `printers.setCompatibleModels({ deviceId, modelIds: <currently-checked ids> })`, shows a loading state on the button while saving, and pushes a success/error toast via `pushToast` (import from `$lib/stores/toast.svelte`, matching PrintersPage.svelte's existing toast usage pattern). Handle the empty-roster case (zero kind_id=1 models exist anywhere) with a muted "Нет моделей картриджей — добавьте модель в разделе «Картриджи»" message instead of an empty checklist. In PrinterDetail.svelte, import `CompatibleModelsEditor` and render `<CompatibleModelsEditor deviceId={printer.deviceId} />` inside a new `<section class="detail-section">` placed immediately AFTER the existing "Установленный картридж" section (after line 192's closing `</section>`), still inside the `{:else}` branch where `printer !== null` is guaranteed.
  </action>
  <acceptance_criteria>
    - `grep -c "printers_get_compatible_models\|getCompatibleModels" ui/src/features/printers/CompatibleModelsEditor.svelte` returns at least 1
    - `grep -c "kind_id === 1\|kind_id==1" ui/src/features/printers/CompatibleModelsEditor.svelte` returns at least 1 (photo-drum exclusion present, not just commented)
    - `grep -c "CompatibleModelsEditor" ui/src/features/printers/PrinterDetail.svelte` returns at least 2 (import + usage)
    - `pnpm --dir ui exec svelte-check` reports zero new errors in these two files
    - `pnpm --dir ui build` succeeds
  </acceptance_criteria>
  <done>PrinterDetail.svelte shows a working checklist of compatible kind_id=1 cartridge models for the current printer, with load + save round-tripping through the 12-05 backend commands; photo-drum models never appear in the list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CompatibleDevicesEditor.svelte + wire into ModelFormModal.svelte</name>
  <files>ui/src/features/cartridges/CompatibleDevicesEditor.svelte, ui/src/features/cartridges/ModelFormModal.svelte</files>
  <read_first>
    - ui/src/features/cartridges/ModelFormModal.svelte (lines 1-90 for Props/isEdit/state, and lines 420-461 for the CompatibilityEditor insertion anchor — already in context above)
    - ui/src/features/printers/api.ts (the printers.list method signature, for fetching the device/printer roster)
  </read_first>
  <behavior>
    - Test 1: opening ModelFormModal in EDIT mode (target !== null) for a model with zero compatibility rows shows an empty/unchecked checklist of all printers (devices type_id=2, via printers.list); checking 2 printers and saving calls `cartridges.modelsSetCompatibleDevices(target.id, [deviceId1, deviceId2])`.
    - Test 2: re-opening the same model reloads the previously saved 2 checked printers from `cartridges.modelsGetCompatibleDevices(target.id)`.
    - Test 3 (D-12 scope guard): opening ModelFormModal in CREATE mode (target === null, isEdit === false) does NOT render the new compatibility-devices section at all (no model id exists yet to link against) — only the OLD free-text CompatibilityEditor (which has always supported create-time entry) remains visible.
  </behavior>
  <action>
    Create ui/src/features/cartridges/CompatibleDevicesEditor.svelte with props `{ cartridgeModelId: number }`. On mount, call `printers.list({ status: null, search: null }, { offset: 0, limit: 500 })` (import `printers` from `'../printers/api'`) to get the full printer roster, and `cartridges.modelsGetCompatibleDevices(cartridgeModelId)` to get currently-linked `deviceIds` for pre-checking. Render a checklist of `<label><input type="checkbox" bind:checked={...} /> {deviceName} {location ? `(${location})` : ''}</label>` rows, one per printer (use `PrinterDto.deviceName`/`deviceLocation` fields for the label — these already exist on the DTO per bindings-phase6.ts), with a "Сохранить" Button calling `cartridges.modelsSetCompatibleDevices(cartridgeModelId, <checked ids>)`, loading state + toast feedback identical in style to Task 2's editor. Handle the empty-roster case ("Принтеры не найдены — заведите принтер в разделе «Принтеры»"). In ModelFormModal.svelte, import `CompatibleDevicesEditor`, and immediately AFTER the existing CompatibilityEditor block (after line 451's closing `</div>`), add a new `{#if isEdit && target}` guarded block rendering a `<div class="field field-full compat-section">` with heading "Совместимые принтеры (по справочнику устройств)" — a heading DISTINCT from the old editor's "Совместимые принтеры" so the two are never visually conflated — containing `<CompatibleDevicesEditor cartridgeModelId={target.id} />`. Do not modify the old `CompatibilityEditor`/`compatibility` state/`handleSubmit`'s old-field submission logic in any way — the new editor saves independently via its own button, not via the modal's main "Сохранить изменения" submit.
  </action>
  <acceptance_criteria>
    - `grep -c "modelsGetCompatibleDevices\|modelsSetCompatibleDevices" ui/src/features/cartridges/CompatibleDevicesEditor.svelte` returns at least 1
    - `grep -c "isEdit" ui/src/features/cartridges/ModelFormModal.svelte | grep -v '^0$'` confirms isEdit guard reused (file already had isEdit before this plan; count increases by at least 1 new usage)
    - `grep -c "CompatibleDevicesEditor" ui/src/features/cartridges/ModelFormModal.svelte` returns at least 2 (import + usage)
    - `grep -c "Совместимые принтеры (по справочнику устройств)" ui/src/features/cartridges/ModelFormModal.svelte` returns at least 1 (distinct heading, no collision with old editor's heading text)
    - `pnpm --dir ui exec svelte-check` reports zero new errors in these two files
    - `pnpm --dir ui build` succeeds
  </acceptance_criteria>
  <done>ModelFormModal.svelte (edit mode only) shows a working checklist of compatible printers for the current cartridge model, saving/loading through the 12-05 backend commands, with zero interference with the pre-existing free-text CompatibilityEditor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin/Manager UI → printers_set_compatible_models / cartridge_models_set_compatible_devices | Authenticated management-role input; device/model ids are numeric, never free text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-07-01 | Elevation of Privilege | CompatibleModelsEditor / CompatibleDevicesEditor rendering | accept | Both new components are mounted inside screens (PrinterDetail, ModelFormModal) already gated to Admin/Manager by the surrounding page; backend commands independently enforce Action::MutatePrinters/MutateCartridges per 12-05 regardless of UI gating (defense in depth already established, no new gap introduced) |
| T-12-07-02 | Tampering | Checked id arrays sent to setCompatibleModels/setCompatibleDevices | mitigate | Backend (12-05) binds all ids via params![] with FK constraints (ON DELETE CASCADE) — a tampered/invalid id is rejected at the DB layer, not silently accepted |
</threat_model>

<verification>
- `pnpm --dir ui build` succeeds with zero new errors
- `pnpm --dir ui exec svelte-check` reports zero new errors
- Manual: open a printer in PrinterDetail, check 2 cartridge models, save, reload page, confirm both still checked
- Manual: open a cartridge model in edit mode, check 2 printers, save, reload, confirm both still checked
- Manual: confirm the OLD CompatibilityEditor (free-text brand/model) in ModelFormModal still works unchanged
</verification>

<success_criteria>
- Printer card exposes a working compatible-cartridge-models editor (kind_id=1 only)
- Cartridge model edit form exposes a working compatible-printers editor
- Both write to the same printer_cartridge_models junction via 12-05 commands
- No regression to the pre-existing free-text compatibility editor
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-07-SUMMARY.md` when done
</output>
