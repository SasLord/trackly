---
phase: 12-cartridge-request-interconnection
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - crates/trackly-app/src/services/request_service.rs
  - crates/trackly-app/tests/phase06_stubs.rs
  - crates/trackly-app/tests/role_endpoint_matrix.rs
  - ui/src/bindings.ts
  - ui/src/bindings-phase6.ts
autonomous: true
requirements: [D-06, D-07]

must_haves:
  truths:
    - "После Complete{linked_cartridge_id} заявка несёт completed_cartridge_id равный id установленного картриджа"
    - "История заявки показывает человекочитаемый код+модель установленного картриджа после complete с linked_cartridge_id"
    - "Employee-сессия получает 403 Forbidden при попытке вызвать cartridges_transition или requests_transition через HTTP"
  artifacts:
    - path: "crates/trackly-app/src/services/request_service.rs"
      provides: "transition() читает cartridge через cartridge_repo и обогащает notes_json кодом+моделью"
      contains: "cartridge_repo"
    - path: "crates/trackly-app/tests/phase06_stubs.rs"
      provides: "test_req_cart_link реализован как #[tokio::test], не #[ignore]"
      contains: "async fn test_req_cart_link"
    - path: "crates/trackly-app/tests/role_endpoint_matrix.rs"
      provides: "Case 20/21 — employee 403 на cartridges_transition/requests_transition"
      contains: "cartridges_transition"
  key_links:
    - from: "crates/trackly-app/src/services/request_service.rs"
      to: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      via: "SqliteCartridgeRepository::get(&conn, id) внутри transition()"
      pattern: "cartridge_repo\\.get"
    - from: "crates/trackly-app/tests/phase06_stubs.rs"
      to: "crates/trackly-app/src/services/request_service.rs"
      via: "RequestService::transition(Complete{linked_cartridge_id})"
      pattern: "linked_cartridge_id"
---

<objective>
Замкнуть связь «установка → заявка» на сервисном уровне: `RequestService::transition()`
при `Complete{linked_cartridge_id: Some(id)}` должен (1) сохранить связь в
`completed_cartridge_id` заявки — это уже происходит на уровне `transition_in_tx`,
но не покрыто работающим тестом — и (2) обогатить JSON-снапшот истории кодом и
моделью картриджа (D-07), читая `CartridgeRepository` синхронно перед записью
audit-записи. Плюс закрыть пробел RBAC: ни `cartridges_transition`, ни
`requests_transition` не покрыты role×endpoint матрицей для employee-deny (T-12-01).

Purpose: Это единственное место, где «выбор картриджа» (Wave 1 — данные есть) и
«запись связи + история» (бизнес-смысл D-06/D-07) физически соединяются. Без этого
плана Wave 3 (фронтенд) будет посылать `linkedCartridgeId`, но backend не будет
насыщать историю осмысленным текстом, и RBAC-пробел останется незакрытым.
Output: рабочий `test_req_cart_link`, обогащённая история заявки, 2 новых
RBAC-кейса (20/21) в `role_endpoint_matrix.rs`, регенерированные `bindings*.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-cartridge-request-interconnection/12-CONTEXT.md
@.planning/phases/12-cartridge-request-interconnection/12-RESEARCH.md

<!-- Wave 1 даёт RequestDto.printer_location и CartridgeFilter.installable_only —
этот план их не использует напрямую, но зависит от того, что domain/DTO слой
уже стабилен (одна и та же RequestRow/RequestDto форма, без повторных миграций SQL). -->

<interfaces>
<!-- RequestService — crates/trackly-app/src/services/request_service.rs, lines 32-59
     Текущие поля: writer, readers, clock, request_repo, audit_repo, ws_tx.
     SqliteCartridgeRepository — zero-sized struct (crates/trackly-infra/src/repos/cartridges_sqlite.rs:27),
     как и SqliteRequestRepository — добавляется аналогично, БЕЗ изменения сигнатуры RequestService::new(). -->
```rust
#[derive(Clone)]
pub struct RequestService {
    pub writer: Arc<WriterHandle>,
    pub readers: Arc<ReaderPool>,
    pub(crate) clock: Arc<dyn Clock + Send + Sync>,
    pub(crate) request_repo: Arc<SqliteRequestRepository>,
    pub(crate) audit_repo: Arc<SqliteAuditLogRepository>,
    pub(crate) ws_tx: Arc<tokio::sync::broadcast::Sender<WsEvent>>,
}

impl RequestService {
    pub fn new(
        writer: Arc<WriterHandle>,
        readers: Arc<ReaderPool>,
        clock: Arc<dyn Clock + Send + Sync>,
        ws_tx: Arc<tokio::sync::broadcast::Sender<WsEvent>>,
    ) -> Self {
        Self {
            writer, readers, clock,
            request_repo: Arc::new(SqliteRequestRepository),
            audit_repo: Arc::new(SqliteAuditLogRepository),
            ws_tx,
        }
    }
}
```

<!-- CartridgeRepository trait — crates/trackly-core/src/ports/cartridges.rs, lines 19-25 -->
```rust
pub trait CartridgeRepository {
    type Conn;
    fn get(&self, conn: &Self::Conn, id: i64) -> Result<CartridgeRow, AppError>;
    // ...
}
```

<!-- CartridgeRow — crates/trackly-core/src/domain/cartridges.rs, lines 16-44 (поля нужные для снапшота: code, model_brand, model_name) -->
```rust
pub struct CartridgeRow {
    pub id: i64,
    pub code: String,           // C-NNNNNN
    pub model_id: i64,
    pub model_brand: Option<String>,
    pub model_name: Option<String>,
    // ... status_id, state_id, location, holder_name, notes, version, etc.
}
```

<!-- transition() — текущая полная логика, crates/trackly-app/src/services/request_service.rs, lines 392-520 -->
```rust
pub async fn transition(
    &self,
    payload: RequestTransitionPayload,
    caller: &Identity,
) -> Result<RequestDto, AppError> {
    authorize(caller, &Action::TransitionRequests)?;
    // ... reject_ad_register special-case ...

    let now = self.clock.unix_seconds();
    let user_id = caller.user_id;
    let request_repo = self.request_repo.clone();
    let audit_repo = self.audit_repo.clone();

    let (request_id, version, op, assigned_to, linked_cartridge_id) = match &payload {
        RequestTransitionPayload::Accept { request_id, version, assigned_to_user_id: _ } => (
            *request_id, *version, RequestTransitionOp::Accept, caller.user_id, None,
        ),
        RequestTransitionPayload::Reject { request_id, version, notes } => (
            *request_id, *version, RequestTransitionOp::Reject { notes: notes.clone() }, None, None,
        ),
        RequestTransitionPayload::Complete { request_id, version, notes, linked_cartridge_id } => (
            *request_id, *version,
            RequestTransitionOp::Complete {
                notes: notes.clone(),
                linked_cartridge_id: linked_cartridge_id.map(|id| id as i64),
            },
            None,
            linked_cartridge_id.map(|id| id as i64),
        ),
    };

    let new_status = op.target_status().to_string();

    // Carry the transition notes into the audit payload so the History
    // block (REQ-07) can show the reject/complete reason. Create/accept
    // have no notes → payload stays NULL.
    let notes_json: Option<String> = match &op {
        RequestTransitionOp::Reject { notes } => notes.clone(),
        RequestTransitionOp::Complete { notes, .. } => notes.clone(),
        RequestTransitionOp::Accept => None,
    }
    .map(|n| serde_json::json!({ "notes": n }).to_string());

    self.writer
        .execute(move |conn| {
            let tx = conn.transaction().map_err(map_rusqlite)?;
            request_repo.transition_in_tx(
                &tx, request_id, version, &op, assigned_to, linked_cartridge_id, now,
            )?;
            audit_repo.insert(&tx, AuditEntry {
                entity_type: "request", entity_id: request_id,
                action: op.audit_action(), user_id,
                before_json: None, after_json: None,
                payload_json: notes_json,
                created_at_utc: now,
            })?;
            tx.commit().map_err(map_rusqlite)?;
            Ok(())
        })
        .await?;

    let dto = self.get(request_id, caller).await?;
    let _ = self.ws_tx.send(WsEvent::RequestStatusChanged { /* ... */ });
    Ok(dto)
}
```

<!-- transition_in_tx — crates/trackly-infra/src/repos/requests_sqlite.rs, lines 115-184 — УЖЕ пишет
     completed_cartridge_id через COALESCE(?4, completed_cartridge_id) — D-06 на уровне SQL уже готов,
     этот план должен это ПОДТВЕРДИТЬ тестом, не переписывать SQL. -->
```rust
let affected = tx.execute(
    "UPDATE requests \
     SET status = ?1, resolution_notes = COALESCE(?2, resolution_notes), \
         assigned_to_user_id = COALESCE(?3, assigned_to_user_id), \
         completed_cartridge_id = COALESCE(?4, completed_cartridge_id), \
         updated_at_utc = ?5, version = version + 1 \
     WHERE id = ?6 AND version = ?7 AND deleted_at_utc IS NULL",
    params![new_status, notes, assigned_to, linked_cartridge_id, now_utc, request_id, version],
).map_err(map_rusqlite)?;
```

<!-- get_history() JSON-parse — crates/trackly-app/src/services/request_service.rs, lines 176-213 -->
```rust
pub async fn get_history(&self, request_id: i64, caller: &Identity) -> Result<Vec<RequestHistoryEntryDto>, AppError> {
    let _ = self.get(request_id, caller).await?; // BOLA closure (D-REQ-01)
    // ...
    Ok(rows.into_iter().map(|r| RequestHistoryEntryDto {
        id: r.id, action: r.action, created_at_utc: r.created_at_utc, actor_name: r.actor_name,
        notes: r.payload_json.as_deref().and_then(|p| {
            serde_json::from_str::<serde_json::Value>(p).ok()
                .and_then(|v| v.get("notes").and_then(|n| n.as_str()).map(|s| s.to_string()))
        }),
    }).collect())
}
```

<!-- test_req_cart_link текущий stub — crates/trackly-app/tests/phase06_stubs.rs, lines 575-578 -->
```rust
/// REQ-05: Complete{linked_cartridge_id} записывает completed_cartridge_id
#[test]
#[ignore]
fn test_req_cart_link() {}
```

<!-- test_request_lifecycle — ПОЛНЫЙ эталонный паттерн seed user → RequestService::new → create → transition,
     crates/trackly-app/tests/phase06_stubs.rs, lines 241-343 (читать файл напрямую для копирования целиком) -->

<!-- role_endpoint_matrix.rs — Case 13/14 эталон (lines 608-641), payload-фикстуры (lines 273-279, 330-339),
     manager_request/employee_request seed-паттерн (lines 735-776), helper post_with_cookie (lines 116-137) -->
```rust
// Case 13: Employee → cartridges_list → 403 (готовый эталон для cartridges_transition)
let status = post_with_cookie(new_app!(), "/api/v1/cartridges_list", cartridges_list_payload.clone(), Some(&employee_cookie)).await;
assert_eq!(status, StatusCode::FORBIDDEN, "...");

// cartridge_payload фикстура (snake_case, нет rename_all на CartridgeTransitionPayload/CartridgeCreateDto):
let cartridge_payload = json!({ "payload": { "model_id": 1, "location": null, "notes": null } });

// Seed employee/manager-owned request (для requests_transition employee-deny нужен СУЩЕСТВУЮЩИЙ request id):
let employee_request = ctx.requests.create(RequestCreateDto {
    request_type: "free_form".to_string(), printer_device_id: None,
    cartridge_model_id: None, category_id: None,
    description: Some("employee-owned request".to_string()),
}, &employee_identity).await.expect("create employee-owned request");
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: transition() — cartridge_repo wiring + снапшот истории (D-06/D-07)</name>
  <files>crates/trackly-app/src/services/request_service.rs, crates/trackly-app/tests/phase06_stubs.rs</files>
  <read_first>
    crates/trackly-app/src/services/request_service.rs lines 1-59 (импорты + struct RequestService + new());
    crates/trackly-app/src/services/request_service.rs lines 176-213 (get_history parse);
    crates/trackly-app/src/services/request_service.rs lines 392-520 (transition() полностью);
    crates/trackly-app/tests/phase06_stubs.rs lines 241-343 (test_request_lifecycle — эталон для копирования сетапа);
    crates/trackly-app/tests/phase06_stubs.rs lines 570-580 (текущий ignored stub test_req_cart_link)
  </read_first>
  <behavior>
    - Test (test_req_cart_link, реализация вместо #[ignore]): seed admin user (по образцу test_request_lifecycle) → создать заявку `cartridge_replace` с `cartridge_model_id` → `Accept` → `Complete{linked_cartridge_id: Some(known_id)}` (картридж создаётся напрямую через `CartridgeService` на том же `writer`/`readers`, либо через прямой `writer.execute` INSERT в таблицу `cartridges` с известным `id`/`code`/`model_id`, если поднимать полный `CartridgeService` избыточно) → `assert_eq!(completed.completed_cartridge_id, Some(known_id))`.
    - Test (history_shows_cartridge_snapshot_after_complete): после того же Complete с linked_cartridge_id вызвать `svc.get_history(request_id, &caller)`, найти запись с `action` соответствующим complete (`"custom:complete"` или `"complete"` — сверить с `op.audit_action()`), убедиться что `entry.notes` содержит код картриджа (например, через `.contains("C-")` или точное сравнение с заранее известным сидированным кодом).
    - Test (history_complete_without_cartridge_keeps_plain_notes): Complete с `linked_cartridge_id: None, notes: Some("текст")` → история показывает `"текст"` без обогащения (regression на существующее поведение reject/complete без картриджа).
  </behavior>
  <action>
    В `crates/trackly-app/src/services/request_service.rs` добавить третье repo-поле в `RequestService`: `pub(crate) cartridge_repo: Arc<trackly_infra::repos::cartridges_sqlite::SqliteCartridgeRepository>,` (по образцу `request_repo`/`audit_repo` — zero-sized struct, инициализируется внутри `new()` как `Arc::new(SqliteCartridgeRepository)`, БЕЗ изменения параметров `RequestService::new(...)` — вызывающий код в `context.rs`/`http/health.rs`/`tauri_cmds/health.rs` не меняется). Добавить `use trackly_core::ports::cartridges::CartridgeRepository;` и `use trackly_infra::repos::cartridges_sqlite::SqliteCartridgeRepository;` в импорты.

    В `transition()`: ПЕРЕД блоком `self.writer.execute(...)`, если `op` это `RequestTransitionOp::Complete` и `linked_cartridge_id.is_some()`, синхронно прочитать картридж через `self.cartridge_repo.get(&conn, id)` внутри `tokio::task::spawn_blocking` (используя `self.readers.clone().acquire()` — тот же паттерн, что в `get()`/`get_history()`), получить `CartridgeRow { code, model_brand, model_name, .. }`. Построить читаемую строку вида `format!("Установлен {} ({})", code, model_label)` где `model_label` — `format!("{} {}", brand.unwrap_or_default(), name.unwrap_or_default()).trim()` (NULL-safe, копируя established `Option<String>` паттерн — не `unwrap()`/`expect()` на joined-полях). Расширить существующий `notes_json` так, чтобы при наличии cartridge-снапшота итоговый `notes` JSON-ключ содержал объединённый текст: если `notes` (из payload) присутствует — `"{notes}; {cartridge_line}"`, если `notes` отсутствует — просто `cartridge_line`. Сохранить текущую форму `serde_json::json!({ "notes": <итоговая_строка> })` — НЕ добавлять новые JSON-ключи и НЕ менять `RequestHistoryEntryDto`/`get_history()` (D-07 реализуется через расширение значения существующего `notes` ключа, по рекомендации RESEARCH.md Open Question 2 — это означает `get_history()` не требует изменений вообще).

    Важно: чтение картриджа должно произойти ДО входа в `self.writer.execute(move |conn| {...})` (потому что внутри замыкания нет доступа к async `spawn_blocking` читателю — `writer.execute` уже работает в своём блокирующем контексте с `rusqlite::Connection`, а `cartridge_repo.get` ожидает `&Connection` читателя). Прочитать картридж заранее, передать готовую строку как `String`/`Option<String>` внутрь `move`-замыкания вместе с остальными захватываемыми переменными.

    В `crates/trackly-app/tests/phase06_stubs.rs` заменить `#[test] #[ignore] fn test_req_cart_link() {}` (lines 575-578) на полноценный `#[tokio::test(flavor = "multi_thread", worker_threads = 2)] async fn test_req_cart_link() { ... }`, скопировав структуру `test_request_lifecycle` (seed user через `writer.execute` INSERT, `Identity { user_id: Some(1), role: Role::Admin }`, `RequestService::new(writer.clone(), readers.clone(), clock.clone(), ws_tx)`). Для картриджа — либо завести его через прямой `writer.execute(move |conn| conn.execute("INSERT INTO cartridges (...) VALUES (...)", params![...]))` с известным `id`, либо (предпочтительно для реалистичности) поднять `CartridgeService::new(writer.clone(), readers.clone(), clock.clone())` параллельно на том же `writer`/`readers` и вызвать `cartridge_svc.create(...)` — оба сервиса делят один SQLite файл через `test_writer_and_readers()`, это безопасно (single-writer паттерн не нарушается, т.к. оба идут через один `WriterHandle`). Добавить два соседних теста (history_shows_cartridge_snapshot_after_complete, history_complete_without_cartridge_keeps_plain_notes) той же структуры.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test phase06_stubs cart_link -- --test-threads=1 && cargo test -p trackly-app --test phase06_stubs history_ -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    `cargo build -p trackly-app` проходит;
    `test_req_cart_link` больше не несёт `#[ignore]` и является `async fn` с `#[tokio::test]`;
    3 теста (test_req_cart_link + 2 history) зелёные;
    `test_request_lifecycle` и прочие тесты `phase06_stubs.rs` остаются зелёными (regression);
    `grep -n "cartridge_repo" crates/trackly-app/src/services/request_service.rs` находит новое поле и его использование в `transition()`.
  </acceptance_criteria>
  <done>RequestService несёт cartridge_repo; Complete{linked_cartridge_id: Some(id)} обогащает audit-снапшот историей в формате "Установлен C-NNNNNN (Brand Model)"; completed_cartridge_id корректно персистится (подтверждено тестом, SQL уже был готов); 3 новых теста зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: RBAC Case 20/21 — employee 403 на cartridges_transition/requests_transition + bindings regen</name>
  <files>crates/trackly-app/tests/role_endpoint_matrix.rs, ui/src/bindings.ts, ui/src/bindings-phase6.ts</files>
  <read_first>
    crates/trackly-app/tests/role_endpoint_matrix.rs lines 1-50 (doc-комментарий с нумерацией кейсов — новые кейсы продолжают нумерацию);
    crates/trackly-app/tests/role_endpoint_matrix.rs lines 273-279 (cartridge_payload фикстура, snake_case);
    crates/trackly-app/tests/role_endpoint_matrix.rs lines 608-641 (Case 13/14 — прямой эталон структуры assert);
    crates/trackly-app/tests/role_endpoint_matrix.rs lines 735-776 (employee_request/manager_request seed через ctx.requests.create — эталон для seed картриджа аналогично через ctx.cartridges)
  </read_first>
  <action>
    В `crates/trackly-app/tests/role_endpoint_matrix.rs` добавить два новых блока Case (продолжая нумерацию после существующего максимума — сверить актуальный последний номер в файле через `grep -n "Case 2[0-9]:" crates/trackly-app/tests/role_endpoint_matrix.rs` перед вставкой, т.к. Cases 20-24 уже зарезервированы предыдущим планом 10-03 — новые кейсы должны получить следующие свободные номера, например 25/26, и обновить doc-комментарий в начале файла соответствующими строками `//! 25. ...`/`//! 26. ...`).

    Case A (Employee → cartridges_transition → 403): использовать существующую фикстуру `cartridge_payload` (line 273) КАК ОСНОВУ, но это `transition`, не `create` — построить новый payload-литерал `cartridges_transition_payload` в формате `json!({ "payload": { "op": "install", "cartridge_id": 1, "version": 1, "date_utc": 1_700_000_000, "given_by_name": "Тест", "given_to_name": "Тест2", "location": "Каб. 1" } })` (snake_case — `CartridgeTransitionPayload` не несёт `rename_all`, подтверждено в dto/cartridge.rs). `cartridge_id: 1` не обязан существовать как реальная запись — RBAC-гейт (`authorize`) отрабатывает РАНЬШЕ чтения из БД, поэтому 403 придёт до попытки найти cartridge_id=1 (по аналогии с Case 5 `cartridges_create`, которая тоже не проверяет существование `model_id: 1`). POST на `/api/v1/cartridges_transition` с `employee_cookie` → `assert_eq!(status, StatusCode::FORBIDDEN, "Case N: Employee → cartridges_transition → expected 403, got {status}")`.

    Case B (Employee → requests_transition на СВОЮ же заявку → 403): в отличие от Case A, здесь нужна реально существующая заявка, т.к. `RequestService::transition()` сначала проверяет `authorize(caller, &Action::TransitionRequests)` — этот гейт срабатывает раньше любого владения, поэтому даже на собственной заявке Employee должен получить 403 (TransitionRequests — Admin|Manager only, независимо от ownership; ReadRequests/CreateRequest — отдельные Action для own-resource). Использовать уже существующий `employee_request` (создан в фикстурах Cases 20-24, lines 747-760) — построить `json!({ "payload": { "op": "accept", "requestId": employee_request.id, "version": employee_request.version, "assignedToUserId": null } })` (camelCase — `RequestTransitionPayload` несёт per-variant `#[serde(rename_all = "camelCase")]`, подтверждено в dto/request.rs). POST на `/api/v1/requests_transition` с `employee_cookie` → `assert_eq!(status, StatusCode::FORBIDDEN, "Case N: Employee → requests_transition (even on own request) → expected 403, got {status}")`.

    Вставить оба новых Case-блока после последнего существующего Case в файле (перед закрывающим `}).await.expect(...)` теста `role_endpoint_matrix_test`), используя тот же `new_app!()` макрос и `post_with_cookie` helper, без изменения сигнатур существующих функций.

    После изменения DTO в Plan 12-01/12-02 (CartridgeFilter.installable_only, RequestDto.printer_location) регенерировать `ui/src/bindings.ts` и `ui/src/bindings-phase6.ts` командой `cargo test -p trackly-app export_bindings -- --ignored` (или эквивалентный существующий проектный механизм генерации — найти точную команду через `grep -rn "specta::export\|Typescript::default" crates/trackly-app/` если она отличается) — это стандартный шаг проекта при изменении DTO, не специфичный для этой фазы. Если автогенерация недоступна без реального запуска (например, требует `#[test]` с `#[ignore]` атрибутом), запустить именно этот тест явно.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test role_endpoint_matrix role_endpoint_matrix_test -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    Новые 2 Case-блока присутствуют в файле с уникальными номерами (не дублируют 20-24);
    `cargo test -p trackly-app --test role_endpoint_matrix` зелёный целиком (включая все существующие 24 кейса — regression);
    `grep -n "cartridges_transition\|requests_transition" crates/trackly-app/tests/role_endpoint_matrix.rs` находит новые POST-запросы к обоим путям;
    `ui/src/bindings.ts` содержит `installable_only` в типе `CartridgeFilter`, `ui/src/bindings-phase6.ts` содержит `printerLocation` в типе `RequestDto` (`grep -n "installable_only" ui/src/bindings.ts && grep -n "printerLocation" ui/src/bindings-phase6.ts`).
  </acceptance_criteria>
  <done>role_endpoint_matrix_test покрывает employee-deny для cartridges_transition и requests_transition (T-12-01 закрыт); bindings*.ts регенерированы и несут новые поля DTO из Wave 1/2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Frontend → requests_transition (Complete) | Клиент передаёт `linked_cartridge_id` — сервер должен проверить через RBAC (`TransitionRequests`, Admin\|Manager only), а не доверять тому, что фронт уже это отфильтровал. |
| Frontend → cartridges_transition (Install) | То же — Employee не должен достигать install ни через UI, ни через прямой curl. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-01 | Elevation of Privilege | `cartridges_transition` / `requests_transition` HTTP-эндпоинты | mitigate | Подтверждено существующим `authorize(caller, &Action::MutateCartridges \| &Action::TransitionRequests)` внутри сервисного слоя (не менялось этим планом) — закрыт ПРОБЕЛ В ТЕСТОВОМ ПОКРЫТИИ через новые Case A/B в `role_endpoint_matrix.rs`, не новый код авторизации. |
| T-12-03 | Tampering | `transition()` cartridge-снапшот в notes_json читает `linked_cartridge_id` от клиента без проверки, что картридж реально только что установлен этим же запросом | accept | `linked_cartridge_id` указывает на реальный существующий картридж (если id не существует, `cartridge_repo.get` вернёт `AppError::NotFound` и вся транзакция упадёт — нет риска инъекции произвольного текста, только authentic существующих данных картриджа попадает в снапшот). Возможность указать ЧУЖОЙ (не только что установленный) cartridge_id в историю — известный design trade-off: тот же `TransitionRequests` Action уже ограничивает вызов до Admin\|Manager, которые имеют легитимный доступ ко всем картриджам через `Action::ReadData`, поэтому это не privilege escalation, а в худшем случае неточная запись в истории при ошибке оператора — не security risk. |
</threat_model>

<verification>
- `cargo build --workspace` — компилируется.
- `cargo test -p trackly-app --test phase06_stubs` — все тесты зелёные (включая 3 новых: test_req_cart_link + 2 history).
- `cargo test -p trackly-app --test role_endpoint_matrix` — весь файл зелёный (24 существующих + 2 новых кейса).
- `cargo clippy --workspace -- -D warnings` — чисто.
- `grep -n "installable_only" ui/src/bindings.ts` и `grep -n "printerLocation" ui/src/bindings-phase6.ts` — оба находят регенерированные поля.
</verification>

<success_criteria>
`RequestService::transition()` обогащает историю заявки человекочитаемым текстом об установленном картридже при Complete с linked_cartridge_id (D-07); completed_cartridge_id персистентность подтверждена тестом (D-06). RBAC-пробел T-12-01 закрыт двумя новыми Case в существующей матрице. Bindings регенерированы и несут поля из Wave 1.
</success_criteria>

<output>
Create `.planning/phases/12-cartridge-request-interconnection/12-02-SUMMARY.md` when done
</output>
