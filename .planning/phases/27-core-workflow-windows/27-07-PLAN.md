---
phase: 27-core-workflow-windows
plan: 07
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - ui/src/features/printers/PrintersPage.svelte
  - ui/src/features/printers/PrintersMasterDetail.svelte
  - ui/src/features/printers/PrintersSearchAndTabs.svelte
  - ui/src/features/printers/PrintersList.svelte
  - ui/src/features/printers/PrinterListRow.svelte
  - ui/src/features/printers/PrinterDetail.svelte
autonomous: true
requirements: [WIN-05]
must_haves:
  truths:
    - "D-02: обе панели PrintersMasterDetail на `--tr-surface-raised` + border + `box-shadow var(--tr-elev-1)` — регресс D-13 для окна Принтеров закрыт"
    - "D-05: PrintersSearchAndTabs использует примитив `Tabs` вместо самописного tablist; переключение вкладок и счётчики работают как раньше"
    - "D-03: PrintersList/PrinterListRow переведены на `Table`/`TableRow` со своими колонками (имя/IP tabular-nums/статус-Badge/TonerGauge инлайном)"
    - "D-01: PrinterDetail (603 стр.) потребляет общий `DetailPanel`/`DetailSection`/`DetailField`; все секции (counter-row/compat-agg/meta/readings) сохранены, async-загрузка readings/aggregates не тронута (SC #4)"
    - "SC #1/D-04: PrintersPage потребляет примитив `PageHeader` (title + `{#snippet actions}`) вместо bespoke `.page-header`/`.page-title`/`.header-actions` — остаток старого класса на уровне оболочки снят; действия шапки сохранены (SC #4)"
    - "SC #4: все поля/действия/агрегаты совместимости окна Принтеров на месте — изменение чисто визуальное"
  artifacts:
    - path: "ui/src/features/printers/PrintersList.svelte"
      provides: "Список принтеров на Table/TableRow"
      contains: "Table"
    - path: "ui/src/features/printers/PrinterDetail.svelte"
      provides: "Деталь принтера на DetailPanel"
      contains: "DetailPanel"
  key_links:
    - from: "ui/src/features/printers/PrinterDetail.svelte"
      to: "ui/src/lib/components/DetailPanel.svelte"
      via: "импорт общего паттерна детали (создан 27-01)"
      pattern: "import DetailPanel"
    - from: "ui/src/features/printers/PrinterListRow.svelte"
      to: "ui/src/features/printers/TonerGauge.svelte"
      via: "TonerGauge инлайном в тонер-ячейке строки"
      pattern: "TonerGauge"
---

<objective>
Перевести структурный слой окна **Принтеров (WIN-05)** на дизайн-систему: поверхности master-detail
(D-02), табы фильтра (D-05), список (D-03) и деталь (D-01, крупнейший bespoke — 603 стр. с async-
readings и агрегатами совместимости). Макета нет — форма из компонентной системы Фаз 23–25 и
прецедента Устройства. **Поля/действия/агрегаты/workflow не меняются (SC #4).**

Purpose: закрыть SC #3 (Принтеры повсеместно на новых токенах/компонентах) для структурного слоя и
регресс D-13 для окна Принтеров.
Output: 6 файлов на `PageHeader`/`Tabs`/`Table`/`TableRow`/`DetailPanel`, без bespoke-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@.planning/phases/27-core-workflow-windows/27-01-SUMMARY.md
@ui/src/features/devices/DeviceFilters.svelte
@ui/src/features/devices/DeviceList.svelte
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrintersPage header→PageHeader + PrintersMasterDetail (D-02) + PrintersSearchAndTabs (D-05)</name>
  <read_first>
    - ui/src/features/printers/PrintersPage.svelte (bespoke `<header class="page-header"><h1 class="page-title">Принтеры</h1>` строки 129–132 + scoped CSS `.page-header`/`.page-title` 211–222; НЕ импортирует PageHeader)
    - ui/src/features/devices/DevicesPage.svelte (эталон Фазы 26: `<PageHeader title=…>{#snippet actions()}…{/snippet}`)
    - ui/src/lib/components/PageHeader.svelte (контракт: `title` + `actions?: Snippet`)
    - ui/src/features/printers/PrintersMasterDetail.svelte (идентичен ActsMasterDetail; master `--tr-surface`, detail `--tr-bg`; grid 35/65 + `<1100px` fallback сохранить)
    - ui/src/features/printers/PrintersSearchAndTabs.svelte (уже `role="tablist"` строка 68, TABS с tab.key `{#each TABS as tab (String(tab.key))}` строка 69 — ближе всего к underline-семантике Tabs)
    - ui/src/features/devices/DeviceFilters.svelte (эталон Tabs + адаптер)
    - ui/src/lib/components/Tabs.svelte, ui/src/styles/_tokens.scss (сверить --tr-surface-raised/--tr-elev-1)
  </read_first>
  <action>
    (Оболочка, D-01/D-04 «форма из системы») В `PrintersPage.svelte` заменить bespoke `<header
    class="page-header">…</header>` (129–132) на `<PageHeader title="Принтеры">{#snippet actions()}`
    <существующие кнопки-действия шапки>`{/snippet}</PageHeader>` по эталону `DevicesPage`; импортировать
    `PageHeader`. Удалить scoped CSS `.page-header`/`.page-title`/`.header-actions` (211–222). Все
    действия шапки сохранить (SC #4; гамбургер nav-toggle приходит из PageHeader).
    (D-02) `PrintersMasterDetail.svelte`: обе панели → `background: var(--tr-surface-raised);
    border: 1px solid var(--tr-border); border-radius: var(--tr-radius-md); box-shadow:
    var(--tr-elev-1);`. Grid 35/65 и `<1100px` fallback сохранить. Убрать detail `--tr-bg`.
    (D-05) `PrintersSearchAndTabs.svelte`: заменить самописный tablist на `<Tabs variant="underline"
    tabs={tabItems} active={String(active)} onchange={...} />`. Адаптер: `tabItems = TABS.map((t) =>
    ({ key: String(t.key), label, count }))`, count встроен в Tabs (убрать отдельные счётчики). Удалить
    bespoke `.tab`/tablist CSS. Поиск/debounce/переключение — логику не менять (D-05 контракт). `Input`
    оставить.
  </action>
  <acceptance_criteria>
    - `PrintersPage.svelte` содержит `import PageHeader` И `<PageHeader` И `grep -c "page-header" PrintersPage.svelte` == 0 (bespoke шапка снята)
    - `grep -c "var(--tr-surface-raised)" PrintersMasterDetail.svelte` == 2 И `grep -c "grid-template-columns: 35% 65%" PrintersMasterDetail.svelte` == 1
    - `PrintersSearchAndTabs.svelte` содержит `<Tabs` И `grep -c 'class="tab"' PrintersSearchAndTabs.svelte` == 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check (обе темы): панели раздельны (raised+elev), вкладки/счётчики фильтра работают как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Принтеры в обеих темах: master/detail всплывают; табы фильтра и счётчики неизменны</human-check>
  </verify>
  <done>MasterDetail на raised+elev-1 (grid цел); SearchAndTabs на Tabs без bespoke tablist; логика фильтра сохранена.</done>
</task>

<task type="auto">
  <name>Task 2: PrintersList + PrinterListRow → Table/TableRow (D-03)</name>
  <read_first>
    - ui/src/features/printers/PrintersList.svelte, PrinterListRow.svelte (текущая структура строки — сохранить те же поля)
    - ui/src/features/printers/TonerGauge.svelte (инлайн-виджет тонера для ячейки — импортировать, API не менять)
    - ui/src/features/devices/DeviceList.svelte, DeviceListRow.svelte (эталон обёртки+строки+`.cell`; ловушка `:global(> td)`)
    - ui/src/lib/components/Table.svelte, TableRow.svelte, ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Перевести список принтеров на `Table`/`TableRow`. Bespoke обёртку/скроллер/футер/пустое состояние
    удалить — их роль забирает `Table`. Строки → `<TableRow selected={...}>`. Колонки (Claude's
    Discretion, поля не менять — SC #4): имя · IP (`font-variant-numeric: tabular-nums`) · статус-
    `Badge` · тонер (**`TonerGauge` инлайном в ячейке**). Select через проп `selected`; клик-на-строку
    на TableRow. `.cell`-truncate из DeviceListRow 84–91; для тонер-ячейки truncate не применять (виджет).
    **Ловушка `:global()`**: `.tr-row :global(> td)`. Table.svelte/TableRow.svelte НЕ менять; потребителей
    (Устройства/витрина/ActFormItemsTable) не ломать. TonerGauge.svelte НЕ менять (это делает 27-08). **Порядок волн:** TonerGauge модифицируется планом 27-08 (wave 1, полностью до wave 2); его публичный API не меняется, поэтому импорт здесь безопасен — wave-order гарантирует готовность.
  </action>
  <acceptance_criteria>
    - `PrintersList.svelte` содержит `<Table` и `import Table`; `PrinterListRow.svelte` содержит `<TableRow` и `TonerGauge`
    - `grep -c 'class="row"' PrinterListRow.svelte` == 0 (bespoke строка удалена)
    - Table.svelte/TableRow.svelte/TonerGauge.svelte не в git diff этого плана
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: список принтеров = таблица; имя/IP/статус-бейдж/тонер-gauge на месте; выбор строки подсвечивает
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Список принтеров: таблица с TonerGauge в ячейке; те же поля; выбор работает</human-check>
  </verify>
  <done>PrintersList/PrinterListRow на Table/TableRow со своими колонками (+TonerGauge инлайн); bespoke удалены; потребители не сломаны; поля сохранены.</done>
</task>

<task type="auto">
  <name>Task 3: PrinterDetail (603) → DetailPanel (D-01)</name>
  <read_first>
    - ui/src/features/printers/PrinterDetail.svelte (603 стр.; bespoke `.printer-detail`/`.detail-header`/`.detail-section`/`.counter-row`/`.compat-agg-row`/`.meta-row`/`.readings-list`/`.reading-row` строки 394–596; async-загрузка readings/aggregates `<script>` 38–120 — ДАННЫЕ, не трогать; tonerEntries 232, compatAggregates 279)
    - ui/src/lib/components/DetailPanel.svelte, DetailSection.svelte, DetailField.svelte (созданы 27-01)
    - ui/src/features/printers/TonerGauge.svelte (используется в шапке/секции — API не менять)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Переписать `PrinterDetail.svelte` на общий паттерн 27-01: empty → `<DetailPanel empty
    emptyTitle emptyBody>`; заполненное → `<DetailPanel title>{#snippet actions}` + тело из
    `<DetailSection>` для КАЖДОЙ существующей секции (характеристики/counter-row/агрегаты
    совместимости/meta/readings) и `<DetailField label value>` для полей. **Все секции и их набор
    полей сохранить дословно (SC #4)** — `.counter-row`/`.compat-agg-row`/`.meta-row`/`.reading-row`
    ре-токенизировать внутри соответствующих `DetailSection` (это специфичная разметка секций — не
    удалять содержимое, только переодеть). Async-загрузку readings/aggregates (`<script>` 38–120) и
    TonerGauge НЕ трогать. Удалить bespoke `.detail-header`/`.detail-section`-контейнеры и внутренний
    фон детали (даёт D-02). Статус — через `Badge`.
  </action>
  <acceptance_criteria>
    - `PrinterDetail.svelte` содержит `import DetailPanel` и `<DetailPanel`
    - `grep -c "detail-header" PrinterDetail.svelte` == 0 (bespoke шапка удалена)
    - Секции сохранены: `grep -c "compatAggregates\|tonerEntries" PrinterDetail.svelte` >= 2 (агрегаты и тонер не удалены)
    - async `<script>`-логика readings/aggregates не изменена по сигнатуре (те же функции/переменные)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check (обе темы): деталь принтера — все секции (характеристики, счётчики, агрегаты совместимости, meta, readings), TonerGauge, действия на месте
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть принтер: все секции и агрегаты совместимости, readings подгружаются, TonerGauge и действия работают как раньше</human-check>
  </verify>
  <done>PrinterDetail на DetailPanel/DetailSection/DetailField; все секции/поля/агрегаты сохранены; async-данные и TonerGauge не тронуты (SC #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — чисто визуальная ре-токенизация окна Принтеров (SC #4). Async-загрузка
readings/aggregates (существующий поток данных SNMP) не изменяется.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Tampering (regression) | Принтеры: список/деталь/фильтр | mitigate | SC #4 scope-fence + svelte-check/check-tokens/lint + human-check обеих тем; Table/TableRow/TonerGauge не модифицируются |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов не ставим |

Угроз уровня `high` нет.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0; `pnpm --dir ui lint` код 0
- Потребители Table/TableRow не сломаны; TonerGauge не в диффе плана
</verification>

<success_criteria>
Структурный слой Принтеров (master-detail, фильтр, список, деталь) целиком на `Tabs`/`Table`/
`TableRow`/`DetailPanel` и токенах, без bespoke-классов; регресс D-13 закрыт; поля/действия/агрегаты/
async-readings сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-07-SUMMARY.md` when done
</output>
