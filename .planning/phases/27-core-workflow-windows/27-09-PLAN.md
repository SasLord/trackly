---
phase: 27-core-workflow-windows
plan: 09
type: execute
wave: 3
depends_on: [27-01, 27-02, 27-03, 27-04, 27-05, 27-06, 27-07, 27-08]
files_modified: []
autonomous: false
requirements: [WIN-03, WIN-04, WIN-05]
must_haves:
  truths:
    - "D-02: обязательный визуальный UAT обеих тем (светлая + тёмная) подтверждает, что master/detail-панели трёх окон читаются раздельно — в тёмной `--tr-surface-raised` светлее `--tr-surface` (всплывают), в светлой разделены рамкой/тенью (урок D-17 Фазы 26)"
    - "SC #1–#3: Акты, Картриджи, Принтеры используют новые токены/компоненты повсеместно — визуальная и сборочная проверка не находит остатков старых классов"
    - "SC #4: сквозная проверка подтверждает — каждое поле/действие/workflow трёх окон на месте и работает; изменение чисто визуальное"
  artifacts: []
  key_links: []
---

<objective>
Финальная сквозная проверка Фазы 27: полная сборка (`pnpm --dir ui build`) + **обязательный
визуальный UAT обеих тем** по трём окнам (Акты/Картриджи/Принтеры). Это последняя волна — она
закрывает мандат D-02 («обязательный визуальный UAT в обеих темах») и якорь SC #4 (чисто визуально,
ничего не сломано). Фаза не имеет фронтенд-тестов; визуал проверяется глазами (D-18 Фазы 26).

Purpose: подтвердить SC #1–#4 до закрытия фазы.
Output: пройденный build-гейт + подтверждённый человеком both-theme UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-CONTEXT.md
@.planning/phases/27-core-workflow-windows/27-01-SUMMARY.md
@.planning/phases/27-core-workflow-windows/27-02-SUMMARY.md
@.planning/phases/27-core-workflow-windows/27-04-SUMMARY.md
@.planning/phases/27-core-workflow-windows/27-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Полная сборка + токен/тип/lint гейты</name>
  <read_first>
    - .planning/phases/27-core-workflow-windows/27-CONTEXT.md (гоча: перед LAN-браузер-проверкой нужен `pnpm --dir ui build` — серверный режим отдаёт ui/dist; prebuild тянет cargo test export_bindings)
  </read_first>
  <action>
    Прогнать финальные гейты Фазы 27 из корня репозитория:
    `node ui/scripts/check-tokens.mjs`, `pnpm --dir ui svelte-check`, `pnpm --dir ui lint`,
    `pnpm --dir ui build` (build тянет prebuild `cargo test -p trackly-app --test export_bindings`;
    учесть память проекта — один `cargo test` за раз). Собранный `ui/dist` нужен для LAN-браузер-
    проверки в Task 2. Любой ненулевой код — устранить до чекпоинта.
  </action>
  <acceptance_criteria>
    - `node ui/scripts/check-tokens.mjs` код 0
    - `pnpm --dir ui svelte-check` код 0
    - `pnpm --dir ui lint` код 0
    - `pnpm --dir ui build` код 0 (свежий ui/dist собран)
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check && pnpm --dir ui lint && pnpm --dir ui build</automated>
  </verify>
  <done>Все автоматические гейты зелёные; свежий ui/dist собран для LAN-проверки.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Both-theme визуальный UAT по трём окнам (D-01/D-02/D-03/D-05, SC #4)</name>
  <action>Провести блокирующую человеческую проверку обеих тем по Актам/Картриджам/Принтерам согласно how-to-verify ниже; не авто-одобрять (gate=blocking).</action>
  <what-built>
    Три транзакционных окна (Акты WIN-03, Картриджи WIN-04, Принтеры WIN-05) переведены на дизайн-
    систему Фаз 23–25: master-detail на `--tr-surface-raised`+`--tr-elev-1` (D-02, закрытие регресса
    D-13), фильтры на `Tabs` (D-05), списки на `Table`/`TableRow` (D-03), детали на общем `DetailPanel`
    (D-01), модалки/виджеты ре-токенизированы (D-04). Поля/действия/workflow не менялись (SC #4).
  </what-built>
  <how-to-verify>
    Проверить в ОБЕИХ темах (переключатель темы) — desktop (`cargo tauri dev`) и, при желании, LAN-
    браузер (свежий `ui/dist` собран в Task 1).
    1. **D-02 (обе темы, критично):** открыть Акты, Картриджи, Принтеры. В ТЁМНОЙ теме master и detail
       должны «всплывать» над контент-фоном (raised светлее surface); в СВЕТЛОЙ — разделяться рамкой и
       тенью (raised==surface, разделение несут border+elev). Нет слияния панелей с фоном.
    2. **D-05:** во всех трёх окнах вкладки фильтра переключаются, счётчики совпадают с прежними, поиск
       (debounce) работает.
    3. **D-03:** списки Актов/Картриджей/Моделей/Принтеров — таблицы; выбор строки подсвечивает её
       (accent-бордер); пустое/загрузочное состояние из `Table`; тонер-gauge в ячейке принтеров.
       Регресс не затронул Устройства, витрину, ActFormItemsTable.
    4. **D-01:** детали трёх окон показывают те же поля/секции/действия, что до фазы; история/агрегаты/
       readings на месте.
    5. **D-04 / SC #4:** пройти ключевые действия — создать/отредактировать акт, возврат по акту, печать;
       операцию жизненного цикла картриджа, создание модели; создание принтера, SNMP-discovery с выбором
       результатов. Всё работает как раньше.
  </how-to-verify>
  <resume-signal>Напишите «approved» либо перечислите визуальные/функциональные проблемы по окнам/темам</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — это проверочная волна. Подтверждает, что визуальная ре-токенизация Фазы 27
не внесла регрессов поведения (SC #4).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09 | Tampering (regression) | три окна целиком | mitigate | Полный build-гейт + блокирующий human-verify обеих тем по всем трём окнам и ключевым действиям |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов в фазе не устанавливалось |

Угроз уровня `high` фаза не вносит; чекпоинт не авто-одобряется (gate=blocking).
</threat_model>

<verification>
- Все автоматические гейты (check-tokens, svelte-check, lint, build) код 0
- Человек подтвердил both-theme UAT по Актам/Картриджам/Принтерам (D-01/D-02/D-03/D-05) и ключевые действия (D-04/SC #4)
</verification>

<success_criteria>
Build зелёный; человек подтвердил, что три окна повсеместно на новой дизайн-системе (SC #1–#3),
master/detail читаются в обеих темах (D-02), и все поля/действия/workflow сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-09-SUMMARY.md` when done
</output>
