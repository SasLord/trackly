---
phase: 27-core-workflow-windows
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - ui/src/features/acts/ActsPage.svelte
  - ui/src/features/acts/ActsMasterDetail.svelte
  - ui/src/features/acts/ActsSearchAndTabs.svelte
  - ui/src/features/acts/ActsList.svelte
  - ui/src/features/acts/ActListRow.svelte
  - ui/src/features/acts/ActDetail.svelte
  - ui/src/features/acts/ActHeaderField.svelte
autonomous: true
requirements: [WIN-03]
must_haves:
  truths:
    - "D-02: обе панели ActsMasterDetail на `--tr-surface-raised` + border + `box-shadow var(--tr-elev-1)` — принятый регресс D-13 Фазы 26 для окна Актов закрыт; master больше не сливается с контент-фоном --tr-surface"
    - "D-05: ActsSearchAndTabs использует примитив `Tabs variant=\"underline\"` со встроенным count вместо самописных `<button class=\"tab\">`; поведение фильтра (debounce 250мс, счётчики, переключение вкладок) не изменилось"
    - "D-03: ActsList/ActListRow переведены на семантику `<table>` через примитивы `Table`/`TableRow` со своими колонками (№/дата/получатель/позиций); bespoke `.pagination`/`.loading`/`.empty`/`.row` удалены"
    - "D-01: ActDetail потребляет общий `DetailPanel`/`DetailSection`/`DetailField`; bespoke `.detail-header`/`.section`/field-классы удалены; отображаемые поля не изменились (SC #4)"
    - "SC #1/D-04: ActsPage потребляет примитив `PageHeader` (title + `{#snippet actions}`) вместо bespoke `.page-header`/`.page-title`/`.header-actions` — последний остаток старого класса на уровне оболочки снят; кнопки-действия шапки сохранены (SC #4)"
    - "SC #4: все существующие поля/действия окна Актов на месте и работают — изменение чисто визуальное"
  artifacts:
    - path: "ui/src/features/acts/ActsList.svelte"
      provides: "Список актов на Table/TableRow"
      contains: "Table"
    - path: "ui/src/features/acts/ActDetail.svelte"
      provides: "Деталь акта на DetailPanel"
      contains: "DetailPanel"
  key_links:
    - from: "ui/src/features/acts/ActsList.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "импорт и потребление Table + TableRow"
      pattern: "import Table"
    - from: "ui/src/features/acts/ActDetail.svelte"
      to: "ui/src/lib/components/DetailPanel.svelte"
      via: "импорт общего паттерна детали (создан 27-01)"
      pattern: "import DetailPanel"
---

<objective>
Перевести структурный слой окна **Актов (WIN-03)** на дизайн-систему: поверхности master-detail
(D-02), табы фильтра (D-05), список (D-03) и деталь (D-01). Транзакционное окно, макета нет — форма
выводится из компонентной системы Фаз 23–25 и прецедента окна Устройства. **Ни одно поле, действие
или workflow не меняется (SC #4)** — только токены/примитивы.

Purpose: закрыть SC #1 (Акты повсеместно на новых токенах/компонентах) для структурного слоя и
закрыть регресс D-13 Фазы 26 для окна Актов.
Output: 7 файлов Актов на примитивах `PageHeader`/`Tabs`/`Table`/`TableRow`/`DetailPanel`, без bespoke-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@.planning/phases/27-core-workflow-windows/27-01-SUMMARY.md
@ui/src/features/devices/DeviceFilters.svelte
@ui/src/features/devices/DeviceList.svelte
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActsPage header→PageHeader + ActsMasterDetail (D-02) + ActsSearchAndTabs (D-05)</name>
  <read_first>
    - ui/src/features/acts/ActsPage.svelte (bespoke `<header class="page-header"><h1 class="page-title">Акты</h1><div class="header-actions"><Button…>` строки 253–256 + scoped CSS `.page-header`/`.page-title`/`.header-actions` 347–364; НЕ импортирует PageHeader)
    - ui/src/features/devices/DevicesPage.svelte (эталон Фазы 26: `<PageHeader title="Устройства">{#snippet actions()}…{/snippet}` строки 231–247)
    - ui/src/lib/components/PageHeader.svelte (контракт: `title` + `actions?: Snippet`; гамбургер nav-toggle встроен)
    - ui/src/features/acts/ActsMasterDetail.svelte (текущие поверхности: master `--tr-surface`, detail `--tr-bg`, строки 31–45; grid 35/65 и `<1100px` fallback сохранить дословно)
    - ui/src/features/acts/ActsSearchAndTabs.svelte (самописные `<button class="tab">` строки 54–72 + CSS 86–119; debounce 250мс + guard `document.activeElement?.id` строки 19–35; ключи `TabKey='handover'|'returns'|'archive'` 37–41)
    - ui/src/features/devices/DeviceFilters.svelte (эталон: Tabs variant="underline", tabItems-адаптер 61–100)
    - ui/src/lib/components/Tabs.svelte (контракт: string key, встроенный count, active $bindable)
    - ui/src/styles/_tokens.scss (сверить: --tr-surface-raised, --tr-elev-1, --tr-border, --tr-radius-md)
  </read_first>
  <action>
    (Оболочка, D-01/D-04 «форма из системы») В `ActsPage.svelte` заменить bespoke `<header
    class="page-header">…</header>` (253–256) на `<PageHeader title="Акты">{#snippet actions()}`
    <существующие кнопки, напр. `+ Создать акт`>`{/snippet}</PageHeader>` по эталону `DevicesPage`.
    Импортировать `PageHeader` из `$lib/components/PageHeader.svelte`. Удалить scoped CSS
    `.page-header`/`.page-title`/`.header-actions` (347–364). Все существующие действия шапки
    сохранить (SC #4 — ни одной кнопки не убирать; гамбургер nav-toggle приходит из PageHeader).
    (D-02) В `ActsMasterDetail.svelte` заменить фоны обеих панелей: `.master` и `.detail` →
    `background: var(--tr-surface-raised); border: 1px solid var(--tr-border);
    border-radius: var(--tr-radius-md); box-shadow: var(--tr-elev-1);`. Grid `35% / 65%` (строки
    23–29) и `<1100px` horizontal-scroll fallback (47–52) — **не трогать (SC #4)**. Убрать у detail
    прежний `background: var(--tr-bg)`.
    (D-05) В `ActsSearchAndTabs.svelte` заменить самописный таб-бар на `<Tabs variant="underline"
    tabs={tabItems} active={String(activeTab)} ariaLabel="Фильтр по актам" onchange={...} />`.
    Ключи уже строковые (`'handover'|'returns'|'archive'`) → адаптер тривиален:
    `tabItems = [{ key, label, count }]`, count из `ActsCountsDto`. Удалить bespoke `.tab`/`.tabs`
    CSS (86–119) и `<Badge>`-счётчики (count теперь встроен в Tabs). `<script>`-логику (debounce,
    guard, счётчики, переключение) НЕ менять — контракт D-05. `Input` оставить как есть.
    Если после D-02 внутренний фон списка/детали давал двойную заливку — согласовать (убрать
    внутренние `background: var(--tr-surface/-bg)` в ActsList/ActDetail, см. Task 2/3).
  </action>
  <acceptance_criteria>
    - `ActsPage.svelte` содержит `import PageHeader` И `<PageHeader` И `grep -c "page-header" ActsPage.svelte` == 0 (bespoke шапка снята)
    - `grep -c "var(--tr-surface-raised)" ActsMasterDetail.svelte` == 2 (обе панели)
    - `grep -c "var(--tr-elev-1)" ActsMasterDetail.svelte` >= 1 И `grep -c "grid-template-columns: 35% 65%" ActsMasterDetail.svelte` == 1 (grid сохранён)
    - `ActsSearchAndTabs.svelte` содержит `<Tabs` И `grep -c 'class="tab"' ActsSearchAndTabs.svelte` == 0
    - `grep -c "250" ActsSearchAndTabs.svelte` >= 1 (debounce сохранён)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check (обе темы): master и detail «всплывают» над контент-фоном в тёмной теме, в светлой разделены рамкой/тенью; вкладки фильтра переключаются, счётчики совпадают с прежними
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть Акты в обеих темах: панели раздельны (raised+elev), табы и счётчики работают как раньше</human-check>
  </verify>
  <done>MasterDetail на --tr-surface-raised+elev-1 (grid цел), SearchAndTabs на Tabs без bespoke .tab, поведение фильтра неизменно.</done>
</task>

<task type="auto">
  <name>Task 2: ActsList + ActListRow → Table/TableRow (D-03)</name>
  <read_first>
    - ui/src/features/acts/ActsList.svelte (bespoke `.rows` скроллер + `.pagination` футер 96–164; emptyConfig-логика 38–69 — сохранить, питает emptyTitle/emptyBody)
    - ui/src/features/acts/ActListRow.svelte (двухстрочный `<div class="row" role="button">` 52–76; select-состояние `.row.selected` 98–102; badge «В архиве» 65–69)
    - ui/src/features/devices/DeviceList.svelte (эталон обёртки: Table columns/head/footer/minWidth 62–120)
    - ui/src/features/devices/DeviceListRow.svelte (эталон строки: TableRow selected + `.cell` truncate 51–91; `:global(> td)`-ловушка 72–82)
    - ui/src/lib/components/Table.svelte, ui/src/lib/components/TableRow.svelte (контракты props)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Перевести список актов на `Table`/`TableRow` по эталону Устройств.
    `ActsList.svelte`: обернуть в `<Table columns={4} loading={skeletonLoading} empty={isEmpty}
    emptyTitle={...} emptyBody={...} head={tableHead} footer={footer} />`. `head`-snippet:
    `<th>№</th><th>Дата</th><th>Получатель</th><th class="th-count">Позиций</th>`. Удалить целиком
    bespoke `.rows`/`.loading`/`.empty`/`.pagination` (96–164) — их роль забирает `Table`.
    emptyConfig-логику (38–69) сохранить. minWidth для узкой master-панели НЕ задавать (плоский
    список, помещается) либо задать умеренное значение — на усмотрение, но не `860px` Устройств.
    `ActListRow.svelte`: `<TableRow selected={act.id === selectedActId} class="...">` со `<td class="cell
    cell-number"><span class="tr-mono">№{act.number}</span></td>`, `<td class="cell">{dateLabel}</td>`,
    `<td class="cell">{act.receiver_name}</td>`, `<td class="cell cell-count">{itemsCount}</td>`.
    Badge «В архиве» (при `activeTab==='archive'`) — как ячейку/в существующей колонке, БЕЗ добавления
    новых отображаемых полей (SC #4). Клик-на-строку (`role="button"`/`onclick`/`onkeydown`) перенести
    на TableRow; select через проп `selected`, не через `.row.selected`. `.cell`-truncate копировать
    из DeviceListRow 84–91. **Ловушка `:global()`:** CSS в caller-`<td>` писать формой
    `.tr-row :global(> td)`, не `:global(.tr-row > td)`.
    **Не ломать** существующих потребителей Table/TableRow (Устройства, витрина, ActFormItemsTable) —
    файлы Table.svelte/TableRow.svelte НЕ менять.
  </action>
  <acceptance_criteria>
    - `ActsList.svelte` содержит `<Table` И `import Table`
    - `ActListRow.svelte` содержит `<TableRow` И `import TableRow`
    - `grep -c 'class="pagination"' ActsList.svelte` == 0 И `grep -c 'class="row"' ActListRow.svelte` == 0 (bespoke удалены)
    - Table.svelte и TableRow.svelte НЕ в диффе (git diff --name-only не содержит их)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: список актов рендерится табличной строкой, выбор строки подсвечивает её (accent-бордер), пустое/загрузочное состояние отдаёт Table
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Список актов = таблица; клик выделяет строку; badge «В архиве» на месте; те же поля, что раньше</human-check>
  </verify>
  <done>ActsList/ActListRow на Table/TableRow со своими колонками; bespoke .pagination/.row удалены; потребители Table не сломаны; поля не изменились.</done>
</task>

<task type="auto">
  <name>Task 3: ActDetail + ActHeaderField → DetailPanel (D-01)</name>
  <read_first>
    - ui/src/features/acts/ActDetail.svelte (bespoke `.act-detail`/`.detail-header`/`.section`/`.header-grid`/`.returns-list` 123–195; empty-блок 61–66; 7× ActHeaderField 91–99; фон `.act-detail{background:var(--tr-bg)}` строка 128 — убрать, фон даёт D-02)
    - ui/src/features/acts/ActHeaderField.svelte (label/value виджет — согласовать/заменить DetailField)
    - ui/src/lib/components/DetailPanel.svelte, DetailSection.svelte, DetailField.svelte (созданы 27-01)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Переписать `ActDetail.svelte` на общий паттерн 27-01. Empty-состояние → `<DetailPanel empty
    emptyTitle="Выберите акт" emptyBody="Выберите акт слева..." >{#snippet emptyActions()}
    <Button variant="primary" onclick={onCreate}>+ Создать акт</Button>{/snippet}</DetailPanel>`.
    Заполненное состояние → `<DetailPanel title={...} >{#snippet actions()}<Button.../>{/snippet}`
    + тело из `<DetailSection>` + `<DetailField label value>` вместо `.header-grid`/`ActHeaderField`.
    Сетку полей (2 колонки, прежняя `.header-grid`) задать scoped-стилем-обёрткой в ActDetail вокруг
    DetailField — число колонок и набор полей сохранить дословно (7 полей: Сдал/Принял/Дата/Дата
    архивации/Сроком до/Расположение/Заметки). `.returns-list` (190–195) ре-токенизировать в
    `DetailSection` (список возвратов — данные, не трогать логику). Удалить bespoke `.detail-header`/
    `.section`/`.field-*` и фон `.act-detail`. `ActHeaderField.svelte`: заменить его использование на
    `DetailField`; если файл больше нигде не импортируется — удалить его; иначе оставить как тонкую
    обёртку над DetailField. **Поля/действия/структура секций не меняются (SC #4).**
  </action>
  <acceptance_criteria>
    - `ActDetail.svelte` содержит `import DetailPanel` И `<DetailPanel`
    - `grep -c "detail-header" ActDetail.svelte` == 0 И `grep -c "background: var(--tr-bg)" ActDetail.svelte` == 0
    - Все 7 field-меток присутствуют: `grep -c "Сдал\|Принял\|Дата\|Сроком до\|Расположение\|Заметки" ActDetail.svelte` покрывает прежний набор (поля не удалены)
    - Если `ActHeaderField` удалён — `grep -rc "ActHeaderField" ui/src` == 0; иначе он импортирует DetailField
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check (обе темы): деталь акта показывает те же поля/секции/кнопки, что и до правки
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть акт: шапка+действия, все 7 полей, список возвратов, кнопки печати/редактирования работают; пустое состояние с «+ Создать акт»</human-check>
  </verify>
  <done>ActDetail на DetailPanel/DetailSection/DetailField; bespoke detail-классы и внутренний фон удалены; поля и действия неизменны (SC #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. Окно Актов — чисто визуальная ре-токенизация (SC #4): поля, действия
(печать/возврат/редактирование), workflow, API не меняются.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Tampering (regression) | Акты: список/деталь/фильтр | mitigate | SC #4 scope-fence + гейты svelte-check/check-tokens/lint + human-check обеих тем; Table/TableRow не модифицируются (потребители не ломаются) |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов не ставим — только существующие примитивы |

Угроз уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0
- `pnpm --dir ui svelte-check` код 0
- `pnpm --dir ui lint` код 0
- Регресс существующих потребителей Table/TableRow (Устройства, витрина, ActFormItemsTable) — визуально не сломаны
</verification>

<success_criteria>
Структурный слой Актов (master-detail, фильтр, список, деталь) целиком на `Tabs`/`Table`/`TableRow`/
`DetailPanel` и токенах `--tr-*`, без bespoke-классов; регресс D-13 закрыт; поля/действия/workflow
сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-02-SUMMARY.md` when done
</output>
