---
phase: 27-core-workflow-windows
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/cartridges/OperationModal.svelte
  - ui/src/features/cartridges/ModelFormModal.svelte
autonomous: true
requirements: [WIN-04]
must_haves:
  truths:
    - "D-04: внутренняя разметка двух крупнейших модалок Картриджей (OperationModal 887 стр., ModelFormModal 580 стр.) переведена на токены/примитивы целиком — bespoke-классов не остаётся; многошаговый workflow операции и форма модели не меняются (SC #4)"
    - "Контролы обеих модалок используют примитивы Input/Select/Checkbox/Button; сырых стилизованных контролов без токенов не осталось"
    - "SC #4: операции жизненного цикла картриджа и создание/правка модели работают как раньше"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "Многошаговая модалка операций на токенах/примитивах"
    - path: "ui/src/features/cartridges/ModelFormModal.svelte"
      provides: "Форма модели картриджа на токенах/примитивах"
  key_links:
    - from: "ui/src/features/cartridges/OperationModal.svelte"
      to: "ui/src/styles/_tokens.scss"
      via: "scoped-стили только через var(--tr-*)"
      pattern: "var\\(--tr-"
---

<objective>
Полная ре-токенизация двух крупнейших модалок окна **Картриджей (WIN-04, D-04)**: `OperationModal`
(887 стр., многошаговая операция жизненного цикла) и `ModelFormModal` (580 стр., форма модели). Чрома
на `Modal` уже готова; переводим внутреннюю разметку форм на токены/примитивы **без остатков bespoke-
классов**. **Многошаговый workflow, поля, валидация и логика не меняются (SC #4).**

Purpose: закрыть SC #2 (Картриджи повсеместно) для самого глубокого модального пласта.
Output: две модалки на токенах/примитивах, чистые от bespoke-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@ui/src/features/devices/DeviceFormBody.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: OperationModal (887) — ре-токенизация (D-04)</name>
  <read_first>
    - ui/src/features/cartridges/OperationModal.svelte (887 стр.; многошаговый workflow; prevStateOptions/stateOptions селекты строки 707/782 — логику не трогать)
    - ui/src/features/devices/DeviceFormBody.svelte (эталон тела формы на примитивах)
    - ui/src/styles/_tokens.scss (сверить имена — check-tokens closed-world; читать перед правкой)
  </read_first>
  <action>
    Ре-токенизировать внутренности `OperationModal.svelte`. Сырые контролы (`<input>/<select>/
    <button>/<textarea>/<label>`) → примитивы `Input`/`Select`/`Checkbox`/`Button` там, где не
    применены; все scoped-стили → `var(--tr-*)`; удалить bespoke form-классы, дублирующие примитивы.
    **Многошаговую логику, переходы шагов, состояния (prev/next state options), валидацию, submit —
    НЕ менять (SC #4)**. Это файл на 887 строк — работать секциями сверху вниз, сохраняя структуру.
    Не вводить hardcode-цвета; `const $props()`.
  </action>
  <acceptance_criteria>
    - `grep -cE "background:\\s*#|color:\\s*#[0-9a-fA-F]|rgba\\((?!.*var)" OperationModal.svelte` == 0 (нет hardcode-цветов)
    - `grep -c "var(--tr-" OperationModal.svelte` > 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: все шаги операции проходятся, состояния/валидация/submit работают как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Пройти операцию жизненного цикла картриджа целиком: шаги, поля, submit неизменны</human-check>
  </verify>
  <done>OperationModal на токенах/примитивах без bespoke-классов; многошаговый workflow сохранён (SC #4).</done>
</task>

<task type="auto">
  <name>Task 2: ModelFormModal (580) — ре-токенизация (D-04)</name>
  <read_first>
    - ui/src/features/cartridges/ModelFormModal.svelte (580 стр.; brand/model автокомплит-подсказки строки 339/381; COLOR_OPTIONS 409 — логику/данные не трогать)
    - ui/src/features/devices/DeviceFormModal.svelte (эталон мигрированной формы)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Ре-токенизировать `ModelFormModal.svelte`: сырые контролы → `Input`/`Select`/`Checkbox`/`Button`;
    подсказки автокомплита (brand/model suggestions) — ре-токенизировать разметку, логику подсказок
    НЕ менять; выбор цвета (COLOR_OPTIONS) — на токены/примитивы, набор опций сохранить. scoped-стили
    → `var(--tr-*)`; удалить bespoke form-классы. Поля, валидация, submit — без изменений (SC #4).
  </action>
  <acceptance_criteria>
    - `grep -cE "background:\\s*#|color:\\s*#[0-9a-fA-F]|rgba\\((?!.*var)" ModelFormModal.svelte` == 0
    - `grep -c "var(--tr-" ModelFormModal.svelte` > 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: создание/правка модели, автокомплит бренда/модели, выбор цвета работают как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Создать/отредактировать модель: поля, подсказки, выбор цвета, submit неизменны</human-check>
  </verify>
  <done>ModelFormModal на токенах/примитивах без bespoke-классов; поля/автокомплит/цвета сохранены (SC #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — чисто визуальная ре-токенизация двух модалок (SC #4).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Tampering (regression) | OperationModal/ModelFormModal | mitigate | SC #4 scope-fence + svelte-check/check-tokens/lint + human-check полного прохода |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов не ставим |

Угроз уровня `high` нет.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0; `pnpm --dir ui lint` код 0
</verification>

<success_criteria>
Две крупнейшие модалки Картриджей целиком на токенах/примитивах, без bespoke-классов; многошаговый
workflow операции и форма модели сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-05-SUMMARY.md` when done
</output>
