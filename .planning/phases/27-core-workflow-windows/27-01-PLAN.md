---
phase: 27-core-workflow-windows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/DetailPanel.svelte
  - ui/src/lib/components/DetailSection.svelte
  - ui/src/lib/components/DetailField.svelte
autonomous: true
requirements: [WIN-03, WIN-04, WIN-05]
must_haves:
  truths:
    - "D-01: существует общий разделяемый паттерн детальной панели (шапка + секции + field-grid) в ui/src/lib/components/, готовый к переиспользованию окнами Актов/Картриджей/Принтеров и Фазой 28"
    - "Общие атомы детали (контейнер+скролл, empty-state, шапка title+actions, секция, field label/value) больше не дублируются пословно в каждом окне"
    - "Новые компоненты не меняют отображаемые поля и структуру секций (SC #4) — это форма, не содержание"
  artifacts:
    - path: "ui/src/lib/components/DetailPanel.svelte"
      provides: "Контейнер+скролл детали, loading/empty-блок, шапка (title + actions Snippet), body Snippet"
      exports: ["default"]
    - path: "ui/src/lib/components/DetailSection.svelte"
      provides: "Секция детали: heading + children"
      exports: ["default"]
    - path: "ui/src/lib/components/DetailField.svelte"
      provides: "Поле field-grid: label + value (с fallback «—»)"
      exports: ["default"]
  key_links:
    - from: "ui/src/lib/components/DetailPanel.svelte"
      to: "ui/src/styles/_tokens.scss"
      via: "все поверхности/отступы/типографика через var(--tr-*)"
      pattern: "var\\(--tr-"
---

<objective>
Извлечь **лёгкий общий паттерн детальной панели (D-01)** как разделяемый артефакт первой волны —
по прецеденту извлечения `PageHeader.svelte` в Фазе 26. Три окна (`ActDetail` 201, `CartridgeDetail`
334, `PrinterDetail` 603) сегодня пословно повторяют один набор классов: контейнер+скролл, loading/
empty-блок, `detail-header` (title + actions), секция (heading + body), field-grid (label/value).
Выносим этот словарь в `ui/src/lib/components/`, чтобы Фаза 27 (три окна) и Фаза 28 (ещё четыре)
его переиспользовали.

Purpose: убрать крупнейший bespoke-пласт и дать окнам wave-2 готовую поверхность детали.
Output: три маленьких компонента-примитива (`DetailPanel`, `DetailSection`, `DetailField`), целиком
на токенах `--tr-*`. **Поля и структура секций не меняются (SC #4) — только форма.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-CONTEXT.md
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@ui/src/lib/components/PageHeader.svelte

<interfaces>
Прецедент формы извлечения — PageHeader.svelte: `{ title: string; actions?: Snippet }`, шапка
рендерит title + `{@render actions()}`. Тот же приём Snippet-слотов применяем к DetailPanel.

Общий словарь трёх *Detail.svelte (из 27-PATTERNS §D-01), который извлекаем:
- контейнер+скролл (`.act-detail`/`.cartridge-detail`/`.printer-detail`)
- loading/empty-блок (идентичен во всех трёх: центрированная колонка, min-height 320px,
  `gap: var(--tr-space-md)`, `color: var(--tr-text-secondary)`)
- шапка `.detail-header` (title + `.actions` с Button×N)
- секция `.section` + `.section-heading`
- field-grid `.fields-grid` + `.field` + `.field-label` + `.field-value`
Существующий `ActHeaderField.svelte` (label/value, 36 строк) — прямой прообраз DetailField;
DetailField должен покрывать его контракт `{ label, value }` с fallback «—» на null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DetailPanel.svelte — контейнер, скролл, empty-state, шапка</name>
  <read_first>
    - ui/src/lib/components/PageHeader.svelte (прецедент Snippet-слотов title + actions)
    - ui/src/features/acts/ActDetail.svelte (эталон empty-блока строки 61–66 + CSS 130–151; шапка 68–86 + CSS 153–172)
    - ui/src/styles/_tokens.scss (closed-world check-tokens.mjs роняет сборку на несуществующем токене — сверять имена: --tr-space-md, --tr-text-secondary, --tr-text-primary, --tr-surface-raised, --tr-radius-md, --tr-font-size-h3, --tr-font-weight-semibold)
  </read_first>
  <action>
    Создать `ui/src/lib/components/DetailPanel.svelte` (Svelte 5 runes). Props:
    `{ title?: string; empty?: boolean; emptyTitle?: string; emptyBody?: string;
    actions?: Snippet; emptyActions?: Snippet; children?: Snippet }`.
    Разметка: корневой `<div class="detail-panel">` (скролл-контейнер, `overflow: auto`,
    `height: 100%`). Когда `empty` → рендерить центрированный empty-блок (`.empty`: flex-column,
    align/justify center, `gap: var(--tr-space-md)`, `min-height: 320px`, `text-align: center`,
    `color: var(--tr-text-secondary)`) с `emptyTitle`/`emptyBody` и `{@render emptyActions?.()}`.
    Иначе → рендерить шапку `<header class="detail-header">` с `<h2 class="detail-title">{title}</h2>`
    и `<div class="actions">{@render actions?.()}</div>`, затем `{@render children?.()}` (тело секций).
    Все цвета/отступы/типографика ТОЛЬКО через `var(--tr-*)` (перенести значения из ActDetail CSS
    130–172 по значению). НЕ задавать фон панели (фон даёт обёртка master-detail из D-02, plan 27-02/04/07) —
    чтобы избежать двойной заливки. `const`-деструктуризация `$props()` (не `let`) — контракт Фазы 24.
    Скролл-контейнер обязан скроллиться независимо (тело детали Фазы 26, D-07).
  </action>
  <acceptance_criteria>
    - `ui/src/lib/components/DetailPanel.svelte` существует и экспортирует Props с полями `title`, `empty`, `emptyTitle`, `emptyBody`, `actions`, `emptyActions`, `children`
    - Файл содержит `{@render children` И `{@render actions` (Snippet-слоты)
    - `grep -c "var(--tr-" DetailPanel.svelte` > 0 И `grep -cE "#[0-9a-fA-F]{3}|background:\\s*#|rgba\\(" DetailPanel.svelte` == 0 (нет hardcode-цветов)
    - Файл НЕ содержит `background:` на `.detail-panel` (фон даёт обёртка D-02)
    - `node ui/scripts/check-tokens.mjs` завершается кодом 0
    - `pnpm --dir ui svelte-check` завершается кодом 0
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>DetailPanel рендерит скролл-контейнер + empty-state + шапку (title/actions) + body Snippet; целиком на токенах; svelte-check и check-tokens зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: DetailSection.svelte + DetailField.svelte — секция и field-grid</name>
  <read_first>
    - ui/src/features/cartridges/CartridgeDetail.svelte (эталон field-grid: `.fields-grid`+`.field`+`.field-label`+`.field-value` строки 163–186 + CSS 282–306; секция 271–280)
    - ui/src/features/acts/ActHeaderField.svelte (существующий прообраз label/value — DetailField должен покрыть его контракт)
    - ui/src/styles/_tokens.scss (сверить: --tr-text-secondary, --tr-text-primary, --tr-font-size-caption, --tr-font-size-body, --tr-space-*, --tr-font-weight-semibold)
  </read_first>
  <action>
    Создать два компонента в `ui/src/lib/components/`.
    (1) `DetailSection.svelte`: Props `{ heading?: string; children?: Snippet }`. Разметка
    `<section class="detail-section">` c `<h3 class="section-heading">{heading}</h3>` (если задан) +
    `{@render children?.()}`. CSS переносит `.section`/`.section-heading` из CartridgeDetail 271–280
    по значению, всё через `var(--tr-*)`.
    (2) `DetailField.svelte`: Props `{ label: string; value?: string | null }`. Разметка
    `<div class="field"><span class="field-label">{label}</span>
    <span class="field-value">{value ?? '—'}</span></div>`. CSS из CartridgeDetail 282–306 по значению.
    Контракт совместим с `ActHeaderField` (`{ label, value }`), fallback `null → «—»`.
    Оба на токенах; `const $props()`.
    НЕ вводить сетку `.fields-grid` внутрь DetailField — сетку задаёт потребитель (окно) своим
    scoped-стилем или через DetailSection, чтобы число колонок каждой панели осталось прежним (SC #4).
  </action>
  <acceptance_criteria>
    - `ui/src/lib/components/DetailSection.svelte` и `ui/src/lib/components/DetailField.svelte` существуют
    - DetailField рендерит `value ?? '—'` (grep `'—'` присутствует) и принимает `label`
    - DetailSection содержит `{@render children` и опциональный `section-heading`
    - `grep -cE "background:\\s*#|rgba\\(|#[0-9a-fA-F]{6}" DetailSection.svelte DetailField.svelte` == 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>DetailSection и DetailField существуют, покрывают контракт секции/поля (в т.ч. ActHeaderField), целиком на токенах; гейты зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет. Фаза чисто визуальная (SC #4): не добавляются входы, эндпоинты, авторизация
или потоки данных. Изменяется только форма представления (токены/примитивы).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering (regression) | новые lib/components детали | mitigate | SC #4 scope-fence (поля/структура не меняются) + гейты svelte-check/check-tokens/lint + визуальный UAT обеих тем (plan 27-09) |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | В фазе НЕ устанавливаются новые пакеты — только переиспользование существующих примитивов; поверхность package-install отсутствует |

Ни одной угрозы уровня `high` фаза не вносит.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0 (closed-world токен-гейт)
- `pnpm --dir ui svelte-check` код 0
- `pnpm --dir ui lint` код 0 (eslint + prettier + check-tokens)
</verification>

<success_criteria>
Три компонента-примитива детали существуют в `ui/src/lib/components/`, покрывают общий словарь
(контейнер/empty/шапка/секция/field), целиком на `--tr-*`, не меняют поля/структуру (SC #4),
готовы к потреблению планами 27-02/27-04/27-07.
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-01-SUMMARY.md` when done
</output>
