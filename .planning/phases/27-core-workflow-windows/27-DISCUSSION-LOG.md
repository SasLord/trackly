# Phase 27: Окна основного рабочего процесса - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-20
**Phase:** 27-core-workflow-windows
**Areas discussed:** Источник истины без макета (детальные панели), Master-detail и инверсия D-06, Строки и таблицы списков, Глубина модалок и виджетов

---

## Детальные панели — источник истины без макета

| Option | Description | Selected |
|--------|-------------|----------|
| Общий паттерн + ре-токен | Извлечь лёгкий общий паттерн детали (шапка + секции + field-grid) как PageHeader в Фазе 26, чтобы Фазы 27–28 переиспользовали; остальное ре-токенизировать; структура/поля не меняются (SC #4). | ✓ |
| По-оконно ре-токен | Каждое окно отдельно на токены/примитивы, bespoke-разметка детали остаётся разной. Быстрее, но Фаза 28 повторит работу. | |
| Полный редизайн панелей | Новая раскладка детали с нуля. Риск выхода за SC #4, UX-сдвиг. | |

**User's choice:** Общий паттерн + ре-токен (D-01)
**Notes:** Окупается так же, как PageHeader — Фаза 28 несёт ещё четыре окна с детальными панелями.

---

## Master-detail и инверсия D-06

| Option | Description | Selected |
|--------|-------------|----------|
| surface-raised + обёртка Table | Обе панели на --tr-surface-raised + border + --tr-elev-1, как обёртка таблицы Фазы 26 (D-11). Закрывает регресс D-13. | ✓ |
| Только границы/разделители | Оставить панели на --tr-surface, развести границами без подъёма фона. Площе, меньше разделения master vs detail. | |

**User's choice:** surface-raised + обёртка Table (D-02)
**Notes:** Структура master-detail (grid 35/65) сохраняется — SC #4; проверить обе темы.

---

## Строки и таблицы списков

| Option | Description | Selected |
|--------|-------------|----------|
| На Table/TableRow как Устройства | Списки → семантика <table> + примитивы Table/TableRow со своими колонками; общая обёртка/футер/пустые состояния. Больше работы, но консистентно. | ✓ |
| Сохранить list-row, ре-токен | Оставить текущую div/grid-структуру строк, ре-токенизировать + обёртка Table вокруг. Быстрее, но строки архитектурно отличны от Устройств. | |

**User's choice:** На Table/TableRow как Устройства (D-03)
**Notes:** Осторожно с общими Table/TableRow — потребители Устройства/витрина/ActFormItemsTable.

---

## Глубина модалок и фича-виджетов

| Option | Description | Selected |
|--------|-------------|----------|
| Полная ре-токенизация, без редизайна | Внутренности форм модалок и виджеты целиком на токены/примитивы, никаких bespoke-классов; функция/раскладка не меняются; DiscoveryResultsTable → Table. Честно закрывает SC. | ✓ |
| Только видимый верхний слой | Ре-токенизировать только видимые поверхности; глубокие модалки/виджеты отложить. Риск: SC «повсеместно» не закроется. | |

**User's choice:** Полная ре-токенизация, без редизайна (D-04)
**Notes:** Чрома (Modal) уже готова у всех 9 модалок; контролы уже на Input/Select.

---

## Claude's Discretion
- Форма общего паттерна детальной панели (компонент vs snippets/классы) и его размещение (D-01).
- Точное сопоставление колонок каждого списка с API TableRow (D-03).
- Дробление на волны/планы; порядок по прецеденту D-19 Фазы 26 (общие файлы первыми).
- Конкретные значения ре-токенизации там, где `.dc` их не задаёт.
- Табы фильтров → примитив Tabs (D-05, прямое повторение D-10 Фазы 26 — принято по умолчанию).

## Deferred Ideas
- Полноценная мобильная адаптивность трёх окон (WIN-03/04/05 её не покрывают).
- Редизайн раскладок детальных панелей / форм модалок (за пределами SC #4).
- Перевод карточек четырёх окон Фазы 28 на --tr-surface-raised (закроет D-13 для остальных).
- AA-контраст, focus ring, паритет Tauri vs LAN-браузер — QA-02/QA-03, Фаза 30.
- Грep-гейт на `:global(` в plain `.scss` — так и не добавлен в CI.
