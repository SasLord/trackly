---
phase: 27-core-workflow-windows
plan: 04
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - ui/src/features/cartridges/CartridgesMasterDetail.svelte
  - ui/src/features/cartridges/CartridgesSearchAndTabs.svelte
  - ui/src/features/cartridges/CartridgeFilters.svelte
  - ui/src/features/cartridges/CartridgesList.svelte
  - ui/src/features/cartridges/CartridgeListRow.svelte
  - ui/src/features/cartridges/ModelsList.svelte
  - ui/src/features/cartridges/ModelListRow.svelte
  - ui/src/features/cartridges/CartridgeDetail.svelte
autonomous: true
requirements: [WIN-04]
must_haves:
  truths:
    - "D-02: обе панели CartridgesMasterDetail на `--tr-surface-raised` + border + `box-shadow var(--tr-elev-1)` — регресс D-13 для окна Картриджей закрыт"
    - "D-05: CartridgesSearchAndTabs и CartridgeFilters используют примитив `Tabs` вместо самописных `<button class=\"tab\">`; счётчики/переключение статуса/типа/модели работают как раньше"
    - "D-03: CartridgesList/CartridgeListRow и ModelsList/ModelListRow переведены на `Table`/`TableRow` со своими колонками (статус-Badge/код/модель); маппинг статус→variant переиспользован из CartridgeDetail"
    - "D-01: CartridgeDetail потребляет общий `DetailPanel`/`DetailSection`/`DetailField`; bespoke `.detail-header`/`.fields-grid`/`.history-list` удалены; отображаемые поля и история не изменились (SC #4)"
    - "SC #4: все поля/действия/workflow окна Картриджей на месте — изменение чисто визуальное"
  artifacts:
    - path: "ui/src/features/cartridges/CartridgesList.svelte"
      provides: "Список картриджей на Table/TableRow"
      contains: "Table"
    - path: "ui/src/features/cartridges/CartridgeDetail.svelte"
      provides: "Деталь картриджа на DetailPanel"
      contains: "DetailPanel"
  key_links:
    - from: "ui/src/features/cartridges/CartridgeDetail.svelte"
      to: "ui/src/lib/components/DetailPanel.svelte"
      via: "импорт общего паттерна детали (создан 27-01)"
      pattern: "import DetailPanel"
    - from: "ui/src/features/cartridges/CartridgesList.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "импорт и потребление Table/TableRow"
      pattern: "import Table"
---

<objective>
Перевести структурный слой окна **Картриджей (WIN-04)** на дизайн-систему: поверхности master-detail
(D-02), два фильтр-бара (D-05), два списка — экземпляры и модели (D-03) и деталь (D-01). Макета нет —
форма из компонентной системы Фаз 23–25 и прецедента Устройства. **Поля/действия/workflow не
меняются (SC #4).**

Purpose: закрыть SC #2 (Картриджи повсеместно на новых токенах/компонентах) для структурного слоя и
регресс D-13 для окна Картриджей.
Output: 8 файлов на `Tabs`/`Table`/`TableRow`/`DetailPanel`, без bespoke-классов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@.planning/phases/27-core-workflow-windows/27-01-SUMMARY.md
@ui/src/features/devices/DeviceFilters.svelte
@ui/src/features/devices/DeviceList.svelte
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartridgesMasterDetail (D-02) + SearchAndTabs + CartridgeFilters (D-05)</name>
  <read_first>
    - ui/src/features/cartridges/CartridgesMasterDetail.svelte (идентичен ActsMasterDetail; master `--tr-surface`, detail `--tr-bg`; grid 35/65 + `<1100px` fallback сохранить)
    - ui/src/features/cartridges/CartridgesSearchAndTabs.svelte (самописные `<button class="tab">` строки 58–73; debounce/логику не трогать)
    - ui/src/features/cartridges/CartridgeFilters.svelte (switch-bar статусов + фильтр типа + фильтр модели, `class="tab"`, по образцу DeviceFilters; строки STATUSES 45–, ключи number|null)
    - ui/src/features/devices/DeviceFilters.svelte (эталон Tabs + String(id)-адаптер 61–100)
    - ui/src/lib/components/Tabs.svelte, ui/src/styles/_tokens.scss (сверить --tr-surface-raised/--tr-elev-1/--tr-radius-md)
  </read_first>
  <action>
    (D-02) `CartridgesMasterDetail.svelte`: обе панели → `background: var(--tr-surface-raised);
    border: 1px solid var(--tr-border); border-radius: var(--tr-radius-md); box-shadow:
    var(--tr-elev-1);`. Grid 35/65 и `<1100px` fallback не трогать. Убрать detail `--tr-bg`.
    (D-05) `CartridgesSearchAndTabs.svelte` и `CartridgeFilters.svelte`: заменить самописные табы на
    `<Tabs variant="underline" ...>`. Для number|null ключей — адаптер как в DeviceFilters:
    `tabItems = STATUSES.map((s) => ({ key: String(s.id), label, count }))`, в onchange —
    `key === 'null' ? null : Number(key)`. Счётчики из `CartridgeCountsDto`, встроены в Tabs (убрать
    `<Badge>`-счётчики). Удалить bespoke `.tab`/`.tabs`-CSS. Фильтры типа/модели (Select) — оставить.
    `<script>`-логику фильтров (поиск, статус/тип/модель) НЕ менять — контракт D-05.
  </action>
  <acceptance_criteria>
    - `grep -c "var(--tr-surface-raised)" CartridgesMasterDetail.svelte` == 2
    - `grep -c "grid-template-columns: 35% 65%" CartridgesMasterDetail.svelte` == 1 (grid сохранён)
    - `CartridgesSearchAndTabs.svelte` и `CartridgeFilters.svelte` содержат `<Tabs`; `grep -c 'class="tab"'` == 0 в обоих
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check (обе темы): панели раздельны (raised+elev), фильтры статус/тип/модель работают, счётчики совпадают
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Картриджи в обеих темах: master/detail всплывают; фильтры и счётчики как раньше</human-check>
  </verify>
  <done>MasterDetail на raised+elev-1 (grid цел); оба фильтр-бара на Tabs без bespoke .tab; логика фильтров неизменна.</done>
</task>

<task type="auto">
  <name>Task 2: Cartridges+Models списки → Table/TableRow (D-03)</name>
  <read_first>
    - ui/src/features/cartridges/CartridgesList.svelte, CartridgeListRow.svelte (экземпляры)
    - ui/src/features/cartridges/ModelsList.svelte, ModelListRow.svelte (модели)
    - ui/src/features/cartridges/CartridgeDetail.svelte (маппинг статус→Badge variant строки 24–34 — переиспользовать)
    - ui/src/features/devices/DeviceList.svelte, DeviceListRow.svelte (эталон обёртки+строки+`.cell` truncate; ловушка `:global(> td)`)
    - ui/src/lib/components/Table.svelte, TableRow.svelte, ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Перевести оба списка на `Table`/`TableRow` по эталону Устройств. Bespoke обёртки/скроллеры/
    футеры/пустые состояния удалить — их роль забирает `Table` (`loading`/`empty`/`emptyTitle`/
    `emptyBody`/`footer`). Строки → `<TableRow selected={...}>` c `<td class="cell">`-ячейками.
    Колонки (Claude's Discretion, но без изменения отображаемых полей — SC #4):
    - Экземпляры (`CartridgeListRow`): статус-`Badge` (маппинг status_id→variant из CartridgeDetail
      24–34) · код (`tr-mono`) · модель — ровно те поля, что строка показывает сейчас.
    - Модели (`ModelListRow`): сохранить дословно текущий набор отображаемых полей строки, разложив
      их по `<td>`-ячейкам (число колонок = число текущих полей).
    Select-состояние через проп `selected` (не bespoke `.row.selected`), клик-на-строку — на TableRow.
    `.cell`-truncate из DeviceListRow 84–91. **Ловушка `:global()`**: `.tr-row :global(> td)`.
    Table.svelte/TableRow.svelte НЕ менять; потребителей (Устройства/витрина/ActFormItemsTable) не ломать.
  </action>
  <acceptance_criteria>
    - `CartridgesList.svelte` и `ModelsList.svelte` содержат `<Table` и `import Table`
    - `CartridgeListRow.svelte` и `ModelListRow.svelte` содержат `<TableRow`
    - `grep -c 'class="row"' CartridgeListRow.svelte ModelListRow.svelte` == 0 (bespoke строки удалены)
    - CartridgeListRow использует `Badge` для статуса (`grep -c "Badge" CartridgeListRow.svelte` >= 1)
    - Table.svelte/TableRow.svelte не в git diff плана
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: списки картриджей и моделей — таблицы; выбор строки подсвечивает; те же поля
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Списки экземпляров и моделей = таблицы; статус-бейджи, код, модель на месте; выбор работает</human-check>
  </verify>
  <done>Оба списка Картриджей на Table/TableRow со своими колонками; bespoke удалены; потребители Table не сломаны; поля сохранены.</done>
</task>

<task type="auto">
  <name>Task 3: CartridgeDetail → DetailPanel (D-01)</name>
  <read_first>
    - ui/src/features/cartridges/CartridgeDetail.svelte (bespoke `.cartridge-detail`/`.detail-header`/`.fields-grid`/`.field`/`.history-list`/`.history-row` строки 207–333; маппинг статус→Badge 24–34; фон detail — убрать, даёт D-02)
    - ui/src/lib/components/DetailPanel.svelte, DetailSection.svelte, DetailField.svelte (созданы 27-01)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Переписать `CartridgeDetail.svelte` на общий паттерн 27-01: empty → `<DetailPanel empty
    emptyTitle emptyBody>` с `emptyActions`; заполненное → `<DetailPanel title>{#snippet actions}`
    + тело из `<DetailSection>` и `<DetailField label value>` вместо `.fields-grid`/`.field-*`.
    `.history-list`/`.history-row` (315–333) ре-токенизировать внутри `DetailSection` (история —
    данные, логику не трогать). Статус в шапке — через `Badge` (маппинг 24–34 сохранить). Удалить
    bespoke `.detail-header`/`.section`/`.field-*`/`.history-*` и внутренний фон детали. Набор полей,
    секции и порядок — дословно как сейчас (SC #4).
  </action>
  <acceptance_criteria>
    - `CartridgeDetail.svelte` содержит `import DetailPanel` и `<DetailPanel`
    - `grep -c "detail-header" CartridgeDetail.svelte` == 0 и `grep -c "fields-grid" CartridgeDetail.svelte` == 0 (bespoke удалены)
    - Секция истории присутствует (история не удалена): `grep -c "history\|История" CartridgeDetail.svelte` >= 1
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check (обе темы): деталь картриджа — те же поля/секции/статус-бейдж/история/действия
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Открыть картридж: поля, статус-бейдж, история, действия жизненного цикла работают как раньше</human-check>
  </verify>
  <done>CartridgeDetail на DetailPanel/DetailSection/DetailField; bespoke detail-классы удалены; поля/история/действия сохранены (SC #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — чисто визуальная ре-токенизация окна Картриджей (SC #4).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Tampering (regression) | Картриджи: списки/деталь/фильтры | mitigate | SC #4 scope-fence + svelte-check/check-tokens/lint + human-check обеих тем; Table/TableRow не модифицируются |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов не ставим |

Угроз уровня `high` нет.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0; `pnpm --dir ui lint` код 0
- Потребители Table/TableRow (Устройства, витрина, ActFormItemsTable) визуально не сломаны
</verification>

<success_criteria>
Структурный слой Картриджей (master-detail, два фильтра, два списка, деталь) целиком на `Tabs`/
`Table`/`TableRow`/`DetailPanel` и токенах, без bespoke-классов; регресс D-13 закрыт; поля/действия/
workflow сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-04-SUMMARY.md` when done
</output>
