---
phase: 27-core-workflow-windows
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/printers/PrinterCreateModal.svelte
  - ui/src/features/printers/DiscoveryModal.svelte
  - ui/src/features/printers/DiscoveryResultsTable.svelte
  - ui/src/features/printers/TonerGauge.svelte
  - ui/src/features/printers/PrinterAlertBanner.svelte
autonomous: true
requirements: [WIN-05]
must_haves:
  truths:
    - "D-04: модалки Принтеров (PrinterCreateModal, DiscoveryModal) и виджеты (TonerGauge, PrinterAlertBanner) переведены на токены/примитивы целиком — bespoke-классов не остаётся; функция/раскладка не меняются (SC #4)"
    - "D-03/D-04: сырой `<table class=\"results-table\">` в DiscoveryResultsTable заменён на примитивы `Table`/`TableRow` (columns=6, чекбоксы через `Checkbox`), пустое состояние отдано `Table`; select-all и dedup-подсветка сохранены"
    - "TonerGauge подтверждён на 100% токенах (порог-цвета <script> — логика, не тронута); PrinterAlertBanner консистентен с близнецом LowStockBanner"
    - "SC #4: создание принтера, SNMP-discovery и выбор результатов работают как раньше"
  artifacts:
    - path: "ui/src/features/printers/DiscoveryResultsTable.svelte"
      provides: "Таблица результатов discovery на Table/TableRow"
      contains: "Table"
  key_links:
    - from: "ui/src/features/printers/DiscoveryResultsTable.svelte"
      to: "ui/src/lib/components/Table.svelte"
      via: "замена сырого <table> на примитив Table + TableRow + Checkbox"
      pattern: "import Table"
---

<objective>
Полная ре-токенизация модалок и виджетов окна **Принтеров (WIN-05, D-04)**: `PrinterCreateModal`,
`DiscoveryModal`, `DiscoveryResultsTable` (сырой `<table>` → `Table`/`TableRow`), `TonerGauge` (аудит),
`PrinterAlertBanner`. **Без остатков bespoke-классов; функция и раскладка не меняются (SC #4).**

Purpose: закрыть SC #3 (Принтеры повсеместно) для модалок/виджетов.
Output: 5 файлов на токенах/примитивах; сырой results-table переведён на `Table`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@ui/src/features/devices/DeviceFormModal.svelte
@ui/src/lib/components/Table.svelte
@ui/src/lib/components/TableRow.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrinterCreateModal + DiscoveryModal (D-04)</name>
  <read_first>
    - ui/src/features/printers/PrinterCreateModal.svelte (уже на Modal; форма создания)
    - ui/src/features/printers/DiscoveryModal.svelte (уже на Modal; streaming SNMP-скан — логику не трогать)
    - ui/src/features/devices/DeviceFormModal.svelte (эталон формы на примитивах)
    - ui/src/styles/_tokens.scss (сверить перед правкой)
  </read_first>
  <action>
    Ре-токенизировать внутренности обеих модалок: сырые контролы → `Input`/`Select`/`Checkbox`/
    `Button`; scoped-стили → `var(--tr-*)`; удалить bespoke form-классы. Streaming-логику discovery
    (SNMP-скан, прогресс) и поля создания принтера НЕ менять (SC #4).
  </action>
  <acceptance_criteria>
    - Оба файла: `grep -cE "background:\\s*#|color:\\s*#[0-9a-fA-F]|rgba\\((?!.*var)" <file>` == 0
    - `grep -c "var(--tr-" PrinterCreateModal.svelte DiscoveryModal.svelte` > 0 в каждом
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: создание принтера и запуск discovery-скана работают как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Создать принтер; запустить SNMP-discovery: поля/прогресс/поведение неизменны</human-check>
  </verify>
  <done>Две модалки Принтеров на токенах/примитивах без bespoke-классов; streaming/поля сохранены (SC #4).</done>
</task>

<task type="auto">
  <name>Task 2: DiscoveryResultsTable — сырой <table> → Table/TableRow (D-04/D-03)</name>
  <read_first>
    - ui/src/features/printers/DiscoveryResultsTable.svelte (bespoke `<table class="results-table">` строки 46–91 + ручные `th,td` CSS 119–156; head со select-all чекбоксом 49–57; `tr.duplicate` цвет `--tr-text-tertiary`; empty `.empty` 37–43; items each 66)
    - ui/src/features/devices/DeviceList.svelte (эталон Table columns/head/empty)
    - ui/src/lib/components/Table.svelte, TableRow.svelte (контракты; ловушка `:global(> td)`)
    - ui/src/lib/components/Checkbox.svelte (замена сырых чекбоксов)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Заменить сырой `<table class="results-table">` на примитив `Table` (`columns=6`, свой `head`-snippet
    с чекбоксом select-all через `Checkbox`) + `TableRow` на каждую строку результата. `tr.duplicate`
    (цвет `--tr-text-tertiary`) → `class` pass-through на `TableRow`. Пустое состояние (`.empty` 37–43)
    отдать `Table` (`empty`/`emptyTitle`/`emptyBody`). Все чекбоксы строк → `Checkbox`-примитив. Удалить
    ручные `th,td`-стили (119–156) и bespoke `.results-table`/`.empty`. **Select-all, per-row select,
    dedup-подсветку и логику выбора НЕ менять (SC #4)** — только разметку/примитивы. Ловушка `:global()`:
    `.tr-row :global(> td)`. Table.svelte/TableRow.svelte не менять.
  </action>
  <acceptance_criteria>
    - `DiscoveryResultsTable.svelte` содержит `<Table` и `<TableRow` и `import Table`
    - `grep -c "results-table" DiscoveryResultsTable.svelte` == 0 (сырой table удалён)
    - Чекбоксы через примитив: `grep -c "<Checkbox" DiscoveryResultsTable.svelte` >= 1 И `grep -c 'type="checkbox"' DiscoveryResultsTable.svelte` == 0
    - Table.svelte/TableRow.svelte не в git diff плана
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: результаты discovery = Table; select-all и выбор строк работают; дубликаты подсвечены приглушённо
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Discovery-результаты: Table с чекбоксами, select-all, dedup-подсветка; выбор строк как раньше</human-check>
  </verify>
  <done>DiscoveryResultsTable на Table/TableRow + Checkbox; сырой results-table и ручные th/td удалены; логика выбора/dedup сохранена (SC #4).</done>
</task>

<task type="auto">
  <name>Task 3: TonerGauge (аудит) + PrinterAlertBanner (D-04)</name>
  <read_first>
    - ui/src/features/printers/TonerGauge.svelte (уже на токенах `--tr-accent`/`--tr-warning`/`--tr-danger`/`--tr-surface`/`--tr-border` строки 61–97; порог-цвета `<script>` 29–37 — ЛОГИКА, не трогать)
    - ui/src/features/printers/PrinterAlertBanner.svelte (warning-SVG + `color-mix(in srgb, var(--tr-warning) 10%, transparent)` строки 65–102)
    - ui/src/features/cartridges/LowStockBanner.svelte (близнец — держать структуру идентичной)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    (1) `TonerGauge.svelte`: аудит — подтвердить 0 остаточных hardcode-значений (всё на `var(--tr-*)`);
    порог-цвета в `<script>` 29–37 не трогать (логика). Если найден hardcode — перевести на токен.
    (2) `PrinterAlertBanner.svelte`: аудит остаточных не-токен значений; `color-mix` с `var(--tr-warning)`
    — допустимый паттерн, оставить; держать структуру идентичной близнецу `LowStockBanner`. Оба —
    без bespoke-классов, дублирующих примитивы; функция/раскладка не меняются (SC #4).
  </action>
  <acceptance_criteria>
    - `TonerGauge.svelte`: `grep -cE "#[0-9a-fA-F]{6}|rgba\\((?!.*var)" TonerGauge.svelte` без hardcode вне svg-атрибутов
    - `grep -c "var(--tr-" TonerGauge.svelte PrinterAlertBanner.svelte` > 0 в каждом
    - Порог-логика TonerGauge (`<script>` 29–37) не изменена по сигнатуре
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: TonerGauge меняет цвет по порогам как раньше; PrinterAlertBanner виден и = LowStockBanner
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>TonerGauge: пороговые цвета корректны; баннер алерта принтера стилистически = LowStockBanner</human-check>
  </verify>
  <done>TonerGauge подтверждён на токенах (порог-логика цела); PrinterAlertBanner на токенах = близнец LowStockBanner (SC #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — чисто визуальная ре-токенизация модалок/виджетов (SC #4). Streaming-логика
SNMP-discovery (существующий поток данных) не изменяется.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Tampering (regression) | модалки/виджеты Принтеров | mitigate | SC #4 scope-fence + svelte-check/check-tokens/lint + human-check; Table/TableRow не модифицируются |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов не ставим |

Угроз уровня `high` нет.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0; `pnpm --dir ui lint` код 0
- Потребители Table/TableRow не сломаны
</verification>

<success_criteria>
Модалки и виджеты Принтеров целиком на токенах/примитивах, без bespoke-классов; сырой results-table
переведён на `Table`; функция/раскладка/логика сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-08-SUMMARY.md` when done
</output>
