---
phase: 27-core-workflow-windows
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/cartridges/CartridgeFormModal.svelte
  - ui/src/features/cartridges/CartridgeFormBody.svelte
  - ui/src/features/cartridges/CompatibilityEditor.svelte
  - ui/src/features/cartridges/CartridgeContextMenu.svelte
  - ui/src/features/cartridges/LowStockBanner.svelte
  - ui/src/features/cartridges/CartridgeFilters.svelte
autonomous: true
requirements: [WIN-04]
must_haves:
  truths:
    - "D-04: форма экземпляра картриджа и фича-виджеты (CompatibilityEditor, CartridgeContextMenu, LowStockBanner) переведены на токены/примитивы целиком — bespoke-классов не остаётся; функция/раскладка не меняются (SC #4)"
    - "CartridgeContextMenu следует паттерну ActionMenu/DeviceContextMenu; LowStockBanner консистентен со своим близнецом PrinterAlertBanner (color-mix warning-фон — допустимый паттерн)"
    - "D-05: CartridgeFilters (второй фильтр-бар: статус + тип + модель) использует примитив `Tabs` вместо самописных `<button class=\"tab\">`; фильтрация статуса/типа/модели работает как раньше (перенесён сюда из 27-04 ради файлового бюджета плана)"
    - "SC #4: форма картриджа, редактор совместимости, контекст-меню, баннер низкого остатка и фильтр CartridgeFilters работают как раньше"
  artifacts:
    - path: "ui/src/features/cartridges/CartridgeFormBody.svelte"
      provides: "Тело формы экземпляра картриджа на токенах/примитивах"
    - path: "ui/src/features/cartridges/CompatibilityEditor.svelte"
      provides: "Редактор совместимости на токенах/примитивах"
  key_links:
    - from: "ui/src/features/cartridges/LowStockBanner.svelte"
      to: "ui/src/features/printers/PrinterAlertBanner.svelte"
      via: "близнецы-баннеры: одинаковая структура warning-SVG + color-mix фон"
      pattern: "color-mix"
---

<objective>
Полная ре-токенизация формы экземпляра картриджа и фича-виджетов окна **Картриджей (WIN-04, D-04)**:
`CartridgeFormModal`/`CartridgeFormBody`, `CompatibilityEditor`, `CartridgeContextMenu`,
`LowStockBanner`, а также миграция второго фильтр-бара `CartridgeFilters` на `Tabs` (D-05, перенесён
из 27-04 ради файлового бюджета). **Без остатков bespoke-классов; функция и раскладка не меняются (SC #4).**

Purpose: закрыть SC #2 (Картриджи повсеместно) для формы и виджетов.
Output: 6 файлов на токенах/примитивах.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@ui/src/features/devices/DeviceFormBody.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartridgeFormModal + CartridgeFormBody (D-04)</name>
  <read_first>
    - ui/src/features/cartridges/CartridgeFormModal.svelte, CartridgeFormBody.svelte (visibleModels/stateOptions строки 213/227 — логику не трогать)
    - ui/src/features/devices/DeviceFormBody.svelte (эталон тела формы на примитивах)
    - ui/src/styles/_tokens.scss (сверить перед правкой)
  </read_first>
  <action>
    Ре-токенизировать форму экземпляра картриджа: сырые контролы → `Input`/`Select`/`Checkbox`/
    `Button`; scoped-стили → `var(--tr-*)`; удалить bespoke form-классы. Поля/валидацию/submit и списки
    опций (модели/состояния) НЕ менять (SC #4).
  </action>
  <acceptance_criteria>
    - `grep -cE "background: *#|color: *#" CartridgeFormBody.svelte` == 0
    - `grep -c "var(--tr-" CartridgeFormBody.svelte` > 0
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: создание/правка экземпляра картриджа работает как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Форма картриджа: поля, выбор модели/состояния, submit неизменны</human-check>
  </verify>
  <done>Форма экземпляра картриджа на токенах/примитивах без bespoke-классов; поля/логика сохранены.</done>
</task>

<task type="auto">
  <name>Task 2: CompatibilityEditor + CartridgeContextMenu + LowStockBanner (D-04)</name>
  <read_first>
    - ui/src/features/cartridges/CompatibilityEditor.svelte (add/remove строки, suggestions 148/176 — логику не трогать)
    - ui/src/features/cartridges/CartridgeContextMenu.svelte (menuItems 147)
    - ui/src/features/cartridges/LowStockBanner.svelte (warning-SVG + `color-mix(in srgb, var(--tr-warning) 10%, transparent)` фон строки 50–95)
    - ui/src/features/devices/DeviceContextMenu.svelte и ui/src/lib/components/ActionMenu.svelte (эталон меню)
    - ui/src/features/printers/PrinterAlertBanner.svelte (близнец LowStockBanner — держать структуру идентичной)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    (1) `CompatibilityEditor.svelte`: ре-токенизировать разметку add/remove-строк и подсказок; сырые
    контролы → примитивы; логику add/remove/suggestions НЕ трогать. (2) `CartridgeContextMenu.svelte`:
    привести к паттерну `DeviceContextMenu`/`ActionMenu` (те же классы/токены/a11y); пункты меню и их
    действия не менять. (3) `LowStockBanner.svelte`: аудит остаточных не-токен значений; `color-mix`-
    фон с `var(--tr-warning)` — допустимый паттерн (встречается в живом DeviceFilters), оставить;
    держать структуру идентичной близнецу `PrinterAlertBanner`. Все три — на `var(--tr-*)`, без
    bespoke-классов, дублирующих примитивы. Функция/раскладка не меняются (SC #4).
  </action>
  <acceptance_criteria>
    - Все три файла: `grep -cE "background: *#|color: *#" <file>` == 0 (color-mix с var(--tr-*) допустим; var() vs raw покрывает check-tokens.mjs)
    - `grep -c "var(--tr-" CompatibilityEditor.svelte CartridgeContextMenu.svelte LowStockBanner.svelte` > 0 в каждом
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: редактор совместимости (add/remove), контекст-меню и баннер низкого остатка работают как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Совместимость: добавить/удалить строку; контекст-меню картриджа; баннер низкого остатка виден и стилистически = PrinterAlertBanner</human-check>
  </verify>
  <done>Три виджета Картриджей на токенах/примитивах; контекст-меню = паттерн ActionMenu; баннер = близнец PrinterAlertBanner; функция сохранена (SC #4).</done>
</task>

<task type="auto">
  <name>Task 3: CartridgeFilters → примитив Tabs (D-05)</name>
  <read_first>
    - ui/src/features/cartridges/CartridgeFilters.svelte (switch-bar статусов + фильтр типа + фильтр модели; самописный `<button class="tab">`; STATUSES ключи number|null; по образцу DeviceFilters)
    - ui/src/features/devices/DeviceFilters.svelte (эталон: `Tabs variant="underline"` + `String(id)`-адаптер строки 61–100)
    - ui/src/lib/components/Tabs.svelte (контракт: string key, встроенный count, active $bindable)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Заменить самописный таб-бар статусов в `CartridgeFilters.svelte` на `<Tabs variant="underline" ...>`
    по эталону `DeviceFilters`. Для number|null ключей — адаптер: `tabItems = STATUSES.map((s) =>
    ({ key: String(s.id), label, count }))`, в onchange — `key === 'null' ? null : Number(key)`.
    Счётчики из `CartridgeCountsDto`, встроены в Tabs (убрать `<Badge>`-счётчики). Удалить bespoke
    `.tab`/`.tabs`-CSS. Фильтры типа/модели (Select) — оставить. `<script>`-логику (статус/тип/модель)
    НЕ менять — контракт D-05.
  </action>
  <acceptance_criteria>
    - `CartridgeFilters.svelte` содержит `<Tabs` И `import Tabs`
    - `grep -c 'class="tab"' CartridgeFilters.svelte` == 0 (bespoke табы удалены)
    - Select-фильтры типа/модели сохранены (`grep -c "Select" CartridgeFilters.svelte` >= 1)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: фильтр статуса/типа/модели работает как раньше, счётчики совпадают
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>CartridgeFilters: переключение статуса через Tabs; фильтры типа/модели и счётчики неизменны</human-check>
  </verify>
  <done>CartridgeFilters на примитиве Tabs без bespoke .tab; логика фильтрации статуса/типа/модели сохранена (SC #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — чисто визуальная ре-токенизация формы и виджетов (SC #4).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Tampering (regression) | форма/виджеты Картриджей | mitigate | SC #4 scope-fence + svelte-check/check-tokens/lint + human-check |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов не ставим |

Угроз уровня `high` нет.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0; `pnpm --dir ui lint` код 0
</verification>

<success_criteria>
Форма экземпляра картриджа и фича-виджеты целиком на токенах/примитивах, без bespoke-классов;
функция/раскладка сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-06-SUMMARY.md` when done
</output>
