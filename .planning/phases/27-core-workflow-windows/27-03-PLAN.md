---
phase: 27-core-workflow-windows
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/features/acts/ReturnModal.svelte
  - ui/src/features/acts/DocumentAcceptanceModal.svelte
  - ui/src/features/acts/PdfPreviewModal.svelte
  - ui/src/features/acts/ActFormModal.svelte
  - ui/src/features/acts/ActFormBody.svelte
  - ui/src/features/acts/ActItemsTable.svelte
  - ui/src/features/acts/ReturnItemsTable.svelte
  - ui/src/features/acts/ActNumberField.svelte
autonomous: true
requirements: [WIN-03]
must_haves:
  truths:
    - "D-04: внутренняя разметка модалок и detail-внутренних таблиц окна Актов переведена на токены/примитивы целиком — bespoke-классов не остаётся; чрома (Modal) и функция/раскладка/поля не меняются (SC #4)"
    - "Все контролы форм модалок Актов используют примитивы Input/Select/Checkbox/Button; сырых стилизованных `<input>/<button>` без токенов не осталось"
    - "SC #4: возврат по акту, документ приёма, предпросмотр PDF, форма акта работают как раньше — изменение чисто визуальное"
  artifacts:
    - path: "ui/src/features/acts/ReturnModal.svelte"
      provides: "Диалог возврата на токенах/примитивах"
    - path: "ui/src/features/acts/ActFormBody.svelte"
      provides: "Тело формы акта на токенах/примитивах"
  key_links:
    - from: "ui/src/features/acts/ActFormBody.svelte"
      to: "ui/src/styles/_tokens.scss"
      via: "scoped-стили только через var(--tr-*)"
      pattern: "var\\(--tr-"
---

<objective>
Полная ре-токенизация внутренностей модалок и detail-внутренних таблиц окна **Актов (WIN-03, D-04)**
без редизайна. Чрома всех модалок уже на `Modal`; остаётся внутренняя разметка форм и таблиц —
переводится на токены/примитивы целиком, **без остатков bespoke-классов**. **Функция, раскладка и
поля не меняются (SC #4).**

Purpose: закрыть SC #1 (Акты повсеместно) для слоя модалок/внутренних таблиц.
Output: 8 файлов Актов на токенах/примитивах, чистые от bespoke-классов.

Примечание: `ActFormItemsTable.svelte` — **уже потребитель `Table`; НЕ трогать и не ломать** (D-03 caution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-core-workflow-windows/27-PATTERNS.md
@ui/src/features/devices/DeviceFormModal.svelte
@ui/src/features/devices/DeviceFormBody.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReturnModal + DocumentAcceptanceModal + PdfPreviewModal (D-04)</name>
  <read_first>
    - ui/src/features/acts/ReturnModal.svelte (уже на Modal; ре-токенизация внутренностей)
    - ui/src/features/acts/DocumentAcceptanceModal.svelte (уже на Modal)
    - ui/src/features/acts/PdfPreviewModal.svelte (preview/iframe; учесть комментарий строк 12–13 про печать — логику НЕ трогать)
    - ui/src/features/devices/DeviceFormModal.svelte (эталон мигрированной модалки — какие классы/примитивы)
    - ui/src/styles/_tokens.scss (сверить имена перед использованием — check-tokens closed-world)
  </read_first>
  <action>
    Для каждого из трёх файлов: заменить любые сырые контролы (`<input>/<select>/<button>/<textarea>/
    <label>`) на примитивы `Input`/`Select`/`Checkbox`/`Button` там, где они ещё не применены; все
    scoped-стили привести к `var(--tr-*)`; удалить bespoke-классы, дублирующие поведение примитивов.
    Раскладку форм, поля, обработчики, workflow (возврат/документ приёма/предпросмотр) НЕ менять —
    только одежда. В `PdfPreviewModal` iframe/blob-логику и комментарии про печать сохранить дословно.
    `const $props()` контракт Фазы 24. Не вводить hardcode-цвета.
  </action>
  <acceptance_criteria>
    - Каждый из трёх файлов: `grep -cE "background:\\s*#|color:\\s*#|rgba\\((?!.*var)" <file>` == 0 (нет hardcode-цветов)
    - Кнопки — через примитив: `grep -c "<button" ReturnModal.svelte DocumentAcceptanceModal.svelte` не растёт относительно замены на `<Button` (сырых стилизованных кнопок не остаётся)
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: диалог возврата, документ приёма и предпросмотр PDF открываются и работают как раньше
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Три модалки Актов визуально консистентны с Устройствами, поведение неизменно</human-check>
  </verify>
  <done>Три модалки Актов на токенах/примитивах без bespoke-классов; поведение сохранено (SC #4).</done>
</task>

<task type="auto">
  <name>Task 2: ActFormModal + ActFormBody + ActNumberField (D-04)</name>
  <read_first>
    - ui/src/features/acts/ActFormModal.svelte, ui/src/features/acts/ActFormBody.svelte (внутренности формы акта)
    - ui/src/features/acts/ActNumberField.svelte (field-виджет номера акта)
    - ui/src/features/devices/DeviceFormBody.svelte (эталон тела формы на примитивах)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Ре-токенизировать тело формы акта. Сырые контролы → `Input`/`Select`/`Checkbox`/`Button`;
    scoped-стили → `var(--tr-*)`; удалить bespoke form-классы, дублирующие примитивы. `ActNumberField`
    привести к токенам (при желании — согласовать с `DetailField`, но поле/логику номера не менять).
    Поля формы, порядок, валидацию, submit-обработчики — НЕ трогать (SC #4). `ActFormItemsTable`
    (уже потребитель Table) в этой задаче не менять.
  </action>
  <acceptance_criteria>
    - `ActFormBody.svelte`: `grep -cE "background:\\s*#|rgba\\((?!.*var)" ActFormBody.svelte` == 0
    - `grep -c "var(--tr-" ActFormBody.svelte` > 0
    - `ActFormItemsTable.svelte` НЕ в git diff этого плана
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: форма создания/редактирования акта — те же поля, submit работает
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Создать и отредактировать акт: поля/валидация/сохранение как раньше</human-check>
  </verify>
  <done>Форма акта на токенах/примитивах; ActFormItemsTable не тронут; поля/логика сохранены.</done>
</task>

<task type="auto">
  <name>Task 3: ActItemsTable + ReturnItemsTable — detail-внутренние таблицы (D-04)</name>
  <read_first>
    - ui/src/features/acts/ActItemsTable.svelte (detail-внутренняя таблица позиций акта)
    - ui/src/features/acts/ReturnItemsTable.svelte (таблица возвращаемых позиций)
    - ui/src/lib/components/TableRow.svelte (ловушка `:global(> td)` если целимся в td)
    - ui/src/styles/_tokens.scss
  </read_first>
  <action>
    Ре-токенизировать две detail-внутренние таблицы: scoped `th/td` стили → `var(--tr-*)`, убрать
    bespoke-классы, дублирующие примитивы. Это внутренние таблицы формы/детали (не master-списки) —
    перевод на примитив `Table` НЕ обязателен (D-03 касается master-списков), достаточно чистой
    ре-токенизации. Колонки, поля, редактируемость строк — не менять (SC #4).
  </action>
  <acceptance_criteria>
    - Оба файла: `grep -cE "background:\\s*#|rgba\\((?!.*var)|color:\\s*#" <file>` == 0
    - `grep -c "var(--tr-" ActItemsTable.svelte ReturnItemsTable.svelte` > 0 в каждом
    - `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0
    - human-check: таблицы позиций и возврата отображают те же колонки, редактирование работает
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && pnpm --dir ui svelte-check</automated>
    <human-check>Таблицы позиций/возврата: колонки и правка строк без изменений</human-check>
  </verify>
  <done>Две detail-внутренние таблицы Актов на токенах без bespoke-классов; колонки/поведение сохранены.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Новых границ доверия нет — чисто визуальная ре-токенизация модалок/таблиц (SC #4).

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering (regression) | модалки/таблицы Актов | mitigate | SC #4 scope-fence + svelte-check/check-tokens/lint + human-check; ActFormItemsTable не модифицируется |
| T-27-SC | Tampering (supply-chain) | npm-установки | accept | Новых пакетов не ставим |

Угроз уровня `high` нет.
</threat_model>

<verification>
- `node ui/scripts/check-tokens.mjs` код 0; `pnpm --dir ui svelte-check` код 0; `pnpm --dir ui lint` код 0
- `ActFormItemsTable.svelte` не в диффе плана
</verification>

<success_criteria>
Модалки и detail-внутренние таблицы Актов целиком на токенах/примитивах, без bespoke-классов;
функция/раскладка/поля сохранены (SC #4).
</success_criteria>

<output>
Create `.planning/phases/27-core-workflow-windows/27-03-SUMMARY.md` when done
</output>
