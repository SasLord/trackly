---
phase: 21-cartridge-drum-codes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs
  - crates/trackly-app/tests/cartridges_numbering.rs
autonomous: true
requirements: [CRT-01]

must_haves:
  truths:
    - "Новый картридж, создаваемый без явно указанного кода, получает автокод в формате C-XXXX (минимум 4 цифры)"
    - "Новый фотобарабан, создаваемый без явно указанного кода, получает автокод в формате D-XXXX (минимум 4 цифры)"
    - "Существующие 6-значные коды (например C-000123) остаются валидными строками, коллизий с новым форматом нет"
    - "Кастомный code_override и retry-loop механизм счётчика не затронуты"
  artifacts:
    - path: "crates/trackly-infra/src/repos/cartridges_sqlite.rs"
      provides: "fn assign_code_in_tx генерирует код через format!(\"{prefix}-{seq:04}\")"
    - path: "crates/trackly-app/tests/cartridges_numbering.rs"
      provides: "Интеграционный тест, проверяющий 4-значный (минимум) формат автокода"
  key_links:
    - from: "crates/trackly-infra/src/repos/cartridges_sqlite.rs::assign_code_in_tx"
      to: "cartridge_seq / drum_seq counters"
      via: "increment_counter_in_tx + format!(\"{prefix}-{seq:04}\")"
      pattern: "\\{seq:04\\}"
---

<objective>
Сократить формат автоматически присваиваемого кода нового картриджа/фотобарабана с 6 цифр (`C-000001`) до 4 цифр (`C-0001`), сохранив существующий механизм счётчика, retry-loop на коллизии и обработку `code_override`.

Purpose: CRT-01 — согласовать формат автокода с требуемым коротким видом; текущий 6-значный формат избыточен для реального объёма картриджей/фотобарабанов в организации.
Output: изменённая строка форматирования кода в `assign_code_in_tx`, обновлённые docstring-комментарии, обновлённый интеграционный тест `cartridges_numbering.rs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Текущее состояние fn assign_code_in_tx, crates/trackly-infra/src/repos/cartridges_sqlite.rs, строки ~109-164 -->
<!-- Executor должен использовать эти сигнатуры напрямую — исследование кодовой базы не требуется -->

```rust
/// Assign a code to a new cartridge/drum inside a transaction.
///
/// - `code_override = Some(s)`: validate UNIQUE; return `(s, false)` or
///   `AppError::Conflict` on collision (D-Code-Override-01).
/// - `code_override = None`: increment the kind-specific counter
///   (`cartridge_seq`→`C-NNNNNN` / `drum_seq`→`D-NNNNNN`) in a retry loop
///   until a unique code is found (D-Code-01). The counter is never lost.
///
/// Returns `(code, was_auto)`.
pub fn assign_code_in_tx(
    tx: &Transaction<'_>,
    code_override: Option<&str>,
    kind_id: i64,
    _now_utc: i64,
) -> Result<(String, bool), AppError> {
    // ... code_override branch unchanged ...

    // Префикс и счётчик зависят от вида расходника: фотобарабаны (kind 2) →
    // D-NNNNNN из drum_seq; картриджи (kind 1) → C-NNNNNN из cartridge_seq.
    let (counter_name, prefix) = if kind_id == 2 {
        ("drum_seq", 'D')
    } else {
        ("cartridge_seq", 'C')
    };

    // Auto-code: increment counter + retry loop (counter never lost on collision).
    loop {
        let seq = increment_counter_in_tx(tx, counter_name)?;
        let candidate = format!("{prefix}-{seq:06}");   // <-- единственная строка генерации формата
        // ... uniqueness check + retry ...
    }
}
```

Единственная строка, которую нужно изменить в теле функции: `format!("{prefix}-{seq:06}")` → `format!("{prefix}-{seq:04}")`. Uniqueness-check и retry-loop вокруг неё — не трогать.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Сократить формат автокода до 4 цифр и обновить numbering-тест</name>
  <files>crates/trackly-infra/src/repos/cartridges_sqlite.rs, crates/trackly-app/tests/cartridges_numbering.rs</files>
  <read_first>
    crates/trackly-infra/src/repos/cartridges_sqlite.rs (fn assign_code_in_tx, строки ~109-164, включая doc-комментарий и inline-комментарий)
    crates/trackly-app/tests/cartridges_numbering.rs (весь файл, 127 строк)
  </read_first>
  <behavior>
    - Test 1 (concurrent_50_unique_codes): 50 конкурентных созданий картриджа без code_override дают 50 уникальных кодов; каждый код соответствует формату `C-` + минимум 4 ASCII-цифры (в штатном прогоне ровно 4, т.к. счётчик стартует с малых значений в свежей тестовой БД).
    - Test 2 (collision_retry_does_not_lose_counter): последовательные создания дают строго возрастающие числовые суффиксы кода; извлечение суффикса (`c[2..]`) остаётся корректным при 4-значном формате.
  </behavior>
  <action>
    В `crates/trackly-infra/src/repos/cartridges_sqlite.rs`, внутри `assign_code_in_tx`: заменить `format!("{prefix}-{seq:06}")` на `format!("{prefix}-{seq:04}")` (per CRT-01, per D-01 из ROADMAP Phase 21). Retry-loop, `counter_name`/`prefix` mapping (`kind_id == 2` → `("drum_seq", 'D')`, иначе `("cartridge_seq", 'C')`), ветку `code_override` и вызов `increment_counter_in_tx` не менять.

    Обновить doc-комментарий функции (строки ~113-114): `cartridge_seq`→`C-NNNNNN` / `drum_seq`→`D-NNNNNN` → `cartridge_seq`→`C-NNNN` / `drum_seq`→`D-NNNN`.

    Обновить inline-комментарий (строки ~140-141): `D-NNNNNN из drum_seq; картриджи (kind 1) → C-NNNNNN из cartridge_seq` → `D-NNNN из drum_seq; картриджи (kind 1) → C-NNNN из cartridge_seq`.

    Не добавлять миграцию: `{:04}` — это минимальная ширина, при seq > 9999 формат естественно выдаёт 5+ цифр; существующие 6-значные коды в БД остаются валидными строками без коллизий с новым форматом (разные строки: `C-0001` ≠ `C-000001`).

    В `crates/trackly-app/tests/cartridges_numbering.rs`, в тесте `concurrent_50_unique_codes` (строки ~74-83): заменить проверку `code.len() == 8 && code.starts_with("C-") && code[2..].chars().all(|c| c.is_ascii_digit())` на проверку с минимальной длиной вместо фиксированной 8: код должен начинаться с `"C-"`, иметь длину ≥ 6 символов (`"C-"` + минимум 4 цифры), и все символы после `"C-"` — ASCII-цифры. Обновить сообщение assert с `"C-NNNNNN"` на `"C-NNNN"` (минимум 4 цифры). Комментарий на строке 4 (`50 concurrent creates produce 50 unique codes in C-NNNNNN format`) и на строке 74 (`All in C-NNNNNN format...`) обновить на `C-NNNN` (минимум 4 цифры).

    Тест `collision_retry_does_not_lose_counter` не требует изменений формата — он извлекает числовой суффикс через `c[2..].parse::<u64>()`, что не зависит от ширины форматирования.

    НЕ трогать литеральные коды-фикстуры в других тестовых файлах (`cartridges_search.rs`, `phase06_stubs.rs`, `acts_suggest.rs`) — это ручные фикстуры с явным `code_override`, не сгенерированный вывод.
  </action>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test cartridges_numbering -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - `crates/trackly-infra/src/repos/cartridges_sqlite.rs` содержит `format!("{prefix}-{seq:04}")` и НЕ содержит `format!("{prefix}-{seq:06}")`
    - doc-комментарий и inline-комментарий в `assign_code_in_tx` ссылаются на `C-NNNN` / `D-NNNN` (не на `C-NNNNNN` / `D-NNNNNN`)
    - `cargo test -p trackly-app --test cartridges_numbering -- --test-threads=1` завершается с exit code 0 (запускать по одному, без параллельных `cargo test`, per project convention)
    - Тест `concurrent_50_unique_codes` проверяет код по паттерну `^C-\d{4,}$` (минимум 4 цифры), не фиксированную длину 8
    - Ни один из файлов `cartridges_search.rs`, `phase06_stubs.rs`, `acts_suggest.rs`, `reports.rs`, `printers_sqlite.rs` не изменён
  </acceptance_criteria>
  <done>Формат автокода — 4 цифры минимум для C- и D-; retry-loop, code_override и счётчики (cartridge_seq/drum_seq) неизменны; numbering-тест зелёный и проверяет новый формат; docstring-комментарии согласованы с кодом.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Изменение затрагивает только внутренний путь автогенерации кода (server-side, без нового входного интерфейса). Нет новых trust boundaries: `code_override` (пользовательский ввод) продолжает проходить через существующую UNIQUE-проверку без изменений; изменяется только формат server-generated значения для ветки без override.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Repudiation/Integrity | assign_code_in_tx (auto-code branch) | accept | Изменение — только ширина форматирования (`{:04}` вместо `{:06}`); генерация кода остаётся server-side, retry-loop на UNIQUE-коллизию сохранён без изменений — риск коллизии/потери счётчика не увеличивается |
| T-21-02 | Tampering | code_override (custom code input) | accept | Путь валидации `code_override` (UNIQUE-проверка, `AppError::Conflict`) в этом плане не затрагивается — существующие ASVS L1 контролы сохраняются как есть |

Новых угроз не привнесено: изменение — это change формата ширины в существующем server-generated auto-numbering path, без нового surface ввода, без изменений auth/authz, без внешнего I/O. Auto-код остаётся server-generated, retry-loop на уникальность сохранён, путь валидации custom-кода не затронут. Block-on high severity: не применимо (угроз high severity не выявлено).
</threat_model>

<verification>
1. `cargo test -p trackly-app --test cartridges_numbering -- --test-threads=1` — exit 0, оба теста зелёные.
2. `grep -n '{seq:04}' crates/trackly-infra/src/repos/cartridges_sqlite.rs` — находит новую строку форматирования; `grep -n '{seq:06}'` в том же файле — не находит ничего.
3. `cargo clippy -p trackly-infra -- -D warnings` — без новых предупреждений (в рамках правки одной строки формата не ожидается).
4. Ручная проверка: код, сгенерированный для нового картриджа без override, соответствует `^C-\d{4,}$`; для нового фотобарабана (модель с `kind_id=2`) — `^D-\d{4,}$`.
</verification>

<success_criteria>
1. Новый картридж без явного кода получает автокод формата `C-XXXX` (4 цифры минимум).
2. Новый фотобарабан без явного кода получает автокод формата `D-XXXX` (4 цифры минимум).
3. Существующие 6-значные коды в БД остаются валидными, миграция не добавлена.
4. `code_override`, retry-loop и mapping `kind_id==2`→D/`drum_seq` не изменены.
</success_criteria>

<output>
Create `.planning/phases/21-cartridge-drum-codes/21-01-SUMMARY.md` when done
</output>
