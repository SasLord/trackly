---
phase: 34-document-header
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/templates/_header.html
  - crates/trackly-app/templates/_legacy_defaults/v21/act_handover.html
  - crates/trackly-app/templates/_legacy_defaults/v21/act_acceptance.html
  - crates/trackly-app/templates/_legacy_defaults/v21/report.html
  - crates/trackly-app/templates/act_handover.html
  - crates/trackly-app/templates/act_acceptance.html
  - crates/trackly-app/templates/report.html
  - crates/trackly-app/src/pdf/html_templates.rs
  - crates/trackly-app/tests/html_header_parity.rs
autonomous: true
requirements: [DOC-04, DOC-05, DOC-06]

must_haves:
  truths:
    - "D-05: _header.html's .header block is a centered single-column flex container (display: flex; flex-direction: column; align-items: center; gap: 6pt; width: 80mm), .header .logo img is max-height: 80pt; max-width: 140pt, .header .orgName is font-size: 12pt; font-weight: bold; line-height: 1.25; text-align: center, and .header .requisites is font-size: 11pt; text-align: center — replacing the old grid auto 1fr / 9pt / left-aligned layout."
    - "D-04: the .orgName block in _header.html renders two INDEPENDENT conditional lines — {{ org.full_name | safe }} guarded by its own presence (non-empty), and a separate {%- if org.name %}<br />({{ org.name }}){%- endif %} — neither line's visibility depends on the other's."
    - "D-06: .header keeps the hard width: 80mm, and .header .orgName / .header .requisites both declare overflow-wrap: break-word; hyphens: auto (plus -webkit-hyphens: auto) so a long full_name or address wraps inside the 80mm block instead of overflowing it."
    - "D-07: the requisites block in _header.html renders, in this exact order, address -> address_line2 -> \"Телефон {{ org.phone }}\" -> \"Факс {{ org.fax }}\" -> \"E-mail: {{ org.email }}\" -> \"ОКПО {{org.okpo}}, ОГРН {{org.ogrn}}\" (one line, ОГРН only when both present) -> \"ИНН {{org.inn}}, КПП {{org.kpp}}\" (one line) — replacing the old order (ИНН/КПП first, separate ОКПО/ОГРН lines, \"Тел.:\"/\"Факс:\" colon-suffixed prefixes)."
    - "D-08: when org.logo_data_uri is empty, the {%- if org.logo_data_uri %} guard around .logo's <img> still renders nothing and no space is reserved for the logo — unchanged from pre-existing behavior, now living in _header.html instead of the three individual templates."
    - "D-09: all three shipped templates' body { font-family; font-size } is exactly \"Times New Roman\", Georgia, \"PT Serif\", \"Liberation Serif\", \"DejaVu Serif\", serif; font-size: 12pt — replacing the old \"DejaVu Sans\", \"Arial\", sans-serif at 11pt (act_handover/act_acceptance) or 10.5pt (report)."
    - "D-10: the body font-family fallback chain in all three templates ends in the generic serif keyword (via Georgia, PT Serif, Liberation Serif, DejaVu Serif) — the sans-serif tail present in the hand-edited reference file is corrected to an all-serif chain in the shipped canon."
    - "D-11: this plan changes only the header block/CSS (now in _header.html), body font-family/font-size, and the top-level document-title font-size (unified to 14pt across act_handover.html's .title, act_acceptance.html's h1, and report.html's .title, down from report's prior 15pt) — no field-row, table, device-block, or signature markup below the title is touched in any of the three files."
    - "D-12: the header markup and its CSS live exclusively inside _header.html; none of act_handover.html, act_acceptance.html, report.html duplicate .header/.orgName/.requisites/.logo CSS rules or markup any more — each references the shared block via {% include \"_header.html\" %}."
    - "D-14: _header.html is registered as a fourth entry in DEFAULT_HTML_TEMPLATES (include_str!) in html_templates.rs, so materialize_defaults_on_startup creates it on any install that is missing it, and it participates in the existing read-on-render load_template fallback exactly like the three main templates."
    - "D-15: crates/trackly-app/templates/_legacy_defaults/v21/{act_handover,act_acceptance,report}.html hold the pre-Phase-34 bodies of the three templates, byte-identical to the pre-change repository content, added as a SECOND element of each filename's KNOWN_LEGACY_DEFAULTS slice — the existing v20 element is untouched, not replaced."
    - "D-16: upgrade_untouched_defaults_on_startup's previously-silent else branch (on-disk content matches neither the current default nor any known legacy snapshot) now calls tracing::warn! naming the specific file path, so a skipped auto-upgrade is observable instead of silent."
    - "DOC-05 (no hardcoded org name): _header.html's .orgName node contains no literal organization name — every non-whitespace, non-markup token inside it is a Jinja expression ({{ }} / {% %}) or the literal parenthesis/br markup from D-04's template, never a hardcoded Cyrillic legal name string."
  artifacts:
    - path: "crates/trackly-app/templates/_header.html"
      provides: "Shared header partial: markup + <style> for logo/orgName/requisites, wrapped in <!-- HEADER-START --> / <!-- HEADER-END --> markers for Plan 34-03's render-gate test"
    - path: "crates/trackly-app/templates/_legacy_defaults/v21/act_handover.html"
      provides: "Pre-Phase-34 snapshot of act_handover.html, for upgrade-detection"
    - path: "crates/trackly-app/templates/_legacy_defaults/v21/act_acceptance.html"
      provides: "Pre-Phase-34 snapshot of act_acceptance.html, for upgrade-detection"
    - path: "crates/trackly-app/templates/_legacy_defaults/v21/report.html"
      provides: "Pre-Phase-34 snapshot of report.html, for upgrade-detection"
    - path: "crates/trackly-app/tests/html_header_parity.rs"
      provides: "Substring gate (all three templates include _header.html) + privacy-safe structural test (no literal org name in the shipped .orgName markup) + v21 upgrade unit test"
  key_links:
    - from: "crates/trackly-app/templates/act_handover.html"
      to: "crates/trackly-app/templates/_header.html"
      via: "{% include \"_header.html\" %} — resolved from the Environment's in-memory template registry (wired in Plan 34-03), never the filesystem loader (D-13, unchanged)"
      pattern: "include \"_header.html\""
    - from: "crates/trackly-app/src/pdf/html_templates.rs::DEFAULT_HTML_TEMPLATES"
      to: "materialize_defaults_on_startup"
      via: "4th tuple (\"_header.html\", include_str!(...)) — any install missing the file gets it created on next startup"
      pattern: "_header.html"
---

<objective>
Rescue the user's hand-edited reference header (`target/debug/templates/act_handover.html`,
`.gitignore`d, dies on `cargo clean`) into the canonical `_header.html` partial — privacy-scrubbed,
replacing the hardcoded real organization name with `org.full_name`/`org.name` placeholders per
D-01..D-04 — BEFORE any other template work in this phase. Then snapshot the pre-Phase-34 bodies
of the three canonical templates as `_legacy_defaults/v21/` (D-15) and only THEN rewrite those
three templates to `{% include "_header.html" %}` plus the D-09/D-10/D-11 typography changes.

This plan intentionally does NOT wire the registration mechanism (`render_with_timeout`'s
`extra_templates` parameter) or the `org.full_name` render context — that lands in Plan 34-03
(interface-first ordering: this plan defines the template CONTRACT, 34-03 WIRES it). As a direct
consequence, every pre-existing test that actually RENDERS `act_handover.html` / `act_acceptance.html`
/ `report.html` (e.g. `html_act_render.rs`, `html_report_render.rs`, `pdf_render_act.rs`,
`pdf_text_extract.rs`, `pdf_column_overflow.rs`, `pdf_logo.rs`, `pdf_logo_aspect.rs`,
`template_edit.rs`, `templates_seed.rs`) will fail with "template not found" or "undefined
variable: full_name" until Plan 34-03 lands — this is EXPECTED and is why this plan's own
`<verify>` commands are scoped to compilation + the new structural tests only, never the full
suite. Do not attempt to "fix" the broken render tests in this plan.

Purpose: guarantee the reference geometry survives even if `target/debug/` is wiped before this
plan runs (the geometry is independently recorded in 34-RESEARCH.md § "Эталон вёрстки" as a
fallback), and establish the shared-partial contract (D-12) that makes DOC-04's "identical header
in all three forms" structurally impossible to violate.
Output: `_header.html`, three `_legacy_defaults/v21/*.html` snapshots, three rewritten canonical
templates, `html_templates.rs`'s materialize/upgrade registries extended, and
`tests/html_header_parity.rs` (substring + privacy-safe structural tests + v21 upgrade test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-document-header/34-CONTEXT.md
@.planning/phases/34-document-header/34-RESEARCH.md

<interfaces>
<!-- target/debug/templates/act_handover.html (mtime 2026-08-08 17:55, MAY BE ALREADY GONE if -->
<!-- cargo clean ran between research and execution — if so, rely EXCLUSIVELY on the -->
<!-- privacy-scrubbed inventory below, copied verbatim from 34-RESEARCH.md § "Эталон вёрстки"): -->
<!--
  body: font-family: "Times New Roman", "DejaVu Sans", "Arial", sans-serif; font-size: 12pt;
        (sans-serif tail is WRONG per D-10 — do not copy it; use the serif chain below instead)
  .header: display: flex; flex-direction: column; align-items: center; gap: 6pt;
           margin-bottom: 16pt; width: 80mm;
  .header .logo img: max-height: 80pt; max-width: 140pt;
  .header .orgName (NEW class, does not exist in canon today):
           font-size: 12pt; line-height: 1.25; font-weight: bold; text-align: center;
  .header .requisites: font-size: 11pt; text-align: center; (line-height NOT set — the
           reference has it commented out, so it inherits from body)

  Markup (privacy-scrubbed — the real hardcoded name is REPLACED by org.full_name/org.name below,
  this is the exact target state to write into _header.html, NOT to be copied from the raw file
  verbatim without this substitution):
    <div class="header">
      <div class="logo">
        {%- if org.logo_data_uri %}
        <img src="{{ org.logo_data_uri | safe }}" alt="Логотип">
        {%- endif %}
      </div>
      <div class="orgName">
        {{ org.full_name | safe }}
        {%- if org.name %}<br />({{ org.name }}){%- endif %}
      </div>
      <div class="requisites">
        {%- if org.address %}<div>{{ org.address }}</div>{%- endif %}
        {%- if org.address_line2 %}<div>{{ org.address_line2 }}</div>{%- endif %}
        {%- if org.phone %}<div>Телефон {{ org.phone }}</div>{%- endif %}
        {%- if org.fax %}<div>Факс {{ org.fax }}</div>{%- endif %}
        {%- if org.email %}<div>E-mail: {{ org.email }}</div>{%- endif %}
        {%- if org.okpo %}<div>ОКПО {{ org.okpo }}{%- if org.ogrn %}, ОГРН {{ org.ogrn }}{%- endif %}</div>{%- endif %}
        {%- if org.inn %}<div>ИНН {{ org.inn }}{% if org.kpp %}, КПП {{ org.kpp }}{% endif %}</div>{%- endif %}
      </div>
    </div>
  (The reference file also has a dead commented-out duplicate "<!-- ...ОГРН... -->" line right
  after the ОКПО/ОГРН div — do NOT carry that comment into _header.html, it is dead code from the
  user's manual edit.)
-->

<!-- html_templates.rs current state (crates/trackly-app/src/pdf/html_templates.rs) — -->
<!-- DEFAULT_HTML_TEMPLATES (3 tuples today), KNOWN_LEGACY_DEFAULTS (3 filenames, 1-element -->
<!-- slice each, v20 only), upgrade_untouched_defaults_on_startup's trailing else-with-only-a- -->
<!-- comment branch — read the full file, it is short (~340 lines incl. tests). -->

<!-- crates/trackly-app/templates/_legacy_defaults/v20/ — existing sibling directory, exactly -->
<!-- 3 files (act_handover.html, act_acceptance.html, report.html), no README/index — v21 mirrors -->
<!-- this shape exactly. -->

<!-- crates/trackly-app/tests/html_page_parity.rs — full file, the closest existing analog for -->
<!-- the new html_header_parity.rs test file (same include_str! + regex-extraction style; NOTE -->
<!-- its extract_page_block regex works only because @page has no nested braces — the .header -->
<!-- block IS nested (.logo/.orgName/.requisites divs), so html_header_parity.rs must NOT reuse -->
<!-- that same nested-brace-unsafe pattern for the full .header block; the substring-only checks -->
<!-- in this plan's Task 3 sidestep that problem entirely by not trying to extract nested HTML). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rescue — create _header.html from the reference, privacy-scrubbed</name>
  <files>crates/trackly-app/templates/_header.html</files>
  <read_first>
    target/debug/templates/act_handover.html (IF STILL PRESENT — check with `ls -la
    target/debug/templates/` first; if absent or its mtime/size does not match "8248 bytes,
    2026-08-08 17:55", treat it as gone and use ONLY the privacy-scrubbed inventory in this
    plan's <interfaces> block and in 34-RESEARCH.md § "Эталон вёрстки" — do not attempt to
    reconstruct it from memory beyond those two sources)
    crates/trackly-app/templates/act_handover.html — current canon header block (~lines 47-61
    CSS, ~127-154 markup) and its doc-comment convention (lines 1-30), to match style/indentation
    .planning/phases/34-document-header/34-CONTEXT.md D-01 through D-14 (this task implements
    D-04, D-05, D-06, D-07, D-08, D-12, D-13 as markup/CSS; D-14 is implemented in Task 3)
  </read_first>
  <action>
    Create `crates/trackly-app/templates/_header.html` as a PARTIAL — no `<!DOCTYPE html>`, no
    `<html>`/`<head>`/`<body>` wrapper, just a `{#- ... -#}` Jinja doc-comment (adapted from the
    three main templates' convention: describe this file as the shared header partial rendering
    logo -> full legal name -> short name -> requisites, `{% include %}`-d by all three parent
    templates via the in-memory Environment registry per D-13 — never a filesystem loader; list
    its own context keys: `org.logo_data_uri`, `org.full_name`, `org.name`, `org.address`,
    `org.address_line2`, `org.phone`, `org.fax`, `org.email`, `org.okpo`, `org.ogrn`, `org.inn`,
    `org.kpp`; note the `org.logo_data_uri | safe` and `org.full_name | safe` exceptions to
    autoescape, mirroring the exact prose already used at the `logo_data_uri` use site for
    `full_name`'s D-03 threat-model comment), then a `<style>` block containing exactly the CSS
    from this plan's `<interfaces>` inventory above for `.header`, `.header .logo img`,
    `.header .orgName`, `.header .requisites`, PLUS (D-06, not present in the reference — add it
    fresh) `overflow-wrap: break-word; hyphens: auto; -webkit-hyphens: auto;` on a combined
    `.header .orgName, .header .requisites` selector, then the markup from `<interfaces>` above —
    literally: `<!-- HEADER-START -->` immediately before `<div class="header">`, the div tree
    exactly as shown (logo / orgName / requisites, with `org.full_name` and `org.name`
    independent per D-04, `org.name`'s real value replacing any hardcoded text — there must be NO
    hardcoded organization name anywhere in this file), then `<!-- HEADER-END -->` immediately
    after the closing `</div>` of `.header` (these two literal HTML comments are load-bearing:
    they survive rendering unlike `{#- -#}` Jinja comments, and Plan 34-03's render-gate test
    extracts the fragment between them via plain substring search, not regex, sidestepping the
    nested-`<div>` problem entirely). Requisites order/wording must match D-07 exactly:
    address -> address_line2 -> "Телефон X" -> "Факс X" -> "E-mail: X" -> "ОКПО X, ОГРН Y" (ОГРН
    nested inside the ОКПО `{%- if %}`, per the reference) -> "ИНН X, КПП Y". Do NOT carry over
    the reference's dead commented-out duplicate ОГРН line.
  </action>
  <acceptance_criteria>
    - `crates/trackly-app/templates/_header.html` exists, starts with `{#-` (Jinja comment, no
      `<!DOCTYPE`), and contains both `<!-- HEADER-START -->` and `<!-- HEADER-END -->` literal
      HTML comments, each exactly once.
    - File contains `width: 80mm`, `max-height: 80pt`, `max-width: 140pt`, `orgName`,
      `line-height: 1.25`, `overflow-wrap: break-word`, `hyphens: auto`.
    - File contains `{{ org.full_name | safe }}` and `{%- if org.name %}` as two textually
      separate, independently-guarded conditionals (grep confirms both patterns present, and that
      `org.name`'s conditional is not nested inside a condition on `org.full_name`).
    - `git diff --cached -- crates/trackly-app/templates/_header.html` (after staging) contains no
      Cyrillic text outside of: label words ("Телефон", "Факс", "E-mail", "ОКПО", "ОГРН", "ИНН",
      "КПП", "Логотип") and the doc-comment prose — i.e., no organization legal-name text. Run
      this check BEFORE committing, not after.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "HEADER-START\|HEADER-END" crates/trackly-app/templates/_header.html</automated>
  </verify>
  <done>
    _header.html exists with the ported geometry, D-04's independent conditionals, D-06's
    overflow guards, D-07's requisites order, and zero hardcoded organization name.
  </done>
</task>

<task type="auto">
  <name>Task 2: v21 snapshot (before editing) + rewrite the three canonical templates</name>
  <files>crates/trackly-app/templates/_legacy_defaults/v21/act_handover.html, crates/trackly-app/templates/_legacy_defaults/v21/act_acceptance.html, crates/trackly-app/templates/_legacy_defaults/v21/report.html, crates/trackly-app/templates/act_handover.html, crates/trackly-app/templates/act_acceptance.html, crates/trackly-app/templates/report.html</files>
  <read_first>
    crates/trackly-app/templates/_legacy_defaults/v20/ — directory listing, confirms the exact
    3-file shape v21 must mirror
    crates/trackly-app/templates/act_handover.html, act_acceptance.html, report.html — full
    current content of all three (their CURRENT state is exactly what v21 must snapshot)
  </read_first>
  <action>
    FIRST, before editing any of the three canonical files, copy their CURRENT on-disk content
    verbatim to `crates/trackly-app/templates/_legacy_defaults/v21/act_handover.html`,
    `.../v21/act_acceptance.html`, `.../v21/report.html` (byte-for-byte copies — e.g. `cp
    crates/trackly-app/templates/act_handover.html
    crates/trackly-app/templates/_legacy_defaults/v21/act_handover.html`, repeated for all three).
    This order is load-bearing (D-15/Pitfall 5): if the snapshot is taken AFTER the rewrite below,
    it captures the wrong (already-new) content and the upgrade-detection mechanism silently loses
    its ability to recognize pre-Phase-34 installs. THEN rewrite each of the three canonical
    files: (a) delete the `.header`/`.logo img`/`.requisites` CSS rules from each file's
    `<style>` block (they now live exclusively in `_header.html`, per D-12); (b) delete the
    `<div class="header">...</div>` markup block from each file's `<body>` and replace it with a
    single line `{% include "_header.html" %}` at that exact location; (c) change `body {
    font-family: ...; font-size: ...; }` in all three files to `font-family: "Times New Roman",
    Georgia, "PT Serif", "Liberation Serif", "DejaVu Serif", serif; font-size: 12pt;` (D-09/D-10 —
    report.html was 10.5pt, act_handover/act_acceptance were 11pt with a `sans-serif` tail; all
    three become this exact serif chain at 12pt); (d) unify the top-level document-title font
    size to 14pt in all three: `act_handover.html`'s `.title` stays 14pt (no change),
    `act_acceptance.html`'s `h1` stays 14pt (no change — it already matches), `report.html`'s
    `.title` changes from 15pt to 14pt (D-11 — do not rename `report.html`'s `.title` class or
    restructure `act_acceptance.html`'s `h1`/`h2` into `.title`/`.subtitle`; that markup
    restructuring is out of scope, Phase 35/36 territory — only the numeric font-size is
    unified). Update each file's opening doc-comment "Context variables" list to add
    `org.full_name` to the `org.*` enumeration, and add one sentence noting the header block now
    lives in `_header.html` via `{% include %}` rather than being inlined.
  </action>
  <acceptance_criteria>
    - `diff crates/trackly-app/templates/_legacy_defaults/v21/act_handover.html
      crates/trackly-app/templates/act_handover.html` is NON-empty (proves the snapshot captured
      the OLD content, not the just-rewritten new content) — repeat for act_acceptance.html and
      report.html.
    - None of the three canonical templates contains the string `class="header"` followed by a
      `grid-template-columns` CSS rule any more (`grep -L "grid-template-columns" ...` — the old
      2-column grid CSS must be gone from all three).
    - All three canonical templates contain the literal substring `{% include "_header.html" %}`
      exactly once.
    - All three canonical templates' `body {` rule contains `"Times New Roman"` and `font-size:
      12pt`.
    - `cargo build -p trackly-app` still succeeds (template `.html` files are not compiled by
      rustc — only `include_str!`'d by `html_templates.rs`, which Task 3 updates; this build
      check just confirms nothing else broke).
  </acceptance_criteria>
  <verify>
    <automated>set -o pipefail; for f in act_handover.html act_acceptance.html report.html; do diff -q "crates/trackly-app/templates/_legacy_defaults/v21/$f" "crates/trackly-app/templates/$f" >/dev/null && { echo "FAIL: v21/$f identical to rewritten canon -- snapshot was taken after rewrite (D-15/Pitfall 5)" >&2; exit 1; }; done; test "$(grep -l "include \"_header.html\"" crates/trackly-app/templates/act_handover.html crates/trackly-app/templates/act_acceptance.html crates/trackly-app/templates/report.html | wc -l)" -eq 3</automated>
  </verify>
  <done>
    v21 snapshots exist and differ from the now-rewritten canon; all three canonical templates
    include _header.html instead of duplicating header markup/CSS, and share the D-09/D-10/D-11
    typography.
  </done>
</task>

<task type="auto">
  <name>Task 3: html_templates.rs registration + D-16 warn branch + structural tests</name>
  <files>crates/trackly-app/src/pdf/html_templates.rs, crates/trackly-app/tests/html_header_parity.rs</files>
  <read_first>
    crates/trackly-app/src/pdf/html_templates.rs — full file (DEFAULT_HTML_TEMPLATES,
    KNOWN_LEGACY_DEFAULTS, the doc-comment "Extension point" instruction directly above
    KNOWN_LEGACY_DEFAULTS, upgrade_untouched_defaults_on_startup's trailing `// else:` comment,
    and the existing #[cfg(test)] mod tests conventions incl.
    upgrade_replaces_untouched_legacy_default_with_current_bundled_body which currently uses
    `.first()` — always v20)
    crates/trackly-app/tests/html_page_parity.rs — full file (include_str! + #[test] style to
    mirror for the new file's header/imports)
  </read_first>
  <action>
    In `html_templates.rs`: add `("_header.html", include_str!("../../templates/_header.html")),`
    as a fourth tuple to `DEFAULT_HTML_TEMPLATES` (after `report.html`). Add
    `include_str!("../../templates/_legacy_defaults/v21/act_handover.html")` /
    `.../v21/act_acceptance.html` / `.../v21/report.html` as a SECOND element (not a replacement)
    of each respective filename's slice in `KNOWN_LEGACY_DEFAULTS` — `_header.html` gets NO entry
    in `KNOWN_LEGACY_DEFAULTS` (new file, nothing to upgrade from; a missing file is
    `materialize_defaults_on_startup`'s job). Replace the trailing `// else: user-customized ...`
    comment inside `upgrade_untouched_defaults_on_startup`'s `if legacy_bodies.iter().any(...) {
    ... }` block with an actual `else { tracing::warn!("Skipped auto-upgrade of {} — on-disk
    content matches neither current default nor any known legacy default (user-customized);
    manual review needed if a header/layout upgrade was expected.", path.display()); }` (message
    wording is Claude's Discretion per D-16 but MUST interpolate `path.display()` and MUST say the
    upgrade was skipped because the file looks user-customized). Add a new `#[test]` in the
    existing test module, `upgrade_replaces_v21_legacy_default_with_current_bundled_body`, that —
    unlike the existing `.first()`-based test — explicitly pulls index `1` (the v21 element) from
    each filename's `KNOWN_LEGACY_DEFAULTS` slice, and FIRST asserts via `assert_ne!` that this v21
    body is NOT EQUAL to the current `DEFAULT_HTML_TEMPLATES` body for that same filename (a
    mandatory precondition guard: if Task 2's snapshot had been taken AFTER the rewrite instead of
    before — D-15/Pitfall 5 — v21 would already equal current and the upgrade assertion below would
    pass trivially without ever exercising a real upgrade; this precondition makes that failure mode
    impossible to pass unnoticed), THEN writes the v21 body to a tempdir, runs
    `upgrade_untouched_defaults_on_startup`, and asserts the result equals the current
    `DEFAULT_HTML_TEMPLATES` body — proving the NEW v21 slice element specifically (not just
    `.first()`/v20) drives a real upgrade. Then create
    `crates/trackly-app/tests/html_header_parity.rs` (new file, modeled on
    `html_page_parity.rs`'s `include_str!` + plain-`#[test]` style, no tokio needed for these two
    tests): Test 1 `all_three_templates_include_header_partial` — asserts
    `ACT_HANDOVER_HTML.contains("{% include \"_header.html\" %}")` and the same for
    `ACT_ACCEPTANCE_HTML`/`REPORT_HTML` (three separate `assert!` calls with distinct messages).
    Test 2 `header_partial_org_name_node_has_no_hardcoded_literal` (DOC-05, privacy-safe positive
    form — this test itself MUST NOT contain the real organization name) — `include_str!` the
    `_header.html` constant, regex-extract the `<div class="orgName">...</div>` fragment
    non-greedy (safe here — unlike `.header`, `.orgName`'s own children are only `{{ }}`/`{% %}`/
    `<br />`/`(`/`)`, no nested `<div>`), then strip every `{{ ... }}` and `{% ... %}` span via
    regex replace, then assert the remainder — after also stripping whitespace, `<br`, `/>`, `(`,
    `)` — contains no `\p{L}` (Unicode letter) character. This proves the org-name node holds no
    hardcoded name text without ever writing the real name into the test file.
  </action>
  <acceptance_criteria>
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib html_templates --
      --test-threads=1` passes, including the new v21-specific upgrade test (4 tests total in
      that module now: materialize x2, upgrade x3 incl. new v21 one, load_template x2, resolve x1
      — exact count not asserted, just confirm no regressions and the new test is present and
      green).
    - `upgrade_replaces_v21_legacy_default_with_current_bundled_body` contains an `assert_ne!`
      comparing each filename's v21 `KNOWN_LEGACY_DEFAULTS[1]` body against its current
      `DEFAULT_HTML_TEMPLATES` body, executed BEFORE the upgrade-result assertion — the
      precondition guard against a mistimed/corrupted v21 snapshot making the test pass without
      exercising a real upgrade.
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_header_parity
      -- --test-threads=1` passes both new tests.
    - `grep -n "tracing::warn!" crates/trackly-app/src/pdf/html_templates.rs` finds a match inside
      `upgrade_untouched_defaults_on_startup`.
    - `DEFAULT_HTML_TEMPLATES` in `html_templates.rs` has exactly 4 tuples (`grep -c
      "include_str!" crates/trackly-app/src/pdf/html_templates.rs` — expect 4 in
      DEFAULT_HTML_TEMPLATES plus 6 in KNOWN_LEGACY_DEFAULTS = 10 total `include_str!` calls in
      this file after this task).
    - `cargo test -p trackly-app --test html_header_parity 2>&1 | grep -v "<реальное"` — sanity
      check that the test SOURCE FILE itself (not just its output) contains no real organization
      name; run `grep -i "<real org name known to the executor>"
      crates/trackly-app/tests/html_header_parity.rs` and confirm zero matches before committing.
  </acceptance_criteria>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib html_templates --test html_header_parity -- --test-threads=1</automated>
  </verify>
  <done>
    _header.html materializes/upgrades like the three main files; skipped upgrades are now
    logged; the substring gate and the privacy-safe structural test are both green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `target/debug/templates/act_handover.html` (hand-edited, contains the real org name) → `crates/trackly-app/templates/_header.html` (public repo, committed) | The single highest-risk boundary in this plan — a naive whole-file copy would commit the real organization name to a public repository, in git history forever. |
| `crates/trackly-app/templates/*.html` (shipped defaults, public repo) → any Trackly install's `templates/` directory | Existing boundary (Phase 16/20), unchanged in kind by this plan — this plan only adds a fourth file and a second legacy-snapshot generation to the same mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-02-01 | Information Disclosure | `_header.html` authoring (Task 1) | mitigate | Task 1's action explicitly requires copying ONLY structure/CSS from the reference, manually substituting `{{ org.full_name \| safe }}` + `{%- if org.name %}` for the hardcoded literal name — never a whole-file copy. Task 1's acceptance criteria include a mandatory pre-commit grep for the real name (known to the executor from reading the reference file, never written into any plan artifact). Task 3's new `header_partial_org_name_node_has_no_hardcoded_literal` test is a permanent regression guard for this same property. |
| T-34-02-02 | Repudiation / regression | v21 snapshot timing (Task 2) | mitigate | Task 2's action mandates the `cp` snapshot step BEFORE any edit to the three canonical files, and its acceptance criteria assert `diff v21/x.html x.html` is non-empty post-rewrite — catching the Pitfall-5 failure mode (snapshot taken after rewrite, silently useless) at task-verification time rather than discovering it only when a real upgrade silently fails to fire for an existing install. |
| T-34-02-03 | Tampering | `{% include "_header.html" %}` resolution | accept | This plan only writes the include directive into the three templates' text; the actual registration mechanism (which determines whether the include resolves safely from the in-memory registry vs. attempting a filesystem load) is Plan 34-03's responsibility — D-13 (never call `env.set_loader`) is unchanged code in `minijinja_env.rs`, not touched by this plan at all. |
| T-34-02-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependency — the new `tests/html_header_parity.rs` uses only `regex` (already a normal, non-dev dependency of `trackly-app`, confirmed in `Cargo.toml`, already used by `tests/html_page_parity.rs`). |
</threat_model>

<verification>
- `cargo build -p trackly-app` succeeds (compile-only check; template `.html` text changes and
  `html_templates.rs` const-array additions do not require any other Rust file to change).
- `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib html_templates --test
  html_header_parity -- --test-threads=1` passes.
- Do NOT run the full `cargo test -p trackly-app` suite as a gate for this plan — pre-existing
  HTML-rendering tests are expected to fail until Plan 34-03 lands (documented above). Full-suite
  green is Plan 34-03/34-06's gate, not this plan's.
</verification>

<success_criteria>
- The hand-edited reference geometry is permanently captured in version control, privacy-scrubbed,
  independent of whether `target/debug/` survives to execution time.
- All three canonical templates share one physical source of header markup/CSS (D-12) — a future
  edit to `_header.html` changes all three simultaneously, structurally guaranteeing DOC-04.
- `_legacy_defaults/v21/` proves an existing v1.3 install (pre-Phase-34) will be recognized as
  "untouched" and eligible for the D-06 upgrade mechanism once Plan 34-03 makes rendering work
  again.
</success_criteria>

<output>
Create `.planning/phases/34-document-header/34-02-SUMMARY.md` when done
</output>
