---
phase: 34-document-header
plan: 04
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - ui/src/features/settings/OrgSettings.svelte
  - ui/src/features/settings/TemplateEditor.svelte
autonomous: true
requirements: [DOC-05]

must_haves:
  truths:
    - "D-02: OrgSettings.svelte's new 'Полное юридическое наименование' field is rendered with the shared Textarea.svelte component (props: value, id, rows, oninput) — not a raw <textarea> and not Input.svelte — so the user, not the browser, controls where line breaks fall in the value sent to settings_save_org_fields, matching the multiline-by-design intent of D-02."
    - "D-01/DOC-05: OrgSettings.svelte's local OrgSettingsDto TypeScript interface and the settings_save_org_fields payload both carry full_name: string alongside the existing org_name, address_line2, etc. — loadOrg() populates it from dto.full_name and saveOrg() sends patch.full_name back, completing the round trip to the Plan 34-01 backend field."
  artifacts: []
  key_links:
    - from: "ui/src/features/settings/OrgSettings.svelte::saveOrg"
      to: "crates/trackly-app/src/dto/reports.rs::OrgPatch.full_name"
      via: "apiCall('settings_save_org_fields', { patch: { ..., full_name: fullName } })"
      pattern: "full_name: fullName"
---

<objective>
Add the D-02 multiline "Полное юридическое наименование" field to the Settings → Организация
form, wired to the `full_name` DB column Plan 34-01 already added, using the project's shared
`Textarea.svelte` component per the recurring design-system feedback (raw HTML controls have been
flagged repeatedly across prior UI phases — see project memory). Also register `org.full_name` as
a documented variable in the TemplateEditor's per-kind variable list, since `_header.html` (Plan
34-02) is now `{% include %}`-d by all three document kinds and references this key.

Purpose: give the user a way to actually populate the field that Plans 34-02/34-03 render.
Output: `OrgSettings.svelte` gains a `fullName` state field wired through load/save/markup;
`TemplateEditor.svelte`'s `VARIABLES_BY_KIND` documents `org.full_name` for all three kinds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-document-header/34-CONTEXT.md
@.planning/phases/34-document-header/34-PATTERNS.md

<interfaces>
<!-- Textarea.svelte (ui/src/lib/components/Textarea.svelte) — full component: -->
<!--   Props: value: string (NOT $bindable in this component's own type; the usage convention -->
<!--   elsewhere in this codebase is `value={x} oninput={(v) => (x = v)}`, one-way + callback, -->
<!--   NOT bind:value — follow that convention, do not attempt bind:value on Textarea), -->
<!--   placeholder?, disabled?, invalid?, id?, rows? (default 3), oninput?: (v: string) => void -->
<!-- Usage precedent (ui/src/features/cartridges/CartridgeFormBody.svelte:324-332): -->
<!--   <div class="field"> -->
<!--     <label class="label" for="cart-notes">Примечания</label> -->
<!--     <Textarea value={notes} placeholder="Необязательно" id="cart-notes" -->
<!--       oninput={(v) => (notes = v)} /> -->
<!--   </div> -->

<!-- OrgSettings.svelte CURRENT shape (full file already read during planning) — the -->
<!-- addressLine2 field is the exact 4-touchpoint sibling to mirror: -->
<!--   1. `let addressLine2 = $state('');` (line ~27) -->
<!--   2. `interface OrgSettingsDto { ...; address_line2: string; }` (lines 9-21) -->
<!--   3. loadOrg(): `addressLine2 = dto.address_line2;` (line ~59) -->
<!--   4. saveOrg() payload: `address_line2: addressLine2,` (line ~113) -->
<!--   5. markup: `<div class="form-field form-field--full"><label class="form-label" -->
<!--      for="org-address-line2">Адрес (2-я строка)</label><Input id="org-address-line2" -->
<!--      type="text" bind:value={addressLine2} placeholder="офис 305, корпус 2" /></div>` -->
<!--      (lines 260-268) — for full_name, this same wrapper div/label pattern is reused but the -->
<!--      control is Textarea, not Input. -->

<!-- TemplateEditor.svelte::VARIABLES_BY_KIND (lines ~33-90ish) — three arrays keyed -->
<!-- "act_handover" / "act_acceptance" / "report", each a VariableEntry[] with {code, desc} -->
<!-- objects; org.name / org.inn / etc. already present in all three; org.full_name must be -->
<!-- added to all three (not just act_handover) since _header.html is shared across all kinds. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fullName field in OrgSettings.svelte (D-02, shared Textarea)</name>
  <files>ui/src/features/settings/OrgSettings.svelte</files>
  <read_first>
    ui/src/features/settings/OrgSettings.svelte — full file, current state
    ui/src/lib/components/Textarea.svelte — full component (props, one-way value + oninput
    convention — confirm it is NOT bind:value before writing the markup)
    ui/src/features/cartridges/CartridgeFormBody.svelte lines ~320-335 (Textarea usage precedent)
  </read_first>
  <action>
    Add `import Textarea from '$lib/components/Textarea.svelte';` to the `<script>` block imports.
    Add `full_name: string;` as the last field of the local `interface OrgSettingsDto`. Add `let
    fullName = $state('');` alongside the existing `addressLine2` state declaration. In `loadOrg`,
    add `fullName = dto.full_name;` alongside `addressLine2 = dto.address_line2;`. In `saveOrg`'s
    `apiCall<void>('settings_save_org_fields', { patch: { ... } })` payload, add `full_name:
    fullName,` alongside the existing `address_line2: addressLine2,` line. In the markup, add a
    new `<div class="form-field form-field--full">` block immediately after the existing
    `org-address-line2` field's closing `</div>` and before the `org-phone` field, containing a
    `<label class="form-label" for="org-full-name">Полное юридическое наименование</label>`
    followed by `<Textarea id="org-full-name" value={fullName} rows={3} placeholder={'Общество с
    ограниченной ответственностью\n«Название»'} oninput={(v) => (fullName = v)} />` — reuses the
    existing `.form-field.form-field--full`/`.form-label` classes already defined in this file's
    `<style lang="scss">` block, no new CSS needed.
  </action>
  <acceptance_criteria>
    - `grep -c "fullName" ui/src/features/settings/OrgSettings.svelte` returns at least 5 (state
      decl, interface field reference via dto.full_name, load, save payload, markup usage).
    - `grep -c "Textarea" ui/src/features/settings/OrgSettings.svelte` returns at least 2 (import
      + usage).
    - `grep -c "<textarea" ui/src/features/settings/OrgSettings.svelte` returns 0 — no raw
      `<textarea>` element was introduced (the shared component wraps it).
    - `pnpm --dir ui exec svelte-check --output human 2>&1 | grep -i "OrgSettings.svelte"` shows
      no new type errors attributable to this change (pre-existing unrelated errors, if any, are
      out of scope).
  </acceptance_criteria>
  <verify>
    <automated>OUT=$(pnpm --dir ui exec svelte-check --output human 2>&1); rc=$?; [ $rc -eq 0 ] || { echo "$OUT" >&2; exit 1; }; ! printf '%s\n' "$OUT" | grep -q "OrgSettings.svelte.*Error"</automated>
  </verify>
  <done>
    OrgSettings.svelte has a working "Полное юридическое наименование" multiline field using the
    shared Textarea component, round-tripping through settings_get_org / settings_save_org_fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document org.full_name in TemplateEditor's variable list (all 3 kinds)</name>
  <files>ui/src/features/settings/TemplateEditor.svelte</files>
  <read_first>
    ui/src/features/settings/TemplateEditor.svelte — VARIABLES_BY_KIND full definition (all three
    `act_handover` / `act_acceptance` / `report` arrays)
  </read_first>
  <action>
    Add `{ code: 'org.full_name', desc: 'полное юридическое наименование (многострочное)' },`
    immediately after the existing `{ code: 'org.name', desc: 'название организации' },` entry in
    ALL THREE kind arrays (`act_handover`, `act_acceptance`, `report`) — `_header.html` is shared
    across all three document kinds, so every kind's variable list must document the key, not just
    `act_handover`'s (which is the only array that currently lists most `org.*` fields;
    `act_acceptance` and `report` also each have their own `org.name` entry to mirror).
  </action>
  <acceptance_criteria>
    - `grep -c "org.full_name" ui/src/features/settings/TemplateEditor.svelte` returns exactly 3
      (one per kind array).
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c "org.full_name" ui/src/features/settings/TemplateEditor.svelte) -eq 3</automated>
  </verify>
  <done>
    org.full_name is documented as an available variable in all three TemplateEditor kind lists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI free-text input (this plan) → `settings_save_org_fields` → `org_settings.full_name` | Client-side; no escaping/validation performed in the browser — the client is not a trust boundary enforcement point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Tampering | `fullName` textarea value | transfer | This plan performs NO client-side escaping or sanitization of the value — that is intentional; the security-relevant HTML-escape-then-`<br>` transform is server-side only (Plan 34-01's `org_full_name_html`, wired in Plan 34-03). The client's only job is faithfully transmitting whatever the authenticated `ManageSettings` user typed, unchanged, to `settings_save_org_fields` (which already enforces `authorize(caller, &Action::ManageSettings)` server-side, unchanged by this plan). |
| T-34-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installed — `Textarea.svelte` is an existing, already-used shared component. |
</threat_model>

<verification>
- `pnpm --dir ui exec svelte-check` shows no new errors attributable to `OrgSettings.svelte` or
  `TemplateEditor.svelte`.
- `pnpm --dir ui build` succeeds (required later by Plan 34-06's LAN-browser UAT anyway — cheap to
  confirm now).
</verification>

<success_criteria>
- A user can type a multi-line legal name into Settings → Организация, save it, reload the page,
  and see it persisted — using the project's shared design-system `Textarea` component.
- `org.full_name` is discoverable in the TemplateEditor's variable reference for every document
  kind that can reference it.
</success_criteria>

<output>
Create `.planning/phases/34-document-header/34-04-SUMMARY.md` when done
</output>
