---
phase: 34-document-header
plan: 03
type: execute
wave: 2
depends_on: ["34-01", "34-02"]
files_modified:
  - crates/trackly-app/src/pdf/minijinja_env.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/src/services/report_service.rs
  - crates/trackly-app/src/services/template_service.rs
  - crates/trackly-app/tests/html_report_render.rs
  - crates/trackly-app/tests/html_header_parity.rs
  - crates/trackly-app/tests/pdf_render_act.rs
autonomous: true
requirements: [DOC-04, DOC-05]

must_haves:
  truths:
    - "D-13: the header partial is resolved purely through render_with_timeout's env.add_template_owned calls (both the partial and the main template) happening BEFORE env.get_template/tmpl.render — env.set_loader is still never called anywhere in minijinja_env.rs (grep confirms zero occurrences)."
    - "C-03: the header source passed into render_with_timeout's extra_templates argument is always the result of html_templates::load_template(templates_dir, \"_header.html\", embedded_default) — never a raw std::fs::read_to_string — so a user-deleted _header.html degrades to the embedded default instead of making every render fail."
    - "D-03 (wired): act_service.rs's render_pdf and render_acceptance_pdf, report_service.rs's export_pdf, and template_service.rs's demo_context_for_kind ALL pass org.full_name through org_full_name_html (the Plan 34-01 helper) before it reaches the _header.html \"| safe\" interpolation — none of the four render paths interpolates the raw multi-line string directly."
    - "C-04: the doc-comment at the top of build_safe_html_env in minijinja_env.rs no longer claims 'No | safe filter is used anywhere in the shipped templates' — it now names both sanctioned exceptions (org.logo_data_uri, org.full_name) and states the invariant that | safe is permitted only for values escaped/assembled exclusively server-side from non-user-HTML input."
    - "Privacy fix: template_service.rs's demo_context_for_kind org block no longer contains the pre-existing real phone/fax/OKPO/OGRN values found in the current source at the start of this plan — every requisite value in that function's org block is fictional."
    - "DOC-04 (render-gate): rendering act_handover.html, act_acceptance.html, and report.html through render_with_timeout with the same demo org context produces byte-identical <!-- HEADER-START -->...<!-- HEADER-END --> fragments across all three outputs."
  artifacts:
    - path: "crates/trackly-app/tests/html_header_parity.rs"
      provides: "render-gate test (Test 3): actual render of all 3 forms, header fragment equality assertion, extending Plan 34-02's substring+privacy tests"
  key_links:
    - from: "crates/trackly-app/src/services/act_service.rs::render_pdf"
      to: "crates/trackly-app/src/pdf/minijinja_env.rs::org_full_name_html"
      via: "ctx[\"org\"][\"full_name\"] = org_full_name_html(&org_dto.full_name)"
      pattern: "org_full_name_html\\(&org_dto\\.full_name\\)"
    - from: "crates/trackly-app/src/pdf/minijinja_env.rs::render_with_timeout"
      to: "crates/trackly-app/src/pdf/html_templates.rs::load_template"
      via: "extra_templates: &[(\"_header.html\", &header_src)] where header_src = load_template(templates_dir, \"_header.html\", embedded_header_default)"
      pattern: "load_template\\(.*\"_header\\.html\""
---

<objective>
Make the `_header.html` contract that Plan 34-02 established actually work: extend
`render_with_timeout` to register the header partial alongside the main template (D-13/Pitfall 4
— both `add_template_owned` calls must land before `get_template`/`render`), then wire
`org.full_name` (via Plan 34-01's `org_full_name_html` escape helper) into all FOUR render
contexts that `UndefinedBehavior::Strict` requires it in: `act_service.rs`'s `render_pdf` and
`render_acceptance_pdf`, `report_service.rs`'s `export_pdf`, and `template_service.rs`'s
`demo_context_for_kind` preview stub. This is the plan that turns the full pre-existing HTML
render test suite (broken by Plan 34-02's template rewrite, by design) back to green — that is
this plan's primary gate, not merely a nice-to-have.

Also fixes the pre-existing privacy leak this plan's own touch of `demo_context_for_kind`
otherwise perpetuates (CLAUDE.md privacy constraint (c)): the function's "demo" org block
currently holds REAL phone/fax/OKPO/OGRN values, already committed to git history since Phase 15
(`6ad0202`) — since this plan edits that exact function anyway to add `full_name`, replacing the
real values with fictional ones in the same touch is in-scope and low-cost.

Purpose: restore a fully working, fully tested render pipeline where all three document types
render an identical, correctly-escaped organization header.
Output: `minijinja_env.rs`'s `render_with_timeout` accepts partials; all 4 context-assembly sites
supply `org.full_name`; the render-gate structural test proves cross-form header identity; the
full pre-existing HTML render test suite is green again.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-document-header/34-CONTEXT.md
@.planning/phases/34-document-header/34-RESEARCH.md

<interfaces>
<!-- render_with_timeout CURRENT signature (crates/trackly-app/src/pdf/minijinja_env.rs:68-107): -->
<!--   pub async fn render_with_timeout(env: &Environment<'static>, name: &str, -->
<!--     template_src: &str, ctx: serde_json::Value) -> Result<String, AppError> -->
<!-- Body: env.clone() -> name.to_owned() -> template_src.to_owned() -> spawn_blocking(move || { -->
<!--   env.add_template_owned(name_owned.clone(), template_src_owned)?; -->
<!--   let tmpl = env.get_template(&name_owned)?; tmpl.render(ctx) }) -->
<!-- 4 call sites, ALL of which must move to the new signature: -->
<!--   act_service.rs render_pdf (~line 2653), render_acceptance_pdf (~line 2801) -->
<!--   report_service.rs export_pdf (~line 657) -->
<!--   template_service.rs validate_preview (~line 341) -->
<!-- 4 existing #[tokio::test]s inside minijinja_env.rs itself (env_renders_simple_template, -->
<!-- env_rejects_undefined, env_render_timeout_returns_validation, env_rejects_parse_error) all -->
<!-- call render_with_timeout with the CURRENT 4-arg signature and must be updated to pass a 5th -->
<!-- arg of &[] (empty slice — these tests exercise build_safe_env's JSON path, not HTML, no -->
<!-- partial needed). -->

<!-- act_service.rs's fallback-DTO struct literal (None arm, both render_pdf ~line 2542 and -->
<!-- render_acceptance_pdf ~line 2693) constructs OrgSettingsDto { org_name, inn, kpp, address, -->
<!-- has_logo: false, phone/fax/email/okpo/ogrn: String::new(), address_line2: String::new() } — -->
<!-- MUST gain full_name: String::new() or this file will not compile after Plan 34-01's DTO -->
<!-- field addition (Plan 34-01 did not touch act_service.rs). -->

<!-- report_service.rs has its OWN #[cfg(test)] mod's empty_org() test helper (~line 1409) -->
<!-- constructing OrgSettingsDto directly — also needs full_name: String::new(). -->
<!-- tests/html_report_render.rs has a SEPARATE, textually-identical empty_org() helper (~line 65) -->
<!-- — also needs full_name: String::new(). Both are independent copies, both must be fixed. -->

<!-- html_templates.rs::DEFAULT_HTML_TEMPLATES (Plan 34-02 output) now has "_header.html" as its -->
<!-- 4th tuple — use `.find(|(f, _)| *f == "_header.html")` the same way the existing 4 call sites -->
<!-- already look up their own main-template embedded_default, e.g.: -->
<!--   let embedded_header_default = crate::pdf::html_templates::DEFAULT_HTML_TEMPLATES -->
<!--     .iter().find(|(f, _)| *f == "_header.html").map(|(_, body)| *body).unwrap_or(""); -->
<!--   let header_src = crate::pdf::html_templates::load_template( -->
<!--     &templates_dir, "_header.html", embedded_header_default); -->

<!-- crates/trackly-app/templates/_header.html (Plan 34-02 output) wraps its header block in -->
<!-- literal <!-- HEADER-START --> / <!-- HEADER-END --> HTML comments — these survive rendering -->
<!-- (they are NOT Jinja {#- -#} comments), so the render-gate test can find them via a plain -->
<!-- string search in the rendered output. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: render_with_timeout accepts extra partials (D-13) + C-04 doc fix</name>
  <files>crates/trackly-app/src/pdf/minijinja_env.rs</files>
  <read_first>
    crates/trackly-app/src/pdf/minijinja_env.rs — full file, current state after Plan 34-01's
    Task 3 addition of org_full_name_html (already present; do not duplicate it)
    .planning/phases/34-document-header/34-RESEARCH.md section "Регистрация partial'а вторым
    шаблоном (D-13/D-14/C-03)" — exact lifetime/ownership reasoning for why extra_templates must
    be converted to owned Strings before entering the spawn_blocking closure
  </read_first>
  <behavior>
    - render_with_timeout(env, "main", "{% include \"partial\" %}Hi {{ name }}", ctx, &[("partial",
      "PARTIAL-")]) renders "PARTIAL-Hi <value>" — proves both templates are registered before
      render and the include resolves.
    - render_with_timeout(env, "main", "{% include \"missing\" %}", ctx, &[]) returns
      Err(AppError::Validation) with a message mentioning the render/template error — proves a
      missing partial fails cleanly (not a panic) when extra_templates is empty.
    - The 4 pre-existing tests (env_renders_simple_template, env_rejects_undefined,
      env_render_timeout_returns_validation, env_rejects_parse_error) still pass with &[] as the
      new 5th argument.
  </behavior>
  <action>
    Change `render_with_timeout`'s signature to `pub async fn render_with_timeout(env: &Environment
    <'static>, name: &str, template_src: &str, ctx: serde_json::Value, extra_templates: &[(&str,
    &str)]) -> Result<String, AppError>`. Before the `spawn_blocking` call, convert
    `extra_templates` into `let extra_owned: Vec<(String, String)> = extra_templates.iter().map(
    |(n, s)| (n.to_string(), s.to_string())).collect();` (borrowed `&str` cannot cross the `move`
    closure's `'static` bound required by `spawn_blocking`, exactly like `template_src.to_owned()`
    already does for the main template). Move `extra_owned` into the closure. Inside the closure,
    BEFORE the existing `env.add_template_owned(name_owned.clone(), template_src_owned)` call, add
    a loop registering every extra: `for (extra_name, extra_src) in extra_owned { env
    .add_template_owned(extra_name, extra_src).map_err(|e| AppError::Validation { field:
    "template".into(), message: format!("Template parse error: {e}") })?; }` (call order between
    extras and the main template does not matter per RESEARCH — MiniJinja resolves `{% include %}`
    at render time, not registration time — but both MUST land before `env.get_template`/
    `tmpl.render`). Update all 4 pre-existing `#[tokio::test]`s in this file's `mod tests` to pass
    `&[]` as the 5th argument. Fix the module's C-04 doc-comment (the paragraph ending "No `|
    safe` filter is used anywhere in the shipped templates.") to instead read something like: "The
    sanctioned `| safe` exceptions are `org.logo_data_uri` (server-constructed base64 + mime
    whitelist) and `org.full_name` (server-side `org_full_name_html`-escaped before `<br>`
    insertion) — `| safe` is permitted ONLY for values escaped or assembled exclusively
    server-side from non-user-HTML input; never for raw user/device/org field text." (exact wording
    is Claude's Discretion, but MUST name both exceptions and state that invariant explicitly).
  </action>
  <acceptance_criteria>
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib pdf::minijinja_env --
      --test-threads=1` passes all tests (4 pre-existing + org_full_name_html's 4 from Plan 34-01
      + any new ones from this task's `<behavior>`).
    - `grep -c "set_loader" crates/trackly-app/src/pdf/minijinja_env.rs` returns 0 (D-13 invariant
      unchanged — only mentioned in comments, never called).
    - `grep -n "No | safe filter is used anywhere" crates/trackly-app/src/pdf/minijinja_env.rs`
      returns no match (the stale sentence is gone).
  </acceptance_criteria>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib pdf::minijinja_env -- --test-threads=1</automated>
  </verify>
  <done>
    render_with_timeout accepts and registers extra partials before rendering; all pre-existing
    and new tests in this file pass; the C-04 doc-comment is accurate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire org.full_name + _header.html into all 4 render call sites (+ privacy fix)</name>
  <files>crates/trackly-app/src/services/act_service.rs, crates/trackly-app/src/services/report_service.rs, crates/trackly-app/src/services/template_service.rs, crates/trackly-app/tests/html_report_render.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs lines ~2530-2660 (render_pdf: fallback DTO
    literal, ctx["org"] assembly, render_with_timeout call) and ~2670-2810 (render_acceptance_pdf,
    same shape)
    crates/trackly-app/src/services/report_service.rs lines ~595-664 (export_pdf: ctx["org"]
    assembly, render_with_timeout call) and its own #[cfg(test)] mod's empty_org() helper
    crates/trackly-app/src/services/template_service.rs lines ~325-410 (validate_preview,
    demo_context_for_kind — READ THE ACTUAL CURRENT VALUES in the org block yourself; this
    plan's <read_first> deliberately does not repeat them, per the project privacy constraint —
    they are real requisites, not fictional, and must never be copied into any planning artifact)
    crates/trackly-app/tests/html_report_render.rs — its own empty_org() helper (~line 65)
  </read_first>
  <action>
    In `act_service.rs`, BOTH `render_pdf` and `render_acceptance_pdf`: (1) add `full_name:
    String::new(),` to the fallback `OrgSettingsDto { ... }` struct literal (None arm); (2) in the
    `ctx["org"]` assembly, add `"full_name": crate::pdf::minijinja_env::org_full_name_html(&org_dto
    .full_name),` alongside the existing `"logo_data_uri": logo_data_uri,` line; (3) look up
    `embedded_header_default` from `crate::pdf::html_templates::DEFAULT_HTML_TEMPLATES` for
    `"_header.html"` (mirroring the existing `embedded_default` lookup for the main template just
    above it) and load `header_src` via `crate::pdf::html_templates::load_template(&templates_dir,
    "_header.html", embedded_header_default)`; (4) change the `render_with_timeout(...)` call to
    pass a 5th argument `&[("_header.html", &header_src)]`. Apply the identical 4-part change to
    `report_service.rs`'s `export_pdf` (using `org.full_name` instead of `org_dto.full_name` — this
    file's local binding is named `org`, confirm via `<read_first>`), and fix its `#[cfg(test)]
    mod`'s `empty_org()` helper by adding `full_name: String::new(),`. In `template_service.rs`'s
    `validate_preview`, add the same `header_src`/`extra_templates` wiring to its
    `render_with_timeout` call. In `demo_context_for_kind`'s shared `org` block (used by ALL THREE
    `match kind` arms): (a) add `"full_name": crate::pdf::minijinja_env::org_full_name_html(
    "Общество с ограниченной ответственностью\n«Демо Организация»"),` (a fictional two-line demo
    value, matching D-02's multiline intent — NEVER the real organization name); (b) replace every
    OTHER field in that same `org` block (`phone`, `fax`, `okpo`, `ogrn` — and check `email`/
    `address_line2` too) with clearly fictional values (e.g. reuse the `+7 495 ...`-style Moscow
    fictional numbers and `770...`-style fictional OKPO/OGRN already used elsewhere in this
    codebase's tests, such as `tests/org_settings.rs`'s `"+7 495 123-45-67"` / `"12345678"` /
    `"1027700123456"` patterns) — the CURRENT values in this function are REAL organization
    requisites (already committed to git history since Phase 15) and this touch is the designated
    place to stop perpetuating that leak for any NEW commit. Finally, fix
    `tests/html_report_render.rs`'s independent `empty_org()` helper by adding `full_name:
    String::new(),`.
  </action>
  <acceptance_criteria>
    - `cargo build -p trackly-app` succeeds with zero errors (all OrgSettingsDto struct literals
      across the crate now compile).
    - `grep -c "org_full_name_html" crates/trackly-app/src/services/act_service.rs` returns 2 (one
      per render function).
    - `grep -c "org_full_name_html" crates/trackly-app/src/services/report_service.rs
      crates/trackly-app/src/services/template_service.rs` each return at least 1.
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_act_render --
      --test-threads=1` passes in full (this is the first point at which these previously-broken-
      by-Plan-34-02 tests are expected to go green again).
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_report_render --
      --test-threads=1` passes in full.
    - Manually diff the OLD vs NEW `demo_context_for_kind` org block values (visible in your own
      `<read_first>` read of the file) and confirm none of the original real phone/fax/okpo/ogrn
      values survive in the new source — do this check before committing, not after.
  </acceptance_criteria>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_act_render --test html_report_render -- --test-threads=1</automated>
  </verify>
  <done>
    All 4 render call sites supply org.full_name (escaped) and the _header.html partial; the
    demo preview stub no longer leaks real organization requisites; both pre-existing HTML render
    integration test files pass again.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render-gate cross-form test + DOC-05 non-empty full_name case + full suite green</name>
  <files>crates/trackly-app/tests/html_header_parity.rs, crates/trackly-app/tests/pdf_render_act.rs</files>
  <read_first>
    crates/trackly-app/tests/html_header_parity.rs (Plan 34-02 output — Test 1 substring gate,
    Test 2 privacy-safe structural test; this task adds Test 3)
    crates/trackly-app/tests/html_act_render.rs — make_full_pipeline()/create_handover() helpers,
    the existing pattern for calling p.acts.render_pdf / render_acceptance_pdf against a live
    pipeline (this task's render-gate test needs equivalent org-context setup)
    crates/trackly-app/tests/pdf_render_act.rs around line 620 (the OrgPatch-based org save,
    render, and assertion pattern to extend with a full_name-populated case)
  </read_first>
  <behavior>
    - Test 3 (render-gate, html_header_parity.rs): after saving org fields including a non-empty
      full_name via the real OrgDbService::save_fields path, rendering act_handover.html,
      act_acceptance.html, and report.html through the real pipeline produces three HTML strings
      whose <!-- HEADER-START -->...<!-- HEADER-END --> substrings (plain string search, not
      regex) are byte-identical to each other.
    - New case in pdf_render_act.rs: an OrgPatch with a non-empty, multi-line full_name renders
      "<br />" in the resulting HTML (proving the newline-to-<br> transform reached the actual
      render output, not just the unit-tested helper in isolation) and does NOT render a literal
      "\n" character inside the org name block.
  </behavior>
  <action>
    In `tests/html_header_parity.rs`, add Test 3 `header_fragment_identical_across_all_three_forms`:
    build (or reuse, if a lighter-weight existing test-pipeline helper already fits) a minimal
    pipeline with `OrgDbService::save_fields` populating every requisite field including a
    non-empty `full_name`, render all three document types with equivalent demo content, extract
    the substring between `"<!-- HEADER-START -->"` and `"<!-- HEADER-END -->"` from each of the
    three rendered strings via `str::find`/slicing (no regex needed — the markers are literal and
    non-nested), and `assert_eq!` all three pairwise. In `tests/pdf_render_act.rs`, extend the
    existing OrgPatch-based render test (around line 620) — or add a new `#[tokio::test]` next to
    it if extending would overload that test's existing assertions — with a case that sets
    `full_name: "Общество с ограниченной ответственностью\nООО «Тест»".to_string()` in the
    `OrgPatch`, renders, and asserts the resulting HTML `.contains("<br />")` and does NOT contain
    a literal `\n` character between the rendered full_name's two halves (assert
    `!html.contains("ответственностью\nООО")` — the raw unescaped newline must not survive into
    HTML output). Finally, run the FULL trackly-app test suite (per VALIDATION.md's phase-gate
    cadence) to confirm this plan restores it to fully green: `pnpm --dir ui build` (required
    prerequisite for `security_headers`'s SPA test) then `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1
    cargo test -p trackly-app -- --test-threads=1` (excluding the pre-existing, unrelated
    `auth_remember_cookie` hang — use `--test-threads=1` and do not run this concurrently with any
    other `cargo test` invocation, per project convention).
  </action>
  <acceptance_criteria>
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_header_parity --
      --test-threads=1` passes all 3 tests (substring gate, privacy-safe structural, render-gate).
    - `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test pdf_render_act --
      --test-threads=1` passes including the new full_name case.
    - `pnpm --dir ui build && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --
      --test-threads=1` (excluding `auth_remember_cookie`, per project memory) reports zero
      failures across the whole crate.
  </acceptance_criteria>
  <verify>
    <automated>TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test html_header_parity --test pdf_render_act -- --test-threads=1</automated>
  </verify>
  <done>
    The render-gate test proves all three document forms share an identical header fragment; the
    escape-and-render path is proven end-to-end with a non-empty full_name; the full crate test
    suite is green again after Plan 34-02's intentional intermediate breakage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `org_settings.full_name` (authenticated write) → `ctx["org"]["full_name"]` → `_header.html`'s `{{ ... | safe }}` → LAN browser preview | The boundary this plan actually CLOSES — Plan 34-01 proved the helper is safe in isolation, this plan proves it is actually invoked on every path that reaches the template. |
| `template_service.rs::demo_context_for_kind` (Rust source literal, not user input) → TemplateEditor preview | Lower-risk boundary (not user-controlled at runtime) but still subject to the project's privacy constraint since it is compiled into the public repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | Tampering / Elevation of Privilege | `act_service.rs` / `report_service.rs` / `template_service.rs` ctx assembly | mitigate | All four sites call `org_full_name_html` (Plan 34-01's escape-then-`<br>` helper) — never interpolate `org_dto.full_name` / `org.full_name` raw into a `| safe` context. Task 2's acceptance criteria grep-confirm the helper call exists at each site. |
| T-34-03-02 | Tampering | `render_with_timeout`'s new `extra_templates` parameter | mitigate | Every production call site sources the header body via `html_templates::load_template` (file-first, embedded-default fallback) — never a raw, unchecked `std::fs::read_to_string` (C-03) — so a missing/deleted `_header.html` degrades to the embedded default instead of making every act/report render fail. |
| T-34-03-03 | Information Disclosure | `demo_context_for_kind`'s org block | mitigate | Every real requisite value in this function (phone, fax, OKPO, OGRN, and any other field found to be real during `<read_first>`) is replaced with fictional values in the same touch that adds `full_name` — this is a scoped fix to the one function this plan already edits, not the full-repo cleanup (that remains PRIV-01, Phase 37). |
| T-34-03-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager dependency in this plan. |
</threat_model>

<verification>
- `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --lib pdf::minijinja_env --test
  html_act_render --test html_report_render --test html_header_parity --test pdf_render_act --
  --test-threads=1` passes.
- `pnpm --dir ui build && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --
  --test-threads=1` (excluding `auth_remember_cookie`) is fully green — this is the phase's
  restored full-suite gate after Plan 34-02's expected intermediate breakage.
</verification>

<success_criteria>
- `org.full_name` reaches every one of the four render paths, always pre-escaped.
- The render-gate test structurally proves DOC-04 (identical header across all three forms) via
  a real render, not just a substring/include check.
- The full pre-existing test suite (broken by design in Plan 34-02) is fully restored to green.
- `demo_context_for_kind`'s pre-existing real-requisite leak does not gain a new commit carrying
  it forward once this plan touches that function.
</success_criteria>

<output>
Create `.planning/phases/34-document-header/34-03-SUMMARY.md` when done
</output>
