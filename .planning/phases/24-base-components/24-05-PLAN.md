---
phase: 24-base-components
plan: 05
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - ui/src/lib/components/Badge.svelte
  - ui/src/features/showcase/sections/BadgeSection.svelte
autonomous: true
requirements: [CMP-03]

must_haves:
  truths:
    - "Badge renders 5 tones (neutral/accent/success/warning/danger) x 4 appearances (soft/solid/dot/count), matching Badges.dc.html."
    - "All 21 existing <Badge> call-sites in ui/src continue to compile and render identically to before this plan — none of those 21 files are modified."
  artifacts:
    - path: "ui/src/lib/components/Badge.svelte"
      provides: "Backward-compatible appearance prop (soft|solid|dot|count, default soft) + internal tone mapping"
      contains: "appearance"
    - path: "ui/src/features/showcase/sections/BadgeSection.svelte"
      provides: "Showcase gallery covering 5 tones x 4 appearances"
      min_lines: 25
  key_links:
    - from: "ui/src/lib/components/Badge.svelte"
      to: "ui/src/styles/_tokens.scss"
      via: "var(--tr-accent-text) usage (added by Plan 24-01)"
      pattern: "\\-\\-tr-accent-text"
    - from: "21 existing call-sites across ui/src/features/*"
      to: "ui/src/lib/components/Badge.svelte"
      via: "unchanged <Badge variant=...> prop usage, appearance defaults to 'soft'"
      pattern: "<Badge variant="
---

<objective>
Extend `Badge.svelte`'s API backward-compatibly per D-08: add an `appearance: 'soft' | 'solid' | 'dot' |
'count'` prop (default `'soft'`, matching the current visual exactly) while keeping the existing `variant`
prop name and its 5 values (`default`/`accent`/`success`/`warning`/`destructive`) completely untouched, so
none of the 21 real `<Badge>` render call-sites in `ui/src` need to change. Internally map `variant` to the
reference's 5 tone names (`default -> neutral`, `destructive -> danger`, others pass through) and implement
the full tone x appearance value matrix from `Badges.dc.html`. Then build the "Бейджи" showcase section.

Purpose: Badge is the single most call-site-dense primitive in this phase (21 usages across acts, requests,
printers, cartridges, devices, reports) — the backward-compat contract is the hard constraint that makes this
plan riskier than a pure new-component build.
Output: Extended `Badge.svelte`; new `BadgeSection.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-RESEARCH.md
@.planning/phases/24-base-components/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Badge.svelte API backward-compatibly (D-08)</name>
  <files>ui/src/lib/components/Badge.svelte</files>
  <read_first>
    - ui/src/lib/components/Badge.svelte — full current file (62 lines). Current `interface Props`: `variant?:
      'default'|'accent'|'success'|'warning'|'destructive'`, `size?: 'sm'|'md'`, `children?: Snippet`. Current
      template: `<span class="badge badge-{variant} badge-{size}">`. Current CSS uses `color-mix(in srgb, ...)`
      for success/warning/destructive soft tones — these must be replaced with the dedicated
      `--tr-success-soft`/`--tr-warning-soft`/`--tr-danger-soft` tokens (already present in `_tokens.scss`).
    - ui/src/styles/_tokens.scss — confirm `--tr-accent-text` now exists (added by Plan 24-01, this plan's
      dependency) alongside `--tr-success-soft`/`-text`, `--tr-warning-soft`/`-text`, `--tr-danger-soft`/`-text`,
      `--tr-surface-sunken`, `--tr-border-strong`, `--tr-text-secondary`/`-primary`/`-tertiary`, `--tr-on-accent`.
    - .planning/reference/design-system-v2/Badges.dc.html — pixel source of truth for the tone x appearance matrix.
    - .planning/phases/24-base-components/24-RESEARCH.md — "Badge" section: full pill-base + 5-tone x
      4-appearance value table, plus "Call-site count correction" (21 real call-sites, full file:line list)
      and "D-08 backward-compat mapping".
    - .planning/phases/24-base-components/24-PATTERNS.md — "Badge.svelte (MODIFY...)" section: exact current
      file excerpt + the same value table, cross-referenced.
    - Run `grep -rn "<Badge[ >]" ui/src` yourself before editing to get the current, authoritative call-site
      count (expected 21) — do not trust a stale count from documentation alone.
  </read_first>
  <action>
    Add `appearance?: 'soft' | 'solid' | 'dot' | 'count'` to `interface Props`, defaulting to `'soft'` in the
    destructure (`appearance = 'soft'`). Keep `variant` prop name and its 5 literal values completely
    unchanged — do not rename it to `tone`, do not change its default (`'default'`), do not change `size`.
    Add an internal tone-mapping constant (e.g. `const TONE_MAP = { default: 'neutral', accent: 'accent',
    success: 'success', warning: 'warning', destructive: 'danger' } as const;`) and derive
    `const tone = $derived(TONE_MAP[variant]);`. Change the template's class list from
    `class="badge badge-{variant} badge-{size}"` to `class="badge badge-{tone} badge-{appearance}
    badge-{size}"`. For the `appearance="dot"` case, render an additional leading `<span class="dot"
    aria-hidden="true"></span>` before `{@render children?.()}` (conditionally, only when
    `appearance === 'dot'`).

    Rewrite the CSS to a tone x appearance matrix (pill base unchanged: `display:inline-flex;
    align-items:center; gap:6px; height:22px; padding:0 10px; border-radius:11px; font-size:12px;
    font-weight:600; white-space:nowrap; line-height:1;` — note this replaces the current smaller `padding: 0
    var(--tr-space-xs); border-radius:10px; font-size:12px;` base rule with the reference's exact 22px-height
    pill). Five `.badge-{tone}` classes (`neutral`/`accent`/`success`/`warning`/`danger`) each defining soft
    (default appearance) bg/color via `--tr-{tone}-soft`/`--tr-{tone}-text` (neutral uses
    `--tr-surface-sunken`/`--tr-text-secondary` since there's no dedicated `--tr-neutral-*` token pair — this
    mirrors the reference exactly), then `&.badge-solid { background: var(--tone-solid-token); color:
    var(--tr-on-accent) (or var(--tr-text-primary) for neutral); }`, and a shared `.dot` class per tone (7px
    circle, `background: <tone dot color>` per RESEARCH.md's table). For `.badge-count`, implement the
    "neutral compact count" shape from RESEARCH.md (`background: var(--tr-surface-sunken); color:
    var(--tr-text-secondary); border-radius:11px; min-width:18px; height:18px; padding:0 6px; font-size:11px;
    justify-content:center;`) as the default count styling for all tones except accent, which additionally
    gets the accent-outlined count shape (`background: var(--tr-accent-soft); color: var(--tr-accent-text);
    border:1px solid var(--tr-accent); border-radius:11px; padding:0 9px; height:20px; font-size:11px;`) —
    use `&.badge-accent.badge-count { ... }` to override the shared count rule for that one tone. Preserve
    `.badge-sm`/`.badge-md` height overrides as-is (16px/20px — these apply on top of the appearance-driven
    height where the appearance doesn't already fix a specific height like count's 18-20px).
  </action>
  <acceptance_criteria>
    - `grep -c "appearance" ui/src/lib/components/Badge.svelte` returns at least 3 (prop declaration,
      destructure default, class usage)
    - `grep -c "variant?:" ui/src/lib/components/Badge.svelte` returns 1 and the line still lists exactly
      `'default' | 'accent' | 'success' | 'warning' | 'destructive'` unchanged
    - `grep -c "color-mix" ui/src/lib/components/Badge.svelte` returns 0 (replaced by dedicated `-soft` tokens)
    - `grep -rn "<Badge[ >]" ui/src | wc -l` returns 21 (unchanged call-site count)
    - `git diff --stat -- ui/src/features` shows zero files changed (none of the 21 call-site files touched
      by this plan — only `ui/src/lib/components/Badge.svelte` should appear in `git diff --stat`)
    - `node ui/scripts/check-tokens.mjs` exits 0 (no raw hex/rgba, `--tr-accent-text` resolves because Plan
      24-01 already added it)
    - `pnpm --dir ui lint` exits 0
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs && test "$(grep -rn '<Badge[ >]' ui/src | wc -l)" = "21" && test -z "$(git diff --stat -- ui/src/features)"</automated>
  </verify>
  <done>Badge.svelte has a backward-compatible appearance prop (default soft) covering 5 tones x 4 appearances; all 21 existing call-sites remain byte-for-byte unmodified (git diff --stat on ui/src/features is empty).</done>
</task>

<task type="auto">
  <name>Task 2: Create BadgeSection.svelte showcase section</name>
  <files>ui/src/features/showcase/sections/BadgeSection.svelte</files>
  <read_first>
    - ui/src/lib/components/Badge.svelte — extended in Task 1; props: `variant`, `size`, `appearance`, `children`.
    - .planning/phases/24-base-components/24-RESEARCH.md — "Badge" section value table (for the demo label
      text — e.g. tone names in Russian, matching how the app already labels statuses).
  </read_first>
  <action>
    Create `ui/src/features/showcase/sections/BadgeSection.svelte`, self-contained (no props, no `$state`
    needed — badges are non-interactive). Structure: `<h2>Бейджи</h2>` heading, then a grid/table with one row
    per tone (label each row using the `variant` prop values that map to it: "default -> neutral", "accent",
    "success", "warning", "destructive -> danger") and one column per appearance (`soft`, `solid`, `dot`,
    `count`), rendering `<Badge variant="..." appearance="...">Метка</Badge>` in each cell (20 cells total: 5
    tones x 4 appearances). Import `Badge` from `'$lib/components/Badge.svelte'`.
  </action>
  <acceptance_criteria>
    - `grep -c "<Badge" ui/src/features/showcase/sections/BadgeSection.svelte` returns 20 (5 tones x 4 appearances)
    - `grep -c "appearance=\"soft\"\|appearance=\"solid\"\|appearance=\"dot\"\|appearance=\"count\"" ui/src/features/showcase/sections/BadgeSection.svelte` returns 20
    - `pnpm --dir ui svelte-check` exits 0 (compiles standalone)
    - `pnpm --dir ui lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>BadgeSection.svelte exists, renders all 5 tones x 4 appearances (20 cells), compiles standalone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Badge is a presentational label component. No user input, no data fetch. The one meaningful risk in this plan is regression, not security: an API change that silently alters the visual output of 21 existing screens if the backward-compat contract is violated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-05-01 | Tampering (regression, not adversarial) | Badge.svelte prop/CSS API | mitigate | `git diff --stat -- ui/src/features` gated in Task 1's acceptance criteria — build fails (in review) if any of the 21 call-site files show as changed. This is a correctness gate, not a classic security threat, but treated with the same rigor since it's this plan's primary risk. |
| T-24-05-02 | Tampering | Badge.svelte CSS values | accept | Token values transcribed verbatim from a reviewed reference file. |

No ASVS-relevant control surface is touched by this plan.
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs` all exit 0.
`grep -rn "<Badge[ >]" ui/src | wc -l` still returns 21. `git diff --stat -- ui/src/features` is empty. Manual
(deferred to Plan 07's showcase-assembly checkpoint): all 20 tone x appearance cells visually match
`Badges.dc.html`; a live app screen using an existing `<Badge variant="warning">` still renders visually
correct (soft appearance, unchanged).
</verification>

<success_criteria>
Badge.svelte renders 5 tones x 4 appearances via a new backward-compatible `appearance` prop; zero of the 21
existing call-sites are modified; BadgeSection.svelte covers the full matrix, ready for Plan 07's showcase wiring.
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-05-SUMMARY.md` when done
</output>
