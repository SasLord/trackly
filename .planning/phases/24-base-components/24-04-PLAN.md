---
phase: 24-base-components
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/Modal.svelte
  - ui/src/features/showcase/sections/ModalSection.svelte
autonomous: true
requirements: [CMP-05]

must_haves:
  truths:
    - "Modal renders an overlay, a header (title + close button), a body, and an action footer, with shadow --tr-elev-3 and radius 12px (--tr-radius-lg)."
    - "A showcase visitor can open and close a demo Modal from the Modal section, verifying overlay/header/body/footer visually."
  artifacts:
    - path: "ui/src/lib/components/Modal.svelte"
      provides: "elev-3 shadow + radius-lg container, matching Modal.dc.html"
      contains: "var(--tr-elev-3)"
    - path: "ui/src/features/showcase/sections/ModalSection.svelte"
      provides: "Showcase section with a trigger button that opens a real Modal demo"
      min_lines: 15
  key_links:
    - from: "ui/src/features/showcase/sections/ModalSection.svelte"
      to: "ui/src/lib/components/Modal.svelte"
      via: "open = \\$state(false) toggled by a trigger <Button>, passed to <Modal open={open} ...>"
      pattern: "import Modal from '\\$lib/components/Modal.svelte'"
---

<objective>
Fix the two confirmed value mismatches in `Modal.svelte` (radius-md -> radius-lg, elev-2 -> elev-3) so it
matches `Modal.dc.html` (CMP-05), then build a "Модальное окно" showcase section with a trigger button that
opens a real Modal demonstrating overlay + header + body + footer.

Purpose: Modal already composes real `<Button>` components in its `footer` snippet slot (e.g.
`ActDetail.svelte`, `CartridgesPage.svelte` delete-confirm dialogs) — once Button is corrected (Plan 02),
Modal's footer buttons inherit that fix automatically with zero Modal-specific button styling required here.
Output: Corrected `Modal.svelte`; new `ModalSection.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-RESEARCH.md
@.planning/phases/24-base-components/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Modal.svelte radius/elevation (CMP-05)</name>
  <files>ui/src/lib/components/Modal.svelte</files>
  <read_first>
    - ui/src/lib/components/Modal.svelte — full file (190 lines). `.modal-container` rule at lines 91-99:
      line 93 `border-radius: var(--tr-radius-md)`, line 94 `box-shadow: var(--tr-elev-2)` — both must change.
      Everything else (overlay at lines 80-89, header/body/footer at lines 119-178) already matches the
      reference — do not touch those.
    - .planning/reference/design-system-v2/Modal.dc.html — target container spec: `background:
      var(--tr-surface); border: 1px solid var(--tr-border); border-radius: 12px (var(--tr-radius-lg));
      box-shadow: var(--tr-elev-3);`
    - ui/src/styles/_tokens.scss — confirm `--tr-radius-lg: 12px;` (line 190) and `--tr-elev-3` (line 80
      light / 155 dark) both already exist — no token addition needed, only usage in Modal.svelte.
  </read_first>
  <action>
    In `.modal-container`, change `border-radius: var(--tr-radius-md);` to
    `border-radius: var(--tr-radius-lg);` and `box-shadow: var(--tr-elev-2);` to
    `box-shadow: var(--tr-elev-3);`. Do not add a `border` line if one isn't already there — check whether
    `.modal-container` currently has `background: var(--tr-surface-raised)`; the reference wants
    `background: var(--tr-surface)` per `Modal.dc.html` — if the current value differs, change it too, but
    only if `--tr-surface-raised` and `--tr-surface` are visually different tokens in `_tokens.scss` (verify
    by reading both token values before editing — if they render identically in both themes this is a no-op
    either way, but use the reference's exact token name for correctness). No other structural changes.
  </action>
  <acceptance_criteria>
    - `grep -c "var(--tr-radius-lg)" ui/src/lib/components/Modal.svelte` returns at least 1
    - `grep -c "var(--tr-radius-md)" ui/src/lib/components/Modal.svelte` returns 0 (fully replaced on `.modal-container`)
    - `grep -c "var(--tr-elev-3)" ui/src/lib/components/Modal.svelte` returns at least 1
    - `grep -c "var(--tr-elev-2)" ui/src/lib/components/Modal.svelte` returns 0 (fully replaced on `.modal-container`)
    - `node ui/scripts/check-tokens.mjs` exits 0
    - `pnpm --dir ui lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && node ui/scripts/check-tokens.mjs</automated>
  </verify>
  <done>Modal.svelte's container uses var(--tr-radius-lg) (12px) and var(--tr-elev-3), matching Modal.dc.html; overlay/header/body/footer left unchanged as already correct.</done>
</task>

<task type="auto">
  <name>Task 2: Create ModalSection.svelte showcase section</name>
  <files>ui/src/features/showcase/sections/ModalSection.svelte</files>
  <read_first>
    - ui/src/lib/components/Modal.svelte — corrected in Task 1; props: `open`, `title`, `size`, `onClose`,
      `children` (Snippet, body), `footer` (Snippet, action buttons).
    - ui/src/lib/components/Button.svelte — used inside the demo footer (e.g. "Отмена"/secondary +
      "Подтвердить"/primary), matches the existing composition pattern already used by
      `ActDetail.svelte`/`CartridgesPage.svelte` delete-confirm modals (do not copy those files, just mirror
      the footer-composition shape: real `<Button>` instances passed via the `footer` snippet).
    - .planning/phases/24-base-components/24-PATTERNS.md — "ComponentShowcasePage.svelte" section notes the
      showcase has no `apiCall`/data-fetch — this section is the same, pure static demo.
  </read_first>
  <action>
    Create `ui/src/features/showcase/sections/ModalSection.svelte`, self-contained. Structure:
    `<h2>Модальное окно</h2>` heading, a local `let open = $state(false)`, a `<Button onclick={() => open =
    true}>Показать модал</Button>` trigger, and a `<Modal {open} title="Демонстрация модального окна"
    onClose={() => open = false}>` with: body content (a short paragraph of placeholder Russian text
    demonstrating the `children` snippet, plus one `<Input>` field to show a realistic form body — import
    `Input` from `'$lib/components/Input.svelte'`), and a `footer` snippet rendering two `<Button>` instances
    (`variant="secondary"` labeled "Отмена" with `onclick={() => open = false}`, `variant="primary"` labeled
    "Подтвердить" with `onclick={() => open = false}`). This exercises overlay + header (title + close ×) +
    body + footer all in one interactive demo.
  </action>
  <acceptance_criteria>
    - `grep -c "<Modal" ui/src/features/showcase/sections/ModalSection.svelte` returns 1
    - `grep -c "footer" ui/src/features/showcase/sections/ModalSection.svelte` returns at least 1 (snippet usage)
    - `grep -c "\\$state(false)" ui/src/features/showcase/sections/ModalSection.svelte` returns at least 1
    - `pnpm --dir ui svelte-check` exits 0 (compiles standalone)
    - `pnpm --dir ui lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>ModalSection.svelte exists with a trigger button that opens a Modal demonstrating overlay + header + body (text + Input) + footer (2 Buttons), compiles standalone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Modal is a presentational overlay component; the demo section uses only hardcoded Russian placeholder text and a local, non-persisted `Input` value. No data leaves the component, no API call is made. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-04-01 | Tampering | Modal.svelte CSS values | accept | Token values transcribed verbatim from a reviewed reference file; no external input. |
| T-24-04-02 | Denial of Service (UX) | Modal backdrop mousedown/mouseup dismiss logic (unchanged, pre-existing) | accept | Not modified by this plan — the existing G-1 fix (Phase 3.1 Plan 06) already guards against accidental dismiss during text-selection drag; out of scope to touch here. |

No ASVS-relevant control surface is touched by this plan.
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs` all exit 0. Manual
(deferred to Plan 07's showcase-assembly checkpoint): the demo Modal visually shows a 12px-radius container
with a clearly heavier shadow than a card, matching `Modal.dc.html`.
</verification>

<success_criteria>
Modal.svelte's container matches `Modal.dc.html`'s radius (12px) and shadow (`--tr-elev-3`); ModalSection.svelte
exists with a working open/close demo exercising all 4 structural regions (overlay/header/body/footer).
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-04-SUMMARY.md` when done
</output>
