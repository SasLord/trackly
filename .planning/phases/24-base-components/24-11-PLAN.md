---
phase: 24-base-components
plan: 11
type: execute
wave: 3
depends_on: ["24-08", "24-09", "24-10"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05]

must_haves:
  truths:
    - "A human has actually performed the visual comparison of all 5 showcase sections against the .dc.html references — the original Task 3 checkpoint in 24-07-PLAN.md was auto-approved under workflow.auto_advance and 24-07-SUMMARY.md explicitly records that this step was never genuinely executed by a human."
    - "The bind:value fix (24-08), the D-09 theme-transition fix (24-08), the Badge count fix (24-09), and the Modal focus-management fix (24-10) are all confirmed working together in one rebuilt ui/dist, not just individually per-plan."
  artifacts: []
  key_links: []
---

<objective>
Perform the genuine human sign-off that 24-VERIFICATION.md flagged as outstanding: the original phase-24 Task
3 checkpoint (24-07-PLAN.md) was closed under `workflow.auto_advance` without an actual human performing the
visual comparison against the `.dc.html` references. This plan is a real, blocking checkpoint — it cannot be
silently auto-approved — that re-runs that verification now that all three BLOCKER gaps (bind:value, Badge
count, D-09 theme-transition) and the Modal focus-management follow-up are closed.

Purpose: Close the loop on the one verification step that has never actually happened for this phase, and
confirm the four gap-closure fixes (24-08, 24-09, 24-10) compose correctly in a single rebuilt bundle before
phases 25-30 start building forms on top of these primitives.
Output: A human-confirmed "approved" (or a documented list of remaining visual/behavioral mismatches) against
all 5 `.dc.html` references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-VERIFICATION.md
@.planning/phases/24-base-components/24-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Genuine human sign-off on showcase + all four gap-closure fixes</name>
  <what-built>
    Four fixes closing the gaps 24-VERIFICATION.md found in the phase-24 showcase and primitives: (1) two-way
    `bind:value` on Input/Select/Textarea (24-08), (2) a working D-09 theme-transition-suppression rule
    (24-08), (3) tone-correct `count`-appearance Badges for all 5 tones (24-09), (4) keyboard focus management
    on Modal (24-10). This checkpoint is the first genuine human pass over the showcase — the equivalent
    checkpoint in 24-07-PLAN.md was auto-approved without a human actually looking at it.
  </what-built>
  <how-to-verify>
    1. Run `pnpm --dir ui build` (do not run concurrently with any other cargo test/build — `prebuild` runs
       `cargo test -p trackly-app --test export_bindings`). Server mode serves `ui/dist`, not live source —
       required per the project's LAN-browser-UAT gotcha; `cargo tauri dev` only HMRs the desktop webview and
       will NOT pick up these fixes for a LAN-browser check.
    2. Start the app (desktop `cargo tauri dev`, or server mode + a LAN/localhost browser tab) and log in as
       an `admin`-role user. Navigate to "Витрина компонентов" in the sidebar.
    3. **Поля ввода**: type into the Input/Select/Textarea "Обычное" demo cells in `FieldsSection`. Confirm the
       value visibly reflects what you typed and does not revert or stay frozen (this is the bind:value fix —
       previously these silently never updated the parent's bound `$state`). Open the Modal demo in
       `ModalSection` and confirm its Input field's bound value also updates as you type.
    4. **Бейджи**: in `BadgeSection`, compare all 20 cells (5 tones x soft/solid/dot/count) against
       `.planning/reference/design-system-v2/Badges.dc.html`. Specifically confirm the `count` row for
       success/warning/destructive now renders in that tone's own color (green/amber/red), not neutral grey.
    5. **Кнопки / Вкладки / Модальное окно**: compare `ButtonsSection`/`TabsSection`/`ModalSection` against
       `Buttons.dc.html`/`Tabs.dc.html`/`Modal.dc.html` — 5 button variants x 2 sizes x states, switch-bar +
       segmented tabs with counters/active-underline, modal overlay/radius/shadow/header/body/footer.
    6. **Modal keyboard behavior**: open the Modal, confirm focus lands on its Input field, Tab through
       Input -> Отмена -> Подтвердить and confirm it wraps back to Input (does not escape into the sidebar),
       close the modal and confirm focus returns to the "Показать модал" button.
    7. **Тема**: toggle light/dark/system via `ThemeSwitcher` rapidly several times while watching
       Button/Badge/Tabs — confirm no visible color "smear" transition sweep occurs (D-09).
    8. **Known, deliberately deferred limitation** (not a regression, not part of this checkpoint's pass/fail):
       the `/showcase` route itself is only gated at the sidebar-visibility level (`roles: ['admin']` in
       `sidebar-config.ts`), not at the `routes.ts` route-map level — a `manager` who knows to type
       `#/showcase` directly can still reach the page. This exactly matches the app's existing pattern for
       `/users` and `/settings` (also sidebar-only gated) and is explicitly NOT fixed here: introducing a
       route-level guard for `/showcase` alone (while `/users`/`/settings` remain sidebar-only) would create a
       new, inconsistent gating pattern rather than close a phase-24 regression. If a systemic route-level
       guard is wanted, it belongs in a dedicated cross-cutting phase (e.g., the QA/access-control work already
       scoped to Phase 30) applied uniformly to all three routes, not as a one-off fix here. The showcase
       renders zero privileged data, so the residual risk is accepted, matching 24-07-PLAN.md's threat model.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 sections match their .dc.html references and the bind:value/Badge-count/theme-transition/Modal-focus behaviors all work as described, or describe any specific visual/behavioral mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan performs verification only — no files are modified. The `/showcase` route-gating gap (sidebar-only, not route-level) is a pre-existing, accepted, app-wide pattern shared with `/users`/`/settings` (see 24-07-PLAN.md's threat model, T-24-07-01) and is explicitly re-affirmed as accepted here, not newly introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-11-01 | Elevation of Privilege | `/showcase` route reachable by `manager` via direct hash navigation | accept | Identical residual risk to the already-shipped `/users`/`/settings` routes; showcase renders zero privileged/API-sourced data. Deferred to any future systemic route-guard work (not scoped to Phase 24) rather than fixed as a one-off here, to avoid an inconsistent gating pattern across the three routes. |

No ASVS-relevant control surface is touched by this plan (verification-only, zero files modified).
</threat_model>

<verification>
Human checkpoint approval (`resume-signal`) is the authoritative verification for this plan — it is the
genuine execution of the human-verification steps that 24-VERIFICATION.md confirmed never actually happened
for Phase 24.
</verification>

<success_criteria>
A human has explicitly typed "approved" (or filed specific mismatches) after visually comparing all 5 showcase
sections against their `.dc.html` references and confirming the bind:value, Badge-count, D-09 theme-transition,
and Modal-focus fixes all work together in one rebuilt `ui/dist`.
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-11-SUMMARY.md` when done
</output>
