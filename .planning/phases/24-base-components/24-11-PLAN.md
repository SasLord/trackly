---
phase: 24-base-components
plan: 11
type: execute
wave: 3
depends_on: ["24-08", "24-09", "24-10"]
files_modified:
  - .planning/config.json
autonomous: false
gap_closure: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05]

must_haves:
  truths:
    - "At the moment the human-verify checkpoint is evaluated, `gsd-sdk query check auto-mode --pick active` returns `false` — because a preceding task in this same plan explicitly set `workflow.auto_advance` to false and recorded its prior value. This is what makes non-auto-approval real: `autonomous: false` and `gate=\"blocking\"` are cosmetic (auto-approval keys on checkpoint TYPE per checkpoints.md:11, and execute-phase.md:963-975 has no carve-out for either attribute), which is exactly how 24-07's equivalent checkpoint was silently auto-approved."
    - "A human has actually performed the visual comparison of all 5 showcase sections against the .dc.html references — the original Task 3 checkpoint in 24-07-PLAN.md was auto-approved under workflow.auto_advance and 24-07-SUMMARY.md:80 explicitly records that this step was never genuinely executed by a human."
    - "The bind:value fix (24-08), the D-09 theme-transition fix (24-08), the Badge count fix (24-09), and the Modal focus-management fix (24-10) are all confirmed working together in one rebuilt ui/dist, not just individually per-plan."
    - "The developer's persistent `workflow.auto_advance` preference is restored to its pre-checkpoint value after sign-off, so this plan does not silently change a global setting."
  artifacts:
    - path: ".planning/config.json"
      provides: "workflow.auto_advance temporarily false across the checkpoint, restored to its recorded prior value afterward"
      contains: "auto_advance"
  key_links:
    - from: ".planning/config.json workflow.auto_advance"
      to: "execute-phase.md checkpoint_handling (AUTO_MODE read at line 967)"
      via: "gsd-sdk query check auto-mode --pick active — read AFTER the executor returns the checkpoint, so an in-plan pre-flight flip takes effect for this very checkpoint"
      pattern: "auto_advance"
---

<objective>
Perform the genuine human sign-off that 24-VERIFICATION.md flagged as outstanding: the original phase-24 Task
3 checkpoint (24-07-PLAN.md) was closed under `workflow.auto_advance` without an actual human performing the
visual comparison against the `.dc.html` references.

Critically, marking this plan `autonomous: false` with `gate="blocking"` is NOT sufficient to prevent a repeat
— both attributes are cosmetic for this purpose. Auto-approval keys on the checkpoint TYPE (`human-verify`),
per `~/.claude/get-shit-done/references/checkpoints.md:11`, and `execute-phase.md:963-975` provides no
carve-out for `gate` or `autonomous`. A plain manual `/gsd-execute-phase 24` clears only the ephemeral
`_auto_chain_active` flag and deliberately does not touch the persistent `workflow.auto_advance`, which is
`true` in this project's `.planning/config.json`. (The `gate="blocking-human"` escape hatch referenced in
`12-03-SUMMARY.md` does not exist — `grep -rn "blocking-human" ~/.claude/get-shit-done/` returns nothing.)
This plan therefore disables auto-approval mechanically before the checkpoint and restores it after.

Purpose: Close the loop on the one verification step that has never actually happened for this phase, and
confirm the four gap-closure fixes (24-08, 24-09, 24-10) compose correctly in a single rebuilt bundle before
phases 25-30 start building forms on top of these primitives.
Output: A human-confirmed "approved" (or a documented list of remaining visual/behavioral mismatches) against
all 5 `.dc.html` references, with the developer's auto-advance preference left exactly as it was found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-VERIFICATION.md
@.planning/phases/24-base-components/24-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight — mechanically disable checkpoint auto-approval</name>
  <files>.planning/config.json</files>
  <read_first>
    - .planning/config.json `workflow` block — confirm the current persistent value of `auto_advance` (it is
      `true` at planning time) and of `_auto_chain_active` (`false` at planning time). Both feed the same
      consolidated boolean.
    - ~/.claude/get-shit-done/references/checkpoints.md line 11 — "Auto-mode bypasses verification/decision
      checkpoints — When `workflow._auto_chain_active` OR `workflow.auto_advance` is true in config:
      human-verify auto-approves". Confirms the trigger is checkpoint TYPE, with no exemption for the task's
      `gate` attribute or the plan's `autonomous` frontmatter field.
    - ~/.claude/get-shit-done/workflows/execute-phase.md lines 963-975 — the `checkpoint_handling` step. Note
      `AUTO_MODE` is read at line 967 (`gsd-sdk query check auto-mode --pick active`) AFTER the executor has
      returned the checkpoint, which is precisely why flipping the config in this task takes effect for the
      Task 2 checkpoint in this same plan.
    - .planning/phases/24-base-components/24-07-SUMMARY.md line 80 — the in-repo precedent: the equivalent
      checkpoint recorded as "closed as a gate-closure only under auto_advance".
  </read_first>
  <action>
    Record the pre-existing value first so restoration in Task 3 is exact: run
    `gsd-sdk query config-get workflow.auto_advance` and write the returned literal (`true` or `false`) into
    the plan's SUMMARY draft notes as PRIOR_AUTO_ADVANCE. Do the same for
    `gsd-sdk query config-get workflow._auto_chain_active`.

    Then run `gsd-sdk query config-set workflow.auto_advance false`. If `_auto_chain_active` read back as
    anything other than `false`, also run `gsd-sdk query config-set workflow._auto_chain_active false` — the
    consolidated `check auto-mode` boolean is an OR of the two, so both must be false.

    Finally assert the result via CLI output rather than assumption: `gsd-sdk query check auto-mode --pick
    active` must print `false`. If it prints `true`, STOP and do not proceed to Task 2 — the checkpoint would
    be auto-approved and the entire purpose of this plan would be defeated.
  </action>
  <acceptance_criteria>
    - `gsd-sdk query config-get workflow.auto_advance` prints `false`
    - `gsd-sdk query config-get workflow._auto_chain_active` prints `false`
    - `gsd-sdk query check auto-mode --pick active` prints `false` (this is the authoritative assertion — it is
      the exact command `execute-phase.md:967` uses to decide whether to auto-approve)
    - The pre-existing values of both keys are recorded verbatim in the execution notes for Task 3's
      restoration (expected at planning time: `auto_advance=true`, `_auto_chain_active=false`)
  </acceptance_criteria>
  <verify>
    <automated>test "$(gsd-sdk query check auto-mode --pick active)" = "false"</automated>
  </verify>
  <done>`gsd-sdk query check auto-mode --pick active` returns `false`, so the Task 2 human-verify checkpoint will genuinely block for a human instead of being auto-approved; the prior values are recorded for exact restoration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Genuine human sign-off on showcase + all four gap-closure fixes</name>
  <what-built>
    Four fixes closing the gaps 24-VERIFICATION.md found in the phase-24 showcase and primitives: (1) two-way
    `bind:value` on Input/Select/Textarea (24-08), (2) a working D-09 theme-transition-suppression rule
    (24-08), (3) tone-correct `count`-appearance Badges for all 5 tones (24-09), (4) keyboard focus management
    on Modal (24-10). This checkpoint is the first genuine human pass over the showcase — the equivalent
    checkpoint in 24-07-PLAN.md was auto-approved without a human actually looking at it, which Task 1 of this
    plan has now mechanically prevented from recurring.
  </what-built>
  <how-to-verify>
    1. Run `pnpm --dir ui build` (do not run concurrently with any other cargo test/build — `prebuild` runs
       `cargo test -p trackly-app --test export_bindings`). Server mode serves `ui/dist`, not live source —
       required per the project's LAN-browser-UAT gotcha; `cargo tauri dev` only HMRs the desktop webview and
       will NOT pick up these fixes for a LAN-browser check.
    2. Start the app (desktop `cargo tauri dev`, or server mode + a LAN/localhost browser tab) and log in as
       an `admin`-role user. Navigate to "Витрина компонентов" in the sidebar.
    3. **Поля ввода**: type into the Input/Select/Textarea "Обычное" demo cells in `FieldsSection`. Confirm the
       value visibly reflects what you typed and does not revert or stay frozen (this is the bind:value fix —
       previously these silently never updated the parent's bound `$state`). Open the Modal demo in
       `ModalSection` and confirm its Input field's bound value also updates as you type.
    4. **Бейджи**: in `BadgeSection`, compare all 20 cells (5 tones x soft/solid/dot/count) against
       `.planning/reference/design-system-v2/Badges.dc.html`. Specifically confirm the `count` row for
       success/warning/destructive now renders in that tone's own color (green/amber/red), not neutral grey.
    5. **Кнопки / Вкладки / Модальное окно**: compare `ButtonsSection`/`TabsSection`/`ModalSection` against
       `Buttons.dc.html`/`Tabs.dc.html`/`Modal.dc.html` — 5 button variants x 2 sizes x states, switch-bar +
       segmented tabs with counters/active-underline, modal overlay/radius/shadow/header/body/footer.
    6. **Modal keyboard behavior**: open the Modal and confirm the real focus order — focus lands first on the
       **× close button** in the header (it is the first focusable element in DOCUMENT order, since the header
       precedes the body; this is standard, correct WAI-ARIA dialog behavior, not a bug). Then Tab through and
       confirm the cycle is exactly **× → Input → Отмена → Подтвердить → wraps back to ×**, never escaping into
       the sidebar behind the backdrop. From × press Shift+Tab and confirm it wraps backwards to Подтвердить.
       Close the modal and confirm focus returns to the "Показать модал" trigger button.
    7. **Тема**: toggle light/dark/system via `ThemeSwitcher` rapidly several times while watching
       Button/Badge/Tabs — confirm no visible color "smear" transition sweep occurs (D-09).
    8. **Known, deliberately deferred limitation** (not a regression, not part of this checkpoint's pass/fail):
       the `/showcase` route itself is only gated at the sidebar-visibility level (`roles: ['admin']` in
       `sidebar-config.ts`), not at the `routes.ts` route-map level — a `manager` who knows to type
       `#/showcase` directly can still reach the page. This exactly matches the app's existing pattern for
       `/users` and `/settings` (also sidebar-only gated) and is explicitly NOT fixed here: introducing a
       route-level guard for `/showcase` alone (while `/users`/`/settings` remain sidebar-only) would create a
       new, inconsistent gating pattern rather than close a phase-24 regression. If a systemic route-level
       guard is wanted, it belongs in a dedicated cross-cutting phase (e.g., the QA/access-control work already
       scoped to Phase 30) applied uniformly to all three routes, not as a one-off fix here. The showcase
       renders zero privileged data, so the residual risk is accepted, matching 24-07-PLAN.md's threat model.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 sections match their .dc.html references and the bind:value/Badge-count/theme-transition/Modal-focus behaviors all work as described, or describe any specific visual/behavioral mismatch.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restore the developer's prior auto-advance preference</name>
  <files>.planning/config.json</files>
  <read_first>
    - The PRIOR_AUTO_ADVANCE and prior `_auto_chain_active` values recorded in Task 1's execution notes — these
      are the exact literals to restore, not assumed defaults.
    - ~/.claude/get-shit-done/workflows/execute-phase.md line 1637 — the phase-advance step re-reads
      `check auto-mode`; restoring here (after the checkpoint, before phase advance) means the developer's own
      preference governs whether the phase auto-advances, exactly as it would have without this plan.
  </read_first>
  <action>
    Run `gsd-sdk query config-set workflow.auto_advance <PRIOR_AUTO_ADVANCE>` using the literal recorded in
    Task 1 (expected `true`). If Task 1 also had to change `_auto_chain_active`, restore it to its recorded
    prior value the same way; if Task 1 found it already `false` and did not change it, leave it alone.

    Run this task even if Task 2's checkpoint surfaced mismatches and the phase needs another gap-closure
    round — the config change must not outlive this plan either way. If Task 2 was rejected, still restore,
    then report the mismatches.
  </action>
  <acceptance_criteria>
    - `gsd-sdk query config-get workflow.auto_advance` prints the exact literal recorded as PRIOR_AUTO_ADVANCE
      in Task 1 (expected `true`)
    - `gsd-sdk query config-get workflow._auto_chain_active` prints its Task-1-recorded prior value (expected
      `false`)
    - `git diff --stat .planning/config.json` shows no net change to the `workflow` block relative to the
      commit that preceded Task 1 (the temporary flip is fully reverted)
  </acceptance_criteria>
  <verify>
    <automated>gsd-sdk query config-get workflow.auto_advance && git diff --stat .planning/config.json</automated>
  </verify>
  <done>`workflow.auto_advance` (and `_auto_chain_active` if it was touched) are back to the exact values Task 1 recorded, leaving the developer's persistent preference unchanged by this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local developer config (`.planning/config.json`) | Tasks 1 and 3 temporarily flip `workflow.auto_advance` in a local, git-tracked planning config. This is a developer-workflow setting with no runtime/production effect — it does not ship in any build artifact and controls only whether the GSD orchestrator pauses for human input. Not a security boundary. |
| None new (application) | This plan modifies zero application source files. The `/showcase` route-gating gap (sidebar-only, not route-level) is a pre-existing, accepted, app-wide pattern shared with `/users`/`/settings` (see 24-07-PLAN.md's threat model, T-24-07-01) and is explicitly re-affirmed as accepted here, not newly introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-11-01 | Elevation of Privilege | `/showcase` route reachable by `manager` via direct hash navigation | accept | Identical residual risk to the already-shipped `/users`/`/settings` routes; showcase renders zero privileged/API-sourced data. Deferred to any future systemic route-guard work (not scoped to Phase 24) rather than fixed as a one-off here, to avoid an inconsistent gating pattern across the three routes. |
| T-24-11-02 | Tampering | `workflow.auto_advance` left disabled if the plan aborts between Task 1 and Task 3 | mitigate | Task 1 records the prior value verbatim before mutating; Task 3 restores it unconditionally (explicitly including the checkpoint-rejected path), and its acceptance criteria assert via `git diff --stat .planning/config.json` that the `workflow` block has no net change. Worst case (hard abort mid-plan) is a developer-visible, git-tracked, single-key diff that `git checkout .planning/config.json` reverts — no data loss, no security impact. |

No ASVS-relevant application control surface is touched by this plan (verification-only; the sole file
modified is a local developer-workflow config).
</threat_model>

<verification>
Task 1's `test "$(gsd-sdk query check auto-mode --pick active)" = "false"` is the gate that makes the human
checkpoint real. Human checkpoint approval (`resume-signal`) is then the authoritative verification of the
phase's visual/behavioral criteria — the genuine execution of the human-verification steps that
24-VERIFICATION.md confirmed never actually happened for Phase 24. Task 3's `git diff --stat` assertion
confirms the config mutation did not leak past this plan.
</verification>

<success_criteria>
`check auto-mode` reads `false` at checkpoint time; a human has explicitly typed "approved" (or filed specific
mismatches) after visually comparing all 5 showcase sections against their `.dc.html` references and
confirming the bind:value, Badge-count, D-09 theme-transition, and Modal-focus fixes all work together in one
rebuilt `ui/dist`; and `workflow.auto_advance` is restored to its pre-plan value.
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-11-SUMMARY.md` when done
</output>
