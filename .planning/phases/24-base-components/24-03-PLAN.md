---
phase: 24-base-components
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/Input.svelte
  - ui/src/lib/components/Select.svelte
  - ui/src/lib/components/Textarea.svelte
  - ui/src/lib/components/Checkbox.svelte
  - ui/src/lib/components/Radio.svelte
  - ui/src/features/showcase/sections/FieldsSection.svelte
autonomous: true
requirements: [CMP-02]

must_haves:
  truths:
    - "Input, Select, and Textarea use --tr-surface/--tr-border-strong (not --tr-bg/--tr-border) matching ctrlBase() from Fields.dc."
    - "Checkbox and Radio exist as new reusable primitives, each wrapping a visually-hidden native <input> for real keyboard/a11y semantics, styled purely via CSS sibling selectors. Radio is built this phase alongside Checkbox even though CMP-02 names only Checkbox (D-04)."
    - "Input/Select/Textarea/Checkbox/Radio are each visually distinguishable in normal/focus/error/disabled states."
  artifacts:
    - path: "ui/src/lib/components/Input.svelte"
      provides: "ctrlBase() bg/border token swap"
      contains: "var(--tr-surface)"
    - path: "ui/src/lib/components/Select.svelte"
      provides: "ctrlBase() bg/border token swap"
      contains: "var(--tr-surface)"
    - path: "ui/src/lib/components/Textarea.svelte"
      provides: "ctrlBase() bg/border token swap"
      contains: "var(--tr-surface)"
    - path: "ui/src/lib/components/Checkbox.svelte"
      provides: "New 18px checkbox primitive, hidden native input + styled box"
      contains: "type=\"checkbox\""
    - path: "ui/src/lib/components/Radio.svelte"
      provides: "New circular radio primitive with bind:group semantics"
      contains: "type=\"radio\""
    - path: "ui/src/features/showcase/sections/FieldsSection.svelte"
      provides: "Showcase gallery section covering all 5 field primitives x 4 states"
      min_lines: 30
  key_links:
    - from: "ui/src/lib/components/Checkbox.svelte"
      to: "native <input type=checkbox>"
      via: "hidden input driving sibling .box via :checked/:focus-visible/:disabled CSS selectors"
      pattern: "native-input"
    - from: "ui/src/lib/components/Radio.svelte"
      to: "native bind:group"
      via: "group = \\$bindable() forwarded to <input type=radio bind:group>"
      pattern: "bind:group"
---

<objective>
Fix the confirmed bg/border token drift on the three existing field primitives (Input/Select/Textarea use
`--tr-bg`/`--tr-border`, reference wants `--tr-surface`/`--tr-border-strong`), and build Checkbox and Radio
from scratch as new primitives (D-04) following the hidden-native-input + styled-sibling-span pattern — never
hand-rolled ARIA. Then create the "Поля ввода" showcase section covering all 5 field types across their
normal/focus/error/disabled states.

Purpose: These are the most-reused form primitives in the app; Checkbox/Radio currently don't exist anywhere
(every checkbox in the app today is a raw, unstyled `<input type=checkbox>`) — CMP-02 requires them as first-
class components, ready for phases 26-28 to adopt later (out of scope here, D-07).
Output: Corrected Input/Select/Textarea; new Checkbox.svelte + Radio.svelte; new FieldsSection.svelte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-RESEARCH.md
@.planning/phases/24-base-components/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Input/Select/Textarea bg/border token drift (ctrlBase transcription)</name>
  <files>ui/src/lib/components/Input.svelte, ui/src/lib/components/Select.svelte, ui/src/lib/components/Textarea.svelte</files>
  <read_first>
    - ui/src/lib/components/Input.svelte — full file (77 lines); `.input` rule at lines 42-75: line 47
      `background: var(--tr-bg)`, line 49 `border: 1px solid var(--tr-border)` — both must change.
    - ui/src/lib/components/Select.svelte — full file (102 lines); `.select` rule at lines 61-91: line 66
      `background: var(--tr-bg)`, line 68 `border: 1px solid var(--tr-border)` — both must change. Note the
      comment at lines 2-6 explaining the caret/portal decision — do not touch that logic, only the two
      token lines.
    - ui/src/lib/components/Textarea.svelte — full file (74 lines); `.textarea` rule at lines 39-72: line 44
      `background: var(--tr-bg)`, line 46 `border: 1px solid var(--tr-border)` — both must change.
    - .planning/reference/design-system-v2/Fields.dc.html — `ctrlBase()`: `background: var(--tr-surface);
      border: 1px solid var(--tr-border-strong);` — the exact target values.
    - .planning/phases/24-base-components/24-RESEARCH.md — "Fields" section: full `ctrlBase()`
      focus/error/disabled spec, confirms `--tr-danger-ring` is already canonical (0.2 alpha) in all three
      files — no ring-alpha change needed, only the base bg/border swap.
  </read_first>
  <action>
    In all three files, change `background: var(--tr-bg);` to `background: var(--tr-surface);` and
    `border: 1px solid var(--tr-border);` to `border: 1px solid var(--tr-border-strong);` on the single base
    rule (`.input`, `.select`, `.textarea` respectively). Do not touch `&:focus-visible`, `&.invalid`, or
    `&:disabled` blocks — those already use the correct tokens (`--tr-accent`, `--tr-focus-ring`,
    `--tr-danger`, `--tr-surface-sunken`) per RESEARCH.md's Fields section. `Select.svelte`'s `.invalid` rule
    currently lacks the `box-shadow: 0 0 0 3px var(--tr-danger-ring);` that `Input.svelte`/`Textarea.svelte`
    are also missing on `.invalid` (only `border-color: var(--tr-danger)` is set in all three) — per
    `ctrlBase()`'s error spec (`border-color: var(--tr-danger); box-shadow: 0 0 0 3px var(--tr-danger-ring);`)
    add the missing `box-shadow: 0 0 0 3px var(--tr-danger-ring);` line to the `&.invalid` block in all
    three files (Input.svelte already has it at line 67 — verify and leave; Select.svelte and Textarea.svelte
    are missing it — add it).
  </action>
  <acceptance_criteria>
    - `grep -c "background: var(--tr-surface)" ui/src/lib/components/Input.svelte ui/src/lib/components/Select.svelte ui/src/lib/components/Textarea.svelte` returns 3 (one per file)
    - `grep -c "border: 1px solid var(--tr-border-strong)" ui/src/lib/components/Input.svelte ui/src/lib/components/Select.svelte ui/src/lib/components/Textarea.svelte` returns 3
    - `grep -c "background: var(--tr-bg)" ui/src/lib/components/Input.svelte ui/src/lib/components/Select.svelte ui/src/lib/components/Textarea.svelte` returns 0
    - `grep -A1 "\.invalid" ui/src/lib/components/Select.svelte ui/src/lib/components/Textarea.svelte` shows a `box-shadow: 0 0 0 3px var(--tr-danger-ring)` line in both
    - `node ui/scripts/check-tokens.mjs` exits 0
    - `pnpm --dir ui lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && node ui/scripts/check-tokens.mjs</automated>
  </verify>
  <done>Input/Select/Textarea all use var(--tr-surface)/var(--tr-border-strong) as their base bg/border, and all three show the danger-ring box-shadow on the invalid state.</done>
</task>

<task type="auto">
  <name>Task 2: Create Checkbox.svelte + Radio.svelte (new, D-04)</name>
  <files>ui/src/lib/components/Checkbox.svelte, ui/src/lib/components/Radio.svelte</files>
  <read_first>
    - ui/src/lib/components/Input.svelte — runes shape analog: `interface Props` + `$props()` +
      `$bindable()` conventions to replicate exactly (no Svelte 4 `export let`).
    - ui/src/lib/components/Select.svelte lines 7,9-25 — `Snippet` children pattern
      (`import type { Snippet } from 'svelte'`, `children?: Snippet`, `{@render children?.()}`).
    - .planning/phases/24-base-components/24-RESEARCH.md — "Pattern 2: Checkbox/Radio — hidden native input +
      styled sibling span" and the "Fields" section's exact box/checkmark/radio-dot/row values.
    - .planning/phases/24-base-components/24-PATTERNS.md — "Checkbox.svelte (NEW component)" and
      "Radio.svelte (NEW component)" sections: full synthesized prop-shape example and exact value list.
    - ui/src/styles/global.scss lines 40-43 — global `*:focus-visible` fallback pattern (for reference; this
      component overrides it locally on `.box`, matching Input.svelte's own `&:focus-visible` override
      convention).
  </read_first>
  <action>
    Create `Checkbox.svelte` with `interface Props { checked?: boolean; disabled?: boolean; invalid?: boolean;
    id?: string; onchange?: (_checked: boolean) => void; children?: Snippet }`, destructured as
    `checked = $bindable(false), disabled = false, invalid = false, id, onchange, children`. Template: a
    `<label class="check-row" class:disabled>` wrapping a `<span class="box-wrap">` containing
    `<input type="checkbox" bind:checked={checked} {disabled} {id} class="native-input" onchange={() =>
    onchange?.(checked)} />` plus a decorative `<span class="box" class:invalid aria-hidden="true"></span>`,
    followed by `{@render children?.()}` for the label text. CSS values (transcribed from `Fields.dc.html`
    `box()`, verbatim, do not recompute): `.box` — `width:18px; height:18px; flex:none; display:inline-flex;
    align-items:center; justify-content:center; border:1.5px solid var(--tr-border-strong); background:
    var(--tr-surface); box-sizing:border-box; border-radius:5px;`. Driven purely via CSS sibling selectors on
    `.native-input`: `:checked ~ .box { background: var(--tr-accent); border-color: var(--tr-accent); }` (plus
    a checkmark pseudo-element or nested span: `width:10px; height:6px; border-left:2px solid
    var(--tr-on-accent); border-bottom:2px solid var(--tr-on-accent); transform: rotate(-45deg)
    translate(0,-1px);` shown only when checked); `:focus-visible ~ .box { box-shadow: 0 0 0 3px
    var(--tr-focus-ring); border-color: var(--tr-accent); }`; `:disabled ~ .box { background:
    var(--tr-surface-sunken); border-color: var(--tr-border); }`. `.box.invalid` (when `invalid` prop true):
    `border-color: var(--tr-danger); box-shadow: 0 0 0 3px var(--tr-danger-ring);` (Checkbox/Radio have no
    explicit error state in the reference — reuse the Input/Select/Textarea danger pair per PATTERNS.md's
    guidance). `.check-row`: `display:inline-flex; align-items:center; gap:10px; font-size:14px; color:
    var(--tr-text-primary); cursor:pointer;`. `.check-row.disabled`: `color: var(--tr-text-disabled);
    cursor:not-allowed;`. Do NOT add `role="checkbox"` or manual `onkeydown` handling — the native input
    supplies all keyboard/a11y behavior for free.

    Create `Radio.svelte` with the same runes shape but `group = $bindable<string | number | null>(null)` (no
    `checked` prop), plus `value: string | number` (required, the radio's own value), `disabled?: boolean`,
    `invalid?: boolean`, `id?: string`, `children?: Snippet` — no `name` prop; forward
    `<input type="radio" bind:group={group} {value} {disabled} {id} class="native-input" />` and let Svelte's
    native `bind:group` compile the correct grouping/`checked` wiring (do not invent a custom event-based
    group-sync mechanism, per RESEARCH.md Open Question 2's resolution). Same `.box` base as Checkbox but
    `border-radius: 50%`; checked state shows an inner dot instead of a checkmark:
    `width:8px; height:8px; border-radius:50%; background: var(--tr-on-accent);`. All other state values
    (focus/disabled/invalid/row) identical to Checkbox's.
  </action>
  <acceptance_criteria>
    - `grep -c "type=\"checkbox\"" ui/src/lib/components/Checkbox.svelte` returns 1
    - `grep -c "type=\"radio\"" ui/src/lib/components/Radio.svelte` returns 1
    - `grep -c "bind:group" ui/src/lib/components/Radio.svelte` returns 1
    - `grep -c "role=\"checkbox\"\|role=\"radio\"\|onkeydown" ui/src/lib/components/Checkbox.svelte ui/src/lib/components/Radio.svelte` returns 0 (confirms native-input-only approach, no hand-rolled ARIA)
    - `grep -c "\\$bindable" ui/src/lib/components/Checkbox.svelte ui/src/lib/components/Radio.svelte` returns 2 (one bindable each: `checked`, `group`)
    - `node ui/scripts/check-tokens.mjs` exits 0
    - `pnpm --dir ui lint` exits 0
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs</automated>
  </verify>
  <done>Checkbox.svelte and Radio.svelte exist, each wrapping a visually-hidden native input, styled entirely via CSS sibling selectors, with no hand-rolled ARIA/keyboard handling.</done>
</task>

<task type="auto">
  <name>Task 3: Create FieldsSection.svelte showcase section</name>
  <files>ui/src/features/showcase/sections/FieldsSection.svelte</files>
  <read_first>
    - ui/src/lib/components/Input.svelte, Select.svelte, Textarea.svelte — corrected in Task 1; props:
      `value` (bindable), `disabled`, `invalid`, `id`, `placeholder`/`children` per component.
    - ui/src/lib/components/Checkbox.svelte, Radio.svelte — created in Task 2; props per Task 2's action.
    - .planning/phases/24-base-components/24-VALIDATION.md — "Manual-Only Verifications": normal/error/
      disabled are prop-driven (need static instances); focus is interaction-driven (tab through live,
      no separate static instance needed).
  </read_first>
  <action>
    Create `ui/src/features/showcase/sections/FieldsSection.svelte`, self-contained (no props). Structure:
    `<h2>Поля ввода</h2>` heading, then one labeled block per field type (`Input`, `Select`, `Textarea`,
    `Checkbox`, `Radio`), each showing 3 static instances side by side: normal, `invalid` (error state),
    `disabled` — labeled "Обычное"/"Ошибка"/"Отключено" so the grid is self-documenting; focus is tested live
    by tabbing through the normal instances during manual verification (do not attempt to force a static
    focus-ring screenshot). Use local `let` bindings (`$state`) only where a component requires a bindable
    value (e.g. `let inputValue = $state('Пример текста')`, `let checked = $state(true)` for the "normal"
    Checkbox instance so its checked visual is visible) — keep it minimal, this is static demo content, not
    an interactive form. For Radio, render at least 2 `<Radio>` instances sharing one `group = $state(...)`
    variable to demonstrate mutual exclusivity. Import all 5 components from `'$lib/components/*.svelte'`.
  </action>
  <acceptance_criteria>
    - `grep -c "<Input\|<Select\|<Textarea\|<Checkbox\|<Radio" ui/src/features/showcase/sections/FieldsSection.svelte` returns at least 15 (5 field types x 3 static states)
    - `grep -c "invalid" ui/src/features/showcase/sections/FieldsSection.svelte` returns at least 5 (one invalid instance per field type)
    - `grep -c "disabled" ui/src/features/showcase/sections/FieldsSection.svelte` returns at least 5
    - `pnpm --dir ui svelte-check` exits 0 (compiles standalone)
    - `pnpm --dir ui lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>FieldsSection.svelte exists, renders Input/Select/Textarea/Checkbox/Radio each in normal/error/disabled static states, compiles standalone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Presentational form primitives + a static demo section. Checkbox/Radio accept no untrusted external data — `checked`/`group` are local component state driven by the parent, exactly like the existing Input/Select/Textarea `value` prop. No new data flow, no new API surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-03-01 | Spoofing (a11y/UX) | Checkbox.svelte, Radio.svelte | mitigate | Use native `<input type=checkbox\|radio>` (Pattern 2) rather than a hand-rolled `role="checkbox"` div — avoids a custom widget silently failing to announce state changes to assistive tech, which would be a real (if minor) integrity issue for users relying on a screen reader to know a checkbox's true state. |
| T-24-03-02 | Tampering | Input/Select/Textarea CSS values | accept | Token values transcribed verbatim from a reviewed, version-controlled reference file. |

No ASVS-relevant control surface (auth, session, crypto, server-side input validation) is touched by this plan.
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs` all exit 0. Manual
(deferred to Plan 07's showcase-assembly checkpoint): every field type's normal/focus/error/disabled states
match `Fields.dc.html` in both themes; Space toggles Checkbox, arrow keys move within a Radio group, both
without any custom keydown handler in the component.
</verification>

<success_criteria>
Input/Select/Textarea use the corrected `--tr-surface`/`--tr-border-strong` tokens; Checkbox.svelte and
Radio.svelte exist as new native-input-backed primitives; FieldsSection.svelte renders all 5 field types
across normal/error/disabled states, ready for Plan 07 to wire into the showcase route.
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-03-SUMMARY.md` when done
</output>
