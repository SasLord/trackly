---
phase: 24-base-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/Button.svelte
  - ui/src/features/showcase/sections/ButtonsSection.svelte
autonomous: true
requirements: [CMP-01]

must_haves:
  truths:
    - "Each of the 5 button variants (primary/secondary/destructive/ghost/link), in both sizes (sm/md), is visually distinguishable in its default, hover, focus, active, disabled, and loading state."
    - "Button transitions are smooth (.12s) again, not instant, matching the reference — while still being suppressed for ~1 frame during a theme switch (handled by Plan 01, not this plan)."
  artifacts:
    - path: "ui/src/lib/components/Button.svelte"
      provides: "5 variants x 2 sizes x 6 states transcribed from Buttons.dc.html"
      contains: "transition: background .12s, box-shadow .12s"
    - path: "ui/src/features/showcase/sections/ButtonsSection.svelte"
      provides: "Showcase gallery section rendering every variant x size x static-state combination"
      min_lines: 30
  key_links:
    - from: "ui/src/features/showcase/sections/ButtonsSection.svelte"
      to: "ui/src/lib/components/Button.svelte"
      via: "import + <Button variant=... size=... disabled/loading> instances"
      pattern: "import Button from '\\$lib/components/Button.svelte'"
---

<objective>
Bring `Button.svelte` into exact visual conformance with `Buttons.dc.html` (CMP-01): restore the `.12s`
micro-transition, fix the disabled opacity (0.5 -> 0.45), fix `secondary`'s background (transparent ->
`var(--tr-surface)`), fix `ghost`'s hover background token, rebuild `link`'s underline behavior (default
underlined, focus removes it — currently inverted), and add the missing `:active` (pressed) state to every
variant. Then build the "Кнопки" showcase section that makes every variant x size x state combination
visually inspectable.

Purpose: Button is reused by every screen and by Modal's footer — getting this transcription exactly right
fixes the visual language everywhere Button is already used, with zero call-site changes (props are unchanged).
Output: Corrected `Button.svelte`; new `ButtonsSection.svelte` showcase section (self-contained, no props).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-RESEARCH.md
@.planning/phases/24-base-components/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe Buttons.dc values into Button.svelte (CMP-01)</name>
  <files>ui/src/lib/components/Button.svelte</files>
  <read_first>
    - ui/src/lib/components/Button.svelte — full current file (138 lines); note line 46 `transition: none`,
      line 51 `opacity: 0.5`, line 89 `background: transparent` (secondary), no `:active` rules anywhere,
      `.btn-ghost:hover` uses `--tr-surface` (line 118), `.btn-link` has inverted underline logic (lines
      125-137).
    - .planning/reference/design-system-v2/Buttons.dc.html — pixel source of truth; embedded
      `<script type="text/x-dc" data-dc-script>` `styleFor(variant,state,size)`/`base()` functions.
    - .planning/phases/24-base-components/24-RESEARCH.md — "Button (`Buttons.dc.html`, CMP-01)" section:
      full base + per-variant x per-state transcription table, already extracted verbatim from the `.dc` file.
    - ui/src/styles/_tokens.scss — confirm `--tr-accent-hover`, `--tr-accent-active`, `--tr-danger-hover`,
      `--tr-danger-active`, `--tr-danger-ring` (0.2 alpha, canonical — do NOT use the `.dc` file's own
      `.32`/`.42` ring alpha, see RESEARCH.md Token Mismatches).
    - ui/src/lib/components/Spinner.svelte — unchanged, already correct for the loading-state spinner.
  </read_first>
  <action>
    Base `.btn` rule: change `border: none` to `border: 1px solid transparent`; change
    `transition: none; // Theme switch...` to `transition: background .12s, box-shadow .12s;` (delete the
    stale comment — D-09 reverses the no-transitions directive project-wide); change `&:disabled { opacity:
    0.5; ... }` to `opacity: 0.45;`; change `&.loading { cursor: wait; pointer-events: none; }` to
    `opacity: 0.85; cursor: default; pointer-events: none;` (loading dims uniformly across all variants at
    the base-rule level — no per-variant loading override needed).

    Per-variant changes (add `&:active:not(:disabled)` blocks that do not currently exist anywhere in the
    file; add explicit `border-color` where the base transparent border needs to match the variant's own
    color; fix drifted values):
    - `.btn-primary`: add `border-color: var(--tr-accent);` to the base rule. Add
      `&:active:not(:disabled) { background: var(--tr-accent-active); border-color: var(--tr-accent-active); }`.
      Hover/focus-visible already correct — no change.
    - `.btn-secondary`: change `background: transparent` to `background: var(--tr-surface);`. Add
      `&:active:not(:disabled) { background: var(--tr-surface-sunken); border-color: var(--tr-text-tertiary); }`.
      Add `border-color: var(--tr-accent);` inside the existing `&:focus-visible` block (alongside the
      existing box-shadow ring — this is the "focusBorder" behavior unique to secondary/ghost).
    - `.btn-destructive`: add `border-color: var(--tr-danger);` to the base rule. Replace
      `&:hover:not(:disabled) { filter: brightness(0.92); }` with
      `&:hover:not(:disabled) { background: var(--tr-danger-hover); border-color: var(--tr-danger-hover); }`.
      Add `&:active:not(:disabled) { background: var(--tr-danger-active); border-color: var(--tr-danger-active); }`.
      Focus-visible ring already uses `var(--tr-danger-ring)` which already resolves to the canonical 0.2
      alpha in `_tokens.scss` — no change needed there.
    - `.btn-ghost`: change `&:hover:not(:disabled) { background: var(--tr-surface); }` to
      `background: var(--tr-surface-sunken);` (current value is wrong — spec wants surface-sunken, not
      surface). Add `&:active:not(:disabled) { background: var(--tr-surface-sunken); border-color:
      var(--tr-border); }`. Add `border-color: var(--tr-accent);` inside the existing `&:focus-visible` block.
      Add `&:disabled { color: var(--tr-text-disabled); }` (ghost-specific disabled color override, on top
      of the base opacity rule).
    - `.btn-link`: rework entirely — default state needs `text-decoration: underline; text-underline-offset:
      2px; padding: 2px 2px; height: auto;` (currently underline only appears on hover, which is backwards).
      Change `&:hover:not(:disabled) { text-decoration: underline; }` to
      `&:hover:not(:disabled) { color: var(--tr-accent-hover); }`. Add
      `&:active:not(:disabled) { color: var(--tr-accent-active); }`. Change `&:focus-visible` to add
      `border-radius: 4px; text-decoration: none;` alongside the existing box-shadow ring (focus removes the
      underline and adds a focus radius — this is unique to link). Add
      `&:disabled { color: var(--tr-text-disabled); text-decoration: none; }` and
      `&.loading { color: var(--tr-text-tertiary); text-decoration: none; }` (link needs its own explicit
      loading/disabled color overrides since the base opacity-dim approach doesn't apply well to a
      no-background text-only variant).
  </action>
  <acceptance_criteria>
    - `grep -n "transition: background .12s, box-shadow .12s" ui/src/lib/components/Button.svelte` matches
    - `grep -c "opacity: 0.5" ui/src/lib/components/Button.svelte` returns 0
    - `grep -c "opacity: 0.45" ui/src/lib/components/Button.svelte` returns 1 (or 2 if link/ghost duplicate it — check both, base rule is authoritative)
    - `grep -c "&:active:not(:disabled)" ui/src/lib/components/Button.svelte` returns 5 (one per variant)
    - `grep -c "background: transparent" ui/src/lib/components/Button.svelte` returns 2 (only `.btn-ghost`
      and `.btn-link` keep transparent backgrounds; `.btn-secondary` must NOT be in this count)
    - `node ui/scripts/check-tokens.mjs` exits 0 (no raw hex/rgba introduced)
    - `pnpm --dir ui lint` exits 0
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs</automated>
  </verify>
  <done>Button.svelte has border:1px solid transparent + .12s transitions + 0.45 disabled opacity + 5 :active rules + corrected secondary/ghost/link colors, with zero raw color literals.</done>
</task>

<task type="auto">
  <name>Task 2: Create ButtonsSection.svelte showcase section</name>
  <files>ui/src/features/showcase/sections/ButtonsSection.svelte</files>
  <read_first>
    - ui/src/lib/components/Button.svelte — corrected in Task 1; props: `variant`, `size`, `loading`,
      `disabled`, `type`, `onclick`, `children` (Snippet).
    - .planning/phases/24-base-components/24-PATTERNS.md — "Svelte 5 runes prop shape" + "Variant-driven CSS
      class naming" shared patterns; this section has no props of its own (pure static demo content, no
      `$props()` needed at all).
    - .planning/phases/24-base-components/24-VALIDATION.md — "Manual-Only Verifications" row for CMP-01:
      hover/focus/active are tested live by the human verifier (interactive pseudo-classes), NOT rendered as
      separate static instances — only `disabled` and `loading` need explicit static instances since those
      are prop-driven, not interaction-driven.
  </read_first>
  <action>
    Create `ui/src/features/showcase/sections/ButtonsSection.svelte` as a self-contained Svelte component
    (no props, default export via the file itself — matches every other `.svelte` component convention, no
    `$props()` call needed). Structure: one `<h2>Кнопки</h2>` heading, then for each of the 5 variants
    (`primary`, `secondary`, `destructive`, `ghost`, `link`) render a labeled row containing, for each of the
    2 sizes (`sm`, `md`): 3 static `<Button>` instances side by side — one plain (interactive: hover/focus/
    active testable live by tabbing/hovering), one with `disabled`, one with `loading` — each button's
    visible label text states which state it represents (e.g. "Обычная", "Отключена", "Загрузка") so the
    grid is self-documenting during manual verification. Import `Button` from `'$lib/components/Button.svelte'`.
    Do not add any interactive JS state (`$state`) — this section is pure static markup, matching the
    "pure presentational content" note in PATTERNS.md for the showcase feature body.
  </action>
  <acceptance_criteria>
    - `grep -c "<Button" ui/src/features/showcase/sections/ButtonsSection.svelte` returns at least 30 (5
      variants x 2 sizes x 3 static states)
    - `grep -c "variant=\"primary\"" ui/src/features/showcase/sections/ButtonsSection.svelte` returns at
      least 1 (and similarly all 5 variant names appear at least once — spot check via grep)
    - File has no `$props()` call (`grep -c "\\$props" ...ButtonsSection.svelte` returns 0)
    - `pnpm --dir ui svelte-check` exits 0 (component compiles standalone even though not yet imported anywhere)
    - `pnpm --dir ui lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>ButtonsSection.svelte exists, renders all 5 variants x 2 sizes x 3 static states (normal/disabled/loading) with self-documenting labels, compiles standalone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure presentational component styling + a static demo section with hardcoded Russian labels. No user input, no data fetch, no new API surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-02-01 | Tampering | Button.svelte CSS values | accept | Values are transcribed verbatim from a reviewed, version-controlled reference file (`Buttons.dc.html`); no external/untrusted input involved. |

No ASVS-relevant control surface is touched by this plan.
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui svelte-check` exit 0. Manual (deferred to Plan 07's showcase-assembly
checkpoint, since the section isn't routable until then): once wired up, every Button cell in the showcase
visually matches `Buttons.dc.html` in both themes.
</verification>

<success_criteria>
Button.svelte's 5 variants x 2 sizes x 6 states (default/hover/focus/active/disabled/loading) match
`Buttons.dc.html` values exactly; ButtonsSection.svelte exists and renders every variant x size x
disabled/loading combination, ready to be wired into the showcase route by Plan 07.
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-02-SUMMARY.md` when done
</output>
