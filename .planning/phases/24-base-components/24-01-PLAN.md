---
phase: 24-base-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/styles/_tokens.scss
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - ui/src/lib/stores/theme.svelte.ts
  - ui/src/styles/global.scss
autonomous: true
requirements: [CMP-03, CMP-04]

must_haves:
  truths:
    - "The token vocabulary contains --tr-accent-text (light + dark), so Badge and Tabs can reference an accent-tinted text color without violating the closed-world lint gate."
    - "REQUIREMENTS.md and ROADMAP.md describe Badge as 5 tones, not 4, matching the actual Badges.dc reference and current Badge.svelte."
    - "Switching the theme (light↔dark) does not produce a visible color-transition sweep across interactive primitives, once those primitives regain their .12s micro-transitions in later plans."
  artifacts:
    - path: "ui/src/styles/_tokens.scss"
      provides: "--tr-accent-text token pair (light #2350bd / dark #8fb0ff)"
      contains: "--tr-accent-text"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CMP-03 corrected to 5 тонов"
      contains: "5 тонов"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 Success Criteria #3 corrected to 5 тонах"
      contains: "5 тонах"
    - path: "ui/src/lib/stores/theme.svelte.ts"
      provides: "applyResolved() toggles .theme-switching class around the dataset mutation"
      contains: "theme-switching"
    - path: "ui/src/styles/global.scss"
      provides: "Global .theme-switching rule forcing transition:none during theme swap"
      contains: ".theme-switching"
  key_links:
    - from: "ui/src/lib/stores/theme.svelte.ts"
      to: "ui/src/styles/global.scss"
      via: "documentElement.classList add/remove('theme-switching') matched by a :global(.theme-switching) CSS rule"
      pattern: "theme-switching"
---

<objective>
Lay the two pieces of shared infrastructure every other Phase 24 plan depends on: (1) add the missing
`--tr-accent-text` design token (needed by the Badge and Tabs plans, Wave 2) and correct the "4 тона" wording
in REQUIREMENTS.md/ROADMAP.md that CONTEXT.md's D-06 flags as a requirement-text bug; (2) implement D-09's
theme-switch transition-suppression hook so that later plans can safely restore `.12s` micro-transitions to
Button/Fields/Badge/Tabs without a visible color-smear when the user toggles light/dark.

Purpose: Unblock Wave 2 (Badge, Tabs) on the token, and unblock every component plan on the transition
behavior, without those plans having to touch shared global files themselves (avoids file-overlap contention
across parallel plans).
Output: `_tokens.scss` with `--tr-accent-text`; corrected requirement/roadmap text; `theme.svelte.ts` +
`global.scss` wired for one-frame transition suppression on theme change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --tr-accent-text token + correct CMP-03 tone count (D-06)</name>
  <files>ui/src/styles/_tokens.scss, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - ui/src/styles/_tokens.scss — canonical token file; `:root, [data-theme='light']` block ends line 84,
      `[data-theme='dark']` block ends line 159; `--tr-focus-ring` sits at line 20 (light) / line 93 (dark)
      inside the "Brand / Accent" group — the new token belongs in that same group.
    - .planning/phases/24-base-components/24-RESEARCH.md — "Token Mismatches" table: `--tr-accent-text`
      confirmed `#2350bd` (light) / `#8fb0ff` (dark), agreeing across `Badges.dc.html` and `Tabs.dc.html`.
    - .planning/REQUIREMENTS.md — line 25, current text: "**CMP-03**: Бейджи-статусы — 4 тона в вариантах
      мягкая подложка / сплошной / с точкой / счётчик-пилюля".
    - .planning/ROADMAP.md — Phase 24 Success Criteria #3, current text: "Бейджи-статусы рендерятся в 4
      тонах в вариантах мягкая подложка/сплошной/с точкой/счётчик-пилюля."
  </read_first>
  <action>
    In `ui/src/styles/_tokens.scss`: add `--tr-accent-text: #2350bd;` immediately after the
    `--tr-accent-active`/`--tr-focus-ring` group in the `:root, [data-theme='light']` block (near line 20),
    and `--tr-accent-text: #8fb0ff;` in the equivalent spot in `[data-theme='dark']` (near line 93). Do not
    recompute these values — they are transcribed verbatim from `Badges.dc.html`/`Tabs.dc.html` per
    RESEARCH.md's Token Mismatches table (both files agree). Do not touch any other token line.

    In `.planning/REQUIREMENTS.md`: change CMP-03's "4 тона" to "5 тонов" (line ~25) — text becomes
    "Бейджи-статусы — 5 тонов в вариантах мягкая подложка / сплошной / с точкой / счётчик-пилюля". This is a
    requirement-text correction per D-06 (CONTEXT.md), not a scope change — Badge.svelte already implements
    5 tones (neutral/accent/success/warning/danger).

    In `.planning/ROADMAP.md`: change Phase 24 Success Criteria #3 "рендерятся в 4 тонах" to "рендерятся в 5
    тонах" (same section, "### Phase 24: Базовые компоненты").
  </action>
  <acceptance_criteria>
    - `grep -c "\-\-tr-accent-text" ui/src/styles/_tokens.scss` returns 2 (one per theme block)
    - `grep "accent-text" ui/src/styles/_tokens.scss` shows `#2350bd` under the light block and `#8fb0ff`
      under the dark block (verify by line-range context, not just presence)
    - `grep "5 тонов" .planning/REQUIREMENTS.md` matches the CMP-03 line
    - `grep "5 тонах" .planning/ROADMAP.md` matches the Phase 24 Success Criteria #3 line
    - `grep -c "4 тона" .planning/REQUIREMENTS.md .planning/ROADMAP.md` returns 0 for both files combined
      (no stray "4 тона"/"4 тонах" left behind)
    - `node ui/scripts/check-tokens.mjs` exits 0 (new token is real CSS, no rule violated by adding it)
  </acceptance_criteria>
  <verify>
    <automated>node ui/scripts/check-tokens.mjs && grep -c "\-\-tr-accent-text" ui/src/styles/_tokens.scss</automated>
  </verify>
  <done>--tr-accent-text exists in both theme blocks with the correct hex values; REQUIREMENTS.md/ROADMAP.md say "5 тонов"/"5 тонах" with zero remaining "4 тона" references.</done>
</task>

<task type="auto">
  <name>Task 2: Theme-switch transition suppression hook (D-09)</name>
  <files>ui/src/lib/stores/theme.svelte.ts, ui/src/styles/global.scss</files>
  <read_first>
    - ui/src/lib/stores/theme.svelte.ts — full file (35 lines); `applyResolved()` (lines 30-35) is the single
      function that mutates `document.documentElement.dataset.theme` — the exact hook point.
    - ui/src/styles/global.scss — lines 47-56 contain the existing `@media (prefers-reduced-motion: reduce)`
      block; the new `.theme-switching` rule goes directly after it (before the "Skip link" section at
      line 58), per RESEARCH.md Pattern 4.
    - .planning/phases/24-base-components/24-RESEARCH.md — "Pattern 4: Theme-switch transition suppression
      (D-09)" — exact rationale for using `requestAnimationFrame` (not `setTimeout`) to remove the class only
      after the browser has painted the new theme's colors.
  </read_first>
  <action>
    In `theme.svelte.ts`'s `applyResolved()`: before `document.documentElement.dataset.theme = r;`, add
    `document.documentElement.classList.add('theme-switching');`. After that assignment, call
    `requestAnimationFrame(() => { document.documentElement.classList.remove('theme-switching'); });`. Do not
    change `initTheme()` or `setTheme()` — both already call `applyResolved()`, so the suppression applies on
    first load and every subsequent switch automatically.

    In `global.scss`: add a new rule immediately after the `@media (prefers-reduced-motion: reduce)` block
    (after its closing `}` at line 56, before the "Skip link" comment): a `:global(.theme-switching)` /
    `:global(.theme-switching) *` selector pair setting `transition: none !important;`. This must NOT replace
    or alter the existing reduced-motion block — both coexist: reduced-motion permanently zeroes transitions
    for users with that OS preference; `.theme-switching` only fires for the ~1-frame window around a theme
    toggle for everyone else.
  </action>
  <acceptance_criteria>
    - `grep -n "theme-switching" ui/src/lib/stores/theme.svelte.ts` shows both an `add(` and a `remove(` call
    - `grep -n "requestAnimationFrame" ui/src/lib/stores/theme.svelte.ts` returns exactly 1 match
    - `grep -n "theme-switching" ui/src/styles/global.scss` shows a `:global(.theme-switching)` rule with
      `transition: none !important`
    - `grep -c "prefers-reduced-motion" ui/src/styles/global.scss` still returns 1 (block preserved, not
      removed or duplicated)
    - `pnpm --dir ui lint` exits 0
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>applyResolved() wraps the dataset.theme mutation with add/rAF-remove of .theme-switching; global.scss has a matching transition-killing rule that does not disturb the existing prefers-reduced-motion block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan edits a CSS token file, two planning-doc `.md` files, and a client-side theme-preference store. No user input is accepted, no network/API call is added or changed, no data is persisted beyond the existing `localStorage['trackly:theme']` write that already existed before this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-01-01 | Information Disclosure | `_tokens.scss` new token | accept | `--tr-accent-text` is a public UI color value (not a secret); no risk from exposure — it ships in the built CSS regardless. |
| T-24-01-02 | Denial of Service (perf) | `global.scss` `.theme-switching` universal selector | accept | `* { transition: none }` is scoped to a single rAF-frame window (added then removed on next frame) and only activates on explicit theme toggle — negligible, bounded cost, matches the existing `prefers-reduced-motion` block's own universal-selector pattern already shipped in Phase 23. |

No ASVS-relevant control surface (auth, session, input validation, crypto) is touched by this plan.
</threat_model>

<verification>
`node ui/scripts/check-tokens.mjs` passes (Rule 3 closed-world gate accepts the new token because it's
defined, not just referenced). `pnpm --dir ui lint && pnpm --dir ui svelte-check` both exit 0. Manual
spot-check: toggle theme via `ThemeSwitcher` in a running dev build and confirm no `.theme-switching` class
remains attached to `<html>` after ~1 frame (DevTools Elements panel).
</verification>

<success_criteria>
`--tr-accent-text` resolves in both themes; REQUIREMENTS.md/ROADMAP.md read "5 тонов"/"5 тонах" with no
"4 тона" text remaining anywhere in either file; theme toggling wraps the dataset mutation in a one-frame
transition-suppression class without touching the reduced-motion block.
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-01-SUMMARY.md` when done
</output>
