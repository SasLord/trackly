---
phase: 24-base-components
plan: 06
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - ui/src/lib/components/Tabs.svelte
  - ui/src/features/showcase/sections/TabsSection.svelte
autonomous: true
requirements: [CMP-04]

must_haves:
  truths:
    - "Tabs supports both an 'underline' variant (switch-bar with count badges and an active underline) and a 'segmented' variant (pill group), per D-05."
    - "The underline variant's active tab shows a count badge and an accent underline; the segmented variant's active segment shows a raised surface with a shadow."
  artifacts:
    - path: "ui/src/lib/components/Tabs.svelte"
      provides: "New Tabs primitive, variant: 'underline' | 'segmented', tabs: {key,label,count?,disabled?}[]"
      contains: "variant"
    - path: "ui/src/features/showcase/sections/TabsSection.svelte"
      provides: "Showcase section covering both variants, with count badges and an active-tab switcher"
      min_lines: 20
  key_links:
    - from: "ui/src/lib/components/Tabs.svelte"
      to: "ui/src/styles/_tokens.scss"
      via: "var(--tr-accent-text) (added by Plan 24-01) + var(--tr-elev-1) substitution for the unmatched raw rgba shadow"
      pattern: "\\-\\-tr-accent-text|\\-\\-tr-elev-1"
---

<objective>
Create `Tabs.svelte` from scratch (no prior primitive exists — only feature-local hardcoded tab bars) with
both styles required by D-05: `underline` (switch-bar with counters + active underline, CMP-04's literal
requirement) and `segmented` (pill group, `.dc`'s "Сегментированный вариант"). Then build the "Вкладки"
showcase section demonstrating both, with interactive tab switching.

Purpose: Tabs generalizes the interaction shape already hand-rolled 4 times in the app
(`RequestsSearchAndTabs`, `CartridgesSearchAndTabs`, `SettingsSubNav`, `ActsSearchAndTabs`) into one reusable
primitive — those 4 files are explicitly NOT retrofitted in this phase (D-07); this plan only builds the new
component + its own demo.
Output: New `Tabs.svelte`; new `TabsSection.svelte`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-base-components/24-CONTEXT.md
@.planning/phases/24-base-components/24-RESEARCH.md
@.planning/phases/24-base-components/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tabs.svelte (underline + segmented, D-05)</name>
  <files>ui/src/lib/components/Tabs.svelte</files>
  <read_first>
    - ui/src/features/requests/RequestsSearchAndTabs.svelte lines 21-58 — interaction-shape analog: click
      handler + `role="tablist"`/`role="tab"`/`aria-selected` pattern to generalize (do not modify this file).
    - ui/src/lib/components/ThemeSwitcher.svelte — full file (71 lines), segmented-variant implementation
      analog: `role="group"` + `class:active` + click-to-select shape for the `segmented` variant.
    - ui/src/lib/components/Button.svelte lines 5-28 (post-Plan-24-02 correction) — variant-prop-to-CSS-class
      convention (`class="tabs tabs-{variant}"`) to replicate.
    - ui/src/styles/_tokens.scss — confirm `--tr-accent-text` (added by Plan 24-01, this plan's dependency),
      `--tr-accent-soft`, `--tr-focus-ring`, `--tr-elev-1` (nearest existing shadow token, used as the
      resolved substitute for the reference's raw, non-tokenized `rgba(16,22,34,.12)` segmented-active shadow
      — see RESEARCH.md Open Question 1, already resolved: use `var(--tr-elev-1)`, do NOT paste the raw rgba).
    - .planning/reference/design-system-v2/Tabs.dc.html — pixel source of truth for both `tabStyle`/
      `badgeStyle` (underline) and `segStyle` (segmented).
    - .planning/phases/24-base-components/24-RESEARCH.md — "Tabs" section: full underline + segmented value
      tables, already extracted verbatim.
  </read_first>
  <action>
    Create `ui/src/lib/components/Tabs.svelte` with `interface Props { variant?: 'underline' | 'segmented';
    tabs: { key: string; label: string; count?: number; disabled?: boolean }[]; active: string; onchange?:
    (_key: string) => void; ariaLabel?: string }`, destructured as `variant = 'underline', tabs, active =
    $bindable(), onchange, ariaLabel`. Template: a container `<div class="tabs tabs-{variant}" role={variant
    === 'segmented' ? 'group' : 'tablist'} aria-label={ariaLabel}>` with an `{#each tabs as tab (tab.key)}`
    loop rendering `<button type="button" class="tab" class:active={tab.key === active} disabled={tab.disabled}
    role={variant === 'segmented' ? undefined : 'tab'} aria-selected={variant === 'segmented' ? undefined :
    tab.key === active} aria-pressed={variant === 'segmented' ? tab.key === active : undefined}
    onclick={() => { if (!tab.disabled) { active = tab.key; onchange?.(tab.key); } }}>` containing
    `{tab.label}` and, when `variant === 'underline' && tab.count != null`, a trailing `<span
    class="tab-count">{tab.count}</span>`.

    CSS — `underline` variant (`.tabs-underline .tab`, transcribed from `Tabs.dc.html` `tabStyle(state)`
    verbatim): `display:inline-flex; align-items:center; gap:6px; height:34px; padding:0 12px;
    background:transparent; border:none; border-bottom:2px solid transparent; margin-bottom:-1px;
    font-size:14px; font-weight:500; color: var(--tr-text-secondary); cursor:pointer; white-space:nowrap;
    border-radius:6px 6px 0 0; outline:none; transition: background .12s, box-shadow .12s;` (transition added
    per D-09 — restored, not omitted). `&.active`: `color: var(--tr-accent-text); border-bottom-color:
    var(--tr-accent); font-weight:600;`. `&:hover:not(.active):not(:disabled)`: `color: var(--tr-text-primary);
    background: var(--tr-surface-sunken);`. `&:focus-visible`: `color: var(--tr-text-primary); box-shadow: 0
    0 0 3px var(--tr-focus-ring);`. `&:disabled`: `color: var(--tr-text-disabled); cursor:not-allowed;`.
    `.tab-count`: `display:inline-flex; align-items:center; justify-content:center; min-width:18px;
    height:18px; padding:0 5px; border-radius:9px; font-size:11px; font-weight:600; line-height:1;
    background: var(--tr-surface-sunken); color: var(--tr-text-secondary);`, with `.tab.active .tab-count`:
    `background: var(--tr-accent-soft); color: var(--tr-accent-text);` and `.tab:disabled .tab-count`:
    `color: var(--tr-text-disabled);`.

    CSS — `segmented` variant (`.tabs-segmented`, from `Tabs.dc.html` `segStyle(act)`): container
    `display:inline-flex; gap:3px; padding:3px; background: var(--tr-surface-sunken); border-radius:7px;`.
    `.tabs-segmented .tab`: `display:inline-flex; align-items:center; height:28px; padding:0 12px;
    border-radius:5px; font-size:13px; font-weight:600; cursor:pointer; background:transparent; border:none;
    color: var(--tr-text-secondary); transition: background .12s, box-shadow .12s;`. `&.active`: `background:
    var(--tr-surface); color: var(--tr-accent-text); box-shadow: var(--tr-elev-1);` (substituting the
    reference's raw, non-tokenized `rgba(16,22,34,.12)` shadow with the nearest existing token — required to
    pass `check-tokens.mjs` Rule 4, per RESEARCH.md's resolved Open Question 1; do not paste the raw rgba
    value). `&:focus-visible`: `box-shadow: 0 0 0 3px var(--tr-focus-ring);`.
  </action>
  <acceptance_criteria>
    - `grep -c "variant?: 'underline' | 'segmented'" ui/src/lib/components/Tabs.svelte` returns 1
    - `grep -c "var(--tr-accent-text)" ui/src/lib/components/Tabs.svelte` returns at least 2 (underline active + segmented active)
    - `grep -c "var(--tr-elev-1)" ui/src/lib/components/Tabs.svelte` returns 1 (segmented active shadow)
    - `grep -c "rgba(" ui/src/lib/components/Tabs.svelte` returns 0 (no raw color-function literal — Rule 4 gate)
    - `grep -c "role=\"tablist\"\|role=\"group\"" ui/src/lib/components/Tabs.svelte` returns at least 1 each (both roles present, conditionally rendered)
    - `node ui/scripts/check-tokens.mjs` exits 0
    - `pnpm --dir ui lint` exits 0
    - `pnpm --dir ui svelte-check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs</automated>
  </verify>
  <done>Tabs.svelte exists with both underline and segmented variants, count badges, active/hover/focus/disabled states, zero raw color literals, using var(--tr-accent-text) and var(--tr-elev-1).</done>
</task>

<task type="auto">
  <name>Task 2: Create TabsSection.svelte showcase section</name>
  <files>ui/src/features/showcase/sections/TabsSection.svelte</files>
  <read_first>
    - ui/src/lib/components/Tabs.svelte — created in Task 1; props: `variant`, `tabs`, `active` (bindable),
      `onchange`, `ariaLabel`.
    - .planning/phases/24-base-components/24-VALIDATION.md — "Manual-Only Verifications" for CMP-04: switch
      active tab live, confirm counter badges — this section must be genuinely interactive (real
      `active = $state(...)`), not a static screenshot-style grid.
  </read_first>
  <action>
    Create `ui/src/features/showcase/sections/TabsSection.svelte`, self-contained. Structure: `<h2>Вкладки</h2>`
    heading, two labeled sub-blocks: "Switch-bar (underline)" and "Сегментированный". For the underline demo,
    define a local `let underlineTabs = [{ key: 'all', label: 'Все', count: 12 }, { key: 'open', label:
    'Открытые', count: 4 }, { key: 'closed', label: 'Закрытые' }, { key: 'archived', label: 'Архив', disabled:
    true }]` and `let underlineActive = $state('all')`, rendering `<Tabs variant="underline" tabs=
    {underlineTabs} bind:active={underlineActive} />` — this demonstrates counters, an active underline, and a
    disabled tab, and is genuinely clickable (state is local, real). For the segmented demo, define
    `let segmentedTabs = [{ key: 'list', label: 'Список' }, { key: 'map', label: 'Карта' }, { key: 'table',
    label: 'Таблица' }]` and `let segmentedActive = $state('list')`, rendering `<Tabs variant="segmented"
    tabs={segmentedTabs} bind:active={segmentedActive} />`. Import `Tabs` from `'$lib/components/Tabs.svelte'`.
  </action>
  <acceptance_criteria>
    - `grep -c "<Tabs" ui/src/features/showcase/sections/TabsSection.svelte` returns 2 (one underline, one segmented)
    - `grep -c "variant=\"underline\"" ui/src/features/showcase/sections/TabsSection.svelte` returns 1
    - `grep -c "variant=\"segmented\"" ui/src/features/showcase/sections/TabsSection.svelte` returns 1
    - `grep -c "count:" ui/src/features/showcase/sections/TabsSection.svelte` returns at least 1 (counters demonstrated)
    - `grep -c "disabled: true" ui/src/features/showcase/sections/TabsSection.svelte` returns at least 1
    - `pnpm --dir ui svelte-check` exits 0 (compiles standalone)
    - `pnpm --dir ui lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir ui lint && pnpm --dir ui svelte-check</automated>
  </verify>
  <done>TabsSection.svelte exists with two live, clickable Tabs instances (underline with counters + a disabled tab, and segmented), compiles standalone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Tabs is a presentational navigation/switch component; the demo section's tab data is hardcoded static content, not user input. No data fetch, no new API surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-24-06-01 | Tampering | Tabs.svelte CSS values | accept | Token values transcribed verbatim from a reviewed reference file; the one non-tokenized value (segmented-active shadow) is deliberately substituted with the nearest existing design token (`--tr-elev-1`) rather than a raw literal, keeping the closed-token-vocabulary guarantee intact. |
| T-24-06-02 | Elevation of Privilege (N/A) | — | accept | Tabs has no role/permission logic; `active`/`onchange` are pure UI state, not authorization gates. |

No ASVS-relevant control surface is touched by this plan.
</threat_model>

<verification>
`pnpm --dir ui lint && pnpm --dir ui svelte-check && node ui/scripts/check-tokens.mjs` all exit 0. Manual
(deferred to Plan 07's showcase-assembly checkpoint): clicking an underline tab moves the active underline and
recolors the count badge; clicking a segmented tab moves the raised-surface highlight; the disabled tab in
the underline demo does not respond to clicks.
</verification>

<success_criteria>
Tabs.svelte exists with both `underline` and `segmented` variants matching `Tabs.dc.html`'s values exactly
(with the one documented, deliberate token substitution for the untokenized shadow); TabsSection.svelte
demonstrates both variants interactively, ready for Plan 07's showcase wiring.
</success_criteria>

<output>
Create `.planning/phases/24-base-components/24-06-SUMMARY.md` when done
</output>
