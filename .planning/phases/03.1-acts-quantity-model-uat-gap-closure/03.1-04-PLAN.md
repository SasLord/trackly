---
phase: 03.1-acts-quantity-model-uat-gap-closure
plan: 04
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - ui/src/lib/components/DatePicker.svelte
  - ui/src/features/acts/ActFormBody.svelte
  - ui/src/features/acts/ActFormModal.svelte
  - crates/trackly-app/src/dto/act.rs
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-core/src/domain/acts.rs
  - ui/src/bindings.ts
  - ui/src/lib/api/acts.ts
autonomous: false
requirements: [G-2, G-3]
threat_model_severity: low
tags: [phase-3.1, gap-closure, ui-shared-component, date-picker, act-form]

must_haves:
  truths:
    - "Новый shared компонент `ui/src/lib/components/DatePicker.svelte` — обёртка над native <input type='date'>, output value = ISO date string (YYYY-MM-DD), Russian locale aware."
    - "ActFormBody: поле «Когда отдали» (новое required) использует DatePicker, default = today (МСК), значение конвертируется в Unix seconds (UTC) и отправляется в payload как `handover_date_utc`."
    - "ActFormBody: поля «Дата» (deadline alias?) и «Сроком до» — оба переведены с plain Input на DatePicker (G-2 fix_pattern)."
    - "Backend: ActCreateDto расширен `handover_date_utc: Option<i64>` (Optional чтобы существующий код без поля не сломался; ActService::create использует now() если None — для backward-compat); persisted в `acts.handover_date_utc` (column из V015)."
    - "G-3 (qty bound stock): на новой G-12 model handover qty=N клонирует device, поэтому проблема «qty=5 при stock=1» не существует на уровне clone-логики; UI defensively добавляет `max` атрибут на qty input = достаточно большой (1000) — backend защищает MAX_CLONE_QTY от 03.1-01 T-03.1-02."
    - "**W-5 defensive UX:** для devices с `serial_number IS NOT NULL` UI рекомендованно enforce qty=1 (disabled max=1 на qty input). Серийный номер уникален к физическому устройству; клоны (qty>1) получат `serial_number = NULL` (см. plan 03.1-01 must_haves.notes), что обычно нежелательно для серийных устройств. Реализация: ActFormItemsTable читает `selectedDevice.serial_number`; если non-null → set max=1 + tooltip «У устройства есть серийный номер; для нескольких единиц используйте отдельные позиции»."
  artifacts:
    - path: ui/src/lib/components/DatePicker.svelte
      provides: "Shared DatePicker — обёртка над native input type=date"
      min_lines: 30
    - path: ui/src/features/acts/ActFormBody.svelte
      provides: "3 поля используют DatePicker (Дата, Сроком до, Когда отдали); handover_date_utc в submit payload"
    - path: crates/trackly-app/src/dto/act.rs
      provides: "ActCreateDto.handover_date_utc: Option<i64>"
  key_links:
    - from: "ui/src/lib/components/DatePicker.svelte"
      to: "ActFormBody.svelte"
      via: "import + 3-time usage"
      pattern: "DatePicker"
    - from: "ActCreateDto.handover_date_utc"
      to: "acts.handover_date_utc column (V015)"
      via: "ActService::create INSERT"
      pattern: "handover_date_utc"
---

<objective>
Закрыть G-2 (DatePicker UX для всех date-полей ActFormModal + новое «Когда отдали») и G-3 (qty bound). G-3 в основном решается на уровне G-12 архитектуры (clone-on-handover) — UI defensively ставит `max` на qty input для UX feedback, backend MAX_CLONE_QTY=1000 защищает defence-in-depth.

Purpose:
- G-2 (severity:major): пользователь хочет normal date-picker, а не plain text YYYY-MM-DD input; + новое поле «Когда отдали».
- G-3 (severity:major): qty не должно превышать наличие; на G-12 model это автоматически работает (clone N device-rows из stock), UI просто limits на forms-level.

Depends on:
- **03.1-01** — V015 миграция добавляет `acts.handover_date_utc` column + MAX_CLONE_QTY backend bound.

Output:
- DatePicker.svelte — shared компонент с Russian locale-aware native date picker.
- ActFormBody: 3 поля переведены на DatePicker; handover_date_utc собирается в payload.
- Backend: ActCreateDto.handover_date_utc + ActService::create вставляет в INSERT.
- bindings.ts regen.

**Autonomous:** false — manual smoke в Task 4 проверяет UAT G-2/G-3 в Tauri webview (date picker — UX-sensitive, нужен реальный browser engine).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-CONTEXT.md
@.planning/phases/03-pdf/03-UAT.md
@.planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-01-PLAN.md
@ui/src/features/acts/ActFormBody.svelte
@ui/src/features/acts/ActFormModal.svelte
@ui/src/features/acts/ActFormItemsTable.svelte
@ui/src/lib/components/Input.svelte
@crates/trackly-app/src/dto/act.rs
@crates/trackly-app/src/services/act_service.rs

<interfaces>
<!-- ActCreateDto (после 03.1-01) -->
ActCreateDto {
  number_override: number | null;
  giver_name: string;
  receiver_name: string;
  location_id: number | null;
  notes: string | null;
  deadline_utc: number | null;
  handover_date_utc?: number | null;  // NEW в этом плане
  items: ActItemNewDto[];
}

<!-- ActFormBody existing date handling -->
let deadlineISO = $state('');  // YYYY-MM-DD picker value
deadline_utc: isoToUnix(deadlineISO),  // в submit

<!-- isoToUnix helper — найти где определён (вероятно ui/src/lib/utils/date.ts) -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI date string → backend Unix seconds | local browser timezone vs server timezone — UI конвертирует через МСК-aware helper; backend хранит как UTC seconds; existing isoToUnix(деадlineISO) — pattern из Phase 3-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-04-01 | Integrity | Date encoding | mitigate | reuse isoToUnix helper (MSK-aware) из ActFormBody / DocumentAcceptanceModal |
| T-03.1-04-02 | DoS | qty input | accept | backend защита MAX_CLONE_QTY (03.1-01 T-03.1-02) — UI max attribute = UX hint, не security boundary |
| T-03.1-04-SC | Tampering | no new packages | accept | DatePicker = wrapper над native <input type="date"> — no JS date library |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DatePicker.svelte shared component</name>
  <files>ui/src/lib/components/DatePicker.svelte</files>
  <read_first>
    - ui/src/lib/components/Input.svelte (для понимания props/styling convention)
    - ui/src/features/acts/ActFormBody.svelte (line 31, 92, 154-157 — current `deadlineISO` + `isoToUnix` pattern)
    - .planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-CONTEXT.md G-2 (wrapper над native input type=date, Russian locale, default=today)
    - ui/src/lib/components/Modal.svelte (для styling tokens convention)
  </read_first>
  <behavior>
    - Render `<input type="date" lang="ru">`; `bind:value` returns ISO YYYY-MM-DD string (native browser behavior).
    - Props:
      - `value: string` (bindable)
      - `id?: string`
      - `min?: string` (YYYY-MM-DD)
      - `max?: string`
      - `disabled?: boolean`
      - `required?: boolean`
      - `placeholder?: string` (для browser placeholder text)
    - Reactive: if value === '' и required → визуально mark error border (CSS state); not enforced by this component (parent validates).
    - Default value не устанавливается компонентом — это parent responsibility (для today() default — parent passes today's YYYY-MM-DD).
  </behavior>
  <action>
    Создать `ui/src/lib/components/DatePicker.svelte`:

    ```svelte
    <script lang="ts">
      let { value = $bindable(''), id, min, max, disabled = false, required = false, placeholder = '' }: {
        value: string;
        id?: string;
        min?: string;
        max?: string;
        disabled?: boolean;
        required?: boolean;
        placeholder?: string;
      } = $props();
    </script>

    <input
      type="date"
      class="date-picker"
      lang="ru"
      bind:value
      {id}
      {min}
      {max}
      {disabled}
      {required}
      {placeholder}
    />

    <style lang="scss">
      .date-picker {
        // reuse tokens (border, radius, font-family) from Input.svelte
        // ::-webkit-datetime-edit (browser-specific) — стилизуется через user-agent;
        // minimal styling — native date-picker UI is browser/OS default.
      }
    </style>
    ```

    Скопировать relevant styling tokens из Input.svelte (border-color, radius, padding, font-family). Native `<input type="date">` отображает локализованный date-picker в WebView2/WKWebView; lang="ru" даёт RU calendar.

    Если в проекте есть `ui/src/lib/utils/date.ts` с `isoToUnix` / `todayISO()` — НЕ trzeba их трогать; DatePicker — pure value-pass-through.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check --threshold error 2>&1 | tail -20 && test -f ui/src/lib/components/DatePicker.svelte && grep -q 'type="date"' ui/src/lib/components/DatePicker.svelte && grep -q 'lang="ru"' ui/src/lib/components/DatePicker.svelte</automated>
  </verify>
  <done>DatePicker.svelte создан, svelte-check чистый, native <input type="date"> с RU locale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend — ActCreateDto.handover_date_utc + ActService::create persists в acts.handover_date_utc</name>
  <files>crates/trackly-app/src/dto/act.rs, crates/trackly-core/src/domain/acts.rs, crates/trackly-app/src/services/act_service.rs</files>
  <read_first>
    - crates/trackly-app/src/dto/act.rs:139-152 (ActCreateDto current — добавить handover_date_utc)
    - crates/trackly-core/src/domain/acts.rs:56-70 (ActNew domain — добавить handover_date_utc если нужно для repo)
    - crates/trackly-app/src/services/act_service.rs:142+ (create — INSERT INTO acts заполняет handover_date_utc; default = now())
    - crates/trackly-infra/src/repos/acts_sqlite.rs:90+ (insert_act_in_tx — добавить column)
    - migrations/V015 (column shape — `handover_date_utc INTEGER NOT NULL DEFAULT 0`)
    - .planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-CONTEXT.md G-2 backend section
  </read_first>
  <behavior>
    - Test 1: ActCreateDto serde: payload с `handover_date_utc: 1700000000` (epoch) сериализуется в snake_case JSON.
    - Test 2: ActCreateDto без handover_date_utc (Option None) — backward-compat, ActService::create использует `clock.now_unix_seconds()` как default.
    - Test 3: post-create `SELECT handover_date_utc FROM acts WHERE id = ?` returns the payload value (или now() для None).
    - Test 4: integration: create handover с explicit handover_date → assert sqlite row matches.
  </behavior>
  <action>
    **2a — DTO (`crates/trackly-app/src/dto/act.rs:139`):**
    ```rust
    pub struct ActCreateDto {
        // ... existing fields
        #[specta(type = Option<i32>)]
        pub handover_date_utc: Option<i64>,  // NEW; None → service uses now()
    }
    ```

    **2b — Domain (`crates/trackly-core/src/domain/acts.rs:56`):**
    ```rust
    pub struct ActNew {
        // ... existing
        pub handover_date_utc: i64,  // domain expects resolved; default берётся в service
    }
    ```

    **2c — Service** (act_service.rs::create):
    Внутри fn создания ActNew: `let handover_date = payload.handover_date_utc.unwrap_or_else(|| self.clock.now_unix_seconds());`; pass into ActNew.

    **2d — Repo** (`acts_sqlite.rs::insert_act_in_tx`):
    Добавить `handover_date_utc` в INSERT column list + params.

    **2e — bindings.ts regen** через `cargo test --test export_bindings`.

    Test cases — extend `crates/trackly-app/tests/acts_crud.rs` или `acts_clone_handover.rs` 2 testом (explicit date + default date).
  </action>
  <verify>
    <automated>cargo test -p trackly-app --tests -- acts_crud acts_clone_handover && cargo test -p trackly-app --test export_bindings && grep -q "handover_date_utc" crates/trackly-app/src/dto/act.rs && grep -q "handover_date_utc" ui/src/bindings.ts && cargo clippy -p trackly-app -- -D warnings</automated>
  </verify>
  <done>Backend persists handover_date_utc; backward-compat для clients without field; tests зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ActFormBody — 3 поля используют DatePicker; handover_date_utc собирается в payload; qty input max bound (G-3)</name>
  <files>ui/src/features/acts/ActFormBody.svelte, ui/src/features/acts/ActFormItemsTable.svelte</files>
  <read_first>
    - ui/src/features/acts/ActFormBody.svelte (full; current text-input для deadline на line 154-157; искать «Дата» field)
    - ui/src/features/acts/ActFormItemsTable.svelte (qty input current shape; добавить max attribute)
    - ui/src/lib/components/DatePicker.svelte (Task 1)
    - .planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-CONTEXT.md G-2 (ActFormModal изменения — добавить «Когда отдали»; «Дата» и «Сроком до» переводятся на DatePicker)
    - .planning/phases/03-pdf/03-UAT.md G-3 fix_pattern (max attribute on qty input)
  </read_first>
  <behavior>
    - 3 date-полей рендерятся как DatePicker: «Дата» (если есть), «Сроком до», «Когда отдали» (новое).
    - «Когда отдали» default = todayISO() (помощник `todayISO()` если есть; иначе inline `new Date().toISOString().slice(0,10)` с MSK adjustment — переиспользовать существующий isoToUnix logic для symmetry).
    - Submit payload: `handover_date_utc: isoToUnix(handoverDateISO)`; если empty → undefined → backend default.
    - qty input в ActFormItemsTable: добавить `max="1000"` (mirror backend MAX_CLONE_QTY) + `min="1"`.
  </behavior>
  <action>
    **3a — ActFormBody.svelte:**

    1. Import: `import DatePicker from '$lib/components/DatePicker.svelte';`
    2. Найти `«Сроком до»` поле (line 151-158) — заменить `<Input type="text" id="act-deadline" value={deadlineISO} oninput={...} />` на `<DatePicker id="act-deadline" bind:value={deadlineISO} />`.
    3. Если есть поле «Дата» (для типа акта) — аналогично DatePicker.
    4. Добавить новое поле «Когда отдали»:
       ```svelte
       let handoverDateISO = $state(todayISO());  // или inline new Date().toISOString().slice(0,10)
       ...
       <div class="field">
         <label class="label" for="act-handover-date">Когда отдали <span class="req">*</span></label>
         <DatePicker id="act-handover-date" bind:value={handoverDateISO} required />
       </div>
       ```
    5. В submit payload: добавить `handover_date_utc: handoverDateISO ? isoToUnix(handoverDateISO) : null,`.

    **3b — ActFormItemsTable.svelte:**

    Найти qty input → добавить `max="1000"` (или константа MAX_CLONE_QTY имитированная из backend) + `min="1"` + `type="number"`.

    Inline validation: если qty > 1000 → mark error border; submit disabled. Не critical, backend защищает.

    **W-5 defensive UX:** если выбранный device имеет `serial_number !== null` (полученный из device autocomplete payload или fetched detail), то для этого row qty input получает `max="1"` (вместо 1000) + visible-but-disabled инкремент-кнопки + title/tooltip = «У устройства есть серийный номер; для нескольких единиц используйте отдельные позиции». Цель: defensive UX — клоны теряют `serial_number` (см. plan 03.1-01 W-5 note), что обычно нежелательно для серийных устройств. Если device.serial_number === null → стандартный max="1000". Этот guard НЕ блокирующий backend (backend всё равно clones с NULL serial если qty>1 пришёл) — только UI hint.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check --threshold error 2>&1 | tail -20 && grep -q "DatePicker" ui/src/features/acts/ActFormBody.svelte && grep -q "handoverDateISO\|handover_date_utc" ui/src/features/acts/ActFormBody.svelte && grep -q 'max="1000"\|MAX_CLONE_QTY' ui/src/features/acts/ActFormItemsTable.svelte && grep -q "serial_number" ui/src/features/acts/ActFormItemsTable.svelte</automated>
  </verify>
  <done>3 date-поля используют DatePicker; «Когда отдали» дефолтится в today; payload содержит handover_date_utc; qty input bounded в [1, 1000]; W-5 defensive guard: serial_number !== null → max=1 на qty input.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT verification G-2 + G-3 на запущенном Tauri-приложении</name>
  <what-built>DatePicker в ActFormModal + handover_date_utc в backend + qty bounded на UI.</what-built>
  <how-to-verify>
    1. `cargo tauri dev` → V015 миграция OK.
    2. Открыть «Акты» → ➕ Создать.
    3. **G-2 verify:**
       - Поля «Дата» (если есть), «Сроком до», «Когда отдали» рендерятся как native date-pickers (с calendar dropdown), не как plain text inputs.
       - «Когда отдали» предзаполнено сегодняшней датой.
       - Можно изменить дату через picker (calendar UI).
    4. **G-3 verify:**
       - В таблице позиций поле «Кол-во» имеет spin-кнопки (number input) и не позволяет ввести > 1000 (browser-enforced max).
       - Попытаться ввести 1001 → browser отвергает или mark invalid.
       - Submit при qty=1001 backend rejects с message «Кол-во не должно превышать 1000» (если UI всё же послал).
    5. Создать акт с qty=3 + handover_date=вчерашняя дата → submit → проверить:
       - Акт создан без ошибок.
       - В detail-pane «Дата выдачи» / «Когда отдали» показывает вчерашнюю дату (если detail-pane уже это рендерит — если нет, проверить через SQLite `SELECT handover_date_utc FROM acts ORDER BY id DESC LIMIT 1`).
  </how-to-verify>
  <action>Pause execution. Provide the verification steps below to the human user and wait for resume-signal before proceeding to the next task or finalizing the plan.</action>
  <verify><human-check>Steps under &lt;how-to-verify&gt; executed manually by the user; user reports pass or describes issue.</human-check></verify>
  <done>User confirmed acceptance via resume-signal.</done>

  <resume-signal>Type "approved — G-2 and G-3 pass" или describe issue</resume-signal>
</task>

</tasks>

<verification>
- [ ] `test -f ui/src/lib/components/DatePicker.svelte`.
- [ ] `grep -c "DatePicker" ui/src/features/acts/ActFormBody.svelte` >= 2 (import + ≥1 usage; ideally 3).
- [ ] `grep -c "handover_date_utc" crates/trackly-app/src/dto/act.rs crates/trackly-app/src/services/act_service.rs crates/trackly-infra/src/repos/acts_sqlite.rs ui/src/bindings.ts` >= 4.
- [ ] `pnpm svelte-check` чистый; `cargo clippy` чистый.
- [ ] Manual UAT (Task 4) approved.
</verification>

<success_criteria>
1. G-2 — все date-поля в ActFormModal используют native DatePicker компонент.
2. handover_date_utc persisted в `acts` table; backward-compat для clients без поля.
3. G-3 — qty input bounded на UI; backend MAX_CLONE_QTY (план 01) защищает defence-in-depth.
4. DatePicker — reusable shared component (Phase 7 reports / другие модалы смогут переиспользовать).
</success_criteria>

<output>
Create `.planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-04-SUMMARY.md` when done.
</output>
