---
status: partial
phase: 03.1-acts-quantity-model-uat-gap-closure
source: [03.1-VERIFICATION.md]
started: 2026-05-31T00:00:00Z
updated: 2026-05-31T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. G-1: backdrop drag-out оставляет модал открытым
expected: Открыть DocumentAcceptanceModal → mousedown внутри модала (на тексте) → перетащить мышь за границу контента (на backdrop) → mouseup → модал НЕ закрывается. То же самое в ActFormModal, ReturnModal, PdfPreviewModal.
result: [pending]

### 2. G-2: DatePicker открывает нативный календарь в ActFormBody
expected: В ActFormModal поля «Когда отдали» и «Сроком до» рендерят <input type=date lang=ru> → клик показывает OS-native calendar UI с русскими названиями месяцев.
result: [pending]

### 3. G-3: qty input в ActFormItemsTable не позволяет >1000
expected: Ввести qty=1500 → browser hard-clamp к 1000 (HTML max attr) ИЛИ submit → backend Validation. При выборе device c serial_no — max=1 + warning hint.
result: [pending]

### 4. G-4: DeviceAutocompleteField закрывается после выбора
expected: В ActFormItemsTable: набрать prefix → подождать debounce → выбрать device из dropdown → dropdown скрывается definitively, input получает blur() и НЕ открывается обратно до явного нового keystroke.
result: [pending]

### 5. G-5: PersonAutocomplete показывает frequency-sorted suggestions
expected: В DocumentAcceptanceModal или ActFormBody: фокус в поле «Кто сдал» → набрать «Ив» → dropdown показывает имена из DB acts.giver_name начинающиеся на «Ив», отсортированные по частоте использования.
result: [pending]

### 6. G-6: ReturnModal apply_to_all symmetric disable + per-row split
expected: (a) applyToAll=true: bulk-поля сверху active, per-row condition/location disabled. (b) applyToAll=false: bulk-поля disabled; для каждой checked row ОБА поля enabled. (c) Несколько rows одного act_item_id с разными overrides → submit splits в N отдельных ActReturnItemDto (W-4).
result: [pending]

### 7. G-7: display rule «в»/«в1»/«в2» в actsList
expected: Создать handover qty=3 → partial return 1 устройства → list показывает return-акт «42в1»; ещё partial return 1 устройства → «42в2»; финальный return последнего → «42в3», parent handover «42» уходит в архив.
result: [pending]

### 8. G-8a: длинные кириллические ФИО в PDF ItemsTable не overlap соседние колонки
expected: Создать device с длинным CYR-name → PDF рендер → текст в колонке truncate с '…' и не пересекает границу с соседней колонкой.
result: [pending]

### 9. G-8b: Save/Open/Print кнопки в PdfPreviewModal
expected: 3 кнопки: «Сохранить как PDF» (native save dialog → tauri-plugin-fs), «Открыть в системном просмотрщике» (через acts_open_pdf_in_system → Preview.app/Edge), «Печать» (через iframe.contentWindow.print).
result: [pending]

### 10. G-9: logo aspect-ratio в PDF
expected: Загрузить логотип с aspect-ratio 2:1 → PDF top-right показывает logo в правильных пропорциях, вписан в bounding box с contain-fit.
result: [pending]

### 11. G-10: ReturnModal показывает только outstanding devices
expected: handover-акт qty=3 → return 2 устройств → создать второй return → ReturnModal показывает ТОЛЬКО 1 outstanding device.
result: [pending]

### 12. Phase-wide: V015 миграция на dev DB безопасна
expected: Apply V015 на dev trackly.db → migration completes; PRAGMA user_version = 15; existing данные не повреждены.
result: [pending]

### 13. Workspace cross-cutting: Modal backdrop fix наследуется всеми 4+ модалами
expected: Тест G-1 повторить во всех модалах (DocumentAcceptanceModal, ActFormModal, ReturnModal, PdfPreviewModal).
result: [pending]

## Summary

total: 13
passed: 0
issues: 0
pending: 13
skipped: 0
blocked: 0

## Gaps
