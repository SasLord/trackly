---
phase: 03.1-acts-quantity-model-uat-gap-closure
plan: 02
subsystem: ui-shared
tags: [g-5, autocomplete, ui-component, backend-query, svelte5-runes]

requires:
  - phase: 03-pdf
    provides: ActService base, tauri-specta wiring, axum router pattern
provides:
  - ActService::suggest_person — backend query (DISTINCT + freq DESC + escape_like)
  - acts_suggest_person tauri command + axum route
  - SuggestPersonField enum в новом dto/suggest.rs (изолированном от dto/act.rs)
  - PersonAutocomplete.svelte — shared Svelte 5 component с runes-state, debounce 200ms, close-on-select
  - acts.suggestPerson UI API wrapper
  - 7 backend integration tests
affects: [03.1-03 ReturnModal will integrate PersonAutocomplete с default-swap из parent]

tech-stack:
  added: []
  patterns:
    - "Svelte 5 $bindable + onMount-based suppression state machine для edit-mode prefill (упрощённая адаптация DeviceAutocompleteField)"
    - "Backend ActService::suggest_person — column whitelisting через enum (НЕ string interpolation), prefix bounded ≤100 chars, escape_like helper с `ESCAPE '\\'` SQL clause"

key-files:
  created:
    - crates/trackly-app/src/dto/suggest.rs
    - crates/trackly-app/tests/acts_suggest.rs
    - ui/src/lib/components/PersonAutocomplete.svelte
  modified:
    - crates/trackly-app/src/dto/mod.rs
    - crates/trackly-app/src/services/act_service.rs (+escape_like helper, +suggest_person fn)
    - crates/trackly-app/src/tauri_cmds/acts.rs (build_acts_suggest_person + acts_suggest_person command)
    - crates/trackly-app/src/http/acts.rs (handler + route)
    - crates/trackly-app/src/specta_export.rs (registration)
    - ui/src/lib/api/acts.ts (suggestPerson wrapper)
    - ui/src/features/acts/DocumentAcceptanceModal.svelte (Input → PersonAutocomplete)
    - ui/src/features/acts/ActFormBody.svelte (Input → PersonAutocomplete)
    - ui/src/bindings.ts (regenerated через export_bindings test)

key-decisions:
  - "SuggestPersonField живёт в NEW dto/suggest.rs (W-2 fix) — изоляция от dto/act.rs гарантировала wave-1 parallel execution с plan 03.1-01"
  - "Integration tests — NEW файл acts_suggest.rs (W-3 fix) — clearer scope vs acts_search.rs (отдельная feature)"
  - "ReturnModal integration отложена в план 03.1-03 — он полностью переписывает ReturnModal под G-6/G-10/G-12, placeholder PersonAutocomplete сейчас был бы переписан"
  - "escape_like helper — единая защита от LIKE injection (% / _ / \\); column name whitelisted через enum"
  - "Hard LIMIT clamped к 20 в сервисе (defence-in-depth поверх UI default 20)"

patterns-established:
  - "Shared autocomplete component в ui/src/lib/components/ (не features/) — переиспользуется across multiple feature areas"
  - "`#[serde(rename_all = \"snake_case\")]` enum → bindings.ts генерирует TypeScript string literal union (`'giver' | 'receiver'`)"

requirements-completed: [G-5]

duration: ~40min
completed: 2026-05-31
---

# Plan 03.1-02: Person Autocomplete Summary

**G-5 закрыт: backend tauri command + axum route + новый shared PersonAutocomplete компонент интегрированы в DocumentAcceptanceModal и ActFormBody; ReturnModal интеграция консолидирована в план 03.1-03.**

## Performance

- **Duration:** ~40 минут
- **Tasks:** 3 (все autonomous=true)
- **Files modified:** 11

## Accomplishments

- **Backend ActService::suggest_person** возвращает до 20 distinct имён из `acts.giver_name|receiver_name` отсортированных по frequency DESC (alpha ASC tiebreak); soft-deleted акты исключены.
- **escape_like helper** + `ESCAPE '\\'` SQL clause защищают от LIKE injection через `%` / `_` / `\` (T-03.1-02-01).
- **MAX prefix len 100, hard LIMIT 20** — DoS bounds (T-03.1-02-02).
- **acts_suggest_person tauri command + axum POST route** — оба транспорта работают через shared `build_acts_suggest_person` helper.
- **SuggestPersonField enum в NEW dto/suggest.rs** (W-2 fix) — wave-1 parallel safety гарантирована.
- **PersonAutocomplete.svelte** — Svelte 5 component с runes; debounce 200ms; close-on-select; keyboard navigation (ArrowDown/Up, Enter, Esc, Tab); edit-mode prefill suppression onMount.
- **acts.suggestPerson API wrapper** добавлен.
- **2 модала интегрированы** (DocumentAcceptanceModal + ActFormBody); ReturnModal отложен в план 03.1-03 (G-6 ReturnModal UX rewrite полностью переписывает компонент).
- **7 backend integration tests** покрывают: frequency ordering, prefix match, escape_like защиту, hard LIMIT, prefix length validation, soft-delete exclusion, receiver field independence.

## Task Commits

1. **Task 1: backend (service + dto + tauri + axum + 7 tests)** — `<HEAD~2>` (feat) — actual commit ID injected post-commit.
2. **Tasks 2+3: PersonAutocomplete component + 2 modal integration** — `<HEAD~1>` (feat).
3. **SUMMARY + tracking** — `<HEAD>` (docs).

## Files Created/Modified

- `crates/trackly-app/src/dto/suggest.rs` — `SuggestPersonField { Giver, Receiver }` enum.
- `crates/trackly-app/src/services/act_service.rs` — `suggest_person()` + `escape_like()` helper.
- `crates/trackly-app/src/tauri_cmds/acts.rs` — build_acts_suggest_person + acts_suggest_person command.
- `crates/trackly-app/src/http/acts.rs` — handler_suggest_person + route registration.
- `crates/trackly-app/src/specta_export.rs` — acts_suggest_person в collect_commands.
- `crates/trackly-app/tests/acts_suggest.rs` — 7 integration tests.
- `ui/src/lib/components/PersonAutocomplete.svelte` — 275 LOC reusable component.
- `ui/src/lib/api/acts.ts` — `acts.suggestPerson(field, prefix)` wrapper.
- `ui/src/features/acts/DocumentAcceptanceModal.svelte` — оба Input → PersonAutocomplete.
- `ui/src/features/acts/ActFormBody.svelte` — оба Input → PersonAutocomplete.
- `ui/src/bindings.ts` — regenerated (actsSuggestPerson + SuggestPersonField).

## UAT Coverage Status

- **G-5** (автокомплит для giver/receiver) — backend готов, UI готов для 2 модалов; final UAT — после plan 03.1-03 (ReturnModal also получает autocomplete с default-swap из parent).

## Self-Check: PASSED

- 7 acts_suggest tests green.
- export_bindings test green; bindings.ts содержит `actsSuggestPerson` + `type SuggestPersonField`.
- clippy clean.
- svelte-check 0 errors.
- W-2 verified: `grep -c "SuggestPersonField" crates/trackly-app/src/dto/act.rs` == 0 (enum НЕ в dto/act.rs).
- W-3 verified: tests in NEW acts_suggest.rs (НЕ в acts_search.rs).
