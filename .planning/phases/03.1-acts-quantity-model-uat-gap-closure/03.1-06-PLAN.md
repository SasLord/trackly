---
phase: 03.1-acts-quantity-model-uat-gap-closure
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/src/lib/components/Modal.svelte
  - ui/src/features/devices/DeviceAutocompleteField.svelte
  - ui/src/lib/components/Modal.test.ts
  - ui/src/features/devices/__tests__/DeviceAutocompleteField.test.ts
autonomous: false
requirements: [G-1, G-4]
threat_model_severity: low
tags: [phase-3.1, gap-closure, ui, modal, autocomplete]

must_haves:
  truths:
    - "Modal.svelte: backdrop dismiss срабатывает ТОЛЬКО если mousedown И mouseup произошли на backdrop element (не если mousedown был внутри модала и mouseup ушёл на backdrop из-за text selection drag) — G-1 fix."
    - "DeviceAutocompleteField.svelte: после `onSelect` callback dropdown явно закрывается (`showDropdown=false`) + input blur; debounce/$effect не реоткрывает dropdown до следующего keystroke — G-4 fix."
  artifacts:
    - path: ui/src/lib/components/Modal.svelte
      provides: "Backdrop discipline: mousedown+mouseup на backdrop required для onClose"
    - path: ui/src/features/devices/DeviceAutocompleteField.svelte
      provides: "Dropdown closes definitively on select; justSelected guard prevents re-open"
    - path: ui/src/lib/components/Modal.test.ts
      provides: "svelte-testing-library тест: mousedown inside → drag outside → mouseup → modal still open"
    - path: ui/src/features/devices/__tests__/DeviceAutocompleteField.test.ts
      provides: "svelte-testing-library тест: input.fill → wait debounce → click первого item → dropdown not visible"
  key_links:
    - from: "Modal.svelte::handleBackdropClick (removed)"
      to: "handleBackdropMousedown + handleBackdropMouseup (split)"
      via: "mouseDownOnBackdrop flag"
      pattern: "mouseDownOnBackdrop|backdrop-mousedown|backdrop-mouseup"
    - from: "DeviceAutocompleteField::onSelect"
      to: "showDropdown = false + justSelected = true"
      via: "explicit state reset"
      pattern: "justSelected|showDropdown\\s*=\\s*false"
---

<objective>
Закрыть две UX-bugs от UAT:
- **G-1 (major, cross-cutting):** Модал DocumentAcceptanceModal (и ВСЕ модалы через shared Modal.svelte) закрывается на mouseup-вне-модала, даже если mousedown был внутри (text selection drag).
- **G-4 (major):** DeviceAutocompleteField после select снова показывает dropdown, пока пользователь не выберет повторно.

Обе исправляются на уровне shared components, поэтому group'нуты в один план. Wave 1, no deps — parallel-friendly.

Output:
- Modal.svelte: split mousedown/mouseup discipline.
- DeviceAutocompleteField.svelte: justSelected guard + explicit close.
- 2 svelte-testing-library тестов (если setup есть; иначе manual smoke в Task 3).

**Autonomous:** true — но Task 3 manual smoke рекомендуется выполнить (cross-cutting fix может задеть другие модалы).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-CONTEXT.md
@.planning/phases/03-pdf/03-UAT.md
@ui/src/lib/components/Modal.svelte
@ui/src/features/devices/DeviceAutocompleteField.svelte

<interfaces>
<!-- Modal.svelte (current) -->
<div class="modal-backdrop" onclick={handleBackdropClick}>
  <div class="modal-content">...</div>
</div>

function handleBackdropClick(e: MouseEvent) {
  if (e.target === e.currentTarget) onClose();
}

<!-- New (G-1 fix per 03.1-CONTEXT.md G-1 Pattern Svelte 5) -->
let mouseDownOnBackdrop = $state(false);

<div class="modal-backdrop"
  onmousedown={(e) => { mouseDownOnBackdrop = (e.target === e.currentTarget); }}
  onmouseup={(e) => {
    if (mouseDownOnBackdrop && e.target === e.currentTarget) onClose();
    mouseDownOnBackdrop = false;
  }}>

<!-- DeviceAutocompleteField current onSelect -->
function selectDevice(d: DeviceDto) { value = d.name; onSelect?.(d); /* dropdown still showed by $effect rerun */ }

<!-- New (G-4 fix) -->
let justSelected = $state(false);
function selectDevice(d: DeviceDto) {
  value = d.name;
  showDropdown = false;
  justSelected = true;
  inputEl?.blur();
  onSelect?.(d);
}

$effect(() => {
  // dependencies на value
  if (justSelected) { justSelected = false; return; }   // skip rerun once
  // ... existing debounce fetch
});
</interfaces>
</context>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-06-01 | UX | Modal backdrop discipline | mitigate | unit-test pattern (mousedown inside → drag out → mouseup → modal остаётся открытым); manual smoke на 3+ модалах после fix |
| T-03.1-06-02 | UX | DeviceAutocomplete re-open | mitigate | justSelected flag — single-shot, resets at $effect bottom; covered svelte-testing-library |
| T-03.1-06-SC | Tampering | no new packages | accept | план не добавляет dependencies (svelte-testing-library уже должна быть установлена для проекта — если нет, skip Task 2 tests; manual smoke compensates) |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modal.svelte — mousedown/mouseup discipline (G-1)</name>
  <files>ui/src/lib/components/Modal.svelte, ui/src/lib/components/Modal.test.ts</files>
  <read_first>
    - ui/src/lib/components/Modal.svelte (full — current handleBackdropClick at line ~30)
    - .planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-CONTEXT.md G-1 (Svelte 5 pattern с mouseDownOnBackdrop flag)
    - .planning/phases/03-pdf/03-UAT.md G-1 (severity:major, cross-cutting; user repro: text selection drag closes modal)
  </read_first>
  <behavior>
    - Test 1 (text selection drag inside-to-outside): mousedown на текст внутри модала → drag mouse outside модала → mouseup на backdrop → assert модал ВСЁ ЕЩЁ открыт.
    - Test 2 (legitimate backdrop click): mousedown на backdrop → mouseup на backdrop → assert модал закрылся.
    - Test 3 (mousedown backdrop → drag inside → mouseup inside): mousedown на backdrop → mouseup на content → assert модал ВСЁ ЕЩЁ открыт (mouseDownOnBackdrop=true но e.target !== currentTarget на mouseup).
    - Edge: rapid double-click на backdrop → закрывается на первом полном цикле.
  </behavior>
  <action>
    **1a — Modal.svelte:**

    Удалить `handleBackdropClick` и `onclick={handleBackdropClick}` атрибут.

    Заменить на:
    ```svelte
    <script lang="ts">
      let { isOpen, onClose, title, children }: ModalProps = $props();
      let mouseDownOnBackdrop = $state(false);

      function handleBackdropMousedown(e: MouseEvent) {
        mouseDownOnBackdrop = (e.target === e.currentTarget);
      }
      function handleBackdropMouseup(e: MouseEvent) {
        if (mouseDownOnBackdrop && e.target === e.currentTarget) {
          onClose();
        }
        mouseDownOnBackdrop = false;
      }
    </script>

    <div class="modal-backdrop"
         role="presentation"
         onmousedown={handleBackdropMousedown}
         onmouseup={handleBackdropMouseup}>
      <div class="modal-content" role="dialog" aria-modal="true" ...>
        ...
      </div>
    </div>
    ```

    Keyboard escape (ESC key) — должно ОСТАВАТЬСЯ работать; не трогать существующий keydown handler (если есть).

    **1b — Test** `ui/src/lib/components/Modal.test.ts`:

    Если в проекте уже настроен vitest+svelte-testing-library (проверить наличие `ui/vitest.config.ts` или `ui/package.json` "vitest" dep) — добавить тест:
    ```ts
    import { render, fireEvent } from '@testing-library/svelte';
    import Modal from './Modal.svelte';

    test('modal stays open when mousedown inside drags to backdrop', async () => {
      const onClose = vi.fn();
      const { container } = render(Modal, { props: { isOpen: true, onClose, title: 'test' } });
      const backdrop = container.querySelector('.modal-backdrop')!;
      const content = container.querySelector('.modal-content')!;

      await fireEvent.mouseDown(content);  // mousedown inside
      await fireEvent.mouseUp(backdrop);   // mouseup outside (target=backdrop, but mouseDownOnBackdrop=false)
      expect(onClose).not.toHaveBeenCalled();
    });

    test('modal closes on full backdrop click', async () => {
      const onClose = vi.fn();
      const { container } = render(Modal, { props: { isOpen: true, onClose, title: 'test' } });
      const backdrop = container.querySelector('.modal-backdrop')!;

      await fireEvent.mouseDown(backdrop);
      await fireEvent.mouseUp(backdrop);
      expect(onClose).toHaveBeenCalledOnce();
    });
    ```

    Если testing-library не установлен — пропустить файл, отметить в SUMMARY как «test deferred to когда testing-library установлен» + полагаться на Task 3 manual smoke.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check --threshold error 2>&1 | tail -20 && grep -q "mouseDownOnBackdrop" ui/src/lib/components/Modal.svelte && grep -q "onmousedown\|onmouseup" ui/src/lib/components/Modal.svelte && (! grep -q "handleBackdropClick" ui/src/lib/components/Modal.svelte || echo "handleBackdropClick still present — fix") && (pnpm --dir ui exec vitest run lib/components/Modal.test 2>/dev/null || echo "vitest not configured — manual smoke only")</automated>
  </verify>
  <done>Modal.svelte использует split mousedown/mouseup; onClose не вызывается при drag-to-backdrop; svelte-check чистый.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeviceAutocompleteField — close on select + justSelected guard (G-4)</name>
  <files>ui/src/features/devices/DeviceAutocompleteField.svelte, ui/src/features/devices/__tests__/DeviceAutocompleteField.test.ts</files>
  <read_first>
    - ui/src/features/devices/DeviceAutocompleteField.svelte (full — current onSelect handler + $effect/debounce search trigger)
    - .planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-CONTEXT.md G-4 (Решение: showDropdown=false + inputEl?.blur() + justSelected flag)
    - .planning/phases/03-pdf/03-UAT.md G-4 fix_pattern (regression bug — тот же был в device-add flow)
  </read_first>
  <behavior>
    - Test 1: render → fireEvent.input «Мыш» → wait debounce 250ms → dropdown shows items → click first item → assert dropdown НЕ visible (queryAllByRole('option').length === 0).
    - Test 2: после select — input.value reflects selected; не происходит повторный fetch (mock invoke not called снова).
    - Test 3: пользователь начинает новый ввод после select → justSelected reset → dropdown снова работает на следующих keystrokes.
  </behavior>
  <action>
    **2a — DeviceAutocompleteField.svelte:**

    1. Добавить state: `let justSelected = $state(false); let inputEl: HTMLInputElement | undefined = $state();`
    2. В onSelect callback (найти function selectDevice или подобную):
       ```ts
       function selectDevice(d: DeviceDto) {
         value = d.name;
         showDropdown = false;
         justSelected = true;
         inputEl?.blur();
         onSelect?.(d);
       }
       ```
    3. В $effect (где debounce + invoke trigger):
       ```ts
       $effect(() => {
         // depend on value
         if (justSelected) {
           justSelected = false;
           return;  // skip this rerun, wait for next genuine keystroke
         }
         // ... existing debounce+fetch logic
       });
       ```
    4. Bind input element: `<input bind:this={inputEl} ... />`.
    5. Дополнительно — на input focus НЕ автоматически re-open dropdown если value не изменилось (current behavior может re-open на refocus); если такая логика есть — добавить `if (!justSelected) showDropdown = ...`.

    **2b — Test** `ui/src/features/devices/__tests__/DeviceAutocompleteField.test.ts`:

    Если testing-library есть — добавить:
    ```ts
    import { render, fireEvent, waitFor } from '@testing-library/svelte';
    import DeviceAutocompleteField from '../DeviceAutocompleteField.svelte';
    import { vi } from 'vitest';

    vi.mock('$lib/api/devices', () => ({
      autocomplete: vi.fn(async (prefix: string) =>
        prefix.length > 0 ? [{ id: 1, name: 'Мышка', ... }] : []
      ),
    }));

    test('dropdown closes after selecting item', async () => {
      const { container, getByRole } = render(DeviceAutocompleteField, { props: { value: '', onSelect: vi.fn() } });
      const input = getByRole('textbox');
      await fireEvent.input(input, { target: { value: 'Мыш' } });
      await waitFor(() => expect(container.querySelectorAll('.dropdown-item').length).toBeGreaterThan(0));
      const item = container.querySelector('.dropdown-item')!;
      await fireEvent.click(item);
      expect(container.querySelectorAll('.dropdown-item').length).toBe(0);
    });
    ```

    Если testing-library нет — defer test файл, manual smoke в Task 3.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check --threshold error 2>&1 | tail -20 && grep -q "justSelected" ui/src/features/devices/DeviceAutocompleteField.svelte && grep -q "showDropdown.*=.*false" ui/src/features/devices/DeviceAutocompleteField.svelte && (pnpm --dir ui exec vitest run features/devices 2>/dev/null || echo "vitest not configured")</automated>
  </verify>
  <done>DeviceAutocompleteField закрывает dropdown на select; justSelected guard предотвращает re-open до следующего keystroke; test passes (или deferred с manual smoke).</done>
</task>

<task type="checkpoint:human-verify" gate="auto-advance">
  <name>Task 3: Manual smoke — modal backdrop discipline + autocomplete close на запущенном Tauri</name>
  <what-built>G-1 cross-cutting Modal fix + G-4 DeviceAutocompleteField fix.</what-built>
  <how-to-verify>
    1. `cargo tauri dev` запускается.
    2. **G-1 smoke:**
       - Открыть любой модал (DocumentAcceptanceModal через правый клик на device → «Печать документа приёма» или ActFormModal через ➕ Создать в Актах).
       - Выделить текст внутри модала (mousedown на любом тексте) → потащить курсор за пределы модала с зажатой кнопкой → отпустить (mouseup на backdrop).
       - **Assert:** модал ОСТАЛСЯ открытым (раньше — закрывался, UAT G-1).
       - Кликнуть на backdrop (clean click) → модал закрылся (нормальное поведение сохранено).
    3. **G-4 smoke:**
       - Открыть «Создать акт» → начать ввод названия устройства в поле автокомплита → дождаться dropdown → выбрать item кликом.
       - **Assert:** dropdown закрылся, value заполнено выбранным; dropdown НЕ снова открывается до следующего ввода с клавиатуры.
       - Начать новый ввод (изменить буквы) → dropdown снова работает (justSelected reset).
       - Аналогично в DevicesPage → создать device → проверить автокомплит «Наименование» поля (regression check — тот же баг был раньше).
  </how-to-verify>
  <action>Pause execution. Provide the verification steps below to the human user and wait for resume-signal before proceeding to the next task or finalizing the plan.</action>
  <verify><human-check>Steps under &lt;how-to-verify&gt; executed manually by the user; user reports pass or describes issue.</human-check></verify>
  <done>User confirmed acceptance via resume-signal.</done>

  <resume-signal>Type "approved — G-1 and G-4 pass" или describe issue</resume-signal>
</task>

</tasks>

<verification>
- [ ] `grep -q "mouseDownOnBackdrop" ui/src/lib/components/Modal.svelte`
- [ ] `! grep -q "handleBackdropClick" ui/src/lib/components/Modal.svelte` (старый handler удалён).
- [ ] `grep -q "justSelected" ui/src/features/devices/DeviceAutocompleteField.svelte`
- [ ] `pnpm svelte-check` чистый.
- [ ] Manual UAT (Task 3) approved.
</verification>

<success_criteria>
1. G-1: text selection drag не закрывает модал; clean backdrop click закрывает.
2. G-4: dropdown definitively closes on select; не re-open до user typing.
3. Cross-cutting Modal fix не задевает other модалов (smoke covers DocumentAcceptanceModal + ActFormModal + ReturnModal).
4. ESC key dismiss continues to work (regression check).
</success_criteria>

<output>
Create `.planning/phases/03.1-acts-quantity-model-uat-gap-closure/03.1-06-SUMMARY.md` фиксируя:
- Final Modal pattern (mousedown/mouseup split).
- Test status (вписан в Modal.test.ts vs deferred until testing-library setup).
- Manual smoke результат — какие модалы проверены.
</output>
