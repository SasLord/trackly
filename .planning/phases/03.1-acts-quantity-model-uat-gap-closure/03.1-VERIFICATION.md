---
phase: 03.1-acts-quantity-model-uat-gap-closure
verified: 2026-05-31T16:00:00Z
status: human_needed
score: 13/13 must-haves verified (code-level)
overrides_applied: 0
human_verification:
  - test: "G-1: backdrop drag-out оставляет модал открытым"
    expected: "Открыть DocumentAcceptanceModal → mousedown внутри модала (на тексте) → перетащить мышь за границу контента (на backdrop) → mouseup → модал НЕ закрывается. То же самое в ActFormModal, ReturnModal, PdfPreviewModal."
    why_human: "DOM mouseup discipline + visual closing animation требует реального WebView pointer flow; код-проверка только убедилась что split mousedown/mouseup handlers корректно ведут флаг."
  - test: "G-2: DatePicker открывает нативный календарь в ActFormBody"
    expected: "В ActFormModal поля «Когда отдали» и «Сроком до» рендерят <input type=date lang=ru> → клик показывает OS-native calendar UI (WebView2 на Windows / WKWebView на macOS) с русскими названиями месяцев."
    why_human: "Native input UX зависит от browser engine — нужен реальный WebView."
  - test: "G-3: qty input в ActFormItemsTable не позволяет >1000"
    expected: "Ввести qty=1500 → browser hard-clamp к 1000 (HTML max attr) ИЛИ submit → backend Validation «Кол-во не должно превышать 1000». При выборе device c serial_no — max=1 + warning hint."
    why_human: "Browser native attribute behaviour, требует UI."
  - test: "G-4: DeviceAutocompleteField закрывается после выбора"
    expected: "В ActFormItemsTable: набрать prefix → подождать debounce → выбрать device из dropdown → dropdown скрывается definitively, input получает blur() и НЕ открывается обратно до явного нового keystroke."
    why_human: "Dropdown timing + blur visual поведение."
  - test: "G-5: PersonAutocomplete показывает frequency-sorted suggestions"
    expected: "В DocumentAcceptanceModal или ActFormBody: фокус в поле «Кто сдал» → набрать «Ив» → dropdown показывает имена с DB acts.giver_name начинающиеся на «Ив», отсортированные по частоте использования. Esc/Tab/Enter работают, dropdown закрывается после select."
    why_human: "Debounce 200ms + dropdown rendering требует браузер."
  - test: "G-6: ReturnModal apply_to_all symmetric disable + per-row split"
    expected: "(a) applyToAll=true: bulk-поля сверху active, per-row condition/location disabled. (b) applyToAll=false: bulk-поля disabled; для каждой checked row ОБА поля condition И location enabled. (c) Несколько rows одного act_item_id с разными overrides → submit splits в N отдельных ActReturnItemDto (W-4 PER-ROW SPLIT INVARIANT, проверено в коде через buildReturnItems composite-key grouping)."
    why_human: "UX flow и backend round-trip требует реального запуска."
  - test: "G-7: display rule «в»/«в1»/«в2» в actsList"
    expected: "Создать handover qty=3 → partial return 1 устройства → list показывает return-акт «42в1»; ещё partial return 1 устройства → «42в2»; финальный return последнего → «42в3», parent handover «42» уходит в архив (archived=1). Backend logic verified в 11 integration tests; UI рендерит готовое значение."
    why_human: "End-to-end сценарий с UI; backend integration tests покрывают логику но финальное отображение в actsList — UI."
  - test: "G-8a: длинные кириллические ФИО в PDF ItemsTable не overlap соседние колонки"
    expected: "Создать device с name «Иванов-Петров-Сидоров Александр Александрович Старший» → PDF рендер → текст в колонке «Наименование» truncate с ellipsis '…' и не пересекает границу с колонкой «Инв. №»."
    why_human: "Visual review PDF на реальных кириллических данных. WR-07 предупреждает что approximation 0.5 может быть hрупкой для CYR — нужен smoke."
  - test: "G-8b: Save/Open/Print кнопки в PdfPreviewModal"
    expected: "Открыть PdfPreviewModal → 3 кнопки: «Сохранить как PDF» (открывает native save dialog → файл записывается через tauri-plugin-fs), «Открыть в системном просмотрщике» (через acts_open_pdf_in_system → секьюрный backend-валидатор пути → tauri-plugin-shell open в Preview.app/Edge), «Печать» (через iframe.contentWindow.print)."
    why_human: "Native dialogs + system viewer integration — только в реальном Tauri."
  - test: "G-9: logo aspect-ratio в PDF"
    expected: "Загрузить логотип организации с aspect-ratio 2:1 → PDF top-right показывает logo в правильных пропорциях (не растянутый/сжатый), вписан в bounding box LOGO_WIDTH_PT × LOGO_HEIGHT_PT с contain-fit."
    why_human: "Visual PDF review с реальным non-square logo."
  - test: "G-10: ReturnModal показывает только outstanding devices"
    expected: "Открыть handover-акт qty=3 → return 2 устройств → создать второй return → ReturnModal показывает ТОЛЬКО 1 outstanding device (не 3). Если все возвращены → empty state «Все позиции уже возвращены»."
    why_human: "End-to-end UI flow."
  - test: "Phase-wide: V015 миграция на dev DB безопасна"
    expected: "Apply V015 на dev trackly.db → migration completes; PRAGMA user_version = 15; existing данные не повреждены (qty=1 акты остаются, безопасный safety guard в 4.0 предотвращает unsafe upgrade на legacy qty>1 базах с pre-V015 returns)."
    why_human: "Migration data-integrity на пользовательской БД."
  - test: "Workspace cross-cutting: Modal backdrop fix наследуется всеми 4+ модалами"
    expected: "Тест G-1 повторить во всех модалах (DocumentAcceptanceModal, ActFormModal, ReturnModal, PdfPreviewModal) — text-selection drag-out не закрывает ни один."
    why_human: "Один shared component, но cross-cutting эффект требует регрессии."
---

# Phase 03.1: Acts Quantity Model + UAT Gap Closure — Verification Report

**Phase Goal:** Закрыть 12 UAT-выявленных gaps (G-1..G-12, G-13 deferred в Phase 8) с архитектурным переходом acts/devices model на clone-on-handover quantity-семантику.

**Verified:** 2026-05-31
**Status:** human_needed
**Re-verification:** Нет — первичная верификация фазы; CR-01..04 BLOCKERs из 03.1-REVIEW.md уже remediated в commit `4c861b6` ДО verification.

## Goal Achievement

### Per-Requirement (G-ID) Coverage

| G-ID | Описание (severity) | План | Code Status | Evidence |
|------|---------------------|------|-------------|----------|
| **G-1** | Modal backdrop dismiss на drag-out (major, cross-cutting) | 06 | PASS | `Modal.svelte:17-36` — split `handleBackdropMousedown` / `handleBackdropMouseup` + `mouseDownOnBackdrop` flag. Backdrop dismiss срабатывает ТОЛЬКО когда оба события на backdrop. Все 4+ модалы наследуют fix через shared component. |
| **G-2** | DatePicker для всех дат в ActFormBody + handover_date_utc (major) | 04 | PASS | `ui/src/lib/components/DatePicker.svelte` exists (1761 bytes); `ActFormBody.svelte:9` import + lines 165/171 — 2 DatePicker usages (handoverDateISO, deadlineISO). Backend: `dto/act.rs` ActCreateDto.handover_date_utc: `Option<i64>` + V015 column `acts.handover_date_utc INTEGER NOT NULL DEFAULT 0`. |
| **G-3** | qty bound: UI max + backend MAX_CLONE_QTY (major) | 04 | PASS | `ActFormItemsTable.svelte:33,180` — `MAX_CLONE_QTY = 1000`, native `max={row.has_serial ? 1 : MAX_CLONE_QTY}`. Backend `act_service.rs:121-129` enforce `MAX_CLONE_QTY = 1000`. W-5 hint для serial devices реализован. |
| **G-4** | DeviceAutocompleteField close-on-select (major) | 06 | PASS | `DeviceAutocompleteField.svelte:69` — existing `suppressDropdown + lastSelected` state machine эквивалентна `justSelected` (verification grep на line 130 указывает на коммент). Добавлен `inputEl?.blur()` для visual closure. |
| **G-5** | Автокомплит giver/receiver (minor) | 02 | PASS | `dto/suggest.rs` (NEW, 731 bytes) с `SuggestPersonField` enum (W-2). `act_service.rs:980` ActService::suggest_person с `escape_like` helper + ESCAPE '\' clause + freq DESC ordering. `tauri_cmds/acts.rs:175` acts_suggest_person + http route. `PersonAutocomplete.svelte` (7345 bytes) с runes/debounce/keyboard nav. 7 integration tests в `acts_suggest.rs` — **green**. |
| **G-6** | ReturnModal apply_to_all symmetric + per-row split W-4 (major) | 03 | PASS | `ReturnItemsTable.svelte:6-11` — формула `disabled = !row.checked \|\| applyToAll` для condition И location (symmetric). `returnPayload.ts` buildReturnItems composite-key grouping (act_item_id, condition, location) — coalesce identical, split distinct. JSDoc examples документируют W-4 INVARIANT. |
| **G-7** | Display rule «в»/«в1»/«в2» (major) | 01 | PASS | Backend `acts_sqlite.rs:438-475` recompute_parent_archived переписан на COUNT(*)-based (line 445) vs COUNT(DISTINCT device_id) returns (line 453). format_act_number derives sibling_return_count корректно — verified в 11 integration tests `acts_clone_handover.rs` (green). |
| **G-8a** | PDF column overflow — ellipsis truncate (blocker) | 05 | PASS | `pdf/renderer.rs:341` `truncate_to_width(text, font_size, max_width)` с B-3 byte-identity early-return. Применён в `render_table_section` (lines 268, 281). 3 tests в `pdf_column_overflow.rs` — green. **Caveat (WR-07):** approximation `0.5 * font_size` неточен для кириллицы, словесный wrap отложен; UAT может выявить hруп. |
| **G-8b** | PdfPreviewModal Save/Open/Print + plugin wiring (blocker) | 05 | PASS | `PdfPreviewModal.svelte:260-267` — 3 кнопки. `handleOpen` (line 195) пишет в `BaseDirectory.Temp` через tauri-plugin-fs → вызывает secure `acts_open_pdf_in_system` (CR-02 fix). `tauri-plugin-shell` + `tauri-plugin-fs` зарегистрированы в Cargo.toml + main.rs. Capability `fs:allow-write-file` ограничен на `*.pdf` в TEMP/HOME/DOCUMENT/DESKTOP/DOWNLOAD. **Note:** capability `shell:allow-open` УДАЛЕНА (CR-02 fix); вся open-логика через backend-validated path. |
| **G-9** | Logo aspect-ratio в PDF (minor) | 05 | PASS | `pdf/renderer.rs:370` `scale_logo_dimensions` (contain-fit + no-upscale `.min(1.0)`). `draw_logo_top_right` line 457 использует helper. 6 tests в `pdf_logo_aspect.rs` — green (width/height/square/extreme/no-upscale/zero cases). |
| **G-10** | ReturnModal только outstanding devices (blocker) | 01+03 | PASS | Backend: `act_service.rs:1610` populate_outstanding_device_ids через SQL EXCEPT (B-1 compliant); `dto/act.rs:108` ActItemDto.outstanding_device_ids. UI: `ReturnModal.svelte:48` — flatMap по `it.outstanding_device_ids`; empty state «Все позиции уже возвращены» when rows.length=0. |
| **G-11** | recompute_parent_archived correctness (major) | 01 | PASS | `acts_sqlite.rs:438-475` — `handover_total = COUNT(*) FROM act_items WHERE act_id`; `returned_total = COUNT(DISTINCT rai.device_id) ... WHERE ra.deleted_at_utc IS NULL`. Покрыто 11 integration tests `acts_clone_handover.rs` (clone count, partial/full return, undo restore). |
| **G-12** | Architectural shift clone-on-handover (blocker) | 01 | PASS | `act_service.rs:256-329` create-path: `clone_device_in_tx` для quantity>1 (effective_device_ids[]), 1 act_item ↔ 1 device_id. `dto/act.rs:152` ActReturnItemDto.device_ids[]; `effective_device_ids` helper (line 1520) — canonical-vs-legacy resolver. V015 migration data-migrate existing qty>1 act_items через recursive CTE + temp tables. |
| ~~G-13~~ | Deferred to Phase 8 | — | DEFERRED | Документировано в ROADMAP + CONTEXT. |

**Score:** 13/13 G-IDs закрыты на code-level (G-1..G-12, G-8a и G-8b разделены). Manual UAT — отдельная категория.

### Code Review Remediation Status (CR-01..04)

| CR | Описание | Remediation Status | Evidence |
|----|----------|--------------------|----------|
| **CR-01** | V015 ломает archived legacy returns с qty>1 | FIXED (commit `4c861b6`) | `V015__acts_clone_on_handover.sql:74-87` — CHECK-constrained TEMP table safety guard: если существуют pre-V015 return act_items для handovers с qty>1, INSERT даёт 0 → constraint violation → refinery rollback. Dev DB (qty всегда 1) — no-op. |
| **CR-02** | `shell:allow-open` без scope — universal open vulnerability | FIXED (commit `4c861b6`) | `capabilities/main.json:1-21` — `shell:allow-open` УДАЛЕНА. `tauri_cmds/acts.rs:192-233` новый `acts_open_pdf_in_system` команда: canonicalize → starts_with(temp_dir) → `.pdf` extension assert → потом передача в `tauri_plugin_shell::open`. `PdfPreviewModal.svelte:214` использует `apiCall('acts_open_pdf_in_system', ...)`. Зарегистрирована в `specta_export.rs:58`. |
| **CR-03** | do_return ломает legacy partial returns с qty>1 | FIXED (commit `4c861b6`) | `act_service.rs:705-729` — legacy mode (`item.device_ids.is_empty()`) требует full-closing: `per_device_qty + already_returned == handover_qty`, иначе Validation с объяснением. G-12 canonical mode не затронут. |
| **CR-04** | V015 `MAX(id)` ≠ AUTOINCREMENT.seq → orphan FK | FIXED (commit `4c861b6`) | `V015__...sql:123-127` — `id_floor = COALESCE((SELECT seq FROM sqlite_sequence WHERE name='devices'), 0)`. Plus 4e CHECK-constrained orphan-FK assertion (lines 164-174) откатывает миграцию если cloned act_items ссылаются на отсутствующие devices. |

Все 4 BLOCKER устранены.

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `migrations/V015__acts_clone_on_handover.sql` | VERIFIED | 8511 bytes; PRAGMA user_version = 15 (line 181); safety guards (4.0, 4e); recursive CTE clone (4a-4d); sqlite_sequence.seq для id_floor (CR-04 fix). |
| `crates/trackly-app/src/dto/suggest.rs` | VERIFIED | 731 bytes; `SuggestPersonField {Giver, Receiver}` с `#[serde(rename_all = "snake_case")]`. Изолирован от `dto/act.rs` (W-2). |
| `crates/trackly-app/src/services/act_service.rs` | VERIFIED | ~1900 LOC (well over min_lines 800); содержит create/clone-path (lines 256-329), MAX_CLONE_QTY (121), suggest_person (980), populate_outstanding_device_ids (1610), effective_device_ids (1520), escape_like (1410), CR-03 legacy fix (705-729). |
| `crates/trackly-infra/src/repos/devices_sqlite.rs::clone_device_in_tx` | VERIFIED | line 414 — clone helper (W-5: serial_number=NULL). |
| `crates/trackly-infra/src/repos/acts_sqlite.rs::recompute_parent_archived` | VERIFIED | line 438 — COUNT(*)/COUNT(DISTINCT) пара (G-11). |
| `crates/trackly-app/src/tauri_cmds/acts.rs` | VERIFIED | `acts_suggest_person` (line 175), `acts_open_pdf_in_system` (line 192). |
| `crates/trackly-app/src/specta_export.rs` | VERIFIED | оба новых command зарегистрированы (lines 56, 58). |
| `crates/trackly-app/capabilities/main.json` | VERIFIED | `shell:allow-open` УДАЛЕНА (CR-02); `fs:allow-write-file` scoped на `*.pdf` в стандартных user paths. |
| `crates/trackly-app/src/pdf/renderer.rs` | VERIFIED | `truncate_to_width` (line 341, G-8a), `scale_logo_dimensions` (line 370, G-9), draw_logo_top_right использует helper (line 457). |
| `ui/src/lib/components/PersonAutocomplete.svelte` | VERIFIED | 7345 bytes (well over min 50); runes + debounce + keyboard nav. |
| `ui/src/lib/components/DatePicker.svelte` | VERIFIED | 1761 bytes (well over min 30); thin wrapper над native input. |
| `ui/src/lib/components/Modal.svelte` | VERIFIED | mouseDownOnBackdrop flag + split mousedown/mouseup (G-1 fix). |
| `ui/src/features/acts/returnPayload.ts` | VERIFIED | 4778 bytes; `buildReturnItems` composite-key grouping (W-4 PER-ROW SPLIT INVARIANT). |
| `ui/src/features/acts/ReturnModal.svelte` | VERIFIED | PersonAutocomplete swap (parent.receiver→giver), outstanding flatten, G-12 device_ids[] payload. |
| `ui/src/features/acts/ReturnItemsTable.svelte` | VERIFIED | Per-device-id rows; symmetric disabled formula (G-6). |
| `ui/src/features/acts/ActFormBody.svelte` | VERIFIED | DatePicker × 2 + PersonAutocomplete × 2 + handover_date_utc payload. |
| `ui/src/features/acts/ActFormItemsTable.svelte` | VERIFIED | MAX_CLONE_QTY=1000 + W-5 serial guard. |
| `ui/src/features/acts/PdfPreviewModal.svelte` | VERIFIED | 3 кнопки (Save/Open/Print) + sanitizeFilename + acts_open_pdf_in_system call. |
| `ui/src/features/devices/DeviceAutocompleteField.svelte` | VERIFIED | suppressDropdown + lastSelected (justSelected equivalent) + inputEl.blur(). |
| `ui/src/bindings.ts` | VERIFIED | `actsSuggestPerson`, `actsOpenPdfInSystem`, `SuggestPersonField` сгенерированы (lines 239/256/606). |

### Key Link Verification

| From | To | Status | Details |
|------|----|----|---------|
| `act_service.rs::create` → `clone_device_in_tx` | wired | line 272 в create-path — `devices_repo.clone_device_in_tx(&tx, item.device_id, now)` |
| `recompute_parent_archived` → `COUNT(*)/COUNT(DISTINCT device_id)` | wired | lines 445, 453 содержат оба SQL counter — соответствует pattern `COUNT\(.*device_id\)` |
| `ReturnModal.svelte` → `acts.actsReturn(act_id, payload)` с device_ids[] | wired | payload через returnPayload.ts buildReturnItems (W-4) |
| `ReturnItemsTable.svelte` → `item.outstanding_device_ids` | wired | line 48 в ReturnModal flatMap → table |
| `PersonAutocomplete.svelte` → `acts.suggestPerson` | wired | через `acts.ts` wrapper → `acts_suggest_person` command |
| `dto/mod.rs` → `dto/suggest.rs` | wired | pub mod suggest |
| `PdfPreviewModal.handleOpen` → `acts_open_pdf_in_system` | wired | line 214 — `apiCall('acts_open_pdf_in_system', ...)` |
| `draw_logo_top_right` → `scale_logo_dimensions` | wired | line 457 — `scale_logo_dimensions(orig_w, orig_h, LOGO_WIDTH_PT, LOGO_HEIGHT_PT)` |
| `render_table_section` → `truncate_to_width` | wired | lines 268, 281 в branch ItemsTable |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend test suite — clone-handover | `cargo test -p trackly-app --test acts_clone_handover` | 11 passed | PASS |
| Backend test suite — returns + legacy | `cargo test -p trackly-app --test acts_returns` | 12 passed | PASS |
| Backend test suite — suggest autocomplete | `cargo test -p trackly-app --test acts_suggest` | 7 passed | PASS |
| Backend test suite — PDF logo aspect | `cargo test -p trackly-app --test pdf_logo_aspect` | 6 passed | PASS |
| Backend test suite — PDF column overflow + B-3 | `cargo test -p trackly-app --test pdf_column_overflow` | 3 passed | PASS |
| Svelte type-check | `pnpm svelte-check` | 0 errors, 13 warnings (unrelated state_referenced_locally) | PASS |
| `acts_open_pdf_in_system` зарегистрирован в bindings | `grep actsOpenPdfInSystem ui/src/bindings.ts` | 1 match (line 256) | PASS |
| V015 PRAGMA user_version | `grep "PRAGMA user_version = 15" migrations/V015...` | line 181 | PASS |

### Anti-Patterns / Warnings

Из 03.1-REVIEW.md остаются открытыми 11 WR (warning) и 4 IN (info). Сводка:

| WR/IN | Описание | Severity | Action |
|-------|----------|----------|--------|
| WR-01 | Modal: mouseup-вне-window не сбрасывает флаг | minor edge | NOT FIXED — рекомендуется fix в follow-up. |
| WR-02 | TZ inconsistency между ActFormBody UTC-midnight и DocumentAcceptanceModal MSK-midnight | minor (MSK-only deploy OK) | NOT FIXED — может проявиться при multi-tz (Phase 5+). |
| WR-03 | `build_fts_query` дублируется между act_service.rs и devices_sqlite.rs | tech-debt | NOT FIXED. |
| WR-04 | doc-comment `escape_like` перепутан с format_ru_date | doc-debt | NOT FIXED. |
| WR-05 | `recompute_parent_archived` всегда инкрементит version | optimistic-lock UX | NOT FIXED — может вызвать random 409. |
| WR-06 | PdfPreviewModal `pdfBytes` всегда null → re-render | perf | NOT FIXED. |
| WR-07 | `truncate_to_width` 0.5 approximation хрупкий для CYR | G-8a edge | NOT FIXED — G-8a manual UAT может выявить. |
| WR-08 | populate_outstanding docstring drift | doc | NOT FIXED. |
| WR-09 | ReturnModal toast `ret.number_raw + suffix` fragile regex | minor | NOT FIXED. |
| WR-10 | MAX_CLONE_QTY дублируется TS+Rust | drift | NOT FIXED (документировано). |
| WR-11 | `handleQtyInput` silently clamps 0→1 | UX | NOT FIXED. |
| IN-01 | `_act_item_row_ref` dead-code stub | cleanup | NOT FIXED. |
| IN-02 | act_dto_from_row outdated doc | doc | NOT FIXED. |
| IN-03 | DeviceAutocompleteField `_userTyping` outdated comment | doc | NOT FIXED. |
| IN-04 | acts_returns тест полагается на legacy fallback | test-debt | NOT FIXED. |

**Classification:** Все WR/IN — minor/tech-debt/doc-drift; ни один не блокирует goal achievement и не противоречит UAT G-IDs. Рекомендация для пользователя — оставить WR-01/02/05/07 на follow-up issue.

### Requirements Coverage

В REQUIREMENTS.md не отслеживаются G-IDs напрямую — это UAT gaps из `03-UAT.md`, прокинутые через CONTEXT.md. Все G-1..G-12 закрыты согласно таблице выше. ACT-* requirements уже maрked complete в REQUIREMENTS.md (Phase 03), и G-IDs представляют semantic-correctness исправления поверх существующего scope без расширения REQUIREMENTS.md.

### Human Verification Required

См. frontmatter — 13 manual UAT items (тасков «UAT» в планах 01, 03, 04, 05, 06 были deferred per skip-smoke convention). Это финальный UAT всей фазы 03.1; до получения user-acceptance нельзя считать G-1..G-12 фактически закрытыми с точки зрения пользователя.

### Gaps Summary

**Нет gaps на code-level.** Все 13 G-IDs (G-1..G-12, с G-8 split на a/b) имеют:
- Implementations с правильным wiring (verified через grep).
- Automated tests на критических местах (acts_clone_handover 11, acts_returns 12, acts_suggest 7, pdf_logo_aspect 6, pdf_column_overflow 3 — все green).
- Cargo clippy clean, svelte-check 0 errors.
- Code review BLOCKERs (CR-01..04) closed в `4c861b6`.

**Open follow-up items** (не блокирующие):
- 11 WR + 4 IN остаются open — рекомендуется завести follow-up issue.
- Manual UAT по 13 пунктам — единственный реальный gate перед merge/release.

---

_Verified: 2026-05-31_
_Verifier: Claude (gsd-verifier)_
_Methodology: Goal-backward verification против must_haves в 6 PLAN frontmatter._
