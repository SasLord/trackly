---
phase: 06-snmp
plan: 06
type: execute
wave: 5
depends_on:
  - 06-04
  - 06-05
files_modified:
  - ui/src/bindings.ts
  - ui/src/app.svelte
  - ui/src/features/cartridges/OperationModal.svelte
autonomous: false
requirements:
  - PRN-01
  - PRN-02
  - PRN-03
  - PRN-04
  - PRN-05
  - PRN-06
  - PRN-07
  - PRN-08
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-07

must_haves:
  truths:
    - "tauri-specta generate обновляет bindings.ts: PrinterDto, RequestDto, WsEvent, TonerLevel, AlertSeverity и все новые типы присутствуют"
    - "cargo check — зелёный без warnings"
    - "pnpm svelte-check — зелёный"
    - "Сотрудник в браузере LAN: открывает /index.html → видит раздел Заявки → создаёт заявку → получает подтверждение"
    - "Специалист получает WS toast при новой заявке от сотрудника"
    - "Принтеры: список → карточка → TonerGauge → Discovery (мок)"
    - "TRACKLY_SNMP_MOCK=1 запускает поллер с фикстурными данными (без реального SNMP)"
  artifacts:
    - path: "ui/src/bindings.ts"
      provides: "Типы для Phase 6: PrinterDto, RequestDto, WsEvent, etc."
      contains: "PrinterDto"
    - path: "ui/src/app.svelte"
      provides: "Nav ссылки на Принтеры и Заявки"
      contains: "Принтеры"
  key_links:
    - from: "ui/src/bindings.ts"
      to: "ui/src/features/printers/api.ts"
      via: "PrinterDto type import"
      pattern: "import.*PrinterDto"
    - from: "ui/src/bindings.ts"
      to: "ui/src/features/requests/api.ts"
      via: "RequestDto type import"
      pattern: "import.*RequestDto"
---

<objective>
Финальный план фазы 6: регенерировать specta bindings, добавить nav-ссылки, проверить preFillPrinterId prop в OperationModal, запустить все проверки компиляции и завершить человеческой верификацией всей фазы.

Purpose: Убедиться, что весь стек Phase 6 связан от DB до UI и работает в режиме mock без реального оборудования.
Output: Полная Phase 6 — компилируется, проходит svelte-check, проходит мок-smoke-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-snmp/06-CONTEXT.md
@.planning/phases/06-snmp/06-PATTERNS.md
@.planning/phases/06-snmp/06-03-SUMMARY.md
@.planning/phases/06-snmp/06-04-SUMMARY.md
@.planning/phases/06-snmp/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить bindings.ts + nav + OperationModal preFillPrinterId prop</name>
  <files>
    ui/src/bindings.ts,
    ui/src/app.svelte,
    ui/src/features/cartridges/OperationModal.svelte
  </files>
  <read_first>
    - ui/src/bindings.ts (текущий вариант — понять структуру, куда добавить новые типы)
    - ui/src/app.svelte (текущая навигация — найти место для Принтеры/Заявки)
    - ui/src/features/cartridges/OperationModal.svelte (Props — добавить preFillPrinterId если не добавлен)
    - crates/trackly-app/src/commands/printers.rs (specta-аннотированные типы для ручного дополнения bindings, если specta generate недоступен)
    - crates/trackly-app/src/commands/requests.rs (аналогично)
    - .planning/phases/06-snmp/06-PATTERNS.md §bindings.ts прогноз новых типов
  </read_first>
  <action>
    bindings.ts: Регенерировать командой tauri-specta (если настроено), иначе вручную добавить типы из Rust DTOs. Новые типы из Phase 6:
    PrinterStatus ('online' | 'offline' | 'warning' | 'error' | 'unknown'),
    AlertSeverity ('info' | 'warning' | 'critical'),
    TonerLevel ({ color_name: string, level_percent: number | null, is_low: boolean }),
    PrinterDto ({ id, device_id, ip_address, usb_host_device_id, community_configured: boolean, profile_id, status, toner_levels: TonerLevel[], last_polled_at: string | null, active_alert_id: number | null }),
    PrinterListItem (сокращённый вариант для списка),
    RequestStatus ('open' | 'in_progress' | 'completed' | 'rejected'),
    RequestType ('cartridge_replace' | 'free_form'),
    RequestDto ({ id, version, request_type: RequestType, status: RequestStatus, printer_device_id: number | null, printer_name: string | null, category_id: number | null, category_name: string | null, description: string | null, requested_by_user_id, requested_by_name, assigned_to_user_id: number | null, assigned_to_name: string | null, created_at, updated_at, resolution_notes: string | null, completed_cartridge_id: number | null }),
    RequestCreateDto ({ request_type: RequestType, printer_device_id?: number, category_id?: number, description?: string }),
    RequestTransitionPayload (union type: Accept | Reject { notes?: string } | Complete { notes?: string, linked_cartridge_id?: number }),
    WsEvent (union: { type: 'NewRequest', payload: RequestDto } | { type: 'PrinterAlert', payload: {...} } | { type: 'RequestUpdated', payload: RequestDto }),
    DiscoveryResult ({ ip: string, hostname: string | null, community: string, profile_id: number, printer_name: string | null }),
    AuditEntry ({ id, action, performed_by_name, created_at, metadata: string | null }).
    Сохранять стиль и структуру существующего bindings.ts (named exports, namespace objects или flat, как уже есть).

    OperationModal.svelte: Убедиться, что prop preFillPrinterId?: number объявлен в Props. Если отсутствует — добавить. При op='install' и preFillPrinterId: передать в форму установки как начальное значение для поля «Принтер» (если такое поле есть). Если OperationModal уже корректно реализован Phase 6 плана 05 — только проверить и не ломать.

    app.svelte: В боковую навигацию добавить ссылки «Принтеры» и «Заявки» если ещё не добавлены. Следовать существующему паттерну иконок и текста. Маршруты: /printers и /requests. Порядок: после раздела Картриджи (или логично рядом с оборудованием).
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check 2>&1 | grep -E "(error|Error)" | head -20; echo "Exit: $?"</automated>
  </verify>
  <done>
    bindings.ts содержит PrinterDto, RequestDto, WsEvent. OperationModal имеет preFillPrinterId prop. app.svelte содержит «Принтеры» и «Заявки» nav ссылки. pnpm svelte-check зелёный.
  </done>
</task>

<task type="auto">
  <name>Task 2: Финальная компиляция cargo check + тест мок-поллера</name>
  <files></files>
  <read_first>
    - crates/trackly-core/src/ports/snmp.rs (SnmpClient trait — проверить что всё подключено)
    - crates/trackly-infra/src/snmp/mock.rs (MockSnmpClient — TRACKLY_SNMP_MOCK)
    - crates/trackly-app/src/context.rs (AppCtx — поля printers, requests, ws_broadcast)
  </read_first>
  <action>
    Запустить cargo check --workspace и исправить все compile ошибки и warnings. Ожидаемые ловушки:
    - async_trait import missing в каком-либо файле — добавить use async_trait::async_trait
    - Secret<String> не реализует Debug — использовать #[allow(dead_code)] или manual Debug impl
    - unused import warnings — убрать или #[allow(unused_imports)] временно
    - type mismatch в AppCtx::build() — проверить что PrinterService::new() и RequestService::new() принимают Arc<dyn Trait>

    Запустить cargo test -p trackly-core --lib 2>&1 чтобы убедиться что domain tests (parse_toner_level, WsEvent::is_visible_to, transition rules) проходят.

    Проверить что TRACKLY_SNMP_MOCK=1 задокументирован в README.md (добавить одну строчку если отсутствует).

    НЕ запускать полный cargo build (долго) — cargo check достаточно для валидации.
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | grep -E "^error" | head -20; echo "cargo check exit: $?"</automated>
  </verify>
  <done>
    cargo check --workspace завершается без error строк. cargo test -p trackly-core --lib проходит.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 полностью спланирована и реализована (Планы 01-06):
    - V020-V024 миграции: printers, oid_profiles (5 vendor seeds), printer_readings, printer_alerts, request_categories, расширение requests
    - snmp2 + SnmpClient trait + MockSnmpClient (TRACKLY_SNMP_MOCK=1)
    - Repositories: SqlitePrinterRepository, SqliteRequestRepository
    - Services: PrinterService (poll loop, discovery, alerts), RequestService (lifecycle, optimistic lock)
    - WsEvent + broadcast channel в AppCtx
    - Tauri commands: printers_*, requests_*
    - axum HTTP handlers + WebSocket /api/v1/ws
    - UI: PrintersPage (discovery, TonerGauge, alerts) + RequestsPage (form, lifecycle, REQ-05 CART-07 link, history)
  </what-built>
  <how-to-verify>
    1. Запустить cargo check --workspace — ошибок нет.
    2. Запустить pnpm --dir ui svelte-check — ошибок нет.
    3. TRACKLY_SNMP_MOCK=1 cargo tauri dev (или cargo run в dev режиме):
       - Открыть раздел «Принтеры» — появляются фикстурные принтеры (Pantum 100%, HP 8%, Canon offline)
       - TonerGauge: Pantum зелёный, HP красный (8%), Canon — без данных
       - Мок-принтер HP: active alert «Тонер менее 10%» — видна плашка PrinterAlertBanner
       - Canon: offline — видна плашка с ошибкой
    4. Открыть раздел «Заявки» — список пустой, кнопка «Создать заявку» видна.
       - Создать заявку «Замена картриджа» → принтер HP → Отправить → toast «Заявка отправлена».
       - В браузере (localhost:8443 или 8080 если без TLS) войти как специалист → Заявки → получить WS toast «Новая заявка» (или обновить и увидеть заявку в списке).
       - Открыть заявку → Принять в работу → статус «В работе».
       - «Установить картридж» → OperationModal открывается с pre-filled принтером.
    5. Проверить раздел «История» в заявке — строки с переходами статусов.
  </how-to-verify>
  <resume-signal>Напишите "ok" если все пункты прошли, или опишите конкретный сбой (какой шаг, что ожидалось, что получили)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cargo check final | Все новые модули скомпилированы без unsafe warnings |
| specta bindings | Типы в bindings.ts точно соответствуют Rust DTOs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18-T | Tampering | bindings.ts ручное редактирование | mitigate | specta generate используется как source of truth; если ручное — reviewer checklist: каждый тип проверяется vs Rust struct |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | snmp2 prошёл Package Legitimacy Gate review в Plan 01; нет новых cargo/npm зависимостей в этом плане |
</threat_model>

<verification>
1. cargo check --workspace — exit 0
2. pnpm --dir ui svelte-check — exit 0
3. grep "PrinterDto" ui/src/bindings.ts — найден
4. grep "RequestDto" ui/src/bindings.ts — найден
5. grep "WsEvent" ui/src/bindings.ts — найден
6. grep "Принтеры\|Заявки" ui/src/app.svelte — nav ссылки найдены
7. grep "preFillPrinterId" ui/src/features/cartridges/OperationModal.svelte — найден
8. Human checkpoint: smoke test пройден
</verification>

<success_criteria>
- cargo check --workspace — зелёный
- pnpm svelte-check — зелёный
- bindings.ts содержит все типы Phase 6
- TRACKLY_SNMP_MOCK=1: фикстурные принтеры видны в UI
- Заявка от сотрудника → toast у специалиста
- OperationModal pre-fill из заявки (REQ-05)
- Секция «История» в RequestDetail
</success_criteria>

<output>
Создать `.planning/phases/06-snmp/06-06-SUMMARY.md` после завершения.

После завершения человеческой верификации: обновить `.planning/STATE.md` — фаза 06 завершена.
</output>
