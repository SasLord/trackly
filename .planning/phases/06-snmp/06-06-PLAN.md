---
phase: 06-snmp
plan: 06
type: execute
wave: 5
depends_on:
  - 06-04
  - 06-05
files_modified:
  - ui/src/bindings.ts
  - ui/src/app.svelte
autonomous: false
requirements:
  - PRN-01
  - PRN-02
  - PRN-03
  - PRN-04
  - PRN-05
  - PRN-06
  - PRN-07
  - PRN-08
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-07

must_haves:
  truths:
    - "specta export или ручная верификация bindings.ts: PrinterDto (с currentCartridgeId), RequestDto, WsEvent (request_status_changed) — все типы соответствуют Rust DTOs"
    - "cargo check — зелёный без warnings"
    - "pnpm svelte-check — зелёный"
    - "Сотрудник в браузере LAN: открывает /index.html → видит раздел Заявки → создаёт заявку → получает подтверждение"
    - "Специалист получает WS toast при новой заявке от сотрудника"
    - "Принтеры: список → карточка → TonerGauge → Discovery (мок)"
    - "TRACKLY_SNMP_MOCK=1 запускает поллер с фикстурными данными (без реального SNMP)"
    - "OperationModal имеет preFillPrinterId prop (добавлен в 06-05)"
  artifacts:
    - path: "ui/src/bindings.ts"
      provides: "Типы для Phase 6: PrinterDto (currentCartridgeId), RequestDto, WsEvent — synced with Rust specta"
      contains: "PrinterDto"
    - path: "ui/src/app.svelte"
      provides: "Nav ссылки на Принтеры и Заявки"
      contains: "Принтеры"
  key_links:
    - from: "ui/src/bindings.ts"
      to: "ui/src/features/printers/api.ts"
      via: "PrinterDto type import — currentCartridgeId поле присутствует"
      pattern: "import.*PrinterDto"
    - from: "ui/src/bindings.ts"
      to: "ui/src/features/requests/api.ts"
      via: "RequestDto type import"
      pattern: "import.*RequestDto"
---

<objective>
Финальный план фазы 6: верифицировать и при необходимости дополнить specta bindings, добавить nav-ссылки, запустить все проверки компиляции и завершить человеческой верификацией всей фазы.

Принципы bindings: specta export — primary source of truth. Ручное редактирование только для типов, которые specta не может сгенерировать. TS-only типы (PrinterStatus, AlertSeverity, TonerLevel без Rust-аналога) — НЕ добавлять; вместо них использовать string literals и inline unions в компонентах. WsEvent variant 'request_status_changed' (не 'request_updated') — единственный верный вариант, синхронизированный с Rust.

preFillPrinterId prop в OperationModal: добавлен в 06-05 Task 1 — этот план только верифицирует его наличие.

Purpose: Убедиться, что весь стек Phase 6 связан от DB до UI и работает в режиме mock без реального оборудования.
Output: Полная Phase 6 — компилируется, проходит svelte-check, проходит мок-smoke-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-snmp/06-CONTEXT.md
@.planning/phases/06-snmp/06-PATTERNS.md
@.planning/phases/06-snmp/06-03-SUMMARY.md
@.planning/phases/06-snmp/06-04-SUMMARY.md
@.planning/phases/06-snmp/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Финальная верификация bindings.ts + nav ссылки</name>
  <files>
    ui/src/bindings.ts,
    ui/src/app.svelte
  </files>
  <read_first>
    - ui/src/bindings.ts (текущий вариант после Plans 04/05 — понять что уже есть)
    - ui/src/app.svelte (текущая навигация — найти место для Принтеры/Заявки)
    - crates/trackly-app/src/dto/printer.rs (из Plan 02 — specta-аннотированные типы; WsEvent с RequestStatusChanged)
    - crates/trackly-app/src/dto/request.rs (из Plan 02 — specta-аннотированные типы)
    - .planning/phases/06-snmp/06-PATTERNS.md §bindings.ts прогноз новых типов
  </read_first>
  <action>
    bindings.ts — specta-first подход:
    1. Найти существующий механизм specta export в кодовой базе (grep "specta_export\|specta-export\|bindings" в src-tauri/ или package.json scripts). Запустить его если он доступен.
    2. После запуска specta export: проверить что СЛЕДУЮЩИЕ типы присутствуют и соответствуют Rust DTO:
       - PrinterDto: поле currentCartridgeId?: number | null (из Rust current_cartridge_id Option<i64>)
       - WsEvent: union по tag "type" с вариантами 'new_request', 'request_status_changed' (не 'request_updated'), 'printer_alert'
       - RequestDto: все поля из Rust RequestDto (camelCase)
       - PrinterFilter, PrinterListResponse, PrinterCreateDto, DiscoveredPrinterDto
       - RequestFilter, RequestListResponse, RequestCreateDto, RequestTransitionPayload
    3. Если specta export недоступен или не добавил нужные типы: добавить вручную ТОЛЬКО недостающие типы строго по форме Rust DTO. НЕ создавать TS-only типы (PrinterStatus enum, AlertSeverity, TonerLevel struct) — они не экспортируются из Rust specta и порождают рассинхронизацию.
    4. Для frontend-удобства (derived TS shapes): если компонент нуждается в 'ok' | 'warning' | 'error' | 'offline' | 'unknown' — объявить как type в компоненте через `type PrinterStatus = PrinterDto['status']` (derive from DTO), не как standalone binding.
    5. Удалить (или не добавлять) TS-only типы из предыдущей версии плана: PrinterStatus, AlertSeverity, TonerLevel — если они уже добавлены вручную и не соответствуют Rust DTO.

    app.svelte: В боковую навигацию добавить ссылки «Принтеры» и «Заявки» если ещё не добавлены. Следовать существующему паттерну иконок и текста. Маршруты: /printers и /requests. Порядок: после раздела Картриджи (или логично рядом с оборудованием).
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check 2>&1 | grep -E "(error|Error)" | head -20; echo "Exit: $?"</automated>
  </verify>
  <done>
    bindings.ts содержит PrinterDto (с currentCartridgeId), RequestDto, WsEvent (с request_status_changed). WsEvent НЕ содержит 'request_updated'. app.svelte содержит «Принтеры» и «Заявки» nav ссылки. pnpm svelte-check зелёный.
  </done>
</task>

<task type="auto">
  <name>Task 2: Финальная компиляция cargo check + тест мок-поллера</name>
  <files>
    crates/trackly-app/src/context.rs
  </files>
  <read_first>
    - crates/trackly-core/src/ports/snmp.rs (SnmpClient trait — проверить что всё подключено)
    - crates/trackly-infra/src/snmp/mock.rs (MockSnmpClient — TRACKLY_SNMP_MOCK)
    - crates/trackly-app/src/context.rs (AppCtx — поля printers, requests, ws_broadcast — возможные правки при compile ошибках)
  </read_first>
  <action>
    Запустить cargo check --workspace и исправить все compile ошибки и warnings. Ожидаемые ловушки:
    - async_trait import missing в каком-либо файле — добавить use async_trait::async_trait
    - Secret<String> не реализует Debug в plain форме — убедиться что Secret<T> имеет impl Debug { write!(f, "***") } (паттерн уже верифицирован test_secret_debug в Plan 02)
    - unused import warnings — убрать или добавить #[allow(unused_imports)] с TODO-комментарием
    - type mismatch в AppCtx::build() — проверить что PrinterService::new() и RequestService::new() принимают Arc<dyn Trait>

    Запустить cargo test -p trackly-core --lib 2>&1 чтобы убедиться что domain tests (parse_toner_level, WsEvent::is_visible_to, transition rules) проходят.

    Запустить cargo test -p trackly-app -- test_ws_unauth_401 test_snmp_mock_switch --test-threads=1 2>&1 — оба теста зелёные.

    НЕ запускать полный cargo build (долго) — cargo check достаточно для валидации.

    context.rs: если нужны правки при compile ошибках — вносить минимальные изменения (добавить missing import, исправить type mismatch). Если context.rs правки не нужны — файл всё равно указан в files_modified для возможных hotfix.
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | grep -E "^error" | head -20; echo "cargo check exit: $?"</automated>
  </verify>
  <done>
    cargo check --workspace завершается без error строк. cargo test -p trackly-core --lib проходит. test_ws_unauth_401 и test_snmp_mock_switch зелёные.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 полностью спланирована и реализована (Планы 00-06):
    - V020-V025 миграции: printers, oid_profiles (5 vendor seeds), printer_readings, printer_alerts, request_categories, расширение requests, cartridges.current_printer_device_id (PRN-07)
    - snmp2 + SnmpClient trait + MockSnmpClient (TRACKLY_SNMP_MOCK=1)
    - Repositories: SqlitePrinterRepository (с current_cartridge_for_printer), SqliteRequestRepository
    - Services: PrinterService (poll loop, discovery, alerts, current_cartridge_for_printer), RequestService (lifecycle, optimistic lock)
    - WsEvent: NewRequest / RequestStatusChanged / PrinterAlert (все варианты согласованы между Rust и TS)
    - broadcast channel в AppCtx + TRACKLY_SNMP_MOCK env switch
    - Tauri commands: printers_*, requests_*
    - axum HTTP handlers + WebSocket /api/v1/ws (auth перед upgrade, 401 при отсутствии сессии)
    - OperationModal preFillPrinterId prop (добавлен в 06-05)
    - UI: PrintersPage (discovery, TonerGauge, alerts, PRN-07 текущий картридж) + RequestsPage (form, lifecycle, REQ-05 CART-07 link, history)
  </what-built>
  <how-to-verify>
    1. Запустить cargo check --workspace — ошибок нет.
    2. Запустить pnpm --dir ui svelte-check — ошибок нет.
    3. TRACKLY_SNMP_MOCK=1 cargo tauri dev (или cargo run в dev режиме):
       - Открыть раздел «Принтеры» — появляются фикстурные принтеры (Pantum 100%, HP 8%, Canon offline)
       - TonerGauge: Pantum зелёный, HP красный (8%), Canon — без данных
       - Мок-принтер HP: active alert «Тонер менее 10%» — видна плашка PrinterAlertBanner
       - Canon: offline — видна плашка с ошибкой
       - Карточка принтера: секция «Установленный картридж» (если установлен — показывает id; если нет — «Картридж не закреплён»)
    4. Открыть раздел «Заявки» — список пустой, кнопка «Создать заявку» видна.
       - Создать заявку «Замена картриджа» → принтер HP → Отправить → toast «Заявка отправлена».
       - В браузере (localhost:8443 или 8080) войти как специалист → Заявки → получить WS toast «Новая заявка» (или обновить и увидеть заявку в списке).
       - Открыть заявку → Принять в работу → статус «В работе».
       - «Установить картридж» → OperationModal открывается с pre-filled принтером (preFillPrinterId работает).
    5. Проверить раздел «История» в заявке — строки с переходами статусов.
  </how-to-verify>
  <resume-signal>Напишите "ok" если все пункты прошли, или опишите конкретный сбой (какой шаг, что ожидалось, что получили)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cargo check final | Все новые модули скомпилированы без unsafe warnings |
| specta bindings | Типы в bindings.ts точно соответствуют Rust DTOs (specta-first) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-18-T | Tampering | bindings.ts ручное редактирование | mitigate | specta generate используется как source of truth; если ручное — reviewer checklist: каждый тип проверяется vs Rust struct; WsEvent variant 'request_status_changed' строго совпадает с Rust |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | snmp2 прошёл Package Legitimacy Gate review в Plan 01; нет новых cargo/npm зависимостей в этом плане |
</threat_model>

<verification>
1. cargo check --workspace — exit 0
2. pnpm --dir ui svelte-check — exit 0
3. grep "PrinterDto" ui/src/bindings.ts — найден
4. grep "currentCartridgeId\|current_cartridge_id" ui/src/bindings.ts — найден (PRN-07)
5. grep "RequestDto" ui/src/bindings.ts — найден
6. grep "request_status_changed" ui/src/bindings.ts — найден (не request_updated)
7. grep -v "request_updated" ui/src/bindings.ts — НЕ содержит неверный вариант
8. grep "Принтеры\|Заявки" ui/src/app.svelte — nav ссылки найдены
9. grep "preFillPrinterId" ui/src/features/cartridges/OperationModal.svelte — найден (из 06-05)
10. Human checkpoint: smoke test пройден
</verification>

<success_criteria>
- cargo check --workspace — зелёный
- pnpm svelte-check — зелёный
- bindings.ts: типы Phase 6 добавлены через specta (primary) или вручную точно по Rust DTO; нет TS-only типов без Rust-аналога
- WsEvent: 'request_status_changed' строго синхронизировано в Rust (dto/printer.rs) и TS (bindings.ts, ws.ts)
- PrinterDto.currentCartridgeId поле присутствует в bindings (PRN-07)
- OperationModal.preFillPrinterId верифицирован (из 06-05)
- TRACKLY_SNMP_MOCK=1: фикстурные принтеры видны в UI
- Заявка от сотрудника → toast у специалиста (WS push)
- OperationModal pre-fill из заявки (REQ-05)
- Секция «История» в RequestDetail
</success_criteria>

<output>
Создать `.planning/phases/06-snmp/06-06-SUMMARY.md` после завершения.

После завершения человеческой верификации: обновить `.planning/STATE.md` — фаза 06 завершена.
</output>
