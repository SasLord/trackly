---
phase: 06-snmp
plan: 01
type: execute
wave: 1
depends_on:
  - 06-00
files_modified:
  - migrations/V020__printers.sql
  - migrations/V021__oid_profiles_seed.sql
  - migrations/V022__printer_readings.sql
  - migrations/V023__printer_alerts.sql
  - migrations/V024__request_categories.sql
  - migrations/V025__cartridge_printer_link.sql
  - crates/trackly-infra/Cargo.toml
  - crates/trackly-core/src/ports/snmp.rs
  - crates/trackly-core/src/ports/printers.rs
  - crates/trackly-core/src/ports/requests.rs
  - crates/trackly-core/src/domain/printers.rs
  - crates/trackly-core/src/domain/requests.rs
  - crates/trackly-core/src/ports/mod.rs
  - crates/trackly-core/src/domain/mod.rs
  - crates/trackly-infra/src/snmp/mod.rs
  - crates/trackly-infra/src/snmp/real.rs
  - crates/trackly-infra/src/snmp/mock.rs
autonomous: true
requirements:
  - PRN-03
  - PRN-04
  - PRN-05
  - PRN-06
  - PRN-07
  - PRN-08
  - REQ-02

must_haves:
  truths:
    - "cargo test --workspace компилируется без ошибок после добавления snmp2 0.4"
    - "test_db() возвращает схему версии 25 (V020-V025 применены)"
    - "test_oid_profiles_seeded: SELECT COUNT(*) FROM oid_profiles = 5 (pantum/kyocera/hp/canon/rfc3805)"
    - "MockSnmpClient::default_fixtures() возвращает детерминированные данные для 3 фикстурных принтеров"
    - "SnmpClient trait — в trackly-core без tokio/snmp2 импортов"
    - "no_io_deps тест trackly-core остаётся зелёным"
    - "cartridges.current_printer_device_id колонка существует (V025 применена)"
    - "CHECK(ip_address IS NOT NULL OR usb_host_device_id IS NOT NULL) в printers — USB-only принтер создаётся с NULL ip_address"
  artifacts:
    - path: "migrations/V020__printers.sql"
      provides: "printers table (FK→devices, community, oid_profile_id, usb_host_device_id)"
      contains: "CREATE TABLE printers"
    - path: "migrations/V021__oid_profiles_seed.sql"
      provides: "oid_profiles table + 5 vendor seeds"
      contains: "INSERT INTO oid_profiles"
    - path: "migrations/V022__printer_readings.sql"
      provides: "printer_readings table (time-series snapshots)"
      contains: "idx_printer_readings_printer_ts"
    - path: "migrations/V023__printer_alerts.sql"
      provides: "printer_alerts table (UNIQUE on printer_id)"
      contains: "UNIQUE REFERENCES printers"
    - path: "migrations/V024__request_categories.sql"
      provides: "request_categories table + ALTER TABLE requests + completed_cartridge_id"
      contains: "ALTER TABLE requests ADD COLUMN category_id"
    - path: "migrations/V025__cartridge_printer_link.sql"
      provides: "ALTER TABLE cartridges ADD COLUMN current_printer_device_id (FK→devices, per D-PRN07-01)"
      contains: "ALTER TABLE cartridges ADD COLUMN current_printer_device_id"
    - path: "crates/trackly-core/src/ports/snmp.rs"
      provides: "SnmpClient trait (async_trait, NO I/O imports)"
      exports: ["SnmpClient", "OidValue", "ProbedDevice"]
    - path: "crates/trackly-infra/src/snmp/mock.rs"
      provides: "MockSnmpClient with default_fixtures()"
      exports: ["MockSnmpClient"]
  key_links:
    - from: "crates/trackly-infra/src/snmp/real.rs"
      to: "snmp2::AsyncSession"
      via: "impl SnmpClient for RealSnmpClient"
      pattern: "impl SnmpClient for RealSnmpClient"
    - from: "migrations/V020__printers.sql"
      to: "migrations/V021__oid_profiles_seed.sql"
      via: "oid_profile_id FK — V021 создаётся первым"
      pattern: "PRAGMA user_version = 21"
    - from: "migrations/V025__cartridge_printer_link.sql"
      to: "crates/trackly-app/src/services/printer_service.rs"
      via: "install CartridgeService ставит current_printer_device_id при CART-07 (per D-PRN07-01)"
      pattern: "current_printer_device_id"
---

<objective>
Заложить полный фундамент Phase 6: 6 миграций (V020-V025), добавить snmp2 0.4 в зависимости, создать hexagonal-слой core (SnmpClient trait, PrinterRepository port, RequestRepository port, domain типы). Создать mock и real SNMP-клиентов в trackly-infra.

V025 добавляет `cartridges.current_printer_device_id` (D-PRN07-01) — FK-связь, по которой PrinterDetail показывает «какой картридж сейчас стоит».

Purpose: Plan 02 и Plan 03 не могут стартовать без схемы БД и domain-типов — они компилятся против этого фундамента.
Output: Компилируемый workspace с миграциями V020-V025, domain structs, port traits, mock+real SNMP adapters.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-snmp/06-CONTEXT.md
@.planning/phases/06-snmp/06-RESEARCH.md
@.planning/phases/06-snmp/06-PATTERNS.md
@.planning/STATE.md

<interfaces>
<!-- Существующие паттерны, которые нужно соблюдать -->

From migrations/V019__users_is_active.sql — последняя миграция, новые начинаются с V020:
  PRAGMA user_version = 19;

From migrations/V006__requests.sql — таблица requests уже существует, V024 добавляет через ALTER TABLE:
  requests(id, request_type CHECK('cartridge_replace','free_form','ad_register'),
           status CHECK('open','in_progress','completed','rejected'),
           requested_by_user_id, assigned_to_user_id, printer_device_id, cartridge_model_id,
           description, resolution_notes, created_at_utc, updated_at_utc, deleted_at_utc, version)

From migrations/V005__cartridges.sql — таблица cartridges уже существует, V025 добавляет колонку:
  cartridges(id, cartridge_model_id FK, status, location_id, assigned_device_id, ...)
  V025 добавляет: current_printer_device_id INTEGER NULL REFERENCES devices(id)
  Это FK не на printers.id, а на devices.id (принтер-как-устройство, per D-PRN07-01)

From crates/trackly-core/src/ports/cartridges.rs — образец port trait:
  pub trait CartridgeRepository { type Conn; fn get/list/counts ... }
  Write paths (*_in_tx helpers) НЕ в trait — на SqliteCartridgeRepository

From crates/trackly-core/src/primitives/clock.rs — образец infra trait в core без I/O:
  pub trait Clock: Send + Sync { fn unix_seconds(&self) -> i64; }
  (SnmpClient строится по той же модели)

From crates/trackly-infra/src/snmp/ — нужно создать mod.rs с pub mod real; pub mod mock;
From crates/trackly-core/src/ports/mod.rs — нужно добавить pub mod printers; pub mod requests; pub mod snmp;
From crates/trackly-core/src/domain/mod.rs — нужно добавить pub mod printers; pub mod requests;

OID профили из 06-RESEARCH.md §OID Reference Sets:
  Pantum vendor_prefix='1.3.6.1.4.1.40093', toner_level_oid='1.3.6.1.4.1.40093.6.3.1', toner_encoding='percent'
  Kyocera vendor_prefix='1.3.6.1.4.1.1347', toner_level_oid='1.3.6.1.2.1.43.11.1.1.9.1.1', toner_encoding='level_over_max'
  HP vendor_prefix='1.3.6.1.4.1.11', toner_encoding='level_over_max'
  Canon vendor_prefix='1.3.6.1.4.1.1602', toner_encoding='level_over_max'
  RFC3805 vendor_prefix='', toner_encoding='level_over_max' (fallback)
  page_counter_oid для всех кроме Pantum: '1.3.6.1.2.1.43.10.2.1.4.1.1'
  status_oid для всех: '1.3.6.1.2.1.25.3.5.1.1.1' (hrPrinterStatus)

snmp2 0.4 добавить в crates/trackly-infra/Cargo.toml:
  snmp2 = { version = "0.4", features = ["crypto-rust", "tokio"] }
  async-trait = добавить если ещё нет (для impl SnmpClient на RealSnmpClient)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Миграции V020-V025 + snmp2 в Cargo.toml</name>
  <files>
    migrations/V020__printers.sql,
    migrations/V021__oid_profiles_seed.sql,
    migrations/V022__printer_readings.sql,
    migrations/V023__printer_alerts.sql,
    migrations/V024__request_categories.sql,
    migrations/V025__cartridge_printer_link.sql,
    crates/trackly-infra/Cargo.toml
  </files>
  <read_first>
    - migrations/V019__users_is_active.sql (последняя миграция — PRAGMA user_version = 19)
    - migrations/V005__cartridges.sql (образец таблицы с FK; V025 добавляет колонку в cartridges)
    - migrations/V001__init_pragmas_and_lookups.sql (образец lookup-таблицы + seed INSERT)
    - migrations/V006__requests.sql (таблица requests уже существует — V024 делает только ALTER TABLE)
    - crates/trackly-infra/Cargo.toml (для добавления snmp2)
    - .planning/phases/06-snmp/06-PATTERNS.md §migrations (полный SQL из паттернов)
    - .planning/phases/06-snmp/06-RESEARCH.md §OID Reference Sets (полные OID для seed)
    - .planning/phases/06-snmp/06-CONTEXT.md §D-PRN07-01 (зачем current_printer_device_id в cartridges)
  </read_first>
  <behavior>
    - test_db() компилируется без изменений и возвращает версию 25 после применения V020-V025
    - SELECT COUNT(*) FROM oid_profiles = 5
    - SELECT name FROM oid_profiles WHERE toner_encoding='percent' = 'pantum'
    - V024 ALTER TABLE requests ADD COLUMN category_id + completed_cartridge_id не падает на уже существующей таблице
    - V025 ALTER TABLE cartridges ADD COLUMN current_printer_device_id — колонка существует, IS NULL для всех текущих строк
    - test_printer_usb_only (из Wave 0 стабов): INSERT printers с usb_host_device_id SET и ip_address NULL — CHECK(ip_address IS NOT NULL OR usb_host_device_id IS NOT NULL) пропускает вставку; SELECT возвращает строку с usb_host_device_id
    - cargo check -p trackly-infra компилируется с snmp2 0.4 (feature crypto-rust + tokio)
  </behavior>
  <action>
    Создать шесть SQL-миграций строго последовательно (V020→V021→V022→V023→V024→V025), соблюдая PRAGMA user_version на каждой.

    V020__printers.sql: Таблица printers. SQLite FK проверяется при INSERT, не при CREATE TABLE — порядок V020/V021 допустим (printers ссылается на oid_profiles, которая создаётся в V021).
    Столбцы: id INTEGER PRIMARY KEY AUTOINCREMENT, device_id INTEGER NOT NULL UNIQUE REFERENCES devices(id), ip_address TEXT NULL, community TEXT NOT NULL DEFAULT 'public', snmp_version TEXT NOT NULL DEFAULT 'v2c' CHECK(snmp_version IN ('v1','v2c','v3')), vendor TEXT NULL, oid_profile_id INTEGER NULL REFERENCES oid_profiles(id), last_seen_utc INTEGER NULL, usb_host_device_id INTEGER NULL REFERENCES devices(id), created_at_utc INTEGER NOT NULL, updated_at_utc INTEGER NOT NULL, version INTEGER NOT NULL DEFAULT 1.
    CONSTRAINT check_ip_or_usb CHECK(ip_address IS NOT NULL OR usb_host_device_id IS NOT NULL).
    PRAGMA user_version = 20.

    V021__oid_profiles_seed.sql: CREATE TABLE oid_profiles(id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE, vendor_prefix TEXT NOT NULL, toner_level_oid TEXT NULL, toner_max_oid TEXT NULL, toner_encoding TEXT NOT NULL DEFAULT 'level_over_max' CHECK(toner_encoding IN ('percent','level_over_max')), page_counter_oid TEXT NULL, status_oid TEXT NOT NULL, serial_oid TEXT NULL, notes TEXT NULL). INSERT 5 строк (pantum/kyocera/hp/canon/rfc3805) с OID из RESEARCH.md. Pantum: toner_encoding='percent', toner_max_oid=NULL. RFC3805: vendor_prefix=''. PRAGMA user_version = 21.

    V022__printer_readings.sql: CREATE TABLE printer_readings(id INTEGER PRIMARY KEY AUTOINCREMENT, printer_id INTEGER NOT NULL REFERENCES printers(id) ON DELETE CASCADE, ts_utc INTEGER NOT NULL, toner_levels TEXT NULL, page_count INTEGER NULL, status TEXT NOT NULL DEFAULT 'unknown' CHECK(status IN ('ok','warning','error','offline','unknown'))). CREATE INDEX idx_printer_readings_printer_ts ON printer_readings(printer_id, ts_utc DESC). PRAGMA user_version = 22.

    V023__printer_alerts.sql: CREATE TABLE printer_alerts(id INTEGER PRIMARY KEY AUTOINCREMENT, printer_id INTEGER NOT NULL UNIQUE REFERENCES printers(id) ON DELETE CASCADE, alert_type TEXT NOT NULL CHECK(alert_type IN ('offline','error')), first_seen_utc INTEGER NOT NULL, last_seen_utc INTEGER NOT NULL, acknowledged_at_utc INTEGER NULL). PRAGMA user_version = 23.

    V024__request_categories.sql: CREATE TABLE request_categories(id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE). INSERT INTO request_categories(name) VALUES ('Ремонт техники'),('Расходные материалы'),('Программное обеспечение'),('Прочее'). ALTER TABLE requests ADD COLUMN category_id INTEGER NULL REFERENCES request_categories(id). ALTER TABLE requests ADD COLUMN completed_cartridge_id INTEGER NULL REFERENCES cartridges(id). PRAGMA user_version = 24.

    V025__cartridge_printer_link.sql (per D-PRN07-01): ALTER TABLE cartridges ADD COLUMN current_printer_device_id INTEGER NULL REFERENCES devices(id). Это позволяет PrinterService при CART-07 install ставить current_printer_device_id = printer.device_id, а PrinterRepository::current_cartridge_for_printer(printer_device_id) делать SELECT из cartridges WHERE current_printer_device_id = ?. PRAGMA user_version = 25.

    crates/trackly-infra/Cargo.toml: добавить snmp2 = { version = "0.4", features = ["crypto-rust", "tokio"] }. Также добавить async-trait = "0.1" если ещё не в зависимостях (нужен для impl SnmpClient).
  </action>
  <verify>
    <automated>cargo test -p trackly-infra test_db -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    cargo test -p trackly-infra test_db — зелёный; schema_version = 25; SELECT COUNT(*) FROM oid_profiles = 5; cartridges.current_printer_device_id колонка существует; snmp2 компилируется.
  </done>
</task>

<task type="auto">
  <name>Task 2: Core domain/ports + Infra SNMP adapters (real + mock)</name>
  <files>
    crates/trackly-core/src/ports/snmp.rs,
    crates/trackly-core/src/ports/printers.rs,
    crates/trackly-core/src/ports/requests.rs,
    crates/trackly-core/src/domain/printers.rs,
    crates/trackly-core/src/domain/requests.rs,
    crates/trackly-core/src/ports/mod.rs,
    crates/trackly-core/src/domain/mod.rs,
    crates/trackly-infra/src/snmp/mod.rs,
    crates/trackly-infra/src/snmp/real.rs,
    crates/trackly-infra/src/snmp/mock.rs,
    crates/trackly-infra/src/lib.rs
  </files>
  <read_first>
    - crates/trackly-core/src/ports/cartridges.rs (ТОЧНЫЙ образец port trait с type Conn, read-only methods, НЕ write paths в trait)
    - crates/trackly-core/src/domain/cartridges.rs (образец domain structs: NO serde/specta derives, только Debug/Clone/PartialEq/Eq)
    - crates/trackly-core/src/primitives/clock.rs (образец infra trait в core без I/O — SnmpClient строится аналогично)
    - crates/trackly-core/src/ports/mod.rs (для добавления pub mod)
    - crates/trackly-core/src/domain/mod.rs (для добавления pub mod)
    - crates/trackly-infra/src/lib.rs (для добавления pub mod snmp)
    - .planning/phases/06-snmp/06-PATTERNS.md §snmp.rs, §printers.rs, §requests.rs (полные сигнатуры)
    - .planning/phases/06-snmp/06-RESEARCH.md §SnmpClient port trait, §MockSnmpClient
    - crates/trackly-core/tests/no_io_deps.rs (если существует — НЕ нарушать)
  </read_first>
  <action>
    Создать hexagonal-слой для Phase 6. Все создаваемые файлы следуют паттернам из PATTERNS.md.

    crates/trackly-core/src/ports/snmp.rs: Определить #[async_trait::async_trait] pub trait SnmpClient: Send + Sync с методами get_oids(target, community, oids: &[&str], timeout_secs) → Result<Option<Vec<OidValue>>, AppError> и probe(target, community) → Result<Option<ProbedDevice>, AppError>. Определить pub struct OidValue { pub oid: String, pub value: SnmpValue } и pub enum SnmpValue { Integer(i64), OctetString(Vec<u8>), Gauge(u64), Counter(u64), Unknown }. Определить pub struct ProbedDevice { pub ip: String, pub sys_object_id: String, pub sys_descr: String, pub sys_name: String }. КРИТИЧНО: NO tokio/snmp2 импортов в trackly-core — только async_trait + AppError.

    crates/trackly-core/src/ports/printers.rs: pub trait PrinterRepository { type Conn; fn get/list/get_last_reading/list_active_alerts/list_oid_profiles/get_oid_profile_by_prefix/current_cartridge_for_printer }. Метод current_cartridge_for_printer(conn, printer_device_id: i64) → Result<Option<i64>, AppError> — возвращает cartridge id если есть. Все методы read-only (ТОЛЬКО чтения). Write paths (*_in_tx) — на SqlitePrinterRepository, НЕ в trait (паттерн cartridges). Сигнатуры из PATTERNS.md §PrinterRepository.

    crates/trackly-core/src/ports/requests.rs: pub trait RequestRepository { type Conn; fn get/list/counts }. Минимальный read-only trait.

    crates/trackly-core/src/domain/printers.rs: Domain structs WITHOUT serde/specta (только Debug/Clone/PartialEq/Eq): PrinterRow (id, device_id, ip_address Option<String>, snmp_version String, vendor Option<String>, oid_profile_id Option<i64>, last_seen_utc Option<i64>, usb_host_device_id Option<i64>, device_name Option<String>, device_location Option<String>, created_at_utc i64, updated_at_utc i64, version i64), PrinterReadingRow (id, printer_id, ts_utc, toner_levels_json String, page_count Option<i64>, status String), PrinterAlertRow (id, printer_id, alert_type String, first_seen_utc, last_seen_utc, acknowledged_at_utc Option<i64>), OidProfileRow (полная из PATTERNS.md), DiscoveredPrinter (ip, sys_object_id, sys_descr, sys_name, vendor Option<String>, oid_profile_id Option<i64>, is_duplicate bool), PrinterFilter { status: Option<String>, search: Option<String> }, PrinterNew { device_id i64, ip_address Option<String>, community_raw String, snmp_version String, oid_profile_id Option<i64>, usb_host_device_id Option<i64> }.

    crates/trackly-core/src/domain/requests.rs: RequestRow (id, request_type, status, requested_by_user_id, assigned_to_user_id Option<i64>, printer_device_id Option<i64>, cartridge_model_id Option<i64>, category_id Option<i64>, completed_cartridge_id Option<i64>, description Option<String>, resolution_notes Option<String>, requester_name Option<String>, printer_name Option<String>, created_at_utc, updated_at_utc, deleted_at_utc Option<i64>, version), RequestNew { request_type String, requested_by_user_id i64, printer_device_id Option<i64>, cartridge_model_id Option<i64>, category_id Option<i64>, description Option<String> }, RequestTransitionOp (enum из PATTERNS.md: Accept, Reject{notes}, Complete{notes, linked_cartridge_id Option<i64>}) с validate_from_status/target_status/audit_action.

    crates/trackly-infra/src/snmp/real.rs: pub struct RealSnmpClient. impl SnmpClient: get_oids создаёт AsyncSession::new_v2c(target, community.as_bytes(), 0).await, затем tokio::time::timeout(Duration::from_secs(timeout_secs), sess.get(&parsed_oids)).await. Timeout или ошибка → Ok(None). probe — то же с [sysObjectID, sysDescr, sysName]. ВСЕГДА оборачивать в tokio::time::timeout (Pitfall 1 из RESEARCH.md). Парсинг varbinds: snmp2::Value::Integer(i) → SnmpValue::Integer(i), ::OctetString(b) → SnmpValue::OctetString(b.to_vec()).

    crates/trackly-infra/src/snmp/mock.rs: pub struct PrinterFixture {toner_pct: u8, page_count: i64, status: &'static str, vendor: &'static str, sys_object_id: &'static str}. pub struct MockSnmpClient { fixtures: HashMap<String, PrinterFixture> }. impl MockSnmpClient { pub fn default_fixtures() → Self } с 3 фикстурами (192.168.1.100=Pantum ok, .101=HP warning 8%, .102=Canon offline). impl SnmpClient for MockSnmpClient: get_oids ищет target в fixtures по ip-части (без :161), возвращает детерминированные OidValue. probe аналогично.

    Обновить mod.rs файлы: trackly-core ports/mod.rs добавить pub mod snmp; pub mod printers; pub mod requests;. domain/mod.rs добавить pub mod printers; pub mod requests;. trackly-infra/src/lib.rs добавить pub mod snmp;.

    ВАЖНО: snmp.rs в trackly-core должен иметь async_trait как зависимость trackly-core или переиспользовать уже существующий. Проверить Cargo.toml trackly-core. Если async-trait ещё не в deps — добавить.
  </action>
  <verify>
    <automated>cargo test --workspace -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <done>
    cargo test --workspace зелёный; trackly-core no_io_deps тест зелёный; MockSnmpClient::default_fixtures() компилируется; все pub mod экспортированы; current_cartridge_for_printer метод в PrinterRepository trait.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → SQLite | SQL в миграциях — embedded compile-time, не пользовательский ввод |
| snmp2 crate → cargo | Внешняя зависимость из crates.io |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-M | Tampering | snmp2 0.4 cargo install | mitigate | snmp2 помечен [ASSUMED] в RESEARCH.md — Plan 01 добавляет `cargo-deny` проверку; slopcheck не установлен, поэтому executor должен вручную верифицировать `cargo search snmp2` → версия 0.4.x, repo roboplc/snmp2 на GitHub |
| T-06-02-M | Information Disclosure | community string в migrations | accept | community в migrations/V020 — DEFAULT 'public', не секрет; реальные community пишутся через Rust-слой с Secret<T>; в SQL-миграции только DEFAULT значение |
| T-06-03-M | Tampering | CHECK constraints V020-V025 | mitigate | Все enum-поля защищены CHECK constraints; FK ON DELETE CASCADE на printer_readings/printer_alerts предотвращает orphan rows |
</threat_model>

<verification>
После выполнения всего плана:

1. cargo test --workspace — зелёный
2. SELECT COUNT(*) FROM oid_profiles в test_db() = 5
3. cargo check -p trackly-infra — компилируется с snmp2
4. trackly-core no_io_deps тест зелёный (SnmpClient trait без I/O imports)
5. PRAGMA table_info(cartridges) | grep current_printer_device_id — строка найдена (V025 применена)
</verification>

<success_criteria>
- Миграции V020-V025 применены; schema_version = 25 в test_db()
- oid_profiles содержит 5 записей (pantum/kyocera/hp/canon/rfc3805)
- cartridges.current_printer_device_id добавлена (per D-PRN07-01)
- SnmpClient trait в trackly-core без tokio/snmp2 imports
- PrinterRepository::current_cartridge_for_printer объявлен в trait
- MockSnmpClient с 3 фикстурами (включая offline-состояние для тестов алертов)
- RealSnmpClient с обязательным timeout на каждый SNMP вызов
- cargo test --workspace зелёный
</success_criteria>

<output>
Создать `.planning/phases/06-snmp/06-01-SUMMARY.md` после завершения.
</output>
