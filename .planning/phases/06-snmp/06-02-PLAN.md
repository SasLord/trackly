---
phase: 06-snmp
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - crates/trackly-infra/src/repos/printers_sqlite.rs
  - crates/trackly-infra/src/repos/requests_sqlite.rs
  - crates/trackly-infra/src/repos/mod.rs
  - crates/trackly-app/src/services/printer_service.rs
  - crates/trackly-app/src/services/request_service.rs
  - crates/trackly-app/src/services/mod.rs
  - crates/trackly-app/src/dto/printer.rs
  - crates/trackly-app/src/dto/request.rs
  - crates/trackly-app/src/dto/mod.rs
autonomous: true
requirements:
  - PRN-01
  - PRN-02
  - PRN-05
  - PRN-06
  - PRN-08
  - REQ-01
  - REQ-03
  - REQ-05
  - REQ-07

must_haves:
  truths:
    - "test_request_create: RequestService::create записывает строку в requests, audit_log"
    - "test_request_lifecycle: Accept/Reject/Complete переходы валидируются; недопустимый переход → AppError::Validation"
    - "test_ws_event_sent: broadcast::Sender получает WsEvent::NewRequest после create"
    - "test_alert_detection: status='error'|'offline' из SNMP → upsert в printer_alerts (dedup — второй upsert не создаёт дубликат)"
    - "test_toner_percent: parse_toner_level(45, 100) = 45%; parse_toner_level(-2, -2) = None (unknown)"
    - "test_readings_prune: prune_old_readings_in_tx удаляет строки старше retention_cutoff"
    - "community НЕ попадает в PrinterDto (Pitfall 4): community_configured: bool вместо строки"
  artifacts:
    - path: "crates/trackly-infra/src/repos/printers_sqlite.rs"
      provides: "SqlitePrinterRepository (impl PrinterRepository + tx-helpers)"
      exports: ["SqlitePrinterRepository"]
    - path: "crates/trackly-infra/src/repos/requests_sqlite.rs"
      provides: "SqliteRequestRepository (impl RequestRepository + tx-helpers)"
      exports: ["SqliteRequestRepository"]
    - path: "crates/trackly-app/src/services/printer_service.rs"
      provides: "PrinterService (list/get/create_from_device/poll_single/poll_all/discover/acknowledge_alert)"
      exports: ["PrinterService", "WsEvent", "run_poll_task"]
    - path: "crates/trackly-app/src/services/request_service.rs"
      provides: "RequestService (create/list/get/transition)"
      exports: ["RequestService"]
    - path: "crates/trackly-app/src/dto/printer.rs"
      provides: "PrinterDto (без community), PrinterCreateDto, DiscoveredPrinterDto, PrinterListResponse"
      exports: ["PrinterDto", "PrinterCreateDto", "DiscoveredPrinterDto"]
    - path: "crates/trackly-app/src/dto/request.rs"
      provides: "RequestDto, RequestCreateDto, RequestTransitionPayload, RequestListResponse"
      exports: ["RequestDto", "RequestTransitionPayload"]
  key_links:
    - from: "crates/trackly-app/src/services/printer_service.rs"
      to: "WriterHandle::execute"
      via: "все write paths через single-writer"
      pattern: "self.writer.execute"
    - from: "crates/trackly-app/src/services/request_service.rs"
      to: "broadcast::Sender"
      via: "ws_tx.send(WsEvent::NewRequest) после create"
      pattern: "ws_tx.send"
    - from: "crates/trackly-app/src/dto/printer.rs"
      to: "PrinterRow"
      via: "From<PrinterRow> for PrinterDto"
      pattern: "impl From<PrinterRow> for PrinterDto"
---

<objective>
Вертикальный срез: создать репозитории SQLite, сервисы PrinterService и RequestService, DTO-слой. Сервисы реализуют core value (единый set методов для обоих транспортов), WsEvent для push, и SNMP-polling логику (обнаружение, snapshot, alert detection, retention prune).

Purpose: Plan 03 (Tauri commands + HTTP handlers) и Plan 04 (AppCtx wire-up) зависят от этих сервисов. Plan 05-06 (UI) зависят от DTO.
Output: PrinterService + RequestService с полным domain-покрытием; WsEvent enum; retention prune; тесты зелёные.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-snmp/06-CONTEXT.md
@.planning/phases/06-snmp/06-RESEARCH.md
@.planning/phases/06-snmp/06-PATTERNS.md
@.planning/phases/06-snmp/06-01-SUMMARY.md

<interfaces>
<!-- Существующие сервисы как образцы -->

From crates/trackly-app/src/services/cartridge_service.rs (ТОЧНЫЙ образец):
  pub struct CartridgeService { writer: Arc<WriterHandle>, readers: Arc<ReaderPool>, clock: Arc<dyn Clock>, ... }
  // Чтения через spawn_blocking → readers.acquire()
  // Записи через writer.execute(|conn| { tx = conn.transaction(); ... tx.commit() })
  // audit_repo.insert в каждой write transaction

From crates/trackly-infra/src/repos/cartridges_sqlite.rs (образец repo):
  const SELECT_*: &str (JOIN query)
  fn map_row(row: &Row) → rusqlite::Result<DomainRow>
  impl CartridgeRepository (read-only methods)
  fn *_in_tx (write helpers НЕ в trait)

From crates/trackly-app/src/dto/cartridge.rs:
  #[derive(Debug, Clone, Serialize, Deserialize, Type)]  // specta::Type для bindings
  #[serde(rename_all = "camelCase")]  // camelCase для HTTP parity
  pub struct CartridgeDto { #[specta(type = i32)] pub id: i64, ... }

From crates/trackly-app/src/services/act_service.rs (паттерн WS push):
  // После mutation: app_handle.emit()/ws_tx.send() — в TAURI cmd, НЕ в service
  // Service возвращает data; transport-специфичный push — в cmd/handler

From crates/trackly-core/src/auth.rs (authorize):
  pub fn authorize(caller: &Identity, action: &Action) -> Result<(), AppError>
  // Новые Action варианты: MutatePrinters, MutateRequests нужно добавить в trackly-core/src/auth.rs

From crates/trackly-infra/src/repos/audit_log_sqlite.rs:
  pub struct AuditEntry { entity_type, entity_id, action, user_id, before_json, after_json, payload_json, created_at_utc }
  pub fn insert(tx, entry) → Result<(), AppError>

WsEvent: определяется в printer_service.rs или отдельном файле dto/ws_event.rs. Должен быть Clone + Serialize + specta::Type.
#[serde(tag = "type", rename_all = "snake_case")] enum с вариантами:
  NewRequest { request_id: i64, request_type: String, requester_name: String }
  RequestStatusChanged { request_id: i64, new_status: String }
  PrinterAlert { printer_id: i64, printer_name: String, alert_type: String }
impl WsEvent::is_visible_to(&Identity) → bool (role filter — Pitfall из RESEARCH: сотрудник не видит PrinterAlert)

broadcast channel создаётся в AppCtx (Plan 04), но сервисы принимают Arc<broadcast::Sender<WsEvent>> в new().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLite репозитории (printers + requests)</name>
  <files>
    crates/trackly-infra/src/repos/printers_sqlite.rs,
    crates/trackly-infra/src/repos/requests_sqlite.rs,
    crates/trackly-infra/src/repos/mod.rs
  </files>
  <read_first>
    - crates/trackly-infra/src/repos/cartridges_sqlite.rs (ТОЧНЫЙ образец: SELECT константа, map_row, impl trait, *_in_tx helpers)
    - crates/trackly-infra/src/repos/mod.rs (для pub mod printers_sqlite; pub mod requests_sqlite)
    - crates/trackly-core/src/ports/printers.rs (из Plan 01 — методы для impl)
    - crates/trackly-core/src/ports/requests.rs (из Plan 01 — методы для impl)
    - crates/trackly-core/src/domain/printers.rs (типы возврата)
    - crates/trackly-core/src/domain/requests.rs (типы возврата)
    - .planning/phases/06-snmp/06-PATTERNS.md §printers_sqlite.rs, §requests_sqlite.rs (SQL константы и tx-helpers)
  </read_first>
  <behavior>
    - test_printer_repo_create_and_get: create_in_tx вставляет строку, get возвращает её
    - test_request_repo_create: insert_in_tx + list возвращают созданную заявку
    - test_prune_old_readings: prune_old_readings_in_tx(cutoff=now-1) удаляет старые строки
    - test_upsert_alert_dedup: два upsert_alert_in_tx с одним printer_id → одна строка (UNIQUE)
  </behavior>
  <action>
    printers_sqlite.rs: следовать PATTERNS.md §printers_sqlite.rs.

    SELECT_PRINTERS константа с LEFT JOIN devices. Функция map_row_printer. impl PrinterRepository for SqlitePrinterRepository с методами get, list (с фильтрацией по status из last reading через subquery или JOIN printer_readings), get_last_reading, list_active_alerts, list_oid_profiles, get_oid_profile_by_prefix.

    Tx-helpers (НЕ в trait): create_in_tx(tx, PrinterNew, now_utc) → Result<i64>, upsert_reading_in_tx(tx, printer_id, snapshot), upsert_alert_in_tx(tx, printer_id, alert_type, now_utc) — использует INSERT OR REPLACE с UNIQUE на printer_id, update_last_seen_in_tx(tx, printer_id, now_utc, status_cache).

    prune_old_readings_in_tx(tx, retention_cutoff_utc: i64, downsample_cutoff_utc: i64): Шаг 1 — DELETE FROM printer_readings WHERE ts_utc < retention_cutoff_utc. Шаг 2 — downsample: DELETE WHERE ts_utc < downsample_cutoff_utc AND id NOT IN (SELECT MIN(id) FROM printer_readings WHERE ts_utc < downsample_cutoff_utc GROUP BY printer_id, date(ts_utc, 'unixepoch')).

    requests_sqlite.rs: SELECT_REQUESTS константа с LEFT JOIN users и LEFT JOIN devices. map_row_request. impl RequestRepository с get, list (фильтрация по status, request_type; для роли Employee — только свои requested_by_user_id=caller_id), counts(conn) → RequestCounts { open, in_progress, completed, rejected }.

    Tx-helpers: insert_in_tx(tx, RequestNew, user_id, now_utc) → Result<i64>, transition_in_tx(tx, request_id, version, op: &RequestTransitionOp, assigned_to, now_utc). transition_in_tx: проверяет optimistic lock (version=expected), обновляет status+resolution_notes+updated_at+version+1.
  </action>
  <verify>
    <automated>cargo test -p trackly-infra repos -- --test-threads=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    Все тесты репозитория зелёные; prune, upsert alert dedup, transition lifecycle проверены.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PrinterService + RequestService + DTO слой</name>
  <files>
    crates/trackly-app/src/services/printer_service.rs,
    crates/trackly-app/src/services/request_service.rs,
    crates/trackly-app/src/services/mod.rs,
    crates/trackly-app/src/dto/printer.rs,
    crates/trackly-app/src/dto/request.rs,
    crates/trackly-app/src/dto/mod.rs
  </files>
  <read_first>
    - crates/trackly-app/src/services/cartridge_service.rs (ТОЧНЫЙ образец структуры сервиса, spawn_blocking паттерн, writer.execute паттерн)
    - crates/trackly-app/src/dto/cartridge.rs (образец DTO: specta::Type, camelCase, i64→i32 с #[specta(type=i32)])
    - crates/trackly-core/src/auth.rs (authorize + Action enum — нужно добавить MutatePrinters, MutateRequests)
    - crates/trackly-infra/src/repos/printers_sqlite.rs (из Task 1)
    - crates/trackly-infra/src/repos/requests_sqlite.rs (из Task 1)
    - .planning/phases/06-snmp/06-PATTERNS.md §printer_service.rs, §request_service.rs, §printer.rs (DTO), §request.rs (DTO)
    - .planning/phases/06-snmp/06-RESEARCH.md §snmp2 API Reference (poll_single паттерн с timeout), §WsEvent
  </read_first>
  <behavior>
    - test_request_create: RequestService::create(RequestCreateDto, &admin_identity) → RequestDto в БД; audit_log запись создана
    - test_request_lifecycle: transition(Accept) open→in_progress OK; transition(Complete) open→completed → AppError::Validation (неверный переход)
    - test_ws_event_sent: после create → broadcast::Receiver.try_recv() == Ok(WsEvent::NewRequest{...})
    - test_alert_detection: PrinterService::detect_and_upsert_alert('error') → INSERT в printer_alerts; второй вызов UPDATE (не INSERT дубликат)
    - test_toner_percent: parse_toner_level(45, 100, "level_over_max") = Some(45); parse_toner_level(-2, -2, "level_over_max") = None
    - test_snmp_mock_switch: TRACKLY_SNMP_MOCK=1 env → MockSnmpClient используется; без env → RealSnmpClient
    - test_readings_prune: RequestService не участвует; PrinterService::prune_old_readings() вызывает writer.execute с prune_old_readings_in_tx
  </behavior>
  <action>
    Сначала добавить в crates/trackly-core/src/auth.rs новые Action варианты: MutatePrinters (admin+specialist), MutateRequests (все для create, admin+specialist для transition), ReadPrinters (admin+specialist), ReadRequests (все). Добавить в authorize() match.

    dto/printer.rs: WsEvent enum (Clone + Serialize + specta::Type, #[serde(tag="type", rename_all="snake_case")]) с вариантами NewRequest/RequestStatusChanged/PrinterAlert + is_visible_to(&Identity). PrinterDto (id i64 #[specta(type=i32)], device_id, ip_address Option<String>, snmp_version, vendor, oid_profile_id Option, last_seen_utc Option, community_configured: bool — НИКОГДА не строка!, device_name, device_location, toner_levels: Option<serde_json::Value>, page_count Option<i64>, status Option<String>, has_alert: bool, alert_type Option<String>, version). impl From<PrinterRow> for PrinterDto с community_configured=true (placeholder — реальная логика в сервисе). PrinterCreateDto (device_id i32, ip_address Option, community_update Option<String>, snmp_version, oid_profile_id Option<i32>, usb_host_device_id Option<i32>). DiscoveredPrinterDto (ip, vendor, model, sys_name, oid_profile_id Option<i32>, is_duplicate bool). PrinterListResponse { items: Vec<PrinterDto>, total: i64 }.

    dto/request.rs: RequestDto (id, request_type, status, requested_by_user_id, assigned_to_user_id Option, printer_device_id Option, cartridge_model_id Option, category_id Option, description Option, resolution_notes Option, requester_name Option, printer_name Option, created_at_utc, updated_at_utc, version — все i64 с #[specta(type=i32)]). RequestCreateDto { request_type String, printer_device_id Option<i32>, cartridge_model_id Option<i32>, category_id Option<i32>, description Option<String> }. RequestTransitionPayload (#[serde(tag="op", rename_all="snake_case")] enum: Accept{request_id i32, version i32, assigned_to_user_id Option<i32>}, Reject{request_id, version, notes Option<String>}, Complete{request_id, version, notes Option<String>, linked_cartridge_id Option<i32>}). RequestListResponse { items, total }.

    services/printer_service.rs: PrinterService struct с writer, readers, clock, printer_repo: Arc<SqlitePrinterRepository>, audit_repo, snmp_client: Arc<dyn SnmpClient>, poll_tx: mpsc::Sender<i64>, ws_tx: Arc<broadcast::Sender<WsEvent>>. fn new(...) → Self.

    Методы: list(filter, pagination) → spawn_blocking+readers; get(id) → spawn_blocking; create_from_device(payload, caller) → authorize(MutatePrinters) + writer.execute(create_in_tx + audit) + self.get(id); discover(ip_start_str, ip_end_str, community_str, caller) → authorize(MutatePrinters) + параллельный scan через tokio::spawn per IP с Semaphore(64) + timeout(2s) per probe; admit(discovered: Vec<DiscoveredPrinterDto>, selected_ips: Vec<String>, caller) → batch create_from_device; poll_single(printer_id) → SNMP get_oids с OID из профиля + writer.execute(upsert_reading + update_last_seen + detect_and_upsert_alert); poll_all() → list all printers + for each poll_single; acknowledge_alert(printer_id, caller) → authorize(MutatePrinters) + writer update acknowledged_at_utc; prune_old_readings() → читает retention из app_settings (default 90 дней / 30 дней downsample) + writer.execute(prune_old_readings_in_tx).

    Вспомогательные функции модуля: fn parse_toner_level(level: i64, max: i64, encoding: &str) → Option<u8>; fn detect_alert_type(status: &str) → Option<&'static str> ("error"|"offline" → Some, иначе None).

    pub async fn run_poll_task(svc: Arc<PrinterService>, mut rx: mpsc::Receiver<i64>, shutdown: CancellationToken): tokio::select! на interval.tick() + rx.recv() + shutdown.cancelled(). Interval из app_settings (default 300 сек). MissedTickBehavior::Skip. Каждый tick: svc.poll_all().await + svc.prune_old_readings().await (retention maintenance).

    services/request_service.rs: RequestService struct с writer, readers, clock, request_repo, audit_repo, ws_tx. Методы: create(dto: RequestCreateDto, caller: &Identity) → authorize(CreateRequests) + writer.execute(insert_in_tx + audit) + ws_tx.send(WsEvent::NewRequest{...}) + self.get(id); list(filter, pagination, caller_id, caller_role) → spawn_blocking (если Employee — filter by requested_by_user_id=caller_id); get(id) → spawn_blocking; transition(payload: RequestTransitionPayload, caller: &Identity) → authorize(TransitionRequests) + validate_from_status (из domain RequestTransitionOp) + writer.execute(transition_in_tx + audit + если Complete{linked_cartridge_id=Some} → update completed_cartridge_id на requests) + ws_tx.send(WsEvent::RequestStatusChanged{...}).

    mod.rs обновить: pub use printer_service::{PrinterService, run_poll_task}; pub use request_service::RequestService;. dto/mod.rs: pub mod printer; pub mod request; (WsEvent экспортировать из printer pub use dto::printer::WsEvent).
  </action>
  <verify>
    <automated>cargo test -p trackly-app services -- --test-threads=1 2>&1 | tail -30</automated>
  </verify>
  <done>
    test_request_create, test_request_lifecycle, test_ws_event_sent, test_alert_detection, test_toner_percent, test_snmp_mock_switch, test_readings_prune — все зелёные. community не в DTO (Pitfall 4 закрыт).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RequestService::create → DB | Пользовательские данные из браузера — validated через DTO |
| PrinterService::discover → сеть | IP-диапазон от admin — потенциальный SSRF к внутренней сети |
| broadcast::Sender → WS clients | Роль-фильтр не в channel, а в WsEvent::is_visible_to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-S | Elevation of Privilege | RequestService::transition | mitigate | authorize(caller, &Action::TransitionRequests) + validate_from_status проверяет допустимый статус; нельзя пропустить через HTTP (D-RBAC-01) |
| T-06-05-T | Tampering | PrinterService::discover (SSRF) | mitigate | discover только для admin (authorize MutatePrinters); IP парсится через Ipv4Addr::from_str (невалидный → ошибка); RFC1918-проверка опциональна — в Phase 6 только admin может запустить |
| T-06-06-I | Information Disclosure | WsEvent broadcast leakage | mitigate | WsEvent::is_visible_to(&identity) server-side role filter; PrinterAlert только admin; сотрудник не видит чужих заявок |
| T-06-07-I | Information Disclosure | community в PrinterDto | mitigate | community_configured: bool (NOT String) в PrinterDto; Secret<String> wrapping при SNMP вызове; Pitfall 4 закрыт структурно |
| T-06-08-T | Tampering | optimistic lock на requests | mitigate | transition_in_tx проверяет version=expected; несовпадение → AppError::Validation (стандартный паттерн Phase 3/4) |
</threat_model>

<verification>
1. cargo test -p trackly-app services -- --test-threads=1 — зелёный
2. grep -r "community" crates/trackly-app/src/dto/printer.rs | grep -v "community_configured" | grep -v "#" — пусто (community_configured — единственное поле)
3. grep "is_visible_to" crates/trackly-app/src/dto/printer.rs — найден (role filter присутствует)
</verification>

<success_criteria>
- PrinterService реализует: list/get/create_from_device/discover/admit/poll_single/poll_all/acknowledge_alert/prune_old_readings
- RequestService реализует: create/list/get/transition
- WsEvent с is_visible_to (role filter server-side)
- run_poll_task с CancellationToken + MissedTickBehavior::Skip + prune в каждом tick
- Все тесты (test_request_create, test_request_lifecycle, test_ws_event_sent, test_alert_detection, test_toner_percent, test_snmp_mock_switch, test_readings_prune) зелёные
</success_criteria>

<output>
Создать `.planning/phases/06-snmp/06-02-SUMMARY.md` после завершения.
</output>
