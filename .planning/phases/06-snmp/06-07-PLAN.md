---
phase: 06-snmp
plan: 07
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - ui/src/features/requests/api.ts
  - ui/src/features/requests/RequestsSearchAndTabs.svelte
  - ui/src/features/printers/PrintersSearchAndTabs.svelte
  - crates/trackly-infra/src/repos/requests_sqlite.rs
  - crates/trackly-app/src/services/request_service.rs
  - crates/trackly-app/src/tauri_cmds/requests.rs
  - crates/trackly-app/src/http/requests.rs
  - crates/trackly-app/src/specta_export.rs
  - ui/src/bindings-phase6.ts
autonomous: false
requirements:
  - REQ-01
  - REQ-02
  - REQ-07

must_haves:
  truths:
    - "Сотрудник создаёт заявку (свободная форма) через UI — invoke не падает, возвращается созданная заявка"
    - "Создание заявки «замена картриджа» через UI возвращает созданную заявку (после исправления источника select в 06-08)"
    - "История заявки (REQ-07) загружается в RequestDetail: requests_get_history возвращает строки audit_log по этой заявке"
    - "Счётчики статусов заявок загружаются через requests_counts (без рассинхрона имён)"
    - "Svelte-check проходит без warning про <nav role=\"tablist\"> в RequestsSearchAndTabs / PrintersSearchAndTabs"
    - "Сотрудник видит «Создать заявку» + свои заявки read-only (ролевой рендер портала корректен)"
  artifacts:
    - path: "ui/src/features/requests/api.ts"
      provides: "create шлёт arg-key 'dto'; statusCounts→requests_counts; getHistory→requests_get_history"
    - path: "crates/trackly-app/src/services/request_service.rs"
      provides: "get_history(request_id) → Vec<AuditEntryDto>"
      contains: "pub async fn get_history"
    - path: "crates/trackly-app/src/tauri_cmds/requests.rs"
      provides: "build_requests_get_history + requests_get_history command"
      contains: "requests_get_history"
    - path: "crates/trackly-infra/src/repos/requests_sqlite.rs"
      provides: "get_history по entity_type='request'"
      contains: "fn get_history"
  key_links:
    - from: "ui/src/features/requests/api.ts"
      to: "requests_create"
      via: "apiCall arg-key 'dto'"
      pattern: "requests_create.*dto"
    - from: "RequestDetail.getHistory"
      to: "requests_get_history"
      via: "apiCall"
      pattern: "requests_get_history"
---

<objective>
Закрыть critical-гэпы заявок из 06-VERIFICATION.md (truths 4 и частично 6) и REQ-07:
1. Рассинхрон arg-key создания заявки — `requests_create` ждёт `dto`, фронт шлёт `payload` (invoke падает).
2. Рассинхрон имён команд — фронт зовёт `requests_status_counts` (бэкенд `requests_counts`) и `requests_get_history` (команды нет вообще).
3. Реализовать историю заявки `requests_get_history` (REQ-07).
4. a11y: `<nav role="tablist">` в SearchAndTabs (svelte warning).
5. Проверить ролевой рендер портала (сотрудник: create + свои заявки read-only) per D-GAP-Employee-Access — БЕЗ тестового employee-логина.

Purpose: Портал заявок (core value фазы) сейчас полностью сломан на создании; история заявок отсутствует. Без этого REQ-01/REQ-02/REQ-07 не выполняются end-to-end.
Output: Рабочее создание заявок на всех транспортах, история заявки, корректные счётчики, чистый svelte-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-snmp/06-VERIFICATION.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — без разведки по кодовой базе. -->

Бэкенд-команда (уже корректна — НЕ менять сигнатуру):
- `requests_create(state, dto: RequestCreateDto) -> RequestDto`  (tauri_cmds/requests.rs)
- HTTP `CreatePayload { dto: RequestCreateDto }` (http/requests.rs) — уже ждёт ключ `dto`.
- `requests_counts(state) -> RequestCountsDto` — БЕЗ аргумента filter. HTTP route `/api/v1/requests_counts` POST без тела-фильтра.

DTO RequestCreateDto (snake_case в Rust, camelCase на проводе через serde rename_all):
  request_type, printer_device_id, cartridge_model_id, category_id, description.

Текущий фронт-баг (ui/src/features/requests/api.ts):
- `create: (payload) => apiCall('requests_create', { payload })`  ← неверный ключ, должно быть `{ dto: payload }`.
- `statusCounts: (filter) => apiCall('requests_status_counts', { filter })`  ← неверное имя + лишний filter.
- `getHistory: (id) => apiCall('requests_get_history', { id })`  ← команды нет на бэкенде.

Аналог для get_history (КОПИРОВАТЬ структуру):
- `cartridges_get_history` (tauri_cmds/cartridges.rs:273) → `build_cartridges_get_history` → `cartridge_service.get_history(id) -> Vec<AuditEntryDto>` (cartridge_service.rs:448) → `cartridges_sqlite.get_history(conn, id)` (cartridges_sqlite.rs:731, фильтр `entity_type='cartridge' AND entity_id=?1 AND action NOT IN ('list','get')`).
- HTTP handler аналог: http/cartridges.rs:239 handler_get_history + route `/api/v1/cartridges_get_history`.
- DTO `AuditEntryDto` уже существует (crates/trackly-app/src/dto/cartridge.rs:442) — переиспользовать, НЕ создавать новый.

audit_log заявок уже пишется с `entity_type: "request"` (request_service.rs:158 create, :260 transition) — данные для истории УЖЕ есть.

Ролевой рендер (уже частично готов, только проверить):
- RequestsPage.svelte: `isEmployee = identity?.role === 'employee'`, baseFilter подставляет requestedByUserId для employee, emptyConfig для employee = «Создать заявку». Backend enforcement на сервисе уже есть (D-RBAC-02).
</interfaces>

<read_first>
- ui/src/features/requests/api.ts (модифицируемый — все три бага здесь)
- ui/src/features/requests/RequestFormModal.svelte:100-112 (вызов requests.create — понять, что передаётся объект без ключа dto)
- ui/src/features/requests/RequestDetail.svelte:75-92 (потребитель getHistory)
- crates/trackly-app/src/tauri_cmds/cartridges.rs:98-102,273-277 (аналог build_+command get_history)
- crates/trackly-app/src/services/cartridge_service.rs:448-470 (аналог service.get_history → AuditEntryDto)
- crates/trackly-infra/src/repos/cartridges_sqlite.rs:731-755 (аналог repo.get_history с фильтром entity_type)
- crates/trackly-app/src/http/cartridges.rs:239-252,419-420 (аналог http handler + route)
- crates/trackly-app/src/dto/cartridge.rs:442 (AuditEntryDto — переиспользовать)
- crates/trackly-app/src/specta_export.rs:72,114-116 (регистрация cartridges_get_history — образец; добавить requests_get_history)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Реализовать requests_get_history на всех слоях (REQ-07)</name>
  <files>crates/trackly-infra/src/repos/requests_sqlite.rs, crates/trackly-app/src/services/request_service.rs, crates/trackly-app/src/tauri_cmds/requests.rs, crates/trackly-app/src/http/requests.rs, crates/trackly-app/src/specta_export.rs</files>
  <read_first>
  - crates/trackly-infra/src/repos/cartridges_sqlite.rs:731-755 (analog repo.get_history)
  - crates/trackly-app/src/services/cartridge_service.rs:448-470 (analog service.get_history)
  - crates/trackly-app/src/tauri_cmds/cartridges.rs:98-102,273-277 (analog build_+command)
  - crates/trackly-app/src/http/cartridges.rs:239-252,419-420 (analog http handler+route)
  - crates/trackly-app/src/dto/cartridge.rs:442 (AuditEntryDto — reuse)
  - crates/trackly-app/src/services/request_service.rs:155-168 (proof entity_type='request' уже в audit)
  </read_first>
  <action>
  Зеркально аналогу cartridges_get_history создать историю заявки.
  (1) В `requests_sqlite.rs` добавить `pub fn get_history(&self, conn: &Connection, request_id: i64) -> Result<Vec<AuditEntryRow>, AppError>` с тем же SELECT из `audit_log`, что в cartridges_sqlite, но фильтр `WHERE entity_type = 'request' AND entity_id = ?1 AND action NOT IN ('list','get')` ORDER BY created_at_utc DESC. Использовать существующий `AuditEntryRow` (тот же, что у cartridges).
  (2) В `request_service.rs` добавить `pub async fn get_history(&self, request_id: i64) -> Result<Vec<AuditEntryDto>, AppError>` через `spawn_blocking` + `readers.acquire()`, маппинг строк в `AuditEntryDto` (импортировать тот же DTO, что cartridge_service). Поле `request_repo` / repo-хэндл — как уже устроен сервис.
  (3) В `tauri_cmds/requests.rs` добавить `build_requests_get_history(ctx, id) -> Vec<AuditEntryDto>` (делегирует `ctx.requests.get_history(id)`) + thin command `requests_get_history(state, id: i32)`.
  (4) В `http/requests.rs` добавить `GetPayload`-подобный handler `handler_get_history` (переиспользовать существующий `GetPayload { id }`) + route `/api/v1/requests_get_history` POST в router().
  (5) В `specta_export.rs` зарегистрировать `crate::tauri_cmds::requests::requests_get_history` в `collect_commands![...]` рядом с requests_counts.
  Имена/типы — точно как в аналоге; не вводить новые DTO.
  </action>
  <verify>
    <automated>cargo test -p trackly-app -p trackly-infra 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - `cargo check --workspace` зелёный без warnings.
  - В specta_export.rs присутствует `requests_get_history` в collect_commands.
  - http/requests.rs router() содержит route `/api/v1/requests_get_history`.
  - requests_sqlite.rs get_history фильтрует `entity_type = 'request'` (grep подтверждает).
  - Регресс-тест: добавить unit/integration тест (по образцу cartridges history test, если есть) — после create заявки `request_service.get_history(id)` возвращает ≥1 строку с action='create'.
  </acceptance_criteria>
  <done>requests_get_history реализована end-to-end (repo→service→tauri+http→specta), история заявки доступна, тест зелёный.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Исправить arg-key и имена команд в requests/api.ts + регенерация bindings</name>
  <files>ui/src/features/requests/api.ts, ui/src/bindings-phase6.ts</files>
  <read_first>
  - ui/src/features/requests/api.ts (все три бага)
  - ui/src/features/requests/RequestFormModal.svelte:100-112 (передаёт объект — wrapper должен обернуть в { dto })
  - ui/src/features/requests/RequestDetail.svelte:75-92 (потребитель getHistory)
  - crates/trackly-app/src/tauri_cmds/requests.rs (arg `dto`; команда requests_counts без filter; новая requests_get_history из Task 1)
  </read_first>
  <action>
  В `ui/src/features/requests/api.ts`:
  (1) `create`: изменить `apiCall<RequestDto>('requests_create', { payload })` → `apiCall<RequestDto>('requests_create', { dto: payload })`. Параметр сохранить как `RequestCreateDto`. Это согласует arg-key с Rust-аргументом `dto` и HTTP `CreatePayload.dto`.
  (2) `statusCounts`: переименовать вызов команды `'requests_status_counts'` → `'requests_counts'` и УБРАТЬ аргумент `{ filter }` (бэкенд `requests_counts` filter не принимает). Сигнатуру обёртки упростить до `counts: () => apiCall<RequestCountsDto>('requests_counts')` (или сохранить имя statusCounts без аргумента — выбрать минимально-инвазивный вариант, согласовав с потребителями; обновить все вызовы в RequestsPage/RequestsSearchAndTabs).
  (3) `getHistory`: команда теперь существует (Task 1) — оставить вызов `'requests_get_history', { id }`; убедиться, что arg-key `id` совпадает с Rust-аргументом `id: i32`.
  Регенерировать `bindings-phase6.ts` (specta export или ручная синхронизация типа AuditEntryDto, если RequestDetail типизирует историю через локальный RequestHistoryEntry — оставить локальный тип, если он совместим с возвращаемым AuditEntryDto; иначе типизировать через bindings).
  НЕ менять Rust-сигнатуры (они корректны).
  </action>
  <verify>
    <automated>cd ui && pnpm svelte-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
  - grep в api.ts: вызов `requests_create` использует ключ `dto` (не `payload`).
  - grep в api.ts: НЕТ строки `requests_status_counts`; присутствует `requests_counts`.
  - Все потребители counts (RequestsPage / RequestsSearchAndTabs) обновлены под новую сигнатуру без filter — `pnpm svelte-check` exits 0.
  - getHistory вызывает `requests_get_history` с ключом `id`.
  </acceptance_criteria>
  <done>api.ts согласован с бэкендом на всех трёх вызовах; svelte-check зелёный.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: a11y tablist + проверка ролевого рендера портала (D-GAP-Employee-Access)</name>
  <files>ui/src/features/requests/RequestsSearchAndTabs.svelte, ui/src/features/printers/PrintersSearchAndTabs.svelte</files>
  <read_first>
  - ui/src/features/requests/RequestsSearchAndTabs.svelte:44-55 (nav role="tablist" + кнопки role="tab")
  - ui/src/features/printers/PrintersSearchAndTabs.svelte:68-78 (тот же паттерн)
  - ui/src/features/requests/RequestsPage.svelte:25-44,98-99,169-200 (ролевой рендер: isEmployee, baseFilter, emptyConfig, WS gating)
  </read_first>
  <action>
  (1) a11y: в обоих SearchAndTabs устранить svelte warning «`<nav>` с role="tablist"». Корректное решение — НЕ навешивать `role="tablist"` на семантический `<nav>` (конфликт ролей). Заменить контейнер `<nav role="tablist">` на `<div role="tablist" aria-label="...">` (убрать `<nav>`), сохранив `role="tab"` + `aria-selected` на кнопках и `aria-label` на контейнере. UI-вид и поведение не меняются.
  (2) Ролевой рендер (D-GAP-Employee-Access — БЕЗ тестового employee-логина): прочитать RequestsPage.svelte и подтвердить, что для роли employee: (а) показывается действие «Создать заявку»; (б) baseFilter подставляет requestedByUserId (видит только свои); (в) detail/действия перехода (Принять/Отклонить/Выполнить) скрыты или недоступны (read-only). Если переходы видны сотруднику в UI — скрыть кнопки перехода в RequestDetail при `isEmployee` (enforcement на сервисе уже есть; это UI-полировка, серверная авторизация остаётся источником истины).
  </action>
  <what-built>Контейнер вкладок без конфликта ARIA-ролей; ролевой рендер портала проверен — сотрудник видит только create + свои заявки read-only.</what-built>
  <how-to-verify>
  1. `cd ui && pnpm svelte-check` — НЕТ warning про nav/tablist.
  2. Запустить `TRACKLY_SNMP_MOCK=1 cargo tauri dev`. Открыть раздел Заявки.
  3. Создать заявку «Свободная форма» → подтверждение «Заявка отправлена», заявка появляется в списке (это проверяет фикс arg-key из Task 2).
  4. Открыть заявку → блок «История» показывает строку создания (REQ-07).
  5. Свитч-бар статусов кликается, счётчики отображаются без ошибок в консоли.
  6. (Ролевой рендер — code-review, employee-логина нет до Phase 8): подтвердить в RequestsPage/RequestDetail, что ветка employee даёт create + read-only.
  </how-to-verify>
  <verify>
    <automated>cd ui && pnpm svelte-check 2>&1 | grep -i "tablist\|nav" || echo "no tablist warnings"</automated>
  </verify>
  <resume-signal>Напишите «approved» или опишите проблемы</resume-signal>
  <acceptance_criteria>
  - `pnpm svelte-check` не выдаёт warning про `<nav>`/`role="tablist"`.
  - Контейнеры вкладок используют `<div role="tablist">` (grep: нет `<nav` с `role="tablist"`).
  - Создание свободной заявки через UI возвращает успех (не падает invoke).
  - История заявки отображается в RequestDetail.
  - Ролевой рендер employee подтверждён ревью: create + свои заявки read-only.
  </acceptance_criteria>
  <done>svelte-check чист, создание заявки и история работают в dev, ролевой рендер подтверждён.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/Tauri → requests_create | недоверенный ввод заявки (description, printer_device_id, category_id) |
| browser → requests_get_history | чтение audit_log по request_id — возможна утечка чужой истории |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | requests_create payload | mitigate | сервис уже валидирует request_type/обяз. поля; arg-key fix не ослабляет валидацию — DTO-десериализация строгая (serde) |
| T-06-07-02 | Information Disclosure | requests_get_history | mitigate | history доступна только аутентифицированным (session_identity в http handler, resolve_tauri_identity в tauri); сотрудник видит свои заявки через baseFilter; серверный authorize остаётся источником истины (D-RBAC-02) |
| T-06-07-03 | Elevation of Privilege | ролевой рендер портала | accept | UI-скрытие переходов — удобство; реальный gate — `authorize(caller, TransitionRequests)` на сервисе (уже есть). UI не является границей доверия |
</threat_model>

<verification>
- `cargo check --workspace` и `cargo test -p trackly-app -p trackly-infra` зелёные.
- `pnpm svelte-check` зелёный, без warning tablist.
- В dev (`TRACKLY_SNMP_MOCK=1 cargo tauri dev`): создание заявки + история + счётчики работают.
</verification>

<success_criteria>
- Создание заявки на всех транспортах согласовано по arg-key `dto`.
- requests_counts вызывается под верным именем; requests_get_history реализована (REQ-07).
- a11y warning устранён; ролевой рендер портала подтверждён (D-GAP-Employee-Access).
</success_criteria>

<output>
Create `.planning/phases/06-snmp/06-07-SUMMARY.md` when done
</output>
