---
phase: 06-snmp
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - ui/src/features/printers/api.ts
  - ui/src/features/printers/PrintersPage.svelte
  - ui/src/features/printers/PrintersSearchAndTabs.svelte
  - ui/src/features/printers/PrintersMasterDetail.svelte
  - ui/src/features/printers/PrintersList.svelte
  - ui/src/features/printers/PrinterListRow.svelte
  - ui/src/features/printers/PrinterDetail.svelte
  - ui/src/features/printers/TonerGauge.svelte
  - ui/src/features/printers/PrinterAlertBanner.svelte
  - ui/src/features/printers/DiscoveryModal.svelte
  - ui/src/features/printers/DiscoveryResultsTable.svelte
  - ui/src/lib/api/ws.ts
  - ui/src/pages/PrintersPage.svelte
  - ui/src/bindings.ts
autonomous: true
requirements:
  - PRN-01
  - PRN-02
  - PRN-03
  - PRN-05
  - PRN-06
  - PRN-07
  - PRN-08

must_haves:
  truths:
    - "Раздел «Принтеры» показывает список принтеров с badge статуса и уровнем тонера"
    - "Карточка принтера содержит секции: уровни тонера (TonerGauge), страничные счётчики, история статусов, alert-баннер"
    - "DiscoveryModal: поля диапазона IP + Community, таблица результатов, чекбоксы, CTA «Завести выбранные»"
    - "Кнопка «Найти принтеры» видна только для роли admin (D-RBAC-03)"
    - "TonerGauge корректно применяет цвет (accent ≥ 25%, warning 10-24%, destructive < 10%, мутный при unknown)"
    - "PrinterAlertBanner рендерится в header PrinterDetail при has_alert=true"
    - "ws.ts подключается к /api/v1/ws в браузере, Tauri-события в десктопе (dual-transport)"
    - "PrinterDetail секция «Установленный картридж» показывает данные из PrinterDto.current_cartridge_id (PRN-07, backed by cartridges.current_printer_device_id FK)"
    - "pnpm svelte-check — зелёный"
  artifacts:
    - path: "ui/src/features/printers/PrintersPage.svelte"
      provides: "Корневой компонент раздела с master-detail layout"
      contains: "PrintersMasterDetail"
    - path: "ui/src/features/printers/TonerGauge.svelte"
      provides: "Горизонтальный progressbar с aria-* + цвет по порогу"
      contains: "role=\"progressbar\""
    - path: "ui/src/features/printers/DiscoveryModal.svelte"
      provides: "Modal для SNMP discovery с review-перед-добавлением"
      contains: "DiscoveryResultsTable"
    - path: "ui/src/lib/api/ws.ts"
      provides: "Dual-transport WS client (browser WS + Tauri events) + reconnect"
      contains: "connectWs"
  key_links:
    - from: "ui/src/features/printers/api.ts"
      to: "apiCall"
      via: "dual-transport printers_* команды"
      pattern: "apiCall.*printers_"
    - from: "ui/src/lib/api/ws.ts"
      to: "WsEvent"
      via: "dispatch(JSON.parse(e.data)) → handlers; WsEvent.type === 'request_status_changed'"
      pattern: "dispatch"
    - from: "ui/src/features/printers/PrinterDetail.svelte"
      to: "PrinterDto.current_cartridge_id"
      via: "секция «Установленный картридж» читает printer.current_cartridge_id из DTO"
      pattern: "current_cartridge_id"
---

<objective>
Вертикальный срез UI принтеров: заменить placeholder PrintersPage на полноценный раздел с master-detail, discovery, TonerGauge, alert-баннером. Создать WS-клиент для real-time уведомлений.

PRN-07 «Установленный картридж» — данные берутся из PrinterDto.current_cartridge_id (поле добавлено в 06-02), которое заполняется запросом current_cartridge_for_printer(). Это backing query, реализованный на стороне сервера в Plan 02/03.

Purpose: Пользователь может видеть принтеры, запускать discovery, просматривать тонер и алерты в карточке. WS-клиент питает toast для push-событий в Plans 05-06.
Output: Работающий раздел «Принтеры» в Tauri и браузере; ws.ts для обоих транспортов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-snmp/06-UI-SPEC.md
@.planning/phases/06-snmp/06-CONTEXT.md
@.planning/phases/06-snmp/06-PATTERNS.md
@.planning/phases/06-snmp/06-03-SUMMARY.md

<interfaces>
<!-- Svelte 5 паттерны из кодовой базы -->

From ui/src/features/cartridges/CartridgesPage.svelte (ТОЧНЫЙ образец):
  let items = $state<CartridgeDto[]>([]);
  let selectedId = $state<number | null>(null);
  const activeFilter = $derived<CartridgeFilter>({...});
  $effect(() => { void activeFilter; refresh(); });
  $effect(() => { const id = selectedId; if (!id) { selected = null; return; }
    api.get(id).then(dto => selected = dto).catch(...); });
  onMount(() => loadAll());

From ui/src/features/cartridges/LowStockBanner.svelte (образец alert banner):
  role="alert", aria-live="polite", warning SVG icon, conditional {#if items.length > 0}
  --color-warning border/icon, background color-mix

From ui/src/features/cartridges/CartridgesMasterDetail.svelte:
  {#snippet master} ... {/snippet}
  {#snippet detail} ... {/snippet}
  grid: '35% 65%', gap: --space-md

From ui/src/lib/api/client.ts (dual-transport apiCall):
  export function apiCall<T>(command: string, args: Record<string, unknown>): Promise<T>
  // Maps: Tauri invoke если isTauri, иначе POST /api/v1/{command} camelCase JSON

From ui/src/features/cartridges/api.ts (образец api.ts):
  import { apiCall } from '$lib/api/client';
  export const cartridges = { list: (filter, pagination) => apiCall<CartridgeListResponse>('cartridges_list', ...) }

PrinterDto из bindings.ts (из Plan 02/03):
  { id: number, deviceId: number, ipAddress: string | null, usbHostDeviceId: number | null,
    communityConfigured: boolean, vendor: string | null, status: string | null,
    tonerLevels: unknown | null,   // serde_json::Value → camelCase → unknown
    pageCount: number | null, hasAlert: boolean, alertType: string | null,
    currentCartridgeId: number | null,   // PRN-07 — заполнен сервером
    lastSeenUtc: number | null, deviceName: string | null, version: number }

WsEvent из bindings.ts (tag-union):
  { type: 'new_request', requestId: number, requestType: string, requesterName: string }
  | { type: 'request_status_changed', requestId: number, newStatus: string }   ← snake_case из Rust
  | { type: 'printer_alert', printerId: number, printerName: string, alertType: string }

UI-SPEC ключевые контракты (06-UI-SPEC.md):
  TonerGauge: role="progressbar", aria-valuenow, aria-valuemin=0, aria-valuemax=100, высота 8px
  PrinterAlertBanner: паттерн LowStockBanner (role="alert", aria-live="polite")
  DiscoveryModal: size="wide" (960px), таблица с чекбоксами, badge «Уже заведён» для дубликатов
  Кнопка «Найти принтеры» — видна только admin (identity.role === 'admin')
  Switch-bar статусов: null=«Все», online=«В сети», warning=«Предупреждение», error=«Ошибка», offline=«Не в сети»

bindings.ts регенерация:
  Использовать specta export как primary mechanism (cargo test с specta-export feature или отдельная команда).
  Ручное дополнение только для типов, которые specta не может экспортировать.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts + ws.ts + bindings регенерация</name>
  <files>
    ui/src/features/printers/api.ts,
    ui/src/lib/api/ws.ts,
    ui/src/bindings.ts
  </files>
  <read_first>
    - ui/src/features/cartridges/api.ts (ТОЧНЫЙ образец dual-transport обёртки)
    - ui/src/lib/api/client.ts (apiCall + isTauri паттерн)
    - ui/src/bindings.ts (текущие bindings — понять формат для регенерации)
    - .planning/phases/06-snmp/06-PATTERNS.md §api.ts, §ws.ts (полные TypeScript паттерны)
    - .planning/phases/06-snmp/06-RESEARCH.md §Frontend WS reconnect pattern, §Tauri events path
  </read_first>
  <action>
    ui/src/features/printers/api.ts: Создать по образцу cartridges/api.ts.
    export const printers = { list, get, create, delete, discover, admit, refresh, acknowledgeAlert }.
    Маппинг команд: printers_list, printers_get, printers_create, printers_delete, printers_discover, printers_admit, printers_refresh, printers_acknowledge_alert.
    Типы из bindings: PrinterDto, PrinterFilter, PrinterListResponse, PrinterCreateDto, DiscoveredPrinterDto, Pagination.
    discover принимает (ipStart: string, ipEnd: string, community: string) → DiscoveredPrinterDto[].
    admit принимает (selectedIps: string[], community: string) → number (кол-во созданных).

    ui/src/lib/api/ws.ts: Полный dual-transport WS клиент из PATTERNS.md §ws.ts и RESEARCH.md.
    WsEventHandler type. handlers: WsEventHandler[]. onWsEvent(handler) → unlisten fn. dispatch(event: WsEvent).
    connectWs(): если isTauri → import('@tauri-apps/api/event').listen('trackly-event', e => dispatch(e.payload)); возвращает unlisten.
    Если браузер → new WebSocket(`${wss_protocol}://${location.host}/api/v1/ws`).
    ws.onmessage → dispatch(JSON.parse(e.data)).
    ws.onclose → setTimeout(connect, reconnectDelay); reconnectDelay = Math.min(delay * 2, 30000).
    ws.onopen → reconnectDelay = 1000; showReconnectingToast() при повторном подключении.
    Экспортировать: onWsEvent, connectWs, disconnectWs.
    КРИТИЧНО: WsEvent в ts-файле — union по полю "type" с вариантами: 'new_request', 'request_status_changed' (не 'request_updated'), 'printer_alert'. Синхронизировано с Rust DTO.

    bindings.ts: Запустить существующий механизм specta export (найти в кодовой базе — обычно cargo test с флагом или cargo run). Если specta export доступен — запустить его и позволить ему перезаписать bindings.ts. Затем проверить что новые типы Phase 6 присутствуют: PrinterDto (с currentCartridgeId), RequestDto, WsEvent (с request_status_changed, не request_updated). Если specta export НЕ доступен или требует отдельной сборки — добавить вручную только недостающие типы, не переписывая существующие. Не создавать TS-only типы (PrinterStatus, AlertSeverity, TonerLevel) если они не экспортированы из Rust specta — вместо этого использовать string literals и inline unions в компонентах.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm svelte-check зелёный; api.ts экспортирует printers объект; ws.ts экспортирует connectWs/onWsEvent с 'request_status_changed' (не 'request_updated'); bindings.ts содержит PrinterDto (с currentCartridgeId) и RequestDto.
  </done>
</task>

<task type="auto">
  <name>Task 2a: Базовые UI компоненты принтеров (list, navigation, layout)</name>
  <files>
    ui/src/features/printers/PrintersPage.svelte,
    ui/src/features/printers/PrintersSearchAndTabs.svelte,
    ui/src/features/printers/PrintersMasterDetail.svelte,
    ui/src/features/printers/PrintersList.svelte,
    ui/src/features/printers/PrinterListRow.svelte,
    ui/src/features/printers/TonerGauge.svelte,
    ui/src/features/printers/PrinterAlertBanner.svelte
  </files>
  <read_first>
    - ui/src/features/cartridges/CartridgesPage.svelte (Svelte 5 runes page паттерн)
    - ui/src/features/cartridges/CartridgesMasterDetail.svelte (master-detail grid, {#snippet} паттерн)
    - ui/src/features/cartridges/CartridgesList.svelte (empty-config объект, loading spinner)
    - ui/src/features/cartridges/CartridgeListRow.svelte (строка списка паттерн)
    - ui/src/features/cartridges/CartridgesSearchAndTabs.svelte (switch-bar + search + toolbar)
    - ui/src/features/cartridges/LowStockBanner.svelte (ТОЧНЫЙ образец для PrinterAlertBanner)
    - ui/src/features/layout/auth-store.svelte.ts (identity/role для role-based UI)
    - .planning/phases/06-snmp/06-UI-SPEC.md §TonerGauge, §PrinterAlertBanner, §switch-bar принтеров
    - .planning/phases/06-snmp/06-PATTERNS.md §PrintersPage.svelte, §TonerGauge, §PrinterAlertBanner
  </read_first>
  <action>
    TonerGauge.svelte: interface Props { label: string; level: number | null; max?: number; encoding: 'percent' | 'level_over_max' }. Вычислять pct: если unknown (level=-2/-3 или null) → null; если encoding='percent' → level; иначе Math.round(level*100/max). Цвет: pct >= 25 → --color-accent; 10-24 → --color-warning; < 10 → --color-destructive; null → --color-surface-sunken (track без fill). role="progressbar", aria-valuenow={pct ?? 0}, aria-valuemin=0, aria-valuemax=100, aria-label="Уровень {label}: {pct !== null ? pct+'%' : 'неизвестно'}". Высота track 8px. Fill width = pct + '%'.

    PrinterAlertBanner.svelte: ТОЧНО по LowStockBanner.svelte паттерну. Props: hasAlert: boolean, alertType: 'offline' | 'error' | null, lastSeenUtc: number | null. {#if hasAlert}: role="alert", aria-live="polite". Warning SVG icon (16×16 inline). Текст по alert_type: offline → "Принтер не в сети", error → "Ошибка принтера". Дата: lastSeenUtc → относительное время. Фон: color-mix(in srgb, var(--color-warning) 10%, transparent). Для error: border --color-destructive.

    PrinterListRow.svelte: Props: printer: PrinterDto, selected: boolean, onclick. Строка высота --row-height (40px). Колонки: имя устройства, IP или «USB», статус-badge (success/warning/destructive/default по status), alert-dot (8×8 --color-destructive) если has_alert, краткий тонер. aria-label="Есть проблема" на dot.

    PrintersList.svelte: аналог CartridgesList. Props: items: PrinterDto[], loading, selectedId, onSelect, emptyConfig. empty-config объект { heading, body, actionLabel, onAction }.

    PrintersSearchAndTabs.svelte: Props: filter, onFilterChange, onDiscoveryClick, identity. Switch-bar: null=«Все», online=«В сети», warning=«Предупреждение», error=«Ошибка», offline=«Не в сети». Поиск debounce 250ms. Кнопка «Найти принтеры» (primary) — visible только если identity.role === 'admin'. role="tablist"/"tab", aria-selected паттерн.

    PrintersMasterDetail.svelte: Идентичный CartridgesMasterDetail — 35/65 grid, {#snippet master} + {#snippet detail}.

    PrintersPage.svelte: аналог CartridgesPage. $state items, selectedId, selectedPrinter, listLoading, detailLoading, discoveryOpen. $effect filter → refresh. $effect selectedId → load detail. onMount loadAll. WS integration: onMount → connectWs() + onWsEvent(handleWsEvent). handleWsEvent: printer_alert → toast warning + refresh. Кнопка «Найти принтеры» управляет discoveryOpen. Рендерит PrintersMasterDetail + PrinterDetail (Task 2b) + DiscoveryModal (Task 2b).
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm svelte-check зелёный; TonerGauge содержит role="progressbar"; PrinterAlertBanner содержит role="alert"; PrintersPage рендерит PrintersMasterDetail и управляет discoveryOpen.
  </done>
</task>

<task type="auto">
  <name>Task 2b: Сложные UI компоненты принтеров (Detail, Discovery) + page routing</name>
  <files>
    ui/src/features/printers/PrinterDetail.svelte,
    ui/src/features/printers/DiscoveryModal.svelte,
    ui/src/features/printers/DiscoveryResultsTable.svelte,
    ui/src/pages/PrintersPage.svelte
  </files>
  <read_first>
    - ui/src/features/cartridges/CartridgeDetail.svelte (карточка с историей — образец для секций)
    - ui/src/features/cartridges/OperationModal.svelte (modal form паттерн для DiscoveryModal)
    - ui/src/features/cartridges/LowStockBanner.svelte (для понимания паттерна alert в detail)
    - ui/src/features/layout/auth-store.svelte.ts (identity.role)
    - .planning/phases/06-snmp/06-UI-SPEC.md §PrinterDetail, §DiscoveryModal, §DiscoveryResultsTable
    - .planning/phases/06-snmp/06-PATTERNS.md §PrinterDetail, §DiscoveryModal
  </read_first>
  <action>
    PrinterDetail.svelte: Props: printer: PrinterDto | null, loading: boolean, onRefresh: () => void.

    Header: имя (font-size-display, semibold), статус-badge, кнопка «Обновить сейчас» (sm, primary, loading во время onRefresh). PrinterAlertBanner если has_alert.

    Секция «Уровни тонера/чернил»: если toner_levels (serde_json::Value из DTO) → распарсить JSON (Object с ключами = метка цвета, значения = number|null) → для каждого ключа → строка с TonerGauge. tabular-nums.

    Секция «Страничные счётчики»: «Всего напечатано: {page_count}» (tabular-nums). null → «Данные недоступны».

    Секция «Установленный картридж» (PRN-07, per D-PRN07-01): читает printer.currentCartridgeId (number | null). Если null → «Картридж не закреплён» (muted text). Если number → показать id как «Картридж #{id}» или вызвать отдельный api-вызов для получения имени (если api.ts имеет cartridges.get — вызвать; если нет — показать id, не изобретать дополнительный endpoint). Данные backing: currentCartridgeId заполнен сервером через current_cartridge_for_printer (Plan 02) и передан в DTO.

    Секция «История статусов» (PRN-05): отдельный вызов printers.getReadings(id) → список последних readings. Формат строки: {дата} — {status}. Добавить getReadings в api.ts (команда printers_get_readings или как часть get — по аналогу с существующими).

    Метаданные: IP (или «USB»), vendor/модель, last_seen (relative) — muted.

    DiscoveryModal.svelte: Modal size="wide" (960px). Поля: ipStart, ipEnd (Input, placeholder «Например: 192.168.1.1»), community (Input, placeholder «public»). Кнопка «Начать поиск» (primary). scanning state → Spinner + «Поиск принтеров… найдено: {N}». discovered state: DiscoveredPrinterDto[]. selected: Set<number> (по индексу). CTA «Завести выбранные ({N})» disabled при 0 выбранных. Успех → toast 'Заведено принтеров: {N}' + onSuccess() + close. Reset на open. handleScan() вызывает printers.discover(ipStart, ipEnd, community). handleCreate() вызывает printers.admit(selectedIps, community).

    DiscoveryResultsTable.svelte: Props: items: DiscoveredPrinterDto[], selected: Set<number>, onToggle. Колонки: чекбокс / IP / Производитель / Модель / Имя / Статус. Дубликаты (is_duplicate=true) → Badge «Уже заведён» (default), чекбокс disabled. Header-чекбокс «выбрать все не-дубликаты».

    ui/src/pages/PrintersPage.svelte: заменить placeholder на import + рендер features/printers/PrintersPage.svelte. Убрать Placeholder.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm svelte-check зелёный; PrinterDetail содержит секцию «Установленный картридж» с currentCartridgeId; DiscoveryModal рендерит DiscoveryResultsTable; ui/src/pages/PrintersPage.svelte не содержит Placeholder.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser JavaScript → /api/v1/ws | WS upgrade — auth только server-side (Plan 03 уже закрыл) |
| UI role-based rendering | Кнопка «Найти принтеры» скрыта для не-admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13-E | Elevation of Privilege | Кнопка «Найти принтеры» в UI | accept | UI скрывает для не-admin; реальная защита — в authorize() на бэкенде (D-RBAC-01); UI-гейт — только UX-слой |
| T-06-14-I | Information Disclosure | WS reconnect toast | accept | Toast «Соединение с сервером потеряно» не раскрывает данных; info уровень; пользователь уже аутентифицирован |
| T-06-SC | Tampering | Новые npm зависимости | accept | Phase 6 не вводит новых npm пакетов (WebSocket — нативный API, Tauri events — @tauri-apps/api уже в workspace) |
</threat_model>

<verification>
1. pnpm --dir ui svelte-check — зелёный
2. grep -r "role=\"progressbar\"" ui/src/features/printers/TonerGauge.svelte — найден
3. grep -r "role=\"alert\"" ui/src/features/printers/PrinterAlertBanner.svelte — найден
4. grep "Placeholder" ui/src/pages/PrintersPage.svelte — НЕ найден (заменён)
5. grep "connectWs" ui/src/lib/api/ws.ts — найден
6. grep "currentCartridgeId" ui/src/features/printers/PrinterDetail.svelte — найден (PRN-07)
7. grep "request_status_changed" ui/src/lib/api/ws.ts — найден (не request_updated)
</verification>

<success_criteria>
- Раздел «Принтеры» полностью рендерится (master-detail, search, switch-bar, discovery modal)
- TonerGauge: progressbar с color coding (accent/warning/destructive/muted для unknown)
- PrinterAlertBanner: role="alert" + паттерн LowStockBanner
- PrinterDetail секция «Установленный картридж» читает currentCartridgeId из PrinterDto (PRN-07 backed by FK)
- DiscoveryModal: 2-step flow (scan → review → admit)
- ws.ts: dual-transport (WS браузер + Tauri events десктоп) + exponential backoff reconnect
- WsEvent variant 'request_status_changed' (не 'request_updated') — синхронизировано с Rust
- pnpm svelte-check зелёный
</success_criteria>

<output>
Создать `.planning/phases/06-snmp/06-04-SUMMARY.md` после завершения.
</output>
