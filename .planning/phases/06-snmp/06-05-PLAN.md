---
phase: 06-snmp
plan: 05
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - ui/src/features/requests/api.ts
  - ui/src/features/requests/RequestsPage.svelte
  - ui/src/features/requests/RequestsSearchAndTabs.svelte
  - ui/src/features/requests/RequestsMasterDetail.svelte
  - ui/src/features/requests/RequestsList.svelte
  - ui/src/features/requests/RequestListRow.svelte
  - ui/src/features/requests/RequestDetail.svelte
  - ui/src/features/requests/RequestFormModal.svelte
  - ui/src/features/cartridges/OperationModal.svelte
  - ui/src/pages/RequestsPage.svelte
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-07

must_haves:
  truths:
    - "Сотрудник может создать заявку через браузер: тип «Замена картриджа» (принтер обязателен) или «Свободная форма» (описание обязательно)"
    - "Специалист/Администратор видит все заявки; Сотрудник — только свои (роль-зависимый список)"
    - "Switch-bar: Все / Созданные / В работе / Выполненные / Отклонённые"
    - "Кнопки lifecycle в RequestDetail: open→Принять в работу/Отклонить; in_progress→Выполнить или «Установить картридж» (для cartridge_replace)"
    - "«Установить картридж» открывает OperationModal pre-filled принтером (REQ-05, D-Req-CART07-01) — preFillPrinterId prop добавлен в OperationModal в этом плане"
    - "История заявки (REQ-07): секция «История» в RequestDetail из audit_log"
    - "WS push: специалист/admin получает toast о новой заявке через connectWs/onWsEvent"
    - "pnpm svelte-check — зелёный (включая корректный preFillPrinterId prop в OperationModal)"
  artifacts:
    - path: "ui/src/features/cartridges/OperationModal.svelte"
      provides: "OperationModal с preFillPrinterId?: number prop (добавлен в Task 1)"
      contains: "preFillPrinterId"
    - path: "ui/src/features/requests/RequestFormModal.svelte"
      provides: "Modal создания заявки с тип-переключателем и условными полями"
      contains: "Замена картриджа"
    - path: "ui/src/features/requests/RequestDetail.svelte"
      provides: "Карточка заявки с lifecycle кнопками + история + OperationModal интеграция"
      contains: "Принять в работу"
    - path: "ui/src/features/requests/RequestsPage.svelte"
      provides: "Корневой компонент с роль-зависимой логикой"
      contains: "RequestsMasterDetail"
  key_links:
    - from: "ui/src/features/requests/RequestDetail.svelte"
      to: "ui/src/features/cartridges/OperationModal.svelte"
      via: "«Установить картридж» → OperationModal op='install' preFillPrinterId={request.printerDeviceId}"
      pattern: "preFillPrinterId"
    - from: "ui/src/features/requests/RequestsPage.svelte"
      to: "ui/src/lib/api/ws.ts"
      via: "onWsEvent(NewRequest) → toast + refresh"
      pattern: "onWsEvent"
---

<objective>
Вертикальный срез UI заявок: заменить placeholder RequestsPage полноценным порталом. Сотрудник создаёт заявки через браузер; специалист управляет lifecycle; REQ-05 «Установить картридж» открывает OperationModal.

ВАЖНО: preFillPrinterId prop добавляется в OperationModal.svelte в Task 1 ЭТОГО плана (не в 06-06). Это нужно чтобы RequestDetail.svelte в Task 2 мог использовать prop без ошибки svelte-check.

Purpose: Сотрудник через LAN-браузер может отправить заявку «одной кнопкой» (core value). Специалист получает real-time уведомление через WS.
Output: Полноценный портал заявок; dual-transport (браузер + Tauri); WS push интеграция.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-snmp/06-UI-SPEC.md
@.planning/phases/06-snmp/06-CONTEXT.md
@.planning/phases/06-snmp/06-PATTERNS.md
@.planning/phases/06-snmp/06-03-SUMMARY.md

<interfaces>
<!-- Точные паттерны и carry-forward из Phase 4/5 -->

From ui/src/features/cartridges/OperationModal.svelte (ТОЧНЫЙ файл для модификации в Task 1):
  // ТЕКУЩИЕ Props (из Phase 4):
  Props: open: boolean, op: 'install' | 'return' | 'refill' | 'retrieve' | 'writeoff',
         cartridgeId?: number, preFillModelId?: number,
         onClose: () => void, onSuccess: (result: CartridgeTransitionResult) => void
  // Task 1 ДОБАВЛЯЕТ: preFillPrinterId?: number
  // При op='install' и preFillPrinterId: передать в форму установки как начальное значение для поля «Принтер»

From ui/src/features/layout/auth-store.svelte.ts:
  export const identity = $state<Identity | null>(null);
  // identity.role: 'admin' | 'specialist' | 'employee'

From ui/src/features/cartridges/CartridgesPage.svelte (образец page с WS):
  onMount(async () => { await loadAll(); const unlisten = connectWs(); onDestroy(unlisten); });
  // onWsEvent → refresh списка

From ui/src/lib/api/ws.ts (из Plan 04 — WsEvent variant names):
  // type 'new_request' → NewRequest (snake_case из Rust tag)
  // type 'request_status_changed' → RequestStatusChanged

UI-SPEC §RequestFormModal (размер md=640px, тип-переключатель, условные поля):
  Замена картриджа: Select принтер (devices type=Принтер) обязательный + Textarea комментарий опц.
  Свободная форма: Select категория опц. + Textarea описание обязательный.
  CTA «Отправить заявку» (primary, loading). Validation перед submit.

UI-SPEC §RequestDetail lifecycle кнопки:
  open: «Принять в работу» (primary) + «Отклонить» (destructive → confirm modal)
  in_progress (free_form): «Выполнить» (primary) + «Отклонить» (destructive)
  in_progress (cartridge_replace): «Установить картридж» (primary, opens OperationModal) + «Отклонить»
  completed/rejected: no buttons, show resolution_notes

UI-SPEC §8 History (REQ-07): секция «История» → audit_log записи о смене статуса
  Формат: {дата/время} — {статус}; {кто}; {комментарий}
  Пусто: «История пуста»

UI-SPEC §Заявки switch-bar:
  null=«Все», open=«Созданные», in_progress=«В работе», completed=«Выполненные», rejected=«Отклонённые»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OperationModal preFillPrinterId prop + api.ts заявок + RequestFormModal + базовый RequestsPage</name>
  <files>
    ui/src/features/cartridges/OperationModal.svelte,
    ui/src/features/requests/api.ts,
    ui/src/features/requests/RequestFormModal.svelte,
    ui/src/features/requests/RequestsPage.svelte,
    ui/src/features/requests/RequestsSearchAndTabs.svelte,
    ui/src/features/requests/RequestsMasterDetail.svelte,
    ui/src/features/requests/RequestsList.svelte,
    ui/src/features/requests/RequestListRow.svelte,
    ui/src/pages/RequestsPage.svelte
  </files>
  <read_first>
    - ui/src/features/cartridges/OperationModal.svelte (ПРОЧИТАТЬ ПОЛНОСТЬЮ — нужно добавить preFillPrinterId?: number в Props и передать в форму install без нарушения существующей логики)
    - ui/src/features/cartridges/api.ts (ТОЧНЫЙ образец dual-transport обёртки)
    - ui/src/features/cartridges/CartridgesPage.svelte (Svelte 5 runes page паттерн)
    - ui/src/features/cartridges/CartridgesMasterDetail.svelte (master-detail grid)
    - ui/src/features/cartridges/CartridgesList.svelte + CartridgeListRow.svelte (список + строка)
    - ui/src/features/cartridges/CartridgesSearchAndTabs.svelte (switch-bar + toolbar)
    - ui/src/features/cartridges/CartridgeFormModal.svelte (form modal паттерн)
    - ui/src/features/layout/auth-store.svelte.ts (identity для role-based rendering)
    - .planning/phases/06-snmp/06-UI-SPEC.md §RequestFormModal, §switch-bar заявок, §пустые состояния заявок
    - .planning/phases/06-snmp/06-PATTERNS.md §api.ts requests, §RequestFormModal.svelte, §RequestsPage.svelte
  </read_first>
  <action>
    OperationModal.svelte — ДОБАВИТЬ preFillPrinterId prop (ПЕРВЫЙ ПРИОРИТЕТ, всё остальное зависит от него):
    В блок Props/interface добавить: preFillPrinterId?: number.
    При op='install': если preFillPrinterId установлен → использовать как начальное значение для поля «Принтер» (set selectedPrinterId = preFillPrinterId в $effect или onMount при открытии modal). НЕ менять логику других op типов (return/refill/retrieve/writeoff). НЕ делать prop обязательным.

    requests/api.ts: export const requests = { list, get, create, transition, listCategories }.
    Команды: requests_list, requests_get, requests_create, requests_transition, requests_list_categories.
    Типы из bindings: RequestDto, RequestFilter, RequestListResponse, RequestCreateDto, RequestTransitionPayload, Pagination.
    list принимает (filter: RequestFilter, pagination: Pagination).
    create принимает RequestCreateDto.
    transition принимает RequestTransitionPayload.

    RequestFormModal.svelte: Props: open boolean, onClose: () => void, onSuccess: () => void.
    Форма: let requestType = $state<'cartridge_replace' | 'free_form'>('cartridge_replace').
    Radio/segmented toggle (два варианта).
    {#if requestType === 'cartridge_replace'}: Select принтер (загружается из printers.list при открытии), required. Textarea комментарий (опц.).
    {#if requestType === 'free_form'}: Select категория (загружается из requests.listCategories(), первый вариант «Без категории», опц.). Textarea описание (required).
    Валидация на submit. handleSubmit: requests.create({request_type, printer_device_id, category_id, description}) → toast success «Заявка отправлена» → onSuccess() → close.

    RequestListRow.svelte: Props: request: RequestDto, selected boolean, onclick. Строка height --row-height. Тип (badge), краткое описание (truncated), статус-badge, автор ФИО, дата (relative).

    RequestsList.svelte: аналог CartridgesList. Props: items RequestDto[], loading, selectedId, onSelect, emptyConfig.

    RequestsSearchAndTabs.svelte: Props: filter, onFilterChange, onCreateClick, identity. Switch-bar: null=«Все», open=«Созданные», in_progress=«В работе», completed=«Выполненные», rejected=«Отклонённые». Кнопка «Создать заявку» (primary, всегда видна).

    RequestsMasterDetail.svelte: идентичен PrintersMasterDetail (35/65 grid).

    RequestsPage.svelte: аналог CartridgesPage. Роль-зависимая логика. $state items, selectedId, selectedRequest, loading. $effect filter → refresh. $effect selectedId → load detail. onMount → loadAll + connectWs() + onWsEvent(handleWsEvent). handleWsEvent: new_request → pushToast('info', 'Новая заявка') + refresh (только если specialist/admin).

    ui/src/pages/RequestsPage.svelte: заменить Placeholder на import/рендер features/requests/RequestsPage.svelte.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm svelte-check зелёный; grep "preFillPrinterId" ui/src/features/cartridges/OperationModal.svelte — найден; RequestFormModal рендерится с тип-переключателем; RequestsPage не содержит Placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 2: RequestDetail с lifecycle кнопками + REQ-05 OperationModal интеграция</name>
  <files>
    ui/src/features/requests/RequestDetail.svelte
  </files>
  <read_first>
    - ui/src/features/cartridges/CartridgeDetail.svelte (карточка с историей — образец для секции истории)
    - ui/src/features/cartridges/OperationModal.svelte (после Task 1 — с preFillPrinterId prop)
    - ui/src/features/layout/auth-store.svelte.ts (identity.role)
    - .planning/phases/06-snmp/06-UI-SPEC.md §8 RequestDetail, §9 REQ-05, §деструктивные подтверждения
    - .planning/phases/06-snmp/06-CONTEXT.md §D-Req-CART07-01, §D-Req-Lifecycle-01
  </read_first>
  <action>
    RequestDetail.svelte: Props: request: RequestDto | null, loading boolean, identity: Identity | null, onTransition: () => void.

    Header: тип-badge (Замена картриджа / Свободная форма), статус-badge, автор (ФИО), дата создания.

    Поля по типу:
    - cartridge_replace: Принтер (printer_name), комментарий (description), модель картриджа если задана.
    - free_form: Категория (если category_id), Описание.

    Кнопки lifecycle (только specialist/admin: identity.role !== 'employee'):
    - status='open': «Принять в работу» (primary, → transition Accept) + «Отклонить» (destructive, → confirm modal)
    - status='in_progress' && type='free_form': «Выполнить» (primary, → опц. комментарий → transition Complete) + «Отклонить» (destructive)
    - status='in_progress' && type='cartridge_replace': «Установить картридж» (primary, → operationModalOpen=true) + «Отклонить» (destructive)
    - status='completed'/'rejected': без кнопок; показать resolution_notes.

    Confirm modal для «Отклонить»: Modal с Textarea «Комментарий специалиста» (опц.) + кнопка «Отклонить» destructive. После confirm → requests.transition({ op: 'reject', request_id, version, notes }) → toast + onTransition().

    «Выполнить» (free_form): inline textarea для комментария резолюции (опц.) → requests.transition({ op: 'complete', request_id, version, notes }) → toast «Заявка выполнена» + onTransition().

    REQ-05 «Установить картридж» (cartridge_replace, in_progress):
    - let operationModalOpen = $state(false).
    - Кнопка «Установить картридж» → operationModalOpen = true.
    - <OperationModal open={operationModalOpen} op="install" preFillPrinterId={request.printerDeviceId} onClose={() => operationModalOpen=false} onSuccess={handleInstallSuccess} />.
      ПРИМЕЧАНИЕ: preFillPrinterId prop уже добавлен в OperationModal в Task 1 — svelte-check пройдёт без ошибок.
    - handleInstallSuccess: requests.transition({ op: 'complete', request_id: request.id, version: request.version, linked_cartridge_id: installedCartridgeId }) → toast «Заявка выполнена» + onTransition() + operationModalOpen=false.

    Секция «История» (REQ-07): Вызов requests.getHistory(id) (добавить в api.ts если нет, команда requests_get_history) или использовать поле RequestDto.historyEntries если добавлено в DTO. Показывать хронологически (новые сверху). Формат: {дата/время} — {статус}; {ФИО кто}; {комментарий}. Пусто → «История пуста».

    ARIA: lifecycle кнопки с aria-label; confirm-modal с aria-modal=true.
  </action>
  <verify>
    <automated>pnpm --dir ui svelte-check 2>&1 | tail -20</automated>
  </verify>
  <done>
    pnpm svelte-check зелёный; RequestDetail содержит OperationModal import с preFillPrinterId prop; кнопки lifecycle роль-зависимы (specialist/admin only); секция «История» присутствует; «Установить картридж» открывает OperationModal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1/requests_create | Сотрудник в браузере (LAN) — данные заявки через axum |
| UI role-based rendering | Кнопки lifecycle видны только specialist/admin в UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-15-E | Elevation of Privilege | requests_transition от employee в UI | accept | UI скрывает кнопки lifecycle для employee; реальная защита — authorize() в build_requests_transition; employee → 403 на HTTP (Plan 03) |
| T-06-16-I | Information Disclosure | RequestDetail показывает чужие заявки employee | mitigate | requests.list передаёт filter; бэкенд RequestService фильтрует по requested_by_user_id если role=employee (D-Req-Lifecycle-01); UI только рендерит то, что вернул бэкенд |
| T-06-17-T | Tampering | REQ-05 OperationModal линк заявки | mitigate | linked_cartridge_id передаётся в RequestTransitionPayload.Complete и записывается в requests.completed_cartridge_id; transition_in_tx проверяет version (optimistic lock) |
</threat_model>

<verification>
1. pnpm --dir ui svelte-check — зелёный
2. grep "preFillPrinterId" ui/src/features/cartridges/OperationModal.svelte — найден (добавлен в Task 1)
3. grep "preFillPrinterId" ui/src/features/requests/RequestDetail.svelte — найден (используется в Task 2)
4. grep "OperationModal" ui/src/features/requests/RequestDetail.svelte — найден
5. grep "role.*employee" ui/src/features/requests/RequestDetail.svelte — role check присутствует
6. grep "История" ui/src/features/requests/RequestDetail.svelte — секция истории присутствует
7. grep "Placeholder" ui/src/pages/RequestsPage.svelte — НЕ найден
</verification>

<success_criteria>
- OperationModal.svelte: preFillPrinterId?: number prop добавлен (первый приоритет Task 1)
- RequestFormModal: тип-переключатель; Замена картриджа → принтер dropdown; Свободная форма → категория + описание; валидация
- RequestDetail: lifecycle кнопки по role+status; «Установить картридж» → OperationModal preFillPrinterId pre-filled; секция «История»
- RequestsPage: роль-зависимая логика; WS push через onWsEvent (new_request → toast)
- ui/src/pages/RequestsPage.svelte: Placeholder удалён
- pnpm svelte-check зелёный
</success_criteria>

<output>
Создать `.planning/phases/06-snmp/06-05-SUMMARY.md` после завершения.
</output>
