---
phase: 06-snmp
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/trackly-app/tests/phase06_stubs.rs
  - crates/trackly-infra/tests/phase06_stubs.rs
autonomous: true
requirements:
  - PRN-01
  - PRN-02
  - PRN-03
  - PRN-04
  - PRN-05
  - PRN-06
  - PRN-07
  - PRN-08
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-05
  - REQ-07

must_haves:
  truths:
    - "cargo test --workspace компилируется с новыми stub-тестовыми файлами (все тесты помечены #[ignore])"
    - "Все тест-имена из VALIDATION.md существуют в файлах: тест-функция объявлена и скомпилирована"
  artifacts:
    - path: "crates/trackly-app/tests/phase06_stubs.rs"
      provides: "Stub-тесты для всех Phase 6 поведений из trackly-app"
      contains: "test_vendor_identify"
    - path: "crates/trackly-infra/tests/phase06_stubs.rs"
      provides: "Stub-тесты trackly-infra (mock SNMP + DB)"
      contains: "test_mock_snmp"
  key_links:
    - from: "crates/trackly-app/tests/phase06_stubs.rs"
      to: "cargo test"
      via: "все тесты #[ignore] — компилируются, не блокируют suite"
      pattern: "#\\[ignore\\]"
---

<objective>
Создать Wave 0 — скаффолд всех тестовых файлов и stub-функций для Phase 6, чтобы VALIDATION.md стал nyquist_compliant: true. Все тесты объявлены как #[ignore] (заглушки), что гарантирует компиляцию без выполнения.

Purpose: Nyquist-правило требует наличия автоматизированного теста для каждого поведения ДО начала реализации. Wave 0 создаёт именованные заглушки, которые волны 1–5 заменяют реальными реализациями.
Output: Два тестовых файла с 15 именованными #[ignore]-тестами. cargo test --workspace компилируется.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-snmp/06-VALIDATION.md
@.planning/phases/06-snmp/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать stub-тестовые файлы Phase 6</name>
  <files>
    crates/trackly-app/tests/phase06_stubs.rs,
    crates/trackly-infra/tests/phase06_stubs.rs
  </files>
  <read_first>
    - crates/trackly-app/Cargo.toml (понять структуру тестовой конфигурации)
    - crates/trackly-infra/Cargo.toml (аналогично)
    - .planning/phases/06-snmp/06-VALIDATION.md (все тест-имена, которые нужно создать)
  </read_first>
  <action>
    Создать два тестовых файла со stub-функциями. Каждая функция: #[test] + #[ignore] + тело panic!("stub: implement in wave N") или просто пустое тело (оба варианта compile-safe).

    crates/trackly-app/tests/phase06_stubs.rs — тесты уровня trackly-app:
      #[test] #[ignore] fn test_vendor_identify() {}
        // PRN-01: parse sysObjectID → vendor + oid_profile match
      #[test] #[ignore] fn test_toner_percent() {}
        // PRN-02: parse_toner_level(45, 100, "level_over_max") = Some(45); (-2,-2) = None
      #[test] #[ignore] fn test_oid_profiles_seeded() {}
        // PRN-03: SELECT COUNT(*) FROM oid_profiles = 5
      #[test] #[ignore] fn test_printer_usb_only() {}
        // PRN-04: printer with usb_host_device_id set + NULL ip_address — созданный, get возвращает usb_host_device_id
      #[test] #[ignore] fn test_alert_detection() {}
        // PRN-06: status='error'|'offline' → upsert printer_alerts (dedup — второй upsert не дублирует)
      #[test] #[ignore] fn test_request_create() {}
        // REQ-01: RequestService::create записывает строку в requests + audit_log
      #[test] #[ignore] fn test_request_lifecycle() {}
        // REQ-03: Accept/Reject/Complete переходы; недопустимый → AppError::Validation
      #[test] #[ignore] fn test_ws_event_sent() {}
        // REQ-04: broadcast::Sender получает WsEvent::NewRequest после create
      #[test] #[ignore] fn test_req_cart_link() {}
        // REQ-05: Complete{linked_cartridge_id} записывает completed_cartridge_id + статус→completed
      #[test] #[ignore] fn test_snmp_mock_switch() {}
        // D-Mock-01: AppCtx с TRACKLY_SNMP_MOCK=1 → MockSnmpClient (тест в trackly-app т.к. switch в AppCtx::build)
      #[test] #[ignore] fn test_readings_prune() {}
        // D-Retention-01: prune_old_readings_in_tx удаляет строки старше retention_cutoff
      #[test] #[ignore] fn test_current_cartridge_for_printer() {}
        // PRN-07: install картриджа с printer_device_id → current_cartridge_for_printer(printer_id) = Some(cartridge_id)
      #[test] #[ignore] fn test_ws_unauth_401() {}
        // ASVS V4: HTTP GET /api/v1/ws без session cookie → 401 (до upgrade)

    crates/trackly-infra/tests/phase06_stubs.rs — тесты уровня trackly-infra:
      #[test] #[ignore] fn test_mock_snmp() {}
        // PRN-08: MockSnmpClient::default_fixtures() get_oids → детерминированные OidValue

    Дополнительно убедиться что оба файла подключены к соответствующим Cargo.toml (если требуется явное объявление [[test]] — добавить; если integration tests автообнаруживаются из tests/ — ничего не нужно, проверить по образцу существующих тестов).

    КРИТИЧНО: не использовать ни один тип из ещё не существующих модулей — все тела должны быть {}. Запрещено импортировать PrinterService, MockSnmpClient и т.д. в этом файле — они создаются в следующих волнах. Файл должен компилироваться из голого Rust без зависимостей от Phase 6 кода.
  </action>
  <verify>
    <automated>cargo test --workspace -- --include-ignored 2>&1 | grep -E "(phase06|FAILED|error\[)" | head -20; cargo check --workspace 2>&1 | grep "^error" | head -10</automated>
  </verify>
  <done>
    cargo check --workspace зелёный; все 14 тест-функций существуют (grep phase06_stubs); cargo test --workspace без panic (все #[ignore]).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs → CI | Stub тесты не выполняются в CI (только #[ignore] — безопасны) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-SC | Tampering | npm/pip/cargo installs | accept | Wave 0 не добавляет новых зависимостей — только .rs файлы |
</threat_model>

<verification>
1. cargo check --workspace — exit 0
2. grep -c "#\[test\]" crates/trackly-app/tests/phase06_stubs.rs — 13
3. grep -c "#\[test\]" crates/trackly-infra/tests/phase06_stubs.rs — 1
4. cargo test --workspace 2>&1 | grep "ignored" — показывает 14 игнорируемых тестов
</verification>

<success_criteria>
- 14 stub-тест-функций объявлены и скомпилированы
- cargo check --workspace — зелёный (Wave 0 не ломает существующие тесты)
- Все тест-имена совпадают с VALIDATION.md
- nyquist_compliant: true может быть выставлен в VALIDATION.md
</success_criteria>

<output>
Создать `.planning/phases/06-snmp/06-00-SUMMARY.md` после завершения.
</output>
