# Phase 03.3: Device-list UX Round 2 — Pattern Map

**Mapped:** 2026-06-07
**Files analyzed:** 11 (8 modified, 1 new test functions, 1 new HTTP route, 1 new HTTP payload)
**Analogs found:** 11 / 11

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `crates/trackly-core/src/domain/devices.rs` | model | transform | self (current state) | exact — модифицируем `DeviceFilter` + `DeviceGroupRow` |
| `crates/trackly-app/src/dto/device.rs` | model/DTO | transform | self (current state) | exact — модифицируем `DeviceFilter` DTO + `DeviceGroup` DTO |
| `crates/trackly-infra/src/repos/devices_sqlite.rs` | repository | CRUD | self (current state) | exact — условный SQL в `list_grouped` |
| `crates/trackly-app/src/services/device_service.rs` | service | CRUD | self (current state) | exact — маппинг domain_filter в `list_grouped` |
| `crates/trackly-app/src/http/devices.rs` | route/controller | request-response | self (current state) | exact — добавить `LocationsAutocompletePayload` + handler + router entry |
| `crates/trackly-app/tests/devices_grouping.rs` | test | CRUD | self (current state) | exact — новые тест-функции рядом с существующими |
| `ui/src/features/devices/DeviceList.svelte` | component | request-response | self (current state) | exact — `showStatus` prop, новая th, условные colspans |
| `ui/src/features/devices/DeviceListRow.svelte` | component | request-response | self (current state) | exact — `showStatus` prop, `title=`, новая td Состояние |
| `ui/src/features/devices/DeviceGroupRow.svelte` | component | request-response | self (current state) | exact — `showStatus` prop, `title=`, conditionDisplay, colspans |
| `ui/src/features/devices/DevicesPage.svelte` | component | request-response | self (current state) | exact — `group_by_condition: false`, `showStatus` pass-through |
| `ui/src/features/devices/DeviceAutocompleteField.svelte` | component | request-response | `ui/src/lib/components/LocationAutocomplete.svelte` | role-match — parallel fetch + secondary dropdown section |
| `ui/src/features/acts/ActFormItemsTable.svelte` | component | request-response | self (current state) | exact — добавить `group_by_condition: true` в существующий вызов |

---

## Pattern Assignments

### `crates/trackly-core/src/domain/devices.rs` — `DeviceFilter` + `DeviceGroupRow`

**Analog:** self  
**Current state (lines 45–55, 96–105):**

```rust
/// Filter parameters for device list/search queries.
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct DeviceFilter {
    pub type_id: Option<i64>,
    pub location_id: Option<i64>,
    pub status_id: Option<i64>,
    pub state: Option<String>,
    pub name_prefix: Option<String>,
    /// Whether to include soft-deleted devices. Defaults to false.
    pub include_deleted: bool,
    // ITEM-1: добавить сюда:
    // pub group_by_condition: bool,
}

/// A grouped device row for non-unique devices (D-Group-01).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct DeviceGroupRow {
    pub repr: DeviceRow,
    pub ids: Vec<i64>,
    pub count: i64,
    // ITEM-1: добавить сюда:
    // pub condition_distinct_count: i64,
}
```

**Что добавить:**
1. В `DeviceFilter`: `pub group_by_condition: bool,` — с `#[derive(Default)]` выставится в `false` автоматически (верное дефолтное поведение для DevicesPage).
2. В `DeviceGroupRow`: `pub condition_distinct_count: i64,` — для передачи на фронтенд через DTO.

---

### `crates/trackly-app/src/dto/device.rs` — `DeviceFilter` DTO + `DeviceGroup` DTO

**Analog:** self  
**Current state (lines 191–203, 236–242):**

```rust
/// Фильтр для списка устройств.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Type, Default)]
pub struct DeviceFilter {
    #[specta(type = Option<i32>)]
    pub type_id: Option<i64>,
    #[specta(type = Option<i32>)]
    pub location_id: Option<i64>,
    #[specta(type = Option<i32>)]
    pub status_id: Option<i64>,
    pub state: Option<String>,
    pub name_prefix: Option<String>,
    pub include_deleted: bool,
    // ITEM-1: добавить:
    // pub group_by_condition: bool,
}

/// Группа одинаковых устройств (DEV-11 / D-Group-01).
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Type)]
pub struct DeviceGroup {
    pub repr: DeviceDto,
    #[specta(type = u32)]
    pub count: u64,
    pub ids: Vec<i32>,
    // ITEM-1: добавить:
    // #[specta(type = i32)]
    // pub condition_distinct_count: i64,
}
```

**Паттерн derives:** `#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Type, Default)]` — все булевы поля без `#[specta(...)]` атрибута (bool уже TypeScript-совместим). Для `condition_distinct_count: i64` нужен `#[specta(type = i32)]` (паттерн уже применяется на `count: u64` → `#[specta(type = u32)]`).

**После правки:** запустить `pnpm run specta` для регенерации `ui/src/bindings.ts`.

---

### `crates/trackly-infra/src/repos/devices_sqlite.rs` — `list_grouped`

**Analog:** self  
**Current SQL (lines 934–966) — полный блок:**

```rust
let mut stmt = conn
    .prepare(
        "SELECT
           MIN(d.id)                       AS repr_id,
           COUNT(*)                        AS cnt,
           GROUP_CONCAT(d.id)              AS id_list,
           d.type_id, d.name,
           MAX(d.model)                    AS model,
           MAX(d.notes)                    AS notes,
           MAX(d.complectation)            AS complectation,
           d.condition                     AS condition,
           MAX(d.location_id)              AS location_id,
           MAX(d.status_id)                AS status_id,
           MAX(d.version)                  AS version,
           MAX(d.created_at_utc)           AS created_at_utc,
           MAX(d.updated_at_utc)           AS updated_at_utc,
           l.name                          AS location_name,
           MAX(d.inventory_number)         AS inv_no,
           MAX(d.serial_number)            AS serial_no
         FROM devices d
         LEFT JOIN locations l ON l.id = (
           SELECT MAX(d2.location_id)
           FROM devices d2
           WHERE d2.type_id = d.type_id
             AND d2.name = d.name
             AND d2.condition IS d.condition        -- убрать когда !group_by_condition
             AND d2.deleted_at_utc IS NULL
             AND (?1 IS NULL OR d2.status_id = ?1)
         )
         WHERE d.deleted_at_utc IS NULL
           AND (?1 IS NULL OR d.status_id = ?1)
         GROUP BY d.type_id, d.name, d.condition   -- условный
         ORDER BY d.name
         LIMIT ?2 OFFSET ?3",
    )
```

**Что изменить — паттерн Rust `if` с двумя SQL строками (Вариант A1 из RESEARCH.md):**

```rust
fn list_grouped(&self, conn, filter, page) -> ... {
    let sql_with_condition = "SELECT
       MIN(d.id) AS repr_id, COUNT(*) AS cnt, GROUP_CONCAT(d.id) AS id_list,
       d.type_id, d.name,
       MAX(d.model) AS model, MAX(d.notes) AS notes, MAX(d.complectation) AS complectation,
       d.condition AS condition,
       COUNT(DISTINCT d.condition) AS condition_distinct_count,  -- NEW
       MAX(d.location_id) AS location_id, MAX(d.status_id) AS status_id,
       MAX(d.version) AS version, MAX(d.created_at_utc) AS created_at_utc,
       MAX(d.updated_at_utc) AS updated_at_utc,
       l.name AS location_name, MAX(d.inventory_number) AS inv_no,
       MAX(d.serial_number) AS serial_no
     FROM devices d
     LEFT JOIN locations l ON l.id = (
       SELECT MAX(d2.location_id) FROM devices d2
       WHERE d2.type_id = d.type_id AND d2.name = d.name
         AND d2.condition IS d.condition          -- сохраняем когда group_by_condition=true
         AND d2.deleted_at_utc IS NULL
         AND (?1 IS NULL OR d2.status_id = ?1)
     )
     WHERE d.deleted_at_utc IS NULL
       AND (?1 IS NULL OR d.status_id = ?1)
     GROUP BY d.type_id, d.name, d.condition
     ORDER BY d.name LIMIT ?2 OFFSET ?3";

    let sql_without_condition = "SELECT
       MIN(d.id) AS repr_id, COUNT(*) AS cnt, GROUP_CONCAT(d.id) AS id_list,
       d.type_id, d.name,
       MAX(d.model) AS model, MAX(d.notes) AS notes, MAX(d.complectation) AS complectation,
       MAX(d.condition) AS condition,               -- MAX вместо d.condition
       COUNT(DISTINCT d.condition) AS condition_distinct_count,  -- NEW
       MAX(d.location_id) AS location_id, MAX(d.status_id) AS status_id,
       MAX(d.version) AS version, MAX(d.created_at_utc) AS created_at_utc,
       MAX(d.updated_at_utc) AS updated_at_utc,
       l.name AS location_name, MAX(d.inventory_number) AS inv_no,
       MAX(d.serial_number) AS serial_no
     FROM devices d
     LEFT JOIN locations l ON l.id = (
       SELECT MAX(d2.location_id) FROM devices d2
       WHERE d2.type_id = d.type_id AND d2.name = d.name
         -- убрать AND d2.condition IS d.condition (нет разбивки по condition)
         AND d2.deleted_at_utc IS NULL
         AND (?1 IS NULL OR d2.status_id = ?1)
     )
     WHERE d.deleted_at_utc IS NULL
       AND (?1 IS NULL OR d.status_id = ?1)
     GROUP BY d.type_id, d.name                    -- без condition
     ORDER BY d.name LIMIT ?2 OFFSET ?3";

    let sql = if filter.group_by_condition { sql_with_condition } else { sql_without_condition };
    let mut stmt = conn.prepare(sql).map_err(map_rusqlite)?;
```

**Паттерн маппинга строк (lines 970–1011):** добавить извлечение `condition_distinct_count` как 9-й столбец (после `state: Option<String> = row.get(8)?`):
```rust
let condition_distinct_count: i64 = row.get(9)?;
// затем сдвинуть индексы всех следующих get(...) на +1
```

**Паттерн сборки `DeviceGroupRow` (lines 1065):** добавить `condition_distinct_count` в конструктор:
```rust
groups.push(DeviceGroupRow { repr, ids, count, condition_distinct_count });
```

---

### `crates/trackly-app/src/services/device_service.rs` — `list_grouped` маппинг

**Analog:** self  
**Current mapping (lines 506–513):**

```rust
let domain_filter = trackly_core::domain::devices::DeviceFilter {
    type_id: filter.type_id,
    location_id: filter.location_id,
    status_id: filter.status_id,
    state: filter.state,
    name_prefix: filter.name_prefix,
    include_deleted: filter.include_deleted,
    // ITEM-1: добавить:
    // group_by_condition: filter.group_by_condition,
};
```

**Current group construction (lines 528–535):**

```rust
let groups = group_rows
    .into_iter()
    .map(|g| DeviceGroup {
        repr: DeviceDto::from(g.repr),
        count: g.count as u64,
        ids: g.ids.into_iter().map(|id| id as i32).collect(),
        // ITEM-1: добавить:
        // condition_distinct_count: g.condition_distinct_count,
    })
    .collect();
```

---

### `crates/trackly-app/src/http/devices.rs` — новый HTTP route для `locations_autocomplete`

**Analog:** существующий `handler_list_by_ids` (lines 231–239) — ближайший по структуре (тоже simple payload → Vec):

```rust
// Паттерн простого POST handler (аналог для locations_autocomplete):
pub async fn handler_list_by_ids(
    State(ctx): State<AppCtx>,
    Json(payload): Json<ListByIdsPayload>,
) -> Result<Json<Vec<DeviceDto>>, AppErrorResponse> {
    Ok(Json(
        build_devices_list_by_ids(&ctx, payload.ids)
            .await
            .map_err(AppErrorResponse::from)?,
    ))
}
```

**Новый payload struct** (добавить рядом с существующими payload structs, lines ~80–84):

```rust
#[derive(serde::Deserialize)]
pub struct LocationsAutocompletePayload {
    pub prefix: String,
}
```

**Новый handler** (добавить перед функцией `router()`):

```rust
pub async fn handler_locations_autocomplete(
    State(ctx): State<AppCtx>,
    Json(payload): Json<LocationsAutocompletePayload>,
) -> Result<Json<Vec<String>>, AppErrorResponse> {
    Ok(Json(
        build_locations_autocomplete(&ctx, payload.prefix)
            .await
            .map_err(AppErrorResponse::from)?,
    ))
}
```

**Import:** добавить `build_locations_autocomplete` в import list (line 19):
```rust
use crate::tauri_cmds::devices::{
    ...,
    build_locations_autocomplete,  // ITEM-4: добавить
    ...
};
```

**Router entry** (lines 294–321, после `.route("/api/v1/devices_list_by_ids", post(handler_list_by_ids))`):
```rust
.route("/api/v1/locations_autocomplete", post(handler_locations_autocomplete))
```

---

### `crates/trackly-app/tests/devices_grouping.rs` — новые тест-функции

**Analog:** существующий `grouping_collapses_non_unique` (lines 45–74) — паттерн:

```rust
#[tokio::test(flavor = "multi_thread", worker_threads = 4)]
async fn grouping_collapses_non_unique() {
    tokio::time::timeout(Duration::from_secs(30), async {
        let (svc, _dir) = make_service();
        // setup: create devices...
        let filter = DeviceFilter::default();  // group_by_condition: false (default)
        let page = Pagination { offset: 0, limit: 50 };
        let groups = svc.list_grouped(filter, page).await.expect("list_grouped");
        assert_eq!(groups.len(), 1, "...");
        assert_eq!(groups[0].count, 5, "...");
    })
    .await
    .expect("... exceeded 30s");
}
```

**Паттерн helper** для устройства с condition:
```rust
fn device_with_condition(name: &str, condition: &str) -> DeviceNew {
    DeviceNew {
        type_id: 1,
        name: name.to_string(),
        inventory_no: None,
        serial_no: None,
        model: Some("Model X".to_string()),
        specs: None,
        kit: None,
        state: Some(condition.to_string()),
        location: None,
        location_id: None,
        status_id: 1,
    }
}
```

**Три новых теста** (по паттерну `grouping_collapses_non_unique`):

1. `grouping_page_mode_ignores_condition` — `group_by_condition: false` → 2 устройства с разным condition = 1 группа, `condition_distinct_count = 2`.
2. `grouping_act_form_keeps_condition_split` — `group_by_condition: true` → 2 устройства с разным condition = 2 группы.
3. `grouping_condition_distinct_count_mixed` — `group_by_condition: false` → смешанная группа → `condition_distinct_count > 1`.

**Существующие тесты, требующие обновления** (передать `group_by_condition: true` чтобы сохранить DEF-2B семантику):
- `grouping_collapses_non_unique` — `filter = DeviceFilter::default()` → оставить как есть (default=false, тест всё равно зелёный — однородные condition)
- Если есть `grouping_groups_devices_with_same_name_and_different_condition` или `condition_key_splits_groups` — обновить на `group_by_condition: true`

---

### `ui/src/features/devices/DeviceList.svelte` — ITEM-1 + ITEM-3

**Analog:** self  
**Current Props interface (lines 6–21):**

```ts
interface Props {
  items: DeviceDto[];
  groups: DeviceGroup[];
  total: number;
  loading: boolean;
  grouped: boolean;
  searchActive: boolean;
  expandedGroups?: Set<string>;
  onExpandToggle?: (_key: string, _expanded: boolean) => void;
  onEdit: (_d: DeviceDto) => void;
  onDelete: () => void;
  onPrintAcceptance?: (_d: DeviceDto) => void;
  // ITEM-3: добавить:
  // showStatus?: boolean;
}
```

**Current thead (lines 59–67 и 88–97) — оба блока меняются одинаково:**

```svelte
<!-- текущий thead -->
<th class="th th-name">Наименование</th>
<th class="th th-numeric">Инвентарный №</th>
<th class="th th-numeric">Серийный №</th>
<th class="th">Модель</th>
<th class="th">Расположение</th>
<th class="th th-status">Статус</th>          <!-- скрыть при !showStatus -->
<th class="th th-actions">Действия</th>

<!-- после правки: -->
<th class="th th-name">Наименование</th>
<th class="th th-numeric">Инвентарный №</th>
<th class="th th-numeric">Серийный №</th>
<th class="th">Модель</th>
<th class="th">Расположение</th>
<th class="th th-condition">Состояние</th>    <!-- ITEM-1: НОВАЯ -->
{#if showStatus}<th class="th th-status">Статус</th>{/if}  <!-- ITEM-3: условная -->
<th class="th th-actions">Действия</th>
```

**Current skeleton (line 72):**
```svelte
{#each { length: 7 } as _}
<!-- после правки: -->
{#each { length: showStatus ? 8 : 7 } as _}
```

**CSS для новой th (добавить рядом с `.th-status`, line 170–172):**
```scss
.th-condition {
  width: 120px;  // симметрично th-status
}
```

**Props destructuring (lines 22–35)** — добавить `showStatus = true` с дефолтом:
```svelte
const {
  ...,
  showStatus = true,
}: Props = $props();
```

**Pass-through в дочерние компоненты (lines 104–114):** добавить `{showStatus}` в `DeviceGroupRow` и `DeviceListRow`.

---

### `ui/src/features/devices/DeviceListRow.svelte` — ITEM-1 + ITEM-2 + ITEM-3

**Analog:** self  
**Current Props (lines 6–14):**

```ts
interface Props {
  device: DeviceDto;
  onEdit: (_d: DeviceDto) => void;
  onDelete: () => void;
  isLastInGroup?: boolean;
  onPrintAcceptance?: (_d: DeviceDto) => void;
  // ITEM-3: добавить:
  // showStatus?: boolean;
}
```

**Current template (lines 41–53):**

```svelte
<tr class="device-row" class:group-last-child={isLastInGroup}>
  <td class="cell cell-name">{device.name}</td>
  <td class="cell cell-numeric">{device.inventory_no ?? '—'}</td>
  <td class="cell cell-numeric">{device.serial_no ?? '—'}</td>
  <td class="cell">{device.model ?? '—'}</td>
  <td class="cell">{device.location ?? '—'}</td>
  <td class="cell cell-status">
    <Badge variant={statusVariant}>{statusLabel}</Badge>
  </td>
  <td class="cell cell-actions">
    <DeviceContextMenu {device} {onEdit} {onDelete} {onPrintAcceptance} />
  </td>
</tr>

<!-- после правки ITEM-1 + ITEM-2 + ITEM-3: -->
<tr class="device-row" class:group-last-child={isLastInGroup}>
  <td class="cell cell-name" title={device.name}>{device.name}</td>
  <td class="cell cell-numeric" title={device.inventory_no ?? ''}>{device.inventory_no ?? '—'}</td>
  <td class="cell cell-numeric" title={device.serial_no ?? ''}>{device.serial_no ?? '—'}</td>
  <td class="cell" title={device.model ?? ''}>{device.model ?? '—'}</td>
  <td class="cell" title={device.location ?? ''}>{device.location ?? '—'}</td>
  <td class="cell" title={device.state ?? ''}>{device.state ?? '—'}</td>  <!-- ITEM-1 NEW -->
  {#if showStatus}
  <td class="cell cell-status">
    <Badge variant={statusVariant}>{statusLabel}</Badge>
  </td>
  {/if}
  <td class="cell cell-actions">
    <DeviceContextMenu {device} {onEdit} {onDelete} {onPrintAcceptance} />
  </td>
</tr>
```

**Примечание ITEM-2:** `title=` на всех текстовых ячейках. Ячейки Badge и DeviceContextMenu — без `title`. CSS `.cell` уже имеет `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 0` (lines 70–80) — новая ячейка «Состояние» наследует автоматически.

---

### `ui/src/features/devices/DeviceGroupRow.svelte` — ITEM-1 + ITEM-2 + ITEM-3

**Analog:** self  
**Current Props (lines 29–39):**

```ts
interface Props {
  group: DeviceGroup;
  expandedGroups: Set<string>;
  onExpandToggle?: (_key: string, _expanded: boolean) => void;
  onEdit: (_d: DeviceDto) => void;
  onDelete: () => void;
  onPrintAcceptance?: (_d: DeviceDto) => void;
  // ITEM-3: добавить:
  // showStatus?: boolean;
}
```

**ITEM-1: новый $derived для conditionDisplay** (добавить после `statusVariant`, ~line 68):

```ts
// ITEM-1: «разное» для смешанной группы
const conditionDisplay = $derived(
  group.condition_distinct_count > 1 ? 'разное' : (group.repr.state ?? '—')
);
```

**Current collapsed row template (lines 130–169):**

```svelte
<tr class="group-row" onclick={toggleExpand}>
  <td class="cell cell-name-wide" colspan="4">  <!-- colspan="4" НЕ меняется -->
    ...{group.repr.name}
  </td>
  <td class="cell">{group.repr.location ?? '—'}</td>
  <td class="cell cell-status">
    <Badge variant={statusVariant}>{statusLabel}</Badge>
  </td>
  <td class="cell cell-actions cell-count">...</td>
</tr>

<!-- после правки ITEM-1 + ITEM-2 + ITEM-3: -->
<tr class="group-row" onclick={toggleExpand}>
  <td class="cell cell-name-wide" colspan="4" title={group.repr.name}>  <!-- ITEM-2 title -->
    ...{group.repr.name}
  </td>
  <td class="cell" title={group.repr.location ?? ''}>{group.repr.location ?? '—'}</td>  <!-- ITEM-2 title -->
  <td class="cell" title={conditionDisplay}>{conditionDisplay}</td>  <!-- ITEM-1 NEW -->
  {#if showStatus}
  <td class="cell cell-status">
    <Badge variant={statusVariant}>{statusLabel}</Badge>
  </td>
  {/if}
  <td class="cell cell-actions cell-count">...</td>
</tr>
```

**Current loading row (line 175):**

```svelte
<td colspan="7" class="children-loading">Загрузка…</td>
<!-- после правки ITEM-3: -->
<td colspan={showStatus ? 8 : 7} class="children-loading">Загрузка…</td>
```

**Pass-through `showStatus` в дочерние `DeviceListRow` (line 179):**
```svelte
<DeviceListRow device={child} onEdit={handleEdit} onDelete={handleDelete}
  isLastInGroup={i === children.length - 1} {onPrintAcceptance} {showStatus} />
```

**Colspan-карта (ITEM-1 + ITEM-3 совместно):**

| Место | Текущее | После правки |
|---|---|---|
| `DeviceGroupRow` collapsed `cell-name-wide` | `colspan="4"` | **не меняется — 4** |
| Loading row в `DeviceGroupRow` (line 175) | `colspan="7"` | `colspan={showStatus ? 8 : 7}` |
| Skeleton в `DeviceList` (line 72) | `{ length: 7 }` | `{ length: showStatus ? 8 : 7 }` |

---

### `ui/src/features/devices/DevicesPage.svelte` — ITEM-1 + ITEM-3

**Analog:** self  
**Current baseFilter (lines 48–55):**

```ts
const baseFilter = $derived<DeviceFilter>({
  type_id: 1,
  location_id: null,
  status_id: statusFilter,
  state: null,
  name_prefix: null,
  include_deleted: false,
  // ITEM-1: добавить:
  // group_by_condition: false,
});
```

**Current DeviceList usage (line ~75, внутри JSX/Svelte template):**
```svelte
<!-- ITEM-3: добавить showStatus prop -->
<DeviceList
  {items}
  {groups}
  {total}
  {loading}
  {grouped}
  {searchActive}
  {expandedGroups}
  {onExpandToggle}
  {onEdit}
  onDelete={handleDelete}
  {onPrintAcceptance}
  showStatus={statusFilter === null}
/>
```

---

### `ui/src/features/acts/ActFormItemsTable.svelte` — ITEM-1 preserve

**Analog:** self  
**Current listGrouped call (lines 101–112):**

```ts
const groups = await devices.listGrouped(
  {
    type_id: null,
    location_id: null,
    status_id: 1,
    state: null,
    name_prefix: v.trim(),
    include_deleted: false,
    // ITEM-1: добавить, чтобы сохранить DEF-2B:
    // group_by_condition: true,
  },
  { offset: 0, limit: 20 },
);
```

Это единственное изменение в файле — добавить `group_by_condition: true`.

---

### `ui/src/features/devices/DeviceAutocompleteField.svelte` — ITEM-4

**Analog:** `ui/src/lib/components/LocationAutocomplete.svelte`  
**LocationAutocomplete fetch pattern (lines 35–41):**

```ts
async function fetchSuggestions(prefix: string) {
  try {
    suggestions = await apiCall<string[]>('locations_autocomplete', { prefix });
  } catch {
    suggestions = [];
  }
}
```

**Import:** добавить `apiCall` в imports (строка 27 в DeviceAutocompleteField):
```ts
import { apiCall } from '$lib/api/client';
```

**Новый state** (добавить после `let suggestions = $state<string[]>([])`, line 61):
```ts
let allLocationSuggestions = $state<string[]>([]);  // ITEM-4
```

**Расширение `$effect` fetch** (добавить в debounceTimer setTimeout после строки 112 `suggestions = await devices.autocomplete(...)`):
```ts
// ITEM-4: параллельная загрузка всех локаций для field="location"
if (field === 'location') {
  const allLocs = await apiCall<string[]>('locations_autocomplete', { prefix: v });
  allLocationSuggestions = allLocs.filter(l => !suggestions.includes(l));
}
```

**Расширение `handleFocus`** (добавить аналогично после line 169 `suggestions = await devices.autocomplete(...)`):
```ts
if (field === 'location') {
  const allLocs = await apiCall<string[]>('locations_autocomplete', { prefix: value });
  allLocationSuggestions = allLocs.filter(l => !suggestions.includes(l));
}
```

**Keyboard navigation** — расширить `activeIndex` на объединённый список. Добавить `$derived` (рядом с другими derived, ~line 64):
```ts
// ITEM-4: объединённый список для навигации клавиатурой
const allItems = $derived(field === 'location'
  ? [...suggestions, ...allLocationSuggestions]
  : suggestions);
```

Заменить в `handleKeydown` `suggestions.length` на `allItems.length` и `suggestions[activeIndex]` на `allItems[activeIndex]` (lines 219–230).

**Template — вторая секция dropdown** (добавить после существующего `{#each suggestions as s, i (s)}...{/each}`, line 298–313):

```svelte
<!-- ITEM-4: вторая секция «Все расположения» — только для field="location" -->
{#if field === 'location' && allLocationSuggestions.length > 0}
  <header class="dropdown-header">Все расположения:</header>
  {#each allLocationSuggestions as s, i ('all_' + s)}
    <button
      type="button"
      id="autocomplete-item-{suggestions.length + i}"
      role="option"
      class="dropdown-item"
      class:active={suggestions.length + i === activeIndex}
      aria-selected={suggestions.length + i === activeIndex}
      onmousedown={(e) => {
        e.preventDefault();
        select(s);
      }}
    >
      {s}
    </button>
  {/each}
{/if}
```

**Паттерн dropdown-header CSS** уже существует в файле (lines 375–382) — новая секция использует тот же класс:

```scss
.dropdown-header {
  padding: var(--space-xs) var(--space-sm);
  font-size: var(--font-size-label);
  color: var(--color-text-secondary);
  border-bottom: 1px solid var(--color-border);
  background: var(--color-surface-sunken);
  font-style: italic;
}
```

---

## Shared Patterns

### Паттерн: условный prop с дефолтом `true` (ITEM-3)

**Source:** `DeviceListRow.svelte` — `isLastInGroup?: boolean = false`  
**Apply to:** `DeviceList`, `DeviceListRow`, `DeviceGroupRow` — все три получают `showStatus?: boolean = true`

```ts
// Паттерн из DeviceListRow (line 16):
const { ..., isLastInGroup = false, ... }: Props = $props();
// По аналогии:
const { ..., showStatus = true, ... }: Props = $props();
```

### Паттерн: DTO маппинг domain → DTO в сервисе

**Source:** `device_service.rs` `list_grouped` (lines 506–535)  
**Apply to:** при добавлении `condition_distinct_count` в domain/DTO — следовать существующему паттерну explicit field mapping без `..` spread operator.

```rust
// Паттерн:
let domain_filter = trackly_core::domain::devices::DeviceFilter {
    type_id: filter.type_id,
    // ... все поля явно
    include_deleted: filter.include_deleted,
    group_by_condition: filter.group_by_condition,  // новое поле
};
```

### Паттерн: `#[specta(type = i32)]` для i64 в DTO

**Source:** `dto/device.rs` (lines 33–55, например `#[specta(type = i32)] pub status_id: i64`)  
**Apply to:** `condition_distinct_count: i64` в `DeviceGroup` DTO нужен `#[specta(type = i32)]`

### Паттерн: HTTP handler через `build_*` helper

**Source:** `http/devices.rs` handler `handler_list_by_ids` (lines 231–239)  
**Apply to:** новый `handler_locations_autocomplete` — делегирует в `build_locations_autocomplete` из `tauri_cmds::devices`, никакой логики в handler.

### Паттерн: `apiCall` для Tauri-команды из Svelte

**Source:** `LocationAutocomplete.svelte` (line 37)  
**Apply to:** ITEM-4 fetch в `DeviceAutocompleteField` — `apiCall<string[]>('locations_autocomplete', { prefix })` работает одинаково в Tauri и browser-mode (абстракция транспорта).

---

## Нет аналога

Все изменения в этой фазе являются модификациями существующих файлов с четкими аналогами в самих этих файлах.

---

## Карта рисков (из RESEARCH.md — паттерны не повторять)

| Pitfall | Файл | Защита |
|---|---|---|
| `condition_distinct_count` не добавлен в SQL SELECT | `devices_sqlite.rs` | Добавить в **обе** SQL-ветки |
| `condition_distinct_count` не добавлен в `DeviceGroupRow` domain | `domain/devices.rs` | Синхронно с SQL |
| `condition_distinct_count` не добавлен в `DeviceGroup` DTO | `dto/device.rs` | Синхронно с domain |
| loading colspan остался 7 | `DeviceGroupRow.svelte` | `colspan={showStatus ? 8 : 7}` |
| skeleton `length: 7` | `DeviceList.svelte` | `length: showStatus ? 8 : 7` |
| `allItems` не используется в handleKeydown | `DeviceAutocompleteField.svelte` | Заменить все `suggestions.length` / `suggestions[activeIndex]` на `allItems.*` |
| `group_by_condition` не передан в `ActFormItemsTable` | `ActFormItemsTable.svelte` | Добавить `group_by_condition: true` в объект фильтра |

---

## Метаданные

**Scope поиска аналогов:**
- `crates/trackly-core/src/domain/devices.rs`
- `crates/trackly-app/src/dto/device.rs`
- `crates/trackly-infra/src/repos/devices_sqlite.rs` (lines 900–1069)
- `crates/trackly-app/src/services/device_service.rs` (lines 360–538)
- `crates/trackly-app/src/http/devices.rs` (lines 1–322)
- `crates/trackly-app/src/tauri_cmds/devices.rs` (lines 55–200)
- `crates/trackly-app/tests/devices_grouping.rs` (lines 1–220)
- `ui/src/features/devices/DeviceList.svelte`
- `ui/src/features/devices/DeviceListRow.svelte`
- `ui/src/features/devices/DeviceGroupRow.svelte`
- `ui/src/features/devices/DevicesPage.svelte` (lines 1–80)
- `ui/src/features/devices/DeviceAutocompleteField.svelte`
- `ui/src/lib/components/LocationAutocomplete.svelte`
- `ui/src/features/acts/ActFormItemsTable.svelte` (lines 95–125)

**Файлов прочитано:** 14  
**Дата маппинга:** 2026-06-07
