---
phase: "03.3"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-core/src/domain/devices.rs
  - crates/trackly-app/src/dto/device.rs
  - crates/trackly-infra/src/repos/devices_sqlite.rs
  - crates/trackly-app/src/services/device_service.rs
  - crates/trackly-app/tests/devices_grouping.rs
  - ui/src/bindings.ts
autonomous: true
requirements:
  - ITEM-1
must_haves:
  truths:
    - "list_grouped с group_by_condition=false схлопывает устройства с разным condition в одну группу (DevicesPage-режим)"
    - "list_grouped с group_by_condition=true сохраняет разбивку по condition (ActForm-режим, DEF-2B не сломан)"
    - "condition_distinct_count в DTO показывает 2+ для смешанной группы и 1 для однородной"
    - "Старые тесты DEF-2B (condition_key_splits_groups, grouping_groups_devices_with_same_name_and_different_condition) обновлены на group_by_condition: true и остаются зелёными"
    - "cargo test -p trackly-app --test devices_grouping зелёный"
    - "TypeScript-биндинги обновлены — DeviceFilter и DeviceGroup содержат новые поля"
  artifacts:
    - path: "crates/trackly-core/src/domain/devices.rs"
      provides: "DeviceFilter.group_by_condition: bool (default false); DeviceGroupRow.condition_distinct_count: i64"
      contains: "group_by_condition"
    - path: "crates/trackly-app/src/dto/device.rs"
      provides: "DeviceFilter DTO с group_by_condition: bool; DeviceGroup DTO с condition_distinct_count: i64"
      exports: ["DeviceFilter", "DeviceGroup"]
    - path: "crates/trackly-infra/src/repos/devices_sqlite.rs"
      provides: "list_grouped с двумя SQL-ветками (with/without condition в GROUP BY)"
      contains: "group_by_condition"
    - path: "crates/trackly-app/tests/devices_grouping.rs"
      provides: "Три новых теста + обновлённые DEF-2B тесты"
      contains: "grouping_page_mode_ignores_condition"
    - path: "ui/src/bindings.ts"
      provides: "Обновлённые TypeScript-биндинги с новыми полями"
      contains: "group_by_condition"
  key_links:
    - from: "crates/trackly-app/src/dto/device.rs"
      to: "crates/trackly-core/src/domain/devices.rs"
      via: "group_by_condition маппинг в device_service.rs list_grouped"
      pattern: "group_by_condition"
    - from: "crates/trackly-infra/src/repos/devices_sqlite.rs"
      to: "crates/trackly-core/src/domain/devices.rs"
      via: "filter.group_by_condition управляет SQL GROUP BY и LEFT JOIN subquery"
      pattern: "group_by_condition"
---

<objective>
Добавить флаг `group_by_condition: bool` в `DeviceFilter` (domain + DTO), сделать SQL `list_grouped`
условным по этому флагу (с двумя статическими SQL-строками), добавить `condition_distinct_count: i64`
в `DeviceGroupRow` и `DeviceGroup` DTO, обновить service-маппинг, написать три новых
интеграционных теста и обновить существующие DEF-2B-тесты, регенерировать TypeScript-биндинги.

Purpose: ITEM-1 требует развести два вызова list_grouped — страница Устройств НЕ разбивает
группы по condition, акт-форма сохраняет DEF-2B разбивку. Флаг `group_by_condition` —
архитектурно минимальная точка изменений (Вариант A из RESEARCH.md).

Output: Зелёные тесты `cargo test -p trackly-app --test devices_grouping` + обновлённые
`ui/src/bindings.ts` с новыми полями DeviceFilter.group_by_condition и DeviceGroup.condition_distinct_count.
</objective>

<execution_context>
@/Users/madsas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madsas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-UAT-ITEMS.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-RESEARCH.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-PATTERNS.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain + DTO + SQL — флаг group_by_condition и condition_distinct_count</name>
  <files>
    crates/trackly-core/src/domain/devices.rs,
    crates/trackly-app/src/dto/device.rs,
    crates/trackly-infra/src/repos/devices_sqlite.rs,
    crates/trackly-app/src/services/device_service.rs
  </files>
  <read_first>
    - crates/trackly-core/src/domain/devices.rs (текущий DeviceFilter строки 45–55, DeviceGroupRow строки 96–105)
    - crates/trackly-app/src/dto/device.rs (DeviceFilter DTO строки 191–203, DeviceGroup DTO строки 236–242)
    - crates/trackly-infra/src/repos/devices_sqlite.rs (весь метод list_grouped строки 903–1069 — SQL SELECT, LEFT JOIN subquery, GROUP BY, маппинг строк)
    - crates/trackly-app/src/services/device_service.rs (маппинг domain_filter строки 506–513, конструктор DeviceGroup строки 528–535)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-PATTERNS.md (паттерны изменений для каждого файла с текущим кодом)
  </read_first>
  <behavior>
    - DeviceFilter с group_by_condition=false + два устройства с разным condition → list_grouped возвращает 1 группу, condition_distinct_count == 2
    - DeviceFilter с group_by_condition=true + два устройства с разным condition → list_grouped возвращает 2 группы
    - DeviceFilter с group_by_condition=false + два устройства с одинаковым condition → list_grouped возвращает 1 группу, condition_distinct_count == 1
    - #[derive(Default)] на DeviceFilter выставляет group_by_condition = false (верное дефолтное поведение для DevicesPage)
  </behavior>
  <action>
    Изменения в четырёх файлах строго по паттернам из 03.3-PATTERNS.md:

    1. В `crates/trackly-core/src/domain/devices.rs`:
       В структуре `DeviceFilter` (после поля `include_deleted: bool`) добавить поле
       `pub group_by_condition: bool,` с doc-комментарием «Если true — GROUP BY включает
       d.condition (для акт-формы). Если false (по умолчанию) — без разбивки по condition».
       В структуре `DeviceGroupRow` добавить поле `pub condition_distinct_count: i64,`.

    2. В `crates/trackly-app/src/dto/device.rs`:
       В структуре `DeviceFilter` (DTO) добавить поле `pub group_by_condition: bool,`
       (без #[specta(...)], bool TypeScript-совместим нативно).
       В структуре `DeviceGroup` (DTO) добавить поле `#[specta(type = i32)] pub condition_distinct_count: i64,`
       (паттерн #[specta(type = i32)] уже применяется к полю count: u64).

    3. В `crates/trackly-infra/src/repos/devices_sqlite.rs`, метод `list_grouped`:
       Заменить одну SQL-строку двумя статическими строками `sql_with_condition` и
       `sql_without_condition` через Rust `if filter.group_by_condition { ... } else { ... }`.
       В `sql_with_condition`:
         - SELECT остаётся с `d.condition AS condition` (текущий вид)
         - Добавить `COUNT(DISTINCT d.condition) AS condition_distinct_count` в SELECT
         - LEFT JOIN subquery сохраняет `AND d2.condition IS d.condition`
         - GROUP BY остаётся `d.type_id, d.name, d.condition`
       В `sql_without_condition`:
         - SELECT заменяет `d.condition AS condition` на `MAX(d.condition) AS condition`
         - Добавить `COUNT(DISTINCT d.condition) AS condition_distinct_count` в SELECT
         - LEFT JOIN subquery убирает строку `AND d2.condition IS d.condition`
         - GROUP BY заменяется на `d.type_id, d.name` (без condition)
       В маппинге строк — добавить извлечение `condition_distinct_count: i64` (новый столбец
       вставить после существующего `state: Option<String>`, сдвинув все последующие индексы
       row.get() на +1). В конструкторе DeviceGroupRow добавить поле `condition_distinct_count`.

    4. В `crates/trackly-app/src/services/device_service.rs`, метод `list_grouped`:
       В конструкторе `domain_filter` добавить поле `group_by_condition: filter.group_by_condition`.
       В маппинге `DeviceGroup` добавить поле `condition_distinct_count: g.condition_distinct_count`.

    ВАЖНО: не использовать `format!()` для динамического SQL — только статические строки через if.
    ВАЖНО: добавить `condition_distinct_count` в ОБЕ SQL-ветки (pitfall из RESEARCH.md).
    ВАЖНО: в LEFT JOIN subquery убрать `AND d2.condition IS d.condition` ТОЛЬКО в `sql_without_condition`.
  </action>
  <verify>
    <automated>cargo build -p trackly-app 2>&amp;&amp; echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p trackly-app` завершается без ошибок компиляции
    - `grep "group_by_condition" crates/trackly-core/src/domain/devices.rs` возвращает совпадение (поле в DeviceFilter)
    - `grep "condition_distinct_count" crates/trackly-core/src/domain/devices.rs` возвращает совпадение (поле в DeviceGroupRow)
    - `grep "group_by_condition" crates/trackly-app/src/dto/device.rs` возвращает совпадение (поле в DeviceFilter DTO)
    - `grep "condition_distinct_count" crates/trackly-app/src/dto/device.rs` возвращает совпадение (поле в DeviceGroup DTO)
    - `grep "sql_with_condition\|sql_without_condition" crates/trackly-infra/src/repos/devices_sqlite.rs` возвращает две переменные (две SQL-ветки)
    - `grep "COUNT(DISTINCT d.condition)" crates/trackly-infra/src/repos/devices_sqlite.rs` — присутствует минимум в двух местах (в обеих SQL-ветках)
    - `grep "group_by_condition: filter.group_by_condition" crates/trackly-app/src/services/device_service.rs` возвращает совпадение
  </acceptance_criteria>
  <done>
    `cargo build -p trackly-app` зелёный; структуры DeviceFilter и DeviceGroup в обоих
    крейтах содержат новые поля; SQL list_grouped имеет две ветки с условным GROUP BY и
    COUNT(DISTINCT d.condition) в обеих ветках.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Тесты — три новых + обновить DEF-2B тесты</name>
  <files>
    crates/trackly-app/tests/devices_grouping.rs
  </files>
  <read_first>
    - crates/trackly-app/tests/devices_grouping.rs (полный файл — текущие тесты, паттерн make_service(), non_unique_device(), tokio::time::timeout)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-PATTERNS.md (секция «Три новых теста» с паттернами)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-VALIDATION.md (Wave 0 Requirements — список тест-функций)
  </read_first>
  <behavior>
    - grouping_page_mode_ignores_condition: group_by_condition=false + 2 устройства с разным condition → groups.len() == 1, condition_distinct_count == 2
    - grouping_act_form_keeps_condition_split: group_by_condition=true + 2 устройства с разным condition → groups.len() == 2
    - grouping_condition_distinct_count_mixed: group_by_condition=false + 2 устройства (один "Новое", один "Б/У") → groups[0].condition_distinct_count > 1
    - condition_key_splits_groups (обновить): фильтр с group_by_condition: true → 2 группы
    - grouping_groups_devices_with_same_name_and_different_condition (обновить): фильтр с group_by_condition: true → 2 группы
    - condition_key_same_condition_collapses: оставить как есть (default=false, одинаковый condition → 1 группа, тест остаётся зелёным)
  </behavior>
  <action>
    1. Добавить вспомогательную функцию `device_with_condition(name: &str, condition: &str) -> DeviceNew`
       по паттерну из PATTERNS.md (поля: type_id=1, state=Some(condition), inventory_no=None,
       serial_no=None, status_id=1).

    2. Добавить три новых теста по паттерну `grouping_collapses_non_unique`:
       - `grouping_page_mode_ignores_condition`: создать d1(Клавиатура, "Новое") и d2(Клавиатура, "Б/У"),
         фильтр `DeviceFilter { group_by_condition: false, ..Default::default() }`,
         assert groups.len() == 1, groups[0].condition_distinct_count == 2
       - `grouping_act_form_keeps_condition_split`: те же два устройства,
         фильтр `DeviceFilter { group_by_condition: true, ..Default::default() }`,
         assert groups.len() == 2, groups.iter().all(|g| g.count == 1)
       - `grouping_condition_distinct_count_mixed`: создать d1(Монитор, "Новое") и d2(Монитор, "Б/У"),
         фильтр с group_by_condition: false,
         assert groups.len() == 1, groups[0].condition_distinct_count > 1

    3. Обновить `condition_key_splits_groups`: изменить `DeviceFilter::default()` на
       `DeviceFilter { group_by_condition: true, ..Default::default() }`.
       Ожидание assert остаётся groups.len() == 2.

    4. Обновить `grouping_groups_devices_with_same_name_and_different_condition`: изменить
       `DeviceFilter::default()` на `DeviceFilter { group_by_condition: true, ..Default::default() }`.
       Ожидание assert остаётся groups.len() == 2.

    Не трогать остальные тесты — они создают устройства с одинаковым condition (или вовсе без),
    и DeviceFilter::default() (group_by_condition=false) для них корректен.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test devices_grouping 2>&amp;&amp; echo "ALL GROUPING TESTS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test devices_grouping` завершается без ошибок, все тесты зелёные
    - Тест `grouping_page_mode_ignores_condition` присутствует в файле и проходит (ITEM-1a)
    - Тест `grouping_act_form_keeps_condition_split` присутствует в файле и проходит (ITEM-1b)
    - Тест `grouping_condition_distinct_count_mixed` присутствует в файле и проходит (ITEM-1c)
    - `grep "group_by_condition: true" crates/trackly-app/tests/devices_grouping.rs` возвращает минимум 2 совпадения (обновлённые DEF-2B тесты, ITEM-1d)
    - `grep "condition_distinct_count" crates/trackly-app/tests/devices_grouping.rs` возвращает совпадение (ITEM-1c assert)
  </acceptance_criteria>
  <done>
    `cargo test -p trackly-app --test devices_grouping` зелёный. Все тесты проходят включая
    три новых (ITEM-1a, ITEM-1b, ITEM-1c) и обновлённые DEF-2B тесты (ITEM-1d).
  </done>
</task>

<task type="auto">
  <name>Task 3: Регенерация TypeScript-биндингов</name>
  <files>
    ui/src/bindings.ts
  </files>
  <read_first>
    - ui/package.json (секция scripts — команда для регенерации биндингов через specta/export_bindings)
    - ui/src/bindings.ts (первые 20 строк — убедиться что файл содержит DeviceFilter и DeviceGroup перед регенерацией)
  </read_first>
  <action>
    Запустить регенерацию TypeScript-биндингов через тест export_bindings.
    Команда: `cargo test -p trackly-app --test export_bindings`
    Эта команда сгенерирует обновлённый `ui/src/bindings.ts` из Specta-аннотированных
    DTO-структур, включая новые поля `group_by_condition: bool` в DeviceFilter и
    `condition_distinct_count: number` в DeviceGroup.
    После регенерации проверить, что файл содержит оба новых поля.
    Если команда export_bindings называется иначе — найти в package.json или Cargo.toml scripts.
  </action>
  <verify>
    <automated>cargo test -p trackly-app --test export_bindings 2>&amp;&amp; grep -c "group_by_condition" ui/src/bindings.ts &amp;&amp; grep -c "condition_distinct_count" ui/src/bindings.ts &amp;&amp; echo "BINDINGS UPDATED"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test export_bindings` завершается без ошибок
    - `grep "group_by_condition" ui/src/bindings.ts` возвращает совпадение (поле присутствует в TypeScript-типе DeviceFilter)
    - `grep "condition_distinct_count" ui/src/bindings.ts` возвращает совпадение (поле присутствует в TypeScript-типе DeviceGroup)
    - `cd ui && pnpm svelte-check` не падает на обращениях к новым полям (DeviceFilter.group_by_condition, DeviceGroup.condition_distinct_count)
  </acceptance_criteria>
  <done>
    `ui/src/bindings.ts` содержит `group_by_condition` и `condition_distinct_count` в
    соответствующих TypeScript-типах. `pnpm svelte-check` не падает на новых полях.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Tauri invoke / HTTP POST | DeviceFilter.group_by_condition передаётся из фронтенда как поле JSON-объекта |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.3-01 | Tampering | `list_grouped` SQL ветки | accept | `group_by_condition` — строго булевое Rust-поле, не строка; SQL генерируется через `if filter.group_by_condition` в скомпилированном Rust-коде, не через `format!` с пользовательским вводом. SQL-инъекция невозможна по конструкции. |
| T-03.3-02 | Information Disclosure | `list_grouped` оба режима | accept | Оба режима возвращают одни и те же устройства, только с разной агрегацией по condition. Новое поле condition_distinct_count не раскрывает данных, недоступных через expand группы. |
| T-03.3-SC | Tampering | npm/pip/cargo installs | accept | Эта задача не устанавливает новых пакетов. Все зависимости уже в Cargo.lock и pnpm-lock.yaml. |
</threat_model>

<verification>
После выполнения всех трёх задач:

1. `cargo test -p trackly-app --test devices_grouping` — все тесты зелёные (включая 3 новых и 2 обновлённых)
2. `cargo build -p trackly-app` — без ошибок компиляции
3. `grep "group_by_condition" ui/src/bindings.ts` — поле присутствует
4. `grep "condition_distinct_count" ui/src/bindings.ts` — поле присутствует
</verification>

<success_criteria>
- Все тесты в `devices_grouping.rs` зелёные (включая новые ITEM-1a/1b/1c и обновлённые ITEM-1d)
- `group_by_condition: false` (дефолт) схлопывает разные condition в одну группу
- `group_by_condition: true` сохраняет DEF-2B разбивку (2 группы с разным condition)
- `condition_distinct_count` корректно возвращается в обеих SQL-ветках
- TypeScript-биндинги обновлены и содержат оба новых поля
- `cargo clippy -- -D warnings` зелёный
</success_criteria>

<output>
Create `.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-01-SUMMARY.md` when done
</output>
