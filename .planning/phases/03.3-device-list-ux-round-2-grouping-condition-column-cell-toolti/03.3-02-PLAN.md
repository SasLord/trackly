---
phase: "03.3"
plan: 2
type: execute
wave: 2
depends_on:
  - "03.3-01"
files_modified:
  - crates/trackly-app/src/http/devices.rs
  - ui/src/features/devices/DevicesPage.svelte
  - ui/src/features/devices/DeviceList.svelte
  - ui/src/features/devices/DeviceListRow.svelte
  - ui/src/features/devices/DeviceGroupRow.svelte
  - ui/src/features/devices/DeviceAutocompleteField.svelte
  - ui/src/features/acts/ActFormItemsTable.svelte
autonomous: false
requirements:
  - ITEM-1
  - ITEM-2
  - ITEM-3
  - ITEM-4
must_haves:
  truths:
    - "DevicesPage передаёт group_by_condition: false в baseFilter — в списке устройств разные condition схлопываются"
    - "ActFormItemsTable передаёт group_by_condition: true — в dropdown акта сохраняется DEF-2B разбивка"
    - "Таблица устройств содержит колонку «Состояние» после «Расположение»"
    - "Свёрнутая группа со смешанным condition показывает «разное»; однородная — конкретное значение"
    - "Каждая текстовая ячейка DeviceListRow и DeviceGroupRow (collapsed) имеет атрибут title="
    - "Колонка «Статус» видна только на вкладке «Все» (statusFilter === null)"
    - "В форме добавления устройства поле «Расположение» показывает вторую секцию «Все расположения:» с distinct-локациями из БД"
    - "HTTP route POST /api/v1/locations_autocomplete работает в browser-режиме"
    - "pnpm svelte-check и pnpm lint зелёные"
  artifacts:
    - path: "ui/src/features/devices/DeviceList.svelte"
      provides: "showStatus prop; th «Состояние»; условный th «Статус»; skeleton colspan условный"
      contains: "showStatus"
    - path: "ui/src/features/devices/DeviceListRow.svelte"
      provides: "showStatus prop; title= на text-td; td «Состояние» (device.state)"
      contains: "device.state"
    - path: "ui/src/features/devices/DeviceGroupRow.svelte"
      provides: "showStatus prop; conditionDisplay ($derived); colspan динамический"
      contains: "conditionDisplay"
    - path: "ui/src/features/devices/DevicesPage.svelte"
      provides: "group_by_condition: false в baseFilter; showStatus={statusFilter === null}"
      contains: "group_by_condition"
    - path: "ui/src/features/acts/ActFormItemsTable.svelte"
      provides: "group_by_condition: true в объекте фильтра listGrouped"
      contains: "group_by_condition: true"
    - path: "ui/src/features/devices/DeviceAutocompleteField.svelte"
      provides: "allLocationSuggestions state; параллельный fetch к locations_autocomplete; вторая секция dropdown"
      contains: "allLocationSuggestions"
    - path: "crates/trackly-app/src/http/devices.rs"
      provides: "LocationsAutocompletePayload + handler_locations_autocomplete + router entry"
      contains: "locations_autocomplete"
  key_links:
    - from: "ui/src/features/devices/DevicesPage.svelte"
      to: "ui/src/features/devices/DeviceList.svelte"
      via: "showStatus={statusFilter === null} prop"
      pattern: "showStatus"
    - from: "ui/src/features/devices/DeviceList.svelte"
      to: "ui/src/features/devices/DeviceGroupRow.svelte"
      via: "{showStatus} prop pass-through"
      pattern: "showStatus"
    - from: "ui/src/features/devices/DeviceGroupRow.svelte"
      to: "ui/src/features/devices/DeviceListRow.svelte"
      via: "{showStatus} prop pass-through в expanded children"
      pattern: "showStatus"
    - from: "ui/src/features/devices/DeviceAutocompleteField.svelte"
      to: "crates/trackly-app/src/http/devices.rs"
      via: "apiCall('locations_autocomplete', { prefix }) через HTTP route"
      pattern: "locations_autocomplete"
---

<objective>
Реализовать все фронтенд-изменения для ITEM-1/2/3/4: колонка «Состояние» + «разное» + tooltip
на ячейки + скрытие колонки «Статус» на вкладках статуса + вторая секция «Все расположения» в
автокомплите. Плюс добавить HTTP route `POST /api/v1/locations_autocomplete` для browser-режима.

Purpose: Завершить все 4 UAT-пункта фазы 03.3 — изменения затрагивают только раздел «Устройства»,
акт-форма сохраняет текущее DEF-2B поведение.

Output: Таблица устройств с 8 колонками (на «Все») / 7 колонками (на вкладке статуса),
tooltip на text-ячейках, вторая секция «Все расположения» в dropdown поля Расположение.
</objective>

<execution_context>
@/Users/madsas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/madsas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-UAT-ITEMS.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-RESEARCH.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-PATTERNS.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-UI-SPEC.md
@.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-01-SUMMARY.md

<interfaces>
<!-- Ключевые типы из Plan 03.3-01, доступные после его выполнения. -->
<!-- Executor использует их напрямую — без исследования кодовой базы. -->

Из crates/trackly-core/src/domain/devices.rs (после плана 01):
  DeviceFilter { group_by_condition: bool, ... }  — default = false
  DeviceGroupRow { condition_distinct_count: i64, ... }

Из crates/trackly-app/src/dto/device.rs (после плана 01):
  DeviceFilter { group_by_condition: bool, ... }
  DeviceGroup { condition_distinct_count: i64, ... }   -- #[specta(type = i32)]

Из ui/src/bindings.ts (после регенерации в плане 01):
  DeviceFilter.group_by_condition: boolean
  DeviceGroup.condition_distinct_count: number

Из crates/trackly-app/src/tauri_cmds/devices.rs (уже существует):
  pub async fn build_locations_autocomplete(ctx: &AppCtx, prefix: String) -> Result<Vec<String>, ...>

Паттерн HTTP handler (из crates/trackly-app/src/http/devices.rs):
  pub async fn handler_list_by_ids(State(ctx): State<AppCtx>, Json(payload): Json<ListByIdsPayload>)
    -> Result<Json<Vec<DeviceDto>>, AppErrorResponse>

Паттерн Svelte prop с дефолтом (из DeviceListRow.svelte):
  const { ..., isLastInGroup = false, ... }: Props = $props();
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTTP route locations_autocomplete + ActFormItemsTable group_by_condition: true</name>
  <files>
    crates/trackly-app/src/http/devices.rs,
    ui/src/features/acts/ActFormItemsTable.svelte
  </files>
  <read_first>
    - crates/trackly-app/src/http/devices.rs (полный файл строки 1–322 — структуры payload строки ~80–84, handler_list_by_ids строки 231–239, router строки 294–321, import list строка 19)
    - crates/trackly-app/src/tauri_cmds/devices.rs (проверить сигнатуру build_locations_autocomplete — строки 62–67 или рядом)
    - ui/src/features/acts/ActFormItemsTable.svelte (вызов devices.listGrouped строки 101–112 — объект фильтра)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-PATTERNS.md (секция «http/devices.rs — новый HTTP route» с паттерном handler и payload struct)
  </read_first>
  <action>
    1. В `crates/trackly-app/src/http/devices.rs`:
       a) Добавить payload struct `LocationsAutocompletePayload` (рядом с другими payload structs,
          паттерн аналогичен `ListByIdsPayload`):
          `#[derive(serde::Deserialize)] pub struct LocationsAutocompletePayload { pub prefix: String }`
       b) Добавить `build_locations_autocomplete` в import list (строка ~19) из `crate::tauri_cmds::devices`
       c) Добавить handler `handler_locations_autocomplete` перед функцией `router()`:
          принимает `State(ctx): State<AppCtx>` и `Json(payload): Json<LocationsAutocompletePayload>`,
          делегирует в `build_locations_autocomplete(&ctx, payload.prefix)`, возвращает
          `Result<Json<Vec<String>>, AppErrorResponse>`
       d) В `router()` добавить route `.route("/api/v1/locations_autocomplete", post(handler_locations_autocomplete))`
          рядом с `.route("/api/v1/devices_list_by_ids", post(handler_list_by_ids))`

    2. В `ui/src/features/acts/ActFormItemsTable.svelte`:
       Найти объект фильтра в вызове `devices.listGrouped(...)` (строки 101–112).
       Добавить в объект фильтра поле `group_by_condition: true` — это сохраняет DEF-2B
       разбивку по condition в dropdown акт-формы.
       Не менять ничего другого в файле.
  </action>
  <verify>
    <automated>cargo build -p trackly-app 2>&amp;&amp; grep -c "locations_autocomplete" crates/trackly-app/src/http/devices.rs &amp;&amp; grep -v "^//" ui/src/features/acts/ActFormItemsTable.svelte | grep -c "group_by_condition: true" &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p trackly-app` завершается без ошибок компиляции
    - `grep "LocationsAutocompletePayload" crates/trackly-app/src/http/devices.rs` возвращает совпадение (payload struct присутствует)
    - `grep "handler_locations_autocomplete" crates/trackly-app/src/http/devices.rs` возвращает минимум 2 совпадения (определение функции + регистрация в router)
    - `grep '"/api/v1/locations_autocomplete"' crates/trackly-app/src/http/devices.rs` возвращает совпадение (route зарегистрирован)
    - `grep "group_by_condition: true" ui/src/features/acts/ActFormItemsTable.svelte` возвращает совпадение (DEF-2B сохранён)
  </acceptance_criteria>
  <done>
    `cargo build -p trackly-app` зелёный.
    `crates/trackly-app/src/http/devices.rs` содержит `handler_locations_autocomplete` и
    router entry для `/api/v1/locations_autocomplete`.
    `ui/src/features/acts/ActFormItemsTable.svelte` содержит `group_by_condition: true`.
  </done>
</task>

<task type="auto">
  <name>Task 2: DevicesPage + DeviceList + DeviceListRow + DeviceGroupRow — ITEM-1/2/3</name>
  <files>
    ui/src/features/devices/DevicesPage.svelte,
    ui/src/features/devices/DeviceList.svelte,
    ui/src/features/devices/DeviceListRow.svelte,
    ui/src/features/devices/DeviceGroupRow.svelte
  </files>
  <read_first>
    - ui/src/features/devices/DevicesPage.svelte (полный файл — baseFilter строки 48–55, DeviceList usage строки ~75+)
    - ui/src/features/devices/DeviceList.svelte (полный файл — Props interface строки 6–21, thead строки 59–67 и 88–97, skeleton строка 72, pass-through строки 104–114)
    - ui/src/features/devices/DeviceListRow.svelte (полный файл — Props строки 6–14, template строки 41–53, CSS строки 70–80)
    - ui/src/features/devices/DeviceGroupRow.svelte (полный файл — Props строки 29–39, $derived declarations ~строки 60–70, collapsed row строки 130–169, loading row строка 175, pass-through строка 179)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-PATTERNS.md (секции для каждого из этих файлов с КОНКРЕТНЫМ целевым кодом)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-UI-SPEC.md (colspan-карта и порядок колонок — ITEM-1 + ITEM-3)
    - ui/src/bindings.ts (убедиться в наличии DeviceFilter.group_by_condition и DeviceGroup.condition_distinct_count перед использованием)
  </read_first>
  <action>
    Изменения строго по паттернам в 03.3-PATTERNS.md и дизайн-контракту 03.3-UI-SPEC.md:

    1. `DevicesPage.svelte`:
       a) В объект `baseFilter` добавить поле `group_by_condition: false` (DevicesPage НЕ разбивает по condition)
       b) В компонент `DeviceList` добавить prop `showStatus={statusFilter === null}`

    2. `DeviceList.svelte`:
       a) В `Props` interface добавить `showStatus?: boolean`
       b) В деструктуризации пропов добавить `showStatus = true` (безопасный дефолт — старое поведение)
       c) В thead (ОБА блока — grouped и plain) после th «Расположение» добавить
          `<th class="th th-condition">Состояние</th>`, изменить th «Статус» на условный
          `{#if showStatus}<th class="th th-status">Статус</th>{/if}`
       d) В skeleton заменить `{ length: 7 }` на `{ length: showStatus ? 8 : 7 }`
       e) В CSS добавить `.th-condition { width: 120px; }` рядом с `.th-status`
       f) В pass-through дочерних компонентов `DeviceGroupRow` и `DeviceListRow` добавить `{showStatus}`

    3. `DeviceListRow.svelte`:
       a) В `Props` interface добавить `showStatus?: boolean`
       b) В деструктуризации добавить `showStatus = true`
       c) Во все текстовые ячейки добавить атрибуты `title=`:
          - Наименование: `title={device.name}`
          - Инв.№: `title={device.inventory_no ?? ''}`
          - Серийный №: `title={device.serial_no ?? ''}`
          - Модель: `title={device.model ?? ''}`
          - Расположение: `title={device.location ?? ''}`
       d) Добавить новую ячейку «Состояние» после ячейки «Расположение»:
          `<td class="cell" title={device.state ?? ''}>{device.state ?? '—'}</td>`
       e) Обернуть существующую ячейку «Статус» (Badge) в `{#if showStatus}...{/if}`
       Ячейки Badge (Статус) и DeviceContextMenu (Действия) — без атрибута title.
       Новая ячейка Состояние наследует стили `.cell` (white-space: nowrap; overflow: hidden;
       text-overflow: ellipsis; max-width: 0) — доп. CSS не нужен.

    4. `DeviceGroupRow.svelte`:
       a) В `Props` interface добавить `showStatus?: boolean`
       b) В деструктуризации добавить `showStatus = true`
       c) Добавить `$derived` для conditionDisplay (после существующих $derived-переменных):
          `const conditionDisplay = $derived(group.condition_distinct_count > 1 ? 'разное' : (group.repr.state ?? '—'))`
       d) В collapsed row:
          - Добавить `title={group.repr.name}` на td cell-name-wide (colspan="4" НЕ меняется — остаётся 4)
          - Добавить `title={group.repr.location ?? ''}` на td Расположение
          - Добавить новую ячейку Состояние: `<td class="cell" title={conditionDisplay}>{conditionDisplay}</td>`
          - Обернуть ячейку «Статус» (Badge) в `{#if showStatus}...{/if}`
       e) В loading row: изменить `colspan="7"` на `colspan={showStatus ? 8 : 7}`
       f) В pass-through дочерних `DeviceListRow`: добавить `{showStatus}`

    Финальный порядок колонок:
    Всегда: Наим | Инв | Серийный | Модель | Расположение | Состояние
    Условно: Статус (только когда showStatus=true)
    Всегда: Действия

    Colspan collapsed group-row ячейки с именем = 4 (Наим + Инв + Серийный + Модель) — НЕ МЕНЯЕТСЯ.
    Текст «разное» — строчными, без акцента, color: --color-text-primary (обычный текст ячейки).
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm svelte-check 2>&amp;&amp; echo "SVELTE-CHECK OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && pnpm svelte-check` завершается без ошибок TypeScript/Svelte
    - `grep "group_by_condition: false" ui/src/features/devices/DevicesPage.svelte` возвращает 1 совпадение (в baseFilter)
    - `grep "showStatus" ui/src/features/devices/DevicesPage.svelte` возвращает совпадение (prop передан в DeviceList)
    - `grep "showStatus" ui/src/features/devices/DeviceList.svelte` возвращает минимум 4 совпадения (interface, destructure, thead, skeleton)
    - `grep "Состояние" ui/src/features/devices/DeviceList.svelte` возвращает совпадение (th заголовок)
    - `grep "conditionDisplay" ui/src/features/devices/DeviceGroupRow.svelte` возвращает минимум 2 совпадения ($derived + использование в template)
    - `grep 'showStatus ? 8 : 7' ui/src/features/devices/DeviceGroupRow.svelte` возвращает совпадение (loading colspan)
    - `grep 'showStatus ? 8 : 7' ui/src/features/devices/DeviceList.svelte` возвращает совпадение (skeleton)
    - `grep 'title={device.name}' ui/src/features/devices/DeviceListRow.svelte` возвращает совпадение (ITEM-2 tooltip)
    - `grep 'device.state' ui/src/features/devices/DeviceListRow.svelte` возвращает совпадение (ITEM-1 новая ячейка)
  </acceptance_criteria>
  <done>
    `pnpm svelte-check` зелёный.
    Все четыре Svelte-компонента содержат ожидаемые изменения по ITEM-1/2/3.
  </done>
</task>

<task type="auto">
  <name>Task 3: DeviceAutocompleteField — вторая секция «Все расположения» (ITEM-4)</name>
  <files>
    ui/src/features/devices/DeviceAutocompleteField.svelte
  </files>
  <read_first>
    - ui/src/features/devices/DeviceAutocompleteField.svelte (полный файл — imports строки ~1–30, state declarations строки ~60–65, $effect с debounceTimer строки ~90–130, handleFocus строки ~165–175, handleKeydown строки ~219–230, template dropdown строки ~290–320, CSS .dropdown-header строки ~375–382)
    - ui/src/lib/components/LocationAutocomplete.svelte (паттерн fetch: строки 35–41, apiCall сигнатура)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-PATTERNS.md (секция DeviceAutocompleteField — конкретные изменения с примерами кода)
    - .planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-UI-SPEC.md (ITEM-4 — структура dropdown, keyboard navigation, дедупликация)
  </read_first>
  <action>
    Добавить вторую секцию «Все расположения» в dropdown только для `field === 'location'`:

    1. Убедиться что `apiCall` импортирован из `$lib/api/client`. Если не импортирован —
       добавить в import list: `import { apiCall } from '$lib/api/client'`.

    2. Добавить новый state после `let suggestions = $state<string[]>([])`:
       `let allLocationSuggestions = $state<string[]>([]);`

    3. Добавить `$derived` для объединённого списка навигации (добавить рядом с другими $derived):
       `const allItems = $derived(field === 'location' ? [...suggestions, ...allLocationSuggestions] : suggestions);`

    4. В `$effect` с debounceTimer, после строки `suggestions = await devices.autocomplete(...)`,
       добавить параллельную загрузку локаций:
       Когда `field === 'location'`:
       - выполнить `apiCall<string[]>('locations_autocomplete', { prefix: v })`
       - отфильтровать дубли: `allLocs.filter(l => !suggestions.includes(l))`
       - записать в `allLocationSuggestions`
       Иначе: `allLocationSuggestions = []`

    5. В `handleFocus`, после аналогичной строки `suggestions = await devices.autocomplete(...)`,
       добавить аналогичный блок для `field === 'location'` с `prefix: value`.

    6. В `handleKeydown` заменить все вхождения `suggestions.length` на `allItems.length`
       и `suggestions[activeIndex]` на `allItems[activeIndex]` (строки ~219–230).

    7. В template dropdown, ПОСЛЕ блока `{#each suggestions as s, i (s)}...{/each}`,
       добавить вторую секцию только для field="location":
       - Заголовок: `<header class="dropdown-header">Все расположения:</header>`
         (CSS класс dropdown-header уже существует — не добавлять новый)
       - Каждый item: кнопка с role="option", class="dropdown-item",
         id="autocomplete-item-{suggestions.length + i}",
         class:active={suggestions.length + i === activeIndex},
         aria-selected={suggestions.length + i === activeIndex},
         onmousedown={(e) => { e.preventDefault(); select(s); }}
       - Ключи #each: `'all_' + s` (для уникальности ключей между секциями)

    Не менять поведение для field !== 'location'. Не менять функцию select().
    При onmousedown — e.preventDefault() (существующий паттерн, чтобы не потерять фокус поля).
    Текст заголовка точно: `Все расположения:` (строчная Буква, с двоеточием, с пробелом).
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly/ui &amp;&amp; pnpm svelte-check 2>&amp;&amp; grep -c "allLocationSuggestions" src/features/devices/DeviceAutocompleteField.svelte &amp;&amp; grep -c "Все расположения" src/features/devices/DeviceAutocompleteField.svelte &amp;&amp; grep -c "suggestions\[activeIndex\]\|suggestions\.length" src/features/devices/DeviceAutocompleteField.svelte | grep -q "^0$" &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cd ui && pnpm svelte-check` завершается без ошибок TypeScript/Svelte
    - `grep "allLocationSuggestions" ui/src/features/devices/DeviceAutocompleteField.svelte` возвращает минимум 3 совпадения (state declaration, fetch assignment, template condition)
    - `grep "allItems" ui/src/features/devices/DeviceAutocompleteField.svelte` возвращает минимум 2 совпадения ($derived + использование в handleKeydown)
    - `grep "Все расположения" ui/src/features/devices/DeviceAutocompleteField.svelte` возвращает 1 совпадение (заголовок секции с точным текстом)
    - `grep "'all_' + s" ui/src/features/devices/DeviceAutocompleteField.svelte` возвращает 1 совпадение (уникальные ключи #each для второй секции)
    - `grep "locations_autocomplete" ui/src/features/devices/DeviceAutocompleteField.svelte` возвращает совпадение (apiCall к endpoint)
    - `grep -c "suggestions\[activeIndex\]\|suggestions\.length" ui/src/features/devices/DeviceAutocompleteField.svelte` возвращает 0 — все старые вхождения заменены на `allItems[activeIndex]` и `allItems.length`
  </acceptance_criteria>
  <done>
    `pnpm svelte-check` зелёный.
    `DeviceAutocompleteField.svelte` содержит `allLocationSuggestions`, `allItems`,
    `Все расположения:`, `'all_' + s` ключи. Клавиатурная навигация использует `allItems`.
    Старые `suggestions[activeIndex]` и `suggestions.length` в handleKeydown полностью заменены.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 03.3-02 полностью выполнен:
    - ITEM-1: колонка «Состояние» добавлена в таблицу устройств, group_by_condition=false в DevicesPage, group_by_condition=true в ActFormItemsTable
    - ITEM-2: title= на всех text-ячейках DeviceListRow и DeviceGroupRow
    - ITEM-3: колонка «Статус» скрыта при выборе конкретного статуса
    - ITEM-4: вторая секция «Все расположения:» в dropdown поля «Расположение»
    - HTTP route POST /api/v1/locations_autocomplete добавлен
  </what-built>
  <how-to-verify>
    1. Запустить приложение (cargo tauri dev или pnpm dev)

    ITEM-1 (группировка + колонка Состояние):
    a) Создать 2 устройства с одинаковым Наименованием («Клавиатура»), разным Состоянием («Новое» и «Б/У»), без инв. и серийного №
    b) На странице «Устройства» (вкладка «Все»): должна быть 1 группа «Клавиатура» с count=2 и колонкой Состояние = «разное»
    c) Развернуть группу: внутри 2 строки, у каждой своё значение Состояния («Новое» и «Б/У»)
    d) Открыть форму создания Акта → в dropdown добавить «Клавиатура»: должны быть видны ДВЕ отдельные строки («Клавиатура — Новое» и «Клавиатура — Б/У»), разбивка DEF-2B сохранена

    ITEM-2 (tooltip):
    e) Создать устройство с длинным названием (более 20 символов)
    f) Навести курсор на ячейку с этим названием в таблице — браузерный tooltip с полным текстом (~500–700ms задержка)

    ITEM-3 (скрытие Статус):
    g) На вкладке «Все» — колонка «Статус» видна (8 колонок: Наим, Инв, Серийный, Модель, Расположение, Состояние, Статус, Действия)
    h) Переключиться на «В работе» — колонка «Статус» исчезает (7 колонок)
    i) Вернуться на «Все» — колонка «Статус» снова видна

    ITEM-4 (вторая секция автокомплита):
    j) Открыть форму добавления устройства → кликнуть на поле «Расположение»
    k) В dropdown должна появиться секция «Все расположения:» (если в БД есть хотя бы одна локация из других устройств)
    l) Ввод символа в поле «Расположение» → обе секции фильтруются по введённому тексту
  </how-to-verify>
  <resume-signal>Написать "approved" если все пункты выполнены, или описать проблемы для исправления</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → HTTP POST /api/v1/locations_autocomplete | Новый endpoint принимает { prefix: String } из браузер-режима |
| Frontend → Tauri invoke locations_autocomplete | Тот же handler через Tauri-транспорт |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.3-03 | Tampering | POST /api/v1/locations_autocomplete | mitigate | Параметр `prefix: String` используется через parameterized query в существующем `DeviceService::locations_autocomplete` (LIKE ?1). SQL-инъекция исключена параметризацией. Endpoint не создаёт/изменяет данные — только читает distinct-локации. |
| T-03.3-04 | Information Disclosure | POST /api/v1/locations_autocomplete | accept | Endpoint возвращает distinct-значения поля location, уже доступные через list_grouped. Нет дополнительного раскрытия. В Phase 5 endpoint будет защищён session-middleware (как все /api/v1/ маршруты); в текущей фазе auth не реализован — принят тот же уровень риска, что и для остальных /api/v1/ endpoints. |
| T-03.3-05 | Spoofing | showStatus prop | accept | showStatus — булевый prop, вычисляется в DevicesPage из statusFilter (локальный Svelte state). Нет пути для подделки через внешний ввод. |
| T-03.3-SC | Tampering | npm/pip/cargo installs | accept | Эта задача не устанавливает новых пакетов. Все зависимости уже в Cargo.lock и pnpm-lock.yaml. |
</threat_model>

<verification>
После выполнения всех задач:

1. `cargo build -p trackly-app` — без ошибок
2. `cd ui && pnpm svelte-check` — без ошибок TypeScript/Svelte
3. `cd ui && pnpm lint` — без ошибок ESLint
4. `grep -c "group_by_condition: false" ui/src/features/devices/DevicesPage.svelte` = 1
5. `grep -c "group_by_condition: true" ui/src/features/acts/ActFormItemsTable.svelte` = 1
6. `grep -c "locations_autocomplete" crates/trackly-app/src/http/devices.rs` >= 3 (payload + handler + route)
7. `grep -c "conditionDisplay" ui/src/features/devices/DeviceGroupRow.svelte` >= 2
8. `grep -c "allLocationSuggestions" ui/src/features/devices/DeviceAutocompleteField.svelte` >= 3
9. Ручной UAT — все 4 пункта ITEM-1..4 проверены и подтверждены
</verification>

<success_criteria>
- ITEM-1: DevicesPage не разбивает группы по condition; свёрнутая смешанная группа показывает «разное»; акт-форма сохраняет DEF-2B
- ITEM-2: title= на всех текстовых ячейках DeviceListRow и DeviceGroupRow (collapsed)
- ITEM-3: колонка «Статус» видна только на вкладке «Все»; colspan корректен во всех состояниях
- ITEM-4: поле «Расположение» показывает вторую секцию «Все расположения:» с distinct-локациями
- HTTP route POST /api/v1/locations_autocomplete работает в browser-режиме
- pnpm svelte-check и pnpm lint зелёные
- cargo build -p trackly-app зелёный
- Ручной UAT подтверждён пользователем
</success_criteria>

<output>
Create `.planning/phases/03.3-device-list-ux-round-2-grouping-condition-column-cell-toolti/03.3-02-SUMMARY.md` when done
</output>
