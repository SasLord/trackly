---
phase: 15-render-word-fidelity
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - crates/trackly-app/src/services/act_service.rs
  - crates/trackly-app/templates/act_handover.minijinja
  - crates/trackly-app/src/services/template_service.rs
  - crates/trackly-app/src/pdf/renderer.rs
autonomous: true
requirements: [PDFA-01, PDFA-02, PDFA-05]

must_haves:
  truths:
    - "An act with a logo saved via org_settings BLOB (Settings UI path) renders that logo in the PDF — not just when a legacy org.json logo_path is set (WR-03 closed)"
    - "The default act_handover template emits, and the renderer draws, all sample blocks in D-09 order: header (2-col) -> title 'Акт приема-передачи' -> number/date -> intro paragraph with receiver_name -> device block(s) -> 'Сроком до' -> signatures 'Выдал'/'Получил'"
    - "An act with 1 device renders a compact identification block (per-device card) plus wrapped long fields (Комплектация/Тех.характеристики/Состояние) without truncation"
    - "An act with N (2+) devices renders each device's identification + long fields as a repeating per-device card, all positions present, no clipping/overlap"
    - "validate_preview (TemplateEditor preview) succeeds against the rewritten template without 'undefined value' errors"
  artifacts:
    - path: "crates/trackly-app/templates/act_handover.minijinja"
      provides: "Rewritten default template: D-09 intro wording, Выдал/Получил signature labels+sublabels, per-device hybrid card loop, extended header fields"
      contains: "Настоящим актом утверждаю"
    - path: "crates/trackly-app/src/services/act_service.rs"
      provides: "render_pdf no longer discards logo_bytes/logo_mime from get_for_pdf()"
      contains: "logo_bytes"
    - path: "crates/trackly-app/src/services/template_service.rs"
      provides: "validate_preview demo_ctx updated with specs/kit/condition + org.phone/fax/email/okpo/ogrn"
      contains: "\"kit\""
  key_links:
    - from: "crates/trackly-app/src/services/act_service.rs::render_pdf"
      to: "crates/trackly-app/src/pdf/docspec.rs::HeaderBlock.logo_bytes"
      via: "DocSpec post-processing after serde_json::from_str, before render_docspec"
      pattern: "spec\\.header\\.logo_bytes"
    - from: "crates/trackly-app/templates/act_handover.minijinja"
      to: "crates/trackly-app/src/pdf/docspec.rs::Section (new hybrid device section shape)"
      via: "MiniJinja items loop emitting per-device JSON section(s)"
      pattern: "for item in act.items"
---

<objective>
Wire the renderer capability built in Plan 15-01 (two-column header, word-wrap, two-line
signatures) into the actual PDF pipeline: fix the WR-03 logo-source bug in `act_service.rs`
(BLOB logo currently discarded), rewrite the default `act_handover.minijinja` template to
match the Word sample structure exactly (D-09 block order and wording, D-06 hybrid
multi-device layout, D-07 signature sublabels), and keep
`template_service.rs::validate_preview`'s `demo_ctx` in lockstep so the TemplateEditor
preview does not break (Pitfall 3).

This plan also adds the DocSpec section shape needed for the D-06 hybrid device layout
(per-device card: compact identification fields + wrapped long fields) — a new
`Section` variant or reuse of `Paragraph`/wrap-primitive sections emitted per device by
the template, rendered by extending `render_section`'s per-Section dispatch (built on
Plan 15-01's `wrap_text_to_width`).

Purpose: Phase 14 delivered the data (specs/kit/condition/requisites/logo BLOB all reach
the render context); Plan 15-01 delivered the rendering primitives. This plan closes the
last gap — the template that decides WHAT to emit, and the one remaining act_service.rs
plumbing bug (logo BLOB dropped).
Output: Rewritten `act_handover.minijinja`, fixed `act_service.rs::render_pdf` logo
plumbing, synced `template_service.rs::validate_preview` demo_ctx, extended
`render_section` dispatch for the new per-device hybrid section shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-render-word-fidelity/15-CONTEXT.md
@.planning/phases/15-render-word-fidelity/15-RESEARCH.md
@.planning/phases/15-render-word-fidelity/15-PATTERNS.md
@.planning/PHASE-BRIEF-act-pdf-word-fidelity.md
@.planning/phases/15-render-word-fidelity/15-01-SUMMARY.md
</context>

<interfaces>
<!-- act_service.rs::render_pdf — current logo-discarding bug (WR-03), exact lines -->
From crates/trackly-app/src/services/act_service.rs (lines 1356-1360, 1410-1422):
```rust
let org_dto = match pipeline.org_db {
    Some(org_db) => {
        let (dto, _logo_bytes, _logo_mime) = org_db.get_for_pdf().await?;
        dto
    }
    None => crate::dto::reports::OrgSettingsDto { /* org_legacy fallback */ },
};
// ...
let ctx = serde_json::json!({
    "org": { /* name/inn/kpp/address/phone/fax/email/okpo/ogrn */
        "logo_path": safe_logo.map(|p| p.display().to_string()),
    },
    "act": { /* number/suffix/date/date_human/giver_name/receiver_name/
                deadline/deadline_human/location_name/items/parent */ },
});
let spec: crate::pdf::docspec::DocSpec = serde_json::from_str(&rendered)...;
pipeline.pdf.render_docspec(&spec)
```

<!-- items_json — already correct, no act_service.rs change needed for these fields -->
From crates/trackly-app/src/services/act_service.rs (lines 1393-1408):
```rust
let items_json: Vec<serde_json::Value> = act.items.iter().map(|it| serde_json::json!({
    "name": it.device_name, "inventory_no": it.inventory_no, "serial_no": it.serial_no,
    "model": it.model, "specs": it.specs, "kit": it.complectation_at_time,
    "condition": it.condition_at_time, "quantity": it.quantity,
})).collect();
```

<!-- HeaderBlock — full struct after Plan 15-01 (unchanged by 15-01, already has logo_bytes) -->
From crates/trackly-app/src/pdf/docspec.rs (lines 33-66): `org_name, org_inn, org_kpp,
org_address, logo_path: Option<String>, logo_bytes: Option<Vec<u8>> (#[serde(default)]),
logo_mime: Option<String> (#[serde(default)]), org_phone/org_fax/org_email/org_okpo/org_ogrn:
String (#[serde(default)]), act_label, date_label`.

<!-- Section::Signature after Plan 15-01 -->
```rust
Signature {
    left_label: String,
    right_label: String,
    #[serde(default = "default_spacer_pt")] spacer_pt: f32,
    #[serde(default)] left_sublabel: Option<String>,
    #[serde(default)] right_sublabel: Option<String>,
},
```

<!-- Current act_handover.minijinja context variable doc comment (to update) -->
From crates/trackly-app/templates/act_handover.minijinja (lines 5-10):
```
org.name, org.inn, org.kpp, org.address, org.logo_path
act.number, act.suffix, act.date, act.date_human,
act.giver_name, act.receiver_name, act.deadline, act.deadline_human,
act.location_name, act.items[], act.parent
return.condition_default, return.location_default
```
Note: act.items[] already carries name/inventory_no/serial_no/model/specs/kit/condition/
quantity per act_service.rs (specs/kit/condition are NOT yet in this doc comment — update it).

<!-- template_service.rs validate_preview demo_ctx — CURRENT gap (Pitfall 3) -->
From crates/trackly-app/src/services/template_service.rs (lines 262-308): `org` block
lacks phone/fax/email/okpo/ogrn; `act.items[0]` lacks specs/kit/condition. Both MUST be
added in lockstep with the template rewrite in this plan.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix WR-03 logo-BLOB plumbing in act_service.rs render_pdf</name>
  <files>crates/trackly-app/src/services/act_service.rs</files>
  <read_first>
    crates/trackly-app/src/services/act_service.rs (targeted range 1330-1460 — read the
    full render_pdf function body), crates/trackly-app/src/services/org_db_service.rs
    (targeted range 360-401 — get_for_pdf signature/return shape), 15-RESEARCH.md
    §Pitfall 5 (logo priority, not replacement) and §Code Examples "Требуемый фикс в
    act_service.rs", 15-PATTERNS.md §act_service.rs section (byte-transport note: bypass
    the template for logo bytes — approach (a))
  </read_first>
  <action>
    In `render_pdf` (~line 1357), stop discarding `get_for_pdf()`'s second and third
    tuple elements: rename `let (dto, _logo_bytes, _logo_mime) = org_db.get_for_pdf().await?;`
    to `let (dto, logo_bytes, logo_mime) = org_db.get_for_pdf().await?;` (keep `dto` name).
    Do NOT remove the `safe_logo`/`org_legacy.safe_logo_canonical` call (line ~1352-1355)
    — it remains the `logo_path` fallback source (Pitfall 5: priority, not replacement).

    Per PATTERNS.md's recommended approach (a) — bypass the MiniJinja JSON round-trip for
    raw image bytes (avoids fuel-cost concerns and Vec<u8>-as-JSON-array inefficiency):
    after the `DocSpec` is parsed from the rendered template JSON (~line 1450-1454,
    `let spec: crate::pdf::docspec::DocSpec = serde_json::from_str(&rendered)...`), and
    BEFORE calling `pipeline.pdf.render_docspec(&spec)`, mutate the parsed `spec`:
    `let mut spec = ...; if let Some(bytes) = logo_bytes { spec.header.logo_bytes =
    Some(bytes); spec.header.logo_mime = logo_mime; }`. This preserves the renderer's
    existing priority (`logo_bytes` wins over `logo_path` — already correct per
    renderer.rs lines 178-183, untouched) while making the real BLOB bytes reach
    `HeaderBlock` regardless of what the template JSON says about `logo_bytes` (templates
    can't reasonably emit raw binary as JSON, so they never set this field — the Rust
    layer is the sole source of truth for `logo_bytes`).

    Do not change the `None` branch (no `org_db` wired) — it already constructs an
    all-empty `OrgSettingsDto` and correctly has no logo bytes to propagate.
  </action>
  <acceptance_criteria>
    - `cargo build -p trackly-app` compiles with no unused-variable warnings on the
      renamed `logo_bytes`/`logo_mime` bindings.
    - Source assertion: `grep -n "_logo_bytes\|_logo_mime" crates/trackly-app/src/services/act_service.rs`
      returns NO matches (both underscore-discarded bindings are gone).
    - Source assertion: `grep -n "spec.header.logo_bytes = Some" crates/trackly-app/src/services/act_service.rs`
      returns exactly one match inside `render_pdf`.
    - `cargo clippy -p trackly-app -- -D warnings` clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p trackly-app && cargo clippy -p trackly-app -- -D warnings</automated>
  </verify>
  <done>render_pdf propagates real org_settings BLOB logo bytes into the rendered HeaderBlock; safe_logo (org.json path) remains as fallback; no dead/discarded bindings remain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hybrid per-device wrap-block dispatch to render_section</name>
  <files>crates/trackly-app/src/pdf/docspec.rs, crates/trackly-app/src/pdf/renderer.rs</files>
  <read_first>
    crates/trackly-app/src/pdf/renderer.rs (render_section full function, lines 204-329,
    especially the ItemsTable arm lines 265-301 and KeyValueTable arm lines 243-264 as
    layout analogs), crates/trackly-app/src/pdf/docspec.rs (Section enum, full),
    15-RESEARCH.md §Architectural Responsibility Map + §Open Question 1 (per-device card
    recommendation) + §Pitfall 4 (why ItemsTable can't just grow columns), 15-CONTEXT.md
    §D-06 (hybrid layout requirement)
  </read_first>
  <behavior>
    - A new `Section::DeviceCard` variant (or equivalently named, Claude's discretion per
      D-06) accepts: a heading label (e.g. "Устройство №{n}: {name}"), a compact
      identification `KvRow`-shaped list (Инв.№/Серийный №/Модель), and a list of
      long-field `(label, value)` pairs to be wrapped (Комплектация/Тех.характеристики/
      Состояние) — each long field renders via `wrap_text_to_width` from Plan 15-01,
      never via `truncate_to_width`.
    - Given a device with short specs/kit/condition text, the card renders as a compact
      block with no unnecessary blank wrapped lines.
    - Given a device with long specs/kit/condition text (multi-line after wrap), the
      card renders all wrapped lines with no overlap with the next device's card (each
      card advances the y-cursor by its own full rendered height, matching the
      `render_section` return-y-cursor contract).
    - Given N device cards in sequence (N=1 and N=5+), all N cards render fully without
      any circular/overlapping y positions (each card's returned y becomes the next
      card's starting y, exactly like the existing per-Section loop in render_docspec).
    - Given an empty/missing long field (e.g. `kit: None` → empty string), that field's
      line is skipped entirely (not rendered as an empty "Комплектация:" line) — matches
      the graceful-degradation idiom used elsewhere in this module.
  </behavior>
  <action>
    In docspec.rs, add a new `Section` variant (name of Claude's choosing, e.g.
    `DeviceCard`) with fields: `heading: String`, `identification: Vec<KvRow>` (reuse the
    existing `KvRow` struct for №/Наименование/Инв.№/Серийный №/Модель), `long_fields:
    Vec<KvRow>` (reuse `KvRow` again for label/value pairs — Комплектация/Тех.характеристики/
    Состояние; empty-string values are filtered OUT by the template before emission, not
    by the renderer, matching the `| default("—", true)`-vs-conditional-omission idiom
    already used in the template for other optional blocks). No `#[serde(default)]` is
    needed here since this is a brand-new variant (old JSON simply never uses this tag,
    fully backward compatible by construction — new tagged-enum variants never break
    existing deserialization of other variants).

    In renderer.rs, add a new match arm to `render_section` for this variant: draw
    `heading` in bold at `HEADING_SIZE_PT - 2.0` (matching the existing `Heading{level:2}`
    size), then render `identification` rows using the same compact key-bold/value-regular
    two-column layout as the existing `KeyValueTable` arm (reuse that inline drawing
    style, not a function extraction — keep this task's diff minimal), then for each
    `long_fields` entry: draw the label in bold, then call `wrap_text_to_width` (from
    Plan 15-01) on the value with `max_width = usable_width` (full content width, not a
    narrow table column — this is the point of the hybrid layout per D-06/Pitfall 4),
    draw each returned line on its own y-row advancing by `BODY_SIZE_PT + 2.0` per line.
    Advance the section's final returned y-cursor by an extra `8.0`pt spacer after the
    last long field (matching the existing `KeyValueTable`/`ItemsTable` trailing-spacer
    convention at lines 263/300).
  </action>
  <acceptance_criteria>
    - New unit test `device_card_renders_identification_and_wrapped_long_fields` in
      renderer.rs `#[cfg(test)] mod tests`: construct a `Section::DeviceCard` with a long
      `Комплектация` value (150+ Cyrillic chars), render via `PdfRenderer::render_docspec`,
      extract text via `pdf_extract`, assert (a) no `'…'` present (proves wrap path used,
      not truncate), (b) a substring from the MIDDLE of the long value is present (proves
      the full value survived across wrapped lines, not truncated).
    - New unit test `device_card_skips_empty_long_field`: construct a `DeviceCard` with
      one `long_fields` entry whose value is `""`, assert the label text for that entry is
      absent from extracted text (fully skipped, not rendered as an empty line) — this
      assertion applies at the template layer in Task 3, but this renderer-level test
      verifies the renderer itself doesn't emit anything when the template legitimately
      omits an empty-value KvRow (i.e. test with an empty `long_fields: vec![]` and assert
      no crash/no stray label).
    - New unit test `two_device_cards_do_not_overlap`: render a `DocSpec` with two
      `Section::DeviceCard` entries back to back (first with a long wrapped value, second
      short), extract text, assert both devices' `heading` strings are present and in the
      correct relative order (first device's heading appears before second device's in
      the extracted text string).
    - `cargo test -p trackly-app --lib pdf::docspec pdf::renderer -- --test-threads=1`
      green including all pre-existing tests.
    - `cargo clippy -p trackly-app -- -D warnings` clean.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --lib pdf::docspec pdf::renderer -- --test-threads=1</automated>
  </verify>
  <done>Section::DeviceCard exists, renders compact identification + wrapped long fields per device, multiple cards stack without overlap, empty long fields are skippable.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite act_handover.minijinja to D-09 block order + D-06 hybrid device loop + D-07 signature sublabels; sync validate_preview demo_ctx</name>
  <files>crates/trackly-app/templates/act_handover.minijinja, crates/trackly-app/src/services/template_service.rs</files>
  <read_first>
    crates/trackly-app/templates/act_handover.minijinja (full current file — the
    optional-field defaulting idiom at line 41 and conditional-injection idiom at lines
    42-49 and 65-76 items-loop idiom MUST be reused, not reinvented),
    crates/trackly-app/src/services/template_service.rs (lines 253-343, validate_preview
    + demo_ctx — MUST be updated in the same task to avoid the Pitfall 3 regression),
    .planning/PHASE-BRIEF-act-pdf-word-fidelity.md §"Точный разбор образца Word" (exact
    wording/labels/order to match), 15-CONTEXT.md §D-09 (block order + exact phrasing),
    15-PATTERNS.md §act_handover.minijinja section + §template_service.rs section
  </read_first>
  <action>
    Rewrite `act_handover.minijinja`'s JSON output to match D-09's block order exactly:
    (1) `header` object — extend to include `org_phone`, `org_fax`, `org_email`,
    `org_okpo`, `org_ogrn` keys (sourced from `org.phone`/`org.fax`/`org.email`/`org.okpo`/
    `org.ogrn`, each via the existing `| default("—", true)` idiom from line 41 — actually
    use empty-string default `| default("", true)` here since `HeaderBlock`'s new fields
    are typed `String` not `Option<String>`, and the renderer's `render_header_two_column`
    from Plan 15-01 already skips empty lines, so emitting `""` is correct, not `"—"`); (2)
    `sections` array in this exact order: `heading` level 1 with text `"Акт приема-передачи"`
    (note: exact sample spelling "приема-передачи" without the ё-diacritic per PHASE-BRIEF,
    distinct from the existing `"Акт приёма-передачи №{{ act.number }}"` used elsewhere —
    keep `title`/`act_label` as today's existing number-suffixed form for internal
    consistency, but this specific `heading` section's `text` must read the PHASE-BRIEF's
    literal sample title); (3) a `paragraph` (or `key_value_table`) section for
    number/date — reuse the existing "Дата" key_value_table row idiom but reframe as
    "№ {{ act.number_raw... }} от {{ date }}" per D-09 (real number/date, not dashes, per
    Phase 14 D-04 already-shipped decision); (4) a new `paragraph` section with the exact
    D-09 intro wording: `"Настоящим актом утверждаю, что мною " ~ act.receiver_name ~ "
    было получено устройство:"` (use `~` string concatenation + `| tojson`, matching the
    existing concatenation idiom at line 22/48); (5) the device block(s) — replace the
    current single `items_table` (columns lines 63-76) with a loop emitting one
    `Section::DeviceCard`-shaped JSON object per `act.items` entry (per Plan 15-02 Task 2's
    new tag, e.g. `"type": "device_card"`), each with `heading` = `"Устройство №" ~
    loop.index ~ ": " ~ item.name`, `identification` = KvRow array for Инв.№/Серийный №/
    Модель (reuse the `| default("—", true)` idiom for missing values), `long_fields` =
    KvRow array for Комплектация (`item.kit`)/Технические характеристики (`item.specs`)/
    Состояние (`item.condition`) — conditionally include each long-field KvRow ONLY when
    the value is non-empty (use `{%- if item.kit %}` style guards per the D-06 renderer
    contract "empty fields skipped entirely", matching the existing `{%- if act.deadline_human %}`
    conditional-injection idiom at lines 42-46); (6) `"Сроком до: " ~ act.deadline_human`
    paragraph section, conditionally included only when `act.deadline_human` or
    `act.deadline` is set (reuse the exact `{%- if act.deadline_human %}...{%- elif
    act.deadline %}...{%- endif %}` idiom, now as a standalone paragraph section instead
    of a key_value_table row); (7) `signature` section with `left_label: "Выдал"`,
    `right_label: "Получил"` (per D-09, replacing the current `"Сдал: " ~
    act.giver_name"`/`"Принял: " ~ act.receiver_name"` labels — the giver/receiver NAMES
    move to the intro paragraph and/or stay as separate KvRow context if still needed
    elsewhere, but the Signature section's own labels become the bare role names per the
    sample), `left_sublabel: "Подпись"`, `right_sublabel: "ФИО"` (D-07), `spacer_pt: 30.0`
    unchanged.

    Update the header doc-comment block (lines 1-13) to list all newly consumed context
    keys: `org.phone, org.fax, org.email, org.okpo, org.ogrn`, and
    `act.items[].specs, act.items[].kit, act.items[].condition` (per Pitfall 3 discipline
    — keep this comment in sync with what the template actually reads).

    In `template_service.rs`, update `validate_preview`'s `demo_ctx` (lines 262-308) in
    the SAME task/commit: add `"phone": "(3919) 75-90-98"`, `"fax": "(3919) 75-08-59"`,
    `"email": "info@demo-org.ru"`, `"okpo": "10176125"`, `"ogrn": "1122452000714"` to the
    `org` object; add `"specs": "Диагональ: 27 дюймов"`, `"kit": "Монитор, подставка,
    кабель питания"`, `"condition": "Новый в заводской упаковке"` to the single
    `act.items[0]` object. This keeps `validate_preview_returns_pdf_bytes` (existing test,
    lines 420-444) passing against the rewritten template — do not skip this, it is the
    Pitfall 3 regression this task must close.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --lib services::template_service -- --test-threads=1`
      passes, including the pre-existing `validate_preview_returns_pdf_bytes` and
      `validate_preview_act_acceptance_returns_pdf_bytes` tests (act_acceptance template
      is untouched by this plan — confirm it still passes as a regression guard).
    - Source assertion: `grep -n "Настоящим актом утверждаю" crates/trackly-app/templates/act_handover.minijinja`
      returns a match.
    - Source assertion: `grep -n '"Выдал"' crates/trackly-app/templates/act_handover.minijinja`
      and `grep -n '"Получил"' crates/trackly-app/templates/act_handover.minijinja` both
      return matches.
    - Source assertion: `grep -n '"kit"' crates/trackly-app/src/services/template_service.rs`
      returns a match inside `demo_ctx`.
    - `cargo build -p trackly-app` compiles (template is embedded via `include_str!`, so a
      malformed Jinja syntax error would only surface at render time, not compile time —
      Task 4/Plan 15-03's full-pipeline tests are the real gate for template correctness,
      but this task's own `validate_preview` test run is the fastest feedback loop).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --lib services::template_service -- --test-threads=1</automated>
  </verify>
  <done>act_handover.minijinja matches D-09 block order/wording and D-06 hybrid device loop and D-07 signature sublabels; validate_preview demo_ctx synced; existing template_service tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| document_templates.body_minijinja (admin-editable, DB-stored) → MiniJinja render → DocSpec JSON | The rewritten default template is still subject to `build_safe_env()`'s existing sandbox (Strict undefined, fuel=100_000, no loader, 5s timeout) — this plan does not touch `minijinja_env.rs` and must not weaken it |
| org_settings.logo_blob (admin-uploaded via Settings UI, ManageSettings-gated) → act_service.rs render_pdf → HeaderBlock.logo_bytes | This plan changes WHICH bytes propagate (fixes WR-03) but not the validation boundary — `save_logo`'s existing mime/size allowlist (T-07-02-01, unchanged) remains the sole gate on what can enter `logo_blob` in the first place |
| act.items[].specs/kit/condition (device.notes / act_items snapshot columns, user-entered free text) → template loop → DeviceCard long_fields → wrap_text_to_width | Arbitrary-length, arbitrary-content free text set by any user with device/act write access flows into the new per-device wrap rendering path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | act_handover.minijinja rewrite | mitigate | Template continues to emit only typed `Section`/`HeaderBlock` JSON fields (no raw/string PDF-operator field introduced) — same structural guarantee as before this plan, verified by `serde_json::from_str::<DocSpec>` failing closed (AppError::Validation) on any malformed shape |
| T-15-06 | Denial of Service | MiniJinja fuel budget vs. new per-device loop complexity | accept | Per RESEARCH Pitfall 6: the hybrid loop adds conditional branches per device but typical N (1-8) stays well under the existing `set_fuel(100_000)` limit (unchanged in this plan); documented as a known limitation for very large N, not fixed here (no requirement calls for raising the fuel limit) |
| T-15-07 | Information Disclosure | Logo BLOB now actually reaches the rendered PDF (WR-03 fix) | accept | This is the INTENDED behavior change (closing a functional gap, not introducing a new exposure) — the logo was always meant to be printable; `save_logo`'s existing admin-only write gate is unchanged |
| T-15-08 | Tampering | Free-text specs/kit/condition rendered via new wrap primitive | accept | Same trust level as all other free-text act/device fields already rendered today (giver_name, receiver_name, location) — no new injection surface, since `Section`/`KvRow` remain typed and the renderer only ever calls `draw_text`/`wrap_text_to_width` on plain `&str` |

</threat_model>

<verification>
- `cargo build -p trackly-app` succeeds.
- `cargo test -p trackly-app --lib pdf::docspec pdf::renderer services::template_service -- --test-threads=1` green.
- `cargo clippy -p trackly-app -- -D warnings` clean.
- `cargo fmt --check` clean.
- NOTE: full-pipeline integration tests (pdf_render_act.rs, pdf_column_overflow.rs,
  pdf_logo.rs, pdf_determinism.rs) are extended/regenerated in Plan 15-03 — do not attempt
  to make `cargo test -p trackly-app --test pdf_determinism` pass in this plan (expected
  break, tracked explicitly in 15-03).
</verification>

<success_criteria>
- `act_service.rs::render_pdf` no longer discards `logo_bytes`/`logo_mime` — BLOB logo
  set via Settings UI now reaches the rendered PDF, with `org.json` `logo_path` remaining
  as fallback only.
- `act_handover.minijinja` emits, in order: 2-column header with extended requisites,
  «Акт приема-передачи» title, real number/date, D-09 intro paragraph with
  `receiver_name`, one `DeviceCard` section per act item (hybrid compact
  identification + wrapped long fields), conditional «Сроком до» block, and a
  two-line-sublabel signature section with «Выдал»/«Получил» + «Подпись»/«ФИО».
- `template_service.rs::validate_preview`'s `demo_ctx` matches the new template's
  variable schema — `validate_preview_returns_pdf_bytes` passes.
- `render_section` in `renderer.rs` dispatches the new hybrid device-card section without
  touching `ItemsTable`/`truncate_to_width`.
</success_criteria>

<output>
Create `.planning/phases/15-render-word-fidelity/15-02-SUMMARY.md` when done
</output>
