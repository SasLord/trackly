---
phase: 15-render-word-fidelity
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - crates/trackly-app/tests/pdf_render_act.rs
  - crates/trackly-app/tests/pdf_column_overflow.rs
  - crates/trackly-app/tests/pdf_logo.rs
  - crates/trackly-app/tests/fixtures/act_42.sha256
autonomous: true
requirements: [PDFA-01, PDFA-02, PDFA-05, PDFA-07, PDFA-08]

must_haves:
  truths:
    - "The full PDF pipeline (act_service::render_pdf, not render_docspec directly) confirms a BLOB logo set via OrgDbService::save_logo actually appears in the generated act PDF (closes the WR-03 regression gap the existing pdf_logo.rs tests never caught)"
    - "An act with 1 device and an act with 5 devices both render all positions, and long specs/kit/condition fields wrap without ellipsis truncation"
    - "The rendered PDF text contains 'Выдал', 'Получил', 'Подпись', 'ФИО' and D-09's exact intro phrase"
    - "The full existing PDF test suite (pdf_render_act, pdf_column_overflow, pdf_logo, pdf_determinism) passes, with the determinism fixture hash intentionally regenerated to reflect the new renderer output"
  artifacts:
    - path: "crates/trackly-app/tests/pdf_render_act.rs"
      provides: "render_handover_multi_device_wraps_long_fields, signature_renders_two_line_labels tests (PDFA-02, PDFA-05)"
      contains: "render_handover_multi_device_wraps_long_fields"
    - path: "crates/trackly-app/tests/pdf_logo.rs"
      provides: "Full-pipeline logo test via act_service::render_pdf (not render_docspec direct) proving WR-03 closed"
      contains: "render_pdf"
    - path: "crates/trackly-app/tests/fixtures/act_42.sha256"
      provides: "Regenerated pinned hash reflecting the new header/wrap/signature renderer output"
  key_links:
    - from: "crates/trackly-app/tests/pdf_logo.rs (new test)"
      to: "crates/trackly-app/src/services/act_service.rs::render_pdf"
      via: "p.acts.render_pdf(act.id) full pipeline call, not PdfRenderer::render_docspec direct"
      pattern: "p\\.acts\\.render_pdf"
    - from: "crates/trackly-app/tests/pdf_render_act.rs (new multi-device test)"
      to: "crates/trackly-app/src/services/act_service.rs::render_pdf"
      via: "seed N devices, create handover act, render, assert no ellipsis + all positions present"
      pattern: "seed_devices\\(&p\\.writer, "
---

<objective>
Close the Wave 0 test gaps RESEARCH/VALIDATION identified, using the renderer capability
from Plan 15-01 and the wired pipeline from Plan 15-02: add a full-pipeline (not
direct-renderer) logo test that would have caught WR-03, add multi-device (1 vs N)
wrap-without-truncation coverage, add signature two-line-label coverage, and regenerate
the `pdf_determinism.rs` pinned-hash fixture — an explicitly planned, expected step, not
an incidental fixup.

Purpose: PDFA-08 requires the existing PDF pipeline tests to keep passing and new tests
to exist for the new template and multi-device rendering. This plan is the final gate —
it proves the D-06/D-07/D-08/D-09 decisions actually produced correct, regression-tested
output end to end.
Output: Extended `pdf_render_act.rs` (2 new tests), extended `pdf_logo.rs` (1 new
full-pipeline test), regenerated `act_42.sha256`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-render-word-fidelity/15-CONTEXT.md
@.planning/phases/15-render-word-fidelity/15-RESEARCH.md
@.planning/phases/15-render-word-fidelity/15-PATTERNS.md
@.planning/phases/15-render-word-fidelity/15-VALIDATION.md
@.planning/phases/15-render-word-fidelity/15-01-SUMMARY.md
@.planning/phases/15-render-word-fidelity/15-02-SUMMARY.md
</context>

<interfaces>
<!-- Full-pipeline fixture construction — reuse exactly, do not reinvent -->
From crates/trackly-app/tests/pdf_render_act.rs (lines 39-103):
```rust
async fn make_full_pipeline() -> Pipeline { /* no org_db wired */ }
async fn make_full_pipeline_with_org_db() -> Pipeline { /* wires OrgDbService via with_org_db */ }
async fn seed_devices(writer: &Arc<WriterHandle>, count: usize) -> Vec<i64> { /* INSERT INTO devices, name only */ }
async fn create_handover_with_giver(svc: &ActService, device_ids: &[i64], giver: &str) -> ActDto { /* ActCreateDto -> svc.create */ }
```

<!-- OrgDbService::save_logo — signature needed for the new full-pipeline logo test -->
From crates/trackly-app/src/services/org_db_service.rs (lines 123-128):
```rust
pub async fn save_logo(&self, caller: &Identity, logo_bytes: Vec<u8>, logo_mime: String) -> Result<(), AppError>
```

<!-- Existing logo test fixture bytes to reuse -->
From crates/trackly-app/tests/pdf_logo.rs (line 21):
```rust
const LOGO_PNG: &[u8] = include_bytes!("fixtures/logo_test.png");
```

<!-- pdf_extract assertion idiom — reuse verbatim -->
From crates/trackly-app/tests/pdf_render_act.rs (lines 400-408):
```rust
let text = pdf_extract::extract_text_from_mem(&bytes).expect("extract");
assert!(text.contains("Ромашка"), "... Head: {:?}", text.chars().take(500).collect::<String>());
```

<!-- pdf_determinism.rs regeneration instructions — test itself prints the actual hash -->
From crates/trackly-app/tests/pdf_determinism.rs (lines 20-37): running
`cargo test -p trackly-app --test pdf_determinism fixture_act_42_renders_to_known_hash`
after Plan 15-01/15-02 land will FAIL with an assertion message containing the actual
SHA256 — copy that value into `tests/fixtures/act_42.sha256`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-device (1 vs N) wrap test and two-line signature test to pdf_render_act.rs</name>
  <files>crates/trackly-app/tests/pdf_render_act.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_render_act.rs (full file — `seed_devices` lines
    105-126 for the device-seeding pattern, `render_pdf_with_filled_specs_and_requisites_surfaces_data`
    lines 344-412 as the near-verbatim template for a new specs-heavy multi-device test,
    including the `writer.execute` direct-UPDATE idiom for setting `devices.notes`),
    crates/trackly-app/src/services/act_service.rs (targeted 1393-1408, confirms item
    context field names: name/inventory_no/serial_no/model/specs/kit/condition/quantity —
    `kit` maps to `complectation_at_time`, `condition` maps to `condition_at_time`, both on
    `act_items`, set at act-creation time from the device snapshot, not `devices` table
    directly — confirm via `act_service.rs` create/clone-on-handover logic around line 409
    which columns to UPDATE for a controlled test value), 15-VALIDATION.md §Wave 0
    Requirements (exact test names expected: render_handover_multi_device_wraps_long_fields,
    signature_renders_two_line_labels), 15-PATTERNS.md §pdf_render_act.rs section
    (device-seeding helper extension guidance)
  </read_first>
  <behavior>
    - `render_handover_multi_device_wraps_long_fields`: seed 5 devices, create ONE
      handover act with all 5 as items, set a long (150+ char) Cyrillic
      `complectation_at_time` value on at least 2 of the resulting `act_items` rows
      directly via `writer.execute` UPDATE (mirroring the `devices.notes` UPDATE idiom at
      pdf_render_act.rs lines 350-362, but targeting `act_items.complectation_at_time
      WHERE act_id = ? AND device_id = ?`), render via `p.acts.render_pdf(act.id)`, assert:
      (a) extracted text contains all 5 device names, (b) extracted text does NOT contain
      `'…'` (proves wrap path used for the long field, not ellipsis truncation), (c) a
      substring from the MIDDLE of the long complectation value is present in extracted
      text (proves the value survived, wasn't cut off).
    - Also assert the N=1 case still works: reuse the existing
      `render_handover_act_produces_cyrillic_pdf` test (unchanged) as the N=1 regression
      anchor — do not duplicate it, just note in a doc comment that it covers N=1 while
      the new test covers N=5.
    - `signature_renders_two_line_labels`: render any handover act (N=1 is sufficient),
      assert extracted text contains all four of `"Выдал"`, `"Получил"`, `"Подпись"`,
      `"ФИО"`.
    - New assertion (folded into either test or a third small test
      `render_handover_act_contains_d09_intro_phrase`): extracted text contains
      `"Настоящим актом утверждаю"` and the act's `receiver_name` value, proving the D-09
      intro paragraph renders with the correct interpolated name.
  </behavior>
  <action>
    Add the new test functions to `pdf_render_act.rs` following the exact
    `#[tokio::test(flavor = "multi_thread", worker_threads = 4)]` +
    `tokio::time::timeout(Duration::from_secs(60), async { ... }).await.expect("timeout")`
    wrapper pattern used by every existing test in this file (do not deviate — this
    pattern is required for the whole-file's timeout-safety convention). Use
    `make_full_pipeline_with_org_db()` (not the bare `make_full_pipeline()`) for the
    multi-device test if it also asserts requisites, otherwise `make_full_pipeline()` is
    sufficient if the test is purely about device wrapping. Extend `seed_devices` usage
    with a direct `writer.execute` UPDATE against `act_items` after `create_handover_with_giver`
    returns the created `ActDto` (which carries `act.id` and each item's `id`/`device_id`)
    to set `complectation_at_time` to a long test string for 2 of the 5 seeded items.
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test pdf_render_act -- --test-threads=1` passes,
      including all pre-existing tests plus the new
      `render_handover_multi_device_wraps_long_fields`,
      `signature_renders_two_line_labels`, and the D-09 intro-phrase assertion.
    - Test command: `cargo test -p trackly-app --test pdf_render_act render_handover_multi_device_wraps_long_fields -- --test-threads=1 --nocapture` exits 0.
    - Test command: `cargo test -p trackly-app --test pdf_render_act signature_renders_two_line_labels -- --test-threads=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test pdf_render_act -- --test-threads=1</automated>
  </verify>
  <done>pdf_render_act.rs covers 1-vs-N device wrapping without truncation, two-line signature labels, and the D-09 intro phrase, all via the full act_service::render_pdf pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add full-pipeline logo test to pdf_logo.rs (closes WR-03 regression gap) + extend pdf_column_overflow.rs contrast test</name>
  <files>crates/trackly-app/tests/pdf_logo.rs, crates/trackly-app/tests/pdf_column_overflow.rs</files>
  <read_first>
    crates/trackly-app/tests/pdf_logo.rs (full file — note ALL existing tests call
    `PdfRenderer::render_docspec` directly, bypassing `act_service`/`OrgDbService`
    entirely; this is exactly why WR-03 was never caught, per 15-RESEARCH.md §Pitfall 5
    and §Wave 0 Gaps), crates/trackly-app/tests/pdf_render_act.rs (lines 39-103,
    `make_full_pipeline_with_org_db` — the fixture this new test must use),
    crates/trackly-app/src/services/org_db_service.rs (lines 123-128, `save_logo`
    signature), crates/trackly-app/tests/pdf_column_overflow.rs (full file — existing
    `long_name_truncated_does_not_overlap_inv_no` test at lines 64-92 is the direct
    contrast analog), 15-PATTERNS.md §pdf_logo.rs section + §pdf_column_overflow.rs section
  </read_first>
  <behavior>
    - New pdf_logo.rs test `blob_logo_via_full_pipeline_renders_in_act_pdf`: given an
      `OrgDbService` with a logo saved via `save_logo(caller, LOGO_PNG.to_vec(),
      "image/png".to_string())`, and a handover act created via the full
      `ActService::render_pdf` pipeline (not `render_docspec` direct), the resulting PDF
      bytes contain `/Subtype /Image` or `/XObject` (same marker the existing direct-renderer
      tests check) — this is the test that would have failed before Plan 15-02 Task 1's
      WR-03 fix and must pass now.
    - New pdf_column_overflow.rs test (or extend existing file):
      `device_card_long_field_wraps_instead_of_truncating` — full-pipeline variant
      complementing the existing direct-DocSpec `long_name_truncated_does_not_overlap_inv_no`
      test: given a long `complectation_at_time` value rendered through the real
      `act_handover.minijinja` template (via `p.acts.render_pdf`), assert the INVERSE of
      the existing ellipsis assertion — `!text.contains('…')` for that specific long
      value's content, contrasted explicitly against the `ItemsTable` truncation behavior
      the existing test in this same file still verifies for the OLD compact-table
      columns (proving both code paths coexist correctly: ItemsTable still truncates
      where used, DeviceCard wrap-blocks don't).
  </behavior>
  <action>
    In pdf_logo.rs, add the new test using `make_full_pipeline_with_org_db()`-equivalent
    setup (either import it if `pdf_render_act` exposes it as `pub`, or inline an
    equivalent local fixture — check whether the existing helper is `pub(crate)`/private
    to `pdf_render_act.rs`'s module and, if not exported, duplicate the minimal subset
    needed locally rather than restructuring `pdf_render_act.rs` to export it, to keep
    this task's diff scoped to test files only). Call `org_db.save_logo(&Identity::trusted_admin(),
    LOGO_PNG.to_vec(), "image/png".to_string())` before creating the act, then
    `p.acts.render_pdf(act.id)`, then assert the `/Subtype /Image`/`/XObject` marker per
    the existing `bytes_contain` helper already defined in this file (line 46-48) — reuse
    it, don't redefine.

    In pdf_column_overflow.rs, add the new full-pipeline test using the same
    `make_full_pipeline()` + `seed_devices` + `create_handover_with_giver` pattern,
    setting a long `complectation_at_time` on the created act's item via direct
    `writer.execute` UPDATE (same idiom as Task 1), rendering via `p.acts.render_pdf`,
    and asserting the inverse-of-truncation behavior described above. Keep the existing
    direct-DocSpec tests in this file completely unchanged (they still validate
    `ItemsTable`'s own truncation behavior, which is untouched by this phase).
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test pdf_logo -- --test-threads=1` passes, including
      all 5 pre-existing tests plus the new
      `blob_logo_via_full_pipeline_renders_in_act_pdf`.
    - `cargo test -p trackly-app --test pdf_column_overflow -- --test-threads=1` passes,
      including all 3 pre-existing tests plus the new full-pipeline wrap test.
    - Source assertion: `grep -n "p.acts.render_pdf" crates/trackly-app/tests/pdf_logo.rs`
      returns at least one match (proves the new test goes through the full pipeline,
      not `render_docspec` direct).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app --test pdf_logo --test pdf_column_overflow -- --test-threads=1</automated>
  </verify>
  <done>pdf_logo.rs has a full-pipeline test proving BLOB logo reaches the rendered act PDF; pdf_column_overflow.rs has a full-pipeline test proving long device fields wrap instead of truncating, alongside the untouched ItemsTable truncation tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run full trackly-app test suite, regenerate pdf_determinism.rs pinned hash, final phase-gate checks</name>
  <files>crates/trackly-app/tests/fixtures/act_42.sha256</files>
  <read_first>
    crates/trackly-app/tests/pdf_determinism.rs (full file — the test prints the actual
    hash on assertion failure per lines 32-36), crates/trackly-app/tests/fixtures/act_42.json
    (fixture input — confirm it is NOT edited in this task, only the `.sha256` output is
    regenerated, since the new Signature sublabel fields are `#[serde(default)]` and this
    fixture's existing `signature` object without sublabels remains valid, exercising the
    backward-compat path end to end), 15-RESEARCH.md §Pitfall 1 (this break is expected,
    not a regression to silently work around)
  </read_first>
  <action>
    Run `cargo test -p trackly-app --test pdf_determinism fixture_act_42_renders_to_known_hash -- --test-threads=1 --nocapture`
    — expect FAILURE with an assertion message containing the actual new SHA256 hash
    (per Pitfall 1, this is the expected/planned outcome of Plans 15-01/15-02's renderer
    changes, not a bug). Copy the printed `actual` hash value and overwrite
    `crates/trackly-app/tests/fixtures/act_42.sha256` with exactly that value (trim
    trailing whitespace/newline to match the existing file's format — inspect the current
    file's exact byte format with `cat -A` before overwriting, since the test does
    `.trim()` on read but the file should still match the existing single-line-hash
    convention). Re-run the same test command and confirm it now passes.

    Then run the full workspace gate: `cargo test -p trackly-app -- --test-threads=1`
    (full suite, one process — per project MEMORY constraint, never run concurrent
    `cargo test` invocations), `cargo clippy -p trackly-app -- -D warnings`, `cargo fmt
    --check`. Fix any residual clippy/fmt issues surfaced by Plans 15-01/15-02's changes
    if this is the first time the full suite runs after all three plans (this task is the
    phase-gate checkpoint per 15-VALIDATION.md "Phase gate: Полный cargo test -p
    trackly-app зелёный + clippy + fmt").
  </action>
  <acceptance_criteria>
    - `cargo test -p trackly-app --test pdf_determinism -- --test-threads=1` passes (both
      `fixture_act_42_renders_to_known_hash` with the regenerated hash, and
      `rendering_twice_yields_identical_bytes` which requires no fixture change).
    - `cargo test -p trackly-app -- --test-threads=1` (full suite) passes with zero
      failures.
    - `cargo clippy -p trackly-app -- -D warnings` exits 0.
    - `cargo fmt --check` exits 0.
    - `git diff --stat crates/trackly-app/tests/fixtures/act_42.json` shows NO changes
      (fixture input untouched — only `.sha256` regenerated).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p trackly-app -- --test-threads=1 && cargo clippy -p trackly-app -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>act_42.sha256 reflects the new deterministic renderer output; full trackly-app test/clippy/fmt suite is green; Phase 15 is ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test fixtures (act_42.json, logo_test.png) — trusted, committed to repo | No new trust boundary introduced; this plan only adds/extends assertions over already-established fixture files |
| CI pipeline running the regenerated determinism hash | The pinned-hash mechanism itself is the security control that catches unintended renderer drift in future phases — this plan's regeneration step must be deliberate and reviewed (visible in the git diff of `act_42.sha256`), not silently automated away |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering | act_42.sha256 regeneration | mitigate | Regeneration is an explicit, single, reviewable task (Task 3) with a fixed procedure (run test, copy printed hash, verify fixture JSON itself is untouched) — not a blanket `UPDATE_EXPECT=1`-style auto-accept that could silently mask an unrelated regression |
| T-15-10 | Repudiation | Full-pipeline logo test using `Identity::trusted_admin()` | accept | Test-only identity, matches the existing test convention across the codebase (e.g. `render_pdf_with_filled_specs_and_requisites_surfaces_data` uses the same caller) — no production auth boundary affected |

</threat_model>

<verification>
- `cargo test -p trackly-app --test pdf_render_act --test pdf_column_overflow --test pdf_logo --test pdf_determinism -- --test-threads=1` all green.
- `cargo test -p trackly-app -- --test-threads=1` (full workspace-crate suite) green.
- `cargo clippy -p trackly-app -- -D warnings` clean.
- `cargo fmt --check` clean.
</verification>

<success_criteria>
- PDFA-02 covered: multi-device (1 vs 5) rendering with long-field wrap (no truncation)
  verified via full-pipeline `pdf_extract` assertions.
- PDFA-05 covered: two-line signature sublabels («Подпись»/«ФИО» under «Выдал»/«Получил»)
  verified via full-pipeline `pdf_extract` assertions.
- PDFA-01 covered: D-09 intro phrase + block presence verified via full-pipeline
  `pdf_extract` assertions.
- PDFA-07 covered: all new assertions operate on Cyrillic text extracted from the
  rendered PDF (embedded-font regression implicitly proven by every passing
  `pdf_extract` assertion in this plan and the pre-existing suite).
- PDFA-08 covered: full existing PDF pipeline test suite passes; new tests exist for
  multi-device and the new template; `pdf_determinism.rs`'s pinned hash is deliberately
  regenerated (not silently broken).
- The WR-03 regression gap (BLOB logo silently dropped) is closed by a test that
  exercises the FULL pipeline (`act_service::render_pdf`), not the renderer in isolation.
</success_criteria>

<output>
Create `.planning/phases/15-render-word-fidelity/15-03-SUMMARY.md` when done
</output>
