---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - crates/trackly-infra/src/paths.rs
  - crates/trackly-infra/src/config.rs
  - crates/trackly-infra/src/lib.rs
  - crates/trackly-infra/tests/paths_test.rs
  - crates/trackly-infra/tests/config_test.rs
  - crates/trackly-app/src/webview_env.rs
  - crates/trackly-app/src/main.rs
  - crates/trackly-app/src/lib.rs
  - crates/trackly-core/src/error.rs
  - crates/trackly-core/src/lib.rs
autonomous: true
requirements:
  - FOUND-04
  - FOUND-05
  - FOUND-11
must_haves:
  truths:
    - "`trackly_infra::Paths::resolve()` returns paths rooted at `std::env::current_exe()?.parent()?` when a `portable.txt` or `trackly.config.toml` sentinel sits next to the .exe."
    - "`trackly_infra::Paths::resolve()` rejects UNC / SMB share roots with a clear `AppError` (V8 data protection)."
    - "`std::env::set_var(\"WEBVIEW2_USER_DATA_FOLDER\", paths.webview_data_dir())` is the FIRST non-trivial statement in `crates/trackly-app/src/main.rs` (before tracing, tokio, or any tauri call), wrapped in `unsafe { ... }` with a SAFETY comment."
    - "Running `trackly --self-test` after this plan creates `<exe_dir>/data/webview/`, `<exe_dir>/logs/` (logs dir is created by Plan 05), reads `trackly.config.toml` if present (or uses defaults), and exits 0 without writing anywhere outside `<exe_dir>`."
    - "`trackly_infra::AppConfig::load_or_default(&path)` parses `trackly.config.toml` with the sections `[server]`, `[paths]`, `[logging]`, `[organization]`, applying defaults for any missing field."
  artifacts:
    - path: "crates/trackly-infra/src/paths.rs"
      provides: "`Paths` struct with methods `resolve() -> Result<Paths, AppError>`, `exe_dir() -> &Path`, `db_path() -> &Path`, `config_file() -> &Path`, `webview_data_dir() -> &Path`, `logs_dir() -> &Path`, `is_portable() -> bool`."
      contains: "std::env::current_exe"
    - path: "crates/trackly-infra/src/config.rs"
      provides: "`AppConfig` (serde::Deserialize from TOML) with `[server]` (enabled, host, port, cert_path), `[paths]` (db_path), `[logging]` (level, format, retention_days), `[organization]` (timezone) sections; `load_or_default(&Path) -> Result<AppConfig, AppError>` ignores missing file by returning defaults."
    - path: "crates/trackly-app/src/webview_env.rs"
      provides: "`set_webview2_data_folder(path: &Path)` function that wraps the `unsafe { std::env::set_var }` call with a SAFETY comment; called from main.rs."
    - path: "crates/trackly-app/src/main.rs"
      provides: "Real `main()` following RESEARCH Code Example 1 ordering (paths → set WEBVIEW2 → parse --self-test flag → load config → [tracing setup deferred to Plan 05] → exit on --self-test). Replaces the Plan 01 placeholder."
    - path: "crates/trackly-core/src/error.rs"
      provides: "Minimal `AppError` enum with `Internal { source_chain: String }` and `Validation { field: String, message: String }` variants; bootstrapped here so paths.rs has a typed error to return. Plan 04 extends it with the full D-AppError-01 variant list."
  key_links:
    - from: "crates/trackly-app/src/main.rs (Step 1)"
      to: "trackly_infra::Paths::resolve()"
      via: "fn call before anything else"
      pattern: "trackly_infra::Paths::resolve"
    - from: "crates/trackly-app/src/main.rs (Step 2)"
      to: "crates/trackly-app/src/webview_env.rs set_webview2_data_folder"
      via: "call MUST precede any tokio/tauri/tracing init"
      pattern: "set_webview2_data_folder"
    - from: "crates/trackly-app/src/main.rs (Step 4)"
      to: "trackly_infra::AppConfig::load_or_default"
      via: "loads paths.config_file()"
      pattern: "AppConfig::load_or_default"
---

<objective>
Implement the portable-mode path discipline (`paths.rs`), the TOML config parser (`config.rs`), and the WebView2 environment variable setup that MUST be the first non-trivial statement of `main()`. Replace the Plan 01 placeholder `main.rs` with the real ordered initialization sequence (per RESEARCH Code Example 1), stopping short of tracing / DB / writer setup (those land in Plans 03–05). This plan establishes the **portability invariants** that the ProcMon test in Plan 06 will verify on Windows CI.

**Purpose:** Without `WEBVIEW2_USER_DATA_FOLDER` set before any Tauri call, WebView2 silently writes to `%LOCALAPPDATA%\<app>\EBWebView` — a portable-mode showstopper (Pitfall #1). Without `paths.rs` rooted at `current_exe()`, the clippy ban on `dirs::*_dir()` is hollow. This plan locks both in.

**Output:** `cargo run -p trackly-app -- --self-test` exits 0 after creating only `<exe_dir>/data/webview/` (and no `logs/` or DB yet — those come in Plans 03/05). `paths_test.rs` + `config_test.rs` cover the happy path, the missing-config fallback, the sentinel detection, and the UNC-rejection guard. The unsafe set_var call is wrapped with a SAFETY comment per Pitfall #8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@Cargo.toml
@crates/trackly-infra/Cargo.toml
@crates/trackly-infra/src/lib.rs
@crates/trackly-app/Cargo.toml
@crates/trackly-app/src/main.rs

<interfaces>
<!-- The Paths and AppConfig contracts that Plans 03, 04, 05, 06 will consume. -->

`crates/trackly-infra/src/paths.rs` MUST export:
```
pub struct Paths {
    exe_dir: PathBuf,
    db_path: PathBuf,
    config_file: PathBuf,
    webview_data_dir: PathBuf,
    logs_dir: PathBuf,
    is_portable: bool,
}

impl Paths {
    pub fn resolve() -> Result<Self, trackly_core::error::AppError>;
    pub fn exe_dir(&self) -> &Path;
    pub fn db_path(&self) -> &Path;             // <exe_dir>/trackly.db by default; overridden by config [paths].db_path
    pub fn config_file(&self) -> &Path;         // <exe_dir>/trackly.config.toml
    pub fn webview_data_dir(&self) -> &Path;    // <exe_dir>/data/webview
    pub fn logs_dir(&self) -> &Path;            // <exe_dir>/logs
    pub fn is_portable(&self) -> bool;          // true if sentinel file exists
}
```

`crates/trackly-infra/src/config.rs` MUST export:
```
#[derive(Debug, Deserialize, Default, Clone)]
pub struct AppConfig {
    #[serde(default)] pub server: ServerConfig,
    #[serde(default)] pub paths: PathsConfig,
    #[serde(default)] pub logging: LoggingConfig,
    #[serde(default)] pub organization: OrganizationConfig,
}

#[derive(Debug, Deserialize, Clone)]
pub struct ServerConfig { pub enabled: bool, pub host: String, pub port: u16, pub cert_path: String }
impl Default for ServerConfig { /* enabled=false, host="127.0.0.1", port=8443, cert_path="" */ }

// Similar for PathsConfig { db_path: String /* "" = default */ },
// LoggingConfig { level: String /* "info" */, format: String /* "compact" */, retention_days: u32 /* 14 */ },
// OrganizationConfig { timezone: String /* "Europe/Moscow" */ }.

impl AppConfig {
    pub fn load_or_default(path: &Path) -> Result<Self, trackly_core::error::AppError>;
}
```

`crates/trackly-core/src/error.rs` MUST export (minimal Plan 02 bootstrap; Plan 04 extends to the full D-AppError-01 variant list):
```
#[derive(Debug, thiserror::Error)]
pub enum AppError {
    #[error("internal: {source_chain}")]
    Internal { source_chain: String },
    #[error("validation: {field}: {message}")]
    Validation { field: String, message: String },
}
```

`crates/trackly-app/src/webview_env.rs` MUST export a function whose body has these properties (no fenced code in `<action>`; this is the contract):
- Signature: `pub fn set_webview2_data_folder(path: &Path) -> std::io::Result<()>`.
- Body Step 1: `std::fs::create_dir_all(path)?` — ensures the dir exists before WebView2 looks for it.
- Body Step 2: `unsafe { std::env::set_var("WEBVIEW2_USER_DATA_FOLDER", path); }` — Rust 2024 `set_var` is `unsafe` (Pitfall #8); MUST be wrapped with a `// SAFETY:` comment documenting that the caller guarantees no other thread is reading env vars yet (this is enforced by calling the function as line 2 of `main()`).
- Body Step 3: `Ok(())`.

The literal `unsafe { std::env::set_var("WEBVIEW2_USER_DATA_FOLDER", ...) }` form is part of the contract because `acceptance_criteria` greps for it; do not refactor through a helper that hides the literal.

`crates/trackly-app/src/main.rs` ORDERING (per RESEARCH Code Example 1; Plan 02 owns Steps 1-4 + Step 11 self-test exit; tracing/DB/AppCtx land in later plans). The function MUST execute in this exact order:

1. `let paths = trackly_infra::Paths::resolve()?;`
2. `trackly_app::webview_env::set_webview2_data_folder(paths.webview_data_dir())?;` — FOUND-05, Pitfall #1; MUST be before any thread spawn, tokio runtime, or tauri::Builder call.
3. `let self_test = std::env::args().any(|a| a == "--self-test");`
4. `let config = trackly_infra::AppConfig::load_or_default(paths.config_file())?;`
5. (Plan 05) tracing-subscriber + tracing-appender setup goes here.
6-10. (Plans 03, 04) DB open, migrations, writer worker, AppCtx.
11. If `self_test`: eprintln diagnostic lines covering `paths.exe_dir()`, `paths.db_path()`, `paths.webview_data_dir()`, `paths.is_portable()`, `config.server.enabled`, `config.logging.level`, plus a "Plan 02 placeholder" marker; then `return Ok(())`.
12. Otherwise: eprintln an "UI not yet wired" message (Phase 1 has no tauri::Builder call yet — Plan 05/Phase 2 wires it) and return Ok.

Return type is `anyhow::Result<()>` so `?` propagates across `AppError`, `std::io::Error`, and `toml::de::Error`. Plan 04 will extend Step 5+ with the DB/writer/AppCtx; Plan 05 inserts tracing init between Steps 4 and 5; the placeholder lines are removed in those plans.
</interfaces>

<canonical_refs>
- Pitfall #1 (RESEARCH §Common Pitfalls): WebView2 leak — `set_var` MUST be first non-trivial statement
- Pitfall #3 (RESEARCH §Common Pitfalls): Cyrillic Windows paths — use `&Path`/`PathBuf`, never `&str`; `set_var` on Windows calls SetEnvironmentVariableW which handles UTF-16
- Pitfall #8 (RESEARCH §Common Pitfalls): `std::env::set_var` is `unsafe` since Rust 2024 / 1.83+ — wrap with SAFETY comment
- D-Config-01 (CONTEXT.md): config file is `trackly.config.toml`, sentinel is `portable.txt` OR `trackly.config.toml`
- ARCHITECTURE.md §paths.rs design: portable detection via sentinel, NOT writability probe
- Security §V8 (RESEARCH §Security Domain): paths.rs SHOULD reject UNC paths to avoid SQLite-on-SMB-share corruption
</canonical_refs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `trackly_infra::paths::Paths` with sentinel detection and UNC rejection</name>
  <read_first>
    - crates/trackly-infra/src/lib.rs (current empty stub from Plan 01)
    - .planning/phases/01-foundation/01-CONTEXT.md §D-Config-01 (sentinel rules)
    - .planning/phases/01-foundation/01-RESEARCH.md §Recommended Project Structure (paths.rs lives in trackly-infra/src/), §Code Example 1 (how main.rs calls it), §Common Pitfalls #3 (cyrillic) + Security V8 (UNC rejection)
    - .planning/research/ARCHITECTURE.md (paths.rs design section)
    - CLAUDE.md (portable constraint: "БД и конфиг — рядом с исполняемым файлом")
  </read_first>
  <files>
    crates/trackly-infra/src/paths.rs,
    crates/trackly-infra/src/lib.rs,
    crates/trackly-core/src/error.rs,
    crates/trackly-core/src/lib.rs,
    crates/trackly-infra/tests/paths_test.rs
  </files>
  <behavior>
    - Test 1: `Paths::resolve()` in a tempfile-based fixture (sandbox dir with a fake `trackly.exe` + `portable.txt`) returns `Paths` whose `exe_dir() == <sandbox>`, `db_path() == <sandbox>/trackly.db`, `is_portable() == true`.
    - Test 2: `Paths::resolve()` with no sentinel and no `trackly.config.toml` still returns paths rooted at `<sandbox>` but `is_portable() == false` (Phase 1 accepts both modes; the ProcMon test asserts portability behaviorally).
    - Test 3: `Paths::resolve()` with `trackly.config.toml` (and no portable.txt) returns `is_portable() == true` (sentinel rule is OR).
    - Test 4: Calling `Paths::resolve()` on a constructed UNC-style path (e.g., `\\\\server\\share\\trackly.exe` on Windows, or simulated via injecting a path) returns `Err(AppError::Validation { field, message })` containing the string "UNC" or "SMB share not supported". On non-Windows the UNC check can be a no-op; gate the test with `#[cfg(windows)]`.
    - Test 5: Cyrillic exe_dir path (`%TEMP%/Документы/Учёт/Trackly_<uuid>/`) is accepted: `Paths::resolve()` succeeds and `webview_data_dir()` returns `<cyrillic_dir>/data/webview` (covers Pitfall #3 + success criterion #1 cyrillic invariant; test creates the dir + fake exe via tempfile, then sets the exe path via a test-only `Paths::resolve_for_exe_dir(path: &Path)` helper since `current_exe()` itself can't be redirected in tests).
  </behavior>
  <action>
First bootstrap the minimal `AppError` enum declared in `<interfaces>` so `paths.rs` has a typed error to return:
- `crates/trackly-core/src/lib.rs` adds `pub mod error;`.
- `crates/trackly-core/src/error.rs` declares the two-variant enum (`Internal` + `Validation`) per `<interfaces>` block. Plan 04 will extend this enum with the full D-AppError-01 variant list — do not pre-add variants here.

Then add `pub mod paths;` to `crates/trackly-infra/src/lib.rs` and re-export `pub use paths::Paths;`.

Implement `crates/trackly-infra/src/paths.rs`:

- `pub struct Paths { exe_dir: PathBuf, db_path: PathBuf, config_file: PathBuf, webview_data_dir: PathBuf, logs_dir: PathBuf, is_portable: bool }` (all private fields; accessors are getters).
- `impl Paths`:
  - `pub fn resolve() -> Result<Self, trackly_core::error::AppError>` — get `std::env::current_exe()?` mapped to `AppError::Internal { source_chain: format!("current_exe failed: {e}") }`; take `.parent()` (map None to `AppError::Internal`); then call `Self::resolve_for_exe_dir(exe_dir)`.
  - `pub fn resolve_for_exe_dir(exe_dir: PathBuf) -> Result<Self, AppError>` — test seam. Logic:
    1. UNC rejection (Windows only): if `exe_dir.to_string_lossy().starts_with(r"\\")` → return `Err(AppError::Validation { field: "exe_dir".into(), message: "UNC/SMB path rejected: SQLite WAL does not support network shares".into() })`. Gate with `#[cfg(windows)]`.
    2. Compute `db_path = exe_dir.join("trackly.db")`, `config_file = exe_dir.join("trackly.config.toml")`, `webview_data_dir = exe_dir.join("data/webview")`, `logs_dir = exe_dir.join("logs")`.
    3. `is_portable = exe_dir.join("portable.txt").exists() || config_file.exists()`.
    4. Return `Ok(Self { ... })`.
  - Accessor methods: `pub fn exe_dir(&self) -> &Path { &self.exe_dir }` etc. for all six fields.

Add `trackly-infra/tests/paths_test.rs` with the five behaviors above. Use `tempfile::TempDir` for sandboxing. For the cyrillic test, use `tempdir.path().join("Документы/Учёт/Trackly_test")` (the underlying tempfile is already in `%TEMP%`).
  </action>
  <verify>
    <automated>cargo test -p trackly-infra --test paths_test</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: `cargo test -p trackly-infra --test paths_test` exits 0 with all five behaviors passing.
    - File assertion: `crates/trackly-infra/src/paths.rs` contains `std::env::current_exe()` and `is_portable`.
    - File assertion: `crates/trackly-infra/src/paths.rs` contains the string `UNC` (proves the rejection branch is wired, even if guarded by `#[cfg(windows)]`).
    - File assertion: `crates/trackly-core/src/error.rs` defines `pub enum AppError` with at least `Internal` and `Validation` variants.
    - Behavior assertion: `cargo clippy --workspace --all-targets -- -D warnings` still passes (no `dirs::*_dir` or banned methods introduced).
  </acceptance_criteria>
  <done>`Paths::resolve()` correctly anchors to `current_exe()`, detects portable mode by sentinel, rejects UNC paths on Windows, and handles cyrillic paths. The minimal `AppError` enum is bootstrapped in `trackly-core` for Plan 04 to extend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `trackly_infra::AppConfig` TOML parser with defaults</name>
  <read_first>
    - crates/trackly-infra/src/lib.rs (just updated in Task 1)
    - crates/trackly-infra/src/paths.rs (provides `Paths::config_file()` consumed here)
    - .planning/phases/01-foundation/01-CONTEXT.md §D-Config-01 (full TOML schema)
    - .planning/phases/01-foundation/01-RESEARCH.md §Standard Stack (toml = "0.8")
  </read_first>
  <files>
    crates/trackly-infra/src/config.rs,
    crates/trackly-infra/src/lib.rs,
    crates/trackly-infra/tests/config_test.rs
  </files>
  <behavior>
    - Test 1: `AppConfig::load_or_default(<nonexistent path>)` returns `Ok(AppConfig::default())` (does NOT error on missing file — config is optional per D-Config-01).
    - Test 2: `AppConfig::load_or_default(<valid trackly.config.toml fixture>)` returns the parsed values verbatim (use a fixture with all four sections populated with non-default values).
    - Test 3: `AppConfig::load_or_default(<partial file with only [server] section>)` returns server values from file, defaults for other sections (`#[serde(default)]` on sections).
    - Test 4: `AppConfig::load_or_default(<file with invalid TOML syntax>)` returns `Err(AppError::Validation { field: "trackly.config.toml", message })` with the TOML parser error in `message`.
    - Test 5: `AppConfig::default()` returns server.enabled=false, server.host="127.0.0.1", server.port=8443, logging.level="info", logging.format="compact", logging.retention_days=14, organization.timezone="Europe/Moscow", paths.db_path="" (empty = use Paths::db_path default).
  </behavior>
  <action>
Add `pub mod config;` to `trackly-infra/src/lib.rs` and `pub use config::AppConfig;`.

Implement `crates/trackly-infra/src/config.rs`:

- Use the `toml` 0.8 crate and `serde::Deserialize`.
- Define `AppConfig`, `ServerConfig`, `PathsConfig`, `LoggingConfig`, `OrganizationConfig` per `<interfaces>` block above. Each section `#[derive(Debug, Deserialize, Clone)]` with a hand-written `impl Default` matching D-Config-01's explicit defaults (prefer hand-written `impl Default` for clarity over `#[serde(default = "...")]`).
- `impl AppConfig::load_or_default(path)` behavior:
  - If `std::fs::read_to_string(path)` succeeds: parse with `toml::from_str::<Self>(&contents)`; on parser error map to `AppError::Validation { field: path.file_name().and_then(|s| s.to_str()).unwrap_or("trackly.config.toml").into(), message: format!("TOML parse error: {e}") }`.
  - If `std::fs::read_to_string` returns `ErrorKind::NotFound`: return `Ok(Self::default())`.
  - Any other I/O error: return `AppError::Internal { source_chain: format!("read config: {e}") }`.

Implement `crates/trackly-infra/tests/config_test.rs` with the five tests above. Use `tempfile::NamedTempFile` for fixtures; for the partial-config test, write a TOML body with only the `[server]` section populated and assert paths/logging/organization come from defaults.
  </action>
  <verify>
    <automated>cargo test -p trackly-infra --test config_test</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: `cargo test -p trackly-infra --test config_test` exits 0 with all five behaviors passing.
    - File assertion: `crates/trackly-infra/src/config.rs` contains `#[derive(.*Deserialize.*)]` on `AppConfig`, `ServerConfig`, `PathsConfig`, `LoggingConfig`, `OrganizationConfig`.
    - File assertion: `crates/trackly-infra/src/config.rs` contains `"127.0.0.1"`, `8443`, `"Europe/Moscow"`, `"info"`, `"compact"`, `14` (default values per D-Config-01).
    - Source assertion: `crates/trackly-infra/Cargo.toml` `[dependencies]` includes `toml = { workspace = true }`.
  </acceptance_criteria>
  <done>`AppConfig` parses `trackly.config.toml` per D-Config-01 schema, applies defaults for missing fields/sections, treats a missing file as "use defaults" (not an error), and reports TOML syntax errors as `AppError::Validation`.</done>
</task>

<task type="auto">
  <name>Task 3: Wire `webview_env::set_webview2_data_folder` and replace Plan 01 main.rs placeholder with the real ordered init</name>
  <read_first>
    - crates/trackly-app/src/main.rs (current placeholder from Plan 01)
    - crates/trackly-infra/src/paths.rs, src/config.rs (just created)
    - .planning/phases/01-foundation/01-RESEARCH.md §Code Example 1 (the canonical main.rs ordering — Plan 02 implements Steps 1-4 + Step 11 self-test exit)
    - .planning/phases/01-foundation/01-CONTEXT.md §D-Workspace-01 (binary name is `trackly`)
    - .planning/phases/01-foundation/01-RESEARCH.md §Common Pitfalls #1 (WebView2 leak), #3 (cyrillic paths), #8 (unsafe set_var)
  </read_first>
  <files>
    crates/trackly-app/src/webview_env.rs,
    crates/trackly-app/src/main.rs,
    crates/trackly-app/src/lib.rs
  </files>
  <action>
Implement `crates/trackly-app/src/webview_env.rs` per the contract declared in `<interfaces>` (signature, body steps 1-3, SAFETY comment). The `unsafe { std::env::set_var("WEBVIEW2_USER_DATA_FOLDER", path) }` literal MUST appear verbatim in the file (acceptance criteria grep checks for it). The function compiles unchanged on non-Windows (the env var is harmless; WebView2 is Windows-only); the `create_dir_all` happens regardless.

Update `crates/trackly-app/src/lib.rs`: add `pub mod webview_env;` and `pub mod context;` declared as `pub mod context { /* Plan 04 fills this */ }` empty inline stub so later plans can splice the real module without churn. Keep room for `logging`, `dto`, `tauri_cmds`, `specta_export` modules that Plans 04/05 add.

Replace `crates/trackly-app/src/main.rs` body with the Plan 02 lifecycle defined in `<interfaces>` Steps 1-4 + 11-12. The function returns `anyhow::Result<()>` and uses `?` to propagate. The `set_webview2_data_folder(paths.webview_data_dir())?` call is Step 2 — the critical ordering invariant from Pitfall #1. The self-test branch prints diagnostic lines covering all `paths.*` accessors and the two config fields named in `<interfaces>`, then returns `Ok(())`. The non-self-test branch prints an "UI not yet wired" message and returns Ok (Plan 04 wires AppCtx; Plan 05/Phase 2 wires tauri::Builder::run).

**Compile-time ordering invariant** (manual code-review checklist; a programmatic clippy lint is deferred to Phase 8). The `set_webview2_data_folder` call MUST appear BEFORE any line that:
- Starts a tokio runtime (`tokio::runtime::Builder`, `#[tokio::main]`)
- Spawns a thread (`std::thread::spawn`, `tokio::task::spawn`)
- Calls anything from `tauri::*`
  </action>
  <verify>
    <automated>cargo run -p trackly-app -- --self-test 2>&1 | grep -q "paths resolved" && cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: `cargo run -p trackly-app -- --self-test` exits 0 and prints lines containing `paths resolved: exe_dir=`, `config loaded:`, and `Plan 02 placeholder`.
    - Command assertion: `cargo clippy --workspace --all-targets -- -D warnings` passes (no `dirs::*_dir` or `chrono::Local` introduced; `unsafe { std::env::set_var }` is permitted because we don't ban `std::env::set_var` in clippy.toml).
    - File assertion: `crates/trackly-app/src/webview_env.rs` contains the literal substring `unsafe { std::env::set_var("WEBVIEW2_USER_DATA_FOLDER"`.
    - File assertion: `crates/trackly-app/src/webview_env.rs` contains a `// SAFETY:` comment immediately before the unsafe block.
    - Source ordering assertion (manual review checklist for the executor): in `crates/trackly-app/src/main.rs`, the call to `set_webview2_data_folder` MUST be the second statement of `main()` (after `Paths::resolve()`), and MUST appear before any `tokio::*`, `tauri::*`, `std::thread::spawn`, or `tracing_subscriber::*` call.
    - Behavior assertion: after running `cargo run -p trackly-app -- --self-test` from a sandbox dir, the directory `<sandbox>/data/webview/` exists (created by `create_dir_all`) and no other dirs/files are written. (Plan 06 wraps this in the ProcMon test for Windows CI.)
  </acceptance_criteria>
  <done>`trackly --self-test` runs the Plan 02 lifecycle (paths → WEBVIEW2 env → config) cleanly and exits 0. The critical ordering invariant from Pitfall #1 is enforced in source. Plans 03/04/05 will splice tracing init, DB open, migrations, and AppCtx between Steps 4 and 11 without disturbing the ordering above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied `trackly.config.toml` → Rust runtime | Hand-edited TOML; could contain malformed types, oversized strings, or path values pointing to UNC/SMB shares |
| `current_exe()` resolution → file-system reads/writes | The exe location determines DB, config, log paths; if compromised (e.g., exe symlinked into APPDATA), portable invariant breaks |
| `WEBVIEW2_USER_DATA_FOLDER` env var → WebView2 runtime | If not set, or set to a path inside %LOCALAPPDATA%, WebView2 silently leaks state outside the portable sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | WebView2 writes to `%LOCALAPPDATA%\<app>\EBWebView` instead of `<exe_dir>/data/webview` | mitigate | `set_webview2_data_folder` is called as the SECOND statement of `main()` (after path resolution); Plan 06's ProcMon test catches any regression on Windows CI; the acceptance criterion in Task 3 explicitly checks ordering |
| T-02-02 | Tampering / Denial of Service | SQLite DB on SMB share corrupts due to WAL/UNC incompatibility | mitigate | `Paths::resolve_for_exe_dir` rejects `\\\\` UNC prefixes with `AppError::Validation` on Windows (Security V8 control); test exists in Task 1 behavior list |
| T-02-03 | Tampering | Malformed `trackly.config.toml` (e.g., port = "abc") crashes the app | mitigate | `AppConfig::load_or_default` returns `AppError::Validation` with the file name + TOML parser error; main.rs propagates via `?` and exits non-zero with a clear message (anyhow::Error formatting); test in Task 2 covers this |
| T-02-04 | Information Disclosure | Cyrillic path causes `Path::to_str()` to silently fail or mangle | mitigate | All path APIs in `paths.rs` use `&Path`/`PathBuf` (never `&str`); test in Task 1 explicitly exercises cyrillic dir; `std::env::set_var` on Windows uses `SetEnvironmentVariableW` (UTF-16, handles cyrillic correctly) per Pitfall #3 |
| T-02-05 | Tampering | Future contributor moves the `set_var` call below tokio init, leaking WebView2 to APPDATA again | accept (with mitigation) | Manual code-review checklist (acceptance criterion in Task 3); a programmatic clippy lint to enforce ordering is deferred to Phase 8. ProcMon test in Plan 06 catches the regression behaviorally. Risk accepted because (a) the comment in main.rs is explicit, (b) ProcMon is the authoritative gate |
| T-02-06 | Tampering | `unsafe { set_var }` block triggered concurrently with another thread reading env vars (UB in Rust 2024) | mitigate | The call site is line 2 of `main()`, before any thread spawn or tokio runtime; SAFETY comment documents this invariant. Per Pitfall #8 the unsafe block is forward-compatible with Rust 2024+ semantics |

**Block on `high` severity:** T-02-01 and T-02-02 are `high` and are mitigated by automated test or source-level check. No `high` threats are accepted.
</threat_model>

<verification>
After this plan completes:

1. `cargo test -p trackly-infra --test paths_test` passes all 5 behaviors.
2. `cargo test -p trackly-infra --test config_test` passes all 5 behaviors.
3. `cargo run -p trackly-app -- --self-test` exits 0 and prints diagnostic lines.
4. `cargo clippy --workspace --all-targets -- -D warnings` remains green.
5. After running self-test in a sandbox, only `<sandbox>/data/webview/` is created — no files in `~/Library/Application Support`, `%APPDATA%`, `%LOCALAPPDATA%`, or `~/.config`. (Manually verifiable on macOS; ProcMon-test in Plan 06 automates the Windows assertion.)
6. The ordering invariant `Paths::resolve → set_webview2_data_folder → (everything else)` is visible in `main.rs` source.
</verification>

<success_criteria>
- FOUND-04 satisfied: paths rooted at `current_exe()`, sentinel-based portable detection, banned-methods (dirs::*) enforced by Plan 01's clippy.toml.
- FOUND-05 satisfied: `WEBVIEW2_USER_DATA_FOLDER` set before any Tauri/tokio call.
- FOUND-11 partially satisfied: source-level ordering invariant in place; Plan 06's ProcMon test gives the behavioral CI proof on Windows.
- `paths.rs` API stable for Plans 03/04/05/06 to consume.
- `AppConfig` API stable; Plans 05 (logging) and 06 (server mode) read fields without further changes to the parser.
- Minimal `AppError` enum exists in `trackly-core`; Plan 04 extends it.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done, documenting:
- The chosen UNC-rejection regex/check (e.g., `starts_with(r"\\")` vs richer parsing).
- Any deviations from RESEARCH Code Example 1's `main.rs` (e.g., chose to defer tokio runtime construction to Plan 04 entirely vs. constructing it in Plan 02).
- Whether the executor needed to extend the minimal `AppError` enum beyond `Internal`/`Validation` (Plan 04 will extend it to the full D-AppError-01 variant list).
- Confirmation that the cyrillic path test passed on the dev macOS host.
</output>
