---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - rustfmt.toml
  - clippy.toml
  - deny.toml
  - .gitignore
  - .editorconfig
  - crates/trackly-core/Cargo.toml
  - crates/trackly-core/src/lib.rs
  - crates/trackly-core/tests/no_io_deps.rs
  - crates/trackly-infra/Cargo.toml
  - crates/trackly-infra/src/lib.rs
  - crates/trackly-app/Cargo.toml
  - crates/trackly-app/src/main.rs
  - crates/trackly-app/src/lib.rs
  - crates/trackly-app/build.rs
  - crates/trackly-app/tauri.conf.json
  - crates/trackly-app/icons/.gitkeep
  - tools/procmon-check/Cargo.toml
  - tools/procmon-check/src/main.rs
  - ui/package.json
  - ui/vite.config.ts
  - ui/tsconfig.json
  - ui/svelte.config.js
  - ui/index.html
  - ui/src/main.ts
  - ui/src/App.svelte
  - ui/src/styles/_tokens.scss
  - ui/.eslintrc.cjs
  - ui/.prettierrc.json
  - .github/workflows/ci-fast.yml
  - .github/workflows/cargo-deny.yml
autonomous: true
requirements:
  - FOUND-01
  - FOUND-09
  - BLD-01
must_haves:
  truths:
    - "`cargo build --workspace` succeeds on a clean checkout."
    - "`cargo clippy --workspace --all-targets -- -D warnings` succeeds with the disallowed-methods list active."
    - "`cargo fmt --all -- --check` succeeds."
    - "`cargo test -p trackly-core --test no_io_deps` succeeds, proving `trackly-core` does NOT pull `tokio`, `rusqlite`, `tauri`, or any I/O crate transitively."
    - "`cd ui && pnpm install --frozen-lockfile && pnpm svelte-check && pnpm lint` succeeds in CI."
    - "GitHub Actions `ci-fast.yml` triggers on push to any branch and on pull_request, running all of the above on `ubuntu-latest`."
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace root declaring `[workspace]` with `crates/trackly-core`, `crates/trackly-infra`, `crates/trackly-app`, and `tools/procmon-check` members; resolver = '2'; shared `[workspace.dependencies]` pinned versions including `axum`, `tower`, `tower-http` so Plan 05 can consume via `workspace = true` without re-declaring."
    - path: "rust-toolchain.toml"
      contains: 'channel = "1.85"'
    - path: "clippy.toml"
      contains: "disallowed-methods"
    - path: "crates/trackly-core/Cargo.toml"
      provides: "Pure-domain crate; deps list MUST NOT contain tokio, rusqlite, tauri, axum, hyper, tower, reqwest, sqlx; allowed: serde, time, thiserror, zeroize, async-trait (only if needed), specta."
    - path: "crates/trackly-infra/Cargo.toml"
      provides: "Infra crate; deps include rusqlite=0.39 (bundled), refinery=0.8 (rusqlite feature), tokio=1, time, serde, serde_json, serde_with, toml, thiserror, tracing, tempfile (dev), trackly-core (workspace)."
    - path: "crates/trackly-app/Cargo.toml"
      provides: "Bin crate `trackly`; deps include tauri=2, tauri-build=2 (build dep), tauri-specta='=2.0.0-rc.21', specta='=2.0.0-rc.22', tracing-subscriber, tracing-appender, tokio, tokio_util, anyhow, trackly-core, trackly-infra; dev-dep specta-typescript=0.0.9."
    - path: "tools/procmon-check/Cargo.toml"
      provides: "Windows-only utility crate; deps gated by `[target.'cfg(windows)'.dependencies]`."
    - path: "ui/package.json"
      provides: "Svelte 5 + Vite 6 + svelte-check + eslint + prettier; `prebuild` script `cargo test -p trackly-app --test export_bindings`; `lint` script `eslint . --ext .ts,.svelte`."
    - path: ".github/workflows/ci-fast.yml"
      provides: "fmt + clippy + cargo test + pnpm install/svelte-check/lint on ubuntu-latest; triggers on `push: branches: ['**']` and `pull_request`."
    - path: ".github/workflows/cargo-deny.yml"
      provides: "Daily cron (`schedule: '0 6 * * *'`) running `cargo deny check`."
    - path: ".gitignore"
      contains: "ui/src/bindings.ts"
  key_links:
    - from: "Cargo.toml [workspace]"
      to: "crates/trackly-{core,infra,app}/Cargo.toml + tools/procmon-check/Cargo.toml"
      via: "workspace members list"
    - from: "crates/trackly-core/tests/no_io_deps.rs"
      to: "crates/trackly-core/Cargo.toml dependency closure"
      via: "spawns `cargo tree -p trackly-core --prefix none` and asserts forbidden crate names are absent"
      pattern: "tokio|rusqlite|tauri|axum|hyper|tower|reqwest|sqlx"
    - from: "clippy.toml `disallowed-methods`"
      to: "every workspace crate (clippy reads workspace-root clippy.toml)"
      via: "`cargo clippy --workspace --all-targets -- -D warnings` step in `ci-fast.yml`"
    - from: "ui/package.json `prebuild`"
      to: "`cargo test -p trackly-app --test export_bindings` (created in Plan 05)"
      via: "npm prebuild lifecycle; this plan only declares the script entry"
---

<objective>
Scaffold the Cargo workspace, three Rust crates, Windows-only ProcMon utility crate, Svelte 5 / Vite 6 `ui/` project, all lint / formatter / dependency-audit configuration files, and the `ci-fast.yml` + `cargo-deny.yml` GitHub Actions workflows. **No business logic, no DB code, no Tauri commands** in this plan — only the empty crates with `Cargo.toml`, minimal `lib.rs` / `main.rs` stubs that compile, and the cross-cutting tooling configuration that locks in every guard-rail (clippy disallowed-methods, rustfmt, Rust 1.85 MSRV, gitignore for generated bindings, cargo-deny advisories, CI fast workflow).

**Purpose:** Establishes the entire workspace topology and CI guard-rails before any plan touches schema or runtime code, so every subsequent plan in Phase 1 (and every Phase 2+ plan) inherits the right boundaries and gets immediate `cargo clippy -- -D warnings` feedback on each commit.

**Output:** A fresh checkout runs `cargo build --workspace` green, `cargo clippy --workspace --all-targets -- -D warnings` green, `cargo fmt --all -- --check` green, and (after `pnpm install`) `pnpm svelte-check && pnpm lint` green. The first push triggers `ci-fast.yml` and all jobs pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-SKELETON.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- This plan creates the workspace contract that every later plan consumes. Key invariants: -->

`Cargo.toml` (workspace root) MUST declare:
```
[workspace]
resolver = "2"
members = ["crates/trackly-core", "crates/trackly-infra", "crates/trackly-app", "tools/procmon-check"]

[workspace.package]
edition = "2021"
rust-version = "1.85"
version = "0.1.0"
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# Pin every shared dep at the workspace level; member crates use `workspace = true`.
# Locked versions (from CONTEXT.md D-Workspace + RESEARCH.md Standard Stack):
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_with = { version = "3", features = ["macros", "time_0_3"] }
time = { version = "0.3", features = ["serde", "macros", "formatting", "parsing"] }
thiserror = "2"
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }
tracing-appender = "0.2"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "sync", "fs", "net", "time"] }
tokio_util = { version = "0.7", features = ["sync"] }
rusqlite = { version = "0.39", features = ["bundled", "chrono", "serde_json", "backup"] }
refinery = { version = "0.8", features = ["rusqlite"] }
toml = "0.8"
tempfile = "3"
zeroize = { version = "1", features = ["derive"] }
async-trait = "0.1"
specta = "=2.0.0-rc.22"
tauri = { version = "2", features = ["devtools"] }
tauri-build = "2"
tauri-specta = { version = "=2.0.0-rc.21", features = ["typescript", "derive"] }
specta-typescript = "0.0.9"
axum = { version = "0.8", default-features = false, features = ["http1", "json", "tokio", "macros"] }
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.6", features = ["trace", "cors"] }
```

`clippy.toml` (workspace root) MUST contain (verbatim from D-CI-02):
```
disallowed-methods = [
  { path = "dirs::data_dir", reason = "portable mode: use trackly_infra::paths" },
  { path = "dirs::data_local_dir", reason = "portable mode: use trackly_infra::paths" },
  { path = "dirs::config_dir", reason = "portable mode: use trackly_infra::paths" },
  { path = "dirs::cache_dir", reason = "portable mode: use trackly_infra::paths" },
  { path = "dirs::home_dir", reason = "portable mode: use trackly_infra::paths" },
  { path = "tauri::Manager::path", reason = "use trackly_infra::paths, not tauri's path resolver" },
  { path = "chrono::Local::now", reason = "UTC only; use time::OffsetDateTime::now_utc" },
  { path = "chrono::offset::Local::now", reason = "UTC only" },
  { path = "std::fs::copy", reason = "for DB backup use rusqlite::backup::Backup; otherwise OK in tests" },
]
disallowed-types = [
  { path = "chrono::DateTime<chrono::Local>", reason = "UTC only" },
]
```

`rust-toolchain.toml`:
```
[toolchain]
channel = "1.85"
components = ["rustfmt", "clippy"]
```

`crates/trackly-core/Cargo.toml` MUST list ONLY: serde, serde_json (only if domain DTOs needed), time, thiserror, zeroize, async-trait (optional), specta. **No tokio, no rusqlite, no tauri, no axum.** The `no_io_deps.rs` test enforces this at CI time.

`ui/package.json` MUST declare dependencies: svelte ^5.55, @sveltejs/vite-plugin-svelte ^4, vite ^6, svelte-check ^4, svelte-preprocess ^6, sass ^1, typescript ^5; devDeps: eslint ^9, eslint-plugin-svelte, prettier ^3, prettier-plugin-svelte; scripts: `dev` = `vite`, `build` = `vite build`, `prebuild` = `cargo test -p trackly-app --test export_bindings`, `svelte-check` = `svelte-check --tsconfig ./tsconfig.json`, `lint` = `eslint . --ext .ts,.svelte && prettier --check .`.

`.gitignore` MUST contain (at minimum): `target/`, `ui/node_modules/`, `ui/dist/`, `ui/src/bindings.ts`, `**/*.tmp`, `*.pml`, `*.pmc.bak`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cargo workspace root, toolchain, lint, gitignore, deny config</name>
  <read_first>
    - CLAUDE.md (stack pins + "What NOT to Use" table)
    - .planning/phases/01-foundation/01-CONTEXT.md §D-Workspace-01, §D-CI-02
    - .planning/phases/01-foundation/01-RESEARCH.md §Recommended Project Structure, §Standard Stack
    - .planning/research/STACK.md (version compatibility table)
  </read_first>
  <files>
    Cargo.toml,
    rust-toolchain.toml,
    rustfmt.toml,
    clippy.toml,
    deny.toml,
    .gitignore,
    .editorconfig
  </files>
  <action>
Create the workspace root files at the repo root.

**`Cargo.toml`:** Workspace declaration as shown in the `<interfaces>` block above. Use `resolver = "2"`. Declare members `crates/trackly-core`, `crates/trackly-infra`, `crates/trackly-app`, `tools/procmon-check`. Add `[workspace.package]` (edition `2021`, rust-version `1.85`, version `0.1.0`, license `MIT OR Apache-2.0`). Add `[workspace.dependencies]` pinning every shared dep verbatim from the version table in `<interfaces>` — including `axum`, `tower`, `tower-http` so Plan 05 can reference via `workspace = true` without re-declaring versions. Add `[workspace.lints.clippy]` section that escalates the disallowed lints to deny: `disallowed_methods = "deny"`, `disallowed_types = "deny"`. Then add `[lints]` workspace-true to each member Cargo.toml in subsequent tasks. Do not invent versions — use exactly the pinned strings above (per D-Workspace + RESEARCH).

**`rust-toolchain.toml`:** As shown in `<interfaces>` (channel 1.85, components rustfmt + clippy).

**`rustfmt.toml`:** Empty file (per RESEARCH Open Question #4 — accept defaults; planner explicitly chose this). Just contains a single comment line: `# accept defaults; revisit if review preferences emerge`.

**`clippy.toml`:** Verbatim from `<interfaces>` (D-CI-02 disallowed-methods + disallowed-types lists). No additions, no omissions.

**`deny.toml`:** Standard `cargo-deny` config with sections `[advisories]` (vulnerability = "deny", unmaintained = "warn", yanked = "deny", notice = "warn"), `[licenses]` (allow MIT/Apache-2.0/BSD-3-Clause/ISC/Unicode-DFS-2016/MPL-2.0; deny GPL-3.0 / AGPL-3.0), `[bans]` (multiple-versions = "warn", wildcards = "deny"), `[sources]` (unknown-registry = "deny", unknown-git = "deny"). Use the cargo-deny `cargo deny init` defaults adapted for these license choices.

**`.gitignore`:** Include `target/`, `ui/node_modules/`, `ui/dist/`, `ui/src/bindings.ts` (generated by tauri-specta — see Plan 05), `**/.DS_Store`, `**/*.tmp`, `*.pml`, `*.swp`, plus the standard Rust/Node ignores (`Cargo.lock` stays — bin crate; `pnpm-debug.log*`, `npm-debug.log*`, `.vite/`, `.svelte-kit/`).

**`.editorconfig`:** `root = true`; for `*` set `indent_style = space`, `indent_size = 4`, `end_of_line = lf`, `charset = utf-8`, `trim_trailing_whitespace = true`, `insert_final_newline = true`. For `*.{md,yml,yaml,json,toml,svelte,ts,js,scss}` set `indent_size = 2`.
  </action>
  <verify>
    <automated>cargo metadata --no-deps --format-version 1 | python3 -c "import sys, json; m = json.load(sys.stdin); members = [p['name'] for p in m['packages']]; assert set(members) == {'trackly-core', 'trackly-infra', 'trackly-app', 'procmon-check'}, f'Expected 4 workspace members, got {members}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `Cargo.toml` contains `[workspace]` and lists all four members.
    - File assertion: `Cargo.toml` `[workspace.dependencies]` contains `axum`, `tower`, `tower-http` entries (so Plan 05 can consume via `workspace = true`).
    - File assertion: `clippy.toml` contains the string `disallowed-methods = [` and at least 9 entries (5 dirs::*, 1 tauri::Manager::path, 2 chrono::*::Local, 1 std::fs::copy).
    - File assertion: `rust-toolchain.toml` contains `channel = "1.85"`.
    - File assertion: `.gitignore` contains the literal line `ui/src/bindings.ts`.
    - Command assertion: `cargo metadata --no-deps` exits 0 (proves workspace parses cleanly).
    - Command assertion: `rustup show active-toolchain` reports `1.85.x` (proves rust-toolchain.toml is honored — may require `rustup install 1.85` first; CI handles via `dtolnay/rust-toolchain@stable` with `toolchain: '1.85'`).
  </acceptance_criteria>
  <done>Workspace skeleton compiles to "no targets" successfully; clippy reads the disallowed-methods list (verified in Task 2 when the first source code exists); .gitignore prevents accidental commit of generated bindings.</done>
</task>

<task type="auto">
  <name>Task 2: Create the three Rust crates (trackly-core, trackly-infra, trackly-app), the procmon-check stub crate, and a `no_io_deps.rs` invariant test</name>
  <read_first>
    - Cargo.toml (workspace root, just created in Task 1)
    - .planning/phases/01-foundation/01-CONTEXT.md §D-Workspace-01
    - .planning/phases/01-foundation/01-RESEARCH.md §Recommended Project Structure (the directory tree under `trackly/`), §Architectural Responsibility Map
    - CLAUDE.md (stack version pins for tauri 2, tauri-build, tauri-plugin-* — Phase 1 only needs `tauri-plugin-single-instance` per RESEARCH Open Question #2)
  </read_first>
  <files>
    crates/trackly-core/Cargo.toml,
    crates/trackly-core/src/lib.rs,
    crates/trackly-core/tests/no_io_deps.rs,
    crates/trackly-infra/Cargo.toml,
    crates/trackly-infra/src/lib.rs,
    crates/trackly-app/Cargo.toml,
    crates/trackly-app/src/main.rs,
    crates/trackly-app/src/lib.rs,
    crates/trackly-app/build.rs,
    crates/trackly-app/tauri.conf.json,
    crates/trackly-app/icons/.gitkeep,
    tools/procmon-check/Cargo.toml,
    tools/procmon-check/src/main.rs
  </files>
  <action>
Create each crate's `Cargo.toml` with `[lints] workspace = true` so they inherit the workspace clippy gates. Use `workspace = true` for every shared dependency. **Use the exact versions from `[workspace.dependencies]` declared in Task 1 — never re-declare versions in member crates.**

**`crates/trackly-core/Cargo.toml`:** `name = "trackly-core"`, `[lib]`, dependencies: `serde = { workspace = true }`, `serde_json = { workspace = true }` (optional via `dep:serde_json`), `time = { workspace = true }`, `thiserror = { workspace = true }`, `zeroize = { workspace = true }`, `async-trait = { workspace = true }` (only if a trait below needs it; Plan 04 decides). **Forbidden:** tokio, rusqlite, tauri, axum, hyper, tower, reqwest, sqlx. Per RESEARCH Architectural Responsibility Map, core is pure-domain.

**`crates/trackly-core/src/lib.rs`:** Empty module declaration: `//! trackly-core — pure domain layer.` plus stub `pub mod error;` etc. left for Plan 04. For now just the doc comment + `#![forbid(unsafe_code)]`.

**`crates/trackly-core/tests/no_io_deps.rs`:** Integration test that shells out to `cargo tree -p trackly-core --prefix none --edges no-build,no-dev` and asserts the output does NOT contain any of: `tokio`, `rusqlite`, `tauri`, `axum`, `hyper`, `tower`, `reqwest`, `sqlx`, `libsqlite3-sys`. Use `std::process::Command::new(env!("CARGO")).args(["tree", "-p", "trackly-core", "--prefix", "none", "--edges", "no-build,no-dev"])`. On non-zero exit or any forbidden substring found, panic with a clear message including the offending dep. This test enforces FOUND-01 at every CI run.

**`crates/trackly-infra/Cargo.toml`:** `name = "trackly-infra"`, `[lib]`, deps include rusqlite, refinery, tokio, tokio_util, time, serde, serde_json, serde_with, toml, thiserror, tracing, zeroize, async-trait (if needed), trackly-core (path = `../trackly-core`); dev-deps: tempfile.

**`crates/trackly-infra/src/lib.rs`:** `//! trackly-infra — adapters and I/O.` plus stub `pub mod paths;` etc. left for Plan 02/03. For now just the doc comment.

**`crates/trackly-app/Cargo.toml`:** `name = "trackly-app"`, both `[lib]` and `[[bin]]` with `name = "trackly"`, `path = "src/main.rs"` (per RESEARCH Code Example 3 note that the test binary needs `[lib]` to import `trackly_app::*`). Deps: tauri (workspace), tauri-specta, specta, tracing, tracing-subscriber, tracing-appender, tokio, tokio_util, anyhow, time, serde, serde_json, thiserror, trackly-core (path), trackly-infra (path), tauri-plugin-single-instance = "2" (per RESEARCH Open Question #2 recommendation: add it; Plan 02 wires the spike-test). Build-dep: tauri-build = { workspace = true }. Dev-deps: specta-typescript, tempfile, anyhow.

**`crates/trackly-app/build.rs`:** Standard Tauri build script: `fn main() { tauri_build::build() }`.

**`crates/trackly-app/tauri.conf.json`:** Minimal Tauri 2 config: productName `Trackly`, version `0.1.0`, identifier `org.trackly.app`, build.frontendDist `../../ui/dist`, build.devUrl `http://localhost:1420`, build.beforeDevCommand `pnpm --dir ../../ui dev`, build.beforeBuildCommand `pnpm --dir ../../ui build`, app.windows = `[{ title = "Trackly", width = 1280, height = 800 }]`, bundle.active false in Phase 1 (Phase 8 turns on NSIS/DMG bundling), plugins = `{}` empty. Icons array points to `icons/`.

**`crates/trackly-app/icons/.gitkeep`:** Empty placeholder so the directory is checked in. Real icons land in Phase 8. Tauri will warn about missing icons during dev — acceptable for Phase 1.

**`crates/trackly-app/src/lib.rs`:** `//! trackly-app library surface for tests + bin reuse.` plus stub modules `pub mod context;` etc. (empty stubs that compile, real impl lives in Plans 02/04/05). For Plan 01 just the doc comment.

**`crates/trackly-app/src/main.rs`:** Minimal `fn main() -> anyhow::Result<()> { let self_test = std::env::args().any(|a| a == "--self-test"); if self_test { println!("trackly self-test placeholder — Plan 02 wires paths, Plan 03 wires migrations, Plan 04 wires AppCtx"); return Ok(()); } println!("trackly Phase 1 scaffold — no UI yet"); Ok(()) }`. **Do NOT call tauri::Builder yet** — that requires `WEBVIEW2_USER_DATA_FOLDER` set first (Plan 02). For Plan 01 the binary exists but is a placeholder so `cargo build --release -p trackly-app` succeeds in CI.

**`tools/procmon-check/Cargo.toml`:** `name = "procmon-check"`. Bin only. Dependencies block uses `[target.'cfg(windows)'.dependencies]` for `anyhow = "1"`, `serde = "1"`, `uuid = { version = "1", features = ["v4"] }`. Per RESEARCH: this crate must compile on non-Windows hosts (Cargo treats target-gated deps as inactive on macOS/Linux); on non-Windows the `main.rs` prints "procmon-check is Windows-only; skipping" and exits 0 — this lets `cargo build --workspace` succeed on dev macOS.

**`tools/procmon-check/src/main.rs`:** Placeholder that compiles cross-platform:
- Non-windows: `#[cfg(not(windows))] fn main() { println!("procmon-check is Windows-only; skipping on this host"); std::process::exit(0); }`
- Windows: `#[cfg(windows)] fn main() { println!("procmon-check Phase 1 scaffold — real implementation lands in Plan 06"); std::process::exit(0); }`

(Plan 06 fills in the real ProcMon orchestration.)
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -20 && cargo test -p trackly-core --test no_io_deps</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: `cargo build --workspace` exits 0 on the dev macOS host (proves all four crates compile, including procmon-check via the cross-platform cfg gate).
    - Command assertion: `cargo test -p trackly-core --test no_io_deps` exits 0 (proves trackly-core has zero I/O deps in its closure).
    - File assertion: `crates/trackly-core/Cargo.toml` does NOT contain the substrings `tokio`, `rusqlite`, `tauri`, `axum`, `hyper`, `tower`, `reqwest`, `sqlx` (grep returns 0 matches when filtered for non-comment lines via `grep -v '^#' | grep -c <token>`).
    - File assertion: `crates/trackly-app/Cargo.toml` contains `tauri-specta = { workspace = true }` and `tauri-plugin-single-instance = "2"`.
    - File assertion: `crates/trackly-app/tauri.conf.json` contains `"frontendDist": "../../ui/dist"`.
    - Command assertion: `cargo run -p trackly-app -- --self-test` exits 0 and prints the placeholder message (proves the bin can be invoked; full lifecycle implementation lands in Plans 02/04).
  </acceptance_criteria>
  <done>All four workspace crates compile on the dev macOS host. `trackly-core` proven I/O-free via integration test. `trackly` binary is invokable as `trackly --self-test` (placeholder output). Plan 02 will replace the main.rs placeholder with the real ordered init from RESEARCH Code Example 1.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the `ui/` Svelte 5 + Vite 6 project with svelte-check, eslint, prettier; pin pnpm-lock</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md §D-Workspace-01 (ui/ at repo root)
    - .planning/phases/01-foundation/01-RESEARCH.md §Recommended Project Structure (ui/ subtree), §Standard Stack (Svelte 5.55+, Vite 6.x)
    - CLAUDE.md (Svelte 5 runes, vitePreprocess + scss preprocessor pattern)
    - .gitignore (just created in Task 1 — confirms ui/src/bindings.ts is ignored)
  </read_first>
  <files>
    ui/package.json,
    ui/vite.config.ts,
    ui/tsconfig.json,
    ui/svelte.config.js,
    ui/index.html,
    ui/src/main.ts,
    ui/src/App.svelte,
    ui/src/styles/_tokens.scss,
    ui/.eslintrc.cjs,
    ui/.prettierrc.json,
    ui/pnpm-lock.yaml
  </files>
  <action>
Initialize the `ui/` workspace. Use `pnpm init` semantics — write the files directly rather than running scaffolding tools (so the executor has full control over exact versions and scripts).

**`ui/package.json`:** Type `module`. Scripts as per `<interfaces>` block. Dependencies: `svelte ^5.55`, `@sveltejs/vite-plugin-svelte ^4`, `vite ^6`, `typescript ^5`, `tslib ^2`, `sass ^1`, `svelte-preprocess ^6`. DevDependencies: `svelte-check ^4`, `eslint ^9`, `@typescript-eslint/parser ^8`, `@typescript-eslint/eslint-plugin ^8`, `eslint-plugin-svelte ^2`, `prettier ^3`, `prettier-plugin-svelte ^3`. `engines.node = ">=20"`, `packageManager = "pnpm@9.x.x"` (pin to a specific 9.x release the executor selects via `pnpm --version` at the time of execution).

**`ui/vite.config.ts`:** Use `defineConfig` from `vite`; import `svelte` and `vitePreprocess` from `@sveltejs/vite-plugin-svelte`; configure the svelte plugin with `preprocess: vitePreprocess({ scss: { prependData: '@use "src/styles/_tokens.scss" as *;' } })`; set `server.port = 1420`, `server.strictPort = true`, `clearScreen: false`, `envPrefix: ['VITE_', 'TAURI_']`. The design-tokens auto-import via `prependData` is required (per CLAUDE.md SCSS setup).

**`ui/svelte.config.js`:** Re-export the same preprocess config so `svelte-check` picks it up: import `vitePreprocess` from `@sveltejs/vite-plugin-svelte`, export default `{ preprocess: vitePreprocess({ scss: { prependData: '@use "src/styles/_tokens.scss" as *;' } }) }`.

**`ui/tsconfig.json`:** Strict TS, `moduleResolution: "bundler"`, target ES2022, include `src/**/*` and `src/**/*.svelte`. Reference the `svelte` types via `"types": ["svelte"]`.

**`ui/index.html`:** Minimal — `<div id="app"></div>` + `<script type="module" src="/src/main.ts"></script>`. Title "Trackly".

**`ui/src/main.ts`:** Minimal Svelte 5 mount: import `mount` from `svelte`, import `App` from `./App.svelte`, call `mount(App, { target: document.getElementById('app')! })`, export default the result.

**`ui/src/App.svelte`:** Trivial placeholder per FOUND scope (no UI in Phase 1). Script tag `lang="ts"` with a `$props()` rune destructure (proves Svelte 5 compiles). Template renders `<main><h1>Trackly — Фундамент</h1></main>`. Style block `lang="scss"` with `main { padding: 2rem; font-family: system-ui, sans-serif; }`. The `$props()` rune is the gate that confirms Svelte 5 (not Svelte 4) syntax compiles.

**`ui/src/styles/_tokens.scss`:** Single line with a placeholder token: `$placeholder: true;` (Phase 2 fills in real design tokens). Without this file the `prependData` directive fails.

**`ui/.eslintrc.cjs`:** Flat-style config disabled (eslint 9 supports both; use legacy for simpler setup): parser `@typescript-eslint/parser`, plugins `['@typescript-eslint', 'svelte']`, extends `['eslint:recommended', 'plugin:@typescript-eslint/recommended', 'plugin:svelte/recommended']`, overrides for `*.svelte` files using `svelte-eslint-parser` with `parser: '@typescript-eslint/parser'`.

**`ui/.prettierrc.json`:** `{ "tabWidth": 2, "singleQuote": true, "semi": true, "plugins": ["prettier-plugin-svelte"], "overrides": [{ "files": "*.svelte", "options": { "parser": "svelte" } }] }`.

**`ui/pnpm-lock.yaml`:** Generated by running `cd ui && pnpm install` once locally. Commit the resulting lockfile so CI `pnpm install --frozen-lockfile` succeeds deterministically.

After writing all files, run `cd ui && pnpm install` to generate `pnpm-lock.yaml`, then `pnpm svelte-check && pnpm lint` to confirm the scaffold passes locally before commit.
  </action>
  <verify>
    <automated>cd ui && pnpm install --frozen-lockfile && pnpm svelte-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Command assertion: `cd ui && pnpm install --frozen-lockfile` exits 0 (proves pnpm-lock.yaml matches package.json).
    - Command assertion: `cd ui && pnpm svelte-check` exits 0 with no errors and no warnings on App.svelte.
    - Command assertion: `cd ui && pnpm lint` exits 0.
    - File assertion: `ui/package.json` `scripts.prebuild` exactly equals `"cargo test -p trackly-app --test export_bindings"`.
    - File assertion: `ui/vite.config.ts` contains the string `@use "src/styles/_tokens.scss" as *;`.
    - File assertion: `ui/src/App.svelte` uses the `$props()` rune (proves Svelte 5 not Svelte 4 syntax).
    - File exists: `ui/pnpm-lock.yaml`.
  </acceptance_criteria>
  <done>`ui/` project installs, type-checks, and lints cleanly. Vite is ready to serve. The `prebuild` hook is declared but harmless until Plan 05 creates the `export_bindings.rs` test it depends on (until then, running `pnpm build` would fail — but Phase 1 has no need to run `pnpm build`; only `pnpm svelte-check && pnpm lint` are required by CI).</done>
</task>

<task type="auto">
  <name>Task 4: Author `.github/workflows/ci-fast.yml` and `cargo-deny.yml`</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md §D-CI-01
    - .planning/phases/01-foundation/01-RESEARCH.md §Code Example 7 (ci-fast.yml verbatim recipe)
    - Cargo.toml, ui/package.json (verify the commands referenced below match what's available)
    - deny.toml (just created in Task 1)
  </read_first>
  <files>
    .github/workflows/ci-fast.yml,
    .github/workflows/cargo-deny.yml
  </files>
  <action>
**`.github/workflows/ci-fast.yml`:** Use the recipe from RESEARCH Code Example 7 verbatim, adapted for the workspace structure:
- Name `ci-fast`
- Triggers: `push: branches: ['**']` and `pull_request:`
- Single job `fast` on `ubuntu-latest`
- Steps in order:
  1. `actions/checkout@v4`
  2. `dtolnay/rust-toolchain@stable` with `toolchain: '1.85'`, `components: clippy, rustfmt`
  3. `Swatinem/rust-cache@v2`
  4. `pnpm/action-setup@v3` with `version: 9`
  5. `actions/setup-node@v4` with `node-version: '20'`, `cache: 'pnpm'`, `cache-dependency-path: ui/pnpm-lock.yaml`
  6. `cargo fmt --all -- --check`
  7. `cargo clippy --workspace --all-targets -- -D warnings`
  8. `cargo test --workspace --no-fail-fast`
  9. `cd ui && pnpm install --frozen-lockfile`
  10. `cd ui && pnpm svelte-check`
  11. `cd ui && pnpm lint`

Use `concurrency: { group: 'ci-fast-${{ github.ref }}', cancel-in-progress: true }` so duplicate pushes don't waste minutes.

**`.github/workflows/cargo-deny.yml`:**
- Name `cargo-deny`
- Triggers: `schedule: - cron: '0 6 * * *'` (daily at 06:00 UTC) and `workflow_dispatch:` (manual trigger)
- Single job on `ubuntu-latest`
- Steps:
  1. `actions/checkout@v4`
  2. `dtolnay/rust-toolchain@stable` with `toolchain: '1.85'`
  3. `EmbarkStudios/cargo-deny-action@v2` (the maintained official action) with default checks (`advisories`, `licenses`, `bans`, `sources`).

Do NOT add `ci-full.yml` here — that lands in Plan 06 alongside the ProcMon runner.
  </action>
  <verify>
    <automated>python3 -c "import yaml; ci = yaml.safe_load(open('.github/workflows/ci-fast.yml')); jobs = ci.get('jobs', {}); assert 'fast' in jobs, 'missing fast job'; steps = jobs['fast'].get('steps', []); cmds = [s.get('run', '') for s in steps if 'run' in s]; assert any('cargo fmt --all -- --check' in c for c in cmds), 'missing fmt'; assert any('cargo clippy --workspace --all-targets -- -D warnings' in c for c in cmds), 'missing clippy'; assert any('cargo test --workspace --no-fail-fast' in c for c in cmds), 'missing test'; assert any('pnpm svelte-check' in c for c in cmds), 'missing svelte-check'; assert any('pnpm lint' in c for c in cmds), 'missing lint'; print('ci-fast OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `.github/workflows/ci-fast.yml` triggers on `push: branches: ['**']` and `pull_request`.
    - File assertion: `.github/workflows/ci-fast.yml` runs all five gates: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace --no-fail-fast`, `pnpm svelte-check`, `pnpm lint` (in any order, but all present).
    - File assertion: `.github/workflows/cargo-deny.yml` has `schedule: - cron: '0 6 * * *'`.
    - Behavior assertion: After committing this plan, the next push to a feature branch triggers `ci-fast` and it must pass green (all crates compile, clippy clean, tests pass, UI lints pass).
    - Command assertion: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci-fast.yml'))"` exits 0 (proves valid YAML).
  </acceptance_criteria>
  <done>CI fast workflow is green on first push; cargo-deny is on a daily schedule and manually triggerable. Plan 06 will add `ci-full.yml` (the matrix + Windows ProcMon job) once the runnable `trackly --self-test` binary exists (Plans 02–05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source repo → GitHub Actions runners | All CI steps execute against contributor pushes; supply-chain compromise via a malicious crate or action enters here |
| Workspace `Cargo.lock` → installed dependency closure | Cargo resolves the dependency graph from `[workspace.dependencies]`; a typosquat or malicious version pin lands as compiled code |
| pnpm-lock → installed npm dependency closure | Same exposure on the JS side |
| Phase 1 source code → all later phases | Architectural decisions (e.g., `Secret<T>`, single-writer pattern, paths.rs) cannot be enforced if Phase 1 misses a clippy lint or a workspace boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Cargo dependency closure (typosquat) | mitigate | All deps user-pinned in CLAUDE.md and Phase 1 RESEARCH.md `## Package Legitimacy Audit` (all marked Approved); `cargo-deny.yml` runs daily `EmbarkStudios/cargo-deny-action@v2` for advisories + bans (rejects yanked crates, multiple-version warnings, wildcard versions, unknown registries) |
| T-01-02 | Tampering | npm dependency closure | mitigate | `ui/pnpm-lock.yaml` committed; CI uses `pnpm install --frozen-lockfile` so unauthorized version drift fails the build; svelte/vite ecosystem packages used (all top-100 by download) |
| T-01-03 | Information Disclosure | Generated `ui/src/bindings.ts` accidentally committed | mitigate | Listed in `.gitignore` (acceptance criterion above asserts the literal line is present); Plan 05's export test regenerates it from Rust DTOs so source-of-truth is Rust |
| T-01-04 | Tampering | Clippy disallowed-methods drift (later phase removes a ban) | mitigate | `clippy.toml` checked in at repo root; CI step `cargo clippy --workspace --all-targets -- -D warnings` blocks merge if any banned method is used; if a later phase needs an exception, it must add `#[allow(clippy::disallowed_methods)]` with a `// SAFETY:` comment — visible in code review |
| T-01-05 | Tampering | `dirs::*_dir()` introduced silently (portable leak) | mitigate | All five `dirs::*_dir` entries banned via `clippy.toml`; ProcMon-test (Plan 06) catches any escape that clippy somehow misses |
| T-01-06 | Information Disclosure | `chrono::Local::now()` introduced (TZ leak into DB) | mitigate | Both `chrono::Local::now` and `chrono::offset::Local::now` banned via `clippy.toml`; `chrono::DateTime<chrono::Local>` type also banned |
| T-01-07 | Tampering / Denial of Service | Rust toolchain drift (CI builds on 1.84, dev on nightly) | mitigate | `rust-toolchain.toml` pins channel `1.85`; CI explicitly sets `toolchain: '1.85'` in `dtolnay/rust-toolchain@stable`; deviation produces compile error or different lint behavior immediately |
| T-01-SC | Tampering | npm/cargo installs in this plan | mitigate | All crates verified in `01-RESEARCH.md ## Package Legitimacy Audit` (Approved disposition for every entry); no `[ASSUMED]` or `[SUS]` packages; Cargo.lock committed; pnpm-lock.yaml committed; CI uses frozen-lockfile |

**Block on `high` severity:** Threats T-01-01, T-01-04, T-01-05, T-01-07 are mitigated by build-time gates (cargo-deny, clippy, rust-toolchain pin) that fail the CI run if violated. No `high` threats are accepted.
</threat_model>

<verification>
After this plan completes, the following must all hold true (verifiable in CI on the next push):

1. `cargo build --workspace` succeeds on the dev macOS host.
2. `cargo clippy --workspace --all-targets -- -D warnings` succeeds (the disallowed-methods list is parsed and active; since no source code yet uses banned methods, nothing fails — but the gate is live for Plans 02+).
3. `cargo fmt --all -- --check` succeeds (no source files yet to mis-format; gate is live).
4. `cargo test --workspace --no-fail-fast` succeeds (`no_io_deps.rs` is the only test and it passes — proves `trackly-core` has zero I/O deps).
5. `cargo run -p trackly-app -- --self-test` exits 0 (placeholder mode; Plans 02-04 add real lifecycle).
6. `cd ui && pnpm install --frozen-lockfile && pnpm svelte-check && pnpm lint` succeeds.
7. The `.gitignore` prevents `ui/src/bindings.ts` from being staged (`git status --porcelain` excludes it even if the file exists after Plan 05 runs).
8. The CI `ci-fast.yml` workflow triggers on push and passes all gates.
</verification>

<success_criteria>
- Workspace topology matches D-Workspace-01 exactly: `crates/trackly-core`, `crates/trackly-infra`, `crates/trackly-app`, `ui/`, `tools/procmon-check/`, `.github/workflows/`.
- `trackly-core` is provably I/O-free (CI test passes).
- All clippy disallowed-methods entries from D-CI-02 are active and `cargo clippy --workspace --all-targets -- -D warnings` reads them.
- `[workspace.dependencies]` includes `axum`, `tower`, `tower-http` so Plan 05 can consume via `workspace = true` (locks the HTTP topology in Wave 0).
- `ui/` Svelte 5 + Vite 6 scaffold installs, type-checks, lints — all green in CI.
- `ci-fast.yml` runs all 5 mandated gates on every push and PR.
- `cargo-deny.yml` runs daily and can be triggered manually.
- The `trackly` binary compiles and accepts `--self-test` (placeholder); real `--self-test` lifecycle lands in Plans 02 (paths/config), 03 (migrations), 04 (writer/AppCtx).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done, documenting:
- Exact pinned versions chosen for any deps where the workspace declaration accepts a range (e.g., the `pnpm` minor version pinned in `package.json` `packageManager`).
- The decision to include `tauri-plugin-single-instance` in `Cargo.toml` (per RESEARCH Open Question #2 recommendation).
- Any cargo-deny warnings that surfaced on first run (with rationale for accepting or fixing).
- Whether the dev box needed `rustup install 1.85` before `cargo build` worked.
</output>
