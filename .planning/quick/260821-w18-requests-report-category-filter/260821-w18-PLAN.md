---
quick_id: 260821-w18
slug: requests-report-category-filter
phase: 260821-w18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/trackly-app/src/dto/reports.rs
  - crates/trackly-app/src/services/report_service.rs
  - crates/trackly-app/tests/report_requests.rs
  - ui/src/features/reports/RequestCategoryFilter.svelte
  - ui/src/features/reports/ReportsPage.svelte
autonomous: true
requirements: [CATF-01, CATF-02, CATF-03, CATF-04]
must_haves:
  truths:
    - "На вкладке «Заявки» в строке периода сразу после PeriodSelector есть secondary-кнопка размера md с иконкой воронки (CATF-01)"
    - "Клик по кнопке открывает попап с 8 чекбоксами строго в порядке: Все, Регистрации, Замена картриджа, Ремонт техники, Расходные материалы, Программное обеспечение, Без категорий, Прочее (CATF-01)"
    - "При включённом «Все» остальные 7 чекбоксов визуально отмечены и disabled; при снятии «Все» они остаются отмеченными, но становятся активными и переключаемыми по одному, включая снятие всех до нуля (CATF-01)"
    - "«Все» = отсутствие WHERE-ограничения по типу/категории на бэкенде (не OR всех известных ключей) — будущие типы/категории не пропадают молча, пока «Все» включено (CATF-02)"
    - "Регистрации -> request_type='ad_register'; Замена картриджа -> request_type='cartridge_replace'; Ремонт техники/Расходные материалы/Программное обеспечение/Прочее -> request_type='free_form' AND category_id = (подзапрос по имени в request_categories); Без категорий -> request_type='free_form' AND category_id IS NULL (CATF-02)"
    - "Изменение любого чекбокса немедленно (без отдельной кнопки «Применить») перефильтровывает таблицу на экране, счётчики вкладок (reports_get_report_counts), CSV-экспорт и печать/PDF — один и тот же фильтр на все три выхода (CATF-03)"
    - "И десктопные Tauri-команды, и HTTP-роуты /api/v1/reports_list_requests_*/reports_export_*/reports_get_report_counts применяют фильтр одинаково — используют общий ReportFilter DTO без отдельного кода на транспорт (CATF-03)"
    - "Manager не видит заявки ad_register ни при фильтре «Все», ни при явном выборе чекбокса «Регистрации» — RBAC-инвариант REQ-06/T-09-11 не ослабляется новым фильтром, он может только сузить видимую выборку (CATF-04)"
  artifacts:
    - path: "crates/trackly-app/src/dto/reports.rs"
      provides: "ReportFilter.request_category_filter: Option<Vec<String>> — новое поле фильтра «Заявки»"
      contains: "request_category_filter"
    - path: "crates/trackly-app/src/services/report_service.rs"
      provides: "category_filter_clause() — построитель WHERE-фрагмента по ключам чекбоксов; query_requests_inner/count_requests_inner принимают доп. параметр category_filter"
      contains: "fn category_filter_clause"
    - path: "crates/trackly-app/tests/report_requests.rs"
      provides: "Интеграционные тесты: none=безограничения, явный выбор, пустой выбор=0 строк, RBAC ad_register всё ещё исключён у Manager, счётчики и CSV отражают фильтр"
      contains: "category_filter"
    - path: "ui/src/features/reports/RequestCategoryFilter.svelte"
      provides: "Кнопка-воронка + попап с 8 чекбоксами, select-all/disable семантика, outside-click/Esc закрытие, portal-позиционирование"
      contains: "Регистрации"
    - path: "ui/src/features/reports/ReportsPage.svelte"
      provides: "request_category_filter в локальном ReportFilter-интерфейсе; RequestCategoryFilter отрендерен в controls-row только для домена requests, сразу после PeriodSelector"
      contains: "request_category_filter"
  key_links:
    - from: "ui/src/features/reports/RequestCategoryFilter.svelte onChange"
      to: "ui/src/features/reports/ReportsPage.svelte filter.request_category_filter"
      via: "filter = { ...filter, request_category_filter: keys } — тот же `filter` объект, что уже спредится в reports_list_requests_*/reports_export_csv/reports_export_pdf/reports_get_report_counts, новых полей в payload вручную не добавляется"
      pattern: "request_category_filter"
    - from: "ui/src/features/reports/RequestCategoryFilter.svelte CHECKBOX_DEFS keys"
      to: "crates/trackly-app/src/services/report_service.rs category_filter_clause match-ветки"
      via: "строковые ключи ad_register|cartridge_replace|repair|consumables|software|no_category|other — это wire-контракт, идентичный на фронте и бэке"
      pattern: "\"repair\" | \"consumables\" | \"software\" | \"other\""
    - from: "crates/trackly-app/src/services/report_service.rs category_filter_clause repair/consumables/software/other-ветки"
      to: "migrations/V024__request_categories.sql сид-строки request_categories.name"
      via: "подзапрос SELECT id FROM request_categories WHERE name = ?N — точные RU-имена 'Ремонт техники'/'Расходные материалы'/'Программное обеспечение'/'Прочее', не хардкод id (таблица lookup, не enum)"
      pattern: "SELECT id FROM request_categories WHERE name"
    - from: "crates/trackly-app/src/services/report_service.rs category_filter_clause (WHERE AND-клауза)"
      to: "trackly_infra::repos::requests_sqlite::ad_register_predicate (exclude_ad_register AND-клауза)"
      via: "оба условия объединяются через AND в общем WHERE — фильтр по категориям может только СУЗИТЬ выборку поверх RBAC-исключения, никогда не отменяет его (выбор чекбокса «Регистрации» Manager'ом даёт 0 строк, не ошибку и не обход)"
      pattern: "ad_register_predicate"
---

<objective>
Добавить в раздел «Отчёты» → домен «Заявки» фильтр по категориям заявок: secondary-кнопка
размера md с иконкой воронки в строке периода (сразу после PeriodSelector), открывающая попап
с 8 чекбоксами (Все + 7 конкретных категорий/типов). Фильтр действует одинаково на экран,
счётчики вкладок, CSV-экспорт и печать/PDF, на десктопе и в LAN-браузере — без ослабления
существующего RBAC-инварианта REQ-06/T-09-11 (Manager не видит ad_register).

Схема-мэппинг (зафиксирован оркестратором, не пересматривается):
- Регистрации -> `request_type = 'ad_register'`
- Замена картриджа -> `request_type = 'cartridge_replace'`
- Ремонт техники / Расходные материалы / Программное обеспечение / Прочее ->
  `request_type = 'free_form' AND category_id = (id соответствующей сид-категории V024)`
- Без категорий -> `request_type = 'free_form' AND category_id IS NULL`
- Все -> ПОЛНОЕ отсутствие WHERE-ограничения по типу/категории (не OR всех известных ключей —
  так будущие типы/категории, добавленные позже, не пропадают молча, пока «Все» включено)

Purpose: сейчас в отчёте «Заявки» (добавлен в 260820-vad) нет способа отфильтровать заявки по
их сути (регистрация / замена картриджа / категория произвольной заявки) — только по статусу
(вкладки) и периоду. Пользователь регулярно хочет посмотреть, например, только заявки на ремонт
техники за месяц, без ручного просеивания.

Output: рабочий funnel-фильтр в UI «Заявки» (экран + CSV + печать), тот же фильтр на десктопе
и в LAN-браузере, без регресса существующих доменов «Устройства»/«Картриджи» (фильтр вообще не
затрагивает их запросы) и без ослабления RBAC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/quick/260820-vad-csv-pdf/260820-vad-SUMMARY.md

<interfaces>
Перечитать crates/trackly-app/src/services/report_service.rs и
crates/trackly-app/src/dto/reports.rs перед правкой — номера строк могли сдвинуться.

ReportFilter (crates/trackly-app/src/dto/reports.rs) — общий фильтр для ВСЕХ доменов отчётов
(devices/cartridges/requests), все поля Option, `#[derive(..., Default, ...)]`. Новое поле
добавляется по тому же паттерну, что `color`/`model_id` (Option-типизировано, без
`#[serde(default)]` — serde уже трактует отсутствующий Option-поле в JSON как None, это
подтверждено предыдущими добавлениями в этот же DTO без спец.атрибутов):

  #[derive(Debug, Clone, Default, Serialize, Deserialize, Type)]
  pub struct ReportFilter {
      ...
      pub color: Option<String>,
      pub search: Option<String>,
      // добавить сюда:
      // pub request_category_filter: Option<Vec<String>>,
  }

ReportFilter уже "сквозно" прокидывается БЕЗ построчного перечисления полей — ни
tauri_cmds/reports.rs, ни http/reports.rs, ни specta_export.rs трогать НЕ требуется: Tauri-команды
принимают `filter: ReportFilter` целиком, HTTP-хендлеры делают `Json<ListWithPeriodPayload>` с
полем `pub filter: ReportFilter` целиком (crates/trackly-app/src/http/reports.rs). Новое поле
ReportFilter автоматически течёт через оба транспорта без изменений в этих файлах — весь фикс
локализован в dto/reports.rs + report_service.rs (CATF-03 выполняется "бесплатно").

Существующие вызовы уже принимают `filter: ReportFilter`, но ИГНОРИРУЮТ его для домена requests
(параметр называется `_filter` — подчёркивание = unused). Переименовать в `filter` и передать в
query_requests_inner/count_requests_inner:

  pub async fn list_requests_all(&self, _filter: ReportFilter, period: PeriodDto, exclude_ad_register: bool) -> ...
  pub async fn list_requests_open(&self, _filter: ReportFilter, period: PeriodDto, exclude_ad_register: bool) -> ...
  pub async fn list_requests_in_progress(&self, _filter: ReportFilter, period: PeriodDto, exclude_ad_register: bool) -> ...
  pub async fn list_requests_completed(&self, _filter: ReportFilter, period: PeriodDto, exclude_ad_register: bool) -> ...

Текущие сигнатуры внутренних query-хелперов (query_acts_inner — образец параметризованного
WHERE-паттерна, next_idx(&owned_params) для позиционных `?N`):

  fn query_requests_inner(conn: &rusqlite::Connection, ts_from: Option<i64>, ts_to: Option<i64>,
      status_filter: Option<&str>, exclude_ad_register: bool) -> Result<ReportResponse, AppError>

  fn count_requests_inner(conn: &rusqlite::Connection, ts_from: Option<i64>, ts_to: Option<i64>,
      status_filter: Option<&str>, exclude_ad_register: bool) -> Result<i64, AppError>

Обе функции строят `clauses: Vec<String>` + `owned_params: Vec<Box<dyn ToSql>>`, объединяют через
`clauses.join(" AND ")`. exclude_ad_register уже добавляет свою AND-клаузу:

  if exclude_ad_register {
      clauses.push(ad_register_predicate("r."));   // "r.request_type != 'ad_register'"
  }

`get_report_counts()` для домена `"requests"` уже вызывает `count_requests_inner` 4 раза (по
одному на вкладку all/open/in_progress/completed), каждый раз с текущим `filter: ReportFilter` в
области видимости (замыкание `move`, `filter` уже владеется closure, `&filter` читается
многократно для веток devices/cartridges — тот же паттерн подходит для requests).

Сид-таблица категорий (migrations/V024__request_categories.sql) — lookup, НЕ CHECK-enum, ровно
4 строки на сегодня, точные RU-имена (использовать ИМЕННО их в подзапросе, не хардкодить id):

  CREATE TABLE request_categories (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE);
  INSERT INTO request_categories (name) VALUES
    ('Ремонт техники'), ('Расходные материалы'), ('Программное обеспечение'), ('Прочее');

requests.request_type CHECK (V006): 'cartridge_replace' | 'free_form' | 'ad_register'.
requests.category_id (V024): nullable FK -> request_categories, заполняется только для free_form.

Test-инфраструктура: minimal_ctx()/seed_user/seed_location/seed_printer_device/seed_request/
seed_fixture/fixture_period() уже существуют в crates/trackly-app/tests/report_requests.rs
(из 260820-vad) — расширять, не дублировать. seed_request() сейчас НЕ пишет category_id:

  async fn seed_request(writer: &WriterHandle, request_type: &str, status: &str,
      requested_by: i64, printer_device_id: Option<i64>, created_at_utc: i64) -> i64

Прямое SQL-чтение lookup-значения в тесте — образец (acts_crud.rs и др., `readers.acquire()`
внутри `tokio::task::spawn_blocking`):

  let readers = ctx.readers.clone();
  let id: i64 = tokio::task::spawn_blocking(move || {
      let conn = readers.acquire();
      conn.query_row("SELECT id FROM request_categories WHERE name = ?1", params![name], |r| r.get(0))
  }).await.expect("spawn_blocking").expect("category exists");

---

Frontend: ui/src/features/reports/ReportsPage.svelte — `.controls-row` уже содержит
`<PeriodSelector .../>` затем `<ReportFilters .../>` (только кнопки экспорта, GAP-R4). Локальный
TS-интерфейс `ReportFilter` в этом файле — руками дублированная копия бэкендового DTO (НЕ импорт
из bindings.ts — так уже сделано для остальных полей типа `act_type`/`location_id`, следовать
этому же паттерну, не переключать на bindings-импорт). `filter` — `$state<Partial<ReportFilter>>`,
уже спредится целиком в payload `loadReport()`/`loadStatusCounts()`/`exportCsv()`/`exportPdf()` —
новое поле в `filter` начинает применяться на все 4 выхода без правок в этих функциях.
`$effect` уже авто-перезагружает отчёт при любом изменении `filter` (весь объект — реактивная
зависимость) — отдельной кнопки «Применить» не требуется, шире существующей архитектуры страницы.
`onDomainChange` уже делает `filter = {}` при смене домена — фильтр категорий автоматически
сбрасывается при уходе с вкладки «Заявки».

Checkbox.svelte props: `checked` ($bindable), `disabled`, `onchange?: (checked: boolean) => void`,
children snippet — рендерит `<label class="check-row">` с native input + кастомный box.

Button.svelte props: `variant` ('primary'|'secondary'|'destructive'|'ghost'|'link')`,
`size` ('sm'|'md')`, `onclick`, children snippet. НЕТ пропа `class`/rest-спреда — кастомную
позиционированную точку-индикатор оборачивать СНАРУЖИ Button в отдельном wrapper-div, не пытаться
передать class внутрь Button.

Инлайн-SVG иконки — существующий паттерн проекта (ui/src/features/reports/ReportFilters.svelte,
иконка печати): `<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor"
stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">` с
`<polyline>`/`<path>`. Feather-style "funnel/filter" иконка для новой кнопки (16x16, тот же
stroke-стиль): `<polygon points="22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3"></polygon>`.

Портал + позиционирование попапа (ui/src/lib/utils/portal.ts, ui/src/lib/utils/dropdownAnchor.ts) —
уже используется PeriodSelector'ом (через Dropdown.svelte) в ЭТОМ ЖЕ `.controls-row`, потому что
родитель `.reports-content` имеет `overflow-y: hidden` и обрезал бы абсолютно спозиционированный
попап без портала. Использование:

  <div use:portal use:dropdownAnchor={{ anchorEl: triggerEl, gap: 4 }} class="...">...</div>

`dropdownAnchor` сам вычисляет `position: fixed` координаты от `anchorEl.getBoundingClientRect()`
и переcчитывает их при скролле/ресайзе — ничего дополнительно вычислять не нужно.

ActionMenu.svelte (ui/src/lib/components/ActionMenu.svelte) — образец outside-click/Escape
закрытия (НЕ образец для клика по контенту панели — там `onclick={() => close(true)}` на всей
панели закрывает попап при любом внутреннем клике, что здесь НЕДОПУСТИМО: чекбоксы должны
переключаться много раз подряд без закрытия попапа). Взять только эту часть:

  $effect(() => {
    function onDown(e: MouseEvent) {
      if (open && rootEl && !rootEl.contains(e.target as Node)) open = false;
    }
    function onKey(e: KeyboardEvent) {
      if (e.key === 'Escape') open = false;
    }
    document.addEventListener('mousedown', onDown);
    document.addEventListener('keydown', onKey);
    return () => {
      document.removeEventListener('mousedown', onDown);
      document.removeEventListener('keydown', onKey);
    };
  });

С порталом `rootEl.contains(e.target)` не сработает для клика ВНУТРИ портального панели (панель
физически не в поддереве `rootEl` после `use:portal` переносит её в `<body>`) — проверять
принадлежность клика И `rootEl` (кнопка) И `panelEl` (портальная панель):
`!rootEl.contains(target) && !panelEl?.contains(target)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — ReportFilter.request_category_filter, category_filter_clause, wiring в query/count_requests_inner и get_report_counts</name>
  <files>crates/trackly-app/src/dto/reports.rs, crates/trackly-app/src/services/report_service.rs, crates/trackly-app/tests/report_requests.rs</files>
  <behavior>
    - `category_filter_clause(None, _)` -> `None` (нет WHERE-ограничения — «Все»).
    - `category_filter_clause(Some(&[]), _)` -> `Some("1 = 0")` (явный пустой выбор — 0 строк, НЕ откат к «Все»).
    - `category_filter_clause(Some(&["ad_register".into()]), _)` -> клауза содержит `r.request_type = 'ad_register'`, без новых bind-параметров.
    - `category_filter_clause(Some(&["repair".into()]), &mut params)` -> клауза содержит подзапрос `SELECT id FROM request_categories WHERE name = ?N`, `params` вырос на 1 элемент со значением `"Ремонт техники"`.
    - `category_filter_clause(Some(&["unknown_future_key".into()]), _)` -> `Some("1 = 0")` (все ключи нераспознаны — не паника, не ошибка, просто "ничего не совпадает").
    - `category_filter_clause(Some(&["ad_register".into(), "unknown".into()]), _)` -> клауза учитывает только `ad_register`, нераспознанный ключ молча пропущен (не превращает результат в "1=0" целиком).
    - Интеграционно (report_requests.rs): фильтр применяется идентично в list_requests_all/open/in_progress/completed, в get_report_counts(domain="requests") и в CSV-экспорте; Manager с фильтром `Some(["ad_register"])` получает 0 строк (RBAC не ослаблен, а не bypass/ошибка).
  </behavior>
  <action>
Перечитать report_service.rs и dto/reports.rs перед правкой (см. interfaces).

1. `crates/trackly-app/src/dto/reports.rs`: в `ReportFilter` добавить поле
   `pub request_category_filter: Option<Vec<String>>,` с doc-комментарием: "VAD-05/CATF-02 —
   funnel-фильтр домена «Заявки» по типу/категории. `None` = «Все» (без ограничения).
   `Some(vec![])` = все чекбоксы сняты, явный пустой результат. `Some(keys)` = allow-list ключей
   ('ad_register'|'cartridge_replace'|'repair'|'consumables'|'software'|'no_category'|'other'),
   см. category_filter_clause в report_service.rs. Игнорируется остальными доменами (devices/
   cartridges)." Поле НЕ участвует в остальных query_*_inner функций — трогать их не нужно.

2. `crates/trackly-app/src/services/report_service.rs`:

   a. Добавить приватную функцию `category_filter_clause(keys: Option<&[String]>, owned_params:
      &mut Vec<Box<dyn ToSql>>) -> Option<String>` рядом с существующими helper-функциями секции
      "Request domain" (translate_request_type и т.д.). Логика: `keys?` — если None, вернуть None
      сразу. Если `keys.is_empty()`, вернуть `Some("1 = 0".to_string())`. Иначе — пройтись по
      каждому ключу, для 'ad_register'/'cartridge_replace' добавить в вектор OR-веток простую
      `r.request_type = '...'`; для 'no_category' добавить
      `(r.request_type = 'free_form' AND r.category_id IS NULL)`; для 'repair'/'consumables'/
      'software'/'other' — определить соответствующее RU-имя категории (см. interfaces —
      ТОЧНЫЕ сид-имена V024), забиндить его через `next_idx(owned_params)` +
      `owned_params.push(Box::new(name.to_string()))`, добавить
      `(r.request_type = 'free_form' AND r.category_id = (SELECT id FROM request_categories
      WHERE name = ?N))`; неизвестный ключ — молча пропустить (без паники и без ошибки). Если
      после прохода вектор OR-веток пуст (все ключи оказались нераспознаны), вернуть
      `Some("1 = 0".to_string())`. Иначе вернуть `Some(format!("({})", ors.join(" OR ")))`.

   b. `query_requests_inner` и `count_requests_inner`: добавить 6-й параметр
      `category_filter: Option<&[String]>`. Внутри, после существующих `if let Some(status) =
      status_filter {...}` и `if exclude_ad_register {...}` клауз, добавить:
      `if let Some(clause) = category_filter_clause(category_filter, &mut owned_params) {
      clauses.push(clause); }`. Порядок добавления клауз в `clauses` не важен для корректности
      (все объединяются через AND), но ставить category-клаузу ПОСЛЕ exclude_ad_register-клаузы
      для читаемости сгенерированного SQL.

   c. `list_requests_all`/`list_requests_open`/`list_requests_in_progress`/`list_requests_completed`:
      переименовать параметр `_filter: ReportFilter` в `filter: ReportFilter`, передать
      `filter.request_category_filter.as_deref()` шестым аргументом в соответствующий вызов
      `query_requests_inner(...)`.

   d. `get_report_counts`: в ветке `domain == "requests"` каждый из 4 вызовов
      `count_requests_inner(&conn, ts_from, ts_to, Some("..."), exclude_ad_register)` (и вызов
      без статус-фильтра для "all") получает 6-й аргумент
      `filter.request_category_filter.as_deref()`. `filter` уже в области видимости closure
      (используется по ссылке для веток devices/cartridges) — трогать сигнатуру
      `get_report_counts` не требуется.

   e. Добавить unit-тесты для `category_filter_clause` в существующий `#[cfg(test)] mod tests`
      этого файла — покрыть все 6 случаев из `<behavior>` выше (None/пустой/один известный
      ключ/known+bind-параметр/один неизвестный/известный+неизвестный вместе).

3. `crates/trackly-app/tests/report_requests.rs`:

   a. Расширить `seed_request(...)` шестым параметром `category_id: Option<i64>`, обновить
      INSERT (добавить колонку `category_id` в список колонок и `params![...]`), обновить все
      существующие 4 вызова в `seed_fixture()` — передать `None` (поведение не меняется).

   b. Добавить приватный тестовый хелпер `category_id_by_name(readers: &Arc<ReaderPool>, name:
      &str) -> i64` (см. interfaces — паттерн `readers.acquire()` внутри `spawn_blocking`).

   c. Добавить новую fixture-функцию (например `seed_category_fixture()`), сеющую по одной
      заявке на каждый из 7 ключей (ad_register / cartridge_replace / free_form с category_id
      каждой из 4 сид-категорий по имени / free_form с category_id = None) — все в
      FIXTURE_CREATED_AT_UTC, все со статусом "open" (статус не важен для этих тестов, фильтр по
      категориям ортогонален статус-вкладкам). Для различения строк в assert-ах использовать `id`
      заявки (возвращаемый `seed_request`), а не giver_name/номер.

   d. Добавить тесты (Identity::trusted_admin(), если не указано иное):
      - `report_requests_category_filter_none_matches_all_types_and_categories` — `filter =
        ReportFilter { request_category_filter: None, ..Default::default() }` -> `total == 7`.
      - `report_requests_category_filter_selects_only_matching_subset` — выбрать 2 непересекающихся
        ключа (например `["repair", "no_category"]`) -> `total == 2`, оба ожидаемых id
        присутствуют, остальные 5 — нет.
      - `report_requests_category_filter_empty_selection_yields_zero_rows_not_all` —
        `Some(vec![])` -> `total == 0` (явно НЕ откат к «Все»).
      - `report_requests_category_filter_ad_register_key_still_excluded_for_manager` — Manager
        identity + `Some(vec!["ad_register".to_string()])` -> `total == 0`; тот же фильтр с
        Admin identity -> `total == 1` (сама ad_register-заявка).
      - `report_requests_category_filter_reflected_in_get_report_counts` —
        `build_reports_get_report_counts` с category-фильтром на подмножество -> счётчик "all"
        совпадает с числом заявок в подмножестве (не с общим числом 7).
      - `report_requests_category_filter_reflected_in_csv_export` — `build_reports_export_csv`
        с category-фильтром на один конкретный тип/категорию -> CSV содержит ожидаемое число
        строк данных (например, по количеству переносов строк) и НЕ содержит переведённые
        значения типов, которые должны быть отфильтрованы.

Приватность (CLAUDE.md): весь новый фикстурный контент — вымышленные данные, следовать
существующему стилю файла (без новых реальных ФИО/названий).
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo check -p trackly-app --all-targets</automated>
  </verify>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && cargo test -p trackly-app --lib services::report_service::tests::category_filter_clause</automated>
  </verify>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test report_requests</automated>
  </verify>
  <done>
`cargo check -p trackly-app --all-targets` чист. Новые unit-тесты category_filter_clause и
интеграционные тесты в report_requests.rs зелёные. Существующие тесты report_requests.rs
(из 260820-vad) продолжают проходить без изменений в ожидаемых значениях (регресс исключён —
`ReportFilter::default()` по-прежнему означает "без ограничения по категориям").
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — RequestCategoryFilter.svelte (кнопка-воронка + попап) и wiring в ReportsPage.svelte</name>
  <files>ui/src/features/reports/RequestCategoryFilter.svelte, ui/src/features/reports/ReportsPage.svelte</files>
  <action>
1. Создать `ui/src/features/reports/RequestCategoryFilter.svelte`:

   - Props: `selectedKeys: string[] | null` (null = «Все»), `onChange: (keys: string[] | null) =>
     void`.
   - Константа `CHECKBOX_DEFS` — массив из 7 объектов `{ key, label }` СТРОГО в порядке:
     `ad_register`/«Регистрации», `cartridge_replace`/«Замена картриджа», `repair`/«Ремонт
     техники», `consumables`/«Расходные материалы», `software`/«Программное обеспечение»,
     `no_category`/«Без категорий», `other`/«Прочее». `ALL_KEYS = CHECKBOX_DEFS.map(d => d.key)`.
   - `allChecked = $derived(selectedKeys === null)`.
   - `isChecked(key: string) = allChecked || (selectedKeys?.includes(key) ?? false)`.
   - `toggleAll(next: boolean)`: `next === true` -> `onChange(null)`; `next === false` ->
     `onChange([...ALL_KEYS])` (визуально все 7 остаются отмеченными, но становятся
     интерактивными — по спецификации).
   - `toggleKey(key: string, next: boolean)`: no-op если `allChecked` (чекбоксы disabled в этом
     состоянии — обработчик физически не должен вызываться, но защититься явной проверкой всё
     равно). Иначе: `const current = selectedKeys ?? []`; `const updated = next ? [...current,
     key] : current.filter((k) => k !== key)`; `onChange(updated)` — даже если `updated` пуст
     (см. Task 1: пустой массив = явный 0-результат, UI не должен ничего "чинить" за
     пользователя).
   - Состояние попапа: `open = $state(false)`, `rootEl`/`triggerEl`/`panelEl` через `bind:this`.
   - Outside-click + Escape закрытие через `$effect` (см. interfaces — образец из ActionMenu,
     НО без auto-close на внутреннем клике панели, и с проверкой принадлежности клика и к
     `rootEl`, и к портальному `panelEl`).
   - Кнопка-триггер: `<Button variant="secondary" size="md" onclick={() => (open = !open)}>` с
     inline SVG-иконкой воронки (см. interfaces — Feather-style polygon, 16x16,
     stroke="currentColor"), обёрнутая снаружи в `<div class="trigger-wrap" bind:this={triggerEl}>`
     (Button не поддерживает проброс class/rest-пропов). `aria-label="Фильтр по категориям
     заявок"` на самой кнопке недостижим напрямую (Button не принимает aria-пропы) — обернуть
     кнопку в элемент с `role="group" aria-label="Фильтр по категориям заявок"` либо добавить
     `<span class="sr-only">Фильтр по категориям заявок</span>` внутри Button рядом с SVG.
   - Индикатор активного фильтра: `{#if !allChecked}<span class="active-dot"
     aria-hidden="true"></span>{/if}` внутри `.trigger-wrap`, `position: absolute` точка поверх
     угла кнопки (см. interfaces).
   - Попап-панель: `{#if open}<div class="popover-panel" role="dialog" aria-label="Фильтр по
     категориям заявок" use:portal use:dropdownAnchor={{ anchorEl: triggerEl, gap: 4 }}
     bind:this={panelEl}>` — внутри: один `<Checkbox checked={allChecked}
     onchange={toggleAll}>Все</Checkbox>`, разделитель, затем `{#each CHECKBOX_DEFS as def}
     <Checkbox checked={isChecked(def.key)} disabled={allChecked} onchange={(c) =>
     toggleKey(def.key, c)}>{def.label}</Checkbox>{/each}`.
   - Стили (`<style lang="scss">`): `.trigger-wrap { position: relative; display: inline-flex; }`,
     `.active-dot { position: absolute; top: -2px; right: -2px; width: 8px; height: 8px;
     border-radius: 50%; background: var(--tr-accent); border: 1.5px solid var(--tr-surface); }`,
     `.popover-panel { z-index: 1000; min-width: 240px; display: flex; flex-direction: column;
     gap: var(--tr-space-2xs); padding: var(--tr-space-sm); background: var(--tr-surface-raised);
     border: 1px solid var(--tr-border); border-radius: var(--tr-radius-md); box-shadow:
     var(--tr-elev-2); }` — использовать существующие `--tr-*` токены (те же, что
     ActionMenu.svelte `.action-menu-panel`), не вводить новые значения.

2. `ui/src/features/reports/ReportsPage.svelte`:

   - Импортировать `RequestCategoryFilter` из `./RequestCategoryFilter.svelte`.
   - В локальный TS-интерфейс `ReportFilter` добавить поле
     `request_category_filter?: string[] | null;`.
   - В `.controls-row`, между `<PeriodSelector .../>` и `<ReportFilters .../>`, добавить:
     `{#if activeDomain === 'requests'}<RequestCategoryFilter selectedKeys={filter
     .request_category_filter ?? null} onChange={(keys) => { filter = { ...filter,
     request_category_filter: keys }; }} />{/if}` — кнопка видна ТОЛЬКО на вкладке «Заявки»,
     существующий авто-reload `$effect` (следит за всем `filter`) подхватывает изменение без
     дополнительной правки.
   - Изменений в `loadReport`/`loadStatusCounts`/`exportCsv`/`exportPdf`/`onDomainChange` не
     требуется — все уже спредят `filter` целиком в payload, `onDomainChange` уже сбрасывает
     `filter = {}` при смене домена (сбрасывает и новое поле).
  </action>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && pnpm --dir ui run svelte-check</automated>
  </verify>
  <verify>
    <automated>cd /Users/madsas/Projects/trackly && pnpm --dir ui build</automated>
  </verify>
  <done>
`svelte-check` — 0 ошибок. `pnpm --dir ui build` проходит (prebuild-хук
`cargo test -p trackly-app --test export_bindings` подтверждает, что бэкенд из Task 1
компилируется и bindings.ts синхронизирован). Кнопка-воронка отрендерена ТОЛЬКО в домене
«Заявки», попап содержит 8 чекбоксов в заданном порядке с корректной select-all/disable
семантикой (подтверждается кодом — визуальная/интерактивная проверка ЖИВЫМ приложением остаётся
UNVERIFIED в рамках этого исполнения, см. проектный урок «Compile gates miss Svelte runtime» —
синтетические гейты не ловят рантайм-ошибки Svelte 5 рун).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Браузер/десктоп UI -> Tauri invoke / HTTP `/api/v1/reports_*` | Некорректный ввод: `request_category_filter` — массив произвольных строк от клиента (LAN-браузер — недоверенный источник) |
| ReportFilter -> SQL (report_service.rs) | Значения ключей маппятся через Rust `match`, единственные конкатенируемые в SQL-текст строки — фиксированные литералы категорий (не пользовательский ввод), связывание — только через `?N`-параметры |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260821-w18-01 | Tampering | `category_filter_clause` (SQL-инъекция через ключ фильтра) | mitigate | Ключи сравниваются через Rust `match` (allow-list), ни один пользовательский `String` не конкатенируется напрямую в SQL-текст — только фиксированные RU-имена категорий (Rust-литералы) идут в `?N`-параметр через `owned_params.push`; неизвестный ключ молча игнорируется, не форматируется в SQL |
| T-260821-w18-02 | Denial of Service | `category_filter_clause` (произвольно большой массив ключей от клиента) | accept | Массив ключей практически ограничен 7 известными значениями UI; даже при десятках дублей/мусорных строк — O(n) проход по коротким строкам, не влияет на LIMIT 1000 в самом запросе; не отличается по риску от уже принятого `ReportFilter.search`/`color` |
| T-260821-w18-03 | Elevation of Privilege | Комбинация `category_filter_clause` + `ad_register_predicate` (обход RBAC-исключения ad_register через явный выбор чекбокса «Регистрации») | mitigate | Обе клаузы объединяются через AND в общем WHERE — категорийный фильтр может только СУЗИТЬ результат exclude_ad_register, никогда не отменяет его; покрыто тестом `report_requests_category_filter_ad_register_key_still_excluded_for_manager` (Task 1) |
| T-260821-w18-04 | Information Disclosure | `RequestCategoryFilter.svelte` кнопка видна независимо от роли (Employee/Manager) | accept | Кнопка лишь СУЖИВАЕТ уже разрешённую по RBAC выборку (backend — источник истины через exclude_ad_register); отображение UI-контрола не раскрывает данные сверх уже читаемых через существующий отчёт |
</threat_model>

<verification>
1. `cargo check -p trackly-app --all-targets` — 0 ошибок после обеих задач.
2. `cargo test -p trackly-app --lib services::report_service::tests::` — существующие +
   новые unit-тесты (включая `category_filter_clause`) зелёные.
3. `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --test report_requests` —
   все тесты (существующие из 260820-vad + новые из Task 1) зелёные.
4. `pnpm --dir ui run svelte-check` — 0 ошибок.
5. `pnpm --dir ui build` — успешно, bindings.ts синхронизирован (через prebuild-хук).
6. Полный регресс: `TRACKLY_AD_MOCK=1 TRACKLY_SNMP_MOCK=1 cargo test -p trackly-app --
   --skip login_remember_persistent_cookie --test-threads=1` — 0 упавших (существующие
   report_acts/report_cartridges/report_csv_export/html_report_render/html_header_parity/
   reports_period_required не регрессируют; ОДИН `cargo test` за раз, не запускать параллельно
   с другими — см. проектный урок про cargo test lock-контеншн).
</verification>

<success_criteria>
- Кнопка-воронка (secondary, md) в строке периода на вкладке «Заявки», сразу после
  PeriodSelector; отсутствует на вкладках «Устройства»/«Картриджи».
- Попап с 8 чекбоксами в заданном порядке; «Все» disable/enable семантика реализована точно
  по спецификации пользователя, включая возможность снять все 7 обычных чекбоксов до нуля.
- Фильтр действует идентично на экран/счётчики/CSV/печать, на десктопе и в LAN-браузере, без
  ослабления RBAC-инварианта REQ-06/T-09-11.
- Ни один существующий отчёт («Устройства», «Картриджи», прежнее поведение «Заявки» с
  `request_category_filter = None`) не регрессирует.
</success_criteria>

<output>
Create `.planning/quick/260821-w18-requests-report-category-filter/260821-w18-SUMMARY.md` when done
</output>
