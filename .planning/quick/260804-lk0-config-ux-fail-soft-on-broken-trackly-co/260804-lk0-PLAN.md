---
quick_id: 260804-lk0
slug: config-ux-fail-soft-on-broken-trackly-config
phase: 260804-lk0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trackly.config.toml.example
  - crates/trackly-app/src/main.rs
  - crates/trackly-app/src/config_recovery.rs
  - crates/trackly-app/src/lib.rs
  - crates/trackly-app/Cargo.toml
  - Cargo.toml
  - crates/trackly-infra/src/config.rs
  - crates/trackly-infra/tests/config_example_test.rs
autonomous: true
requirements: [LK0-01, LK0-02, LK0-03]
must_haves:
  truths:
    - "A malformed trackly.config.toml no longer makes the GUI silently exit with code 1 — the app boots with default config, logs the failure, writes config-error.txt next to the exe, and (in interactive/non-self-test runs) shows a best-effort native dialog"
    - "trackly.config.toml.example matches the real AppConfig struct field-for-field: [paths] not [storage], server.host not server.bind, server.enabled and server.cert_path present"
    - "The shipped example, with every commented config line uncommented, parses into AppConfig without error (regression-tested against the actual repo-root file via include_str!, not a hand-copied duplicate)"
    - "Windows path examples in the shipped file use TOML single-quoted literal strings (no backslash escaping needed)"
  artifacts:
    - path: "trackly.config.toml.example"
      provides: "Corrected portable-mode config template matching AppConfig's real field names and required/optional status per section"
      contains: "[paths]"
    - path: "crates/trackly-app/src/config_recovery.rs"
      provides: "load_or_recover / write_config_error_file / clear_config_error_file / show_best_effort_dialog — fail-soft config load + failure surfacing"
      exports: ["load_or_recover", "write_config_error_file", "clear_config_error_file", "show_best_effort_dialog"]
    - path: "crates/trackly-infra/tests/config_example_test.rs"
      provides: "Regression test parsing the real trackly.config.toml.example (include_str!) after uncommenting"
      contains: "shipped_example_fully_uncommented_parses_into_app_config"
  key_links:
    - from: "crates/trackly-app/src/main.rs"
      to: "crates/trackly-app/src/config_recovery.rs"
      via: "let (config, config_load_error) = trackly_app::config_recovery::load_or_recover(paths.config_file());"
      pattern: "config_recovery::load_or_recover"
    - from: "crates/trackly-app/src/main.rs"
      to: "logging::init"
      via: "logging::init(&paths, &config) runs on the ALREADY-RECOVERED config, before any error is surfaced"
      pattern: "trackly_app::logging::init\\(&paths, &config\\)"
    - from: "crates/trackly-infra/tests/config_example_test.rs"
      to: "trackly.config.toml.example"
      via: "include_str!(concat!(env!(\"CARGO_MANIFEST_DIR\"), \"/../../trackly.config.toml.example\"))"
      pattern: "include_str!"
---

<objective>
Fix the "GUI silently exits with code 1 on a broken trackly.config.toml" bug and the
config-example/struct drift that causes it.

Root cause (confirmed by reading `crates/trackly-app/src/main.rs`): step 4
(`AppConfig::load_or_default(...)?`) runs BEFORE step 5 (`logging::init`). A malformed
TOML makes `load_or_default` return `Err(AppError::Validation)`, which the `?` propagates
straight out of `fn main() -> anyhow::Result<()>`. Under `#[cfg_attr(not(debug_assertions),
windows_subsystem = "windows")]` (release builds), there is no console attached, so the
`Error: ...` the Rust runtime prints to stderr on an `Err` return is invisible — the app
just vanishes with exit code 1 and zero user-visible diagnostics.

Compounding cause: `trackly.config.toml.example` (shipped in the portable ZIP per
`.github/workflows/release.yml`) does not match `crates/trackly-infra/src/config.rs`'s
real `AppConfig` struct — `[storage]` doesn't exist (`PathsConfig` lives under `[paths]`),
`[server]` uses `bind` instead of `host`, and is missing `enabled`/`cert_path` entirely.
Worse, `ServerConfig`/`LoggingConfig`/`OrganizationConfig`/`AdConfig`'s base fields have NO
`#[serde(default)]` — once a section header is uncommented, ALL its non-optional fields are
mandatory, so a user following the (also wrong) "uncomment only what you need" instruction
gets exactly the silent-exit crash described above.

Purpose: nobody — including future-us on the Windows test machine — should ever hit a
silent, diagnosticless exit from a config typo again; and the shipped example must be a
trustworthy, parseable reference, verified by a test that reads the actual shipped file.

Output: `AppConfig` load failures always boot with defaults + surface visibly (log +
config-error.txt + best-effort dialog); `trackly.config.toml.example` matches the struct
field-for-field with correct Windows-path literal syntax; a regression test proves the
shipped file (uncommented) parses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Current main.rs ordering (BEFORE this plan) — crates/trackly-app/src/main.rs:29-53 -->
```rust
fn main() -> anyhow::Result<()> {
    let paths = Paths::resolve()?;                                    // Step 1
    trackly_app::server::tls::ensure_crypto_provider();                // Step 1b
    webview_env::set_webview2_data_folder(paths.webview_data_dir())?;  // Step 2
    let self_test = std::env::args().any(|a| a == "--self-test");      // Step 3
    let config = AppConfig::load_or_default(paths.config_file())?;     // Step 4 — BUG: `?` exits
                                                                        //   silently under windows_subsystem="windows",
                                                                        //   BEFORE logging exists.
    let log_guard = trackly_app::logging::init(&paths, &config)?;      // Step 5
    // ... rt.block_on(...) with tauri::Builder etc.
}
```

<!-- AppConfig::load_or_default contract — crates/trackly-infra/src/config.rs (UNCHANGED by this plan) -->
```rust
impl AppConfig {
    /// - Файл отсутствует → Self::default() (Ok, not an error).
    /// - I/O ошибка чтения → Err(AppError::Internal { source_chain }).
    /// - Не парсится → Err(AppError::Validation { field, message }).
    /// - Валиден → Ok(parsed AppConfig).
    pub fn load_or_default(path: &Path) -> Result<Self, AppError>;
}
```
`AppError` derives `thiserror::Error` — `Validation` Display is
`"validation [{field}]: {message}"`, `Internal` is `"internal: {source_chain}"` (both
usable directly via `.to_string()`/`{err}`).

<!-- Paths — crates/trackly-infra/src/paths.rs (relevant accessors, UNCHANGED) -->
```rust
impl Paths {
    pub fn exe_dir(&self) -> &Path;        // <exe_dir> — portable-mode root, config-error.txt goes here
    pub fn config_file(&self) -> &Path;    // <exe_dir>/trackly.config.toml
}
```

<!-- Struct fields WITHOUT #[serde(default)] at the field level (mandatory once their
     section header is uncommented) — this is the exact list the corrected example and
     its top-of-file banner must document accurately: -->
- `[server]`: enabled, host, port, cert_path — mandatory. key_path — optional (`#[serde(default)]`).
- `[paths]`: db_path — mandatory IF the section is present, but the whole section is optional
  (AppConfig's `paths` field itself has `#[serde(default)]`).
- `[logging]`: level, format, retention_days — mandatory (whole section optional).
- `[organization]`: timezone — mandatory (whole section optional).
- `[ad]`: enabled, use_mock, host, domain, base_dn, name_attr, no_tls_verify — mandatory.
  Everything else (port, sso_enabled, spn, keytab_path, bind_dn, bind_password,
  display_name_cache_ttl_secs, group_cache_ttl_secs, role_mapping, ldap_tls_mode,
  admin_logins) is `#[serde(default)]` — optional. (Whole `[ad]` section is optional too.)

<!-- rfd 0.16.0 MessageDialog API (already resolved in Cargo.lock transitively via
     tauri-plugin-dialog — verified against the vendored crate source) -->
```rust
rfd::MessageDialog::new()
    .set_level(rfd::MessageLevel::Warning)
    .set_title(impl Into<String>)
    .set_description(impl Into<String>)
    .set_buttons(rfd::MessageButtons::Ok)
    .show() -> rfd::MessageDialogResult   // synchronous/blocking call
```

<!-- Existing precedent for promoting an already-transitively-resolved dep to a direct one
     — crates/trackly-app/Cargo.toml, `base64 = "0.22"` comment: "Already resolved
     transitively at 0.22.1 (axum/krilla/rcgen/tauri) — pinned as a direct dep explicitly,
     adding zero new supply-chain surface." Mirror this exact rationale for `rfd`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix trackly.config.toml.example to match the real AppConfig struct</name>
  <files>trackly.config.toml.example</files>
  <action>
Replace the ENTIRE contents of `trackly.config.toml.example` (repo root — this is the exact
file `.github/workflows/release.yml`'s "Assemble portable ZIP" step copies into the release
artifact, confirmed by grep; there is no other copy to keep in sync) with the text below.
Write it VERBATIM — every commented line's exact spacing/prefix matters because Task 3's
regression test uncomments lines using a strict per-line heuristic (a line becomes real TOML
only if, after stripping one leading `#` and at most one following space, it starts with an
ASCII identifier immediately followed by `=`, or starts with `[`; genuine prose comments in
this file are always Cyrillic-led or indented, so they can never accidentally match — verify
this claim yourself by scanning every line below before moving to Task 3).

Corrections applied, per LK0-02:
1. `[storage]` renamed to `[paths]` (real section name; `PathsConfig` lives under `[paths]`
   in `crates/trackly-infra/src/config.rs`, not `[storage]` — the old name silently no-opped
   via serde's forward-compat unknown-key tolerance instead of erroring).
2. `[server]`: `bind` renamed to `host` (real field name); `enabled` and `cert_path` added
   (both previously entirely absent, both mandatory once `[server]` is uncommented).
3. Top-of-file banner corrected: "uncomment only what you need" is replaced with an accurate
   statement that `[server]`/`[ad]`/`[logging]`/`[organization]`'s base fields are mandatory
   once their section header is uncommented (no field-level defaults in the code), plus which
   fields are the exceptions (optional).
4. `[logging]` and `[organization]` sections added (previously entirely absent from the
   example, despite the code requiring their base fields once present).
5. Windows path syntax documented using TOML single-quoted literal strings (no backslash
   escaping) — `db_path` example updated from a double-quoted/escaped form to single-quoted;
   a cross-reference note added under `[server]` (NOT a second standalone `cert_path = ...`
   line — that would create a duplicate TOML key once Task 3 uncomments the file, since
   `cert_path` is already declared once above it).

Exact file contents to write:

```
# Trackly — шаблон конфигурации portable-режима
# Скопируйте этот файл в trackly.config.toml (рядом с trackly.exe) и настройте нужные
# секции.
#
# ВАЖНО (частая ошибка): если вы раскомментировали ЗАГОЛОВОК секции — [server], [ad],
# [logging] или [organization] — вы ОБЯЗАНЫ также раскомментировать и заполнить ВСЕ её
# обязательные поля (см. пометки «обязательно» ниже в каждой секции). У этих полей нет
# дефолтов в коде: частично заполненная секция не распарсится, и приложение при старте
# откатится на дефолты целиком, запишет причину в config-error.txt рядом с trackly.exe и
# покажет диалог с ошибкой. Поля, отмеченные «опционально», можно оставлять
# закомментированными — для них есть дефолт. Секция [paths] — единственная, которую можно
# оставить полностью закомментированной без последствий (вся секция целиком опциональна).
#
# Windows-пути: используйте TOML single-quoted literal-строки (одинарные кавычки), а не
# двойные — тогда обратный слеш не нужно экранировать. Пример ниже (db_path) написан
# именно так: db_path = 'D:\trackly-data\trackly.db'. В двойных кавычках то же самое
# пришлось бы писать как "D:\\trackly-data\\trackly.db".

# [paths]
# Секция целиком опциональна.
# Путь к файлу базы данных. По умолчанию: рядом с trackly.exe.
# db_path = 'D:\trackly-data\trackly.db'

# [server]
# Все поля этой секции ОБЯЗАТЕЛЬНЫ, если она раскомментирована — кроме key_path
# (опционально).
# Включить встроенный сервер локальной сети (LAN). Обязательно.
# enabled = false
# Адрес привязки. 127.0.0.1 — только локальные подключения, 0.0.0.0 — вся локальная
# сеть. Обязательно.
# host = "127.0.0.1"
# Порт HTTPS сервера. Обязательно.
# port = 8443
# Путь к TLS-сертификату (PEM). Пустая строка "" — сгенерировать self-signed сертификат
# при первом запуске (рекомендуется, если нет готового сертификата). Обязательно (можно
# оставить пустой строкой). На Windows указывайте путь так же, как для db_path выше —
# одинарными кавычками, без экранирования обратного слеша.
# cert_path = ""
# Путь к TLS-приватному ключу (PEM). Опционально — пусто = вывести из cert_path (замена
# расширения на .key).
# key_path = ""

# [ad]
# Параметры Active Directory (Phase 9 / Phase 31, D-AD-01).
# Bootstrap-дефолты на первом запуске; live-переключатели (enabled/автоприём)
# редактируются администратором через Настройки → Active Directory в приложении.
# Обязательные поля при раскомментированной секции: enabled, use_mock, host, domain,
# base_dn, name_attr, no_tls_verify. Остальные поля секции — опциональны (есть дефолты).

# Включить ли AD-вход. По умолчанию: false.
# enabled = false
# Использовать MockAdClient вместо RealAdClient (только для разработки,
# не для production/Windows). По умолчанию: false.
# use_mock = false
# Хост контроллера домена. Пусто → auto-detect (DNS SRV / env).
# host = "dc1.example.local"
# Порт LDAP-подключения. Если закомментирован, порт выводится из
# ldap_tls_mode: 636 для ldaps, 389 для plain/starttls. Раскомментированный
# порт ВСЕГДА побеждает дефолт режима.
# port = 636
# Транспортный режим LDAP-подключения. Значения:
#   ldaps    — (по умолчанию) TLS с самого начала.
#   plain    — незашифрованный LDAP — пароли идут в открытом виде, только
#              для DC без рабочего LDAPS.
#   starttls — LDAP с апгрейдом до TLS через StartTLS.
# БЕЗОПАСНОСТЬ (plain): пароль служебной учётной записи и учётные данные
# пользователей передаются в открытом виде по сети.
# ldap_tls_mode = "ldaps"
# DNS-суффикс домена. Пусто → auto-detect (USERDNSDOMAIN).
# domain = "example.local"
# Base DN для LDAP-поиска. Пусто → выводится из domain.
# base_dn = "dc=example,dc=local"
# Имя атрибута для ФИО. По умолчанию: displayName (fallback на cn, затем на логин).
# name_attr = "displayName"
# Отключить проверку TLS-сертификата LDAPS (небезопасный opt-in для
# нестандартных сетей). По умолчанию: false.
# no_tls_verify = false

# ── AD SSO (Kerberos/SPNEGO passwordless вход) ──────────────────────────────
# Включить passwordless-вход через AD (Kerberos/Negotiate) в server mode.
# Требует заполненных spn и keytab_path. По умолчанию: false.
# sso_enabled = false
# Service Principal Name сервиса (например HTTP/web.example.local), под
# который сгенерирован keytab. Пусто → SSO не активируется.
# spn = "HTTP/web.example.local"
# Путь к .keytab (ktpass /crypto AES256-SHA1 /out server.keytab) рядом с
# исполняемым файлом.
# keytab_path = "server.keytab"

# ── Служебный AD-bind (Phase 31, SSO-01/SSO-03) ─────────────────────────────
# ВАЖНО: bind_dn/bind_password — учётные данные служебной учётной записи
# (read-only, наименьшие привилегии). Реальные значения храните ТОЛЬКО в
# рантайм-конфиге рядом с БД (файл gitignored) — в git этого репозитория
# должны попадать только плейсхолдеры вида ниже, никогда не реальный логин/пароль.
# DN (или UPN) служебной учётной записи для read-only LDAP-bind.
# bind_dn = "svc-trackly-ro@example.local"
# Пароль служебной учётной записи. НИКОГДА не логируется/не печатается.
# bind_password = "CHANGE_ME"
# TTL (сек) кэша резолва displayName. По умолчанию: 1800 (30 минут) —
# низкие ставки безопасности, можно кэшировать дольше.
# display_name_cache_ttl_secs = 1800
# TTL (сек) кэша резолва роли/группы. По умолчанию: 300 (5 минут) — короче
# displayName-кэша, т.к. напрямую влияет на авторизацию (доступ должен
# отзываться оперативно).
# group_cache_ttl_secs = 300

# Таблица «AD-группа → роль» (Phase 31, SSO-03). Приоритет при попадании
# пользователя в НЕСКОЛЬКО групп: Admin > Manager > Employee
# (highest-privilege-wins) НЕЗАВИСИМО от порядка записей ниже.
# Указывайте ПОЛНЫЙ distinguished name группы (не короткое имя) — избегает
# лишнего LDAP round-trip на резолв имени в DN.
# [[ad.role_mapping]]
# group_dn = "CN=IT-Admins,OU=Groups,DC=example,DC=local"
# role = "admin"
# [[ad.role_mapping]]
# group_dn = "CN=IT-Managers,OU=Groups,DC=example,DC=local"
# role = "manager"

# Список доменных логинов (Phase 32, SSO-02) — sAMAccountName, case-insensitive
# (us100, US100@example.local, EXAMPLE\us100 — все матчатся на "us100").
# Любой логин из этого списка получает роль admin И немедленную активацию
# на КАЖДОМ AD/SSO-входе, В ОБХОД ad_auto_accept и ручной блокировки/pending-
# заявки (побеждает над group→role маппингом role_mapping). Пустой список
# (по умолчанию) = фича полностью выключена.
# БЕЗОПАСНОСТЬ: кто редактирует этот файл — фактически может создать
# администратора; относитесь к доступу на запись в trackly.config.toml так
# же серьёзно, как к доступу на подмену .exe.
# Требует ПЕРЕЗАПУСКА процесса — изменения не подхватываются "на лету"
# (как и все остальные TOML-only [ad]-настройки).
# admin_logins = ["us100", "us777"]

# [logging]
# Все поля этой секции ОБЯЗАТЕЛЬНЫ, если она раскомментирована.
# Уровень логирования: trace|debug|info|warn|error. Обязательно.
# level = "info"
# Формат файла лога: compact|json. Обязательно.
# format = "compact"
# Сколько дней хранить файлы логов (фоновая очистка). Обязательно.
# retention_days = 14

# [organization]
# Поле этой секции ОБЯЗАТЕЛЬНО, если она раскомментирована.
# Часовой пояс для отображения дат в UI (в БД всё хранится в UTC). Обязательно.
# timezone = "Europe/Moscow"
```
  </action>
  <verify>
    <automated>test "$(grep -c '^# \[storage\]' /Users/madsas/Projects/trackly/trackly.config.toml.example)" = "0" && grep -q '^# \[paths\]' /Users/madsas/Projects/trackly/trackly.config.toml.example && grep -q '^# host = "127.0.0.1"' /Users/madsas/Projects/trackly/trackly.config.toml.example && grep -q '^# enabled = false' /Users/madsas/Projects/trackly/trackly.config.toml.example && grep -q '^# \[logging\]' /Users/madsas/Projects/trackly/trackly.config.toml.example && grep -q '^# \[organization\]' /Users/madsas/Projects/trackly/trackly.config.toml.example && echo OK</automated>
  </verify>
  <done>trackly.config.toml.example has no [storage] section, has [paths]/[server]/[ad]/[logging]/[organization], server section uses host (not bind) plus enabled/cert_path, Windows path examples use single-quoted TOML literals, and the top banner correctly states which fields are mandatory per section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail-soft config load — never silently exit main() again</name>
  <files>crates/trackly-app/src/config_recovery.rs, crates/trackly-app/src/main.rs, crates/trackly-app/src/lib.rs, crates/trackly-app/Cargo.toml, Cargo.toml, crates/trackly-infra/src/config.rs</files>
  <behavior>
    - `load_or_recover(path)` on a missing file: returns `(AppConfig::default(), None)` — file absence is normal portable-mode operation, NOT an error to surface.
    - `load_or_recover(path)` on malformed TOML: returns `(AppConfig::default(), Some(msg))` where `msg` is non-empty — NEVER panics, NEVER returns `Err` to the caller (the whole point: this function cannot propagate a fatal error).
    - `write_config_error_file(paths, msg)`: creates `<exe_dir>/config-error.txt` containing both `msg` and the attempted config file path, returns the written path.
    - `clear_config_error_file(paths)`: removes `<exe_dir>/config-error.txt` if present; is a silent no-op (never panics) if the file is absent.
  </behavior>
  <action>
**1. Create `crates/trackly-app/src/config_recovery.rs`** (new file) with this exact content:

```rust
//! Fail-soft recovery for `trackly.config.toml` load failures (quick task 260804-lk0).
//!
//! Root cause of the "GUI silently exits with code 1" bug: `main()` used to call
//! `AppConfig::load_or_default(...)?` at step 4, BEFORE `logging::init` (step 5). Any parse
//! error propagated straight out of `fn main() -> anyhow::Result<()>`; under
//! `#[cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` (release builds)
//! there is no console attached, so the printed `Error: ...` was invisible — the app just
//! vanished.
//!
//! Fix: config load never propagates fatally anymore. `load_or_recover` always returns a
//! usable `AppConfig` (real or `AppConfig::default()`), plus an optional human-readable
//! error string. `main.rs` inits logging with whatever config comes back, THEN surfaces the
//! captured error through every channel available: the log file (always), a
//! portable-mode-safe `config-error.txt` next to the exe (always — works headless too), and
//! a best-effort native dialog (desktop/interactive only — `main.rs` skips this for
//! `--self-test` so CI never blocks on a dialog nobody can dismiss).

use std::path::{Path, PathBuf};

use time::format_description::well_known::Rfc3339;
use trackly_infra::{AppConfig, Paths};

/// Loads `trackly.config.toml`, never fatally. On any load/parse error, falls back to
/// `AppConfig::default()` and returns the error's `Display` string as `Some(..)` for the
/// caller to surface. A successful load — including "file absent", which is normal
/// portable-mode operation, not an error — returns `None`.
pub fn load_or_recover(config_path: &Path) -> (AppConfig, Option<String>) {
    match AppConfig::load_or_default(config_path) {
        Ok(cfg) => (cfg, None),
        Err(e) => (AppConfig::default(), Some(e.to_string())),
    }
}

/// Writes `<exe_dir>/config-error.txt` with a timestamped, Russian, human-readable
/// explanation of the failure — the portable-mode-safe fallback that works even when no
/// console/dialog is available (headless / no display server). Best-effort: callers should
/// log (not propagate) an `Err`.
pub fn write_config_error_file(paths: &Paths, message: &str) -> std::io::Result<PathBuf> {
    let path = paths.exe_dir().join("config-error.txt");
    let ts = time::OffsetDateTime::now_utc()
        .format(&Rfc3339)
        .unwrap_or_else(|_| "unknown-time".to_string());
    let contents = format!(
        "Trackly: не удалось загрузить trackly.config.toml — используются настройки по \
         умолчанию.\n\
         Время: {ts}\n\
         Файл: {}\n\
         Ошибка: {message}\n\n\
         Сверьтесь с trackly.config.toml.example рядом с исполняемым файлом, исправьте \
         конфигурацию и перезапустите Trackly.\n",
        paths.config_file().display()
    );
    std::fs::write(&path, contents)?;
    Ok(path)
}

/// Removes a stale `config-error.txt` after a successful config load, so a leftover error
/// file from a previous broken run doesn't confuse the user once they've fixed the config.
/// Best-effort: `NotFound` and any other I/O error are swallowed — there is nothing
/// actionable to do about a failed best-effort cleanup.
pub fn clear_config_error_file(paths: &Paths) {
    let _ = std::fs::remove_file(paths.exe_dir().join("config-error.txt"));
}

/// Best-effort native message dialog surfacing the failure to an interactive desktop user.
/// Runs on a detached OS thread (never blocks startup) and wraps the call in
/// `catch_unwind` — a missing display server / headless environment must never crash
/// startup. Callers MUST NOT invoke this for `--self-test` / CI runs (see `main.rs`) — a
/// blocking dialog nobody can dismiss would hang the process indefinitely.
pub fn show_best_effort_dialog(message: &str) {
    let message = message.to_string();
    std::thread::spawn(move || {
        let _ = std::panic::catch_unwind(|| {
            rfd::MessageDialog::new()
                .set_level(rfd::MessageLevel::Warning)
                .set_title("Trackly — ошибка конфигурации")
                .set_description(format!(
                    "trackly.config.toml не удалось загрузить, используются настройки по \
                     умолчанию.\n\nПодробности записаны в config-error.txt рядом с \
                     trackly.exe.\n\n{message}"
                ))
                .set_buttons(rfd::MessageButtons::Ok)
                .show();
        });
    });
}

#[cfg(test)]
mod tests {
    use super::*;

    fn tempdir_paths() -> (Paths, tempfile::TempDir) {
        let dir = tempfile::TempDir::new().expect("tempdir");
        let paths = Paths::resolve_for_exe_dir(dir.path().to_path_buf()).expect("paths");
        (paths, dir)
    }

    #[test]
    fn load_or_recover_missing_file_returns_defaults_and_no_error() {
        let (paths, _dir) = tempdir_paths();
        let (cfg, err) = load_or_recover(paths.config_file());
        assert!(err.is_none(), "missing file is not an error condition");
        assert_eq!(cfg.server.host, AppConfig::default().server.host);
    }

    #[test]
    fn load_or_recover_malformed_toml_returns_defaults_and_error_message() {
        let (paths, _dir) = tempdir_paths();
        std::fs::write(paths.config_file(), "[server\nenabled = true\n").unwrap();
        let (cfg, err) = load_or_recover(paths.config_file());
        let err = err.expect("malformed TOML must surface an error message");
        assert!(!err.is_empty());
        assert_eq!(
            cfg.server.host,
            AppConfig::default().server.host,
            "must fall back to defaults, not panic or propagate"
        );
    }

    #[test]
    fn write_config_error_file_creates_file_with_message_and_path() {
        let (paths, _dir) = tempdir_paths();
        let written = write_config_error_file(&paths, "boom: unexpected end of input")
            .expect("write must succeed in a writable tempdir");
        assert_eq!(written, paths.exe_dir().join("config-error.txt"));
        let contents = std::fs::read_to_string(&written).unwrap();
        assert!(contents.contains("boom: unexpected end of input"));
        assert!(contents.contains(&paths.config_file().display().to_string()));
    }

    #[test]
    fn clear_config_error_file_removes_existing_file() {
        let (paths, _dir) = tempdir_paths();
        write_config_error_file(&paths, "x").unwrap();
        assert!(paths.exe_dir().join("config-error.txt").exists());
        clear_config_error_file(&paths);
        assert!(!paths.exe_dir().join("config-error.txt").exists());
    }

    #[test]
    fn clear_config_error_file_is_noop_when_absent() {
        let (paths, _dir) = tempdir_paths();
        // Must not panic when there is nothing to remove.
        clear_config_error_file(&paths);
    }
}
```

**2. `crates/trackly-app/src/lib.rs`**: add `pub mod config_recovery;` — insert it BEFORE the
existing `pub mod context;` line (alphabetical: "config_recovery" < "context").

**3. `Cargo.toml`** (workspace root, `[workspace.dependencies]`): add, near the existing
`tauri-plugin-dialog = "2"` line:
```toml
# Standalone native message dialogs for pre-Tauri-Builder startup failures (quick task
# 260804-lk0). Already resolved transitively at 0.16.0 via tauri-plugin-dialog — pinning as
# a direct dep adds zero new supply-chain surface (same precedent as `base64` above).
rfd = "0.16"
```

**4. `crates/trackly-app/Cargo.toml`**: add `rfd = { workspace = true }` immediately after
the existing `tauri-plugin-dialog = { workspace = true }` line.

**5. `crates/trackly-app/src/main.rs`**:
- Change `use trackly_infra::{AppConfig, Paths};` to `use trackly_infra::Paths;` — `AppConfig`
  is no longer referenced directly in this file once step 4/5 below are rewired.
- In the module-level doc comment near the top of the file, replace the two lines
  ```
  //! 4. `AppConfig::load_or_default()` — read `trackly.config.toml` or use defaults.
  //! 5. `trackly_app::logging::init(&paths, &config)` — tracing-subscriber + tracing-appender daily rotation;
  //!    возвращает `WorkerGuard`, который дальше живёт внутри AppCtx (Pitfall #6).
  ```
  with:
  ```
  //! 4. `trackly_app::config_recovery::load_or_recover(...)` — read `trackly.config.toml`;
  //!    NEVER propagates a fatal error (quick task 260804-lk0 — this used to be
  //!    `AppConfig::load_or_default(...)?`, which silently killed the process pre-logger).
  //!    Malformed/unreadable config falls back to `AppConfig::default()` and carries the
  //!    error message forward for step 5b.
  //! 5. `trackly_app::logging::init(&paths, &config)` — tracing-subscriber + tracing-appender
  //!    daily rotation, using whichever config step 4 produced (real or recovered default);
  //!    возвращает `WorkerGuard`, который дальше живёт внутри AppCtx (Pitfall #6).
  //! 5b. If step 4 recovered from an error, surface it now that logging exists:
  //!     `tracing::error!` + `config_recovery::write_config_error_file` (always, portable-mode
  //!     safe) + best-effort native dialog (desktop/interactive only, skipped for
  //!     `--self-test`).
  ```
- Replace the two-line block
  ```rust
      // Step 4: load config (or defaults).
      let config = AppConfig::load_or_default(paths.config_file())?;

      // Step 5: tracing-subscriber + tracing-appender (D-Logging-01). Файлы
      // ложатся в `<exe_dir>/logs/trackly.log.<YYYY-MM-DD>` (portable-mode
      // invariant). Возвращённый WorkerGuard живёт внутри AppCtx; пока он
      // не drop'нется, background-thread аппендера не остановится.
      let log_guard = trackly_app::logging::init(&paths, &config)?;
  ```
  with:
  ```rust
      // Step 4: load config, fail-soft (quick task 260804-lk0). Never propagates a fatal `?`
      // for a malformed trackly.config.toml — that used to exit main() silently under
      // windows_subsystem="windows" (no console to print the Err to) BEFORE the logger
      // existed. Falls back to AppConfig::default() and carries the error message for
      // step 5b to surface once logging is up.
      let (config, config_load_error) =
          trackly_app::config_recovery::load_or_recover(paths.config_file());

      // Step 5: tracing-subscriber + tracing-appender (D-Logging-01). Файлы
      // ложатся в `<exe_dir>/logs/trackly.log.<YYYY-MM-DD>` (portable-mode
      // invariant). Возвращённый WorkerGuard живёт внутри AppCtx; пока он
      // не drop'нется, background-thread аппендера не остановится.
      let log_guard = trackly_app::logging::init(&paths, &config)?;

      // Step 5b: NOW that logging exists, surface a recovered config error loudly through
      // every channel available — log file (always), a portable-mode-safe config-error.txt
      // next to the exe (always, works headless too), and a best-effort native dialog
      // (desktop/interactive only; skipped for --self-test so CI never blocks on a dialog
      // nobody can dismiss).
      if let Some(err_msg) = config_load_error {
          tracing::error!(
              error = %err_msg,
              "trackly.config.toml failed to load — falling back to defaults"
          );
          if let Err(e) = trackly_app::config_recovery::write_config_error_file(&paths, &err_msg)
          {
              tracing::error!("failed to write config-error.txt: {e}");
          }
          if !self_test {
              trackly_app::config_recovery::show_best_effort_dialog(&err_msg);
          }
      } else {
          trackly_app::config_recovery::clear_config_error_file(&paths);
      }
  ```
  Note: `self_test` is already computed at Step 3, earlier in the function — reuse it, do not
  recompute.

**6. `crates/trackly-infra/src/config.rs`**: the module doc comment currently reads (near the
top of the file):
```
//! Malformed TOML → `AppError::Validation { field: "trackly.config.toml", ... }`,
//! ловится в `main.rs` через `?` и печатается админу.
```
Replace with:
```
//! Malformed TOML → `AppError::Validation { field: "trackly.config.toml", ... }`. `main.rs`
//! no longer propagates this via `?` (quick task 260804-lk0) — it routes through
//! `trackly_app::config_recovery::load_or_recover`, which falls back to `Self::default()`
//! and surfaces the error via log + config-error.txt + best-effort dialog.
```
`AppConfig::load_or_default`'s own signature/behavior is UNCHANGED — this is a doc-only edit.
  </action>
  <verify>
    <automated>cargo build -p trackly-app 2>&1 | tail -40 && cargo test -p trackly-app --lib config_recovery:: 2>&1 | tail -40</automated>
  </verify>
  <done>trackly-app compiles with the new fail-soft flow; all 5 config_recovery unit tests pass; main.rs no longer calls AppConfig::load_or_default directly or propagates its Err via `?`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression test — shipped example, uncommented, parses into AppConfig</name>
  <files>crates/trackly-infra/tests/config_example_test.rs</files>
  <behavior>
    - The real `trackly.config.toml.example` (loaded via `include_str!`, not duplicated),
      with every commented-out config line uncommented, parses into `AppConfig` with no error.
    - A handful of known example values (server.host, server.port, logging.level,
      organization.timezone, ad.admin_logins) come through correctly after uncommenting —
      proves the uncommenting actually activated real lines, not a near-empty document that
      trivially parses via defaults.
    - The file no longer contains the stale `[storage]` section name (locks in Task 1's fix).
  </behavior>
  <action>
Create `crates/trackly-infra/tests/config_example_test.rs` with this exact content. This
depends on Task 1 having already corrected `trackly.config.toml.example` — run Task 1 first.

```rust
//! Regression test (quick task 260804-lk0): the SHIPPED `trackly.config.toml.example` must,
//! once every commented-out config line is uncommented, parse into `AppConfig` without
//! error. Loaded via `include_str!` against the exact file that ships in the portable ZIP
//! (`.github/workflows/release.yml`'s "Assemble portable ZIP" step copies this same
//! repo-root file) — never a hand-copied duplicate, so this test cannot silently drift from
//! what users actually receive.

use trackly_infra::AppConfig;

const EXAMPLE: &str = include_str!(concat!(
    env!("CARGO_MANIFEST_DIR"),
    "/../../trackly.config.toml.example"
));

/// True if `rest` (already stripped of its leading `#`/`# `) is a `[section]` or
/// `[[array.of.tables]]` header — every character is `[`, `]`, a word char, or `.`.
fn is_section_header(rest: &str) -> bool {
    !rest.is_empty()
        && rest.starts_with('[')
        && rest
            .chars()
            .all(|c| c.is_ascii_alphanumeric() || "[]._".contains(c))
}

/// True if `rest` is a `key = value` line — starts with an ASCII identifier
/// (`[A-Za-z_][A-Za-z0-9_]*`) immediately followed (after optional whitespace) by `=`.
/// Genuine prose comments in this file are Cyrillic-led or indented, so they can never
/// satisfy this — Cyrillic letters are outside `[A-Za-z_]`, and an indented line's first
/// char is whitespace, not a letter.
fn is_key_value(rest: &str) -> bool {
    let mut chars = rest.char_indices();
    let starts_ok = matches!(chars.next(), Some((_, c)) if c.is_ascii_alphabetic() || c == '_');
    if !starts_ok {
        return false;
    }
    let key_end = rest
        .find(|c: char| !(c.is_ascii_alphanumeric() || c == '_'))
        .unwrap_or(rest.len());
    rest[key_end..].trim_start().starts_with('=')
}

/// Uncomments only lines that are commented-out TOML config — leaves genuine prose comments
/// (this file's explanatory text is entirely in Russian/Cyrillic, or indented continuation
/// lines) untouched. See `is_section_header`/`is_key_value` for the exact heuristic.
fn uncomment_example(src: &str) -> String {
    src.lines()
        .map(|line| {
            let Some(rest) = line.strip_prefix('#') else {
                return line.to_string();
            };
            let rest = rest.strip_prefix(' ').unwrap_or(rest);
            if is_section_header(rest) || is_key_value(rest) {
                rest.to_string()
            } else {
                line.to_string()
            }
        })
        .collect::<Vec<_>>()
        .join("\n")
}

#[test]
fn shipped_example_fully_uncommented_parses_into_app_config() {
    let uncommented = uncomment_example(EXAMPLE);
    let cfg: AppConfig = toml::from_str(&uncommented).unwrap_or_else(|e| {
        panic!(
            "shipped trackly.config.toml.example, fully uncommented, failed to parse: {e}\n\
             --- uncommented content ---\n{uncommented}"
        )
    });

    // Sanity: prove uncommenting activated real lines, not just an empty document.
    assert_eq!(cfg.server.host, "127.0.0.1");
    assert_eq!(cfg.server.port, 8443);
    assert!(cfg.server.enabled == false || cfg.server.enabled == true); // field exists & parsed
    assert_eq!(cfg.logging.level, "info");
    assert_eq!(cfg.organization.timezone, "Europe/Moscow");
    assert!(
        !cfg.ad.role_mapping.is_empty(),
        "example [[ad.role_mapping]] tables should have uncommented"
    );
    assert_eq!(
        cfg.ad.admin_logins,
        vec!["us100".to_string(), "us777".to_string()]
    );
}

#[test]
fn shipped_example_uses_real_paths_section_not_stale_storage() {
    // Regression guard for the original bug report: the example used to ship a [storage]
    // section that does not exist in AppConfig (PathsConfig lives under [paths]) — silently
    // ignored by serde's forward-compat unknown-key tolerance, so the typo never surfaced as
    // a parse error. Assert the corrected file uses the real section name.
    assert!(
        EXAMPLE.contains("[paths]"),
        "example must use the real [paths] section name"
    );
    assert!(
        !EXAMPLE.contains("[storage]"),
        "example must not reintroduce the non-existent [storage] section"
    );
}
```
  </action>
  <verify>
    <automated>cargo test -p trackly-infra --test config_example_test 2>&1 | tail -40</automated>
  </verify>
  <done>Both tests pass; the first proves the shipped example (uncommented) parses into AppConfig and a handful of known values survive; the second locks in that [storage] never reappears.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local admin filesystem → `trackly.config.toml` / `config-error.txt` | Same-privilege local admin who can already edit `trackly.config.toml` or replace `trackly.exe` — no new privilege boundary is crossed by reading or writing files next to the executable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-lk0-01 | Information Disclosure | `config_recovery::write_config_error_file` / `show_best_effort_dialog` | accept | The raw `toml`-crate parse-error text (line/col + syntax description) is written to `config-error.txt` and shown in a dialog. This is local-machine-only content the same local admin who wrote `trackly.config.toml` already has full read access to — no boundary crossed. Secrets (`bind_password`) are never included: this is a raw syntax-parse error, not a rendering of the parsed struct (which is where `AdConfig`'s manual `Debug` redaction applies elsewhere in this codebase). |
| T-lk0-02 | Tampering | `config_recovery::write_config_error_file` path construction | mitigate | Path is built exclusively from `Paths::exe_dir()` (resolved once at startup from `std::env::current_exe()`, never from user/network input) joined with the fixed literal `"config-error.txt"` — no attacker-controlled path segments, no traversal surface. |
| T-lk0-SC | Tampering (supply chain) | New direct `rfd` dependency | accept | `rfd` is NOT a new supply-chain addition — already resolved and compiled in `Cargo.lock` at 0.16.0 as a transitive dependency of `tauri-plugin-dialog` (an already-`[dependencies]`-listed, official Tauri plugin). Promoting it to a direct dependency at the SAME locked version adds zero new code to the build — mirrors the existing `base64 = "0.22"` precedent already documented in `crates/trackly-app/Cargo.toml`. No Package Legitimacy Gate checkpoint needed. |
| T-lk0-03 | Denial of Service | `config_recovery::show_best_effort_dialog` | mitigate | Runs on a detached `std::thread::spawn` (never blocks the tokio runtime / startup path) wrapped in `catch_unwind` (a missing display server must never crash startup); `main.rs` skips calling it entirely when `--self-test` is set, so CI/automation can never hang waiting for a dialog dismissal that will never come. |
</threat_model>

<verification>
1. `cargo build -p trackly-app` compiles clean (proves `main.rs` + `config_recovery.rs` +
   `Cargo.toml` wiring is correct).
2. `cargo test -p trackly-app --lib config_recovery::` — all 5 unit tests pass.
3. `cargo test -p trackly-infra --test config_example_test` — both regression tests pass.
4. `cargo test -p trackly-infra --test config_test` — pre-existing `AppConfig` test suite
   still green (proves the doc-comment-only edit to `config.rs` broke nothing).
5. `cargo clippy -p trackly-app -p trackly-infra --all-targets -- -D warnings`.
6. Manual/human-check (NOT automatable — `windows_subsystem = "windows"` hides the console by
   design, which is the exact mechanism being fixed): on the Windows test machine, copy
   `trackly.config.toml.example` to `trackly.config.toml`, uncomment `[server]` but delete
   one required line (e.g. `cert_path`), launch `trackly.exe`, confirm (a) the app still
   opens using default settings, (b) `config-error.txt` appears next to the exe with a
   readable Russian message, (c) a native dialog appears once. Record the result in the
   SUMMARY — flag as a pending Windows-machine follow-up if this plan is executed from macOS.
</verification>

<success_criteria>
- Malformed `trackly.config.toml` never causes a silent process exit — the app boots on
  defaults every time, in both `--self-test` and normal launch.
- `config-error.txt` and the dialog both include the real parse-error message and the
  attempted config file path.
- `trackly.config.toml.example` has zero drift from `AppConfig`'s real field names; the new
  regression test enforces this going forward (fails on any future struct/example
  divergence, including a `[storage]`-style reintroduction).
- Windows path syntax in the example uses single-quoted TOML literals.
- `cargo fmt` applied only to the touched Rust files (matches this project's convention
  around pre-existing workspace-wide `fmt --check` drift).
</success_criteria>

<output>
Create `.planning/quick/260804-lk0-config-ux-fail-soft-on-broken-trackly-co/260804-lk0-SUMMARY.md` when done
</output>
