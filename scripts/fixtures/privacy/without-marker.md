# Тестовая фикстура (fictional, C-01)

Пример записи из вымышленной заявки: Пчёлкин закрыл заявку на замену
картриджа, которую ранее оформил Артём в отделе снабжения.

Те же два слова, что и в `with-marker.md`, присутствуют здесь по отдельности
(не рядом друг с другом) — ни одна 2-словная n-грамма не совпадает с
хэшем в `tokens.fixture.sha256`, поэтому гейт должен пройти (exit 0).

Все имена в этом файле полностью вымышлены и созданы для self-test гейта
приватности — см. `scripts/fixtures/privacy/README.md`.
