// [check-privacy] TEST FIXTURE — mode-1 (allowlist) regression, self-test
// only (C-02). This is a `.rs`-shaped snippet with the extension changed to
// `.txt` so it never enters `cargo build`. check-privacy.mjs's
// REQUISITE_FILE_RE DOES match this path (the filter accepts an extension
// chain, `.rs.txt`, not only a final extension), so mode 1 inspects it as
// Rust-shaped content — that is exactly what this fixture is for. What
// requires the self-test to name it as an explicit positional target is a
// different constant: AUTO_SCAN_EXCLUDED_PREFIXES excludes
// scripts/fixtures/privacy/ from --staged / full-HEAD auto-discovery, so
// this deliberately-violating file cannot fail the hook and CI forever.
// See scripts/fixtures/privacy/README.md.
//
// The inn/ogrn digit strings below are clearly-fake-but-unlisted — they do
// NOT appear in check-privacy.mjs's ported ALLOWED array, so mode 1 must
// report at least one violation against this file (exit 1).
struct FakeOrg {
    inn: "9998887770",
    ogrn: "1234567890123",
}
